---
phase: 09-tjt-subpanel-datatable-editor-tab
plan: 06
type: execute
wave: 5
depends_on: ["09-04", "09-05"]
files_modified:
  - UtinniCoreDotNet/Formats/Datatable/CsvCellCoercion.cs
  - UtinniCoreDotNet/UtinniCoreDotNet.csproj
  - UtinniCoreDotNet.Tests/FormatsTests/Datatable/CsvCellCoercionTests.cs
  - UtinniCoreDotNet/Formats/Datatable/DataTableCellValue.cs
  - UtinniCoreDotNet/Editing/DatatableEditCommands.cs
  - UtinniCoreDotNet.Tests/FormatsTests/Datatable/DatatableEditControllerTests.cs
  - UtinniCoreDotNet.Tests/UITests/DatatableSortViewOnlyTests.cs
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/DatatableCsvSerializer.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormCsvImportPreviewDialog.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormCsvImportPreviewDialog.Designer.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.Designer.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"
autonomous: true
requirements: [PROD-W1-DT]
assumes:
  - "Plan 09-04 has shipped the DatatableEditController with the `ApplyCsvImport` stub factory throwing NotImplementedException — Plan 09-06 replaces the stub with the actual `ApplyCsvImportCommand` implementation that wraps the per-cell diffs as a single undo transaction (D-08)."
  - "Plan 09-05 has shipped the Save▾ wiring + FormDatatableEditor's toolbar buttons rendered (Find / Replace toggle stubs, Import CSV… / Export CSV… stubs). Plan 09-06 replaces those stubs with the actual functionality."
  - "DataGridView VirtualMode fallback is conditional on Plan 09-03 Task 3's profiling probe measurement: if recorded latency was > 100 ms, this plan ships a 4th task introducing VirtualMode + `CellValueNeeded`/`CellValuePushed` handlers per UI-SPEC § Grid surface paragraph; if ≤ 100 ms, Task 4 is SKIPPED. **If the 09-03 measurement is ABSENT (probe deferred), Task 4 DEFAULTS TO non-VirtualMode + a documented follow-up — NOT an unconditional VirtualMode build. VirtualMode is enabled only on a measured > 100 ms result OR an explicit maintainer checkpoint** (iter-2 item 7 — cursor MEDIUM: the perf probe now measures the production path per revised 09-03 Task 3, so an absent measurement means 'not yet evidenced', which must not silently trigger a substantial VirtualMode re-implementation). The Plan 09-03 SUMMARY records the measurement and the decision."
  - "Per checker B-1 / VALIDATION § Wave 0 Gaps recommendation (lines 939-940): the per-cell CSV coercion check is extracted to a framework helper `CsvCellCoercion.cs` in `UtinniCoreDotNet/Formats/Datatable/` (mirrors the LooseOverridePath / LivePatchValidator framework-side posture from Phase 8). This makes the coercion CI-coverable from xUnit without needing the TJT UI host."
  - "The D-09 view-only-sort defense (RESEARCH Pitfall 9) is enforced via TWO mechanisms in Plan 09-06: (a) the writer-side grep gate `grep -c \"dataGridView\" DatatableSaveTargets.cs` MUST remain 0 (Plan 09-05 ships this file with no UI reference; Plan 09-06 MUST NOT introduce one), AND (b) an xUnit fact `Sort_DoesNotMutateModelOrder` asserts that after `dataGridView.Sort(col, SortOrder.Ascending)`, calling `new DataTableWriter(mutDoc).Serialize()` produces bytes equal to the pre-sort serialization."
  - "Find/Replace + sort + frozen DT_Comment-row toggle are all built-in DataGridView features per RESEARCH § Don't Hand-Roll table. Plan 09-06 wires them via the existing BCL APIs (DataGridViewColumn.SortMode = Automatic + CellFormatting + Rows[N].Frozen) — no new framework code beyond CsvCellCoercion.cs."
must_haves:
  truths:
    - "CSV/TSV export (D-08) writes the current MutableDataTableDocument to a user-picked file via `SaveFileDialog`; format: bare-name header (UI-SPEC Assumption 8 + RESEARCH Open Question 4 default), optional `#`-prefixed second row carrying the type-spec (Excel-compatible passthrough), UTF-8 with BOM, RFC-4180-ish double-quote escape; DT_Comment rows EXPORT (Plan 09-06 default per UI-SPEC `editCommentRows` toggle default off — but exported nonetheless for completeness)."
    - "CSV/TSV import (D-08 — the byte-exact-on-untouched gate) computes a per-cell diff via `CsvCellCoercion.PlanImport`; cells with imported value EQUAL to current value stay clean (preserve originalSlice per CF-04); cells with different value run through `DataTableColumnType.MangleValue` → on success queue an `EditCellValueCommand`; on failure list in `FormCsvImportPreviewDialog`'s Color.Red invalid-rows section. Preview modal surfaces locked D-08 wording `{N} cells will change. {M} cells will stay as original bytes. {K} cells in the CSV would be type-invalid and will be skipped (see list below).` Apply runs a SINGLE `ApplyCsvImportCommand` transaction (one undo entry for the whole import)."
    - "Find/Replace pane (D-07) toggles via Ctrl+F / Ctrl+H toolbar buttons; Find returns first-match cell coords; Replace honors per-column type validation (D-03 strict edit-time — invalid replacements blocked by the same parse-back path); F3 / Shift+F3 cycle next/previous match; Esc collapses the pane. Match-case / regex / include-comments scope toggles per UI-SPEC § Layout Contract Find pane row."
    - "Column-click sort (D-09) is wired via `DataGridViewColumn.SortMode = SortMode.Automatic` — built-in DataGridView feature; per UI-SPEC § States `Sort active` row shows the built-in ▲/▼ glyph; the view-only contract is ENFORCED by both the writer-side grep gate AND the `Sort_DoesNotMutateModelOrder` xUnit fact (CF-04 + CF-09 inheritance — model order NEVER changes due to view sort)."
    - "DT_Comment frozen-header treatment: first DT_Comment row (when present) renders with `Frozen=true` + `BackColor=Colors.PrimaryShadow()` + `ForeColor=Colors.FontDisabled()` by default; toolbar `Edit comment rows` `UtinniToggleButton` toggles editability (when ON: row.Frozen=false + cells editable; OFF: locked). Persisted to `[DatatableEditor] editCommentRows` ini key."
    - "DataGridView VirtualMode fallback ONLY if Plan 09-03 measured latency > 100 ms (Task 4 conditional). If shipped: `gridSurface.VirtualMode = true` + `CellValueNeeded` reads from `MutableDataTableCell.Value` + `CellValuePushed` writes via `controller.Apply(DatatableEditCommands.EditCellValue(...))`. If skipped (measured ≤ 100 ms): SUMMARY confirms the decision and omits Task 4. **If the 09-03 measurement is ABSENT: default to non-VirtualMode + a documented follow-up (NOT unconditional VirtualMode) — VirtualMode requires a > 100 ms measurement or an explicit maintainer checkpoint (iter-2 item 7).**"
  artifacts:
    - path: "UtinniCoreDotNet/Formats/Datatable/CsvCellCoercion.cs"
      provides: "Framework-side per-cell coercion + CSV import plan builder (checker B-1 extraction); pure-managed + BCL-only; xUnit-testable from CI"
      contains: "PlanImport"
    - path: "UtinniCoreDotNet/UtinniCoreDotNet.csproj"
      provides: "Old-style explicit-compile — adds <Compile Include> entry for the new Formats\\Datatable\\CsvCellCoercion.cs file"
      contains: "Formats\\Datatable\\CsvCellCoercion.cs"
    - path: "UtinniCoreDotNet.Tests/FormatsTests/Datatable/CsvCellCoercionTests.cs"
      provides: "~10-15 [Fact]s covering per-DT_* coercion success + failure + the byte-exact-on-untouched property (CSV round-trip with no value changes leaves all cells clean)"
      contains: "CsvCellCoercionTests"
    - path: "UtinniCoreDotNet/Editing/DatatableEditCommands.cs"
      provides: "MODIFY — replace the ApplyCsvImport stub from Plan 09-04 with the actual transaction wrapper (private nested ApplyCsvImportCommand class wrapping N EditCellValueCommands as a single undo entry); UndoOp undoes all N edits in reverse order"
      contains: "ApplyCsvImportCommand"
    - path: "UtinniCoreDotNet.Tests/FormatsTests/Datatable/DatatableEditControllerTests.cs"
      provides: "MODIFY — adds 3-5 [Fact]s for ApplyCsvImport: single-transaction undo identity; SC4 byte-exact when imported values equal current values; partial import (some skipped invalid rows) transaction"
      contains: "ApplyCsvImport"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/DatatableCsvSerializer.cs"
      provides: "TJT-side CSV file I/O + parser (hand-rolled RFC-4180-ish per RESEARCH § Don't Hand-Roll — < 150 lines; UTF-8 BOM, double-quote escape); calls framework CsvCellCoercion.PlanImport for the per-cell diff + coercion"
      contains: "DatatableCsvSerializer"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormCsvImportPreviewDialog.cs"
      provides: "Per-call modal — `using (var dlg = new FormCsvImportPreviewDialog(plan))` showing summary copy + per-column diff list + invalid-rows Color.Red list + Import/Cancel buttons; UI-SPEC assumption #7"
      contains: "FormCsvImportPreviewDialog"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormCsvImportPreviewDialog.Designer.cs"
      provides: "Hand-written Designer; ThemedDataGridView for per-column diff + ListView for invalid rows + Import/Cancel UtinniButtons"
      contains: "InitializeComponent"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs"
      provides: "MODIFY — wire Find/Replace pane toggle + match navigation (F3/Shift+F3) + CellFormatting search-match overlay (Plan 09-03 stubbed the formatter, Plan 09-06 fills the search-match branch); wire Import CSV… + Export CSV… toolbar buttons to DatatableCsvSerializer; wire column-click sort via DataGridViewColumn.SortMode = Automatic on all bindable columns; wire DT_Comment frozen-row toggle via Edit-comment-rows UtinniToggleButton + [DatatableEditor] editCommentRows ini key"
      contains: "DatatableCsvSerializer"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.Designer.cs"
      provides: "MODIFY — populate the Find/Replace pane controls (UtinniTextbox txtFind, UtinniLabel lblFindCount, UtinniButton btnFindPrev/Next, UtinniTextbox txtReplace, UtinniButton btnReplaceOne/All, UtinniToggleButton tglMatchCase/tglRegex/tglIncludeComments, UtinniButton btnClose); add Edit-comment-rows UtinniToggleButton to the toolbar"
      contains: "txtFind"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"
      provides: "Old-style explicit-compile — adds <Compile Include> entries for Saving\\DatatableCsvSerializer.cs + UI\\Forms\\FormCsvImportPreviewDialog.cs + .Designer.cs (round-2 HIGH-A)"
      contains: "DatatableCsvSerializer.cs"
  key_links:
    - from: "FormDatatableEditor.OnImportCsvClick"
      to: "DatatableCsvSerializer.LoadAndPlan → FormCsvImportPreviewDialog → controller.Apply(DatatableEditCommands.ApplyCsvImport(plan))"
      via: "CSV import flow: load + plan → modal preview → single-transaction apply"
      pattern: "ApplyCsvImport"
    - from: "DatatableCsvSerializer.LoadAndPlan"
      to: "CsvCellCoercion.PlanImport (framework — checker B-1)"
      via: "Per-cell diff + DataTableColumnType.MangleValue coercion check, returns CsvImportPlan"
      pattern: "CsvCellCoercion\\.PlanImport"
    - from: "Sort_DoesNotMutateModelOrder xUnit fact"
      to: "writer iterates MutableDataTableDocument.Rows (D-09 view-only enforcement)"
      via: "Assert: dataGridView.Sort(col, Asc) → new DataTableWriter(mDoc).Serialize() == pre-sort serialize"
      pattern: "Sort_DoesNotMutateModelOrder"
    - from: "FormDatatableEditor.tglEditCommentRows.CheckedChanged"
      to: "iterate Rows where ColumnType.Type == DT_Comment, set Frozen + CellStyle"
      via: "Edit-comment-rows toggle persists to [DatatableEditor] editCommentRows ini key"
      pattern: "editCommentRows"
---

<objective>
Ship the Phase 9 **bulk-edit + productivity features**: CSV/TSV delta-import + export (D-08 — the
byte-exact-on-untouched CSV-round-trip gate), Find/Replace pane (D-07), column-click view-only
sort (D-09 — with the writer-side defense gate), and the DT_Comment frozen-header treatment per
UI-SPEC. This is the last functionality plan before the Tier-4 live-SWG smoke (Plan 09-07).

The framework gets ONE new file: `CsvCellCoercion.cs` (per checker B-1 extraction — mirrors
Phase 8's `LooseOverridePath` / `LivePatchValidator` posture; the per-cell coercion check is
pure-managed BCL-only so it lives framework-side and is xUnit-testable from CI). The DatatableEditController
gets ONE extension: the `ApplyCsvImport` factory stub from Plan 09-04 is replaced with the real
transaction-wrapping `ApplyCsvImportCommand`. The TJT side gets ONE new save file
(`DatatableCsvSerializer.cs` — TJT-side file I/O + hand-rolled RFC-4180-ish parser per RESEARCH
§ Don't Hand-Roll), ONE new modal (`FormCsvImportPreviewDialog` per UI-SPEC assumption #7), and
the FormDatatableEditor wiring for Find/Replace + sort + frozen toggle.

**Conditional Task 4 (VirtualMode fallback):** Plan 09-03 Task 3 measures DataGridView bind
latency on `BuildV1CombatDataTableLike()` against the PRODUCTION path (a DataGridView with a
representative `CellFormatting` overlay handler attached, per revised 09-03); the SUMMARY records
the value. If > 100 ms, Plan 09-06 ships Task 4 introducing `VirtualMode = true` per UI-SPEC
§ Grid surface paragraph. If ≤ 100 ms, Task 4 is SKIPPED and the SUMMARY confirms the non-virtual
commitment. **If the measurement is ABSENT (probe deferred), Task 4 defaults to non-VirtualMode
+ a documented follow-up — NOT an unconditional VirtualMode build (iter-2 item 7 — cursor MEDIUM).**

**Why this decomposition (Wave 5, single-plan):** CSV + Find/Replace + sort + frozen-row are
cohesive productivity-layer features that depend on BOTH the controller (09-04) AND the save +
entry-point wiring (09-05). Splitting them would force one plan to ship CSV without Find/Replace
or vice versa; both touch the same Form + grid surface. Wave 5 single-plan keeps the context
cost tractable (3 framework files + 4 TJT files; ~50% context budget); the cross-AI review will
gate it before Plan 09-07's live-SWG smoke.

**Cross-repo paired commits:** Tasks ship Utinni + UtinniPlugins atomically per
`feedback_utinniplugins_authority`.

**Round-2-style fixes folded:** The framework-side CsvCellCoercion extraction (checker B-1 +
VALIDATION § Wave 0 Gaps row 938-940 explicit recommendation) is folded in; no checker round
needed to surface it. The D-09 view-only-sort defense's xUnit fact + grep gate ships in this plan
per RESEARCH § "What the planner must NOT skip" mandate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-CONTEXT.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-RESEARCH.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-PATTERNS.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-VALIDATION.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-UI-SPEC.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-01-PLAN.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-04-PLAN.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-05-PLAN.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-03-SUMMARY.md   # MUST exist by Wave 4; carries the DataGridView bind-latency measurement

# Plan 09-01 framework primitives
@UtinniCoreDotNet/Formats/Datatable/DataTableColumnType.cs
@UtinniCoreDotNet/Formats/Datatable/DataTableCellValue.cs
@UtinniCoreDotNet/Formats/Datatable/MutableDataTableCell.cs
@UtinniCoreDotNet/Formats/Datatable/MutableDataTableDocument.cs

# Plan 09-04 controller — ApplyCsvImport stub Plan 09-06 fills
@UtinniCoreDotNet/Editing/DatatableEditController.cs
@UtinniCoreDotNet/Editing/DatatableEditCommands.cs
@UtinniCoreDotNet.Tests/FormatsTests/Datatable/DatatableEditControllerTests.cs

# Plan 09-05 save + entry-point wiring (the FormDatatableEditor.cs as it stands after 09-05)
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.Designer.cs

# Plan 09-03 controls (ThemedDataGridView with the CellFormatting overlay hooks; per-type column factory)
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/ThemedDataGridView.cs
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/DatatableColumnFactory.cs

# Phase 8 per-call modal analog for FormCsvImportPreviewDialog
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormSaveConfirmDialog.cs

<interfaces>
<!-- Plan 09-01 / 09-04 / 09-05 public surface Plan 09-06 consumes -->

From UtinniCoreDotNet/Formats/Datatable/DataTableColumnType.cs (Plan 09-01):
- public bool MangleValue(ref string value)   // Plan 09-06's CsvCellCoercion calls this per cell

From UtinniCoreDotNet/Formats/Datatable/MutableDataTableCell.cs (Plan 09-01):
- public DataTableCellValue Value { get; set; }   // setter no-ops on equality (preserves originalSlice — CF-04)
- public bool IsDirty { get; }
- public bool NeedsReview { get; internal set; }

From UtinniCoreDotNet/Editing/DatatableEditCommands.cs (Plan 09-04):
- public static IDatatableEditCommand EditCellValue(MutableDataTableCell cell, DataTableCellValue newValue)
- public static IDatatableEditCommand ApplyCsvImport(MutableDataTableDocument doc, object plan)   // STUB throws NotImplementedException — Plan 09-06 REPLACES with real implementation

From UtinniCoreDotNet/Editing/DatatableEditController.cs (Plan 09-04):
- public void Apply(IDatatableEditCommand cmd)   // FormDatatableEditor calls this with the ApplyCsvImport command

From ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/ThemedDataGridView.cs (Plan 09-03):
- public void BindMutable(MutableDataTableDocument doc, IReadOnlyList<DataGridViewColumn> columns)
- public void RefreshMutable(MutableDataTableDocument doc)
- // CellFormatting handler has hooks for search-match overlay + frozen DT_Comment row — Plan 09-06 fills the bodies

From ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs (Plans 09-03/04/05):
- protected override bool ProcessCmdKey(...)   // Plan 09-06 ADDS Ctrl+F / Ctrl+H / F3 / Shift+F3 / Esc bindings
- private void RefreshSaveMenuEnabledState()   // Plan 09-06 does NOT modify (Plan 09-05 ships the composed gate)
- public DataTableDocument dtDocument;
- private DatatableEditController controller;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship framework CsvCellCoercion + ApplyCsvImportCommand + their xUnit coverage (checker B-1 extraction)</name>
  <files>UtinniCoreDotNet/Formats/Datatable/CsvCellCoercion.cs, UtinniCoreDotNet/UtinniCoreDotNet.csproj, UtinniCoreDotNet.Tests/FormatsTests/Datatable/CsvCellCoercionTests.cs, UtinniCoreDotNet/Editing/DatatableEditCommands.cs, UtinniCoreDotNet.Tests/FormatsTests/Datatable/DatatableEditControllerTests.cs</files>
  <read_first>
    - UtinniCoreDotNet/Formats/Datatable/DataTableColumnType.cs (Plan 09-01 — MangleValue + Type/BasicType for the coercion check)
    - UtinniCoreDotNet/Formats/Datatable/MutableDataTableCell.cs (Plan 09-01 — Value get/set + the no-op-on-equality preservation of originalSlice)
    - UtinniCoreDotNet/Formats/Datatable/MutableDataTableDocument.cs (Plan 09-01 — Columns + Rows lists; Find-column-by-name lookup)
    - UtinniCoreDotNet/Formats/Datatable/DataTableCellValue.cs (Plan 09-01 — Equals static for the per-cell value comparison)
    - UtinniCoreDotNet/Editing/DatatableEditCommands.cs (Plan 09-04 — the ApplyCsvImport STUB to replace; the private nested EditCellValueCommand pattern to mirror for the transaction wrapper)
    - UtinniCoreDotNet.Tests/FormatsTests/Datatable/DatatableEditControllerTests.cs (Plan 09-04 — the existing test fixture infrastructure Plan 09-06 extends with 3-5 new ApplyCsvImport facts)
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-RESEARCH.md § Pattern 5 (CsvImportPlan.Build inline at lines 498-519 — the spec for PlanImport) + § "What the planner must NOT skip" (the CSV byte-exact-on-untouched property)
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-VALIDATION.md § Wave 0 Requirements lines 938-940 (the checker B-1 framework-side extraction recommendation) + § Per-Task Verification Map row CSV delta-import (D-08)
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-CONTEXT.md § Implementation Decisions D-08 (CSV / TSV export + delta-import; per-cell diff preserves byte-exact-on-untouched)
    - UtinniCoreDotNet/UtinniCoreDotNet.csproj (old-style explicit-compile)
  </read_first>
  <behavior>
    - CsvCellCoercion.cs:
      - `namespace UtinniCoreDotNet.Formats.Datatable`; `public static class CsvCellCoercion`.
      - Public types: `public sealed class CsvImportPlan { public List<EditCellPatch> Changes; public List<UnchangedCellMatch> Unchanged; public List<InvalidCellEntry> Invalid; public int RowsTouched; public int ColumnsTouched; }` + supporting record types `EditCellPatch(int row, int col, DataTableCellValue newValue)`, `UnchangedCellMatch(int row, int col)`, `InvalidCellEntry(int row, int col, string csvValue, string columnName, string reason)`.
      - `public static CsvImportPlan PlanImport(MutableDataTableDocument target, IReadOnlyList<string> csvHeader, IReadOnlyList<IReadOnlyList<string>> csvRows)`:
        - For each csvHeader entry, find the matching column in target by name; if not found, skip (silently — out-of-schema columns are user-error but not fatal; Plan 09-06 surfaces them in the preview modal's status copy).
        - For each csvRow × matching csvColumn:
          - Look up the corresponding `MutableDataTableCell`.
          - Convert the existing cell.Value back to a string representation (Plan 09-06 ships a `DataTableCellValue.ToCsvString(DataTableColumnType ct)` helper on DataTableCellValue — small extension to Plan 09-01's type; document the dependency).
          - If `csvValue.Equals(currentCellAsString)` → add to `Unchanged` list; skip MangleValue (preserves originalSlice via CF-04 — cell stays clean).
          - Else: copy csvValue to a mutable string; call `columnType.MangleValue(ref csvValue)`. On success: convert mangled value to `DataTableCellValue` (typed) and add `EditCellPatch` to Changes. On failure: add `InvalidCellEntry` with the column name + a human-readable reason (planner-friendly — e.g. "DT_Int requires whole number; got 'foo'").
        - Return plan with tallies.
      - DOS mitigation per RESEARCH § Security Domain "malicious CSV input causes massive heap allocation": cap row count at 100,000 rows AND cell-size at 16 KB per cell (the same `_loadRow` buffer SOE uses per DataTableWriter.cpp:768); over-cap throws `CsvParseException("Row count exceeds sanity cap (100,000)")` or `CsvParseException("Cell size exceeds sanity cap (16 KB)")` so the caller surfaces the failure in the preview modal red status before any allocation explosion.
      - Also expose: `public static string SerializeCellToCsv(MutableDataTableCell cell, DataTableColumnType ct)` — symmetric of the coercion check; used by the TJT-side serializer for export.
    - DataTableCellValue.ToCsvString(DataTableColumnType ct): small extension to Plan 09-01's discriminated-union value type — for DT_Int → integer.ToString(InvariantCulture); for DT_Float → float.ToString("R", InvariantCulture) (round-trippable); for DT_String → return the string as-is; for DT_HashString → since the cell stores the int32 (Pitfall 4), this returns the int32 as the CSV cell content. **This is lossy for source-string reconstruction (iter-2 item 9 — both reviewers): once a HashString cell is saved, only the int32 hash exists on disk, so CSV cannot round-trip the original source string. Per the 09-03 DT_HashString UX decision, the editor shows the stored int32 by default and offers an explicit "enter source string to compute hash" affordance (in-session only). Emit a `#`-comment line in the CSV header documenting that DT_HashString columns carry the int32 hash, not the source string; 09-07 smoke Step 10 verifies this behavior (it does NOT expect source-string round-trip).**
    - DatatableEditCommands.cs MODIFY — replace the `ApplyCsvImport` stub:
      - Private nested class `ApplyCsvImportCommand : IDatatableEditCommand`:
        - Stores: `CsvImportPlan plan`, `List<(MutableDataTableCell cell, DataTableCellValue oldValue, byte[] oldOriginalSlice, bool oldIsDirty)> capturedState` for undo.
        - `Do(MutableDataTableDocument doc)`: for each `EditCellPatch p` in plan.Changes: lookup `cell = doc.Rows[p.row].Cells[p.col]`; capture (oldValue, oldOriginalSlice, oldIsDirty); set `cell.Value = p.newValue` (Plan 09-01 setter handles dirty/slice propagation).
        - `UndoOp(MutableDataTableDocument doc)`: iterate captured state in REVERSE order; restore each cell's value + originalSlice + IsDirty via internal-setter access (or via the `MutableDataTableCell` exposing an internal `RestoreCapturedState(...)` method — Plan 09-06 adds this small surface if needed).
      - Public factory `ApplyCsvImport(MutableDataTableDocument doc, CsvImportPlan plan)` replaces the Plan 09-04 NotImplementedException stub.
      - Update the existing factory signature: `public static IDatatableEditCommand ApplyCsvImport(MutableDataTableDocument doc, CsvImportPlan plan)` (was `object plan` stub in Plan 09-04; Plan 09-06 specializes to the real type — this is safe since the stub had only one caller-pattern and no production code consumed it yet).
    - CsvCellCoercionTests.cs (~10-15 [Fact]s):
      - `PlanImport_AllCellsMatchCurrent_ReturnsZeroChanges` — load fixture, build CSV from current values, plan import; assert Changes.Count == 0 and Unchanged.Count == rows×cols.
      - `PlanImport_OneCellDiffers_ReturnsOneChange_OthersUnchanged` — flip one CSV value; assert Changes.Count == 1, Unchanged.Count == (rows×cols - 1).
      - `PlanImport_InvalidValueForDtInt_ReturnsInvalid_NotInChanges` — CSV with "foo" in a DT_Int column; assert Invalid.Count == 1, Changes does NOT contain that cell.
      - `PlanImport_DtBoolCellWithValueTwo_FlaggedInvalid` — DT_Bool only accepts "0"/"1" per Plan 09-01 MangleValue; "2" goes to Invalid.
      - `PlanImport_DtEnumCellWithUnknownLabel_FlaggedInvalid` — enum value not in EnumMap → Invalid.
      - `PlanImport_DtHashStringAlwaysCoerces_NoInvalid` — any string coerces to int32 via CRC; assert all DT_HashString CSV cells succeed.
      - `PlanImport_OverRowCap_ThrowsCsvParseException` — 100,001 rows → throws.
      - `PlanImport_OverCellSizeCap_ThrowsCsvParseException` — single cell > 16 KB → throws.
      - `SerializeCellToCsv_AllDtTypes_RoundTripsThroughPlanImport` — for each DT_*: export cell to CSV string, then PlanImport with that string against the same fixture, assert no change (Unchanged contains the cell). This is the SC4-at-the-CSV-layer fact.
    - DatatableEditControllerTests.cs ADDITIONS (3-5 facts):
      - `ApplyCsvImport_FivePatches_AppliesAllInSingleTransaction` — build plan with 5 changes; Apply; assert all 5 cells dirty + undoStack.Count == 1.
      - `ApplyCsvImport_Undo_RevertsAllPatches` — Apply → Undo; assert all 5 cells restored to original Value + IsDirty=false; netAppliedCount == 0.
      - `ApplyCsvImport_RedoAfterUndo_ReAppliesAll` — Apply → Undo → Redo; assert all 5 cells dirty again.
      - `ApplyCsvImport_BytesEqualOnUnchangedCells` — SC4-via-controller fact: load fixture; Apply a plan where Unchanged set covers all cells except one (one cell changed); Serialize via DataTableWriter; assert the unchanged cells' serialized payloads byte-equal the loaded payloads (CF-04 + D-08 + CF-02 composition).
  </behavior>
  <action>
    Create `CsvCellCoercion.cs` per the behavior block; 23-line MIT header + Allman + 4-space +
    `// ToDo` (no colon). Namespace `UtinniCoreDotNet.Formats.Datatable`. XML `<remarks>` block
    cites RESEARCH § Pattern 5 lines 498-519 AND VALIDATION § Wave 0 Gaps lines 938-940 (checker
    B-1 framework-side extraction rationale).

    Add the `DataTableCellValue.ToCsvString(DataTableColumnType ct)` extension method — this is a
    small Plan 09-01 type extension; document via XML comment that Plan 09-06 added it for the CSV
    serializer's needs.

    Modify `DatatableEditCommands.cs`: replace the Plan 09-04 NotImplementedException stub for
    `ApplyCsvImport` with the real `ApplyCsvImportCommand` implementation (private nested class).
    Update the factory signature from `object plan` to `CsvImportPlan plan` (safe — no production
    caller existed for the stub).

    Update `UtinniCoreDotNet.csproj`: 1 new `<Compile Include="Formats\Datatable\CsvCellCoercion.cs" />`
    entry, alphabetically within the existing `Formats\Datatable\` block.

    Create `CsvCellCoercionTests.cs` with the ~10-15 [Fact]s per the behavior block. Use Plan
    09-01's fixture builders for setup.

    Extend `DatatableEditControllerTests.cs` with the 3-5 new ApplyCsvImport facts per the
    behavior block.

    SDK-style test csproj — no test-csproj edits.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~CsvCellCoercionTests|FullyQualifiedName~ApplyCsvImport" 2>&1 | tail -15  # MUST exit 0 with >= 12 combined passing facts; grep -c "Formats\\\\Datatable\\\\CsvCellCoercion.cs" UtinniCoreDotNet/UtinniCoreDotNet.csproj  # MUST return 1; grep -c "NotImplementedException" UtinniCoreDotNet/Editing/DatatableEditCommands.cs  # MUST return 0 (the stub is replaced); grep -c "ApplyCsvImportCommand" UtinniCoreDotNet/Editing/DatatableEditCommands.cs  # MUST return >= 1 (the private nested class exists)</automated>
  </verify>
  <done>CsvCellCoercion.cs ships with ≥ 10 passing [Fact]s including the CSV round-trip SC4 property; ApplyCsvImportCommand replaces the Plan 09-04 stub with ≥ 3 passing controller-level [Fact]s; full Datatable test subsuite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship TJT-side DatatableCsvSerializer + FormCsvImportPreviewDialog</name>
  <files>../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/DatatableCsvSerializer.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormCsvImportPreviewDialog.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormCsvImportPreviewDialog.Designer.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj</files>
  <read_first>
    - UtinniCoreDotNet/Formats/Datatable/CsvCellCoercion.cs (Task 1 — PlanImport + SerializeCellToCsv API the TJT serializer consumes)
    - UtinniCoreDotNet/Editing/DatatableEditCommands.cs (Task 1 — ApplyCsvImport factory now takes CsvImportPlan)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormSaveConfirmDialog.cs (Phase 8 per-call modal lifecycle analog for FormCsvImportPreviewDialog)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTypeChangeCascadeDialog.cs (Plan 09-04 — sibling per-call modal with embedded ThemedDataGridView; same architectural pattern)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/ThemedDataGridView.cs (Plan 09-03 — embedded in the preview modal for per-column diff display)
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-UI-SPEC.md § Destructive CSV/TSV bulk import row (LOCKED copy: heading `Import {csvFileName} into {tabFileName}?`; body `{N} cells will change. {M} cells will stay as original bytes. {K} cells in the CSV would be type-invalid and will be skipped (see list below).`; per-column row `{columnName} ({DT_Type}): {touched} rows touched`; invalid row `Row {rowIndex}, column {columnName}: "{csvValue}" cannot convert to {DT_Type}`; buttons `Import` / `Cancel`)
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-CONTEXT.md § Implementation Decisions D-08 + Claude's Discretion CSV serialization details
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-RESEARCH.md § Don't Hand-Roll CSV parsing row (~145 lines; UTF-8 BOM detection + Excel-compatible double-quote escape; alternative `Microsoft.VisualBasic.FileIO.TextFieldParser` REJECTED — over-weight)
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-PATTERNS.md § File Classification → DatatableCsvSerializer + FormCsvImportPreviewDialog rows
  </read_first>
  <behavior>
    - DatatableCsvSerializer.cs:
      - `namespace TheJawaToolboxDotNet.Saving`; `public static class DatatableCsvSerializer`.
      - `public static void Export(MutableDataTableDocument doc, string path, bool includeTypeRow)`:
        - Open file via `new FileStream(path, FileMode.Create)` + `using (var sw = new StreamWriter(stream, new UTF8Encoding(encoderShouldEmitUTF8Identifier: true)))` (UTF-8 BOM per UI-SPEC Assumption 8 + SOE convention).
        - Write header row: `string.Join(",", columns.Select(c => CsvEscape(c.Name)))`.
        - If `includeTypeRow == true`: write `#` + `string.Join(",", columns.Select(c => c.ColumnType.TypeSpec))` — Excel-compatible passthrough.
        - For each row: `string.Join(",", row.Cells.Select((c, i) => CsvEscape(CsvCellCoercion.SerializeCellToCsv(c, columns[i].ColumnType))))`.
        - `CsvEscape`: if value contains comma OR double-quote OR newline → wrap in double-quotes + escape internal double-quotes by doubling (RFC-4180 + Excel convention).
      - `public static CsvImportPlan LoadAndPlan(MutableDataTableDocument target, string csvPath)`:
        - Read file via `new StreamReader(csvPath, Encoding.UTF8, detectEncodingFromByteOrderMarks: true)`.
        - Parse via hand-rolled RFC-4180-ish state machine (< 100 lines): track in-quotes flag; double-quote inside quotes escapes; commas split fields when not in quotes; CR/LF terminate rows when not in quotes; preserve empty cells.
        - First row = header. If first cell of second row starts with `#` → skip as type annotation. Subsequent rows = data.
        - Call `CsvCellCoercion.PlanImport(target, header, rows)` and return the plan.
      - `public static async Task<TaskCompletionSource<bool>> ImportAsync(MutableDataTableDocument target, string csvPath, DatatableEditController controller, IWin32Window owner)`:
        - Call `LoadAndPlan` on a background Task to avoid UI freeze on large CSVs.
        - On main thread: open `using (var dlg = new FormCsvImportPreviewDialog(plan, csvPath, owner.GetDisplayName())) { if (dlg.ShowDialog(owner) == DialogResult.OK) controller.Apply(DatatableEditCommands.ApplyCsvImport(target, plan)); }`.
        - Return success/cancel outcome to caller.
    - FormCsvImportPreviewDialog.cs + Designer:
      - `namespace TJT.UI.Forms`; `public partial class FormCsvImportPreviewDialog : UtinniForm`.
      - Constructor `FormCsvImportPreviewDialog(CsvImportPlan plan, string csvFileName, string tabFileName)`.
      - Designer: header `UtinniLabel lblHeading` with locked copy `Import {csvFileName} into {tabFileName}?`; body `UtinniLabel lblBody` with the locked D-08 wording `{N} cells will change. {M} cells will stay as original bytes. {K} cells in the CSV would be type-invalid and will be skipped (see list below).`; embedded `ThemedDataGridView gridPerColumnDiff` (Dock=Top, Height=200) showing per-column row-touched counts (3 columns: Column name / DT_Type / Rows touched); a `ListView lvInvalidRows` (Dock=Fill) at `Color.Red` showing invalid-row entries (locked copy `Row {rowIndex}, column {columnName}: "{csvValue}" cannot convert to {DT_Type}`); footer Panel (Dock=Bottom) with `UtinniButton btnImport "Import"` (DialogResult=OK) + `UtinniButton btnCancel "Cancel"` (DialogResult=Cancel).
      - Modal sizing: 800×600 default.
      - Per-call modal lifecycle (`using (...)`).
    - csproj: 3 new `<Compile Include>` entries (DatatableCsvSerializer.cs, FormCsvImportPreviewDialog.cs with `<SubType>Form</SubType>`, FormCsvImportPreviewDialog.Designer.cs with `<DependentUpon>`).
  </behavior>
  <action>
    Create `DatatableCsvSerializer.cs` per the behavior block; 23-line MIT header + Allman +
    4-space + `// ToDo` (no colon). Namespace `TheJawaToolboxDotNet.Saving`. The hand-rolled RFC-4180
    parser stays under 100 lines; XML `<remarks>` cites RESEARCH § Don't Hand-Roll CSV row and
    explains the Microsoft.VisualBasic.FileIO.TextFieldParser rejection rationale.

    Create `FormCsvImportPreviewDialog.cs` + Designer per the behavior block; per-call modal
    lifecycle XML comment per RESEARCH § Pitfall 6 (NOT hide-not-dispose); locked copy verbatim
    from UI-SPEC § Destructive + § Copywriting.

    Update `TheJawaToolboxDotNet.csproj`: 3 new explicit `<Compile Include>` entries.

    No new tests in this task — Task 1's CsvCellCoercionTests cover the framework layer; the TJT
    integration is exercised by Plan 09-07's live-SWG smoke (a maintainer-driven CSV import on a
    real .tab fixture).
  </action>
  <verify>
    <automated>powershell -Command "& 'D:/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe' '../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj' /p:Configuration=Debug /p:Platform=x86 /v:minimal" 2>&1 | tail -10  # build green; grep -c "DatatableCsvSerializer" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/DatatableCsvSerializer.cs"  # MUST return >= 1; grep -c "CsvCellCoercion" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/DatatableCsvSerializer.cs"  # MUST return >= 1 (the serializer consumes the framework helper); grep -c "Saving\\\\DatatableCsvSerializer.cs" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"  # MUST return 1; grep -c "FormCsvImportPreviewDialog.cs" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"  # MUST return 2 (one for .cs, one for .Designer.cs)</automated>
  </verify>
  <done>DatatableCsvSerializer.cs + FormCsvImportPreviewDialog.cs + Designer ship; csproj entries added; TJT MSBuild Debug|x86 green; the hand-rolled CSV parser stays under 100 lines; locked UI-SPEC copy applied verbatim in the modal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire FormDatatableEditor — Find/Replace pane + Import/Export CSV + column-click sort + DT_Comment frozen-row toggle + Sort_DoesNotMutateModelOrder regression fact</name>
  <files>../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.Designer.cs, UtinniCoreDotNet.Tests/UITests/DatatableSortViewOnlyTests.cs</files>
  <read_first>
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs (the file as it stands after Plans 09-03/04/05 — Plan 09-06 modifies the ProcessCmdKey + adds Find/Replace handlers + wires Import CSV / Export CSV buttons + adds column SortMode + wires Edit-comment-rows toggle)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.Designer.cs (Plan 09-03 — the Find/Replace pane control instantiations need to be added; Plan 09-03 reserved the pnlFindReplace Panel space but did NOT instantiate the inner controls)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/DatatableCsvSerializer.cs (Task 2 — Import/Export/ImportAsync API the form consumes)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/ThemedDataGridView.cs (Plan 09-03 — CellFormatting handler with hooks for search-match overlay; Plan 09-06 fills the body via a Find pane state field)
    - UtinniCoreDotNet/Formats/Datatable/MutableDataTableDocument.cs (Plan 09-01 — Rows iteration for sort-vs-save check)
    - UtinniCoreDotNet/Formats/Datatable/DataTableWriter.cs (Plan 09-01 — Serialize() iterates MutableDataTableDocument.Rows directly; the Sort_DoesNotMutateModelOrder fact asserts this invariant)
    - UtinniCoreDotNet.Tests/UITests/DatatableSortViewOnlyTests.cs (NEW STA test file — the real Sort_DoesNotMutateModelOrder [Fact]; binds a plain System.Windows.Forms.DataGridView, calls grid.Sort, asserts model serialization unchanged; uses the cross-repo UI-test-placement decision shared with Plan 09-03 — no TJT reference)
    - UtinniCoreDotNet.Tests/FormatsTests/Datatable/DataTableWriterTests.cs (Plan 09-01 — read for the existing round-trip fixture-load pattern the new STA fact mirrors; do NOT add the sort fact here — it needs STA + WinForms and lives in UITests/)
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-UI-SPEC.md § Layout Contract Find/Replace pane row + § States Find pane open / Replace pane open / Sort active / CSV import in progress + § Copywriting Find/Replace labels + LOCKED sort tooltip "View order only — save serializes physical row order." + Frozen-comment-row toggle copy
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-CONTEXT.md § Implementation Decisions D-07 (Find/Replace) + D-09 (column-click sort view-only)
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-RESEARCH.md § Pitfall 9 (sort vs save-order divergence — the writer-side grep gate + the xUnit fact)
  </read_first>
  <behavior>
    - FormDatatableEditor.cs MODIFICATIONS:
      - **Find/Replace pane wire:**
        - Add private fields: `string currentFindQuery`, `bool findMatchCase`, `bool findRegex`, `bool findIncludeComments`, `List<(int row, int col)> currentMatches`, `int currentMatchIndex`.
        - `ToggleFindPane()` method: if pnlFindReplace.Visible == false → show + focus txtFind + set Replace controls Visible=false; else collapse pane.
        - `ToggleReplacePane()` method: similar but Replace controls (txtReplace + btnReplaceOne + btnReplaceAll) Visible=true.
        - `txtFind.TextChanged` handler: debounce 200 ms (per RESEARCH Pitfall 7 mitigation — avoid CellFormatting jitter on every keystroke) → call `RecomputeMatches()` → set `lblFindCount.Text = $"{currentMatchIndex+1} / {currentMatches.Count}"` (or `0 / 0` at Colors.FontDisabled() if empty).
        - `RecomputeMatches()`: iterate `dtDocument.Mutable.Rows × Cells`, for each cell convert value to string via `CsvCellCoercion.SerializeCellToCsv`, run match per scope toggles (case + regex + include-comments), build `currentMatches` list; set `currentMatchIndex = 0`; `gridSurface.CurrentCell = matchCell` if found; force `gridSurface.Invalidate()` so CellFormatting paints the search-match overlay. **When the regex toggle is on, construct the matcher as `new Regex(pattern, regexOptions, TimeSpan.FromSeconds(2))` (iter-2 item 10 — cursor LOW) and wrap the match loop in try/catch for `RegexMatchTimeoutException` → set `lblFindCount.Text = "pattern too slow — simplify"` at `Colors.Secondary()` and leave `currentMatches` empty, rather than letting a catastrophic pattern freeze the UI thread.**
        - `FindNext()` / `FindPrev()` (F3 / Shift+F3): cycle currentMatchIndex modulo list length; jump CurrentCell.
        - `btnReplaceOne.Click`: get currentMatch cell; honor per-column type validation by running `controller.Apply(DatatableEditCommands.EditCellValue(cell, newValue))` (which goes through `MangleValue` internally per Plan 09-04's EditCellValueCommand); on type-invalid, surface red status copy `Replacement "{value}" cannot convert to {DT_Type}.`
        - `btnReplaceAll.Click`: for each match, build a list of edits → wrap as a single transaction via a temporary CsvImportPlan-like batch (or as a single composite `ApplyCsvImport`-style call). On invalid replacement(s), surface count in status; continue with valid ones.
        - `btnClose.Click` (or Esc while focused): collapse pane.
        - Wire `Ctrl+F` / `Ctrl+H` / `F3` / `Shift+F3` in ProcessCmdKey (Plan 09-03 ships them as deferred; Plan 09-06 fills the handler bodies).
        - Wire CellFormatting search-match overlay in ThemedDataGridView (the handler hook from Plan 09-03 takes a callback OR the form subscribes additively): if (row, col) ∈ currentMatches → set CellStyle.BackColor = Colors.Secondary() (at 40% opacity per UI-SPEC § Cell-state visual overlays — implementable via the CellPainting event or a derived BackColor; planner discretion).
      - **Import CSV…** toolbar button click: open `OpenFileDialog` (filter `CSV files (*.csv)|*.csv|TSV files (*.tsv)|*.tsv|All files|*.*`); on accept, `await DatatableCsvSerializer.ImportAsync(dtDocument.Mutable, dlg.FileName, controller, this)`. Status strip flow per UI-SPEC § States `CSV import in progress` row.
      - **Export CSV…** toolbar button click: open `SaveFileDialog` (filter `CSV files (*.csv)|*.csv|TSV files (*.tsv)|*.tsv`); on accept, `DatatableCsvSerializer.Export(dtDocument.Mutable, dlg.FileName, includeTypeRow: true)` (the `#`-prefixed type row is included by default per Open Question 4 recommendation). Status strip green `Exported {N} rows × {M} cols to {fileName}`.
      - **Column-click sort (D-09)**: in `LoadDocument` (after `BindMutable`), iterate `gridSurface.Columns` and set `col.SortMode = DataGridViewColumnSortMode.Automatic` for each (this enables the built-in click-to-sort + ▲/▼ glyph). Hover tooltip on the glyph reads the LOCKED copy `View order only — save serializes physical row order.` (set via `col.ToolTipText`). **CRITICAL:** confirm via grep that `DatatableSaveTargets.cs` AND `DataTableWriter.cs` AND `MutableDataTableDocument.cs` all return 0 for `grep -c "dataGridView"` — the model serialization MUST NOT iterate the BindingSource view order.
      - **DT_Comment frozen-row toggle**: in `LoadDocument`, for the first row whose any cell's column ColumnType.Type == DT_Comment (planner-friendly: iterate Rows; find the first row that's effectively a comment row per Plan 09-01's comment-aware model), set `row.Frozen = true` + `row.DefaultCellStyle.BackColor = Colors.PrimaryShadow()` + `row.DefaultCellStyle.ForeColor = Colors.FontDisabled()` IF `editCommentRows == false` (ini-loaded). Toolbar `UtinniToggleButton tglEditCommentRows` (LOCKED label `Edit comment rows`): on toggle, set `editCommentRows` field + persist to `[DatatableEditor] editCommentRows` ini key + re-apply Frozen state.
    - FormDatatableEditor.Designer.cs MODIFICATIONS:
      - Populate the `pnlFindReplace` controls Plan 09-03 reserved but didn't instantiate: `txtFind` (UtinniTextbox, width 240), `lblFindCount` (UtinniLabel), `btnFindPrev` ("◀"), `btnFindNext` ("▶"), separator, `txtReplace` (UtinniTextbox, width 240, Visible=false initially), `btnReplaceOne` ("Replace"), `btnReplaceAll` ("Replace all"), separator, `tglMatchCase` ("Aa"), `tglRegex` (".*"), `tglIncludeComments` ("Cmt"), `btnClose` ("✕").
      - Add tooltips per UI-SPEC R-02: btnFindPrev → `Find previous (Shift+F3)`; btnFindNext → `Find next (F3)`; btnClose → `Close (Esc)`.
      - Add `tglEditCommentRows` UtinniToggleButton to the toolbar (placement per UI-SPEC § Layout Contract — recommend after the Find/Replace separator group).
    - DatatableSortViewOnlyTests.cs (NEW — `UtinniCoreDotNet.Tests/UITests/`, SDK-style auto-glob; **STA required** for the WinForms `DataGridView`; uses a plain `System.Windows.Forms.DataGridView` per the cross-repo UI-test-placement decision shared with Plan 09-03 — NO `TheJawaToolboxDotNet` reference, NO `ThemedDataGridView`; 1 new fact). Use the same STA mechanism Plan 09-03's UITests use (`[StaFact]` or a `Thread(...).SetApartmentState(ApartmentState.STA)` wrapper):
      - `Sort_DoesNotMutateModelOrder` — the REAL D-09 view-only enforcement: a genuine `DataGridView.Sort` on the VIEW must NOT change the bytes `DataTableWriter` emits from the MODEL. The prior body only serialized the model twice (it never sorted anything), proving only writer determinism — which Plan 09-01 already guarantees (codex MEDIUM / cursor HIGH). Real test body (on an STA thread):
        - Load `DatatableFixtures.BuildV1CombatDataTableLike()` via `IffReader.Read` + `MutableIffDocument.FromDocument` + `DataTableDocument.FromIff`. Pick column index `C` whose model order is NOT already ascending (e.g. an int/string column with shuffled values), so a view sort visibly reorders the rows.
        - Capture pre-sort MODEL serialization: `byte[] preSortBytes = new DataTableWriter(dtDoc.Mutable).Serialize();`.
        - Construct a plain `System.Windows.Forms.DataGridView`; add one `DataGridViewTextBoxColumn` per model column with `Columns[C].SortMode = DataGridViewColumnSortMode.Automatic`; populate rows in MODEL order via `Rows.Add(...)` (per-cell `.Value` from each `MutableDataTableCell`).
        - Record the displayed column-`C` order before sort: `var preView = grid.Rows.Cast<DataGridViewRow>().Where(r => !r.IsNewRow).Select(r => r.Cells[C].Value?.ToString()).ToList();`.
        - Invoke the built-in sort for real: `grid.Sort(grid.Columns[C], System.ComponentModel.ListSortDirection.Ascending);`.
        - Record the displayed order after sort and assert the VIEW actually reordered (proves the sort RAN, not a no-op): `Assert.NotEqual(preView, postView);` (with `postView` collected the same way).
        - Re-serialize the MODEL: `byte[] postSortBytes = new DataTableWriter(dtDoc.Mutable).Serialize();`.
        - Assert MODEL bytes are byte-identical despite the live view sort: `Assert.Equal(preSortBytes, postSortBytes);`.
        (This is the exact D-09 risk both reviewers named — a view sort leaking into save order. Because `DataTableWriter` takes a `MutableDataTableDocument` directly, a sorted grid view cannot reorder the saved rows; the fact now PROVES the sort genuinely reorders the view AND the model serialization is unchanged. The writer-side grep gate `grep -c "dataGridView" DataTableWriter.cs MutableDataTableDocument.cs DatatableSaveTargets.cs` remains a SECONDARY check that no UI concept leaked into the save path.)
  </behavior>
  <action>
    Modify FormDatatableEditor.cs per the behavior block:
    - Add Find/Replace state fields + handler methods + ProcessCmdKey wiring (Plan 09-03 stubs replaced).
    - Wire Import CSV / Export CSV toolbar button click handlers.
    - In LoadDocument, set DataGridViewColumnSortMode.Automatic + tooltip per column.
    - Wire Edit-comment-rows toggle + persistence + frozen-row re-apply.
    - Add the CellFormatting search-match overlay subscription (or extend the ThemedDataGridView
      hook from Plan 09-03 — planner discretion).

    Modify FormDatatableEditor.Designer.cs to populate the pnlFindReplace inner controls + add
    tglEditCommentRows to the toolbar. Tooltips per UI-SPEC R-02.

    Create `UtinniCoreDotNet.Tests/UITests/DatatableSortViewOnlyTests.cs` with the real STA
    `Sort_DoesNotMutateModelOrder` [Fact] per the behavior block — bind a plain
    `System.Windows.Forms.DataGridView`, call `grid.Sort(...)` for real, assert the VIEW reordered
    AND the model serialization is byte-identical. The UITests directory may already exist (Plan
    09-03); SDK-style auto-glob — no test-csproj edit. NO `TheJawaToolboxDotNet` reference.

    Cross-repo paired commit: `feat(09): CSV import/export + Find/Replace + column-click sort +
    DT_Comment frozen-row toggle`.
  </action>
  <verify>
    <automated>powershell -Command "& 'D:/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe' '../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj' /p:Configuration=Debug /p:Platform=x86 /v:minimal" 2>&1 | tail -10  # build green; dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~Sort_DoesNotMutateModelOrder" 2>&1 | tail -10  # MUST exit 0; grep -c "dataGridView" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/DatatableSaveTargets.cs" UtinniCoreDotNet/Formats/Datatable/DataTableWriter.cs UtinniCoreDotNet/Formats/Datatable/MutableDataTableDocument.cs  # MUST return 0:0:0 (D-09 view-only-sort defense — writer layer has no UI concept); grep -c "DataGridViewColumnSortMode.Automatic\|SortMode = SortMode.Automatic\|SortMode.Automatic" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs"  # MUST return >= 1 (sort wired); grep -c "DatatableCsvSerializer" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs"  # MUST return >= 2 (Import + Export); grep -c "Edit comment rows" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.Designer.cs"  # MUST return >= 1 (locked label); grep -c "View order only" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs"  # MUST return >= 1 (LOCKED tooltip copy)</automated>
  </verify>
  <done>FormDatatableEditor.cs has Find/Replace + CSV Import/Export + column-click sort + DT_Comment frozen-row toggle wired; Designer populates the Find/Replace pane controls with R-02 tooltips; Sort_DoesNotMutateModelOrder xUnit fact passes; D-09 grep gate is 0 across writer-side files; full Datatable test subsuite green; TJT MSBuild Debug|x86 green.</done>
</task>

<task type="auto">
  <name>Task 4 (CONDITIONAL — only if Plan 09-03 measured > 100 ms DataGridView bind latency): VirtualMode fallback</name>
  <files>../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/ThemedDataGridView.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs</files>
  <read_first>
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-03-SUMMARY.md (the recorded DataGridView bind-latency measurement; this task EXECUTES ONLY if the measurement was > 100 ms)
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-UI-SPEC.md § Grid surface (paragraph documenting VirtualMode as the V1 fallback IF profiling indicates)
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-RESEARCH.md § Pitfall 7 (DataGridView performance — virtual mode complicates CellFormatting + Frozen rows; planner must rework the search-match overlay strategy)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/ThemedDataGridView.cs (Plan 09-03 — current non-virtual BindMutable; Task 4 swaps to VirtualMode)
    - BCL DataGridView CellValueNeeded / CellValuePushed docs (BCL pattern reference; standard MSDN)
  </read_first>
  <action>
    **PRE-FLIGHT (iter-2 item 7 — cursor MEDIUM, supersedes the iter-1 fix-#6 unconditional default):** Before reading the SUMMARY for the latency
value, run `grep -E "DataGridViewBindLatency.*[0-9]+\.[0-9]+ ms" .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-03-SUMMARY.md`. If grep returns 0 matches, treat the measurement as **ABSENT** (Plan 09-03 Task 3 deferred). **On ABSENT, DEFAULT TO non-VirtualMode + a documented follow-up: SKIP this task, record the ABSENT disposition + a `[follow-up] VirtualMode not yet evidenced — re-run the 09-03 probe or maintainer-checkpoint before enabling` note in 09-06-SUMMARY.md, and ship non-virtual.** Do NOT build VirtualMode unconditionally on a missing measurement — VirtualMode is a substantial re-implementation (it complicates CellFormatting + Frozen rows per RESEARCH Pitfall 7) and must be backed by either a measured > 100 ms result OR an explicit maintainer checkpoint, never by absence of evidence. (Rationale: revised 09-03 Task 3 now measures the PRODUCTION path — a DataGridView with a CellFormatting overlay — so "no measurement" genuinely means "unknown", not "slow".)

**GATE: Read `.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-03-SUMMARY.md` first.
    Look for the recorded DataGridView bind-latency value. If the value is ≤ 100 ms, SKIP this
    task — record in the 09-06-SUMMARY that VirtualMode was deemed unnecessary per the
    measurement, mark this task as `<status>skipped</status>`, exit with success. Only proceed if
    the measurement was > 100 ms.**

    If proceeding:
    - Modify ThemedDataGridView.cs `BindMutable` to set `VirtualMode = true` instead of populating
      `RowCount = doc.Rows.Count` directly.
    - Add `CellValueNeeded` handler: `e.Value = doc.Rows[e.RowIndex].Cells[e.ColumnIndex].Value`
      converted to display via `CsvCellCoercion.SerializeCellToCsv` (for read-only display) OR via
      per-type widget value extraction (for editable cells).
    - Add `CellValuePushed` handler: `controller.Apply(DatatableEditCommands.EditCellValue(
      doc.Rows[e.RowIndex].Cells[e.ColumnIndex], DataTableCellValue.FromString((string)e.Value)))`.
    - Re-implement the search-match overlay via the CellPainting event since virtual mode bypasses
      CellFormatting for non-rendered cells (planner must handle the off-screen-but-matched case
      — recommend painting only the visible row range and using `gridSurface.FirstDisplayedScrollingRowIndex`
      to refresh visible matches).
    - Re-implement the Frozen DT_Comment row treatment via `e.RowIndex == 0 && firstRowIsComment ?
      e.CellStyle.BackColor = Colors.PrimaryShadow() : ...` in the CellPainting / CellFormatting
      branch.

    Re-measure latency after the VirtualMode switch; document the new measurement in the SUMMARY.

    This task is intentionally minimal — VirtualMode is a meaningful re-implementation, but
    if it's needed it's the only way to meet the V1 performance target.
  </action>
  <verify>
    <automated>echo "Task 4 is conditional — verify status in 09-06-SUMMARY"  # this verify is intentionally a no-op; the SUMMARY documents whether Task 4 executed or skipped; if executed, the bind-latency re-measurement is recorded</automated>
  </verify>
  <done>If executed: VirtualMode is enabled in ThemedDataGridView; CellValueNeeded + CellValuePushed handlers wired; the re-measured bind latency is recorded in SUMMARY; TJT MSBuild Debug|x86 green; the full Datatable test subsuite green (no regression). If skipped: SUMMARY documents the skip with the 09-03 measurement citation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → process (CSV import) | OpenFileDialog OS-mediated; CsvCellCoercion.PlanImport enforces row + cell-size sanity caps (DoS mitigation) |
| process → disk (CSV export) | SaveFileDialog OS-mediated; UTF-8 BOM + RFC-4180 escape; no raw bytes the user couldn't reproduce by inspecting the .tab |
| user input → grid (Find/Replace) | regex toggle accepts user pattern; .NET `Regex` constructed with a `matchTimeout` of `TimeSpan.FromSeconds(2)` so a catastrophic-backtracking pattern raises `RegexMatchTimeoutException` (caught → surfaced as a red "pattern too slow — simplify" status in the Find pane) instead of freezing the UI thread (iter-2 item 10 — cursor LOW) |
| grid sort → save | D-09 view-only contract enforced by grep gate (no `dataGridView` reference in writer-side files) + Sort_DoesNotMutateModelOrder xUnit fact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-27 | DoS (malicious CSV heap blow-up) | CsvCellCoercion.PlanImport | mitigate | Row count capped at 100,000; cell size capped at 16 KB (same `_loadRow` buffer SOE uses per `DataTableWriter.cpp:768`); over-cap throws `CsvParseException` before allocation explosion. Tested via 2 [Fact]s in CsvCellCoercionTests. |
| T-09-28 | Tampering (silent schema corruption via CSV import) | ApplyCsvImportCommand | mitigate | D-08 byte-exact-on-untouched invariant: cells with imported value equal to current value stay clean (preserve originalSlice). Type-invalid cells go to InvalidCellEntry list, NOT applied — D-04 cascade does not run for CSV imports (those would surface as red rows in the preview, not silent corruption). Tested via SC4 controller-layer fact + framework-layer fact in CsvCellCoercionTests. |
| T-09-29 | Tampering (sort vs save-order divergence) | DataGridView column-click sort | mitigate | DUAL enforcement: grep gate `grep -c "dataGridView" DatatableSaveTargets.cs DataTableWriter.cs MutableDataTableDocument.cs` MUST return 0 (writer-layer cannot reference UI concepts) + xUnit fact `Sort_DoesNotMutateModelOrder` asserts writer iterates model order regardless of view sort. |
| T-09-30 | DoS (regex catastrophic backtracking in Find/Replace) | RecomputeMatches with regex toggle | mitigate (iter-2 item 10) | The regex is constructed with a `matchTimeout` (`new Regex(pattern, options, TimeSpan.FromSeconds(2))`); a pathological pattern raises `RegexMatchTimeoutException`, caught in RecomputeMatches and surfaced as a red Find-pane status ("pattern too slow — simplify") rather than freezing the UI thread. Tested via 1 [Fact] feeding a known catastrophic pattern against a long input and asserting the timeout path returns gracefully (no hang). |
| T-09-31 | Information disclosure (CSV export of DT_HashString stores int32 not source string) | DatatableCsvSerializer.Export | accept (documented) | RESEARCH § Pitfall 4 — DT_HashString cells persist the int32 hash on disk (the source string is NOT stored). Per the 09-03 DT_HashString UX decision, the editor displays the stored int32 by default with an explicit "enter source string to compute hash" affordance; that entered source string is in-session only. CSV export therefore writes the int32 (or, if the editor tracks an in-session source string for an unsaved cell, that string) — and a CSV round-trip CANNOT reconstruct a source string for an already-saved HashString cell. This is documented in the CSV format-header comment AND flagged in 09-07 smoke Step 10 so the maintainer does not expect source-string round-trip. |
| T-09-32 | Information disclosure (CSV cell injection — value starting with `=` interpreted as Excel formula) | Exported CSV opened in Excel | out of scope | UI-SPEC § Security Domain explicitly documents this as out-of-scope; exported CSVs are for human review / programmatic re-import, not direct execution. |
| T-09-SC | Tampering | npm/pip/cargo installs | N/A | Plan 09-06 adds ZERO new external packages (CSV parser is hand-rolled per RESEARCH § Don't Hand-Roll). |

**ASVS controls applied:**
- V5 Input Validation — CsvCellCoercion.PlanImport runs MangleValue per cell + flags failures; row + cell sanity caps; Find/Replace honors per-column type validation.
- V12 File and Resources — CSV file I/O via OpenFileDialog/SaveFileDialog (OS-mediated); no path traversal surface (the user picks the path); UTF-8 BOM + RFC-4180 escape produce well-formed output.
</threat_model>

<verification>
Plan-level acceptance:

1. `dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~CsvCellCoercion|FullyQualifiedName~ApplyCsvImport|FullyQualifiedName~Sort_DoesNotMutateModelOrder"` exits 0 with ≥ 14 facts.
2. Both repos MSBuild Debug|x86 + Release|x86 green.
3. `grep -c "dataGridView" ../UtinniPlugins/The\ Jawa\ Toolbox/TheJawaToolboxDotNet/Saving/DatatableSaveTargets.cs UtinniCoreDotNet/Formats/Datatable/DataTableWriter.cs UtinniCoreDotNet/Formats/Datatable/MutableDataTableDocument.cs` returns 0 (D-09 grep gate).
4. `grep -c "NotImplementedException" UtinniCoreDotNet/Editing/DatatableEditCommands.cs` returns 0 (ApplyCsvImport stub replaced).
5. `grep -c "Formats\\\\Datatable\\\\CsvCellCoercion" UtinniCoreDotNet/UtinniCoreDotNet.csproj` returns 1.
6. `grep -c "Saving\\\\DatatableCsvSerializer\\|UI\\\\Forms\\\\FormCsvImportPreviewDialog" ../UtinniPlugins/The\ Jawa\ Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj` returns ≥ 3.
7. Full UtinniCoreDotNet.Tests + Utinni.Cli.Tests + PreservationAudit suites stay green.
8. Task 4 status documented in SUMMARY (executed OR skipped, with the 09-03 measurement citation).
</verification>

<success_criteria>
- CSV/TSV import + export ship with the byte-exact-on-untouched property (D-08 / CF-04 / CF-02 composition); SC4 enforced at THREE layers now (framework cell + controller + CLI + CSV).
- Find/Replace works with per-column type validation honored; F3/Shift+F3 cycle navigation; Esc collapses pane.
- Column-click sort is view-only; the D-09 grep gate + Sort_DoesNotMutateModelOrder fact prevent silent save-order corruption.
- DT_Comment frozen-row toggle persists to ini + re-applies on next session.
- The framework-side CsvCellCoercion is xUnit-testable from CI (checker B-1 extraction); the TJT-side serializer is a thin file-I/O + parser layer that consumes it.
- VirtualMode fallback decision is documented in SUMMARY based on Plan 09-03's measurement (either executed in Task 4 or explicitly skipped).
- Plan 09-07 (Tier-4 live-SWG smoke) can exercise the full feature surface end-to-end without further plan dependencies.
</success_criteria>

<output>
Create `.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-06-SUMMARY.md` when done. Record:
- CsvCellCoercion line count + xUnit fact count.
- ApplyCsvImportCommand implementation detail (transaction wrapper + capturedState shape).
- DatatableCsvSerializer line count (target < 250 lines total; parser < 100 lines).
- D-09 grep gate confirmation (must be 0 across writer-side files).
- Sort_DoesNotMutateModelOrder fact result (green).
- Task 4 status: EXECUTED (with re-measured latency) OR SKIPPED (with 09-03 measurement citation).
- Cross-repo paired commit hashes (per task).
- Any deviation from the UI-SPEC R-02 tooltip wording or the LOCKED D-08/D-09 copy.
</output>

## Revision History

- 2026-05-29 (iter 1): wave bumped 4 -> 5 (cascade from Issue #5 09-05 wave shift). Task 4 (conditional VirtualMode) gained a PRE-FLIGHT fail-fast guard per checker WARNING #6: grep for DataGridViewBindLatency in 09-03-SUMMARY; if absent, build VirtualMode unconditionally (never silently skip).
- 2026-05-29 iter-2 (reviews): REPLACED the `Sort_DoesNotMutateModelOrder` fact — the old body only serialized the model twice (it never sorted anything), proving only writer determinism (codex MEDIUM / cursor HIGH). New body is a real STA `[Fact]` in a NEW file `UtinniCoreDotNet.Tests/UITests/DatatableSortViewOnlyTests.cs` that binds a plain `System.Windows.Forms.DataGridView`, calls `grid.Sort(...)` for real, asserts the VIEW reordered (`Assert.NotEqual`) AND the model `DataTableWriter.Serialize()` bytes are byte-identical (`Assert.Equal`); uses the cross-repo UI-test-placement decision shared with revised 09-03 (plain DataGridView, NO TheJawaToolboxDotNet reference). Writer-side grep gate kept as secondary [item 6]. REVERSED Task 4's absent-measurement default from "build VirtualMode unconditionally" to "non-VirtualMode + documented follow-up; VirtualMode only on a > 100 ms measurement or a maintainer checkpoint" (updated assumes + truth + objective + PRE-FLIGHT) [cursor item 7]. Added `UtinniCoreDotNet/Formats/Datatable/DataTableCellValue.cs` to files_modified (Task 1 extends it with ToCsvString) [codex item 8]. Added a 2 s `Regex` matchTimeout (`RegexMatchTimeoutException` → red Find-pane status) in RecomputeMatches + flipped T-09-30 accept→mitigate [cursor item 10]. Documented DT_HashString CSV lossiness explicitly (int32 on disk; source string in-session only; no source-string CSV round-trip) aligned to the 09-03 UX decision, in ToCsvString + T-09-31 + cross-ref to 09-07 smoke Step 10 [both, item 9].
