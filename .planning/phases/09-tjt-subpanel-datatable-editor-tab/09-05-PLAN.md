---
phase: 09-tjt-subpanel-datatable-editor-tab
plan: 05
type: execute
wave: 4
depends_on: ["09-03", "09-04"]
files_modified:
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/DatatableSaveTargets.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"
  - UtinniCoreDotNet.Tests/SavingTests/DatatableSaveTargetsTests.cs
  - UtinniCoreDotNet.Tests/SavingTests/DatatableReloadRoutingTests.cs
autonomous: true
requirements: [PROD-W1-DT]
assumes:
  - "Plan 09-03 has shipped FormDatatableEditor with the Save▾ menu shape + stub click handlers. Plan 09-05 replaces the stubs with the actual save-target dispatch. Plan 09-04 wires the NeedsReview gate on RefreshSaveMenuEnabledState; Plan 09-05 composes the provenance gate ON TOP (`item.Enabled = provenanceAllows && !blockedByCascade`). The two plans converge in FormDatatableEditor.cs but modify DIFFERENT method bodies (no merge conflicts expected; cross-AI review will confirm)."
  - "Phase 8's framework helpers — `LooseOverridePath.Resolve`, `TreBackupPath`, `TreRepackLock`, `ReloadAssetClassifier` — are SHIPPED in `UtinniCoreDotNet/Saving/` and READY for verbatim reuse. Phase 8's plugin-side `IffSaveTargets`, `ClientReloadDispatcher`, `TreRepackSaveTarget` are SHIPPED in `TheJawaToolboxDotNet/Saving/` and READY for verbatim reuse. Plan 09-05's `DatatableSaveTargets.cs` is a THIN composition shim (< 100 lines) per PATTERNS § File Classification + § High-Leverage Reuse Points #1+#2."
  - "Mode 3 (in-memory live patch) stays DISABLED per CF-03. Plan 09-05 wires the Save▾ ▸ Patch live client menu item to the same provenance-gated disabled state Phase 8 ships (`Source is OpenSource.ClientMemory` AND `Game.IsRunning` — no Phase 9 open path constructs ClientMemory, so the gate stays false; tooltip = inherited Phase 8 wording `Live patch requires opening from client memory — not wired in this phase.`)."
  - "V6000+ archive repack refusal per Phase 8 WR-06: `TreRepackSaveTarget.Apply` already refuses V6000 archives in its existing implementation. Plan 09-05 inherits the refusal — Phase 9's Save▾ ▸ Repack code path passes the bytes through; the existing TreRepackSaveTarget refusal logic runs. Plan 09-05 adds ONE xUnit fact confirming the V6000 refusal still triggers on a DTII payload going into a V6000-source archive (mirrors Phase 8 Plan 07 TreRepackLogicalPathTests pattern)."
  - "Reload routing: the existing `ReloadAssetClassifier.Classify(\".iff\", \"DTII\")` already returns `PendingNextSceneChange` per Phase 8 Plan 05 routing-table test (`ClientReloadDispatcherTests.cs`). Plan 09-05's reload-button click delegates to `ClientReloadDispatcher.Dispatch(savedPath, \"DTII\")` which routes correctly. Plan 09-05 adds ONE confirming xUnit fact in DatatableReloadRoutingTests.cs to make the DTII routing explicit for Phase 9 (even though Phase 8 already covered it — defense in depth)."
  - "TRE Browser hand-off (D-10.2) and IFF Editor hand-off (D-10.3) edit FormTreBrowser.cs + FormIffEditor.cs additively — one new context-menu item + handler per file. Existing signatures of `OpenFromTreEntry` etc. MUST NOT change per Binary-compat audit § Hot-zone (Plan 09-PATTERNS lines 731-742 — `LoadDocument`, `OpenFromTreEntry`, etc. on FormIffEditor stay locked)."
must_haves:
  truths:
    - "Save▾ ▸ Save (in place) writes the .tab back to its `OpenSource.LooseFile` path via Phase 8's `IffSaveTargets.SaveInPlace`; provenance-gated on `Source is OpenSource.LooseFile`."
    - "Save▾ ▸ Save as loose override writes the .tab under the client's loose-override directory via Phase 8's `IffSaveTargets.SaveLooseOverride` + `LooseOverridePath.Resolve`; provenance-gated on (Source is LooseFile OR TreArchive); root-containment defense inherited verbatim."
    - "Save▾ ▸ Save As… writes the .tab to a user-picked path via Phase 8's `IffSaveTargets.SaveToPath`; ALWAYS enabled (round-2 MEDIUM 5 / Phase 8 escape-hatch posture) UNLESS blocked by Plan 09-04's NeedsReview gate."
    - "Save▾ ▸ Patch live client (in memory) stays DISABLED with the inherited Phase 8 tooltip — no Phase 9 open path constructs `OpenSource.ClientMemory` (CF-03)."
    - "Save▾ ▸ Repack into source .tre… dispatches to Phase 8's `TreRepackSaveTarget.Apply(ta, dataTableWriter.Serialize(), createBackup)`; provenance-gated on `Source is OpenSource.TreArchive`; V6000+ archive refusal inherited (Phase 8 WR-06)."
    - "TRE Browser D-10.2: a new context-menu item `Open in Datatable Editor` appears on selected entries where `extension == \".tab\" OR root tag == \"DTII\"`; click handler mirrors `OnOpenInIffEditor` verbatim (off-UI-thread `TrePayloadResolver.TryResolve` → `BeginInvoke` → `FindOrCreateDatatableEditor()` → `editor.OpenFromTreEntry(payload, archivePath, logicalPath, offset)`). Visibility predicate is HIDDEN (not just disabled) when the entry doesn't match (mirrors Phase 8 IFF hand-off shape per UI-SPEC § Copywriting D-10.2 row)."
    - "IFF Editor D-10.3: a new menu item `Switch to typed datatable view` appears on FormIffEditor's menu, visible only when `document.Root.TypeId == \"DTII\"`. Manual hand-off — NOT auto-route. Click handler finds-or-creates FormDatatableEditor + hands the MutableIffDocument bytes + Source directly without re-parsing (the typed wrap happens via `DataTableDocument.FromIff(mutIff)` inside FormDatatableEditor.OpenFromMutableIff — a new public method on FormDatatableEditor)."
    - "After any successful save, `ClientReloadDispatcher.Dispatch(savedPath, \"DTII\")` is invoked → routes via `ReloadAssetClassifier.Classify(\".iff\", \"DTII\")` → `PendingNextSceneChange` (tier-(b) per CF-05); the reload-status badge stays on the locked CF-05 copy (Plan 09-03 already wires the badge text); no fresh binding call (per Anti-Patterns Pitfall 10)."
    - "`OpenSource.Unknown` Save-As escape hatch preserved: when the .tab opened from a TRE entry but `TreRecordIndexResolver.ResolveOrUnknown` failed (degraded W-3 path), Source is set to `OpenSource.Unknown.Instance`. In that state, Save (in place), Save loose override, Save Live patch, Save Repack TRE are ALL DISABLED; only Save As… remains enabled (round-2 MEDIUM 5)."
  artifacts:
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/DatatableSaveTargets.cs"
      provides: "Thin composition shim — Build via DataTableWriter, then dispatch to Phase 8's existing IffSaveTargets + TreRepackSaveTarget verbatim; < 100 lines per PATTERNS"
      contains: "DataTableWriter"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs"
      provides: "MODIFY — replace Plan 09-03 Save▾ stubs with click handlers dispatching to DatatableSaveTargets; extend RefreshSaveMenuEnabledState with the provenance gate composed on top of Plan 09-04's NeedsReview gate; wire reload-button to ClientReloadDispatcher.Dispatch (still keeps the CF-05 locked status copy as the user-facing UI surface — the dispatch is for the actual routing path); add `OpenFromMutableIff(MutableIffDocument, OpenSource, string)` public method for the IFF Editor D-10.3 hand-off; add `OpenFromTreEntry(byte[], string, string, long)` public method for the TRE Browser D-10.2 hand-off"
      contains: "DatatableSaveTargets"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs"
      provides: "MODIFY — add `_miOpenInDatatableEditor` context-menu item + click handler + `FindOrCreateDatatableEditor` helper, mirroring `_miOpenInIffEditor` verbatim with the extension/root-tag visibility predicate"
      contains: "FindOrCreateDatatableEditor"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs"
      provides: "MODIFY — add `_miSwitchToDatatableView` menu item visible when `document.Root.TypeId == \"DTII\"`; click handler finds-or-creates FormDatatableEditor + calls `editor.OpenFromMutableIff(this.mutableIffDocument, this.Source, this.displayName)`"
      contains: "Switch to typed datatable view"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"
      provides: "Explicit <Compile Include> entry for the new Saving/DatatableSaveTargets.cs (round-2 HIGH-A)"
      contains: "Saving\\DatatableSaveTargets.cs"
    - path: "UtinniCoreDotNet.Tests/SavingTests/DatatableSaveTargetsTests.cs"
      provides: "~6-8 [Fact]s: DatatableSaveTargets.SaveLooseOverride composes through IffSaveTargets correctly (mock the destination + assert byte-exact write); SaveToPath success; SaveInPlace success; RepackIntoSourceTre V6000 refusal still triggers on DTII payload (Phase 8 WR-06 inheritance fact)"
      contains: "DatatableSaveTargetsTests"
    - path: "UtinniCoreDotNet.Tests/SavingTests/DatatableReloadRoutingTests.cs"
      provides: "~2-3 [Fact]s confirming `ReloadAssetClassifier.Classify(\".iff\", \"DTII\")` returns `PendingNextSceneChange` (defense-in-depth — Phase 8 already covers this in `ClientReloadDispatcherTests.cs`, but Phase 9 makes the DTII case explicit)"
      contains: "DatatableReloadRoutingTests"
  key_links:
    - from: "FormDatatableEditor.OnSaveInPlaceClick"
      to: "DatatableSaveTargets.SaveInPlace → IffSaveTargets.SaveInPlace (Phase 8 verbatim reuse)"
      via: "Build bytes via DataTableWriter, then call Phase 8 dispatcher"
      pattern: "IffSaveTargets\\.SaveInPlace"
    - from: "DatatableSaveTargets.RepackIntoSourceTre"
      to: "TreRepackSaveTarget.Apply (Phase 8 verbatim reuse) — already refuses V6000"
      via: "Composition: DataTableWriter.Serialize() bytes flow through unchanged"
      pattern: "TreRepackSaveTarget\\.Apply"
    - from: "FormTreBrowser._miOpenInDatatableEditor click"
      to: "TrePayloadResolver.TryResolve → BeginInvoke → FormDatatableEditor.OpenFromTreEntry"
      via: "Mirrors Phase 8 _miOpenInIffEditor verbatim (off-UI-thread payload resolve + BeginInvoke marshal)"
      pattern: "FindOrCreateDatatableEditor"
    - from: "FormIffEditor._miSwitchToDatatableView click"
      to: "FormDatatableEditor.OpenFromMutableIff(this.mutableIffDocument, this.Source, this.displayName)"
      via: "Manual hand-off; hands the in-memory MutableIffDocument directly (no re-parse)"
      pattern: "OpenFromMutableIff"
    - from: "FormDatatableEditor.OnReloadClicked"
      to: "ClientReloadDispatcher.Dispatch(lastSavedPath, \"DTII\") (Phase 8 verbatim)"
      via: "Tiered reload routing — DTII case routes to PendingNextSceneChange (tier-(b) per CF-05)"
      pattern: "ClientReloadDispatcher\\.Dispatch"
    - from: "FormDatatableEditor.RefreshSaveMenuEnabledState (extended by Plan 09-05)"
      to: "OpenSource pattern-match + NeedsReviewCount gate (composed)"
      via: "item.Enabled = provenanceAllowsThisMode && controller.NeedsReviewCount == 0"
      pattern: "OpenSource is"
---

<objective>
Ship the **entry points + save targets** for Phase 9, completing the editor's connection to disk
and live-client reload. This plan introduces a < 100-line `DatatableSaveTargets.cs` composition
shim that builds DTII bytes via Plan 09-01's `DataTableWriter` and dispatches to Phase 8's
existing `IffSaveTargets` (modes 1 + 2) and `TreRepackSaveTarget` (mode 4) verbatim — no new save
plumbing, no new path-defense, no new repack orchestration (PATTERNS § High-Leverage Reuse Points
#1, #2, #3).

It wires the three D-10 entry points: file picker `Open…` (already in Plan 09-03), TRE Browser
`Open in Datatable Editor` (D-10.2 — modifies FormTreBrowser.cs additively), IFF Editor `Switch to
typed datatable view` (D-10.3 — modifies FormIffEditor.cs additively). It replaces Plan 09-03's
Save▾ NotImplementedException stubs with click handlers dispatching to DatatableSaveTargets, and
EXTENDS Plan 09-04's NeedsReview-gated `RefreshSaveMenuEnabledState` by composing the provenance
gate on top (`item.Enabled = provenanceAllowsThisMode && controller.NeedsReviewCount == 0`).

The reload-button click delegates to Phase 8's `ClientReloadDispatcher.Dispatch` which routes the
DTII case to `PendingNextSceneChange` per the existing routing-table test — the user-facing UI
surface stays on the CF-05 locked copy from Plan 09-03 (NO new reload binding, NO scene-setup
trigger — Anti-Patterns Pitfall 10).

**Why this decomposition (Wave 4, serial after Plan 09-04 — checker iter-1 fix):** Plan 09-05 was originally Wave 3 parallel with Plan 09-04 but iter-1 review (Issue #5, checker fix Option B) bumped it to Wave 4 with a hard `depends_on: [09-03, 09-04]` edge to eliminate the FormDatatableEditor.cs merge risk (Plan 09-04 lands its LoadDocument controller-wire + RefreshSaveMenuEnabledState NeedsReview-gate edits FIRST; Plan 09-05 rebases its provenance-gate composition on top). Plan 09-05 is the "save + entry-point"
plan; Plan 09-04 is the "controller + structural ops + cascade" plan. They share the FormDatatableEditor.cs
file but modify non-overlapping method bodies (09-04 wires LoadDocument controller instantiation +
Add row/Add column + RefreshSaveMenuEnabledState NeedsReview gate; 09-05 wires Save▾ click handlers +
RefreshSaveMenuEnabledState provenance gate composition + OpenFromTreEntry + OpenFromMutableIff +
reload-button dispatch). Both list FormDatatableEditor.cs in `files_modified`; the orchestrator's
implicit-dependency rule (files_modified overlap → later wave) is intentionally OVERRIDDEN here via
the explicit `depends_on: [\"09-03\"]` — both depend only on 09-03's form host shape, and the cross-AI
review will confirm the additive non-overlapping nature. If the executor agents conflict, the
SUMMARY documents which plan committed which method-body region.

**Cross-repo paired commits:** Tasks 1+2 commit Utinni (2 new test files) AND UtinniPlugins
(DatatableSaveTargets.cs + 3 modified Forms + csproj) atomically per `feedback_utinniplugins_authority`.

**Round-2-style fixes folded:** OpenSource.Unknown Save-As escape hatch posture inherited from
Phase 8 round-2 MEDIUM 5 (Save As stays enabled; the other 4 modes disable). V6000 archive
repack refusal inherited from Phase 8 WR-06 — Plan 09-05 adds a confirming xUnit fact even though
TreRepackSaveTarget already enforces it (defense in depth for SC4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-CONTEXT.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-RESEARCH.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-PATTERNS.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-VALIDATION.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-UI-SPEC.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-01-PLAN.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-03-PLAN.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-04-PLAN.md

# Phase 8 save infrastructure — VERBATIM reuse targets
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/IffSaveTargets.cs
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/TreRepackSaveTarget.cs
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/ClientReloadDispatcher.cs
@UtinniCoreDotNet/Saving/LooseOverridePath.cs
@UtinniCoreDotNet/Saving/ReloadAssetClassifier.cs
@UtinniCoreDotNet/Saving/TreBackupPath.cs
@UtinniCoreDotNet/Saving/TreRepackLock.cs
@UtinniCoreDotNet/Formats/Tre/TreRecordIndexResolver.cs

# Phase 8 OpenSource pattern-match — provenance gate Plan 09-05 composes
@UtinniCoreDotNet/Formats/Iff/OpenSource.cs

# Phase 8 IFF Editor — the analog for FormDatatableEditor's Save▾ click handlers + OpenFromTreEntry + reload-dispatch wire
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs

# Phase 8 TRE Browser — the analog for the D-10.2 hand-off; mirror _miOpenInIffEditor verbatim
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs

# Plan 09-01 framework primitives — public API the save shim consumes
@UtinniCoreDotNet/Formats/Datatable/DataTableDocument.cs
@UtinniCoreDotNet/Formats/Datatable/DataTableWriter.cs
@UtinniCoreDotNet/Formats/Datatable/MutableDataTableDocument.cs

# Phase 8 reload routing — the routing-table test pattern Plan 09-05 mirrors
@UtinniCoreDotNet.Tests/SavingTests/ClientReloadDispatcherTests.cs

# Plan 09-03 FormDatatableEditor — MODIFY target
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs

<interfaces>
<!-- Phase 8 save infrastructure public surface — Plan 09-05 consumes verbatim -->

From TheJawaToolboxDotNet/Saving/IffSaveTargets.cs (Phase 8 — the verbatim dispatcher):
- public static class IffSaveTargets
  - public struct SaveResult { public string SavedPath; public bool Succeeded; public string ErrorMessage; }
  - public static Task<SaveResult> SaveLooseOverride(MutableIffDocument doc, OpenSource source, string resolvedClientRoot, string looseOverrideSubDir)
  - public static Task<SaveResult> SaveToPath(MutableIffDocument doc, string targetPath)
  - public static Task<SaveResult> SaveInPlace(MutableIffDocument doc, OpenSource source)
  // All three handle Flush(true) MEDIUM-9 barrier, atomic write, root containment, error normalization

From TheJawaToolboxDotNet/Saving/TreRepackSaveTarget.cs (Phase 8 — verbatim):
- public static class TreRepackSaveTarget
  - public enum TreRepackResult { Replaced, BackedUpThenReplaced, RefusedClientHoldsArchive_LooseOverrideRecommended, RefusedV6000Encrypted, ... }
  - public static Task<TreRepackResult> Apply(OpenSource.TreArchive ta, byte[] rewrittenIffBytes, bool createBackup)
  // Atomic File.Replace, TreRepackLock probe, V6000 refusal, TreBackupPath timestamped backup, parse-back sanity

From TheJawaToolboxDotNet/Saving/ClientReloadDispatcher.cs (Phase 8 — tiered reload, DTII routes to PendingNextSceneChange):
- public static class ClientReloadDispatcher
  - public enum ReloadOutcome { ReloadedTextures, ReloadedTerrain, PendingNextSceneChange, ReloadUnavailable, Failed }
  - public static ReloadOutcome Dispatch(string savedPath, string rootTypeId)
  // For (".iff", "DTII"): returns PendingNextSceneChange via ReloadAssetClassifier.Classify(...)
  // Game.IsRunning gate built in; AddMainLoopCall marshaling for binding calls

From UtinniCoreDotNet/Saving/ReloadAssetClassifier.cs (Phase 8 — already routes DTII; Plan 09-05 just adds defense-in-depth fact):
- public enum ReloadTier { Textures, Terrain, PendingNextSceneChange, Unknown }
- public static ReloadTier Classify(string extension, string rootTypeId)
  // (".iff", "DTII") → PendingNextSceneChange

From UtinniCoreDotNet/Saving/LooseOverridePath.cs (Phase 8 — root-containment helper, 14-Fact-tested):
- public static class LooseOverridePath
  - public static string Resolve(string clientRoot, string subDir, string logicalAssetPath)
  // Normalizes, rejects rooted/.., StartsWith verification, OrdinalIgnoreCase on Windows

From UtinniCoreDotNet/Formats/Tre/TreRecordIndexResolver.cs (Phase 8 — W-3 degraded fallback):
- public static class TreRecordIndexResolver
  - public static OpenSource ResolveOrUnknown(string treArchivePath, long archiveLocalOffset, string logicalPath)
  // Linear scan; returns OpenSource.TreArchive on match, OpenSource.Unknown.Instance on miss

From UtinniCoreDotNet/Formats/Iff/OpenSource.cs (Phase 8 — 4-case provenance):
- public abstract class OpenSource
- public sealed class OpenSource.LooseFile : OpenSource { public string Path; }
- public sealed class OpenSource.TreArchive : OpenSource { public string TreArchivePath; public int RecordIndex; public string LogicalPath; }
- public sealed class OpenSource.ClientMemory : OpenSource { /* Phase 8 — Plan 09-05 does NOT construct this case */ }
- public sealed class OpenSource.Unknown : OpenSource { public static readonly Unknown Instance; }

From FormIffEditor.cs lines 1416-1460 (OpenFromTreEntry analog Plan 09-05 mirrors for FormDatatableEditor):
- public void OpenFromTreEntry(byte[] payload, string resolvedArchivePath, string logicalPath, long archiveLocalOffset)
  // Reads bytes, IffReader.Read, MutableIffDocument.FromDocument, calls TreRecordIndexResolver.ResolveOrUnknown for OpenSource construction, LoadDocument

From FormIffEditor.cs lines 933-1019 (RefreshSaveMenuEnabledState — the provenance pattern-match Plan 09-05 ports):
- private void RefreshSaveMenuEnabledState()
  // switch on Source pattern-match: LooseFile → SaveInPlace+SaveLoose+SaveAs+SaveLivePatch-disabled+RepackTre-disabled; TreArchive → SaveInPlace-disabled+SaveLoose+SaveAs+SaveLivePatch-disabled+RepackTre; ClientMemory → ...; Unknown → only SaveAs enabled

From FormTreBrowser.cs lines 185-235 (the OnOpenInIffEditor analog Plan 09-05 mirrors for OnOpenInDatatableEditor):
- private void OnOpenInIffEditor(object sender, EventArgs e)
  // Off-UI-thread Task.Run (TrePayloadResolver.TryResolve) → BeginInvoke marshal → FindOrCreateIffEditor → editor.OpenFromTreEntry(...) → editor.Show + Activate
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship DatatableSaveTargets shim + DatatableReloadRoutingTests (defense in depth)</name>
  <files>../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/DatatableSaveTargets.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj, UtinniCoreDotNet.Tests/SavingTests/DatatableSaveTargetsTests.cs, UtinniCoreDotNet.Tests/SavingTests/DatatableReloadRoutingTests.cs</files>
  <read_first>
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/IffSaveTargets.cs (FULL FILE — the verbatim dispatcher Plan 09-05 composes on; particularly the 3 public methods SaveLooseOverride / SaveToPath / SaveInPlace and the SaveResult struct)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/TreRepackSaveTarget.cs (FULL FILE — the repack dispatcher; particularly the Apply method signature and the TreRepackResult enum including RefusedV6000Encrypted)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/ClientReloadDispatcher.cs (FULL FILE — the Dispatch method + the routing-table contract)
    - UtinniCoreDotNet/Saving/ReloadAssetClassifier.cs (FULL FILE — Classify(extension, rootTypeId) signature; the DTII case)
    - UtinniCoreDotNet.Tests/SavingTests/ClientReloadDispatcherTests.cs (FULL FILE — the routing-table [Theory] pattern Plan 09-05's DatatableReloadRoutingTests mirrors with explicit DTII facts)
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-PATTERNS.md § File Classification → DatatableSaveTargets.cs row + § High-Leverage Reuse Points #1 (IffSaveTargets) + #2 (TreRepackSaveTarget)
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-VALIDATION.md § Per-Task Verification Map row PROD-W1-DT(2d) for the save-target test coverage
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-RESEARCH.md § Open Question 5 (V6000 refusal — Plan 09-05 inherits; adds confirming fact)
    - UtinniCoreDotNet/Formats/Datatable/DataTableWriter.cs (Plan 09-01 — BuildMutableIff() public method the shim consumes; Serialize() public method for the bytes-only repack path)
    - UtinniCoreDotNet/Formats/Datatable/MutableDataTableDocument.cs (Plan 09-01 — the doc the shim wraps)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj (old-style explicit-compile — round-2 HIGH-A)
  </read_first>
  <behavior>
    - DatatableSaveTargets.cs:
      - `namespace TheJawaToolboxDotNet.Saving`; `public static class DatatableSaveTargets`.
      - Five `Task<...>` methods composing the existing Phase 8 dispatchers:
        ```
        public static Task<IffSaveTargets.SaveResult> SaveLooseOverride(MutableDataTableDocument dt, OpenSource source, string resolvedRoot, string looseOverrideSubDir)
        {
            MutableIffDocument mIff = new DataTableWriter(dt).BuildMutableIff();
            return IffSaveTargets.SaveLooseOverride(mIff, source, resolvedRoot, looseOverrideSubDir);
        }
        public static Task<IffSaveTargets.SaveResult> SaveToPath(MutableDataTableDocument dt, string targetPath)
        {
            MutableIffDocument mIff = new DataTableWriter(dt).BuildMutableIff();
            return IffSaveTargets.SaveToPath(mIff, targetPath);
        }
        public static Task<IffSaveTargets.SaveResult> SaveInPlace(MutableDataTableDocument dt, OpenSource source)
        {
            MutableIffDocument mIff = new DataTableWriter(dt).BuildMutableIff();
            return IffSaveTargets.SaveInPlace(mIff, source);
        }
        public static Task<TreRepackSaveTarget.TreRepackResult> RepackIntoSourceTre(MutableDataTableDocument dt, OpenSource.TreArchive ta, bool createBackup)
        {
            byte[] bytes = new DataTableWriter(dt).Serialize();
            return TreRepackSaveTarget.Apply(ta, bytes, createBackup);
        }
        ```
      - File length target < 100 lines (the four composition methods + XML headers + namespace).
    - csproj: 1 new `<Compile Include="Saving\DatatableSaveTargets.cs" />` entry.
    - DatatableSaveTargetsTests.cs (~6-8 [Fact]s):
      - `SaveToPath_RoundTripsByteExact_NoEdits` — load `BuildV1AllTypes()`, save via SaveToPath to a temp path, read back, assert bytes equal the original.
      - `SaveToPath_PreservesByteExactOnUntouchedCellsAfterOneEdit` — load fixture, edit cell (0,0), save via SaveToPath, parse the saved file, assert untouched cells re-emit identical bytes (SC4 at the save-shim layer).
      - `SaveLooseOverride_UsesLooseOverridePathResolve` — invoke with a known clientRoot + subDir + a TreArchive source with logical path "datatables/foo.tab"; assert SavedPath StartsWith (clientRoot + subDir + ...) — Phase 8 path-defense inherited.
      - `SaveInPlace_RequiresLooseFileSource_RejectsOthers` — assert SaveInPlace with `OpenSource.TreArchive` source returns SaveResult with Succeeded=false + ErrorMessage indicating provenance mismatch (or whatever Phase 8's IffSaveTargets actually does — confirm the contract via read of the actual implementation).
      - `RepackIntoSourceTre_V6000Archive_RefusedWithLooseOverrideRecommendation` — the WR-06 inheritance fact: construct a synthetic V6000 .tre archive in a temp dir; invoke RepackIntoSourceTre with a DTII payload; assert TreRepackResult == RefusedV6000Encrypted (or equivalent V6000 refusal enum value). Mirrors Phase 8 TreRepackByteDiffTests style.
      - `RepackIntoSourceTre_ValidArchive_SucceedsWithBackup` — non-V6000 synthetic archive + DTII payload + createBackup=true; assert Replaced+BackedUp outcome; assert backup file exists; assert the new archive parses cleanly and the DTII payload is inside.
    - DatatableReloadRoutingTests.cs (~2-3 [Fact]s):
      - `ReloadAssetClassifier_IffWithDtiiRootTypeId_ReturnsPendingNextSceneChange` — assert `ReloadAssetClassifier.Classify(".iff", "DTII") == ReloadTier.PendingNextSceneChange`. (This is already covered by Phase 8's ClientReloadDispatcherTests routing-table [Theory]; Plan 09-05 adds an explicit DTII-specific fact for Phase 9 traceability.)
      - `ClientReloadDispatcher_SavedPathDtii_NoClient_ReturnsReloadUnavailable` — verify the no-live-client gate still returns ReloadUnavailable for the DTII case. (Confirms the Plan 09-03 reload-button click is safe to wire even outside an injected SWG session.)
  </behavior>
  <action>
    Create `DatatableSaveTargets.cs` per the behavior block. 23-line MIT header + Allman + 4-space
    + `// ToDo` (no colon). XML `<remarks>` on each method cites the Phase 8 analog (`IffSaveTargets.
    SaveLooseOverride` etc.) and notes that Phase 9 just adds the typed-build composition; all
    path-defense + Flush(true) MEDIUM-9 barrier + atomic write + repack-orchestration come for
    free.

    Update `TheJawaToolboxDotNet.csproj`: 1 new `<Compile Include="Saving\DatatableSaveTargets.cs" />`
    entry, alphabetically within the existing `Saving\` block.

    Create both xUnit test files under `UtinniCoreDotNet.Tests/SavingTests/` (SDK-style auto-glob;
    no test-csproj edit needed). For DatatableSaveTargetsTests, use the Plan 09-01 fixture builder
    for typed setup; use Phase 8's existing TreFixtureBuilder + IffBuilder helpers for the .tre
    archive synthesis facts (the V6000 fixture builder exists in Phase 8's test infrastructure —
    grep `BuildV6000Archive` or equivalent to find it; if the helper name differs, use whatever
    Phase 8 ships and document).

    Cross-repo commit: `feat(09): ship DatatableSaveTargets composition shim + reload-routing
    defense-in-depth facts`.
  </action>
  <verify>
    <automated>powershell -Command "& 'D:/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe' '../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj' /p:Configuration=Debug /p:Platform=x86 /v:minimal" 2>&1 | tail -10  # build green; dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~DatatableSaveTargetsTests|FullyQualifiedName~DatatableReloadRoutingTests" 2>&1 | tail -15  # MUST exit 0 with >= 8 passing facts; wc -l "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/DatatableSaveTargets.cs"  # SHOULD be < 100 lines (excluding MIT header) — confirms thin shim posture; grep -c "Saving\\\\DatatableSaveTargets.cs" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"  # MUST return 1</automated>
  </verify>
  <done>DatatableSaveTargets.cs ships as < 100-line composition shim; csproj entry added; both test files exist with combined ≥ 8 passing facts including the V6000 refusal + the DTII reload-routing defense-in-depth fact; cross-repo paired commit landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire FormDatatableEditor Save▾ + reload-dispatch + OpenFromMutableIff; modify FormTreBrowser (D-10.2) + FormIffEditor (D-10.3) for the hand-offs</name>
  <files>../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs</files>
  <read_first>
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs lines 880-1019 (BuildSaveMenu + RefreshSaveMenuEnabledState — the verbatim provenance gate Plan 09-05 ports; the executor MUST compose this ON TOP of Plan 09-04's NeedsReview gate already in FormDatatableEditor.cs)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs lines 1416-1495 (OpenFromTreEntry + OnReloadClicked — analog port for FormDatatableEditor.OpenFromTreEntry + reload-dispatch wire)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs lines 138-248 (_miOpenInIffEditor + OnOpenInIffEditor + FindOrCreateIffEditor — Plan 09-05's `_miOpenInDatatableEditor` + `OnOpenInDatatableEditor` + `FindOrCreateDatatableEditor` mirror these verbatim with the extension/root-tag visibility predicate)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs (Plan 09-03 / Plan 09-04 — MODIFY target; specifically the Save▾ stub click handlers, RefreshSaveMenuEnabledState NeedsReview-only gate Plan 09-04 wired, reload-button OnReloadClicked Plan 09-03 wired)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs full file — find the menu section to add `_miSwitchToDatatableView`; planner can place it in the existing menu strip after the existing structural ops (V2 modders may add their own menu items elsewhere)
    - UtinniCoreDotNet/Formats/Iff/OpenSource.cs (Plan 09-05 uses pattern-match `Source is OpenSource.LooseFile lf` / `Source is OpenSource.TreArchive ta` / `Source is OpenSource.Unknown` for provenance gating)
    - UtinniCoreDotNet/Formats/Tre/TreRecordIndexResolver.cs (Plan 09-05's FormDatatableEditor.OpenFromTreEntry calls ResolveOrUnknown for the W-3 degraded path per Phase 8 contract)
    - UtinniCoreDotNet/Formats/Datatable/DataTableDocument.cs (Plan 09-01 — FromIff method; Plan 09-05's OpenFromMutableIff wraps it)
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-RESEARCH.md § Code Examples "Mirror the Phase 8 FormIffEditor.BuildSaveMenu pattern" + "Mirror the Phase 8 OpenFromTreEntry for FormDatatableEditor" sections (RESEARCH lines ~705-781 give the exact code shape)
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-UI-SPEC.md § States row "Save in progress" / "Save succeeded" / "Save failed" (the status-strip messages Plan 09-05 wires) + § Copywriting Save▾ menu disabled tooltips
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-PATTERNS.md § MODIFIED FormIffEditor (D-10.3) row + § MODIFIED FormTreBrowser (D-10.2) row
  </read_first>
  <behavior>
    - FormDatatableEditor.cs MODIFICATIONS:
      - **Save▾ click handlers** (replace Plan 09-03's NotImplementedException stubs):
        - `OnSaveInPlaceClick`: `var result = await DatatableSaveTargets.SaveInPlace(dtDocument.Mutable, this.Source);` → status strip green `Saved {displayName} (in place)` on success; red `<reason>. Your edits are kept in the editor — try another save target.` on failure (UI-SPEC § States Save-failed row). On success: invoke `ClientReloadDispatcher.Dispatch(result.SavedPath, "DTII")` (game-thread marshal built in); clear controller's net-applied-count via controller-internal `MarkSaved()` (NEW seam on controller — Plan 09-04 may need to add this; if missing from Plan 09-04, Plan 09-05 adds it as a small surface extension). Visual: clear ● glyph from title + clear lblDirty.
        - `OnSaveLooseOverrideClick`: resolve `clientRoot` via existing `Game.GetWorkingDirectory()` Phase-7 helper OR ini fallback; resolve `looseOverrideSubDir` from `[DatatableEditor] looseOverrideDir` ini key (or empty if unset); call `await DatatableSaveTargets.SaveLooseOverride(dtDocument.Mutable, this.Source, clientRoot, subDir)`. Status flow same as in-place. **On the first save in a session, if `looseOverrideDir` ini key is empty, surface a small one-time hint in status strip: `Saving to {resolvedSubDir}. Change the loose-override directory in [DatatableEditor] looseOverrideDir if needed.`** (Phase 8 Open Q2 inherits the user-picks-path-on-first-save posture).
        - `OnSaveAsClick`: use `SaveFileDialog` (OS-mediated; filter `Datatable files (*.tab)|*.tab|All files|*.*`); on user-accept, call `await DatatableSaveTargets.SaveToPath(dtDocument.Mutable, dlg.FileName)`. Status flow same. Save-As is ALWAYS enabled (Phase 8 escape-hatch posture) unless blocked by NeedsReview.
        - `OnPatchLiveClientClick`: NOT WIRED (CF-03 keeps Mode 3 DISABLED); the menu item stays disabled with the inherited Phase 8 tooltip. If somehow clicked (shouldn't be possible since disabled), no-op + status strip red `Live patch is disabled in this phase.`.
        - `OnRepackTreClick`: provenance-gated on `Source is OpenSource.TreArchive ta`; if not TreArchive, no-op (button should be disabled in this state anyway). On confirm via FormSaveConfirmDialog (Phase 8 inherited modal; locked Phase 8 copy for repack confirm), call `await DatatableSaveTargets.RepackIntoSourceTre(dtDocument.Mutable, ta, dlg.BackupRequested)`. Handle TreRepackResult: Replaced/BackedUpThenReplaced → status green; RefusedClientHoldsArchive_LooseOverrideRecommended → status red `Client holds the archive — try Save as loose override instead.`; RefusedV6000Encrypted → status red `This .tre archive is V6000-encrypted and cannot be repacked. Use Save as loose override.` (Phase 8 WR-06 inheritance).
      - **`RefreshSaveMenuEnabledState` EXTENSION** (compose ON TOP of Plan 09-04's NeedsReview-only stub):
        ```
        bool hasDoc = dtDocument != null;
        bool blockedByCascade = controller != null && controller.NeedsReviewCount > 0;
        string cascadeTooltip = blockedByCascade ? ("Resolve " + controller.NeedsReviewCount + " cell(s) that need review before saving.") : null;

        bool isLooseFile = Source is OpenSource.LooseFile;
        bool isTreArchive = Source is OpenSource.TreArchive;
        bool isClientMemory = Source is OpenSource.ClientMemory;   // never true in Phase 9 per CF-03
        bool isUnknown = Source is OpenSource.Unknown;
        bool clientUp = false;
        try { clientUp = Game.IsRunning; } catch { /* binding throws outside an injected client */ }

        if (miSaveInPlace != null) {
            miSaveInPlace.Enabled = hasDoc && isLooseFile && !blockedByCascade;
            miSaveInPlace.ToolTipText = blockedByCascade ? cascadeTooltip : (isLooseFile ? null : "Cannot save in place — file came from .tre or unknown source. Use Save as loose override or Save As.");
        }
        if (miSaveLooseOverride != null) {
            miSaveLooseOverride.Enabled = hasDoc && (isLooseFile || isTreArchive) && !blockedByCascade;
            miSaveLooseOverride.ToolTipText = blockedByCascade ? cascadeTooltip : ((isLooseFile || isTreArchive) ? null : "Cannot resolve archive record — use Save As to write to a chosen file.");
        }
        if (miSaveAs != null) {
            miSaveAs.Enabled = hasDoc && !blockedByCascade;   // ALWAYS enabled (escape hatch — round-2 MEDIUM 5)
            miSaveAs.ToolTipText = blockedByCascade ? cascadeTooltip : null;
        }
        if (miPatchLive != null) {
            miPatchLive.Enabled = false;   // CF-03 — disabled inherited
            miPatchLive.ToolTipText = "Live patch requires opening from client memory — not wired in this phase.";
        }
        if (miRepackTre != null) {
            miRepackTre.Enabled = hasDoc && isTreArchive && !blockedByCascade;
            miRepackTre.ToolTipText = blockedByCascade ? cascadeTooltip : (isTreArchive ? null : "Open from a packed .tre to repack the source archive.");
        }
        btnSave.Enabled = hasDoc && !blockedByCascade && (isLooseFile || isTreArchive);
        ```
      - **Public method `OpenFromTreEntry(byte[] payload, string resolvedArchivePath, string logicalPath, long archiveLocalOffset)`** per RESEARCH § "Mirror the Phase 8 OpenFromTreEntry" lines 770-781 verbatim, swapping the IFF wrap with a typed wrap: `var iffDoc = IffReader.Read(ms); var mIff = MutableIffDocument.FromDocument(iffDoc, payload); var dtDoc = DataTableDocument.FromIff(mIff); var src = TreRecordIndexResolver.ResolveOrUnknown(resolvedArchivePath, archiveLocalOffset, logicalPath); LoadDocument(dtDoc, src, Path.GetFileName(logicalPath));`.
      - **Public method `OpenFromMutableIff(MutableIffDocument mutIff, OpenSource source, string displayName)`** (NEW for D-10.3): `var dtDoc = DataTableDocument.FromIff(mutIff); LoadDocument(dtDoc, source, displayName);`. The IFF Editor passes its existing mutIff + Source directly without re-parsing bytes — efficient hand-off.
      - **OnReloadClicked** (extend Plan 09-03's CF-05-locked stub): if `lastSavedPath != null && Game.IsRunning` (try/catch the binding access), invoke `ClientReloadDispatcher.Dispatch(lastSavedPath, "DTII")` — the dispatch is for the routing-table audit trail; the user-facing status copy STAYS the locked CF-05 wording (the dispatch returns PendingNextSceneChange anyway). If `lastSavedPath == null` (never saved this session), keep Plan 09-03's status copy unchanged (just the candor message).
      - **Save state machine for `saveInFlight` flag** (PATTERNS § High-Leverage Reuse Points #4 + Phase 8 MEDIUM-9 stale-bytes barrier): during any Save▾ click, set `saveInFlight = true` → disable Save▾ + Reload button → after the await completes (success or failure), set `saveInFlight = false` → call `RefreshSaveMenuEnabledState` + `RefreshReloadButtonState`.
    - FormTreBrowser.cs MODIFICATIONS (D-10.2 — additive per PATTERNS § MODIFIED FormTreBrowser):
      - Add `_miOpenInDatatableEditor` `ToolStripMenuItem` to the existing context-menu in the same code region where `_miOpenInIffEditor` lives. Caption: `Open in Datatable Editor` (UI-SPEC § Copywriting D-10.2 row LOCKED).
      - Visibility predicate evaluated on context-menu Opening event: `extension == ".tab" OR rootTag == "DTII"`. Root-tag check uses a cheap byte-scan via a new framework helper `IffReader.TryPeekRootTypeId(Stream stream, out string typeId)` if added (RESEARCH Assumption A7); ELSE fall back to extension-only with a TODO XML comment for the helper. **Recommend: ship extension-only for V1** (the user can still right-click → Open in IFF Editor → Switch to typed datatable view for the corner case of `.iff` containing DTII; the cost of adding `IffReader.TryPeekRootTypeId` is small but creates another framework-side surface to test).
      - Click handler `OnOpenInDatatableEditor` mirrors `OnOpenInIffEditor` (FormTreBrowser.cs lines 185-235) VERBATIM with:
        - swap `FindOrCreateIffEditor` → `FindOrCreateDatatableEditor`
        - swap `editor.OpenFromTreEntry(...)` call stays the same signature (FormDatatableEditor's OpenFromTreEntry mirrors FormIffEditor's).
      - New helper `FindOrCreateDatatableEditor()` mirrors `FindOrCreateIffEditor()`: walks the plugin's GetForms() list to find the existing FormDatatableEditor singleton; returns null if not registered (which would only happen if Plan 09-03's Plugin.cs entry failed at MEF load).
    - FormIffEditor.cs MODIFICATIONS (D-10.3 — additive per PATTERNS § MODIFIED FormIffEditor):
      - Add `_miSwitchToDatatableView` `ToolStripMenuItem` to the existing menu / context menu (placement: planner picks — recommend an additional context-menu entry on the IFF chunk tree, OR a dedicated `View` menu addition; UI-SPEC § Copywriting D-10.3 row only specifies the menu item text, not its placement). Caption: `Switch to typed datatable view` LOCKED.
      - Visibility predicate (Opening event OR set after LoadDocument): `Visible = (document?.Root?.TypeId == "DTII")`. Plan 09-05 inserts the visibility logic at the existing handler that fires after LoadDocument (e.g. inside `UpdateDirtyVisuals` or a new `RefreshSwitchMenuVisibility` method called from `LoadDocument`).
      - Click handler `OnSwitchToDatatableViewClick`: `var editor = FindOrCreateDatatableEditor(); if (editor == null) { lblStatus.Text = "Datatable Editor is unavailable."; return; } editor.OpenFromMutableIff(this.mutableIffDocument, this.Source, this.displayName); editor.Show(); editor.Activate();`. Manual hand-off — does NOT close this IFF Editor (the user can keep both windows open and switch back).
      - `FindOrCreateDatatableEditor()` helper mirrors the FormTreBrowser version (planner can consider extracting it to a shared helper class to avoid duplication, but per PATTERNS § Binary-Compat Audit § Modified files, the existing `FindOrCreateIffEditor` pattern in FormTreBrowser was duplicated naturally because both forms talk to the plugin's GetForms list; same posture is acceptable here. V2 refactor candidate).
  </behavior>
  <action>
    Modify FormDatatableEditor.cs per the behavior block:
    - Replace Save▾ stub click handlers (5 handlers) with the actual dispatch logic + status-strip flow.
    - EXTEND RefreshSaveMenuEnabledState by composing the provenance gate ON TOP of Plan 09-04's
      NeedsReview gate. The merge with Plan 09-04's edit: Plan 09-04 sets the NeedsReview-only
      version; Plan 09-05 REWRITES the entire method body with the composed gate. The cross-AI
      review will confirm the final method body matches the inline code in this behavior block.
    - Add public OpenFromTreEntry method (mirrors FormIffEditor).
    - Add public OpenFromMutableIff method (NEW for D-10.3).
    - Extend OnReloadClicked with the optional ClientReloadDispatcher.Dispatch call.
    - Add saveInFlight flag + Reload-button-disabled-during-save MEDIUM-9 barrier.

    Modify FormTreBrowser.cs additively:
    - Add `_miOpenInDatatableEditor` ToolStripMenuItem (Designer or programmatic in the existing
      context-menu builder).
    - Add OnOpenInDatatableEditor click handler mirroring OnOpenInIffEditor verbatim.
    - Add FindOrCreateDatatableEditor helper mirroring FindOrCreateIffEditor.
    - The visibility predicate runs on context-menu Opening event; extension-only check ships V1.

    Modify FormIffEditor.cs additively:
    - Add `_miSwitchToDatatableView` ToolStripMenuItem to the menu strip.
    - Add visibility-refresh logic at LoadDocument or in a new RefreshSwitchMenuVisibility method.
    - Add OnSwitchToDatatableViewClick handler.
    - Add FindOrCreateDatatableEditor helper (or extract to a shared helper class — planner
      discretion; if extracted, document the new shared file in files_modified).

    All three file modifications are ADDITIVE per Binary-compat audit § Modified files — no
    existing public signature changes (LoadDocument, OpenFromTreEntry on FormIffEditor stay
    unchanged).

    No new tests in this task — the save-target unit tests live in Task 1 (framework-side via
    DatatableSaveTargetsTests). The hand-off integration is exercised by the Plan 09-07
    Tier-4 live-SWG smoke. The Plan 09-03 hide-not-dispose regression guard already protects
    against the second-open singleton crash.

    Cross-repo paired commit: `feat(09): wire FormDatatableEditor Save▾ to DatatableSaveTargets +
    add TRE-Browser D-10.2 + IFF-Editor D-10.3 hand-offs`.
  </action>
  <verify>
    <automated>powershell -Command "& 'D:/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe' '../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj' /p:Configuration=Debug /p:Platform=x86 /v:minimal" 2>&1 | tail -10  # build green; grep -c "DatatableSaveTargets" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs"  # MUST return >= 4 (one per save target wire: SaveInPlace, SaveLooseOverride, SaveToPath, RepackIntoSourceTre); grep -c "FindOrCreateDatatableEditor" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs"  # MUST return >= 1 per file; grep -c "OpenFromMutableIff" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs"  # MUST return >= 1 (new public method); grep -c "OpenFromTreEntry" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs"  # MUST return >= 1 (new public method); grep -c "Switch to typed datatable view" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs"  # MUST return >= 1 (locked menu item text); grep -c "Open in Datatable Editor" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs"  # MUST return >= 1 (locked menu item text); dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~Datatable" 2>&1 | tail -10  # full Datatable subsuite green (no regression)</automated>
  </verify>
  <done>FormDatatableEditor has 5 working Save▾ click handlers + extended RefreshSaveMenuEnabledState (provenance + NeedsReview gates composed) + public OpenFromTreEntry + public OpenFromMutableIff + saveInFlight barrier + reload-dispatch wire; FormTreBrowser has the D-10.2 hand-off; FormIffEditor has the D-10.3 hand-off; TJT MSBuild Debug|x86 green; full Datatable test subsuite green; the locked menu item texts appear verbatim per UI-SPEC § Copywriting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → process | OpenFileDialog (in Plan 09-03 + here for Save As) and TRE-Browser hand-off (Phase 8 inherited) — both go through Phase 8's hardened path-defense (LooseOverridePath.Resolve + TreRecordIndexResolver.ResolveOrUnknown + the IffReader 64 MB chunk cap inherited by DataTableDocument.FromIff per Plan 09-01) |
| process → disk (save) | All saves go through Phase 8's IffSaveTargets or TreRepackSaveTarget — Flush(true) MEDIUM-9 barrier + atomic write + repack lock + V6000 refusal all inherited verbatim |
| process → live client (reload) | ClientReloadDispatcher.Dispatch routes DTII to PendingNextSceneChange — no fresh binding call from Plan 09-05 (CF-05 lock); Game.IsRunning gate via try/catch per Phase 8 pattern |
| singleton-form re-open from hand-offs | Plan 09-03's hide-not-dispose intercept means TRE-Browser / IFF-Editor hand-offs that re-open FormDatatableEditor work correctly (the singleton stays alive between opens) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-20 | Tampering (path traversal in Save As / loose override) | DatatableSaveTargets composition | mitigate | Inherits Phase 8 LooseOverridePath.Resolve verbatim (14-Fact test); SaveAs path is OS-mediated via SaveFileDialog (the user picks). No new path-defense surface. |
| T-09-21 | Tampering (stale-bytes reload race) | FormDatatableEditor.OnReloadClicked while save in flight | mitigate | saveInFlight flag disables Reload button during save Task; Flush(true) barrier inherited from Phase 8 IffSaveTargets. MEDIUM-9 inheritance. |
| T-09-22 | Tampering (V6000 archive corruption via repack) | DatatableSaveTargets.RepackIntoSourceTre | mitigate | Phase 8 TreRepackSaveTarget.Apply refuses V6000 archives (WR-06 fix); Plan 09-05 inherits + adds confirming xUnit fact in DatatableSaveTargetsTests. The status-strip surfaces the locked refusal copy with the loose-override recommendation. |
| T-09-23 | Spoofing (TRE-Browser hand-off opens wrong file) | OnOpenInDatatableEditor → OpenFromTreEntry | mitigate | TrePayloadResolver.TryResolve + TreRecordIndexResolver.ResolveOrUnknown both inherited from Phase 8 W-3 contract; if resolution fails, OpenSource degrades to Unknown.Instance and the four provenance-dependent save modes disable per the RefreshSaveMenuEnabledState composed gate. |
| T-09-24 | Tampering (IFF Editor D-10.3 hand-off hands wrong bytes) | OnSwitchToDatatableViewClick → OpenFromMutableIff | accept | The IFF Editor's `this.mutableIffDocument` is the in-memory DOM the user has been editing; if it contains malformed DTII (which shouldn't be possible since the user opened it as DTII), DataTableDocument.FromIff will throw DataTableParseException (Plan 09-01 T-09-01 cell-count cap + missing-chunks reject); status strip surfaces the failure red. |
| T-09-25 | Information disclosure (loose-override directory exposes file system path) | SaveLooseOverride status copy | accept | The status copy mentions the resolved subDir — the user already knows their own client filesystem layout. No PII / no secrets. |
| T-09-26 | DoS (save in flight blocks UI thread) | Save Task await on UI thread | mitigate | IffSaveTargets.SaveX methods all run on background Task per Phase 8 D-05.1/2 — the await is non-blocking; UI stays responsive. saveInFlight flag prevents concurrent save attempts. |
| T-09-SC | Tampering | npm/pip/cargo installs | N/A | Plan 09-05 adds ZERO new external packages. |

**ASVS controls applied:**
- V5 Input Validation — `Source` pattern-match guards the provenance-gated save modes (cannot SaveInPlace from a TreArchive source; cannot RepackTre from a LooseFile source); SaveFileDialog filter restricts user-chosen path to .tab; reload-dispatch routing-table covers DTII case explicitly.
- V12 File and Resources — all writes go through Phase 8's hardened helpers (LooseOverridePath.Resolve, TreRepackLock probe, TreBackupPath timestamped backup, atomic File.Replace); zero new file-I/O surface introduced.
</threat_model>

<verification>
Plan-level acceptance:

1. `dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~DatatableSaveTargetsTests|FullyQualifiedName~DatatableReloadRoutingTests"` exits 0 with ≥ 8 facts; V6000 refusal fact explicit; DTII routing fact explicit.
2. Both repos MSBuild Debug|x86 + Release|x86 green.
3. `grep -c "DatatableSaveTargets" ../UtinniPlugins/The\ Jawa\ Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs` returns ≥ 4 (one per save-target wire).
4. `grep -c "ClientReloadDispatcher.Dispatch" ../UtinniPlugins/The\ Jawa\ Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs` returns ≥ 1 (reload routing wired in addition to the CF-05-locked UI surface).
5. `grep -c "Open in Datatable Editor" ../UtinniPlugins/The\ Jawa\ Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs` returns ≥ 1 (D-10.2 menu item LOCKED text).
6. `grep -c "Switch to typed datatable view" ../UtinniPlugins/The\ Jawa\ Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs` returns ≥ 1 (D-10.3 menu item LOCKED text).
7. Full UtinniCoreDotNet.Tests + Utinni.Cli.Tests + PreservationAudit suites stay green (no regression of Plans 09-01..04 baseline).
8. RefreshSaveMenuEnabledState composes both gates: provenance from Plan 09-05 + NeedsReview from Plan 09-04 — visual confirmation that all 5 menu items + the top-level button respect BOTH gates.
</verification>

<success_criteria>
- All 4 V1 save modes (in-place / loose override / Save As / repack) dispatch correctly via the Phase 8 reused helpers; the V1-disabled mode 3 stays disabled with the inherited tooltip.
- The three D-10 entry points are wired: file picker (Plan 09-03), TRE Browser hand-off (Plan 09-05 D-10.2), IFF Editor hand-off (Plan 09-05 D-10.3).
- RefreshSaveMenuEnabledState composes provenance + NeedsReview gates correctly — every Save▾ menu item respects BOTH gates per UI-SPEC R-04.
- Save-As escape-hatch posture preserved per Phase 8 round-2 MEDIUM 5 (Unknown provenance leaves only Save As enabled).
- V6000 archive repack refusal inherited from Phase 8 WR-06 with a confirming xUnit fact for the DTII payload case.
- Reload-button dispatches via ClientReloadDispatcher to the correct routing tier (PendingNextSceneChange) without violating the CF-05 locked user-facing copy.
- Plan 09-06 can ship CSV import + Find/Replace + sort without touching any save-target or entry-point code (clean separation of concerns).
</success_criteria>

<output>
Create `.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-05-SUMMARY.md` when done. Record:
- DatatableSaveTargets.cs line count (confirm < 100 lines — the thin shim posture).
- Save▾ click handler dispatch table (5 handlers × source provenance gate × NeedsReview gate matrix).
- TRE-Browser visibility-predicate decision (extension-only for V1, or extension + root-tag scan).
- IFF-Editor menu placement (which menu strip, planner choice; for V2 reviewer reference).
- Any merge-conflict resolution between Plan 09-04 + Plan 09-05's RefreshSaveMenuEnabledState
  edits — confirm the final method body matches the composed-gate inline code from this plan's
  behavior block.
- Cross-repo paired commit hashes (one per task) for the cross-repo edit chain.
- Confirmation that the V6000 refusal + DTII routing facts pass green (defense-in-depth for SC4).
</output>

## Revision History

- 2026-05-29 (iter 1): wave bumped 3 -> 4; depends_on extended to [09-03, 09-04] per checker WARNING #5 Option B (eliminate FormDatatableEditor.cs merge risk between 09-04 and 09-05).
