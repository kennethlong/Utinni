---
phase: 09-tjt-subpanel-datatable-editor-tab
plan: 07
type: execute
wave: 6
depends_on: ["09-04", "09-05", "09-06"]
files_modified:
  - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-07-SMOKE-LOG.md
autonomous: false
requirements: [PROD-W1-DT]
assumes:
  - "Plans 09-01..06 are complete and CI-green: framework primitives shipped; CLI roundtrip-tab gate green; FormDatatableEditor + per-type widgets shipped; T4 structural ops + cascade + needs-review save block wired; entry-point hand-offs from TRE Browser + IFF Editor wired; CSV import/export + Find/Replace + sort wired."
  - "Phase 8 smoke precedent (smoke=automation-augmented per Plan 08-07 Task 4 outcome) applies verbatim: the on-disk save contracts + parse/serialize round-trip + V6000 refusal + repack atomic-replace + reload-routing are AUTOMATED by Plans 09-01..06 (combined ~120+ [Fact]s across UtinniCoreDotNet.Tests + Utinni.Cli.Tests). The Tier-4 maintainer-driven live-SWG observation residual is documented but NOT hard-blocking for V1 sign-off (Open Q residuals deferred-but-acceptable per Phase 8 P05/P06/P07 precedent)."
  - "The maintainer's live-SWG observation is the only way to verify (a) the form opens against an injected SWG client, (b) the user's manual TJT chat-command scene change re-resolves an edited .tab, (c) the three entry points (file picker + TRE Browser + IFF Editor hand-offs) work under live MEF, (d) the singleton-form pattern survives multiple open/close cycles in the real editor host. None of these can be automated without a mock SWG client (deferred to V2 per REQ-V2-tier-3-mock-d3d9)."
  - "If the maintainer's live observation reveals a defect, Plan 09-07's outcome is the deferred-acceptable disposition with the defect documented in 09-07-SMOKE-LOG.md for follow-up by a future post-V1 phase OR a Phase 9 gap-closure plan (`/gsd:plan-phase --gaps`). The decision to ship V1 with the deferred residual mirrors Phase 8 Plan 07's Open Q1/Q5 disposition."
must_haves:
  truths:
    - "All Phase 9 automation gates (Plans 09-01..06) are green on CI before the smoke runs: full UtinniCoreDotNet.Tests + Utinni.Cli.Tests + PreservationAudit suites pass on both Debug|x86 + Release|x86 across both repos."
    - "The maintainer (or executor-driven automation harness, whichever is achievable per Phase 8 precedent) observes the editor opening against live SWG, opening a `.tab` via each of the three entry points, editing a cell + adding a row + changing a column type with cascade resolution, saving via each enabled mode, triggering a TJT chat-command scene change and confirming the edit propagates."
    - "Open Question 1 (CRC reference values — was the port port-exact?), Open Question 2 (PackedObjVars/BitVector validator depth), Open Question 5 (V6000 archive repack refusal under live client), and any other RESEARCH-flagged Open Questions are either RESOLVED via the smoke OR explicitly deferred-but-acceptable with documented rationale (Phase 8 precedent — these mirror Plan 08-07 Open Q1/Q5 deferred status)."
    - "Phase 9 SUMMARY documents the smoke outcome with the same vocabulary Phase 8 Plan 07 used: 'smoke=automation-augmented; live ACK deferred-but-acceptable for V1' OR 'smoke=full-live-OK with all entry points + saves + CSV + reload verified against an injected SWG session'."
    - "If the smoke reveals a defect that affects Phase 9's PROD-W1-DT acceptance (any of the 4 success criteria), the defect is documented in 09-07-SMOKE-LOG.md AND a decision is made: (a) immediate inline fix if scoped (a Plan 09-08 gap-closure), (b) deferred-acceptable for V1 with rationale, (c) ship-blocker requiring re-plan. The decision is recorded with maintainer sign-off."
  artifacts:
    - path: ".planning/phases/09-tjt-subpanel-datatable-editor-tab/09-07-SMOKE-LOG.md"
      provides: "Maintainer-signed smoke observation log: per-step pass/fail, defects found (if any), Open Question dispositions, V1 sign-off recommendation"
      contains: "smoke=automation-augmented"
  key_links:
    - from: "Plans 09-01..06 automated test surface"
      to: "Plan 09-07 smoke=automation-augmented outcome"
      via: "Phase 8 Plan 07 precedent (smoke approved on automation + augmented by live observation; live ACK deferred-but-acceptable)"
      pattern: "automation-augmented"
    - from: "09-07-SMOKE-LOG.md"
      to: "phase-level Phase 9 VERIFICATION.md (created post-smoke if needed)"
      via: "smoke outcome feeds the consolidated phase sign-off"
      pattern: "VERIFICATION"
---

<objective>
Phase 9's **Tier-4 maintainer-driven live-SWG smoke**. This is the only plan in the phase that
requires a human-action checkpoint (per Phase 8 Plan 07 precedent + memory `feedback_max_harness`
which mandates a harness-first approach when one is achievable — for Phase 9 the bulk of the
testable surface IS automated, with only the live-client residual remaining).

Per Phase 8 Plan 08-07 maintainer direction ("automate this against test assets, this should be
automated if possible"), the on-disk save contracts + parse/serialize round-trip + V6000 refusal
+ repack atomic-replace + reload-routing are ALL covered by Plans 09-01..06's automation
(combined ~120+ [Fact]s). What remains is the live-SWG observation: the form opening against an
injected client, the three entry points working under live MEF, the singleton hide-not-dispose
surviving multiple open/close cycles, and the CF-05 tier-(b) reload-on-next-scene-change
manifesting correctly when the user runs a TJT chat-command scene change after saving.

**Outcome framing (per orchestrator letter L + Plan 08-07 precedent):** smoke is approved by
maintainer on automation OR live observation OR option-3 automation-only — Open Q residuals
deferred-but-acceptable, mirroring Phase 8 P05/P06/P07 sign-off precedent. The plan body does
NOT pretend live ACK is hard-blocking — it documents the smoke surface, the maintainer-driven
observation steps, the disposition options, and feeds the result into the consolidated Phase 9
SUMMARY for V1 sign-off.

**Why this decomposition (Wave 6, single-task checkpoint):** The smoke is the natural integration
gate after all 6 implementation plans land. It depends on all of them (full feature surface
must be present); it requires a human at a live SWG injection session; it can produce one of
three outcomes (full pass / partial-with-deferred-residual / blocker requiring re-plan). The
plan is intentionally small (1 checkpoint task) — the work is in the maintainer's observation,
not in code authoring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-CONTEXT.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-RESEARCH.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-PATTERNS.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-VALIDATION.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-UI-SPEC.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-01-SUMMARY.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-02-SUMMARY.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-03-SUMMARY.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-04-SUMMARY.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-05-SUMMARY.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-06-SUMMARY.md

# Phase 8 smoke precedent — Plans 08-05/06/07 SUMMARYs document the "automation-augmented" outcome shape Plan 09-07 mirrors
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-05-SUMMARY.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-06-SUMMARY.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-07-SUMMARY.md

# RESEARCH Open Questions Plan 09-07 either resolves OR defers
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Maintainer Tier-4 live-SWG smoke for Phase 9 Datatable Editor (smoke=automation-augmented; Phase 8 precedent)</name>
  <what-built>
    Plans 09-01..06 have shipped the complete Phase 9 Datatable Editor feature surface:
    - Framework primitives: `UtinniCoreDotNet/Formats/Datatable/` with typed reader, mutable
      hybrid-DOM, writer (composes on Phase 8 IffWriter); xUnit ~60 facts in Formats/Datatable.
    - CLI gate: `utinni-cli roundtrip-tab` with golden suite covering the byte-exact-on-untouched
      structural SC4 enforcer; xUnit ~10 facts.
    - TJT host + grid: `FormDatatableEditor` with `ThemedDataGridView`, per-type cell widgets,
      singleton hide-not-dispose pattern from commit 1, Plugin.cs registration.
    - T4 controller: `DatatableEditController` with 11 commands including the D-04 type-change
      cascade that flags NeedsReview cells + R-04 save-block on every Save▾ menu item.
    - Save targets: `DatatableSaveTargets` composition shim dispatching to Phase 8's `IffSaveTargets`
      (modes 1/2) + `TreRepackSaveTarget` (mode 4); mode 3 disabled inherited; V6000 refusal
      inherited.
    - Entry points: TRE Browser "Open in Datatable Editor" (D-10.2); IFF Editor "Switch to typed
      datatable view" (D-10.3); both manual hand-off per UI-SPEC.
    - Bulk-edit: CSV/TSV delta-import preserving byte-exact-on-untouched + Find/Replace + column-
      click view-only sort + DT_Comment frozen-row toggle.

    Combined automated coverage: ~120+ xUnit [Fact]s across UtinniCoreDotNet.Tests + Utinni.Cli.Tests
    + PreservationAudit; full suites green on Debug|x86 + Release|x86 across both repos.

    What remains is the maintainer-driven live-SWG observation that confirms the feature works
    under real MEF + injected SWG conditions — the part automation cannot reach without a mock
    SWG client (V2 — REQ-V2-tier-3-mock-d3d9).
  </what-built>
  <how-to-verify>
    **Smoke disposition (per Phase 8 precedent — choose ONE):**
    - **Option A (full live observation):** Maintainer runs the full smoke steps below against an
      injected SWG session; records pass/fail in 09-07-SMOKE-LOG.md.
    - **Option B (automation-augmented):** Maintainer reviews the automation surface (Plans 09-01..06
      passing facts) + executes a subset of the smoke steps that don't require deep in-game
      observation; defers the live-ACK residual with the same vocabulary Plan 08-07 used.
    - **Option C (automation-only):** Maintainer accepts the automation surface alone as
      sufficient for V1 sign-off (Phase 8 Plan 06 precedent — "smoke=automation-only" disposition
      acceptable when the risk surface is purely on-disk + the automation covers it
      structurally); defers the full smoke to a later observation pass.

    **Smoke steps (for Options A + B — perform the steps you can; document the ones you skip):**

    1. **Build + launch:** Build both repos (Utinni + UtinniPlugins) Debug|x86 via VS 2026 MSBuild;
       launch the Utinni editor host alongside an injected SWGEmu / Restoration client per the
       standard injection workflow (memory `project_swg_context_routing` + `project_scene_change_via_tjt`
       for the inject-and-load loop). Confirm no JIT debugger pop on startup; confirm TJT loads
       and the Datatable Editor menu item is registered in the GetForms list (Plan 09-03 Plugin.cs
       try/catch block).

    2. **Entry point 1 — File picker (D-10.1):** Open the Datatable Editor via the standard TJT
       host menu. Click `Open…`. Pick a real `.tab` file (extract one via Phase 7 TRE Browser to
       a temp path if needed). Confirm: the grid populates; per-type widgets render correctly
       (DT_Bool as checkbox, DT_Enum as dropdown, DT_HashString shows the floating hash preview
       in Consolas 9pt when a cell is edited); the reload-status badge reads the LOCKED CF-05
       copy `Reloads on next scene change.`.

    3. **Entry point 2 — TRE Browser hand-off (D-10.2):** Open the TRE Browser; right-click a
       `.tab` entry; click `Open in Datatable Editor`. Confirm: the Datatable Editor opens (or
       activates the already-open singleton); the grid populates with the entry's content; the
       provenance is set to `OpenSource.TreArchive` (visible indirectly via which Save▾ menu
       items are enabled: Save loose override + Save As + Repack should be enabled; Save in place
       should be disabled because Source != LooseFile; Patch live client disabled inherited).

    4. **Entry point 3 — IFF Editor hand-off (D-10.3):** Open the IFF Editor; load a `.tab`
       file (or a DTII-rooted IFF file); confirm the new menu item `Switch to typed datatable
       view` appears (it should — the visibility predicate is `Root.TypeId == "DTII"`). Click it.
       Confirm: FormDatatableEditor opens with the same document; the IFF Editor stays open
       (manual hand-off, NOT auto-close).

    5. **Edit + structural ops:** With a `.tab` loaded, edit a cell (commit-on-CellEndEdit, NOT
       per-keystroke); confirm the row-header ● glyph appears + window title gains leading ● +
       lblDirty reads `Unsaved changes` at Colors.Secondary(). Ctrl+Z → undo; Ctrl+Y → redo —
       confirm both work. Click `Add row` → confirm a new row appended with ＋ row-header glyph.
       Click `Add column…` → FormAddColumnDialog opens; pick name + type; click `Add column`;
       confirm the new column appears.

    6. **Type-change cascade (D-04 — high-leverage test):** Right-click a column header → pick
       `Change column type`; select a type that will cause some cells to fail mangling (e.g.
       DT_String → DT_Int on a column containing non-integer strings). Confirm:
       FormTypeChangeCascadeDialog opens; the affected cells are listed in the embedded grid; the
       banner reads `Save is disabled until every cell is resolved.` at Colors.Secondary();
       confirm Save▾ button face AND each menu item are disabled with the tooltip `Resolve N
       cell(s) that need review before saving.` (UI-SPEC R-04 — verify ALL menu items, not just
       the top-level button). Click `Revert type change` → the cascade reverts; save items
       re-enable.

    7. **Column reorder safety-net (D-02):** Drag a column header to reorder. Confirm:
       FormSaveConfirmDialog opens with the LOCKED D-02 body `This may break runtime consumers
       that read columns by index. Proceed?` + Color.Red emphasis + Proceed/Cancel buttons +
       `Don't ask again this session` checkbox. Tick the checkbox + Proceed. Drag another column
       — confirm the modal does NOT re-appear (session-suppress works).

    8. **Save modes (Plan 09-05):** With a clean (or dirty) `.tab` loaded from a `.tre` archive
       (Step 3 provenance), exercise:
       - `Save loose override` — confirm green status `Saved <name> (loose override)`; check the
         file appears under the resolved loose-override directory.
       - `Save As…` — pick a temp path; confirm green status `Saved <name> (save as)`; check the
         file exists at the picked path.
       - `Repack into source .tre…` — FormSaveConfirmDialog confirms; on Proceed, status reads
         `Saved <name> (repack)` or the V6000 refusal copy if the archive is V6000.
       - `Save in place` — disabled (Source is TreArchive, not LooseFile); confirm tooltip
         explains.

    9. **TJT chat-command scene change reload (CF-05 / PROD-W1-DT(3)):** With an edit saved via
       Step 8, open the TJT chat-command panel and trigger a scene change (e.g. `loadScene Naboo`
       per memory `project_scene_change_via_tjt`). After the scene loads, confirm: the edited
       datatable value is visible in-game (e.g. modified weapon damage / NPC stat / whatever the
       fixture covered). NOTE: per memory `project_tjt_scene_change_naked_baseline.md`, the user
       will land naked — this is baseline, NOT a regression.

    10. **CSV import (D-08 byte-exact-on-untouched):** Click `Export CSV…`; pick a temp path;
        confirm the file is created. Open in Excel or a text editor; confirm header row +
        optional `#`-prefixed type row + data rows; confirm UTF-8 BOM. Modify ONE cell in the
        CSV in place + save the CSV. Back in the editor, click `Import CSV…`; pick the modified
        file. Confirm: FormCsvImportPreviewDialog opens; the locked D-08 wording shows e.g.
        `1 cell will change. N cells will stay as original bytes. 0 cells in the CSV would be
        type-invalid and will be skipped.`; click `Import`. Confirm the single cell changes; the
        other cells stay clean (no ● glyph). Save via any mode → confirm bytes equal what they
        would be if you'd edited the single cell directly.

    11. **Find/Replace (D-07):** Press Ctrl+F → Find pane slides in below toolbar. Type a
        query → confirm matches highlight (Colors.Secondary at 40% opacity per UI-SPEC);
        `lblFindCount` reads `{i} / {n}`. F3 → next match; Shift+F3 → previous. Press Ctrl+H →
        Replace pane extends. Type a replacement → click `Replace`; confirm one replacement
        applied (undoable via Ctrl+Z). Try `Replace all` with a type-invalid value (e.g. "foo"
        in a DT_Int column) → confirm status strip surfaces the red validation copy AND no
        invalid replacements were applied (the valid ones were).

    12. **Column-click sort (D-09 view-only):** Click any column header → grid sorts ascending +
        ▲ glyph; click again → descending. Hover the glyph → confirm tooltip reads the LOCKED
        copy `View order only — save serializes physical row order.`. Save via any mode →
        confirm the on-disk row order is the ORIGINAL physical order, NOT the sorted view order
        (open the saved file in the editor again or via `utinni-cli roundtrip-tab` to verify).

    13. **Singleton-form hide-not-dispose (RESEARCH § Pitfall 5):** Close the FormDatatableEditor
        via the X button. Re-open from any of the three entry points (file picker, TRE Browser,
        IFF Editor). Confirm: NO `ObjectDisposedException` dialog; the form re-opens with empty
        state OR the previously-loaded document (depending on whether the singleton's state is
        cleared on close — planner discretion in Plan 09-03; either is acceptable).

    **Smoke pass conditions:**
    - Option A pass: Steps 1-13 ALL pass (or document defects per step in 09-07-SMOKE-LOG.md).
    - Option B pass: Steps 1-3 + 5 + 6 + 8 + 13 pass (the minimum smoke surface for V1 sign-off);
      Steps 4, 7, 9, 10, 11, 12 deferred to a later observation pass with the deferred-acceptable
      vocabulary.
    - Option C pass: maintainer accepts Plans 09-01..06 automation surface alone (CI green +
      ~120 [Fact]s) as sufficient for V1; documents the live-ACK deferral in 09-07-SMOKE-LOG.md
      with the citation to Plan 08-06 Task 5 precedent ("smoke=automation-only" — approved by
      maintainer "approved, dig in" 2026-05-28).

    **Smoke fail conditions:**
    - Critical defect in any of Steps 1-3 (the form doesn't load at all): BLOCKING. Likely
      indicates a Plan 09-03 Plugin.cs or csproj regression. Requires a Plan 09-08 inline fix.
    - Critical defect in Step 6 (cascade dialog doesn't surface OR Save▾ items don't disable):
      BLOCKING. Indicates Plan 09-04 NeedsReview gate regression. Requires a Plan 09-08 fix.
    - Critical defect in Step 8 (saves don't write or write corrupted bytes): BLOCKING. Indicates
      a Plan 09-05 dispatch regression OR a Plan 09-01 writer bug. Requires investigation +
      gap-closure plan.
    - Any other defect: deferred-acceptable per Phase 8 precedent UNLESS it affects SC4 (byte-
      exact correctness) — those are always blocking.

    **Record the outcome in `.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-07-SMOKE-LOG.md`** with:
    - Smoke option chosen (A / B / C) + rationale.
    - Per-step pass/fail (for the steps performed).
    - Defects found + disposition (inline fix / gap-closure plan / deferred-acceptable).
    - Open Question 1 status (CRC reference value verification — did any DT_HashString round-trip
      reveal a CRC mismatch?).
    - V1 sign-off recommendation: APPROVED / APPROVED-WITH-DEFERRED-RESIDUAL / REQUIRES-GAP-CLOSURE.
    - Maintainer signature: `Smoke approved by: <name> on <date>; outcome: <vocab>`.
  </how-to-verify>
  <resume-signal>Type "approved" if the smoke passes per Options A/B/C; describe the defect(s) if any blocking issues surfaced. Include a one-line summary citing the disposition (e.g. "approved — Option B; live ACK deferred for Steps 9-12 per Phase 8 precedent; Open Q1 CRC parity confirmed via Step 10 CSV round-trip").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated CI gate → live SWG observation | The automation surface (Plans 09-01..06) covers ~120 [Fact]s across the framework + CLI + controller + save-target + CSV layers. The live observation residual covers the MEF + injection + scene-change + manual-cell-edit-UX surface that cannot be automated without a mock SWG client (V2 deferred per REQ-V2-tier-3-mock-d3d9). |
| maintainer judgment → V1 sign-off | The decision to ship with Open Q residuals (CRC parity, live-ACK on tier-(b) reload) is the maintainer's per Phase 8 precedent — automation-augmented sign-off is a documented acceptable disposition. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-33 | DoS (smoke reveals editor crash on second open) | FormDatatableEditor singleton + MEF re-open | mitigate | Plan 09-03 ships the hide-not-dispose pattern from commit 1; Plan 09-03 Task 2 xUnit guard regression test. If the smoke reveals a crash, it indicates a guard-test gap — escalate to Plan 09-08. |
| T-09-34 | Tampering (silent SC4 corruption discovered in live observation) | DataTableWriter / DatatableSaveTargets / CsvCellCoercion | mitigate | THREE independent automated SC4 enforcement layers (framework cell + controller + CLI + CSV) PLUS the live smoke confirming the saved file re-loads cleanly. Phase 8 precedent: SC4 corruption never surfaced in live smoke after the structural enforcement at the format-primitive layer. |
| T-09-35 | Repudiation (smoke outcome not recorded) | 09-07-SMOKE-LOG.md | mitigate | The plan REQUIRES the maintainer to record the smoke outcome in the named artifact with signature; the consolidated Phase 9 SUMMARY cites the smoke log. No verbal sign-off accepted (memory `feedback_max_harness` — record outcome in the harness, not in conversation). |
| T-09-SC | Tampering | npm/pip/cargo installs | N/A | Plan 09-07 adds ZERO new external packages — pure observation + documentation. |

**ASVS controls applied:**
- V12 File and Resources — smoke exercises file save / file load / TRE archive resolution; all inherited from Phase 8 hardened helpers.
- V5 Input Validation — smoke exercises per-type cell widgets + cascade + CSV import preview; all the validation pathways are automated, the smoke confirms the UX flow surfaces them correctly.
</threat_model>

<verification>
Plan-level acceptance:

1. **Before the smoke runs:** Full suites green on BOTH repos, BOTH configs:
   - `dotnet test UtinniCoreDotNet.Tests --no-build` exits 0.
   - `dotnet test Utinni.Cli.Tests --no-build` exits 0.
   - `dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~PreservationAudit"` exits 0.
   - VS 2026 MSBuild Debug|x86 + Release|x86 of Utinni.sln + UtinniPlugins TJT solution both succeed.
2. **Smoke artifact:** `.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-07-SMOKE-LOG.md` exists with:
   - Smoke option (A/B/C) recorded.
   - At least the minimum step set (per the option) documented pass/fail.
   - Open Question dispositions documented.
   - V1 sign-off recommendation documented.
   - Maintainer signature line.
3. **Phase SUMMARY update:** The phase-level Phase 9 SUMMARY (Plan 09-07's SUMMARY plus a
   consolidated phase-level VERIFICATION.md if appropriate per the phase-end workflow) cites
   the smoke log + records the sign-off disposition.
</verification>

<success_criteria>
- The 4 Phase 9 ROADMAP success criteria are met (per the smoke + the automated coverage):
  1. Datatable Editor subpanel loads inside TJT against live SWG (smoke Step 1).
  2. User can open a .tab file (3 entry points — smoke Steps 2-4), view rows/columns, edit cell
     values (smoke Step 5), save back (smoke Step 8 — 4 modes).
  3. The live SWG client picks up the edit on the relevant reload path (smoke Step 9 — TJT
     chat-command scene change + observation in-game).
  4. Edits preserve schema (column types, foreign-key-style references) without silent corruption
     (automated SC4 enforcement at 4 layers: framework cell + controller + CLI + CSV — smoke
     confirms the file re-loads cleanly).
- The smoke produces a documented V1 sign-off recommendation in 09-07-SMOKE-LOG.md.
- Any Open Question residuals are either resolved (e.g. CRC parity confirmed via Step 10) or
  documented as deferred-but-acceptable with Phase 8 precedent citation.
- The phase is ready for the consolidated Phase 9 sign-off via `/gsd:verify-phase 9` (or the
  phase-end workflow chosen by the orchestrator).
</success_criteria>

<output>
Create `.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-07-SMOKE-LOG.md` (the smoke
artifact described in the task). Also create `.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-07-SUMMARY.md`
with the standard plan-level SUMMARY (citing the smoke log + the phase-end disposition).

The phase is COMPLETE when the smoke log + SUMMARY are committed and the maintainer's V1 sign-off
is recorded — at that point Phase 9 contributes PROD-W1-DT to the PROD-02 Wave-1 aggregate, and
the project advances to Phase 10 (String-table Editor) per ROADMAP.md execution order.
</output>

## Revision History

- 2026-05-29 (iter 1): wave bumped 5 -> 6 (cascade from Issue #5 09-05 wave shift).
