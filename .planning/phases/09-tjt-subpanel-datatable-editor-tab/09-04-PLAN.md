---
phase: 09-tjt-subpanel-datatable-editor-tab
plan: 04
type: execute
wave: 3
depends_on: ["09-03"]
files_modified:
  - UtinniCoreDotNet/Editing/DatatableEditController.cs
  - UtinniCoreDotNet/Editing/IDatatableEditCommand.cs
  - UtinniCoreDotNet/Editing/DatatableEditCommands.cs
  - UtinniCoreDotNet/UtinniCoreDotNet.csproj
  - UtinniCoreDotNet.Tests/FormatsTests/Datatable/DatatableEditControllerTests.cs
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormAddColumnDialog.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormAddColumnDialog.Designer.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTypeChangeCascadeDialog.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTypeChangeCascadeDialog.Designer.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"
autonomous: true
requirements: [PROD-W1-DT]
assumes:
  - "Plan 09-03 has shipped FormDatatableEditor with the toolbar buttons rendered (stubbed). Plan 09-04 replaces the stubs for Add row / Add column… / Undo / Redo / structural ops with controller-backed commands. Save▾ wiring and entry-point hand-offs are Plan 09-05's concern; CSV import + Find/Replace + sort are Plan 09-06's concern."
  - "DatatableEditController ships FRAMEWORK-SIDE in `UtinniCoreDotNet/Editing/` (CF-06 — mirrors IffEditController.cs placement). Pure-managed BCL-only (no WinForms, no UtinniCore.Memory). xUnit-testable from CI."
  - "The two new modals (FormAddColumnDialog + FormTypeChangeCascadeDialog) follow the PER-CALL modal lifecycle (`using (var dlg = …) { dlg.ShowDialog(this); }`) per RESEARCH § Pitfall 6 — do NOT apply the hide-not-dispose pattern. Default WinForms dispose-on-close is CORRECT for per-call modals."
  - "Column-reorder + Column-delete D-02 safety-net REUSES the existing FormSaveConfirmDialog with the `showBackupCheckbox` slot relabeled 'Don't ask again this session' per UI-SPEC assumption #5 — no new safety-net dialog file."
  - "Save-blocked-while-needs-review (D-04 / UI-SPEC R-04) MUST apply to EVERY Save▾ menu item, not just the top-level button. Plan 09-04 adds the `NeedsReviewCount` seam to DatatableEditController and updates `RefreshSaveMenuEnabledState` (currently a stub from Plan 09-03) to read it. Plan 09-05 will wire the provenance-gated save menu state on top of Plan 09-04's NeedsReview gate — the two gates compose (provenance enables a save mode AND NeedsReview == 0 → menu item enabled)."
  - "The xUnit guard for SC4 structural property (`load → EditCellValue(0,0) → Undo → bytes EXACTLY equal loaded bytes`) ships in this plan's `DatatableEditControllerTests.cs` per RESEARCH § 'What the planner must NOT skip' — the framework-test-layer enforcement of the CF-04 invariant going through the controller (Plan 09-01 Task 2 ships the direct-mutation version of this test; Plan 09-04 adds the controller-mediated version)."
must_haves:
  truths:
    - "A pure-managed `DatatableEditController` wraps a `MutableDataTableDocument` with editor-local undo/redo (CF-06) — Apply(IDatatableEditCommand) / Undo / Redo / CanUndo / CanRedo / IsDirty / NeedsReviewCount / EditApplied event / Document property — verbatim shape from Phase 8 IffEditController.cs lines 67-160."
    - "Eleven `IDatatableEditCommand` implementations cover the full D-01 T4 mutation set: `EditCellValue`, `AddRow`, `RemoveRow`, `MoveRowUp`, `MoveRowDown`, `AddColumn`, `RemoveColumn`, `MoveColumnLeft`, `MoveColumnRight`, `RenameColumn`, `ChangeColumnType` (+ a 12th `ApplyCsvImport` factory stub that Plan 09-06 fills with the bulk-import transaction wrapper)."
    - "`ChangeColumnType` cascade per D-04: runs `DataTableColumnType.MangleValue` on every cell in the changed column; cells that fail the mangle are flagged `NeedsReview = true` (sets the Color.Red BackColor overlay via Plan 09-03's CellFormatting handler); save is BLOCKED while `NeedsReviewCount > 0` (R-04)."
    - "`FormTypeChangeCascadeDialog` per UI-SPEC assumption #6: NEW per-call modal with embedded `ThemedDataGridView` listing affected cells (columns: row index / original value / mangled value / Accept-mangled / Edit-cell buttons); footer `Done` (closes; remaining red cells stay flagged) + `Revert type change` (rolls the whole ChangeColumnType command back via the editor-local undo stack)."
    - "`FormAddColumnDialog` per UI-SPEC § Layout — small per-call modal with `UtinniTextbox` for column name + `UtinniComboBox` of the 10 DT_* types + `Add column` / `Cancel` buttons; validation per UI-SPEC § Copywriting (empty name → red status, duplicate name → red status)."
    - "Column reorder/delete D-02 safety-net REUSES `FormSaveConfirmDialog` with locked CONTEXT D-02 body copy + `Don't ask again this session` relabeled checkbox; session-suppress flag is per-editor-instance (resets on form close)."
    - "Save▾ menu items + top-level button DISABLE when `controller.NeedsReviewCount > 0`, with the locked UI-SPEC § Copywriting tooltip 'Resolve {N} cell(s) that need review before saving.' surfaced on EACH menu item (UI-SPEC R-04 explicitly — not just the button face)."
    - "Toolbar `Undo` / `Redo` / `Add row` / `Add column…` / structural-op context-menu items wired through the controller; `Ctrl+Z` / `Ctrl+Y` in `ProcessCmdKey` dispatch to controller (replaces Plan 09-03 stubs)."
    - "The `RemoveCommand` insert-by-reference fix from Phase 8 IffEditController CR-01 (IffEditController.cs lines 333-361) is ported verbatim for DatatableEditController's RemoveRow + RemoveColumn — re-attaches the original node by reference on Undo so a subsequent Do() finds it in the parent collection and removes it cleanly. Avoids the snapshot+Materialize trap."
  artifacts:
    - path: "UtinniCoreDotNet/Editing/DatatableEditController.cs"
      provides: "Pure-managed controller (CF-06) — Apply/Undo/Redo + NeedsReviewCount + EditApplied event; mirrors IffEditController shape verbatim"
      contains: "NeedsReviewCount"
    - path: "UtinniCoreDotNet/Editing/IDatatableEditCommand.cs"
      provides: "Command interface — Do(MutableDataTableDocument) + UndoOp(MutableDataTableDocument)"
      contains: "IDatatableEditCommand"
    - path: "UtinniCoreDotNet/Editing/DatatableEditCommands.cs"
      provides: "Static factory class — 11 command factories + the ChangeColumnType cascade + the RemoveCommand insert-by-reference fix"
      contains: "ChangeColumnType"
    - path: "UtinniCoreDotNet/UtinniCoreDotNet.csproj"
      provides: "Old-style explicit-compile — adds <Compile Include> entries for 3 new Editing/*.cs files (round-2 HIGH-A; no default glob)"
      contains: "Editing\\DatatableEditController.cs"
    - path: "UtinniCoreDotNet.Tests/FormatsTests/Datatable/DatatableEditControllerTests.cs"
      provides: "~30-40 [Fact]s covering each command × Apply/Undo/Redo + cascade + needs-review save-block + baseline-clean-dirty + the SC4 structural property at the controller layer"
      contains: "DatatableEditControllerTests"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs"
      provides: "MODIFY — instantiate DatatableEditController in LoadDocument; subscribe EditApplied; wire toolbar buttons (Add row, Add column…, Undo, Redo) to controller; replace Plan 09-03 ProcessCmdKey stubs with controller calls; extend RefreshSaveMenuEnabledState to read NeedsReviewCount and disable all save items + top-level button with locked tooltip; wire ChangeColumnType to surface FormTypeChangeCascadeDialog when NeedsReviewCount > 0 after the cascade; wire column-reorder/delete to FormSaveConfirmDialog safety-net"
      contains: "DatatableEditController"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormAddColumnDialog.cs"
      provides: "Per-call modal — `using (var dlg = new FormAddColumnDialog(existingColumnNames))` returns (newName, newColumnType) on Apply"
      contains: "FormAddColumnDialog"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormAddColumnDialog.Designer.cs"
      provides: "Hand-written Designer; UtinniTextbox + UtinniComboBox + Add/Cancel UtinniButtons"
      contains: "InitializeComponent"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTypeChangeCascadeDialog.cs"
      provides: "Per-call modal with embedded ThemedDataGridView listing cells needing review; Accept-mangled / Edit-cell per row; Done / Revert footer"
      contains: "FormTypeChangeCascadeDialog"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTypeChangeCascadeDialog.Designer.cs"
      provides: "Hand-written Designer; ThemedDataGridView Fill + footer Panel Bottom"
      contains: "InitializeComponent"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"
      provides: "Explicit <Compile Include> entries for the 4 new Form files (2 Form .cs + 2 Designer.cs); Forms use <SubType>Form</SubType>; Designers use <DependentUpon>"
      contains: "FormTypeChangeCascadeDialog.cs"
  key_links:
    - from: "FormDatatableEditor.LoadDocument"
      to: "new DatatableEditController(dtDoc.Mutable)"
      via: "controller instantiation replaces Plan 09-03's null-controller staging"
      pattern: "DatatableEditController"
    - from: "DatatableEditController.NeedsReviewCount"
      to: "FormDatatableEditor.RefreshSaveMenuEnabledState"
      via: "R-04 save-block gate: NeedsReviewCount > 0 → all save items disabled with locked tooltip"
      pattern: "NeedsReviewCount"
    - from: "DatatableEditCommands.ChangeColumnType.Do"
      to: "DataTableColumnType.MangleValue (per-cell) + cell.NeedsReview = (!mangleSucceeded)"
      via: "D-04 cascade — sets needs-review flag on failing cells; controller's NeedsReviewCount tallies them"
      pattern: "MangleValue"
    - from: "DatatableEditCommands.ChangeColumnType.UndoOp"
      to: "restore original column type AND clear all NeedsReview flags it set"
      via: "Undo must roll back BOTH the column-type change AND the per-cell needs-review markings"
      pattern: "NeedsReview = false"
    - from: "FormDatatableEditor (column reorder/delete handler)"
      to: "FormSaveConfirmDialog with locked D-02 body + relabeled session-suppress checkbox"
      via: "UI-SPEC assumption #5 — reuse, do NOT clone"
      pattern: "FormSaveConfirmDialog"
    - from: "DatatableEditCommands.RemoveRow.UndoOp / RemoveColumn.UndoOp"
      to: "insert-by-reference (the original row/column object, not a snapshot)"
      via: "Phase 8 IffEditController CR-01 fix port"
      pattern: "InsertAt"
---

## Execution Notes (iter-1 fix #8)

**Cascade-context tracking moves into the framework controller (was form-local `lastCascadeContext` in iter-0).**

Per checker WARNING #8, the form-local `lastCascadeContext` field on FormDatatableEditor (which Plan 09-04 Task 2 originally proposed) is replaced by a framework-side public read-only property on `DatatableEditController` so the cascade state machine is xUnit-testable.

**Controller surface additions (Task 1):**
- `public PendingTypeChangeCascade? PendingCascadeContext { get; }` â€” nullable record exposing `{ ColumnIndex, OldType, NewType, NeedsReviewCellRefs IReadOnlyList<MutableDataTableCell> }`.
- `public int NeedsReviewCount { get; }` (already in iter-0 plan; unchanged).
- Internal setter wired inside `ChangeColumnTypeCommand.Do` (sets `PendingCascadeContext`); cleared in `ChangeColumnTypeCommand.UndoOp` AND cleared automatically when `NeedsReviewCount` transitions to 0 via successful per-cell resolution edits (the controller listens for cell-value sets on the cells referenced in `NeedsReviewCellRefs` and recomputes).

**Form-side rewire (Task 2):**
- Drop the `lastCascadeContext` private field from `FormDatatableEditor`.
- The `Resolve cascade...` toolbar button reads `controller.PendingCascadeContext` (null-check guard) to re-open `FormTypeChangeCascadeDialog` with the current affected-cell list.
- The cascade-button visibility predicate becomes `btnResolveCascade.Visible = controller.PendingCascadeContext != null`.

**New xUnit facts in `DatatableEditControllerTests.cs` (Task 1) â€” three required:**
1. `ChangeColumnType_Then_Undo_ClearsPendingCascade` â€” Apply ChangeColumnType with failing cells; assert `PendingCascadeContext != null`; Undo; assert `PendingCascadeContext == null` AND `NeedsReviewCount == 0`.
2. `ChangeColumnType_Then_ResolveAllCells_ClearsPendingCascade` â€” Apply ChangeColumnType with 3 failing cells; Apply 3 EditCellValue commands on those 3 cells with valid values; assert `PendingCascadeContext == null` after the 3rd successful resolution (and `NeedsReviewCount == 0`).
3. `ChangeColumnType_Then_PartialResolve_PendingCascadeRetained` â€” Apply ChangeColumnType with 3 failing cells; resolve 2 of 3 via EditCellValue; assert `PendingCascadeContext != null` AND `NeedsReviewCount == 1` AND `PendingCascadeContext.NeedsReviewCellRefs.Count == 1` (only the unresolved cell remains in the cell-refs list).

These three facts add to the Plan 09-04 Task 1 fact count target (was ~30; now ~33-35 with these additions).

<objective>
Ship the **T4 schema-mutation engine** for Phase 9. This plan introduces the framework-side
`DatatableEditController` (CF-06 — pure-managed, BCL-only, xUnit-testable from CI) with the 11
structural commands covering the full D-01 T4 scope: cell edits, row add/remove/reorder, column
add/remove/reorder, column rename, and the D-04 column type-change cascade. The cascade is the
high-leverage piece — it runs `DataTableColumnType.MangleValue` on every cell in the changed
column, flags the failing cells `NeedsReview = true`, and BLOCKS all Save▾ menu items + the
top-level button (UI-SPEC R-04) until `NeedsReviewCount` returns to 0.

The plan also ships two TJT-side per-call modals: `FormAddColumnDialog` (small input for new
column name + type) and `FormTypeChangeCascadeDialog` (the cascade resolution UI with embedded
ThemedDataGridView listing affected cells per UI-SPEC assumption #6 — its own dialog, NOT a
FormSaveConfirmDialog clone). The D-02 column-reorder/delete safety-net REUSES the existing
FormSaveConfirmDialog per UI-SPEC assumption #5 (no new dialog file for the safety-net).

The xUnit suite is the largest in the phase — ~30-40 [Fact]s — covering each command × Apply/
Undo/Redo identity, the baseline-clean-dirty invariant (Phase 8 Codex-unique-concern resolution),
the SC4 structural property at the controller layer (load → EditCellValue → Undo → bytes EXACTLY
equal loaded), and the D-04 cascade matrix.

**Why this decomposition (Wave 3 parallel with Plan 09-05):** The controller + ops are independent
from the entry points + save targets in Plan 09-05 — both depend on Plan 09-03's form host but
NOT on each other. Splitting them lets two executor agents work concurrently. Both converge in
the FormDatatableEditor.cs file (which Plan 09-03 ships, then both Plan 09-04 and 09-05 modify
DIFFERENT methods — 09-04 wires LoadDocument's controller instantiation + structural-op handlers;
09-05 wires Save▾ click handlers + entry-point methods). The shared file is acceptable here
because the modifications are surgically additive in non-overlapping sections; both plans
explicitly list FormDatatableEditor.cs in `files_modified` so the orchestrator gates the wave
correctly.

**Round-2-style fixes folded:** insert-by-reference fix from Phase 8 CR-01 ported verbatim for
RemoveRow + RemoveColumn (CONTEXT carry-forward via PATTERNS § Pattern Assignments lines 228-236).
DT_PackedObjVars/DT_BitVector parse-back validators consumed via the Plan 09-01 `MangleValue`
port (no re-derivation here).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-CONTEXT.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-RESEARCH.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-PATTERNS.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-VALIDATION.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-UI-SPEC.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-01-PLAN.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-03-PLAN.md

# Phase 8 controller — VERBATIM port reference; Plan 09-04 mirrors lines 67-160 + 167-262 + 333-361
@UtinniCoreDotNet/Editing/IffEditController.cs

# Phase 8 controller tests — pattern reference for Plan 09-04's test file
@UtinniCoreDotNet.Tests/EditingTests/IffEditControllerTests.cs

# Phase 8 FormSaveConfirmDialog — reused for D-02 safety-net per UI-SPEC assumption #5
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormSaveConfirmDialog.cs

# Phase 8 small-input modal analog — FormAddColumnDialog shape (UtinniForm + 1 text input + 2 buttons)
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormFourCcDialog.cs
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormFourCcDialog.Designer.cs

# Plan 09-01 framework primitives — public API the controller mutates
@UtinniCoreDotNet/Formats/Datatable/DataTableColumnType.cs
@UtinniCoreDotNet/Formats/Datatable/MutableDataTableCell.cs
@UtinniCoreDotNet/Formats/Datatable/MutableDataTableColumn.cs
@UtinniCoreDotNet/Formats/Datatable/MutableDataTableRow.cs
@UtinniCoreDotNet/Formats/Datatable/MutableDataTableDocument.cs

# Plan 09-03 FormDatatableEditor — MODIFY target
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs

# Plan 09-03 ThemedDataGridView — embedded inside FormTypeChangeCascadeDialog
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/ThemedDataGridView.cs

<interfaces>
<!-- Phase 8 IffEditController public surface — the verbatim port shape -->

From UtinniCoreDotNet/Editing/IffEditController.cs (Plan 09-04 mirrors verbatim):
- public sealed class IffEditController
  - public IffEditController(MutableIffDocument document)
  - public MutableIffDocument Document { get; }
  - public bool IsDirty { get; }                                    // netAppliedCount > 0 (baseline-clean dirty — Codex unique concern)
  - public bool CanUndo { get; }
  - public bool CanRedo { get; }
  - public event EventHandler EditApplied
  - public void Apply(IIffEditCommand cmd)                          // cmd.Do(); push undoStack; clear redoStack; netAppliedCount++; raise EditApplied
  - public void Undo()                                              // undoStack.Pop().UndoOp(); push redoStack; netAppliedCount--; raise EditApplied
  - public void Redo()                                              // redoStack.Pop().Do(); push undoStack; netAppliedCount++; raise EditApplied

From UtinniCoreDotNet/Editing/IffEditController.cs lines 167-262 (the IIffEditCommand interface + IffEditCommands factory class):
- public interface IIffEditCommand
  - void Do(MutableIffDocument doc)
  - void UndoOp(MutableIffDocument doc)
- public static class IffEditCommands
  - 10 factory methods — Plan 09-04's IDatatableEditCommand + DatatableEditCommands MIRROR this shape

From UtinniCoreDotNet/Editing/IffEditController.cs lines 333-361 (the CR-01 insert-by-reference fix the executor MUST port for RemoveRow + RemoveColumn):
- // Re-attach the original node BY REFERENCE so a subsequent Do() finds it in
- // parent.Children and removes it cleanly. (Avoid the snapshot+Materialize trap.)
- public void UndoOp(MutableIffDocument doc)
- { parent.InsertChildAtInternal(originalIndex, node); }

From Plan 09-01:
- DataTableColumnType.MangleValue(ref string value) → bool   (Plan 09-04 ChangeColumnType calls this per-cell)
- MutableDataTableCell.Value get/set (the setter clears originalSlice when value changes)
- MutableDataTableCell.NeedsReview get; internal set (Plan 09-04 sets this in ChangeColumnType.Do; clears in UndoOp + any successful Value set)
- MutableDataTableDocument.Columns / Rows lists (the controller mutates these via the command Do/UndoOp pairs)

From Phase 8 FormSaveConfirmDialog (the modal Plan 09-04 reuses for D-02):
- public partial class FormSaveConfirmDialog : UtinniForm
- public enum ConfirmOutcome { Confirmed, Cancelled }
- public ConfirmOutcome Outcome { get; private set; }
- public bool BackupRequested { get; private set; }   // Plan 09-04 relabels the checkbox to "Don't ask again this session"; reads the bool flag for the session-suppress
- public bool ShowBackupCheckbox { get; }
- public FormSaveConfirmDialog(string heading, string body, string acceptVerb, string cancelVerb, bool showBackupCheckbox, string backupCheckboxLabel)
  // Per-call: using (var dlg = new FormSaveConfirmDialog(...)) { dlg.ShowDialog(this); }

From Phase 8 FormFourCcDialog (the small-input-modal analog for FormAddColumnDialog):
- public partial class FormFourCcDialog : UtinniForm
- public string Value { get; }
- public FormFourCcDialog(string title, string initialValue)
- // Per-call: using (var dlg = new FormFourCcDialog(...)) { dlg.ShowDialog(this); }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship DatatableEditController + IDatatableEditCommand + DatatableEditCommands (11 commands + cascade)</name>
  <files>UtinniCoreDotNet/Editing/DatatableEditController.cs, UtinniCoreDotNet/Editing/IDatatableEditCommand.cs, UtinniCoreDotNet/Editing/DatatableEditCommands.cs, UtinniCoreDotNet/UtinniCoreDotNet.csproj, UtinniCoreDotNet.Tests/FormatsTests/Datatable/DatatableEditControllerTests.cs</files>
  <read_first>
    - UtinniCoreDotNet/Editing/IffEditController.cs (FULL FILE — the verbatim port shape; specifically lines 67-160 controller class, 167-262 IIffEditCommand interface + IffEditCommands factory class, 333-361 the CR-01 insert-by-reference UndoOp fix Plan 09-04 MUST port for RemoveRow + RemoveColumn, 446-517 NodeSnapshot.Capture — port a similar `RowSnapshot` if needed for RemoveRow undo, though the insert-by-reference path avoids most of the snapshot complexity)
    - UtinniCoreDotNet.Tests/EditingTests/IffEditControllerTests.cs (FULL FILE — the test-shape analog; particularly the apply/undo/redo identity tests per command, the baseline-clean-dirty fact, and the multi-command transaction tests)
    - UtinniCoreDotNet/Formats/Datatable/DataTableColumnType.cs (Plan 09-01 — MangleValue signature + EnumMap + Type/BasicType properties the cascade reads)
    - UtinniCoreDotNet/Formats/Datatable/MutableDataTableCell.cs (Plan 09-01 — Value get/set + NeedsReview get; internal set the cascade flips)
    - UtinniCoreDotNet/Formats/Datatable/MutableDataTableColumn.cs (Plan 09-01 — Name + ColumnType setters)
    - UtinniCoreDotNet/Formats/Datatable/MutableDataTableRow.cs + MutableDataTableDocument.cs (Plan 09-01 — Rows / Columns lists, MarkDirty)
    - UtinniCoreDotNet.Tests/FormatsTests/Datatable/DatatableFixtures.cs (Plan 09-01 — Build helpers the controller tests use)
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-PATTERNS.md § Pattern Assignments → DatatableEditController (lines 167-237 of PATTERNS — exact public-API shape) AND § "What the planner must NOT skip" (the SC4 structural property test at the controller layer; the NeedsReviewCount seam) AND § Wave 0 Gaps row DatatableEditControllerTests.cs (~30-40 [Fact]s target)
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-RESEARCH.md § Pattern 4 (DatatableEditController shape) AND § Anti-Patterns Per-character EditCellValue commits (commit-on-CellEndEdit, not CellValueChanged — relevant for Task 2's UI wire but documented here for the controller's transactional intent)
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-VALIDATION.md § Wave 0 Requirements DatatableEditControllerTests.cs entry + § Per-Task Verification Map row T4 schema mutation
  </read_first>
  <behavior>
    - DatatableEditController.cs:
      - `namespace UtinniCoreDotNet.Editing`; `public sealed class DatatableEditController`.
      - Constructor: `DatatableEditController(MutableDataTableDocument document)` — guards null, stores doc, initializes empty stacks + `netAppliedCount = 0`.
      - Properties: `Document` get-only; `IsDirty => netAppliedCount > 0` (baseline-clean per Phase 8 D-08 + Codex unique concern); `CanUndo => undoStack.Count > 0`; `CanRedo => redoStack.Count > 0`; `NeedsReviewCount` get-only — walks every cell and counts `cell.NeedsReview == true` (cheap for V1 sizes; if perf surfaces as an issue, cache via incremental delta — defer to follow-up).
      - Event: `public event EventHandler EditApplied` — fires after every Apply/Undo/Redo.
      - Public methods: `Apply(IDatatableEditCommand cmd)` (cmd.Do(document); undoStack.Push; redoStack.Clear; netAppliedCount++; raise EditApplied); `Undo()` (cmd = undoStack.Pop; cmd.UndoOp(document); redoStack.Push; netAppliedCount--; raise EditApplied); `Redo()` (cmd = redoStack.Pop; cmd.Do(document); undoStack.Push; netAppliedCount++; raise EditApplied).
      - Mirrors IffEditController shape verbatim — keep variable names, ordering, and the netAppliedCount idiom identical for review-pattern consistency.
    - IDatatableEditCommand.cs:
      - `namespace UtinniCoreDotNet.Editing`; `public interface IDatatableEditCommand { void Do(MutableDataTableDocument doc); void UndoOp(MutableDataTableDocument doc); }`.
    - DatatableEditCommands.cs:
      - `namespace UtinniCoreDotNet.Editing`; `public static class DatatableEditCommands`.
      - Eleven factory methods returning concrete `IDatatableEditCommand` implementations (private nested classes inside the static factory, mirroring `IffEditCommands` layout):
        1. `EditCellValue(MutableDataTableCell cell, DataTableCellValue newValue)` — captures (oldValue, oldOriginalSlice, oldIsDirty) on Do; restores on UndoOp. Do: `cell.Value = newValue` (the setter handles the originalSlice null + IsDirty true). UndoOp: restore captured state directly via internal setter access.
        2. `AddRow(MutableDataTableDocument doc, int atIndex)` — creates a new MutableDataTableRow with default cells per column type (calls `MutableDataTableCell` ctor + `DataTableColumnType.MangleValue("", ...)` to seed defaults). Do: doc.Rows.Insert(atIndex, newRow); newRow.IsAdded = true. UndoOp: doc.Rows.RemoveAt(atIndex).
        3. `RemoveRow(MutableDataTableRow row)` — captures (row reference, originalIndex). Do: int idx = doc.Rows.IndexOf(row); doc.Rows.RemoveAt(idx). **UndoOp: doc.Rows.Insert(originalIndex, row) — INSERT THE SAME ROW INSTANCE BY REFERENCE per Phase 8 CR-01 fix; do NOT snapshot+materialize (the cell back-refs would break).**
        4. `MoveRowUp(MutableDataTableRow row)` — index swap with index-1; UndoOp swaps back.
        5. `MoveRowDown(MutableDataTableRow row)` — symmetric.
        6. `AddColumn(MutableDataTableDocument doc, string name, DataTableColumnType ct, int atIndex)` — creates new MutableDataTableColumn(name, ct, isAdded=true); inserts at atIndex; for each row, inserts a new default-cell at atIndex per the column type. UndoOp: removes the column AND each row's cell at atIndex.
        7. `RemoveColumn(MutableDataTableColumn col)` — captures (col, originalIndex, list-of-removed-cells-by-row). Do: int idx = doc.Columns.IndexOf(col); doc.Columns.RemoveAt(idx); for each row, captures row.Cells[idx] then removes it. **UndoOp: doc.Columns.Insert(originalIndex, col); for each row, Insert the captured cell BY REFERENCE — same insert-by-reference posture as RemoveRow.**
        8. `MoveColumnLeft(MutableDataTableColumn col)` — column-index swap; for each row, swap row.Cells[idx]/[idx-1] too (keep cell-column alignment). UndoOp swaps back.
        9. `MoveColumnRight(MutableDataTableColumn col)` — symmetric.
        10. `RenameColumn(MutableDataTableColumn col, string newName)` — capture oldName; Do: col.Name = newName. UndoOp: col.Name = oldName.
        11. `ChangeColumnType(MutableDataTableColumn col, DataTableColumnType newCt)` — captures (oldCt, list-of-per-cell-(oldValue, oldNeedsReview)). Do: col.ColumnType = newCt; for each row's cell at this column: call `newCt.MangleValue(ref currentValue)`; if false → `cell.NeedsReview = true` AND keep the current value (the user resolves via the cascade modal); if true → `cell.Value = newValueFromMangle`. UndoOp: col.ColumnType = oldCt; for each row's cell, restore the captured (oldValue, oldNeedsReview) — CLEAR every NeedsReview flag this command set.
      - Stub factory: `ApplyCsvImport(MutableDataTableDocument doc, /* CsvImportPlan */ object plan)` — throws `NotImplementedException("Wired by Plan 09-06")`; the signature is present so Plan 09-06's CsvImportPlan integration is purely additive.
    - csproj: 3 new `<Compile Include>` entries for `Editing\DatatableEditController.cs`, `Editing\IDatatableEditCommand.cs`, `Editing\DatatableEditCommands.cs` — alphabetically in the existing `Editing\` block.
    - DatatableEditControllerTests.cs (~30-40 [Fact]s):
      - **Controller invariants:** baseline IsDirty == false; after Apply IsDirty == true; after Apply→Undo IsDirty == false (the Codex baseline-clean fact); CanUndo/CanRedo state machine across Apply/Undo/Redo sequences; EditApplied event fires once per operation.
      - **Per-command Apply/Undo/Redo identity** (11 commands × 3 ops = 33 facts minimum; some can combine in a parameterized [Theory]):
        - EditCellValue: cell value changes; Undo restores; Redo re-applies.
        - AddRow / RemoveRow: row count delta on Do; row count restore on Undo; row-instance equality on RemoveRow→Undo (the insert-by-reference assertion); cells preserve their originalSlice across RemoveRow→Undo.
        - MoveRowUp / MoveRowDown: row at the moved position is the SAME instance; index shifts correctly.
        - AddColumn / RemoveColumn: column count delta; each row's cell count delta; insert-by-reference fact for RemoveColumn→Undo.
        - MoveColumnLeft / MoveColumnRight: column at new position is SAME instance; every row's cells track the column reorder.
        - RenameColumn: name change + undo restore.
        - **ChangeColumnType cascade (the D-04 high-leverage tests):**
          - DT_String → DT_Int with all-integer-strings: every cell mangles successfully; NeedsReviewCount == 0 after Do.
          - DT_String → DT_Int with one non-integer string: one cell flagged NeedsReview; NeedsReviewCount == 1; the other cells mangled.
          - DT_Int → DT_Bool with values {"0", "1", "5"}: cells with "0"/"1" mangle; cell with "5" flagged NeedsReview.
          - ChangeColumnType.UndoOp: column type restored; ALL NeedsReview flags cleared; values restored.
          - DT_String → DT_HashString: every cell mangles (CRC accepts any string); int32 hash stored.
      - **SC4 structural property at controller layer** (MANDATORY per RESEARCH § "What the planner must NOT skip" — Plan 09-01 Task 2 ships the direct-mutation version; Plan 09-04 Task 1 ships the controller-mediated version): `load BuildV1AllTypes() → new DatatableEditController(mDoc) → controller.Apply(DatatableEditCommands.EditCellValue(cell[0,0], newValue)) → controller.Undo() → new DataTableWriter(mDoc).Serialize() bytes EXACTLY equal the loaded bytes`. This is the controller-layer enforcement of CF-04.
      - **NeedsReviewCount tally fact:** after a cascade that flags 3 cells, `controller.NeedsReviewCount == 3`; after Undo, == 0.
    - SDK-style test csproj — no test-csproj edit.
  </behavior>
  <action>
    Create three `Editing/*.cs` files. Each file gets the 23-line MIT header (copy from
    IffEditController.cs), Allman braces, 4-space indent, `// ToDo` (no colon) marker convention.
    Namespace `UtinniCoreDotNet.Editing` (matches IffEditController placement).

    `DatatableEditController.cs` — VERBATIM port of `IffEditController.cs` lines 67-160 with
    type-name swaps (`MutableIffDocument` → `MutableDataTableDocument`; `IIffEditCommand` →
    `IDatatableEditCommand`); ADD the `NeedsReviewCount` get-only property.

    `IDatatableEditCommand.cs` — minimal interface; XML comment cites the IffEditController.cs
    `IIffEditCommand` analog.

    `DatatableEditCommands.cs` — 11 factory methods + private nested concrete command classes
    (mirror IffEditCommands layout). Particular attention to:
    - RemoveRow + RemoveColumn UndoOp implement the insert-by-reference pattern explicitly (XML
      comment cites Phase 8 CR-01 fix at IffEditController.cs lines 333-361 + RESEARCH § Pattern 4
      paragraph "RemoveCommand insert-by-reference fix").
    - ChangeColumnType captures BOTH the old column type AND a per-cell (oldValue, oldNeedsReview)
      snapshot list; UndoOp restores both in lock-step.
    - The 12th `ApplyCsvImport` factory throws `NotImplementedException("Wired by Plan 09-06")` so
      Plan 09-06's integration is additive (the signature already exists in the surface).

    Update `UtinniCoreDotNet/UtinniCoreDotNet.csproj`: 3 new `<Compile Include="Editing\...">`
    entries alphabetically within the existing `Editing\` block.

    Create `UtinniCoreDotNet.Tests/FormatsTests/Datatable/DatatableEditControllerTests.cs` with
    the ~30-40 [Fact]s per the behavior block. Use the Plan 09-01 fixture builders for setup.
    The SC4 controller-layer [Fact] gets a `[Fact(DisplayName = "SC4 (controller): EditCellValue
    → Undo → bytes EXACTLY equal loaded")]` display name so it surfaces clearly.

    SDK-style auto-glob — no test-csproj edit.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~DatatableEditControllerTests" 2>&1 | tail -15  # MUST exit 0 with >= 30 passing facts; grep -c "Editing\\\\DatatableEditController.cs" UtinniCoreDotNet/UtinniCoreDotNet.csproj  # MUST return 1; grep -c "InsertAt" UtinniCoreDotNet/Editing/DatatableEditCommands.cs  # MUST return >= 2 (RemoveRow.UndoOp + RemoveColumn.UndoOp insert-by-reference); grep -c "SC4 (controller)" UtinniCoreDotNet.Tests/FormatsTests/Datatable/DatatableEditControllerTests.cs  # MUST return 1</automated>
  </verify>
  <done>Three new Editing/*.cs files compile; csproj has explicit Compile entries for all three; DatatableEditControllerTests runs green with ≥ 30 [Fact]s including the SC4 controller-layer enforcement and the D-04 cascade matrix; insert-by-reference patterns documented in RemoveRow + RemoveColumn UndoOps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship FormAddColumnDialog + FormTypeChangeCascadeDialog + wire FormDatatableEditor to controller (Add row, Add column…, Undo, Redo, ChangeColumnType, column reorder/delete safety-net, NeedsReview save block)</name>
  <files>../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormAddColumnDialog.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormAddColumnDialog.Designer.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTypeChangeCascadeDialog.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTypeChangeCascadeDialog.Designer.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj</files>
  <read_first>
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormFourCcDialog.cs + .Designer.cs (FULL FILES — the analog for FormAddColumnDialog: small per-call modal with one input + Add/Cancel buttons; `public string Value { get; }` return-on-Apply pattern Plan 09-04 mirrors)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormSaveConfirmDialog.cs (FULL FILE — the per-call modal lifecycle Plan 09-04 mirrors for FormTypeChangeCascadeDialog + the EXISTING reused-as-D-02-safety-net dialog Plan 09-04 invokes; particularly the constructor signature + Outcome/BackupRequested return)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/ThemedDataGridView.cs (Plan 09-03 — embedded inside FormTypeChangeCascadeDialog showing affected cells)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs (Plan 09-03 — MODIFY target; specifically LoadDocument stub, the Add row / Add column… toolbar button click handlers that throw NotImplementedException, ProcessCmdKey Ctrl+Z/Y stubs, RefreshSaveMenuEnabledState stub)
    - UtinniCoreDotNet/Editing/DatatableEditController.cs (Plan 09-04 Task 1 — the public API the form wires to)
    - UtinniCoreDotNet/Editing/DatatableEditCommands.cs (Plan 09-04 Task 1 — the 11 command factories the form invokes)
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-UI-SPEC.md § Layout Contract (Editor toolbar buttons), § States row "Needs review" + "Reordered column" + "Deleted column / row", § Destructive table (Column reorder/delete safety net, Type-change cascade resolution), § Copywriting (Add column dialog title + field labels + buttons; Type-change cascade modal heading + body banner + per-cell affordances + footer buttons; Column reorder/delete safety-net body LOCKED D-02 copy; Save BLOCKED tooltip locked copy)
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-PATTERNS.md § File Classification → FormAddColumnDialog (analog FormFourCcDialog) AND FormTypeChangeCascadeDialog (analog FormSaveConfirmDialog + embedded ThemedDataGridView)
  </read_first>
  <behavior>
    - FormAddColumnDialog.cs + Designer:
      - `namespace TJT.UI.Forms`; `public partial class FormAddColumnDialog : UtinniForm`.
      - Constructor `FormAddColumnDialog(IEnumerable<string> existingColumnNames)` — stores the existing names for duplicate-check.
      - Designer: `UtinniTextbox txtColumnName` + `UtinniComboBox cmbColumnType` (10 DT_* items per UI-SPEC) + `UtinniButton btnAdd "Add column"` + `UtinniButton btnCancel "Cancel"` + status `UtinniLabel lblStatus` for validation messages. Layout per UI-SPEC.
      - On `btnAdd.Click`: validate name non-empty (locked copy `Column name must be non-empty.` red); validate name not in existingColumnNames (locked copy `A column named "{name}" already exists. Pick another.` red); construct `DataTableColumnType` from the combo-selected discriminator (e.g. selecting "DT_Enum" populates a default `e()`-empty spec which the user can refine later by editing the column type in the grid context menu — Plan 09-04 ships a basic version; richer type-spec authoring is V2). On success: set `Result.Name` + `Result.ColumnType` + `DialogResult = DialogResult.OK` + `Close()`.
      - Public properties: `Result.Name` get; `Result.ColumnType` get (or two flat properties — planner discretion).
      - Per-call modal lifecycle: `using (var dlg = new FormAddColumnDialog(existingNames)) { if (dlg.ShowDialog(parent) == DialogResult.OK) ... }`. NO hide-not-dispose.
    - FormTypeChangeCascadeDialog.cs + Designer:
      - `namespace TJT.UI.Forms`; `public partial class FormTypeChangeCascadeDialog : UtinniForm`.
      - Constructor `FormTypeChangeCascadeDialog(MutableDataTableColumn col, DataTableColumnType newCt, IList<MutableDataTableCell> affectedCells)` — affectedCells is the subset where NeedsReview == true after the cascade.
      - Designer per UI-SPEC § Destructive `Type-change cascade resolution` row: heading `UtinniLabel` reading `{N} cell(s) need review after changing column "{colName}" to {newType}`; body banner `UtinniLabel` at `Colors.Secondary()` reading `Save is disabled until every cell is resolved. Use Accept mangled, Revert type, or Edit per cell.`; embedded `ThemedDataGridView` (Dock=Fill) listing affected cells (columns: row index / original value / mangled-attempt value / Accept-mangled button / Edit-cell button); footer `Panel` (Dock=Bottom) with `UtinniButton btnDone "Done"` + `UtinniButton btnRevert "Revert type change"`.
      - On `Accept-mangled` row button: set `cell.Value = mangledAttemptValue` (clears NeedsReview); remove the row from the cascade grid; update heading count.
      - On `Edit-cell` row button: close the cascade modal with `DialogResult.None`; the calling form (FormDatatableEditor) puts focus on the offending cell in the main grid; the user edits there; the user reopens the cascade modal manually via the toolbar `Resolve cascade...` button (Plan 09-04 ships this as a separate toolbar button that surfaces while NeedsReviewCount > 0).
      - On `btnDone`: `Close()` (remaining red cells stay flagged; save stays blocked).
      - On `btnRevert`: caller-side Undo the ChangeColumnType command via the controller; `Close()`.
      - Per-call modal lifecycle: `using (var dlg = new FormTypeChangeCascadeDialog(col, newCt, affectedCells)) { dlg.ShowDialog(parent); }`.
    - FormDatatableEditor.cs MODIFICATIONS:
      - In `LoadDocument(DataTableDocument dtDoc, OpenSource src, string name)`: REPLACE the Plan 09-03 staging note with `this.controller = new DatatableEditController(dtDoc.Mutable); this.controller.EditApplied += OnEditApplied;`.
      - `OnEditApplied(object sender, EventArgs e)` (PATTERNS lines 422-432): `gridSurface.RefreshMutable(dtDocument.Mutable); UpdateUndoRedoState(); UpdateDirtyVisuals(); RefreshSaveMenuEnabledState();`.
      - `UpdateUndoRedoState()`: `btnUndo.Enabled = controller?.CanUndo ?? false; btnRedo.Enabled = controller?.CanRedo ?? false;`.
      - `ProcessCmdKey` Ctrl+Z / Ctrl+Y branches REPLACE the Plan 09-03 stubs: `if (controller?.CanUndo == true) controller.Undo();` and the symmetric Redo.
      - **`RefreshSaveMenuEnabledState` extension** (per PATTERNS lines 446-457 + UI-SPEC R-04):
        ```
        bool blockedByCascade = controller != null && controller.NeedsReviewCount > 0;
        string cascadeTooltip = "Resolve " + controller.NeedsReviewCount + " cell(s) that need review before saving.";
        if (miSaveInPlace      != null) { miSaveInPlace.Enabled      &= !blockedByCascade; if (blockedByCascade) miSaveInPlace.ToolTipText      = cascadeTooltip; }
        if (miSaveLooseOverride != null){ miSaveLooseOverride.Enabled &= !blockedByCascade; if (blockedByCascade) miSaveLooseOverride.ToolTipText = cascadeTooltip; }
        if (miSaveAs           != null) { miSaveAs.Enabled           &= !blockedByCascade; if (blockedByCascade) miSaveAs.ToolTipText           = cascadeTooltip; }
        if (miPatchLive        != null) { miPatchLive.Enabled        &= !blockedByCascade; if (blockedByCascade) miPatchLive.ToolTipText        = cascadeTooltip; }
        if (miRepackTre        != null) { miRepackTre.Enabled        &= !blockedByCascade; if (blockedByCascade) miRepackTre.ToolTipText        = cascadeTooltip; }
        btnSave.Enabled = hasDoc && !blockedByCascade;
        ```
        (Plan 09-05 will compose the provenance gates ON TOP of this NeedsReview gate.)
      - **Add row** toolbar click handler: invoke `controller.Apply(DatatableEditCommands.AddRow(dtDocument.Mutable, dtDocument.Mutable.Rows.Count))` (append at end; refine via context-menu insert-at-selected-row in V2).
      - **Add column…** toolbar click handler: `using (var dlg = new FormAddColumnDialog(dtDocument.Mutable.Columns.Select(c => c.Name))) { if (dlg.ShowDialog(this) == DialogResult.OK) controller.Apply(DatatableEditCommands.AddColumn(dtDocument.Mutable, dlg.Result.Name, dlg.Result.ColumnType, dtDocument.Mutable.Columns.Count)); }`.
      - **Grid context menu** (right-click on row header / column header / cell): minimal V1 set per UI-SPEC § Layout (Remove row, Remove column, Move row up/down, Move column left/right, Rename column, Change column type) — each wired to a controller command; Remove row + Remove column + Move column trigger the D-02 safety-net modal (see below).
      - **Change column type** context-menu action: opens a small inline `UtinniComboBox` dialog (or reuse FormAddColumnDialog's type-combo? planner discretion — recommend a tiny inline `FormChangeColumnTypeDialog` adapted from FormFourCcDialog with just the combo + Apply/Cancel; this is a 3rd small modal but the simpler path; ALTERNATIVELY do the type change inline via a header context-menu submenu listing the 10 DT_* types — pick the simpler-to-implement option). After the user picks: `controller.Apply(DatatableEditCommands.ChangeColumnType(col, newCt))`; then check `controller.NeedsReviewCount > 0` → surface FormTypeChangeCascadeDialog. Cascade modal `btnRevert` invokes `controller.Undo()` to roll back the ChangeColumnType.
      - **Column reorder/delete D-02 safety-net** (UI-SPEC assumption #5 — REUSE FormSaveConfirmDialog):
        - On first column reorder OR delete attempt of the session: `using (var dlg = new FormSaveConfirmDialog(heading: "Reorder or delete a column?", body: "This may break runtime consumers that read columns by index. Proceed?", acceptVerb: "Proceed", cancelVerb: "Cancel", showBackupCheckbox: true, backupCheckboxLabel: "Don't ask again this session")) { if (dlg.ShowDialog(this) == ConfirmOutcome.Cancelled) return; if (dlg.BackupRequested) sessionSuppressColumnSafetyNet = true; }`. Then proceed with the controller command.
        - Subsequent reorder/delete in the same session bypass the modal when `sessionSuppressColumnSafetyNet == true`. The session flag is per-form-instance; resets on Hide (which is what UserClosing does in the singleton-form pattern — intentional per UI-SPEC assumption #5).
      - **NeedsReview-resolved cascade button** in toolbar: `UtinniButton btnResolveCascade` visible only when `controller.NeedsReviewCount > 0` (set Visible in `OnEditApplied`); on click: re-opens FormTypeChangeCascadeDialog with the current affected cells (the controller doesn't store the cascade context across reopens — Plan 09-04 stores `lastCascadeContext` as a private field on the form; on revert via Undo, lastCascadeContext is cleared).
    - csproj: 4 new `<Compile Include>` entries (FormAddColumnDialog.cs + .Designer.cs, FormTypeChangeCascadeDialog.cs + .Designer.cs) with the same `<SubType>Form</SubType>` + `<DependentUpon>` pattern Plan 09-03 used for FormDatatableEditor.
    - No new tests in this task — Task 1's DatatableEditControllerTests cover the controller layer; the form-wire integration is covered by Plan 09-07's live-SWG smoke (cascade modal visual verification) + Plan 09-04 Task 1's NeedsReviewCount tally fact (which proves the seam exists for the form to read).
  </behavior>
  <action>
    Create FormAddColumnDialog.cs + .Designer.cs and FormTypeChangeCascadeDialog.cs + .Designer.cs
    under `D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/`. Each .cs file
    gets the 23-line MIT header + Allman + 4-space + `// ToDo` (no colon) + per-call-modal
    lifecycle XML comment per the behavior block. Designer files are hand-written (no .resx per
    memory `feedback_dotnet_build_msbuild_resources`).

    Modify FormDatatableEditor.cs per the behavior block:
    - Instantiate DatatableEditController in LoadDocument.
    - Wire OnEditApplied / UpdateUndoRedoState / extended RefreshSaveMenuEnabledState.
    - Wire Add row + Add column… toolbar click handlers + grid context-menu structural-op handlers
      + ChangeColumnType cascade flow.
    - Wire the D-02 safety-net (reused FormSaveConfirmDialog) for column reorder + column delete.
    - Replace ProcessCmdKey Ctrl+Z/Y stubs with controller calls.
    - Replace the Plan 09-03 `RefreshSaveMenuEnabledState` stub with the NeedsReview gate (Plan
      09-05 will compose the provenance gate on top).

    Update TheJawaToolboxDotNet.csproj: 4 new explicit `<Compile Include>` entries with
    `<SubType>Form</SubType>` (for .cs) and `<DependentUpon>` (for .Designer.cs).

    Cross-repo paired commit per `feedback_utinniplugins_authority`. Commit message: `feat(09):
    ship DatatableEditController + 11 T4 commands + FormAddColumnDialog + FormTypeChangeCascadeDialog
    + cascade save-block (D-04 / R-04)`.
  </action>
  <verify>
    <automated>powershell -Command "& 'D:/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe' '../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj' /p:Configuration=Debug /p:Platform=x86 /v:minimal" 2>&1 | tail -10  # build green; grep -c "FormAddColumnDialog" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs"  # MUST return >= 1 (instantiated in Add column handler); grep -c "FormSaveConfirmDialog" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs"  # MUST return >= 1 (D-02 safety-net reuse); grep -c "NeedsReviewCount" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs"  # MUST return >= 2 (one in RefreshSaveMenuEnabledState, one in btnResolveCascade.Visible); grep -c "controller.Apply" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs"  # MUST return >= 5 (Add row + Add column + RemoveRow + ChangeColumnType + at least one more reorder); dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~Datatable" 2>&1 | tail -10  # full Datatable subsuite green</automated>
  </verify>
  <done>FormAddColumnDialog + FormTypeChangeCascadeDialog exist with documented per-call-modal lifecycle; FormDatatableEditor.cs has the controller wire + Add row + Add column + structural ops + cascade flow + D-02 safety-net + NeedsReview save block; TJT MSBuild Debug|x86 green; full Datatable test subsuite green (no regressions).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → controller | Toolbar / context-menu actions invoke controller commands; commands operate on pure-managed `MutableDataTableDocument`; no live-client coupling |
| modal lifecycle | Per-call modal pattern (`using (var dlg = …)`); default WinForms dispose-on-close is CORRECT for FormAddColumnDialog + FormTypeChangeCascadeDialog (NOT the singleton hide-not-dispose — RESEARCH § Pitfall 6) |
| column-reorder/delete → runtime consumers | Engine consumers reading `getColumnNumber("name")` / `getIntValue(...,N)` are out of scope per CONTEXT D-05 (no engine-consumer scan in V1); D-02 safety-net modal is the V1 user-facing risk mitigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-15 | Tampering (silent schema corruption) | ChangeColumnType cascade | mitigate | D-04 NeedsReview flag + R-04 save-block on every menu item — the user CANNOT save a table with cells in failed-mangle state. Cascade dialog gives Accept-mangled / Edit-cell / Revert affordances. Tested via DatatableEditControllerTests cascade matrix. |
| T-09-16 | Tampering (column reorder breaks runtime consumers) | MoveColumnLeft/Right + RemoveColumn | mitigate (UX) | D-02 safety-net modal surfaces once per session per UI-SPEC assumption #5 (reused FormSaveConfirmDialog with locked CONTEXT D-02 body). No engine-consumer scan in V1 (deferred to V1.5 if reviewers push back per CONTEXT D-02). The "Don't ask again this session" flag is per-editor-instance — resets on Hide (singleton-form intentional). |
| T-09-17 | DoS (undo stack memory) | DatatableEditController.undoStack growing unbounded | accept | V1 editor sessions are bounded by user attention; in practice <1000 ops per session. If profiling shows memory issues, V2 could cap the stack depth (e.g. 500). For V1 accept the unbounded stack. |
| T-09-18 | Tampering (RemoveRow/RemoveColumn snapshot+materialize trap) | RemoveRow.UndoOp + RemoveColumn.UndoOp | mitigate | Insert-by-reference pattern per Phase 8 IffEditController CR-01 — re-attach the SAME row/column instance (and the SAME cell instances for RemoveColumn) so subsequent Do() finds them in the parent collection and removes cleanly. Snapshot+Materialize would re-allocate new objects and break the cell back-refs. xUnit fact in DatatableEditControllerTests asserts row-instance equality after RemoveRow→Undo. |
| T-09-19 | DoS (per-character undo entries) | EditCellValue committing on every keystroke | mitigate | Plan 09-03's grid wires `EditCellValue` to `CellEndEdit` (commit-on-edit-complete), NOT `CellValueChanged` (per-keystroke). One undo entry per cell-edit cycle. Plan 09-04 Task 2 confirms this wire in FormDatatableEditor.cs. |
| T-09-SC | Tampering | npm/pip/cargo installs | N/A | Plan 09-04 adds ZERO new external packages. |

**ASVS controls applied:**
- V5 Input Validation — FormAddColumnDialog validates non-empty + non-duplicate column names per UI-SPEC § Copywriting; ChangeColumnType cascade runs `DataTableColumnType.MangleValue` per cell + flags failures with NeedsReview.
- V12 File and Resources — N/A in this plan (no file I/O; controller mutates in-memory model only).
</threat_model>

<verification>
Plan-level acceptance:

1. `dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~DatatableEditControllerTests"` exits 0 with ≥ 30 [Fact]s; the SC4 controller-layer [Fact] is exercised.
2. Both repos MSBuild Debug|x86 + Release|x86 green; no errors / 0 warnings in the new files.
3. `grep -c "Editing\\\\DatatableEditController" UtinniCoreDotNet/UtinniCoreDotNet.csproj` returns 1.
4. `grep -c "NeedsReviewCount" UtinniCoreDotNet/Editing/DatatableEditController.cs ../UtinniPlugins/The\ Jawa\ Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs` returns ≥ 3 (controller seam + form gate + cascade button visibility).
5. `grep -c "InsertAt\|Insert(originalIndex" UtinniCoreDotNet/Editing/DatatableEditCommands.cs` returns ≥ 2 (RemoveRow + RemoveColumn insert-by-reference pattern).
6. `grep -c "FormSaveConfirmDialog" ../UtinniPlugins/The\ Jawa\ Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs` returns ≥ 1 (D-02 safety-net reuse per UI-SPEC assumption #5).
7. Full UtinniCoreDotNet.Tests + Utinni.Cli.Tests + PreservationAudit suites stay green (no regression of Plans 09-01/02/03 baseline).
</verification>

<success_criteria>
- The DatatableEditController + 11 commands cover the full D-01 T4 schema-mutation scope; xUnit ≥ 30 [Fact]s exercise each command × Apply/Undo/Redo + the D-04 cascade matrix.
- The SC4 structural invariant is enforced at TWO independent layers: direct mutation (Plan 09-01) + controller-mediated (Plan 09-04) + CLI (Plan 09-02). Three automated assertions of the same property.
- R-04 save-block: every Save▾ menu item AND the top-level button disable when `NeedsReviewCount > 0`; the locked tooltip surfaces on EACH menu item per UI-SPEC R-04 — the executor MUST iterate each item, not just the button face.
- Insert-by-reference pattern from Phase 8 CR-01 is ported verbatim for RemoveRow + RemoveColumn UndoOps; xUnit assertion of row-instance equality after Undo prevents the snapshot+materialize regression.
- Plan 09-05 can compose Save▾ provenance gating on top of Plan 09-04's NeedsReview gate without touching the cascade flow.
- Plan 09-06 can ship `DatatableEditCommands.ApplyCsvImport` body without modifying the controller signature (the stub factory is already in place).
</success_criteria>

<output>
Create `.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-04-SUMMARY.md` when done. Record:
- DatatableEditController public API surface (signatures + xUnit fact counts per method).
- Cascade dialog flow diagram (textual — Do cascade → modal opens → Accept-mangled / Edit-cell /
  Done / Revert paths).
- NeedsReview gate flow (form reads NeedsReviewCount → updates 5 save menu items + button —
  Plan 09-05 will compose provenance gating).
- Insert-by-reference fix confirmation (RemoveRow + RemoveColumn UndoOps verified via xUnit).
- Cross-repo paired commit hash for the cross-repo edit.
- Any deviation from the IffEditController port (the controller shape should be a near-verbatim
  port; if any deviation surfaced — e.g. NeedsReviewCount requiring a more sophisticated cache —
  document the rationale for Plan 09-06 + future maintainers).
</output>

## Revision History

- 2026-05-29 iter-1: cascade-context tracking moved from form-local field into DatatableEditController.PendingCascadeContext per checker WARNING #8; added three xUnit facts for the cascade state machine; form-side rewire documented (drop lastCascadeContext field, btnResolveCascade reads controller.PendingCascadeContext).
