---
phase: 09-tjt-subpanel-datatable-editor-tab
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/ThemedDataGridView.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/DatatableColumnFactory.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/DatatableHashStringEditor.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/DatatableNumericUpDownEditingControl.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.Designer.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"
  - UtinniCoreDotNet.Tests/UITests/FormDatatableEditorHideNotDisposeTests.cs
  - UtinniCoreDotNet.Tests/PerfProbes/DataGridViewBindLatencyProbeTests.cs
autonomous: false
requirements: [PROD-W1-DT]
assumes:
  - "Plan 09-01 has shipped `UtinniCoreDotNet/Formats/Datatable/*` (typed reader, mutable hybrid DOM, writer, fixture builder). Plan 09-03 consumes these directly via the existing `UtinniCoreDotNet.dll` HintPath in `TheJawaToolboxDotNet.csproj` (CF-01)."
  - "Cross-repo paired commits per memory `feedback_utinniplugins_authority` — Plan 09-03 modifies both Utinni (2 new test files, both under SDK-style auto-glob test csproj) AND UtinniPlugins (4 controls + 1 form + Designer + Plugin.cs + csproj). Commit atomically; the live-SWG smoke (Plan 09-07) is the only required human-action checkpoint in the phase."
  - "`TheJawaToolboxDotNet.csproj` is OLD-STYLE explicit-compile (no SDK glob — verified via Phase 8 Plan 05 round-2 HIGH-A). Every new `.cs` in `UI/Controls/` and `UI/Forms/` requires an explicit `<Compile Include>` entry; WinForms `.cs` use `<SubType>Form</SubType>` (forms) or no SubType (controls); `.Designer.cs` partials use `<DependentUpon>` pointing to the parent."
  - "No `.resx` for FormDatatableEditor — per memory `feedback_dotnet_build_msbuild_resources` and the Phase 8 precedent (FormIffEditor, FormFourCcDialog, FormSaveConfirmDialog all ship without .resx). All Designer code is hand-written. Build via VS2026 MSBuild; `dotnet test --no-build` for xUnit."
  - "The singleton-form hide-not-dispose pattern is MANDATORY from Task 1 — not deferred to a post-smoke fix. The xUnit guard test (Task 2 last [Fact]) is part of the canonical regression suite from this plan's commit onward (memory `singleton-form-hide-not-dispose`)."
  - "Plan 09-03 ships the host + grid + per-type widgets + Open... entry point ONLY. T4 structural ops (row/col add/remove/reorder, type-change cascade), Save▾ wiring, TRE-Browser hand-off, IFF-Editor hand-off, CSV bulk import, Find/Replace, and column-click sort are subsequent plans (09-04/05/06). The Save▾ button face exists in Task 1's toolbar Designer but the menu items + click handlers are stubs that throw `NotImplementedException` — Plan 09-05 wires them. This staging keeps each plan's context cost tractable."
  - "DataGridView profiling micro-task (Task 3) is gated on RESEARCH Assumption A5 + Pitfall 7. If the measured initial-bind latency for `BuildV1CombatDataTableLike()` is ≤ 100 ms, V1 ships non-virtual and Plan 09-06 does NOT need a `VirtualMode` fallback subtask. If > 100 ms, the SUMMARY flags it for Plan 09-06 attention. Measurement is the decision-branch hinge."
must_haves:
  truths:
    - "A new `FormDatatableEditor` (resizable `UtinniForm`, default 1200×760, min 900×560) loads a `.tab` from disk via toolbar `Open…` and renders rows × columns in a `ThemedDataGridView` with per-type cell widgets (D-03)."
    - "The `ThemedDataGridView` wrapper (in `TJT.UI.Controls`, alongside `IffChunkTree`) applies the UI-SPEC § ThemedDataGridView token map verbatim — all 25+ properties from `Colors.*()` accessors, no raw ARGB literals (Phases 10/11 inherit)."
    - "Per-type cell widget contract per UI-SPEC § Per-type cell widget contract (Assumption A1 enum syntax corrected per Plan 09-01 Task 0): `DT_Int`/`DT_Float` → `UtinniNumericUpDown` editor swap-in via `DataGridView.EditingControlShowing`; `DT_Bool` → `DataGridViewCheckBoxColumn`; `DT_Enum` → `DataGridViewComboBoxColumn` populated from `DataTableColumnType.EnumMap`; `DT_HashString` → text + floating `UtinniLabel` hash preview (Consolas 9pt); `DT_String`/`DT_Comment`/`DT_PackedObjVars`/`DT_BitVector` → text + parse-back validator on commit; `DT_Unknown` → read-only hex render (UI-SPEC R-03)."
    - "Singleton-form hide-not-dispose pattern applied FROM COMMIT 1 — `FormDatatableEditor_FormClosing` intercepts `CloseReason.UserClosing` and `Hide()`s instead of disposing; editor-host shutdown reasons (`ApplicationExitCall` / `TaskManagerClosing` / `WindowsShutDown`) fall through normally. An xUnit guard test constructs the form, fires `OnFormClosing(UserClosing)`, asserts `!IsDisposed && !Visible` (RESEARCH § Pitfall 5; memory `singleton-form-hide-not-dispose`)."
    - "`FormDatatableEditor` is registered in `Plugin.cs` `GetForms()` inside its own try/catch isolation block (MEF SPI unchanged; the new entry mirrors the Phase 8 `FormIffEditor` registration verbatim per PATTERNS § MODIFIED Plugin.cs row)."
    - "Settings persistence via `UtINI` — `[DatatableEditor]` section with `width`, `height`, `splitterDistance`, `findReplaceVisible`, `editCommentRows`, `looseOverrideDir` keys (mirrors Phase 8 `FormIffEditor.CreateSettings`)."
    - "DataGridView initial-bind latency measured on `BuildV1CombatDataTableLike()` (~200 rows × ~30 cols); measurement recorded in plan SUMMARY; if > 100 ms, Plan 09-06 inherits a VirtualMode-fallback subtask; if ≤ 100 ms, the V1 non-virtual-mode commitment from UI-SPEC § Grid surface is confirmed."
    - "D-05 boundary (V1 anti-scope) — `FormDatatableEditor` is single-document-per-window: `LoadDocument(DataTableDocument, OpenSource, string)` replaces the open document; no table-corpus subsystem maintains related `.tab` / hashtable files. Open-replace honors a dirty-discard prompt (`FormSaveConfirmDialog` reuse, wired in Plan 09-04). Restated as a truth so the V2 boundary is enforced at execution time."
    - "D-06 boundary (V1 anti-scope) — `DatatableHashStringEditor` floats a hash preview computed via `DataTableHashCrc.Compute` only; it does NOT open/parse the referenced hashtable, validate that the hashed value resolves, or surface dangling-FK warnings. `DT_Int` columns have NO FK awareness whatsoever. Logical-FK dropdown pickers + engine-canonical FK maps are deferred to V2 (matches SOE `SwgDataTableTool` parity)."
  artifacts:
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/ThemedDataGridView.cs"
      provides: "TJT-side themed wrapper around `System.Windows.Forms.DataGridView` applying the UI-SPEC § ThemedDataGridView token map in its constructor; Phases 10/11 inherit"
      contains: "ThemedDataGridView"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/DatatableColumnFactory.cs"
      provides: "Static factory mapping `DataTableColumnType.Type` → concrete `DataGridViewColumn` subclass per UI-SPEC § Per-type cell widget contract"
      contains: "Build"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/DatatableHashStringEditor.cs"
      provides: "Floating `UtinniLabel` hash preview (Consolas 9pt, `Colors.FontDisabled()`) anchored to a `DT_HashString` cell on `CellBeginEdit`; disposed on `CellEndEdit`; live recompute via `DataTableHashCrc.Compute`"
      contains: "Compute"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/DatatableNumericUpDownEditingControl.cs"
      provides: "`UtinniNumericUpDown` adapted to `IDataGridViewEditingControl` for `DT_Int` / `DT_Float` cell-editor swap-in via `EditingControlShowing`"
      contains: "IDataGridViewEditingControl"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs"
      provides: "Singleton `UtinniForm` editor host; hide-not-dispose intercept from commit 1; LoadDocument(DataTableDocument, OpenSource, string); ProcessCmdKey (Ctrl+Z/Y/S — Find/Replace deferred to Plan 09-06); CreateSettings; BuildSaveMenu STUB (items disabled-throw, wired in 09-05); RefreshSaveMenuEnabledState STUB (returns provenance-gated false for all items; 09-04 + 09-05 wire); UpdateDirtyVisuals; reload-status badge with CF-05 locked copy"
      contains: "FormDatatableEditor"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.Designer.cs"
      provides: "Hand-written Designer; Controls.Add order per CF-09 (gridSurface added FIRST = Fill front-most, then pnlStatus Bottom, then pnlFindReplace Top hidden-by-default, then toolbar Top added LAST = outermost)"
      contains: "this.Controls.Add(this.gridSurface)"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs"
      provides: "MEF plugin entry; new try/catch block adds `new FormDatatableEditor(this)` to the GetForms() list (mirrors Phase 8 FormIffEditor registration verbatim)"
      contains: "FormDatatableEditor"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"
      provides: "Old-style explicit-compile csproj — MUST list the 4 new Control `.cs` files + the new Form `.cs` + `.Designer.cs` partial via `<Compile Include>` (round-2 HIGH-A); Form entry uses `<SubType>Form</SubType>`; Designer uses `<DependentUpon>FormDatatableEditor.cs</DependentUpon>`"
      contains: "FormDatatableEditor.cs"
    - path: "UtinniCoreDotNet.Tests/UITests/FormDatatableEditorHideNotDisposeTests.cs"
      provides: "Singleton-form regression guard: constructs FormDatatableEditor in a STA-thread fixture, fires OnFormClosing(UserClosing), asserts !IsDisposed && !Visible (Pitfall 5)"
      contains: "FormDatatableEditorHideNotDisposeTests"
    - path: "UtinniCoreDotNet.Tests/PerfProbes/DataGridViewBindLatencyProbeTests.cs"
      provides: "Performance probe: instantiates DataGridView, binds BuildV1CombatDataTableLike()-loaded DataTableDocument, measures elapsed; records into Trace; not a fail-on-threshold test (the threshold gates Plan 09-06's VirtualMode decision, not this Fact's pass/fail). Result captured in plan SUMMARY."
      contains: "DataGridViewBindLatencyProbeTests"
  key_links:
    - from: "FormDatatableEditor.LoadDocument(DataTableDocument, OpenSource, string)"
      to: "ThemedDataGridView.BindMutable(MutableDataTableDocument)"
      via: "host wires the typed model to the grid surface; per-column DataGridViewColumn built via DatatableColumnFactory.Build"
      pattern: "BindMutable"
    - from: "Plugin.cs GetForms()"
      to: "new FormDatatableEditor(this)"
      via: "MEF singleton registration inside try/catch isolation block (same as FormIffEditor)"
      pattern: "FormDatatableEditor"
    - from: "FormDatatableEditor_FormClosing"
      to: "Hide() instead of Dispose on CloseReason.UserClosing"
      via: "singleton-form pattern from Phase 8 commit b899504"
      pattern: "CloseReason\\.UserClosing"
    - from: "DatatableColumnFactory.Build(DT_Int|DT_Float)"
      to: "EditingControlShowing → DatatableNumericUpDownEditingControl"
      via: "DataGridView per-edit editor swap-in"
      pattern: "EditingControlShowing"
    - from: "DatatableHashStringEditor"
      to: "DataTableHashCrc.Compute (framework)"
      via: "live hash preview on every keystroke during DT_HashString cell edit"
      pattern: "DataTableHashCrc"
---

## Execution Notes (iter-1 fix #4)

- **Task 3 (DataGridView bind-latency probe) deferral signal:** If Task 3 is deferred under context pressure, the executor MUST record the deferral in 09-03-SUMMARY.md. The deferral signal Plan 09-06 watches is: 09-03-SUMMARY.md does NOT contain a parseable `DataGridViewBindLatency` measurement line. When that signal fires, Plan 09-06 Task 4 (conditional VirtualMode fallback) treats the missing measurement as **ABSENT** and proceeds with the VirtualMode build **unconditionally** (the safe default; never silently skip). See Plan 09-06 Task 4 PRE-FLIGHT for the corresponding consumer-side fix.
- **Task 3 acceptance hardening (iter-1 fix #6):** Task 3 `<done>` clause is extended with an explicit acceptance grep: `grep -E "DataGridViewBindLatency.*[0-9]+\.[0-9]+ ms" 09-03-SUMMARY.md` MUST return at least 1 line. This forces the executor to write the measurement into SUMMARY rather than only emitting it to stdout.

<objective>
Ship the TJT-side **host + grid + per-type cell widgets** for Phase 9. This plan introduces the
`FormDatatableEditor` singleton form (mirrors `FormIffEditor` pattern verbatim per
RESEARCH § Code Examples + PATTERNS § File Classification), the `ThemedDataGridView` TJT-side
themed wrapper (Phases 10/11 inherit), per-type `DataGridViewColumn` factory + the two custom
edit controls (`DatatableHashStringEditor` floating hash preview + `DatatableNumericUpDownEditingControl`
NumericUpDown swap-in), and the `Plugin.cs` MEF registration.

The plan applies the **singleton-form hide-not-dispose pattern from commit 1** (RESEARCH § Pitfall 5
— canonical Phase 8 smoke-discovered defect class), ships its xUnit regression guard, ships the
DataGridView initial-bind latency profiling probe (RESEARCH Assumption A5 — the measurement gates
Plan 09-06's VirtualMode-fallback decision), and wires the `Open…` toolbar entry point only. Save▾,
T4 structural ops, TRE-Browser hand-off, IFF-Editor hand-off, CSV import, Find/Replace, and
column-click sort are subsequent plans (09-04/05/06).

**Why this decomposition (Wave 2, single-plan):** The host + grid surface is the largest single
coherent slice in the phase (~9 new TJT files + 2 test files), all tightly coupled at the
DataGridView-binding seam. Splitting it would force one plan to ship the form without the grid or
vice versa — both lose coherence. Keeping it as a single plan in its own wave lets Wave 3's two
parallel plans (09-04 controller + ops; 09-05 entry points + save targets) both depend on a stable
form-host API. The plan's context cost is at the high end of the ~50% budget; the profiling
micro-task is intentionally last so it can be skipped if context pressure hits.

**Cross-repo paired commits:** Tasks 1+2 commit to Utinni (UtinniCoreDotNet.Tests/UITests + PerfProbes)
AND UtinniPlugins (4 controls + 1 form + Designer + Plugin.cs + csproj) atomically. The live-SWG
smoke verification is Plan 09-07's concern — this plan ships everything CI-verifiable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-CONTEXT.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-RESEARCH.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-PATTERNS.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-VALIDATION.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-UI-SPEC.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-01-PLAN.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-05-SUMMARY.md

# Phase 8 host pattern — port VERBATIM with DataGridView replacing IffChunkTree
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.Designer.cs

# Phase 7 themed control TJT-side analog — namespace + UserControl pattern ThemedDataGridView mirrors
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/IffChunkTree.cs

# Plugin registration analog — exact 7-line try/catch isolation block Plan 09-03 mirrors
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs

# Existing csproj for the round-2 HIGH-A explicit-compile gates
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj

# Themed control suite Plan 09-03 reuses (already shipped)
@UtinniCoreDotNet/UI/Controls/UtinniNumericUpDown.cs
@UtinniCoreDotNet/UI/Controls/UtinniLabel.cs
@UtinniCoreDotNet/UI/Controls/UtinniButton.cs
@UtinniCoreDotNet/UI/Controls/UtinniContextMenuStrip.cs
@UtinniCoreDotNet/UI/Controls/UtinniForm.cs
@UtinniCoreDotNet/UI/Theme/Colors.cs

# Plan 09-01 framework primitives — public API to consume
@UtinniCoreDotNet/Formats/Datatable/DataTableDocument.cs
@UtinniCoreDotNet/Formats/Datatable/MutableDataTableDocument.cs
@UtinniCoreDotNet/Formats/Datatable/DataTableColumnType.cs
@UtinniCoreDotNet/Formats/Datatable/DataTableHashCrc.cs

<interfaces>
<!-- Phase 8 host public API that Plan 09-03's FormDatatableEditor mirrors -->

From FormIffEditor.cs (the verbatim port shape, key method signatures):
- public partial class FormIffEditor : UtinniForm, IEditorForm
- public FormIffEditor(IPlugin plugin)
- public void LoadDocument(MutableIffDocument document, OpenSource source, string displayName)   // Plan 09-03 → LoadDocument(DataTableDocument, OpenSource, string)
- public void OpenFromTreEntry(byte[] payload, string resolvedArchivePath, string logicalPath, long archiveLocalOffset)   // Plan 09-05 mirrors this signature
- protected override bool ProcessCmdKey(ref Message msg, Keys keyData)   // Ctrl+Z/Y/S intercept; Plan 09-03 ships these; Find/Replace bindings deferred to 09-06
- private void FormIffEditor_FormClosing(object sender, FormClosingEventArgs e)   // hide-not-dispose intercept; Plan 09-03 ports VERBATIM with form name swap
- private void CreateSettings()                                                   // [IffEditor] → [DatatableEditor] settings.ini section
- private void BuildSaveMenu()                                                    // Plan 09-03 ships STUB (items disabled-throw); 09-05 wires them
- private void RefreshSaveMenuEnabledState()                                      // Plan 09-03 ships STUB (all items disabled); 09-05 wires provenance + 09-04 adds the NeedsReviewCount gate
- private void UpdateDirtyVisuals()                                               // ● glyph + lblDirty; Plan 09-03 ships this for the dirty-on-edit visual contract (controller hook is 09-04)

From IffChunkTree.cs (the namespace + UserControl-with-themed-WinForms-control pattern):
- namespace TJT.UI.Controls
- public partial class IffChunkTree : UserControl

From UtinniCoreDotNet.UI.Controls.UtinniNumericUpDown.cs (the BCL NumericUpDown subclass Plan 09-03 wraps in IDataGridViewEditingControl):
- public sealed class UtinniNumericUpDown : NumericUpDown
- // Theme colors already applied in ctor

From UtinniCoreDotNet.PluginFramework (CON-M-01/02):
- public interface IEditorForm : IDisposable
- public interface IPlugin

From Plan 09-01 framework primitives:
- public sealed class DataTableDocument
  - public static DataTableDocument FromIff(MutableIffDocument)
  - public MutableDataTableDocument Mutable { get; }
- public sealed class MutableDataTableDocument
  - public string Version { get; }
  - public IList<MutableDataTableColumn> Columns { get; }
  - public IList<MutableDataTableRow> Rows { get; }
- public sealed class DataTableColumnType
  - public DataType Type { get; }
  - public DataType BasicType { get; }
  - public IReadOnlyDictionary<string,int> EnumMap { get; }
  - public string TypeSpec { get; }
- public static class DataTableHashCrc
  - public static uint Compute(string input)
</interfaces>

# UI-SPEC sections Plan 09-03 IMPLEMENTS (all already approved 2026-05-28 + amended Plan 09-01 Task 0)
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship ThemedDataGridView + DatatableColumnFactory + DatatableHashStringEditor + DatatableNumericUpDownEditingControl (TJT controls)</name>
  <files>../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/ThemedDataGridView.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/DatatableColumnFactory.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/DatatableHashStringEditor.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/DatatableNumericUpDownEditingControl.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj</files>
  <read_first>
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-UI-SPEC.md § ThemedDataGridView token map (25+ DataGridView properties + their Colors.*() values) AND § Per-type cell widget contract (10 DT_* → widget mapping) AND § Cell-state visual overlays (dirty / added / needs-review / search-match BackColor/ForeColor rules)
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-PATTERNS.md § "NEW — TJT controls" rows for all four control files (analog mapping + match-quality column) AND § Pattern Assignments → ThemedDataGridView (line 519-538 of PATTERNS) AND § "No Analog Found" rows for DatatableHashStringEditor + DatatableNumericUpDownEditingControl (BCL pattern, no in-repo analog)
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-RESEARCH.md § Pattern 3 (DatatableColumnFactory.Build inline at lines 425-458) AND § Anti-Patterns (NOT hand-painting the grid)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/IffChunkTree.cs (full file — namespace TJT.UI.Controls + UserControl-with-themed-WinForms-control pattern; ThemedDataGridView's ctor mirrors this idiom but wraps DataGridView not TreeView)
    - UtinniCoreDotNet/UI/Controls/UtinniNumericUpDown.cs (the existing themed NumericUpDown; DatatableNumericUpDownEditingControl inherits + implements `IDataGridViewEditingControl` per BCL convention)
    - UtinniCoreDotNet/UI/Controls/UtinniLabel.cs (the themed Label; DatatableHashStringEditor floats one anchored to the active edit cell)
    - UtinniCoreDotNet/UI/Theme/Colors.cs (full file — every color accessor used by the token map: Primary, PrimaryHighlight, PrimaryShadow, Secondary, Font, FontDisabled, ControlBorder)
    - UtinniCoreDotNet/Formats/Datatable/DataTableColumnType.cs (Plan 09-01 — Type/BasicType/EnumMap/TypeSpec — the switch in DatatableColumnFactory.Build)
    - UtinniCoreDotNet/Formats/Datatable/DataTableHashCrc.cs (Plan 09-01 — Compute(string) for the live hash preview in DatatableHashStringEditor)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj (the existing <Compile Include> block — old-style explicit-compile per round-2 HIGH-A; insert 4 new entries alphabetically under UI\Controls\)
  </read_first>
  <behavior>
    - ThemedDataGridView:
      - `namespace TJT.UI.Controls`; `public partial class ThemedDataGridView : DataGridView`.
      - Constructor applies the UI-SPEC § ThemedDataGridView token map verbatim. ~25 property assignments; all values come from `Colors.*()` (e.g. `BackgroundColor = Colors.PrimaryHighlight()`, `GridColor = Colors.ControlBorder()`, `ColumnHeadersDefaultCellStyle.BackColor = Colors.PrimaryShadow()`, etc.). NO `Color.FromArgb` literals.
      - Public method `BindMutable(MutableDataTableDocument doc, IReadOnlyList<DataGridViewColumn> columns)` — Columns.Clear → Columns.AddRange(columns) → RowCount = doc.Rows.Count → triggers `CellValueNeeded`/`CellFormatting` as the rebinding proceeds. (Note: Plan 09-03 ships **non-virtual** binding by default per UI-SPEC § Grid surface; if Task 3 profiling shows > 100 ms latency, Plan 09-06 introduces VirtualMode.)
      - Public method `RefreshMutable(MutableDataTableDocument doc)` — `InvalidateRow(int)` or full `Invalidate()` on cell value changes; called from `FormDatatableEditor.OnEditApplied` in Plan 09-04.
      - `CellFormatting` handler painting the cell-state overlays per UI-SPEC § Cell-state visual overlays (dirty ForeColor = Secondary, added row-header ＋ glyph, needs-review BackColor = Color.Red, frozen DT_Comment row visuals). Search-match overlay (Plan 09-06) and frozen-row toggle (Plan 09-06) are deferred — the formatter has hooks but does nothing for those states until 09-06 wires them.
    - DatatableColumnFactory:
      - `namespace TJT.UI.Controls`; `public static class DatatableColumnFactory`.
      - `public static DataGridViewColumn Build(string columnName, DataTableColumnType ct)` per RESEARCH § Pattern 3 lines 425-458 — switch on `ct.Type`:
        - `DT_Bool` → `DataGridViewCheckBoxColumn`
        - `DT_Enum` → `DataGridViewComboBoxColumn` with `Items.AddRange(ct.EnumMap.Keys.ToArray())`
        - `DT_Int` / `DT_Float` → `DataGridViewTextBoxColumn` (the actual editor swap-in to UtinniNumericUpDown happens via `FormDatatableEditor`'s `EditingControlShowing` handler)
        - `DT_Comment` → `DataGridViewTextBoxColumn` (frozen-header visual is in `CellFormatting`, not column type)
        - `DT_HashString` → `DataGridViewTextBoxColumn` (floating hash preview via `DatatableHashStringEditor` is wired in FormDatatableEditor's `CellBeginEdit`/`CellEndEdit` handlers)
        - `DT_String` / `DT_PackedObjVars` / `DT_BitVector` → `DataGridViewTextBoxColumn`
        - `DT_Unknown` (UI-SPEC R-03) → `DataGridViewTextBoxColumn` with `ReadOnly = true`, header tooltip "Unknown column type — read-only"
      - For DT_PackedObjVars / DT_BitVector: set the column's `ToolTipText` to the format syntax hint per UI-SPEC § Per-type cell widget contract Copywriting row.
    - DatatableHashStringEditor:
      - `namespace TJT.UI.Controls`; `public sealed class DatatableHashStringEditor : IDisposable`.
      - Constructor takes a `DataGridView` host + a `Control` parent + the cell rect; creates a `UtinniLabel` (Consolas 9pt, `Colors.FontDisabled()`, format `→ 0x{hash:X8}`) anchored to the right of the cell rect (or below if no horizontal room); positions the label as a sibling of the grid.
      - `Update(string text)` method — called on every `CellEditingControl` text change (subscribe in FormDatatableEditor's `CellBeginEdit`); computes `DataTableHashCrc.Compute(text)`; updates label text.
      - `Dispose()` removes the label from its parent and disposes it.
      - Floats are managed by the host (FormDatatableEditor's `CellBeginEdit` creates the editor; `CellEndEdit` disposes it).
    - DatatableNumericUpDownEditingControl:
      - `namespace TJT.UI.Controls`; `public sealed class DatatableNumericUpDownEditingControl : UtinniNumericUpDown, IDataGridViewEditingControl`.
      - Implements every member of `IDataGridViewEditingControl` per BCL convention: `EditingControlDataGridView`, `EditingControlFormattedValue`, `EditingControlValueChanged`, `EditingPanelCursor`, `EditingControlRowIndex`, `EditingControlWantsInputKey`, `ApplyCellStyleToEditingControl`, `PrepareEditingControlForEdit`, `RepositionEditingControlOnValueChange`.
      - `Minimum` / `Maximum` / `DecimalPlaces` / `Increment` set by FormDatatableEditor's `EditingControlShowing` handler based on `DataTableColumnType.Type` (DT_Int → DecimalPlaces=0, Min=int.MinValue, Max=int.MaxValue; DT_Float → DecimalPlaces=6, Min=float.MinValue, Max=float.MaxValue).
    - csproj: 4 new `<Compile Include="UI\Controls\Themed\DataGridView.cs">` etc. entries inserted alphabetically within the existing UI\Controls\ block — round-2 HIGH-A per Phase 8.
  </behavior>
  <action>
    Create the four TJT control files under `D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/`.
    Each file gets the 23-line MIT header (copy verbatim from an existing TJT file like
    `IffChunkTree.cs`), Allman braces, 4-space indent, `// ToDo` (no colon) marker convention.
    Namespace `TJT.UI.Controls` (matches `IffChunkTree` placement).

    `ThemedDataGridView.cs` — apply the UI-SPEC § ThemedDataGridView token map verbatim in the
    constructor; do NOT condense or paraphrase the 25-property block (every line traces back to a
    UI-SPEC row); include an XML `<remarks>` block citing UI-SPEC § ThemedDataGridView token map +
    UI-SPEC § Cell-state visual overlays for the CellFormatting handler.

    `DatatableColumnFactory.cs` — implement `Build(columnName, ct)` per the behavior block;
    XML comment cites RESEARCH § Pattern 3 lines 425-458 + UI-SPEC § Per-type cell widget contract.

    `DatatableHashStringEditor.cs` — minimal float-overlay pattern with `IDisposable`; XML comment
    notes that this is a NEW pattern (no in-repo analog) and UI-SPEC § Per-type cell widget
    contract row `DT_HashString` is the authoritative spec.

    **Deterministic floating-label placement policy (iter-1 fix #7):** Anchor the floating `UtinniLabel`
    to the RIGHT of the editing `cellRect` by default with a 4px gap (so `label.Left = cellRect.Right + 4`).
    If `cellRect.Right + labelWidth + 4 > gridSurface.ClientRectangle.Right`, anchor BELOW the cell
    rect with a 2px gap (`label.Top = cellRect.Bottom + 2`). Clip `label.Left` to
    `Math.Min(label.Left, gridSurface.ClientRectangle.Right - labelWidth - 4)` to keep the label
    visible. Never anchor LEFT or ABOVE the cell. Z-order: `label.BringToFront()` so it overlays
    cell-state overlays. Record this policy verbatim in the file XML <remarks> block.

    `DatatableNumericUpDownEditingControl.cs` — implement `IDataGridViewEditingControl` interface
    on a `UtinniNumericUpDown` subclass; the BCL pattern is well-documented; XML comment cites the
    BCL interface contract.

    Update `TheJawaToolboxDotNet.csproj`: add 4 explicit `<Compile Include="UI\Controls\..." />`
    entries, alphabetically positioned in the existing UI\Controls\ block. No `<SubType>` entries
    needed (these are controls, not Forms). No `.resx` files for any of them.

    No new tests in this task — Task 2 ships the FormDatatableEditor host + the hide-not-dispose
    regression test that exercises the full integration. The control units are exercised via the
    host integration (tests for ThemedDataGridView in isolation are deferred — TJT-side test
    project doesn't exist for V1; the framework-side tests cover the data layer).
  </action>
  <verify>
    <automated>powershell -Command "& 'D:/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe' '../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj' /p:Configuration=Debug /p:Platform=x86 /v:minimal" 2>&1 | tail -10  # MUST report build succeeded with 0 errors; grep -c "UI\\\\Controls\\\\ThemedDataGridView.cs" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"  # MUST return 1; grep -c "Color.FromArgb" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/ThemedDataGridView.cs"  # MUST return 0 (no raw ARGB literals per UI-SPEC)</automated>
  </verify>
  <done>Four new TJT control files exist with the documented public APIs; csproj has explicit Compile entries for all four; TheJawaToolboxDotNet.csproj builds clean Debug|x86 via VS2026 MSBuild; no raw `Color.FromArgb` literals slipped into ThemedDataGridView.cs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship FormDatatableEditor + Designer + Plugin.cs registration + xUnit hide-not-dispose regression guard (singleton-form pattern from commit 1)</name>
  <files>../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.Designer.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj, UtinniCoreDotNet.Tests/UITests/FormDatatableEditorHideNotDisposeTests.cs</files>
  <read_first>
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs (FULL FILE — the verbatim port shape; specifically lines 131 ctor, 205-232 LoadDocument, 234-242 CreateSettings, 246-260 SetTitle, 271-300 ProcessCmdKey, 306-365 OnEditApplied + UpdateDirtyVisuals, 880-928 BuildSaveMenu, 933-1019 RefreshSaveMenuEnabledState, 1416-1460 OpenFromTreEntry, 1464-1495 OnReloadClicked, 1569-1596 FormClosing hide-not-dispose intercept)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.Designer.cs (full file — the Controls.Add ORDER per CF-09 / winforms-dockfill-zorder memory: splitContainer Fill added FIRST → pnlStatus Bottom → toolbar Top added LAST. Plan 09-03 ports: gridSurface Fill FIRST → pnlStatus Bottom → pnlFindReplace Top hidden → toolbar Top LAST)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs lines 60-82 (the existing try/catch-isolated GetForms() entries for FormTreBrowser + FormIffEditor; Plan 09-03 inserts a third identical block for FormDatatableEditor)
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-UI-SPEC.md § Layout Contract (Window regions top→bottom: toolbar 28px + Find/Replace pane 32px hidden + grid Fill + status strip 22px) AND § Add-order rules (the literal Controls.Add sequence) AND § Copywriting (every UI string Plan 09-03 places into Designer)
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-PATTERNS.md § File Classification → FormDatatableEditor row (analog `FormIffEditor.cs` lines 131, 205-232, 234-242, 246-260, 271-300, 306-365, 880-1019, 1410-1495, 1569-1596) AND § Pattern Assignments → FormDatatableEditor (lines 369-503 of PATTERNS — verbatim member-by-member port reference)
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-RESEARCH.md § Pitfall 5 (hide-not-dispose pattern; the canonical Phase 8 smoke-discovered defect class) AND § Pitfall 6 (per-call modal lifecycle is DIFFERENT — do NOT apply hide-not-dispose to modals; documented for awareness even though this plan doesn't ship any modals)
    - UtinniCoreDotNet/UI/Controls/UtinniForm.cs (the base class — public methods including Show / Activate / nameFont; the singleton-form intercept is at the FormClosing event level)
    - UtinniCoreDotNet/Utility/UtINI.cs (the settings helper — AddSetting, GetInt, GetBool, GetString APIs Plan 09-03's CreateSettings consumes)
    - UtinniCoreDotNet/PluginFramework/IEditorPlugin.cs + IEditorForm.cs (MEF SPI; FormDatatableEditor implements IEditorForm — CON-M-01/02)
    - UtinniCoreDotNet/UI/Theme/Colors.cs (color accessors used in Designer code-behind)
    - memory: singleton-form-hide-not-dispose (the rationale: Phase 8 hit this defect TWICE during live-SWG smoke — FormIffEditor commit b899504 + FormTreBrowser defensive commit ce2a0a4)
  </read_first>
  <behavior>
    - FormDatatableEditor (port FormIffEditor verbatim with these specific deltas):
      - `namespace TJT.UI.Forms`; `public partial class FormDatatableEditor : UtinniForm, IEditorForm`.
      - Constructor `FormDatatableEditor(IPlugin plugin)` — calls `InitializeComponent()`, calls `CreateSettings()`, loads `[DatatableEditor]` ini values, applies window size from `width`/`height` keys, subscribes `FormClosing` handler.
      - `LoadDocument(DataTableDocument dtDoc, OpenSource source, string displayName)` per PATTERNS lines 401-419 (verbatim shape, type swap):
        - guards, stores `dtDocument` / `source` / `displayName`, sets `rootTypeId = "DTII"` always, calls `gridSurface.BindMutable(dtDoc.Mutable, columns)` (where `columns` = `dtDoc.Mutable.Columns.Select(c => DatatableColumnFactory.Build(c.Name, c.ColumnType))`), enables Save button, calls `UpdateDirtyVisuals()`, `RefreshSaveMenuEnabledState()`, `RefreshReloadButtonState()`.
        - **In Plan 09-03 the `controller` is NOT yet constructed** — Plan 09-04 ships `DatatableEditController`. For now, `LoadDocument` sets up the read-only view; cell edits via the grid commit to the model directly via Plan 09-01's `MutableDataTableCell.Value` setter (the IsDirty bit fires, but there's no undo until 09-04). Document this staging explicitly in the XML comment: "Until Plan 09-04 ships DatatableEditController, cell edits commit to the model directly without undo/redo — that is the deliberate staging."
      - `ProcessCmdKey` (PATTERNS lines 388-399): handle Ctrl+S (default save fallback — Plan 09-05 wires the actual targets), Ctrl+Z and Ctrl+Y — **stubbed to no-op in Plan 09-03 with a TODO XML comment pointing to Plan 09-04** (controller not yet shipped). Ctrl+F / Ctrl+H / F3 / Shift+F3 deferred to Plan 09-06.
      - `CreateSettings` (PATTERNS lines 492-502): `[DatatableEditor]` section, six keys per UI-SPEC § Host Placement settings persistence row.
      - `BuildSaveMenu` (PATTERNS lines 446-457 — VERBATIM port of FormIffEditor.BuildSaveMenu): same five `ToolStripMenuItem`s (`Save (in place)` / `Save as loose override` / `Save As…` / `Patch live client (in memory)` / `Repack into source .tre…`) with the inherited disabled tooltips from UI-SPEC § Copywriting. **All five click handlers throw `NotImplementedException("Wired by Plan 09-05")` in Plan 09-03** — the menu shape exists so the toolbar renders correctly; behavior arrives in 09-05.
      - `RefreshSaveMenuEnabledState` — STUB in Plan 09-03: all items disabled with the locked tooltip from UI-SPEC. Plan 09-05 wires the provenance pattern-match against `Source`; Plan 09-04 adds the `NeedsReviewCount > 0` blocking gate.
      - `UpdateDirtyVisuals` (PATTERNS lines 434-443): `lblDirty.Text = (controller?.IsDirty ?? false) ? "Unsaved changes" : ""`; `SetTitle(controller?.IsDirty ?? false ? "●" : null)`. In Plan 09-03 `controller` is null → always clean visuals. Plan 09-04 instantiates the controller and the visuals come alive.
      - `OnReloadClicked` (Plan 09-03 ships the CF-05 locked-copy version per PATTERNS lines 479-489):
        - `lblStatus.Text = "Datatables re-resolve on the next scene change. Trigger one via TJT's chat-command load."`
        - `lblStatus.ForeColor = Colors.Font()`.
        - Optional 1s `Colors.Secondary()` accent-pulse on `lblReloadBadge` via `System.Windows.Forms.Timer`.
        - **DO NOT call any reload binding** — RESEARCH § Anti-Patterns Pitfall 10 + CF-05 lock.
      - `lblReloadBadge.Text` LOCKED to UI-SPEC copy: `"Reloads on next scene change."` set in Designer.
      - **`FormDatatableEditor_FormClosing` (THE CRITICAL HIDE-NOT-DISPOSE INTERCEPT, MANDATORY FROM COMMIT 1):**
        ```
        try { TrySaveSettings(); } catch { /* best-effort */ }
        if (e.CloseReason == CloseReason.UserClosing) { e.Cancel = true; Hide(); return; }
        // Editor-host shutdown → fall through, dispose normally.
        ```
        Port verbatim from PATTERNS lines 374-384 / FormIffEditor.cs lines 1569-1596.
      - `IEditorForm.Dispose()` and finalization via `partial` Designer file's standard generated `Dispose(bool)` — same as FormIffEditor; do NOT add custom finalization.
    - FormDatatableEditor.Designer.cs (hand-written, no .resx per memory `feedback_dotnet_build_msbuild_resources`):
      - `InitializeComponent()` constructs: `toolbar` (FlowLayoutPanel/Panel Dock=Top, Height=28); left-cluster buttons (`Open…`, `Save▾` split-button, separator, `Undo`, `Redo`, separator, `Add row`, `Add column…`, separator, `Import CSV…`, `Export CSV…`, separator, `Find`, `Replace`, separator, `Reload in client`); right-cluster (`lblReloadBadge` w=220, `lblDirty` w=140); `pnlFindReplace` (Dock=Top, Height=32, Visible=false initially); `gridSurface` of type `ThemedDataGridView` (Dock=Fill); `pnlStatus` (Dock=Bottom, Height=22) containing `lblStatus` (Dock=Fill) + right-aligned `lblCounters` (Width=280).
      - **Controls.Add ORDER per UI-SPEC § Add-order rules (CF-09 / winforms-dockfill-zorder memory):**
        ```
        this.Controls.Add(this.gridSurface);     // Fill FIRST → front-most
        this.Controls.Add(this.pnlStatus);       // Bottom
        this.Controls.Add(this.pnlFindReplace);  // Top (hidden by default)
        this.Controls.Add(this.toolbar);         // Top LAST → outermost
        ```
        This order is NON-NEGOTIABLE per CF-09; the memory `feedback_winforms_dockfill_zorder` documents the symptom of getting it wrong (Fill sent to back starves Top/Bottom siblings).
      - Buttons in `Add row` / `Add column…` / `Import CSV…` / `Export CSV…` / `Find` / `Replace` slots are ENABLED but click handlers throw `NotImplementedException` (wired by subsequent plans) — toolbar renders correctly, just no-ops. Document each in XML.
    - Plugin.cs MODIFICATION (PATTERNS § MODIFIED Plugin.cs row — verbatim 7-line addition):
      - In `GetForms()`, after the existing `try { forms.Add(new FormIffEditor(this)); } catch (Exception ex) { Log.Info(...); }` block, add:
        ```
        try { forms.Add(new FormDatatableEditor(this)); }
        catch (Exception ex) { Log.Info("Failed to create FormDatatableEditor; Datatable Editor will be unavailable: " + ex); }
        ```
      - DO NOT widen `IEditorPlugin` / `IPlugin` (CON-M-01/02 preservation guard-rail); DO NOT touch `GetSubPanels()`.
    - TheJawaToolboxDotNet.csproj round-2 HIGH-A: explicit Compile entries for `UI\Forms\FormDatatableEditor.cs` (with `<SubType>Form</SubType>`) and `UI\Forms\FormDatatableEditor.Designer.cs` (with `<DependentUpon>FormDatatableEditor.cs</DependentUpon>`). Inserted alphabetically in the UI\Forms\ block.
    - FormDatatableEditorHideNotDisposeTests.cs (UtinniCoreDotNet.Tests/UITests/ — NEW directory, SDK-style auto-glob; STA-thread fixture required for WinForms instantiation):
      - `[StaFact]` (or equivalent xUnit STA marker — Phase 8 testing of UI forms uses the pattern `[Fact]` with `STAThreadAttribute` via xUnit Sdk; if not present, hand-roll a Thread(...).SetApartmentState(ApartmentState.STA) wrapper for the test body): construct `new FormDatatableEditor(stubPlugin)`, fire `OnFormClosing(new FormClosingEventArgs(CloseReason.UserClosing, false))` via reflection (the handler is private), assert `!form.IsDisposed && !form.Visible`.
      - Additional [StaFact]: `FormClosing_ApplicationExitCall_FallsThroughAndDisposes` — same pattern, but with `CloseReason.ApplicationExitCall`; asserts `form.IsDisposed` after second close call.
      - Stub `IPlugin` and `IEditorPlugin` implementations via the same pattern Phase 8's UITests (if any exist) used; if not, hand-roll minimal stubs in the test file.
      - Document in XML comment that this is the canonical regression for the Phase-8-smoke-discovered defect class; reference memory `singleton-form-hide-not-dispose`.
  </behavior>
  <action>
    Create `FormDatatableEditor.cs` + `FormDatatableEditor.Designer.cs` under
    `D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/`. Port the entire shape
    of `FormIffEditor.cs` member-by-member per PATTERNS § Pattern Assignments lines 369-503,
    swapping `MutableIffDocument → DataTableDocument`, `IffChunkTree → ThemedDataGridView`,
    `[IffEditor] → [DatatableEditor]` ini section, and applying the Plan 09-03 staging notes (no
    controller until 09-04; Save▾ menu items throw NotImplementedException until 09-05; Find
    pane invisible / no Ctrl+F until 09-06).

    Create `FormDatatableEditor.Designer.cs` (hand-written, no .resx). Apply the UI-SPEC § Layout
    Contract regions verbatim with the CF-09 Controls.Add order. Every UI string traces back to
    UI-SPEC § Copywriting (LOCKED rows MUST match verbatim — `lblReloadBadge.Text =
    "Reloads on next scene change."`, button labels, menu item captions all literal from UI-SPEC).

    Modify `Plugin.cs`: insert the 7-line try/catch block after the FormIffEditor entry, matching
    the existing 7-line pattern exactly (just swapping the form type name in 3 places: the `new
    FormX(this)`, the log string, and any nullable-check shape).

    Modify `TheJawaToolboxDotNet.csproj`: 2 new `<Compile Include>` entries for the Form + Designer
    partials, with the documented `<SubType>` and `<DependentUpon>` per the round-2 HIGH-A pattern.

    Create `UtinniCoreDotNet.Tests/UITests/FormDatatableEditorHideNotDisposeTests.cs` per behavior
    block. SDK-style auto-glob — no csproj edit. The UITests subdirectory may be new; create it.

    Cross-repo paired commit: stage both repos' changes; commit message format `feat(09): ship
    FormDatatableEditor host + grid surface + Plugin.cs registration + hide-not-dispose regression
    guard`; commit to both `D:/Code/Utinni` and `D:/Code/UtinniPlugins` in lock-step per
    `feedback_utinniplugins_authority`.
  </action>
  <verify>
    <automated>powershell -Command "& 'D:/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe' '../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj' /p:Configuration=Debug /p:Platform=x86 /v:minimal" 2>&1 | tail -10  # build green; dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~FormDatatableEditorHideNotDisposeTests" 2>&1 | tail -10  # MUST exit 0 with >= 2 passing facts; grep -c "FormDatatableEditor" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs"  # MUST return >= 1; grep -c "this.Controls.Add(this.gridSurface);" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.Designer.cs"  # MUST return 1; grep -n "this.Controls.Add" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.Designer.cs"  # ORDER must be gridSurface BEFORE pnlStatus BEFORE pnlFindReplace BEFORE toolbar (visual check; reviewer confirms during checkpoint Task 4)</automated>
  </verify>
  <done>FormDatatableEditor.cs + Designer.cs exist; TJT MSBuild Debug|x86 green; Plugin.cs has the 7-line try/catch block; csproj has explicit Compile entries; hide-not-dispose regression test ships green with ≥ 2 [StaFact]s; Controls.Add order matches CF-09 (gridSurface first, toolbar last).</done>
</task>

<task type="auto">
  <name>Task 3: Profile DataGridView initial-bind latency on BuildV1CombatDataTableLike() — record measurement; decision-branch hinge for Plan 09-06</name>
  <files>UtinniCoreDotNet.Tests/PerfProbes/DataGridViewBindLatencyProbeTests.cs</files>
  <read_first>
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-RESEARCH.md § Pitfall 7 (DataGridView performance on hundreds-of-rows tables) AND § Assumptions Log row A5 (DataGridView non-virtual mode is acceptable IF measurement confirms it) AND § Open Questions (none specifically on DataGridView perf — Assumption A5 is the gate)
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-UI-SPEC.md § Grid surface (paragraph "no virtual mode required for V1" — conditional on measurement)
    - .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-PATTERNS.md § Phase-specific Planning Constraints letter I (the orchestrator's letter I) — "Plan 09-03 must include a profiling micro-task: load BuildV1CombatDataTableLike() fixture (~200 rows × ~30 cols), measure initial-bind latency + scroll redraw latency. If > 100 ms initial bind on a typical dev box, plan 09-06 must include a VirtualMode fallback subtask; otherwise document the measured value and proceed with concrete bind."
    - UtinniCoreDotNet.Tests/FormatsTests/Datatable/DatatableFixtures.cs (created by Plan 09-01 — BuildV1CombatDataTableLike() returns ~200 rows × ~30 cols of mixed-type DTII bytes)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/ThemedDataGridView.cs (created Task 1 — BindMutable method the probe invokes)
    - UtinniCoreDotNet/Formats/Datatable/DataTableDocument.cs (FromIff parses the fixture bytes into the typed model)
  </read_first>
  <action>
    Create `UtinniCoreDotNet.Tests/PerfProbes/PerfProbes/DataGridViewBindLatencyProbeTests.cs`. The
    PerfProbes subdirectory is new; create it. SDK-style auto-glob — no csproj edit.

    Implement a single [StaFact] (`STAThread` required for WinForms) `BindLatency_CombatDataTableLike_RecordsMeasurement`:
    1. Load `DatatableFixtures.BuildV1CombatDataTableLike()` via `IffReader.Read +
       MutableIffDocument.FromDocument + DataTableDocument.FromIff` (purely in test code; no need
       to instantiate FormDatatableEditor).
    2. Construct a bare `ThemedDataGridView` (from `TJT.UI.Controls`).
    3. Build the per-column `DataGridViewColumn` list via `DatatableColumnFactory.Build(...)` for
       each column.
    4. Start a `Stopwatch`; call `gridSurface.BindMutable(dtDoc.Mutable, columns)`; stop watch.
    5. `Trace.WriteLine($"DataGridViewBindLatency_CombatDataTableLike: {elapsed.TotalMilliseconds:F2} ms ({dtDoc.Mutable.Rows.Count} rows × {dtDoc.Mutable.Columns.Count} cols)")` so the measurement appears in `dotnet test` console output.
    6. Optionally measure a second pass: `gridSurface.Invalidate()` + force redraw → measure (the scroll-redraw proxy).
    7. **DOES NOT FAIL on threshold** — the test is a measurement probe. It records the value and
       always passes (Assert.True(elapsed.TotalMilliseconds < 10_000) as a sanity floor against
       hangs only).

    Cross-repo dependency: the test references `TJT.UI.Controls.ThemedDataGridView` which lives in
    UtinniPlugins. The test csproj `UtinniCoreDotNet.Tests.csproj` does NOT currently reference
    UtinniPlugins assemblies. **Options:**
    (a) Add a hint-path reference to `TheJawaToolboxDotNet.dll` (cross-repo build dependency in
        tests; precedent in Phase 8?)
    (b) Refactor `ThemedDataGridView` to live framework-side (REJECTED per UI-SPEC Assumption #1
        — TJT-side placement is locked for V1).
    (c) Implement the probe against a plain `System.Windows.Forms.DataGridView` (no theming) +
        document that the theming overhead is negligible (which can be argued — the constructor
        property assignments are one-time; per-cell `CellFormatting` overlays are the run-time
        cost, and those are what `BindMutable` triggers).
    **Recommended path: (c).** The measurement is about BCL `DataGridView` row-binding cost; the
    theming overhead is constant per cell and is what `CellFormatting` triggers anyway. Document
    in the test XML comment that "this probe approximates the live cost; the production form
    cost may add ~5-10% for the ThemedDataGridView CellFormatting overlay paint."

    Record the measured value in the plan SUMMARY explicitly:
    - If ≤ 100 ms: SUMMARY notes "Plan 09-06 does NOT need VirtualMode fallback — measurement
      confirms UI-SPEC § Grid surface non-virtual commitment."
    - If > 100 ms: SUMMARY flags "Plan 09-06 MUST include VirtualMode-fallback subtask per
      RESEARCH Pitfall 7 + Assumption A5 — measured {X} ms exceeds 100 ms threshold."
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~DataGridViewBindLatencyProbeTests" -- xUnit.parallelizeAssembly=false 2>&1 | tail -15  # MUST exit 0; the measurement line MUST appear in stdout (look for "DataGridViewBindLatency_CombatDataTableLike:" prefix); the measured value MUST be recorded in the plan SUMMARY (manual note)</automated>
  </verify>
  <done>Probe test ships in PerfProbes/ subdirectory; runs green; emits the measurement to stdout via Trace; the measured value is recorded in the plan SUMMARY with the Plan 09-06 decision-branch annotation (≤ 100 ms → no VirtualMode; > 100 ms → VirtualMode required). **Acceptance grep (iter-1 fix #6):** `grep -E "DataGridViewBindLatency.*[0-9]+\.[0-9]+ ms" .planning/phases/09-tjt-subpanel-datatable-editor-tab/09-03-SUMMARY.md` MUST return at least 1 line; the executor MUST write the measurement into SUMMARY, not only emit it to stdout.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Maintainer visual sanity check — FormDatatableEditor open path renders in editor host</name>
  <what-built>
    Plan 09-03 has shipped the host + grid + per-type widgets + Plugin.cs registration. The form is
    instantiable; the singleton-form pattern is in place; the toolbar renders with the locked
    UI-SPEC copy; the grid surface accepts a typed DataTableDocument and binds the per-type
    columns. Save▾ menu items throw NotImplementedException (wired in Plan 09-05); T4 structural
    ops (Add row / Add column / Undo / Redo) are deferred to Plan 09-04; Find / Replace / sort /
    CSV deferred to Plan 09-06. The visual sanity check confirms the host renders correctly
    against the editor host without needing live SWG.
  </what-built>
  <how-to-verify>
    1. Build both repos Debug|x86 via VS 2026 MSBuild (Utinni + UtinniPlugins).
    2. Launch the Utinni editor host WITHOUT a live SWG client (just the WinForms editor host —
       same path you use for Phase 8 FormIffEditor open-from-disk smoke).
    3. From the host menu (typically TJT plugin menu), open the Datatable Editor.
    4. Verify the form opens at 1200×760, dark theme applied, toolbar at top showing the
       buttons listed in UI-SPEC § Layout Contract Editor toolbar row, status strip at bottom
       reading the empty-state body copy `Open a .tab file from the toolbar, or use "Open in
       Datatable Editor" from the TRE Browser.` per UI-SPEC § States row "Empty (no document)".
    5. Click `Open…` toolbar button; pick a real `.tab` file (extract one from a TRE archive
       via Phase 7 TRE Browser if needed — or use a synthetic builder-output written to disk:
       `File.WriteAllBytes(@"C:\temp\test.tab", DatatableFixtures.BuildV1AllTypes())`).
    6. Verify the grid populates with per-type columns: integer columns show numbers, bool
       columns show checkboxes, enum columns show dropdowns when clicked into edit mode, hash
       string columns show free text + the floating hash preview (Consolas 9pt) anchored to
       the right of the cell on `CellBeginEdit`.
    7. Close the form via the X button. Re-open it from the same menu path. Confirm NO
       `ObjectDisposedException` dialog (the hide-not-dispose pattern works under live MEF
       conditions, not just the xUnit STA fixture).
    8. Confirm the reload-status badge in the top-right reads exactly `Reloads on next scene
       change.` (UI-SPEC § Copywriting locked CF-05 row).
    9. Confirm clicking `Reload in client` writes the locked CF-05 status copy to `lblStatus`:
       `Datatables re-resolve on the next scene change. Trigger one via TJT's chat-command load.`
    10. Click `Save▾`; verify the dropdown opens; click `Save (in place)` — expect a
        NotImplementedException dialog (this is INTENDED — Plan 09-05 wires the targets; the
        message body should mention "Wired by Plan 09-05" per the Task 2 action note).

    **The maintainer's visual check is the only way to confirm the dark theme + control suite +
    Designer Controls.Add order rendered correctly under real WinForms (the xUnit harness can
    only assert non-visual properties). This is a deliberately scoped checkpoint — it does NOT
    require live SWG (which is Plan 09-07's concern).**
  </how-to-verify>
  <resume-signal>Type "approved" if the editor opens, grid binds, hide-not-dispose holds, and the locked copy renders correctly. If any item fails, describe the symptom + which step + paste a screenshot or copy the failure dialog text.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → process | `Open…` toolbar reads a user-supplied `.tab` via `OpenFileDialog` (OS-mediated); parsed via Plan 09-01's hardened `DataTableDocument.FromIff` (cell-count cap + malformed-input rejection inherited) |
| process-internal | `DataTableHashCrc.Compute` consumed by `DatatableHashStringEditor` for live preview; pure-function (Plan 09-01); no live-client coupling |
| UI thread → MEF host | `Plugin.cs` registration of `FormDatatableEditor` via `GetForms()`; failure isolated in try/catch — does NOT take down TJT |
| singleton instance → second-open | the hide-not-dispose intercept on `CloseReason.UserClosing` IS the mitigation for the Phase 8 smoke-discovered defect class (T-09-12 below) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | DoS | FormDatatableEditor.Open — opens user-supplied .tab | mitigate | Inherits Plan 09-01's `DataTableDocument.FromIff` cell-count sanity cap (T-09-01) and `IffReader` 64 MB chunk cap. Parse failures wrapped in try/catch surface the `Color.Red` status copy from UI-SPEC § Copywriting `Save-failed status` row pattern. |
| T-09-11 | DoS (UI freeze) | DataGridView bind on CombatDataTable-scale fixtures | accept (measure to confirm) | Task 3 profiling probe measures the actual bind latency. If ≤ 100 ms, V1 ships non-virtual per UI-SPEC § Grid surface. If > 100 ms, Plan 09-06 inherits a VirtualMode fallback subtask. Per RESEARCH § Pitfall 7 + Assumption A5. |
| T-09-12 | DoS (editor crash on second open) | Singleton FormDatatableEditor lifecycle | mitigate | Hide-not-dispose intercept on `CloseReason.UserClosing` applied from commit 1 — the canonical mitigation for the Phase 8 smoke-discovered defect class (FormIffEditor `b899504` + FormTreBrowser defensive `ce2a0a4`). xUnit regression guard in `FormDatatableEditorHideNotDisposeTests.cs` prevents future regression. |
| T-09-13 | Tampering | Plugin.cs MEF registration | mitigate | The 7-line try/catch isolation block ensures a FormDatatableEditor construction failure does NOT take down TJT (the other forms still register). Same pattern Phase 7 + 8 use. |
| T-09-14 | Information disclosure | DT_HashString live preview displays computed hash | accept | DT_HashString hashes are not secrets — they're content-addressing of in-game asset names. No information beyond what's already exposed in the source `.tab` file. |
| T-09-SC | Tampering | npm/pip/cargo installs | N/A | Plan 09-03 adds ZERO new external packages (all controls + form types consume in-repo or BCL only). |

**ASVS controls applied:**
- V5 Input Validation — per-type cell widgets enforce keystroke validation (NumericUpDown won't accept letters; ComboBox is dropdown-only; CheckBox is binary); free-text columns will get parse-back validation in Plan 09-06 (Find/Replace path also validates).
- V12 File and Resources — `Open…` uses `OpenFileDialog` (OS-mediated path resolution); writes are Plan 09-05's concern (this plan ships the menu shape only).
</threat_model>

<verification>
Plan-level acceptance:

1. Both repos MSBuild Debug|x86 AND Release|x86 green (no errors / 0 warnings in the new files): `MSBuild Utinni.sln /p:Configuration=Debug /p:Platform=x86` AND `MSBuild "The Jawa Toolbox.sln" /p:Configuration=Debug /p:Platform=x86` (the actual TJT solution name in UtinniPlugins).
2. `dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~Form|FullyQualifiedName~DataGridViewBindLatency"` exits 0 with ≥ 3 passing facts (Task 2: ≥ 2 hide-not-dispose facts; Task 3: ≥ 1 perf probe).
3. `grep -c "Color.FromArgb" ../UtinniPlugins/The\ Jawa\ Toolbox/TheJawaToolboxDotNet/UI/Controls/*.cs ../UtinniPlugins/The\ Jawa\ Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs` returns 0 (no raw ARGB literals slipped past the Colors.*() accessor rule).
4. `grep -c "FormDatatableEditor" ../UtinniPlugins/The\ Jawa\ Toolbox/TheJawaToolboxDotNet/Plugin.cs` returns ≥ 1 (registration block present).
5. `grep -c "CloseReason.UserClosing" ../UtinniPlugins/The\ Jawa\ Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs` returns ≥ 1 (hide-not-dispose intercept present).
6. Maintainer visual sanity check (Task 4) approved.
7. Full UtinniCoreDotNet.Tests + Utinni.Cli.Tests + PreservationAudit suites stay green (no regression of Plan 09-01/02 baseline).
8. DataGridViewBindLatency measurement recorded in plan SUMMARY with the Plan 09-06 decision-branch note.
</verification>

<success_criteria>
- `FormDatatableEditor` opens, renders the toolbar + grid + status strip per UI-SPEC § Layout Contract, binds a typed `DataTableDocument` to the grid with per-type cell widgets, and persists window geometry to `[DatatableEditor]` ini section.
- The singleton-form hide-not-dispose pattern works in BOTH the xUnit STA fixture AND the live MEF editor host (Task 4 maintainer check); second-open does not throw `ObjectDisposedException`.
- The `ThemedDataGridView` token map is applied verbatim per UI-SPEC; Phases 10/11 inherit the wrapper.
- DataGridView initial-bind latency is measured against `BuildV1CombatDataTableLike()` and the measurement is recorded; the Plan 09-06 VirtualMode decision branch is resolved.
- Save▾ menu items render correctly but are stubbed (NotImplementedException) — Plan 09-05 will replace the stubs with the actual save-target dispatch.
- T4 structural-op buttons (Add row, Add column…) render but no-op — Plan 09-04 will replace the stubs with the controller-backed commands.
- Find / Replace pane is constructed in Designer but `Visible = false` — Plan 09-06 will wire the toggle + the search planner.
</success_criteria>

<output>
Create `.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-03-SUMMARY.md` when done. Record:
- DataGridView bind-latency measurement (mandatory — gates Plan 09-06)
- Total LOC added per file (rough — gives Plan 09-04/05/06 estimators a basis)
- Any deviation from the FormIffEditor port shape (e.g. if a particular member required a
  different signature due to the typed-document layer; Plan 09-04 needs this for the controller
  wire)
- Hide-not-dispose test count + green status
- Plugin.cs registration block diff (3-7 lines added — confirm the cross-repo paired commit
  landed cleanly)
- Any Designer.cs Controls.Add reorder issues that surfaced (CF-09 compliance check)
</output>

## Revision History

- 2026-05-29 iter-1: added Execution Notes for Task 3 deferral signal [WARNING #4]; strengthened Task 3 done with explicit grep acceptance gate on SUMMARY [WARNING #6]; pinned DatatableHashStringEditor floating-label placement policy deterministically [WARNING #7].
