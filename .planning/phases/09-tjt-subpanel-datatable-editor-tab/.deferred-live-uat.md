# Phase 09 — Deferred Live Verification (batch at phase end with 09-07)

Per user decision 2026-05-29: 09-03 live-host smoke deferred and batched with the 09-07 live-SWG smoke (Phase 8 posture — live residuals deferred-but-acceptable for V1). Code is built + tested green in both repos; this is human-verify only.

## 09-03 — Datatable Editor host live-host smoke (no live SWG required)
1. Launch Utinni editor host WITHOUT live SWG (Phase 8 FormIffEditor open-from-disk path).
2. Open Datatable Editor from TJT menu — verify 1200x760, dark theme, toolbar top, status strip bottom, empty-state copy.
3. Open a real .tab (or synth: File.WriteAllBytes(@"C:\temp\test.tab", DatatableFixtures.BuildV1AllTypes())).
4. Per-type columns: ints=numbers, bool=checkbox, enum=dropdown, DT_HashString=stored int32 + floating Consolas-9pt `{source} -> 0x{hash:X8}` preview.
5. Edit text/int/bool cell, tab away — change persists (commit-back).
6. Close via X, re-open from menu — NO ObjectDisposedException (hide-not-dispose under live MEF).
7. Reload badge reads `Reloads on next scene change.`; Reload-in-client writes the locked CF-05 copy.
8. Save▾ / Add row / Import CSV… / Find disabled + tooltip (not throwing).

## 09-07 — Tier-4 maintainer live-SWG smoke (added when Wave 6 runs)
