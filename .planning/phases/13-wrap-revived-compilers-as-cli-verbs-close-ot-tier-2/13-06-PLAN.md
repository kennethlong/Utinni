---
phase: 13-wrap-revived-compilers-as-cli-verbs-close-ot-tier-2
plan: 06
type: execute
wave: 4
depends_on: ["13-05"]
files_modified:
  - UtinniCoreDotNet/Formats/ObjectTemplate/ObjectTemplateSchema.cs
  - UtinniCoreDotNet/Formats/ObjectTemplate/ObjectTemplateSchemaLoader.cs
  - UtinniCoreDotNet.Tests/ObjectTemplate/ObjectTemplateSchemaTests.cs
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormObjectTemplateEditor.cs"
autonomous: false
requirements: [RESID-01]
must_haves:
  truths:
    - "The OT editor loads the static per-class schema artifact (from 13-05) and renders the common list/struct params (slots/attributes/hair) as structured typed widgets instead of raw hex"
    - "The rare/exotic multi-chunk tail degrades gracefully to a typed LABEL (from schema) + hex value, not bare hex"
    - "Typed display is a read/display layer over existing bytes — the Phase-11 codec still round-trips byte-exact (no codec rewrite)"
  artifacts:
    - path: "UtinniCoreDotNet/Formats/ObjectTemplate/ObjectTemplateSchema.cs"
      provides: "Managed model of the per-class param->type schema (ParamType+ListType)"
      min_lines: 30
    - path: "UtinniCoreDotNet/Formats/ObjectTemplate/ObjectTemplateSchemaLoader.cs"
      provides: "Loads the committed schema JSON; resolves a param's (type, list_type)"
      min_lines: 25
  key_links:
    - from: "FormObjectTemplateEditor.cs"
      to: "ObjectTemplateSchemaLoader -> the committed schema artifact"
      via: "schema lookup of (type, list_type) -> structured widget vs typed-label+hex"
      pattern: "ObjectTemplateSchema|ListType"
    - from: "ObjectTemplateSchemaLoader.cs"
      to: "the 13-05 committed schema JSON"
      via: "static-artifact load at editor open (zero native-tool dependency, D-08)"
      pattern: "schema"
---

<objective>
Close RESID-01 (the carried V1 Tier-2 residual): the Object Template Editor displays the ~17% multi-chunk list/struct params currently shown as raw `RawBytesHexFallback` hex (draft-schematic slots, attributes, hair tint lists) as structured TYPED widgets, by consuming the static per-class param->type schema artifact emitted by `compile-definition` (13-05, D-08). The exotic/rare multi-chunk tail degrades gracefully to a typed LABEL (from schema) + hex value — closing the 80/20 (D-07) without chasing the long tail.

Purpose: RESID-01 is the centerpiece authoring-residual this phase pays alongside the build verbs. The schema's `ListType != LIST_NONE` params ARE EXACTLY the codec's raw-fallback residual (VERIFIED: ObjectTemplateParamCodec routes interior-structure params to RawBytesHexFallback; MutableObjectTemplate names the residual as slots/attributes/hair). This is a DISPLAY layer over existing bytes — the Phase-11 codec already round-trips byte-exact, so NO codec rewrite (the Encode inverse already round-trips RawBytesHexFallback byte-for-byte).
Output: managed schema model + loader (UtinniCoreDotNet) + the FormObjectTemplateEditor typed-widget rendering (UtinniPlugins, cross-repo) + a human-verify checkpoint for the visual Tier-4 residual.

Depends on 13-05 (the committed schema artifact). Cross-repo: UtinniPlugins write authority is standing (feedback_utinniplugins_authority); the paired commit does NOT need a human-action checkpoint (only the live-SWG visual smoke does). Build via VS2026 MSBuild; framework xUnit via `dotnet test --no-build`; the WinForms host logic is verified by MSBuild-green per the Phase 8/9/10/11 precedent (not project-referenceable from the x86 test project) + the Tier-4 visual checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-wrap-revived-compilers-as-cli-verbs-close-ot-tier-2/13-RESEARCH.md
@.planning/phases/13-wrap-revived-compilers-as-cli-verbs-close-ot-tier-2/13-PATTERNS.md
@.planning/phases/13-wrap-revived-compilers-as-cli-verbs-close-ot-tier-2/13-05-PLAN.md
@UtinniCoreDotNet/Formats/ObjectTemplate/ObjectTemplateParamCodec.cs
@UtinniCoreDotNet/Formats/ObjectTemplate/MutableObjectTemplate.cs

<interfaces>
<!-- The codec residual RESID-01 types (VERIFIED, PATTERNS). -->
From UtinniCoreDotNet/Formats/ObjectTemplate/ObjectTemplateParamCodec.cs:
  // interior-structure params route to Fallback(...) -> ObjectTemplateParamValue.FromRawBytes(...) (:188-210)
  // Encode inverse already round-trips RawBytesHexFallback byte-for-byte (:217-269) — display-only change

From UtinniCoreDotNet/Formats/ObjectTemplate/ObjectTemplateParamValue.cs:
  // union: Bool/Int/Float/String/None/RawBytesHexFallback + verbatim delta byte + ParamTypeLabel

<!-- Schema artifact (from 13-05, D-08): per-class JSON keyed by class name; each param {name,type(ParamType),list_type(ListType),extendedName,enum_list_name}. -->
<!-- ListType != LIST_NONE == the slots/attributes/hair residual to render structured. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: managed schema model + loader (UtinniCoreDotNet)</name>
  <files>UtinniCoreDotNet/Formats/ObjectTemplate/ObjectTemplateSchema.cs, UtinniCoreDotNet/Formats/ObjectTemplate/ObjectTemplateSchemaLoader.cs, UtinniCoreDotNet.Tests/ObjectTemplate/ObjectTemplateSchemaTests.cs</files>
  <read_first>
    - .planning/phases/.../13-05-PLAN.md (the committed schema artifact shape: per-class JSON, ParamType(14)/ListType(4) vocabulary)
    - .planning/phases/.../schema/ (the actual committed schema JSON from 13-05 — load THIS)
    - UtinniCoreDotNet/Formats/ObjectTemplate/ObjectTemplateParamValue.cs (ParamTypeLabel + RawBytesHexFallback union — the display target)
    - .planning/phases/.../13-RESEARCH.md (§AUTH-02/RESID-01 lines 203-216: ListType != LIST_NONE == the residual)
  </read_first>
  <behavior>
    - ObjectTemplateSchema models the per-class param->type map: for a (className, paramName) it returns {ParamType, ListType, extendedName, enum_list_name}.
    - ObjectTemplateSchemaLoader loads the committed schema JSON (static artifact, D-08) once and caches it; an absent/malformed schema degrades gracefully (returns no-match, never throws on the editor open path).
    - A lookup classifies a param: ListType != LIST_NONE -> structured (the residual); LIST_NONE scalar -> already typed by Phase-11; unknown -> typed-label + hex.
    - Pure managed, BCL + Newtonsoft only (net472, no WinForms) so it is unit-testable.
  </behavior>
  <action>
    Create `UtinniCoreDotNet/Formats/ObjectTemplate/ObjectTemplateSchema.cs` (the model: ParamType enum mirror [14 values], ListType enum mirror [4 values], a per-class param lookup) + `ObjectTemplateSchemaLoader.cs` (load + cache the committed schema JSON via Newtonsoft; graceful no-match on absent/malformed schema — never throw on the open path). Provide a classify method: given (className, paramName) -> {ParamType, ListType, isStructured = ListType != LIST_NONE}. Write `ObjectTemplateSchemaTests` asserting: the committed 13-05 schema loads; the known list/struct classes (slots/attributes/hair-bearing) classify as isStructured=true; a missing param -> graceful no-match; a malformed schema -> no-throw degradation.
  </action>
  <verify>
    <automated>dotnet test --no-build --filter ObjectTemplateSchema passes (loads committed schema; ListType!=LIST_NONE classified structured; absent/malformed -> graceful no-throw)</automated>
  </verify>
  <acceptance_criteria>
    - ObjectTemplateSchema/Loader load the committed 13-05 schema artifact (static, D-08 — no native-tool call on the editor path).
    - classify() returns isStructured=true exactly for ListType != LIST_NONE params (slots/attributes/hair).
    - Absent/malformed schema degrades to no-match without throwing (open-path safety).
    - Pure managed (no WinForms reference) — unit-testable from the x86 test project.
    - `dotnet test --no-build --filter ObjectTemplateSchema` green.
  </acceptance_criteria>
  <done>Managed schema model + loader consume the static artifact; the structured-vs-hex classification is unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: FormObjectTemplateEditor structured typed-widget rendering (UtinniPlugins, cross-repo)</name>
  <files>D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormObjectTemplateEditor.cs</files>
  <read_first>
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormObjectTemplateEditor.cs (Phase-11 host: typed-scalar widgets + the Consolas hex fallback FormParamHexEditor for complex params — the rows currently showing raw hex)
    - UtinniCoreDotNet/Formats/ObjectTemplate/ObjectTemplateSchema.cs + ObjectTemplateSchemaLoader.cs (Task 1 — the lookup the editor consumes)
    - .planning/phases/.../13-PATTERNS.md (§OT typed-display close: schema's ListType!=LIST_NONE params get structured widgets; rare tail -> typed-label + hex; display layer only, no codec rewrite)
    - feedback_winforms_dockfill_zorder + project_ot_multichunk_list_params (auto-memories — multi-chunk param structure: named header chunk + nameless element chunks)
  </read_first>
  <action>
    In `FormObjectTemplateEditor.cs`, load the schema via `ObjectTemplateSchemaLoader` (off the background-resolve thread, matching the Phase-11 off-thread pattern; cache). For each param row, look up its (type, list_type): render the COMMON structured params (slots/attributes/hair — ListType != LIST_NONE) with a structured typed widget (a small typed sub-grid/list editor over the existing decoded element bytes — DISPLAY over the codec's RawBytesHexFallback bytes, NOT a re-decode); render the rare/exotic multi-chunk tail as a typed LABEL (the schema's param name + ParamType label) ABOVE the existing Consolas hex value (graceful degradation, D-07). Preserve byte-exactness: the underlying ObjectTemplateParamValue bytes and the Phase-11 codec Encode path are UNCHANGED — this is display-only. Keep the singleton hide-not-dispose pattern + the no-SPI-widen invariant (CON-M-01/02). Paired commit in UtinniPlugins (standing authority).
  </action>
  <verify>
    <automated>UtinniCoreDotNet + TJT build clean Debug+Release|x86 via VS2026 MSBuild; full framework xUnit `dotnet test --no-build` green (schema tests + no regression in the 624 OT-editor-adjacent suite)</automated>
  </verify>
  <acceptance_criteria>
    - The editor loads the static schema and renders ListType != LIST_NONE params (slots/attributes/hair) as structured typed widgets, not raw hex.
    - The rare multi-chunk tail shows a typed LABEL (schema name + ParamType) + hex (graceful degradation, D-07).
    - The change is display-only: ObjectTemplateParamValue bytes + the codec Encode path are unchanged (source assertion: no edit to ObjectTemplateParamCodec.Encode); byte-exact round-trip preserved.
    - UtinniCoreDotNet + TJT build clean Debug+Release|x86; framework xUnit green (no regression).
    - Singleton hide-not-dispose + no-SPI-widen invariants preserved (CON-M-01/02).
  </acceptance_criteria>
  <done>OT editor renders the common list/struct residual typed; rare tail = typed-label+hex; byte-exactness preserved; both repos build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Tier-4 visual verification — OT editor typed list-param display</name>
  <files>(none — maintainer visual verification of the Task 2 editor rendering against a live client)</files>
  <action>
    PAUSE for maintainer Tier-4 visual verification — there is no automation in this task (CON-TT-03: FlaUI deliberately skipped, this is the documented maintainer-in-the-loop residual). It confirms the Task 2 rendering against a live client. See <what-built> and <how-to-verify> below for the exact steps; the executor presents these and waits for the resume signal.
  </action>
  <what-built>
    The Object Template Editor now renders the previously-raw-hex multi-chunk list/struct params (draft-schematic slots, attributes, hair tint lists) as structured typed widgets, sourced from the committed compile-definition schema artifact; the rare/exotic tail degrades to a typed label + hex. This is the RESID-01 closure — the one Tier-4 visual residual (CON-TT-03: FlaUI deliberately skipped, so this is maintainer-in-the-loop per the documented Tier-4 boundary).
  </what-built>
  <how-to-verify>
    1. Build TJT + UtinniCore (VS2026 MSBuild, Debug|x86) and launch the editor host against a live SWGEmu client (or open an object-template .iff via the TRE Browser / IFF Editor hand-off).
    2. Open an object template KNOWN to carry list/struct params — a draft schematic (slots/attributes) or a creature/hair template (hair tint list).
    3. CONFIRM: the slots/attributes/hair params display as STRUCTURED typed rows (named sub-fields with typed values), NOT a single Consolas hex blob.
    4. CONFIRM: an exotic/rare multi-chunk param (if present) shows a typed LABEL + hex (graceful degradation), not bare hex with no label.
    5. CONFIRM: editing + saving a scalar still round-trips (no regression to the Phase-11 byte-exact save path).
    6. Note: "naked after TJT-driven scene change" is the documented baseline (project_tjt_scene_change_naked_baseline) — NOT a failure signal here.
  </how-to-verify>
  <verify><human-check>Maintainer confirms list/struct params render typed and the rare tail degrades to typed-label+hex against a live client.</human-check></verify>
  <done>Maintainer approves the typed list-param display; RESID-01 visually confirmed closed.</done>
  <resume-signal>Type "approved" if the list/struct params render typed + the tail degrades gracefully; otherwise describe what still shows as raw hex or renders incorrectly.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed schema JSON -> editor open path | The editor loads a static schema artifact at open; a malformed/absent schema must not crash the editor. |
| object-template bytes -> typed display | The display layer reads existing decoded bytes; it must not mutate them (byte-exactness). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-16 | Denial of Service | malformed/absent schema on editor open | mitigate | ObjectTemplateSchemaLoader degrades to no-match without throwing on the open path (Task 1) — a bad schema falls back to the existing hex display, never crashes the editor. |
| T-13-17 | Tampering | typed display accidentally mutating param bytes | mitigate | Display-only change; acceptance asserts no edit to the codec Encode path and byte-exact round-trip is preserved. The schema is advisory display metadata, never a write path. |
| T-13-18 | Information disclosure | abs-path leak via the schema artifact | accept | The schema is banner-normalized at generation (13-05 T-13-14); no new leak surface introduced by the display consumer. |
</threat_model>

<verification>
- `dotnet test --no-build --filter ObjectTemplateSchema` green; full framework xUnit green (no regression in the OT suite).
- UtinniCoreDotNet + TJT build clean Debug+Release|x86 (WinForms host verified by MSBuild-green per Phase 8-11 precedent + the Tier-4 checkpoint).
- Tier-4 visual checkpoint approved (the documented CON-TT-03 maintainer-in-the-loop residual).
- The codec Encode path is unchanged (display-only; byte-exactness preserved).
</verification>

<success_criteria>
The Object Template Editor displays list/struct params (slots/attributes/hair) TYPED rather than raw hex, sourced from the committed compile-definition schema (D-08); the rare tail degrades to typed-label + hex (D-07); byte-exactness is preserved (no codec rewrite). RESID-01 closed.
</success_criteria>

<output>
Create `.planning/phases/13-wrap-revived-compilers-as-cli-verbs-close-ot-tier-2/13-06-SUMMARY.md` when done.
</output>
