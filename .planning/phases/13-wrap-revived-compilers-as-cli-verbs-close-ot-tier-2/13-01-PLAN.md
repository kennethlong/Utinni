---
phase: 13-wrap-revived-compilers-as-cli-verbs-close-ot-tier-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/src/engine/shared/library/sharedXml/build/win32/sharedXml.vcxproj
  - tools/src/external/3rd/library/libxml2-2.6.7.win32/
  - tools/src/engine/shared/application/DataTableTool/build/win32/DataTableTool.vcxproj
  - tools/src/engine/shared/application/ArmorExporterTool/build/win32/ArmorExporterTool.vcxproj
  - tools/src/engine/shared/application/CoreWeaponExporterTool/build/win32/CoreWeaponExporterTool.vcxproj
  - tools/Utinni.Tools.sln
  - tools/Directory.Build.props
  - tools/DEPENDENCY-MANIFEST.md
autonomous: true
requirements: [AUTH-06]
must_haves:
  truths:
    - "DataTableTool.exe, ArmorExporterTool.exe, CoreWeaponExporterTool.exe build + link standalone at v145/Win32 from tools/Utinni.Tools.sln"
    - "The full tools/Utinni.Tools.sln (now 35 projects) builds green — the CI AUTH-01 hard-gate lane auto-extends to the 3 new exes"
    - "Each exporter no longer FATALs on the absent Perforce client (p4 helpers source-stubbed to no-op)"
  artifacts:
    - path: "tools/src/engine/shared/library/sharedXml/build/win32/sharedXml.vcxproj"
      provides: "The one genuinely-new leaf lib (libxml2-backed) DataTableTool needs"
    - path: "tools/src/engine/shared/application/DataTableTool/build/win32/DataTableTool.vcxproj"
      provides: "Native datatable spreadsheet/XML -> .iff compiler project"
    - path: "tools/src/engine/shared/application/ArmorExporterTool/build/win32/ArmorExporterTool.vcxproj"
      provides: "Native armor item exporter project"
    - path: "tools/src/engine/shared/application/CoreWeaponExporterTool/build/win32/CoreWeaponExporterTool.vcxproj"
      provides: "Native weapon item exporter project"
    - path: "tools/DEPENDENCY-MANIFEST.md"
      provides: "AUTH-06 revival deltas recorded (sharedXml/libxml2, /SAFESEH:NO, Perforce-stub, TemplateCompiler-chain note)"
  key_links:
    - from: "tools/Utinni.Tools.sln"
      to: "the 3 new app vcxprojs + sharedXml.vcxproj"
      via: "Project() entries"
      pattern: "DataTableTool|ArmorExporterTool|CoreWeaponExporterTool|sharedXml"
    - from: "tools/Directory.Build.props"
      to: "sharedXml/include/public + libxml2/include"
      via: "AdditionalIncludeDirectories (already forward-prepared in Phase 12)"
      pattern: "sharedXml|libxml"
---

<objective>
Lift the 3 AUTH-06 natives (`DataTableTool`, `ArmorExporterTool`, `CoreWeaponExporterTool`) plus the one new leaf lib (`sharedXml` + its prebuilt `libxml2-2.6.7` backing) from the pinned `swg-client-v2@5fce7bb8` into the Utinni-owned `tools/` tree, and make the whole `tools/Utinni.Tools.sln` build green at v145/Win32. This is a mini-Phase-12 lift (D-01) — the build foundation every AUTH-06 BUILD verb in Plan 13-04 wraps. Per RESEARCH, all 3 are CLEAN client lifts (server-taint disproven: they touch serverGame *enums only*, which already live in Phase-12-lifted `sharedTemplate`/`sharedGame`); the D-02 escape-hatch will NOT fire.

Purpose: The compile/export BUILD verbs cannot wrap a tool that does not compile (the AUTH-01 hard-gate principle, one layer down). This plan delivers the exes; Plan 13-04 wraps them.
Output: 3 new `tools/` exes + 1 new lib green in `Utinni.Tools.sln` (31 -> 35 projects); CI hard-gate auto-extended; DEPENDENCY-MANIFEST.md updated.

INLINE-BUILD CONSTRAINT (project_gsd_worktrees_off): run this native build wave on the MAIN tree, NOT a worktree — a fresh worktree lacks `vcpkg_installed` and the executor has no PowerShell/MSBuild tool for a 20-40min rebuild. Build with VS2026 MSBuild against `tools/Utinni.Tools.sln`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-wrap-revived-compilers-as-cli-verbs-close-ot-tier-2/13-CONTEXT.md
@.planning/phases/13-wrap-revived-compilers-as-cli-verbs-close-ot-tier-2/13-RESEARCH.md
@.planning/phases/13-wrap-revived-compilers-as-cli-verbs-close-ot-tier-2/13-PATTERNS.md
@tools/DEPENDENCY-MANIFEST.md
@tools/PINNED-SHA.md
@tools/Directory.Build.props
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lift sharedXml leaf lib + libxml2 prebuilt; add to Utinni.Tools.sln</name>
  <files>tools/src/engine/shared/library/sharedXml/, tools/src/external/3rd/library/libxml2-2.6.7.win32/, tools/Utinni.Tools.sln, tools/Directory.Build.props</files>
  <read_first>
    - tools/PINNED-SHA.md (the @5fce7bb8 lift SHA — lift from THIS, never branch HEAD)
    - tools/Directory.Build.props (lines ~9-12 already list sharedXml/include/public + libxml/include + the UTINNI_TOOLS_NO_SHAREDLOG define — Phase 12 forward-prepared the include surface)
    - tools/src/engine/shared/library/sharedRandom/build/win32/sharedRandom.vcxproj (the leaf-lib vcxproj analog — 0 ProjectReferences shape)
    - tools/DEPENDENCY-MANIFEST.md (Phase-12 lift procedure: git archive -> add to sln -> Directory.Build.props auto-imports the shim)
    - D:/Code/swg-client-v2/src/engine/shared/library/sharedXml/ (lift SOURCE — read-only; NEVER build here)
  </read_first>
  <action>
    `git -C D:/Code/swg-client-v2 archive 5fce7bb8 src/engine/shared/library/sharedXml src/external/3rd/library/libxml2-2.6.7.win32 | tar -x -C D:/Code/Utinni/tools` (Phase-12 lift method, per RESEARCH Installation block). Add the `sharedXml.vcxproj` as a new `Project()` entry in `tools/Utinni.Tools.sln` (31 -> 32 projects). `sharedXml` is a leaf lib: 0 ProjectReferences, backed by the prebuilt `libxml2-win32-{debug,release}.lib`. Confirm `Directory.Build.props` already exposes `sharedXml\include\public` and the `libxml\include` path (Phase 12 prepared these — line ~9); if a lib-path entry for libxml2 is missing, add it. Expect the Phase-12 revival deltas if needed: `/SAFESEH:NO` (libxml2 prebuilt predates Safe-SEH) and the `UTINNI_TOOLS_NO_SHAREDLOG` global define (already in Directory.Build.props). Build ONLY the sharedXml project first to isolate the lib lift.
  </action>
  <verify>
    <automated>MSBuild tools/Utinni.Tools.sln /t:sharedXml /p:Configuration=Debug;Platform=Win32 builds green (sharedXml.lib produced); links the libxml2 prebuilt without unresolved externals</automated>
  </verify>
  <acceptance_criteria>
    - sharedXml.vcxproj exists under tools/src/engine/shared/library/sharedXml/build/win32/ and appears as a Project() in Utinni.Tools.sln.
    - libxml2-2.6.7.win32 prebuilt .lib present under tools/src/external/3rd/library/.
    - `MSBuild tools/Utinni.Tools.sln /t:sharedXml /p:Configuration=Debug /p:Platform=Win32` exits 0 and emits sharedXml.lib.
    - No reference into D:/Code/swg-client-v2 (lift-and-shift constraint — grep the new vcxproj for "swg-client-v2" returns zero, excluding comments).
  </acceptance_criteria>
  <done>sharedXml builds green at v145/Win32 in tools/; libxml2 prebuilt linked; sln at 32 projects.</done>
</task>

<task type="auto">
  <name>Task 2: Lift the 3 app vcxprojs; source-stub Perforce; build all green</name>
  <files>tools/src/engine/shared/application/{DataTableTool,ArmorExporterTool,CoreWeaponExporterTool}/, tools/Utinni.Tools.sln</files>
  <read_first>
    - D:/Code/swg-client-v2/src/engine/shared/application/DataTableTool/src/shared/DataTableTool.cpp (main + DataTableWriter::loadFromSpreadsheet + isXmlFile branch; lift SOURCE, read-only)
    - D:/Code/swg-client-v2/src/engine/shared/application/ArmorExporterTool/src/shared/ArmorExporterTool.cpp (run() — getFileFromPerforce FATAL at ~line 300/326; system("TemplateCompiler -compile") at ~564-621; ConfigArmorExporterTool reads tools.cfg)
    - D:/Code/swg-client-v2/src/engine/shared/application/CoreWeaponExporterTool/src/shared/CoreWeaponExporterTool.cpp (same p4 + system chain pattern)
    - tools/src/engine/shared/application/TemplateCompiler/build/win32/TemplateCompiler.vcxproj (the app-vcxproj analog — Phase-12 lift shape, /SAFESEH:NO, C++20 ports)
    - tools/DEPENDENCY-MANIFEST.md (the UTINNI_TOOLS_NO_SHAREDLOG decouple precedent to mirror for the p4 stub)
  </read_first>
  <action>
    `git archive @5fce7bb8` the 3 app source trees into `tools/src/engine/shared/application/`. Add all 3 `*.vcxproj` as `Project()` entries in `Utinni.Tools.sln` (32 -> 35 projects). DataTableTool needs the `sharedXml` ProjectReference (its 19th ref — the other 18 + all exporter refs already exist in the sln per RESEARCH verdict). The serverGame `#include`s are DEAD ALIASES that redirect (via Directory.Build.props include-path shim) to the already-lifted `sharedTemplate`/`sharedGame` enum copies — NO serverGame ProjectReference is needed (none of the 3 lists it). Apply revival deltas as Phase 12 established: `/SAFESEH:NO` per EXE, `UTINNI_TOOLS_NO_SHAREDLOG`, C++20 `char16_t`/`wchar_t` ports if an exporter touches `Unicode::String`. PERFORCE-STUB (Pitfall 1, the real AUTH-06 work): source-stub the exporters' `getFileFromPerforce`/`addFileToPerforce` helpers to no-op (mirror the `UTINNI_TOOLS_NO_SHAREDLOG` decouple) so the `FATAL(p4 == NULL, "Cannot access Perforce")` path cannot fire headless — p4 is pure VCS bookkeeping (edit/add), not data, per RESEARCH A2. Leave the `system("TemplateCompiler")` chain in place (Plan 13-04's wrapper stages the exe + sets WorkingDirectory). Do NOT touch the older 38-column WeaponExporterTool (CONTEXT pins CoreWeaponExporterTool).
  </action>
  <verify>
    <automated>MSBuild tools/Utinni.Tools.sln /p:Configuration=Debug;Platform=Win32 builds green (all 35 projects); DataTableTool.exe, ArmorExporterTool.exe, CoreWeaponExporterTool.exe produced</automated>
  </verify>
  <acceptance_criteria>
    - All 3 app vcxprojs appear as Project() in Utinni.Tools.sln (sln at 35 projects).
    - Full `MSBuild tools/Utinni.Tools.sln /p:Configuration=Debug /p:Platform=Win32` exits 0 with zero unresolved externals.
    - The 3 exes exist in the Debug/Win32 output dir.
    - grep of the lifted exporter .cpp confirms the p4 helper bodies are stubbed (no live `popen("p4` reachable on the run() path) — the "Cannot access Perforce" FATAL is unreachable headless.
    - No ProjectReference or #include resolves into D:/Code/swg-client-v2 (grep returns zero non-comment hits).
  </acceptance_criteria>
  <done>All 3 exes build+link green at v145/Win32; Perforce FATAL neutralized; sln at 35 projects.</done>
</task>

<task type="auto">
  <name>Task 3: Update DEPENDENCY-MANIFEST.md; confirm CI hard-gate auto-extends</name>
  <files>tools/DEPENDENCY-MANIFEST.md</files>
  <read_first>
    - tools/DEPENDENCY-MANIFEST.md (existing per-tool closure + revival-delta format to extend)
    - .github/workflows/ci.yml (the "Build tools solution — AUTH-01 hard gate" step at ~line 174-176 builds the WHOLE tools\Utinni.Tools.sln — so it auto-extends; confirm, do not edit unless a per-project target is hardcoded)
  </read_first>
  <action>
    Extend `tools/DEPENDENCY-MANIFEST.md` with the 3 AUTH-06 tools: per-tool `#include` closure (DataTableTool's 19 ProjectReferences incl. the new `sharedXml`; the 2 exporters' 18/14 refs — all enum-only on serverGame), the new `sharedXml`/`libxml2-2.6.7` dependency, and the revival deltas (`/SAFESEH:NO`, the Perforce-stub source delta with rationale "p4 = VCS bookkeeping only", and the `system("TemplateCompiler")` runtime-chain note that Plan 13-04 stages the exe for). Record the lift SHA reference (still `@5fce7bb8`, no new SHA — same pin). Confirm the CI lane at ci.yml builds `tools\Utinni.Tools.sln` (whole-sln) so the AUTH-01 hard-gate auto-extends to the 3 new exes with NO ci.yml edit needed; if the lane targets specific projects, add the 3.
  </action>
  <verify>
    <automated>grep -v '^#' tools/DEPENDENCY-MANIFEST.md | grep -c -E "DataTableTool|ArmorExporterTool|CoreWeaponExporterTool|sharedXml|Perforce-stub" is >= 5</automated>
  </verify>
  <acceptance_criteria>
    - DEPENDENCY-MANIFEST.md documents all 3 AUTH-06 tools' closures + the sharedXml/libxml2 addition + the Perforce-stub delta + the TemplateCompiler-chain runtime note.
    - ci.yml's tools-build step is confirmed whole-sln (builds tools\Utinni.Tools.sln) — gate auto-extends; OR the 3 projects are added if the step is project-scoped.
    - Push triggers the self-hosted v145 CI runner and the tools build lane stays green.
  </acceptance_criteria>
  <done>Manifest documents the 3 AUTH-06 lifts + deltas; CI hard-gate covers all 35 projects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| swg-client-v2 source -> tools/ (lift) | Lift-and-shift: source crosses from the read-only sibling repo into the Utinni-owned build tree. Risk = accidental ProjectReference/#include back into swg-client-v2 (forbidden — it is mid-D3D9->D3D11 migration). |
| exporter runtime -> OS shell | The lifted exporters call `system("TemplateCompiler")` + (pre-stub) `popen("p4 …")` — a shell-out surface inherited from the SOE source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | the 4 lifted vcxprojs | mitigate | Acceptance asserts zero ProjectReference/#include into swg-client-v2 (grep gate); lift from pinned SHA @5fce7bb8 only. |
| T-13-02 | Elevation of Privilege | exporters' `popen("p4 …")` | mitigate | Source-stub the p4 helpers to no-op (Task 2) — the live shell-out is removed on the headless run() path, not just FATAL-guarded. |
| T-13-SC | Tampering | sharedXml/libxml2 lift (supply chain) | accept | No registry resolution — verbatim lift from in-repo SHA-pinned swg-client-v2@5fce7bb8 (RESEARCH Package Legitimacy Audit: slopcheck N/A, no registry surface). libxml2 2.6.7 known CVEs accepted offline/trusted-input this phase (re-evaluated when inputs become agent-influenceable in Phase 14). |
| T-13-03 | Tampering/EoP | exporters' `system("TemplateCompiler "+path)` | mitigate (deferred to 13-04) | The internal `system()` IS injectable; the wrapping verb (Plan 13-04) validates the input path before invoking and never reaches an interactive/no-arg branch. Tracked here as the inherited surface; the wrapper is the control point. |
</threat_model>

<verification>
- Full `tools/Utinni.Tools.sln` builds green Debug/Win32 (35 projects) via VS2026 MSBuild on the main tree (inline).
- The 3 new exes exist and run their `-h`/usage path is NOT triggered in automation (RESEARCH stdout-pollution note: native tools `getchar()` on usage).
- Push to the self-hosted v145 CI runner keeps the AUTH-01 build lane green.
- Zero references back into swg-client-v2.
</verification>

<success_criteria>
DataTableTool, ArmorExporterTool, CoreWeaponExporterTool build + link standalone at v145/Win32 from tools/Utinni.Tools.sln; sharedXml + libxml2 lifted; Perforce FATAL neutralized; CI hard-gate auto-extended; DEPENDENCY-MANIFEST.md updated. (AUTH-06 build foundation — verbs wrap these in 13-04.)
</success_criteria>

<output>
Create `.planning/phases/13-wrap-revived-compilers-as-cli-verbs-close-ot-tier-2/13-01-SUMMARY.md` when done.
</output>
