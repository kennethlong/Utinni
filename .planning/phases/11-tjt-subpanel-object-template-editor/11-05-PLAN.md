---
phase: 11-tjt-subpanel-object-template-editor
plan: 05
type: execute
wave: 5
depends_on: ["11-04"]
files_modified:
  - docs/ai/test-harness-plan.md
  - .planning/phases/11-tjt-subpanel-object-template-editor/11-V1-RELEASE-GATE.md
autonomous: false
requirements: [PROD-W1-OT, PROD-02]

must_haves:
  truths:
    - "The full automated suite (UtinniCoreDotNet.Tests + Utinni.Cli.Tests roundtrip-ot goldens) is green on both build configs"
    - "The Object Template Editor demos end-to-end inside TJT against a live SWG client: open → view inherited fields → edit/override/revert → save back"
    - "The live client reflects the edit on the honest CF-05 reload path (relog-reliable, scene-change-conditional, respawn-best-effort)"
    - "The V1 release gate is evidenced: all five Wave-1 subpanels demo, Tier 1 + Tier 2 CI is green on main, the 15 critical bugs are confirmed closed"
    - "The V1 tag is applied (manual release action) once the gate is signed off"
  artifacts:
    - path: ".planning/phases/11-tjt-subpanel-object-template-editor/11-V1-RELEASE-GATE.md"
      provides: "the aggregate V1 release-gate evidence checklist (5 subpanels, CI green, 15 bugs closed) + sign-off"
  key_links:
    - from: "11-V1-RELEASE-GATE.md"
      to: "ROADMAP Phase 11 SC4"
      via: "evidence rows mapping each gate condition to its proof"
      pattern: "release gate"
---

<objective>
Close the phase and the V1 milestone: a maintainer-in-the-loop live-SWG smoke of the Object Template Editor (SC1/SC2/SC3), then the V1 release-gate aggregate verification (SC4 — all five Wave-1 subpanels demo, Tier 1 + Tier 2 CI green on main, 15 critical bugs closed) and the V1 tag.

Purpose: Per ROADMAP, this phase's success closes the "Demo + CI green" milestone and ships V1. SC3's live observation is the documented Tier-4 residual (precedent: Phases 8/9/10 accepted automation-augmented smoke) because the `ObjectTemplateList` cache makes respawn unreliable — relog guarantees re-resolution.
Output: a smoke checkpoint, the V1 release-gate evidence doc, and the V1 tag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-tjt-subpanel-object-template-editor/11-CONTEXT.md
@.planning/phases/11-tjt-subpanel-object-template-editor/11-RESEARCH.md
@.planning/phases/11-tjt-subpanel-object-template-editor/11-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated regression + V1 release-gate evidence doc</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 11 Success Criteria, esp. SC4 the V1 release gate; the five Wave-1 subpanel rows; Phase 2 STAB-01 the 15 critical bugs)
    - .planning/REQUIREMENTS.md (PROD-W1-OT, PROD-02 acceptance; the Traceability table)
    - .planning/STATE.md (Phase 7/8/9/10 completion evidence + commit hashes; any tracked SC3 residuals e.g. Phase 10)
    - .planning/phases/11-tjt-subpanel-object-template-editor/11-RESEARCH.md (Validation Architecture: full-suite command + SC3 honest tier-(b) limits)
    - docs/ai/test-harness-plan.md (Tier-4 residual doc to append the OT SC3 residual to)
  </read_first>
  <action>
Run the full automated suite: MSBuild VS2026 Debug+Release|x86 across the solution + TheJawaToolboxDotNet, then `dotnet test --no-build` across `UtinniCoreDotNet.Tests` and `Utinni.Cli.Tests` (the ObjectTemplate unit suites + the `roundtrip-ot` goldens: multi-level chain, complex-param hex-fallback, unresolved-base degradation). Record pass counts.

Create `.planning/phases/11-tjt-subpanel-object-template-editor/11-V1-RELEASE-GATE.md` — the SC4 aggregate evidence checklist. One evidence row per gate condition with its proof:
  - All five Wave-1 subpanels demo end-to-end inside TJT (TRE Browser P7, IFF Editor P8, Datatable Editor P9, String-table Editor P10, Object Template Editor P11) — cite each phase's completion + smoke evidence from STATE.md and this phase's Task 2 smoke.
  - Tier 1 (dotnet test + native Catch2) + Tier 2 (CLI golden) CI green on `main` — cite the latest green CI run.
  - The 15 critical bugs (C-01..C-15 / STAB-01) closed — cite the Phase 2 / 02.1 evidence.
  - Note the honest SC3 residual: object-template live re-resolution is relog-reliable, scene-change-conditional, respawn-best-effort (CF-05 cache reality) — documented as the Tier-4 residual, mirroring Phase 10's deferred SC3 live-reload residual. Append this OT SC3 residual row to `docs/ai/test-harness-plan.md` Tier-4 section.

Leave the maintainer sign-off line and the V1-tag line UNCHECKED for Task 2/3 (the live smoke + tag are the human-gated steps).
  </action>
  <verify>
    <automated>cd /d D:/Code/Utinni && dotnet test UtinniCoreDotNet.Tests --no-build --filter ObjectTemplate && dotnet test Utinni.Cli.Tests --no-build --filter RoundtripOt</automated>
  </verify>
  <acceptance_criteria>
    - Both filtered suites pass; the full `UtinniCoreDotNet.Tests` + `Utinni.Cli.Tests` suites are green on Debug+Release|x86 (record counts in the SUMMARY).
    - `.planning/phases/11-tjt-subpanel-object-template-editor/11-V1-RELEASE-GATE.md` exists with an evidence row for each SC4 condition (5 subpanels, Tier 1+2 CI green, 15 bugs closed) and an explicit honest SC3-residual row.
    - `docs/ai/test-harness-plan.md` Tier-4 section gains the OT SC3 live-reload residual row.
  </acceptance_criteria>
  <done>The automated suite is green and the V1 release-gate evidence doc enumerates every SC4 condition with proof, with the SC3 cache-reality residual documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-SWG smoke — Object Template Editor (SC1/SC2/SC3) + five-subpanel demo</name>
  <what-built>
The Object Template Editor SubPanel (Plans 01–04): typed object-template format core, DERV-chain effective-inheritance resolver, editor-local override/revert/edit, host form with the Field·Effective value·Origin·Type grid + ancestor breadcrumb, both hand-offs (TRE Browser + IFF Editor), Save modes 1/2/4, and the locked CF-05 tier-(b) reload badge. All automated gates (round-trip byte-exactness, resolver semantics, controller undo, classifier tier) are green.
  </what-built>
  <how-to-verify>
Launch Utinni against a live SWG client with TJT loaded, then:
  1. Open the Object Template Editor SubPanel (SC1: it loads inside TJT).
  2. Open a tangible object template with at least one local override and a resolvable base (e.g. via TRE Browser "Open in Object Template Editor", or IFF Editor "Switch to typed object-template view"). Confirm the effective view shows one row per field, the ancestor breadcrumb, local-override rows accented, inherited rows greyed/italic with "inherited from <base>" origin (SC2: view inherited fields).
  3. Edit a scalar local override (e.g. a bool/string), promote an inherited field to a local override, and revert a local override back to inherited — confirm undo/redo works and dirty state shows (SC2: edit overrideable fields).
  4. Save back via mode 1 (loose override) or mode 4 (repack). Confirm the save succeeds and the reload badge reads "Reloads on next scene change (relog to guarantee)."
  5. SC3 (honest tier-(b)): trigger a scene change via TJT's chat-command load, OR relog the client, then observe the edited template's object reflects the edit. Per the cache reality, RELOG is the reliable path; respawn alone may show the cached (stale) value — that is expected, not a defect.
  6. Confirm all FIVE Wave-1 subpanels (TRE Browser, IFF Editor, Datatable Editor, String-table Editor, Object Template Editor) each open and demo inside TJT in the same session (the SC4 five-subpanel demo).
Report: which steps passed, any defects, and whether SC3 reflected on relog (and/or scene change).
  </how-to-verify>
  <resume-signal>Type "approved" (optionally "approved, with residual: <note>" if SC3 is relog-only or deferred per the Phase 8/9/10 automation-augmented precedent), or describe issues to fix.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: V1 release-gate sign-off + tag V1</name>
  <what-built>
The V1 release-gate evidence doc (Task 1) and the live-SWG smoke approval (Task 2). All SC4 conditions are evidenced: five Wave-1 subpanels demo end-to-end, Tier 1 + Tier 2 CI green on `main`, the 15 critical bugs closed.
  </what-built>
  <how-to-verify>
Review `.planning/phases/11-tjt-subpanel-object-template-editor/11-V1-RELEASE-GATE.md` and confirm every SC4 row is satisfied (with the SC3 residual acknowledged per the Phase 8/9/10 precedent). When satisfied, apply the V1 release tag — this is a deliberate manual release action, not an executor step:
  - Tag the agreed commit on `main` as the V1 release (e.g. `v1.0.0`), following the Phase 6 `release.yml` / `v1.0.0-rc.1` precedent.
  - Confirm the GitHub release/artifact is produced as expected.
Mark the sign-off + tag lines in `11-V1-RELEASE-GATE.md` checked, with the tag name and commit recorded.
  </how-to-verify>
  <resume-signal>Type "tagged <tagname>" once the V1 tag is applied and recorded, or "hold: <reason>" to defer the tag.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor save -> live SWG client | The smoke writes edited bytes that a live client loads; a corrupt write could destabilize the client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-15 | Tampering | A bad save bricks the live client during the smoke | mitigate | Saves go through the Phase 8 targets (backup + atomic replace + V6000 refusal); the round-trip golden already proved byte-exactness pre-smoke; loose-override mode is non-destructive to source archives |
| T-11-16 | Repudiation | The V1 gate is tagged without evidence | mitigate | The V1-RELEASE-GATE doc requires a proof row per SC4 condition and a blocking maintainer sign-off before the tag; the tag is a deliberate human-action step |
| T-11-SC | Tampering | npm/pip/cargo installs | accept (N/A) | No packages installed this plan |
| — | S/R/I/E (auth/session/network) | — | accept (N/A) | Local desktop tool + manual release; no auth/session/network surface |
</threat_model>

<verification>
- Full automated suite green on both configs before the smoke.
- Live smoke approved (SC1/SC2/SC3 to tier-(b) candor) and the five-subpanel demo confirmed.
- V1 release-gate doc fully evidenced and signed off; V1 tag recorded.
</verification>

<success_criteria>
- SC1/SC2 demonstrated live; SC3 reflected on the honest CF-05 path (relog-reliable).
- SC4 V1 release gate evidenced (5 subpanels, Tier 1+2 CI green, 15 bugs closed) and signed off.
- V1 tagged.
</success_criteria>

<output>
Create `.planning/phases/11-tjt-subpanel-object-template-editor/11-05-SUMMARY.md` when done.
</output>
