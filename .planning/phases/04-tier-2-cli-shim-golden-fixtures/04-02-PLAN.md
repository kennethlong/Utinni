---
id: 04-02
phase: 04-tier-2-cli-shim-golden-fixtures
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - UtinniCoreDotNet/Formats/Tre/TreFile.cs
  - UtinniCoreDotNet/Formats/Tre/TreHeader.cs
  - UtinniCoreDotNet/Formats/Tre/TreRecord.cs
  - UtinniCoreDotNet/Formats/Tre/TreParseException.cs
  - UtinniCoreDotNet/UtinniCoreDotNet.csproj
  - UtinniCoreDotNet.Tests/FormatsTests/Tre/TreFileTests.cs
  - UtinniCoreDotNet.Tests/FormatsTests/Tre/TreFileFixtures.cs
  - Utinni.Cli/Commands/ParseTreCommand.cs
  - Utinni.Cli/Commands/ListObjectsCommand.cs
  - Utinni.Cli.Tests/Commands/ParseTreCommandTests.cs
  - Utinni.Cli.Tests/Commands/ListObjectsCommandTests.cs
  - Utinni.Cli.Tests/Fixtures/tre/synthesized-3record-v0005.tre
  - Utinni.Cli.Tests/Fixtures/tre/synthesized-3record-v0005.expected.json
  - Utinni.Cli.Tests/Fixtures/tre/synthesized-2record-v0006.tre
  - Utinni.Cli.Tests/Fixtures/tre/synthesized-2record-v0006.expected.json
  - Utinni.Cli.Tests/Fixtures/tre/malformed-magic.tre
  - Utinni.Cli.Tests/Fixtures/tre/malformed-magic.expected.json
  - Utinni.Cli.Tests/Fixtures/tre/truncated.tre
  - Utinni.Cli.Tests/Fixtures/tre/truncated.expected.json
  - Utinni.Cli.Tests/Fixtures/tre/unsupported-version.tre
  - Utinni.Cli.Tests/Fixtures/tre/unsupported-version.expected.json
  - Utinni.Cli.Tests/Fixtures/world-snapshot/synthesized-ws.iff
  - Utinni.Cli.Tests/Fixtures/world-snapshot/synthesized-ws.expected.json
  - docs/ai/assessment.md
autonomous: true
requirements: [TEST-03]

must_haves:
  truths:
    - "`utinni-cli parse-tre <synthesized-3-record-v0005.tre>` emits a JSON envelope whose TOP-LEVEL keys are `{ command, result, schemaVersion }` (REVIEWS HIGH-6 — no nested schemaVersion in result). `result.header.version == \"0005\"`, `result.header.recordCount == 3` (REVIEWS HIGH-7 — `recordCount` NOT `resourceCount`), `result.records` is an array of 3 objects each carrying `id`, `name`, `offset`, `compressedSize`, `uncompressedSize`, `compressionKind`, `checksum` keys (REVIEWS HIGH-7 — `compressionKind` enum string NOT `dataCompression` int, `id` stable identifier as `tre:<ordinal>`) — and exits with code 0."
    - "`utinni-cli parse-tre <synthesized-2-record-v0006.tre>` exits 0 and emits `result.header.version == \"0006\"` with `result.header.recordCount == 2` (REVIEWS LOW-20 — v0006 now exercised by a dedicated Tier-2 golden)."
    - "`utinni-cli parse-tre <malformed-magic.tre>` emits the structured error envelope with TOP-LEVEL `{ command, error, schemaVersion }` and `error.kind == \"BadMagic\"`; exits with code 2."
    - "`utinni-cli parse-tre <truncated.tre>` emits `error.kind == \"Truncated\"`; exits with code 2."
    - "`utinni-cli parse-tre <unsupported-version.tre>` (header version `0009`) emits `error.kind == \"UnsupportedVersion\"`; exits with code 2."
    - "`utinni-cli parse-tre /tmp/does-not-exist.tre` emits `error.kind == \"FileNotFound\"`; exits with code 3."
    - "`utinni-cli list-objects <synthesized-ws.iff>` reads a minimal synthesized world-snapshot IFF, extracts the embedded object-template-path strings, and emits `result.objects: [{ id, templateName }, ...]` (REVIEWS HIGH-7 — each object carries a stable `id` field = the templateName string; objects sorted by `id` Ordinal for golden stability) — and exits with code 0."
    - "Tier-1 parser tests in `UtinniCoreDotNet.Tests/FormatsTests/Tre/TreFileTests.cs` exercise the TRE reader directly against in-memory byte sequences AND against the on-disk synthesized fixtures (both v0005 and v0006); they cover happy-path, bad-magic, truncated, unsupported-version, mixed-compression record stream, and the EAGER-READ contract (REVIEWS HIGH-4 — GetRecordData returns from in-memory cache, NOT a lazy-seek on a possibly-disposed stream)."
    - "The TRE parser caps each record's decompressed size at 256 MB and each header field's claimed size at the remaining file bytes — malformed inputs claiming gigabytes throw `TreParseException` rather than allocating attacker-controlled buffers (T-04-V5 + T-04-DoS)."
    - "The TRE parser EAGER-READS all record payloads into byte[] arrays during `Open(Stream)` (REVIEWS HIGH-4 fix path A — chosen because D-08 fixtures are <128KB so memory cost is negligible). `TreFile.GetRecordData(int idx)` returns a reference to the cached byte[]; no stream ownership / IDisposable / post-disposal seek bugs are possible."
    - "Both Tier-1 and Tier-2 fixtures are committed in-repo under 128 KB each (D-03); no LFS; CON-O-09 disposition row added to docs/ai/assessment.md."
    - "Plan 04-02 honors CONTEXT.md decisions D-04 (parse-tre + list-objects emit JToken.DeepEquals-compatible sorted-key JSON read by Tier-2 GoldenTestRunner), D-06 (TreFile/TreHeader/TreRecord/TreParseException land in UtinniCoreDotNet/Formats/Tre/ as pure managed C# with Tier-1 tests in UtinniCoreDotNet.Tests/FormatsTests/Tre/), D-10 (Task 2.2 BuildResult emits sorted-key indented JSON with top-level schemaVersion envelope via JsonOutput), and D-11 (Task 2.4 verify gate confirms the second dotnet test lane from 04-01 covers the new tests on master)."
  artifacts:
    - path: "UtinniCoreDotNet/Formats/Tre/TreFile.cs"
      provides: "Public pure-C# TRE container reader: `public static TreFile Open(string path)` + `public static TreFile Open(Stream stream)` (the Stream overload is the testability seam for Tier-1 tests; the path overload wraps `new FileStream` + delegates). **REVIEWS HIGH-4 fix path A:** `Open(Stream)` EAGER-READS every record's payload bytes into an internal `byte[][]` cache during open. `Open(string)` opens the FileStream, delegates, returns. Stream lifetime is bounded by `Open(Stream)`'s body — no lazy reads after dispose. Exposes `Header { get; }` (TreHeader sealed class) and `Records { get; }` (IReadOnlyList<TreRecord>) and `GetRecordData(int index)` returning the cached `byte[]` (a defensive copy is NOT made — callers must not mutate; document this in xmldoc). Reads little-endian header per Research §Format-Specific Knowledge §TRE; supports v0005 + v0006; throws TreParseException for v0004/v0009/etc."
      min_lines: 120
    - path: "UtinniCoreDotNet/Formats/Tre/TreParseException.cs"
      provides: "Structured TreParseException with `TreParseError Kind { get; }` enum field. Kind values: BadMagic, UnsupportedVersion, Truncated, ChunkLengthExceedsCap, NegativeLength, DeflateExpansionExceedsCap."
    - path: "Utinni.Cli/Commands/ParseTreCommand.cs"
      provides: "Real `Run(ParseTreOptions o)` implementation that opens the TRE, builds a sorted-key JSON dump of header + records via JsonOutput.EmitSuccess, returns 0; catches TreParseException → EmitError with kind from ex.Kind, exit 2; catches FileNotFoundException → EmitError with kind FileNotFound, exit 3. The BuildResult method emits the CONTRACT-LOCKED field shape (REVIEWS HIGH-7): records[] entries carry `id`, `name`, `offset`, `compressedSize`, `uncompressedSize`, `compressionKind` (enum string), `checksum`; header carries `recordCount` (NOT resourceCount), `version`, etc. NO nested schemaVersion (REVIEWS HIGH-6)."
    - path: "Utinni.Cli/Commands/ListObjectsCommand.cs"
      provides: "Real `Run(ListObjectsOptions o)` implementation: opens the supplied path as raw bytes, scans for the embedded `OBJS` chunk's payload, extracts each null-terminated template-name string, emits `{ result: { source: <path>, objects: [{ id: <templateName>, templateName: <string> }, ...] } }` (REVIEWS HIGH-7 — objects[] carry a stable `id` field = the templateName, sorted Ordinal). Returns 0; the same exit-code mapping as parse-tre on errors. The OBJS-sentinel byte scan is ARCHITECTURAL DEBT (REVIEWS MEDIUM-13) — see `<pitfalls>` for the Phase 6+ refactor note rewriting list-objects on top of IffReader once 04-03 lands and is stable."
    - path: "UtinniCoreDotNet.Tests/FormatsTests/Tre/TreFileTests.cs"
      provides: "Tier-1 xUnit parser tests with [Fact] coverage for: valid v0005 header, valid v0006 header (REVIEWS LOW-20), bad-magic, unsupported-version, truncated-record-table, mixed-compression record stream (one uncompressed + one deflated record verifying System.IO.Compression.DeflateStream path), the 256MB cap, and a dedicated `GetRecordData_AfterOpenStreamDisposed_StillReturnsBytes` test (REVIEWS HIGH-4 regression guard — proves the eager-read contract holds by disposing the stream before calling GetRecordData)."
    - path: "Utinni.Cli.Tests/Fixtures/tre/synthesized-3record-v0005.tre"
      provides: "Hand-crafted 3-record TRE binary (v0005): magic `EERT`, version `0005`, three records covering (record 0: small uncompressed name `hello.txt`, payload `Hello, World!`), (record 1: deflated payload), (record 2: uncompressed). Lifecycle: built once by the executor via a one-shot C# helper program (or PowerShell BinaryWriter), committed as-is; the same bytes flow into the Tier-2 golden test."
      max_size_bytes: 4096
    - path: "Utinni.Cli.Tests/Fixtures/tre/synthesized-3record-v0005.expected.json"
      provides: "JToken golden — the exact JSON `parse-tre` emits against the v0005 synthesized fixture. Manually inspected for correctness, then locked. Conforms to the REVIEWS HIGH-7 field shape (records[].id, compressionKind, header.recordCount)."
    - path: "Utinni.Cli.Tests/Fixtures/tre/synthesized-2record-v0006.tre"
      provides: "Hand-crafted 2-record TRE binary (v0006) (REVIEWS LOW-20 — closes v0006 coverage gap). Magic `EERT`, version `0006`, two records covering (record 0: uncompressed small text), (record 1: deflated payload). Demonstrates v0006 header parses identically to v0005 (no v0006-specific field differences within Phase 4's scope per Research §Format-Specific Knowledge §TRE)."
      max_size_bytes: 2048
    - path: "Utinni.Cli.Tests/Fixtures/tre/synthesized-2record-v0006.expected.json"
      provides: "Golden for the v0006 fixture. `result.header.version == \"0006\"`."
    - path: "Utinni.Cli.Tests/Fixtures/world-snapshot/synthesized-ws.iff"
      provides: "Minimal IFF carrying a `FORM WSNP` container with one `OBJS` leaf chunk listing three null-terminated template-paths like `object/creature/player/shared_player_human_male.iff`."
      max_size_bytes: 2048
  key_links:
    - from: "Utinni.Cli/Commands/ParseTreCommand.cs"
      to: "UtinniCoreDotNet/Formats/Tre/TreFile.cs"
      via: "static method call"
      pattern: "TreFile\\.Open\\("
    - from: "Utinni.Cli/Commands/ParseTreCommand.cs"
      to: "Utinni.Cli/Output/JsonOutput.cs"
      via: "static method call (EmitSuccess returns 0, EmitError returns exitCode)"
      pattern: "JsonOutput\\.Emit(Success|Error)"
    - from: "Utinni.Cli/Commands/ListObjectsCommand.cs"
      to: "(byte-scan over file bytes — no TreFile dependency; documented as architectural debt per REVIEWS MEDIUM-13)"
      via: "Phase 6+ refactor: consume IffReader.Read once 04-03 lands and is stable on master"
      pattern: "(N/A — current scan is byte-level; refactor target documented in <pitfalls>)"
    - from: "UtinniCoreDotNet.Tests/FormatsTests/Tre/TreFileTests.cs"
      to: "UtinniCoreDotNet/Formats/Tre/TreFile.cs"
      via: "direct ProjectReference"
      pattern: "using UtinniCoreDotNet.Formats.Tre"
    - from: "Utinni.Cli.Tests/Commands/ParseTreCommandTests.cs"
      to: "Utinni.Cli.Tests/Fixtures/tre/*.expected.json"
      via: "GoldenTestRunner.Matches"
      pattern: "GoldenTestRunner\\.Matches\\(\"tre/"
---

<objective>
Ship the TRE container parser and the first two real CLI commands: `parse-tre` and `list-objects`. Lands the Tier-1 unit tests against the parser library directly + the Tier-2 golden suite against the CLI surface, plus the in-repo synthesized fixtures (D-03). Replaces the not-implemented stubs from Plan 04-01 with working code; commands now exit 0 on success and emit stable sorted-key JSON conforming to the LOCKED envelope (REVIEWS HIGH-6 — top-level schemaVersion + command). Closes CON-O-09 (fixture storage = in-repo synth + tiny real, no LFS) in docs/ai/assessment.md.

Per CONTEXT.md §specifics: parser-with-Tier-1-tests ships in ONE atomic commit; CLI surface (`ParseTreCommand` + `ListObjectsCommand`) is a second commit; fixtures + Tier-2 golden tests is a third commit. ~3 commits for this plan.

Per CONTEXT.md D-01: the TRE parser is a clean reimplementation under MIT. Reference reads (LICENSE-EXCLUDED) at `D:/Code/swg-client-v2/src/engine/shared/library/sharedFile/src/shared/{TreeFile.h,TreeFile.cpp}` (SOE/Bootprint, All Rights Reserved) are consulted for FORMAT UNDERSTANDING ONLY — zero code, comments, or identifier names copied. Implementation is original to Utinni; every new file under `UtinniCoreDotNet/Formats/Tre/` carries the standardised MIT header + D-01 disposition addendum line.

Purpose: First parser. Establishes the parser pattern Plan 04-03 (IFF) repeats. Lifts the largest LOC component of Phase 4.

Output: Two working CLI commands + Tier-1 parser tests + Tier-2 golden tests + five TRE fixtures (3-record v0005, 2-record v0006, 3 negatives) + one synthesized world-snapshot IFF + CON-O-09 disposition row.

**Honored CONTEXT.md decisions** (plan-checker iter-2 coverage-gate hooks):
- D-04: Stable JSON output + JToken.DeepEquals goldens — `parse-tre` / `list-objects` emit sorted-key JSON read via JToken.DeepEquals in Tier-2 tests.
- D-06: Parser home — `TreFile`/`TreHeader`/`TreRecord`/`TreParseException` live in `UtinniCoreDotNet/Formats/Tre/` (pure managed C#, Tier-1 unit-testable).
- D-10: Sorted-key indented JSON + top-level `schemaVersion` envelope — Task 2.2 BuildResult emits the locked shape via JsonOutput.
- D-11: CI lane — Task 2.4 verify gate confirms the second `dotnet test` lane (from 04-01) picks up the new tests on master.

**Reviews-mode revisions applied (2026-05-22 from 04-REVIEWS.md):**
- HIGH-4: TreFile EAGER-READS payload bytes into `byte[][]` cache during `Open(Stream)`. No stream lifetime issue. Tier-1 test asserts GetRecordData works AFTER the source stream is disposed.
- HIGH-6: JSON envelope locked to `{ schemaVersion, command, result | error }` at the ROOT level. No nested schemaVersion. Tests assert top-level shape.
- HIGH-7: BuildResult emits the CONTRACT-LOCKED field names: `records[].id`, `records[].compressionKind` (enum string), `header.recordCount` (NOT resourceCount); `objects[].id` (= templateName). React Flow portability preserved.
- MEDIUM-13: `list-objects` OBJS-sentinel scan documented as architectural debt with explicit Phase 6+ refactor note in `<pitfalls>`. No Task change required beyond the doc comment.
- LOW-20: Dedicated v0006 fixture (`synthesized-2record-v0006.tre`) added to Task 2.3 with its own Tier-2 golden so v0006 is exercised end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tier-2-cli-shim-golden-fixtures/04-CONTEXT.md
@.planning/phases/04-tier-2-cli-shim-golden-fixtures/04-RESEARCH.md
@.planning/phases/04-tier-2-cli-shim-golden-fixtures/04-PATTERNS.md
@.planning/phases/04-tier-2-cli-shim-golden-fixtures/04-VALIDATION.md
@.planning/phases/04-tier-2-cli-shim-golden-fixtures/04-REVIEWS.md
@.planning/phases/04-tier-2-cli-shim-golden-fixtures/04-01-SUMMARY.md
@.planning/intel/constraints.md

<interfaces>
<!-- Reference reads (LICENSE-EXCLUDED): D:/Code/swg-client-v2/src/engine/shared/library/sharedFile/src/shared/{TreeFile.h, TreeFile.cpp} — read for FORMAT UNDERSTANDING ONLY. Copy zero code, comments, or identifier names. Implementation original to Utinni under MIT (per D-01). The disposition addendum sits in each new parser .cs file's MIT header. -->

From `UtinniCoreDotNet/UtinniCoreDotNet.csproj` (existing — needs a new `<Compile Include>` block or relies on the default glob to pick up `Formats/Tre/*.cs`):
- UtinniCoreDotNet.csproj is the LEGACY full-XML csproj (ToolsVersion 15.0), not SDK-style. New .cs files need explicit `<Compile Include="Formats\Tre\TreFile.cs" />` etc. entries inside the relevant ItemGroup; the default glob does NOT auto-pick up new folders.

From Plan 04-01 §Output (JsonOutput contract — POST-REVISION):
- `JsonOutput.EmitSuccess(string command, object result, TextWriter writer = null)` returns 0 on success.
- The envelope is built INSIDE JsonOutput — it wraps `result` in `{ schemaVersion: 1, command: <command>, result: <result> }` at the ROOT level (REVIEWS HIGH-6 — schemaVersion + command are NEVER nested inside `result`).
- Callers DO NOT pre-add `schemaVersion` to their result object. The CONTRACT-LOCKED behavior is: the caller passes a `result` payload object that contains command-specific fields (header, records, objects, etc.) and JsonOutput wraps it.
- `JsonOutput.EmitError(string command, string kind, string message, int exitCode, TextWriter writer = null)` returns the exitCode argument verbatim. Error envelope: `{ schemaVersion: 1, command: <command>, error: { kind: <kind>, message: <message> } }`. No nested schemaVersion inside `error`.
- The output is sorted-key, indented, LF, UTF-8 no BOM — the parser/command code doesn't worry about formatting; just build the object/JObject and pass to JsonOutput. JsonOutput writes through `Console.Out` (REVIEWS HIGH-3).

From Plan 04-01 §Output (golden harness):
- `GoldenTestRunner.Matches(string fixtureKey, string actualJson)` — fixtureKey is the relative path WITHOUT `.expected.json` suffix.
- `InProcessCliRunner.Run(params string[] args)` returns `{ ExitCode, Stdout, Stderr }`; stdout is CRLF→LF normalised.
- `FixturePath.Resolve(string relativePath)` returns absolute path under `AppContext.BaseDirectory/Fixtures/...`.

From Research §"Format-Specific Knowledge §TRE container format" (cross-confirmed against `swg-client-v2/TreeFile.cpp` format-reference-read):
- Magic: 4 ASCII bytes `EERT` at offset 0 (the string "TREE" stored as little-endian 32-bit = `0x45455254`).
- Version: 4 ASCII bytes at offset 4. Phase 4 supports `0005` + `0006`. Any other value → TreParseException(UnsupportedVersion).
- Byte order: little-endian for all 4-byte integers in the header and per-record info.
- Header (28 bytes post-version): ResourceCount(int32), InfoOffset(int32), InfoCompression(int32), InfoCompressedSize(int32), NameCompression(int32), NameCompressedSize(int32), NameSize(int32).
- Per-record TreInfo struct (24 bytes): DataSize(int32), DataOffset(int32), DataCompression(int32), DataCompressedSize(int32), Checksum(int32), NameOffset(int32). InfoSize derived as ResourceCount * 24.
- Names block: concatenated null-terminated ASCII filenames at InfoOffset + InfoSize (if InfoCompression == 0) or after inflated InfoBlock if InfoCompression != 0.
- Compression: zlib/deflate when *Compression == 1. Phase 4 uses `System.IO.Compression.DeflateStream` for inflation; raw deflate (no zlib 2-byte header) is the swg-client-v2 convention.
- Lookup semantics: case-insensitive filename match (out of scope for Phase 4 - we just emit the names; consumers do their own lookup).

From Research §Security Domain (mitigation requirements):
- T-04-V5/DoS: every `length` / `*Size` field must be checked: `if (claimedSize < 0 || claimedSize > maxBound)` → TreParseException. maxBound = 256 MB per record, 256 MB per inflated block.
- T-04-DoS: DeflateStream output capped at 256 MB via a wrapper stream that throws if the cumulative read exceeds the cap.

From REVIEWS HIGH-4 (TreFile stream lifecycle — chosen fix path A):
- `Open(string)` opens a FileStream and IMMEDIATELY delegates to `Open(Stream)`. The FileStream's `using` scope must extend across `Open(Stream)` returning — i.e., `Open(string)` is `using (var fs = new FileStream(...)) { return Open(fs); }`. `Open(Stream)` eagerly reads ALL record payloads before returning.
- `Open(Stream)` reads the header, the names block, the record info table, AND every record's compressed bytes into a `byte[][]` cache field on the returned TreFile. Each cached byte[] is the COMPRESSED on-disk payload — decompression (for deflated records) happens on demand inside `GetRecordData(int)` via DeflateStream, but the source bytes are already in memory so no stream needed.
- `GetRecordData(int idx)`: looks up the cached `byte[]` (compressed bytes for record idx), checks compression kind, returns the bytes directly if uncompressed, or inflates via DeflateStream + cap-enforcing read if compressed.
- Memory profile: D-08 fixtures are <128KB each, so the eager-read cost is negligible in Phase 4. Documented in the parser xmldoc as a Phase 4-scoped tradeoff; large-file refactor goes to a future phase if needed (see <pitfalls>).
</interfaces>
</context>

<threat_model>
<!-- Plan 04-02 owns the TRE parser bounds-check + deflate cap mitigations from Research §Security Domain. -->
<threat id="T-04-V5" stride="Tampering/DoS" applies="yes" mitigation_task="Task 2.1 — TreFile.cs validates every header field length is non-negative and bounded by remaining-file-bytes; throws TreParseException(NegativeLength | ChunkLengthExceedsCap | Truncated) before any allocation. Negative-case fixtures malformed-magic.tre, truncated.tre, unsupported-version.tre cover the test side." />
<threat id="T-04-DoS" stride="DoS" applies="yes" mitigation_task="Task 2.1 — Deflate path wraps the DeflateStream output in a length-capped read; total inflated bytes per record capped at 256 MB (defined as private const in TreFile.cs); cap-exceed throws TreParseException(DeflateExpansionExceedsCap). Synthesized fixture covers the happy path; Tier-1 parser test asserts a hand-crafted deflate-bomb input throws." />
<threat id="T-04-V12" stride="Information Disclosure" applies="no" mitigation_task="`parse-tre <path>` reads whatever path the operator supplies; sandboxing is Phase 6+. The `--help` for parse-tre/list-objects mentions 'reads the given file with operator privileges'; no further mitigation in this plan." />
<threat id="T-04-EoP" stride="Elevation of Privilege" applies="no" mitigation_task="parse-tre + list-objects do not load DLLs; Plan 04-04 owns the validate-plugin EoP." />
<threat id="T-04-Json" stride="Code Execution" applies="no" mitigation_task="Plan 04-01 locked TypeNameHandling.None in JsonOutput; this plan inherits the safe default." />
</threat_model>

<json_contract>
<!-- The LOCKED Plan 04-02 JSON contract. Aligned with REVIEWS HIGH-6 (envelope shape) + HIGH-7 (field-name drift fix). -->

**`parse-tre <path>` output shape (LOCKED — REVIEWS HIGH-6 + HIGH-7):**
```
{
  "command": "parse-tre",
  "result": {
    "header": {
      "infoCompressedSize": <int>,
      "infoCompression": <int>,
      "infoOffset": <int>,
      "nameCompressedSize": <int>,
      "nameCompression": <int>,
      "nameSize": <int>,
      "recordCount": <int>,
      "version": "0005" | "0006"
    },
    "records": [
      {
        "checksum": <int32>,
        "compressionKind": "none" | "deflate",
        "compressedSize": <int>,
        "id": "tre:<ordinal>",
        "name": "<filename>",
        "offset": <int>,
        "uncompressedSize": <int>
      },
      ...
    ],
    "source": "<masked-path>"
  },
  "schemaVersion": 1
}
```

**LOCKED FIELD NAMES (REVIEWS HIGH-7 — DO NOT RENAME IN CLEANUP):**
- `header.recordCount` (NOT `resourceCount`) — chosen because React Flow consumers expect `recordCount`; matches IFF chunkCount idiom.
- `records[].id` = `"tre:" + ordinal.ToString(InvariantCulture)` where ordinal is the 0-based index in the TRE info table. Stable across runs because TRE record order is deterministic from the on-disk layout.
- `records[].compressionKind` = `"none"` (DataCompression == 0) or `"deflate"` (DataCompression == 1). Enum STRING, NOT integer.
- `records[].compressedSize` (NOT `dataCompressedSize`) — the on-disk compressed byte count.
- `records[].uncompressedSize` (NOT `dataSize`) — the post-inflation byte count (same as compressedSize when compressionKind == "none").
- `records[].offset` (NOT `dataOffset`) — file offset.

**Stable sort:** `records[]` is emitted in the order found in the TRE info table (NOT sorted). Future React Flow consumer can `useNodesState(json.result.records.map(r => ({ id: r.id, data: { label: r.name, kind: r.compressionKind } })))` directly.

**`list-objects <ws.iff>` output shape (LOCKED — REVIEWS HIGH-7):**
```
{
  "command": "list-objects",
  "result": {
    "objects": [
      { "id": "<templateName>", "templateName": "<string>" },
      ...
    ],
    "source": "<masked-path>"
  },
  "schemaVersion": 1
}
```

**LOCKED FIELD NAMES:**
- `objects[].id` = the templateName string. Stable identifier — matches templateName because OBJS-payload strings are themselves unique within the ws.iff scope. (If a real-world ws.iff has duplicates, a follow-up phase can add an `:ordinal` suffix; Phase 4's synthesized fixture has no duplicates.)
- `objects[].templateName` = the same string. Keeping both fields lets future consumers strip the `tre:` prefix from records[].id without breaking list-objects parity.

**Stable sort:** `objects[]` is sorted by `id` (Ordinal string comparison) for golden stability across `OS Directory.GetFiles` enumeration variance. A future React/React Flow consumer ingests `result.objects` as a flat node list directly.

**React Flow portability note (load-bearing, verbatim — do NOT drop in cleanup):**
"Both `parse-tre` and `list-objects` ship flat-list outputs as part of schemaVersion: 1 — a future React/React Flow tool can lift `result.records` / `result.objects` as a flat node array without re-shaping. Do NOT rename the `id`/`templateName`/`compressionKind`/`compressedSize`/`recordCount` keys in cleanup refactors; they are the contract per REVIEWS HIGH-7. Plan 04-02 commands intentionally stay flat (vs. 04-03 inspect-iff's tree+flat dual projection) because TRE records and world-snapshot OBJS entries have no inherent hierarchy at this CLI tier."
</json_contract>

<pitfalls>
<!-- Per REVIEWS MEDIUM-13 — architectural debt to be addressed in a future phase. -->

**Pitfall A (REVIEWS MEDIUM-13): list-objects byte-scan is provisional.**

`ListObjectsCommand.Run` scans raw file bytes for the 4-byte `OBJS` sentinel and reads the subsequent big-endian length + null-terminated string payload. This is intentionally INDEPENDENT of Plan 04-03's `IffReader` so 04-02 and 04-03 can run in parallel waves per CONTEXT.md D-05 (04-02 depends on 04-01 only; not on 04-03).

The byte-scan is BRITTLE on real-world world-snapshot files:
- `IndexOf("OBJS")` can false-positive inside opaque chunk payloads that happen to contain the ASCII byte sequence `0x4F 0x42 0x4A 0x53`.
- No FORM-boundary validation — the scan doesn't verify that the matched `OBJS` is a chunk-id at a valid offset; it just trusts the first match.
- No big-endian header length validation beyond `N+8+L <= bytes.Length`.

**Phase 6+ refactor TODO (NOT this plan's scope):** Once Plan 04-03 lands and `UtinniCoreDotNet.Formats.Iff.IffReader` is stable on master, rewrite `ListObjectsCommand.Run` to:
1. Open the file via `IffReader.Read(string)`.
2. Walk `IffDocument.AllNodesInPreorder` filtering `IffLeafChunk` instances with `TypeId == "OBJS"`.
3. Decode each OBJS leaf's `Data` payload (big-endian length + null-terminated strings) using the IFF chunk semantics.

This is a pure refactor — same JSON contract, more robust scanning. Tracked here as architectural debt; not blocking Phase 4 close. A follow-up issue or Phase 6 plan tasks the rewrite.

**Pitfall B (REVIEWS HIGH-4 forward-looking): TreFile eager-read works for <128KB fixtures.**

D-08 fixtures are <128KB so the eager-read cost is trivial (sub-millisecond at full memory cost). For very large TRE files (>100MB, realistic SWG production sizes), the eager-read consumes memory equal to file size, which may surprise large-batch consumers in Phase 7+ (TRE Browser plugin).

**Phase 7+ refactor option (NOT this plan's scope):** If Phase 7's TRE Browser shows memory-pressure symptoms reading production TRE files, refactor `TreFile` to implement `IDisposable` and own a `FileStream` (REVIEWS HIGH-4 fix path B). The contract change: `using (var tre = TreFile.Open(path)) { ... }`. `GetRecordData` then seeks within the live stream. Phase 4's eager-read contract is preserved as the default for the small-file CLI inspection use case.

**Pitfall C (REVIEWS LOW-20 closed): v0006 covered.**

Earlier plan revision had v0006 mentioned in artifacts but only v0005 tested. Task 2.3 now ships `synthesized-2record-v0006.tre` + golden. Task 2.4 adds an explicit `[InlineData]` row for v0006 in `ParseTreCommandTests`. No further action needed.
**Pitfall D (plan-checker iter-2 WARNING-8 — fixture-cap documentation).** Per CONTEXT.md D-03 the absolute fixture cap is 256KB; this plan enforces a tighter operational 128KB cap (per CONTEXT.md D-08's observed "small (<128KB each)" target) because synthesized fixtures comfortably fit there. The 128KB gate (`131072` bytes in PowerShell `Where-Object { $_.Length -gt 131072 }` verify-gates) is a self-imposed regression guard, NOT a contradiction of D-03's ceiling. If a future revision needs to ship a single sample between 128KB and 256KB (still legal per D-03), the verify-gate threshold for that fixture is bumped explicitly in the plan diff — never silently.

</pitfalls>

<tasks>

<task type="auto" tdd="true">
  <name>Task 2.1: Land UtinniCoreDotNet/Formats/Tre/ parser with EAGER-READ contract + Tier-1 tests (REVIEWS HIGH-4 fix path A)</name>
  <files>UtinniCoreDotNet/Formats/Tre/TreFile.cs, UtinniCoreDotNet/Formats/Tre/TreHeader.cs, UtinniCoreDotNet/Formats/Tre/TreRecord.cs, UtinniCoreDotNet/Formats/Tre/TreParseException.cs, UtinniCoreDotNet/UtinniCoreDotNet.csproj, UtinniCoreDotNet.Tests/FormatsTests/Tre/TreFileTests.cs, UtinniCoreDotNet.Tests/FormatsTests/Tre/TreFileFixtures.cs</files>
  <read_first>
    - UtinniCoreDotNet/PluginFramework/PluginLoader.cs lines 1-23 (MIT header — copy verbatim to every new parser file; then append the D-01 disposition addendum line for files under `Formats/Tre/`).
    - UtinniCoreDotNet/UtinniCoreDotNet.csproj (FULL FILE — legacy non-SDK csproj uses explicit `<Compile Include="...">` entries inside ItemGroups; new Formats/Tre/*.cs files need explicit entries).
    - UtinniCoreDotNet.Tests/HotkeyTests.cs (pure-managed Tier-1 test template — no native deps, no fixture-DLL dance; the cleanest analog for parser unit tests).
    - .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-PATTERNS.md §"`UtinniCoreDotNet/Formats/Tre/TreFile.cs` (+ TreHeader, TreRecord, TreParseException)", §"`UtinniCoreDotNet.Tests/FormatsTests/Tre/TreFileTests.cs` (+ `Iff/IffReaderTests.cs`)", §"MIT License Header (apply to EVERY new .cs file)" (incl. D-01 disposition addendum).
    - .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-RESEARCH.md §"Format-Specific Knowledge §TRE container format" (binary layout reference), §"Code Examples §Example 1" (idiom only — implementer rewrites in Utinni style), §"Common Pitfalls §Pitfall 4" (test class layout — nested per-format subdirs), §"Security Domain §Known Threat Patterns" (T-04-V5 + T-04-DoS mitigations).
    - .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-REVIEWS.md §"HIGH #4" (TreFile stream lifecycle — fix path A: eager-read into byte[][] cache; chosen because D-08 fixtures are <128KB).
    - Reference reads (LICENSE-EXCLUDED): D:/Code/swg-client-v2/src/engine/shared/library/sharedFile/src/shared/{TreeFile.h, TreeFile.cpp} — read for FORMAT UNDERSTANDING ONLY. Copy zero code, comments, or identifier names. Implementation original to Utinni under MIT.
  </read_first>
  <behavior>
    - Test 1 (happy path v0005, in-memory): Hand-build a 3-record TRE byte sequence with magic `EERT`, version `0005`, record-count 3. `TreFile.Open(new MemoryStream(bytes))` returns a TreFile whose `Header.RecordCount == 3`, `Header.Version == "0005"`, `Records.Count == 3`, each Record has the expected UncompressedSize/Offset/Name. The MemoryStream is then disposed; subsequent `GetRecordData(0)` STILL returns the expected payload bytes (REVIEWS HIGH-4 regression guard — proves the eager-read cache).
    - Test 2 (happy path v0006, in-memory): Same shape, with version `0006`. `Header.Version == "0006"`. Asserts v0006 parses identically within Phase 4's scope. (REVIEWS LOW-20 — v0006 coverage.)
    - Test 3 (bad magic): Hand-build bytes starting with `XXXX` instead of `EERT`. Open throws TreParseException with Kind=BadMagic.
    - Test 4 (unsupported version): Hand-build bytes with magic `EERT` but version `0009`. Open throws TreParseException with Kind=UnsupportedVersion; the exception's Message includes the string `"0009"` for triage.
    - Test 5 (truncated record table): Hand-build bytes with header claiming RecordCount=3 but file ends mid-second-record. Open throws TreParseException with Kind=Truncated.
    - Test 6 (deflated record + eager-read across dispose): Hand-build a TRE whose record 1 has DataCompression=1 with a known deflate-compressed payload. Open the stream INSIDE a `using` block; after the using block exits (stream disposed), call `tre.GetRecordData(1)` — it MUST return the inflated bytes matching the original (proves eager-read). This is the central REVIEWS HIGH-4 regression guard.
    - Test 7 (deflate cap — T-04-DoS): Hand-build a TRE whose record 0 has DataCompressedSize=10 (small) but DataSize=1_000_000_000 (1 GB claimed; >256 MB cap). Opening header succeeds; but `GetRecordData(0)` throws TreParseException with Kind=DeflateExpansionExceedsCap. (Per the eager-read model, the cap check happens at Open-time if the parser inflates eagerly OR at GetRecordData-time if inflation is lazy. Implementer chooses: recommended is to cache the COMPRESSED bytes during Open and inflate on demand inside GetRecordData — minimises memory cost for files where consumers don't read every record. The cap is enforced inside GetRecordData's inflation loop. Document this in xmldoc.)
    - Test 8 (negative length — T-04-V5): Hand-build a TRE whose RecordCount field is `-1` (or `0x80000001`). Open throws TreParseException with Kind=NegativeLength.
    - Test 9 (chunk exceeds file — T-04-V5): Hand-build a TRE with InfoOffset=999_999 in a 100-byte file. Open throws TreParseException with Kind=ChunkLengthExceedsCap or Truncated (either is acceptable; pick one and codify).
    - Test 10 (REVIEWS HIGH-4 regression guard — explicit): A dedicated `GetRecordData_AfterOpenStreamDisposed_StillReturnsBytes` test that:
      1. Wraps `new MemoryStream(bytes)` in a `using` block.
      2. Calls `TreFile.Open(stream)` and captures the result.
      3. Lets the `using` exit so the MemoryStream is disposed.
      4. Calls `tre.GetRecordData(0)` and asserts the result is non-null and matches the expected bytes.
      This test would FAIL if `Open(Stream)` or `GetRecordData` lazy-seeked the original stream.
  </behavior>
  <action>
    Create the four parser files under `UtinniCoreDotNet/Formats/Tre/`. Each starts with the 23-line MIT header copied verbatim from `UtinniCoreDotNet/PluginFramework/PluginLoader.cs:1-23`, followed by the D-01 disposition addendum block (4 lines, exact wording from PATTERNS.md §"MIT License Header" §"D-01 disposition addendum for parser files only"). Namespace `UtinniCoreDotNet.Formats.Tre`. Allman braces, 4-space indent, PascalCase, no underscore prefix per existing repo style.

    `TreParseException.cs`: `public enum TreParseError { BadMagic, UnsupportedVersion, Truncated, NegativeLength, ChunkLengthExceedsCap, DeflateExpansionExceedsCap }`. `public sealed class TreParseException : System.Exception` with `public TreParseError Kind { get; }` and a single ctor `public TreParseException(TreParseError kind, string message) : base(message) { Kind = kind; }`.

    `TreHeader.cs`: `public sealed class TreHeader` carrying (all properties have `public ... { get; internal set; }`):
    - `Version` (string)
    - `RecordCount` (int) — REVIEWS HIGH-7 naming (replaces the earlier "ResourceCount" candidate).
    - `InfoOffset` (int)
    - `InfoCompression` (int)
    - `InfoCompressedSize` (int)
    - `NameCompression` (int)
    - `NameCompressedSize` (int)
    - `NameSize` (int)

    `TreRecord.cs`: `public sealed class TreRecord` carrying (all properties have `public ... { get; internal set; }`):
    - `UncompressedSize` (int) — REVIEWS HIGH-7 naming (replaces "DataSize").
    - `Offset` (int) — REVIEWS HIGH-7 naming (replaces "DataOffset").
    - `CompressionKind` (string) — `"none"` if raw, `"deflate"` if deflated. Set in parser from the int compression field.
    - `CompressedSize` (int) — REVIEWS HIGH-7 naming (replaces "DataCompressedSize").
    - `Checksum` (int)
    - `NameOffset` (int)
    - `Name` (string) — set after parsing the names block.

    `TreFile.cs` is the heart of the parser. Class shape:
    - `public sealed class TreFile`.
    - Private field `private readonly byte[][] _recordCompressedBytes;` — REVIEWS HIGH-4 fix path A. Holds the on-disk compressed bytes for each record, populated during `Open(Stream)`. Indexed by record ordinal.
    - `public TreHeader Header { get; }` + `public IReadOnlyList<TreRecord> Records { get; }`.
    - Private constructor: `private TreFile(TreHeader header, IReadOnlyList<TreRecord> records, byte[][] recordCompressedBytes) { Header = header; Records = records; _recordCompressedBytes = recordCompressedBytes; }`.
    - `public static TreFile Open(string path)` — wraps `using (var fs = new FileStream(path, FileMode.Open, FileAccess.Read, FileShare.Read)) { return Open(fs); }`. The `using` extends across the `Open(Stream)` call — `Open(Stream)` MUST complete its read before this method returns. Throws `FileNotFoundException` if path missing (caught by the CLI command and surfaced as exit 3).
    - `public static TreFile Open(Stream stream)` — the testable seam AND the eager-read enforcer. Reads the header, validates magic + version + sane field ranges, reads the names block (decompressing if NameCompression != 0), reads the per-record info table (decompressing if InfoCompression != 0), assembles `Records` with names resolved from NameOffset. THEN — the critical REVIEWS HIGH-4 step — for each record, SEEKS to record.Offset, reads record.CompressedSize bytes into a fresh byte[], stores in `_recordCompressedBytes[i]`. After this method returns, the original `Stream` is no longer referenced — caller may dispose it freely. Total upper-bound bound check: every claimed offset/size must satisfy `0 <= value <= streamLength`; every claimed length must satisfy `0 <= value <= 256 * 1024 * 1024` (`MaxBlockSize = 256 * 1024 * 1024` private const).
    - `public byte[] GetRecordData(int index)` — REVIEWS HIGH-4 fix: NO STREAM REQUIRED. Look up `_recordCompressedBytes[index]`. If `Records[index].CompressionKind == "none"`, return the bytes directly. If `"deflate"`, wrap in `MemoryStream` + `DeflateStream` (Decompress mode) + cap-enforcing read that throws `TreParseException(DeflateExpansionExceedsCap, ...)` if total inflated bytes exceed `MaxBlockSize` or `record.UncompressedSize` (whichever is smaller). The cap-enforcing read pattern: copy from DeflateStream into a `MemoryStream`, in a loop reading at most `MaxBlockSize - bytesReadSoFar` bytes per iteration, throwing on the next iteration if more remain. After successful inflate, assert `output.Length == record.UncompressedSize` (Tampering check — if zlib produced a different output size than the metadata claims, the file is corrupt).
    - Private helpers: `private static void ValidateLength(int claimed, long max, TreParseError kindOnNegative, TreParseError kindOnOverflow, string fieldName)` — single source of truth for bounds checking; throws with the field name in the message.
    - Use `System.IO.BinaryReader` with default little-endian semantics for header + record reads.
    - Endianness sanity assertion at the top of Open: `if (!System.BitConverter.IsLittleEndian) throw new System.NotSupportedException(...)` — Phase 4 only runs on Windows x86, but document the assumption.
    - xmldoc note on `TreFile`: "Phase 4 CLI inspection — eager-read contract. `Open(Stream)` reads ALL record payload bytes into memory before returning, so the source stream may be disposed immediately after. Memory cost: O(total compressed size). For files >100MB consider a future IDisposable refactor (see Plan 04-02 §pitfalls). REVIEWS HIGH-4 path A."

    Modify `UtinniCoreDotNet/UtinniCoreDotNet.csproj` (the legacy non-SDK csproj): add `<Compile Include="Formats\Tre\TreFile.cs" />`, `<Compile Include="Formats\Tre\TreHeader.cs" />`, `<Compile Include="Formats\Tre\TreRecord.cs" />`, `<Compile Include="Formats\Tre\TreParseException.cs" />` to the existing `<Compile>` ItemGroup (the one already containing `PluginFramework\PluginLoader.cs`-style entries — locate it by Read).

    Create `UtinniCoreDotNet.Tests/FormatsTests/Tre/TreFileTests.cs`. MIT header (no D-01 addendum needed — tests don't derive from external sources). Namespace `UtinniCoreDotNet.Tests.FormatsTests.Tre`. Per Research §Pitfall 4, nested folder organisation is the agreed pattern. Test class `public class TreFileTests` with the ten [Fact] tests described above. Use the Phase 1 D-04 naming convention: `Open_ValidV5000ThreeRecord_ReturnsHeaderAndRecords`, `Open_ValidV0006TwoRecord_ReturnsHeaderAndRecords`, `Open_BadMagic_ThrowsBadMagicException`, `Open_UnsupportedVersionField_ThrowsUnsupportedVersionException`, `Open_TruncatedRecordTable_ThrowsTruncatedException`, `GetRecordData_DeflatedRecordAfterDispose_ReturnsInflatedBytes`, `GetRecordData_DeflateClaimedGigabyte_ThrowsExpansionCapException`, `Open_NegativeResourceCount_ThrowsNegativeLengthException`, `Open_InfoOffsetExceedsFile_ThrowsTruncatedOrCapException`, `GetRecordData_AfterOpenStreamDisposed_StillReturnsBytes`.

    Create `UtinniCoreDotNet.Tests/FormatsTests/Tre/TreFileFixtures.cs` (test helper, not a test). Public static methods that return byte[] for each test scenario: `BuildValidV5000ThreeRecord()`, `BuildValidV0006TwoRecord()`, `BuildBadMagic()`, `BuildUnsupportedVersion(string version = "0009")`, `BuildTruncatedRecordTable()`, `BuildDeflatedRecord(byte[] payload, string version = "0005")`, `BuildClaimedGigabyteDeflate()`, `BuildNegativeResourceCount()`, `BuildInfoOffsetExceedsFile()`. Each helper uses `System.IO.MemoryStream` + `System.IO.BinaryWriter` (default little-endian) to assemble the byte sequence per the format reference. The deflate helper uses `System.IO.Compression.DeflateStream` to produce the compressed bytes for embedding.

    Update `UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj` if needed: the `<DefaultItemExcludes>$(DefaultItemExcludes);Fixtures\**</DefaultItemExcludes>` line excludes only the `Fixtures/` subtree (Phase 1 csproj convention) — `FormatsTests/` is NOT excluded, so the default Compile glob picks up the new test files without explicit `<Compile Include>` entries. No csproj edit needed for the test project.

    Commit message format (atomic per CONTEXT.md §specifics — parser + Tier-1 tests in ONE commit):
    `feat(04-02): TRE container parser with eager-read contract + Tier-1 tests (D-01 clean-room MIT; REVIEWS HIGH-4 fix)`
  </action>
  <verify>
    <automated>msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86 succeeds. dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release --filter "FullyQualifiedName~TreFileTests" exits 0 with 10 tests passing. grep -nF "Format understood by reading swg-client-v2" UtinniCoreDotNet/Formats/Tre/TreFile.cs returns one match (D-01 disposition line present). grep -nF "Format understood by reading swg-client-v2" UtinniCoreDotNet/Formats/Tre/TreHeader.cs UtinniCoreDotNet/Formats/Tre/TreRecord.cs UtinniCoreDotNet/Formats/Tre/TreParseException.cs returns three matches (one per file). grep -nE "Compile Include=\"Formats\\\\Tre\\\\Tre(File|Header|Record|ParseException)\\.cs\"" UtinniCoreDotNet/UtinniCoreDotNet.csproj returns 4 matches (all four files wired into the legacy csproj). grep -cE "private const.*MaxBlockSize.*256" UtinniCoreDotNet/Formats/Tre/TreFile.cs returns at least 1 (256 MB cap landed; T-04-DoS mitigation visible). grep -nE "byte\\[\\]\\[\\]\\s+_recordCompressedBytes" UtinniCoreDotNet/Formats/Tre/TreFile.cs returns at least one match (REVIEWS HIGH-4 eager-read cache field present). grep -nE "AfterOpenStreamDisposed_StillReturnsBytes" UtinniCoreDotNet.Tests/FormatsTests/Tre/TreFileTests.cs returns exactly one match (the REVIEWS HIGH-4 regression guard test).</automated>
  </verify>
  <done>TRE parser is functional, security-checked, Tier-1-tested, AND immune to stream-lifetime bugs (REVIEWS HIGH-4 fix path A — eager-read into byte[][] cache). The ten tests are independent of the CLI layer — they would still pass if the CLI were deleted, because they exercise TreFile.Open(Stream) directly. D-01 disposition line present on every new Formats/Tre/*.cs file. The 256 MB cap and bounds checks are landed and tested. Field names align with REVIEWS HIGH-7: `RecordCount`, `UncompressedSize`, `Offset`, `CompressionKind`, `CompressedSize`. Ready for Task 2.2 to consume.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.2: Implement parse-tre + list-objects CLI commands with CONTRACT-LOCKED field shape (REVIEWS HIGH-7)</name>
  <files>Utinni.Cli/Commands/ParseTreCommand.cs, Utinni.Cli/Commands/ListObjectsCommand.cs</files>
  <read_first>
    - Utinni.Cli/Commands/ParseTreCommand.cs (current Plan 04-01 stub — overwrite the Run body).
    - Utinni.Cli/Commands/ListObjectsCommand.cs (current Plan 04-01 stub — overwrite the Run body).
    - Utinni.Cli/Output/JsonOutput.cs (the API contract — `EmitSuccess(string command, object result, TextWriter writer = null)` returns int; `EmitError(...)` returns int; envelope is wrapped at ROOT level per REVIEWS HIGH-6).
    - UtinniCoreDotNet/Formats/Tre/TreFile.cs (Task 2.1 output — the parser surface to consume).
    - This plan's `<json_contract>` block (the LOCKED field shape per REVIEWS HIGH-7).
    - .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-PATTERNS.md §"`Utinni.Cli/Commands/ParseTreCommand.cs` (and siblings)" (exit code wiring + try/catch pattern).
    - .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-RESEARCH.md §"Code Examples §Example 3" (ValidatePluginCommand idiom — same shape for parse-tre / list-objects).
    - .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-CONTEXT.md §"Decisions §D-02" (exit code contract: 0=ok, 1=usage, 2=parse-error, 3=fixture-not-found).
    - .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-REVIEWS.md §"HIGH #7" (field-name drift fix — contract is correct, BuildResult must match it).
  </read_first>
  <behavior>
    - parse-tre <valid.tre> → exit 0 + JSON envelope (top-level keys `command, result, schemaVersion` per REVIEWS HIGH-6; NO nested schemaVersion). `result` carries `header { ... }, records [ ... ], source <masked-path>`. Per REVIEWS HIGH-7: header.recordCount (NOT resourceCount); records[] entries carry `id` (`tre:<ordinal>`), `name`, `offset`, `compressedSize`, `uncompressedSize`, `compressionKind` (`"none"` or `"deflate"`), `checksum`. Keys sorted alphabetically per JsonOutput contract.
    - parse-tre <malformed.tre> → exit 2 + JSON envelope with `error.kind == "BadMagic"`. The `error` sub-object has exactly `kind` and `message`; no nested schemaVersion.
    - parse-tre /tmp/nonexistent.tre → exit 3 + JSON envelope with `error.kind == "FileNotFound"`.
    - parse-tre <iff-not-tre.iff> → exit 2 (parser throws BadMagic when the IFF magic `FORM` doesn't match TRE's `EERT`).
    - list-objects <synthesized-ws.iff> → exit 0 + JSON envelope. `result.objects` is sorted by `id` (Ordinal). Each object carries `{ id: <templateName>, templateName: <string> }` per REVIEWS HIGH-7.
    - list-objects /tmp/nonexistent.iff → exit 3 + FileNotFound error envelope.
    - list-objects <malformed.iff> → exit 2 + parse-error envelope. (For Plan 04-02 list-objects uses a simple sentinel-scan over raw bytes per `<pitfalls>` Pitfall A; the full IFF parser lands in Plan 04-03.)
  </behavior>
  <action>
    Replace `Utinni.Cli/Commands/ParseTreCommand.cs`'s Run body with the real implementation. Keep the existing [Verb] options class definition + the MIT header at the top of the file. New Run body:
    - Check `File.Exists(o.Path)` first; if false, return `JsonOutput.EmitError("parse-tre", "FileNotFound", "TRE file not found: " + o.Path, exitCode: 3)`.
    - Wrap the actual parse in try/catch:
      - `try { var tre = UtinniCoreDotNet.Formats.Tre.TreFile.Open(o.Path); return JsonOutput.EmitSuccess("parse-tre", BuildResult(tre, o.Path)); }`
      - `catch (UtinniCoreDotNet.Formats.Tre.TreParseException ex) { return JsonOutput.EmitError("parse-tre", ex.Kind.ToString(), ex.Message, exitCode: 2); }`
      - `catch (System.IO.IOException ex) { return JsonOutput.EmitError("parse-tre", "IoError", ex.Message, exitCode: 2); }` — catch-all for OS-level read failures. Do NOT catch generic Exception — let unexpected exception types bubble for diagnosis.

    - `private static object BuildResult(TreFile tre, string sourcePath)` returns an anonymous object MATCHING THE LOCKED CONTRACT (REVIEWS HIGH-7):

      ```csharp
      return new {
          source = sourcePath,
          header = new {
              version            = tre.Header.Version,
              recordCount        = tre.Header.RecordCount,            // NOT resourceCount
              infoOffset         = tre.Header.InfoOffset,
              infoCompression    = tre.Header.InfoCompression,
              infoCompressedSize = tre.Header.InfoCompressedSize,
              nameCompression    = tre.Header.NameCompression,
              nameCompressedSize = tre.Header.NameCompressedSize,
              nameSize           = tre.Header.NameSize
          },
          records = tre.Records.Select((r, ordinal) => new {
              id               = "tre:" + ordinal.ToString(System.Globalization.CultureInfo.InvariantCulture),  // stable id per REVIEWS HIGH-7
              name             = r.Name,
              offset           = r.Offset,
              compressedSize   = r.CompressedSize,
              uncompressedSize = r.UncompressedSize,
              compressionKind  = r.CompressionKind,                    // enum string "none" | "deflate", NOT int
              checksum         = r.Checksum
          }).ToList()
      };
      ```

      NOTE: Plan 04-02 callers do NOT pre-add `schemaVersion` — JsonOutput.EmitSuccess wraps with `{ schemaVersion: 1, command: "parse-tre", result: <above> }` at the ROOT level per REVIEWS HIGH-6 fix in Plan 04-01 Task 1.2.

      Anonymous-object property naming uses camelCase. The SortedKeyContractResolver from Plan 04-01 will alphabetise at serialise time.

      Per Research §"Assumptions Log" A2: Checksum is emitted as a raw int32 without validation; Phase 4 reports, doesn't verify.

    Replace `Utinni.Cli/Commands/ListObjectsCommand.cs`'s Run body. The list-objects command operates on a world-snapshot IFF — for Plan 04-02, use a byte-level scan documented as architectural debt in `<pitfalls>` Pitfall A. Plan 04-03 lands the full IffReader and Phase 6+ rewrites this command.

    New Run body for ListObjectsCommand:
    - Check `File.Exists(o.Path)` → exit 3 + FileNotFound on miss.
    - Read all bytes via `System.IO.File.ReadAllBytes(o.Path)`. Cap at 256 MB (`if (bytes.Length > 256 * 1024 * 1024) return EmitError(... "FileTooLarge", ..., 2)`).
    - Scan for the synthesized fixture's known chunk-id: pick `OBJS` (4 ASCII bytes `0x4F 0x42 0x4A 0x53`) as the sentinel. The chunk-id appears at a known offset in the synthesized fixture; in production .ws.iff files, the OBJS chunk follows a FORM container and carries 4 bytes of big-endian length then the payload.
    - Locate the OBJS chunk: search the byte array for the four-byte sequence; on miss, return `EmitError("list-objects", "NoObjsChunk", "...", 2)`. On hit at offset N: read N+4..N+8 as big-endian uint32 (chunk length L), validate `N+8+L <= bytes.Length`, then read the L payload bytes starting at N+8.
    - Walk the L-byte payload extracting null-terminated ASCII strings: iterate `current = 0`; while `current < L`: scan forward to the next null byte (or end-of-payload), `Encoding.ASCII.GetString(payload, current, nextNull - current)` is the template path; if length > 0, add to the result list; `current = nextNull + 1`.
    - SORT the resulting list of template paths by Ordinal string comparison (REVIEWS HIGH-7 — golden stability).
    - Emit per the LOCKED contract (REVIEWS HIGH-7 — every object carries `id` AND `templateName`):

      ```csharp
      return JsonOutput.EmitSuccess("list-objects", new {
          source  = o.Path,
          objects = templatePaths
              .OrderBy(p => p, System.StringComparer.Ordinal)
              .Select(p => new {
                  id           = p,         // stable id = templateName per REVIEWS HIGH-7
                  templateName = p
              })
              .ToList()
      });
      ```

      Add an inline source comment above the OBJS scan: `// REVIEWS MEDIUM-13: byte-scan is provisional architectural debt; Phase 6+ refactor on top of Plan 04-03's IffReader. See 04-02-PLAN.md <pitfalls> Pitfall A.`

    Both commands inherit the [Verb]/[Value] options classes unchanged from Plan 04-01 — only the Run method body changes.
  </action>
  <verify>
    <automated>msbuild Utinni.sln /m /p:Configuration=Release /p:Platform=x86 succeeds. PowerShell: `& bin\Release\utinni-cli.exe parse-tre ($env:TEMP + '\totally-missing.tre')` exits 3 and prints valid JSON containing the literal substring "FileNotFound". (After Task 2.3 lands fixtures:) PowerShell: `& bin\Release\utinni-cli.exe parse-tre Utinni.Cli.Tests\bin\Release\net472\Fixtures\tre\synthesized-3record-v0005.tre` exits 0 and prints valid JSON whose `result.records` array has 3 entries, each carrying `id, name, offset, compressedSize, uncompressedSize, compressionKind, checksum`. `& bin\Release\utinni-cli.exe parse-tre Utinni.Cli.Tests\bin\Release\net472\Fixtures\tre\malformed-magic.tre` exits 2 and prints JSON containing "BadMagic". grep -nF "TreFile.Open" Utinni.Cli/Commands/ParseTreCommand.cs returns one match. grep -nF "TreParseException" Utinni.Cli/Commands/ParseTreCommand.cs returns at least one match (the catch clause). grep -nE "\"tre:\"\\s*\\+\\s*ordinal" Utinni.Cli/Commands/ParseTreCommand.cs returns one match (stable id per REVIEWS HIGH-7). grep -nE "recordCount\\s*=\\s*tre\\.Header\\.RecordCount" Utinni.Cli/Commands/ParseTreCommand.cs returns one match (HIGH-7 field naming). grep -nE "compressionKind\\s*=\\s*r\\.CompressionKind" Utinni.Cli/Commands/ParseTreCommand.cs returns one match. grep -nE "OBJS|0x4F.*0x42.*0x4A.*0x53" Utinni.Cli/Commands/ListObjectsCommand.cs returns at least one match (sentinel scan). grep -nE "id\\s*=\\s*p.*templateName\\s*=\\s*p|templateName\\s*=\\s*p.*id\\s*=\\s*p" Utinni.Cli/Commands/ListObjectsCommand.cs returns at least one match (REVIEWS HIGH-7 — both id and templateName present). grep -nF "REVIEWS MEDIUM-13" Utinni.Cli/Commands/ListObjectsCommand.cs returns at least one match (architectural debt note inline).</automated>
  </verify>
  <done>Both commands produce real output instead of the NotImplemented stub. parse-tre exit codes 0/2/3 align with D-02. list-objects works against the (forthcoming Task 2.3) synthesized world-snapshot fixture. The commands consume Task 2.1's parser surface; if any Task 2.1 invariant breaks, the commands fail loudly with the structured error envelope. CRITICAL: field shape matches the LOCKED contract per REVIEWS HIGH-7 — `records[].id`, `records[].compressionKind`, `header.recordCount`, `objects[].id` — and the architectural debt of list-objects's byte-scan is recorded inline pointing to the Phase 6+ refactor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.3: Build + commit TRE fixtures (synthesized 3-record v0005 + 2-record v0006 + 3 negatives + 1 synthesized ws.iff) + their expected.json goldens</name>
  <files>Utinni.Cli.Tests/Fixtures/tre/synthesized-3record-v0005.tre, Utinni.Cli.Tests/Fixtures/tre/synthesized-3record-v0005.expected.json, Utinni.Cli.Tests/Fixtures/tre/synthesized-2record-v0006.tre, Utinni.Cli.Tests/Fixtures/tre/synthesized-2record-v0006.expected.json, Utinni.Cli.Tests/Fixtures/tre/malformed-magic.tre, Utinni.Cli.Tests/Fixtures/tre/malformed-magic.expected.json, Utinni.Cli.Tests/Fixtures/tre/truncated.tre, Utinni.Cli.Tests/Fixtures/tre/truncated.expected.json, Utinni.Cli.Tests/Fixtures/tre/unsupported-version.tre, Utinni.Cli.Tests/Fixtures/tre/unsupported-version.expected.json, Utinni.Cli.Tests/Fixtures/world-snapshot/synthesized-ws.iff, Utinni.Cli.Tests/Fixtures/world-snapshot/synthesized-ws.expected.json</files>
  <read_first>
    - UtinniCoreDotNet.Tests/FormatsTests/Tre/TreFileFixtures.cs (Task 2.1 output — the byte-builder helpers; reuse the build logic for committing the .tre fixtures).
    - Utinni.Cli/Commands/ParseTreCommand.cs (Task 2.2 output — the JSON shape parse-tre emits drives the expected.json content).
    - Utinni.Cli/Commands/ListObjectsCommand.cs (Task 2.2 output — drives the ws expected.json content).
    - .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-PATTERNS.md §"`Utinni.Cli.Tests/Fixtures/{tre,iff,plugins,world-snapshot}/`" (fixture layout convention).
    - .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-CONTEXT.md §"Decisions §D-03" (in-repo synth, <256KB cap), §"Decisions §D-08" (per-command harness shape; TRE negative cases enumerated).
    - .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-RESEARCH.md §"Format-Specific Knowledge §TRE container format" §"Edge cases the synthesized 3-record fixture should cover".
    - .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-REVIEWS.md §"LOW #20" (v0006 in artifacts but only v0005 tested — closed by adding a dedicated v0006 fixture).
  </read_first>
  <behavior>
    - synthesized-3record-v0005.tre is < 4 KB. Magic EERT, version 0005, 3 records: record 0 carries uncompressed ASCII payload `Hello, World!` with name `hello.txt`; record 1 carries deflated payload of the ASCII string `The quick brown fox jumps over the lazy dog.` with name `quick.txt`; record 2 carries uncompressed empty payload (UncompressedSize=0) with name `empty.bin`.
    - synthesized-2record-v0006.tre is < 2 KB (REVIEWS LOW-20). Magic EERT, version 0006, 2 records: record 0 carries uncompressed ASCII `v0006 marker` with name `marker.txt`; record 1 carries deflated payload of `Phase 4 v0006 coverage` with name `v6.txt`. Demonstrates v0006 parses identically to v0005 within Phase 4's scope.
    - malformed-magic.tre is < 256 bytes. Header starts `XXXX` then has a valid-looking version + zeroed body. Triggers TreParseException(BadMagic).
    - truncated.tre is < 256 bytes. Magic+version+claim-ResourceCount=3, but file ends after only one TreInfo struct. Triggers TreParseException(Truncated).
    - unsupported-version.tre is < 256 bytes. Magic EERT, version `0009`, otherwise empty. Triggers TreParseException(UnsupportedVersion).
    - synthesized-ws.iff is < 2 KB. Starts with FORM container, contains an OBJS leaf chunk whose 4-byte big-endian length covers a payload of three null-terminated template paths: `object/creature/player/shared_player_human_male.iff\0object/tangible/utility/shared_chronicle_repository_s1.iff\0object/tangible/utility/shared_kiosk_unique_corellia_basicvar01.iff\0`.
    - Each `.expected.json` is committed verbatim as the canonical output of running the CLI against the corresponding fixture. Re-running the CLI on the same machine + later on CI produces byte-identical output (modulo path-string interpolation in `source` field).
    - Volatile fields: `result.source` interpolates the absolute path passed on the command line. Tests post-process the actual JSON to replace the absolute path with sentinel `<fixture-path>`; the committed expected.json carries `<fixture-path>` for this field. Same pattern as Plan 04-01's assembly-version masking.
  </behavior>
  <action>
    Build the fixtures using the Task 2.1 `TreFileFixtures.Build*` helpers (Option A from the original plan). Add a one-shot xUnit test class `TreFixtureCommitter` inside `UtinniCoreDotNet.Tests/FormatsTests/Tre/` whose [Fact] methods call each Build* helper + `File.WriteAllBytes("Utinni.Cli.Tests/Fixtures/tre/<name>.tre", bytes)`. Run the committer once. Manually delete the TreFixtureCommitter test class. Commit only the resulting .tre files + the new fixture builder helpers (`BuildValidV0006TwoRecord()` is the new one for REVIEWS LOW-20).

    For the synthesized 3-record v0005 fixture, the deflate-compressed payload for record 1 is generated via `System.IO.Compression.DeflateStream` in `CompressionMode.Compress` against the ASCII bytes of `The quick brown fox jumps over the lazy dog.`. Record the resulting compressed bytes' length in the TreInfo's DataCompressedSize field. UncompressedSize is 44 bytes.

    For the synthesized 2-record v0006 fixture (REVIEWS LOW-20), the header version field is `"0006"` instead of `"0005"`; otherwise the same byte layout. Records: record 0 uncompressed `"v0006 marker"` (12 bytes) name `marker.txt`; record 1 deflated `"Phase 4 v0006 coverage"` name `v6.txt`.

    To generate each `expected.json`:
    1. Build the .tre / .iff fixture.
    2. Run `bin\Release\utinni-cli.exe parse-tre <path-to-fixture>` (or `list-objects` for the ws.iff fixture) and capture stdout.
    3. Inspect the captured JSON manually to confirm correctness — verify TOP-LEVEL keys are `{ command, result, schemaVersion }` (REVIEWS HIGH-6); verify `result.header.version`, `result.header.recordCount`, each record's `id == "tre:<ordinal>"`, `name`, `offset`, `compressedSize`, `uncompressedSize`, `compressionKind` (string `"none"` or `"deflate"`), `checksum` (REVIEWS HIGH-7 field names).
    4. Post-process the captured text: replace the absolute path in `source` with `<fixture-path>`. PowerShell: `(Get-Content -Raw out.txt).Replace($absolutePath, "<fixture-path>")`.
    5. Save the masked JSON as `Utinni.Cli.Tests/Fixtures/tre/<fixture>.expected.json` with LF line endings (`.gitattributes` from Plan 04-01 enforces this on commit).

    For the negative cases (malformed-magic, truncated, unsupported-version), the same procedure: run parse-tre, capture the error envelope JSON (top-level `command, error, schemaVersion`), mask the absolute path, commit. The parser's exception messages should NOT contain paths — structure messages around the failure kind so expected.json is stable across machines.

    Build the synthesized ws.iff using BinaryWriter against an in-memory FORM/OBJS layout:
    - 4 bytes ASCII `FORM`
    - 4 bytes big-endian length L1
    - 4 bytes ASCII `WSNP` (form sub-type)
    - 4 bytes ASCII `OBJS`
    - 4 bytes big-endian length L2 (size of object-name payload)
    - L2 bytes of concatenated null-terminated template paths
    - If L2 is odd, append a single pad byte
    L1 = 4 (WSNP) + 4 (OBJS) + 4 (L2) + L2 + padbyte.

    Commit the fixtures + expected.json files. Each binary fixture is < 256 KB per D-03 cap.
  </action>
  <verify>
    <automated>git ls-files Utinni.Cli.Tests/Fixtures/tre/ returns at least 10 files (5 .tre — v0005, v0006, malformed-magic, truncated, unsupported-version — + 5 .expected.json). git ls-files Utinni.Cli.Tests/Fixtures/world-snapshot/ returns 2 files. PowerShell: `Get-ChildItem Utinni.Cli.Tests/Fixtures/tre/*.tre | Where-Object { $_.Length -gt 131072 }` returns no items (D-03 cap respected). msbuild Utinni.sln /m /p:Configuration=Release /p:Platform=x86 succeeds (fixtures land in bin via Plan 04-01 `<Content>` glob). Test-Path Utinni.Cli.Tests\bin\Release\net472\Fixtures\tre\synthesized-3record-v0005.tre returns True. Test-Path Utinni.Cli.Tests\bin\Release\net472\Fixtures\tre\synthesized-2record-v0006.tre returns True (REVIEWS LOW-20 file present). `& bin\Release\utinni-cli.exe parse-tre Utinni.Cli.Tests\bin\Release\net472\Fixtures\tre\synthesized-3record-v0005.tre` exits 0 with stdout containing the literal substring "hello.txt" AND the literal substring "tre:0" (HIGH-7 stable id). `& bin\Release\utinni-cli.exe parse-tre Utinni.Cli.Tests\bin\Release\net472\Fixtures\tre\synthesized-2record-v0006.tre` exits 0 with stdout containing the literal substring "0006".</automated>
  </verify>
  <done>Six binary fixtures (3-record v0005, 2-record v0006, 3 negative TREs, 1 ws.iff) + six expected.json goldens committed. Each fixture is under 128 KB. The CLI's actual output against each fixture matches expected.json under the `<fixture-path>` mask. Fixtures are bit-stable across machines. REVIEWS LOW-20 closed: v0006 has its own Tier-2 golden coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.4: Land Tier-2 golden tests in Utinni.Cli.Tests/Commands/{ParseTreCommandTests,ListObjectsCommandTests}.cs (with explicit envelope-shape assertion per REVIEWS HIGH-6 + v0006 row per REVIEWS LOW-20)</name>
  <files>Utinni.Cli.Tests/Commands/ParseTreCommandTests.cs, Utinni.Cli.Tests/Commands/ListObjectsCommandTests.cs</files>
  <read_first>
    - Utinni.Cli.Tests/Commands/CommandDispatchTests.cs (Plan 04-01 output — the canonical xUnit shape for Phase 4 golden tests; copy the InProcessCliRunner + GoldenTestRunner invocation pattern).
    - Utinni.Cli.Tests/Fixtures/tre/*.expected.json (Task 2.3 output — the goldens these tests will pass).
    - .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-PATTERNS.md §"`Utinni.Cli.Tests/Commands/<Command>CommandTests.cs` (×4)".
    - UtinniCoreDotNet.Tests/PluginLoaderTests.cs lines 1-50 (the analog xUnit class shape).
  </read_first>
  <behavior>
    - ParseTreCommandTests.Run_WithSynthesizedV0005ThreeRecord_ExitsZeroAndMatchesGolden: Run("parse-tre", path-to-v0005) returns ExitCode=0; masked stdout matches `tre/synthesized-3record-v0005`.
    - ParseTreCommandTests.Run_WithSynthesizedV0006TwoRecord_ExitsZeroAndMatchesGolden: same shape for v0006 (REVIEWS LOW-20).
    - ParseTreCommandTests.Run_WithMalformedMagic_ExitsTwoAndMatchesGolden: malformed-magic.tre; ExitCode=2.
    - ParseTreCommandTests.Run_WithTruncated_ExitsTwoAndMatchesGolden: truncated.tre; ExitCode=2.
    - ParseTreCommandTests.Run_WithUnsupportedVersion_ExitsTwoAndMatchesGolden: unsupported-version.tre; ExitCode=2.
    - ParseTreCommandTests.Run_WithMissingFile_ExitsThreeAndMatchesFileNotFoundShape: Run("parse-tre", "Z:\\nonexistent\\path.tre") returns ExitCode=3; structural assertion (no committed golden — host-dependent path).
    - ParseTreCommandTests.Run_AnyHappyPath_EnvelopeHasTopLevelSchemaVersionAndCommand: REVIEWS HIGH-6 regression guard — parse the v0005 happy-path output, assert root["schemaVersion"].Value<int>() == 1, root["command"].Value<string>() == "parse-tre", root["result"]["schemaVersion"] is null (no nesting), root["result"]["command"] is null.
    - ListObjectsCommandTests.Run_WithSynthesizedWsIff_ExitsZeroAndMatchesGolden: world-snapshot/synthesized-ws.iff; three template paths in sorted Ordinal order.
    - ListObjectsCommandTests.Run_WithMissingFile_ExitsThreeAndMatchesFileNotFoundShape: same shape as parse-tre's missing-file test.
  </behavior>
  <action>
    Create `Utinni.Cli.Tests/Commands/ParseTreCommandTests.cs` with the 23-line MIT header, namespace `Utinni.Cli.Tests.Commands`, using `Xunit;`, `Newtonsoft.Json.Linq;`, `Utinni.Cli.Tests.Infrastructure;`. Test class `public class ParseTreCommandTests`. Use a helper `private static string MaskPath(string actual, string path)` that returns `actual.Replace(path, "<fixture-path>")` — same mask applied to expected.json during Task 2.3.

    Seven tests per the behaviour spec. Six follow the same golden skeleton:
    ```
    var fixturePath = FixturePath.Resolve("tre/<name>.tre");
    var result = InProcessCliRunner.Run("parse-tre", fixturePath);
    Assert.Equal(<expected-exit-code>, result.ExitCode);
    var maskedStdout = result.Stdout.Replace(fixturePath, "<fixture-path>");
    GoldenTestRunner.Matches("tre/<name>", maskedStdout);
    ```

    For the missing-file test, skip GoldenTestRunner.Matches; use direct JToken assertions:
    ```
    var result = InProcessCliRunner.Run("parse-tre", "Z:\\nonexistent\\path-that-cannot-exist.tre");
    Assert.Equal(3, result.ExitCode);
    var root = JToken.Parse(result.Stdout);
    Assert.Equal("parse-tre", root["command"].Value<string>());
    Assert.Equal("FileNotFound", root["error"]["kind"].Value<string>());
    ```

    For the envelope-shape regression guard (REVIEWS HIGH-6):
    ```
    var fixturePath = FixturePath.Resolve("tre/synthesized-3record-v0005.tre");
    var result = InProcessCliRunner.Run("parse-tre", fixturePath);
    Assert.Equal(0, result.ExitCode);
    var root = JToken.Parse(result.Stdout);
    Assert.Equal(1, root["schemaVersion"].Value<int>());  // top-level
    Assert.Equal("parse-tre", root["command"].Value<string>());  // top-level
    Assert.Null(root["result"]["schemaVersion"]);  // NOT nested
    Assert.Null(root["result"]["command"]);  // NOT nested
    Assert.Null(root["error"]);  // mutually exclusive with result
    ```

    Create `Utinni.Cli.Tests/Commands/ListObjectsCommandTests.cs` with identical structure. Two [Fact] tests: one against `world-snapshot/synthesized-ws.iff` calling GoldenTestRunner.Matches, and one against a missing path asserting exit 3 + FileNotFound shape.

    Naming convention: Phase 1 D-04 `[Method]_[Scenario]_[ExpectedOutcome]` throughout.
  </action>
  <verify>
    <automated>msbuild Utinni.sln /m /p:Configuration=Release /p:Platform=x86 succeeds. dotnet test Utinni.Cli.Tests/Utinni.Cli.Tests.csproj --no-build --configuration Release --filter "FullyQualifiedName~ParseTreCommandTests" exits 0 with 7 tests passing. dotnet test ... --filter "FullyQualifiedName~ListObjectsCommandTests" exits 0 with 2 tests passing. The COMBINED Utinni.Cli.Tests suite (Plan 04-01 17 + Plan 04-02 9 = 26+ tests) all green. grep -nF "GoldenTestRunner.Matches" Utinni.Cli.Tests/Commands/ParseTreCommandTests.cs returns at least 5 matches. grep -nF "v0006" Utinni.Cli.Tests/Commands/ParseTreCommandTests.cs returns at least one match (REVIEWS LOW-20 — v0006 test present).</automated>
  </verify>
  <done>The two new Tier-2 command surfaces are golden-protected, with explicit REVIEWS HIGH-6 envelope-shape assertions and a dedicated v0006 Tier-2 row (REVIEWS LOW-20). Combined with Task 2.1's ten Tier-1 parser tests, the TRE surface has 10 + 7 + 2 = 19 independent assertions. Any future change to the TRE parser, the parse-tre/list-objects JSON shape (including envelope structure), or the fixture content surfaces as a clear test failure.</done>
</task>

<task type="auto">
  <name>Task 2.5: Update docs/ai/assessment.md to record the CON-O-09 disposition (fixtures in-repo synth + tiny real, no LFS per D-03)</name>
  <files>docs/ai/assessment.md</files>
  <read_first>
    - docs/ai/assessment.md §"Open questions for project history" (resolved-paragraph style — same shape as Plan 04-01 Task 1.7's CON-O-11 entry).
    - .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-CONTEXT.md §"Decisions §D-03" + §"Resolves CON-O-09 disposition".
    - .planning/intel/constraints.md lines 142-143 (CON-O-09 original statement).
  </read_first>
  <behavior>
    - The §"Open questions" section in docs/ai/assessment.md carries a Resolution paragraph for CON-O-09 styled identically to CON-O-01..-O-07 and the just-added CON-O-11.
    - CON-O-09 disposition: in-repo synthesized + tiny real samples (<128 KB each), no LFS, no env-var bring-your-own path, no opt-in live-snapshot tier.
  </behavior>
  <action>
    Open `docs/ai/assessment.md` and locate the §"Open questions for project history" section. Add a Resolution block under (or immediately following) the CON-O-09 question paragraph, matching the styling of CON-O-11's resolution. Exact text:

    ```
       **Resolved 2026-05-23 (CON-O-09, Phase 4 Plan 04-02 — D-03):** Fixture storage =
       **in-repo synthesized + tiny real samples, no LFS**. Synthesized fixtures (hand-
       crafted minimal cases — 3-record TRE v0005, 2-record TRE v0006, 5-chunk IFF,
       3-object world-snapshot, 4 plugin sub-fixtures) are the primary suite and always-
       run in CI. Small (<128KB each, <256KB cap per file) real samples supplement when
       synthesized-only would miss real-world edge cases. Synthesized-first policy
       sidesteps redistribution-rights questions on SWG game assets; committed real
       samples are format-trivial enough to be defensible as "minimal reproducer"
       rather than "redistributed game content". No `.gitattributes`-LFS pointer files,
       no env-var bring-your-own path, no opt-in 'live-snapshot' tier. If Phase 5
       (Catch2 C++) or Phase 7+ (Wave-1 subpanels) surface needs that synthesized
       fixtures can't satisfy, LFS revisit is a Phase 6+ decision (Phase 4 CONTEXT.md
       "Deferred Ideas").
    ```

    Use 3-space leading indent matching existing resolution blocks. Do NOT touch CON-O-06 / CON-O-08 (Phase 6 still owns those).
  </action>
  <verify>
    <automated>grep -n "Resolved 2026-05-23 (CON-O-09, Phase 4 Plan 04-02" docs/ai/assessment.md returns exactly one match. grep -cnE "Resolved 2026-05-23 (CON-O-(09|11)" docs/ai/assessment.md returns at least 2 (Plan 04-01 added CON-O-11; Plan 04-02 adds CON-O-09).</automated>
  </verify>
  <done>CON-O-09 disposition recorded. STAB-05 acceptance criterion advanced — only CON-O-06 + CON-O-08 remain open after Phase 4 (both Phase 6 territory).</done>
</task>

</tasks>

<verification>
After all five tasks complete:

1. `msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86` succeeds. UtinniCoreDotNet.dll exports the Formats.Tre.* types.
2. `dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release` exits 0; new TreFileTests adds 10 tests (REVIEWS HIGH-4 fix path A's eager-read regression guard is included).
3. `dotnet test Utinni.Cli.Tests/Utinni.Cli.Tests.csproj --no-build --configuration Release` exits 0; new ParseTreCommandTests + ListObjectsCommandTests add 9 tests (Plan 04-01 17 + Plan 04-02 9 = 26).
4. PowerShell: `& bin\Release\utinni-cli.exe parse-tre Utinni.Cli.Tests\bin\Release\net472\Fixtures\tre\synthesized-3record-v0005.tre` exits 0; output's top-level JSON keys are `{ command, result, schemaVersion }` (REVIEWS HIGH-6); `result.header.recordCount == 3` (REVIEWS HIGH-7 — NOT resourceCount); each record carries `id` matching `^tre:\d+$`, `compressionKind` in `{none, deflate}`.
5. PowerShell: `& bin\Release\utinni-cli.exe parse-tre Utinni.Cli.Tests\bin\Release\net472\Fixtures\tre\synthesized-2record-v0006.tre` exits 0; `result.header.version == "0006"` (REVIEWS LOW-20).
6. PowerShell: `& bin\Release\utinni-cli.exe parse-tre Utinni.Cli.Tests\bin\Release\net472\Fixtures\tre\malformed-magic.tre` exits 2 + BadMagic.
7. PowerShell: `& bin\Release\utinni-cli.exe list-objects Utinni.Cli.Tests\bin\Release\net472\Fixtures\world-snapshot\synthesized-ws.iff` exits 0; each `result.objects[i]` carries both `id` AND `templateName` (REVIEWS HIGH-7).
8. `docs/ai/assessment.md` carries the CON-O-09 disposition line for Phase 4 Plan 04-02.
9. CI gate (per D-05): Plan 04-02 merge commit lands green on master before Plans 04-03 / 04-04 trigger (NOTE: 04-03 and 04-04 depend only on 04-01, not on 04-02, per CONTEXT.md §"Plan Structuring" — so they may proceed in parallel with 04-02 as long as 04-01 is green).
</verification>

<success_criteria>
- [ ] `UtinniCoreDotNet/Formats/Tre/{TreFile,TreHeader,TreRecord,TreParseException}.cs` exist; each carries the MIT header + D-01 disposition addendum.
- [ ] TreFile EAGER-READS payload bytes into a `byte[][]` cache during `Open(Stream)` — `GetRecordData` works AFTER the source stream is disposed (REVIEWS HIGH-4 regression guard test passes).
- [ ] `UtinniCoreDotNet/UtinniCoreDotNet.csproj` lists all four new Formats/Tre/*.cs files in its `<Compile Include>` block.
- [ ] `UtinniCoreDotNet.Tests/FormatsTests/Tre/TreFileTests.cs` exists with 10 [Fact] tests covering happy-path v0005, happy-path v0006, bad-magic, unsupported-version, truncated, deflated-record-after-dispose, 256MB cap, negative-length, chunk-exceeds-file, AND the explicit AfterOpenStreamDisposed_StillReturnsBytes regression guard.
- [ ] `Utinni.Cli/Commands/ParseTreCommand.cs` BuildResult emits the LOCKED field shape: `records[].id` (`tre:<ordinal>`), `records[].compressionKind` (enum string), `header.recordCount` (NOT resourceCount), `records[].compressedSize/uncompressedSize/offset` — per REVIEWS HIGH-7.
- [ ] `Utinni.Cli/Commands/ListObjectsCommand.cs` emits `objects[].id` (= templateName) AND `objects[].templateName`, sorted by id Ordinal (REVIEWS HIGH-7). Source comment cites REVIEWS MEDIUM-13 architectural-debt refactor TODO.
- [ ] Six TRE fixtures (3-record v0005 + 2-record v0006 + 3 negatives) + their expected.json goldens committed under `Utinni.Cli.Tests/Fixtures/tre/`. Each .tre < 128 KB.
- [ ] One world-snapshot fixture (synthesized-ws.iff) + expected.json committed under `Utinni.Cli.Tests/Fixtures/world-snapshot/`. < 128 KB.
- [ ] `Utinni.Cli.Tests/Commands/{ParseTreCommandTests,ListObjectsCommandTests}.cs` exist with 7+2 = 9 tests, all green, including the REVIEWS HIGH-6 envelope-shape regression guard.
- [ ] `docs/ai/assessment.md` carries the CON-O-09 disposition row (Plan 04-02 D-03).
- [ ] All Phase 1/2/2.1/3/4-01 tests still green (no regression).
- [ ] T-04-V5 + T-04-DoS mitigations visible in `TreFile.cs` (`MaxBlockSize` cap + ValidateLength helper).
- [ ] No code under `UtinniCoreDotNet/Formats/Tre/` shares identifiers / variable names / comment style with the format references at `swg-client-v2/src/engine/shared/library/sharedFile/src/shared/{TreeFile.h, TreeFile.cpp}` (D-01 clean-room mandate).
</success_criteria>

<output>
Create `.planning/phases/04-tier-2-cli-shim-golden-fixtures/04-02-SUMMARY.md` when done — record:
- TRE parser API surface (public methods + exception types).
- Confirmation that `Open(Stream)` eager-reads into `byte[][]` cache (REVIEWS HIGH-4 fix path A landed).
- Byte-level summary of each committed fixture (offset table + key fields) — including the v0006 fixture (REVIEWS LOW-20).
- Final test count (Tier-1 + Tier-2). Confirm REVIEWS HIGH-6 envelope assertion present.
- Confirmation that BuildResult uses the locked field names (records[].id, compressionKind, header.recordCount, objects[].id) per REVIEWS HIGH-7.
- Confirmation that CON-O-09 is closed in docs/ai/assessment.md.
- Inline note that list-objects byte-scan is provisional architectural debt (REVIEWS MEDIUM-13) — Phase 6+ refactor target.
- Spot-check note re: D-01 clean-room.
- Wave-2 entry in 04-VALIDATION.md Per-Task Verification Map: each task ID populated.
- REVIEWS revisions applied: HIGH-4, HIGH-6, HIGH-7, MEDIUM-13, LOW-20 — each cited inline.
</output>
