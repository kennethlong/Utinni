---
id: 04-04
phase: 04-tier-2-cli-shim-golden-fixtures
plan: 04
type: execute
wave: 4
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - Utinni.Cli/Utinni.Cli.csproj
  - Utinni.Cli/Commands/ValidatePluginCommand.cs
  - Utinni.Cli/Commands/PluginInspection.cs
  - Utinni.Cli/Commands/NativeExportProbe.cs
  - Utinni.Cli/Properties/AssemblyInfo.cs
  - Utinni.Cli/packages.lock.json
  - Utinni.Cli.Tests/Utinni.Cli.Tests.csproj
  - Utinni.Cli.Tests/Commands/ValidatePluginCommandTests.cs
  - Utinni.Cli.Tests/Commands/PluginInspectionTests.cs
  - Utinni.Cli.Tests/Fixtures/plugins/valid-plugin/expected.json
  - Utinni.Cli.Tests/Fixtures/plugins/missing-createplugin/MissingCreatePlugin.dll
  - Utinni.Cli.Tests/Fixtures/plugins/missing-createplugin/expected.json
  - Utinni.Cli.Tests/Fixtures/plugins/missing-destroyplugin/MissingDestroyPlugin.dll
  - Utinni.Cli.Tests/Fixtures/plugins/missing-destroyplugin/expected.json
  - Utinni.Cli.Tests/Fixtures/plugins/wrong-iplugin-shape/WrongIPluginShape.dll
  - Utinni.Cli.Tests/Fixtures/plugins/wrong-iplugin-shape/expected.json
  - .planning/PROJECT.md
  - docs/ai/assessment.md
autonomous: true
requirements: [TEST-03]

# REVIEWS MEDIUM-16 + plan-checker iter-2 WARNING-4: depends_on is [04-01, 04-02, 04-03]
# so the wave-ordering invariant (DEC-C3 promotion requires all four commands shipped)
# is captured in the primary contract, not just the Task 4.4 PRE-PROMOTION GATE. Wave still
# resolves to 4 (max(1,2,3)+1=4). The Task 4.4 Test-Path backstop is retained as defense-in-depth.

must_haves:
  truths:
    - "`utinni-cli validate-plugin <dir>` exits 0 and emits a stable sorted-key JSON envelope with TOP-LEVEL keys `{ command, result, schemaVersion }` (REVIEWS HIGH-6) describing every .dll under <dir> with per-DLL kind (managed|native|mixed|unknown — REVIEWS MEDIUM-9), per-check pass/fail, and a summary block."
    - "`utinni-cli validate-plugin <dir>` against a directory containing only the Phase 3 R-B `Utinni.CrtMatchPlugin.dll` reports kind=native + createplugin-export=pass + destroyplugin-export=pass + overallStatus=pass; matches `Fixtures/plugins/valid-plugin/expected.json` under JToken.DeepEquals."
    - "`utinni-cli validate-plugin <dir>` against `missing-createplugin/` reports kind=native + createplugin-export=fail + destroyplugin-export=pass + overallStatus=fail (regression guard for the createPlugin half of the Phase 3 D-13/D-14 ABI contract)."
    - "`utinni-cli validate-plugin <dir>` against `missing-destroyplugin/` reports createplugin-export=pass + destroyplugin-export=fail + overallStatus=fail."
    - "`utinni-cli validate-plugin <dir>` against `wrong-iplugin-shape/` (managed DLL without `[InheritedExport(typeof(IPlugin))]`) reports kind=managed + iplugin-export-shape=fail + overallStatus=fail."
    - "`utinni-cli validate-plugin --help` output contains the verbatim warning line: `Loads each .dll under the given directory; only run against trusted plugin directories. PluginLoader composition + managed-DLL inspection execute static initialisers as a side effect.`"
    - "PluginInspection.cs MANAGED PATH actually calls `new PluginLoader(autoLoad: false).Load(dir)` ONCE for the whole directory (REVIEWS HIGH-5 fix path A) and reports the loader's `Plugins` count + `LoadErrors` at the TOP-LEVEL `result.loaderObserved` (plan-checker iter-2 WARNING-5 — directory-scoped, NOT per-DLL). The reflection-only scan is the secondary fast path; the loader is the AUTHORITATIVE managed-plugin signal."
    - "PluginInspection.cs NATIVE PATH uses `NativeExportProbe.HasExport(dllPath, symbol)` which uses `System.Reflection.Metadata.PEReader` to parse the PE export table WITHOUT loading the DLL (REVIEWS HIGH-1 fix path A). NO `LoadLibraryExW` + `GetProcAddress` pair (which would have been buggy per MSDN). Provides the strongest T-04-EoP mitigation — DLL code is never mapped into the process address space."
    - "PluginInspection.cs reflection helpers do not call `Console.*` and do not reference `Newtonsoft.Json.*`; ValidatePluginCommand.cs is the only file that emits JSON. Source-level enforcement uses a robust grep (comment-stripped per REVIEWS — `grep -v '^[[:space:]]*//'` filter) and an `[assembly: AssemblyMetadata(\"validate-plugin-version\", \"1\")]` marker (REVIEWS MEDIUM-15) read via reflection on the BUILT assembly — NOT a fragile source-path walk from BaseDirectory."
    - "PROJECT.md Key Decisions table row for DEC-C3 reads `LOCKED ✓` (not `Candidate — non-locked`) with a `Promoted at Phase 4 close (Plan 04-04)` note. Task 4.4's pre-promotion verify gate confirms 04-02-SUMMARY.md AND 04-03-SUMMARY.md exist on disk (REVIEWS MEDIUM-16 — DEC-C3 only promotes after all four commands ship)."
    - "Both CON-O-09 and CON-O-11 in docs/ai/assessment.md §`Open questions` carry a `Resolved` paragraph citing Phase 4 plan SHAs (closed by Plans 04-01 + 04-02; this plan only verifies and patches if needed)."
    - "Plan 04-04 honors CONTEXT.md decisions D-04 (validate-plugin emits JToken.DeepEquals-compatible sorted-key JSON read by Tier-2 GoldenTestRunner), D-07 (Task 4.4 promotes DEC-C3 from Candidate to LOCKED in PROJECT.md as the Phase-closing commit), D-09 (validate-plugin runs without SWG; no new Tier-4 carve-outs — every check is statically verifiable on the build machine), D-10 (Task 4.2 emits sorted-key indented JSON with top-level schemaVersion envelope via JsonOutput), and D-12 (all deliverables stay in kennethlong/Utinni; UtinniPlugins is untouched in this phase)."
  artifacts:
    - path: "Utinni.Cli/Commands/PluginInspection.cs"
      provides: "Pure-managed reflection helpers — `InspectDirectory(string dir)` returns a `DirectoryReport` (plan-checker iter-2 WARNING-5 — top-level shape, NOT a flat IReadOnlyList<PluginReport>). **REVIEWS HIGH-5 fix path A:** the managed path constructs `new PluginLoader(autoLoad: false)`, calls `Load(dir)` ONCE for the directory, captures `Plugins` (the discovered IPlugin instances) AND `LoadErrors` (per-DLL failure messages) into `DirectoryReport.LoaderObserved`. This makes validate-plugin a TRUE smoke-test of the production loader, not a static-only reflection. The ReflectionOnly path is the secondary fast-path that classifies kind = managed vs native without composing. `DirectoryReport { string Directory; LoaderObservation LoaderObserved; IReadOnlyList<PluginReport> Plugins; ResultSummary Summary; }`. PluginReport POCO `{ Id, Path, Kind, Exports, Checks, OverallStatus }` (NO per-plugin LoaderObserved). CheckResult `{ Id, Status, Detail }`. LoaderObservation `{ int PluginsCount; IReadOnlyList<string> LoadErrors; }`. No Console.*, no Newtonsoft.* references."
    - path: "Utinni.Cli/Commands/NativeExportProbe.cs"
      provides: "**REVIEWS HIGH-1 fix path A — PE export-table parsing via `System.Reflection.Metadata` `PEReader`.** `bool HasExport(string dllPath, string symbol)` opens the DLL with `PEReader` over a `FileStream`, walks `PEHeaders.CorHeader` (null for native DLLs) + the export directory, reads the export name table, returns true if `symbol` is found. NO `LoadLibraryExW`. NO `GetProcAddress`. NO `FreeLibrary`. NO DLL code mapped into the address space (strongest T-04-EoP mitigation). Returns false on parse failure (does not throw). Documents fallback option in xmldoc: `LoadLibraryExW(path, NULL, DONT_RESOLVE_DLL_REFERENCES)` (0x1) — DllMain not called, GetProcAddress works per MSDN — could replace PE-parsing if a future phase needs better compatibility with packed/obfuscated DLLs."
    - path: "Utinni.Cli/Utinni.Cli.csproj"
      provides: "**REVIEWS HIGH-1 + #17 fix** — adds `<PackageReference Include=\"System.Reflection.Metadata\" Version=\"1.6.0\" />` AND `<PackageReference Include=\"System.Collections.Immutable\" Version=\"1.5.0\" />` (its dependency). `System.Reflection.Metadata` is NOT in net472's BCL; explicit NuGet add is required."
    - path: "Utinni.Cli/Properties/AssemblyInfo.cs"
      provides: "**REVIEWS MEDIUM-15 fix** — carries `[assembly: System.Reflection.AssemblyMetadata(\"validate-plugin-version\", \"1\")]` (and any other assembly metadata Utinni.Cli accumulates). PluginInspectionTests Test 5 (the source-separation contract test) reads THIS metadata via reflection on the built assembly — replacing the previously fragile `File.ReadAllText` source-path walk from `BaseDirectory`. The marker version bumps in lockstep with the schemaVersion: 1 contract — any future contract break also bumps this marker."
    - path: "Utinni.Cli/Commands/ValidatePluginCommand.cs"
      provides: "Verb-handler implementation replacing Plan 04-01's stub. Reads `<dir>`, delegates to PluginInspection.InspectDirectory, converts the `IReadOnlyList<PluginReport>` to the JSON envelope (REVIEWS HIGH-6 — envelope wrapping at root via JsonOutput.EmitSuccess). [Verb] HelpText includes the verbatim T-04-EoP warning line — updated to reflect actual loader behavior (REVIEWS HIGH-5 + HIGH-8 alignment — managed path executes module ctors via PluginLoader composition; native path does NOT execute DllMain because PE-parsing doesn't load code)."
    - path: "Utinni.Cli.Tests/Commands/ValidatePluginCommandTests.cs"
      provides: "Four [Theory] rows (one per sub-fixture) plus one [Fact] for the --help-warning text + one [Fact] asserting PluginInspection.cs has zero Console.* / Newtonsoft.* references (via robust comment-stripped grep) + one [Fact] asserting the AssemblyMetadata marker is readable (REVIEWS MEDIUM-15 — replaces the fragile source-walk test) + one [Fact] for the REVIEWS HIGH-6 envelope-shape regression guard + one [Fact] for the plan-checker iter-2 WARNING-5 `result.loaderObserved` top-level placement guard (asserts `root["result"]["loaderObserved"]` is non-null AND `root["result"]["plugins"][0]["loaderObserved"]` IS null)."
    - path: "Utinni.Cli.Tests/Fixtures/plugins/valid-plugin/expected.json"
      provides: "Golden expected JSON for the valid CrtMatchPlugin case: top-level `{ command, result, schemaVersion }` (REVIEWS HIGH-6); plugin entry with kind=native, both export checks=pass, overallStatus=pass, summary {totalPlugins:1, passing:1, failing:0}. Also includes `result.loaderObserved: { pluginsCount: 0, loadErrors: [] }` AT THE TOP-LEVEL `result` (plan-checker iter-2 WARNING-5 — hoisted from per-plugin to directory-scoped). Native plugins surface no MEF Exports, so PluginLoader.Plugins is empty and LoadErrors is empty (REVIEWS HIGH-5 — loader IS exercised on every fixture so the field is always populated)."
    - path: "Utinni.Cli.Tests/Fixtures/plugins/{missing-createplugin,missing-destroyplugin,wrong-iplugin-shape}/<Stem>.dll"
      provides: "Three pre-built fixture DLLs (≤16 KB each) committed in-repo (per Task 4.3 feasibility probe — see PRE-FLIGHT step). `MissingCreatePlugin.dll` exports only `destroyPlugin`; `MissingDestroyPlugin.dll` exports only `createPlugin`; `WrongIPluginShape.dll` is a managed net472 class library decorated with `[Export(typeof(SomeBogusInterface))]` rather than `[Export(typeof(IPlugin))]` so MEF discovers nothing — and PluginLoader.Load(dir) returns Plugins.Count==0 with LoadErrors possibly carrying a ReflectionTypeLoadException message. Each fixture's `expected.json` surfaces this in `result.loaderObserved` at the TOP-LEVEL (plan-checker iter-2 WARNING-5)."
    - path: ".planning/PROJECT.md"
      provides: "DEC-C3 Key Decisions table row promoted from `Candidate — non-locked` to `LOCKED ✓`. Promotion is gated by REVIEWS MEDIUM-16 verification — Task 4.4 confirms `04-02-SUMMARY.md` AND `04-03-SUMMARY.md` exist before flipping the row."
    - path: "docs/ai/assessment.md"
      provides: "CON-O-09 + CON-O-11 §Open questions rows confirmed closed (or patched if not already closed by Plans 04-01 + 04-02)."
  key_links:
    - from: "Utinni.Cli/Commands/ValidatePluginCommand.cs"
      to: "Utinni.Cli/Commands/PluginInspection.cs"
      via: "static method call"
      pattern: "PluginInspection\\.InspectDirectory"
    - from: "Utinni.Cli/Commands/PluginInspection.cs"
      to: "Utinni.Cli/Commands/NativeExportProbe.cs"
      via: "static method call (PE export-table parsing)"
      pattern: "NativeExportProbe\\.HasExport"
    - from: "Utinni.Cli/Commands/PluginInspection.cs"
      to: "UtinniCoreDotNet/PluginFramework/PluginLoader.cs"
      via: "**REVIEWS HIGH-5 fix path A — actually invoked, not just referenced** — constructs `new PluginLoader(autoLoad: false)` and calls `Load(dir)` for every managed-DLL inspection. Captures `Plugins` count + `LoadErrors` into the JSON envelope."
      pattern: "new PluginLoader\\(autoLoad: ?false\\).*\\.Load\\("
    - from: "Utinni.Cli/Commands/NativeExportProbe.cs"
      to: "System.Reflection.Metadata.PEReader (NuGet)"
      via: "REVIEWS HIGH-1 fix path A — PE export-table parse WITHOUT LoadLibrary"
      pattern: "System\\.Reflection\\.Metadata\\.PEReader|new PEReader\\("
    - from: "Utinni.Cli.Tests/Commands/PluginInspectionTests.cs"
      to: "Utinni.Cli AssemblyMetadata marker"
      via: "REVIEWS MEDIUM-15 — reflect on the BUILT assembly's `[assembly: AssemblyMetadata]` attributes, NOT a source-file walk from BaseDirectory"
      pattern: "AssemblyMetadataAttribute|GetCustomAttributes.*AssemblyMetadata"
    - from: "Utinni.Cli.Tests/Commands/ValidatePluginCommandTests.cs"
      to: "Utinni.Cli/Program.cs"
      via: "InProcessCliRunner.Run -> Utinni.Cli.Program.Main"
      pattern: "InProcessCliRunner\\.Run"
    - from: "Utinni.Cli.Tests/Utinni.Cli.Tests.csproj"
      to: "Utinni.CrtMatchPlugin.dll (Phase 3 R-B build output)"
      via: "MSBuild `<Content>` reference + post-build Target"
      pattern: "Utinni\\.CrtMatchPlugin\\.dll"
---

<objective>
Implement Plan 04-04 — the `validate-plugin <dir>` command + four sub-fixtures + the Phase-closing DEC-C3 promotion. This is the final plan of Phase 4 and closes the four-command roadmap commitment (TEST-03).

Three deliverables:

1. **`validate-plugin` command.** Pure-managed reflection over a directory of plugin DLLs. For each `.dll`:
   - **Native classification** via `NativeExportProbe.HasExport(dllPath, symbol)` which uses `System.Reflection.Metadata.PEReader` to parse the PE export table WITHOUT loading the DLL (REVIEWS HIGH-1 fix path A — replaces the broken `LoadLibraryExW(..., LOAD_LIBRARY_AS_DATAFILE) + GetProcAddress` combination that MSDN forbids).
   - **Managed classification** via `new PluginLoader(autoLoad: false).Load(dir)` invoked ONCE for the whole directory — the ACTUAL production loader, not a static reflection-only stand-in (REVIEWS HIGH-5 fix path A). Captures `Plugins` count + `LoadErrors` and surfaces them in the JSON envelope at TOP-LEVEL `result.loaderObserved` (plan-checker iter-2 WARNING-5 — directory-scoped, NOT per-DLL).
   - Per-DLL checks: createPlugin/destroyPlugin native exports (Phase 3 D-13/D-14 symmetric-ABI contract), MEF `[InheritedExport(typeof(IPlugin))]` shape compliance, `IEditorPlugin` shape compliance.
   - Emits a stable sorted-key JSON envelope (REVIEWS HIGH-6 — top-level schemaVersion + command).

2. **Four sub-fixtures.** `valid-plugin/` reuses the Phase 3 R-B native `Utinni.CrtMatchPlugin.dll` build output. The other three (`missing-createplugin/`, `missing-destroyplugin/`, `wrong-iplugin-shape/`) ship as pre-built fixture DLLs committed in-repo (with a feasibility probe + human-action fallback for the build-toolchain dependency).

3. **DEC-C3 promotion (phase-closing).** Edit PROJECT.md Key Decisions row to `LOCKED ✓` ONLY AFTER verifying Plans 04-02 + 04-03 have shipped (REVIEWS MEDIUM-16 — promotion requires all four commands present in CI).

Purpose: Closes TEST-03 with the fourth and last roadmap-required command. Locks DEC-C3 once the full Tier-2 CLI shim is real. Establishes the regression guard for Phase 3 R-B's symmetric createPlugin/destroyPlugin ABI contract AND for the PluginLoader production composition path (via the LoaderObserved field).

Output: Buildable + green `validate-plugin` command using PE-parsing for native + actual PluginLoader.Load for managed. Four golden fixtures. Phase 4 closed with DEC-C3 LOCKED ✓.

**Honored CONTEXT.md decisions** (plan-checker iter-2 coverage-gate hooks):
- D-04: Stable JSON output + JToken.DeepEquals goldens — `validate-plugin` emits sorted-key JSON read via JToken.DeepEquals in Tier-2 tests.
- D-07: DEC-C3 promotion — Task 4.4 flips the PROJECT.md row from `Candidate — non-locked` to `LOCKED ✓` at phase close.
- D-09: No new Tier-4 carve-outs — `validate-plugin` runs without SWG; all checks are statically verifiable.
- D-10: Sorted-key indented JSON + top-level `schemaVersion` envelope — Task 4.2 emits the locked shape via JsonOutput.
- D-12: No cross-repo work — all deliverables stay in `kennethlong/Utinni`; UtinniPlugins is untouched.

**Reviews-mode revisions applied (2026-05-22 from 04-REVIEWS.md):**
- HIGH-1 + #17: NativeExportProbe rewritten to use `System.Reflection.Metadata.PEReader` (PE export-table parse without DLL load). The broken `LoadLibraryExW + LOAD_LIBRARY_AS_DATAFILE + GetProcAddress` combination is REMOVED. Utinni.Cli.csproj adds NuGet refs for `System.Reflection.Metadata` 1.6.0 + `System.Collections.Immutable` 1.5.0. Added Windows API smoke test sub-step in Task 4.1 BEFORE locking goldens — minimal native fixture DLL parse + known-export assertion.
- HIGH-5 (refined by plan-checker iter-2 WARNING-5): PluginInspection.cs MANAGED PATH actually invokes `new PluginLoader(autoLoad: false).Load(dir)` ONCE for the directory. The result (Plugins count + LoadErrors) is surfaced in the JSON envelope at TOP-LEVEL `result.loaderObserved` — NOT duplicated onto every PluginReport (directory-scoped, honest). ReflectionOnly path remains as secondary fast-path for kind classification.
- HIGH-6: JSON envelope wraps at the ROOT level via JsonOutput.EmitSuccess (per Plan 04-01 Task 1.2 fix). Tier-2 regression-guard test included.
- HIGH-8: Threat model rewritten so claims align with actual code. Managed path EXECUTES module ctors (truthful disclosure); native path does NOT execute DllMain (because PE-parse doesn't load code). Phase 2 C-06 per-plugin try/catch isolation is now correctly cited as the safety net under managed-path composition.
- MEDIUM-9: `kind = "unknown"` documented in the JSON contract enum. Unknown-kind plugins have all four checks return `status=fail` with explanatory detail.
- MEDIUM-15: Source-walk test replaced with an `[assembly: AssemblyMetadata("validate-plugin-version", "1")]` marker read via reflection on the built assembly. Robust + working-directory-independent.
- MEDIUM-16 (refined by plan-checker iter-2 WARNING-4): `depends_on: [04-01, 04-02, 04-03]` makes the wave-ordering invariant explicit in the plan contract (not just runtime CI). Task 4.4 pre-promotion verify gate (`Test-Path 04-02-SUMMARY.md` AND `Test-Path 04-03-SUMMARY.md`) is retained as defense-in-depth; `must_haves.truths` carries an explicit statement of this gate. CI gate per D-05 enforces wave ordering as the second backstop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tier-2-cli-shim-golden-fixtures/04-CONTEXT.md
@.planning/phases/04-tier-2-cli-shim-golden-fixtures/04-RESEARCH.md
@.planning/phases/04-tier-2-cli-shim-golden-fixtures/04-PATTERNS.md
@.planning/phases/04-tier-2-cli-shim-golden-fixtures/04-VALIDATION.md
@.planning/phases/04-tier-2-cli-shim-golden-fixtures/04-REVIEWS.md
@.planning/phases/04-tier-2-cli-shim-golden-fixtures/04-01-PLAN.md
@.planning/phases/04-tier-2-cli-shim-golden-fixtures/04-02-PLAN.md
@.planning/phases/03-strategic-reworks-r-a-r-h/03-CONTEXT.md
@.planning/intel/constraints.md

<interfaces>
<!-- Existing in-repo interfaces this plan consumes. Executor should use these directly. -->

From UtinniCoreDotNet/PluginFramework/PluginLoader.cs (Phase 2 C-06 + Phase 3 R-B test seam — lines 55-141):
- `public class PluginLoader` — non-sealed; can be constructed directly from CLI code.
- `public PluginLoader(bool autoLoad)` — testability ctor; pass `false` to skip the production auto-Load.
- `public void Load(string pluginDir = null)` — directory-walk overload; iterates `Directory.GetFiles(pluginDir, "*.dll")`, opens each as its own `DirectoryCatalog(pluginDir, dllName)`, composes via MEF `[ImportMany(typeof(IPlugin))]`, isolates failures per-DLL via try/catch in `LoadCatalog` (line 143-178).
- `public IEnumerable<IPlugin> Plugins` (field, line 39) — discovered plugins after Load returns.
- `public IList<string> LoadErrors` (line 45) — per-DLL load-failure messages; cleared on each Load() call.
- **REVIEWS HIGH-5 contract:** validate-plugin's managed path MUST call `loader.Load(dir)` and inspect `loader.Plugins` + `loader.LoadErrors`. Surfacing these in the JSON envelope makes validate-plugin a TRUE smoke-test of the production loader (the Phase 2 C-06 isolation contract is implicitly tested every run).

From UtinniCoreDotNet/PluginFramework/IPlugin.cs (lines 25-50):
- `[InheritedExport(typeof(IPlugin))] public interface IPlugin { PluginInformation Information { get; } UtINI GetConfig(); }`
- `public class PluginInformation { string Name; string Description; string Author; }`

From UtinniCoreDotNet/PluginFramework/IEditorPlugin.cs (lines 33-52):
- `[InheritedExport(typeof(IEditorPlugin))] public interface IEditorPlugin : IPlugin { ... }`

From Utinni.CrtMatchPlugin (Phase 3 R-B fixture):
- C++ native DLL built under `bin\<Configuration>\Utinni.CrtMatchPlugin.dll`.
- Exports `extern "C" __declspec(dllexport) utinni::UtinniPlugin* createPlugin()` and `extern "C" __declspec(dllexport) void destroyPlugin(utinni::UtinniPlugin*)`.

From UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj `CopyNativeArtifactsForTests` Target lines 63-86 (the pattern to clone for the Utinni.CrtMatchPlugin copy).

From Utinni.Cli/Output/JsonOutput.cs (Plan 04-01 output — POST-REVISION per REVIEWS HIGH-6):
- `EmitSuccess(string command, object result, TextWriter writer = null)` wraps `result` in `{ schemaVersion: 1, command: <command>, result: <result> }` at the ENVELOPE ROOT. Callers do NOT pre-add schemaVersion.
- `EmitError(string command, string kind, string message, int exitCode, TextWriter writer = null)` emits `{ schemaVersion: 1, command: <command>, error: { kind: <kind>, message: <message> } }`.

From Plan 04-01 Task 1.4 (`Utinni.Cli/Commands/ValidatePluginCommand.cs` STUB):
- Existing [Verb("validate-plugin", ...)] options class with `[Value(0, MetaName="dir", Required=true)] public string Dir { get; set; }`.
- This plan REPLACES the Run body with the real implementation that returns 0 / 3 / 1 per D-02.

From Utinni.Cli.Tests/Infrastructure/{FixturePath,InProcessCliRunner,GoldenTestRunner}.cs (Plan 04-01 Task 1.3 output).

From System.Reflection.Metadata (NuGet 1.6.0 — REVIEWS HIGH-1 + #17):
- net472 does NOT have `System.Reflection.Metadata` in the BCL. Explicit NuGet ref required.
- `System.Reflection.PortableExecutable.PEReader` (namespace `System.Reflection.PortableExecutable`) — primary entry point.
- `PEReader(System.IO.Stream peStream)` ctor.
- `PEHeaders Headers` property — provides access to `PEHeader.ExportTableDirectory` (RVA + size of export table).
- For reading export table contents, use `PEReader.GetEntireImage()` + `PEMemoryBlock` + manual walk of the IMAGE_EXPORT_DIRECTORY structure. (System.Reflection.Metadata does NOT have a high-level export-iteration API; the export table is a low-level PE construct and must be walked manually.)
- Manual walk pattern: locate ExportTableDirectory RVA, use `PEHeaders.GetContainingSectionIndex(rva)` + `PEHeaders.SectionHeaders[i].PointerToRawData` to compute file offset. Read IMAGE_EXPORT_DIRECTORY (40 bytes): NumberOfNames + AddressOfNames (RVA to name-RVAs array). Iterate name-RVAs; for each, dereference to ASCII string via PEMemoryBlock.GetReader().ReadUtf8NullTerminated().

From .planning/PROJECT.md (the DEC-C3 row to promote — REVIEWS MEDIUM-16 gated):
- Current text approximately: `| **DEC-C3 (candidate D-08)**: ... | ... | Candidate — non-locked | Promote to ADR when Tier 2 CLI shim lands (Phase 4). |`
- Target text: same row, columns 3 + 4 changed to `LOCKED ✓` and `Promoted at Phase 4 close (Plan 04-04)`.
- Pre-promotion gate (REVIEWS MEDIUM-16): Task 4.4 confirms `Test-Path .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-02-SUMMARY.md` returns True AND `Test-Path .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-03-SUMMARY.md` returns True before flipping the row.

From docs/ai/assessment.md (CON-O-09 + CON-O-11 disposition rows):
- Plan 04-01 Task 1.7 inserted the CON-O-11 Resolution paragraph.
- Plan 04-02 Task 2.5 closes CON-O-09 at its fixture-landing commit.
- Plan 04-04 verifies both are present and patches if needed.

From .gitattributes (Plan 04-01 Task 1.3 output):
- `Utinni.Cli.Tests/Fixtures/**/*.expected.json text eol=lf` — already in place.
</interfaces>

<json_contract>
<!-- REVIEWS HIGH-6 envelope shape + REVIEWS MEDIUM-9 (kind=unknown documented) + REVIEWS HIGH-5 (loaderObserved field).
     Plan-checker iter-2 WARNING-5: loaderObserved HOISTED from per-plugin to top-level result.
     PluginLoader.Load(dir) operates on the whole directory; the same snapshot was being
     duplicated onto every PluginReport entry. Honest shape places it once at result.loaderObserved. -->

```
{
  "command": "validate-plugin",
  "result": {
    "directory":     "<input dir argument>",
    "loaderObserved": {
      "loadErrors":   [ "<msg>", ... ],
      "pluginsCount": <int>
    },
    "plugins": [
      {
        "checks": [
          { "id": "createplugin-export",       "status": "pass|fail|n/a", "detail": "..." },
          { "id": "destroyplugin-export",      "status": "pass|fail|n/a", "detail": "..." },
          { "id": "iplugin-export-shape",      "status": "pass|fail|n/a", "detail": "..." },
          { "id": "editor-plugin-export-shape","status": "pass|fail|n/a", "detail": "..." }
        ],
        "exports": {
          "createPlugin":         true,
          "destroyPlugin":        true,
          "managedIEditorPlugin": false,
          "managedIPlugin":       false
        },
        "id":   "<dll-stem-name>",
        "kind": "managed|native|mixed|unknown",
        "overallStatus": "pass|fail",
        "path":          "<absolute or relative path to the dll>"
      }
    ],
    "summary": {
      "failing":      0,
      "passing":      1,
      "totalPlugins": 1
    }
  },
  "schemaVersion": 1
}
```

**Stable contract elements (schemaVersion: 1):**
- TOP-LEVEL keys after sorted serialization: `command`, `result`, `schemaVersion` (REVIEWS HIGH-6 — schemaVersion at ROOT).
- `result.directory`, `result.loaderObserved`, `result.plugins`, `result.summary` — all four MUST appear.
- `result.loaderObserved` is at the TOP LEVEL of `result` (plan-checker iter-2 WARNING-5 — NOT nested inside each PluginReport). Per-plugin objects do NOT carry a `loaderObserved` field.
- Per-plugin object MUST carry `id`, `path`, `kind`, `exports`, `checks`, `overallStatus`. (No per-plugin `loaderObserved`.)
- `kind` ∈ `{managed, native, mixed, unknown}` (REVIEWS MEDIUM-9 — `unknown` documented and surfaced when a DLL has neither native exports nor managed plugin types).
- For `kind == "unknown"`: ALL FOUR checks return `status == "fail"` with detail `"DLL is neither native-exporting (createPlugin/destroyPlugin) nor managed-plugin-shaped ([InheritedExport(typeof(IPlugin))]). Cannot validate as a Utinni plugin."`. `overallStatus == "fail"`.
- `exports` is a stable four-key object — all four keys always present (true/false).
- Each `checks[].id` is a stable slug: `createplugin-export`, `destroyplugin-export`, `iplugin-export-shape`, `editor-plugin-export-shape`.
- `result.loaderObserved` (REVIEWS HIGH-5) — captures the result of calling `new PluginLoader(autoLoad: false).Load(dir)` ONCE for the entire directory. `pluginsCount` is `loader.Plugins.Count()` AFTER load; `loadErrors` is `loader.LoadErrors` (string array). For a directory of native plugins, pluginsCount is typically 0 and loadErrors is empty (MEF DirectoryCatalog scans for managed types only). For a directory with managed IPlugin Exports, pluginsCount >= 1. For wrong-iplugin-shape, pluginsCount == 0 and loadErrors MAY contain a ReflectionTypeLoadException detail.
- `overallStatus` = `pass` iff every check is `pass` or `n/a`; `fail` if any check is `fail`.

**Sort order for golden stability:**
- `plugins[]` is sorted by `id` (Ordinal). Stabilises against `Directory.GetFiles` OS-dependent enumeration order.
- `checks[]` is emitted in the fixed order listed above. Sorting checks alphabetically would be wrong for human read; SortJObjectKeys does NOT reorder array elements.
- `exports` object keys land in alphabetical order via JsonOutput's recursive sort. `result.loaderObserved` is also alphabetised.

**React Flow portability (load-bearing):**
The plugin → checks shape is stable. Future React Flow tooling can ingest directly: `plugins.flatMap(p => p.checks.map(c => ({ id: p.id + '/' + c.id, parentId: p.id })))`. Do NOT rename `id`/`status`/`overallStatus`/`summary`/`loaderObserved`/`pluginsCount`/`loadErrors` keys. `loaderObserved` is at `result.loaderObserved` — NOT nested inside each plugin (plan-checker iter-2 WARNING-5).
</json_contract>

<threat_model>
<!-- REVIEWS HIGH-8 fix: claims align with the ACTUAL Task 4.1 implementation post-revision. -->

| Threat ID | STRIDE | Applies | Mitigation (TRUTHFUL post-revision) |
|-----------|--------|---------|------------|
| T-04-EoP | Elevation of Privilege | **yes** | `validate-plugin --help` text contains the verbatim warning: `Loads each .dll under the given directory; only run against trusted plugin directories. PluginLoader composition + managed-DLL inspection execute static initialisers as a side effect.`<br><br>**Native path:** `NativeExportProbe.cs` uses `System.Reflection.Metadata.PEReader` to parse the PE export table — DOES NOT call `LoadLibrary*` (REVIEWS HIGH-1 fix path A). Native DLL code is NEVER mapped into the process address space. Strongest possible T-04-EoP mitigation for the native classification path.<br><br>**Managed path:** `PluginInspection.cs` calls `new PluginLoader(autoLoad: false).Load(dir)` (REVIEWS HIGH-5 fix path A). This is the SAME composition path the production framework uses. MEF DirectoryCatalog opens each .dll for reflection — managed module ctors + static initialisers DO execute as a side effect. **The safety net is Phase 2 C-06**: PluginLoader.cs:143-178 wraps each per-DLL load in try/catch; throw-on-ctor surfaces in `LoadErrors` rather than escaping. validate-plugin observes `loader.LoadErrors` and reports them in the JSON envelope (`loaderObserved.loadErrors`).<br><br>No sandbox in Phase 4 — operator-privilege boundary. Optional `--sandbox` flag deferred to Phase 6+. |
| T-04-V5 | Tampering / DoS | no | No binary parsing in this plan beyond PE export-table reads (which are size-bounded by file size). T-04-V5 lives in Plans 04-02 (TRE parser) + 04-03 (IFF parser). |
| T-04-DoS | DoS | no | No deflate / chunk-bomb surface. PEReader's export-table walk is bounded by `PEHeaders.ExportTableDirectory.Size` which is itself bounded by the PE file size. |
| T-04-V12 | Information Disclosure | no | File-system access is bounded to `Directory.GetFiles(dir, "*.dll")` of the operator-supplied directory; no traversal. Operator-privilege boundary. |
| T-04-Json | Code Execution | no | `validate-plugin` emits JSON only; never deserializes untrusted JSON input. Inherits the TypeNameHandling.None setting from Plan 04-01's JsonOutput.cs. |
| T-04-SC | Tampering | yes (post-revision) | NuGet additions (REVIEWS HIGH-1 + #17): `System.Reflection.Metadata` 1.6.0 + `System.Collections.Immutable` 1.5.0 are Microsoft-published packages, both `[ASSUMED]`-tier-or-better verified (`microsoft.com` author, signed). Package legitimacy approved at the Task 4.1 PRE-FLIGHT step before any install. RestorePackagesWithLockFile=true is already enforced; the lock file in this plan's diff documents the resolved versions. |
</threat_model>

<additional_context>
**Plan-checker iter-2 WARNING-8 — fixture-cap documentation.** Per CONTEXT.md D-03 the absolute fixture cap is 256KB; this plan enforces a tighter operational 128KB cap (per CONTEXT.md D-08's observed "small (<128KB each)" target). The Task 4.3 verify-gate `Get-ChildItem ... | Where-Object { $_.Length -gt 16384 }` even tightens to 16KB for the three pre-built plugin DLL fixtures (because tiny missing/wrong-export stubs comfortably fit there). The tighter operational gates are self-imposed regression guards, NOT contradictions of D-03's 256KB ceiling. If a future revision needs to ship a sample between 16KB and 256KB (still legal per D-03), the verify-gate threshold for that fixture is bumped explicitly in the plan diff — never silently.
</additional_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 4.1: PluginInspection.cs + NativeExportProbe.cs (PE-parse via System.Reflection.Metadata per REVIEWS HIGH-1) + PluginLoader.Load invocation per REVIEWS HIGH-5 + Tier-1 self-tests including a Windows API smoke test</name>
  <files>Utinni.Cli/Commands/PluginInspection.cs, Utinni.Cli/Commands/NativeExportProbe.cs, Utinni.Cli/Properties/AssemblyInfo.cs, Utinni.Cli/Utinni.Cli.csproj, Utinni.Cli/packages.lock.json, Utinni.Cli.Tests/Commands/PluginInspectionTests.cs</files>
  <read_first>
    - UtinniCoreDotNet/PluginFramework/PluginLoader.cs (entire file — the `Load(string pluginDir)` test seam at lines 72-141 is the production reuse point per REVIEWS HIGH-5; the `LoadFromDirectory` per-DLL try/catch shape at lines 121-141 documents what failure modes the inspection layer must tolerate).
    - UtinniCoreDotNet/PluginFramework/IPlugin.cs (lines 25-50 — the `[InheritedExport(typeof(IPlugin))]` attribute).
    - UtinniCoreDotNet/PluginFramework/IEditorPlugin.cs (lines 33-52).
    - Utinni.CrtMatchPlugin/main.cpp lines 36-100 (confirms the C++ native ABI export shape).
    - .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-PATTERNS.md §"`Utinni.Cli/Commands/ValidatePluginCommand.cs`" and §"Shared Patterns" §"MIT License Header".
    - .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-RESEARCH.md §"Code Examples §Example 3" + §"Security Domain" + §"Package Legitimacy Audit" (CommandLineParser + Newtonsoft.Json approved; System.Reflection.Metadata + System.Collections.Immutable to be probed at PRE-FLIGHT).
    - .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-REVIEWS.md §"HIGH #1" (NativeExportProbe Windows API bug — fix path A PE-parse) + §"HIGH #5" (PluginLoader reuse — fix path A actually invoke `Load(dir)`) + §"MEDIUM #9" (kind=unknown) + §"MEDIUM #10" (managed plugin reflection fragility — partially mitigated by HIGH-5) + §"MEDIUM #15" (source-walk → AssemblyMetadata marker) + §"#17" (PEReader needs explicit NuGet on net472).
    - System.Reflection.Metadata 1.6.0 docs (NuGet metadata + Microsoft docs on PEReader / PEHeaders / ExportTableDirectory; if Context7 MCP is available use it, else cross-reference the dotnet/runtime source).
  </read_first>
  <behavior>
    - **PRE-FLIGHT: Package Legitimacy** — before any code lands, the executor confirms System.Reflection.Metadata 1.6.0 and System.Collections.Immutable 1.5.0 on nuget.org are Microsoft-published (`microsoft.com` URL) and have multi-year download history. Both packages are part of `dotnet/runtime`. Record approval in commit message.

    - **PRE-FLIGHT: Windows API smoke test (REVIEWS HIGH-1 sub-step)** — before locking goldens, the executor writes a minimal C# program inside the Tier-1 tests:
      1. Build (or use the already-built) `Utinni.CrtMatchPlugin.dll`.
      2. Open it with `PEReader`.
      3. Walk the export table.
      4. Assert the known export `createPlugin` is found in the name table AND `destroyPlugin` is found.
      If either assertion fails, the PE-parsing implementation is broken — STOP and revisit before any further work. This is mandatory.

    - Test 1 (NativeExportProbe.HasExport — positive case via CrtMatchPlugin): With `Utinni.CrtMatchPlugin.dll` at `$(SolutionDir)bin\Release\Utinni.CrtMatchPlugin.dll`, `NativeExportProbe.HasExport(crtPath, "createPlugin") == true` and `NativeExportProbe.HasExport(crtPath, "destroyPlugin") == true`. This is the PRE-FLIGHT smoke promoted to a permanent Tier-1 test.
    - Test 2 (NativeExportProbe.HasExport — negative case): `NativeExportProbe.HasExport(crtPath, "totallyNotExportedSymbol") == false`.
    - Test 3 (NativeExportProbe.HasExport — missing-file does not throw): `NativeExportProbe.HasExport("Z:/nonexistent/file.dll", "createPlugin") == false`.
    - Test 4 (NativeExportProbe.HasExport — managed-DLL has no native exports): point at any managed DLL (e.g., `UtinniCoreDotNet.dll`); the export table is either empty or contains no `createPlugin`. `HasExport(...) == false`.
    - Test 5 (PluginInspection.InspectDirectory — empty directory): InspectDirectory on a directory with no .dll files returns a `DirectoryReport` with `Plugins` count == 0, `LoaderObserved.PluginsCount == 0`, `LoaderObserved.LoadErrors` empty, `Summary.TotalPlugins == 0` (plan-checker iter-2 WARNING-5 — top-level shape).
    - Test 6 (PluginInspection.InspectDirectory — picks up native CrtMatchPlugin and classifies + invokes loader per REVIEWS HIGH-5): Point InspectDirectory at a temp directory containing a copy of `Utinni.CrtMatchPlugin.dll`. Assert: `dirReport.Plugins.Count == 1`; entry.Id == "Utinni.CrtMatchPlugin"; entry.Kind == "native"; entry.Exports.CreatePlugin == true; entry.Exports.DestroyPlugin == true; entry.Exports.ManagedIPlugin == false; entry.Exports.ManagedIEditorPlugin == false; entry.OverallStatus == "pass"; AND `dirReport.LoaderObserved.PluginsCount == 0` (PluginLoader.Load was invoked but found no managed types — REVIEWS HIGH-5 evidence at the DIRECTORY level per plan-checker iter-2 WARNING-5); `dirReport.LoaderObserved.LoadErrors` is empty. The per-plugin PluginReport no longer carries a LoaderObserved field — that data lives once at `dirReport.LoaderObserved`.
    - Test 7 (PluginInspection — kind=unknown branch — REVIEWS MEDIUM-9): Build/copy a DLL that has neither native exports nor managed plugin types (e.g., a tiny C# DLL with one bogus class, no [Export] attribute). InspectDirectory reports kind == "unknown", all four checks status == "fail", overallStatus == "fail". This test consumes the wrong-iplugin-shape fixture in a partial form OR builds an inline minimal DLL via Roslyn-emit if available; planner's choice.
    - Test 8 (separation contract — REVIEWS MEDIUM-15 — replaces source-walk): Read the `[assembly: AssemblyMetadata("validate-plugin-version", "1")]` attribute via reflection on `typeof(Utinni.Cli.Commands.PluginInspection).Assembly.GetCustomAttributes(typeof(System.Reflection.AssemblyMetadataAttribute))`. Find the entry where `Key == "validate-plugin-version"`, assert `Value == "1"`. This is the regression marker — bumping the validate-plugin contract bumps this attribute in the same commit. The test is working-directory-independent (reads the built assembly).
    - Test 9 (source-purity check — robust grep): `File.ReadAllText` PluginInspection.cs source from a path resolved by walking up from `typeof(PluginInspection).Assembly.Location` until finding `Utinni.Cli/Commands/PluginInspection.cs`. Apply comment-stripping (`Regex.Replace(content, @"//.*", "")` for single-line; multi-line comments are not used in this file). Assert no `Console.` occurrences in the comment-stripped text. Assert no `Newtonsoft.` occurrences. This complements Test 8 — Test 8 enforces the marker is present; Test 9 enforces no banned imports leak in.
  </behavior>
  <action>
    **PRE-FLIGHT (mandatory, before any code edit):**
    1. Confirm System.Reflection.Metadata 1.6.0 + System.Collections.Immutable 1.5.0 on nuget.org are Microsoft-published. Record in commit message body.
    2. Write a temporary console program (or in-test code) that opens `Utinni.CrtMatchPlugin.dll` via `PEReader` and walks the export table. Confirm `createPlugin` + `destroyPlugin` are found. If they're NOT found, STOP — the PE-parsing approach is unworkable on the actual binary; fall back to REVIEWS HIGH-1 alternative `LoadLibraryExW(path, NULL, DONT_RESOLVE_DLL_REFERENCES)` and update this task accordingly. If they ARE found, proceed.

    **Csproj edit** — open `Utinni.Cli/Utinni.Cli.csproj` and append two new PackageReferences inside the existing ItemGroup that contains CommandLineParser:
    ```xml
    <PackageReference Include="System.Reflection.Metadata" Version="1.6.0" />
    <PackageReference Include="System.Collections.Immutable" Version="1.5.0" />
    ```
    Run `dotnet restore Utinni.Cli/Utinni.Cli.csproj` to materialise the new lock file entries. Commit both csproj + packages.lock.json.

    **Create `Utinni.Cli/Properties/AssemblyInfo.cs`** (REVIEWS MEDIUM-15):
    ```csharp
    // <23-line MIT header copied from PluginLoader.cs:1-23>

    using System.Reflection;

    [assembly: AssemblyMetadata("validate-plugin-version", "1")]
    ```
    No namespace declaration. The marker bumps in lockstep with the validate-plugin JSON contract — any future contract break also bumps this marker. PluginInspectionTests Test 8 reads it.

    **Create `Utinni.Cli/Commands/NativeExportProbe.cs`** with the 23-line MIT header. Namespace `Utinni.Cli.Commands`. `public static class NativeExportProbe`. **REVIEWS HIGH-1 fix path A — PE export-table parsing via `System.Reflection.Metadata.PEReader`. NO LoadLibrary* calls.**

    - `public static bool HasExport(string dllPath, string symbolName)`:
      ```csharp
      if (string.IsNullOrEmpty(dllPath) || !File.Exists(dllPath)) return false;
      try
      {
          using (var fs = new FileStream(dllPath, FileMode.Open, FileAccess.Read, FileShare.Read))
          using (var peReader = new System.Reflection.PortableExecutable.PEReader(fs))
          {
              return PeReaderHasExport(peReader, symbolName);
          }
      }
      catch
      {
          return false;
      }
      ```
    - `private static bool PeReaderHasExport(PEReader peReader, string symbolName)`:
      - Read `peReader.PEHeaders.PEHeader.ExportTableDirectory` — gives `(RelativeVirtualAddress, Size)`.
      - If `Size == 0`, no export table → return false.
      - Get the entire PE image bytes via `peReader.GetEntireImage().GetReader()` or `peReader.GetSectionData(rva)` — use the latter to scope the read to the export-containing section.
      - Locate the section containing the export RVA via `peReader.PEHeaders.GetContainingSectionIndex(rva)` + `peReader.PEHeaders.SectionHeaders[i]`. Compute the byte offset into the section: `sectionOffset = rva - section.VirtualAddress`. The PEMemoryBlock for the section is obtained from `peReader.GetSectionData(section.VirtualAddress)`.
      - Walk IMAGE_EXPORT_DIRECTORY (40 bytes):
        - bytes 0-3: Characteristics (skip)
        - bytes 4-7: TimeDateStamp (skip)
        - bytes 8-11: MajorVersion + MinorVersion (skip)
        - bytes 12-15: Name (RVA, skip)
        - bytes 16-19: Base (skip)
        - bytes 20-23: NumberOfFunctions
        - bytes 24-27: NumberOfNames
        - bytes 28-31: AddressOfFunctions (RVA)
        - bytes 32-35: AddressOfNames (RVA — array of name RVAs)
        - bytes 36-39: AddressOfNameOrdinals (RVA)
      - Read `numberOfNames` (uint32 at offset 24).
      - Read AddressOfNames RVA (uint32 at offset 32). Convert to file/image offset via section table.
      - Iterate `i` from 0 to `numberOfNames - 1`:
        - Read 4-byte name RVA at AddressOfNames + i*4.
        - Convert name RVA to image offset.
        - Read null-terminated ASCII string at that offset.
        - If string == symbolName, return true.
      - Return false.
    - xmldoc note documenting REVIEWS HIGH-1 fix path A choice + alternative path (`LoadLibraryExW + DONT_RESOLVE_DLL_REFERENCES`) as a future option if PE-parsing proves incompatible with some packed/obfuscated DLLs.

    **Create `Utinni.Cli/Commands/PluginInspection.cs`** with the 23-line MIT header. Namespace `Utinni.Cli.Commands`. Define the POCO graph (plan-checker iter-2 WARNING-5 — DirectoryReport new top-level shape; PluginReport no longer carries LoaderObserved):
    - `public sealed class DirectoryReport { public string Directory; public LoaderObservation LoaderObserved; public IReadOnlyList<PluginReport> Plugins; public ResultSummary Summary; }` — REVIEWS HIGH-5 evidence lives here ONCE for the entire directory.
    - `public sealed class PluginReport { public string Id; public string Path; public string Kind; public PluginExports Exports; public IReadOnlyList<CheckResult> Checks; public string OverallStatus; }` — NO LoaderObserved field (directory-scoped, not per-plugin).
    - `public sealed class PluginExports { public bool CreatePlugin; public bool DestroyPlugin; public bool ManagedIPlugin; public bool ManagedIEditorPlugin; }`
    - `public sealed class CheckResult { public string Id; public string Status; public string Detail; }`
    - `public sealed class LoaderObservation { public int PluginsCount; public IReadOnlyList<string> LoadErrors; }` — REVIEWS HIGH-5 evidence; consumed once by DirectoryReport.LoaderObserved.
    - `public sealed class ResultSummary { public int TotalPlugins; public int Passing; public int Failing; }`

    Implement `public static class PluginInspection`:
    - `public static DirectoryReport InspectDirectory(string dir)` (plan-checker iter-2 WARNING-5 — return type CHANGED from `IReadOnlyList<PluginReport>` to `DirectoryReport`):
      1. Validate `dir != null && Directory.Exists(dir)` — otherwise return a `DirectoryReport { Directory=dir, LoaderObserved=LoaderObservation.Empty, Plugins=Array.Empty<PluginReport>(), Summary=new ResultSummary { TotalPlugins=0, Passing=0, Failing=0 } }`.
      2. Enumerate `Directory.GetFiles(dir, "*.dll", SearchOption.TopDirectoryOnly)`, sort by `Path.GetFileNameWithoutExtension(p)` under `StringComparer.Ordinal`.
      3. **REVIEWS HIGH-5: invoke PluginLoader ONCE for the directory.** `var loader = new UtinniCoreDotNet.PluginFramework.PluginLoader(autoLoad: false); loader.Load(dir);`. Capture `loader.Plugins.Count()` + `loader.LoadErrors.ToList()` into a single `LoaderObservation` instance. This is the DIRECTORY-LEVEL observation per plan-checker iter-2 WARNING-5 — NOT duplicated onto per-plugin reports.
      4. For each .dll, call `InspectSingle(dllPath)`. PluginReport no longer carries LoaderObserved.
      5. Build `Summary { TotalPlugins=reports.Count, Passing=reports.Count(r=>r.OverallStatus=="pass"), Failing=reports.Count(r=>r.OverallStatus=="fail") }`.
      6. Return `new DirectoryReport { Directory=dir, LoaderObserved=loaderObs, Plugins=reports, Summary=summary }`.
    - `private static PluginReport InspectSingle(string dllPath)` (plan-checker iter-2 WARNING-5 — no `loaderObserved` parameter; PluginReport is directory-independent):
      - Id = `Path.GetFileNameWithoutExtension(dllPath)`.
      - Native probe: `NativeExportProbe.HasExport(dllPath, "createPlugin")` + `NativeExportProbe.HasExport(dllPath, "destroyPlugin")`.
      - Managed probe via ReflectionOnlyLoadFrom (secondary fast-path for kind classification, NOT authoritative — the DirectoryReport.LoaderObserved field IS the authority):
        ```csharp
        try
        {
            var asm = Assembly.ReflectionOnlyLoadFrom(dllPath);
            foreach (var t in asm.GetTypes())
            {
                foreach (var attrData in CustomAttributeData.GetCustomAttributes(t))
                {
                    var attrTypeName = attrData.AttributeType.FullName;
                    if (attrTypeName == "System.ComponentModel.Composition.InheritedExportAttribute"
                        && attrData.ConstructorArguments.Count >= 1)
                    {
                        var exportedType = attrData.ConstructorArguments[0].Value as Type;
                        if (exportedType != null)
                        {
                            if (exportedType.FullName.EndsWith(".IPlugin")) managedIPlugin = true;
                            if (exportedType.FullName.EndsWith(".IEditorPlugin")) managedIEditorPlugin = true;
                        }
                    }
                }
            }
        }
        catch (BadImageFormatException) { /* native DLL — expected for CrtMatchPlugin */ }
        catch (ReflectionTypeLoadException) { /* malformed managed DLL — expected for wrong-iplugin-shape */ }
        catch (FileLoadException) { /* dep resolution failure */ }
        ```
      - Kind discrimination (REVIEWS MEDIUM-9 — `unknown` documented):
        ```csharp
        string kind;
        bool hasNative  = createPlugin || destroyPlugin;
        bool hasManaged = managedIPlugin || managedIEditorPlugin;
        if (hasNative && hasManaged) kind = "mixed";
        else if (hasNative)          kind = "native";
        else if (hasManaged)         kind = "managed";
        else                         kind = "unknown";
        ```
      - Per-plugin checks (in the FIXED order createplugin-export, destroyplugin-export, iplugin-export-shape, editor-plugin-export-shape):
        - For native plugins: createplugin-export status = createPlugin ? "pass" : "fail"; destroyplugin-export status = destroyPlugin ? "pass" : "fail"; iplugin-export-shape + editor-plugin-export-shape status = "n/a" with detail "Plugin is native; MEF [InheritedExport] does not apply."
        - For managed plugins: createplugin-export + destroyplugin-export status = "n/a" with detail "Plugin is managed; native createPlugin/destroyPlugin do not apply."; iplugin-export-shape status = managedIPlugin ? "pass" : "fail" with detail explaining; editor-plugin-export-shape status = managedIEditorPlugin ? "pass" : (managedIPlugin ? "n/a" : "fail") — n/a means "IPlugin only, IEditorPlugin not required".
        - For mixed plugins: run all four checks against their respective signals.
        - For kind == "unknown" (REVIEWS MEDIUM-9): ALL FOUR checks return status = "fail" with detail "DLL is neither native-exporting nor managed-plugin-shaped. Cannot validate as a Utinni plugin."
      - Overall status: `OverallStatus = report.Checks.Any(c => c.Status == "fail") ? "fail" : "pass"`.
      - PluginReport does NOT carry LoaderObserved (plan-checker iter-2 WARNING-5 — that field lives once at DirectoryReport.LoaderObserved).

    **Create `Utinni.Cli.Tests/Commands/PluginInspectionTests.cs`** with the 23-line MIT header. Namespace `Utinni.Cli.Tests.Commands`. Tests 1-9 per <behavior>. The CrtMatchPlugin path resolver:
    ```csharp
    private static string ResolveCrtMatchPluginPath()
    {
        var baseDir = System.AppContext.BaseDirectory;
        var candidates = new[]
        {
            Path.Combine(baseDir, "..", "..", "..", "..", "bin", "Release", "Utinni.CrtMatchPlugin.dll"),
            Path.Combine(baseDir, "..", "..", "..", "..", "bin", "Debug",   "Utinni.CrtMatchPlugin.dll"),
        };
        foreach (var c in candidates) { if (File.Exists(c)) return Path.GetFullPath(c); }
        throw new FileNotFoundException("Could not locate Utinni.CrtMatchPlugin.dll - Phase 3 R-B prerequisite missing");
    }
    ```

    Test 8 (REVIEWS MEDIUM-15 — AssemblyMetadata marker):
    ```csharp
    [Fact]
    public void Assembly_ValidatePluginVersionMarker_IsPresent()
    {
        var asm = typeof(Utinni.Cli.Commands.PluginInspection).Assembly;
        var attrs = asm.GetCustomAttributes(typeof(System.Reflection.AssemblyMetadataAttribute), inherit: false);
        var marker = attrs.Cast<System.Reflection.AssemblyMetadataAttribute>()
                          .FirstOrDefault(a => a.Key == "validate-plugin-version");
        Assert.NotNull(marker);
        Assert.Equal("1", marker.Value);
    }
    ```

    Test 9 (robust comment-stripped source grep):
    ```csharp
    [Fact]
    public void PluginInspectionSource_HasNoConsoleOrNewtonsoftImports()
    {
        var asmDir = Path.GetDirectoryName(typeof(Utinni.Cli.Commands.PluginInspection).Assembly.Location);
        var sourcePath = Path.GetFullPath(Path.Combine(asmDir, "..", "..", "..", "..", "Utinni.Cli", "Commands", "PluginInspection.cs"));
        if (!File.Exists(sourcePath))
        {
            // CI working-directory variance — fail loudly so the executor fixes the resolver,
            // don't false-pass on a missing file.
            Assert.True(false, "Could not locate PluginInspection.cs source for grep test. Path tried: " + sourcePath);
        }
        var raw = File.ReadAllText(sourcePath);
        // Strip single-line comments (the file has no multi-line /* */ blocks).
        var stripped = System.Text.RegularExpressions.Regex.Replace(raw, @"//.*$", "", System.Text.RegularExpressions.RegexOptions.Multiline);
        Assert.False(System.Text.RegularExpressions.Regex.IsMatch(stripped, @"\bConsole\."), "PluginInspection.cs must not reference Console.* in active code.");
        Assert.False(System.Text.RegularExpressions.Regex.IsMatch(stripped, @"\bNewtonsoft\."), "PluginInspection.cs must not reference Newtonsoft.* in active code.");
    }
    ```

    Note that Test 9's source-walk is intentionally retained as a SECONDARY check — Test 8 is the PRIMARY contract (assembly-marker via reflection). Test 9 catches the case where a future refactor accidentally imports Console/Newtonsoft into PluginInspection.cs.
  </action>
  <verify>
    <automated>msbuild Utinni.sln /m /p:Configuration=Release /p:Platform=x86 succeeds (exit 0); Test-Path bin\Release\Utinni.CrtMatchPlugin.dll returns True (Phase 3 R-B prerequisite); dotnet test Utinni.Cli.Tests/Utinni.Cli.Tests.csproj --no-build --configuration Release --filter "FullyQualifiedName~PluginInspectionTests" exits 0 with all nine tests green. Source separation contract enforced: Test 9 + Test 8 both pass. grep -nE "PEReader|System\\.Reflection\\.Metadata" Utinni.Cli/Commands/NativeExportProbe.cs returns at least one match (REVIEWS HIGH-1 fix path A — PE-parse landed). grep -n "LoadLibraryExW\\|LoadLibraryEx\\|GetProcAddress" Utinni.Cli/Commands/NativeExportProbe.cs returns ZERO matches (REVIEWS HIGH-1 prohibition — the buggy Windows API combo is removed). grep -nE "new PluginLoader\\(autoLoad: ?false\\)" Utinni.Cli/Commands/PluginInspection.cs returns at least one match (REVIEWS HIGH-5 — PluginLoader actually invoked). grep -nE "\\.Load\\(dir\\)|loader\\.Load\\(" Utinni.Cli/Commands/PluginInspection.cs returns at least one match. grep -nE "kind\\s*=\\s*\"unknown\"" Utinni.Cli/Commands/PluginInspection.cs returns at least one match (REVIEWS MEDIUM-9 — unknown branch documented). grep -nF "validate-plugin-version" Utinni.Cli/Properties/AssemblyInfo.cs returns exactly one match. grep -nF "System.Reflection.Metadata" Utinni.Cli/Utinni.Cli.csproj returns at least one match (NuGet ref present per REVIEWS HIGH-1 + #17). grep -nE "public sealed class DirectoryReport" Utinni.Cli/Commands/PluginInspection.cs returns exactly one match (plan-checker iter-2 WARNING-5 — DirectoryReport POCO landed). grep -nE "public DirectoryReport InspectDirectory" Utinni.Cli/Commands/PluginInspection.cs returns exactly one match (return-type hoisted from IReadOnlyList<PluginReport>). grep -nE "LoaderObserved" Utinni.Cli/Commands/PluginInspection.cs | grep -v -E "^[^:]*://" returns matches ONLY on DirectoryReport/LoaderObservation lines — NO `report.LoaderObserved =` per-plugin assignment (plan-checker iter-2 WARNING-5).</automated>
  </verify>
  <done>PluginInspection.cs + NativeExportProbe.cs compile and pass nine Tier-1 self-tests against the real Phase 3 R-B CrtMatchPlugin.dll. The PE-parsing approach is proven via the PRE-FLIGHT smoke (Test 1). The reflection layer is provably free of Console.* and Newtonsoft.* references via BOTH the assembly-marker check (Test 8 — REVIEWS MEDIUM-15) and the comment-stripped source grep (Test 9). NO LoadLibraryExW/GetProcAddress calls remain (REVIEWS HIGH-1 fix landed). PluginLoader.Load is ACTUALLY invoked for the managed path (REVIEWS HIGH-5 fix landed). The `kind = "unknown"` branch is documented and tested (REVIEWS MEDIUM-9). The `loaderObserved` POCO field carries the PluginLoader's Plugins count + LoadErrors into the JSON envelope. T-04-EoP mitigation is stronger than the original plan: native DLL code is NEVER loaded into the process address space.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4.2: Replace ValidatePluginCommand stub with real Run body; extend --help with T-04-EoP warning (truthful post-revision per REVIEWS HIGH-8)</name>
  <files>Utinni.Cli/Commands/ValidatePluginCommand.cs</files>
  <read_first>
    - Utinni.Cli/Commands/ValidatePluginCommand.cs (Plan 04-01 Task 1.4 output — the existing stub).
    - Utinni.Cli/Commands/PluginInspection.cs (Task 4.1 output — the InspectDirectory contract).
    - Utinni.Cli/Output/JsonOutput.cs (Plan 04-01 — envelope at root level per REVIEWS HIGH-6).
    - .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-02-PLAN.md (Plan 04-02 — the per-command exit-code mapping idiom).
    - .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-CONTEXT.md §"Implementation Decisions §D-02" (exit codes).
    - This plan's `<json_contract>` block (REVIEWS HIGH-6 envelope + REVIEWS HIGH-5 loaderObserved field + REVIEWS MEDIUM-9 unknown kind).
    - This plan's `<threat_model>` block (REVIEWS HIGH-8 — truthful warning text).
  </read_first>
  <behavior>
    - `utinni-cli validate-plugin /nonexistent/dir` exits 3 and emits envelope `{ command: "validate-plugin", error: { kind: "DirectoryNotFound", message: ... }, schemaVersion: 1 }`.
    - `utinni-cli validate-plugin <empty-dir>` exits 0 and emits envelope `{ command: "validate-plugin", result: { directory, plugins: [], summary: { totalPlugins: 0, passing: 0, failing: 0 } }, schemaVersion: 1 }`.
    - `utinni-cli validate-plugin <dir-with-CrtMatchPlugin>` exits 0; stdout JSON matches `<json_contract>`; `result.plugins[0].id = "Utinni.CrtMatchPlugin"`, `kind = "native"`, `overallStatus = "pass"`; `result.loaderObserved.pluginsCount = 0` AT TOP-LEVEL result (REVIEWS HIGH-5 + plan-checker iter-2 WARNING-5).
    - `utinni-cli validate-plugin --help` output contains the verbatim warning lines (per REVIEWS HIGH-8 — truthful post-revision text): `Loads each .dll under the given directory; only run against trusted plugin directories. PluginLoader composition + managed-DLL inspection execute static initialisers as a side effect.`
    - Pure-managed Run body: never references `Console.*` directly (uses JsonOutput); never opens raw files (delegates to PluginInspection.InspectDirectory).
  </behavior>
  <action>
    Modify `Utinni.Cli/Commands/ValidatePluginCommand.cs`. Preserve the MIT header + using directives.

    Update the `[Verb(...)]` HelpText (REVIEWS HIGH-8 — truthful claims):

    `HelpText = "Reflect on a plugin directory and report compliance. WARNING: Loads each .dll under the given directory; only run against trusted plugin directories. PluginLoader composition + managed-DLL inspection execute static initialisers as a side effect."`

    Replace the body of `ValidatePluginCommand.Run(ValidatePluginOptions o)`:

    1. `if (string.IsNullOrEmpty(o.Dir)) return JsonOutput.EmitError("validate-plugin", "MissingArgument", "Plugin directory argument is required.", exitCode: 1);`

    2. `if (!System.IO.Directory.Exists(o.Dir)) return JsonOutput.EmitError("validate-plugin", "DirectoryNotFound", "Plugin directory does not exist: " + o.Dir, exitCode: 3);`

    3. `var dirReport = PluginInspection.InspectDirectory(o.Dir);` (plan-checker iter-2 WARNING-5 — return type is now `DirectoryReport`, not `IReadOnlyList<PluginReport>`).

    4. Build the result payload as an anonymous object matching `<json_contract>` exactly (NO nested schemaVersion — JsonOutput.EmitSuccess wraps at root per REVIEWS HIGH-6; `loaderObserved` is at TOP-LEVEL `result` per plan-checker iter-2 WARNING-5 — NOT inside each plugin):

       ```csharp
       var result = new
       {
           directory      = dirReport.Directory,
           loaderObserved = new
           {
               loadErrors   = dirReport.LoaderObserved.LoadErrors.ToList(),
               pluginsCount = dirReport.LoaderObserved.PluginsCount
           },
           plugins = dirReport.Plugins.Select(r => new
           {
               checks = r.Checks.Select(c => new { id = c.Id, status = c.Status, detail = c.Detail }).ToList(),
               exports = new
               {
                   createPlugin         = r.Exports.CreatePlugin,
                   destroyPlugin        = r.Exports.DestroyPlugin,
                   managedIEditorPlugin = r.Exports.ManagedIEditorPlugin,
                   managedIPlugin       = r.Exports.ManagedIPlugin
               },
               id            = r.Id,
               kind          = r.Kind,
               overallStatus = r.OverallStatus,
               path          = r.Path
               // NO loaderObserved field per plan-checker iter-2 WARNING-5 — directory-scoped at result.loaderObserved.
           }).ToList(),
           summary = new
           {
               failing      = dirReport.Summary.Failing,
               passing      = dirReport.Summary.Passing,
               totalPlugins = dirReport.Summary.TotalPlugins
           }
       };
       ```

    5. `return JsonOutput.EmitSuccess("validate-plugin", result);` — returns 0.
  </action>
  <verify>
    <automated>msbuild Utinni.sln /m /p:Configuration=Release /p:Platform=x86 succeeds. PowerShell: `& bin\Release\utinni-cli.exe validate-plugin --help 2>&1 | Out-String | Select-String -SimpleMatch "PluginLoader composition + managed-DLL inspection execute static initialisers"` returns one match (REVIEWS HIGH-8 truthful text). PowerShell: `& bin\Release\utinni-cli.exe validate-plugin Z:\nonexistent\dir` exits 3; stdout parses as JSON with top-level keys `{ command, error, schemaVersion }` (REVIEWS HIGH-6) and `error.kind == "DirectoryNotFound"`. PowerShell: `& bin\Release\utinni-cli.exe validate-plugin $env:TEMP` (assume empty for purposes; real CI runs against an empty temp dir explicitly) exits 0 with `result.summary.totalPlugins == 0`. grep -nF "PluginLoader composition + managed-DLL inspection execute static initialisers" Utinni.Cli/Commands/ValidatePluginCommand.cs returns exactly one match. grep -cE "EmitError|EmitSuccess" Utinni.Cli/Commands/ValidatePluginCommand.cs returns at least 3. grep -nF "loaderObserved = new" Utinni.Cli/Commands/ValidatePluginCommand.cs returns exactly one match (plan-checker iter-2 WARNING-5 — loaderObserved emitted ONCE at top-level result, NOT inside the plugins[] Select projection). grep -nE "dirReport\.LoaderObserved" Utinni.Cli/Commands/ValidatePluginCommand.cs returns at least one match (REVIEWS HIGH-5 — field consumed from DirectoryReport).</automated>
  </verify>
  <done>ValidatePluginCommand emits the documented JSON contract (REVIEWS HIGH-6 envelope + REVIEWS HIGH-5 loaderObserved AT TOP-LEVEL result per plan-checker iter-2 WARNING-5 + REVIEWS MEDIUM-9 unknown kind support inherited from PluginInspection). Exit 3 on directory-not-found, exit 1 on missing argument, exit 0 on success (regardless of per-plugin pass/fail). --help carries the verbatim REVIEWS HIGH-8 truthful warning. Pure-managed Run body; no Console.* / no File.OpenRead. Stub from Plan 04-01 fully replaced. Consumes DirectoryReport (plan-checker iter-2 WARNING-5).</done>
</task>

<task type="auto">
  <name>Task 4.3: Land the four sub-fixtures + four expected.json goldens + wire ValidatePluginCommandTests (with envelope-shape regression guard per REVIEWS HIGH-6)</name>
  <files>Utinni.Cli.Tests/Utinni.Cli.Tests.csproj, Utinni.Cli.Tests/Commands/ValidatePluginCommandTests.cs, Utinni.Cli.Tests/Fixtures/plugins/valid-plugin/expected.json, Utinni.Cli.Tests/Fixtures/plugins/missing-createplugin/MissingCreatePlugin.dll, Utinni.Cli.Tests/Fixtures/plugins/missing-createplugin/expected.json, Utinni.Cli.Tests/Fixtures/plugins/missing-destroyplugin/MissingDestroyPlugin.dll, Utinni.Cli.Tests/Fixtures/plugins/missing-destroyplugin/expected.json, Utinni.Cli.Tests/Fixtures/plugins/wrong-iplugin-shape/WrongIPluginShape.dll, Utinni.Cli.Tests/Fixtures/plugins/wrong-iplugin-shape/expected.json</files>
  <read_first>
    - UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj lines 63-86 (`CopyNativeArtifactsForTests` Target — the verbatim template for the post-build copy of Utinni.CrtMatchPlugin.dll).
    - Utinni.Cli.Tests/Utinni.Cli.Tests.csproj (Plan 04-01 Task 1.1 output — the SDK-style csproj this task extends).
    - Utinni.Cli/Commands/ValidatePluginCommand.cs (Task 4.2 output — the JSON envelope shape every expected.json must match).
    - Utinni.Cli/Commands/PluginInspection.cs (Task 4.1 output — the InspectDirectory contract).
    - Utinni.Cli.Tests/Infrastructure/{FixturePath,InProcessCliRunner,GoldenTestRunner}.cs.
    - .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-PATTERNS.md §"`Utinni.Cli.Tests/Utinni.Cli.Tests.csproj`" §"CopyNativeArtifactsForTests adaptation".
    - .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-CONTEXT.md §"D-08".
  </read_first>
  <behavior>
    - `msbuild Utinni.sln /m /p:Configuration=Release /p:Platform=x86` produces `Utinni.Cli.Tests\bin\Release\net472\Fixtures\plugins\valid-plugin\Utinni.CrtMatchPlugin.dll` via the post-build Target.
    - The three other DLLs are present via the existing `<Content Include="Fixtures\**\*" CopyToOutputDirectory="PreserveNewest" />` glob from Plan 04-01.
    - Each `expected.json` matches the actual JSON output of `utinni-cli validate-plugin <sub-fixture-dir>` under JToken.DeepEquals, with top-level shape `{ command, result, schemaVersion }` (REVIEWS HIGH-6).
    - All test rows pass.
    - `path` and `directory` fields are masked via `<TEST_FIXTURE_DIR>` sentinel.
  </behavior>
  <action>
    **PRE-FLIGHT FEASIBILITY PROBE** (same as the original plan — unchanged): from a VS 2022/2026 Developer Command Prompt, verify `cl.exe` + `msbuild` present. If yes, build the three DLLs in-line per the per-DLL sections below. If no, switch to autonomous: false + commit source + build.cmd + checkpoint:human-action. Document outcome in commit message.

    **Csproj edit** — append the `CopyValidPluginFixture` Target to `Utinni.Cli.Tests/Utinni.Cli.Tests.csproj`:
    ```xml
    <Target Name="CopyValidPluginFixture" AfterTargets="Build">
      <ItemGroup>
        <_ValidPluginArtifacts Include="$(SolutionDir)bin\$(Configuration)\Utinni.CrtMatchPlugin.dll" />
      </ItemGroup>
      <Copy SourceFiles="@(_ValidPluginArtifacts)"
            DestinationFolder="$(TargetDir)Fixtures\plugins\valid-plugin\"
            SkipUnchangedFiles="true"
            Condition="Exists('%(_ValidPluginArtifacts.Identity)')" />
    </Target>
    ```

    **Pre-built fixture DLLs** (same as original plan):
    - `missing-createplugin/MissingCreatePlugin.dll` — native C++, exports only `destroyPlugin`. Build via `cl.exe /LD /MD missing.c /Fe:MissingCreatePlugin.dll /link /def:missing.def`.
    - `missing-destroyplugin/MissingDestroyPlugin.dll` — native C++, exports only `createPlugin`.
    - `wrong-iplugin-shape/WrongIPluginShape.dll` — managed net472 class library decorated with `[Export(typeof(SomeBogusInterface))]`.

    **Generate the four expected.json files** — for each sub-fixture, run `utinni-cli validate-plugin <fixture-dir>` once, sanity-check the result manually (verify top-level shape per REVIEWS HIGH-6, verify loaderObserved field per REVIEWS HIGH-5, verify kind/exports/checks/overallStatus), apply the `<TEST_FIXTURE_DIR>` mask to `directory` + `path` fields, commit as `expected.json`.

    Approximate `valid-plugin/expected.json` (plan-checker iter-2 WARNING-5 — `loaderObserved` is at TOP-LEVEL `result`, NOT inside the plugin entry):
    ```json
    {
      "command": "validate-plugin",
      "result": {
        "directory": "<TEST_FIXTURE_DIR>/plugins/valid-plugin",
        "loaderObserved": {
          "loadErrors":   [],
          "pluginsCount": 0
        },
        "plugins": [
          {
            "checks": [
              { "detail": "Native DLL exports createPlugin.",                                "id": "createplugin-export",        "status": "pass" },
              { "detail": "Native DLL exports destroyPlugin.",                               "id": "destroyplugin-export",       "status": "pass" },
              { "detail": "Plugin is native; MEF [InheritedExport] does not apply.",         "id": "iplugin-export-shape",       "status": "n/a"  },
              { "detail": "Plugin is native; MEF [InheritedExport] does not apply.",         "id": "editor-plugin-export-shape", "status": "n/a"  }
            ],
            "exports": {
              "createPlugin":         true,
              "destroyPlugin":        true,
              "managedIEditorPlugin": false,
              "managedIPlugin":       false
            },
            "id":   "Utinni.CrtMatchPlugin",
            "kind": "native",
            "overallStatus": "pass",
            "path": "<TEST_FIXTURE_DIR>/plugins/valid-plugin/Utinni.CrtMatchPlugin.dll"
          }
        ],
        "summary": {
          "failing":      0,
          "passing":      1,
          "totalPlugins": 1
        }
      },
      "schemaVersion": 1
    }
    ```

    Similar shapes for the three negative fixtures (planner pinpoints each at golden-generation time). All four expected.json files MUST place `loaderObserved` at `result.loaderObserved` (top-level) per plan-checker iter-2 WARNING-5 — never inside `plugins[].loaderObserved`.

    **Test class** — create `Utinni.Cli.Tests/Commands/ValidatePluginCommandTests.cs` with MIT header. Namespace `Utinni.Cli.Tests.Commands`. Tests:

    1. `[Theory] [InlineData("valid-plugin")] [InlineData("missing-createplugin")] [InlineData("missing-destroyplugin")] [InlineData("wrong-iplugin-shape")] public void Run_AgainstSubFixture_MatchesGolden(string subFixture)` — Resolve fixture dir; run InProcessCliRunner.Run; assert ExitCode == 0 (per <json_contract> — overallStatus=fail inside JSON doesn't affect exit code); apply `<TEST_FIXTURE_DIR>` mask to both actual + expected; JToken.DeepEquals compare; on mismatch dump to TestResults/.

    2. `[Fact] public void Help_ContainsTEoPMitigationWarning()` — Run("validate-plugin", "--help"); assert combined output contains the verbatim truthful warning (REVIEWS HIGH-8): `"PluginLoader composition + managed-DLL inspection execute static initialisers as a side effect."`

    3. `[Fact] public void Help_TopLevelListsValidatePluginVerb()` — Run("--help"); regression guard for the [Verb] attribute.

    4. `[Fact] public void Run_AgainstEmptyDirectory_EnvelopeHasTopLevelSchemaVersionAndCommand()` — REVIEWS HIGH-6 envelope-shape regression guard:
       ```csharp
       var tempDir = MakeTempDir();
       try
       {
           var result = InProcessCliRunner.Run("validate-plugin", tempDir);
           Assert.Equal(0, result.ExitCode);
           var root = JToken.Parse(result.Stdout);
           Assert.Equal(1, root["schemaVersion"].Value<int>());        // top-level
           Assert.Equal("validate-plugin", root["command"].Value<string>()); // top-level
           Assert.Null(root["result"]["schemaVersion"]);               // NOT nested
           Assert.Null(root["result"]["command"]);                     // NOT nested
           Assert.Null(root["error"]);                                 // mutually exclusive
       }
       finally { Directory.Delete(tempDir, recursive: true); }
       ```

    5. `[Fact] public void Run_AgainstValidPlugin_LoaderObservedAtTopLevelResult()` — REVIEWS HIGH-5 regression guard + plan-checker iter-2 WARNING-5 placement guard. Asserts that `root["result"]["loaderObserved"]` is non-null and carries `pluginsCount` (int) + `loadErrors` (array). CRITICALLY asserts `root["result"]["plugins"][0]["loaderObserved"]` IS null (loaderObserved is NOT per-plugin per plan-checker iter-2 WARNING-5). This proves PluginLoader.Load was invoked AND that the field is at the directory-scoped top-level result, not duplicated per plugin.
  </action>
  <verify>
    <automated>msbuild Utinni.sln /m /p:Configuration=Release /p:Platform=x86 succeeds; Test-Path `Utinni.Cli.Tests\bin\Release\net472\Fixtures\plugins\valid-plugin\Utinni.CrtMatchPlugin.dll` returns True. Test-Path each of the three other DLL fixtures returns True. git ls-files Utinni.Cli.Tests/Fixtures/plugins/missing-createplugin/MissingCreatePlugin.dll Utinni.Cli.Tests/Fixtures/plugins/missing-destroyplugin/MissingDestroyPlugin.dll Utinni.Cli.Tests/Fixtures/plugins/wrong-iplugin-shape/WrongIPluginShape.dll returns three matches. git ls-files Utinni.Cli.Tests/Fixtures/plugins/*/expected.json returns four matches. Each .dll file is ≤16384 bytes. dotnet test Utinni.Cli.Tests/Utinni.Cli.Tests.csproj --no-build --configuration Release --filter "FullyQualifiedName~ValidatePluginCommandTests" exits 0 with all 8 logical test executions (4 Theory rows + 4 Facts) passing. PowerShell: `git check-attr eol Utinni.Cli.Tests/Fixtures/plugins/valid-plugin/expected.json` reports `eol: lf`. grep -nF "\"loaderObserved\":" Utinni.Cli.Tests/Fixtures/plugins/valid-plugin/expected.json returns exactly one match (REVIEWS HIGH-5 — field present; plan-checker iter-2 WARNING-5 — surfacing exactly ONCE means it is at result-level, not duplicated per plugin). PowerShell: `(ConvertFrom-Json (Get-Content -Raw Utinni.Cli.Tests/Fixtures/plugins/valid-plugin/expected.json)).result.loaderObserved` is non-null AND `(...).result.plugins[0].loaderObserved` is $null (plan-checker iter-2 WARNING-5 placement guard). grep -nF "EnvelopeHasTopLevelSchemaVersionAndCommand" Utinni.Cli.Tests/Commands/ValidatePluginCommandTests.cs returns exactly one match (REVIEWS HIGH-6 regression guard). grep -nF "LoaderObservedAtTopLevelResult" Utinni.Cli.Tests/Commands/ValidatePluginCommandTests.cs returns exactly one match (REVIEWS HIGH-5 regression guard + plan-checker iter-2 WARNING-5 placement guard).</automated>
  </verify>
  <done>The four sub-fixtures are committed + copied to test bin/ on every build. The four expected.json goldens lock the JSON contract conforming to REVIEWS HIGH-6 envelope + REVIEWS HIGH-5 loaderObserved field AT TOP-LEVEL result (plan-checker iter-2 WARNING-5 — NOT per-plugin) + REVIEWS MEDIUM-9 kind enum. The test class enforces per-sub-fixture goldens, --help warning text (REVIEWS HIGH-8 truthful version), top-level verb listing, the envelope-shape regression guard (REVIEWS HIGH-6), and the LoaderObserved-top-level-placement guard (REVIEWS HIGH-5 + plan-checker iter-2 WARNING-5). The regression guard for Phase 3 R-B D-13/D-14 (symmetric createPlugin/destroyPlugin ABI) is live in CI.</done>
</task>

<task type="auto">
  <name>Task 4.4: Promote DEC-C3 in PROJECT.md to LOCKED ✓ (phase-closing commit) — REVIEWS MEDIUM-16 gate: confirm 04-02-SUMMARY.md + 04-03-SUMMARY.md exist before promoting; verify CON-O-09 + CON-O-11 closure in assessment.md</name>
  <files>.planning/PROJECT.md, docs/ai/assessment.md</files>
  <read_first>
    - .planning/PROJECT.md line 118-121 (Key Decisions table: rows DEC-C1, DEC-C2, DEC-C3).
    - docs/ai/assessment.md (search for `CON-O-09` and `CON-O-11`).
    - .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-CONTEXT.md §"D-07" (PROJECT.md row-edit instruction).
    - .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-01-PLAN.md §"Task 1.7".
    - .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-02-PLAN.md §"Task 2.5".
    - .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-REVIEWS.md §"MEDIUM #16" (phase-closure ordering — DEC-C3 promotion requires all four commands shipped).
  </read_first>
  <behavior>
    - **REVIEWS MEDIUM-16 GATE:** Task 4.4 verifies that `.planning/phases/04-tier-2-cli-shim-golden-fixtures/04-02-SUMMARY.md` AND `.planning/phases/04-tier-2-cli-shim-golden-fixtures/04-03-SUMMARY.md` exist before promoting DEC-C3. If either is missing, the task PAUSES with a `checkpoint:human-verify` informing the operator that Plans 04-02 + 04-03 must land before DEC-C3 can be promoted. The verify-gate uses `Test-Path` (PowerShell-native per REVIEWS LOW-19 norm).
    - `.planning/PROJECT.md` line 120 reads `LOCKED ✓` in column 3 and `Promoted at Phase 4 close (Plan 04-04)` in column 4.
    - `docs/ai/assessment.md` contains the literal string `Resolved 2026-05-23 (CON-O-11, Phase 4 Plan 04-01` (from Plan 04-01 Task 1.7) AND a `Resolved 2026-05-23 (CON-O-09, Phase 4 Plan 04-02` paragraph (from Plan 04-02 Task 2.5). If either is missing, this task patches.
    - Commit message: `docs(04-04): promote DEC-C3 to LOCKED + verify CON-O-09/11 closure (phase 4 close)`.
  </behavior>
  <action>
    **REVIEWS MEDIUM-16 PRE-PROMOTION GATE (mandatory first step):**

    Run PowerShell checks:
    ```powershell
    $p02 = Test-Path .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-02-SUMMARY.md
    $p03 = Test-Path .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-03-SUMMARY.md
    if (-not ($p02 -and $p03)) {
        Write-Host "REVIEWS MEDIUM-16 gate: cannot promote DEC-C3 until Plans 04-02 + 04-03 land."
        # Pause this task with checkpoint:human-verify (executor handles via the workflow's pause primitive).
        exit 1
    }
    ```

    If the gate FAILS:
    - Pause task with `checkpoint:human-verify` block (REVIEWS MEDIUM-16):
      ```
      Plan 04-04 Task 4.4 cannot proceed:
        - 04-02-SUMMARY.md present: $p02
        - 04-03-SUMMARY.md present: $p03
      DEC-C3 promotion requires all four commands shipped per REVIEWS MEDIUM-16.
      Land Plans 04-02 + 04-03 (in any order) before resuming Plan 04-04 Task 4.4.
      ```
    - Do NOT edit PROJECT.md or assessment.md yet.

    If the gate PASSES, proceed:

    **PROJECT.md edit:** Open `.planning/PROJECT.md` and locate line 120 (the DEC-C3 row). Replace:

    Before: `| **DEC-C3 (candidate D-08)**: Testing strategy ... | test-harness-plan.md "Testing philosophy" | Candidate — non-locked | Promote to ADR when Tier 2 CLI shim lands (Phase 4). |`

    After: `| **DEC-C3 (LOCKED, was candidate D-08)**: Testing strategy is pragmatic and tiered, not blanket TDD. TDD applies only to pure-logic and file-format layers; native detours and WinForms UI use smoke/integration tests via Tiers 2–4. | test-harness-plan.md "Testing philosophy" | LOCKED ✓ | Promoted at Phase 4 close (Plan 04-04). |`

    Do NOT modify DEC-C1 or DEC-C2.

    **assessment.md verification + patch:** Check `grep -n "Resolved 2026-05-23 (CON-O-11, Phase 4 Plan 04-01" docs/ai/assessment.md` returns exactly one match. If 0, Plan 04-01 Task 1.7 did not land — patch by appending the paragraph (text per Plan 04-01 Task 1.7 §action lines 397-409).

    Check `grep -n "Resolved 2026-.*CON-O-09" docs/ai/assessment.md` returns at least one match. If 0, Plan 04-02 Task 2.5 did not close CON-O-09 — patch by appending the paragraph (text per Plan 04-02 Task 2.5 §action).

    **Phase-closing commit message:** `docs(04-04): promote DEC-C3 to LOCKED + verify CON-O-09/11 closure (phase 4 close)`.
  </action>
  <verify>
    <automated>PowerShell: `Test-Path .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-02-SUMMARY.md` returns True. PowerShell: `Test-Path .planning/phases/04-tier-2-cli-shim-golden-fixtures/04-03-SUMMARY.md` returns True (REVIEWS MEDIUM-16 gate satisfied). grep -n "DEC-C3.*LOCKED ✓" .planning/PROJECT.md returns exactly one match (the promoted row). grep -n "Promoted at Phase 4 close (Plan 04-04)" .planning/PROJECT.md returns exactly one match. grep -n "Candidate.*non-locked.*DEC-C3" .planning/PROJECT.md returns 0 matches. grep -n "Resolved 2026-05-23 (CON-O-11, Phase 4 Plan 04-01" docs/ai/assessment.md returns 1 match. grep -nE "Resolved 2026-.*CON-O-09" docs/ai/assessment.md returns at least 1 match. Whole-phase regression check: `dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release && dotnet test Utinni.Cli.Tests/Utinni.Cli.Tests.csproj --no-build --configuration Release` exits 0 (both lanes still green).</automated>
  </verify>
  <done>DEC-C3 promoted from candidate to LOCKED ✓ in PROJECT.md (only after REVIEWS MEDIUM-16 gate confirms 04-02 + 04-03 SUMMARY files exist). Both CON-O-09 + CON-O-11 dispositions confirmed in docs/ai/assessment.md. Phase 4 closes with DEC-C3 ADR-equivalent status. The commit message identifies this as the phase-closing commit; `/gsd:verify-work` runs next.</done>
</task>

</tasks>

<verification>
After all four tasks complete:

1. `msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86` exits 0. Post-build copy of `Utinni.CrtMatchPlugin.dll` into `valid-plugin/` directory present.

2. `dotnet test Utinni.Cli.Tests/Utinni.Cli.Tests.csproj --no-build --configuration Release` exits 0. New tests added by this plan:
   - PluginInspectionTests: 9 tests (Task 4.1 — includes REVIEWS HIGH-1 PRE-FLIGHT promoted to Test 1, REVIEWS MEDIUM-9 unknown branch, REVIEWS MEDIUM-15 assembly-marker, robust comment-stripped grep).
   - ValidatePluginCommandTests: 8 logical executions (4 [Theory rows] + 4 [Fact] — includes REVIEWS HIGH-6 envelope guard and REVIEWS HIGH-5 LoaderObserved guard).
   - Total Plan 04-04 additions: 17.

3. `dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release` exits 0 — no Phase 1/2/2.1/3 regression.

4. PowerShell: `& bin\Release\utinni-cli.exe validate-plugin Utinni.Cli.Tests\bin\Release\net472\Fixtures\plugins\valid-plugin` exits 0; output's TOP-LEVEL keys are `{ command, result, schemaVersion }` (REVIEWS HIGH-6); `result.plugins[0].id == "Utinni.CrtMatchPlugin"`; `result.plugins[0].overallStatus == "pass"`; `result.loaderObserved.pluginsCount` is present AT TOP-LEVEL result (REVIEWS HIGH-5 + plan-checker iter-2 WARNING-5); `result.plugins[0].loaderObserved` is ABSENT (hoisting verified).

5. PowerShell: `& bin\Release\utinni-cli.exe validate-plugin --help` stdout contains the verbatim two-sentence T-04-EoP warning (REVIEWS HIGH-8 — truthful: mentions PluginLoader composition and static initialisers, NOT just DllMain).

6. `.planning/PROJECT.md` shows DEC-C3 in column 3 reading `LOCKED ✓` (only after REVIEWS MEDIUM-16 gate confirms 04-02 + 04-03 SUMMARY files exist).

7. `docs/ai/assessment.md` carries both CON-O-09 and CON-O-11 Resolution paragraphs.

8. CI gate (per D-05): the merge commit for Plan 04-04 turns the next master CI workflow green on BOTH `dotnet test` steps. Phase 4 is officially closed when both lanes are green on master with Plan 04-04's commits at HEAD.

9. Source separation contract: PluginInspectionTests Test 8 (AssemblyMetadata marker read via reflection — REVIEWS MEDIUM-15) AND Test 9 (comment-stripped source grep) both pass.

10. REVIEWS HIGH-1 fix verified: `grep -n "LoadLibraryEx\\|GetProcAddress" Utinni.Cli/Commands/NativeExportProbe.cs` returns ZERO matches. `grep -n "PEReader\\|System.Reflection.Metadata" Utinni.Cli/Commands/NativeExportProbe.cs` returns at least one match.

11. REVIEWS HIGH-5 fix verified: `grep -nE "new PluginLoader\\(autoLoad: ?false\\)\\.Load\\(" Utinni.Cli/Commands/PluginInspection.cs` returns at least one match.

Phase-closing sequence after Plan 04-04 lands green:
- `/gsd:verify-work 4` confirms every must-have artifact + truth is satisfied.
- `/gsd:code-review 04` runs.
- ROADMAP.md Phase 4 entry flipped from `[ ]` to `[x]`; Phase 5 (Catch2 + TEST-02) becomes the next planning target.
</verification>

<success_criteria>
- [ ] All four tasks committed (one commit per task, atomic, in the order 4.1 → 4.2 → 4.3 → 4.4).
- [ ] `Utinni.Cli/Commands/PluginInspection.cs` + `NativeExportProbe.cs` exist; PluginInspection has zero `Console.*` and zero `Newtonsoft.*` references (source-level grep enforced + AssemblyMetadata marker per REVIEWS MEDIUM-15).
- [ ] NativeExportProbe uses `System.Reflection.Metadata.PEReader` for PE export-table parsing — NO `LoadLibraryExW`/`GetProcAddress` calls (REVIEWS HIGH-1 fix path A).
- [ ] PluginInspection MANAGED PATH actually invokes `new PluginLoader(autoLoad: false).Load(dir)` ONCE for the directory and surfaces `loader.Plugins.Count + loader.LoadErrors` in the JSON envelope at TOP-LEVEL `result.loaderObserved` — NOT per-plugin (REVIEWS HIGH-5 + plan-checker iter-2 WARNING-5).
- [ ] PluginInspection emits `kind = "unknown"` with all-fail checks when a DLL is neither native-exporting nor managed-plugin-shaped (REVIEWS MEDIUM-9).
- [ ] `Utinni.Cli/Properties/AssemblyInfo.cs` carries `[assembly: AssemblyMetadata("validate-plugin-version", "1")]` (REVIEWS MEDIUM-15).
- [ ] `Utinni.Cli/Utinni.Cli.csproj` carries PackageReferences on `System.Reflection.Metadata` 1.6.0 + `System.Collections.Immutable` 1.5.0 (REVIEWS HIGH-1 + #17). Lock file regenerated.
- [ ] `Utinni.Cli/Commands/ValidatePluginCommand.cs` no longer carries the Plan 04-01 NotImplemented stub; its --help text contains the verbatim truthful T-04-EoP warning (REVIEWS HIGH-8).
- [ ] JSON envelope has TOP-LEVEL `{ command, result | error, schemaVersion }` (REVIEWS HIGH-6).
- [ ] `Utinni.Cli.Tests/Fixtures/plugins/{valid-plugin,missing-createplugin,missing-destroyplugin,wrong-iplugin-shape}/` exist, each with an `expected.json` (with `loaderObserved` at TOP-LEVEL `result` per REVIEWS HIGH-5 + plan-checker iter-2 WARNING-5) and (for the latter three) a small pre-built `.dll` ≤16 KB.
- [ ] `Utinni.Cli.Tests/Utinni.Cli.Tests.csproj` carries the `CopyValidPluginFixture` Target.
- [ ] PowerShell: `& bin\Release\utinni-cli.exe validate-plugin <valid-plugin-dir>` exits 0; goldens green.
- [ ] `.planning/PROJECT.md` DEC-C3 row says `LOCKED ✓` (only after REVIEWS MEDIUM-16 gate passes).
- [ ] `docs/ai/assessment.md` carries Resolution paragraphs for CON-O-09 AND CON-O-11.
- [ ] Both CI lanes green on master.
- [ ] No regression in Phase 1/2/2.1/3 tests.
- [ ] Threat model claims align with code (REVIEWS HIGH-8): native path = no DllMain (PE-parse); managed path = module ctors execute via PluginLoader + Phase 2 C-06 isolation.
- [ ] Four-command roadmap commitment from TEST-03 fully delivered: parse-tre + list-objects (Plan 04-02) + inspect-iff (Plan 04-03) + validate-plugin (Plan 04-04).
</success_criteria>

<output>
Create `.planning/phases/04-tier-2-cli-shim-golden-fixtures/04-04-SUMMARY.md` when done — record:
- Final shape of PluginInspection.cs's POCO graph including the DirectoryReport top-level POCO and the LoaderObservation field on it (REVIEWS HIGH-5 + plan-checker iter-2 WARNING-5).
- Confirmation that NativeExportProbe uses PEReader and NO LoadLibrary* (REVIEWS HIGH-1).
- Confirmation that PluginInspection invokes PluginLoader.Load (REVIEWS HIGH-5).
- Confirmation that kind=unknown branch is documented and tested (REVIEWS MEDIUM-9).
- Confirmation that AssemblyMetadata marker is read by Test 8 (REVIEWS MEDIUM-15).
- Whether the three pre-built fixture DLLs were built inline or required the build.cmd fallback (note the .dll sizes).
- Final expected.json contents for all four sub-fixtures.
- The post-build Target diff added to `Utinni.Cli.Tests.csproj`.
- The DEC-C3 PROJECT.md row diff.
- Confirmation status for CON-O-09 + CON-O-11 closure.
- Test count added by Plan 04-04: 17 logical test executions.
- Confirmation that `--help` for `validate-plugin` contains the verbatim truthful T-04-EoP warning (REVIEWS HIGH-8).
- Phase 4 wave check: confirm 04-02 + 04-03 landed green ahead of 04-04 (REVIEWS MEDIUM-16 gate evidence).
- Next phase: Phase 5 (Catch2 + TEST-02). ROADMAP.md Phase 4 entry flipped from `[ ]` to `[x]`.
- REVIEWS revisions applied: HIGH-1, HIGH-5, HIGH-6, HIGH-8, MEDIUM-9, MEDIUM-10 (partial — mitigated by HIGH-5 fix), MEDIUM-15, MEDIUM-16, #17 — each cited inline.
</output>
