---
phase: 19-dx11backend-config-detection-resize
plan: 03
type: execute
wave: 3
depends_on: ["19-02"]
files_modified:
  - UtinniCore/swg/ui/imgui_impl.cpp
  - UtinniCore/utinni.cpp
  - UtinniCore/swg/graphics/directx11.cpp
autonomous: false
requirements: [RNDR-02, RNDR-03, RNDR-04]
must_haves:
  truths:
    - "imgui_impl::setup() selects exactly one backend per session via a single gl11_r.dll detection check: default D3D9, Dx11Backend only on positive D3D11 detection (D-15/D-17)"
    - "A one-shot diagnostic log names the selected backend (gl11_r.dll/Dx11Backend or default D3D9) (RNDR-03)"
    - "imgui_impl.cpp stays API-neutral — the detection branch introduces no directx11.h / DX11 / DXGI symbol (the D-06 gate passes)"
    - "The directX11::tryInstall poll installs the DXGI detours once the advertised swapchain is non-null, latched (one-backend-per-session, no doubled context)"
    - "The maintainer live-smoke confirms RNDR-02/03/04 on the live 32-bit D3D11 gl11_r.dll client (D-22)"
  artifacts:
    - path: "UtinniCore/swg/ui/imgui_impl.cpp"
      provides: "one-backend-per-session detection + selection at setup()"
      contains: "selectBackend"
  key_links:
    - from: "imgui_impl::setup detection branch"
      to: "render_backend::set(dx9Singleton/dx11Singleton)"
      via: "selectBackend(gl11Present, getHookPointsResolved)"
      pattern: "selectBackend"
    - from: "the D3D11 install poll"
      to: "directX11::tryInstall"
      via: "per-frame latched poll (swapChain != null gate)"
      pattern: "tryInstall"
---

<objective>
Wire the one-backend-per-session detection + install path and gate the phase on the maintainer live-smoke.
`imgui_impl::setup()` does a single `gl11_r.dll` check (default D3D9, Dx11Backend only on positive detect)
via the API-neutral `selectBackend()` function; the DXGI detour install fires from a latched per-frame poll
of `directX11::tryInstall()` (the advertised swapchain may not exist at first setup); a one-shot diagnostic
log names the chosen backend. Then the maintainer injects into the live D3D11 client and confirms
RNDR-02/03/04 (the final acceptance gate, D-22).

Purpose: This is the RNDR-03 selection logic + the integration point that makes RNDR-02/04 live. It must
keep imgui_impl.cpp DX9/DX11-neutral (D-05/D-06) — the install poll lives in the DX11 hook tier, NOT in
imgui_impl. The live-smoke is the only place RNDR-02/03/04 are truly proven (CI cannot run live D3D11).
Output: detection wiring in setup(), the latched install poll, any required utinni.cpp kick-off, live-smoke sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-dx11backend-config-detection-resize/19-CONTEXT.md
@.planning/phases/19-dx11backend-config-detection-resize/19-RESEARCH.md
@.planning/phases/19-dx11backend-config-detection-resize/19-PATTERNS.md
@.planning/phases/19-dx11backend-config-detection-resize/19-INSTRUMENTATION-SPEC.md

<interfaces>
<!-- From Plan 01: the API-neutral selection function (names NO DX11 type, safe to call from imgui_impl). -->
From UtinniCore/swg/graphics/backend_select.h:
  enum class BackendChoice { Dx9, Dx11 };
  BackendChoice selectBackend(bool gl11Present, bool getHookPointsResolved);   // Dx11 iff both true (D-15/D-17)

<!-- From Plan 01/02: the seam accessors + the DX11 hook tier. -->
render_backend::set(render_backend::dx9Singleton());  // existing default install at imgui_impl.cpp:283
render_backend::dx11Singleton();                      // the Dx11 twin
directX11::tryInstall();                              // poll + latched detour install (declared in directx11.h)

<!-- The existing setup() seam (imgui_impl.cpp ~263-293): isSetup latch, ImGui::CreateContext once,
     render_backend::set(...) BEFORE isSetup flips, then dx9Singleton()->init(nullptr) -> HWND, bail on null. -->
<!-- The existing DX9 install model (directx9.cpp hkPresent 378-386): the device-bearing install fires from
     the HOOK TIER (gated on !imgui_impl::isReady()), NOT from imgui_impl. Mirror this for DX11. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: One-backend-per-session detection at setup() + latched DX11 install poll</name>
  <files>UtinniCore/swg/ui/imgui_impl.cpp, UtinniCore/utinni.cpp, UtinniCore/swg/graphics/directx11.cpp</files>
  <read_first>
    - UtinniCore/swg/ui/imgui_impl.cpp (the full setup() 262-310: isSetup latch, CreateContext, the unconditional render_backend::set(dx9Singleton()) at 283, dx9Singleton()->init(nullptr) + bail-on-null)
    - UtinniCore/swg/graphics/directx9.cpp (hkPresent 362-388 — the !imgui_impl::isReady() one-shot install gate that runs the device-bearing install FROM the hook tier, the model for the DX11 poll)
    - UtinniCore/utinni.cpp (createDetours ~109-166 + the before-detours eager-init ordering ~367-375; the directX::detour() call site model — note the DX9 tier installs from graphics.cpp::hkInstall, not createDetours)
    - UtinniCore/swg/graphics/backend_select.h (the selectBackend pure function from Plan 01)
    - UtinniCore/swg/graphics/directx11.h (directX11::tryInstall from Plan 02)
    - .planning/phases/19-dx11backend-config-detection-resize/19-PATTERNS.md (MODIFIED imgui_impl.cpp setup section — the RECONCILE note: keep directx11.h OUT of imgui_impl.cpp; MODIFIED utinni.cpp section — likeliest outcome: no change)
    - .planning/phases/19-dx11backend-config-detection-resize/19-RESEARCH.md (Code Examples: detection+selection; Open Q2 poll location; Pitfall 3 doubled-input)
  </read_first>
  <action>
    In imgui_impl::setup(): replace the unconditional `render_backend::set(render_backend::dx9Singleton())`
    at line 283 with a single one-backend-per-session selection. Compute the choice via the API-neutral
    `selectBackend(gl11Present, getHookPointsResolved)` from backend_select.h — gl11Present =
    (GetModuleHandleA("gl11_r.dll") || GetModuleHandleA("gl11_d.dll")) (plain Win32 string calls, which
    pass the D-06 gate); getHookPointsResolved = the latched result of the DX11 install poll. CRITICAL
    (D-05/D-06): imgui_impl.cpp MUST NOT include directx11.h or name any DX11/DXGI symbol — the install
    poll (directX11::tryInstall) lives in the DX11 HOOK TIER (directx11.cpp), mirroring how the DX9
    device-bearing install fires from directx9.cpp::hkPresent (gated on !isReady()), NOT from imgui_impl.
    Choose the shape that keeps directx11.h out of imgui_impl.cpp: setup() selects dx9Singleton() vs
    dx11Singleton() based on selectBackend; the actual DXGI detour install + ImGui_ImplDX11_Init runs from a
    latched per-frame poll in directx11.cpp (the swapchain may not be ready at first setup — spec §3.3 /
    Open Q2). Emit a ONE-SHOT diagnostic log naming the chosen backend (info: "Render backend: D3D11 (gl11
    detected, GetHookPoints advertised)" / info: "Render backend: D3D9 (default; no gl11 detected)" /
    warning: "gl11 detected but GetHookPoints absent/not-ready; defaulting D3D9") — RNDR-03's one-shot log.
    The existing isSetup/CreateContext latch already guarantees exactly one ImGui context (Pitfall 3). In
    directx11.cpp: ensure the tryInstall poll is wired to fire per-frame until latched — register a DX11
    kick-off where the DX9 path mirrors directX::detour() (graphics.cpp::hkInstall call site model) OR poll
    from a minimal Present-equivalent; confirm whether utinni.cpp needs any change (likeliest: none — the
    detection+install is self-contained in the hook tier + setup; if a kick-off must be registered, place it
    beside the directX::detour() site with the CON-H-01/CON-N-01 rationale, preserving the before-detours
    init ordering). Build INLINE (x86 Release). git checkout -- Generated/UtinniCore.cs after.
  </action>
  <verify>
    <automated>cd UtinniCore.Tests && grep -c "directx11.h" ../UtinniCore/swg/ui/imgui_impl.cpp</automated>
  </verify>
  <acceptance_criteria>
    - imgui_impl.cpp calls `selectBackend(...)` and installs dx9Singleton() or dx11Singleton() accordingly (one selection, no second backend set)
    - `grep -c "directx11.h" UtinniCore/swg/ui/imgui_impl.cpp` returns 0 (DX11 install stays in the hook tier)
    - `grep -c "directX11::" UtinniCore/swg/ui/imgui_impl.cpp` returns 0 and `grep -c "ID3D11" UtinniCore/swg/ui/imgui_impl.cpp` returns 0
    - The extended D-06 neutrality gate `[rndr01][graphics]` still passes (imgui_impl.cpp/.h free of DX11/DXGI symbols)
    - A one-shot diagnostic log line names the selected backend (info/warning per the three paths)
    - The DX11 install poll (directX11::tryInstall) is latched — a second call after install is a no-op (no doubled context / doubled detour)
    - UtinniCore.dll builds green (x86 Release); `UtinniCore.Tests.exe "[graphics]"` green (seam + offset + detection + neutrality)
  </acceptance_criteria>
  <done>One-backend-per-session detection wired at setup() with a one-shot log; DX11 install poll latched in the hook tier; imgui_impl.cpp stays DX11/DXGI-neutral; full graphics test lane green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Maintainer live-smoke — RNDR-02/03/04 on the live D3D11 client (D-22)</name>
  <what-built>
    The Dx11Backend twin behind the Phase 18 seam: DXGI Present (vtbl 8) + ResizeBuffers (vtbl 13) hooks via
    the advertised gl11_r.dll GetHookPoints() contract, per-frame backbuffer-RTV rebind (flip-discard),
    one-backend-per-session gl11_r.dll detection with a one-shot diagnostic log, and the true DXGI
    ResizeBuffers RTV release/recreate. All four D-21 automated Catch2/xUnit layers are green in CI; this is
    the final manual acceptance gate (CI cannot run a live D3D11 SWG session).
  </what-built>
  <how-to-verify>
    PREREQUISITE (D-14 sequencing): a built gl11_r.dll that EXPORTS GetHookPoints (undecorated, __cdecl,
    returning the live {IDXGISwapChain1*, ID3D11Device*, ID3D11DeviceContext*}) must be staged in the live
    32-bit D3D11 SWG-Source client. The export is implemented in the pinned swg-client-v2 @ 056632a
    (Direct3d11.cpp:879-888) per the handoff spec; confirm the DEPLOYED gl11_r.dll carries it (a non-exporting
    build means no overlay by design — graceful bail, not a crash).

    Then, maintainer-only:
    1. Inject UtinniCore.dll into the running 32-bit D3D11 SWG-Source client (gl11_r.dll loaded).
    2. RNDR-03: confirm the utinni log shows the ONE-SHOT detection line naming gl11_r.dll + the D3D11
       backend (Dx11Backend), exactly one ImGui context, and no doubled input.
    3. RNDR-02: confirm the ImGui overlay RENDERS and mouse/keyboard map in RENDER-TARGET SPACE (the overlay
       tracks the client, input lands where expected — the same RT-space discipline as the D3D9 path).
    4. RNDR-04: trigger a WM_DISPLAYCHANGE (monitor resolution/refresh mode change) in the live client;
       confirm the overlay re-renders, the RTV is recreated, and there is NO DXGI_ERROR_INVALID_CALL in the
       log. (Free WM_SIZE panel drag-resize is the deliberately-deferred RESID-04 bound per Open Q1 / D-18b —
       do NOT score it here; RNDR-04 acceptance is the mode-change path only.)

    If the deployed gl11_r.dll lacks GetHookPoints, confirm the GRACEFUL bail: utinni logs a warning and
    runs no overlay, no crash (spec §7.2) — then stage the export and re-smoke.
  </how-to-verify>
  <resume-signal>Type "approved" (RNDR-02/03/04 confirmed live), or describe the issue (renders-but-no-input / INVALID_CALL on mode change / doubled context / no-export-crash) so it can be diagnosed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gl11_r.dll detection -> backend selection | A single GetModuleHandleA check decides the whole session's backend. A wrong/duplicate selection would double-install — guarded by the one-shot selection + isSetup/CreateContext latch. |
| (runtime hook boundaries) | The advertised-pointer + detour boundaries are owned by Plan 02 (T-19-03..07); this plan adds only the selection + latched poll wiring. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-08 | Elevation / Tampering | Doubled backend install or dual ImGui context (RNDR-03 anti-target) | mitigate | Single selectBackend() decision + the existing isSetup/CreateContext latch (one context); the tryInstall poll is latched (idempotent after install) — verified by the live-smoke step 2. |
| T-19-04 | Denial of Service (crash) | gl11 detected but GetHookPoints absent on the deployed dll | mitigate | selectBackend returns Dx9 when getHookPointsResolved is false -> graceful default-D9 + warning log, no overlay, no crash (spec §7.2); confirmed in the live-smoke graceful-bail check. |

**N/A for this offline local modding overlay:** Authentication, Session, Access Control, Cryptography — no
network/IPC/untrusted-input surface introduced (selection is a local GetModuleHandle check + a same-process
advertised contract). Consistent with the locked anti-goals (DEC-A1/A4: local/offline editing only).
</threat_model>

<verification>
- imgui_impl.cpp selects exactly one backend, stays DX11/DXGI-neutral (D-06 gate green).
- One-shot diagnostic log names the chosen backend (RNDR-03).
- DX11 install poll latched (no doubled context/detour).
- Maintainer live-smoke confirms RNDR-02 (render + RT-space input), RNDR-03 (single backend + log), RNDR-04
  (survives WM_DISPLAYCHANGE, no DXGI_ERROR_INVALID_CALL) on the live D3D11 client.
</verification>

<success_criteria>
- One-backend-per-session detection wired with a one-shot log; imgui_impl.cpp DX11/DXGI-neutral.
- Full `[graphics]` Catch2 lane + extended neutrality gate green (CI-enforceable layers).
- Maintainer live-smoke: RNDR-02/03/04 confirmed on the live 32-bit D3D11 gl11_r.dll client (D-22).
</success_criteria>

<output>
Create `.planning/phases/19-dx11backend-config-detection-resize/19-03-SUMMARY.md` when done.
</output>
