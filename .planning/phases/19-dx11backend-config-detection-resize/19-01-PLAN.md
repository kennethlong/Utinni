---
phase: 19-dx11backend-config-detection-resize
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vcpkg.json
  - UtinniCore/swg/graphics/render_backend.h
  - UtinniCore.Tests/Graphics/ImguiApiNeutralityTests.cpp
  - UtinniCore.Tests/Graphics/RenderBackendSeamTests.cpp
  - UtinniCore.Tests/Graphics/Dx11VtblOffsetTests.cpp
  - UtinniCore.Tests/Graphics/Dx11DetectionTests.cpp
  - UtinniCore.Tests/UtinniCore.Tests.vcxproj
autonomous: true
requirements: [RNDR-02, RNDR-03]
must_haves:
  truths:
    - "The vcpkg imgui dx11-binding feature is enabled so imgui_impl_dx11.{h,cpp} resolve"
    - "render_backend.h declares Dx11Backend final : public IRenderBackend with the same 10 vtable overrides + a DX11 init() + dx11Singleton(), with NO <d3d11.h>/<dxgi1_2.h> include"
    - "The D-06 API-neutrality gate also bans D3D11/DXGI symbol forms in imgui_impl.{cpp,h}"
    - "DXGI vtbl offsets Present=8 and ResizeBuffers=13 are pinned by a Catch2 test"
    - "gl%02d_r.dll detection logic (default-D9 / positive-D11 / ambiguous->D9+log) is a pure testable function with a passing Catch2 unit test (D-16 number->API mapping; D-21 automated layer 4)"
    - "The Dx11 mock-dispatch contract is asserted against the device-free IRenderBackend ABC (no concrete Dx11Backend instantiation) — D-21 automated layers 1/2 (vtbl-offset pin + mock dispatch)"
  artifacts:
    - path: "vcpkg.json"
      provides: "imgui dx11-binding feature"
      contains: "dx11-binding"
    - path: "UtinniCore/swg/graphics/render_backend.h"
      provides: "Dx11Backend declaration + dx11Singleton()"
      contains: "class Dx11Backend final"
    - path: "UtinniCore.Tests/Graphics/Dx11VtblOffsetTests.cpp"
      provides: "Present=8 / ResizeBuffers=13 ABI-drift fence"
    - path: "UtinniCore.Tests/Graphics/Dx11DetectionTests.cpp"
      provides: "detection/fallback unit test"
  key_links:
    - from: "render_backend.h Dx11Backend decl"
      to: "render_backend_dx11.cpp (Plan 02)"
      via: "override signatures the impl must match"
      pattern: "class Dx11Backend final"
    - from: "ImguiApiNeutralityTests.cpp dx11/dxgi tokens"
      to: "imgui_impl.cpp neutrality"
      via: "extended banned-token set"
      pattern: "ID3D11Device"
---

<objective>
Land the Phase 19 contracts and test scaffolding that everything downstream builds against: enable the
vcpkg imgui `dx11-binding` feature (genuinely missing today), declare the `Dx11Backend` twin in the
existing Phase 18 seam header, extend the D-06 API-neutrality gate to also ban D3D11/DXGI symbol forms,
and create the three device-free Catch2 layers (vtbl-offset pin, detection-logic unit, Dx11 mock-dispatch).

Purpose: Interface-first ordering. Plan 02 implements `Dx11Backend`/`directX11::` against the decls here;
Plan 03 wires detection against the testable detection function here. The vcpkg feature is the hard Wave 0
prerequisite for any TU that includes `<imgui_impl_dx11.h>`.
Output: vcpkg.json feature, `Dx11Backend` decl in render_backend.h, extended neutrality gate, 2 new Catch2
files (offset + detection) + an extended seam-dispatch test, registered in the test vcxproj.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-dx11backend-config-detection-resize/19-CONTEXT.md
@.planning/phases/19-dx11backend-config-detection-resize/19-RESEARCH.md
@.planning/phases/19-dx11backend-config-detection-resize/19-PATTERNS.md

<interfaces>
<!-- The Phase 18 seam ABC the Dx11Backend twin plugs into. Extracted from render_backend.h.
     The 10 pure virtuals (6 ROADMAP-named + A2 color + A2 stage get/set) are below; Dx11Backend
     overrides ALL 10, mirroring the existing Dx9Backend decl block. -->

From UtinniCore/swg/graphics/render_backend.h (the IRenderBackend ABC):
  virtual void newFrame() = 0;
  virtual void renderDrawData(ImDrawData* drawData) = 0;
  virtual void onPreResize() = 0;
  virtual void onPostResize() = 0;
  virtual int renderTargetWidth() = 0;
  virtual int renderTargetHeight() = 0;
  virtual ImTextureID sceneDepthTexture() = 0;
  virtual ImTextureID sceneColorTexture() = 0;
  virtual int sceneDepthStage() = 0;
  virtual void setSceneDepthStage(int stage) = 0;

The existing Dx9Backend decl block (lines 84-126) is the structural template. It forward-declares
`struct IDirect3DDevice9;` at line 37 to keep <d3d9.h> out of the header (D-05 gate). The accessor trio
at the bottom: `IRenderBackend* get(); void set(IRenderBackend*); Dx9Backend* dx9Singleton();`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable vcpkg imgui dx11-binding + reinstall</name>
  <files>vcpkg.json</files>
  <read_first>
    - vcpkg.json (the imgui features array, lines 19-26)
    - .planning/phases/19-dx11backend-config-detection-resize/19-RESEARCH.md (Pitfall 1; Summary; Environment Availability)
    - .planning/phases/19-dx11backend-config-detection-resize/19-PATTERNS.md (MODIFIED vcpkg.json section)
  </read_first>
  <action>
    Add the string "dx11-binding" to the imgui `features` array in vcpkg.json (currently
    ["docking-experimental", "dx9-binding", "win32-binding"]). This is genuinely missing — AGENTS.md's
    claim it is already present is outdated/incorrect (verified). Place it alphabetically-adjacent to
    "dx9-binding" for readability. Then trigger a manifest-mode vcpkg reinstall by building UtinniCore via
    VS2026 MSBuild on Utinni.sln (/p:Configuration=Release /p:Platform=x86) — the x86-windows triplet
    reinstall produces imgui_impl_dx11.{h,cpp} under vcpkg_installed/x86-windows/. The self-hosted runner
    has flaked the vcpkg-install step before (project_ci_debug_gitignore_trap) — if the install fails,
    re-run the build once before treating it as a real failure. Do NOT commit Generated/UtinniCore.cs
    (git checkout -- it if it churned). Build INLINE on the main tree (worktrees OFF).
  </action>
  <verify>
    <automated>test -f vcpkg_installed/x86-windows/include/imgui_impl_dx11.h && echo PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"dx11-binding"' vcpkg.json` returns 1
    - File `vcpkg_installed/x86-windows/include/imgui_impl_dx11.h` exists after the MSBuild reinstall
    - File `vcpkg_installed/x86-windows/include/imgui_impl_dx11.cpp` exists (vcpkg imgui port ships the .cpp under include/)
    - UtinniCore.dll builds green (x86 Release)
  </acceptance_criteria>
  <done>vcpkg.json lists dx11-binding; imgui_impl_dx11.{h,cpp} present in vcpkg_installed; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Declare Dx11Backend in the seam header + extend the D-06 neutrality gate</name>
  <files>UtinniCore/swg/graphics/render_backend.h, UtinniCore.Tests/Graphics/ImguiApiNeutralityTests.cpp</files>
  <read_first>
    - UtinniCore/swg/graphics/render_backend.h (the full file — the Dx9Backend decl block 84-126 + the line-37 IDirect3DDevice9 forward-decl + the accessor trio 123-125)
    - UtinniCore/swg/graphics/render_backend_dx9.cpp (the init() shape + dx9Singleton() the twin mirrors)
    - UtinniCore.Tests/Graphics/ImguiApiNeutralityTests.cpp (the dx9Tokens() set 60-88 + the stripper-hygiene self-check 93-127)
    - .planning/phases/19-dx11backend-config-detection-resize/19-PATTERNS.md (MODIFIED render_backend.h section + EXTENDED ImguiApiNeutralityTests section + Pitfall 5)
  </read_first>
  <behavior>
    - render_backend.h compiles with NO <d3d11.h>/<dxgi1_2.h> include (forward-decls only)
    - ImguiApiNeutralityTests stripper-hygiene self-check planting a DX11 token inside a comment counts 0 stripped / >0 raw (proves the stripper still does work on at least one new token)
    - The imgui_impl.cpp/.h neutrality SECTIONs still pass (no DX11/DXGI symbols present yet — they are added behind the seam in Plan 02)
  </behavior>
  <action>
    In render_backend.h: add a `class Dx11Backend final : public IRenderBackend` declaration directly
    after the Dx9Backend block, mirroring its 10 overrides verbatim (newFrame, renderDrawData, onPreResize,
    onPostResize, renderTargetWidth, renderTargetHeight, sceneDepthTexture, sceneColorTexture,
    sceneDepthStage, setSceneDepthStage). Add a NON-VIRTUAL off-vtable `HWND init(ID3D11Device* device,
    ID3D11DeviceContext* context);` (signature DIFFERS from Dx9's single-device init — per the line-98
    rationale). Add `Dx11Backend* dx11Singleton();` beside the existing `dx9Singleton();` accessor. CRITICAL
    (D-05 purge): keep <d3d11.h>/<dxgi1_2.h> OUT of this header — add forward declarations
    `struct ID3D11Device;` and `struct ID3D11DeviceContext;` beside the existing `struct IDirect3DDevice9;`
    forward-decl (line 37). Do NOT add any private member that names a concrete DX11 type by value (use
    pointers; the RTV/device/context state lives in render_backend_dx11.cpp's translation unit — declare any
    needed private pointer members as forward-declared-pointer types, e.g. `ID3D11RenderTargetView* m_rtv`).
    In ImguiApiNeutralityTests.cpp: extend the banned token set to ALSO ban the DX11/DXGI concrete symbol
    forms: "#include <d3d11.h>", "#include \"d3d11.h\"", "#include <dxgi1_2.h>", "#include <dxgi.h>",
    "#include <imgui_impl_dx11.h>", "imgui_impl_dx11", "directx11.h", "directX11::", "ID3D11Device",
    "ID3D11DeviceContext", "ID3D11RenderTargetView", "ID3D11ShaderResourceView", "IDXGISwapChain". Keep
    gating on CONCRETE symbol forms — do NOT add the bare strings "D3D11" or "DXGI" (comments mention them;
    grep-gate hygiene per feedback_gsd_grep_gate_hygiene). Add at least one new DX11 token to the
    stripper-hygiene self-check SECTION (plant it in a comment, assert stripped==0 / raw>0).
  </action>
  <verify>
    <automated>cd UtinniCore.Tests && grep -c "ID3D11Device" Graphics/ImguiApiNeutralityTests.cpp</automated>
  </verify>
  <acceptance_criteria>
    - render_backend.h declares `class Dx11Backend final : public IRenderBackend` (grep matches `class Dx11Backend final`)
    - render_backend.h declares `Dx11Backend* dx11Singleton();`
    - `grep -c '#include <d3d11.h>' UtinniCore/swg/graphics/render_backend.h` returns 0 (forward-decls only)
    - render_backend.h contains forward-decls `struct ID3D11Device;` and `struct ID3D11DeviceContext;`
    - ImguiApiNeutralityTests.cpp dx11 token set includes "ID3D11Device", "directX11::", "imgui_impl_dx11", "IDXGISwapChain"
    - UtinniCore.Tests Catch2 exe runs the `[rndr01][graphics]` neutrality test green (imgui_impl still neutral — DX11 symbols not present yet)
  </acceptance_criteria>
  <done>Dx11Backend declared behind the seam with zero DX11 includes; neutrality gate extended and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the vtbl-offset pin, detection-logic unit test, and Dx11 mock-dispatch test</name>
  <files>UtinniCore.Tests/Graphics/Dx11VtblOffsetTests.cpp, UtinniCore.Tests/Graphics/Dx11DetectionTests.cpp, UtinniCore.Tests/Graphics/RenderBackendSeamTests.cpp, UtinniCore.Tests/UtinniCore.Tests.vcxproj</files>
  <read_first>
    - UtinniCore.Tests/Graphics/RenderBackendSeamTests.cpp (the MockBackend + TEST_CASE template 41-103)
    - UtinniCore.Tests/UtinniCore.Tests.vcxproj (the <ClCompile> registration block + the Option-A split comment ~141-146 explaining why render_backend_dx9.cpp is NOT compiled into the test exe)
    - .planning/phases/19-dx11backend-config-detection-resize/19-PATTERNS.md (the three NEW test sections: Dx11VtblOffsetTests, Dx11DetectionTests, EXTENDED RenderBackendSeamTests)
    - .planning/phases/19-dx11backend-config-detection-resize/19-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map + Wave 0 Gaps)
  </read_first>
  <behavior>
    - Dx11VtblOffsetTests: a Catch2 case tagged "[dxgi][offsets]" REQUIREs Present index == 8 and ResizeBuffers index == 13 (static-assert or REQUIRE against named constants the Plan 02 hook tier will share — declare the enum locally in the test if directx11.h does not exist yet, with a comment that Plan 02 must keep dxgi_Present_Index/dxgi_ResizeBuffers_Index in sync)
    - Dx11DetectionTests: cases tagged "[rndr03][detect]" — given (gl11Present=true) -> select D3D11; (gl11Present=false) -> select D3D9 default; (gl11Present=true, getHookPointsAbsent=true) -> select D3D9 + log fallback; (both/ambiguous) -> D3D9 + log. The detection decision must be a PURE function taking module-presence/export-presence booleans (no real DLLs), so the test injects states directly.
    - RenderBackendSeamTests: a sibling Dx11-flavored case tagged "[rndr02][graphics]" asserts the same 10-vtable contract routes through the installed backend via the device-free MockBackend ABC (NOT a concrete Dx11Backend instantiation — that would drag in imgui_impl_dx11/DetourXS/directx11 -> LNK2019, per the Option-A split)
  </behavior>
  <action>
    Define the detection decision as a pure function so Plan 03 can call it from imgui_impl::setup(). Put
    the function in a small header the test and Plan 03 share — propose `UtinniCore/swg/graphics/backend_select.h`
    declaring an inline `enum class BackendChoice { Dx9, Dx11 }` and an inline pure function
    `BackendChoice selectBackend(bool gl11Present, bool getHookPointsResolved)` returning Dx11 ONLY when both
    are true, else Dx9 (D-15/D-17). This header names NO DX11 types so imgui_impl.cpp may include it without
    tripping the neutrality gate. Write Dx11DetectionTests.cpp to exercise selectBackend across the four
    module-presence states. Write Dx11VtblOffsetTests.cpp to pin Present=8/ResizeBuffers=13 (Claude's
    discretion whether to reference a directx11.h enum or declare the named constants locally; if local, add
    a comment binding them to the Plan-02 hook-tier constants). Add a Dx11 sibling TEST_CASE in
    RenderBackendSeamTests.cpp tagged "[rndr02][graphics]" reusing the device-free MockBackend (do NOT
    instantiate Dx11Backend). Register Dx11VtblOffsetTests.cpp and Dx11DetectionTests.cpp as <ClCompile> in
    UtinniCore.Tests.vcxproj beside the existing graphics test entries. Do NOT register render_backend_dx11.cpp
    or directx11.cpp in the test vcxproj (Option-A split — same exclusion rationale as render_backend_dx9.cpp).
    The offset/detection test .cpp files include <d3d11.h>/<dxgi1_2.h> only if needed; those are OS SDK headers
    on the default include path (no extra include dir needed — confirm build link).
  </action>
  <verify>
    <automated>cd UtinniCore.Tests && ls Graphics/Dx11VtblOffsetTests.cpp Graphics/Dx11DetectionTests.cpp</automated>
  </verify>
  <acceptance_criteria>
    - `UtinniCore.Tests.exe "[dxgi][offsets]"` exits 0 and asserts Present==8, ResizeBuffers==13
    - `UtinniCore.Tests.exe "[rndr03][detect]"` exits 0 across all four detection states
    - `UtinniCore.Tests.exe "[rndr02][graphics]"` exits 0 (Dx11 mock-dispatch via the ABC)
    - `backend_select.h` declares `selectBackend(bool, bool)` and names zero DX11 types (grep for "ID3D11" returns 0)
    - UtinniCore.Tests.vcxproj registers Dx11VtblOffsetTests.cpp + Dx11DetectionTests.cpp and does NOT register render_backend_dx11.cpp/directx11.cpp
  </acceptance_criteria>
  <done>Three device-free Catch2 layers (offset pin, detection logic, Dx11 mock-dispatch) green; selectBackend pure function lives in a neutral header.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new this plan) | This plan adds only build config, a header decl, and offline Catch2 tests. No runtime hook surface, no new input/IPC/network. The advertised-pointer trust boundary is introduced in Plan 02. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | DXGI vtbl ABI drift silently hooking the wrong method | mitigate | Dx11VtblOffsetTests pins Present=8/ResizeBuffers=13; a DXGI ABI change fails the build (D-21 L1). |
| T-19-02 | Tampering | DX11/DXGI symbol leaking into the API-neutral imgui_impl (D-05/D-06 regression) | mitigate | ImguiApiNeutralityTests banned-token set extended to DX11/DXGI concrete forms; build fails on a leak. |
| T-19-SC | Tampering | vcpkg package install (dx11-binding feature) | accept | Feature toggle of an already-pinned, already-vendored imgui port (RESEARCH Package Legitimacy Audit — no new registry package, no [ASSUMED]/[SUS]). No legitimacy checkpoint required. |
</threat_model>

<verification>
- vcpkg.json carries dx11-binding; imgui_impl_dx11.{h,cpp} resolve under vcpkg_installed/x86-windows.
- render_backend.h declares Dx11Backend + dx11Singleton with zero DX11 includes (forward-decls only).
- Extended neutrality gate green; offset pin, detection-logic, and Dx11 mock-dispatch tests green.
- `git checkout -- UtinniCoreDotNet/Generated/UtinniCore.cs` after any build (never commit the churn).
</verification>

<success_criteria>
- `grep -c '"dx11-binding"' vcpkg.json` == 1 and imgui_impl_dx11.h present.
- `class Dx11Backend final` + `dx11Singleton()` declared in render_backend.h; D-05 forward-decls only.
- `UtinniCore.Tests.exe "[dxgi][offsets],[rndr03][detect],[rndr02][graphics]"` all green.
- Neutrality gate `[rndr01][graphics]` green with the extended DX11/DXGI token set.
</success_criteria>

<output>
Create `.planning/phases/19-dx11backend-config-detection-resize/19-01-SUMMARY.md` when done.
</output>
