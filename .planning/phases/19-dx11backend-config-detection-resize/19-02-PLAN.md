---
phase: 19-dx11backend-config-detection-resize
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - UtinniCore/swg/graphics/directx11.h
  - UtinniCore/swg/graphics/directx11.cpp
  - UtinniCore/swg/graphics/render_backend_dx11.cpp
  - UtinniCore/UtinniCore.vcxproj
  - UtinniCoreDotNetGen/HeaderDiscovery.cs
  - UtinniCore.Tests/Graphics/Dx11DummyDeviceHarvestTests.cpp
  - UtinniCore.Tests/UtinniCore.Tests.vcxproj
autonomous: true
requirements: [RNDR-02, RNDR-04]
must_haves:
  truths:
    - "A directX11:: DXGI hook tier exists (hkSwapChainPresent idx 8, hkResizeBuffers idx 13, GetHookPoints consumer poll, DetourXS install)"
    - "Dx11Backend implements all 10 vtable overrides + the DX11 init(); newFrame rebinds the backbuffer RTV per-frame (flip-discard); onPreResize/onPostResize release/recreate the RTV (NOT no-ops)"
    - "The advertised swapChain/device/context are borrowed and never Released; only Utinni-created RTVs are released"
    - "directx11.cpp + render_backend_dx11.cpp are the ONLY new TUs that include <d3d11.h>/<dxgi1_2.h>/<imgui_impl_dx11.h>"
    - "The offline WARP dummy-device harvest test validates vtbl idx 8/13 + asserts no device leak (skip-with-log if WARP unavailable)"
    - "directx11.h CppSharp parse-stage handling is resolved (discovered if it parses clean, excluded beside render_backend.h if it faults)"
  artifacts:
    - path: "UtinniCore/swg/graphics/directx11.cpp"
      provides: "DXGI Present/ResizeBuffers hooks + GetHookPoints consumer + detour install"
      min_lines: 80
    - path: "UtinniCore/swg/graphics/directx11.h"
      provides: "directX11:: free-function decls (tryInstall/cleanup/getSwapChain/getDevice/getContext)"
      contains: "namespace directX11"
    - path: "UtinniCore/swg/graphics/render_backend_dx11.cpp"
      provides: "Dx11Backend impl + dx11Singleton()"
      contains: "Dx11Backend::newFrame"
    - path: "UtinniCore.Tests/Graphics/Dx11DummyDeviceHarvestTests.cpp"
      provides: "WARP harvest + no-leak invariant test"
  key_links:
    - from: "directx11.cpp hkResizeBuffers"
      to: "render_backend::get()->onPreResize/onPostResize"
      via: "seam dispatch around the original ResizeBuffers"
      pattern: "onPreResize"
    - from: "render_backend_dx11.cpp Dx11Backend::newFrame"
      to: "ImGui_ImplDX11_NewFrame + OMSetRenderTargets"
      via: "per-frame backbuffer RTV rebind (flip-discard)"
      pattern: "OMSetRenderTargets"
    - from: "directx11.cpp tryInstall"
      to: "gl11_r.dll GetHookPoints export"
      via: "GetProcAddress + poll swapChain != null"
      pattern: "GetHookPoints"
---

<objective>
Build the DX11 backend twin behind the Phase 18 seam: a `directx11.{cpp,h}` DXGI hook tier (mirrors
`directx9.cpp`) and `render_backend_dx11.cpp` (the `Dx11Backend` impl, the DX11-bearing half of the seam),
plus the offline WARP harvest test and the CppSharp parse-stage validation for `directx11.h`. Hook
acquisition is via the client's advertised `GetHookPoints()` export (D-09/D-10), NOT a blind throwaway
harvest; resize is the true DXGI `ResizeBuffers` RTV release/recreate (D-18), NOT the D3D9 never-Reset rule.

Purpose: This is the consumer side of the locked advertisement contract (19-INSTRUMENTATION-SPEC.md) and
the RNDR-02/RNDR-04 implementation. Plan 03 wires detection to select this backend at install.
Output: directx11.{cpp,h}, render_backend_dx11.cpp, WARP harvest test, build registration, HeaderDiscovery decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-dx11backend-config-detection-resize/19-CONTEXT.md
@.planning/phases/19-dx11backend-config-detection-resize/19-RESEARCH.md
@.planning/phases/19-dx11backend-config-detection-resize/19-PATTERNS.md
@.planning/phases/19-dx11backend-config-detection-resize/19-INSTRUMENTATION-SPEC.md

<interfaces>
<!-- The advertised contract POD (MUST be byte-identical to the producer — Pitfall 6).
     From 19-INSTRUMENTATION-SPEC.md §3.1 / producer Direct3d11.cpp:872-877. -->
struct UtinniDx11HookPoints { IDXGISwapChain1* swapChain; ID3D11Device* device; ID3D11DeviceContext* context; };
extern "C" __cdecl UtinniDx11HookPoints GetHookPoints();   // exported by gl11_r.dll (already shipped @ 056632a)

<!-- DXGI vtbl indices (pinned by Plan 01's Dx11VtblOffsetTests). base IDXGISwapChain::Present (NOT Present1). -->
Present index = 8, ResizeBuffers index = 13.
using pSwapChainPresent = HRESULT(__stdcall*)(IDXGISwapChain*, UINT SyncInterval, UINT Flags);
using pResizeBuffers    = HRESULT(__stdcall*)(IDXGISwapChain*, UINT BufferCount, UINT Width, UINT Height, DXGI_FORMAT, UINT Flags);

<!-- The seam decl from Plan 01 (render_backend.h): Dx11Backend final : public IRenderBackend with the 10
     overrides + HWND init(ID3D11Device*, ID3D11DeviceContext*) + dx11Singleton(). -->

<!-- Producer anchors (swg-client-v2 @ 056632a, read-only reference corpus):
     Direct3d11_Device.cpp:570 FLIP_DISCARD swapchain; :922-923 per-frame RTV bind;
     :1121-1172 present->Present(1,0); :1199-1216 displayModeChanged->ResizeBuffers (release RTV before). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: directx11.{cpp,h} — DXGI hook tier (Present/ResizeBuffers + GetHookPoints consumer + detour install)</name>
  <files>UtinniCore/swg/graphics/directx11.h, UtinniCore/swg/graphics/directx11.cpp, UtinniCore/UtinniCore.vcxproj</files>
  <read_first>
    - UtinniCore/swg/graphics/directx9.cpp (the entire directX:: namespace: hkPresent 266-388 first-fire diag + render-before-original + isReady install gate 378-386; getVtbl harvest 460-551 for the GetModuleHandleA/GetProcAddress shape; detour 553-641 for Detour::CheckPointer/Detour::Create(DETOUR_TYPE_PUSH_RET); cleanup 643-660 WR-03 do-not-free-on-detach)
    - UtinniCore/swg/graphics/directx9.h (the directX:: namespace shape + UTINNI_API markers — directX11:: needs NONE exported)
    - external/DetourXS/detourxs.h (Detour::Create / Detour::CheckPointer / DETOUR_TYPE_PUSH_RET / DETOUR_LEN_AUTO)
    - .planning/phases/19-dx11backend-config-detection-resize/19-PATTERNS.md (NEW directx11.cpp + NEW directx11.h sections — includes pattern, trampoline typedefs, vtbl enum, hkSwapChainPresent body, hkResizeBuffers body, tryInstall consumer, detour install, cleanup)
    - .planning/phases/19-dx11backend-config-detection-resize/19-INSTRUMENTATION-SPEC.md (§3 contract, §4 borrowed-pointer guarantees, §7 graceful-bail-on-absent-export)
    - .planning/phases/19-dx11backend-config-detection-resize/19-RESEARCH.md (Pattern 1, Pattern 2, Pattern 3, Pitfall 2, Pitfall 6, Anti-Patterns)
    - UtinniCore/UtinniCore.vcxproj (the directx9.cpp <ClCompile> + directx9.h <ClInclude> registration ~210-292)
  </read_first>
  <behavior>
    - hkSwapChainPresent(IDXGISwapChain*, UINT, UINT): one-shot first-fire info log; calls imgui_impl::render() BEFORE origPresent; NO blockPresentCall branch, NO depth-texture reach-in (D3D9-only); returns origPresent(...)
    - hkResizeBuffers(IDXGISwapChain*, UINT, UINT, UINT, DXGI_FORMAT, UINT): null-guard render_backend::get(); call onPreResize() (release Utinni RTV) BEFORE origResizeBuffers; call original; call onPostResize() (recreate RTV); return the original's HRESULT — no DXGI_ERROR_INVALID_CALL
    - tryInstall(): GetModuleHandleA("gl11_r.dll") then "gl11_d.dll"; if absent return false (leave Dx9 default); GetProcAddress("GetHookPoints"); if absent log warning + return false (graceful bail, spec §7.2); call it; if swapChain==null return false (poll again next frame); else run the LOCKED install sequence below, latch installed, return true. Idempotent — a second call after the latch is a no-op (the latch fronts the whole sequence so no double-init / double-detour / second ImGui context — Pitfall 3).
    - The borrowed swapChain/device/context are stored for getSwapChain/getDevice/getContext accessors and NEVER Released
    - LOCKED install sequence inside tryInstall (the seam the whole phase converges on — do NOT leave to discretion):
        (1) stash the borrowed {swapChain, device, context} for the getSwapChain/getDevice/getContext accessors;
        (2) read vtbl idx 8/13 off the LIVE swapchain, Detour::CheckPointer + Detour::Create(DETOUR_TYPE_PUSH_RET) both -> origPresent/origResizeBuffers;
        (3) render_backend::set(render_backend::dx11Singleton()) — install the DX11 backend BEFORE init (mirrors imgui_impl.cpp:283 "set before init");
        (4) const HWND hwnd = render_backend::dx11Singleton()->init(device, context); — the DX11 device-bearing init (ImGui_ImplDX11_Init inside, HWND from swapChain->GetHwnd()); if hwnd==null, undo with render_backend::set(nullptr) + return false (no overlay rather than half-install);
        (5) imgui_impl::setup(hwnd) — drives the HWND-bearing tail (CreateContext-if-needed, setSwgHwnd, ImGui_ImplWin32_Init, WndProc subclass, style/font, isSetup=true). Plan 03 Task 1 makes setup() SKIP its own step-2 dx9Singleton()->init(nullptr) when the installed backend is NOT the dx9 singleton (the DX11 device init already happened here in step 4).
      This mirrors the DX9 model exactly (directx9.cpp::hkPresent stashes the device, set()s, then drives setup(hwnd) from the hook tier) — the DX11 twin just gets device+context+HWND from the advertised contract instead of GetCreationParameters.
    - PER-FRAME POLL KICK-OFF (ownership pinned to THIS task): tryInstall must be INVOKED every frame until it latches. directx11.cpp owns the recurring invocation site. Choose the kick-off shape that mirrors the DX9 model: register a minimal DXGI Present-equivalent detour (or a directX11::kickoff() registered from graphics.cpp::hkInstall, mirroring how directX::detour() is invoked there) that, while not-yet-latched, calls tryInstall() once per frame; once latched, hkSwapChainPresent is the live per-frame hook and the kick-off is a no-op. The acceptance criterion below requires the recurring invocation actually exists — NOT merely that tryInstall() is defined.
  </behavior>
  <action>
    Create directx11.h: #pragma once, #include "utinni.h", #include <dxgi1_2.h>, a `namespace directX11`
    declaring `bool tryInstall();`, `void cleanup();`, `IDXGISwapChain1* getSwapChain();`,
    `ID3D11Device* getDevice();`, `ID3D11DeviceContext* getContext();`, and the two named vtbl-index
    constants `dxgi_Present_Index = 8` / `dxgi_ResizeBuffers_Index = 13` (so Plan 01's offset test can pin
    them — keep them in sync). Mark NOTHING UTINNI_API (the hook tier needs no managed binding, unlike
    directX::getDevice). Create directx11.cpp: include directx11.h, <d3d11.h>, <dxgi1_2.h>,
    <imgui_impl_dx11.h>, utinni.h, swg/ui/imgui_impl.h, render_backend.h, external/DetourXS/detourxs.h.
    Define the trampoline typedefs (pSwapChainPresent/pResizeBuffers, __stdcall, base IDXGISwapChain — NOT
    Present1, per spec §4.4), the UtinniDx11HookPoints POD copied VERBATIM from the spec §3.1 (field
    order/type IDXGISwapChain1*/ID3D11Device*/ID3D11DeviceContext* — Pitfall 6 ABI match), the
    pGetHookPoints __cdecl typedef, hkSwapChainPresent, hkResizeBuffers, tryInstall (the advertised-contract
    consumer that REPLACES the throwaway harvest in production), the detour install reading the vtable off
    the live swapchain (`*(swgptr**)hp.swapChain`), and cleanup (mirror directx9.cpp::cleanup — do NOT free
    borrowed device-bound state on DLL_PROCESS_DETACH, WR-03; never Release the borrowed swapChain/device/
    context). Prefer DETOUR_LEN_AUTO (feedback_detourxs_explicit_len). Implement tryInstall as the LOCKED
    install sequence in the behavior block above — concretely: stash borrowed pointers -> Detour::Create both
    -> render_backend::set(dx11Singleton()) -> hwnd = dx11Singleton()->init(device, context) (bail + set(nullptr)
    if null) -> imgui_impl::setup(hwnd) -> latch. The ImGui_ImplDX11_Init call lives in render_backend_dx11.cpp
    (Task 2, inside Dx11Backend::init) so the D-05 purge keeps <imgui_impl_dx11.h>'s ImGui_ImplDX11_* calls in
    the seam twin; directx11.cpp drives the seam (set + init + setup) but does NOT itself call ImGui_ImplDX11_*.
    The ordering is FIXED (not discretion) so there is no double-init / overlay-never-initializes ambiguity
    (checker Blocker 1 / Warning 2). ALSO own the per-frame poll kick-off: wire a recurring invocation of
    tryInstall() (per the behavior block — a minimal Present-equivalent or a directX11::kickoff() registered
    from graphics.cpp::hkInstall mirroring directX::detour()) so the poll actually runs each frame until
    latched (checker Warning 4). Register directx11.cpp as <ClCompile> and directx11.h as <ClInclude> in
    UtinniCore.vcxproj beside the directx9 entries. Build INLINE (x86 Release). git checkout --
    Generated/UtinniCore.cs after.
  </action>
  <verify>
    <automated>cd UtinniCore.Tests && ls ../UtinniCore/swg/graphics/directx11.cpp ../UtinniCore/swg/graphics/directx11.h</automated>
  </verify>
  <acceptance_criteria>
    - directx11.h declares `namespace directX11` with tryInstall/cleanup/getSwapChain/getDevice/getContext and `dxgi_Present_Index = 8` / `dxgi_ResizeBuffers_Index = 13`
    - directx11.cpp's UtinniDx11HookPoints struct field order is exactly {IDXGISwapChain1* swapChain; ID3D11Device* device; ID3D11DeviceContext* context;}
    - `grep -c "Present1" UtinniCore/swg/graphics/directx11.cpp` returns 0 (base Present only)
    - `grep -c "->Release()" UtinniCore/swg/graphics/directx11.cpp` shows Release only on Utinni-created RTVs, never on swapChain/device/context (manual code-review acceptance: no `swapChain->Release`/`device->Release`/`context->Release`)
    - hkResizeBuffers calls onPreResize() before origResizeBuffers and onPostResize() after (grep both present, ordered)
    - tryInstall calls GetProcAddress(..., "GetHookPoints") and returns false (graceful) when the export is absent
    - tryInstall's install sequence is the locked order: stash -> Detour::Create -> render_backend::set(dx11Singleton()) -> dx11Singleton()->init(device, context) -> imgui_impl::setup(hwnd) -> latch (set() precedes init() precedes setup(); the latch guards re-entry — a second tryInstall after latch is a no-op)
    - directx11.cpp does NOT call ImGui_ImplDX11_* directly (`grep -c "ImGui_ImplDX11" UtinniCore/swg/graphics/directx11.cpp` returns 0 — those calls live in render_backend_dx11.cpp per the D-05 purge)
    - A recurring per-frame invocation of tryInstall() exists (a kick-off detour or a directX11::kickoff() call site) — grep shows tryInstall() is CALLED from a per-frame/hook-tier site, not only defined; the poll runs until latched
    - UtinniCore.vcxproj registers directx11.cpp (<ClCompile>) + directx11.h (<ClInclude>)
    - UtinniCore.dll builds green (x86 Release)
  </acceptance_criteria>
  <done>DXGI hook tier links into UtinniCore.dll; advertised-contract consumer + Present/ResizeBuffers hooks + DetourXS install present; borrowed pointers never Released.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: render_backend_dx11.cpp — the Dx11Backend seam twin (per-frame RTV rebind + resize RTV recreate)</name>
  <files>UtinniCore/swg/graphics/render_backend_dx11.cpp, UtinniCore/UtinniCore.vcxproj</files>
  <read_first>
    - UtinniCore/swg/graphics/render_backend_dx9.cpp (the entire file — the structural template: per-frame forwards 44-52, no-op resize hooks 54-58, RT-dims forwards 61-69, scene depth/color/stage 73-105, stashDevice 110-118, non-virtual init 122-147, static singleton + dx9Singleton 149-155)
    - UtinniCore/swg/graphics/render_backend.h (the Dx11Backend decl from Plan 01 — the 10 overrides + init(ID3D11Device*, ID3D11DeviceContext*) signature + dx11Singleton())
    - UtinniCore/swg/graphics/directx11.h (getSwapChain/getDevice/getContext the twin reads)
    - .planning/phases/19-dx11backend-config-detection-resize/19-PATTERNS.md (NEW render_backend_dx11.cpp section — newFrame RTV rebind, onPreResize/onPostResize real bodies, RT-dims verbatim, scene accessors return 0, non-virtual init, singleton, Pitfall 4)
    - .planning/phases/19-dx11backend-config-detection-resize/19-RESEARCH.md (Pattern 4 flip-discard rebind; "ImGui DX11 backend call sequence" code example; Architectural Responsibility Map; Pitfall 4)
  </read_first>
  <behavior>
    - newFrame(): if m_rtv is null, create the backbuffer RTV (getSwapChain()->GetBuffer(0) + device->CreateRenderTargetView); OMSetRenderTargets(1, &m_rtv, nullptr) to rebind (flip-discard unbinds after each Present); then ImGui_ImplDX11_NewFrame(). No heap allocation per frame — the RTV is cached, recreated only on resize.
    - renderDrawData(dd): ImGui_ImplDX11_RenderDrawData(dd)
    - onPreResize(): release m_rtv (m_rtv->Release(); m_rtv = nullptr) — release BEFORE the original ResizeBuffers so DXGI does not return DXGI_ERROR_INVALID_CALL
    - onPostResize(): recreate m_rtv from the resized backbuffer (same createBackbufferRtv path)
    - renderTargetWidth/Height(): forward to utinni::Graphics::getCurrentRenderTargetWidth/Height (verbatim from Dx9, API-neutral, D-19)
    - sceneDepthTexture/sceneColorTexture(): return (ImTextureID)0; sceneDepthStage(): return 0; setSceneDepthStage(int): no-op (no DX11 depth SRV this phase — MVP)
    - init(ID3D11Device*, ID3D11DeviceContext*): ImGui_ImplDX11_Init(device, context); derive HWND via getSwapChain()->GetHwnd() (or GetDesc1().OutputWindow); return that HWND
    - dx11Singleton(): address of a static-storage Dx11Backend (no heap)
  </behavior>
  <action>
    Create render_backend_dx11.cpp mirroring render_backend_dx9.cpp's structure. Include render_backend.h,
    <d3d11.h>, <dxgi1_2.h>, <imgui_impl_dx11.h>, directx11.h, graphics.h, <cassert>. This is the ONLY new
    TU (with directx11.cpp) allowed <imgui_impl_dx11.h>. Implement all 10 vtable overrides per the behavior
    block. KEY DIFFERENCES from Dx9: newFrame rebinds the cached backbuffer RTV before ImGui_ImplDX11_NewFrame
    (flip-discard, RESEARCH Pattern 4 + producer Direct3d11_Device.cpp:922-923); onPreResize/onPostResize are
    NOT no-ops — they release/recreate the RTV (D-18, RESEARCH Pattern 2); init takes device+context. Add a
    private helper `createBackbufferRtv()` that does getSwapChain()->GetBuffer(0, IID, &backbuffer) +
    getDevice()->CreateRenderTargetView(backbuffer, nullptr, &m_rtv) + backbuffer->Release() (the backbuffer
    temp IS released — it is Utinni-acquired; the swapChain/device are borrowed and NOT released). Keep the
    static-storage singleton + dx11Singleton() exactly like dx9Singleton(). Register render_backend_dx11.cpp
    as <ClCompile> in UtinniCore.vcxproj. CRITICAL (Pitfall 4): this file uses m_rtv->Release() / possibly
    ComPtr::Reset() — do NOT add render_backend_dx11.cpp or directx11.cpp to NoDeviceResetTests.cpp's guarded
    file list (that gate counts .Reset(/->Reset( and would false-trip on RTV management; the guarded list
    stays directx9.cpp/direct_input.cpp/PanelGame.cs only). Build INLINE (x86 Release). git checkout --
    Generated/UtinniCore.cs after.
  </action>
  <verify>
    <automated>cd UtinniCore.Tests && grep -c "OMSetRenderTargets" ../UtinniCore/swg/graphics/render_backend_dx11.cpp</automated>
  </verify>
  <acceptance_criteria>
    - render_backend_dx11.cpp defines all 10 Dx11Backend overrides + the non-virtual init(ID3D11Device*, ID3D11DeviceContext*) + dx11Singleton()
    - newFrame() calls OMSetRenderTargets and ImGui_ImplDX11_NewFrame; the RTV is cached (created only when m_rtv is null)
    - onPreResize() releases m_rtv; onPostResize() recreates it (grep both present)
    - sceneDepthTexture/sceneColorTexture return (ImTextureID)0; sceneDepthStage returns 0
    - render_backend_dx11.cpp is NOT in NoDeviceResetTests.cpp's guarded file list (grep that list for render_backend_dx11/directx11 returns 0)
    - UtinniCore.dll builds green (x86 Release) with render_backend_dx11.cpp registered
  </acceptance_criteria>
  <done>Dx11Backend twin links; per-frame RTV rebind + resize release/recreate implemented; not added to the NoDeviceReset guarded list.</done>
</task>

<task type="auto">
  <name>Task 3: WARP dummy-device harvest test + directx11.h CppSharp parse-stage validation</name>
  <files>UtinniCore.Tests/Graphics/Dx11DummyDeviceHarvestTests.cpp, UtinniCore.Tests/UtinniCore.Tests.vcxproj, UtinniCoreDotNetGen/HeaderDiscovery.cs</files>
  <read_first>
    - UtinniCore/swg/graphics/directx9.cpp (getVtbl harvest 460-551 — the throwaway-device + 1x1 hidden window + snapshot vtable + Release + no-leak structure this test ports to DXGI/WARP)
    - UtinniCore.Tests/Graphics/RenderBackendSeamTests.cpp (Catch2 file header + TEST_CASE skeleton)
    - UtinniCore.Tests/UtinniCore.Tests.vcxproj (the <ClCompile> graphics-test registration block + the AdditionalIncludeDirectories ~73)
    - UtinniCoreDotNetGen/HeaderDiscovery.cs (the render_backend.h discovery-exclusion 86-100 — the pattern to mirror for directx11.h IF it faults the clang-11 parser)
    - .planning/phases/19-dx11backend-config-detection-resize/19-PATTERNS.md (NEW Dx11DummyDeviceHarvestTests section + Shared Pattern: CppSharp parse-stage exclusion)
    - .planning/phases/19-dx11backend-config-detection-resize/19-RESEARCH.md (D-21 layer 3; Assumption A1 WARP availability/skip-with-log; Assumption A2)
  </read_first>
  <action>
    Create Dx11DummyDeviceHarvestTests.cpp tagged "[dxgi][harvest]" mirroring getVtbl's structure ported to
    DXGI/WARP: D3D11CreateDeviceAndSwapChain with D3D_DRIVER_TYPE_WARP and a hidden 1x1 window (Claude's
    discretion: message-only vs 1x1 hidden — match getVtbl's 1x1 WS_POPUP STATIC window), snapshot the
    swapchain vtable, REQUIRE idx 8 (Present) and idx 13 (ResizeBuffers) resolve to non-null addresses, then
    Release the device/swapchain/context and assert no leak (track the device refcount via a final Release
    returning 0, or assert the create-then-release pair completes without comptr leak). CRITICAL (A1):
    make WARP device-create FAILURE a Catch2 SKIP (not a FAIL) with a log line — the self-hosted CI runner
    may lack a usable WARP path for a 32-bit test process; the offset invariants are interface-defined so a
    skip on a GPU-less runner does not weaken the build. Register Dx11DummyDeviceHarvestTests.cpp as
    <ClCompile> in UtinniCore.Tests.vcxproj (it includes <d3d11.h>/<dxgi.h> from the OS SDK on the default
    include path — confirm it links; add d3d11.lib/dxgi.lib to the test project's link line if
    D3D11CreateDeviceAndSwapChain is unresolved, since the test CALLS the API rather than patching it).
    Then run the CppSharp parse-stage validation: build/run UtinniCoreDotNetGen.exe (a full Build, not an
    incremental skip — the post-build gen must actually run) and check whether the clang-11 parser faults
    (AccessViolation) on directx11.h's <dxgi1_2.h> include. directx9.h includes <d3d9.h> and is NOT excluded
    (it parses + projects UTINNI_API exports); directx11.h exports NO UTINNI_API symbols. If the parser
    faults on directx11.h, add a directx11.h discovery-exclusion in HeaderDiscovery.cs beside the
    render_backend.h one (96-100); if it parses clean, leave it discovered (mirroring directx9.h) and make NO
    HeaderDiscovery change. ALWAYS git checkout -- UtinniCoreDotNet/Generated/UtinniCore.cs after running the
    gen (never commit the churn — project_utinnicore_cs_regen_churn). Build INLINE (x86 Release).
  </action>
  <verify>
    <automated>cd UtinniCore.Tests && ls Graphics/Dx11DummyDeviceHarvestTests.cpp</automated>
  </verify>
  <acceptance_criteria>
    - `UtinniCore.Tests.exe "[dxgi][harvest]"` exits 0 (either asserts idx 8/13 resolve + no leak, OR cleanly SKIPs with a log when WARP is unavailable)
    - Dx11DummyDeviceHarvestTests.cpp is registered <ClCompile> in UtinniCore.Tests.vcxproj
    - UtinniCoreDotNetGen.exe runs to completion (full Build) without an unhandled clang-11 parse fault; if it faulted on directx11.h, HeaderDiscovery.cs now excludes directx11.h beside render_backend.h
    - `git status` shows UtinniCoreDotNet/Generated/UtinniCore.cs NOT staged for commit (checked out after gen)
    - SUMMARY records the HeaderDiscovery decision (excluded vs left-discovered) with the observed parser behavior
  </acceptance_criteria>
  <done>WARP harvest/no-leak test green (or skip-with-log); directx11.h CppSharp parse-stage decision made and recorded; Generated/UtinniCore.cs not committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gl11_r.dll GetHookPoints -> Utinni | Same-process, Utinni-controlled SWG-Source client advertises live COM pointers. Trusted source (local/offline injection per DEC-A1/A4), but the export may be absent or the swapchain not-yet-ready — both must be handled without crash/UB. |
| Utinni detours -> live swapchain vtbl | Utinni patches function bodies at addresses read off the live swapchain. A bad/unmapped address must be guarded before the detour installs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Denial of Service (crash) | tryInstall reading a null/stale borrowed swapChain | mitigate | Poll `hp.swapChain != null` before install (spec §3.3); a null field returns false and re-polls next frame — no deref. |
| T-19-04 | Denial of Service (crash) | GetHookPoints absent on a non-instrumented gl11_r.dll | mitigate | GetProcAddress-null -> log warning + return false to a safe no-overlay state (spec §7.2). Must NOT crash. |
| T-19-05 | Information Disclosure / UAF | Releasing the borrowed swapChain/device/context | mitigate | Borrowed-pointer discipline (spec §4.1): never AddRef/Release the advertised trio; only Utinni-created RTVs (and the GetBuffer backbuffer temp) are Released. cleanup() does NOT free borrowed/device-bound state on DLL_PROCESS_DETACH (WR-03). |
| T-19-06 | Denial of Service (crash) | Detour faulting on a bad/unmapped harvested vtbl address | mitigate | Detour::CheckPointer guard before Detour::Create (the directx9.cpp:569+ pattern); address read off the LIVE advertised swapchain, not a hardcoded RVA. |
| T-19-07 | Tampering | DXGI_ERROR_INVALID_CALL from ResizeBuffers (outstanding backbuffer ref) | mitigate | onPreResize releases Utinni's RTV BEFORE the original ResizeBuffers runs (Pattern 2 / Pitfall 2); the producer already releases its own RTV first. |
| T-19-01 | Tampering | DXGI vtbl ABI drift | mitigate | (carried from Plan 01) Dx11VtblOffsetTests pins idx 8/13; the WARP harvest test independently confirms idx 8/13 resolve. |

**N/A for this offline local modding overlay:** Authentication (V2), Session (V3), Access Control (V4 —
all editing local/offline), Cryptography (V6). No network/IPC/untrusted-input surface is introduced; the
named pipe MCP bridge (Phase 16) is unrelated to this render hook.
</threat_model>

<verification>
- UtinniCore.dll links with directx11.{cpp} + render_backend_dx11.cpp (x86 Release, inline build).
- `[dxgi][harvest]` green or skip-with-log; `[dxgi][offsets]` (Plan 01) still pins idx 8/13.
- Borrowed swapChain/device/context never Released; only Utinni RTVs + GetBuffer temp released.
- directx11.h CppSharp parse decision made; Generated/UtinniCore.cs not committed.
</verification>

<success_criteria>
- directX11:: hook tier + Dx11Backend twin compile and link into UtinniCore.dll (x86 Release).
- hkResizeBuffers releases-before / recreates-after the original (no DXGI_ERROR_INVALID_CALL path).
- newFrame rebinds the cached backbuffer RTV per-frame (flip-discard) with no per-frame heap alloc.
- WARP harvest test green or cleanly skipped; HeaderDiscovery decision recorded.
</success_criteria>

<output>
Create `.planning/phases/19-dx11backend-config-detection-resize/19-02-SUMMARY.md` when done.
</output>
