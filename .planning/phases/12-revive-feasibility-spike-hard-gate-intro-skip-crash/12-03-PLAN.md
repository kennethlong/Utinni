---
phase: 12-revive-feasibility-spike-hard-gate-intro-skip-crash
plan: 03
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - tools/DEPENDENCY-MANIFEST.md
  - tools/smoke/
  - .github/workflows/ci.yml
autonomous: false
requirements: [AUTH-01]
user_setup: []

must_haves:
  truths:
    - "Each of the three tools runs headless against a real shipped asset and its output is byte-exact vs a known-good reference (D-09) — OR a non-byte-exact result is surfaced as an explicit gate finding and resolved with the maintainer (no structural/round-trip fallback)."
    - "A per-tool dependency manifest enumerates each tool's real #include closure + required shared libs, with dead include paths shown pruned and the keep-or-stub Perforce decision recorded per tool."
    - "The tools solution build is wired into the self-hosted v145 CI runner so the hard gate is continuously enforced from day one (D-07)."
    - "Reference-pair availability was confirmed with the maintainer BEFORE any byte-exact smoke was committed (A1 gate checkpoint)."
  artifacts:
    - path: "tools/DEPENDENCY-MANIFEST.md"
      provides: "Per-tool #include closure + shared libs + pruned-dead-deps + Perforce decision + zlib-1.1.4 pin (D-08)"
      contains: "TreeFileBuilder"
    - path: ".github/workflows/ci.yml"
      provides: "Utinni.Tools.sln build-lane step on the self-hosted v145 runner (D-07)"
      contains: "Utinni.Tools.sln"
    - path: "tools/smoke/"
      provides: "Per-tool byte-exact Get-FileHash compare scripts (D-09)"
  key_links:
    - from: ".github/workflows/ci.yml (new tools build step)"
      to: "tools/Utinni.Tools.sln"
      via: "msbuild invocation on runs-on: [self-hosted, windows, x64, utinni-v145]"
      pattern: "msbuild tools.\\Utinni.Tools.sln"
    - from: "tools/smoke/*.ps1"
      to: "TreeFileBuilder_d.exe / TemplateCompiler_d.exe / TemplateDefinitionCompiler_d.exe"
      via: "run tool then Get-FileHash compare vs known-good reference"
      pattern: "Get-FileHash"
---

<objective>
Close the AUTH-01 hard gate's verification half: confirm byte-exact reference-pair availability with the maintainer (A1 — the single biggest feasibility unknown), run the per-tool byte-exact smokes (D-09, NO structural fallback), author `tools/DEPENDENCY-MANIFEST.md` (D-08), and wire the `tools/Utinni.Tools.sln` build into the self-hosted v145 CI runner (D-07) so the gate is continuously enforced.

Purpose: A standalone build (12-01/12-02) is necessary but not sufficient — the hard gate also requires each tool to produce byte-exact output against a real shipped asset, the dependency closure to be documented, and CI to keep the gate green. Byte-exactness is the gate's TRUE risk (RESEARCH §Byte-Exact Feasibility): reference pairs are largely absent from swg-client-v2 (0 .tpf/.tdf, 1 .tre), .tre is zlib-1.1.4-locked, and TemplateDefinitionCompiler's generated C++ likely embeds a timestamp banner. Each of these is a "surface and resolve" gate finding under D-09, not a free pass.

Output: maintainer-confirmed reference pairs (or surfaced gate findings), three byte-exact smokes, DEPENDENCY-MANIFEST.md, and a green tools build lane on the self-hosted CI runner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-CONTEXT.md
@.planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-RESEARCH.md
@.planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-PATTERNS.md
@.planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-01-SUMMARY.md
@.planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-02-SUMMARY.md
@.github/workflows/ci.yml
@docs/ai/toolchain-inventory.md

<interfaces>
CI analog (.github/workflows/ci.yml — exact-match analog, same file):
- Runner targeting (ci.yml:23): `runs-on: [self-hosted, windows, x64, utinni-v145]` — reuse, no new runner registration (A6)
- `defaults.run.shell: powershell` (5.1-safe syntax only)
- Verify-v145-then-build precedent: `Verify v145 build tools` step (ci.yml:119-132) already gates the job; `microsoft/setup-msbuild@v2` (ci.yml:166); full-solution build `msbuild Utinni.sln /m ... /p:Platform=x86` (ci.yml:171)
- Targeted-build precedent: `/t:<Project>` native-test steps (ci.yml:205, :211)
- Failure-artifact precedent: `actions/upload-artifact@v4` with `if: failure()` (ci.yml:181, :194, :235)

Golden-compare analog (Utinni.Cli.Tests/Infrastructure/GoldenTestRunner.cs:40-89) — dump-expected+actual-on-mismatch escape hatch. D-09 divergences: REAL assets (not synth), BINARY hash (not normalized-text) for .tre/.iff, text-normalized ONLY for generated C++, NO structural fallback.

Tool CLI surfaces (RESEARCH §Code Examples):
- TreeFileBuilder_d.exe -r <build.rsp> <out.tre>   (sibling TreeFileRspBuilder in swg-client-v2 derives an .rsp from a file list — Open Q3)
- TemplateCompiler_d.exe -compile <input.tpf>       (→ object-template .iff; TpfFile::makeIffFiles, no time(NULL) in output)
- TemplateDefinitionCompiler_d.exe -compile <input.tdf>  (→ generated C++ .cpp/.h; inspect the header banner for embedded __DATE__/time/absolute paths — Pitfall 6)

Byte-exact compare shape (PowerShell):
  $ref = Get-FileHash known_good.<ext> -Algorithm SHA256
  $got = Get-FileHash out.<ext>        -Algorithm SHA256
  if ($ref.Hash -ne $got.Hash) { Write-Error "BYTE-EXACT FAIL (D-09 gate finding)"; exit 1 }
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Reference-pair availability checkpoint (A1 gate — before any smoke)</name>
  <files>(no files modified — maintainer availability checkpoint; reference paths feed Task 2 smoke scripts + the manifest)</files>
  <action>Pause for the maintainer to confirm, per tool, whether a byte-exact source→known-good reference pair exists and where it comes from (real SWG client install / dsrc corpus). This MUST resolve before any byte-exact smoke is committed (Pitfall 4). See the structured decision below.</action>
  <verify>
    <human-check>Per tool (TreeFileBuilder / TemplateCompiler / TemplateDefinitionCompiler): a reference-pair file path is provided, OR a "gate-finding" disposition is recorded. For .tre, the retail_mini_0005 source-tree/.rsp situation is confirmed (Open Q3). For TemplateDefinitionCompiler, the generated-banner normalization disposition is confirmed (Pitfall 6).</human-check>
  </verify>
  <done>Per-tool reference-pair availability (path or gate-finding) is recorded for all three tools, unblocking Task 2.</done>
  <decision>Per tool: does a byte-exact source→known-good reference pair exist, and where does it come from?</decision>
  <context>
    RESEARCH §Byte-Exact Feasibility + Assumption A1 (HIGH risk): swg-client-v2 has ~no reference pairs (0 .tpf, 0 .tdf, exactly 1 .tre = tools/swg_blender/tests/golden/retail_mini_0005.tre, version 0005, with NO recorded source .rsp). D-09 mandates byte-exact with NO structural/round-trip fallback, so this checkpoint MUST be resolved before any smoke is committed (Pitfall 4: missing references surface only at the smoke step). The maintainer is the only source of truth for what their real SWG client install / authoring corpus (dsrc) contains.
  </context>
  <options>
    <option id="provide-pairs">
      <name>Maintainer provides a real source→known-good pair per tool (from client install / dsrc corpus)</name>
      <pros>Enables a true byte-exact smoke per tool; passes the gate as designed (D-09).</pros>
      <cons>Requires the maintainer to locate a known .tpf+.iff, a source-tree+.tre (with .rsp or derivable via TreeFileRspBuilder), and a .tdf+generated-C++ set.</cons>
    </option>
    <option id="gate-finding">
      <name>No pair exists for a given tool → record an explicit D-09 gate finding for that tool</name>
      <pros>Honest disposition per D-09 ("a gate finding to surface and resolve, not a free pass"); unblocks the other tools.</pros>
      <cons>That tool's byte-exact criterion is met by a documented finding + maintainer-agreed resolution path, not a green smoke.</cons>
    </option>
  </options>
  <resume-signal>Per tool (TreeFileBuilder / TemplateCompiler / TemplateDefinitionCompiler): provide the reference-pair file paths, OR confirm "gate-finding" for that tool. For .tre, also confirm whether the retail_mini_0005 source tree exists or an .rsp must be derived (Open Q3). For TemplateDefinitionCompiler, confirm the generated-banner normalization disposition (Pitfall 6).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Per-tool byte-exact smoke scripts (real assets, NO structural fallback) + manifest</name>
  <files>tools/smoke/, tools/DEPENDENCY-MANIFEST.md</files>
  <read_first>
    - Utinni.Cli.Tests/Infrastructure/GoldenTestRunner.cs (the dump-on-mismatch escape-hatch pattern to mirror, lines 40-89)
    - .planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-RESEARCH.md (§Byte-Exact Feasibility — the three per-tool determinism verdicts; §Standard Stack — the 12/26/28 ProjectReference tables to copy into the manifest)
    - .planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-02-SUMMARY.md (the per-tool Perforce keep-or-stub + final toolset decisions to record in the manifest)
    - docs/ai/toolchain-inventory.md (the table-driven census + [VERIFIED: …] markdown convention for DEPENDENCY-MANIFEST.md)
  </read_first>
  <action>
    For each tool whose reference pair was confirmed in Task 1, write a PowerShell smoke script under tools/smoke/ that runs the lifted *_d.exe against the maintainer-provided real source asset and byte-exact-compares the output to the known-good reference: binary SHA256 Get-FileHash for TreeFileBuilder (.tre, link the vendored zlib 1.1.4 — Pitfall 3) and TemplateCompiler (.iff); text compare for TemplateDefinitionCompiler's generated C++ with ONLY the maintainer-approved banner normalization (Pitfall 6) — no broader normalization. Mirror the GoldenTestRunner dump-expected+actual-on-mismatch escape hatch so a failure leaves both byte streams on disk for triage. NO structural/round-trip fallback — a mismatch exits non-zero and is surfaced as a D-09 gate finding. For any tool marked "gate-finding" in Task 1, write the finding into DEPENDENCY-MANIFEST.md instead of a passing smoke. Author tools/DEPENDENCY-MANIFEST.md (D-08): per-tool #include closure + required shared libs (copy the 12/26/28 ProjectReference tables from RESEARCH §Standard Stack), dead include paths shown as pruned (the sharedTemplate perforce/include DIR), the per-tool Perforce keep-or-stub decision from 12-02, and the zlib 1.1.4 pin with the byte-exact-vs-CVE tension note (T-12-02).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (!(Test-Path 'tools/DEPENDENCY-MANIFEST.md')) { exit 1 }; $m = Get-Content 'tools/DEPENDENCY-MANIFEST.md' -Raw; foreach ($t in @('TreeFileBuilder','TemplateCompiler','TemplateDefinitionCompiler','zlib','1.1.4','Perforce')) { if ($m -notmatch [regex]::Escape($t)) { Write-Error \"manifest missing $t\"; exit 1 } }; $smokes = Get-ChildItem -Path tools/smoke -Filter *.ps1 -ErrorAction SilentlyContinue; if (!$smokes) { Write-Error 'no smoke scripts'; exit 1 }; foreach ($s in $smokes) { if (!(Select-String -Path $s.FullName -Pattern 'Get-FileHash|approved banner normalization' -Quiet)) { Write-Error \"smoke $($s.Name) lacks byte-exact compare\"; exit 1 } }; exit 0"</automated>
  </verify>
  <done>For each confirmed-pair tool a tools/smoke/*.ps1 byte-exact compare exists (Get-FileHash for binary, approved-normalization text for generated C++, dump-on-mismatch, no structural fallback); any gate-finding tool is documented in the manifest; DEPENDENCY-MANIFEST.md enumerates per-tool dep closures + pruned dead deps + Perforce decisions + zlib 1.1.4 pin.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the Utinni.Tools.sln build lane (+ available smokes) into the self-hosted v145 CI runner</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the EXACT analog — read lines 23 runs-on, 119-132 Verify v145 build tools, 166 setup-msbuild, 171 msbuild Utinni.sln, 181/194/235 upload-artifact if:failure, 205/211 /t: targeted builds)
    - .planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-PATTERNS.md (§ "Self-hosted v145 CI runner (build lane)" — the new-step shape)
  </read_first>
  <action>
    Add a new step to .github/workflows/ci.yml modeled on the existing v145 build steps (PATTERNS §CI): `Build tools solution (Debug|Win32) — AUTH-01 hard gate` running `msbuild tools\Utinni.Tools.sln /m /p:Configuration=Debug /p:Platform=Win32`, placed after the existing `Verify v145 build tools` step (which already gates the job — no new runner registration, A6) and reusing `runs-on: [self-hosted, windows, x64, utinni-v145]` + `shell: powershell` (5.1-safe). For every tool whose byte-exact smoke is green (per Task 1 + Task 2), add a follow-on step invoking that tools/smoke/*.ps1 script, with an `actions/upload-artifact@v4 if: failure()` step uploading the dumped expected+actual bytes for triage (mirroring ci.yml:181/194/235). The byte-exact smoke step is SEPARATE from the build step (build = AUTH-01 build gate; smoke = D-09 gate). Tools parked behind a gate-finding get a build step but no smoke step (the finding lives in the manifest). Do NOT fold tools\Utinni.Tools.sln into the existing Utinni.sln build steps — it is standalone (D-07).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ci = Get-Content '.github/workflows/ci.yml' -Raw; if ($ci -notmatch 'tools.\\Utinni\.Tools\.sln') { Write-Error 'ci.yml lacks tools solution build step'; exit 1 }; if ($ci -notmatch 'AUTH-01') { Write-Error 'ci.yml tools step not labeled as AUTH-01 gate'; exit 1 }; if ($ci -notmatch 'Platform=Win32') { Write-Error 'tools step not Win32'; exit 1 }; exit 0"</automated>
  </verify>
  <done>ci.yml has a standalone `Build tools solution (Debug|Win32) — AUTH-01 hard gate` step (Win32, self-hosted v145 runner) plus a byte-exact smoke step + failure-artifact upload for each green-smoke tool; gate-finding tools have a build step only; the existing Utinni.sln steps are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Real SWG client install / dsrc corpus → tools/smoke reference assets | Maintainer-provided known-good references; checkpoint confirms provenance before commit |
| CI runner → tools build/smoke | Same self-hosted v145 machine; runner registration is machine-local (not in git), workflow file is in git (A6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Tampering | Silent zlib upgrade to pass/break the .tre byte-exact smoke | mitigate | Manifest pins zlib 1.1.4 explicitly with the byte-exact-vs-CVE tension note; the smoke links the vendored 1.1.4 (Pitfall 3); never substitute a modern zlib |
| T-12-08 | Spoofing/Tampering | A non-byte-exact result silently treated as a pass | mitigate | D-09 forbids structural/round-trip fallback; smoke exits non-zero on mismatch + dumps both byte streams; mismatch is surfaced as a gate finding, not swallowed |
| T-12-09 | Tampering | CI workflow change weakens the gate (e.g. build step that doesn't fail the job) | mitigate | Tools build step reuses the job-gating `Verify v145` precedent + non-zero MSBuild exit fails the job; smoke uploads failure artifacts; standalone sln keeps the gate distinct from Utinni.sln |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs — reference assets are maintainer-provided files; control is provenance checkpoint + SHA pinning |
</threat_model>

<verification>
- Reference-pair availability confirmed with the maintainer per tool BEFORE any smoke committed (A1).
- Each confirmed-pair tool's byte-exact smoke is green (binary hash / approved-normalization text); each gate-finding tool is documented in DEPENDENCY-MANIFEST.md.
- DEPENDENCY-MANIFEST.md enumerates per-tool #include closures + shared libs + pruned dead deps + Perforce decisions + zlib 1.1.4 pin (D-08).
- ci.yml runs the standalone tools build lane + available smokes on the self-hosted v145 runner (D-07); Utinni.sln steps untouched.
</verification>

<success_criteria>
- ROADMAP Phase 12 success criterion #2 (per-tool dependency manifest with dead deps pruned).
- ROADMAP Phase 12 success criterion #4 (each built tool runs headless against a sample input and produces a non-empty artifact byte-exact vs known-good, OR a captured/surfaced gate finding for a tool parked behind missing references / non-determinism).
- AUTH-01 hard gate continuously enforced on the self-hosted v145 CI runner (D-07).
</success_criteria>

<output>
Create `.planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-03-SUMMARY.md` when done
</output>
