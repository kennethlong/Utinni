---
phase: 12-revive-feasibility-spike-hard-gate-intro-skip-crash
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/Utinni.Tools.sln
  - tools/PINNED-SHA.md
  - tools/src/engine/shared/application/TreeFileBuilder/
  - tools/src/engine/shared/library/
  - tools/src/external/3rd/library/zlib/
  - .gitignore
autonomous: true
requirements: [AUTH-01]
user_setup: []

must_haves:
  truths:
    - "D-01: tools/ is lifted verbatim from swg-client-v2 and never #includes or ProjectReferences back into the live swg-client-v2 tree (the violation-guard verify greps for swg-client-v2 refs)."
    - "D-02: v145 is the shared target toolset for this build; v143 remains the documented per-tool fallback (no v143 drop here — TreeFileBuilder has no expected v145 blocker; the fallback path is exercised in 12-02)."
    - "D-05: all THREE tools are in scope and 'is v145 in the vcxproj' != 'actually builds+links' — resolved by an actual build pass; TreeFileBuilder (the cheapest first-green, unverified like all three per RESEARCH) is built+linked here, the other two in 12-02."
    - "A new Utinni-owned tools/ tree exists, lifted verbatim from swg-client-v2 @ 5fce7bb8, with the relative ProjectReference graph preserved (no reference into swg-client-v2)."
    - "TreeFileBuilder builds AND links standalone at v145/Win32 from tools/Utinni.Tools.sln, producing TreeFileBuilder_d.exe."
    - "The exact lifted-from x86 SHA (5fce7bb8) is recorded in-repo as a SHA, not a branch."
    - "The tools' compile/ OutDir is git-ignored so no built binaries are committed."
  artifacts:
    - path: "tools/Utinni.Tools.sln"
      provides: "Standalone Utinni-owned solution (D-07); Debug|Win32 + Release|Win32 only"
      contains: "TreeFileBuilder"
    - path: "tools/PINNED-SHA.md"
      provides: "Pinned source provenance (D-08/D-03)"
      contains: "5fce7bb8368c86d5a2330a0173d1541866786196"
    - path: "tools/src/engine/shared/application/TreeFileBuilder/build/win32/TreeFileBuilder.vcxproj"
      provides: "Lifted TreeFileBuilder project, v145/Win32"
      contains: "v145"
  key_links:
    - from: "tools/src/engine/shared/application/TreeFileBuilder/build/win32/TreeFileBuilder.vcxproj"
      to: "tools/src/engine/shared/library/shared*/build/win32/*.vcxproj"
      via: "ProjectReference (relative paths within the copied tree)"
      pattern: "ProjectReference"
    - from: "tools/src/engine/shared/application/TreeFileBuilder/build/win32/TreeFileBuilder.vcxproj"
      to: "tools/src/external/3rd/library/zlib/lib/win32/zlib.lib"
      via: "AdditionalDependencies / AdditionalLibraryDirectories (zlib 1.1.4, NOT zlib-1.2.3)"
      pattern: "zlib"
---

<objective>
Create the Utinni-owned `tools/` tree from scratch by lifting-and-shifting `TreeFileBuilder` and its full dependency closure (12 shared-library ProjectReferences + zlib 1.1.4) from `swg-client-v2` @ pinned SHA `5fce7bb8`, stand up `tools/Utinni.Tools.sln`, and prove TreeFileBuilder builds + links standalone at v145/Win32. This is the cheapest first-green (12 ProjectReferences, zlib-only, NO Perforce, NO pcre) per RESEARCH §Standard Stack — front-load the empirical build risk here.

Purpose: This is the foundation of the AUTH-01 hard gate. It establishes the lift-and-shift pattern (D-01), the pinned-SHA provenance contract (D-03/D-08), and the standalone solution (D-07) that plans 12-02 and 12-03 extend. The "is v145 in the vcxproj" vs "actually builds + links" distinction is resolved here by an actual build pass, not by reading (RESEARCH Pitfall 2 / Anti-Pattern: reading vcxproj toolset to infer build status).

Output: `tools/Utinni.Tools.sln`, the lifted TreeFileBuilder + dep-tree source, `tools/PINNED-SHA.md`, a `.gitignore` entry for the tools OutDir, and a green TreeFileBuilder build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-CONTEXT.md
@.planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-RESEARCH.md
@.planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-PATTERNS.md
@.planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-VALIDATION.md

<lift_source>
Read-only sibling corpus (NEVER #include / ProjectReference across into it — D-01):
- Repo: D:/Code/swg-client-v2, branch koogie-msvc-cpp20-base, SHA 5fce7bb8368c86d5a2330a0173d1541866786196
- TreeFileBuilder app: D:/Code/swg-client-v2/src/engine/shared/application/TreeFileBuilder/ (FirstTreeFileBuilder.{cpp,h}, TreeFileBuilder.{cpp,h}, .dox + build/win32/TreeFileBuilder.vcxproj — 835 LOC, TAG_0005)
- TreeFileBuilder's 12 ProjectReferences (RESEARCH §Standard Stack): fileInterface, sharedCompression, sharedDebug, sharedFile, sharedFoundationTypes, sharedFoundation, sharedIoWin, sharedMath, sharedMemoryManager, sharedRandom, sharedSynchronization, sharedThread — all under D:/Code/swg-client-v2/src/engine/shared/library/shared*/
- zlib 1.1.4 external: D:/Code/swg-client-v2/src/external/3rd/library/zlib/ (lib/win32/zlib.lib) — this is the 1.1.4 lib; do NOT lift the sibling zlib-1.2.3 dir (Pitfall 3, byte-exact determinant)
- Path arithmetic: vcxproj relative paths use 6x `..` from build/win32 → resolve to src/ (RESEARCH §Dependency Closure). Preserve directory shape so they resolve within the copy.
</lift_source>

<interfaces>
TreeFileBuilder CLI surface (from TreeFileBuilder.cpp run(), RESEARCH §Code Examples) — for downstream smoke wiring in 12-03, no implementation needed here:
- Invocation: TreeFileBuilder_d.exe -r <responseFile.rsp> <out.tre>
- Sibling tool TreeFileRspBuilder exists in swg-client-v2 (Open Q3 — relevant later for deriving an .rsp; NOT lifted this plan)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lift TreeFileBuilder + 12-dep closure + zlib 1.1.4 into tools/, preserving the relative graph</name>
  <files>tools/Utinni.Tools.sln (created Task 2), tools/src/engine/shared/application/TreeFileBuilder/, tools/src/engine/shared/library/, tools/src/external/3rd/library/zlib/, .gitignore</files>
  <read_first>
    - D:/Code/swg-client-v2/src/engine/shared/application/TreeFileBuilder/build/win32/TreeFileBuilder.vcxproj (the exact ProjectReference list + AdditionalDependencies + include paths to preserve)
    - D:/Code/swg-client-v2/src/engine/shared/application/TreeFileBuilder/src/shared/TreeFileBuilder.cpp (confirm zlib-only, no Perforce, no pcre includes)
    - .planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-PATTERNS.md (§ "Lifted compilers" — exact source paths + "preserve relative directory shape" rule)
    - .gitignore (existing build-output rules at lines 3-4 Release/ Debug/, line 19 obj/, line 58 /vcpkg_installed/ — convention to follow)
  </read_first>
  <action>
    Create tools/ at repo root. Copy verbatim from swg-client-v2 @ 5fce7bb8 into tools/, preserving the relative directory shape so the vcxproj `..\..\..\..\..\..\external\…` and ProjectReference paths resolve WITHIN the copy (D-01, RESEARCH Pattern 1): copy src/engine/shared/application/TreeFileBuilder/ → tools/src/engine/shared/application/TreeFileBuilder/; copy each of the 12 shared-library projects (fileInterface, sharedCompression, sharedDebug, sharedFile, sharedFoundationTypes, sharedFoundation, sharedIoWin, sharedMath, sharedMemoryManager, sharedRandom, sharedSynchronization, sharedThread) from src/engine/shared/library/<lib>/ → tools/src/engine/shared/library/<lib>/; copy ONLY the zlib 1.1.4 external (src/external/3rd/library/zlib/) → tools/src/external/3rd/library/zlib/ — do NOT copy zlib-1.2.3. Copy any transitive externals these 12 libs reference (resolve by reading each lib's vcxproj AdditionalDependencies/include paths; copy the referenced src/external/… leaf into the same relative location). NEVER add a path that points back into D:/Code/swg-client-v2. Add a .gitignore entry for the tools OutDir: the vcxprojs' OutDir is compile/win32/<Tool>/<Config>/ (RESEARCH §Runtime State Inventory), which the existing Release//Debug/ rules do NOT cover — add a `tools/compile/` ignore with a one-line WHY comment per the heavily-commented .gitignore convention. Do NOT commit any *_d.exe / *_r.exe binaries.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (!(Test-Path 'tools/src/engine/shared/application/TreeFileBuilder/build/win32/TreeFileBuilder.vcxproj')) { exit 1 }; if (!(Test-Path 'tools/src/external/3rd/library/zlib/lib/win32/zlib.lib')) { exit 1 }; if (Test-Path 'tools/src/external/3rd/library/zlib-1.2.3') { Write-Error 'zlib-1.2.3 wrongly lifted'; exit 1 }; $r = Select-String -Path 'tools/src/engine/shared/application/TreeFileBuilder/build/win32/TreeFileBuilder.vcxproj' -Pattern 'swg-client-v2','D:\\Code\\swg-client'; if ($r) { Write-Error 'vcxproj references live swg-client-v2 tree (D-01 violation)'; exit 1 }; exit 0"</automated>
  </verify>
  <done>tools/ contains the TreeFileBuilder app + its 12 ProjectReference libs + zlib 1.1.4 (NOT 1.2.3), relative graph preserved, no path references back into swg-client-v2, and tools/compile/ is git-ignored.</done>
</task>

<task type="auto">
  <name>Task 2: Create tools/Utinni.Tools.sln + PINNED-SHA.md, build TreeFileBuilder green at v145/Win32</name>
  <files>tools/Utinni.Tools.sln, tools/PINNED-SHA.md</files>
  <read_first>
    - Utinni.sln (repo root — the .sln project-list / GUID / configuration-matrix convention only; this new sln is standalone, NOT folded in — D-07)
    - tools/src/engine/shared/application/TreeFileBuilder/build/win32/TreeFileBuilder.vcxproj (the project GUID + the 12 ProjectReference GUIDs to re-home under the new sln)
    - .planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-RESEARCH.md (§ "The genuine v145 conformance deltas" — _HAS_AUTO_PTR_ETC, /Zc:twoPhase- escape hatches if needed; §Pinned source for PINNED-SHA.md content)
    - docs/ai/toolchain-inventory.md (the [VERIFIED: …] provenance convention for PINNED-SHA.md)
  </read_first>
  <action>
    Create tools/Utinni.Tools.sln as a standalone Utinni-owned solution containing TreeFileBuilder.vcxproj + its 12 transitive ProjectReference projects, with internally-consistent GUIDs re-homed under this one solution (RESEARCH Pattern 1 "re-home all lifted GUIDs"). Solution configuration matrix is Debug|Win32 and Release|Win32 ONLY (x86 only, CON-P-02/D-03) — no x64, no AnyCPU. Build TreeFileBuilder via the VS2026 MSBuild at `D:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe` with `tools\Utinni.Tools.sln /p:Configuration=Debug /p:Platform=Win32 /t:TreeFileBuilder /m /nologo /v:minimal`. If v145 conformance errors surface, apply ONLY the narrow documented escape hatches (_HAS_AUTO_PTR_ETC, /Zc:twoPhase-, enum/template keyword fixes per RESEARCH); these are not a v143 fallback (TreeFileBuilder has no expected v145 blockers — shared libs already v145 on this branch). Create tools/PINNED-SHA.md recording Repo D:/Code/swg-client-v2, Branch koogie-msvc-cpp20-base, SHA 5fce7bb8368c86d5a2330a0173d1541866786196 [VERIFIED: git rev-parse, 2026-06-02], and the x64bit-Upgrade / MSVC-CPP20-Upgrade divergence watch (x64 collides with CON-P-02 — re-sync deliberately). Record the SHA as a SHA, never a branch HEAD (D-03).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "& 'D:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe' 'tools\Utinni.Tools.sln' /p:Configuration=Debug /p:Platform=Win32 /t:TreeFileBuilder /m /nologo /v:minimal; if ($LASTEXITCODE -ne 0) { exit 1 }; $exe = Get-ChildItem -Recurse -Path tools -Filter 'TreeFileBuilder_d.exe' -ErrorAction SilentlyContinue; if (!$exe) { Write-Error 'TreeFileBuilder_d.exe not produced'; exit 1 }; if (!(Select-String -Path 'tools/PINNED-SHA.md' -Pattern '5fce7bb8368c86d5a2330a0173d1541866786196' -Quiet)) { exit 1 }; exit 0"</automated>
  </verify>
  <done>MSBuild exits 0 for /t:TreeFileBuilder at Debug|Win32; TreeFileBuilder_d.exe exists; PINNED-SHA.md records the exact SHA 5fce7bb8 + branch + x64 watch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| swg-client-v2 sibling → Utinni tools/ | Source + prebuilt static libs (zlib 1.1.4) are copied in; the pinned SHA is the integrity anchor against upstream force-push |
| TreeFileBuilder ← .rsp/source tree | Offline, maintainer-controlled input this phase; no agent/network surface added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | Lifted source moves under us (upstream force-push on koogie-msvc-cpp20-base) | mitigate | Pin exact SHA 5fce7bb8 in PINNED-SHA.md (D-03/D-08); never lift a branch HEAD; deliberate re-sync only |
| T-12-02 | Tampering/DoS | Ancient vendored zlib 1.1.4 carries known CVEs | accept | Tool runs offline on trusted inputs this phase; do NOT silently upgrade zlib (breaks byte-exact); note version + tension in DEPENDENCY-MANIFEST.md (12-03). Agent-exposed input surface is Phase-14 Deferred. |
| T-12-03 | Elevation/DoS | Fixed char[10*1024] buffers in TreeFileBuilder::addResponseFile (2002-era parser) | accept | Offline/trusted-input only this phase; flagged for the Phase-14 MCP threat register when inputs become agent-influenceable — out of scope here |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs this phase — all code is lift-and-shift MSVC C++ + pre-vendored static libs; no registry surface (RESEARCH §Package Legitimacy Audit — N/A). The supply-chain control that applies is SHA pinning (T-12-01). |
</threat_model>

<verification>
- TreeFileBuilder builds + links green at v145/Win32 from tools/Utinni.Tools.sln (MSBuild exit 0, TreeFileBuilder_d.exe produced).
- No lifted vcxproj references the live swg-client-v2 tree (D-01).
- zlib 1.1.4 lifted (not zlib-1.2.3).
- PINNED-SHA.md records SHA 5fce7bb8 as a SHA + x64 divergence watch.
- tools/compile/ git-ignored; no binaries committed.
</verification>

<success_criteria>
- ROADMAP Phase 12 success criterion #1 (partial: TreeFileBuilder of the three builds+links standalone at v145 from tools/Utinni.Tools.sln with no swg-client-v2 reference).
- ROADMAP Phase 12 success criterion #3 (pinned SHA recorded in-repo as a SHA).
</success_criteria>

<output>
Create `.planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-01-SUMMARY.md` when done
</output>
