---
phase: 12-revive-feasibility-spike-hard-gate-intro-skip-crash
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - tools/Utinni.Tools.sln
  - tools/src/engine/shared/application/TemplateCompiler/
  - tools/src/engine/shared/application/TemplateDefinitionCompiler/
  - tools/src/engine/shared/library/
  - tools/src/external/3rd/library/pcre/
  - tools/src/external/3rd/library/perforce/
autonomous: false
requirements: [AUTH-01]
user_setup: []

must_haves:
  truths:
    - "TemplateCompiler builds AND links standalone at v145/Win32 (or documented v143 per D-10 after stop-and-ask), with its Perforce link kept or stubbed per an explicit recorded decision."
    - "TemplateDefinitionCompiler builds AND links standalone at v145/Win32 (or documented v143 after stop-and-ask), Perforce kept or stubbed per an explicit recorded decision."
    - "No tool drops to v143 without a maintainer stop-and-ask decision being captured first (D-10)."
    - "The byte-exact -compile path remains Perforce-free at runtime regardless of keep-or-stub (P4 only behind -edit/-submit)."
  artifacts:
    - path: "tools/src/engine/shared/application/TemplateCompiler/build/win32/TemplateCompiler.vcxproj"
      provides: "Lifted TemplateCompiler project, v145/Win32 (26 ProjectReferences)"
      contains: "v145"
    - path: "tools/src/engine/shared/application/TemplateDefinitionCompiler/build/win32/TemplateDefinitionCompiler.vcxproj"
      provides: "Lifted TemplateDefinitionCompiler project, v145/Win32 (28 ProjectReferences)"
      contains: "v145"
  key_links:
    - from: "tools/src/engine/shared/application/TemplateCompiler/build/win32/TemplateCompiler.vcxproj"
      to: "tools/src/external/3rd/library/pcre/4.1/win32/lib/libpcre.a"
      via: "AdditionalDependencies (PCRE_STATIC)"
      pattern: "pcre"
    - from: "tools/Utinni.Tools.sln"
      to: "TemplateCompiler.vcxproj + TemplateDefinitionCompiler.vcxproj"
      via: "solution project entries re-homed under the standalone sln"
      pattern: "TemplateCompiler|TemplateDefinitionCompiler"
---

<objective>
Lift `TemplateCompiler` (26 ProjectReferences) and `TemplateDefinitionCompiler` (28 ProjectReferences, the largest) into the `tools/` tree created by 12-01, add them to `tools/Utinni.Tools.sln`, and prove each builds + links standalone at v145/Win32. For each, make and record an explicit Perforce keep-link-or-stub decision (RESEARCH Pitfall 1 corrects D-04: Perforce is LIVE code in both app sources, not dead). If either tool refuses v145, STOP and surface the failure to the maintainer before any v143 fallback (D-10).

Purpose: Closes the remaining two of the three AUTH-01 build legs. These carry pcre 4.1 + live Perforce ClientAPI usage, so they are riskier than TreeFileBuilder. The keep-or-stub decision and the v143 stop-and-ask are the load-bearing judgment calls — both are maintainer-in-the-loop per the high-rigor disposition (CONTEXT §specifics).

Output: TemplateCompiler + TemplateDefinitionCompiler added to the solution and building green at v145 (or documented-v143-after-approval), with the per-tool Perforce decision recorded for 12-03's manifest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-CONTEXT.md
@.planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-RESEARCH.md
@.planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-PATTERNS.md
@.planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-01-SUMMARY.md

<lift_source>
Read-only sibling corpus (NEVER #include / ProjectReference across into it — D-01). swg-client-v2 @ 5fce7bb8:
- TemplateCompiler app: D:/Code/swg-client-v2/src/engine/shared/application/TemplateCompiler/ (TemplateCompiler.cpp, FirstTemplateCompiler.{cpp,h} — 634 LOC). Live `#include "clientapi.h"`, MyPerforceUser subclass, -compile→TpfFile::makeIffFiles (P4-free).
- TemplateDefinitionCompiler app: D:/Code/swg-client-v2/src/engine/shared/application/TemplateDefinitionCompiler/ (TemplateDefinitionCompiler.cpp, FirstTemplateDefinitionCompiler.{cpp,h} — 778 LOC). Live Perforce; -compile generates C++ source (not asset), P4-free.
- ProjectReference sets (RESEARCH §Standard Stack — exact per-tool counts): TemplateCompiler = 26 (TreeFileBuilder's 12 + archive, localizationArchive, localization, unicodeArchive, unicode, sharedLog, sharedMessageDispatch, sharedNetworkMessages, sharedRegex, sharedTemplateDefinition, sharedTemplate, sharedTerrain, sharedUtility); TemplateDefinitionCompiler = 28 (TemplateCompiler's set + sharedMathArchive, sharedObject, sharedSwitcher). Many of the 12 are already lifted by 12-01 — copy only the NEW ones.
- pcre 4.1: D:/Code/swg-client-v2/src/external/3rd/library/pcre/4.1/win32/lib/libpcre.a (GNU .a archive, PCRE_STATIC). Needed by BOTH template tools.
- perforce: D:/Code/swg-client-v2/src/external/3rd/library/perforce/lib/win32/ (libclient/librpc/libsupp.lib + ws2_32). LIVE link unless stubbed.
- The dead-dep vestige: sharedTemplate.vcxproj carries a perforce/include path present-but-never-#included — that include DIR is the only genuinely-dead perforce path (prune that one; the apps' P4 is live).
</lift_source>

<interfaces>
P4 keep-or-stub stub option (RESEARCH Pattern 2 / Pitfall 1) — if the link refuses v145 or to drop the P4 libs entirely. Stubbing removes libclient/librpc/libsupp/ws2_32 and the #include "clientapi.h" + MyPerforceUser/StrBufFixed classes; costs only the -edit/-submit verbs (which Utinni never uses). Signatures to stub: `int checkOut(const char*)` and `int checkIn(const char*)` returning -1. The byte-exact -compile verb (TpfFile::makeIffFiles / parseTemplateDefinitionFile) is P4-free either way.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lift TemplateCompiler + TemplateDefinitionCompiler + their new deps (pcre, perforce, extra shared libs) into tools/</name>
  <files>tools/src/engine/shared/application/TemplateCompiler/, tools/src/engine/shared/application/TemplateDefinitionCompiler/, tools/src/engine/shared/library/ (new ProjectReference projects), tools/src/external/3rd/library/pcre/, tools/src/external/3rd/library/perforce/</files>
  <read_first>
    - D:/Code/swg-client-v2/src/engine/shared/application/TemplateCompiler/build/win32/TemplateCompiler.vcxproj (exact 26 ProjectReference list + AdditionalDependencies incl pcre + perforce libs + include paths)
    - D:/Code/swg-client-v2/src/engine/shared/application/TemplateDefinitionCompiler/build/win32/TemplateDefinitionCompiler.vcxproj (exact 28 ProjectReference list)
    - D:/Code/swg-client-v2/src/engine/shared/application/TemplateCompiler/src/shared/TemplateCompiler.cpp (confirm the live clientapi.h include + MyPerforceUser + the P4-free -compile path before deciding keep-or-stub)
    - .planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-PATTERNS.md (§ "Per-tool keep-or-stub decision" + the exact source paths)
    - .planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-01-SUMMARY.md (which of the 12 shared libs are ALREADY lifted — avoid duplicating)
  </read_first>
  <action>
    Copy verbatim from swg-client-v2 @ 5fce7bb8, preserving relative directory shape (D-01, RESEARCH Pattern 1): the two app dirs → tools/src/engine/shared/application/{TemplateCompiler,TemplateDefinitionCompiler}/; the NEW shared-library ProjectReference projects not already lifted by 12-01 (archive, localizationArchive, localization, unicodeArchive, unicode, sharedLog, sharedMessageDispatch, sharedNetworkMessages, sharedRegex, sharedTemplateDefinition, sharedTemplate, sharedTerrain, sharedUtility, sharedMathArchive, sharedObject, sharedSwitcher) → tools/src/engine/shared/library/<lib>/; pcre 4.1 (src/external/3rd/library/pcre/4.1/) and perforce (src/external/3rd/library/perforce/) → the same relative locations under tools/src/external/3rd/library/. Copy any further transitive externals these new libs reference into their preserved relative locations (resolve by reading each new lib's vcxproj). Prune the single genuinely-dead perforce/include path in the lifted sharedTemplate.vcxproj (the present-but-never-#included include DIR — D-04's real target). Do NOT prune the apps' live Perforce usage. Verify no lifted project path points back into D:/Code/swg-client-v2.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "foreach ($p in @('tools/src/engine/shared/application/TemplateCompiler/build/win32/TemplateCompiler.vcxproj','tools/src/engine/shared/application/TemplateDefinitionCompiler/build/win32/TemplateDefinitionCompiler.vcxproj','tools/src/external/3rd/library/pcre/4.1/win32/lib/libpcre.a')) { if (!(Test-Path $p)) { Write-Error \"missing $p\"; exit 1 } }; $hits = Get-ChildItem -Recurse -Path tools -Include *.vcxproj | Select-String -Pattern 'D:\\\\Code\\\\swg-client','\\.\\.[\\\\/]swg-client-v2'; if ($hits) { Write-Error 'lifted vcxproj references live swg-client-v2 tree (D-01 violation)'; exit 1 }; exit 0"</automated>
  </verify>
  <done>Both template apps + pcre 4.1 + perforce (or the planned stub source) + all NEW shared-lib deps are lifted into tools/ with relative graph preserved; the dead sharedTemplate perforce/include path is pruned; no path references swg-client-v2.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Per-tool Perforce keep-link-or-stub decision</name>
  <files>tools/src/engine/shared/application/TemplateCompiler/, tools/src/engine/shared/application/TemplateDefinitionCompiler/ (P4 link config / stub source applied in Task 3 after this decision)</files>
  <action>Pause for the maintainer to decide, per template tool, whether to keep the vendored Perforce link or stub checkOut/checkIn. The executor first ATTEMPTS the v145 link with the vendored P4 libs (Open Question 2 recommendation), then surfaces the result and the options below. Do NOT auto-prune Perforce — it is LIVE code in both apps (RESEARCH Pitfall 1). See the structured decision below.</action>
  <verify>
    <human-check>A per-tool keep-link-or-stub decision is recorded for both TemplateCompiler and TemplateDefinitionCompiler, for use in Task 4 and 12-03's manifest.</human-check>
  </verify>
  <done>Per-tool Perforce disposition (keep-link or stub) selected and recorded for both template tools.</done>
  <decision>Per template tool: keep the Perforce link, or stub checkOut/checkIn and drop the P4 libs?</decision>
  <context>
    RESEARCH Pitfall 1 (corrects D-04): Perforce is LIVE code in both TemplateCompiler.cpp and TemplateDefinitionCompiler.cpp (they #include "clientapi.h" and subclass ClientUser/StrBuf). The byte-exact -compile verb is P4-free at runtime; P4 is only reached via -edit/-submit, which Utinni never invokes. This is a per-tool decision the plan must make explicitly, not an automatic prune. The executor should ATTEMPT the v145 link with the vendored P4 libs first, then surface the result here.
  </context>
  <options>
    <option id="keep-link">
      <name>Keep the Perforce link (vendored libclient/librpc/libsupp.lib)</name>
      <pros>Zero source edits to the lifted app; closest to upstream; if the v145 link "just works" this is lowest-effort.</pros>
      <cons>Retains a 24-year-old binary-compat risk; pulls ws2_32 + 3 P4 libs into the link.</cons>
    </option>
    <option id="stub">
      <name>Stub checkOut/checkIn to return -1, delete the clientapi.h include + MyPerforceUser/StrBufFixed</name>
      <pros>Removes libclient/librpc/libsupp/ws2_32 from the link and the binary-compat risk; costs nothing Utinni uses (only -edit/-submit become no-ops).</pros>
      <cons>Small source edit per app (a deviation from verbatim lift, recorded in the manifest).</cons>
    </option>
  </options>
  <resume-signal>Select per tool: "keep-link" or "stub" for TemplateCompiler and for TemplateDefinitionCompiler (may differ). Record the decision for 12-03's DEPENDENCY-MANIFEST.md.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: v145 build+link result + D-10 stop-and-ask gate</name>
  <files>tools/Utinni.Tools.sln (template-tool entries + any v143 retarget applied per the maintainer decision)</files>
  <action>Surface the per-tool v145 build+link result and, for any tool that refused v145, STOP and ask the maintainer before any v143 fallback (D-10). Apply the approved keep-or-stub decision from Task 2 and only the narrow v145 escape hatches (_HAS_AUTO_PTR_ETC, /Zc:twoPhase-) where needed. See <what-built>/<how-to-verify>/<resume-signal>.</action>
  <verify>
    <human-check>Per tool: v145 build+link SUCCEEDED (exit 0, *_d.exe produced) is reported, OR a maintainer-approved v143 fallback decision is recorded with the exact failing errors. No tool dropped to v143 without the maintainer's per-tool approval.</human-check>
  </verify>
  <done>Both template tools are confirmed green at v145, or any v143 fallback is a recorded maintainer decision (D-10) with the failing errors captured for the manifest.</done>
  <what-built>
    Attempted v145/Win32 build+link of TemplateCompiler and TemplateDefinitionCompiler from tools/Utinni.Tools.sln, applying the keep-or-stub decision from Task 2 and the narrow v145 conformance escape hatches (_HAS_AUTO_PTR_ETC, /Zc:twoPhase-) where needed. Both projects added to tools/Utinni.Tools.sln (Debug|Win32 + Release|Win32 only). Build invocation:
    `MSBuild tools\Utinni.Tools.sln /p:Configuration=Debug /p:Platform=Win32 /t:TemplateCompiler` and `/t:TemplateDefinitionCompiler` (VS2026 MSBuild at D:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe).
  </what-built>
  <how-to-verify>
    This checkpoint is the D-10 stop-and-ask gate. The executor MUST surface, per tool:
    1. Whether the v145 build+link SUCCEEDED (MSBuild exit 0, TemplateCompiler_d.exe / TemplateDefinitionCompiler_d.exe produced) or FAILED.
    2. If FAILED at v145: the exact compiler/linker errors, and a recommendation. Do NOT auto-fallback to v143 — the maintainer decides per tool (D-10). If the maintainer approves v143 for a tool, document the failure + the v143 decision in the SUMMARY (for 12-03's manifest) and rebuild that tool at v143 in tools/ (the lift-and-shift constraint forbids building IN swg-client-v2, not building at a given toolset in our own tree — D-02).
    3. The keep-or-stub Perforce decision applied per tool.
  </how-to-verify>
  <resume-signal>Type "approved" if both build green at v145; or specify the per-tool v143 fallback decision for any tool that refused v145 (D-10 stop-and-ask).</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Finalize sln entries + record per-tool toolset + Perforce decisions for the manifest</name>
  <files>tools/Utinni.Tools.sln</files>
  <read_first>
    - tools/Utinni.Tools.sln (the existing TreeFileBuilder entries from 12-01 — add the two template tools + their new ProjectReference projects with internally-consistent GUIDs)
    - .planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-RESEARCH.md (§ "v145 conformance deltas" — only the narrow escape hatches; §Alternatives for the stub rationale)
  </read_first>
  <action>
    Apply the approved keep-or-stub and toolset decisions from Tasks 2 and 3. Ensure TemplateCompiler.vcxproj + TemplateDefinitionCompiler.vcxproj + all their NEW ProjectReference projects are present in tools/Utinni.Tools.sln with consistent GUIDs and Debug|Win32 + Release|Win32 mappings only. Run the full-solution build to confirm all three tools build together with no GUID/config-matrix breakage. Record in the SUMMARY, per tool: the final PlatformToolset used (v145, or v143-with-maintainer-approval), and the Perforce disposition (keep-link or stub) — these are the inputs 12-03 writes into DEPENDENCY-MANIFEST.md.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "& 'D:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe' 'tools\Utinni.Tools.sln' /p:Configuration=Debug /p:Platform=Win32 /m /nologo /v:minimal; if ($LASTEXITCODE -ne 0) { exit 1 }; $tc = Get-ChildItem -Recurse -Path tools -Filter 'TemplateCompiler_d.exe' -ErrorAction SilentlyContinue; $td = Get-ChildItem -Recurse -Path tools -Filter 'TemplateDefinitionCompiler_d.exe' -ErrorAction SilentlyContinue; $tf = Get-ChildItem -Recurse -Path tools -Filter 'TreeFileBuilder_d.exe' -ErrorAction SilentlyContinue; if (!$tc -or !$td -or !$tf) { Write-Error 'one or more tool exes missing'; exit 1 }; exit 0"</automated>
  </verify>
  <done>Full tools\Utinni.Tools.sln builds green at Debug|Win32; all three *_d.exe present; the per-tool toolset + Perforce decisions are recorded in the SUMMARY for 12-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| swg-client-v2 sibling → Utinni tools/ | Lifted source + prebuilt static libs (pcre 4.1, Perforce ClientAPI); pinned SHA is the integrity anchor |
| TemplateCompiler/TemplateDefinitionCompiler ← .tpf/.tdf | Offline maintainer-controlled input this phase; no agent/network surface added |
| App → live Perforce server (via -edit/-submit) | NOT crossed by Utinni — byte-exact path is -compile only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Info disclosure | Perforce -edit/-submit verbs reaching a live P4 server | mitigate | Stub or never invoke -edit/-submit (RESEARCH Pattern 2); Utinni's byte-exact path is -compile only; the keep-or-stub checkpoint makes this explicit per tool |
| T-12-05 | Tampering/DoS | Ancient vendored pcre 4.1 + Perforce ClientAPI libs carry known CVEs | accept | Offline trusted-input only this phase; document pinned versions in DEPENDENCY-MANIFEST.md (12-03); agent-input exposure is Phase-14 Deferred |
| T-12-06 | Elevation/DoS | 2002-era fixed-buffer parsers in the template apps | accept | Offline/trusted-input only; flagged for the Phase-14 MCP threat register when inputs become agent-influenceable — out of scope here |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs — lift-and-shift MSVC C++ + pre-vendored static libs only; control is SHA pinning (12-01 T-12-01) |
</threat_model>

<verification>
- TemplateCompiler + TemplateDefinitionCompiler build + link green at v145/Win32 (or documented-v143-after-maintainer-approval per D-10).
- Full tools\Utinni.Tools.sln builds all three tools together; all three *_d.exe present.
- Per-tool Perforce keep-or-stub + toolset decisions recorded for 12-03's manifest.
- No lifted vcxproj references the live swg-client-v2 tree (D-01).
</verification>

<success_criteria>
- ROADMAP Phase 12 success criterion #1 (completes: all three tools build+link standalone at v145, or documented v143 fallback per tool, from tools/Utinni.Tools.sln with no swg-client-v2 reference).
- ROADMAP Phase 12 success criterion #2 partial input: the per-tool kept/pruned dep + Perforce decisions feed the DEPENDENCY-MANIFEST.md authored in 12-03.
</success_criteria>

<output>
Create `.planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-02-SUMMARY.md` when done
</output>
