---
phase: 12-revive-feasibility-spike-hard-gate-intro-skip-crash
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-RESID-02-RCA.md
  - UtinniCore/
autonomous: false
requirements: [RESID-02]
user_setup: []

must_haves:
  truths:
    - "D-06: the VEH crash-logger is already deployed (d1096ac); this plan is the capture+diagnose+fix run, NOT a re-instrumentation run — the logger is read-only here and stays deployed after capture."
    - "The intro-skip scene-transition crash is reproduced via the TJT-driven scene-change path on a live injected session, and its faulting module+RVA is captured from the deployed VEH logger's `VEH FATAL` log line."
    - "The fault is dispositioned per D-11: if it resolves into Utinni's own injection/detour/callback surface it is root-cause-fixed there (no masking guard/detour shortcut); if it resolves into SWG.exe game code Utinni does not own, the deliverable is a documented root-cause analysis (module+RVA+mechanism)."
    - "The VEH crash-logger remains deployed after capture (D-11)."
    - "Landing naked after a TJT scene change is treated as the expected baseline, NOT a crash signal."
  artifacts:
    - path: ".planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-RESID-02-RCA.md"
      provides: "Captured VEH FATAL line (code/EIP/module/base/rva/ESP/access) + root-cause analysis + disposition (Utinni-side fix OR documented game-side RCA)"
      contains: "VEH FATAL"
  key_links:
    - from: "live injected SWGEmu session (TJT scene-change → Return-skip intro)"
      to: "utinni.log (utinni::log::warning spdlog sink)"
      via: "deployed VEH handler utinniBreakpointVEH (utinni.cpp, d1096ac)"
      pattern: "VEH FATAL"
---

<objective>
Reproduce, diagnose, and dispose of the dump-less intro-skip scene-transition crash (RESID-02) using the already-deployed VEH crash-logger (commit `d1096ac`). Reproduce via the TJT-driven scene-change path (the known trigger), read the faulting `module+rva` out of Utinni's `VEH FATAL` log line, and resolve per D-11's two-branch disposition: a full root-cause fix if the fault is in Utinni's own injection/detour/callback surface, or a documented root-cause analysis if it resolves into SWG.exe game code Utinni does not own.

Purpose: RESID-02 is an independent, self-contained V1 residual that blocks nothing else — it ships early in this phase, fully parallel to the AUTH-01 track. The VEH logger was deployed in Phase 11 as the invented harness for this exact dump-less fault; this is the capture+diagnose+fix run, not a re-instrumentation run (D-06). No masking guard or detour shortcut is acceptable (D-11) — the fix must address the underlying defect.

Output: a captured `VEH FATAL` faulting module+RVA, a root-cause analysis document, and either a committed Utinni-side fix or a documented game-side RCA.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-CONTEXT.md
@.planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-RESEARCH.md
@.planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-PATTERNS.md
@UtinniCore/utinni.cpp
@.planning/phases/11-tjt-subpanel-object-template-editor/11-05-SUMMARY.md

<interfaces>
Deployed VEH logger (UtinniCore/utinni.cpp, commit d1096ac — READ ONLY, do NOT re-instrument, D-06):
- Handler: utinniBreakpointVEH (utinni.cpp:136), returns EXCEPTION_CONTINUE_SEARCH (pure observation, utinni.cpp:162/188/196)
- Module handles: g_utinniModule (UtinniCore.dll), g_swgModule (SWGEmu.exe) set in utinni_init before AddVectoredExceptionHandler (utinni.cpp:73-74)
- Install: AddVectoredExceptionHandler(1, utinniBreakpointVEH) — FirstHandler=1, runs before other VEHs
- Logs only if EIP is inside SWGEmu.exe or UtinniCore.dll, or an unmapped EIP (wild jump); CLR/system AVs skipped
- Triggers: ACCESS_VIOLATION, IN_PAGE_ERROR, ILLEGAL_INSTRUCTION, PRIV_INSTRUCTION, STACK_OVERFLOW, DATATYPE_MISALIGNMENT, 0x4000001F
- Output line to grep:  VEH FATAL: code=0x%08X EIP=0x%08X module=%s base=0x%08X rva=0x%08X ESP=0x%08X [WRITE|EXEC|READ target=0x%08X]  — the `rva` field is the deliverable
- Sink: utinni::log::warning → spdlog with flush_on(info), capped at 64 fatal lines
- Log file path: utinni::log writes "utinni.log" via getPath()+"utinni.log" (UtinniCore/utility/log.cpp:145-147) — confirm the concrete on-disk path next to the injecting host process

Detour single-sourcing pattern (UtinniCore/swg/scene/ground_scene.cpp — for a Utinni-side fix, D-11 branch 1):
- RVAs single-sourced as typed function pointers: `using pCtor = ...(__thiscall*)(...);  pCtor ctor = (pCtor)0x00519830;` (ground_scene.cpp:46-58), hooked via Detour::Create (CON-H-03)
- Use DETOUR_LEN_AUTO for any detour (memory feedback_detourxs_explicit_len — explicit len corrupts target)
- Prior-art for THIS fault class: project_rh_snapshot_no_heap_alloc — a prior scene-change crash at 0x0051fb0a was a per-frame std::vector::reserve fragmenting SWG's allocator; fixed with a stack-allocated fixed-size snapshot (dispatchSnapshot template in ground_scene.cpp). CHECK the snapshot-dispatch migration status of the scene-change callback path FIRST.
- If input/context routing: project_swg_context_routing (Enter dispatches as chatEnter not openChat under injection)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Live-injection repro + VEH FATAL capture (Tier-4 manual)</name>
  <files>(no files modified — live-injection reproduction; captured VEH FATAL line feeds Task 2's RCA)</files>
  <action>Pause for the maintainer to reproduce the intro-skip crash on a live injected SWGEmu session via the TJT-driven scene-change path and capture the deployed VEH logger's `VEH FATAL` line verbatim. Do NOT disable TJT (it is the trigger). See <what-built>/<how-to-verify>/<resume-signal>.</action>
  <verify>
    <human-check>A `VEH FATAL` line (code/EIP/module/base/rva/ESP/access-target) is captured verbatim from utinni.log after a TJT-driven intro-skip repro, and the concrete utinni.log path is confirmed. If no crash reproduces after several attempts, that is reported (A5).</human-check>
  </verify>
  <done>The verbatim VEH FATAL line + confirmed utinni.log path are recorded for Task 2 (or a no-repro report per A5).</done>
  <what-built>
    Nothing built yet — this is the live-injection reproduction + capture step. The VEH crash-logger is already deployed (d1096ac); this task drives it.
  </what-built>
  <how-to-verify>
    On the maintainer's machine with a live SWGEmu client:
    1. Confirm the concrete utinni.log path (getPath()+"utinni.log", UtinniCore/utility/log.cpp:145-147 — next to the injecting host process). Note the path in the SUMMARY.
    2. Inject Utinni + TJT (do NOT disable TJT — the scene-change trigger is TJT-driven via addCreateCommandParserCallback; disabling TJT loses the repro path).
    3. Trigger the TJT-driven scene change and press Return to skip the intro cinematic on the intro→login transition (the known trigger). Expected: dump-less crash, no SWG stage dump, no WER event.
    4. Read the `VEH FATAL: code=... EIP=... module=... base=... rva=... ESP=... [READ|WRITE|EXEC] target=...` line from utinni.log. Capture ALL fields verbatim.
    Baseline reminder (NOT a crash signal): landing naked after the TJT scene change is the expected baseline (project_tjt_scene_change_naked_baseline). "Naked, but in world" = success, not a fault.
    If the crash is intermittent (A5), repeat until a VEH FATAL line is captured (VEH is already deployed, so cost is low).
  </how-to-verify>
  <resume-signal>Paste the captured `VEH FATAL` line(s) verbatim (code/EIP/module/base/rva/ESP/access-target), and the confirmed utinni.log path. If no crash reproduces after several attempts, report that (A5) so disposition can adjust.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Resolve module+RVA and author the root-cause analysis</name>
  <files>.planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-RESID-02-RCA.md</files>
  <read_first>
    - UtinniCore/utinni.cpp (the VEH handler + module-handle setup — to interpret the captured fields; already deployed, read-only per D-06)
    - UtinniCore/swg/scene/ground_scene.cpp (the scene-change callback path + the dispatchSnapshot template — check the snapshot-dispatch migration status FIRST per project_rh_snapshot_no_heap_alloc)
    - .planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-PATTERNS.md (§ "RESID-02 fix surface" — the two-branch disposition + prior-art memories)
  </read_first>
  <action>
    Resolve the captured `module` + `rva`. If module=UtinniCore.dll → the fault is in Utinni's own surface (D-11 branch 1): map the rva to the source site (the detour/callback/dispatch function), and check the scene-change callback path's snapshot-dispatch migration status FIRST — the intro-skip crash may be the same per-frame-heap-alloc class as the prior 0x0051fb0a crash (project_rh_snapshot_no_heap_alloc). If module=SWGEmu.exe or `<unmapped-EIP>` (wild jump) → match the rva against known SWG RVAs / Utinni hook addresses and the access type (READ/WRITE/EXEC + target); this is likely branch 2 (game-side). Author .planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-RESID-02-RCA.md recording: the verbatim VEH FATAL line(s), the resolved module+rva→source-site mapping, the mechanism (what the faulting code was doing at the scene transition), and the disposition decision (branch 1 Utinni-fix vs branch 2 documented-game-side-RCA).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$p = '.planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-RESID-02-RCA.md'; if (!(Test-Path $p)) { exit 1 }; $r = Get-Content $p -Raw; if ($r -notmatch 'VEH FATAL') { Write-Error 'RCA missing captured VEH FATAL line'; exit 1 }; if ($r -notmatch 'rva' -or $r -notmatch '(module=|module )') { Write-Error 'RCA missing module/rva resolution'; exit 1 }; if ($r -notmatch '(branch 1|branch 2|Utinni-side|game-side)') { Write-Error 'RCA missing D-11 disposition'; exit 1 }; exit 0"</automated>
  </verify>
  <done>12-RESID-02-RCA.md records the verbatim VEH FATAL line, the module+rva→source-site mapping + mechanism, and an explicit D-11 branch-1-or-branch-2 disposition.</done>
</task>

<task type="auto">
  <name>Task 3: Apply the root-cause fix (branch 1) or finalize the documented RCA (branch 2)</name>
  <files>UtinniCore/ (the faulting detour/callback/dispatch source, branch 1), .planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-RESID-02-RCA.md (branch 2 finalization)</files>
  <read_first>
    - .planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-RESID-02-RCA.md (the disposition from Task 2)
    - UtinniCore/swg/scene/ground_scene.cpp (the detour-table pattern + dispatchSnapshot stack-allocated snapshot, if branch 1)
    - UtinniCore/utinni.cpp (confirm the VEH logger stays deployed — do NOT remove it, D-11)
  </read_first>
  <action>
    Branch 1 (fault in Utinni's surface): apply a full root-cause fix — NO masking guard / detour shortcut (D-11). If it is the per-frame-heap-alloc class (project_rh_snapshot_no_heap_alloc), migrate the offending scene-change callback dispatch to the stack-allocated fixed-size snapshot pattern (dispatchSnapshot, ground_scene.cpp). If it is a detour/RVA issue, use the single-sourced typed-function-pointer detour-table pattern with DETOUR_LEN_AUTO (CON-H-03, feedback_detourxs_explicit_len). Build UtinniCore (Debug|x86) green. Branch 2 (game-side, Utinni does not own the code): no code fix — finalize 12-RESID-02-RCA.md as the deliverable (module+rva+mechanism + why it cannot be Utinni-fixed); the live-session success criterion is met by analysis (accepted per D-11). EITHER way: confirm the VEH logger remains deployed in utinni.cpp (D-11). The re-run-repro confirmation is the live Tier-4 manual smoke in Task 4.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (!(Select-String -Path 'UtinniCore/utinni.cpp' -Pattern 'AddVectoredExceptionHandler\(1, utinniBreakpointVEH' -Quiet)) { Write-Error 'VEH logger removed — must stay deployed (D-11)'; exit 1 }; & 'D:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe' 'Utinni.sln' /m /p:Configuration=Debug /p:Platform=x86 /t:UtinniCore /nologo /v:minimal; if ($LASTEXITCODE -ne 0) { exit 1 }; exit 0"</automated>
  </verify>
  <done>Branch 1: a root-cause Utinni-side fix is committed (no masking guard), UtinniCore builds green Debug|x86, VEH logger still deployed. Branch 2: 12-RESID-02-RCA.md is finalized as the documented game-side deliverable, VEH logger still deployed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live re-run confirmation (branch 1) or RCA acceptance (branch 2)</name>
  <files>(no files modified — live Tier-4 confirmation smoke)</files>
  <action>Pause for the maintainer to confirm the disposition: branch 1 re-run the live repro to confirm no crash; branch 2 accept the documented RCA. See <what-built>/<how-to-verify>/<resume-signal>.</action>
  <verify>
    <human-check>Branch 1: the previously-crashing TJT scene-change → Return-skip intro path produces NO VEH FATAL line and the session continues ("naked, but in world" = success). Branch 2: the documented game-side RCA (module+rva+mechanism) is accepted as the deliverable.</human-check>
  </verify>
  <done>Maintainer confirms intro-skip no longer crashes (branch 1) or accepts the documented RCA (branch 2); the VEH logger remains deployed.</done>
  <what-built>
    The branch-1 root-cause fix (or branch-2 documented RCA). For branch 1: re-run the live repro to confirm the intro-skip path no longer crashes.
  </what-built>
  <how-to-verify>
    On the maintainer's live SWGEmu machine (Tier-4 manual smoke — the VEH logger is the invented harness, TEST-04 / max-harness):
    - Branch 1: inject the rebuilt UtinniCore + TJT, trigger the TJT scene change → Return-skip intro path that previously crashed. Confirm NO VEH FATAL line and the session continues. "Naked, but in world" after the scene change = SUCCESS (expected baseline, project_tjt_scene_change_naked_baseline), NOT a crash. Optionally confirm utinni.log shows the clean scene-transition chain.
    - Branch 2: no live re-run needed for a code fix; confirm the 12-RESID-02-RCA.md documented root-cause (module+rva+mechanism) is accepted as the deliverable.
  </how-to-verify>
  <resume-signal>Type "approved" (branch 1: intro-skip no longer crashes / branch 2: documented RCA accepted), or describe a remaining crash (paste the new VEH FATAL line) so the fix can iterate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Injected UtinniCore.dll ↔ SWGEmu.exe address space | Detour/callback surface; a faulting write/exec here is the RESID-02 surface (memory-safety of any Utinni-side fix) |
| VEH handler → utinni.log on disk | Observation-only (EXCEPTION_CONTINUE_SEARCH); flush_on(info) gets the line to disk before the process dies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 | Tampering (memory-safety) | A Utinni-side fix in the scene-change detour/callback dispatch (branch 1) | mitigate | Root-cause fix only (no masking guard, D-11); prefer the stack-allocated fixed-size snapshot (dispatchSnapshot) over per-frame heap alloc; single-sourced RVAs + DETOUR_LEN_AUTO (CON-H-03, feedback_detourxs_explicit_len) to avoid corrupting the target prologue |
| T-12-11 | Denial of Service | The dump-less crash itself (SWG's SEH bypassed → no WER event) | accept | This phase diagnoses + fixes (branch 1) or documents (branch 2) it; the VEH logger stays deployed as the standing observation harness (D-11) |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs this plan — pure native diagnosis/fix in UtinniCore |
</threat_model>

<verification>
- VEH FATAL line captured (module+rva) from a live TJT-driven intro-skip repro.
- 12-RESID-02-RCA.md records the line + module+rva→source mapping + mechanism + D-11 disposition.
- Branch 1: root-cause Utinni-side fix committed (no masking guard), UtinniCore builds Debug|x86 green, repro no longer crashes. Branch 2: documented game-side RCA accepted.
- VEH logger remains deployed (D-11).
</verification>

<success_criteria>
- ROADMAP Phase 12 success criterion #5 (intro-skip crash diagnosed to a faulting address/module via the VEH logger and fixed — or documented if game-side; intro-skip path no longer crashes on a live injected session, with "naked but in world" recognized as the baseline).
</success_criteria>

<output>
Create `.planning/phases/12-revive-feasibility-spike-hard-gate-intro-skip-crash/12-04-SUMMARY.md` when done
</output>
