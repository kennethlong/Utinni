---
phase: 14-headless-mcp-server-utinni-mcp-the-centerpiece
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Utinni.Mcp/Utinni.Mcp.csproj
  - Utinni.Mcp/Program.cs
  - Utinni.Mcp/Server/ResolvedRoot.cs
  - Utinni.Mcp/Server/CliDispatcher.cs
  - Utinni.Mcp/Server/CliLocator.cs
  - Utinni.Mcp/Server/CliInvocationResult.cs
  - UtinniCoreDotNet.PathContainment/UtinniCoreDotNet.PathContainment.csproj
  - UtinniCoreDotNet.PathContainment/LooseOverridePath.cs
  - UtinniCoreDotNet/Saving/LooseOverridePath.cs
  - Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj
  - Utinni.Mcp.Tests/ResolvedRootTests.cs
  - Utinni.Mcp.Tests/DispatcherTests.cs
  - Utinni.sln
  - .github/workflows/ci.yml
autonomous: false
requirements: [MCP-01, MCP-02]
user_setup: []

must_haves:
  truths:
    - "The net10 Utinni.Mcp project builds via `dotnet build` against the installed net10 SDK (10.0.300)."
    - "ResolvedRoot fails closed at startup when no --root arg and no UTINNI_MCP_ROOT env are set, or the path does not exist."
    - "A relative path containing `..`, a rooted path, a UNC path, a drive-relative path, or a prefix-attack sibling is rejected by ResolvedRoot.Resolve (SC3)."
    - "CliDispatcher spawns utinni-cli.exe, captures stdout/stderr/exit, and returns a hard-error result (not a hang) when the child exceeds the 60s timeout or the exe is missing."
    - "The net10 dotnet test lane is added to CI alongside the x86 MSBuild lane and does not force the net10 project through the Release|x86 MSBuild pass."
  artifacts:
    - path: "Utinni.Mcp/Utinni.Mcp.csproj"
      provides: "net10 SDK-style console project referencing ModelContextProtocol 1.4.0"
      contains: "net10.0"
    - path: "Utinni.Mcp/Server/ResolvedRoot.cs"
      provides: "fail-closed startup pin + per-call Resolve delegating to LooseOverridePath"
      contains: "PinOrThrow"
    - path: "Utinni.Mcp/Server/CliDispatcher.cs"
      provides: "subprocess seam with 60s timeout, stdin-close, kill-on-timeout"
      contains: "ArgumentList"
    - path: "UtinniCoreDotNet.PathContainment/LooseOverridePath.cs"
      provides: "netstandard2.0 single-source-of-truth path-containment helper"
      contains: "Resolve"
    - path: "Utinni.Mcp.Tests/ResolvedRootTests.cs"
      provides: "SC3 path-escape unit tests"
      contains: "ResolvedRootTests"
  key_links:
    - from: "Utinni.Mcp/Server/ResolvedRoot.cs"
      to: "UtinniCoreDotNet.PathContainment.LooseOverridePath.Resolve"
      via: "delegate call in per-call Resolve"
      pattern: "LooseOverridePath\\.Resolve"
    - from: "UtinniCoreDotNet/Saving/LooseOverridePath.cs"
      to: "UtinniCoreDotNet.PathContainment.LooseOverridePath"
      via: "type-forward or thin delegate (net472 keeps consuming via existing reference)"
      pattern: "PathContainment"
---

<objective>
Stand up the net10 `Utinni.Mcp` project foundation: the SDK-style csproj on `ModelContextProtocol` 1.4.0, the fail-closed `ResolvedRoot` startup pin, the `CliDispatcher` subprocess seam (60s timeout, mirroring Phase-13 `NativeToolRunner` one layer up), the `utinni-cli.exe` locator, and the **separate net10** `Utinni.Mcp.Tests` project carrying the Wave-0 unit tests (SC3 path-escape + dispatcher timeout/exe-missing). Resolve the path-containment cross-TFM fork by extracting `LooseOverridePath` into a `netstandard2.0` shared lib both TFMs reference. Add the net10 `dotnet test` CI lane.

Purpose: Every downstream tool plan (read tools, write tools, repack) builds on this foundation — the host bootstrap, the access-control boundary (`ResolvedRoot`), and the dispatch seam. Interface-first: this plan defines the contracts the Wave-2 tool plans consume.
Output: A buildable, testable net10 project skeleton with the two non-protocol contracts (`ResolvedRoot`, `CliDispatcher`) proven by unit tests, the shared netstandard2.0 containment lib, and a green net10 CI lane.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-RESEARCH.md
@.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-PATTERNS.md
@.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-VALIDATION.md

<interfaces>
<!-- Contracts this plan establishes for the Wave-2 tool plans. Extracted from the analogs. -->

The reused containment helper (UtinniCoreDotNet/Saving/LooseOverridePath.cs) — to be moved to netstandard2.0:
```csharp
// public static class LooseOverridePath
// public static string Resolve(string resolvedRoot, string relAssetPath);
//   throws ArgumentException on: empty args, rooted relAssetPath (C:\, D:foo, \unc, /abs),
//   any ".." segment, escape-after-canonicalization, prefix-match attack (swg-clientx vs swg-client).
```

The Phase-13 subprocess analog (Utinni.Cli/Commands/Subprocess/NativeToolRunner.cs):
```csharp
// private const int ToolTimeoutMs = 60000;
// NativeToolResult { bool ExeFound; bool TimedOut; int ExitCode; string Stdout; string Stderr; }
// File.Exists(toolExe) check returns ExeFound=false WITHOUT throwing.
// Start → StandardInput.Close() → ReadToEndAsync both streams → WaitForExit(60000) else Kill()+WaitForExit(5000).
```

Contracts this plan PUBLISHES (downstream plans depend on these signatures):
```csharp
// Utinni.Mcp/Server/CliInvocationResult.cs
public sealed class CliInvocationResult {
    public bool ExeFound;       // false → host maps to hard MCP error
    public bool TimedOut;       // true  → host maps to hard MCP error
    public int ExitCode;        // 0 ok / 1 usage / 2 domain / 3 notfound
    public string Stdout;       // the sorted-key JSON envelope on success/in-band-error
    public string Stderr;
    // factory helpers: ExeMissing(path), TimedOut(ms), Completed(exit, stdout, stderr)
}
// Utinni.Mcp/Server/ResolvedRoot.cs
public sealed class ResolvedRoot {
    public string Path { get; }
    public static ResolvedRoot PinOrThrow(string[] args);   // reads --root | UTINNI_MCP_ROOT, GetFullPath, must exist
    public string Resolve(string relativeAssetPath);         // delegates to LooseOverridePath.Resolve; throws on escape
}
// Utinni.Mcp/Server/CliDispatcher.cs
public sealed class CliDispatcher {
    public CliDispatcher(string cliExePath);
    public Task<CliInvocationResult> RunAsync(string verb, IReadOnlyList<string> args);  // 60s backstop
}
```
</interfaces>

@Utinni.Cli/Commands/Subprocess/NativeToolRunner.cs
@Utinni.Cli/Commands/Subprocess/NativeToolResolver.cs
@UtinniCoreDotNet/Saving/LooseOverridePath.cs
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Verify ModelContextProtocol 1.4.0 package legitimacy (T-14-SC supply-chain gate)</name>
  <files>(none — verification-only checkpoint; no files written. Task 1 adds the PackageReference after approval.)</files>
  <read_first>
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-RESEARCH.md (§Package Legitimacy Audit — slopcheck was unavailable; planner-mandated install gate)
  </read_first>
  <action>
    Mandatory pre-install legitimacy checkpoint for the FIRST NuGet install in this phase. NO files are written in this task. RESEARCH could not run slopcheck (no network pip), so the package id + version must be confirmed against nuget.org before Task 1 adds the PackageReference; the package is `[ASSUMED]` per fallback policy until confirmed. Present the verification steps to the maintainer and wait for explicit approval before proceeding to Task 1.
  </action>
  <what-built>
    Nothing — verification-only checkpoint gating the first NuGet install (Task 1 adds the PackageReference on approval).
  </what-built>
  <how-to-verify>
    1. Open https://www.nuget.org/packages/ModelContextProtocol/1.4.0
    2. Confirm the package id is exactly `ModelContextProtocol`, version `1.4.0`, published by the Microsoft/modelcontextprotocol org (source repo github.com/modelcontextprotocol/csharp-sdk).
    3. Confirm it targets `net10.0` (and net8.0/net9.0/netstandard2.0).
    4. Confirm `Microsoft.Extensions.Hosting` and `Microsoft.Extensions.Logging.Console` are first-party dotnet/runtime packages.
  </how-to-verify>
  <verify>Human confirms id+version+publisher against nuget.org (manual; this gate is never auto-approvable).</verify>
  <acceptance_criteria>
    Human confirms `ModelContextProtocol` `1.4.0` id+version+publisher against nuget.org. This checkpoint is NOT auto-approvable (legitimacy gate; `workflow.auto_advance` ignored). On approval, Task 1 may add the PackageReference.
  </acceptance_criteria>
  <done>Maintainer typed "approved" after confirming the id+version+publisher on nuget.org; Task 1 may proceed.</done>
  <resume-signal>Type "approved" once the id+version+publisher are confirmed on nuget.org, or describe the discrepancy.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Extract LooseOverridePath to a netstandard2.0 shared lib + scaffold the net10 Utinni.Mcp project</name>
  <files>UtinniCoreDotNet.PathContainment/UtinniCoreDotNet.PathContainment.csproj, UtinniCoreDotNet.PathContainment/LooseOverridePath.cs, UtinniCoreDotNet/Saving/LooseOverridePath.cs, Utinni.Mcp/Utinni.Mcp.csproj, Utinni.Mcp/Program.cs, Utinni.sln</files>
  <read_first>
    - UtinniCoreDotNet/Saving/LooseOverridePath.cs (the helper being moved; preserve every defense verbatim)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-RESEARCH.md (§Recommended Project Structure; §Pitfall 4 / A2 — the cross-TFM fork ruling)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-PATTERNS.md (Shared Pattern: Path Containment — option (a) chosen)
    - Utinni.sln (solution config mapping; the net10 project must NOT be added to the Release|x86 mapping)
  </read_first>
  <action>
    RULING ON FORK 2 (path-containment cross-TFM, RESEARCH Pitfall 4 / A2): adopt **option (a)** — single source of truth. Create a new `netstandard2.0` project `UtinniCoreDotNet.PathContainment` containing `LooseOverridePath` (move the class verbatim; namespace `UtinniCoreDotNet.Saving` preserved so existing net472 callers are untouched). Replace `UtinniCoreDotNet/Saving/LooseOverridePath.cs` with a type-forward (`[assembly: TypeForwardedTo(typeof(...))]`) OR a thin re-export, and add a ProjectReference from `UtinniCoreDotNet` to `UtinniCoreDotNet.PathContainment` so net472 keeps consuming the same type via the existing `UtinniCoreDotNet.dll` reference (do not change any net472 callsite). The net10 `Utinni.Mcp` references `UtinniCoreDotNet.PathContainment` directly (netstandard2.0 loads cleanly under net10 — no NU1701).

    Create the net10 SDK-style `Utinni.Mcp/Utinni.Mcp.csproj` with `<TargetFramework>net10.0</TargetFramework>`, `<OutputType>Exe</OutputType>`, and PackageReferences `ModelContextProtocol` `1.4.0`, `Microsoft.Extensions.Hosting`, `Microsoft.Extensions.Logging.Console` (per RESEARCH Installation block). Reference `UtinniCoreDotNet.PathContainment`. Create `Program.cs` as the generic-host bootstrap: `Host.CreateApplicationBuilder(args)`, logging to **stderr** (`LogToStandardErrorThreshold = LogLevel.Trace` — stdout is the MCP transport, Pitfall 2), `ResolvedRoot.PinOrThrow(args)` registered as a singleton BEFORE the server starts, `CliDispatcher` registered as a singleton (constructed with the located cli path via `CliLocator`), and `.AddMcpServer().WithStdioServerTransport().WithToolsFromAssembly()`. No tools exist yet — the assembly scan finds none; that is expected this wave.

    Add `Utinni.Mcp` + `UtinniCoreDotNet.PathContainment` to `Utinni.sln`. Per RESEARCH A3: do NOT map `Utinni.Mcp` into the `Release|x86` solution configuration in a way that forces the x86 MSBuild pass to compile it (a net10 project cannot target x86-only). Keep it buildable via `dotnet build Utinni.Mcp/Utinni.Mcp.csproj`.
  </action>
  <verify>
    <automated>dotnet build Utinni.Mcp/Utinni.Mcp.csproj</automated>
  </verify>
  <acceptance_criteria>`dotnet build Utinni.Mcp/Utinni.Mcp.csproj` exits 0; `UtinniCoreDotNet.PathContainment` (netstandard2.0) holds the single `LooseOverridePath`; `dotnet build` of the net472 UtinniCoreDotNet still succeeds with no callsite change.</acceptance_criteria>
  <done>The net10 project builds. `UtinniCoreDotNet.PathContainment` (netstandard2.0) holds the single `LooseOverridePath`; the net472 `UtinniCoreDotNet` build still resolves the same type with no callsite change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ResolvedRoot (fail-closed pin + Resolve) + CliDispatcher + CliLocator + Wave-0 unit tests</name>
  <files>Utinni.Mcp/Server/CliInvocationResult.cs, Utinni.Mcp/Server/ResolvedRoot.cs, Utinni.Mcp/Server/CliLocator.cs, Utinni.Mcp/Server/CliDispatcher.cs, Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj, Utinni.Mcp.Tests/ResolvedRootTests.cs, Utinni.Mcp.Tests/DispatcherTests.cs, Utinni.sln</files>
  <read_first>
    - Utinni.Cli/Commands/Subprocess/NativeToolRunner.cs (60000ms constant, stdin-close, WaitForExit-then-Kill, ExeFound-without-throw — copy the discipline)
    - Utinni.Cli/Commands/Subprocess/NativeToolResolver.cs (explicit-override → adjacent-probe → best-effort-fallback locator shape)
    - UtinniCoreDotNet.PathContainment/LooseOverridePath.cs (the containment defenses ResolvedRoot.Resolve delegates to)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-RESEARCH.md (§Code Examples: ResolvedRoot.PinOrThrow + CliDispatcher; §Validation Architecture tests 1 + 3)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-PATTERNS.md (CliDispatcher: net10 ArgumentList improvement, do NOT port the 65-line quoting algorithm)
  </read_first>
  <behavior>
    ResolvedRootTests (SC3 — each must throw ArgumentException; legit case resolves under root):
    - `../../etc` → rejected (`..` segment scan)
    - `C:\evil.iff` → rejected (rooted)
    - `\\unc\x` → rejected (rooted)
    - `D:foo` → rejected (drive-relative)
    - sibling prefix attack: root `...\swg-client`, rel resolving toward `...\swg-clientx\loot` → rejected (StartsWith with trailing separator)
    - `creature/path.iff` → resolves to an absolute path UNDER root
    - PinOrThrow with no --root and no UTINNI_MCP_ROOT → throws (fail-closed)
    - PinOrThrow with --root pointing at a non-existent dir → throws (fail-closed)
    DispatcherTests:
    - exe-missing: dispatcher pointed at a non-existent .exe returns a result with ExeFound=false WITHOUT throwing
    - non-zero exit: a stub child (`%ComSpec% /c exit 7`) yields ExitCode=7, TimedOut=false
    - timeout: a stub that never exits (`%ComSpec% /c pause` with stdin closed) returns TimedOut=true within ~60s and the child is killed
  </behavior>
  <action>
    Implement `Utinni.Mcp/Server/CliInvocationResult.cs` with the published shape (see `<interfaces>`) + the `ExeMissing`/`TimedOut`/`Completed` factories. Implement `ResolvedRoot.cs`: `PinOrThrow(args)` reads `--root` arg then `UTINNI_MCP_ROOT` env, fails closed (throws `InvalidOperationException` with a clear stderr-bound message) when neither is set or `Path.GetFullPath` / `Directory.Exists` fails; `Resolve(relativeAssetPath)` delegates to `LooseOverridePath.Resolve(Path, relativeAssetPath)`. Implement `CliLocator.cs` mirroring `NativeToolResolver`: explicit `--cli-path` arg / `UTINNI_CLI_PATH` env wins, else probe `utinni-cli.exe` adjacent to `AppContext.BaseDirectory` / a configured relative location, else return the canonical name so the dispatcher's `File.Exists` surfaces a clear not-found error. Drop the `_r/_d/_o` SOE suffix variants. Implement `CliDispatcher.cs` per the RESEARCH net10 example: `ProcessStartInfo` with `UseShellExecute=false`, `CreateNoWindow=true`, redirected stdin/stdout/stderr, `psi.ArgumentList.Add` per arg (net10 — NO manual quoting; do not port the 65-line CommandLineToArgvW algorithm), `StandardInput.Close()` after start, `ReadToEndAsync` both streams, `WaitForExitAsync` under a 60_000 ms `CancellationTokenSource`, `Kill(true)` + drain on cancellation. The 60s constant is the same backstop as Phase-13.

    Create the **separate net10** `Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj` (`net10.0`, `xunit` 2.9.3, `Microsoft.NET.Test.Sdk` 17.13.0, ProjectReference to `Utinni.Mcp` and `UtinniCoreDotNet.PathContainment`; it CANNOT be net472 — Pitfall 1). Add it to `Utinni.sln` (net10 lane, not the x86 pass). Write `ResolvedRootTests.cs` and `DispatcherTests.cs` per `<behavior>`. The dispatcher tests drive `%ComSpec%` (cmd.exe — always present) as the deterministic stub, mirroring `NativeToolRunnerTests`.
  </action>
  <verify>
    <automated>dotnet test Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj --filter "ResolvedRootTests|DispatcherTests"</automated>
  </verify>
  <acceptance_criteria>`dotnet test Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj --filter "ResolvedRootTests|DispatcherTests"` passes; the containment test runs against the SHIPPED netstandard2.0 `LooseOverridePath`.</acceptance_criteria>
  <done>`ResolvedRootTests` (SC3 path-escape + fail-closed pin) and `DispatcherTests` (exe-missing/non-zero/timeout) all pass on the net10 lane.</done>
</task>

<task type="auto">
  <name>Task 3: Add the net10 dotnet-test CI lane</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the existing x86 MSBuild + net472 dotnet-test lanes; add the net10 lane after them, before artifact-upload-on-failure)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-RESEARCH.md (§D-02 CI lane note + §Environment Availability — net10 SDK 10.0.300 + runtime 10.0.8 verified on the self-hosted runner)
  </read_first>
  <action>
    Add two PowerShell `run:` steps to `ci.yml` (5.1-safe syntax, same self-hosted runner): (1) `dotnet build Utinni.Mcp/Utinni.Mcp.csproj -c Release` (net10 — builds against the installed 10.0.300 SDK, NOT through the `Release|x86` MSBuild pass per A3); (2) `dotnet test Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj -c Release --logger "trx;LogFileName=mcp-test-results.trx"`. Add a matching `upload-artifact` on-failure step for `mcp-test-results.trx`. Do not alter the existing x86/native lanes. Update the job `name:` to note the added net10 lane.
  </action>
  <verify>
    <automated>dotnet test Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>The net10 build + test steps are present in ci.yml after the net472 lanes; the net10 project does not enter the x86 MSBuild pass; local `dotnet test -c Release` of the MCP test project is green.</acceptance_criteria>
  <done>The net10 build + test steps are present in ci.yml after the net472 lanes; the net10 project does not enter the x86 MSBuild pass. Local `dotnet test -c Release` of the MCP test project is green (the CI step is a direct mirror of the locally-verified command).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| `--root` arg / `UTINNI_MCP_ROOT` env → ResolvedRoot.PinOrThrow | Server-launch config supplied by the trusted MCP client; canonicalized once, fail-closed. | filesystem path |
| relative tool path → ResolvedRoot.Resolve | Agent-supplied relative asset path; the access-control boundary. | composed filesystem path |
| host → Process.Start(utinni-cli.exe) | net10 host spawns the net472/x86 CLI; the honest two-process seam. | argv + captured stdout |
| NuGet restore → build | First package install in the phase (supply-chain). | package bytes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Elevation/Tampering | ResolvedRoot.Resolve | mitigate | Delegates to the netstandard2.0 LooseOverridePath (rooted/`..`/canonicalization/prefix-attack defenses); SC3 unit test in this plan proves each rejection. |
| T-14-04 | Elevation | ResolvedRoot.PinOrThrow | mitigate | Fail-closed: no root configured → server refuses to start (throws before the transport opens); no absolute path ever accepted from the agent. |
| T-14-05 | DoS | CliDispatcher | mitigate | 60s WaitForExitAsync + Kill + stdin-close; ExeFound=false without throw → host maps to hard error (DispatcherTests prove timeout + exe-missing). |
| T-14-06 | Tampering | CliDispatcher argv | mitigate | UseShellExecute=false + ProcessStartInfo.ArgumentList (per-arg, no shell, no concatenation). |
| T-14-SC | Tampering | NuGet install of ModelContextProtocol 1.4.0 | mitigate | Task 0 blocking-human checkpoint confirms id+version+publisher against nuget.org (slopcheck unavailable in research). Consolidated in MCP-SECURITY.md (Plan 04). |
</threat_model>

<verification>
- `dotnet build Utinni.Mcp/Utinni.Mcp.csproj` succeeds (net10, SDK 10.0.300).
- `dotnet build UtinniCoreDotNet/UtinniCoreDotNet.csproj` (or the net472 build) still succeeds — the LooseOverridePath move did not break net472 callers.
- `dotnet test Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj` green (ResolvedRootTests + DispatcherTests).
- ci.yml contains the net10 build + test steps and does not route the net10 project through Release|x86.
</verification>

<success_criteria>
- net10 `Utinni.Mcp` project builds; generic-host bootstrap logs to stderr.
- `ResolvedRoot` fails closed and rejects every SC3 escape case; legit relative paths resolve under root.
- `CliDispatcher` returns hard-error results (no hang) on timeout and exe-missing.
- `LooseOverridePath` is single-sourced in netstandard2.0; both TFMs consume it.
- net10 CI lane added.
</success_criteria>

<output>
Create `.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-01-SUMMARY.md` when done.
</output>
