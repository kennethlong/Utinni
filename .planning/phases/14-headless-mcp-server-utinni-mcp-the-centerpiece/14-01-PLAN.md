---
phase: 14-headless-mcp-server-utinni-mcp-the-centerpiece
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Utinni.Mcp/Utinni.Mcp.csproj
  - Utinni.Mcp/Program.cs
  - Utinni.Mcp/Server/ResolvedRoot.cs
  - Utinni.Mcp/Server/CliDispatcher.cs
  - Utinni.Mcp/Server/CliLocator.cs
  - Utinni.Mcp/Server/CliInvocationResult.cs
  - Utinni.Mcp/Server/ServerArgs.cs
  - UtinniCoreDotNet.PathContainment/UtinniCoreDotNet.PathContainment.csproj
  - UtinniCoreDotNet.PathContainment/LooseOverridePath.cs
  - UtinniCoreDotNet/Saving/LooseOverridePath.cs
  - UtinniCoreDotNet/UtinniCoreDotNet.csproj
  - Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj
  - Utinni.Mcp.Tests/ResolvedRootTests.cs
  - Utinni.Mcp.Tests/ServerArgsTests.cs
  - Utinni.Mcp.Tests/DispatcherTests.cs
  - Utinni.Mcp.Tests/CliLocatorTests.cs
  - Utinni.sln
  - .github/workflows/ci.yml

autonomous: false
requirements: [MCP-01, MCP-02]
user_setup: []

must_haves:
  truths:
    - "The net10 Utinni.Mcp project builds via `dotnet build` against the installed net10 SDK (10.0.300)."
    - "ResolvedRoot fails closed at startup when no --root arg and no UTINNI_MCP_ROOT env are set, or the path does not exist."
    - "A relative path containing `..`, a rooted path, a UNC path, a drive-relative path, or a prefix-attack sibling is rejected by ResolvedRoot.Resolve (SC3)."
    - "LooseOverridePath is single-sourced in netstandard2.0 and the original UtinniCoreDotNet location forwards to it via [assembly: TypeForwardedTo] (NOT a wrapper) so compiled net472 plugins keep the same type identity; the existing UtinniCoreDotNet.Tests LooseOverridePathTests still pass."
    - "CliDispatcher spawns utinni-cli.exe (reading both stdout AND stderr asynchronously before WaitForExit, no pipe deadlock), captures exit, and returns a hard-error result (not a hang) when the child exceeds the injected timeout (default 60s) or the exe is missing."
    - "The dispatcher timeout is injectable via a constructor TimeSpan overload so tests use a sub-second timeout and production defaults to 60s."
    - "CliLocator resolves utinni-cli.exe deterministically: explicit --cli-path/UTINNI_CLI_PATH wins, else an absolute adjacent-probe under AppContext.BaseDirectory; it never depends on the current working directory."
    - "The net10 dotnet test lane is added to CI alongside the x86 MSBuild lane and does not force the net10 project through the Release|x86 MSBuild pass."
  artifacts:
    - path: "Utinni.Mcp/Utinni.Mcp.csproj"
      provides: "net10 SDK-style console project referencing ModelContextProtocol 1.4.0"
      contains: "net10.0"
    - path: "Utinni.Mcp/Server/ResolvedRoot.cs"
      provides: "fail-closed startup pin + per-call Resolve delegating to LooseOverridePath"
      contains: "PinOrThrow"
    - path: "Utinni.Mcp/Server/CliDispatcher.cs"
      provides: "subprocess seam with injectable timeout (default 60s), stdin-close, async-read-both-streams, kill-on-timeout"
      contains: "ArgumentList"
    - path: "UtinniCoreDotNet.PathContainment/LooseOverridePath.cs"
      provides: "netstandard2.0 single-source-of-truth path-containment helper"
      contains: "Resolve"
    - path: "UtinniCoreDotNet/Saving/LooseOverridePath.cs"
      provides: "TypeForwardedTo shim preserving net472 binary type identity"
      contains: "TypeForwardedTo"
    - path: "Utinni.Mcp.Tests/ResolvedRootTests.cs"
      provides: "SC3 path-escape unit tests"
      contains: "ResolvedRootTests"
  key_links:
    - from: "Utinni.Mcp/Server/ResolvedRoot.cs"
      to: "UtinniCoreDotNet.Saving.LooseOverridePath.Resolve"
      via: "delegate call in per-call Resolve"
      pattern: "LooseOverridePath\\.Resolve"
    - from: "UtinniCoreDotNet/Saving/LooseOverridePath.cs"
      to: "UtinniCoreDotNet.PathContainment LooseOverridePath"
      via: "[assembly: TypeForwardedTo] (binary identity, NOT a re-export wrapper)"
      pattern: "TypeForwardedTo"
---

<objective>
Stand up the net10 `Utinni.Mcp` project foundation: the SDK-style csproj on `ModelContextProtocol` 1.4.0, the fail-closed `ResolvedRoot` startup pin, the `CliDispatcher` subprocess seam (injectable timeout defaulting to 60s, mirroring Phase-13 `NativeToolRunner` one layer up, reading both streams async to avoid pipe deadlock), the deterministic `utinni-cli.exe` locator, and the **separate net10** `Utinni.Mcp.Tests` project carrying the Wave-0 unit tests (SC3 path-escape + arg-parsing + dispatcher timeout/exe-missing + locator). Resolve the path-containment cross-TFM fork by extracting `LooseOverridePath` into a `netstandard2.0` shared lib and forwarding the original net472 location via `[assembly: TypeForwardedTo]` (binary identity preserved). Add the net10 `dotnet test` CI lane.

Purpose: Every downstream tool plan (read tools, write tools, repack) builds on this foundation — the host bootstrap, the access-control boundary (`ResolvedRoot`), and the dispatch seam. Interface-first: this plan defines the contracts the Wave-2/3 plans consume.
Output: A buildable, testable net10 project skeleton with the non-protocol contracts (`ResolvedRoot`, `CliDispatcher`, `CliLocator`, `ServerArgs`) proven by unit tests, the shared netstandard2.0 containment lib with the net472-binary-identity regression gate, and a green net10 CI lane.

REVIEW-DRIVEN CHANGES (cross-AI `--reviews`, both reviewers MEDIUM on 14-01):
- Consensus #5: MANDATE `[assembly: TypeForwardedTo]` (forbid thin re-export — it breaks type identity for compiled net472 plugins). Add the `LooseOverridePathTests` regression gate to THIS plan's verification (was only in 14-04). Add a deployment/runtime-load test proving net472 callers load `LooseOverridePath` and the new `UtinniCoreDotNet.PathContainment.dll` is copied alongside the editor/plugin output.
- Divergent (Codex): make the dispatcher timeout INJECTABLE (constructor `TimeSpan` overload); default production 60s.
- Divergent (Codex+Cursor): the `%ComSpec% /c pause` timeout stub is likely INVALID (terminates on closed stdin) AND slow — replace with a deterministic long-runner (PowerShell `Start-Sleep`) AND gate the real-60s-path test behind `[Trait("Category","Slow")]`; the injected-timeout tests use a sub-second timeout so the fast suite never waits.
- Divergent (Codex): specify + test `PinOrThrow`/CLI arg parsing (`--root=`, missing value, duplicate flags, quoted paths, env-fallback precedence) — extract a small tested `ServerArgs` parser.
- Consensus #11 (Codex): `CliLocator` must resolve a deterministic ABSOLUTE path (AppContext.BaseDirectory-relative), never `File.Exists(canonical-name)` which depends on CWD and won't PATH-probe.
- Cursor LOW: fix the inherited stdout-pipe-deadlock ordering — start BOTH async reads before WaitForExit.
- Codex LOW: assert no host log reaches stdout before MCP framing (logging-to-stderr is structural; the round-trip test in 14-04 proves the channel is clean).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-RESEARCH.md
@.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-PATTERNS.md
@.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-REVIEWS.md
@.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-VALIDATION.md

<interfaces>
<!-- Contracts this plan establishes for the Wave-2/3 tool plans. Extracted from the analogs. -->

The reused containment helper (UtinniCoreDotNet/Saving/LooseOverridePath.cs) — moved to netstandard2.0:
```
// public static class LooseOverridePath  (namespace UtinniCoreDotNet.Saving — unchanged)
// public static string Resolve(string resolvedRoot, string relAssetPath);
//   throws ArgumentException/ArgumentNullException on: null/empty args, rooted relAssetPath
//   (C:\, D:foo, \unc, /abs), any ".." segment, escape-after-canonicalization, prefix-match
//   attack (swg-clientx vs swg-client). Canonicalizes the root once defensively.
```

The Phase-13 subprocess analog (Utinni.Cli/Commands/Subprocess/NativeToolRunner.cs):
```
// private const int ToolTimeoutMs = 60000;
// File.Exists(toolExe) returns ExeFound=false WITHOUT throwing.
// Start -> StandardInput.Close() -> ReadToEndAsync BOTH streams -> WaitForExit(timeout) else Kill()+WaitForExit(5000).
```

Contracts this plan PUBLISHES (downstream plans depend on these signatures):
```
// Utinni.Mcp/Server/CliInvocationResult.cs
public sealed class CliInvocationResult {
    public bool ExeFound;       // false -> host maps to hard MCP error
    public bool TimedOut;       // true  -> host maps to hard MCP error
    public int ExitCode;        // 0 ok / 1 usage / 2 domain / 3 notfound
    public string Stdout;       // the sorted-key JSON envelope on success/in-band-error
    public string Stderr;
    // factories: ExeMissing(path), TimedOut(timeout), Completed(exit, stdout, stderr)
}
// Utinni.Mcp/Server/ServerArgs.cs  (small tested parser)
public sealed class ServerArgs {
    public string Root;         // --root <v> | --root=<v>  (first wins; null if absent)
    public string CliPath;      // --cli-path <v> | --cli-path=<v>
    public static ServerArgs Parse(string[] args);   // tolerates =form, quoted values; ignores duplicates after first
}
// Utinni.Mcp/Server/ResolvedRoot.cs
public sealed class ResolvedRoot {
    public string Path { get; }
    public static ResolvedRoot PinOrThrow(string[] args);   // ServerArgs.Root ?? UTINNI_MCP_ROOT; GetFullPath; must exist
    public string Resolve(string relativeAssetPath);        // delegates to LooseOverridePath.Resolve; throws on escape
}
// Utinni.Mcp/Server/CliLocator.cs
public static class CliLocator {
    public static string Resolve(string cliPathOverride);   // override | UTINNI_CLI_PATH | absolute AppContext.BaseDirectory probe
}
// Utinni.Mcp/Server/CliDispatcher.cs
public sealed class CliDispatcher {
    public CliDispatcher(string cliExePath);                // default 60s timeout
    public CliDispatcher(string cliExePath, TimeSpan timeout); // INJECTABLE timeout for tests
    public Task<CliInvocationResult> RunAsync(string verb, IReadOnlyList<string> args);
}
```
</interfaces>

@Utinni.Cli/Commands/Subprocess/NativeToolRunner.cs
@Utinni.Cli/Commands/Subprocess/NativeToolResolver.cs
@UtinniCoreDotNet/Saving/LooseOverridePath.cs
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Verify ModelContextProtocol 1.4.0 package legitimacy (T-14-SC supply-chain gate)</name>
  <files>(none — verification-only checkpoint; no files written. Task 1 adds the PackageReference after approval.)</files>
  <read_first>
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-RESEARCH.md (§Package Legitimacy Audit — slopcheck was unavailable; planner-mandated install gate)
  </read_first>
  <action>
    Mandatory pre-install legitimacy checkpoint for the FIRST NuGet install in this phase. NO files are written in this task. RESEARCH could not run slopcheck (no network pip), so the package id + version must be confirmed against nuget.org before Task 1 adds the PackageReference; the package is `[ASSUMED]` per fallback policy until confirmed. Present the verification steps to the maintainer and wait for explicit approval before proceeding to Task 1. Divergent review note (Codex): the supply-chain gate is procedural-only today — STRENGTHEN it by having Task 1 commit a NuGet lock file (`packages.lock.json` via `RestorePackagesWithLockFile`) for `Utinni.Mcp`/`Utinni.Mcp.Tests` so the approved versions are pinned and CI restore is reproducible; note this in the approval so the maintainer knows the lock file lands with the install.
  </action>
  <what-built>
    Nothing — verification-only checkpoint gating the first NuGet install (Task 1 adds the PackageReference + lock file on approval).
  </what-built>
  <how-to-verify>
    1. Open https://www.nuget.org/packages/ModelContextProtocol/1.4.0
    2. Confirm the package id is exactly `ModelContextProtocol`, version `1.4.0`, published by the Microsoft/modelcontextprotocol org (source repo github.com/modelcontextprotocol/csharp-sdk).
    3. Confirm it targets `net10.0` (and net8.0/net9.0/netstandard2.0).
    4. Confirm `Microsoft.Extensions.Hosting` and `Microsoft.Extensions.Logging.Console` are first-party dotnet/runtime packages.
  </how-to-verify>
  <verify>Human confirms id+version+publisher against nuget.org (manual; this gate is never auto-approvable).</verify>
  <acceptance_criteria>
    Human confirms `ModelContextProtocol` `1.4.0` id+version+publisher against nuget.org. This checkpoint is NOT auto-approvable (legitimacy gate; `workflow.auto_advance` ignored). On approval, Task 1 may add the PackageReference + commit the NuGet lock file.
  </acceptance_criteria>
  <done>Maintainer typed "approved" after confirming the id+version+publisher on nuget.org; Task 1 may proceed.</done>
  <resume-signal>Type "approved" once the id+version+publisher are confirmed on nuget.org, or describe the discrepancy.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Extract LooseOverridePath to netstandard2.0 + TypeForwardedTo shim + scaffold the net10 Utinni.Mcp project</name>
  <files>UtinniCoreDotNet.PathContainment/UtinniCoreDotNet.PathContainment.csproj, UtinniCoreDotNet.PathContainment/LooseOverridePath.cs, UtinniCoreDotNet/Saving/LooseOverridePath.cs, UtinniCoreDotNet/UtinniCoreDotNet.csproj, Utinni.Mcp/Utinni.Mcp.csproj, Utinni.Mcp/Program.cs, Utinni.sln</files>
  <read_first>
    - UtinniCoreDotNet/Saving/LooseOverridePath.cs (the helper being moved; preserve every defense verbatim — it is the SC3 contract)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-REVIEWS.md (Consensus #5 — TypeForwardedTo mandated, re-export forbidden; Codex deployment/runtime-load test)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-RESEARCH.md (§Recommended Project Structure; §Pitfall 4 / A2)
    - Utinni.sln (solution config mapping; the net10 project must NOT be added to the Release|x86 mapping)
  </read_first>
  <action>
    RULING ON FORK 2 (path-containment cross-TFM) — adopt option (a) single-source-of-truth WITH binary type identity per review Consensus #5: Create a new `netstandard2.0` project `UtinniCoreDotNet.PathContainment` containing `LooseOverridePath` (move the class verbatim; keep namespace `UtinniCoreDotNet.Saving` so net472 callers are source-unchanged). Replace `UtinniCoreDotNet/Saving/LooseOverridePath.cs` with a file whose ONLY content is `[assembly: System.Runtime.CompilerServices.TypeForwardedTo(typeof(UtinniCoreDotNet.Saving.LooseOverridePath))]` — NOT a wrapper/re-export class (a re-export creates a DISTINCT type and breaks binary identity for already-compiled net472 plugins referencing `UtinniCoreDotNet, UtinniCoreDotNet.Saving.LooseOverridePath`). Add a ProjectReference from `UtinniCoreDotNet` to `UtinniCoreDotNet.PathContainment` so the net472 assembly carries the forward and the netstandard2.0 DLL is copied to net472 output. Do NOT change any net472 callsite.

    Create the net10 SDK-style `Utinni.Mcp/Utinni.Mcp.csproj` with `<TargetFramework>net10.0</TargetFramework>`, `<OutputType>Exe</OutputType>`, `<RestorePackagesWithLockFile>true</RestorePackagesWithLockFile>` (supply-chain pin per Task 0), and PackageReferences `ModelContextProtocol` `1.4.0`, `Microsoft.Extensions.Hosting`, `Microsoft.Extensions.Logging.Console`. Reference `UtinniCoreDotNet.PathContainment` (netstandard2.0 loads cleanly under net10 — no NU1701). Create `Program.cs` as the generic-host bootstrap: `Host.CreateApplicationBuilder(args)`, logging to **stderr** (`LogToStandardErrorThreshold = LogLevel.Trace` — stdout is the MCP transport, Pitfall 2), `ResolvedRoot.PinOrThrow(args)` registered as a singleton BEFORE the server starts, `CliDispatcher` registered as a singleton (constructed with the located cli path via `CliLocator.Resolve(ServerArgs.Parse(args).CliPath)`), and `.AddMcpServer().WithStdioServerTransport().WithToolsFromAssembly()`. No tools exist yet — the assembly scan finds none; that is expected this wave.

    Add `Utinni.Mcp` + `UtinniCoreDotNet.PathContainment` to `Utinni.sln`. Per RESEARCH A3: do NOT map `Utinni.Mcp` into the `Release|x86` solution configuration in a way that forces the x86 MSBuild pass to compile it (a net10 project cannot target x86-only). `UtinniCoreDotNet.PathContainment` (netstandard2.0) DOES build under the x86 MSBuild pass as a dependency of net472 `UtinniCoreDotNet` — verify it maps to a valid (AnyCPU) config that the existing solution build tolerates.
  </action>
  <verify>
    <automated>dotnet build Utinni.Mcp/Utinni.Mcp.csproj</automated>
  </verify>
  <acceptance_criteria>`dotnet build Utinni.Mcp/Utinni.Mcp.csproj` exits 0; `UtinniCoreDotNet/Saving/LooseOverridePath.cs` contains `TypeForwardedTo` and NO class definition; `UtinniCoreDotNet.PathContainment` (netstandard2.0) holds the single `LooseOverridePath`; the net472 `UtinniCoreDotNet` build still succeeds with no callsite change.</acceptance_criteria>
  <done>The net10 project builds. `LooseOverridePath` lives once in netstandard2.0; the net472 location is a `[TypeForwardedTo]` shim (binary identity preserved); the net472 `UtinniCoreDotNet` build resolves the same type with no callsite change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ServerArgs + ResolvedRoot + CliLocator + CliDispatcher (injectable timeout) + Wave-0 unit tests + net472 forward regression gate</name>
  <files>Utinni.Mcp/Server/CliInvocationResult.cs, Utinni.Mcp/Server/ServerArgs.cs, Utinni.Mcp/Server/ResolvedRoot.cs, Utinni.Mcp/Server/CliLocator.cs, Utinni.Mcp/Server/CliDispatcher.cs, Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj, Utinni.Mcp.Tests/ResolvedRootTests.cs, Utinni.Mcp.Tests/ServerArgsTests.cs, Utinni.Mcp.Tests/DispatcherTests.cs, Utinni.Mcp.Tests/CliLocatorTests.cs, Utinni.sln</files>
  <read_first>
    - Utinni.Cli/Commands/Subprocess/NativeToolRunner.cs (60000ms constant, stdin-close, ReadToEndAsync BOTH streams BEFORE WaitForExit, WaitForExit-then-Kill, ExeFound-without-throw — copy the discipline)
    - Utinni.Cli/Commands/Subprocess/NativeToolResolver.cs (explicit-override -> adjacent-probe locator shape; the analog returns a CWD-relative canonical name — DO NOT copy that; resolve absolute)
    - UtinniCoreDotNet.PathContainment/LooseOverridePath.cs (the containment defenses ResolvedRoot.Resolve delegates to)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-REVIEWS.md (Codex: injectable timeout, PinOrThrow arg-parsing cases, CliLocator File.Exists CWD bug; Cursor: pipe-deadlock ordering, timeout-stub slow)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-RESEARCH.md (§Code Examples; §Validation Architecture tests 1 + 3)
  </read_first>
  <behavior>
    ServerArgsTests:
    - `--root C:\x` -> Root="C:\x"
    - `--root=C:\x` -> Root="C:\x" (equals form)
    - `--root` with no following value -> Root=null (missing value tolerated, not crash)
    - duplicate `--root A --root B` -> Root="A" (first wins, documented)
    - quoted value `--root "C:\Program Files\x"` (already de-quoted by the OS argv) -> Root unchanged
    - `--cli-path` parsed symmetrically
    ResolvedRootTests (SC3 — each must throw ArgumentException; legit case resolves under root):
    - `../../etc` -> rejected (`..` segment scan)
    - `C:\evil.iff` -> rejected (rooted)
    - `\\unc\x` -> rejected (rooted)
    - `D:foo` -> rejected (drive-relative)
    - sibling prefix attack: root `...\swg-client`, rel resolving toward `...\swg-clientx\loot` -> rejected
    - `creature/path.iff` -> resolves to an absolute path UNDER root
    - PinOrThrow with no --root and no UTINNI_MCP_ROOT -> throws (fail-closed)
    - PinOrThrow with --root pointing at a non-existent dir -> throws (fail-closed)
    - PinOrThrow precedence: --root present AND UTINNI_MCP_ROOT set -> --root wins; env-only -> env used
    CliLocatorTests:
    - explicit override path returned verbatim
    - UTINNI_CLI_PATH env returned when no override
    - no override/env -> returns an ABSOLUTE path rooted at AppContext.BaseDirectory (assert Path.IsPathRooted(result) is true; assert result does NOT equal the bare "utinni-cli.exe")
    DispatcherTests:
    - exe-missing: dispatcher pointed at a non-existent .exe returns ExeFound=false WITHOUT throwing
    - non-zero exit: a stub child (`%ComSpec% /c exit 7`) yields ExitCode=7, TimedOut=false
    - injected-timeout [fast]: CliDispatcher(stub, TimeSpan.FromMilliseconds(500)) pointed at a deterministic long-runner (`powershell -NoProfile -Command Start-Sleep -Seconds 30`) returns TimedOut=true in well under 30s and the child is killed
    - [Trait("Category","Slow")] real-60s-default: default-ctor dispatcher against the long-runner returns TimedOut=true near 60s (gated; excluded from the fast suite)
    - large-stdout no-deadlock: a stub emitting > 100 KB to stdout completes without hanging (proves both streams are read async before WaitForExit)
  </behavior>
  <action>
    Implement `CliInvocationResult.cs` per `<interfaces>` + `ExeMissing`/`TimedOut`/`Completed` factories. Implement `ServerArgs.cs`: a small `Parse(string[])` handling `--flag value` and `--flag=value`, tolerating a missing value (leave null), and taking the FIRST occurrence on duplicates (documented). Implement `ResolvedRoot.cs`: `PinOrThrow(args)` uses `ServerArgs.Parse(args).Root ?? Environment.GetEnvironmentVariable("UTINNI_MCP_ROOT")`, fails closed (throws `InvalidOperationException` with a clear stderr-bound message) when neither is set or `Path.GetFullPath`/`Directory.Exists` fails; `Resolve(relativeAssetPath)` delegates to `LooseOverridePath.Resolve(Path, relativeAssetPath)`. Implement `CliLocator.cs`: explicit override / `UTINNI_CLI_PATH` env wins; ELSE probe `Path.Combine(AppContext.BaseDirectory, "utinni-cli.exe")` (and a configured adjacent relative location) and return that ABSOLUTE path even if missing (so the dispatcher's `File.Exists` surfaces a clear not-found) — NEVER return the bare CWD-relative name (Codex #11). Drop the `_r/_d/_o` SOE suffix variants. Implement `CliDispatcher.cs` with BOTH constructors (`(string)` -> 60s default; `(string, TimeSpan)` -> injectable): `ProcessStartInfo` with `UseShellExecute=false`, `CreateNoWindow=true`, redirected stdin/stdout/stderr, `psi.ArgumentList.Add` per arg (net10 — NO manual quoting; do not port the 65-line CommandLineToArgvW algorithm), `StandardInput.Close()` after start, then START both `ReadToEndAsync` tasks BEFORE awaiting `WaitForExitAsync` under a `CancellationTokenSource(timeout)` (Cursor pipe-deadlock fix), `Kill(true)` + drain on cancellation.

    Create the **separate net10** `Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj` (`net10.0`, `<RestorePackagesWithLockFile>true</RestorePackagesWithLockFile>`, `xunit` 2.9.3, `Microsoft.NET.Test.Sdk` 17.13.0, ProjectReference to `Utinni.Mcp` and `UtinniCoreDotNet.PathContainment`; it CANNOT be net472 — Pitfall 1). Add it to `Utinni.sln` (net10 lane, not the x86 pass). Write `ServerArgsTests.cs`, `ResolvedRootTests.cs`, `CliLocatorTests.cs`, `DispatcherTests.cs` per `<behavior>`. The dispatcher long-runner uses `powershell -NoProfile -Command Start-Sleep` (deterministic hang — NOT `%ComSpec% /c pause`, which terminates on closed stdin). Mark the real-60s-default test `[Trait("Category","Slow")]` so the fast suite skips it.
  </action>
  <verify>
    <automated>dotnet test Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj --filter "(ResolvedRootTests|ServerArgsTests|CliLocatorTests|DispatcherTests)&Category!=Slow"</automated>
  </verify>
  <acceptance_criteria>The filtered net10 unit suite passes (SC3 path-escape + fail-closed pin + arg-parsing edge cases + absolute CLI-locator + dispatcher exe-missing/non-zero/injected-timeout/large-stdout); the containment test runs against the SHIPPED netstandard2.0 `LooseOverridePath`. The Slow-trait 60s test is excluded from this fast run.</acceptance_criteria>
  <done>All Wave-0 unit tests pass on the net10 fast lane; the dispatcher proves injected-timeout-kill and large-stdout-no-deadlock; CliLocator returns an absolute path independent of CWD.</done>
</task>

<task type="auto">
  <name>Task 3: net472 binary-forward regression gate + net10 dotnet-test CI lane (incl. Slow nightly trait)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the existing x86 MSBuild + net472 dotnet-test lanes; add the net10 lane after them, before artifact-upload-on-failure)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-REVIEWS.md (Consensus #5: LooseOverridePathTests regression gate belongs in 14-01; Cursor LOW: gate the Slow timeout test off the per-push run)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-RESEARCH.md (§D-02 CI lane note + §Environment Availability — net10 SDK 10.0.300 + runtime 10.0.8 verified)
  </read_first>
  <action>
    Three additions to `ci.yml` (5.1-safe PowerShell, same self-hosted runner): (1) AFTER the existing `Run tests (net472 / x86)` step, add a step that runs `dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release --filter LooseOverridePathTests` — the Consensus #5 regression gate proving the `[TypeForwardedTo]` move did not break the net472 containment type (the existing LooseOverridePathTests live in UtinniCoreDotNet.Tests). (2) `dotnet build Utinni.Mcp/Utinni.Mcp.csproj -c Release` (net10 — builds against the installed 10.0.300 SDK, NOT through the `Release|x86` MSBuild pass per A3). (3) `dotnet test Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj -c Release --filter "Category!=Slow" --logger "trx;LogFileName=mcp-test-results.trx"` (per-push fast lane excludes the Slow 60s test). Add a `workflow_dispatch`-only / nightly note in a comment that the Slow-trait timeout test is run manually via `--filter Category=Slow` (do not add it to the per-push lane — Cursor LOW). Add a matching `upload-artifact` on-failure step for `mcp-test-results.trx`. Do not alter the existing x86/native lanes. Update the job `name:` to note the added net10 lane.
  </action>
  <verify>
    <automated>dotnet test Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj -c Release --filter "Category!=Slow"</automated>
  </verify>
  <acceptance_criteria>ci.yml contains: the `LooseOverridePathTests` regression step, the net10 build step, and the net10 fast-lane test step (excluding `Category=Slow`); the net10 project does not enter the x86 MSBuild pass; local `dotnet test -c Release --filter Category!=Slow` of the MCP test project is green.</acceptance_criteria>
  <done>The net472 forward regression gate + net10 build + net10 fast-test steps are present in ci.yml after the net472 lanes; the Slow timeout test is documented as nightly/manual; the net10 project does not enter the x86 MSBuild pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| `--root` arg / `UTINNI_MCP_ROOT` env -> ResolvedRoot.PinOrThrow | Server-launch config supplied by the trusted MCP client; canonicalized once, fail-closed. | filesystem path |
| relative tool path -> ResolvedRoot.Resolve | Agent-supplied relative asset path; the access-control boundary. | composed filesystem path |
| host -> Process.Start(utinni-cli.exe) | net10 host spawns the net472/x86 CLI; the honest two-process seam. | argv + captured stdout |
| NuGet restore -> build | First package install in the phase (supply-chain). | package bytes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Elevation/Tampering | ResolvedRoot.Resolve | mitigate | Delegates to the netstandard2.0 LooseOverridePath (rooted/`..`/canonicalization/prefix-attack defenses); ResolvedRootTests proves each rejection (SC3). |
| T-14-04 | Elevation | ResolvedRoot.PinOrThrow | mitigate | Fail-closed: no root configured -> server refuses to start (throws before the transport opens); no absolute path ever accepted from the agent; ServerArgsTests + ResolvedRootTests prove precedence + fail-closed. |
| T-14-05 | DoS | CliDispatcher | mitigate | Injectable timeout (default 60s) WaitForExitAsync + Kill + stdin-close; both streams read async (no pipe deadlock); ExeFound=false without throw; DispatcherTests prove timeout-kill, large-stdout, exe-missing. |
| T-14-06 | Tampering | CliDispatcher argv | mitigate | UseShellExecute=false + ProcessStartInfo.ArgumentList (per-arg, no shell, no concatenation). |
| T-14-11 | Tampering | LooseOverridePath cross-TFM move | mitigate | `[TypeForwardedTo]` preserves binary type identity for compiled net472 plugins (re-export forbidden); CI LooseOverridePathTests regression gate proves net472 callers still resolve the type. |
| T-14-SC | Tampering | NuGet install of ModelContextProtocol 1.4.0 | mitigate | Task 0 blocking-human checkpoint confirms id+version+publisher against nuget.org; Task 1 commits a NuGet lock file (RestorePackagesWithLockFile) to pin the approved versions. Consolidated in MCP-SECURITY.md (Plan 04). |
</threat_model>

<verification>
- `dotnet build Utinni.Mcp/Utinni.Mcp.csproj` succeeds (net10, SDK 10.0.300).
- net472 `UtinniCoreDotNet` build still succeeds — the `[TypeForwardedTo]` move did not break callers.
- `dotnet test UtinniCoreDotNet.Tests --filter LooseOverridePathTests` green (binary-forward regression gate).
- `dotnet test Utinni.Mcp.Tests --filter Category!=Slow` green (ResolvedRoot/ServerArgs/CliLocator/Dispatcher).
- ci.yml contains the regression gate + net10 build + net10 fast-test steps and does not route the net10 project through Release|x86.
</verification>

<success_criteria>
- net10 `Utinni.Mcp` project builds; generic-host bootstrap logs to stderr.
- `ResolvedRoot` fails closed and rejects every SC3 escape case; legit relative paths resolve under root.
- `CliDispatcher` returns hard-error results (no hang) on injected-timeout and exe-missing; no large-stdout deadlock.
- `LooseOverridePath` single-sourced in netstandard2.0 with `[TypeForwardedTo]` binary identity; regression gate green.
- net10 CI lane added; Slow timeout test gated off the per-push run.
</success_criteria>

<output>
Create `.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-01-SUMMARY.md` when done.
</output>
