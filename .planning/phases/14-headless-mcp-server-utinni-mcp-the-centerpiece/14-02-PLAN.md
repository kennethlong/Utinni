---
phase: 14-headless-mcp-server-utinni-mcp-the-centerpiece
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - Utinni.Mcp/Tools/CliResultMapper.cs
  - Utinni.Mcp/Tools/ReadTools.cs
  - Utinni.Mcp.Tests/CliResultMapperTests.cs
autonomous: true
requirements: [MCP-01]
user_setup: []

must_haves:
  truths:
    - "An agent can call read_tre / inspect_iff / decode_iff / list_world_objects with a relative path and receive the utinni-cli JSON envelope back."
    - "decode_iff covers all typed reads (datatable/stf/OT/appearance/structure) via the CLI's root-FORM auto-dispatch — no per-format read tools."
    - "get_template_schema (ReadOnly) fetches the per-class param->type schema via compile-definition --skip-native, with no native compiler invoked."
    - "A successful CLI call (exit 0) returns the envelope as BOTH a text mirror and structuredContent, isError=false."
    - "An in-band CLI failure (exit 1/2/3 with a valid JSON envelope) returns the envelope as the tool result with isError=true (agent self-corrects)."
    - "A CLI exe-missing / non-JSON-stdout / timeout outcome surfaces as a hard MCP tool error, not a tool result."
  artifacts:
    - path: "Utinni.Mcp/Tools/ReadTools.cs"
      provides: "the 4 read MCP tools (parse-tre/inspect-iff/decode-iff/list-objects) + get_template_schema"
      contains: "McpServerTool"
    - path: "Utinni.Mcp/Tools/CliResultMapper.cs"
      provides: "CliInvocationResult → CallToolResult pass-through (text + structuredContent), exit-code taxonomy"
      contains: "structuredContent"
  key_links:
    - from: "Utinni.Mcp/Tools/ReadTools.cs"
      to: "ResolvedRoot.Resolve + CliDispatcher.RunAsync + CliResultMapper.ToCallToolResult"
      via: "resolve path → dispatch verb → map envelope"
      pattern: "cli\\.RunAsync"
    - from: "Utinni.Mcp/Tools/CliResultMapper.cs"
      to: "the sorted-key JSON envelope (JsonDocument.Parse, opaque)"
      via: "parse-as-opaque-JSON, never rewrite fields"
      pattern: "JsonDocument\\.Parse"
---

<objective>
Build the read MCP tools (`read_tre`, `inspect_iff`, `decode_iff`, `list_world_objects`, plus the ReadOnly `get_template_schema` schema fetch) plus the `CliResultMapper` that turns a `CliInvocationResult` into an MCP `CallToolResult` — passing the CLI's sorted-key JSON envelope through as both a text mirror and `structuredContent`, and applying the exit-code → MCP error taxonomy. Each read tool is a thin attribute-decorated wrapper: resolve the relative path under the pinned root, dispatch the CLI verb, hand the result to the mapper. Zero format/business logic. Delivers MCP-01.

Purpose: The read half of the centerpiece — an agent reads any supported asset through the net10 stdio server. The mapper is the shared pass-through that Plan 03's write tools also consume.
Output: 4 read tools + get_template_schema + the envelope mapper, unit-tested for the exit-code taxonomy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-RESEARCH.md
@.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-PATTERNS.md

<interfaces>
<!-- Contracts from Plan 14-01 (already shipped). The mapper consumes the CLI envelope. -->
```csharp
// From Utinni.Mcp/Server/ (Plan 01):
public sealed class ResolvedRoot { public string Path; public string Resolve(string relativeAssetPath); /* throws on escape */ }
public sealed class CliDispatcher { public Task<CliInvocationResult> RunAsync(string verb, IReadOnlyList<string> args); }
public sealed class CliInvocationResult { bool ExeFound; bool TimedOut; int ExitCode; string Stdout; string Stderr; }
```
The CLI envelope (Utinni.Cli/Output/JsonOutput.cs — pass through, NEVER reshape):
```jsonc
// SUCCESS (exit 0): { "command": cmd, "result": {...}, "schemaVersion": 1 }
// ERROR (exit 1/2/3): { "command": cmd, "error": { "kind": k, "message": m }, "schemaVersion": 1 }
```
Read verb → tool mapping (verb names confirmed in Utinni.Cli/Commands/):
| MCP tool | CLI verb | Annotations |
| read_tre | parse-tre | ReadOnly, Idempotent |
| inspect_iff | inspect-iff | ReadOnly, Idempotent |
| decode_iff | decode-iff (auto-dispatches datatable/stf/OT/appearance/structure by root FORM) | ReadOnly, Idempotent |
| list_world_objects | list-objects | ReadOnly, Idempotent |
| get_template_schema | compile-definition --tdf <abs> --out <temp> --skip-native | ReadOnly, Idempotent |
</interfaces>

@Utinni.Cli/Output/JsonOutput.cs
@Utinni.Cli/Commands/ParseTreCommand.cs
@Utinni.Cli/Commands/CompileDefinitionCommand.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CliResultMapper — envelope pass-through + exit-code taxonomy</name>
  <files>Utinni.Mcp/Tools/CliResultMapper.cs, Utinni.Mcp.Tests/CliResultMapperTests.cs</files>
  <read_first>
    - Utinni.Cli/Output/JsonOutput.cs (the success/error envelope shapes the mapper passes through unchanged)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-RESEARCH.md (§D-04 error taxonomy table)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-PATTERNS.md (CliResultMapper: parse-as-opaque-JSON, decision table)
    - Utinni.Mcp/Server/CliInvocationResult.cs (the input type, from Plan 01)
  </read_first>
  <behavior>
    - exit 0 with a `result` envelope → CallToolResult with a text content block (raw JSON) + StructuredContent (parsed envelope), isError=false
    - exit 1/2/3 with an `error` envelope → CallToolResult returning the envelope as text + StructuredContent, isError=true
    - ExeFound=false → hard MCP error (throw McpException / error result with no envelope)
    - TimedOut=true → hard MCP error "utinni-cli did not exit within 60s (killed)"
    - exit 0 but stdout is NOT valid JSON → hard MCP error (transport/exec failure, not a domain answer)
  </behavior>
  <action>
    Implement `Utinni.Mcp/Tools/CliResultMapper.cs` with `ToCallToolResult(CliInvocationResult r)`. Parse `r.Stdout` with `System.Text.Json.JsonDocument.Parse` as OPAQUE JSON — attach it as `StructuredContent` and mirror the raw string as a text content block. NEVER rewrite envelope fields (that is forbidden business logic — RESEARCH anti-pattern). Decision keys: (a) did we get a valid JSON envelope, (b) the exit code. ExeFound=false / TimedOut=true / non-JSON-stdout → throw `McpException` (hard MCP error). exit 0 + valid envelope → isError=false. exit 1/2/3 + valid envelope → return envelope with isError=true (in-band; agent self-corrects). Also add a `DryRunNotice(string abs)` helper stub for Plan 03's repack tool (returns a CallToolResult describing what WOULD happen, no CLI spawn) so the contract is published from the shared mapper.
  </action>
  <verify>
    <automated>dotnet test Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj --filter CliResultMapperTests</automated>
  </verify>
  <acceptance_criteria>`dotnet test --filter CliResultMapperTests` passes; a field-equality assertion proves the envelope is passed through verbatim (no rewrite).</acceptance_criteria>
  <done>CliResultMapper maps all five taxonomy cases per `<behavior>`; the envelope is passed through verbatim. CliResultMapperTests green.</done>
</task>

<task type="auto">
  <name>Task 2: The read tools (read_tre / inspect_iff / decode_iff / list_world_objects + get_template_schema)</name>
  <files>Utinni.Mcp/Tools/ReadTools.cs</files>
  <read_first>
    - Utinni.Cli/Commands/ParseTreCommand.cs (the verb shape each read tool wraps; confirm the [Verb] name "parse-tre")
    - Utinni.Cli/Commands/DecodeIffCommand.cs (confirm decode-iff auto-dispatches on root FORM — A4)
    - Utinni.Cli/Commands/CompileDefinitionCommand.cs (the get_template_schema target: --tdf + --out + --skip-native; ReadOnly schema fetch)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-RESEARCH.md (§D-01 read-tool table incl. get_template_schema; Pattern 2 read-tool shape; BUILD verbs deferred)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-PATTERNS.md (ReadTools: verb→tool mapping; anti-pattern: never parse CLI output in the tool)
    - Utinni.Mcp/Program.cs (the WithToolsFromAssembly scan + DI registration the tools rely on, from Plan 01)
  </read_first>
  <action>
    Implement `Utinni.Mcp/Tools/ReadTools.cs` as an `[McpServerToolType]` static class with 4 `[McpServerTool(ReadOnly = true, Idempotent = true)]` methods: `read_tre`→`parse-tre`, `inspect_iff`→`inspect-iff`, `decode_iff`→`decode-iff`, `list_world_objects`→`list-objects`. Each method takes `ResolvedRoot root`, `CliDispatcher cli` (DI), and a single `[Description(...)] string relativePath` ("Asset path relative to the configured client root. Never an absolute path."). Body: `string abs = root.Resolve(relativePath);` (throws on escape → host maps to tool error), `var r = await cli.RunAsync("<verb>", new[] { abs });`, `return CliResultMapper.ToCallToolResult(r);`. Each method's `[Description]` names the verb it wraps and that it returns the utinni-cli JSON envelope. Do NOT parse or transform the CLI output inside any tool — the only jobs are resolve-path, pick-verb, dispatch, map. `decode_iff` is the single typed-read tool (A4: decode-iff auto-dispatches); do NOT add per-format read tools. `inspect_iff` stays distinct from `decode_iff` (raw chunk tree vs typed model).

    Add a 5th ReadOnly tool `get_template_schema` (RESEARCH D-01) wrapping `compile-definition --skip-native`: it takes `[Description] string tdfRelativePath`, resolves it under root, and (because compile-definition writes the schema to an `--out` path rather than stdout) dispatches `compile-definition` with `--tdf <abs>`, `--out <a host temp file>`, `--skip-native`, then returns the schema-fetch envelope via the mapper (the envelope reports the produced schema path; the tool may also surface the schema file contents as text if convenient — but parse nothing). This is the read-only schema fetch that lets an agent understand OT typed structure; `--skip-native` ensures NO native compiler runs (stays read-adjacent). Do NOT ship the BUILD verbs (compile-template/build-tre/compile-datatable/export-*) as MCP tools (D-01 defers them).
  </action>
  <verify>
    <automated>dotnet build Utinni.Mcp/Utinni.Mcp.csproj</automated>
  </verify>
  <acceptance_criteria>`dotnet build Utinni.Mcp/Utinni.Mcp.csproj` exits 0; grep confirms no IFF/CRC/format parsing in ReadTools.cs; 5 read tools are present.</acceptance_criteria>
  <done>5 read tools (4 read + get_template_schema) compile and are discoverable by `WithToolsFromAssembly`. Each resolves the path under root, dispatches its verb, and returns the mapper output. No CLI-output parsing in any tool body. BUILD verbs are NOT exposed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| agent tool args → ReadTools → ResolvedRoot.Resolve | Agent-supplied relative path; resolved under the pinned root before any file op. | relative path |
| CliInvocationResult.Stdout → CliResultMapper | CLI's own JSON envelope; parsed as opaque, never rewritten. | JSON bytes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Elevation/Tampering | ReadTools path arg | mitigate | `root.Resolve(relativePath)` (Plan 01 containment) before any dispatch; absolute/`..`/rooted rejected. |
| T-14-07 | Tampering | CliResultMapper | mitigate | Envelope parsed as opaque JSON (JsonDocument.Parse) and passed through verbatim; no field rewrite (no business-logic seam crossing). |
| T-14-02 | Spoofing/Tampering | ReadOnly/Idempotent annotations | accept | Annotations are advisory UX hints, NOT enforcement — the structural layers enforce. Documented in MCP-SECURITY.md (Plan 04). |
| T-14-05 | DoS | CliDispatcher (inherited) | mitigate | 60s timeout from Plan 01; mapper turns TimedOut into a hard MCP error. |
</threat_model>

<verification>
- `dotnet build Utinni.Mcp/Utinni.Mcp.csproj` succeeds with the 4 read tools + get_template_schema.
- `dotnet test Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj --filter CliResultMapperTests` green.
- Grep confirms no IFF/CRC/format parsing in ReadTools.cs (no business logic across the seam).
</verification>

<success_criteria>
- 4 read tools wrap parse-tre/inspect-iff/decode-iff/list-objects; decode_iff is the single auto-dispatching typed read; get_template_schema is the ReadOnly schema fetch.
- The mapper passes the envelope through as text + structuredContent and applies the exit-code taxonomy (in-band error vs hard error).
- MCP-01 read surface is in place (round-trip proven in Plan 04).
</success_criteria>

<output>
Create `.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-02-SUMMARY.md` when done.
</output>
