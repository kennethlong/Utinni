---
phase: 14-headless-mcp-server-utinni-mcp-the-centerpiece
plan: 04
type: execute
wave: 4
depends_on: ["14-02", "14-03", "14-03a"]
files_modified:
  - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/MCP-SECURITY.md
  - Utinni.Mcp.Tests/RoundTripTests.cs
  - Utinni.Mcp.Tests/Fixtures/README.md
  - Utinni.Mcp.Tests/Fixtures/sample.tab
  - Utinni.Mcp.Tests/Fixtures/sample.iff
  - Utinni.Mcp.Tests/Fixtures/sample.stf
  - Utinni.Mcp.Tests/Fixtures/sample_ot.iff
  - Utinni.Mcp.Tests/Fixtures/sample_v0006.tre
  - Utinni.Mcp.Tests/Fixtures/sample.tdf
  - Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj
autonomous: true
requirements: [MCP-01, MCP-02]
user_setup: []

must_haves:
  truths:
    - "A real McpClient (SDK) completes the stdio handshake against the built Utinni.Mcp.exe and ListToolsAsync returns the EXACT 11-tool surface by name (5 read + 4 save + repack_tre + roundtrip_check) — no 'tolerate N optional' fudge (SC5)."
    - "read_tre + decode_iff round-trip: decode_iff is exercised over .tab, .stf, AND OT fixtures (not just one) so SC1 'read any supported asset' is integration-proven across formats."
    - "An edit->save round-trip via save_datatable writes a loose-override file AND a READ-BACK (decode_iff/CLI) confirms the EDITED cell value is present, with the file hash changed before/after — a no-op re-serialize cannot pass (SC2/SC5)."
    - "repack_tre with dry_run=true (default) does NOT write (size+hash unchanged); dry_run=false on an ISOLATED COPY of a supported NON-encrypted v0006 archive rewrites it + populates backupPath under the expected backup policy (SC2)."
    - "MCP-SECURITY.md documents the 5-layer model + the advisory-not-enforcement caveat + the named ZERO-verbs exception (apply-save-*), with the full T-14-NN register where EACH threat row cites the specific TEST that proves its mitigation (SC4)."
  artifacts:
    - path: ".planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/MCP-SECURITY.md"
      provides: "design-time threat register mirroring 07-SECURITY.md; 5-layer model + advisory caveat + per-threat test citation"
      contains: "threats_open"
    - path: "Utinni.Mcp.Tests/RoundTripTests.cs"
      provides: "real-McpClient stdio handshake + multi-format read + edit->save-with-read-back + isolated-copy repack"
      contains: "McpClient"
    - path: "Utinni.Mcp.Tests/Fixtures/README.md"
      provides: "provenance notes for the committed binary fixtures"
      contains: "provenance"
  key_links:
    - from: "Utinni.Mcp.Tests/RoundTripTests.cs"
      to: "the built Utinni.Mcp.exe over StdioClientTransport (--cli-path pinned)"
      via: "McpClient.CreateAsync launching the server as a child process with a pinned utinni-cli.exe path"
      pattern: "StdioClientTransport"
    - from: "MCP-SECURITY.md"
      to: "each T-14-NN threat's proving test"
      via: "register row cites the test method/file (not just a string count)"
      pattern: "T-14-"
---

<objective>
Close the phase: author `MCP-SECURITY.md` (the first-class design-time threat register, mirroring `07-SECURITY.md`'s format, where EACH threat row cites the specific test proving its mitigation and the named `apply-save-*` ZERO-verbs exception is documented), commit minimal binary `Fixtures/` (with provenance), and write the real-MCP-client `RoundTripTests` that launch the built `Utinni.Mcp.exe` over stdio and exercise handshake + multi-format read + edit->save-WITH-READ-BACK + isolated-copy repack-dry-run. This is SC4 (security doc) + SC5 (round-trip client) — plus the integration coverage that ties Plans 02/03/03a together and PROVES persistence (not a no-op).

Purpose: The security contract is un-retrofittable once agents depend on the tools, so it ships as a design-time deliverable. The round-trip test is the DEC-C3 automatable proof that a genuine MCP client drives the server end-to-end AND that the save actually persists the edit (read-back).
Output: MCP-SECURITY.md (per-threat test citations) + committed Fixtures/ (+ provenance README) + RoundTripTests (handshake/multi-format-read/edit-save-read-back/isolated-repack); both CI lanes green.

REVIEW-DRIVEN CHANGES (cross-AI `--reviews`, both reviewers MEDIUM-HIGH on the OLD 14-04):
- Consensus #2 (HIGH): EditSaveRoundTrip must READ BACK after save and assert the EDITED value is present + compare file hash before/after — `{written,path,bytesWritten,validated}` + file-existence alone is a no-op-pass and is FORBIDDEN. (Now feasible because 14-03a genuinely persists.)
- Consensus #8 (HIGH): net10 tests CANNOT reference net472 TreFixtureBuilder/IffBuilder; those are runtime-generated, not committed. COMMIT minimal binary fixtures under Utinni.Mcp.Tests/Fixtures/ (.tre, .tab/DTII .iff, .stf, OT .iff, .tdf) WITH provenance notes; get_template_schema needs the .tdf fixture.
- Consensus #9 (HIGH): RepackDryRun dry_run=false is destructive in CI — operate on an ISOLATED COPY only; needs a supported NON-encrypted v0006 archive (NOT V6000/encrypted); account for nondeterministic bytes, file locks, backup-path cleanup; assert the backup lands under the expected backup policy.
- Consensus #10 (HIGH): MCP-SECURITY.md verify must NOT be shallow — each threat row cites the TEST proving its mitigation; require specific threat IDs AND required headings, not just T-14-/advisory string counts. Document save-tool envelope orchestration as a distinct host layer (layer 4) vs read pass-through.
- Tool-count consistency (both): assert the EXACT 11-tool surface by name (5 read + 4 save + repack_tre + roundtrip_check) — drop the old "9 core, tolerate 2".
- Remove recorded-transcript fallback (both): SC5 requires a REAL MCP client over stdio; if launch is flaky, fix process launch/test isolation, do NOT downgrade the bar.
- Consensus #11 (both): pin the `utinni-cli.exe` path for the net10 RoundTripTests via `--cli-path` (don't rely on test discovery / CWD).
- Cursor MEDIUM: SC1 broaden — exercise decode_iff over .tab/.stf/OT, not just read_tre+one decode.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-RESEARCH.md
@.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-PATTERNS.md
@.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-REVIEWS.md
@.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-VALIDATION.md

<interfaces>
<!-- The EXACT tool surface (Plans 02 + 03) the round-trip test asserts on — 11 tools, by name. -->
```
Read  (Plan 02): read_tre, inspect_iff, decode_iff, list_world_objects, get_template_schema   (5)
Write (Plan 03): save_iff, save_datatable, save_stringtable, save_object_template               (4)
Destructive (Plan 03): repack_tre                                                               (1)
Verify (Plan 03): roundtrip_check                                                               (1)
TOTAL = 11 tools (assert exact set + names; no "tolerate optional").
```
Server launch (RESEARCH §Validation 2 — pin the CLI path):
```
// McpClient.CreateAsync(new StdioClientTransport(new StdioClientTransportOptions {
//   Command = "<path to built Utinni.Mcp.exe or dotnet + Utinni.Mcp.dll>",
//   Arguments = ["--root", "<fixtureRoot>", "--cli-path", "<resolved utinni-cli.exe>"] }))
// -> ListToolsAsync() -> CallToolAsync("read_tre", ...) / decode_iff x3 / save_datatable + read-back / repack dry+real-on-copy
```
Save envelope fields (from apply-save-*, passed through): { written, path, bytesWritten, backupPath, validated, bytesEqualUntouched }.
```
</interfaces>

@.planning/phases/07-tjt-subpanel-tre-browser-read-only/07-SECURITY.md
@Utinni.Cli.Tests/Commands/SaveCommandTests.cs
@Utinni.Cli.Tests/Infrastructure/TreFixtureBuilder.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit binary Fixtures/ (+ provenance) and pin them into the net10 test project</name>
  <files>Utinni.Mcp.Tests/Fixtures/README.md, Utinni.Mcp.Tests/Fixtures/sample.tab, Utinni.Mcp.Tests/Fixtures/sample.iff, Utinni.Mcp.Tests/Fixtures/sample.stf, Utinni.Mcp.Tests/Fixtures/sample_ot.iff, Utinni.Mcp.Tests/Fixtures/sample_v0006.tre, Utinni.Mcp.Tests/Fixtures/sample.tdf, Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj</files>
  <read_first>
    - Utinni.Cli.Tests/Infrastructure/TreFixtureBuilder.cs (the net472 builder — RUN it once OFFLINE to GENERATE bytes to commit; the net10 test cannot reference it — Consensus #8)
    - Utinni.Cli.Tests/Infrastructure/IffBuilder.cs + DataTableFixtureBuilder.cs + StringTableFixtureBuilder.cs (same: generate-then-commit the .iff/.tab/.stf/OT bytes)
    - Utinni.Cli.Tests/Commands/CompileDefinitionGoldenTests.cs (the minimal authored .tdf fixture shape get_template_schema needs — Consensus #8 calls out the missing .tdf)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-REVIEWS.md (Consensus #8 fixtures; #9 v0006 non-encrypted .tre)
  </read_first>
  <action>
    Generate and COMMIT minimal binary fixtures under `Utinni.Mcp.Tests/Fixtures/` (the net10 test project cannot reference the net472 builders — Consensus #8): `sample.tab` (a small DTII datatable with at least one editable cell), `sample.iff` (a plain IFF with a mutable leaf), `sample.stf` (a string table including a non-ASCII Joao entry + a known key for edit), `sample_ot.iff` (a minimal object template with a local override), `sample_v0006.tre` (a SUPPORTED, NON-encrypted v0006 archive — NOT V6000/encrypted — so repack-tre can actually rewrite it; generate via TreFixtureBuilder's supported-0006 path), and `sample.tdf` (a minimal authored template-definition for get_template_schema). To produce these, run the net472 fixture builders / `utinni-cli` ONCE locally and capture the bytes (do NOT add a net472 ProjectReference to the net10 test project). Author `Fixtures/README.md` documenting each fixture's PROVENANCE (which builder/verb + version produced it, its format/version, what each test uses it for) so the committed binaries don't silently rot. Configure `Utinni.Mcp.Tests.csproj` to copy `Fixtures/**` to the test output dir (`<None Include="Fixtures\**" CopyToOutputDirectory="PreserveNewest" />` or `<Content>`).
  </action>
  <verify>
    <automated>dotnet build Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>The 6 binary fixtures + README.md exist under Utinni.Mcp.Tests/Fixtures/; the csproj copies them to output; `dotnet build` of the test project succeeds; README documents provenance for each fixture; sample_v0006.tre is a supported non-encrypted v0006 archive (not V6000).</acceptance_criteria>
  <done>Committed, provenance-documented binary fixtures (.tab/.iff/.stf/OT/.tdf + supported v0006 .tre) are in the net10 test project and copied to output.</done>
</task>

<task type="auto">
  <name>Task 2: RoundTripTests — real McpClient handshake + multi-format read + edit->save-with-read-back + isolated-copy repack</name>
  <files>Utinni.Mcp.Tests/RoundTripTests.cs</files>
  <read_first>
    - Utinni.Cli.Tests/Commands/SaveCommandTests.cs (the temp-work-dir + IDisposable cleanup + envelope-field-assert discipline to mirror — but the net10 test reuses COPIED fixture FILES, not the net472 builder)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-REVIEWS.md (Consensus #2 read-back assertion; #9 isolated-copy repack; #11 pin --cli-path; tool-count exact 11; no recorded-transcript fallback; Cursor SC1 multi-format read)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-RESEARCH.md (§Validation 2 scripted in-process McpClient over StdioClientTransport)
    - Utinni.Mcp/Tools/ReadTools.cs + SaveTools.cs + RepackTool.cs + VerifyTools.cs (the EXACT 11 tool names/args the test asserts + calls)
    - Utinni.Mcp/Server/CliLocator.cs (how to resolve the utinni-cli.exe path to pin via --cli-path)
  </read_first>
  <behavior>
    - Handshake/ToolCount: ListToolsAsync returns EXACTLY 11 tools; assert the exact name set { read_tre, inspect_iff, decode_iff, list_world_objects, get_template_schema, save_iff, save_datatable, save_stringtable, save_object_template, repack_tre, roundtrip_check }.
    - ReadRoundTrip: read_tre on sample_v0006.tre -> structured content carries the parse-tre `result` envelope.
    - DecodeMultiFormat (SC1 broadened): decode_iff on sample.tab, sample.stf, AND sample_ot.iff each returns a typed-decode envelope of the right kind (datatable / stf / object-template) — proving "read any supported asset" across formats.
    - EditSaveRoundTrip (SC2 — READ-BACK MANDATORY): copy sample.tab under tempRoot; compute pre-edit file hash; save_datatable { relativePath, recordIndex, columnId, value:<new> } -> exit-0 envelope { written:true, validated:true, bytesEqualUntouched:true }; THEN call decode_iff (or the CLI) on the SAME loose path and ASSERT the edited cell now holds <new>; ASSERT the post-edit file hash != pre-edit hash. A no-op re-serialize MUST fail this test.
    - EditSaveVerifyFail: a save_datatable whose underlying apply-save-tab verify would fail surfaces isError=true (in-band envelope) and the on-disk file hash is UNCHANGED (no write).
    - RepackDryRun: copy sample_v0006.tre to an ISOLATED temp path; repack_tre { relativePath, dry_run:true } -> NO bytes written (size+hash unchanged, no backup created); then repack_tre { dry_run:false } on the ISOLATED COPY -> archive rewritten (re-parses to same record count) AND a backup file exists under the expected TreBackupPath policy; clean up the backup. Tolerate nondeterministic repacked bytes (assert re-parse validity + record count, not byte-identity).
    - PathEscapeAtBoundary: save_datatable { relativePath:"../../outside.tab" } -> hard error, no file created outside root (complements the unit test in 14-03).
  </behavior>
  <action>
    Write `RoundTripTests.cs` (xUnit, net10): a fixture-class that (a) creates a temp resolved-root dir, copies the committed `Fixtures/*` under it (IDisposable recursive cleanup mirroring SaveCommandTests), (b) resolves the built `Utinni.Mcp.exe` (or `dotnet Utinni.Mcp.dll`) AND the built `utinni-cli.exe` (pin the latter explicitly via `--cli-path` — Consensus #11; locate via the same AppContext.BaseDirectory-relative probe CliLocator uses, or an env override set in the test), (c) `McpClient.CreateAsync(new StdioClientTransport(new StdioClientTransportOptions { Command = <server>, Arguments = ["--root", tempRoot, "--cli-path", cliPath] }))`. Implement all `<behavior>` tests. The EditSaveRoundTrip read-back (decode_iff after save_datatable + hash-before/after) is the centerpiece persistence proof — it is MANDATORY and must fail on a no-op re-serialize. The RepackDryRun real-write runs ONLY on an isolated copy and asserts re-parse validity + backup-under-policy, then cleans the backup. On server-stderr capture: on any test failure, surface the launched child's stderr for diagnostics while keeping stdout reserved for MCP framing. There is NO recorded-transcript fallback — if child-process launch is flaky, fix process launch / test isolation (Consensus: do not downgrade SC5).
  </action>
  <verify>
    <automated>dotnet test Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj --filter RoundTripTests</automated>
  </verify>
  <acceptance_criteria>`dotnet test --filter RoundTripTests` passes: handshake returns exactly 11 named tools; read_tre + decode_iff(x3 formats) return typed envelopes; EditSaveRoundTrip read-back confirms the edited cell value AND a changed file hash (a no-op re-serialize would fail); EditSaveVerifyFail leaves the file unchanged; RepackDryRun true=no-write / false=rewrite+backup on an isolated copy; PathEscapeAtBoundary is a hard error with no out-of-root file. SC1/SC2/SC5 proven.</acceptance_criteria>
  <done>RoundTripTests pass against the built server exe over real stdio (no transcript fallback); the read-back proves the save actually persists the edit; repack runs only on an isolated copy; the exact 11-tool surface is asserted.</done>
</task>

<task type="auto">
  <name>Task 3: MCP-SECURITY.md — design-time threat register with per-threat test citations (SC4)</name>
  <files>.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/MCP-SECURITY.md</files>
  <read_first>
    - .planning/phases/07-tjt-subpanel-tre-browser-read-only/07-SECURITY.md (the EXACT format to mirror — front-matter, Trust Boundaries, T-NN register, Accepted Risks, Sign-Off)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-REVIEWS.md (Consensus #10 — each row cites its proving test; required headings; layer-4 host orchestration; #6 temp boundary; #12 absolute-path leakage)
    - the T-14-NN rows declared in 14-01/14-02/14-03/14-03a PLAN.md threat_model blocks (consolidate them here)
    - Utinni.Mcp/Tools/SaveTools.cs + Utinni.Cli/Commands/ApplySaveTabCommand.cs + Utinni.Mcp/Server/ResolvedRoot.cs + CliDispatcher.cs + RepackTool.cs (the shipped code the register cites by file:line)
  </read_first>
  <action>
    Author `MCP-SECURITY.md` in the phase dir, mirroring `07-SECURITY.md` section-for-section: YAML front-matter (`phase: 14`, `slug`, `status: draft`, `threats_open`, `asvs_level: 1`, `created`); a Trust Boundaries table (agent->stdio->host; host->Process.Start->net472/x86 CLI; relative-path->ResolvedRoot; CLI stdout->CliResultMapper; get_template_schema host-temp --out outside root; NuGet restore->build); the **Threat Register** consolidating every `T-14-NN` from Plans 01/02/03/03a (absolute-path/`..` escape; annotation-as-enforcement; repack corruption; arg-injection; CLI hang/DoS; stdout-pollution; supply-chain NuGet + lock file; TypeForwardedTo binary identity; get_template_schema temp boundary; absolute-path leakage; save-without-verify; persisted-bytes-mismatch; concurrent same-path writes), each row with Category, Component, Disposition (mitigate/accept), **file:line evidence in the shipped code**, AND **the specific TEST that proves the mitigation** (e.g. T-14-08 -> ApplySaveTabCommandTests failed-verify-no-write + RoundTripTests.EditSaveVerifyFail; T-14-15 -> RoundTripTests.EditSaveRoundTrip read-back; T-14-01 -> ResolvedRootTests + McpBoundaryPathEscapeTests; T-14-11 -> CI LooseOverridePathTests). An Accepted Risks Log (annotation-advisory, elicitation-advisory, UTINNI_MCP_ROOT-is-a-path-not-a-secret, absolute-path-leakage T-14-12, RoundTripTests temp fixtures). A **Sign-Off** section.

    Encode the **5-layer model** (annotations -> elicitation -> loose-override-default -> verify-before-commit -> backup/recovery) with the explicit caveat that **layers 1-2 are advisory** (MCP tool hints are NOT a security boundary) and **layers 3-5 are the deterministic enforcement**. Add a distinct subsection documenting the SAVE-TOOL HOST ORCHESTRATION as layer 4 (the host wraps the single apply-save-* verb and decides on EXIT CODE — distinct from the read pass-through) (Consensus #10). Add a clearly-labeled subsection naming the SCOPED ZERO-VERBS EXCEPTION: the `apply-save-*` CLI verb family (why it was added — the review-confirmed gap — and that it is golden-tested-first in Utinni.Cli, keeping MCP a thin dispatcher and the net10/stdio/separate-process seam intact). Set `threats_open: 0` only if every threat has a disposition WITH present file:line evidence AND a cited test. Heading text MUST include the literal strings `## Trust Boundaries`, `## Threat Register`, `## Accepted Risks`, `5-layer`, `apply-save`, and `advisory` so the structural verify passes.
  </action>
  <verify>
    <automated>powershell -NoProfile -File .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/verify-mcp-security.ps1</automated>
  </verify>
  <acceptance_criteria>MCP-SECURITY.md exists with the 07-SECURITY.md structure; >=10 distinct T-14-NN ids each with a disposition + file:line evidence + a cited proving test; the required headings (Trust Boundaries / Threat Register / Accepted Risks), the 5-layer model, the `apply-save` ZERO-verbs exception subsection, and the advisory-not-enforcement caveat are all present. The verify script exits 0.</acceptance_criteria>
  <done>MCP-SECURITY.md authored; SC4 met (5-layer model + advisory caveat + named apply-save exception + full T-14-NN register where each row cites its proving test).</done>
</task>

<task type="auto">
  <name>Task 4: Phase-close verification — both lanes green + the security-doc verify script</name>
  <files>.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/verify-mcp-security.ps1</files>
  <read_first>
    - .github/workflows/ci.yml (the x86 + net10 lanes from 14-01 Task 3)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-REVIEWS.md (Consensus #10 — the doc verify must check ids + headings, not bare string counts)
  </read_first>
  <action>
    Create `verify-mcp-security.ps1` (5.1-safe) used by Task 3's verify: it reads MCP-SECURITY.md, counts DISTINCT `T-14-\d+` ids (require >=10), and asserts each required heading/substring is present (`## Trust Boundaries`, `## Threat Register`, `## Accepted Risks`, `5-layer`, `apply-save`, `advisory`); exits 0 only if all checks pass, else prints what is missing and exits 1. Then run the full phase close: confirm BOTH CI lanes are green locally — `dotnet test Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj -c Release --filter "Category!=Slow"` (net10 lane: ResolvedRoot/ServerArgs/CliLocator/Dispatcher/CliResultMapper/SaveComposition/McpBoundaryPathEscape/RoundTrip) AND `dotnet test UtinniCoreDotNet.Tests` + `dotnet test Utinni.Cli.Tests` (x86 lanes: incl. LooseOverridePathTests regression + the 4 ApplySave*CommandTests). Record the green result in the SUMMARY.
  </action>
  <verify>
    <automated>dotnet test Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj -c Release --filter "Category!=Slow"</automated>
  </verify>
  <acceptance_criteria>verify-mcp-security.ps1 exists and exits 0 against the authored MCP-SECURITY.md; the net10 fast lane is green; the x86 lanes (UtinniCoreDotNet.Tests incl. LooseOverridePathTests + Utinni.Cli.Tests incl. ApplySave*CommandTests) are green.</acceptance_criteria>
  <done>The security-doc verify script passes; both CI lanes (net10 + x86) are green locally; the phase's MCP-01 + MCP-02 success criteria are all proven by automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| McpClient (test) -> built Utinni.Mcp.exe over stdio | The real handshake the round-trip test exercises; the same transport a live client uses. | JSON-RPC framing |
| committed fixture bytes -> temp root -> tools | The fixtures ARE the adversarial inputs; copied into a temp resolved-root per test. | committed binary |
| repack real-write -> isolated temp copy | dry_run=false runs ONLY on an isolated copy; never on a committed fixture in place. | archive bytes + backup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01..T-14-16 | (consolidated) | all phase components | mitigate/accept | This plan's Task 3 authors the consolidated register with file:line evidence AND a cited proving test per threat; every threat declared in Plans 01/02/03/03a is dispositioned here. |
| T-14-10 | Information Disclosure | RoundTripTests temp-root fixtures | accept | Test-only temp dirs, recursively cleaned on Dispose; no shipping code reads them. Logged in MCP-SECURITY.md Accepted Risks. |
| T-14-17 | Tampering/DoS | RepackDryRun real-write in CI | mitigate | dry_run=false runs ONLY on an isolated copy of a supported non-encrypted v0006 archive; backup asserted under TreBackupPath policy + cleaned up; never touches a committed fixture in place. |
| T-14-SC | Tampering | NuGet supply-chain | mitigate | Gate from Plan 01 Task 0 + the committed NuGet lock file, recorded in the consolidated register. |
</threat_model>

<verification>
- `dotnet test Utinni.Mcp.Tests --filter RoundTripTests` green (handshake/11-tools/multi-format-read/edit-save-read-back/verify-fail/isolated-repack/path-escape).
- `dotnet test Utinni.Mcp.Tests -c Release --filter Category!=Slow` (full net10 fast lane) green.
- `dotnet test UtinniCoreDotNet.Tests` (incl. LooseOverridePathTests regression) + `dotnet test Utinni.Cli.Tests` (incl. ApplySave*CommandTests) green (x86 lanes; the apply-save-* persist path + the TypeForwardedTo move did not regress).
- verify-mcp-security.ps1 exits 0 (>=10 distinct T-14 ids + required headings + 5-layer + apply-save exception + advisory caveat).
</verification>

<success_criteria>
- SC5: a real McpClient round-trips one read + one edit->save (with read-back) against the built server exe (automated, no transcript fallback); the exact 11-tool surface is asserted.
- SC1: decode_iff reads .tab/.stf/OT (multi-format) + read_tre reads the v0006 archive — "read any supported asset" integration-proven.
- SC2: edit->save read-back proves persistence (no-op re-serialize would fail); repack dry-run no-write / real-write-on-isolated-copy + backup proven.
- SC4: MCP-SECURITY.md documents the 5-layer model + advisory caveat + named apply-save ZERO-verbs exception, full T-14-NN register with per-threat test citations.
- Both CI lanes green.
</success_criteria>

<output>
Create `.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-04-SUMMARY.md` when done.
</output>
