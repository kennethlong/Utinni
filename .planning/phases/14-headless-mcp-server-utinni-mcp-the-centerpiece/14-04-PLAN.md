---
phase: 14-headless-mcp-server-utinni-mcp-the-centerpiece
plan: 04
type: execute
wave: 4
depends_on: ["14-02", "14-03"]
files_modified:
  - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/MCP-SECURITY.md
  - Utinni.Mcp.Tests/RoundTripTests.cs
  - Utinni.Mcp.Tests/Fixtures/
autonomous: true
requirements: [MCP-01, MCP-02]
user_setup: []

must_haves:
  truths:
    - "A real McpClient (SDK) completes the stdio handshake against the built Utinni.Mcp.exe and ListToolsAsync returns the expected tool surface (SC5)."
    - "read_tre / decode_iff round-trips: the tool returns the CLI envelope as structured content for a fixture asset (SC1/SC5)."
    - "An edit→save round-trip writes a loose-override file and returns {written,path,bytesWritten,validated} (SC2/SC5)."
    - "repack_tre with dry_run=true (default) does NOT write; dry_run=false writes + populates backupPath (SC2)."
    - "MCP-SECURITY.md documents the 5-layer model + the advisory-not-enforcement caveat on tool hints, with the full T-14-NN threat register (SC4)."
  artifacts:
    - path: ".planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/MCP-SECURITY.md"
      provides: "design-time threat register mirroring 07-SECURITY.md; 5-layer model + advisory caveat"
      contains: "threats_open"
    - path: "Utinni.Mcp.Tests/RoundTripTests.cs"
      provides: "real-McpClient stdio handshake + read + edit→save + repack-dry-run integration tests"
      contains: "McpClient"
  key_links:
    - from: "Utinni.Mcp.Tests/RoundTripTests.cs"
      to: "the built Utinni.Mcp.exe over StdioClientTransport"
      via: "McpClient.CreateAsync launching the server as a child process"
      pattern: "StdioClientTransport"
    - from: "MCP-SECURITY.md"
      to: "the T-14-NN threats from Plans 01-03 + RESEARCH Security Domain"
      via: "consolidated register with file:line evidence"
      pattern: "T-14-"
---

<objective>
Close the phase: author `MCP-SECURITY.md` (the first-class design-time threat register, mirroring `07-SECURITY.md`'s format — front-matter, Trust Boundaries, the `T-14-NN` register with file:line evidence, Accepted Risks, the 5-layer model + advisory caveat), and write the real-MCP-client `RoundTripTests` that launch the built `Utinni.Mcp.exe` over stdio and exercise handshake + read + edit→save + repack-dry-run against copied fixtures. This is SC4 (security doc) + SC5 (round-trip client) — the two phase-closing success criteria — plus the integration coverage that ties Plans 02/03 together.

Purpose: The security contract is un-retrofittable once agents depend on the tools, so it ships as a design-time deliverable, not a later pass. The round-trip test is the DEC-C3 automatable proof that a genuine MCP client drives the server end-to-end.
Output: MCP-SECURITY.md + RoundTripTests (handshake/read/edit-save/repack-dry-run) + fixtures; both CI lanes green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-RESEARCH.md
@.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-PATTERNS.md
@.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-VALIDATION.md

<interfaces>
<!-- The full tool surface (Plans 02 + 03) the round-trip test asserts on. -->
```
Read (Plan 02):  read_tre, inspect_iff, decode_iff, list_world_objects, get_template_schema
Write (Plan 03): save_iff, save_datatable, save_stringtable, save_object_template, repack_tre, roundtrip_check
```
Server launch (RESEARCH §Validation 2):
```csharp
// McpClient.CreateAsync(new StdioClientTransport(new StdioClientTransportOptions {
//   Command = "<path to built Utinni.Mcp.exe or dotnet + Utinni.Mcp.dll>",
//   Arguments = ["--root", "<fixtureRoot>", "--cli-path", "<utinni-cli.exe>"] }))
// → ListToolsAsync() → CallToolAsync("read_tre", ...) / CallToolAsync("save_datatable", ...) / repack dry-run
```
Envelope fields to assert (from the CLI, passed through): { written, path, bytesWritten, validated }.
</interfaces>

@.planning/phases/07-tjt-subpanel-tre-browser-read-only/07-SECURITY.md
@Utinni.Cli.Tests/Commands/SaveCommandTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: RoundTripTests — real McpClient stdio handshake + read + edit→save + repack-dry-run</name>
  <files>Utinni.Mcp.Tests/RoundTripTests.cs, Utinni.Mcp.Tests/Fixtures/ (copied sample .tre/.iff/.tab/.stf), Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj (fixture-copy-to-output + ModelContextProtocol client ref)</files>
  <read_first>
    - Utinni.Cli.Tests/Commands/SaveCommandTests.cs (the temp-work-dir + IDisposable cleanup + fixture-build + envelope-field-assert discipline to mirror — but the net10 test reuses a COPIED fixture file, it cannot reference the net472 builder)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-RESEARCH.md (§Validation 2 — scripted in-process McpClient; §Validation Wave-0 — reuse Utinni.Cli.Tests fixtures under a temp resolved-root)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-PATTERNS.md (RoundTripTests: net-new McpClient transport, borrowed cleanup discipline)
    - Utinni.Mcp/Tools/ReadTools.cs (the tool names/args the test calls, from Plan 02)
    - Utinni.Mcp/Tools/SaveTools.cs + RepackTool.cs (the write tools the test calls, from Plan 03)
  </read_first>
  <behavior>
    - Handshake: ListToolsAsync returns the expected tool names (assert the 9 core read+write tools present; tolerate the 2 optional ones).
    - ReadRoundTrip: read_tre on the .tre fixture → structured content carries the parse-tre envelope (a `result` object).
    - DecodeRoundTrip: decode_iff on the .tab fixture → typed-decode envelope returned.
    - EditSaveRoundTrip: save_datatable {relativePath, recordIndex, columnId, value} → a loose-override file exists under tempRoot AND envelope { written:true, path, bytesWritten, validated:true }.
    - RepackDryRun: repack_tre {relativePath, dry_run:true} → NO bytes written (size/mtime unchanged); then dry_run:false → archive rewritten AND a backup file exists (backupPath populated).
  </behavior>
  <action>
    Add a `Fixtures/` directory to `Utinni.Mcp.Tests` holding a small sample `.tre`/`.iff`/`.tab`/`.stf` (copy bytes from the existing `Utinni.Cli.Tests` fixtures — the net10 project cannot reference the net472 `IffBuilder`/`DataTableFixtureBuilder` types, so commit the fixture FILES, not the builder). Configure the csproj to copy fixtures to the output dir. Write `RoundTripTests.cs` (xUnit, net10): a fixture-class that (a) creates a temp resolved-root dir, copies the fixtures under it (IDisposable recursive cleanup mirroring SaveCommandTests), (b) locates the built `Utinni.Mcp.exe` (or `dotnet Utinni.Mcp.dll`) and the built `utinni-cli.exe` via `--cli-path`, (c) `McpClient.CreateAsync(new StdioClientTransport(new StdioClientTransportOptions { Command = ..., Arguments = ["--root", tempRoot, "--cli-path", cliPath] }))`. Implement the 5 tests per `<behavior>`. If launching a child process in CI proves flaky, the documented fallback (RESEARCH §Validation 2) is a recorded stdio transcript — but prefer the live child-process form.
  </action>
  <verify>
    <automated>dotnet test Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj --filter RoundTripTests</automated>
  </verify>
  <acceptance_criteria>`dotnet test --filter RoundTripTests` passes: handshake + read + decode + edit→save + repack(dry-run true=no-write / false=write+backup) all behave per `<behavior>`. SC1/SC2/SC5 proven by automated test.</acceptance_criteria>
  <done>RoundTripTests pass against the built server exe over real stdio; ListToolsAsync returns the surface; read + decode + edit→save + repack-dry-run all correct.</done>
</task>

<task type="auto">
  <name>Task 2: MCP-SECURITY.md — design-time threat register (SC4)</name>
  <files>.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/MCP-SECURITY.md</files>
  <read_first>
    - .planning/phases/07-tjt-subpanel-tre-browser-read-only/07-SECURITY.md (the EXACT format to mirror — front-matter, Trust Boundaries, T-NN register, Accepted Risks, Sign-Off)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-RESEARCH.md (§Security Domain — ASVS categories, the 8 threat patterns, the 5-layer model + advisory caveat)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-PATTERNS.md (MCP-SECURITY.md: structure to replicate + T-14-NN ids + file:line evidence)
    - the T-14-NN rows declared in 14-01/14-02/14-03 PLAN.md threat_model blocks (consolidate them here)
  </read_first>
  <action>
    Author `MCP-SECURITY.md` in the phase dir, mirroring `07-SECURITY.md` section-for-section: YAML front-matter (`phase: 14`, `slug`, `status: draft`, `threats_open`, `asvs_level: 1`, `created`); a Trust Boundaries table (agent→stdio→host; host→Process.Start→net472/x86 CLI; relative-path→ResolvedRoot; CLI stdout→CliResultMapper; NuGet restore→build); the **Threat Register** consolidating every `T-14-NN` from Plans 01-03 plus the RESEARCH Security Domain rows (absolute-path/`..` escape; annotation-as-enforcement; repack corruption; arg-injection; CLI hang/DoS; stdout-pollution; decompression-bomb-inherited; supply-chain NuGet), each with Category, Component, Disposition (mitigate/accept), and **file:line evidence** in the shipped code (e.g. `ResolvedRoot.cs`, `CliDispatcher.cs`, `RepackTool.cs`, `SaveTools.cs`, the netstandard2.0 `LooseOverridePath.cs`); an Accepted Risks Log (annotation-advisory, elicitation-advisory, UTINNI_MCP_ROOT-is-a-path-not-a-secret, RoundTripTests temp fixtures); and a Sign-Off section. Encode the **5-layer model** (annotations → elicitation → loose-override-default → verify-before-commit → backup/recovery) with the explicit caveat that **layers 1-2 are advisory** (MCP tool hints are NOT a security boundary) and **layers 3-5 are the deterministic enforcement** (resolved-root pinning, loose-override default, byte-exact verify-before-commit, backup/recovery). Set `threats_open: 0` only if every threat has a disposition with present evidence.
  </action>
  <verify>
    <automated>powershell -Command "$f='.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/MCP-SECURITY.md'; if ((Select-String -Path $f -Pattern 'T-14-' -SimpleMatch).Count -ge 8 -and (Select-String -Path $f -Pattern 'advisory').Count -ge 1 -and (Select-String -Path $f -Pattern 'threats_open').Count -ge 1) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>MCP-SECURITY.md exists with the 07-SECURITY.md structure, ≥8 T-14-NN threats each with a disposition + file:line evidence, the 5-layer model, and the advisory-not-enforcement caveat. The verify command exits 0.</acceptance_criteria>
  <done>MCP-SECURITY.md authored; SC4 met (5-layer model + advisory caveat + full T-14-NN register).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| McpClient (test) → built Utinni.Mcp.exe over stdio | The real handshake the round-trip test exercises; the same transport a live client uses. | JSON-RPC framing |
| fixture bytes under temp root → tools | The fixtures ARE the adversarial inputs; live only in the test assembly under a temp resolved-root. | synthetic binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01..T-14-09 | (consolidated) | all phase components | mitigate/accept | This plan's Task 2 authors the consolidated register with file:line evidence; every threat declared in Plans 01-03 is dispositioned here. |
| T-14-10 | Information Disclosure | RoundTripTests temp-root fixtures | accept | Test-only temp dirs, recursively cleaned on Dispose; no shipping code reads them. Logged in MCP-SECURITY.md Accepted Risks. |
| T-14-SC | Tampering | NuGet supply-chain | mitigate | Gate from Plan 01 Task 0 recorded in the consolidated register. |
</threat_model>

<verification>
- `dotnet test Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj --filter RoundTripTests` green (handshake/read/decode/edit-save/repack-dry-run).
- `dotnet test Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj` (full net10 lane) green.
- `dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj` + `dotnet test Utinni.Cli.Tests/Utinni.Cli.Tests.csproj` (x86 lanes) still green (LooseOverridePath move did not regress).
- MCP-SECURITY.md present with the T-14-NN register + 5-layer model + advisory caveat.
</verification>

<success_criteria>
- SC5: a real McpClient round-trips one read + one edit→save against the built server exe (automated).
- SC1/SC2: read + edit→save + repack-dry-run proven by RoundTripTests.
- SC4: MCP-SECURITY.md documents the 5-layer model + advisory-not-enforcement caveat with the full T-14-NN register.
- Both CI lanes green.
</success_criteria>

<output>
Create `.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-04-SUMMARY.md` when done.
</output>
