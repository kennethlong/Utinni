---
phase: 14-headless-mcp-server-utinni-mcp-the-centerpiece
plan: 03
type: execute
wave: 3
depends_on: ["14-01", "14-02"]
files_modified:
  - Utinni.Mcp/Tools/SaveTools.cs
  - Utinni.Mcp/Tools/RepackTool.cs
  - Utinni.Mcp/Tools/VerifyTools.cs
  - Utinni.Mcp/Server/RoundtripVerb.cs
  - Utinni.Mcp.Tests/SaveCompositionTests.cs
autonomous: true
requirements: [MCP-02]
user_setup: []

must_haves:
  truths:
    - "An agent can apply ONE typed edit + persist to loose-override via a save_* tool, taking typed structured args only (record index / column id / typed value) — never 'apply the change you inferred'."
    - "The save path is byte-exact verify-before-commit: the typed edit is applied + verified via a roundtrip-* verb (asserts byte-identity on untouched cells) BEFORE the save verb persists to loose-override."
    - "save_* tools default to the loose-override tier and pass the {written,path,bytesWritten,backupPath,validated} envelope through unchanged."
    - "repack_tre is a distinct, off-by-default tool: dry_run defaults to true and does NOT spawn repack-tre; dry_run=false invokes the backup-routed destructive verb."
    - "repack_tre is annotated Destructive and is NOT reachable through any save_* tool."
    - "roundtrip_check (verify-only) runs a roundtrip-* verb to confirm byte-exact-on-untouched without persisting — supporting verify-before-commit."
  artifacts:
    - path: "Utinni.Mcp/Tools/SaveTools.cs"
      provides: "save_iff/save_datatable/save_stringtable/save_object_template — typed-edit two-step composition"
      contains: "McpServerTool"
    - path: "Utinni.Mcp/Tools/RepackTool.cs"
      provides: "repack_tre, Destructive, dry_run=true default, host-side gate"
      contains: "dry_run"
    - path: "Utinni.Mcp/Tools/VerifyTools.cs"
      provides: "roundtrip_check verify-only tool"
      contains: "roundtrip_check"
  key_links:
    - from: "Utinni.Mcp/Tools/SaveTools.cs"
      to: "roundtrip-* (apply+verify) then save (persist)"
      via: "two-step host composition (logic-free orchestration — Fork 1 ruling A1)"
      pattern: "RunAsync\\(\"roundtrip"
    - from: "Utinni.Mcp/Tools/RepackTool.cs"
      to: "repack-tre verb (only when dry_run=false)"
      via: "host-side dry_run gate (verb has no --dry-run flag)"
      pattern: "if \\(dry_run\\)"
---

<objective>
Build the write surface: 4 `save_*` tools (`save_iff`, `save_datatable`, `save_stringtable`, `save_object_template`), the distinct off-by-default `repack_tre` destructive tool, and the `roundtrip_check` verify-only tool. Rule the save-tool typed-edit fork and implement it. Delivers MCP-02's write half. Zero format/business logic — the host orchestrates golden-tested CLI verbs.

Purpose: The edit→save half of the centerpiece. The locked constraints demand "typed structured args only" AND "byte-exact verify-before-commit" — this plan composes the existing `roundtrip-*` verbs (which take typed mutations and assert byte-identity on untouched cells) with the `save` verb (loose-override persist) to satisfy both with zero new CLI work.
Output: 4 save tools + repack_tre + roundtrip_check + the verb-name helper, unit-tested for the composition + dry_run gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-RESEARCH.md
@.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-PATTERNS.md

<interfaces>
<!-- Plan 01 (host seam) + Plan 02 (mapper) contracts. -->
```csharp
public sealed class ResolvedRoot { public string Path; public string Resolve(string relativeAssetPath); }
public sealed class CliDispatcher { public Task<CliInvocationResult> RunAsync(string verb, IReadOnlyList<string> args); }
// CliResultMapper.ToCallToolResult(CliInvocationResult) — from Plan 02
// CliResultMapper.DryRunNotice(string abs) — from Plan 02 (repack dry-run path)
```
The save verb (Utinni.Cli/Commands/SaveCommand.cs) — loose-override persist, NOT a typed-edit verb:
```
// save <relAsset> --root <clientRoot>   → re-serialize-in-place under root, atomic write
// envelope: { written, path, bytesWritten, backupPath(null for loose-override), validated }
```
The roundtrip-tab verb (Utinni.Cli/Commands/RoundtripTabCommand.cs) — DOES take ONE typed mutation + asserts byte-identity:
```
// roundtrip-tab <path> --mutate-cell row,col --mutate-value <v>   (also --remove-row / --remove-column)
// envelope: { bytesEqualUntouched, comparisonGranularity, firstMismatch, mutationApplied, ... }
// sibling verbs: roundtrip-iff (--mutate-leaf/--remove-leaf), roundtrip-ot (add/remove/edit override), roundtrip-stf (entry edit)
```
The repack verb (Utinni.Cli/Commands/RepackTreCommand.cs) — destructive, NO --dry-run flag:
```
// repack-tre <path>   → TreRepackLock.Probe → backup via TreBackupPath → TreWriter.Repack → atomic overwrite
// V6000/encrypted → NotSupported exit 2; live-held archive → FileInUse exit 2
// envelope: { written, path, bytesWritten, backupPath(POPULATED), validated }
```
</interfaces>

@Utinni.Cli/Commands/SaveCommand.cs
@Utinni.Cli/Commands/RoundtripTabCommand.cs
@Utinni.Cli/Commands/RepackTreCommand.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: save_* tools (typed-edit two-step composition — Fork 1 ruling) + roundtrip_check</name>
  <files>Utinni.Mcp/Tools/SaveTools.cs, Utinni.Mcp/Tools/VerifyTools.cs, Utinni.Mcp/Server/RoundtripVerb.cs, Utinni.Mcp.Tests/SaveCompositionTests.cs</files>
  <read_first>
    - Utinni.Cli/Commands/SaveCommand.cs (the persist leg: loose-override default, locked envelope, .tre→repack-tre redirect, re-serialize-in-place — does NOT take edit args)
    - Utinni.Cli/Commands/RoundtripTabCommand.cs (the typed-mutation + byte-exact-on-untouched verify leg — --mutate-cell/--mutate-value, exit codes)
    - Utinni.Cli/Commands/RoundtripIffCommand.cs + RoundtripOtCommand.cs + RoundtripStfCommand.cs (the per-format mutation arg shapes for save_iff/save_object_template/save_stringtable)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-RESEARCH.md (§D-01 save-tool nuance; Open Q1 / A1 — the recommended interpretation 2 two-step)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-PATTERNS.md (SaveTools: THE ONE GENUINE FORK; locked envelope; loose-override containment)
    - Utinni.Mcp/Tools/CliResultMapper.cs (the mapper from Plan 02)
  </read_first>
  <action>
    RULING ON FORK 1 (save-tool typed-edit shape, RESEARCH Open Q1 / A1): ADOPT **interpretation 2 — host-side two-step composition**. Record this ruling in the SUMMARY truths. Rationale: it is the only shape satisfying BOTH locked constraints ("typed structured args only" AND "byte-exact verify-before-commit") with zero new CLI work; the two-step is logic-free orchestration (the host sequences two golden-tested verbs and passes their envelopes through — it parses no asset bytes, computes no CRCs, transforms no format data), so it does NOT cross the zero-business-logic seam. Do NOT plan a net-new CLI verb (Phase-13-class, out of scope).

    Implement `Utinni.Mcp/Tools/SaveTools.cs` as an `[McpServerToolType]` static class with 4 write tools: `save_iff`, `save_datatable`, `save_stringtable`, `save_object_template`. Each takes `ResolvedRoot root`, `CliDispatcher cli` (DI), a `[Description] string relativePath`, and the format-appropriate **typed** mutation args (NOT a free-form blob) — for `save_datatable`: `int recordIndex` (row), `string columnId` (index-or-name), `string value`; for `save_object_template`: typed field-override args mirroring roundtrip-ot; for `save_stringtable`: typed entry-edit args mirroring roundtrip-stf; for `save_iff`: typed leaf args mirroring roundtrip-iff. Compose two steps: (1) resolve the path under root, then `cli.RunAsync("roundtrip-<fmt>", [abs, "--mutate-...", ...])` to apply ONE typed edit AND verify byte-identity on untouched data — if `bytesEqualUntouched` is false OR the verb returns an in-band error, return that envelope (isError=true) and do NOT persist; (2) on a clean verify, `cli.RunAsync("save", [relativePath, "--root", root.Path])` to persist to loose-override, and return that envelope via `CliResultMapper.ToCallToolResult`. Pass both envelopes through unchanged. The save verb already enforces loose-override containment as a defense-in-depth second gate; the host's `ResolvedRoot` is the first.

    Add `Utinni.Mcp/Server/RoundtripVerb.cs` mapping format → verb name + mutation-arg builder so the four save tools (and roundtrip_check) share the composition without duplicating arg-assembly.

    Add `Utinni.Mcp/Tools/VerifyTools.cs` with a `roundtrip_check` tool (`[McpServerTool(ReadOnly = false, Idempotent = true)]` — it writes to temp inside the verb but does not persist to the asset): takes `relativePath` + optional typed mutation args, resolves under root, runs the matching `roundtrip-<fmt>` verb (no-mutation = full-file byte-exact check; with-mutation = per-cell untouched-slice check), and returns the verify envelope via the mapper. This is the explicit verify-before-commit surface an agent can call before a save.
  </action>
  <verify>
    <automated>dotnet test Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj --filter SaveCompositionTests</automated>
  </verify>
  <acceptance_criteria>`dotnet test --filter SaveCompositionTests` passes; the tests prove the roundtrip(verify)→save(persist) verb sequence, that a failed verify (bytesEqualUntouched=false) does NOT call save, and that the save envelope is passed through. Grep confirms no asset/CRC/format parsing in SaveTools.cs.</acceptance_criteria>
  <done>4 save tools take typed structured args only and run the roundtrip(verify)→save(persist) two-step; roundtrip_check exposes verify-only; a failed verify short-circuits before persist. SaveCompositionTests green.</done>
</task>

<task type="auto">
  <name>Task 2: repack_tre — distinct Destructive tool with host-side dry_run gate</name>
  <files>Utinni.Mcp/Tools/RepackTool.cs</files>
  <read_first>
    - Utinni.Cli/Commands/RepackTreCommand.cs (the verb has NO --dry-run flag — the gate is host-side; backup/lock/refuse-encrypted all live inside the verb)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-RESEARCH.md (§Pattern 3; §D-04 / A5 — host-side dry_run gate; §D-03 elicitation advisory-only)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-PATTERNS.md (RepackTool: dry_run gate is HOST-SIDE)
    - Utinni.Mcp/Tools/CliResultMapper.cs (ToCallToolResult + DryRunNotice from Plan 02)
  </read_first>
  <action>
    Implement `Utinni.Mcp/Tools/RepackTool.cs` as an `[McpServerToolType]` with a single `[McpServerTool(Destructive = true, Idempotent = false, ReadOnly = false)]` method `repack_tre(ResolvedRoot root, CliDispatcher cli, [Description] string relativePath, [Description] bool dry_run = true)`. Body: `string abs = root.Resolve(relativePath);` then — because the `repack-tre` verb has no `--dry-run` flag (confirmed in source) — the gate is HOST-SIDE: `if (dry_run) return CliResultMapper.DryRunNotice(abs);` (do NOT spawn the verb; report what WOULD happen). Only when `dry_run == false`: `var r = await cli.RunAsync("repack-tre", new[] { abs }); return CliResultMapper.ToCallToolResult(r);` (the envelope's `backupPath` is populated by the verb). The `[Description]` states the tool is destructive, that `dry_run` defaults to true (validates without writing), and that the backup is timestamped. repack_tre is NEVER reachable from any save_* tool (the save verb itself rejects .tre inputs). Wire elicitation as ADVISORY-ONLY: if the SDK exposes an elicitation capability and the client declares it, optionally confirm "destructive repack of X" when `dry_run=false` — but NEVER as the gate (real safety is dry_run-default + backup/recovery). Keep elicitation optional; if it complicates the build, ship without it and note in the SUMMARY that the structural layers are the enforcement.
  </action>
  <verify>
    <automated>dotnet build Utinni.Mcp/Utinni.Mcp.csproj</automated>
  </verify>
  <acceptance_criteria>`dotnet build Utinni.Mcp/Utinni.Mcp.csproj` exits 0; repack_tre is annotated Destructive with dry_run defaulting to true; grep confirms the host-side `if (dry_run)` gate and that no save_* tool references repack-tre.</acceptance_criteria>
  <done>repack_tre compiles, is annotated Destructive, defaults dry_run=true (host-side gate, no verb spawn on dry-run), and invokes repack-tre only when dry_run=false. Not reachable from save_*. (dry-run-does-not-write behavior is proven by RoundTripTests.RepackDryRun in Plan 04.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| agent typed edit args → SaveTools → roundtrip-* verb | Typed structured mutation (index/column/value); applied + byte-exact-verified before any persist. | typed mutation args |
| agent → repack_tre(dry_run) | Destructive intent; host-side dry_run gate defaults to validate-only. | relative path + dry_run flag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Elevation/Tampering | SaveTools/RepackTool path arg | mitigate | `root.Resolve(relativePath)` (Plan 01) + the save/repack verbs' own LooseOverridePath gate (defense-in-depth). |
| T-14-03 | Tampering/DoS | repack_tre destructive overwrite | mitigate | Distinct off-by-default tool; dry_run=true default (host-gated, no spawn); verb routes through TreBackupPath (backup-before-overwrite) + TreRepackLock (refuse in-use) + refuses V6000/encrypted. |
| T-14-08 | Tampering | save without verify | mitigate | Two-step: roundtrip-* asserts byte-identity on untouched data BEFORE save persists; failed verify short-circuits (no persist). |
| T-14-02 | Spoofing/Tampering | Destructive annotation | accept | Advisory hint only; dry_run-default + backup are the deterministic enforcement. Documented in MCP-SECURITY.md (Plan 04). |
| T-14-09 | Tampering | elicitation treated as a gate | accept | Elicitation is advisory; may be auto-accepted by a headless agent. The gate is dry_run-default + backup, never the prompt. |
</threat_model>

<verification>
- `dotnet build Utinni.Mcp/Utinni.Mcp.csproj` succeeds with 4 save tools + repack_tre + roundtrip_check.
- `dotnet test Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj --filter SaveCompositionTests` green.
- Grep confirms no asset parsing / CRC / format transform in SaveTools.cs or RepackTool.cs.
</verification>

<success_criteria>
- save_* tools take typed structured args only and run roundtrip(verify)→save(persist) with verify-before-commit.
- repack_tre is a distinct Destructive tool, dry_run=true default (host-side gate), unreachable from save_*.
- roundtrip_check exposes verify-only.
- MCP-02 write surface in place (round-trip + dry-run proven in Plan 04).
</success_criteria>

<output>
Create `.planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/14-03-SUMMARY.md` when done.
</output>
