---
phase: 06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut
plan: 05
type: execute
wave: 5
depends_on: [06-04]
files_modified:
  - .clang-format
  - .git-blame-ignore-revs
  - Directory.Build.props
  - "**/*.vcxproj"
  - "**/*.csproj"
  - "**/*.cs"
  - "**/*.cpp"
  - "**/*.h"
  - UtinniCoreDotNet/Resources/
  - UtinniCoreDotNet/UI/Forms/
  - UtinniCoreDotNet/Utility/Native.cs
  - UtinniCoreDotNetGen/UtinniCoreDotNetGen.csproj
  - UtinniCoreDotNetGen/Generated/StdEdited.cs
  - sdk/examples/ExampleEditorPlugin/ExampleEditorPlugin.csproj
  - .gitignore
  - Launcher/main.cpp
  - UtINI/utini.cpp
  - UtinniCore/swg/scene/render_world.cpp
  - UtinniCore/swg/scene/client_world.cpp
  - UtinniCore/swg/misc/io_win.cpp
  - UtinniCore/swg/appearance/particle.cpp
  - UtinniCore/swg/scene/scene.cpp
  - licenses.txt
  - UtinniCoreDotNet/Plugins/Commands/IUndoCommand.cs
  - UtinniCoreDotNet.Tests/PreservationAudit/
  - docs/ai/assessment.md
  - .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-AUDIT.md
  - .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-05-TJT-ICO-CROSS-REPO-NOTES.md
autonomous: true
requirements: [STAB-03, STAB-04]
must_haves:
  truths:
    - "A single atomic full-repo `.clang-format -i` commit is on master; .clang-format file at repo root codifies Allman braces + 4-space indent per CONVENTIONS.md; .git-blame-ignore-revs at repo root lists that commit's SHA so `git blame --ignore-revs-file` skips the bulk reformat."
    - "TJT.ico is removed from UtinniCoreDotNet and UtinniForm's default icon resolves to a neutral framework icon (gear/wrench — planner picks); paired cross-repo commit in kennethlong/UtinniPlugins receives the TJT.ico at TheJawaToolbox/Resources/."
    - "The polish bundle items (a) through (h) per D-16 are all landed atomically per item: Windows SDK target unification in a new shared Directory.Build.props; Prefer32Bit removal from UtinniCoreDotNetGen.csproj; ExampleEditorPlugin Release output path fix; .gitignore Std/StdEdited convention doc; empty `namespace Std {}` deletion; Native.SendMessage int → IntPtr WITH a binary-compat shim because Native.SendMessage is `public` per [[feedback-caller-attrs-binary-compat]]; licenses.txt updates (DetourXS + nvapi + João mojibake fix); typo fixes (detatch → detach, redose → redoes, stray semicolons)."
    - "Other dead-code targets per D-14 are deleted: Launcher/main.cpp attachToVisualStudio block, utini.cpp commented detours, render_world.cpp hkRender/hkClearVisibleCells disabled bodies, client_world.cpp:46-58,63, io_win.cpp:50-57, empty stub files particle.cpp + scene.cpp. **EXPLICITLY EXCLUDED:** swg/ui/ commented detours (cui_chat_window.cpp:166, cui_io.cpp:96, cui_hud.cpp:164,168, appearance.cpp:102-103) per D-14 + [[feedback-keep-scaffolding-wip]] — these are NOT touched in this plan."
    - "STAB-04 audit `06-AUDIT.md` documents each of CON-N-01..09 / CON-M-01..09 / CON-T-01..05 (24 items) with a one-line 'still intact at SHA <ref>' disposition; UtinniCoreDotNet.Tests/PreservationAudit/ contains automated xUnit tests that grep the repo for each foundation pattern and fail-on-violation per D-18 + Phase 4 D-11 'warn-only is the wrong shape'."
    - "Docs/ai/assessment.md §Status tracking shows every STAB-03 enumerated cleanup item as done OR explicitly excluded (swg/ui/ deferred); §Easy cleanups is updated to reflect completion."
    - "CI green on master across all three lanes after every commit in this plan."
    - "CONTEXT.md decisions addressed by this plan: D-12 (full-repo .clang-format atomic commit + .git-blame-ignore-revs), D-13 (Add*Callback consolidation deferred to V2 — codified by absence + assessment.md disposition), D-14 (swg/ui/ commented-detour cleanups SKIPPED entirely per [[feedback-keep-scaffolding-wip]]), D-15 (TJT.ico cross-repo ejection to UtinniPlugins per [[feedback-utinniplugins-authority]]), D-16 (polish bundle items a-h including Native.SendMessage int→IntPtr with visibility check per [[feedback-caller-attrs-binary-compat]]), D-18 (STAB-04 preservation audit 06-AUDIT.md + automated grep tests for CON-N-01..09 / CON-M-01..09 / CON-T-01..05). Also touches D-04 (assessment.md §Status tracking updates), D-09 (toolset v144 sweep verification context), D-10 (VSIX manifest context), D-11 (overlay-debug gate cross-reference)."
  artifacts:
    - path: ".clang-format"
      provides: "Repo-wide formatter config: Allman braces, 4-space indent, PascalCase preserved"
      contains: "BreakBeforeBraces"
    - path: ".git-blame-ignore-revs"
      provides: "SHA list for git blame --ignore-revs-file to skip the bulk clang-format commit"
      contains: "[0-9a-f]{40}"
    - path: "Directory.Build.props"
      provides: "Shared MSBuild props at repo root unifying WindowsTargetPlatformVersion across all .vcxproj"
      contains: "WindowsTargetPlatformVersion"
    - path: ".planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-AUDIT.md"
      provides: "STAB-04 audit of 24 preservation foundations"
      contains: "CON-N-01"
      min_lines: 80
    - path: "UtinniCoreDotNet.Tests/PreservationAudit/"
      provides: "xUnit automated grep tests fenced per [[feedback-max-harness]]"
  key_links:
    - from: ".git-blame-ignore-revs"
      to: "the clang-format commit SHA"
      via: "literal SHA on its own line"
      pattern: "^[a-f0-9]{40}$"
    - from: "Directory.Build.props (root)"
      to: "every .vcxproj WindowsTargetPlatformVersion"
      via: "MSBuild auto-import"
      pattern: "<WindowsTargetPlatformVersion>"
    - from: "UtinniCoreDotNet/Utility/Native.cs SendMessage int overload (shim)"
      to: "Native.SendMessage(IntPtr, int, IntPtr, IntPtr) (new primary)"
      via: "binary-compat shim that boxes int → IntPtr per [[feedback-caller-attrs-binary-compat]]"
      pattern: "public static void SendMessage\\(System\\.IntPtr hWnd"
---

<objective>
06-05 consolidates the STAB-03 cleanup sweep + STAB-04 preservation audit into a single CI-gated plan that runs AFTER the dep-bumps + open-Qs + CI-flake-fixes (06-02 → 06-03 → 06-04) so the rebase pain of a full-repo `clang-format -i` is contained. Plan structure: clang-format bulk reformat (Task 1), TJT.ico cross-repo ejection + polish bundle (Task 2), dead-code purge (Task 3), STAB-04 audit + automated grep tests (Task 4).

Purpose: D-12 codifies CONVENTIONS.md style; D-15 ships the framework-vs-plugin separation cleanly; D-16 lands the eight-item polish bundle; D-14 removes dead code WITHOUT touching `swg/ui/` per [[feedback-keep-scaffolding-wip]]; D-13 explicitly preserves the Add*Callback vs Add*Call naming as wrappers per Phase 3 D-10 + [[feedback-caller-attrs-binary-compat]] (no consolidation here; deferred to V2); D-18 audits the 24 preservation foundations with fail-on-violation grep tests per [[feedback-max-harness]].

Output: A repo whose grep surface confirms every CON-N / CON-M / CON-T item intact, with style codified by .clang-format, neutral framework icon in UtinniForm, polished build paths, no dead code outside the explicit `swg/ui/` carve-out, and CI-runnable preservation-audit tests that fail-on-violation.

Per the planner-discretion area in D-18: the STAB-04 audit tests live in `UtinniCoreDotNet.Tests/PreservationAudit/` (extending the existing single-test-project per Phase 1 D-04 / Phase 5 single-test-project convention rather than spinning up a new sibling project — fewer projects, less ceremony, same test home as the GameCallbacks fence).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-CONTEXT.md
@.planning/intel/constraints.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
@docs/ai/assessment.md
@UtinniCoreDotNet/Utility/Native.cs

<interfaces>
<!-- Reference state captured at planning time; executor reads files at task time. -->

From .planning/codebase/CONVENTIONS.md:
- Allman braces, 4-space indent, PascalCase, MIT license header (23-line block) on every C# file.
- The clang-format file MUST encode these via: `BreakBeforeBraces: Allman`, `IndentWidth: 4`, `UseTab: Never`, no namespace indent, attached braces avoided.

From UtinniCoreDotNet/Utility/Native.cs (verified at planning time):
- Line 94-95: `[DllImport("user32.dll", EntryPoint = "SendMessage")] public static extern void SendMessage(System.IntPtr hWnd, int wMsg, int wParam, int lParam);`
- The third+fourth parameters use `int` for `wParam`/`lParam`. D-16(f) calls for IntPtr to be safe in x64 contexts (Utinni is x86 — so this is preventive rather than corrective; per [[feedback-caller-attrs-binary-compat]] memory the signature change WILL break pre-built plugin DLLs at MEF compose because Native.SendMessage is `public`).
- Mitigation per D-16(f): keep the existing int-int overload as a binary-compat shim and add the IntPtr-IntPtr overload as a new primary; both forward to the underlying DllImport.

From .planning/intel/constraints.md:
- CON-N-01: detour-table `pX x = (pX)0x...` pattern — grep-testable.
- CON-N-02: utinni:: thin-wrapper firewall — grep `utinni::` namespace headers for `swg::` includes (must return zero direct includes; allowed only through the thin-wrapper).
- CON-N-04: memory::copy VirtualProtect bracket — grep `VirtualProtect` paired around `memory::copy` writes.
- CON-N-06: imgui device-loss handling (isSetup guard) — grep `isSetup` in imgui_impl.cpp.
- CON-N-08: PIMPL idiom — grep `struct Impl;` + `Impl* pImpl{}` in PluginManager + UtINI.
- CON-N-09: spdlog `base_sink<std::mutex>` for OutputSink — fenced by 06-02 Task 2's OutputSinkRoundTripTests already; preservation-audit test re-asserts via grep.
- CON-M-01: IPlugin/IEditorPlugin SPI — grep that PluginFramework/IPlugin.cs exports Initialize/GetForms/GetEditors (read at task time for exact method names).
- CON-M-05: UndoRedoManager.OnCleanupCallback — grep `OnCleanupCallback`.
- CON-M-07: Log.AddOutputSinkCallback — grep that name exactly.
- CON-T-02: triple-config (Debug + Release + RelWithDbgInfo) — every .vcxproj must have all three.
- CON-T-04: Props.cs factoring — central plugin MSBuild boilerplate; grep file existence.

From assessment.md §"Easy cleanups" (read at task time):
- ~30 enumerated items; the items NOT in this plan are the swg/ui/ commented-detour cleanups (D-14 explicit exclusion).

From [[project-loader-lock-harness-ci-flake]] + [[feedback-keep-scaffolding-wip]] + [[feedback-caller-attrs-binary-compat]]:
- The Caller* attribute memory specifically applies to Native.SendMessage's int → IntPtr change: cross-binary plugin DLLs break at MEF compose if signatures change. Plan ships both signatures (compat + primary).
- The keep-scaffolding-wip memory specifically applies to swg/ui/ — those cleanups are deferred ENTIRELY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bulk `.clang-format` reformat + `.git-blame-ignore-revs` + CI clang-format-check step</name>
  <files>
    .clang-format,
    .git-blame-ignore-revs,
    .github/workflows/ci.yml,
    UtinniCore/**/*.cpp,
    UtinniCore/**/*.h,
    UtINI/utini.cpp,
    UtINI/utini.h,
    Launcher/main.cpp,
    UtinniCore-Symbols/**/*.cpp,
    Utinni.LoaderLockHarness/main.cpp,
    Utinni.CrtMatchPlugin/**/*.cpp,
    Utinni.LegacyPlugin/**/*.cpp,
    UtinniCore.Tests/**/*.cpp,
    UtinniCore.Tests/**/*.h,
    sdk/**/*.cpp,
    sdk/**/*.h
  </files>
  <read_first>
    .planning/codebase/CONVENTIONS.md,
    .editorconfig,
    UtinniCore/swg/graphics/depth_texture.cpp,
    .github/workflows/ci.yml
  </read_first>
  <action>
    Author `.clang-format` at repo root encoding the CONVENTIONS.md style: `BasedOnStyle: LLVM` as a base, then overrides — `Language: Cpp`, `BreakBeforeBraces: Allman`, `IndentWidth: 4`, `TabWidth: 4`, `UseTab: Never`, `ColumnLimit: 0` (CONVENTIONS.md does not mention a wrap column; turn off to avoid accidental line-rewraps), `NamespaceIndentation: None`, `AccessModifierOffset: -4`, `AlignAfterOpenBracket: Align`, `AllowShortFunctionsOnASingleLine: Empty`, `KeepEmptyLinesAtTheStartOfBlocks: false`, `SortIncludes: false` (existing #include ordering is load-bearing in several files — do not auto-sort). Read the existing .editorconfig and ensure no conflict (4-space indent matches; if .editorconfig says 2-space anywhere, leave it — clang-format is C++ only and .editorconfig governs cross-language).

    Run `clang-format -i` recursively across the entire C++ tree EXCLUDING `external/` (vendored deps), `bin/`, `obj/`, `Release/`, `Debug/`, and `UtinniCoreDotNet/Generated/` (CppSharp auto-generated). Use the exact include set:
    - `UtinniCore/**/*.{cpp,h}`
    - `UtINI/*.{cpp,h}`
    - `Launcher/*.{cpp,h}`
    - `UtinniCore-Symbols/**/*.{cpp,h}`
    - `Utinni.LoaderLockHarness/*.{cpp,h}`
    - `Utinni.CrtMatchPlugin/**/*.{cpp,h}`
    - `Utinni.LegacyPlugin/**/*.{cpp,h}`
    - `UtinniCore.Tests/**/*.{cpp,h}`
    - `sdk/**/*.{cpp,h}`

    Verify `msbuild Utinni.sln /p:Configuration=Release /p:Platform=x86` succeeds against the reformatted tree. The reformat is a cosmetic-only diff — no semantic changes, no behaviour changes.

    Commit atomically as `style(06-05): full-repo clang-format application`. Capture the SHA. Then in a SECOND commit, add `.git-blame-ignore-revs` at repo root with the clang-format commit's SHA on its own line. Commit as `chore(06-05): record clang-format SHA in .git-blame-ignore-revs`.

    Then in a THIRD commit, extend .github/workflows/ci.yml with a `clang-format check` step inserted AFTER `Checkout` and BEFORE `Build solution`: runs `clang-format --dry-run --Werror` against the same file set; fails CI on style violations introduced by future commits. Commit as `ci(06-05): add clang-format-check step`.
  </action>
  <verify>
    <automated>msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86</automated>
  </verify>
  <acceptance_criteria>
    - `.clang-format` exists at repo root and grep returns matches for `BreakBeforeBraces: Allman` AND `IndentWidth: 4`.
    - `.git-blame-ignore-revs` exists at repo root containing at least one 40-char hex SHA (regex `^[a-f0-9]{40}$` per line).
    - `.git-blame-ignore-revs` SHA matches the clang-format reformat commit (verify via `git log --oneline --grep="full-repo clang-format" --pretty=%H`).
    - .github/workflows/ci.yml grep `clang-format` returns at least one match in a step name.
    - Release x86 solution build exit code 0 after the reformat.
    - Three atomic commits on master: `style(06-05):`, `chore(06-05):`, `ci(06-05):`.
    - CI green on master after all three commits.
  </acceptance_criteria>
  <done>.clang-format codified, full-repo reformatted in a single commit, .git-blame-ignore-revs landed, CI gated on clang-format-check going forward.</done>
</task>

<task type="auto">
  <name>Task 2: TJT.ico cross-repo ejection + neutral framework icon + polish bundle (D-16 items a-h)</name>
  <files>
    UtinniCoreDotNet/Resources/,
    UtinniCoreDotNet/UI/Forms/UtinniForm.cs,
    UtinniCoreDotNet/Utility/Native.cs,
    UtinniCoreDotNetGen/UtinniCoreDotNetGen.csproj,
    UtinniCoreDotNetGen/Generated/StdEdited.cs,
    sdk/examples/ExampleEditorPlugin/ExampleEditorPlugin.csproj,
    .gitignore,
    UtinniCoreDotNet/Plugins/Commands/IUndoCommand.cs,
    UtINI/utini.cpp,
    Directory.Build.props,
    UtinniCore/UtinniCore.vcxproj,
    UtinniCore.Tests/UtinniCore.Tests.vcxproj,
    Launcher/Launcher.vcxproj,
    UtINI/UtINI.vcxproj,
    UtinniCore-Symbols/UtinniCore-Symbols.vcxproj,
    Utinni.LoaderLockHarness/Utinni.LoaderLockHarness.vcxproj,
    Utinni.CrtMatchPlugin/Utinni.CrtMatchPlugin.vcxproj,
    Utinni.LegacyPlugin/Utinni.LegacyPlugin.vcxproj,
    sdk/UtinniCppPluginTemplate/UtinniCppPluginTemplate/UtinniCppPluginTemplate.vcxproj,
    sdk/examples/ExampleCppPlugin/ExampleCppPlugin.vcxproj,
    licenses.txt,
    .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-05-TJT-ICO-CROSS-REPO-NOTES.md
  </files>
  <read_first>
    UtinniCoreDotNet/UI/Forms/UtinniForm.cs,
    UtinniCoreDotNet/Utility/Native.cs,
    UtinniCoreDotNetGen/UtinniCoreDotNetGen.csproj,
    UtinniCoreDotNetGen/Generated/StdEdited.cs,
    sdk/examples/ExampleEditorPlugin/ExampleEditorPlugin.csproj,
    UtinniCore/UtinniCore.vcxproj,
    UtINI/utini.cpp,
    UtinniCoreDotNet/Plugins/Commands/IUndoCommand.cs,
    licenses.txt
  </read_first>
  <action>
    Ship D-15 + D-16 (a)-(h) as one atomic commit per item where feasible, otherwise small bundles:

    **(D-15) TJT.ico ejection — paired cross-repo:**
    1. Locate the icon currently bundled in UtinniCoreDotNet that resolves as UtinniForm's default — grep `TJT.ico` in UtinniCoreDotNet (resource file or designer .resx); read UtinniForm.cs to identify the icon-loading code.
    2. In Utinni: delete the TJT.ico file from UtinniCoreDotNet/Resources/ (or wherever it lives), update the .resx + UtinniForm icon assignment to point to a new neutral framework icon (planner-picked gear or wrench glyph; ship a small PNG-or-ICO under UtinniCoreDotNet/Resources/utinni.ico — original or public-domain). Commit as `feat(06-05): replace TJT.ico with neutral utinni.ico in UtinniForm default`.
    3. In sibling repo D:/Code/UtinniPlugins (per [[feedback-utinniplugins-authority]] standing authority — no human-action checkpoint required for code commits, only the live-SWG smoke needs one): add the original TJT.ico to TheJawaToolbox/Resources/ and ensure TJT's plugin assigns TJT.ico as its panel icon. Commit as `feat(tjt): receive TJT.ico from Utinni framework (paired with utinni#<SHA>)`.
    4. Write .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-05-TJT-ICO-CROSS-REPO-NOTES.md capturing both commit SHAs + the cross-repo pairing rationale per Phase 02 D-09 precedent. The notes file lives in Utinni only (UtinniPlugins repo records the pairing in its commit message).

    **(D-16 a) Windows SDK target unification:**
    1. Create a new `Directory.Build.props` at Utinni repo root containing:
       ```
       <Project>
         <PropertyGroup>
           <WindowsTargetPlatformVersion>10.0</WindowsTargetPlatformVersion>
         </PropertyGroup>
       </Project>
       ```
       Choose `10.0` as the unified value because all current .vcxproj already use either `10.0` or a more specific 10.0.x SDK; `10.0` resolves to the latest installed on the runner.
    2. Remove the `<WindowsTargetPlatformVersion>` element from every .vcxproj (it now inherits from Directory.Build.props automatically). Verify CON-T-02 triple-config preservation.
    3. Commit as `build(06-05): unify WindowsTargetPlatformVersion via Directory.Build.props (CON-B-02)`.

    **(D-16 b) Prefer32Bit removal from UtinniCoreDotNetGen.csproj:**
    1. Grep `Prefer32Bit` in UtinniCoreDotNetGen.csproj — delete the element across all configurations. The csproj already targets x64 so Prefer32Bit is incoherent.
    2. Commit as `build(06-05): remove Prefer32Bit from UtinniCoreDotNetGen.csproj`.

    **(D-16 c) ExampleEditorPlugin Release output path fix:**
    1. Read sdk/examples/ExampleEditorPlugin/ExampleEditorPlugin.csproj line ~28 (per 06-CONTEXT.md `<decisions>` D-16 (c)): the Release OutputPath currently says `bin\Debug\Plugins\...` — change to `bin\Release\Plugins\...`.
    2. Commit as `build(06-05): fix ExampleEditorPlugin Release output path`.

    **(D-16 d) .gitignore Std/StdEdited convention documentation:**
    1. Append a comment block to .gitignore explaining that `UtinniCoreDotNetGen/Generated/Std.cs` is gitignored because it's CppSharp-regenerated every build, while `StdEdited.cs` is committed because it contains hand-curated edits (per CON-O-05 disposition). Keep the actual ignore patterns intact.
    2. Commit as `docs(06-05): document Std/StdEdited convention in .gitignore`.

    **(D-16 e) Empty `namespace Std {}` deletion:**
    1. Read UtinniCoreDotNetGen/Generated/StdEdited.cs; if it contains literal `namespace Std {}` (empty block) lines, delete them.
    2. Commit as `chore(06-05): delete empty namespace Std blocks in StdEdited.cs`.

    **(D-16 f) Native.SendMessage int → IntPtr WITH binary-compat shim:**
    1. Read UtinniCoreDotNet/Utility/Native.cs lines 94-95. Per [[feedback-caller-attrs-binary-compat]] memory + 06-CONTEXT.md `<specifics>` directive: Native.SendMessage is `public`. Add a new primary overload `public static extern void SendMessage(System.IntPtr hWnd, int wMsg, System.IntPtr wParam, System.IntPtr lParam);` (or wrap an internal DllImport with this signature, then have the existing int-int public method delegate to it via `new IntPtr(wParam)` / `new IntPtr(lParam)`). The existing int-int signature remains as a binary-compat shim that calls the IntPtr version. No callsite update required in the same commit.
    2. Update the single in-repo callsite at UtinniCoreDotNet/UI/Forms/UtinniForm.cs:219 (`Native.SendMessage(this.Handle, Native.WM_SYSCOMMAND, Native.SC_DRAGMOVE, 0);`) to use the new IntPtr overload (e.g., `Native.SendMessage(this.Handle, Native.WM_SYSCOMMAND, new IntPtr(Native.SC_DRAGMOVE), IntPtr.Zero);`).
    3. Commit as `feat(06-05): add IntPtr overload for Native.SendMessage; preserve int shim for binary compat`.

    **(D-16 g) licenses.txt updates:**
    1. Append entries to licenses.txt for DetourXS and nvapi (both vendored before 06-02 — if they were migrated to vcpkg in 06-02, the licenses still need to be acknowledged in licenses.txt because the source code referenced them through 06-01). Read 06-02-VCPKG-RESEARCH.md disposition; if a dep was kept vendored, the license entry is still required.
    2. Fix the existing `Jo�o` mojibake in licenses.txt to `João` (UTF-8 `Jo\xc3\xa3o`).
    3. Commit as `docs(06-05): licenses.txt += DetourXS + nvapi; fix João mojibake`.

    **(D-16 h) Typo fixes:**
    1. UtINI/utini.cpp grep `void detatch` — if present, rename to `void detach` (per 06-CONTEXT.md `<decisions>` D-16 (h); verify no other callsite uses the misspelled name before renaming).
    2. UtinniCoreDotNet/Plugins/Commands/IUndoCommand.cs:31 — change `Executes/redose` to `Executes/redoes`.
    3. Grep entire repo for `#include "utinni.h";` (with trailing semicolon) — delete the semicolon at every match site.
    4. Commit as `chore(06-05): fix typos (detatch→detach, redose→redoes, stray semicolons)`.
  </action>
  <verify>
    <automated>msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86</automated>
  </verify>
  <acceptance_criteria>
    - UtinniCoreDotNet grep `TJT.ico` returns zero matches.
    - Directory.Build.props exists at repo root; grep `WindowsTargetPlatformVersion` returns at least one match.
    - For every Utinni-owned .vcxproj outside external/: grep `<WindowsTargetPlatformVersion>` returns zero matches (now inherits from props).
    - UtinniCoreDotNetGen.csproj grep `Prefer32Bit` returns zero matches.
    - sdk/examples/ExampleEditorPlugin/ExampleEditorPlugin.csproj grep `bin\\Debug\\Plugins` returns zero matches in Release config; grep `bin\\Release\\Plugins` returns at least one match in Release config.
    - .gitignore grep `Std.cs.*generated|StdEdited.*committed` returns at least one comment-block match (planner's wording is allowed flexibility but the convention IS documented).
    - StdEdited.cs grep `namespace Std \{\s*\}` returns zero matches.
    - Native.cs grep `public static.*SendMessage.*IntPtr.*IntPtr.*IntPtr` returns at least one match (the new IntPtr primary overload).
    - Native.cs grep `public static.*SendMessage.*int wParam.*int lParam` returns at least one match (the binary-compat shim retained).
    - UtinniForm.cs:219 grep `IntPtr.Zero|new IntPtr` returns at least one match in the SendMessage call.
    - licenses.txt grep `DetourXS` returns at least one match; grep `nvapi` returns at least one match.
    - licenses.txt grep `Jo�o` (mojibake) returns zero matches; grep `João` returns at least one match.
    - UtINI/utini.cpp grep `detatch` returns zero matches.
    - UtinniCoreDotNet/Plugins/Commands/IUndoCommand.cs grep `redose` returns zero matches; grep `redoes` returns at least one match.
    - Repo-wide grep `#include "utinni.h";` returns zero matches.
    - 06-05-TJT-ICO-CROSS-REPO-NOTES.md exists with both Utinni + UtinniPlugins commit SHAs.
    - Release x86 solution build exit code 0.
    - CI green on master post-merge: `gh run list --branch master --limit 1 --json conclusion -q '.[0].conclusion'` returns "success".
    - At least 9 atomic commits on master with prefixes per the items above (TJT.ico + 8 D-16 items).
  </acceptance_criteria>
  <done>TJT.ico ejected cross-repo with neutral icon replacement; polish bundle items (a)-(h) all landed atomically; binary-compat shim preserved for Native.SendMessage per [[feedback-caller-attrs-binary-compat]].</done>
</task>

<task type="auto">
  <name>Task 3: Dead-code purge (D-14 — explicitly excluding swg/ui/)</name>
  <files>
    Launcher/main.cpp,
    UtINI/utini.cpp,
    UtinniCore/swg/scene/render_world.cpp,
    UtinniCore/swg/scene/client_world.cpp,
    UtinniCore/swg/misc/io_win.cpp,
    UtinniCore/swg/appearance/particle.cpp,
    UtinniCore/swg/scene/scene.cpp,
    UtinniCore/UtinniCore.vcxproj
  </files>
  <read_first>
    Launcher/main.cpp,
    UtINI/utini.cpp,
    UtinniCore/swg/scene/render_world.cpp,
    UtinniCore/swg/scene/client_world.cpp,
    UtinniCore/swg/misc/io_win.cpp,
    UtinniCore/swg/appearance/particle.cpp,
    UtinniCore/swg/scene/scene.cpp,
    .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-CONTEXT.md
  </read_first>
  <action>
    Delete the dead-code targets enumerated in D-14, one atomic commit per source file (or one commit per logical cluster — e.g., the two empty-stub files together). **DO NOT TOUCH** `swg/ui/` files — that carve-out is explicit per D-14 + [[feedback-keep-scaffolding-wip]] memory + 06-CONTEXT.md `<deferred>` section.

    Targets:
    1. **Launcher/main.cpp:33-172** — `attachToVisualStudio` block. Read the file at those line numbers; delete the function definition AND any callsite that references it (grep `attachToVisualStudio`). Commit as `chore(06-05): delete dead attachToVisualStudio block from Launcher`.
    2. **UtINI/utini.cpp:71, 75** — commented detours (verify at task time; the line numbers reflect pre-06-03 state; after the LeksysINI replacement in 06-03 these lines may have shifted — grep for the commented `// Detour::Create` patterns instead). If the lines still exist post-06-03, delete them. If 06-03's rewrite already removed them, mark this item as no-op in the commit. Commit as `chore(06-05): delete commented detour scaffolding in utini.cpp` (or skip with explicit no-op note).
    3. **UtinniCore/swg/scene/render_world.cpp** — `hkRender` / `hkClearVisibleCells` disabled experimental bodies. Grep `#if 0|// disabled|/* disabled` in render_world.cpp; delete the entire `#if 0 ... #endif` blocks (or `/* ... */` blocks) that contain experimental hook bodies. Preserve any non-experimental commented documentation. Commit as `chore(06-05): delete disabled hkRender + hkClearVisibleCells experimental bodies`.
    4. **UtinniCore/swg/scene/client_world.cpp:46-58, 63** — read those lines; delete the dead block. Commit as `chore(06-05): delete dead block in client_world.cpp`.
    5. **UtinniCore/swg/misc/io_win.cpp:50-57** — read those lines; delete the dead block. Commit as `chore(06-05): delete dead block in io_win.cpp`.
    6. **Empty stub files** — `UtinniCore/swg/appearance/particle.cpp` and `UtinniCore/swg/scene/scene.cpp`. Verify both files are empty (or contain only the MIT header + no code body); if confirmed empty: delete the files in git AND remove the corresponding `<ClCompile Include="...">` entries from UtinniCore/UtinniCore.vcxproj. Commit as `chore(06-05): delete empty stub files particle.cpp + scene.cpp`.

    For each target above, verify Release x86 build before committing. If a delete breaks the build (callsite still references the deleted symbol), abort that item and document the abort in 06-05-SUMMARY.md.

    **Explicit non-action: swg/ui/cui_chat_window.cpp:166, swg/ui/cui_io.cpp:96, swg/ui/cui_hud.cpp:164,168, swg/ui/appearance.cpp:102-103 are NOT touched.** Append a section to docs/ai/assessment.md noting these are "deferred pending post-overlay-fix audit" per [[feedback-keep-scaffolding-wip]].
  </action>
  <verify>
    <automated>msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86</automated>
  </verify>
  <acceptance_criteria>
    - Launcher/main.cpp grep `attachToVisualStudio` returns zero matches.
    - UtinniCore/swg/scene/render_world.cpp grep `#if 0` returns zero matches (or fewer matches than before the commit, per executor's per-file inventory).
    - `git ls-files UtinniCore/swg/appearance/particle.cpp` returns zero entries.
    - `git ls-files UtinniCore/swg/scene/scene.cpp` returns zero entries.
    - UtinniCore.vcxproj grep `particle.cpp` returns zero matches.
    - UtinniCore.vcxproj grep `<ClCompile Include=.*scene\\\\scene.cpp` returns zero matches.
    - **swg/ui carve-out check:** UtinniCore/swg/ui/cui_chat_window.cpp line 166 area is byte-identical to pre-plan state (executor verifies via `git diff --stat HEAD~10..HEAD UtinniCore/swg/ui/` showing zero changes); similarly for cui_io.cpp, cui_hud.cpp, appearance.cpp.
    - docs/ai/assessment.md grep `deferred pending post-overlay-fix audit` returns at least one match.
    - Release x86 solution build exit code 0.
    - At least 5 atomic commits on master with prefix `chore(06-05):`.
    - CI green on master post-merge.
  </acceptance_criteria>
  <done>Dead-code purge complete except for swg/ui/ carve-out; deferred swg/ui/ items documented; build green.</done>
</task>

<task type="auto">
  <name>Task 4: STAB-04 preservation audit + automated fail-on-violation grep tests</name>
  <files>
    .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-AUDIT.md,
    UtinniCoreDotNet.Tests/PreservationAudit/PreservationAuditTests.cs,
    UtinniCoreDotNet.Tests/PreservationAudit/RepoRoot.cs,
    UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj,
    docs/ai/assessment.md
  </files>
  <read_first>
    .planning/intel/constraints.md,
    UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj,
    UtinniCoreDotNet.Tests/GameCallbacksTests.cs,
    UtinniCore/swg/game/game.cpp,
    PluginFramework/IPlugin.cs,
    UtINI/utini.h
  </read_first>
  <action>
    Create 06-AUDIT.md with one section per constraint family (Native, Managed, Process/Tooling) and one subsection per item (24 total: CON-N-01..09, CON-M-01..09, CON-T-01..05). Each subsection: (a) Constraint statement (one line, copied from intel/constraints.md), (b) Audit procedure (the grep command or file probe that confirms the item is intact at the current SHA), (c) Result (PASS / FAIL with evidence), (d) Last-verified SHA (the merge SHA of this audit commit).

    Then create UtinniCoreDotNet.Tests/PreservationAudit/PreservationAuditTests.cs as an xUnit test class with one `[Fact]` per audit item (24 total). Each Fact loads the repo root via a small helper (`RepoRoot.cs`) that walks up from `AppDomain.CurrentDomain.BaseDirectory` until it finds the `Utinni.sln` file. The Fact then runs the relevant grep or file probe as a C# `Directory.EnumerateFiles` + `File.ReadAllText` + `Regex.IsMatch` pipeline. Each Fact MUST fail if the foundation is violated (per Phase 4 D-11 "warn-only is the wrong shape").

    Examples of the grep checks (executor expands to the full 24):
    - **CON-N-01** (detour-table pattern): walk `UtinniCore/swg/**/*.cpp`, count files containing `(p[A-Z][a-zA-Z]+)\s+[a-z][a-zA-Z]+\s*=\s*\(\1\)0x[0-9A-Fa-f]+` — Assert >= 50 files match (adjust the threshold based on actual repo state at task time).
    - **CON-N-02** (thin-wrapper firewall): walk `UtinniCore/utinni/**/*.h` (or wherever the `utinni::` namespace headers live — read STRUCTURE.md), confirm zero files contain `#include "swg/`.
    - **CON-N-04** (VirtualProtect bracket): walk `UtinniCore/utility/memory.cpp`, confirm at least 2 `VirtualProtect` calls per `memcpy` / write block.
    - **CON-N-06** (imgui isSetup guard): walk `UtinniCore/swg/ui/imgui_impl.cpp`, confirm grep `isSetup` returns at least one match.
    - **CON-N-08** (PIMPL): walk `UtinniCore/plugins/PluginManager.h` AND `UtINI/utini.h`, confirm both contain `struct Impl;` followed by `Impl* pImpl{}` (or equivalent forward-decl + pointer pattern).
    - **CON-N-09** (spdlog base_sink<std::mutex>): walk `UtinniCore/utility/log.cpp` or wherever OutputSink is defined, confirm `base_sink<std::mutex>` literal present.
    - **CON-M-01** (IPlugin SPI shape): probe `UtinniCoreDotNet/Plugins/IPlugin.cs` (or wherever — read at task time), confirm interface declares `Initialize`, `GetForms`, `GetEditors` methods (verify names exactly at task time).
    - **CON-M-02** ([InheritedExport]): probe IPlugin.cs / IEditorPlugin.cs for `[InheritedExport(typeof(...))]` attribute.
    - **CON-M-05** (UndoRedoManager.OnCleanupCallback): grep UndoRedoManager.cs for `OnCleanupCallback`.
    - **CON-M-07** (Log.AddOutputSinkCallback): grep for that exact identifier.
    - **CON-T-01** (UtinniCore post-build chain): probe UtinniCore.vcxproj for `<PostBuildEvent>` containing `xcopy data` AND `UtinniCoreDotNetGen.exe`.
    - **CON-T-02** (triple-config): walk every .vcxproj, confirm each contains exactly three `<ProjectConfiguration Include=` entries (Debug+Release+RelWithDbgInfo).
    - **CON-T-03** (two-language template parity): probe `sdk/UtinniCppPluginTemplate/` AND `sdk/UtinniDotNetPluginTemplate/` (or wherever the templates live) exist as sibling directories.
    - **CON-T-04** (Props.cs factoring): probe `sdk/...Props.cs` exists.
    - **CON-T-05** (Jawa Toolbox *Impl separation): cross-repo probe — kennethlong/UtinniPlugins TheJawaToolbox has `*Impl.cs` files paired with their panel files; document in 06-AUDIT.md but skip the xUnit test for this one (it's cross-repo; planner discretion to either add a cross-repo CI step or document-only-no-test for CON-T-05).

    Add UtinniCoreDotNet.Tests/PreservationAudit/PreservationAuditTests.cs + RepoRoot.cs to UtinniCoreDotNet.Tests.csproj `<Compile>` ItemGroup. Confirm `dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj` runs all 23+ new Facts and they pass on the current SHA.

    Update docs/ai/assessment.md §"Solid foundations" with a "STAB-04 audit at v1.0-rc.1: all 24 intact, see .planning/phases/06-.../06-AUDIT.md" disposition.

    Commit atomically as `test(06-05): STAB-04 preservation audit + 24 automated grep tests`.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --configuration Release --logger "console;verbosity=normal"</automated>
  </verify>
  <acceptance_criteria>
    - 06-AUDIT.md exists with at least 24 subsections (grep `CON-N-0|CON-M-0|CON-T-0` returns at least 23 matches; CON-T-05 may be document-only per planner discretion).
    - UtinniCoreDotNet.Tests/PreservationAudit/PreservationAuditTests.cs exists; grep `[Fact]` returns at least 23 matches.
    - UtinniCoreDotNet.Tests/PreservationAudit/RepoRoot.cs exists.
    - UtinniCoreDotNet.Tests.csproj grep `PreservationAudit` returns at least 1 match in a Compile element (or it's auto-included by the SDK-style csproj; verify with `dotnet build` listing the file).
    - Local dotnet test exit code 0; PreservationAudit tests all pass.
    - Repeat the test with a deliberate violation in a scratch branch (e.g., remove the `isSetup` guard from imgui_impl.cpp and re-run) — at least one Fact MUST fail, proving fail-on-violation per [[feedback-max-harness]]. Document this regression-probe run in 06-05-SUMMARY.md (do NOT merge the scratch branch).
    - docs/ai/assessment.md grep `STAB-04 audit at v1.0-rc.1` returns at least one match.
    - One atomic commit on master with prefix `test(06-05):`.
    - CI green on master post-merge.
  </acceptance_criteria>
  <done>STAB-04 audit complete: 24 items documented in 06-AUDIT.md with PASS dispositions; 23+ automated xUnit Facts ship in UtinniCoreDotNet.Tests/PreservationAudit/ and fail on regression per [[feedback-max-harness]].</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cross-repo UtinniPlugins (D:/Code/UtinniPlugins) commit | Standing authority per [[feedback-utinniplugins-authority]] — no human-action checkpoint required. Code commits proceed autonomously; only the live-SWG smoke test requires manual UAT (not in this plan; lands in 06-06). |
| .clang-format full-repo reformat | Cosmetic-only diff; no semantic change; CON-T-02 triple-config preserved across the .vcxproj files because they aren't touched by clang-format. |
| .gitignore changes | Documentation only — pattern lines preserved. |
| Native.SendMessage signature change | Public-API surface — handled by retaining the int-int shim per [[feedback-caller-attrs-binary-compat]]. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Tampering | Bulk clang-format silently changes semantic behaviour | mitigate | Three-commit structure (reformat, .git-blame-ignore-revs, ci clang-format-check) lets a maintainer bisect; SortIncludes=false in .clang-format prevents include-reordering which would have semantic consequences for build-order-dependent headers; Release x86 build exit 0 is the gate before commit. |
| T-06-05-02 | Tampering | dead-code purge removes code that's actually live (callsite still references it) | mitigate | Per-file atomic commits + Release build verify before commit; if a file resists deletion, the executor documents the abort rather than force-deleting. |
| T-06-05-03 | Tampering | Native.SendMessage IntPtr migration breaks pre-built plugin DLLs at MEF compose | mitigate | Binary-compat shim preserves the int-int signature per [[feedback-caller-attrs-binary-compat]]; both signatures coexist; new code uses IntPtr, old DLLs continue to find the int signature. |
| T-06-05-04 | Tampering | STAB-04 preservation audit grep test runs against the wrong path (escape-character bugs in C# string literals) | mitigate | RepoRoot.cs anchors to Utinni.sln location; each test asserts AT LEAST a numeric threshold (>= N matches) so off-by-one path bugs surface as test failures. |
| T-06-05-05 | Tampering | swg/ui/ commented-detour cleanups touched accidentally | mitigate | Task 3 acceptance criteria explicitly check `git diff` shows zero changes under UtinniCore/swg/ui/; carve-out enforced at PR-review level. |
| T-06-05-06 | Information Disclosure | Cross-repo paired commit on UtinniPlugins leaks Utinni internal SHAs | accept | UtinniPlugins is also a public-by-design fork in kennethlong's namespace; the SHA reference is a feature, not a leak. |
| T-06-05-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in 06-05. |

</threat_model>

<verification>
- Task 1 leaves three commits on master (style, chore, ci); CI green.
- Task 2 leaves at least 9 commits on master plus one paired commit on UtinniPlugins; all D-16 acceptance grep checks pass.
- Task 3 leaves at least 5 commits on master; swg/ui/ carve-out enforced by `git diff` check; CI green.
- Task 4 leaves one commit on master; 06-AUDIT.md ships with 24-item enumeration; 23+ xUnit Facts pass locally and on CI.
</verification>

<success_criteria>
1. Full-repo `.clang-format` reformat landed as a single commit + .git-blame-ignore-revs + CI clang-format-check step.
2. TJT.ico ejected cross-repo with neutral framework icon; D-16 (a)-(h) all landed atomically.
3. Dead-code purge complete except swg/ui/ carve-out; swg/ui/ deferral documented in assessment.md.
4. STAB-04 preservation audit + 23+ automated grep tests ship and pass; fail-on-violation regression probe demonstrated.
5. CI green on master across all three lanes (plus the new preservation-audit Facts) after every commit.
</success_criteria>

<output>
Create `.planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-05-SUMMARY.md` when done.
</output>
