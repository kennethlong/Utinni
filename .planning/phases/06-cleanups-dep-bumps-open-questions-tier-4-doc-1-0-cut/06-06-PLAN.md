---
phase: 06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut
plan: 06
type: execute
wave: 6
depends_on: [06-05]
files_modified:
  - .planning/codebase/TESTING.md
  - .planning/codebase/CONVENTIONS.md
  - .github/workflows/release.yml
  - installer/Utinni.Installer.wixproj
  - installer/Product.wxs
  - installer/CustomActions/SwgPathDetector.cs
  - installer/README.md
  - .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-VERIFICATION.md
  - .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-06-MSI-TJT-PINNING.md
  - Utinni.sln
  - README.md
autonomous: false
requirements: [TEST-04, STAB-03, STAB-04, STAB-05]
user_setup:
  - service: github
    why: "Tag-push trigger for v1.0.0-rc.1 + GitHub Pre-release artifact upload"
    env_vars: []
    dashboard_config:
      - task: "Verify GITHUB_TOKEN permissions on Utinni repo allow Actions to create releases + upload assets (default permissions are sufficient on a personal repo)"
        location: "github.com/kennethlong/Utinni/settings/actions"
must_haves:
  truths:
    - ".planning/codebase/TESTING.md contains a Tier-4 residual enumeration covering at minimum: (a) imgui overlay rendering (06-01 procedure verbatim), (b) PanelGame.WndProc forwarding to live SWG, (c) hkPresent + MMO render lifecycle, (d) D3D9 device-loss/reset paths, (e) plugin loader against real plugin DLLs (TJT), (f) drag-drop + WinForms STA, (g) GPU-driver-specific bugs, (h) WinForms UI smoke (FlaUI explicitly EXCLUDED per CON-TT-03). Each scenario row: procedure, success criterion, last-verified SHA."
    - ".planning/codebase/CONVENTIONS.md references .planning/codebase/TESTING.md as the canonical Tier-4 boundary doc per REQUIREMENTS.md §TEST-04."
    - ".github/workflows/release.yml exists and is triggered on `v1.0*` tag push (wildcard scope matches v1.0.0-rc.1 AND future v1.0.x tags per CONTEXT.md `<additional_context>` threat model note); builds Release/x86, builds MSI, attaches MSI to GitHub Release, marks the release as Pre-release for the rc.1 tag."
    - "installer/Utinni.Installer.wixproj exists and produces a WiX 5 MSI; MSI install behaviour: (a) files copy to C:\\Program Files (x86)\\Utinni\\, (b) Start menu shortcut to Launcher.exe, (c) uninstaller, (d) optional default-OFF 'detect SWG client path + seed utinni.cfg' checkbox preserving CON-D-01 ships-blank policy via opt-in only."
    - "MSI bundles UtinniPlugins/TheJawaToolbox at a pinned commit SHA; pinning strategy (submodule vs CI checkout-with-ref) chosen by planner and documented in 06-06-MSI-TJT-PINNING.md."
    - "Maintainer-signed Tier-4 UAT documented in 06-VERIFICATION.md: overlay Demo screen, WndProc forwarding, drag-drop, plugin load with TJT, MSI install + uninstall round-trip on a clean VM."
    - "v1.0.0-rc.1 tag exists on master; corresponding GitHub Pre-release exists with the MSI attached; release notes summarise Phases 1-6 + auto-generated commit list."
    - "CONTEXT.md decisions addressed by this plan: D-19 (TEST-04 Tier-4 residual enumeration extending .planning/codebase/TESTING.md + CONVENTIONS.md cross-ref), D-20 (WiX 5 MSI installer at installer/ + .github/workflows/release.yml tag-triggered), D-21 (MSI bundles TJT per DEC-C4 pair distribution; cross-repo paired commits for the pin), D-22 (v1.0.0-rc.1 tagged + GitHub Pre-release; bake period N=7-14 days; v1.0.0 final promotion is post-phase), D-23 (MSI unsigned for V1; SmartScreen warning accepted), D-24 (pre-tag Tier-4 UAT signed in 06-VERIFICATION.md per [[feedback-max-harness]]). Also references D-01 + D-06 final dispositions at the rc.1 tag."
  artifacts:
    - path: ".planning/codebase/TESTING.md"
      provides: "Tier-4 residual enumeration; reference target from CONVENTIONS.md per TEST-04"
      contains: "Tier 4"
      min_lines: 80
    - path: ".github/workflows/release.yml"
      provides: "Tag-triggered MSI build + GitHub Release publish"
      contains: "v1.0*"
    - path: "installer/Utinni.Installer.wixproj"
      provides: "WiX 5 MSI project producing Utinni.msi installable on Windows 10/11"
      contains: "WiX"
    - path: ".planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-VERIFICATION.md"
      provides: "Maintainer-signed Tier-4 UAT log with date, machine identifier, scenario results"
      contains: "v1.0.0-rc.1"
    - path: ".planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-06-MSI-TJT-PINNING.md"
      provides: "Documented decision for how UtinniPlugins/TheJawaToolbox is pinned in the MSI build (submodule vs checkout-with-ref)"
  key_links:
    - from: ".planning/codebase/CONVENTIONS.md"
      to: ".planning/codebase/TESTING.md Tier-4 enumeration"
      via: "literal cross-reference line in CONVENTIONS.md"
      pattern: "TESTING\\.md"
    - from: ".github/workflows/release.yml `on: push: tags`"
      to: "MSI build + GitHub Release upload"
      via: "actions/checkout + setup-msbuild + WiX build + softprops/action-gh-release"
      pattern: "v1\\.0"
    - from: "Utinni.sln"
      to: "installer/Utinni.Installer.wixproj"
      via: "<Project> entry in solution"
      pattern: "Utinni\\.Installer"
---

<objective>
06-06 is the phase-closing plan: extend `.planning/codebase/TESTING.md` with the full Tier-4 residual enumeration (TEST-04), reference it from CONVENTIONS.md, ship a new `.github/workflows/release.yml` triggered on `v1.0*` tag pushes, create a new `installer/Utinni.Installer.wixproj` WiX 5 MSI project that bundles UtinniCore + UtinniCoreDotNet + Launcher + TheJawaToolbox (cross-repo pinned per D-21), run maintainer-signed Tier-4 UAT capturing the overlay Demo + WndProc forwarding + drag-drop + plugin load with TJT + MSI install/uninstall round-trip, and push the `v1.0.0-rc.1` tag with corresponding GitHub Pre-release.

Purpose: D-19 closes TEST-04; D-20 ships MSI; D-21 bundles TJT (DEC-C4 pair distribution); D-22 tags v1.0.0-rc.1; D-23 ships unsigned; D-24 maintainer-signed pre-tag Tier-4 UAT per [[feedback-max-harness]] (CI green is necessary but not sufficient).

Output: TESTING.md Tier-4 enumeration referenced from CONVENTIONS.md; release.yml + installer/ WiX project + cross-repo TJT pinning + maintainer-signed 06-VERIFICATION.md + v1.0.0-rc.1 tag on master + corresponding GitHub Pre-release with MSI artifact.

This plan is NOT autonomous (`autonomous: false`) because the maintainer-signed Tier-4 UAT (Task 4) is a hard human-verify checkpoint per D-24 + REQUIREMENTS.md §TEST-04, and the v1.0.0-rc.1 tag push (Task 5) is a checkpoint:decision-shaped action that requires explicit maintainer approval ("ship the release candidate").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-CONTEXT.md
@.planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-01-DEMO-PROBE-NOTES.md
@.planning/intel/constraints.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@docs/ai/test-harness-plan.md

<interfaces>
<!-- Reference state captured at planning time; executor reads files at task time. -->

From REQUIREMENTS.md §TEST-04:
- Acceptance: "Tier 4 boundary documented in `.planning/codebase/TESTING.md` or a sibling file; documentation referenced from CONVENTIONS.md; V1 ships with the documented residual rather than attempting to automate it."

From 06-CONTEXT.md D-19 enumeration (cited verbatim):
- (a) imgui overlay rendering — the 06-01 exit-criterion procedure becomes the canonical Tier-4 row.
- (b) PanelGame.WndProc forwarding to live SWG — R-C precedent from Phase 3 D-06.
- (c) hkPresent + MMO render lifecycle.
- (d) D3D9 device-loss/reset paths — CON-N-06 preservation context.
- (e) Plugin loader against real plugin DLLs (TJT, future Wave-1 plugins).
- (f) Drag-drop in editor + WinForms STA — post-Phase 02.1 WR-09.
- (g) GPU-driver-specific bugs.
- (h) WinForms UI smoke (FlaUI explicitly EXCLUDED per CON-TT-03 — deliberate Tier-3 V2 deferral).

From 06-CONTEXT.md D-20 + D-21 + D-22 + D-23 + D-24:
- WiX 5 (latest). New `installer/` directory at repo root. release.yml triggered by `v1.0*` tag push: builds Release/x86, builds MSI, attaches to GitHub Release.
- Install path: `C:\Program Files (x86)\Utinni\` (x86 per CON-P-02).
- Optional default-OFF SWG-client-path detection checkbox + utinni.cfg seed (probes `C:\SWGEmu\`, `C:\Program Files\SWGEmu\`, registry keys).
- No file-type associations in 1.0-rc.1 (defer to V1 release at Phase 11 closure).
- TJT pinned at a specific UtinniPlugins commit SHA; pinning strategy = planner's discretion (submodule vs checkout-with-ref).
- v1.0.0-rc.1 = SemVer rc; auto-generated release notes from git log; bake period N=7-14 days planner picks; promotion to v1.0.0 final happens POST-PHASE.
- MSI unsigned for V1 (SmartScreen warning acceptable; cost not justified).
- Maintainer-signed Tier-4 UAT in 06-VERIFICATION.md before the tag push.

From .planning/codebase/TESTING.md (existing content):
- TL;DR: "There Are No Tests" — this doc was written at ingest time before the Phase-1/3/4/5 test infrastructure landed. The Tier-4 enumeration must extend this doc and may need to add a "Test infrastructure status" note correcting the no-tests TL;DR; alternatively the Tier-4 row is appended in a new section without rewriting the historical narrative. Planner discretion at execution time.

From .planning/codebase/CONVENTIONS.md:
- Existing content focuses on style. A new section "Tier 4 manual residual" with a one-line cross-reference to TESTING.md is added per TEST-04 acceptance criterion.

From CONTEXT.md `<additional_context>` security gate notes:
- MSI installer: file install paths + optional SWG-client-path detection probing user paths/registry + opt-in seed of utinni.cfg.
- Cross-repo paired commits + new .github/workflows/release.yml — release pipeline integrity (token scopes, no secrets in logs).
- Tag-push trigger on `v1.0*` — wildcard scope matches v1.0.0-rc.1 plus all future v1.0.x tags; intentional, but document in the threat model.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TESTING.md Tier-4 enumeration + CONVENTIONS.md cross-reference (TEST-04)</name>
  <files>
    .planning/codebase/TESTING.md,
    .planning/codebase/CONVENTIONS.md
  </files>
  <read_first>
    .planning/codebase/TESTING.md,
    .planning/codebase/CONVENTIONS.md,
    .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-01-DEMO-PROBE-NOTES.md,
    .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-01-VERIFICATION.md,
    docs/ai/test-harness-plan.md
  </read_first>
  <action>
    Extend `.planning/codebase/TESTING.md` with a new top-level section titled `## Tier 4 — Manual Residual (TEST-04)` placed after the existing content. Begin the section with a short paragraph correcting the existing TL;DR: as of Phase 6 the repo ships three CI test lanes (dotnet test + CLI golden + UtinniCore.Tests.exe) plus the new PreservationAudit Facts; the Tier-4 residual below is the BOUNDED scope that remains manual.

    For each of D-19's eight enumeration items (a-h), author a subsection with this skeleton:

    ```
    ### Tier-4 Scenario (a): Imgui overlay rendering

    - **Why manual:** Visual judgment + live d3d9 device state inside an injected SWG process; no scriptable assertion exists for "did the user see the demo screen correctly."
    - **Procedure:**
      1. Set `g_showDemoWindowProbe = true` in `UtinniCore/swg/ui/imgui_impl.cpp`.
      2. Build Release x86: `msbuild Utinni.sln /p:Configuration=Release /p:Platform=x86`.
      3. Launch SWGEmu via Launcher.exe; log in; observe the imgui Demo window over the SWG render.
      4. Exercise menus + sliders + buttons + tabs + plots + popups + drag-and-drop.
    - **Success criterion:** Each widget category behaves as in the standalone imgui demo app.
    - **Last-verified SHA:** <06-01-VERIFICATION.md merge SHA>
    - **Failure-mode escalation:** Re-open 06-01 plan; rerun [[feedback-d3d9-hook-diagnosis]] first-move check.
    ```

    Repeat the skeleton for (b) PanelGame.WndProc forwarding (procedure: launch SWGEmu via Launcher, log in, press alphanumeric + arrow keys, confirm SWG chat receives them; success: WM_CHAR/WM_KEYDOWN reach SWG; last-verified SHA: identify the most recent verified commit from STATE.md history). (c) hkPresent + MMO render lifecycle (procedure: launch, log in, transition between scenes 3+ times, confirm single-cycle setup callbacks fire and no allocator fragmentation crashes; success: utinni.log shows clean `hkMainLoop: loadNewScene -> Game::cleanupScene -> hkSetScene(null) -> hkMainLoop: setupScene -> hkSetScene(<new>) -> firing 1 setSceneCallbacks` per transition). (d) D3D9 device-loss/reset paths (procedure: launch, alt-tab repeatedly OR change SWG resolution, confirm imgui_impl::isSetup invalidates+recreates per CON-N-06; success: no D3DERR_INVALIDCALL fatal). (e) Plugin loader against real plugin DLLs (procedure: install Utinni via MSI on a clean Windows VM with TJT bundled; launch; confirm TJT loads as a panel; success: TJT plugin visible in editor host with no exceptions in utinni.log). (f) Drag-drop in editor + WinForms STA (procedure: open FormObjectBrowser, drag a template into the world panel; success: preview-object follows cursor, ray-cast commits on drop). (g) GPU-driver-specific bugs (procedure: run on at least two GPU vendors — Nvidia + Intel/AMD; success: render correct on both, depth-resolve callbacks fire correctly). (h) WinForms UI smoke (procedure: open every form in the editor host, resize, minimize, restore; success: no UI hangs, no exceptions; **FlaUI explicitly EXCLUDED per CON-TT-03 — this remains a manual smoke walk forever**).

    Each subsection MUST have a "Last-verified SHA" field — placeholder if not yet verified, filled-in if Task 4 sign-off has already happened for that scenario.

    Update `.planning/codebase/CONVENTIONS.md` by appending a new section `## Tier 4 — Manual Verification Residual` containing a one-line cross-reference: "See `.planning/codebase/TESTING.md` §'Tier 4 — Manual Residual (TEST-04)' for the bounded scope of manual verification that ships unautomated in V1." Per REQUIREMENTS.md §TEST-04 acceptance: "Tier 4 boundary documented in `.planning/codebase/TESTING.md` or a sibling file; documentation referenced from CONVENTIONS.md."

    Commit atomically as `docs(06-06): Tier-4 residual enumeration in TESTING.md; CONVENTIONS.md cross-reference (TEST-04)`.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$t = Get-Content '.planning/codebase/TESTING.md' -Raw; $c = Get-Content '.planning/codebase/CONVENTIONS.md' -Raw; if (($t -match 'Tier 4.*TEST-04') -and ($t -match 'Imgui overlay') -and ($t -match 'FlaUI.*EXCLUDED') -and ($c -match 'TESTING\.md')) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/codebase/TESTING.md grep `Tier 4` returns at least one section-header match.
    - .planning/codebase/TESTING.md grep `Scenario \([a-h]\)` returns 8 matches (one per enumeration item a..h).
    - .planning/codebase/TESTING.md grep `Last-verified SHA` returns at least 8 matches (one per scenario).
    - .planning/codebase/TESTING.md grep `FlaUI.*EXCLUDED|EXCLUDED.*FlaUI|CON-TT-03` returns at least one match (scenario (h) exclusion documented).
    - .planning/codebase/CONVENTIONS.md grep `TESTING.md` returns at least one match.
    - One atomic commit on master with prefix `docs(06-06):`.
    - CI green on master post-merge.
  </acceptance_criteria>
  <done>TEST-04 acceptance criteria met: Tier-4 residual enumerated in TESTING.md; CONVENTIONS.md references it.</done>
</task>

<task type="auto">
  <name>Task 2: WiX 5 MSI installer scaffold (installer/) + Utinni.sln integration</name>
  <files>
    installer/Utinni.Installer.wixproj,
    installer/Product.wxs,
    installer/CustomActions/SwgPathDetector.cs,
    installer/CustomActions/SwgPathDetector.csproj,
    installer/README.md,
    Utinni.sln
  </files>
  <read_first>
    Utinni.sln,
    UtinniCore/UtinniCore.vcxproj,
    UtinniCoreDotNet/UtinniCoreDotNet.csproj,
    Launcher/Launcher.vcxproj,
    data/,
    .planning/intel/constraints.md
  </read_first>
  <action>
    Create a new `installer/` directory at Utinni repo root. Inside, create `Utinni.Installer.wixproj` as a WiX 5 .NET-SDK-style project — verify WiX 5 install instructions at task time (`dotnet add package WixToolset.Sdk` or equivalent; Microsoft.WiX 5.x via vcpkg / NuGet). Project structure:

    1. **installer/Utinni.Installer.wixproj** — WiX SDK csproj-style file declaring `<Sdk Name="WixToolset.Sdk" Version="5.x.x"/>` and a `<ProjectReference>` to the SwgPathDetector custom-actions project.
    2. **installer/Product.wxs** — the WiX source file defining:
       - `<Package Manufacturer="Kenneth Long" Name="Utinni" Version="1.0.0" UpgradeCode="<planner-picked stable GUID>" InstallScope="perMachine">` — UpgradeCode MUST be a stable GUID across versions per WiX upgrade-path convention; generate one once at execution time and document in installer/README.md.
       - `<MajorUpgrade DowngradeErrorMessage="..."/>` to enable in-place upgrades for future v1.0.x releases.
       - Install dir: `<StandardDirectory Id="ProgramFiles6432Folder">` → `<Directory Id="INSTALLFOLDER" Name="Utinni">` resolves to `C:\Program Files (x86)\Utinni\` (per CON-P-02 x86 build).
       - File components: UtinniCore.dll, UtinniCoreDotNet.dll, Launcher.exe, UtINI.dll, data/utinni.cfg (template with blank login fields per CON-D-01), licenses.txt, plus the `Plugins\TheJawaToolbox\` subdirectory populated from Task 3.
       - Start Menu shortcut: `<StandardDirectory Id="ProgramMenuFolder">` with a shortcut to Launcher.exe + uninstall shortcut.
       - **Opt-in checkbox** — UI feature `<UI Id="UtinniInstallerUi">` declaring a custom WiX dialog with a "Detect SWG client path and seed utinni.cfg (optional)" checkbox defaulting to UNCHECKED. If checked, the SwgPathDetector custom action runs at the InstallFinalize stage.
    3. **installer/CustomActions/SwgPathDetector.cs** — managed C# custom action (.NET Framework 4.7.2 to match the rest of the project) using `Microsoft.Deployment.WindowsInstaller.CustomAction`. Probes:
       - Registry: HKLM\SOFTWARE\WOW6432Node\SWGEmu, HKCU\SOFTWARE\SWGEmu, HKLM\SOFTWARE\StarWarsGalaxies
       - Filesystem: C:\SWGEmu\, C:\Program Files\SWGEmu\, C:\Program Files (x86)\SWGEmu\
       - If exactly one path is found AND user opted in: prompt for confirmation via MsiBox, write the path to `C:\Program Files (x86)\Utinni\data\utinni.cfg` Launcher.swgClientPath. If multiple/none found, log a friendly note + leave utinni.cfg blank (CON-D-01).
    4. **installer/CustomActions/SwgPathDetector.csproj** — managed csproj for the custom action.
    5. **installer/README.md** — documents:
       - WiX 5 SDK version pinned.
       - UpgradeCode GUID (so anyone forking the installer knows the upgrade path is preserved).
       - ProductCode versioning strategy: each release auto-generates a new ProductCode (default WiX behaviour when `Version` changes), UpgradeCode stays stable.
       - Build command: `dotnet build installer/Utinni.Installer.wixproj -c Release` produces `installer/bin/Release/Utinni.msi`.
       - Test-on-clean-VM procedure.

    Add `installer/Utinni.Installer.wixproj` AND `installer/CustomActions/SwgPathDetector.csproj` to Utinni.sln (read the existing solution structure and add both projects in the same Solution Folder if convenient — planner discretion).

    Commit atomically as `feat(06-06): WiX 5 MSI installer scaffold (installer/, opt-in SWG-path detector, CON-D-01 preserved)`.

    Verify locally: `dotnet build installer/Utinni.Installer.wixproj -c Release` exits 0 and produces a `.msi` artifact. Do NOT yet run the MSI — Task 4 covers the install/uninstall round-trip on a clean VM.
  </action>
  <verify>
    <automated>dotnet build installer/Utinni.Installer.wixproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - installer/Utinni.Installer.wixproj exists; grep `WixToolset.Sdk` returns at least one match.
    - installer/Product.wxs exists; grep `UpgradeCode=` returns at least one match (a stable GUID).
    - installer/Product.wxs grep `Program Files \(x86\)|ProgramFiles6432Folder` returns at least one match (CON-P-02 install path).
    - installer/Product.wxs grep `Detect SWG client path` (or equivalent UI string) returns at least one match (opt-in checkbox).
    - installer/CustomActions/SwgPathDetector.cs exists; grep `SWGEmu` returns at least one match (registry/filesystem probe).
    - installer/README.md exists with UpgradeCode GUID + build instructions.
    - Utinni.sln grep `installer\\Utinni.Installer.wixproj` returns at least one match.
    - Local build exit 0: `dotnet build installer/Utinni.Installer.wixproj -c Release` produces a .msi file (verify with `Test-Path installer/bin/Release/Utinni.msi` or equivalent).
    - One atomic commit on master with prefix `feat(06-06):`.
    - CI green on master post-merge (the installer csproj may or may not build on CI depending on WiX SDK availability; verify at task time whether to add `dotnet build installer/` to ci.yml or restrict installer build to release.yml).
  </acceptance_criteria>
  <done>WiX 5 MSI scaffold ships; SwgPathDetector custom action implements opt-in detection preserving CON-D-01; local MSI build succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: release.yml workflow + cross-repo TJT pinning</name>
  <files>
    .github/workflows/release.yml,
    .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-06-MSI-TJT-PINNING.md,
    installer/Product.wxs
  </files>
  <read_first>
    .github/workflows/ci.yml,
    .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-CONTEXT.md,
    installer/Utinni.Installer.wixproj,
    installer/Product.wxs
  </read_first>
  <action>
    Decide TJT pinning strategy. Author 06-06-MSI-TJT-PINNING.md comparing:
    - **OPT-A:** git submodule — UtinniPlugins added as a submodule at `external-plugins/UtinniPlugins/` (NOT inside `external/` which is reserved for vcpkg-eligible deps); release workflow does `submodule update --init` before MSI build; pin SHA is in `.gitmodules`. Pro: most reproducible; con: extra ceremony for non-release contributors.
    - **OPT-B:** CI checkout-with-ref — release.yml has a second `actions/checkout@v4` step targeting `kennethlong/UtinniPlugins` at a hard-coded ref captured as a workflow input or hard-coded SHA literal in the YAML. Pro: simpler for contributors; con: less reproducible if the SHA literal drifts.

    Pick one with planner rationale (recommend OPT-B because the MSI is built only on release tags and the UtinniPlugins commit SHA is meaningful only at release time; the submodule overhead doesn't justify the win for a single rare workflow). Implement the choice.

    For OPT-B: author `.github/workflows/release.yml`:
    ```
    name: Release

    on:
      push:
        tags: [v1.0*]

    permissions:
      contents: write  # required to create GitHub Releases

    jobs:
      build-and-publish-msi:
        runs-on: windows-2022
        steps:
          - name: Checkout Utinni
            uses: actions/checkout@v4
            with: { fetch-depth: 0 }
          - name: Checkout UtinniPlugins (pinned)
            uses: actions/checkout@v4
            with:
              repository: kennethlong/UtinniPlugins
              ref: <pinned-SHA-from-06-06-MSI-TJT-PINNING.md>
              path: external-plugins/UtinniPlugins
          # ...vcpkg setup mirroring ci.yml...
          - name: Setup MSBuild
            uses: microsoft/setup-msbuild@v2
          - name: Build Utinni (Release|x86)
            run: msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86
          - name: Build TheJawaToolbox (Release|AnyCPU)
            working-directory: external-plugins/UtinniPlugins
            run: msbuild TheJawaToolbox.sln /m /restore /p:Configuration=Release
          - name: Stage TJT into installer payload
            shell: pwsh
            run: |
              # Copy TheJawaToolbox.dll + its dependencies into installer/Payload/Plugins/TheJawaToolbox/
              New-Item -ItemType Directory -Force -Path installer/Payload/Plugins/TheJawaToolbox
              Copy-Item external-plugins/UtinniPlugins/TheJawaToolbox/bin/Release/*.dll installer/Payload/Plugins/TheJawaToolbox/
          - name: Build MSI
            run: dotnet build installer/Utinni.Installer.wixproj -c Release
          - name: Create GitHub Release (Pre-release for rc tags)
            uses: softprops/action-gh-release@v2
            with:
              files: installer/bin/Release/Utinni.msi
              prerelease: ${{ contains(github.ref, '-rc') }}
              generate_release_notes: true
              body: |
                # Utinni ${{ github.ref_name }}

                Pre-release candidate for v1.0; bake period 7-14 days before promotion to final v1.0.0.

                ## Highlights (Phases 1-6)
                - Phase 1: CI + xUnit scaffold
                - Phase 2 + 02.1: 15 critical bug closures + correctness gap closures
                - Phase 3: 8 strategic reworks (R-A..R-H)
                - Phase 4: Tier-2 CLI shim + golden fixtures
                - Phase 5: Tier-1 C++ unit tests (Catch2)
                - Phase 6: vcpkg migration, custom INI parser, DXSDK removal, full Tier-4 doc, MSI installer, TJT bundling

                ## Install
                Download `Utinni.msi`, run, accept SmartScreen warning (unsigned — V1 policy). Installs to `C:\Program Files (x86)\Utinni\`. TJT panel bundled.
    ```

    Update installer/Product.wxs to read TJT DLLs from `installer/Payload/Plugins/TheJawaToolbox/` (the path the release.yml workflow stages into); under `<Component Id="TjtComponent">` group, declare files from that staging path. Locally (without release.yml), this directory won't exist — the .wixproj must tolerate the empty directory with a build-time pre-step that creates an empty placeholder OR fail gracefully with a clear "TJT payload not staged — this build is local-only" warning.

    Commit atomically as `ci(06-06): release.yml triggered on v1.0* tag; bundles TJT pinned at <SHA>`.

    Verify release.yml syntax: `gh workflow view release.yml` or push to a feature branch and observe the workflow listed in `gh workflow list`. Do NOT push the v1.0.0-rc.1 tag yet — Task 5 covers that.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$y = Get-Content '.github/workflows/release.yml' -Raw; if (($y -match 'v1\.0\*') -and ($y -match 'softprops/action-gh-release') -and ($y -match 'kennethlong/UtinniPlugins') -and ($y -match 'prerelease')) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/release.yml exists.
    - .github/workflows/release.yml grep `tags: \[v1\.0\*\]` (or equivalent) returns at least one match.
    - .github/workflows/release.yml grep `softprops/action-gh-release` returns at least one match.
    - .github/workflows/release.yml grep `kennethlong/UtinniPlugins` returns at least one match (cross-repo pin).
    - .github/workflows/release.yml grep `prerelease: \$\{\{ contains` (or equivalent prerelease-on-rc-suffix logic) returns at least one match.
    - 06-06-MSI-TJT-PINNING.md exists; grep `OPT-A|OPT-B` returns at least one match with planner-selected option.
    - 06-06-MSI-TJT-PINNING.md grep `40-char SHA` for the literal SHA hex (regex `[a-f0-9]{40}`) returns at least one match identifying the pinned UtinniPlugins commit.
    - installer/Product.wxs grep `Plugins\\TheJawaToolbox|TjtComponent` returns at least one match.
    - One atomic commit on master with prefix `ci(06-06):`.
    - CI green on master post-merge (CI workflow itself is not changed; release workflow is dormant until tag-push).
  </acceptance_criteria>
  <done>release.yml ships triggered on v1.0* tag-push; cross-repo TJT pinning documented + implemented per planner's selected OPT; release notes template prepared.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Maintainer-signed Tier-4 UAT (06-VERIFICATION.md per D-24)</name>
  <files>
    .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-VERIFICATION.md,
    .planning/codebase/TESTING.md
  </files>
  <read_first>
    .planning/codebase/TESTING.md,
    .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-01-VERIFICATION.md,
    .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-CONTEXT.md
  </read_first>
  <what-built>
    Tasks 1-3 shipped the TESTING.md Tier-4 enumeration, the WiX 5 MSI installer scaffold (with opt-in SwgPathDetector custom action), and the release.yml tag-triggered MSI publish workflow with cross-repo TJT pinning. The codebase is now ready for the maintainer-signed pre-tag Tier-4 UAT per D-24 + [[feedback-max-harness]].
  </what-built>
  <how-to-verify>
    Per D-24: CI green is necessary but not sufficient — the maintainer hand-walks every Tier-4 scenario before the v1.0.0-rc.1 tag is pushed. Each step in the list below must pass; ANY failure re-opens the relevant earlier plan rather than papering over.

    1. **Scenario (a) — Imgui overlay Demo screen:** flip `g_showDemoWindowProbe = true`, rebuild Release x86, inject via Launcher.exe into live SWGEmu, log in, exercise menus + sliders + buttons + tabs + plots + popups + drag-and-drop. Expected: each widget category works as in the standalone imgui demo. Update TESTING.md scenario (a) `Last-verified SHA` with the current master SHA.

    2. **Scenario (b) — PanelGame.WndProc forwarding:** in the same SWG session, press alphanumeric keys + arrow keys + Tab; confirm SWG chat receives them. Update TESTING.md scenario (b).

    3. **Scenario (c) — hkPresent + MMO render lifecycle:** transition between scenes 3+ times via TJT's "Load Scene" panel (Tatooine → Naboo → Lok → Tatooine); confirm utinni.log shows single-cycle setup callbacks per transition; no allocator fragmentation crashes. Update TESTING.md scenario (c).

    4. **Scenario (d) — D3D9 device-loss/reset paths:** alt-tab away from SWG and back; confirm imgui_impl::isSetup invalidates+recreates per CON-N-06; no D3DERR_INVALIDCALL fatal in utinni.log. Update TESTING.md scenario (d).

    5. **Scenario (e) — Plugin loader against TJT:** [will be re-verified after MSI install in step 7; for now, confirm TJT loads via the dev-build path]. Update TESTING.md scenario (e) marker.

    6. **Scenario (f) — Drag-drop + WinForms STA:** in the editor host, open FormObjectBrowser; drag a template into the world panel; preview-object follows cursor; commit on drop succeeds. Update TESTING.md scenario (f).

    7. **Scenario (g) — GPU-driver-specific bugs:** run once on Nvidia hardware (your usual dev machine) AND once on Intel/AMD if available. Document GPU vendor + driver version in 06-VERIFICATION.md. If only one GPU vendor is available locally, note the deferral for the rc-bake period. Update TESTING.md scenario (g).

    8. **Scenario (h) — WinForms UI smoke:** open every form in the editor host (FormMain, FormLog, FormObjectBrowser, FormPlugins, etc. — enumerate at task time), resize, minimize, restore; no UI hangs, no exceptions. Update TESTING.md scenario (h).

    9. **MSI install + uninstall round-trip on a clean VM:** spin up a clean Windows 10/11 VM (Hyper-V, VirtualBox, or VMware — planner picks). On the VM:
       - Download the locally-built `installer/bin/Release/Utinni.msi` (transfer via shared folder or HTTP).
       - Run the MSI; accept SmartScreen warning (D-23 unsigned).
       - Verify install path: `C:\Program Files (x86)\Utinni\` populated with UtinniCore.dll, UtinniCoreDotNet.dll, Launcher.exe, UtINI.dll, data/utinni.cfg (blank), licenses.txt, Plugins/TheJawaToolbox/*.dll.
       - Verify Start Menu shortcut launches Launcher.exe.
       - If the install prompted for "Detect SWG client path", confirm BOTH paths work: opt-in (with a known SWG install on the VM) AND opt-out (default; utinni.cfg stays blank per CON-D-01).
       - Run Launcher.exe on the VM (no live SWG injection — this is install/uninstall validation, not full Tier-4 (e) scenario which already ran on the dev machine). Confirm Launcher GUI comes up.
       - Uninstall via Settings → Apps. Verify `C:\Program Files (x86)\Utinni\` is removed AND no leftover registry keys.
       - Reinstall over the uninstall to verify the install state is clean (no "files in use" errors).

    10. Write `.planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-VERIFICATION.md` capturing:
        - Date + dev-machine identifier + clean-VM identifier (OS version, virtualization platform).
        - Per-scenario PASS/FAIL with maintainer-signed notes.
        - Any DEFERRED items (e.g., second GPU vendor) explicitly listed with bake-period follow-up plan.
        - Final disposition: "All Tier-4 scenarios PASS; v1.0.0-rc.1 tag clear to push."

    11. Commit atomically as `docs(06-06): Tier-4 UAT signoff + TESTING.md last-verified SHAs (pre-tag)`.

    12. Confirm CI green on master after the doc-only commit.
  </how-to-verify>
  <resume-signal>Type "approved — push the rc.1 tag" once all scenarios PASS and 06-VERIFICATION.md is committed; describe any failure that re-opens an earlier plan.</resume-signal>
  <acceptance_criteria>
    - 06-VERIFICATION.md exists with date + dev-machine + clean-VM identifiers + per-scenario PASS/FAIL.
    - 06-VERIFICATION.md grep `v1.0.0-rc.1.*clear|tag clear to push` returns at least one match.
    - TESTING.md `Last-verified SHA` placeholders for each scenario (a)-(h) are filled in with the merge SHA of this commit.
    - MSI install/uninstall round-trip on a clean VM documented in 06-VERIFICATION.md with screenshots OR a step-by-step log.
    - One atomic commit on master with prefix `docs(06-06):`.
    - CI green on master post-merge.
  </acceptance_criteria>
  <done>Maintainer-signed Tier-4 UAT complete per D-24; TESTING.md last-verified SHAs filled in; v1.0.0-rc.1 tag cleared to push.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 5: Push v1.0.0-rc.1 tag + verify GitHub Pre-release artifact</name>
  <files>
    .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-VERIFICATION.md
  </files>
  <read_first>
    .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-VERIFICATION.md,
    .github/workflows/release.yml
  </read_first>
  <decision>
    Push the v1.0.0-rc.1 tag now, triggering release.yml, OR identify a blocker that requires re-opening an earlier plan.
  </decision>
  <context>
    Per D-22, Phase 6 ends with v1.0.0-rc.1 tag + GitHub Pre-release. Per D-24, maintainer-signed Tier-4 UAT precedes the tag. Per [[feedback-max-harness]], we don't ship on green-CI alone — Task 4 has already happened. If 06-VERIFICATION.md disposition is "tag clear to push", the only remaining decision is whether the planner-chosen bake period (7-14 days) is acceptable. The bake period itself is POST-PHASE per 06-CONTEXT.md `<specifics>` — Phase 6 ends at the rc.1 tag + Pre-release; promotion to v1.0.0 final lives outside Phase 6.
  </context>
  <options>
    <option id="option-push-now">
      <name>Push v1.0.0-rc.1 tag now</name>
      <pros>06-VERIFICATION.md signed; release.yml workflow ready; MSI build verified locally; CI green; on-time release closure.</pros>
      <cons>Bake period clock starts; if a critical issue surfaces in rc.1 bake, ship rc.2 (still inside Phase 6's "rc cycle" if needed, or as a small post-phase increment).</cons>
    </option>
    <option id="option-extend-uat">
      <name>Extend Tier-4 UAT before tagging (additional GPU vendor, additional clean-VM OS, longer dev-machine soak)</name>
      <pros>More coverage before public artifact.</pros>
      <cons>Phase 6 stays open longer; the bake period is the right place for extended coverage, not the pre-tag UAT.</cons>
    </option>
    <option id="option-reopen-earlier-plan">
      <name>A blocker was found in Task 4; re-open the relevant earlier plan (06-01 / 06-02 / 06-03 / 06-04 / 06-05)</name>
      <pros>Don't ship broken; fix at the source.</pros>
      <cons>Phase 6 timeline slips; resume Task 4 after fix.</cons>
    </option>
  </options>
  <action>
    If `option-push-now` is selected:
    1. From repo root: `git tag -a v1.0.0-rc.1 -m "v1.0.0-rc.1 — framework 1.0 cut" <06-VERIFICATION.md commit SHA>` (annotated tag pointing to the verified commit, NOT to HEAD if HEAD has drifted; verify with `git log --oneline -5`).
    2. `git push origin v1.0.0-rc.1` (push the tag — pre-authorized per [[feedback-push-permission]] standing permission).
    3. Wait for release.yml to run: `gh run watch` or `gh run list --workflow=Release --limit 1`.
    4. After release.yml completes, verify:
       - `gh release view v1.0.0-rc.1 --json isPrerelease,assets,name,body -q '.isPrerelease'` returns true.
       - `gh release view v1.0.0-rc.1 --json assets -q '.assets[].name'` includes `Utinni.msi`.
       - Download the MSI from the Release page on a fresh machine + repeat the install/uninstall round-trip (extending Task 4's local-build VM test to the actual artifact).
    5. Append to 06-VERIFICATION.md: "v1.0.0-rc.1 tag pushed at <UTC datetime>; GitHub Pre-release published; MSI artifact at <release URL>. Bake period begins; promotion to v1.0.0 happens post-phase per D-22 (planner picks N=7-14 days)."
    6. Commit the doc update as `docs(06-06): v1.0.0-rc.1 release artifact verified; bake period begins`.

    If `option-reopen-earlier-plan` is selected:
    1. Document the blocker in 06-VERIFICATION.md with the earlier plan ID and the specific scenario that failed.
    2. Re-open that plan via `/gsd:plan-phase 06 --revision` or by editing the plan directly per gap-closure mode.
    3. Re-run Task 4 after the fix lands.
  </action>
  <resume-signal>Select: option-push-now, option-extend-uat, or option-reopen-earlier-plan (with the plan ID).</resume-signal>
  <acceptance_criteria>
    - If option-push-now selected: `git tag -l v1.0.0-rc.1` returns the tag locally; `gh release view v1.0.0-rc.1` succeeds (returns the release); `gh release view v1.0.0-rc.1 --json isPrerelease -q '.isPrerelease'` returns true; `gh release view v1.0.0-rc.1 --json assets -q '.assets[].name' | grep -i 'utinni.msi'` returns at least one match.
    - 06-VERIFICATION.md final commit grep `v1.0.0-rc.1 tag pushed` returns at least one match (post-tag entry).
    - One atomic commit on master with prefix `docs(06-06):` capturing the post-tag verification.
    - CI green on master post-merge.
  </acceptance_criteria>
  <done>v1.0.0-rc.1 tag pushed; release.yml ran successfully; GitHub Pre-release with MSI artifact verified; bake period begins; Phase 6 complete.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MSI installer file payload | Trusted side (built from Utinni source + pinned UtinniPlugins commit); installer writes to Program Files which requires admin elevation — standard MSI threat model. |
| SwgPathDetector custom action probing user filesystem + registry | Reads HKLM/HKCU keys + filesystem paths; writes to utinni.cfg only with user opt-in consent + confirmation prompt — preserves CON-D-01 ships-blank policy. |
| Cross-repo checkout of kennethlong/UtinniPlugins in release.yml | Pinned to a 40-char SHA literal in the workflow; pin is immutable per release; no untrusted ref resolution. |
| Tag-push wildcard `v1.0*` | Matches v1.0.0-rc.1 + future v1.0.x AND v1.0.0-rc.2/3 etc. — intentional per CONTEXT.md `<additional_context>`; trigger is content-addressable to the maintainer's push credentials. |
| GITHUB_TOKEN scope in release.yml | `permissions: contents: write` minimum required to publish releases; no other scopes granted. |
| MSI unsigned (D-23 V1 policy) | SmartScreen warning surfaces on first install; user-consent gate; documented in installer README + GitHub Release notes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Spoofing | Tag-push trigger fires on a stolen credential or a force-pushed tag | mitigate | Tag-push permission is gated by GitHub's repo-write ACL; force-push to protected tags can be disabled in repo settings (planner notes this as a follow-up in 06-VERIFICATION.md); SHA in the annotated tag message provides forensic provenance. |
| T-06-06-02 | Tampering | SwgPathDetector writes to utinni.cfg without consent | mitigate | Default-OFF checkbox; explicit confirmation prompt before write; CON-D-01 ships-blank policy explicit in Product.wxs UI strings; opt-out leaves utinni.cfg blank. |
| T-06-06-03 | Tampering | SwgPathDetector reads invalid registry/filesystem state and writes garbage to utinni.cfg | mitigate | "Multiple/none found" branch logs a friendly note + leaves utinni.cfg blank; only exactly-one-path-found triggers the write prompt; user can always cancel. |
| T-06-06-04 | Repudiation | Release.yml runs unattended; if compromised could publish a bogus artifact | mitigate | Workflow source is in-tree (review-able via PR before merging changes to release.yml); `permissions: contents: write` is least-privilege; release notes auto-generate from git log so tampered content is detectable. |
| T-06-06-05 | Information Disclosure | Release.yml logs leak secrets | mitigate | `permissions:` block restricts token scope; no `${{ secrets.* }}` references except the implicit GITHUB_TOKEN; SwgPathDetector custom action runs client-side only, not in CI logs. |
| T-06-06-06 | Denial of Service | release.yml runs every push (mis-trigger) | mitigate | `on: push: tags: [v1.0*]` only fires on TAG pushes (not branch pushes); wildcard scope is intentional per CONTEXT.md threat model. |
| T-06-06-07 | Elevation of Privilege | MSI install elevates to admin (writes to Program Files) | accept | Standard Windows installer threat model; install-time elevation is required for Program Files writes; user consent gate via UAC prompt. |
| T-06-06-08 | Spoofing | MSI is unsigned (D-23) | accept | V1 policy; SmartScreen warning surfaces to user; documented in README + release notes; revisit in V2 once funding allows Authenticode cert. |
| T-06-06-09 | Tampering | Cross-repo TJT bundling resolves to a different SHA than the documented pin | mitigate | OPT-B uses a hard-coded SHA literal in release.yml; CI's git history records the exact YAML at tag-build time. |
| T-06-06-SC | Tampering | npm/pip/cargo installs (WiX 5 NuGet SDK) | mitigate | WiX 5 SDK package installed via `<Sdk Name="WixToolset.Sdk" Version="5.x.x"/>`; legitimacy table below covers it. |

## Package Legitimacy Audit

The installer scaffold introduces one new NuGet dependency. Other dep installs (vcpkg) are already covered in 06-02's audit and not re-litigated here.

| Package | Source | Disposition | Evidence |
|---------|--------|-------------|----------|
| WixToolset.Sdk | nuget.org | [VERIFIED] | Official WiX Toolset publisher (wixtoolset.org); WiX 5.x is the latest major; pinned version in installer/Utinni.Installer.wixproj; legitimacy table includes version SHA at task time. |
| WixToolset.Util.wixext (or equivalent custom-action support package) | nuget.org | [VERIFIED if needed] | Official WiX Toolset publisher. |
| Microsoft.Deployment.WindowsInstaller | nuget.org | [VERIFIED] | Microsoft-published Windows Installer .NET interop assemblies, used by SwgPathDetector custom action. |

| Package | Verified at task time by | Status |
|---------|--------------------------|--------|
| softprops/action-gh-release@v2 | gh action verification — well-known publisher in GitHub Marketplace | [VERIFIED] |
| actions/checkout@v4 | GitHub official action | [VERIFIED] |
| microsoft/setup-msbuild@v2 | Microsoft official action | [VERIFIED] |

All packages above are [VERIFIED] from established publishers; no [ASSUMED] / [SUS] / [SLOP] entries.

</threat_model>

<verification>
- TESTING.md grep `Tier 4` returns at least one section-header match; grep `Scenario \([a-h]\)` returns 8 matches; CONVENTIONS.md grep `TESTING.md` returns at least one match.
- installer/Utinni.Installer.wixproj builds locally to a .msi artifact; installer/Product.wxs declares the UpgradeCode + Program Files (x86) install path + opt-in SWG-path checkbox.
- .github/workflows/release.yml triggers on `v1.0*` tag push; checks out kennethlong/UtinniPlugins at a pinned SHA; builds MSI; publishes GitHub Release as Pre-release for `-rc` tags.
- 06-VERIFICATION.md contains maintainer-signed Tier-4 UAT per-scenario PASS dispositions + MSI install/uninstall round-trip on a clean VM.
- Post-tag-push: `gh release view v1.0.0-rc.1 --json isPrerelease` returns true; Utinni.msi attached as asset.
</verification>

<success_criteria>
1. TESTING.md Tier-4 enumeration covers all 8 D-19 items; CONVENTIONS.md cross-references the doc.
2. WiX 5 MSI installer scaffold ships with opt-in SWG-path detector preserving CON-D-01.
3. release.yml triggers on v1.0* tag-push and publishes GitHub Pre-release with MSI + TJT bundled at pinned UtinniPlugins SHA.
4. Maintainer-signed 06-VERIFICATION.md with all 8 Tier-4 scenarios PASS + MSI clean-VM round-trip PASS.
5. v1.0.0-rc.1 tag pushed; GitHub Pre-release published; bake period begins.
6. CI green on master.
</success_criteria>

<output>
Create `.planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-06-SUMMARY.md` when done.
</output>
