---
phase: 06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - UtinniCore/swg/graphics/depth_texture.cpp
  - UtinniCore/swg/graphics/depth_texture.h
  - UtinniCore/UtinniCore.vcxproj
  - UtinniCore-Symbols/UtinniCore-Symbols.vcxproj
  - .github/workflows/ci.yml
  - UtINI/utini.cpp
  - UtINI/utini.h
  - UtINI/UtINI.vcxproj
  - external/LeksysINI/
  - UtinniCore.Tests/UtINI/IniParserTests.cpp
  - UtinniCore.Tests/UtinniCore.Tests.vcxproj
  - docs/ai/assessment.md
  - .planning/codebase/CONVENTIONS.md
autonomous: true
requirements: [STAB-05, STAB-03]
must_haves:
  truths:
    - "UtinniCore/swg/graphics/depth_texture.cpp no longer includes <d3dx9.h>; the D3DXVECTOR3 vDummyPoint usage is replaced with a local 3-float struct; sizeof(D3DXVECTOR3) is replaced with sizeof of the local struct."
    - "No .vcxproj in the Utinni-owned tree (excluding external/) references DXSDK Jun 2010 include or lib paths; the CI workflow's DXSDK cache + install steps are removed."
    - "LeksysINI is deleted from external/; UtINI/utini.cpp's Impl uses a hand-rolled INI parser (<300 LOC) that handles section headers, key=value, quoted strings, ;/# comments, sectionless keys, and round-trip-preserves unrecognized lines."
    - "UtinniCore.Tests/UtINI/IniParserTests.cpp ships at least 12 Catch2 TEST_CASEs covering: round-trip, quoted strings with embedded =, sectionless keys, ; and # comments, comments preserved on save, empty values, repeated keys, malformed sections (negative), missing = (negative), runaway quotes (negative); every test fails if the parser is reverted to a broken state per [[feedback-max-harness]]."
    - "docs/ai/assessment.md §\"Open questions\" rows for CON-O-06 and CON-O-08 are updated with resolved-pointer commit SHAs; §\"Status tracking\" reflects the closures; a one-paragraph disposition note about DirectXMath substitution lives in either CONVENTIONS.md or assessment.md."
    - "CI stays green on master across all three lanes after every commit in this plan."
    - "CONTEXT.md decisions addressed by this plan: D-01 (CON-O-08 DXSDK removal, structurally closes CON-B-03), D-02 (CON-O-06 LeksysINI replaced with custom parser inside UtINI::Impl), D-03 (INI parser Catch2 tests in UtinniCore.Tests/UtINI/), D-04 (all 8 CON-O-* dispositioned in docs/ai/assessment.md §Open questions at 1.0-rc.1)."
  artifacts:
    - path: "UtinniCore.Tests/UtINI/IniParserTests.cpp"
      provides: "Catch2 regression test suite for the custom INI parser; one TEST_CASE per documented edge case"
      contains: "TEST_CASE"
      min_lines: 200
    - path: "docs/ai/assessment.md"
      provides: "Updated §Open Questions disposition rows for CON-O-06 + CON-O-08 + §Status tracking closure entries"
      contains: "CON-O-06"
    - path: "UtINI/utini.cpp"
      provides: "Custom INI parser inside UtINI::Impl (PIMPL boundary preserved per CON-N-08)"
      contains: "struct UtINI::Impl"
  key_links:
    - from: "UtinniCore/swg/graphics/depth_texture.cpp"
      to: "local 3-float struct (no D3DX9 math dependency)"
      via: "literal `struct Vec3 { float x, y, z; };` (or planner-renamed equivalent) at file scope"
      pattern: "struct\\s+Vec3|float\\s+x,\\s*y,\\s*z"
    - from: "UtINI/utini.cpp::UtINI::Impl"
      to: "custom INI parser (no LeksysINI dependency)"
      via: "Impl owns std::vector<Section> + std::vector<RawLine> for round-trip preservation; Load/Save/GetSection/SetValue are member methods of Impl"
      pattern: "(class|struct)\\s+UtINI::Impl"
---

<objective>
06-03 closes the last two open questions in STAB-05: CON-O-08 (remove DXSDK June 2010 dependency by replacing the sole `D3DXVECTOR3` use in depth_texture.cpp with a local 3-float struct, then stripping DXSDK include/lib paths from every .vcxproj — side-effect closes CON-B-03 structurally) and CON-O-06 (replace LeksysINI with a hand-rolled ~200-LOC INI parser inside UtINI's PIMPL Impl, preserving the existing public ABI so all 15+ callsites in Launcher/UtinniCore/UtinniCoreDotNet stay untouched). Both changes are fenced by Catch2 regression tests per [[feedback-max-harness]].

Purpose: D-01 + D-02 honour the long-promised "LeksysINI is temporary, will most likely be replaced" README commitment and structurally retire the legacy DXSDK dependency. After this plan: zero open CON-O-* at 1.0-rc.1 tag (six were closed by Phases 2-4; this closes the last two; CON-O-09..-11 are TEST-03 territory and already resolved in Phase 4).

Output: depth_texture.cpp with local Vec3 struct, every .vcxproj DXSDK-clean, LeksysINI deleted, UtINI/utini.cpp with custom parser inside Impl, IniParserTests.cpp Catch2 suite, assessment.md updated, CONVENTIONS.md DirectXMath note. Sequencing within this plan: CON-O-08 (DXSDK strip) lands FIRST because it touches every .vcxproj which 06-02 already churned via vcpkg — minimises rebase pain. CON-O-06 (LeksysINI) lands SECOND because it's isolated to UtINI + UtinniCore.Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-CONTEXT.md
@.planning/intel/constraints.md
@.planning/codebase/CONVENTIONS.md
@UtinniCore/swg/graphics/depth_texture.cpp
@UtINI/utini.cpp
@UtINI/utini.h

<interfaces>
<!-- Current state extracted at planning time. Executor reads files at task time to capture exact line numbers and surrounding context. -->

From UtinniCore/swg/graphics/depth_texture.cpp (verified at planning time):
- Line 28: `#include <d3dx9.h>` — the ONLY DXSDK include in this file.
- Line 227: `D3DXVECTOR3 vDummyPoint(0.0f, 0.0f, 0.0f);` — sole use of the type.
- Line 231: `pDevice->DrawPrimitiveUP(D3DPT_POINTLIST, 1, vDummyPoint, sizeof(D3DXVECTOR3));` — sizeof argument depends on the type.
- These three lines are the entirety of the DXSDK math surface in UtinniCore. No other `D3DXVECTOR*`, `D3DXMATRIX*`, or `D3DXVec*` references exist in UtinniCore code.

From UtINI/utini.h (verified at planning time):
- Public ABI (lines 33-86): `UtINI` class, nested `Value` struct, ~15 public methods including load/save/createUtinniSettings/addSetting/DeleteSection/getString/getBool/getInt/getFloat/setString/setBool/setInt/setFloat. **All public ABI must be preserved byte-for-byte.**
- Line 83-84: `struct Impl; Impl* pImpl{};` — PIMPL boundary (CON-N-08).

From UtINI/utini.cpp (verified at planning time):
- Line 26: `#include "LeksysINI/iniparser.hpp"` — sole external dependency.
- Lines 57-61: `struct UtINI::Impl { INI::File ini; std::vector<Value> settings; };` — Impl holds a LeksysINI File object.
- Lines 75-180: every public method delegates to `pImpl->ini.GetSection(sectionName)->...` or `pImpl->ini.Load/Save`.
- The custom parser must replicate exactly these semantics:
  - `GetSection(name)->GetValue(name).AsString()/.AsBool()/.AsInt()/.AsDouble()`
  - `GetSection(name)->SetValue(name, value)` — overload accepts std::string, bool, int, float.
  - `Load(filename)` returns false on failure; on success populates the in-memory representation. On failure, public load() falls back to writing defaults.
  - `Save(filename)` writes back to disk preserving original line ordering + comments + unrecognized lines.
  - `DeleteSection(name)` removes a whole section.

From .github/workflows/ci.yml lines 38-69:
- DXSDK cache key `dxsdk-jun2010-v1` (line 43) + install step (lines 45-69) — both must be removed entirely once depth_texture.cpp no longer needs d3dx9.h. The S1023 VC++ 2010 SP1 uninstall + DXSDK_Jun10.exe download disappear.

From .planning/intel/constraints.md:
- CON-N-08: PluginManager (and UtINI) PIMPL idiom keeping STL out of the DLL boundary — preserve.
- CON-B-03: DXSDK include/lib paths in all configs — closed STRUCTURALLY by D-01 (removing DXSDK entirely).
- CON-O-06: LeksysINI replacement plan — closed by D-02.
- CON-O-08: DXSDK June 2010 vs Windows 10 SDK — closed by D-01.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CON-O-08 — remove DXSDK; replace D3DXVECTOR3 with local struct; strip DXSDK paths from .vcxproj + CI</name>
  <files>
    UtinniCore/swg/graphics/depth_texture.cpp,
    UtinniCore/swg/graphics/depth_texture.h,
    UtinniCore/UtinniCore.vcxproj,
    UtinniCore-Symbols/UtinniCore-Symbols.vcxproj,
    .github/workflows/ci.yml,
    docs/ai/assessment.md,
    .planning/codebase/CONVENTIONS.md
  </files>
  <read_first>
    UtinniCore/swg/graphics/depth_texture.cpp,
    UtinniCore/swg/graphics/depth_texture.h,
    UtinniCore/UtinniCore.vcxproj,
    .github/workflows/ci.yml,
    docs/ai/assessment.md
  </read_first>
  <action>
    Edit UtinniCore/swg/graphics/depth_texture.cpp:
    (a) Remove the line `#include <d3dx9.h>` at line 28.
    (b) Inside an anonymous namespace at file scope (above the existing anonymous namespace at line 51 or merged into it), declare `struct Vec3 { float x; float y; float z; };` — name `Vec3` per planner; readable, no D3DX coupling.
    (c) Replace `D3DXVECTOR3 vDummyPoint(0.0f, 0.0f, 0.0f);` at line 227 with `Vec3 vDummyPoint{0.0f, 0.0f, 0.0f};`.
    (d) Replace `sizeof(D3DXVECTOR3)` at line 231 with `sizeof(Vec3)`.
    Verify depth_texture.h does NOT include d3dx9.h or use D3DXVECTOR3 (read first); if it does, mirror the substitution there.

    Then strip DXSDK include/lib paths from UtinniCore/UtinniCore.vcxproj and UtinniCore-Symbols/UtinniCore-Symbols.vcxproj — grep `DXSDK_DIR` and `Microsoft DirectX SDK` in those files and remove every matching path token from `AdditionalIncludeDirectories`, `AdditionalLibraryDirectories`, and `AdditionalDependencies` across all three configurations (Debug + Release + RelWithDbgInfo) per CON-T-02. Remove the `d3dx9` / `d3dx9d` lib references from `AdditionalDependencies` if present (these are the linker pulls for D3DX9 math; only `d3d9.lib` remains, which ships in the Windows SDK).

    Edit .github/workflows/ci.yml: delete the `Cache DirectX SDK (June 2010)` step (lines ~38-44) and the `Install DirectX SDK (June 2010)` step (lines ~45-69) in their entirety. After deletion the workflow flow becomes Checkout → Cache NuGet → (vcpkg setup from 06-02) → Setup MSBuild → Build → Test lanes.

    Update docs/ai/assessment.md §"Open questions" CON-O-08 row: change disposition to "RESOLVED Phase 6 — DXSDK June 2010 removed entirely; sole `D3DXVECTOR3` usage in depth_texture.cpp replaced with a local 3-float struct. Closes CON-B-03 structurally. See commit <SHA>." Update §"Status tracking" with a new row capturing the closure. Also update .planning/codebase/CONVENTIONS.md with a one-paragraph DirectXMath substitution note: "D3DX9 math was retired in Phase 6 commit <SHA>; new code that needs GPU-side vector/matrix math should use DirectXMath (from the Windows SDK, <DirectXMath.h>) rather than reintroducing the legacy DXSDK."

    Commit atomically as `feat(06-03): remove DXSDK June 2010; close CON-O-08 + CON-B-03`. Push, confirm CI green on master (note: removed DXSDK install step must not break the existing build because depth_texture.cpp no longer needs d3dx9.h).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if ((Select-String -Path 'UtinniCore/swg/graphics/depth_texture.cpp' -Pattern 'd3dx9|D3DXVECTOR' -SimpleMatch -ErrorAction SilentlyContinue)) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - depth_texture.cpp grep `d3dx9` returns zero matches (powershell verify above exits 0).
    - depth_texture.cpp grep `D3DXVECTOR` returns zero matches.
    - depth_texture.cpp grep `struct Vec3` OR `Vec3 vDummyPoint` returns at least one match.
    - UtinniCore.vcxproj grep `DXSDK_DIR` returns zero matches; grep `Microsoft DirectX SDK` returns zero matches.
    - UtinniCore-Symbols.vcxproj grep `DXSDK_DIR` returns zero matches.
    - .github/workflows/ci.yml grep `DirectX SDK` returns zero matches; grep `DXSDK` returns zero matches.
    - docs/ai/assessment.md grep `CON-O-08.*RESOLVED` returns at least one match.
    - .planning/codebase/CONVENTIONS.md grep `DirectXMath` returns at least one match.
    - Release x86 solution build exit code 0: `msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86`.
    - CI green on master post-merge: `gh run list --branch master --limit 1 --json conclusion -q '.[0].conclusion'` returns "success".
    - One atomic commit on master with prefix `feat(06-03):`.
  </acceptance_criteria>
  <done>DXSDK June 2010 dependency removed; depth_texture.cpp uses local Vec3 struct; all DXSDK paths stripped from .vcxproj + CI; CON-O-08 + CON-B-03 closed in assessment.md.</done>
</task>

<task type="auto">
  <name>Task 2: CON-O-06 — replace LeksysINI with custom INI parser inside UtINI::Impl; preserve public ABI</name>
  <files>
    UtINI/utini.cpp,
    UtINI/utini.h,
    UtINI/UtINI.vcxproj,
    external/LeksysINI/,
    docs/ai/assessment.md
  </files>
  <read_first>
    UtINI/utini.cpp,
    UtINI/utini.h,
    UtINI/UtINI.vcxproj,
    external/LeksysINI/inifile.hpp,
    external/LeksysINI/iniparser.hpp
  </read_first>
  <action>
    Rewrite UtINI/utini.cpp's `struct UtINI::Impl` to own a hand-rolled INI parser. **Public ABI in utini.h MUST NOT change** — the 15+ callsites in Launcher/UtinniCore/UtinniCoreDotNet must continue to work without recompilation.

    Custom parser requirements (targeting ~200-300 LOC inside utini.cpp; planner discretion on internal helper class layout):
    - Storage model: `std::vector<Line>` where `Line` is a sum-type (variant or tagged struct) of { Section{name}, KeyValue{section,key,value,raw_inline_comment}, Comment{text}, Blank{} } — preserves original line order + unrecognized content for round-trip Save.
    - Plus an index `std::unordered_map<std::string, std::unordered_map<std::string, size_t>>` (section -> key -> index into vector<Line>) for O(1) GetValue/SetValue lookups.
    - Section header parsing: literal `[Name]` (square-bracket delimited); whitespace before `[` and after `]` trimmed; malformed `[Name` (missing `]`) treated as a parse error and the line preserved as an opaque Comment so Save round-trips it intact (do NOT crash; do NOT silently drop).
    - Key/value parsing: split on FIRST `=` only (values may contain `=`); leading/trailing whitespace on key + value trimmed; quoted values (`"..."` or `'...'`) trim the quotes and preserve interior whitespace exactly; unquoted values are trimmed.
    - Comment handling: leading `;` or `#` marks a full-line comment; inline `;` or `#` after a value is preserved as a per-line `raw_inline_comment` and re-emitted on Save.
    - Sectionless keys: keys appearing before any `[Section]` are stored under an empty-string section name; GetSection("") retrieves them.
    - Repeated keys: last-write wins for GetValue; the original line is updated in place on SetValue (not appended).
    - Runaway quotes / unterminated string: line preserved as opaque Comment; no crash.

    Replace the `Impl::ini` field with the new parser (rename to something like `IniStore store` for readability). Reimplement every public method in utini.cpp (load/save/createUtinniSettings/addSetting/DeleteSection/getString/getBool/getInt/getFloat/setString/setBool/setInt/setFloat) against the new Impl. Public method signatures are FROZEN — match utini.h exactly. For the Value types (vt_string/vt_bool/vt_int/vt_float/vt_array/vt_map), getBool parses "true"/"false"/"1"/"0"/"yes"/"no" (case-insensitive); getInt uses `std::stoi`; getFloat uses `std::stof` — match LeksysINI's coercion semantics, validate against the current data/utinni.cfg shape (read the file once to confirm expected values parse correctly).

    Delete `external/LeksysINI/` from git. Remove the `#include "LeksysINI/iniparser.hpp"` line in utini.cpp. Remove any LeksysINI include path tokens from UtINI/UtINI.vcxproj `AdditionalIncludeDirectories` across all three configurations.

    Update docs/ai/assessment.md §"Open questions" CON-O-06 row: change disposition to "RESOLVED Phase 6 — LeksysINI replaced with a hand-rolled custom INI parser inside UtINI::Impl. Public ABI preserved; all callsites untouched. See commit <SHA>." Update §"Status tracking" with closure entry. Also remove the README's "LeksysINI is temporary, will most likely be replaced" line if present (grep README.md).

    Commit atomically as `feat(06-03): replace LeksysINI with custom INI parser inside UtINI::Impl; close CON-O-06`. Do NOT add tests in this commit — Task 3 ships the tests.
  </action>
  <verify>
    <automated>msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86</automated>
  </verify>
  <acceptance_criteria>
    - utini.cpp grep `LeksysINI` returns zero matches.
    - utini.cpp grep `INI::File` returns zero matches.
    - utini.cpp grep `struct UtINI::Impl` returns at least one match.
    - utini.h grep `struct Impl` returns at least one match (PIMPL boundary preserved — utini.h is unchanged except possibly forward decls).
    - utini.h diff against pre-plan state shows zero public-method-signature changes (executor verifies via `git diff` and reports in the commit body).
    - `git ls-files external/LeksysINI/` returns zero entries.
    - Release x86 solution build exit code 0: `msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86`.
    - docs/ai/assessment.md grep `CON-O-06.*RESOLVED` returns at least one match.
    - docs/ai/assessment.md §"Open questions" final-state audit: `grep -E "^.*CON-O-0[1-8]" docs/ai/assessment.md` returns 8 lines and every match line contains `RESOLVED` or `DEFERRED` (D-04 mandate: all 8 CON-O-* dispositioned at 1.0-rc.1; catches stale rows where a Phase 2/3/4 closure was committed to code but never updated in assessment.md).
    - One atomic commit on master with prefix `feat(06-03):`.
  </acceptance_criteria>
  <done>LeksysINI deleted; UtINI::Impl owns the new parser; public ABI preserved; CON-O-06 closed in assessment.md; all 8 CON-O-* show RESOLVED/DEFERRED in §"Open questions" final state.</done>
</task>

<task type="auto">
  <name>Task 3: Catch2 regression test suite for the custom INI parser (max-harness fence)</name>
  <files>
    UtinniCore.Tests/UtINI/IniParserTests.cpp,
    UtinniCore.Tests/UtinniCore.Tests.vcxproj
  </files>
  <read_first>
    UtINI/utini.cpp,
    UtINI/utini.h,
    UtinniCore.Tests/UtinniCore.Tests.vcxproj,
    UtinniCore.Tests/StringUtilityTests.cpp
  </read_first>
  <action>
    Create UtinniCore.Tests/UtINI/IniParserTests.cpp with Catch2 `TEST_CASE` entries covering AT LEAST the following twelve scenarios per D-03 and [[feedback-max-harness]]:
    1. round_trip_load_mutate_save_reload_equal — write a known INI text to a temp file, load, mutate one value, save, reload, assert the mutated value persisted AND every unrelated section/key/comment is byte-equal to original.
    2. quoted_string_with_embedded_equals — value `"a=b=c"` parses to literal `a=b=c`; SetValue then Save round-trips it.
    3. sectionless_keys — keys before any `[Section]` are retrievable via GetSection("") or whatever convention the implementation chose; document the convention in the test.
    4. semicolon_comment_preserved — `; this is a comment` round-trips byte-equal through Load → Save.
    5. hash_comment_preserved — `# this is a comment` round-trips byte-equal.
    6. inline_comment_preserved — `key = value ; trailing` preserves the inline comment on save.
    7. empty_value — `key =` returns empty string from getString; setString to empty round-trips.
    8. repeated_keys_last_write_wins — INI with two `[A] key=x` ... `[A] key=y` returns "y" from getString and Save emits only the second occurrence (or the first slot updated in place — document which).
    9. malformed_section_preserved_as_opaque — `[NotClosed` line is round-tripped intact through Save and does NOT crash Load.
    10. missing_equals_preserved_as_opaque — `keywithoutequals` line is round-tripped intact through Save.
    11. runaway_quote_preserved — `key = "unterminated` does not crash; the value behaviour is documented in the test (planner picks: empty string, opaque-comment, or include-the-rest-of-line).
    12. type_coercion_matches_legacy — getBool("true") == true, getBool("1") == true, getBool("FALSE") == false, getInt("42") == 42, getFloat("3.14") == 3.14 within epsilon. Use the same INI text against the previous implementation's reference values (the tests can pin the expected behaviour numerically).

    Use `std::filesystem::temp_directory_path()` to write throwaway INI files per test (or in-memory string + a small overload — planner discretion). Each TEST_CASE must include a `SECTION` documenting the failure mode so that if a future cleanup reverts the parser to a broken state the test name + section label identify the regression class.

    Register IniParserTests.cpp in UtinniCore.Tests/UtinniCore.Tests.vcxproj `<ClCompile>` ItemGroup. The test exe must link UtINI; if it doesn't already, add the project reference (read UtinniCore.Tests.vcxproj's <ProjectReference> ItemGroup — UtinniCore is already linked, which transitively pulls UtINI via UtinniCore's link line; if not, add UtINI/UtINI.vcxproj as a sibling <ProjectReference>).

    Commit atomically as `test(06-03): Catch2 fence for custom INI parser (12 cases)`.

    Run Release x86 build + UtinniCore.Tests.exe and confirm all 12 test cases pass.
  </action>
  <verify>
    <automated>msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86 && bin\Release\UtinniCore.Tests.exe --reporter console</automated>
  </verify>
  <acceptance_criteria>
    - UtinniCore.Tests/UtINI/IniParserTests.cpp grep `TEST_CASE` returns at least 12 matches.
    - UtinniCore.Tests.vcxproj grep `IniParserTests.cpp` returns 1 match in a `<ClCompile>` element.
    - Release build succeeds and UtinniCore.Tests.exe exits 0 (Catch2 console reporter returns non-zero on failure).
    - UtinniCore.Tests.exe output grep `INI parser` (or the section names) returns at least 12 passing-test references.
    - CI green on master post-merge: `gh run list --branch master --limit 1 --json conclusion -q '.[0].conclusion'` returns "success".
    - One atomic commit on master with prefix `test(06-03):`.
  </acceptance_criteria>
  <done>IniParserTests.cpp Catch2 suite ships with 12+ TEST_CASEs; all pass against the new parser; CI green on master; 06-03 complete.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| utini.cfg INI text on disk | User-edited; possibly malformed. Parser MUST NOT crash on malformed input. Malformed section / runaway quote / repeated key are the security-relevant edge cases per 06-CONTEXT.md `<additional_context>` security gate notes. |
| depth_texture.cpp render path | DrawPrimitiveUP point payload is now a local-struct pointer; replaces D3DXVECTOR3 which had the same memory layout. No new untrusted input handled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | Custom INI parser facing malformed utini.cfg | mitigate | Three explicit edge-case TEST_CASEs (malformed_section_preserved_as_opaque, missing_equals_preserved_as_opaque, runaway_quote_preserved) — parser preserves unparseable lines as opaque, never crashes. |
| T-06-03-02 | Denial of Service | INI parser facing very large or pathological input (e.g. 1 MB of repeated quotes) | accept | utini.cfg is a local config file shipped blank per CON-D-01; the threat model is "user pastes pathological content" which is self-inflicted; no parser hardening beyond not-crash is justified. |
| T-06-03-03 | Information Disclosure | Round-trip preservation accidentally leaks comments to log output | accept | No log-on-parse pathway in the new parser; round-trip preservation writes back to disk only, no debug logging of file content. |
| T-06-03-04 | Tampering | depth_texture.cpp Vec3 struct replacing D3DXVECTOR3 with same layout | mitigate | sizeof(Vec3) substitution preserved alongside the type change; struct member layout (three floats, no alignment qualifiers) matches D3DXVECTOR3 byte-for-byte. |
| T-06-03-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in 06-03. (06-02 already exercised the audit gate for vcpkg ports; this plan introduces no new packages.) |

</threat_model>

<verification>
- depth_texture.cpp + UtinniCore.vcxproj + UtinniCore-Symbols.vcxproj grep `d3dx9|DXSDK|D3DXVECTOR` returns zero matches.
- .github/workflows/ci.yml grep `DirectX SDK|DXSDK` returns zero matches.
- utini.cpp grep `LeksysINI` returns zero matches; `git ls-files external/LeksysINI/` returns zero entries.
- utini.h diff vs pre-plan state shows zero public-method-signature changes.
- IniParserTests.cpp ships 12+ TEST_CASEs; UtinniCore.Tests.exe passes them.
- docs/ai/assessment.md §"Open questions" rows for CON-O-06 + CON-O-08 marked RESOLVED with commit SHAs.
- CI green on master after each of the three commits.
</verification>

<success_criteria>
1. DXSDK June 2010 dependency removed entirely; depth_texture.cpp uses local Vec3 struct; CON-O-08 + CON-B-03 closed.
2. LeksysINI replaced with custom parser inside UtINI::Impl; public ABI preserved byte-for-byte; CON-O-06 closed.
3. IniParserTests.cpp Catch2 suite (12+ TEST_CASEs) passes; would fail if parser reverted to broken state.
4. docs/ai/assessment.md §"Open questions" + §"Status tracking" updated.
5. CONVENTIONS.md DirectXMath substitution note added.
6. CI green on master across all three lanes.
</success_criteria>

<output>
Create `.planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-03-SUMMARY.md` when done.
</output>
