---
phase: 06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - UtinniCore/swg/ui/imgui_impl.cpp
  - UtinniCore/swg/ui/imgui_impl.h
  - UtinniCore/swg/graphics/directx9.cpp
  - .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-01-DEMO-PROBE-NOTES.md
  - .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-01-VERIFICATION.md
  - .planning/codebase/TESTING.md
autonomous: false
requirements: [STAB-03]
must_haves:
  truths:
    - "directX::getVtbl() in directx9.cpp is the dummy-device approach (post-Phase 02.1 commit 2c57d38); pattern-scan is NOT the cause of imgui not rendering."
    - "ImGui::ShowDemoWindow renders visibly over a live SWG client, with menus + sliders + buttons + tabs + plots + popups + drag-and-drop all functional end-to-end."
    - "isSetup gate in imgui_impl.cpp:264 is observed to flip true at least once during a live SWG session before any render frame; root cause of pre-fix non-render is documented in 06-01-DEMO-PROBE-NOTES.md."
    - "A new Tier-4 row 'imgui overlay Demo screen over live SWG' is appended to .planning/codebase/TESTING.md with procedure + success criterion + last-verified SHA."
    - "Any code-level fix (if required to clear the gate) is captured by a regression-targetable diag tripwire that fires (warn-level log) if the regression returns; gate is documented even if no fix was needed."
    - "CONTEXT.md decisions addressed by this plan: D-06 (gates imgui docking-branch switch in 06-02), D-11 (06-01 is the dedicated investigation plan; d3d9 pattern-scan 30-sec first move; ShowDemoWindow exit criterion)."
  artifacts:
    - path: ".planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-01-DEMO-PROBE-NOTES.md"
      provides: "Investigation log: d3d9 pattern-scan check result, isSetup gate observation, hkPresent/NewFrame/EndFrame ordering review, and root-cause disposition."
    - path: ".planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-01-VERIFICATION.md"
      provides: "Maintainer-signed Tier-4 evidence for the Demo screen exercising menus + sliders + buttons + tabs + plots + popups + drag-and-drop."
    - path: ".planning/codebase/TESTING.md"
      provides: "Tier-4 row for the imgui overlay Demo screen procedure (referenced again from 06-06 in the final TEST-04 enumeration)."
  key_links:
    - from: "UtinniCore/swg/graphics/directx9.cpp::getVtbl()"
      to: "d3d9.dll dummy-device approach"
      via: "Direct3DCreate9 + 1x1 hidden window + CreateDevice(HAL) + vtable snapshot"
      pattern: "Direct3DCreate9|GetModuleHandleA\\(.d3d9\\.dll.\\)"
    - from: "UtinniCore/swg/ui/imgui_impl.cpp::render()"
      to: "ImGui::ShowDemoWindow()"
      via: "diagnostic call added inside the isSetup branch and gated behind a temporary flag"
      pattern: "ShowDemoWindow"
---

<objective>
06-01 closes the long-standing concern that the imgui in-game overlay has never displayed in Utinni-injected sessions. The plan executes the [[feedback-d3d9-hook-diagnosis]] memory's 30-second check FIRST (pattern-scan vs dummy-device) and only then walks the imgui state machine. Exit gate is intentionally high: `ImGui::ShowDemoWindow` exercising the full Demo screen (menus, sliders, buttons, tabs, plots, popups, drag-and-drop) over a live SWG client. This is a Tier-4 manual verification recorded against a maintainer-signed SHA, and it gates 06-02's imgui docking-branch switch (D-06, D-11).

Purpose: Without a working overlay, Wave-1 TJT subpanels (Phases 7-11) cannot demo. The imgui docking-branch bump in 06-02 is wasted churn if base imgui does not render. Investigation lives in its own plan so 06-02 inherits a known-good baseline.

Output: Probe notes + Tier-4 verification artifact + TESTING.md Tier-4 row + optional fix (only if investigation reveals a defect) with regression-targetable diag tripwire. Any architectural escalation (e.g. imgui bump downgrade in 06-02) is surfaced explicitly in 06-01-DEMO-PROBE-NOTES.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-CONTEXT.md
@.planning/intel/constraints.md
@.planning/codebase/TESTING.md
@.planning/codebase/ARCHITECTURE.md

<interfaces>
<!-- Key contracts inside imgui_impl.cpp the executor must NOT alter behaviorally; CON-N-06 (device-loss handling) must be preserved. -->

From UtinniCore/swg/ui/imgui_impl.cpp (file-scope):
- `bool isSetup = false;` — flips true once after first hkBeginScene-driven setup; guards the per-frame render() body.
- `void render()` — invoked from the render-thread path; calls `ImGui_ImplDX9_NewFrame()` -> `ImGui_ImplWin32_NewFrame()` -> `ImGui::NewFrame()` then user windows; gated on `isSetup`.
- Render callback registry: `renderCallbacks` (vector<CallbackEntry>), `renderCallbacksMutex`, dispatched via the file-scope `dispatchSnapshot` helper. Preserve as-is.

From UtinniCore/swg/graphics/directx9.cpp:
- `swgptr* directX::getVtbl()` — post-Phase 02.1 dummy-device path (creates Direct3D9, hidden 1x1 window, CreateDevice(HAL), reads vtable, 119 entries). Replaced the broken pattern-scan in commit 2c57d38 (2026-05-19). This file is the authoritative read for confirming the pattern-scan suspicion is already neutralised.
- `directX::initPresentBlockedEvent()` — eagerly creates the hPresentBlockedEvent before any detour fires (do not touch).

From .planning/STATE.md "Blockers/Concerns" item #2 (RESOLVED 2026-05-19, commit 2c57d38):
- Pattern-scan in d3d9.dll proven to be unfixable on modern Windows; dummy-device approach is the canonical path. **This means the [[feedback-d3d9-hook-diagnosis]] memory's "test pattern-scan FIRST" advice resolves in 30 seconds to "already not the cause."** The investigation must record this disposition in 06-01-DEMO-PROBE-NOTES.md and move to the next probe.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: d3d9 pattern-scan disposition + isSetup observation probe</name>
  <files>
    .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-01-DEMO-PROBE-NOTES.md,
    UtinniCore/swg/ui/imgui_impl.cpp
  </files>
  <read_first>
    UtinniCore/swg/graphics/directx9.cpp,
    UtinniCore/swg/ui/imgui_impl.cpp,
    .planning/STATE.md,
    .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-CONTEXT.md
  </read_first>
  <action>
    Per [[feedback-d3d9-hook-diagnosis]] memory cited in 06-CONTEXT.md, this is the 30-second first move. Create 06-01-DEMO-PROBE-NOTES.md and record the d3d9 pattern-scan disposition by quoting the `getVtbl` body at directx9.cpp line ~436 and STATE.md "Blockers/Concerns" item #2: pattern-scan was replaced with the dummy-device approach in commit 2c57d38 (2026-05-19). Conclude in writing that pattern-scan is NOT the cause. Then add temporary diagnostic instrumentation to imgui_impl.cpp's setup() and render() paths: an `info`-level utinni::log call the first time `isSetup` flips to true (one-shot guarded by a static bool), and a `debug`-level utinni::log call (one-shot, again guarded by a static bool) the first time render()'s `isSetup`-branch body actually executes. The diag must NOT log on every frame; use the existing utinni::log facade (do not introduce new dependencies). Land the probe in a single atomic commit titled "diag(06-01): imgui isSetup + render-entry one-shot probe". The maintainer will run a live SWG session and report whether both log lines appear, then attach those observations to 06-01-DEMO-PROBE-NOTES.md before Task 2 starts.
  </action>
  <verify>
    <automated>msbuild Utinni.sln /p:Configuration=Release /p:Platform=x86 /t:UtinniCore</automated>
    <human-check>Maintainer runs Launcher.exe against live SWGEmu, logs into Tatooine; utinni.log contains exactly one "imgui_impl::setup complete, isSetup=true" line and exactly one "imgui_impl::render entered isSetup branch" line during the session. Observations appended to 06-01-DEMO-PROBE-NOTES.md.</human-check>
  </verify>
  <acceptance_criteria>
    - 06-01-DEMO-PROBE-NOTES.md exists and quotes both `directx9.cpp::getVtbl` (line range around 436-460) and STATE.md Blockers item #2 verbatim as evidence that pattern-scan is not the cause.
    - imgui_impl.cpp grep `ShowDemoWindow` returns no matches yet (Task 1 does not call it).
    - imgui_impl.cpp grep `imgui_impl::setup complete` returns exactly one match inside an `if (!sLoggedOnce)` (or equivalently named) static-bool guard inside the setup() function body.
    - imgui_impl.cpp grep `render entered isSetup branch` returns exactly one match inside an `if (!sLoggedOnceRender)` (or equivalently named) static-bool guard inside the render() function body, inside the `if (isSetup)` branch.
    - Release build succeeds: `msbuild Utinni.sln /p:Configuration=Release /p:Platform=x86 /t:UtinniCore` exit code 0.
    - One atomic commit on master with prefix `diag(06-01):`.
  </acceptance_criteria>
  <done>Pattern-scan disposition written, isSetup + render-entry probe shipped behind one-shot static-bool guards, live observations captured in 06-01-DEMO-PROBE-NOTES.md.</done>
</task>

<task type="auto">
  <name>Task 2: ShowDemoWindow exercise + root-cause disposition</name>
  <files>
    UtinniCore/swg/ui/imgui_impl.cpp,
    .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-01-DEMO-PROBE-NOTES.md
  </files>
  <read_first>
    UtinniCore/swg/ui/imgui_impl.cpp,
    .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-01-DEMO-PROBE-NOTES.md,
    external/imgui/imgui_demo.cpp
  </read_first>
  <action>
    In imgui_impl.cpp::render(), inside the existing `if (isSetup)` branch and immediately after `ImGui::NewFrame()` (around line 399), add an unconditional call to `ImGui::ShowDemoWindow(nullptr)` gated behind a new file-scope `static bool g_showDemoWindowProbe = true;` flag (so it can be flipped off without recompiling if needed). Do NOT remove the existing renderCallbacks dispatch path; the Demo window is additive instrumentation. The Demo window MUST execute end-to-end every frame while the flag is true. Build Release x86; commit atomically as "diag(06-01): ShowDemoWindow probe additive to render()". Maintainer runs a live SWG session and exercises menus + sliders + buttons + tabs + plots + popups + drag-and-drop inside the Demo window. Append to 06-01-DEMO-PROBE-NOTES.md whether the Demo screen rendered (yes/no), and if no, the observed failure mode (no window, blank window, crash, missing input, partial render). If yes — disposition is "no fix required; render path is healthy, prior 'never displayed' belief was stale post-Phase 02.1." If no — investigation expands within 06-01 per D-11 escalation rule: walk hkPresent/hkReset state, ImGui_ImplDX9 init order, ImGui_ImplWin32_Init HWND identity, and add fix tasks to this plan via revision. The exit criterion is binary: full Demo screen functional, or escalation documented.
  </action>
  <verify>
    <automated>msbuild Utinni.sln /p:Configuration=Release /p:Platform=x86 /t:UtinniCore</automated>
    <human-check>Maintainer launches live SWG, logs in, exercises every Demo screen widget category (menus, sliders, buttons, tabs, plots, popups, drag-and-drop); each behaves as in standalone imgui demo apps; observations captured in 06-01-DEMO-PROBE-NOTES.md.</human-check>
  </verify>
  <acceptance_criteria>
    - imgui_impl.cpp grep `ShowDemoWindow` returns at least one match inside the render() body, gated behind a static-bool flag literally named `g_showDemoWindowProbe` (so the next plan can grep for it deterministically).
    - imgui_impl.cpp grep `g_showDemoWindowProbe` returns exactly one declaration and at least one read.
    - Release build succeeds: `msbuild Utinni.sln /p:Configuration=Release /p:Platform=x86 /t:UtinniCore` exit code 0.
    - 06-01-DEMO-PROBE-NOTES.md contains a "Demo Screen Exercise Result" section with explicit YES/NO + maintainer-signed observation transcript.
    - If YES: disposition section explicitly states "no fix required" and 06-02 imgui docking-branch switch is unblocked.
    - If NO: escalation section enumerates next probes (hkReset state-loss, ImGui_ImplWin32_Init HWND identity check, render-target rebinding inspection) and re-opens this plan for revision rather than auto-advancing.
    - One atomic commit on master with prefix `diag(06-01):`.
  </acceptance_criteria>
  <done>ShowDemoWindow probe lives behind g_showDemoWindowProbe flag, maintainer-exercised Demo screen result captured, disposition (no-fix vs escalation) recorded in 06-01-DEMO-PROBE-NOTES.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Tier-4 sign-off + TESTING.md Tier-4 row + diag rollback</name>
  <files>
    .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-01-VERIFICATION.md,
    .planning/codebase/TESTING.md,
    UtinniCore/swg/ui/imgui_impl.cpp
  </files>
  <read_first>
    .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-01-DEMO-PROBE-NOTES.md,
    .planning/codebase/TESTING.md,
    UtinniCore/swg/ui/imgui_impl.cpp
  </read_first>
  <what-built>
    Tasks 1+2 shipped the d3d9 pattern-scan disposition write-up, the one-shot isSetup+render-entry diag tripwires, and the ShowDemoWindow probe gated behind g_showDemoWindowProbe. The maintainer exercised the full Demo screen (menus + sliders + buttons + tabs + plots + popups + drag-and-drop) over a live SWG session.
  </what-built>
  <how-to-verify>
    1. Open .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-01-DEMO-PROBE-NOTES.md and confirm the "Demo Screen Exercise Result" section reads YES end-to-end for all seven widget categories.
    2. Flip g_showDemoWindowProbe to `false` (literal `static bool g_showDemoWindowProbe = false;` in imgui_impl.cpp) AND remove the two one-shot static-bool diag log lines from setup() + render() OR convert them to `debug` level only (planner discretion at sign-off — pick one and stay consistent). Keep the ShowDemoWindow call site itself so future Wave-1 work can re-enable by flipping the flag. Build Release x86 and confirm exit code 0.
    3. Author .planning/codebase/TESTING.md Tier-4 enumeration row "Imgui overlay Demo screen over live SWG" with: procedure (set g_showDemoWindowProbe=true, build Release x86, inject via Launcher, exercise all seven widget categories), success criterion (each category behaves as in the standalone imgui demo), last-verified SHA (the merge SHA of this plan).
    4. Write .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-01-VERIFICATION.md as the maintainer-signed Tier-4 evidence with date, machine identifier, GPU vendor, and a paragraph describing the exercise.
    5. Commit atomically as "docs(06-01): Tier-4 overlay Demo signoff + TESTING.md row + diag rollback".
    6. Confirm CI is green on master for the merge: `gh run list --branch master --limit 1 --json conclusion -q '.[0].conclusion'` returns "success".
  </how-to-verify>
  <resume-signal>Type "approved" once Tier-4 evidence is signed and CI is green; or describe any escalation that re-opens this plan.</resume-signal>
  <acceptance_criteria>
    - 06-01-VERIFICATION.md exists with date + machine identifier + GPU vendor + maintainer signature line.
    - .planning/codebase/TESTING.md grep `Imgui overlay Demo screen` returns at least one match in a Tier-4 enumeration row.
    - imgui_impl.cpp grep `g_showDemoWindowProbe = false` returns at least one match (flag flipped off post-verification).
    - imgui_impl.cpp grep `ShowDemoWindow` still returns at least one match (the call site itself remains for future re-enablement).
    - Release build succeeds: `msbuild Utinni.sln /p:Configuration=Release /p:Platform=x86 /t:UtinniCore` exit code 0.
    - CI green on master post-merge: `gh run list --branch master --limit 1 --json conclusion -q '.[0].conclusion'` returns "success".
    - One atomic commit on master with prefix `docs(06-01):`.
  </acceptance_criteria>
  <done>Tier-4 sign-off recorded, TESTING.md Tier-4 row landed, diag instrumentation rolled back to dormant state, CI green on master; 06-02 unblocked.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live SWG process / injected UtinniCore.dll | Diagnostic log writes from inside the render thread; ImGui::ShowDemoWindow consumes mouse + keyboard input from the SWG message pump. No external input handled. |
| utinni.log file on disk | Logged strings under maintainer control; no user-supplied content in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | imgui_impl.cpp diag log writes during overlay-debug | accept | Logs are utinni::log-routed strings written under maintainer's local user account; no privilege escalation surface; one-shot static-bool guards prevent log flooding. |
| T-06-01-02 | Denial of Service | per-frame ShowDemoWindow call inside render() | mitigate | Gated behind `g_showDemoWindowProbe` flag flipped off in Task 3 sign-off; default state post-plan is dormant. Pre-flag, ShowDemoWindow itself is upstream imgui code with no malformed-input pathway in this plan. |
| T-06-01-03 | Information Disclosure | utinni.log contains overlay state observations | accept | Log content is "imgui_impl::setup complete, isSetup=true" + "render entered isSetup branch" — no PII, no secrets, no SWG account data. |
| T-06-01-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in 06-01. (Package legitimacy gate is exercised in 06-02 if vcpkg dep migration introduces install tasks; no audit table required here because no installs.) |

</threat_model>

<verification>
- Source-level: imgui_impl.cpp contains the `g_showDemoWindowProbe` flag (flipped off at sign-off) and the ShowDemoWindow call site (retained for re-enablement); diag log lines either removed or downgraded to `debug` level.
- Behavior: Demo screen exercise transcript in 06-01-DEMO-PROBE-NOTES.md confirms all seven widget categories functional, OR escalation section enumerates next probes and re-opens the plan.
- Documentation: .planning/codebase/TESTING.md has a Tier-4 row "Imgui overlay Demo screen over live SWG"; 06-01-VERIFICATION.md captures the maintainer-signed Tier-4 evidence.
- CI gate: master is green post-merge; 06-02 is unblocked.
</verification>

<success_criteria>
1. d3d9.dll pattern-scan disposition recorded (already-not-the-cause per Phase 02.1 commit 2c57d38).
2. ImGui::ShowDemoWindow renders end-to-end over live SWG with all seven widget categories functional, OR a deep architectural blocker is documented and 06-02's imgui-docking switch is downgraded per D-11 escalation rule.
3. Tier-4 row authored in .planning/codebase/TESTING.md with procedure + success criterion + last-verified SHA.
4. 06-01-VERIFICATION.md maintainer-signed.
5. CI green on master before 06-02 starts.
</success_criteria>

<output>
Create `.planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-01-SUMMARY.md` when done.
</output>
