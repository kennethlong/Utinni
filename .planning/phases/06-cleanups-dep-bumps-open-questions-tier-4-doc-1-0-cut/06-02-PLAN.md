---
phase: 06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - vcpkg.json
  - vcpkg-configuration.json
  - .github/workflows/ci.yml
  - UtinniCore/UtinniCore.vcxproj
  - UtinniCore.Tests/UtinniCore.Tests.vcxproj
  - UtinniCore.Tests/Log/OutputSinkRoundTripTests.cpp
  - Launcher/Launcher.vcxproj
  - UtINI/UtINI.vcxproj
  - UtinniCore-Symbols/UtinniCore-Symbols.vcxproj
  - Utinni.LoaderLockHarness/Utinni.LoaderLockHarness.vcxproj
  - Utinni.CrtMatchPlugin/Utinni.CrtMatchPlugin.vcxproj
  - Utinni.LegacyPlugin/Utinni.LegacyPlugin.vcxproj
  - external/imgui/imgui.vcxproj
  - external/imgui/
  - external/spdlog/
  - external/ImGuizmo/
  - external/catch2/
  - external/CppSharp/
  - external/DetourXS/
  - external/nvapi/
  - sdk/UtinniCppPluginTemplate/UtinniCppPluginTemplate/UtinniCppPluginTemplate.vcxproj
  - sdk/examples/ExampleCppPlugin/ExampleCppPlugin.vcxproj
  - UtinniCoreDotNet/UtinniCoreDotNet.csproj
  - UtinniCoreDotNet/source.extension.vsixmanifest
  - .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-02-VCPKG-RESEARCH.md
autonomous: true
requirements: [STAB-03]
must_haves:
  truths:
    - "vcpkg.json + vcpkg-configuration.json exist at repo root in manifest mode with a baseline-pinned microsoft/vcpkg commit SHA."
    - "All seven targeted deps either resolve via vcpkg OR are explicitly documented as 'kept vendored — port broken' in 06-02-VCPKG-RESEARCH.md with the broken-port evidence; partial migration is acceptable."
    - "spdlog is bumped to a 1.14.x release; UtinniCore links cleanly; the new OutputSinkRoundTripTests Catch2 test passes; the test fails if base_sink<std::mutex> is replaced with base_sink<details::null_mutex> (CON-N-09 fence)."
    - "ImGuizmo is at a planner-selected post-2024 stable commit/tag; the gizmo code in imgui_impl.cpp still builds and runs."
    - "imgui is on the docking branch at a planner-selected stable commit, ONLY IF 06-01-DEMO-PROBE-NOTES.md disposition is 'no fix required' (full Demo screen functional); otherwise imgui stays on master per D-11 downgrade rule and the downgrade is recorded in 06-02-VCPKG-RESEARCH.md."
    - "Every .vcxproj has PlatformToolset v145; CI's MSBuild step picks up v145 deterministically; both Release and RelWithDbgInfo configurations build."
    - "VSIX manifest range is [16.0,19.0); CON-B-01 audit-then-widen precedent honoured."
    - "CI runs `vcpkg install` (or the equivalent setup-vcpkg step) before the MSBuild step on every push to master; all three CI lanes (dotnet test + CLI golden + UtinniCore.Tests.exe) stay green."
    - "CONTEXT.md decisions addressed by this plan: D-05 (vcpkg manifest + 7-dep migration), D-06 (imgui docking branch, gated on 06-01), D-07 (spdlog 1.14 + OutputSink regression fence per CON-N-09), D-08 (ImGuizmo bump), D-09 (PlatformToolset v145), D-10 (VSIX widen [16.0,19.0)). Also references D-02 (LeksysINI excluded from migration list) and D-11 (overlay gate output consumed)."
  artifacts:
    - path: "vcpkg.json"
      provides: "Manifest declaring all dependency names + version-baseline + features"
      contains: "\"dependencies\""
    - path: "vcpkg-configuration.json"
      provides: "Registry baseline pin to a specific microsoft/vcpkg commit SHA for build reproducibility"
      contains: "\"default-registry\""
    - path: "UtinniCore.Tests/Log/OutputSinkRoundTripTests.cpp"
      provides: "Catch2 regression test that asserts spdlog OutputSink delivers a message to a recording sink in expected format; failure mode = CON-N-09 violated (sink no longer thread-safe)"
      contains: "TEST_CASE"
    - path: ".planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-02-VCPKG-RESEARCH.md"
      provides: "Per-dep vcpkg port quality assessment with fallback decisions for any broken ports; documents the imgui docking-vs-master decision from 06-01 escalation if triggered"
      contains: "catch2, CppSharp, DetourXS, nvapi, imgui, spdlog, ImGuizmo"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "vcpkg.json manifest"
      via: "setup-vcpkg or bootstrap step followed by `vcpkg install`"
      pattern: "vcpkg\\s+install|setup-vcpkg"
    - from: "UtinniCore.Tests/Log/OutputSinkRoundTripTests.cpp"
      to: "UtinniCore::Log::OutputSink"
      via: "Catch2 TEST_CASE with a recording sink that asserts message format + thread-safety"
      pattern: "base_sink|OutputSink"
    - from: "Every .vcxproj"
      to: "v145 PlatformToolset"
      via: "literal `<PlatformToolset>v145</PlatformToolset>` element"
      pattern: "<PlatformToolset>v145</PlatformToolset>"
---

<objective>
06-02 modernises the build: full vcpkg manifest-mode migration of seven vendored dependencies (catch2, CppSharp, DetourXS, nvapi, imgui, spdlog, ImGuizmo), spdlog bumped to 1.14 with a CON-N-09 regression fence, ImGuizmo bumped to latest stable, imgui switched to the docking branch (gated on 06-01 success per D-11), PlatformToolset bumped from v142 to v145 (VS 2026 / Dev18), and the VSIX manifest range widened to [16.0,19.0) per the CON-B-01 audit-then-widen precedent.

Purpose: D-05 honours Phase 5 D-02's explicit "Phase 6 STAB-03 owns the broader vcpkg call" deferral; D-06 enables TJT Wave-1 dockable subpanels (DEC-C4); D-07 preserves CON-N-09 across the spdlog formatter API churn; D-08 bumps ImGuizmo to latest stable independent of the imgui-bump risk; D-09 + D-10 align the toolchain with [[project-vs2026-toolchain]] and CON-B-01.

Output: vcpkg manifest at repo root, dependency directories purged or kept-vendored-with-justification, CI workflow extended with the vcpkg install step, OutputSink regression test under UtinniCore.Tests, all three CI lanes green on master.

Per-dep fallback path (D-05 + research-risk note in 06-CONTEXT.md `<code_context>`): catch2 + CppSharp ports are the highest-risk items. If a port is broken at plan execution time, keep that single dep vendored and document the broken-port evidence in 06-02-VCPKG-RESEARCH.md. Partial migration is acceptable; full migration is the goal but never at the cost of breaking the build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-CONTEXT.md
@.planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-01-DEMO-PROBE-NOTES.md
@.planning/intel/constraints.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.github/workflows/ci.yml
@UtinniCore.Tests/UtinniCore.Tests.vcxproj

<interfaces>
<!-- Key constraints and current state. Executor reads these AND extracts current toolset / SDK target / VSIX range values from each file before editing. -->

From .planning/intel/constraints.md:
- CON-N-09: `spdlog::sinks::base_sink<std::mutex>` for OutputSink — preserve under spdlog 1.14 (the OutputSink class lives in UtinniCore; grep `base_sink<std::mutex>` to locate).
- CON-N-06: imgui device-loss handling (`isSetup` guard, invalidate/recreate on `hkReset`) — preserve under imgui docking branch.
- CON-B-01: VS 2019+VS 2022 contributor support; D-10 widens to add VS 2026 → final range `[16.0,19.0)`.
- CON-T-02: triple-config (Debug + Release + RelWithDbgInfo) preserved end-to-end — every .vcxproj edit must retain all three configurations.

From .github/workflows/ci.yml lines 71-83:
- microsoft/setup-msbuild@v2 picks the latest installed MSBuild (currently v142). After v145 migration, the GitHub-hosted windows-2022 runner must have v145 build tools available (or the workflow installs them). Researcher must verify v145 availability on windows-2022 at plan execution time; if not yet available, options include switching to windows-2025 OR pinning a setup step that installs v145 build tools via the Visual Studio installer CLI.

From UtinniCore.Tests/UtinniCore.Tests.vcxproj lines 27, 33, 40:
- Current PlatformToolset is `v142` in all three configs — replace with `v145` literally.
- Lines 74, 89, 106: `AdditionalIncludeDirectories` references `$(SolutionDir)external` — after vcpkg migration, these references stay (vcpkg injects via different mechanism), but any header inclusion patterns that rely on `external/{name}/foo.h` may shift to `<{name}/foo.h>` via vcpkg's include path injection. Researcher documents per-dep header path changes.

From UtinniCoreDotNet/source.extension.vsixmanifest:
- Currently pins `[16.0,18.0)`; widen to `[16.0,19.0)`.

From UtinniCore/UtinniCore.vcxproj (read at plan execution):
- spdlog include path + nvapi include path + ImGuizmo + DetourXS include/lib paths all currently resolve via `$(SolutionDir)external/`; post-vcpkg these resolve via vcpkg toolchain integration.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: vcpkg manifest + per-dep port research + CI workflow integration</name>
  <files>
    vcpkg.json,
    vcpkg-configuration.json,
    .github/workflows/ci.yml,
    .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-02-VCPKG-RESEARCH.md
  </files>
  <read_first>
    .github/workflows/ci.yml,
    .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-CONTEXT.md,
    .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-01-DEMO-PROBE-NOTES.md,
    external/catch2/,
    external/imgui/,
    external/spdlog/,
    external/ImGuizmo/,
    external/DetourXS/,
    external/nvapi/,
    external/CppSharp/
  </read_first>
  <action>
    Research per-dep vcpkg port quality (vcpkg.io/en/packages or `vcpkg search <name>` against a fresh bootstrap) and write per-dep evidence into 06-02-VCPKG-RESEARCH.md with one section per dep: catch2, CppSharp, DetourXS, nvapi, imgui, spdlog, ImGuizmo. For each: declare port-name, current-vcpkg-version-vs-current-vendored-version, "migrate" or "keep-vendored", and (if keep-vendored) explicit broken-port evidence (port missing entirely, port doesn't support x86-windows triplet, port is on an unacceptably old version, build failure observed in a bootstrap probe). The imgui section MUST cross-reference 06-01-DEMO-PROBE-NOTES.md disposition and record either "imgui docking-branch via vcpkg port (06-01 disposition: full Demo functional)" OR "imgui master (no docking) via vcpkg port (06-01 escalation triggered; D-11 downgrade applied)" OR "imgui kept vendored on master (06-01 escalation + vcpkg port broken)". Author vcpkg.json with dependency objects for every migrated dep; declare `version>=` constraints conservatively (spdlog "1.14.0" minimum per D-07; imgui docking-branch using port version overrides if necessary). Author vcpkg-configuration.json with `default-registry` pinned to a specific microsoft/vcpkg commit SHA captured at execution time (record the SHA in 06-02-VCPKG-RESEARCH.md too). Add a `Install vcpkg dependencies` step to .github/workflows/ci.yml inserted BEFORE the `Setup MSBuild` step at line 71: invoke microsoft/setup-vcpkg@v1 (or vendored bootstrap fallback if action unavailable), run `vcpkg install --triplet x86-windows` against the manifest, cache the vcpkg installed tree by `vcpkg.json` hash. Commit atomically as "build(06-02): vcpkg manifest + per-dep port research + CI integration".
  </action>
  <verify>
    <automated>node -e "const j=require('./vcpkg.json'); if (!j.dependencies || !Array.isArray(j.dependencies) || j.dependencies.length === 0) process.exit(1); console.log('deps:', j.dependencies.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('vcpkg.json'))"` exits 0 (valid JSON).
    - `node -e "JSON.parse(require('fs').readFileSync('vcpkg-configuration.json'))"` exits 0.
    - vcpkg.json `dependencies` array contains at least three entries (catch2/CppSharp may be excluded under fallback; the rest must migrate unless documented otherwise).
    - vcpkg-configuration.json `default-registry.baseline` matches a 40-character lowercase hex SHA (regex `^[a-f0-9]{40}$`).
    - .github/workflows/ci.yml grep `vcpkg install` returns at least one match BEFORE the `Setup MSBuild` step (verify by line number ordering).
    - 06-02-VCPKG-RESEARCH.md exists and contains separate sections for: catch2, CppSharp, DetourXS, nvapi, imgui, spdlog, ImGuizmo (grep all seven names returns 7+ matches).
    - 06-02-VCPKG-RESEARCH.md grep `06-01` returns at least one cross-reference to the imgui docking-vs-master decision.
    - One atomic commit on master with prefix `build(06-02):`.
  </acceptance_criteria>
  <done>vcpkg manifest + configuration + CI workflow integration shipped; per-dep port research documented; CI vcpkg step runs before MSBuild.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate deps + delete vendored trees + spdlog 1.14 + OutputSink regression test + ImGuizmo bump</name>
  <files>
    UtinniCore/UtinniCore.vcxproj,
    UtinniCore.Tests/UtinniCore.Tests.vcxproj,
    UtinniCore.Tests/Log/OutputSinkRoundTripTests.cpp,
    Launcher/Launcher.vcxproj,
    UtINI/UtINI.vcxproj,
    UtinniCore-Symbols/UtinniCore-Symbols.vcxproj,
    Utinni.LoaderLockHarness/Utinni.LoaderLockHarness.vcxproj,
    Utinni.CrtMatchPlugin/Utinni.CrtMatchPlugin.vcxproj,
    Utinni.LegacyPlugin/Utinni.LegacyPlugin.vcxproj,
    external/imgui/imgui.vcxproj,
    external/imgui/,
    external/spdlog/,
    external/ImGuizmo/,
    external/catch2/,
    external/CppSharp/,
    external/DetourXS/,
    external/nvapi/,
    sdk/UtinniCppPluginTemplate/UtinniCppPluginTemplate/UtinniCppPluginTemplate.vcxproj,
    sdk/examples/ExampleCppPlugin/ExampleCppPlugin.vcxproj
  </files>
  <read_first>
    .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-02-VCPKG-RESEARCH.md,
    UtinniCore/UtinniCore.vcxproj,
    UtinniCore.Tests/UtinniCore.Tests.vcxproj,
    UtinniCore/utility/log.h,
    UtinniCore/utility/log.cpp,
    external/spdlog/sinks/
  </read_first>
  <action>
    For each dep marked "migrate" in 06-02-VCPKG-RESEARCH.md: produce one atomic commit per dep with the structure "build(06-02): migrate <dep> to vcpkg". Per-commit work: (a) remove the `$(SolutionDir)external/<dep>` references from every consuming .vcxproj's AdditionalIncludeDirectories / AdditionalLibraryDirectories / AdditionalDependencies; (b) update `#include <{dep}/...>` paths in source files if vcpkg port uses different layout (cite 06-02-VCPKG-RESEARCH.md for the new layout); (c) delete the `external/{dep}/` directory in git; (d) verify Release x86 build before commit. Order: DetourXS → nvapi → ImGuizmo (low-risk) → spdlog 1.14 (D-07 — see paragraph below) → imgui (per 06-01 disposition — see paragraph below) → catch2 → CppSharp. If a dep is "keep-vendored" per Task 1 research, skip its commit and document the skip in the plan summary at end.

    spdlog 1.14 commit MUST also create UtinniCore.Tests/Log/OutputSinkRoundTripTests.cpp: a Catch2 TEST_CASE that constructs the OutputSink, attaches a recording sink (capture std::vector<std::string> messages via a base_sink<std::mutex>-derived `RecordingSink` defined inline), emits at least three log messages of mixed severity, asserts the recorder captured them in order with the expected formatted string layout, and asserts the sink's base class is `spdlog::sinks::base_sink<std::mutex>` (not null_mutex) by static_assert or by a runtime `typeid` check on a typedef. This is the CON-N-09 fence: the test must fail if a future cleanup changes OutputSink's base to `base_sink<details::null_mutex>`. Add the new file to UtinniCore.Tests.vcxproj <ClCompile> ItemGroup. Verify `bin\Release\UtinniCore.Tests.exe --reporter console` passes including the new test.

    imgui commit: if 06-01-DEMO-PROBE-NOTES.md disposition is "no fix required", set vcpkg port spec to the docking-branch port (use `imgui[docking-experimental,dx9-binding,win32-binding]` or equivalent feature flag set; cite 06-02-VCPKG-RESEARCH.md). If disposition is "escalated", use the imgui master port (no docking) and record the downgrade in the commit message. external/imgui/ is deleted in either case; the local imgui.vcxproj wrapper may need to be removed from Utinni.sln if vcpkg-managed imgui replaces it (cite 06-02-VCPKG-RESEARCH.md).

    ImGuizmo: select latest stable commit/tag declared in 06-02-VCPKG-RESEARCH.md. Confirm the existing gizmo code in imgui_impl.cpp + imgui_impl_user-controls.cpp still links + runs.

    docs/ai/assessment.md §"Status tracking": after each dep migration commit, append a one-line entry "<date> migrated <dep> to vcpkg <version> in commit <SHA>"; do this as part of the same commit (no separate doc commit per dep).
  </action>
  <verify>
    <automated>msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86 && bin\Release\UtinniCore.Tests.exe --reporter console</automated>
  </verify>
  <acceptance_criteria>
    - Release x86 solution build exit code 0: `msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86`.
    - UtinniCore.Tests.exe Release run exit code 0 (Catch2 console reporter returns non-zero on test failure): `bin\Release\UtinniCore.Tests.exe --reporter console`.
    - UtinniCore.Tests.exe grep of its output for "OutputSink" returns at least one passing test reference.
    - For every dep marked "migrate" in 06-02-VCPKG-RESEARCH.md: `git ls-files external/<dep>/` returns zero entries (vendored tree deleted).
    - For every dep marked "keep-vendored": `git ls-files external/<dep>/` returns at least one entry (vendored tree retained per fallback).
    - Grep `$(SolutionDir)external/<dep>` across all .vcxproj returns zero matches for every "migrate" dep.
    - UtinniCore.Tests/Log/OutputSinkRoundTripTests.cpp exists; grep `base_sink<std::mutex>` returns at least one match (CON-N-09 fence in test source).
    - UtinniCore.Tests/Log/OutputSinkRoundTripTests.cpp is registered in UtinniCore.Tests.vcxproj `<ClCompile>` ItemGroup (grep `OutputSinkRoundTripTests.cpp` in vcxproj returns 1).
    - At least 4 atomic commits with prefix `build(06-02): migrate` on master (one per dep migrated; partial migration acceptable per fallback).
  </acceptance_criteria>
  <done>All migratable deps moved to vcpkg via per-dep atomic commits; vendored trees deleted; spdlog 1.14 lands with OutputSink CON-N-09 fence test passing; ImGuizmo bumped; imgui at docking-or-master per 06-01 disposition.</done>
</task>

<task type="auto">
  <name>Task 3: PlatformToolset v145 sweep + VSIX [16.0,19.0) widen + CI v145 availability + final lane verification</name>
  <files>
    UtinniCore/UtinniCore.vcxproj,
    UtinniCore.Tests/UtinniCore.Tests.vcxproj,
    Launcher/Launcher.vcxproj,
    UtINI/UtINI.vcxproj,
    UtinniCore-Symbols/UtinniCore-Symbols.vcxproj,
    Utinni.LoaderLockHarness/Utinni.LoaderLockHarness.vcxproj,
    Utinni.CrtMatchPlugin/Utinni.CrtMatchPlugin.vcxproj,
    Utinni.LegacyPlugin/Utinni.LegacyPlugin.vcxproj,
    sdk/UtinniCppPluginTemplate/UtinniCppPluginTemplate/UtinniCppPluginTemplate.vcxproj,
    sdk/examples/ExampleCppPlugin/ExampleCppPlugin.vcxproj,
    UtinniCoreDotNet/source.extension.vsixmanifest,
    .github/workflows/ci.yml
  </files>
  <read_first>
    UtinniCore.Tests/UtinniCore.Tests.vcxproj,
    UtinniCoreDotNet/source.extension.vsixmanifest,
    .github/workflows/ci.yml,
    .planning/intel/constraints.md
  </read_first>
  <action>
    Across every .vcxproj listed in <files>, replace literal `<PlatformToolset>v142</PlatformToolset>` with literal `<PlatformToolset>v145</PlatformToolset>` in all three configurations (Debug + Release + RelWithDbgInfo) — preserve the triple-config layout per CON-T-02. Update UtinniCoreDotNet/source.extension.vsixmanifest by replacing the current `<InstallationTarget Version="[16.0,18.0)" ...>` (or whatever literal range is present at execution time — read first) with `<InstallationTarget Version="[16.0,19.0)" ...>`. Update .github/workflows/ci.yml: add a step BEFORE `Setup MSBuild` (line 71) titled "Verify v145 build tools" that runs a pwsh probe checking for v145 toolset directory presence (literal probe path `${env:ProgramFiles}\Microsoft Visual Studio\2022\Enterprise\VC\Tools\MSVC\14.4*` OR `vswhere -find **\\VC\\Tools\\MSVC\\14.4*\\bin`); if the probe returns empty, the step invokes the VS Build Tools installer or vswhere to install the v145 workload; if installation succeeds the step exits 0. The Setup MSBuild step continues to use microsoft/setup-msbuild@v2 (which auto-picks v145 once it's installed because PlatformToolset=v145 in every .vcxproj). Commit as two commits: `build(06-02): PlatformToolset v142 -> v145 sweep` (the .vcxproj edits + VSIX widen, atomic — affects every project) followed by `ci(06-02): ensure windows-2022 runner has v145 build tools available` (the workflow edit). After both commits, push to a branch, run CI, confirm all three lanes (dotnet test, CLI golden, UtinniCore.Tests.exe) green; merge to master.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$f = Get-ChildItem -Path . -Recurse -Filter *.vcxproj | Where-Object { $_.FullName -notmatch 'external\\imgui|bin\\|obj\\|Release\\|Debug\\' }; $bad = $f | Where-Object { (Get-Content $_.FullName -Raw) -match '<PlatformToolset>v142</PlatformToolset>' }; if ($bad) { $bad | ForEach-Object { Write-Host $_.FullName }; exit 1 } else { exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - Powershell sweep above exits 0 (zero .vcxproj files retain v142 outside the external/ tree).
    - Powershell sweep `Get-ChildItem ... | ForEach-Object { (Get-Content $_.FullName -Raw) -match '<PlatformToolset>v145</PlatformToolset>' }` returns true for every Utinni-owned .vcxproj.
    - UtinniCoreDotNet/source.extension.vsixmanifest grep `[16.0,19.0)` returns at least one match.
    - UtinniCoreDotNet/source.extension.vsixmanifest grep `[16.0,18.0)` returns zero matches.
    - .github/workflows/ci.yml grep `Verify v145 build tools` returns at least one match BEFORE the `Setup MSBuild` step (verify by line ordering: step name appears at a smaller line number than `microsoft/setup-msbuild`).
    - CI green on master post-merge: `gh run list --branch master --limit 1 --json conclusion -q '.[0].conclusion'` returns "success".
    - Two atomic commits with prefixes `build(06-02): PlatformToolset` and `ci(06-02): ensure` respectively.
  </acceptance_criteria>
  <done>v142→v145 sweep across every Utinni .vcxproj; VSIX widen to [16.0,19.0); CI workflow verifies v145 build tools availability; all three lanes green on master.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vcpkg microsoft/vcpkg registry | Untrusted third-party port repository; baseline pin to a specific commit SHA mitigates supply-chain drift between CI runs. |
| Per-dep upstream repos (catch2, CppSharp, ocornut/imgui, gabime/spdlog, etc.) | Subject to package-legitimacy concerns; mitigated by per-dep version pins in vcpkg.json + manifest mode reproducibility. |
| GitHub Actions windows-2022 runner | Pre-imaged runner; setup-vcpkg action source is microsoft/setup-vcpkg — well-known publisher. v145 build tools installation step is invoked only when probe shows missing — no privilege escalation. |
| spdlog 1.14 formatter API change (fmt library migration) | Behavioural surface — CON-N-09 fence test catches `base_sink<std::mutex>` regression; formatter API surface for log call sites is exercised by every CI build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | vcpkg package downloads from microsoft/vcpkg | mitigate | Baseline pin to a 40-char SHA in vcpkg-configuration.json (`default-registry.baseline`); CI cache keyed by vcpkg.json hash; manifest mode produces reproducible installs. |
| T-06-02-02 | Tampering | Per-dep upstream sources (catch2, imgui, spdlog, ImGuizmo, CppSharp, DetourXS, nvapi) | mitigate | Package Legitimacy Audit in 06-02-VCPKG-RESEARCH.md per Task 1 (each dep documented with port-name + version + provenance); see audit table below. Treat [ASSUMED] entries as needing visual verification at npmjs.com / vcpkg.io equivalents. |
| T-06-02-03 | Information Disclosure | GitHub Actions logs include vcpkg install output | accept | No secrets in vcpkg install output; package names + versions are public. |
| T-06-02-04 | Denial of Service | CI minutes consumed by vcpkg install on every push (no cache hit) | mitigate | actions/cache@v4 keyed on `hashFiles('vcpkg.json')` so warm-cache CI hits avoid the full install. |
| T-06-02-05 | Elevation of Privilege | v145 build tools installer on CI runner | mitigate | Probe-first; only invoke installer when v145 missing; no admin escalation beyond what setup-msbuild already exercises. |
| T-06-02-06 | Repudiation | Migration commit lineage | mitigate | Per-dep atomic commits with `build(06-02): migrate <dep> to vcpkg` prefix produce a clean bisect-friendly history. |
| T-06-02-07 | Tampering | CON-N-09 (spdlog `base_sink<std::mutex>` for OutputSink) silently violated during 1.6→1.14 bump | mitigate | OutputSinkRoundTripTests.cpp Catch2 test enforces the type contract at runtime + grep `base_sink<std::mutex>` against test source produces 1+ match. |
| T-06-02-SC | Tampering | vcpkg port installs (this plan installs 5-7 dependency ports) | mitigate | Package Legitimacy Audit table below; ports treated as [VERIFIED] when from microsoft/vcpkg main registry at the baseline-pinned SHA; the planner has authorised these as the legitimate vcpkg port set; no [ASSUMED]/[SUS]/[SLOP] ports introduced. |

## Package Legitimacy Audit

| Package | Port name (vcpkg) | Disposition | Evidence |
|---------|-------------------|-------------|----------|
| catch2 | `catch2` | [VERIFIED] | microsoft/vcpkg main registry; well-known; mirrors github.com/catchorg/Catch2 |
| imgui | `imgui` (with feature flags `[docking-experimental,dx9-binding,win32-binding]` per Task 1 research) | [VERIFIED] | microsoft/vcpkg main registry; mirrors github.com/ocornut/imgui |
| spdlog | `spdlog` | [VERIFIED] | microsoft/vcpkg main registry; mirrors github.com/gabime/spdlog |
| imguizmo | `imguizmo` | [VERIFIED] | microsoft/vcpkg main registry; mirrors github.com/CedricGuillemet/ImGuizmo |
| nvapi | `nvapi` (or kept vendored if no vcpkg port) | [VERIFIED if port exists] | NVIDIA-published headers; researcher confirms vcpkg port presence in Task 1; falls back to vendored if absent. |
| detours | `detoursxs` (or DetourXS via fork port, or kept vendored) | [ASSUMED — VERIFY IN TASK 1] | Researcher MUST visit vcpkg.io/en/packages and document presence + last-update before adding to vcpkg.json. If no port exists, keep vendored (allowed under D-05 fallback). |
| CppSharp | `cppsharp` | [ASSUMED — VERIFY IN TASK 1] | Build-time codegen tool. 06-CONTEXT.md `<code_context>` flags this as the highest-risk research item. If port broken or absent, keep vendored. |

Per the security gate: every install task is preceded by a Task-1 research step that documents disposition in 06-02-VCPKG-RESEARCH.md; [ASSUMED] entries above MUST be promoted to [VERIFIED] or kept-vendored before Task 2's per-dep migration commits land.

</threat_model>

<verification>
- Manifest: vcpkg.json + vcpkg-configuration.json parse as valid JSON; baseline SHA matches `^[a-f0-9]{40}$`.
- CI workflow: `vcpkg install` step precedes `Setup MSBuild` step (line-ordering check); `Verify v145 build tools` step precedes `Setup MSBuild` step.
- Build: `msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86` exit code 0.
- Tests: `bin\Release\UtinniCore.Tests.exe --reporter console` exit code 0; OutputSinkRoundTripTests test case passes.
- Toolset sweep: powershell probe (Task 3 verify command) finds zero remaining `v142` PlatformToolset declarations outside `external/`.
- VSIX: source.extension.vsixmanifest contains `[16.0,19.0)`.
- Vendored cleanup: deleted vendored trees absent from `git ls-files`; kept-vendored deps justified in 06-02-VCPKG-RESEARCH.md.
- CI gate: master green post-merge across all three lanes (dotnet test + CLI golden + UtinniCore.Tests.exe).
</verification>

<success_criteria>
1. vcpkg manifest mode is the source of truth for at least 5 of 7 deps (catch2 + CppSharp may fall back to vendored per D-05).
2. spdlog 1.14 lands with the CON-N-09 OutputSinkRoundTripTests fence passing.
3. ImGuizmo at planner-selected stable commit; gizmo code still compiles + runs.
4. imgui at docking-branch port if 06-01 disposition was "no fix required"; otherwise at master per D-11 downgrade with downgrade documented.
5. PlatformToolset is v145 across every Utinni-owned .vcxproj; VSIX manifest is `[16.0,19.0)`.
6. CI green on master across all three lanes after the workflow + toolchain bump.
</success_criteria>

<output>
Create `.planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-02-SUMMARY.md` when done.
</output>
