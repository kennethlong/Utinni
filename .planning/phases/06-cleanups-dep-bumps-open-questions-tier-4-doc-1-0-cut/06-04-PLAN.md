---
phase: 06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut
plan: 04
type: execute
wave: 4
depends_on: [06-03]
files_modified:
  - Utinni.LoaderLockHarness/main.cpp
  - Utinni.LoaderLockHarness/Utinni.LoaderLockHarness.vcxproj
  - UtinniCoreDotNet.Tests/GameCallbacksTests.cs
  - UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj
  - .github/workflows/ci.yml
  - .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-04-FLAKE-INVESTIGATION.md
autonomous: true
requirements: [STAB-03]
must_haves:
  truths:
    - "Utinni.LoaderLockHarness is hardened against the 50ms-threshold flake on shared windows-2022 runners under contention — either the threshold is raised with documented justification, OR the harness uses a different signalling mechanism (warmup + best-of-N + median rather than first-call timer), OR the DllMain heaviness probe is split from the timing probe."
    - "UtinniCoreDotNet.Tests GameCallbacksTests.RegisterCallback_ForceGCCollect_CallbackStillFiresWithoutAV is hardened against the intermittent AV — the native bridge call is gated behind a deterministic-availability probe, OR the test is restructured to isolate the managed-side GC-survival assertion (Probe 2) from the native-trigger AV-probe (Probe 1)."
    - "Each fix lands its own atomic commit with a regression-targeting test or harness assertion per [[feedback-max-harness]]; reverting the fix MUST cause CI to turn red within one push."
    - "06-04-FLAKE-INVESTIGATION.md documents the root cause analysis for each flake including reproducer steps (where reproduction was achieved), failure-mode evidence (links to past CI run failures via gh run list), and the chosen mitigation rationale."
    - "CI runs all three lanes (dotnet test + CLI golden + UtinniCore.Tests.exe + LoaderLockHarness) green on master for at least three consecutive pushes after the fixes land."
    - "CONTEXT.md decisions addressed by this plan: D-17 (folded CI-flake todos — loader-lock-harness 50ms threshold + GameCallbacks GC AV — atomic per-fix + regression test per [[feedback-max-harness]]; gates the 1.0-rc.1 tag)."
  artifacts:
    - path: ".planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-04-FLAKE-INVESTIGATION.md"
      provides: "Root-cause investigation + chosen mitigation evidence for both flakes"
      contains: "loader-lock-harness, GameCallbacks"
    - path: "Utinni.LoaderLockHarness/main.cpp"
      provides: "Hardened DllMain-heaviness harness; either rebuilt timing methodology OR split into deterministic + timing probes"
  key_links:
    - from: "Utinni.LoaderLockHarness/main.cpp"
      to: "CI windows-2022 runner under contention"
      via: "best-of-N / warmup / median timing or alternative signal"
      pattern: "QueryPerformanceCounter|elapsedMs|samples"
    - from: "UtinniCoreDotNet.Tests/GameCallbacksTests.cs"
      to: "deterministic native bridge availability probe"
      via: "explicit File.Exists(UtinniCore.dll) check + Probe-1 vs Probe-2 separation"
      pattern: "File\\.Exists|DllNotFoundException"
---

<objective>
06-04 closes the two folded CI-stability todos (D-17): the loader-lock-harness 50ms-threshold flake on shared windows-2022 runners and the GameCallbacksTests.RegisterCallback_ForceGCCollect_CallbackStillFiresWithoutAV intermittent AV. Both are atomic per-fix commits per [[feedback-max-harness]]: each fix lands with a regression-targeted assertion that turns red if reverted. This plan gates the 1.0-rc.1 tag in 06-06 because the 1.0 success criterion #5 explicitly requires CI green on master.

Purpose: [[project-loader-lock-harness-ci-flake]] memory and 06-CONTEXT.md `<decisions>` D-17 both flag that the 50ms threshold flakes under contention (shared CI runners). The GameCallbacks AV is a separate-but-related test-infra brittleness. Without these fixes, every subsequent CI push faces re-run lottery before progressing — that breaks the cleanup sweep in 06-05 and the tag-push trigger in 06-06.

Output: hardened loader-lock-harness, hardened GameCallbacksTests, an investigation write-up capturing root cause + mitigation rationale, and at least three consecutive green CI runs on master after the fixes.

Sequencing within this plan: investigation FIRST (Task 1, no code edits), then the loader-lock-harness fix (Task 2), then the GameCallbacks fix (Task 3). Each code commit must demonstrate green CI before the next starts so we know we're fixing flakes one at a time without compounding regressions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-CONTEXT.md
@Utinni.LoaderLockHarness/main.cpp
@UtinniCoreDotNet.Tests/GameCallbacksTests.cs
@.github/workflows/ci.yml

<interfaces>
<!-- Current state of each flake. Executor reads source files at task time. -->

From Utinni.LoaderLockHarness/main.cpp (verified at planning time):
- Single LoadLibraryA("UtinniCore.dll") call, single QueryPerformanceCounter measurement, exit-code policy: `return (elapsedMs < 50.0) ? 0 : 1;`
- No warmup, no best-of-N, no median; one cold sample on a shared runner under unknown background load.
- The 50.0 ms threshold is hard-coded — a single number against a single sample with no statistical model.

From UtinniCoreDotNet.Tests/GameCallbacksTests.cs (verified at planning time):
- Two-probe structure (lines 72-88): Probe 1 is `NativeBridge.Utinni_TriggerInstallCallbacks()` via P/Invoke; Probe 2 is `InvokeCallbacksViaManagedReflection()`.
- Probe 1 is wrapped in `Record.Exception` + a soft "skip if DllNotFoundException || EntryPointNotFoundException" filter — BUT any other exception type from the native side (including AV materialised as a SEHException or ExecutionEngineException) propagates through `Assert.Null(ex)` and red-flags the test.
- 06-CONTEXT.md folded todos: `gamecallbacks-gc-av-flake-fix` (score 0.4) — `RegisterCallback_ForceGCCollect_CallbackStillFiresWithoutAV` intermittent AV under CI.

From .github/workflows/ci.yml lines 81-92 (dotnet test step):
- Project-targeted dotnet test for UtinniCoreDotNet.Tests; output to TestResults/test-results.trx; on-failure artifact upload. The LoaderLockHarness exe is invoked separately — search the workflow for `LoaderLockHarness` to find the current invocation step (read at task time).

Past failure evidence (collected via gh CLI at task time):
- Task 1 captures specific run IDs + failure URLs by running `gh run list --branch master --limit 50 --json conclusion,databaseUrl,headSha,event,name` and filtering for failed runs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root-cause investigation + failure-mode evidence collection (write-up only, no code edits)</name>
  <files>
    .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-04-FLAKE-INVESTIGATION.md
  </files>
  <read_first>
    Utinni.LoaderLockHarness/main.cpp,
    UtinniCoreDotNet.Tests/GameCallbacksTests.cs,
    .github/workflows/ci.yml,
    .planning/STATE.md,
    .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-CONTEXT.md
  </read_first>
  <action>
    Create 06-04-FLAKE-INVESTIGATION.md with two top-level sections — "Loader-Lock-Harness 50ms threshold flake" and "GameCallbacks ForceGCCollect AV flake" — each with subsections: (a) Symptom (what fails, how often, on which CI runner type), (b) Evidence (gh CLI commands + run IDs + specific failure log line excerpts), (c) Root cause hypothesis (per 06-CONTEXT.md D-17 + [[project-loader-lock-harness-ci-flake]] memory; planner expands), (d) Reproduction attempt (a local repro attempt OR an explicit "not locally reproducible — relies on shared-runner contention" disposition), (e) Chosen mitigation (planner picks one option per flake from the candidate list below and justifies the choice).

    Loader-lock-harness candidate mitigations (pick exactly one):
    - **OPT-A:** Best-of-3 timing — three LoadLibraryA/FreeLibrary cycles, take the minimum elapsed; threshold stays at 50.0 ms but applied to min-of-N (less noisy).
    - **OPT-B:** Warmup + median-of-5 — first LoadLibraryA/FreeLibrary cycle ignored as warmup, next five measured, median compared to 50.0 ms.
    - **OPT-C:** Threshold raise with documented justification — raise to a higher number (e.g. 200 ms) backed by empirical data from past CI runs collected in Task 1 evidence section.
    - **OPT-D:** Split the probe — DllMain heaviness validated by a deterministic side-channel (e.g., the DLL exposes a `getDllMainEntryExitMs()` debug export that captures the timing internally; harness reads that value rather than measuring externally). This is the cleanest fix but the largest scope.

    GameCallbacks AV candidate mitigations (pick exactly one):
    - **OPT-A:** Skip Probe 1 (native trigger) entirely when running outside an injected SWG session — gate on `File.Exists("UtinniCore.dll")` AND an additional liveness probe (e.g., a small native function that returns a sentinel without touching the callback list). Probe 2 (managed-side reflection) covers the GC-survival assertion in isolation.
    - **OPT-B:** Catch SEHException + AccessViolationException explicitly in Probe 1's `Record.Exception` filter; treat AV as expected when running in a test process (no SWG attached) rather than a green-light. Document the semantic shift in the test comment.
    - **OPT-C:** Split into two test methods — `RegisterCallback_ForceGCCollect_ManagedSideGcSurvival` (Probe 2 only, deterministic) and `RegisterCallback_ForceGCCollect_NativeBridgeNoAV` (Probe 1 only, marked `[SkippableFact]` with the native-availability gate). The latter is allowed to skip on CI runners; the former is the green-CI gate.

    Run the gh CLI evidence commands and attach output to the doc:
    ```
    gh run list --branch master --workflow=CI --limit 50 --json conclusion,databaseId,headSha,createdAt --jq '.[] | select(.conclusion == "failure")'
    gh run view <ID> --log-failed | grep -i "loader\|gamecallback\|elapsedMs\|ForceGCCollect" | head -20
    ```
    No code changes in this task. Commit atomically as `docs(06-04): flake investigation + mitigation selection`. Push, confirm CI still green (no code changed, doc-only commit).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if ((Get-Content '.planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-04-FLAKE-INVESTIGATION.md' -Raw) -match 'OPT-[A-D]' -and (Get-Content '.planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-04-FLAKE-INVESTIGATION.md' -Raw) -match 'GameCallbacks') { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - 06-04-FLAKE-INVESTIGATION.md exists with at least two top-level sections (`# Loader-Lock-Harness ...` and `# GameCallbacks ...`).
    - Grep `06-04-FLAKE-INVESTIGATION.md` for "OPT-A|OPT-B|OPT-C|OPT-D" returns at least two matches (one selected option per flake).
    - Grep for "Selected:" returns at least two matches identifying the chosen mitigation per flake.
    - Grep for "gh run" returns at least one match (evidence-collection command attached).
    - One atomic commit on master with prefix `docs(06-04):`.
  </acceptance_criteria>
  <done>Investigation written, mitigation OPT chosen for each flake, evidence attached, no code changes.</done>
</task>

<task type="auto">
  <name>Task 2: Loader-lock-harness flake fix (per OPT selected in Task 1)</name>
  <files>
    Utinni.LoaderLockHarness/main.cpp,
    Utinni.LoaderLockHarness/Utinni.LoaderLockHarness.vcxproj
  </files>
  <read_first>
    Utinni.LoaderLockHarness/main.cpp,
    .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-04-FLAKE-INVESTIGATION.md
  </read_first>
  <action>
    Implement the loader-lock-harness mitigation selected in 06-04-FLAKE-INVESTIGATION.md. If OPT-A (best-of-3): replace the single LoadLibraryA + measurement with a loop of three cycles, take `std::min` across all three; threshold stays 50.0 ms applied to the minimum. If OPT-B (warmup + median-of-5): six cycles total (one warmup discarded, five measured), sort the five samples ascending, compare `samples[2]` (median) to 50.0 ms. If OPT-C (threshold raise): replace `50.0` with the planner-selected higher value AND embed a code comment quoting the empirical data from 06-04-FLAKE-INVESTIGATION.md (the comment must include at least one specific run ID + observed elapsedMs value as evidence). If OPT-D (split probe): add an exported `__declspec(dllexport) double getDllMainEntryExitMs()` to UtinniCore that captures DllMain's entry+exit QueryPerformanceCounter timestamps in static-storage globals at the very start and very end of DllMain (CON-H-01 still preserved — DllMain stays heavy-work-free; we just record two timestamps), then change Utinni.LoaderLockHarness/main.cpp to read that export via GetProcAddress after LoadLibraryA returns; threshold stays 50.0 ms but applied to the in-DLL-measured value (no contention noise from the LoadLibraryA call itself).

    The fix must include a code-comment cross-reference to the 06-04-FLAKE-INVESTIGATION.md OPT label so a future maintainer can trace back to the rationale (e.g., `// OPT-B: warmup + median-of-5 per 06-04-FLAKE-INVESTIGATION.md`).

    Add a regression-target assertion: a deliberately-failing test trigger in main.cpp behind a `#ifdef LOADER_LOCK_HARNESS_REGRESSION_PROBE` block — a `#if 0` or compile-time-defined sleep that would push the elapsed time over the threshold; this is not built by default but exists so a maintainer can flip the macro and confirm the harness still catches DllMain regressions. Document the regression probe in 06-04-FLAKE-INVESTIGATION.md.

    Commit atomically as `fix(06-04): harden loader-lock-harness against contention flake (OPT-<X>)`. Push to a branch and run CI at least three times (re-run if needed) before merging to master to confirm the harness is stable.
  </action>
  <verify>
    <automated>msbuild Utinni.sln /m /p:Configuration=Release /p:Platform=x86 /t:Utinni_LoaderLockHarness && bin\Release\Utinni.LoaderLockHarness.exe</automated>
  </verify>
  <acceptance_criteria>
    - Utinni.LoaderLockHarness/main.cpp grep `OPT-A|OPT-B|OPT-C|OPT-D` returns at least one match in a comment.
    - Utinni.LoaderLockHarness/main.cpp grep `06-04-FLAKE-INVESTIGATION` returns at least one match.
    - For OPT-A/B: grep `samples|min_elapsed|median` returns at least one identifier match.
    - For OPT-C: grep against main.cpp source returns NO `50.0` literal in a comparison context (replaced with the new threshold).
    - For OPT-D: UtinniCore exports `getDllMainEntryExitMs` — `dumpbin /exports bin/Release/UtinniCore.dll` lists the symbol.
    - Local Release run exit code 0: `bin\Release\Utinni.LoaderLockHarness.exe`.
    - CI on master green for at least 3 consecutive runs after the merge: `gh run list --branch master --limit 3 --json conclusion -q '[.[].conclusion]'` returns three "success" entries.
    - One atomic commit on master with prefix `fix(06-04):`.
  </acceptance_criteria>
  <done>Loader-lock-harness hardened per selected OPT; regression-probe macro embedded; 3 consecutive green CI runs on master.</done>
</task>

<task type="auto">
  <name>Task 3: GameCallbacks ForceGCCollect AV flake fix (per OPT selected in Task 1)</name>
  <files>
    UtinniCoreDotNet.Tests/GameCallbacksTests.cs,
    UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj
  </files>
  <read_first>
    UtinniCoreDotNet.Tests/GameCallbacksTests.cs,
    UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj,
    .planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-04-FLAKE-INVESTIGATION.md
  </read_first>
  <action>
    Implement the GameCallbacks mitigation selected in 06-04-FLAKE-INVESTIGATION.md.

    If OPT-A (gate Probe 1 behind deterministic-availability probe): add a `File.Exists` check for UtinniCore.dll AND a P/Invoke to a small new sentinel function `utinni_testHarnessProbe` (which UtinniCore exports — add the export to UtinniCore as part of this commit; the function returns a fixed magic value like `0xDEADBEEFu` and touches no callback state). If either gate fails, skip Probe 1 entirely (Probe 2 still runs and is the green-CI assertion).

    If OPT-B (explicit SEHException + AccessViolationException catch): import `System.Runtime.InteropServices` for SEHException; widen Probe 1's exception filter to also accept AccessViolationException (via `[HandleProcessCorruptedStateExceptions]` attribute on the test method if necessary on .NET Framework 4.7.2) AND SEHException — treat them as expected-skip outcomes rather than test failures. Embed a comment explaining that the AV is a test-infra brittleness (no SWG memory layout in the test process), not a regression.

    If OPT-C (split into two test methods): create `RegisterCallback_ForceGCCollect_ManagedSideGcSurvival` containing only the current Probe 2 (InvokeCallbacksViaManagedReflection + Assert.True(fired)) AND `RegisterCallback_ForceGCCollect_NativeBridgeNoAV` marked with `[SkippableFact]` (add Xunit.SkippableFact NuGet ref to UtinniCoreDotNet.Tests.csproj) containing only Probe 1; the latter calls `Skip.IfNot(NativeAvailable())` where NativeAvailable() returns false unless UtinniCore.dll + the sentinel probe succeed. Delete the original combined test method.

    Whichever OPT lands, add a regression-target comment: `// Regression: this fix is fenced — if the gate/catch/split is removed, Probe 1 will rejoin Probe 2 and the test will flake on CI again. See 06-04-FLAKE-INVESTIGATION.md.`

    Commit atomically as `test(06-04): isolate GameCallbacks GC-survival assertion from native AV flake (OPT-<X>)`. Push to a branch, run CI at least three times to confirm stability before merging to master.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --configuration Release --logger "console;verbosity=normal"</automated>
  </verify>
  <acceptance_criteria>
    - GameCallbacksTests.cs grep `OPT-A|OPT-B|OPT-C` returns at least one match in a comment.
    - GameCallbacksTests.cs grep `06-04-FLAKE-INVESTIGATION` returns at least one match.
    - For OPT-A: grep `utinni_testHarnessProbe` returns at least one match; UtinniCore exports the symbol (dumpbin verifies).
    - For OPT-B: grep `AccessViolationException|SEHException` returns at least one match; the catch widening is visible.
    - For OPT-C: grep `SkippableFact` returns at least one match; the test method count for the relevant scenario is 2 (the original is split into two).
    - Local dotnet test exit code 0: `dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --configuration Release`.
    - CI on master green for at least 3 consecutive runs after the merge: `gh run list --branch master --limit 3 --json conclusion -q '[.[].conclusion]'` returns three "success" entries.
    - One atomic commit on master with prefix `test(06-04):`.
  </acceptance_criteria>
  <done>GameCallbacks AV flake fixed per selected OPT; regression comment + cross-reference embedded; 3 consecutive green CI runs on master.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions windows-2022 runner | Pre-imaged; shared-runner contention is the root cause of the loader-lock-harness flake. No untrusted input crosses into the harness. |
| UtinniCore.dll under test | Local build artifact loaded by the harness and by the test process; trusted side. |
| Test process AV materialised through P/Invoke | Probe 1 of GameCallbacksTests calls a native function that may AV in the test process (no SWG memory layout). The AV is benign (test-infra brittleness, not a runtime defect). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering | Loader-lock-harness threshold raised silently to mask real regressions | mitigate | Task 2 regression-probe macro (compile-time-gated) lets a maintainer force the harness over the threshold to confirm it still catches DllMain heaviness regressions. Comment cross-references the OPT label in 06-04-FLAKE-INVESTIGATION.md so the rationale is one grep away. |
| T-06-04-02 | Denial of Service | OPT-A/OPT-B introduce best-of-3 / median-of-5 loops — CI duration increases | accept | Three to six LoadLibraryA cycles per CI run is single-digit-second overhead on the windows-2022 runner; CI lane already has 25-minute timeout. |
| T-06-04-03 | Repudiation | GameCallbacks AV mitigation could mask a real native-side regression | mitigate | OPT-A's sentinel `utinni_testHarnessProbe` export decouples "UtinniCore loadable + responsive" from "callback list iterates without AV"; OPT-C's split keeps the native-bridge assertion alive as a separate SkippableFact that maintainers can run locally where SWG memory layout is available. OPT-B preserves the green CI but the widened catch is explicit, not silent. |
| T-06-04-04 | Information Disclosure | Test logs may contain AV stack traces | accept | Local test logs only; failure-mode artifact upload remains gated on test failure; no PII or secrets in AV traces. |
| T-06-04-SC | Tampering | npm/pip/cargo installs | n/a | If OPT-C (SkippableFact NuGet package) is selected, the install IS in scope; legitimacy table below covers it. |

## Package Legitimacy Audit (conditional)

If OPT-C is selected for Task 3, Xunit.SkippableFact is added as a NuGet dependency.

| Package | Source | Disposition | Evidence |
|---------|--------|-------------|----------|
| Xunit.SkippableFact | nuget.org | [VERIFIED if OPT-C selected, otherwise n/a] | Published by Andrew Arnott (AArnott) — well-known xunit ecosystem contributor; verify by visiting `https://www.nuget.org/packages/Xunit.SkippableFact` at task time and pinning a specific version in UtinniCoreDotNet.Tests.csproj. Task 3 acceptance criteria check the package version pin. |

If OPT-A or OPT-B is selected, no new packages are introduced and the audit is empty.

</threat_model>

<verification>
- 06-04-FLAKE-INVESTIGATION.md ships with both flake sections and explicit OPT selection.
- Loader-lock-harness main.cpp contains the selected OPT mitigation + a regression-probe macro + a cross-reference comment.
- GameCallbacksTests.cs contains the selected OPT mitigation + a regression-target comment + a cross-reference.
- 3 consecutive green CI runs on master after each fix lands (`gh run list --limit 3` verification).
- Local Release build + Utinni.LoaderLockHarness.exe + dotnet test all exit 0.
</verification>

<success_criteria>
1. Both flakes have a documented root-cause hypothesis + selected mitigation in 06-04-FLAKE-INVESTIGATION.md.
2. Loader-lock-harness hardened per selected OPT with a regression-probe macro.
3. GameCallbacksTests AV mitigation lands per selected OPT with a regression comment.
4. 3 consecutive green CI runs on master after the plan completes.
5. 06-05 cleanup sweep unblocked.
</success_criteria>

<output>
Create `.planning/phases/06-cleanups-dep-bumps-open-questions-tier-4-doc-1-0-cut/06-04-SUMMARY.md` when done.
</output>
