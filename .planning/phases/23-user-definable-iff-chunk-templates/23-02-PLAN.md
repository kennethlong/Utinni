---
phase: 23-user-definable-iff-chunk-templates
plan: 02
type: execute
wave: 1
depends_on: ["23-01"]
files_modified:
  - UtinniCoreDotNet/Formats/Template/TemplateModel.cs
  - UtinniCoreDotNet/Formats/Template/TemplateJson.cs
  - UtinniCoreDotNet/Formats/Template/Presets/vector.json
  - UtinniCoreDotNet/Formats/Template/Presets/quaternion.json
  - UtinniCoreDotNet/Formats/Template/Presets/matrix.json
  - UtinniCoreDotNet/Formats/Template/Presets/color.json
  - UtinniCoreDotNet/Formats/Template/Presets/colorArgb32.json
  - UtinniCoreDotNet/Formats/Template/Presets/colorArgbF.json
  - UtinniCoreDotNet/Formats/Template/Presets/stringId.json
  - UtinniCoreDotNet/UtinniCoreDotNet.csproj
autonomous: true
requirements: [PROD-IFFT-01]

must_haves:
  truths:
    - "A modder-authored template JSON file (de)serializes to a fixed POCO model round-trip-stable"
    - "The shipped presets (vector/quaternion/matrix/3 colors/stringId) are JSON sugar over the kernel with EXACT D-09 byte layouts, not engine code"
    - "Deserialization uses a fixed POCO model with TypeNameHandling disabled (no hostile-template gadget)"
  artifacts:
    - path: "UtinniCoreDotNet/Formats/Template/TemplateModel.cs"
      provides: "JSON-backed template POCO: version, match key, FieldRecord tree, repeat-spec, enum/flags map, encoding"
      min_lines: 60
    - path: "UtinniCoreDotNet/Formats/Template/TemplateJson.cs"
      provides: "Newtonsoft (de)serialize with sorted keys, fixed POCO, no TypeNameHandling"
      min_lines: 40
    - path: "UtinniCoreDotNet/Formats/Template/Presets/quaternion.json"
      provides: "w,x,y,z (w FIRST) 4×f32 LE struct preset per D-09"
      contains: "w"
  key_links:
    - from: "UtinniCoreDotNet/Formats/Template/TemplateJson.cs"
      to: "Newtonsoft.Json.Linq"
      via: "JObject (de)serialize to fixed POCO"
      pattern: "Newtonsoft\\.Json"
    - from: "UtinniCoreDotNet/Formats/Template/Presets/vector.json"
      to: "TemplateModel"
      via: "preset is a struct of 3 f32 fields named x,y,z"
      pattern: "x.*y.*z"
---

<objective>
The template SCHEMA layer (D-01/D-08/D-09/D-11/D-13): the JSON-backed POCO model, its Newtonsoft
(de)serializer, and the shipped preset JSON files with EXACT swg-client-v2-pinned byte layouts.
This is the "describe an arbitrary layout as a named reusable template" half of PROD-IFFT-01 — the
data model the kernel codec (23-03) reads/writes through.

Purpose: a template is a portable JSON file = canonical source of truth (D-01). Presets are pure
sugar (pre-built structs over the kernel, D-08) so byte-exactness can live ENTIRELY in the kernel
and a preset can never introduce a parity bug.
Output: TemplateModel.cs + TemplateJson.cs (headless) + 7 preset JSON files under a shipped pack dir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-user-definable-iff-chunk-templates/23-RESEARCH.md
@.planning/phases/23-user-definable-iff-chunk-templates/23-CONTEXT.md
@.planning/phases/23-user-definable-iff-chunk-templates/23-PATTERNS.md
@UtinniCoreDotNet/Formats/Template/ITemplateContracts.cs

<interfaces>
<!-- From 23-01 ITemplateContracts.cs (already in the tree): -->
<!-- TemplateModel { int Version; string MatchAncestorPath; string MatchLeafTag; bool MatchTagOnly; List<FieldRecord> Fields } -->
<!-- FieldRecord { string Name; KernelType Type; int ByteWidth; string Encoding; RepeatSpec Repeat; NamedValueMap ValueMap; List<FieldRecord> StructFields } -->
<!-- RepeatSpec { RepeatKind Kind; int FixedCount; string CountFieldName } ; RepeatKind { FixedCount, CountFromField, UntilEnd } -->
<!-- NamedValueMap { bool IsFlags; Dictionary<string,long> Entries } -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TemplateModel + TemplateJson (de)serializer</name>
  <files>UtinniCoreDotNet/Formats/Template/TemplateModel.cs, UtinniCoreDotNet/Formats/Template/TemplateJson.cs, UtinniCoreDotNet/Formats/Template/Presets/vector.json, UtinniCoreDotNet/Formats/Template/Presets/quaternion.json, UtinniCoreDotNet/Formats/Template/Presets/matrix.json, UtinniCoreDotNet/Formats/Template/Presets/color.json, UtinniCoreDotNet/Formats/Template/Presets/colorArgb32.json, UtinniCoreDotNet/Formats/Template/Presets/colorArgbF.json, UtinniCoreDotNet/Formats/Template/Presets/stringId.json, UtinniCoreDotNet/UtinniCoreDotNet.csproj</files>
  <read_first>
    - UtinniCoreDotNet/Formats/Template/ITemplateContracts.cs (the frozen POCO shapes to flesh out)
    - Utinni.Cli/Commands/ApplySaveIffCommand.cs (Newtonsoft JObject/JValue build idiom, lines ~31, ~202-214)
    - UtinniCoreDotNet/Formats/ClientEffect/ClientEffectCommand.cs (POCO model discipline — immutable-ish, defensive copies — for style)
    - .planning/phases/23-user-definable-iff-chunk-templates/23-RESEARCH.md (§"D-08 Kernel ↔ SOE .tdf Vocabulary Map", §Security — "Deserialize to a fixed POCO; do NOT enable TypeNameHandling")
  </read_first>
  <behavior>
    - Test: a TemplateModel with one field of each kernel type + an array (CountFromField) + a struct + an enum map + a flags map serializes to JSON and deserializes back to an equal model. Filter: "Template&Json&Roundtrip".
    - Test: deserializing a template JSON missing the `version` field defaults safely and does not throw (forward-migration, D-13). Filter: "Template&Json&Version".
    - Test: a JSON document with a `$type`/TypeNameHandling gadget payload is ignored (POCO-only deserialize, no type resolution). Filter: "Template&Json&Security".
  </behavior>
  <action>
    Flesh out TemplateModel.cs from the Task-1 (23-01) contract shapes — concrete POCO with the
    D-13 `version` field, the D-04 match-key fields (MatchAncestorPath, MatchLeafTag, MatchTagOnly),
    and the FieldRecord tree (name / KernelType / ByteWidth / Encoding default "ASCII" / RepeatSpec /
    NamedValueMap / nested StructFields). Follow ClientEffectCommand POCO discipline (defensive list
    copies). Create TemplateJson.cs: static Serialize(TemplateModel)->string and
    Deserialize(string)->TemplateModel using Newtonsoft. Serialize with sorted keys
    (Formatting.Indented + a contract resolver or stable property order) so files diff cleanly (D-01).
    Deserialize with JsonSerializerSettings explicitly setting TypeNameHandling = None (security:
    hostile-template gadget mitigation). Enum members (KernelType/RepeatKind) serialize as strings
    via StringEnumConverter for human-authorability. Throw a typed TemplateException on a malformed
    enum value rather than silently defaulting. Add the new files to UtinniCoreDotNet.csproj if it
    does not auto-glob; mark the Presets/*.json as Content / CopyToOutputDirectory=PreserveNewest so
    the shipped pack ships next to the assembly. Generated/UtinniCore.cs MUST NOT be touched.
    Grep-gate hygiene: no gated SWG type tokens in comments.
  </action>
  <verify>
    <automated>MSBuild Utinni.sln /p:Configuration=Release /p:Platform=x86 then `dotnet test Utinni.Cli.Tests --no-build --filter "Template&Json"` passes (un-skip the JSON tests authored here)</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `--filter "Template&Json&Roundtrip"` passes (model→JSON→model equality).
    - Source assertion: TemplateJson Deserialize sets TypeNameHandling = None explicitly.
    - Source assertion: TemplateModel has an int Version field (D-13).
  </acceptance_criteria>
  <done>TemplateModel + TemplateJson compile headless; JSON round-trip + version-default + security tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Shipped preset JSON files (D-09 PINNED byte layouts)</name>
  <files>UtinniCoreDotNet/Formats/Template/TemplateModel.cs, UtinniCoreDotNet/Formats/Template/TemplateJson.cs, UtinniCoreDotNet/Formats/Template/Presets/vector.json, UtinniCoreDotNet/Formats/Template/Presets/quaternion.json, UtinniCoreDotNet/Formats/Template/Presets/matrix.json, UtinniCoreDotNet/Formats/Template/Presets/color.json, UtinniCoreDotNet/Formats/Template/Presets/colorArgb32.json, UtinniCoreDotNet/Formats/Template/Presets/colorArgbF.json, UtinniCoreDotNet/Formats/Template/Presets/stringId.json, UtinniCoreDotNet/UtinniCoreDotNet.csproj</files>
  <read_first>
    - .planning/phases/23-user-definable-iff-chunk-templates/23-RESEARCH.md (§"D-09 SWG Composite Byte Layouts (PINNED — do NOT guess)" — the table with file:line citations)
    - .planning/phases/23-user-definable-iff-chunk-templates/23-CONTEXT.md (D-08 presets-are-sugar, D-09 multiplicity, D-14 ship the set)
    - UtinniCoreDotNet/Formats/Template/TemplateJson.cs (the schema the preset files must parse cleanly into)
  </read_first>
  <action>
    Author the preset JSON files under UtinniCoreDotNet/Formats/Template/Presets/ as struct templates
    over the kernel (each preset = a TemplateModel with a Struct field whose StructFields are the
    pinned components). Use the EXACT D-09 layouts (do NOT guess — RESEARCH has file:line):
    - vector.json: struct { x: f32, y: f32, z: f32 }  (3×f32 LE, 12 bytes; Iff.cpp:826)
    - quaternion.json: struct { w: f32, x: f32, y: f32, z: f32 }  (w FIRST, 4×f32 LE, 16 bytes; Iff.cpp:876)
    - matrix.json: struct of 12 f32 in row-major 3×4 order m00..m03,m10..m13,m20..m23 (48 bytes; Iff.cpp:861) — name fields so the y:0..2,x:0..3 iteration order is unambiguous; comment in a JSON description field that it is a 3×4 affine, NOT 4×4.
    - color.json (default = PackedRgb): struct { r: u8, g: u8, b: u8 } (3 bytes; AffectorColor.cpp:170-194)
    - colorArgb32.json: u32 LE single field, ARGB packed (A<<24|R<<16|G<<8|B), 4 bytes (PackedArgb.h)
    - colorArgbF.json: struct { a: f32, r: f32, g: f32, b: f32 } (4×f32 LE, 16 bytes; Iff.cpp:843)
    - stringId.json: struct { table: cstring, text: cstring } (two NUL C-strings; document as the override-prone preset per Pitfall 2 / Assumption A1).
    Each file carries the D-13 `version` field. These ARE the shipped pack and double as teaching
    artifacts (D-14). They must deserialize cleanly through TemplateJson.Deserialize.
  </action>
  <verify>
    <automated>`dotnet test Utinni.Cli.Tests --no-build --filter "Template&Preset"` — each preset JSON loads via TemplateJson.Deserialize and (once 23-03 lands the codec) decodes its component layout; for THIS plan a load+schema-shape assertion suffices (quaternion field order w,x,y,z; matrix 12 fields; color default 3×u8)</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: every Presets/*.json deserializes without throwing (schema-valid).
    - Source assertion: quaternion.json field order is w,x,y,z (w first); matrix.json has 12 f32 fields; color.json default is 3×u8 r,g,b.
    - Source assertion: each preset JSON has a `version` field.
  </acceptance_criteria>
  <done>7 preset JSON files ship under Presets/, schema-valid, with EXACT D-09 layouts (w-first quaternion, 3×4 row-major matrix, 3 color forms).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-authored template JSON → Newtonsoft deserialize | a template file is attacker-controllable input crossing into the engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02-DE | Tampering | TemplateJson.Deserialize | mitigate | Deserialize to a FIXED POCO model with `TypeNameHandling = None` explicitly set — no `$type` gadget resolution (RESEARCH Security). |
| T-23-02-EN | Tampering | malformed enum / out-of-range KernelType in JSON | mitigate | StringEnumConverter throws a typed TemplateException on an unknown member rather than silently defaulting to type 0. |
| T-23-SC | Tampering | NuGet installs | accept | Zero new packages; Newtonsoft already referenced by the project (RESEARCH Package Audit N/A). |
</threat_model>

<verification>
- MSBuild solution build green (x86 Release).
- `dotnet test Utinni.Cli.Tests --no-build --filter "Template&Json"` and `--filter "Template&Preset"` pass.
- Generated/UtinniCore.cs unchanged (`git status` clean for that file).
</verification>

<success_criteria>
- TemplateModel + TemplateJson compile headless (no WinForms dependency — D-17.1).
- JSON round-trip stable; TypeNameHandling disabled; version-field forward-migration safe.
- 7 preset JSON files ship with EXACT D-09 byte layouts, schema-valid.
</success_criteria>

<output>
Create `.planning/phases/23-user-definable-iff-chunk-templates/23-02-SUMMARY.md` when done.
</output>
