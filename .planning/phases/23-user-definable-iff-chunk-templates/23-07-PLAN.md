---
phase: 23-user-definable-iff-chunk-templates
plan: 07
type: execute
wave: 4
depends_on: ["23-03", "23-04"]
files_modified:
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs"
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.Designer.cs"
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/TemplateBuilderPane.cs"
autonomous: true
requirements: [PROD-IFFT-03]

must_haves:
  truths:
    - "The IFF Editor has a Template leaf-pane mode: select a byte range -> assign type+name -> the template grows, decode preview updates live, a byte-exact round-trip indicator runs continuously"
    - "Un-annotated bytes stay visibly raw (un-boxed); annotated spans get a box + tag so 'you haven't consumed the whole payload' is impossible to miss"
    - "A matching template auto-applies silently on green; a key-match that doesn't round-trip shows the red does-not-fit flag; the built-in-claimed file disables the Template toggle with the altitude tooltip"
    - "The new pane obeys Pitfall 8 (Dock.Fill front-most, nested SplitContainer Size-before-SplitterDistance, MEF-safe ctor)"
  artifacts:
    - path: "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/TemplateBuilderPane.cs"
      provides: "the Tier-B hex-driven builder pane (raw-byte grid + field list + round-trip indicator) calling the headless engine"
      min_lines: 120
  key_links:
    - from: "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/TemplateBuilderPane.cs"
      to: "UtinniCoreDotNet.Formats.Template.KernelCodec / FitReport / TemplateResolver"
      via: "live decode + FitReport + SetPayload(kernel-encoded bytes)"
      pattern: "KernelCodec|FitReport|TemplateResolver"
    - from: "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs"
      to: "MutableIffNode.GetPayloadCopy / SetPayload"
      via: "the template mode reads/commits the bound leaf payload"
      pattern: "GetPayloadCopy|SetPayload"
---

<objective>
The Tier-B in-place hex-driven template builder (D-02, the heart of the phase) as the fourth
leaf-pane MODE of `FormIffEditor.pnlLeafEditor` (DEC-C4 — inside The Jawa Toolbox). Select a byte
range → assign type+name → the template grows, the decode preview updates live, a byte-exact
round-trip indicator runs continuously, un-annotated bytes stay visibly raw. The pane CALLS the
headless engine (23-03/23-04) — it owns NO format logic.

Purpose: the better mousetrap — bind authoring to the live bytes (offsets are selections, not
arithmetic). The pane is UI-only (D-16, a legitimate non-exception to verbs-first: an interaction,
not a batch capability). Cross-repo: lands in `D:/Code/UtinniPlugins` (standing write authority;
paired commit, no human checkpoint except the live smoke in 23-08).
Output: TemplateBuilderPane.cs + the `btnTemplateMode` toggle wired into FormIffEditor per UI-SPEC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-user-definable-iff-chunk-templates/23-UI-SPEC.md
@.planning/phases/23-user-definable-iff-chunk-templates/23-PATTERNS.md
@.planning/phases/23-user-definable-iff-chunk-templates/23-RESEARCH.md

<interfaces>
<!-- Engine (23-03/04, headless UtinniCoreDotNet — call these, own NO format logic): -->
<!--   KernelCodec.Decode(TemplateModel, byte[]) / .Encode(TemplateModel, values) -->
<!--   FitReport.FitCheck(TemplateModel, byte[]) -> { ConsumedExactly, BytesConsumed, BytesTotal, PerField } -->
<!--   TemplateResolver.Resolve(tree, leaf) -> matched template + FitReport (or no-match / multi-candidate) -->
<!--   TemplateJson.Serialize/Deserialize ; TemplatePackStore (scan/load) -->
<!--   TypePlausibility (select->suggest-type assist) -->
<!-- Host (FormIffEditor.cs, sibling repo — extend, do not restyle): -->
<!--   currentLeaf (MutableIffNode), GetPayloadCopy(), SetPayload(byte[]) -->
<!--   textModeActive flag + btnHexMode/btnTextMode toggles (pattern for btnTemplateMode) -->
<!--   IffEditController controller (ride for document/value edits, Ctrl+Z/Y/S via ProcessCmdKey) -->
<!--   txtHex Consolas-9pt + HexDump format ; Colors.*() accessors ; UtinniContextMenuStrip -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TemplateBuilderPane control — raw-byte grid + field list + round-trip indicator</name>
  <files>D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs, D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.Designer.cs, D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/TemplateBuilderPane.cs</files>
  <read_first>
    - .planning/phases/23-user-definable-iff-chunk-templates/23-UI-SPEC.md (WHOLE FILE — host surface, Pitfall-8 LOCKED layout, color table incl. the one sanctioned status-green, copywriting contract, interaction contracts)
    - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs" (the hex/text mode machinery, txtHex/HexDump, IffEditController, Colors usage — the self-extension analog)
    - .planning/phases/23-user-definable-iff-chunk-templates/23-PATTERNS.md (§FormIffEditor.cs — Pitfall 8 locked invariants, undo decision, reuse manifest)
    - auto-memory [[feedback_winforms_dockfill_zorder]] + [[feedback_caller_attrs_binary_compat]] (Pitfall 8 + MEF binary-compat)
  </read_first>
  <action>
    Create UI/Controls/TemplateBuilderPane.cs (a UserControl) in the sibling UtinniPlugins repo.
    Internal layout per UI-SPEC (Pitfall 8 LOCKED): pnlTemplate (Dock.Fill, added to its parent
    Controls FIRST so Fill is front-most) containing pnlTemplateStatus (Dock.Bottom, 22px, 3px pad)
    + splitTemplate (nested SplitContainer, Orientation=Horizontal; set Size BEFORE SplitterDistance;
    wrap any ini splitter-restore in try/catch; ctor MUST NOT throw — MEF silent-reject guard). TOP
    panel = raw-byte monospace hex grid (reuse the txtHex Consolas-9pt + HexDump format), selectable
    by click-drag; selected span highlights in Colors.Secondary() (blue); annotated spans get a 1px
    Colors.ControlBorder() box + tag label + faint tint; UN-annotated bytes render plain (default
    Colors.Font() on PrimaryHighlight()) — visibly raw. BOTTOM panel = the decoded FieldRecord list
    (name · type · decoded value · span; name in chrome font, values in Consolas 9pt). pnlTemplateStatus
    = the live round-trip indicator: green "Round-trip OK · M of M bytes consumed"
    (Color.FromArgb(78,201,76) — the ONE new sanctioned direct), neutral "N of M consumed — R bytes
    still raw" (FontDisabled), red "Template doesn't fit these bytes — decode stopped at 0x{off}"
    (Color.Red). The pane takes the bound leaf bytes, runs the headless KernelCodec.Decode +
    FitReport.FitCheck, and renders — it owns NO format logic. Use the EXACT copy strings from the
    UI-SPEC Copywriting Contract. All colors via Colors.*() except the two sanctioned directs.
    Grep-gate hygiene on comments.
  </action>
  <verify>
    <automated>MSBuild the TJT solution (sibling repo) /p:Configuration=Release /p:Platform=x86 — TemplateBuilderPane compiles; grep confirms `Color.FromArgb(78, 201, 76)` appears exactly once and no raw ARGB literals elsewhere (only Colors.*() / Color.Red / the one green)</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: TJT builds with TemplateBuilderPane; the control instantiates without throwing (MEF-safe ctor).
    - Source assertion: pnlTemplate added front-most; splitTemplate sets Size before SplitterDistance; the one status-green is the only new direct color.
    - Source assertion: the pane calls KernelCodec/FitReport (headless engine) and owns no decode/encode byte logic.
  </acceptance_criteria>
  <done>TemplateBuilderPane renders the raw-byte grid + field list + round-trip indicator per UI-SPEC, calling the headless engine, Pitfall-8 compliant.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the Template mode into FormIffEditor — select→assign, field-list edits, auto-apply</name>
  <files>D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs, D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.Designer.cs, D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/TemplateBuilderPane.cs</files>
  <read_first>
    - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs" (btnHexMode/btnTextMode toggle wiring, OnTreeAfterSelect→ShowLeafReadOnly, RefreshSaveMenuEnabledState, IffEditController)
    - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.Designer.cs" (the Dock.Right toggle cluster, pnlLeafHeader, splitContainer Designer block to mirror)
    - .planning/phases/23-user-definable-iff-chunk-templates/23-UI-SPEC.md (§"Interaction Contracts" 1-4, §"Undo stack decision", §"Reuse Manifest")
  </read_first>
  <action>
    Add btnTemplateMode (50px, Dock.Right, "Template") in pnlLeafHeader, added FIRST among the
    Dock.Right cluster so it renders leftmost of the three; always visible when a leaf is selected.
    Add a templateModeActive flag (or a 3-way mode enum) alongside textModeActive; mode commits on
    Leave/Validating (never per-keystroke). Mount the TemplateBuilderPane inside pnlLeafEditor.
    Wire the select→assign gesture (Interaction 1): right-click a byte selection → UtinniContextMenuStrip
    item "Assign type & name…" → a small "Assign field" modal (Name textbox, Type drop-down, optional
    repeat-spec chooser with the 3 D-10 kinds [Fixed count / Count from field… / Until end of chunk],
    optional enum/flags map with the EXPLICIT radio "Enum (one value)" vs "Flags (combinable, bit
    1..32)" per Pitfall 4, encoding) → on accept append a FieldRecord (in file byte order), box+tag
    the span, add the typed row, re-run decode+FitReport, update the indicator. Field-list edits
    (Interaction 2): add/remove/reorder rows (mirror the tree Move up/Move down/Remove verbs); edit
    value inline (commit on Leave) → drives leaf.SetPayload(KernelCodec.Encode(...)) through the
    EXISTING IffEditController (document/value edits ride it; template-SHAPE edits get a builder-local
    stack — two stacks, clear boundary per UI-SPEC). Show the D-10 length-changing feedback
    ("payload {old}→{new} bytes · count field recomputed"). Auto-apply surfacing (Interaction 3): on
    selecting a no-built-in leaf, call TemplateResolver.Resolve — green FitReport → open Template mode
    silently; key-match-but-no-round-trip → open Template mode with the red does-not-fit flag; genuine
    multi-match → the small picker (most-specific pre-selected). Built-in-claimed file → disable
    btnTemplateMode with the altitude tooltip. Reuse the Save▾ provenance matrix UNCHANGED (a template
    edit is a normal dirty MutableIffDocument). Do NOT add defaulted [Caller*] params to any public
    method the pre-built plugins call (binary-compat).
  </action>
  <verify>
    <automated>MSBuild the TJT solution + the paired UtinniCore build; the MEF-compose fixture (frozen-DLL gate from Phase 17) still composes FormIffEditor — `dotnet test --no-build` MEF-compose lane green; grep confirms btnTemplateMode added before the other Dock.Right toggles</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: TJT builds + MEF-composes FormIffEditor (no silent reject).
    - Source assertion: btnTemplateMode wired; the assign-field modal offers the 3 D-10 repeat kinds + the explicit enum-vs-flags radio (Pitfall 4); value edits commit on Leave via IffEditController.
    - Source assertion: auto-apply calls TemplateResolver.Resolve; built-in-claimed disables the toggle with the altitude tooltip.
  </acceptance_criteria>
  <done>FormIffEditor has a working Template mode: select→assign, field-list edit, live round-trip indicator, auto-apply surfacing per UI-SPEC; MEF-composes; binary-compat preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-authored template (in the builder) → headless engine | the in-UI template is user input; the engine (23-03) already bounds it |
| pre-built plugin DLLs ↔ FormIffEditor public surface | MEF compose must not break on a binary-incompat signature change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07-MEF | Denial of Service | FormIffEditor / TemplateBuilderPane ctor throwing → MEF silent-reject | mitigate | Ctor cannot throw (defaults survive a bad ini); splitter Size-before-SplitterDistance; no new defaulted [Caller*] params on public methods (binary-compat). Phase-17 MEF-compose fixture gates it. |
| T-23-07-ENG | Tampering | format logic leaking into the UI | mitigate | The pane calls the headless engine only; byte-exactness lives in the kernel (23-03), not the UI. |
| T-23-07-OOB | Information Disclosure | over-read on a hostile chunk in the live preview | mitigate | The engine's cursor Truncated guard surfaces as the red does-not-fit flag, never an out-of-bounds read. |
| T-23-SC | Tampering | NuGet installs | accept | Zero new packages in the TJT repo. |
</threat_model>

<verification>
- MSBuild the TJT solution (sibling repo) + paired UtinniCore build, x86 Release.
- MEF-compose fixture (Phase 17 frozen-DLL gate) still composes FormIffEditor.
- No raw ARGB literals beyond Colors.*() / Color.Red / the one sanctioned status-green.
- Cross-repo paired commit (no human checkpoint — the live smoke is 23-08).
</verification>

<success_criteria>
- Template leaf-pane mode: select→assign, field-list editing, continuous byte-exact round-trip indicator, un-annotated-stays-raw candor.
- Auto-apply surfacing (silent green / red does-not-fit / multi-match picker / built-in altitude disable).
- Pitfall-8 compliant; MEF-safe; binary-compat; engine-only format logic (UI owns none).
</success_criteria>

<output>
Create `.planning/phases/23-user-definable-iff-chunk-templates/23-07-SUMMARY.md` when done.
</output>
