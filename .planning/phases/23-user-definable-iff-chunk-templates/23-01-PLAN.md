---
phase: 23-user-definable-iff-chunk-templates
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - UtinniCoreDotNet/Formats/Template/ITemplateContracts.cs
  - Utinni.Cli.Tests/Template/TemplateTestFixtures.cs
  - Utinni.Cli.Tests/Template/KernelCodecTests.cs
  - Utinni.Cli.Tests/Template/RoundtripTemplateCommandTests.cs
  - Utinni.Cli.Tests/Template/ApplySaveTemplateTests.cs
  - Utinni.Cli.Tests/Utinni.Cli.Tests.csproj
autonomous: true
requirements: [PROD-IFFT-01, PROD-IFFT-02, PROD-IFFT-03]

must_haves:
  truths:
    - "A test project compiles against a frozen template-engine interface surface (model + kernel codec signatures) before any engine logic exists"
    - "The count-from-prior array grow/shrink byte-exact round-trip golden exists and is RED (skipped or failing pending engine)"
    - "Synthesize-through-IffWriter fixtures build template-eligible IFF chunks at low + high version FORMs"
  artifacts:
    - path: "UtinniCoreDotNet/Formats/Template/ITemplateContracts.cs"
      provides: "Frozen public contracts (TemplateModel/FieldRecord/RepeatSpec/IKernelCodec/FitReport shapes) the rest of the phase implements against"
      min_lines: 40
    - path: "Utinni.Cli.Tests/Template/TemplateTestFixtures.cs"
      provides: "synthesize-through-IffWriter fixture builders for template-eligible chunks"
      min_lines: 40
    - path: "Utinni.Cli.Tests/Template/KernelCodecTests.cs"
      provides: "per-type + count-recompute RED goldens"
      min_lines: 30
  key_links:
    - from: "Utinni.Cli.Tests/Template/TemplateTestFixtures.cs"
      to: "UtinniCoreDotNet.Formats.Iff.IffWriter"
      via: "MutableIffNode.NewContainer/AddContainer/AddLeaf + IffWriter.Write"
      pattern: "IffWriter\\.Write"
    - from: "Utinni.Cli.Tests/Template/KernelCodecTests.cs"
      to: "UtinniCoreDotNet.Formats.Template"
      via: "references frozen contracts from ITemplateContracts.cs"
      pattern: "Formats\\.Template"
---

<objective>
Wave-0 test scaffolding (VALIDATION.md mandate). Freeze the template-engine public interface
surface as a contracts file, then write the synthesize-through-`IffWriter` fixtures and the RED
test files that every later plan implements against — including the CRITICAL count-from-prior
array grow/shrink byte-exact round-trip golden (DEC-C3, the phase's make-or-break encode-parity
test).

Purpose: tests-exist-before-the-code-they-cover (Nyquist). Defining `ITemplateContracts.cs` first
means Waves 1-2 receive the exact type names and method signatures instead of reverse-engineering
them — interface-first ordering, no scavenger hunt.
Output: a frozen contract file (compiles, no logic) + 4 test files that compile and run RED
(skipped via `Skip=` with a tracking note, NOT silently absent) pending the engine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-user-definable-iff-chunk-templates/23-RESEARCH.md
@.planning/phases/23-user-definable-iff-chunk-templates/23-VALIDATION.md
@.planning/phases/23-user-definable-iff-chunk-templates/23-PATTERNS.md

<interfaces>
<!-- The frozen surface this plan creates. Later plans implement against these EXACT names. -->
<!-- D-08 kernel types: sized ints (i8/u8/i16/u16/i32/u32), f32/f64, cstring, char[n], raw[n], pad[n], struct, array. -->
<!-- D-10 array kinds: FixedCount(int) / CountFromField(name) / UntilEnd. -->
<!-- D-13: TemplateModel carries a `version` int field. -->
<!-- D-04 match key: ancestor-FORM-path + leaf tag (string predicate over MutableIffDocument.DeriveStableId). -->
<!-- FitReport (D-17.2): { bool ConsumedExactly; PerFieldPlausibility[] } -->

From UtinniCoreDotNet/Formats/Iff/MutableIffNode.cs (existing, reuse):
  static MutableIffNode NewContainer(string type, string subType)
  MutableIffNode AddContainer(string type, string subType)
  MutableIffNode AddLeaf(string tag, byte[] payload)
  byte[] GetPayloadCopy()
  void SetPayload(byte[] payload)

From UtinniCoreDotNet/Formats/Iff/IffWriter.cs (existing, reuse):
  static byte[] Write(MutableIffDocument doc)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze the template-engine contract surface</name>
  <files>UtinniCoreDotNet/Formats/Template/ITemplateContracts.cs, Utinni.Cli.Tests/Template/TemplateTestFixtures.cs, Utinni.Cli.Tests/Template/KernelCodecTests.cs, Utinni.Cli.Tests/Template/RoundtripTemplateCommandTests.cs, Utinni.Cli.Tests/Template/ApplySaveTemplateTests.cs, Utinni.Cli.Tests/Utinni.Cli.Tests.csproj</files>
  <read_first>
    - .planning/phases/23-user-definable-iff-chunk-templates/23-RESEARCH.md (§"D-08 Kernel ↔ SOE .tdf Vocabulary Map", §"D-09 SWG Composite Byte Layouts (PINNED)", §"Recommended Project Structure")
    - .planning/phases/23-user-definable-iff-chunk-templates/23-PATTERNS.md (§"No Analog Found" — TemplateModel/TypePlausibility net-new shapes)
    - UtinniCoreDotNet/Formats/ClientEffect/ClefFieldCodec.cs (MIT header + provenance-comment idiom, lines 1-29)
    - UtinniCoreDotNet/Formats/Decoders/IffPayloadCursor.cs (Need()/Remaining/Length surface to mirror)
  </read_first>
  <action>
    Create UtinniCoreDotNet/Formats/Template/ITemplateContracts.cs in the headless net4.7.2
    UtinniCoreDotNet project (NOT TJT — D-17.1 load-bearing; Generated/UtinniCore.cs UNTOUCHED).
    Open with the MIT header + provenance comment cloned from ClefFieldCodec.cs lines 1-29 (cite
    sharedFile/.../Iff.cpp composite write order; assert "no code copied; original to Utinni under MIT").
    Declare the public shapes the phase implements against — definitions only, no logic bodies:
    (1) enum KernelType { I8, U8, I16, U16, I32, U32, F32, F64, CString, FixedChar, RawBytes, Pad, Struct, Array };
    (2) enum RepeatKind { FixedCount, CountFromField, UntilEnd };
    (3) class RepeatSpec { RepeatKind Kind; int FixedCount; string CountFieldName; };
    (4) class NamedValueMap { bool IsFlags; Dictionary<string,long> Entries; }  // D-11 enum vs flags (bit-position 1..32)
    (5) class FieldRecord { string Name; KernelType Type; int ByteWidth; string Encoding; RepeatSpec Repeat; NamedValueMap ValueMap; List<FieldRecord> StructFields; };
    (6) class TemplateModel { int Version; string MatchAncestorPath; string MatchLeafTag; bool MatchTagOnly; List<FieldRecord> Fields; };
    (7) class FitReport { bool ConsumedExactly; int BytesConsumed; int BytesTotal; List<FieldPlausibility> PerField; };
    class FieldPlausibility { string FieldName; bool Plausible; };
    (8) static class declarations / signatures for the codec entry points the tests call:
        KernelCodec.Decode(TemplateModel, byte[] payload) -> decoded field value map; and
        KernelCodec.Encode(TemplateModel, <decoded values>) -> byte[]. Use a simple value carrier
        type (e.g. DecodedTemplate with field name -> value/elements) so the tests have a concrete
        type to assert against. Keep methods as `throw new NotImplementedException()` stubs OR
        leave the engine class to Wave 1 and declare ONLY the model/enums/POCOs here — pick the
        split that lets the test files COMPILE. Mark every field name in comments to its D-decision.
    Grep-gate hygiene (Pitfall 5): do NOT put gated SWG type tokens in comments where a later plan
    might assert "grep returns zero" — keep historical names in docs only.
  </action>
  <verify>
    <automated>MSBuild Utinni.sln /p:Configuration=Release /p:Platform=x86 /t:UtinniCoreDotNet (file compiles); grep confirms `namespace UtinniCoreDotNet.Formats.Template` and `class TemplateModel`</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: ITemplateContracts.cs declares TemplateModel, FieldRecord, RepeatSpec (3 kinds), NamedValueMap, FitReport, KernelType enum with all D-08 kernel members.
    - Behavior assertion: file compiles in UtinniCoreDotNet (headless, no WinForms using directives).
    - Source assertion: MIT header + provenance comment present (matches ClefFieldCodec.cs idiom).
  </acceptance_criteria>
  <done>Frozen contract file compiles; every D-08 kernel type, D-10 array kind, D-11 value-map, and FitReport shape is a named public type.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Synthesize-through-IffWriter fixtures + RED test files</name>
  <files>UtinniCoreDotNet/Formats/Template/ITemplateContracts.cs, Utinni.Cli.Tests/Template/TemplateTestFixtures.cs, Utinni.Cli.Tests/Template/KernelCodecTests.cs, Utinni.Cli.Tests/Template/RoundtripTemplateCommandTests.cs, Utinni.Cli.Tests/Template/ApplySaveTemplateTests.cs, Utinni.Cli.Tests/Utinni.Cli.Tests.csproj</files>
  <read_first>
    - Utinni.Cli.Tests/ClientEffect/ClefTestFixtures.cs (THE synthesize-through-the-writer idiom to clone — MutableIffNode.NewContainer + AddContainer + AddLeaf + IffWriter.Write)
    - .planning/phases/23-user-definable-iff-chunk-templates/23-VALIDATION.md (§"Wave 0 Requirements", §"Dual-lineage fixture matrix", §"Per-Task Verification Map" — exact --filter strings)
    - .planning/phases/23-user-definable-iff-chunk-templates/23-PATTERNS.md (§"TemplateTestFixtures.cs" — CRITICAL count-from-prior golden)
    - Utinni.Cli/Commands/ApplySaveIffCommand.cs (TestPerturbSerialized seam at line ~74 to drive the fail-closed test)
    - UtinniCoreDotNet/Formats/Template/ITemplateContracts.cs (the surface created in Task 1)
  </read_first>
  <behavior>
    - Test (KernelCodecTests): each kernel type (i8/u8/i16/u16/i32/u32/f32/f64/cstring/char[n]/raw/pad/struct) decodes the synthesized bytes to the expected value, then re-encodes byte-identical. Filter: "Template&KernelCodec".
    - Test (KernelCodecTests, CRITICAL): a count-from-prior chunk (count field then N records) — decode, ADD one element, re-encode; assert the count field's on-wire bytes now read N+1 AND the whole payload round-trips byte-exact; repeat REMOVE one element. Filter: "Template&CountRecompute&Roundtrip".
    - Test (KernelCodecTests): trailing-remainder + fixed-count arrays round-trip byte-exact. Filter: "Template&Array&Roundtrip".
    - Test (KernelCodecTests): D-09 presets decode to exact values (vector x,y,z; quaternion w,x,y,z w-FIRST; matrix 3x4 row-major 12 floats; color PackedRgb 3x u8 r,g,b). Filter: "Template&Preset".
    - Test (KernelCodecTests): version-FORM divergence — a CLEF-CPAP-style chunk built at low vs high version FORM picks the right layout. Filter: "Template&VersionMatch".
    - Test (RoundtripTemplateCommandTests): verb-level decode->encode of a fixture is byte-identical. Filter: "Template&Roundtrip".
    - Test (ApplySaveTemplateTests): with TestPerturbSerialized corrupting an untouched leaf, apply-save-template exits 2 and writes NOTHING. Filter: "ApplySaveTemplate&FailClosed".
    - Test (KernelCodecTests/Resolver): D-05 altitude — a template never engages on a file whose root-FORM has a built-in. Filter: "Template&Precedence".
  </behavior>
  <action>
    Create the four test files under Utinni.Cli.Tests/Template/ (add a Template/ folder to the
    csproj if it does not auto-glob): TemplateTestFixtures.cs, KernelCodecTests.cs,
    RoundtripTemplateCommandTests.cs, ApplySaveTemplateTests.cs. Clone ClefTestFixtures.cs's
    builder shape verbatim, substituting kernel-encoded payloads. Build fixture chunks under a
    root-FORM sub-type Utinni does NOT decode (so the leaf is template-eligible per RESEARCH A2);
    add the dual-lineage version-FORM axis (low + high version FORM where the layout diverges,
    mirroring CpapPayload(version)). Each fixture must round-trip through IffWriter.Write so it is
    canonical-by-construction. Write each test referencing the Task-1 frozen contracts. EVERY test
    that targets unbuilt engine logic uses `[Fact(Skip = "RED — Wave 1/2 implements KernelCodec; tracking PROD-IFFT-0X")]`
    (or `[Theory(Skip=...)]`) so the file compiles and the suite is GREEN-with-skips, never red-broken.
    Use the EXACT trait/name tokens from VALIDATION.md so the `--filter` strings resolve
    (KernelCodec, CountRecompute, Roundtrip, Array, Preset, VersionMatch, Precedence, WorkedExample,
    FailClosed). Do NOT enable Newtonsoft TypeNameHandling anywhere.
  </action>
  <verify>
    <automated>MSBuild Utinni.sln /p:Configuration=Release /p:Platform=x86 then `dotnet test Utinni.Cli.Tests --no-build --filter "FullyQualifiedName~Template"` — all Template tests are discovered; the CountRecompute test name resolves under `--filter "Template&CountRecompute&Roundtrip"`</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `dotnet test Utinni.Cli.Tests --no-build --filter "Template&CountRecompute&Roundtrip"` discovers ≥1 test (currently Skipped, not absent).
    - Source assertion: TemplateTestFixtures.cs calls IffWriter.Write and builds at ≥2 version FORMs.
    - Behavior assertion: full `dotnet test Utinni.Cli.Tests --no-build` is GREEN (skips count as pass; nothing red).
  </acceptance_criteria>
  <done>Four Template test files compile and are discovered; every VALIDATION.md row has a named (skipped) test resolvable by its `--filter`; fixtures are synthesize-through-IffWriter canonical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-author → IffWriter | fixtures are author-controlled but built through the trusted writer — no attacker surface in tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Information Disclosure | test fixtures embedding real asset bytes | accept | Fixtures are synthesized-through-the-writer (≤200 bytes), no real proprietary payloads committed |
| T-23-SC | Tampering | NuGet installs | mitigate | This plan installs ZERO packages (RESEARCH Package Legitimacy Audit: N/A). Acceptance forbids adding any PackageReference; xUnit already present in Utinni.Cli.Tests. |
</threat_model>

<verification>
- MSBuild `Utinni.sln /p:Configuration=Release /p:Platform=x86` succeeds (never `dotnet build` — MSB3823 on WinForms .resx).
- `dotnet test Utinni.Cli.Tests --no-build --filter "FullyQualifiedName~Template"` discovers the full RED suite; nothing red.
- No new PackageReference added to any csproj.
</verification>

<success_criteria>
- ITemplateContracts.cs frozen and compiling in headless UtinniCoreDotNet.
- 4 Template test files compile, discovered, RED-via-Skip with tracking notes.
- CRITICAL count-from-prior round-trip golden exists and is resolvable by `--filter "Template&CountRecompute&Roundtrip"`.
- Fixtures are synthesize-through-IffWriter canonical across ≥2 version FORMs.
</success_criteria>

<output>
Create `.planning/phases/23-user-definable-iff-chunk-templates/23-01-SUMMARY.md` when done.
</output>
