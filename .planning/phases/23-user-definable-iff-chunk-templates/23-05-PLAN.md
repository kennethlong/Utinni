---
phase: 23-user-definable-iff-chunk-templates
plan: 05
type: execute
wave: 4
depends_on: ["23-03", "23-04"]
files_modified:
  - Utinni.Cli/Commands/DecodeWithTemplateCommand.cs
  - Utinni.Cli/Commands/RoundtripTemplateCommand.cs
  - Utinni.Cli/Commands/ApplySaveTemplateCommand.cs
  - Utinni.Cli/Commands/ListTemplatesCommand.cs
  - Utinni.Cli/Program.cs
  - UtinniCoreDotNet/Formats/Template/Examples/counted_records.json
  - UtinniCoreDotNet/Formats/Template/Examples/flat_composite.json
autonomous: true
requirements: [PROD-IFFT-02, PROD-IFFT-03]

must_haves:
  truths:
    - "decode-with-template emits a navigable decode envelope for a matched leaf via the headless engine"
    - "roundtrip-template proves byte-exact decode->encode of a fixture (the DEC-C3 golden gate)"
    - "apply-save-template writes a template-edited leaf --root-contained, atomic, fail-closed on a failed untouched-leaf verify"
    - "list-templates enumerates the scanned packs; two worked-example templates ship and round-trip byte-exact"
    - "D-15: verbs-first for the engine — decode/roundtrip/apply-save/list land as utinni-cli verbs gated by a byte-exact golden round-trip (DEC-C3)"
  artifacts:
    - path: "Utinni.Cli/Commands/ApplySaveTemplateCommand.cs"
      provides: "--root-contained atomic template-edit save cloning ApplySaveIffCommand"
      min_lines: 80
      exports: ["Run"]
    - path: "Utinni.Cli/Program.cs"
      provides: "4 template verbs registered in ParseArguments + Dispatch"
      contains: "TemplateOptions"
    - path: "UtinniCoreDotNet/Formats/Template/Examples/counted_records.json"
      provides: "worked-example count-from-prior chunk template (golden + teaching artifact, D-14)"
      contains: "CountFromField"
  key_links:
    - from: "Utinni.Cli/Commands/ApplySaveTemplateCommand.cs"
      to: "UtinniCoreDotNet.Saving.LooseOverridePath"
      via: "--root containment + SaveCommandIo.WriteAtomic"
      pattern: "LooseOverridePath\\.Resolve"
    - from: "Utinni.Cli/Program.cs"
      to: "Utinni.Cli.Commands template verbs"
      via: "ParseArguments Type[] + Dispatch switch case"
      pattern: "TemplateCommand\\.Run"
---

<objective>
The verbs-first engine surface (D-15, DEC-V2-VERBS-FIRST): expose template decode / roundtrip /
apply-save / list as `utinni-cli` verbs over the headless engine, register them in Program.cs, and
ship the two D-14 worked-example templates that double as goldens. The byte-exact round-trip gate
(DEC-C3) lands here as the `roundtrip-template` verb + the worked-example goldens.

Purpose: every capability is a golden-tested verb FIRST; the UI (23-07) and MCP (23-06) consume
these verbs, never re-implement format logic.
Output: 4 verb files + Program.cs registration + 2 example JSON templates, turning the 23-01 RED
Roundtrip / ApplySaveTemplate&FailClosed / WorkedExample goldens GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-user-definable-iff-chunk-templates/23-RESEARCH.md
@.planning/phases/23-user-definable-iff-chunk-templates/23-PATTERNS.md
@Utinni.Cli/Commands/DecodeTrnCommand.cs
@Utinni.Cli/Program.cs

<interfaces>
<!-- Engine (23-02..04): TemplateJson, KernelCodec.Decode/Encode, TemplateResolver.Resolve, TemplatePackStore.LoadAll, FitReport. -->
<!-- ApplySaveIffCommand (clone): --root (Required), --rel-asset, --mutate-leaf <stableId>; LooseOverridePath.Resolve; -->
<!--   MutableIffDocument.FromDocument; FindMutableLeafByStableId; SetPayload; IffWriter.Write; CompareUntouchedLeaves; -->
<!--   SaveCommandIo.WriteAtomic; internal static Func<byte[],byte[]> TestPerturbSerialized seam. -->
<!-- Exit codes: 0 ok / 2 parse|decode|containment|verify-fail / 3 not-found. Generic Exception NOT caught. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: decode-with-template + roundtrip-template + list-templates verbs</name>
  <files>Utinni.Cli/Commands/DecodeWithTemplateCommand.cs, Utinni.Cli/Commands/RoundtripTemplateCommand.cs, Utinni.Cli/Commands/ApplySaveTemplateCommand.cs, Utinni.Cli/Commands/ListTemplatesCommand.cs, Utinni.Cli/Program.cs, UtinniCoreDotNet/Formats/Template/Examples/counted_records.json, UtinniCoreDotNet/Formats/Template/Examples/flat_composite.json</files>
  <read_first>
    - Utinni.Cli/Commands/DecodeTrnCommand.cs (THE alias-delegation precedent — copy the [Verb]/[Value(0)]/File.Exists→exit3/typed-catch ladder/"Generic Exception NOT caught" tail verbatim)
    - Utinni.Cli/Commands/DecodeIffCommand.cs (the result-builder pattern + altitude dispatch to delegate to)
    - Utinni.Cli/Commands/RoundtripIffCommand.cs (the byte-exact verify verb shape)
    - .planning/phases/23-user-definable-iff-chunk-templates/23-RESEARCH.md (§"Verb + MCP Surface (D-15 / D-16) grounding")
    - UtinniCoreDotNet/Formats/Template/TemplateResolver.cs + TemplatePackStore.cs
  </read_first>
  <behavior>
    - Test: roundtrip-template on a synthesized fixture decodes then re-encodes byte-identical → exit 0; a corrupted fixture → exit 2. Filter: "Template&Roundtrip".
    - Test: decode-with-template on a template-eligible leaf emits the decode envelope; a missing file → exit 3; an unmatched chunk → a clear no-match envelope (not a crash). Filter: "Template&Decode".
    - Test: list-templates enumerates the scanned packs and reports each template's match key. Filter: "Template&List".
  </behavior>
  <action>
    Create three verb files cloning DecodeTrnCommand's whole-file shape (swap the codec call): the
    [Verb] attr, [Value(0)] path, File.Exists→exit-3 guard, the typed-exception→exit-2 catch ladder
    (TemplateException / DecoderException / IffParseException / IOException), and the load-bearing
    `// NOTE: Generic Exception intentionally NOT caught.` tail. Verb names (Claude's discretion per
    D-15, following the alias-delegation precedent): `decode-with-template` (delegate to the SAME
    result builder a `decode-iff --template` branch would use so they cannot drift — load the pack,
    resolve, decode, emit envelope incl. the FitReport), `roundtrip-template` (decode→encode→assert
    byte-identical, the DEC-C3 gate), `list-templates` (TemplatePackStore.LoadAll → emit each
    template's name + match key + source pack). Use a `--template <path>` and/or `--templates-dir`
    option where the verb needs an explicit template rather than pack auto-resolution (discretion).
    Emit envelopes via the existing JsonOutput.EmitSuccess/EmitError. Grep-gate hygiene on comments.
  </action>
  <verify>
    <automated>MSBuild then `dotnet test Utinni.Cli.Tests --no-build --filter "Template&Roundtrip"` and `--filter "Template&Decode"` and `--filter "Template&List"` pass</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: roundtrip-template byte-exact (exit 0) and fail (exit 2) paths pass.
    - Source assertion: each verb has the typed-catch ladder + the "Generic Exception NOT caught" tail.
    - Source assertion: decode-with-template delegates to the shared decode-iff template-result builder (no forked logic).
  </acceptance_criteria>
  <done>Three template verbs compile and pass; roundtrip-template is the byte-exact gate; envelopes carry the FitReport.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: apply-save-template verb + Program.cs registration + worked-example templates</name>
  <files>Utinni.Cli/Commands/DecodeWithTemplateCommand.cs, Utinni.Cli/Commands/RoundtripTemplateCommand.cs, Utinni.Cli/Commands/ApplySaveTemplateCommand.cs, Utinni.Cli/Commands/ListTemplatesCommand.cs, Utinni.Cli/Program.cs, UtinniCoreDotNet/Formats/Template/Examples/counted_records.json, UtinniCoreDotNet/Formats/Template/Examples/flat_composite.json</files>
  <read_first>
    - Utinni.Cli/Commands/ApplySaveIffCommand.cs (THE clone target — --root containment lines ~100-111, the apply-save backbone ~113-225, TestPerturbSerialized seam ~74, CompareUntouchedLeaves ~236-275)
    - Utinni.Cli/Program.cs (ParseArguments Type[] list + Dispatch switch — add 4 cases)
    - .planning/phases/23-user-definable-iff-chunk-templates/23-RESEARCH.md (§Open Questions Q2 — ship one count-from-prior example + one flat-composite example)
    - UtinniCoreDotNet/Formats/Template/KernelCodec.cs (the Encode call that produces newPayload)
  </read_first>
  <behavior>
    - Test (CRITICAL): apply-save-template applies a template field edit, --root-contained, atomic; with TestPerturbSerialized corrupting an untouched leaf it exits 2 and writes NOTHING. Filter: "ApplySaveTemplate&FailClosed".
    - Test: a --root escape attempt exits 2 (PathContainment), no write. Filter: "ApplySaveTemplate&Containment".
    - Test: the two worked-example templates round-trip byte-exact against their synthesized fixtures. Filter: "Template&WorkedExample&Roundtrip".
  </behavior>
  <action>
    Create ApplySaveTemplateCommand.cs cloning ApplySaveIffCommand's control flow EXACTLY — the ONLY
    difference is HOW newPayload is produced: `byte[] newPayload = KernelCodec.Encode(template,
    editedFieldValues);` replacing the --mutate-hex literal parse. Reuse: [Option("root",
    Required=true)] containment via LooseOverridePath.Resolve (exit 2 on PathContainment), File.Exists
    → exit 3, MutableIffDocument.FromDocument, FindMutableLeafByStableId, SetPayload, IffWriter.Write
    (leaf len + parent FORM ripple FREE), re-parse-for-validity, CompareUntouchedLeaves (fail-closed
    exit 2, no write), SaveCommandIo.WriteAtomic on clean verify, AND the internal static
    TestPerturbSerialized seam (reuse the exact seam so the fail-closed test can corrupt the bytes).
    Register all FOUR verbs in Program.cs: add each *Options typeof(...) to ParseArguments and a
    `case Commands.XxxOptions o: return Commands.XxxCommand.Run(o);` to Dispatch (mechanical — the
    Type[] overload has no arity cap; 29 verbs already registered). Author the two D-14 example
    templates under Formats/Template/Examples/: counted_records.json (a count-then-N-records chunk —
    exercises the D-10 count recompute) and flat_composite.json (vector + quaternion + color preset
    fields — exercises the D-09 presets). Both ship as Content/CopyToOutputDirectory and double as
    the WorkedExample goldens.
  </action>
  <verify>
    <automated>MSBuild then `dotnet test Utinni.Cli.Tests --no-build --filter "ApplySaveTemplate"` and `--filter "Template&WorkedExample&Roundtrip"` pass; run `utinni-cli list-templates` to confirm the 4 verbs registered and the examples enumerate</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion (CRITICAL): apply-save-template fail-closed test passes (exit 2, no write on a failed untouched-leaf verify).
    - Behavior assertion: --root containment escape exits 2; worked-example goldens round-trip byte-exact.
    - Source assertion: Program.cs ParseArguments + Dispatch each list all 4 template verbs; newPayload comes from KernelCodec.Encode (not a hex literal).
  </acceptance_criteria>
  <done>apply-save-template fail-closed + atomic + contained; 4 verbs registered; 2 worked-example goldens ship and round-trip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args (--root, --rel-asset, --template) → filesystem | untrusted path input crosses to disk writes |
| user-authored template + chunk payload → KernelCodec via the verb | attacker-controllable input drives the codec |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05-PATH | Tampering | apply-save-template write path | mitigate | --root containment via LooseOverridePath.Resolve (exit 2 on escape) + SaveCommandIo.WriteAtomic + fail-closed untouched-leaf verify (clone ApplySaveIffCommand verbatim). |
| T-23-05-VERIFY | Tampering | a template edit silently corrupts an untouched leaf | mitigate | CompareUntouchedLeaves re-parse + byte-identity verify → exit 2, NO write, on any mismatch (TestPerturbSerialized seam proves it). |
| T-23-05-OOM | Denial of Service | hostile template/payload via the verb | mitigate | Engine bounds (23-03) — never allocate on attacker counts; cursor Truncated surfaces as decode error exit 2. |
| T-23-SC | Tampering | NuGet installs | accept | Zero new packages; CommandLineParser/Newtonsoft already referenced. |
</threat_model>

<verification>
- MSBuild solution build green (x86 Release).
- `dotnet test Utinni.Cli.Tests --no-build --filter "FullyQualifiedName~Template"` fully GREEN (no remaining skips for engine/verb rows).
- `utinni-cli list-templates` enumerates the shipped + example packs.
- Generated/UtinniCore.cs unchanged.
</verification>

<success_criteria>
- 4 template verbs registered and golden-tested; roundtrip-template + worked-examples are the DEC-C3 gate.
- apply-save-template clones ApplySaveIffCommand's contained/atomic/fail-closed backbone exactly.
- 2 worked-example templates ship (count-from-prior + flat-composite) and round-trip byte-exact.
</success_criteria>

<output>
Create `.planning/phases/23-user-definable-iff-chunk-templates/23-05-SUMMARY.md` when done.
</output>
