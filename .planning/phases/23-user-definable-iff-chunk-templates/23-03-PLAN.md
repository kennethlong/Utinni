---
phase: 23-user-definable-iff-chunk-templates
plan: 03
type: execute
wave: 2
depends_on: ["23-01", "23-02"]
files_modified:
  - UtinniCoreDotNet/Formats/Template/KernelCodec.cs
  - UtinniCoreDotNet/Formats/Template/FitReport.cs
  - UtinniCoreDotNet/Formats/Template/TypePlausibility.cs
  - UtinniCoreDotNet/Formats/Decoders/IffPayloadCursor.cs
autonomous: true
requirements: [PROD-IFFT-01, PROD-IFFT-02]

must_haves:
  truths:
    - "The kernel decodes every kernel type + preset struct from a leaf payload, bounds-checked, never over-reading on attacker counts"
    - "The kernel re-encodes a decoded-then-edited payload byte-exact, AUTO-RECOMPUTING a count-from-prior field on write"
    - "A count-from-prior array grow/shrink round-trips byte-exact (the CRITICAL DEC-C3 golden)"
    - "FitReport reports consumed-exactly + per-field plausibility as a pure function"
  artifacts:
    - path: "UtinniCoreDotNet/Formats/Template/KernelCodec.cs"
      provides: "byte-exact decode + encode (with D-10 count auto-recompute) over the kernel + presets"
      min_lines: 120
    - path: "UtinniCoreDotNet/Formats/Template/FitReport.cs"
      provides: "pure (template, payloadBytes) -> FitReport { consumedExactly, perFieldPlausibility }"
      min_lines: 30
    - path: "UtinniCoreDotNet/Formats/Template/TypePlausibility.cs"
      provides: "looksLikeFloat / looksLikeCStringRun / looksLikeCount standalone predicate library"
      min_lines: 30
  key_links:
    - from: "UtinniCoreDotNet/Formats/Template/KernelCodec.cs"
      to: "UtinniCoreDotNet.Formats.Decoders.IffPayloadCursor"
      via: "decode reads through the bounds-checked cursor (extended with new primitives)"
      pattern: "IffPayloadCursor"
    - from: "UtinniCoreDotNet/Formats/Template/KernelCodec.cs"
      to: "count-from-prior recompute"
      via: "encoder writes elements.Count into the named count field, never the stale value"
      pattern: "Count"
---

<objective>
The byte-exact codec engine — the make-or-break of the phase (PROD-IFFT-02 encode-parity). Extend
`IffPayloadCursor` with the missing kernel read primitives; build `KernelCodec.Decode/Encode`
modelled on `ClefFieldCodec`'s variable-length `MemoryStream` writers; implement the D-10
count-from-prior AUTO-RECOMPUTE (the ONE genuinely new encode obligation); add the pure
`FitReport` function and the standalone `TypePlausibility` predicate library (D-17.2/D-17.3).

Purpose: byte-exactness lives ENTIRELY in the kernel (D-08). The IFF-level length ripple is already
free (`MutableIffNode.SetPayload` + `IffWriter`); the only new parity risk is the in-payload count
recompute — golden-tested here (Pitfall 1).
Output: KernelCodec.cs + FitReport.cs + TypePlausibility.cs + extended IffPayloadCursor.cs, all
headless, turning the 23-01 RED kernel/preset/count-recompute tests GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-user-definable-iff-chunk-templates/23-RESEARCH.md
@.planning/phases/23-user-definable-iff-chunk-templates/23-PATTERNS.md
@UtinniCoreDotNet/Formats/Decoders/IffPayloadCursor.cs
@UtinniCoreDotNet/Formats/ClientEffect/ClefFieldCodec.cs

<interfaces>
<!-- From 23-02: TemplateModel / FieldRecord / RepeatSpec / RepeatKind / NamedValueMap (concrete POCOs). -->
<!-- IffPayloadCursor (existing): ReadInt32Le, ReadUInt32Le, ReadInt8, ReadBytes, ReadFloatLe, ReadCString(Encoding), Need(int), Remaining, Length. -->
<!-- ClefFieldCodec (existing): WriteCString, WriteFloatLe, WriteInt32Le, WriteUInt8 (the host-endian-safe writer idioms to mirror). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend IffPayloadCursor with kernel read primitives</name>
  <files>UtinniCoreDotNet/Formats/Template/KernelCodec.cs, UtinniCoreDotNet/Formats/Template/FitReport.cs, UtinniCoreDotNet/Formats/Template/TypePlausibility.cs, UtinniCoreDotNet/Formats/Decoders/IffPayloadCursor.cs</files>
  <read_first>
    - UtinniCoreDotNet/Formats/Decoders/IffPayloadCursor.cs (the WHOLE file — Need() guard at 155-163, ReadInt32Le at 62-71 to mirror)
    - .planning/phases/23-user-definable-iff-chunk-templates/23-PATTERNS.md (§KernelCodec DECODE side — every new read calls Need(n) first)
    - .planning/phases/23-user-definable-iff-chunk-templates/23-RESEARCH.md (§Security V5 — never allocate on attacker-controlled counts)
  </read_first>
  <behavior>
    - Test: ReadInt16Le / ReadUInt16Le / ReadUInt32Le / ReadDoubleLe / ReadFixedChar(n) / SkipPad(n) each decode the expected value AND throw DecoderException(Truncated) one byte past end. Filter: "Template&Cursor".
  </behavior>
  <action>
    Add the missing primitives to IffPayloadCursor, each calling Need(n) FIRST (verbatim mirror of
    the existing ReadInt32Le/ReadFloatLe idiom, host-endian-safe via the byte-assemble or
    BitConverter+Array.Reverse-on-BE pattern): ReadInt16Le, ReadUInt16Le (return int/long to stay
    overflow-safe), ReadDoubleLe (8 bytes), ReadFixedChar(int n) (fixed char[n], trim at first NUL
    for display but keep the n-byte span on encode — note the asymmetry in a comment), and a
    SkipPad(int n)/ReadRawBytes(int n) for explicit padding/raw. Keep the class internal-or-public
    consistent with its current accessibility (it is `internal sealed` — KernelCodec lives in the
    same assembly so internal is fine; if KernelCodec needs broader reach, widen deliberately and
    note why). Do NOT change existing method signatures (Pitfall: caller binary-compat).
  </action>
  <verify>
    <automated>MSBuild then `dotnet test Utinni.Cli.Tests --no-build --filter "Template&Cursor"` passes</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: new primitives decode correctly and throw Truncated past end-of-payload.
    - Source assertion: every new Read* method calls Need(n) before touching _data.
    - Source assertion: no existing IffPayloadCursor method signature changed.
  </acceptance_criteria>
  <done>IffPayloadCursor exposes i16/u16/u32/f64/char[n]/pad reads, all bounds-checked; cursor tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: KernelCodec decode + encode with D-10 count auto-recompute</name>
  <files>UtinniCoreDotNet/Formats/Template/KernelCodec.cs, UtinniCoreDotNet/Formats/Template/FitReport.cs, UtinniCoreDotNet/Formats/Template/TypePlausibility.cs, UtinniCoreDotNet/Formats/Decoders/IffPayloadCursor.cs</files>
  <read_first>
    - UtinniCoreDotNet/Formats/ClientEffect/ClefFieldCodec.cs (the WHOLE file — MemoryStream build idiom, WriteCString/WriteFloatLe/WriteInt32Le/WriteUInt8, version-mismatch throw discipline)
    - .planning/phases/23-user-definable-iff-chunk-templates/23-RESEARCH.md (§"Pattern 2: D-10 count-from-prior-field auto-recompute", §"Common Pitfalls" Pitfall 1)
    - .planning/phases/23-user-definable-iff-chunk-templates/23-PATTERNS.md (§KernelCodec ENCODE side — the count-recompute branch inside the field loop)
    - UtinniCoreDotNet/Formats/Template/TemplateModel.cs (FieldRecord/RepeatSpec to walk)
  </read_first>
  <behavior>
    - Test: each kernel type decodes from synthesized bytes to expected value and re-encodes byte-identical. Filter: "Template&KernelCodec".
    - Test (CRITICAL): a count-then-N-records chunk — decode, ADD an element, encode → the count field bytes read N+1 and the whole payload is byte-exact; REMOVE an element → count reads N-1, byte-exact. Filter: "Template&CountRecompute&Roundtrip".
    - Test: trailing-remainder + fixed-count arrays round-trip byte-exact. Filter: "Template&Array&Roundtrip".
    - Test: D-09 presets decode to exact values (vector x,y,z; quaternion w,x,y,z; matrix 3×4; color 3×u8). Filter: "Template&Preset".
    - Test: a count-from-prior field claiming more elements than the payload holds throws Truncated (surfaced, not over-read). Filter: "Template&KernelCodec&Truncated".
  </behavior>
  <action>
    Open KernelCodec.cs with the MIT header + provenance comment (clone ClefFieldCodec lines 1-29;
    cite Iff.cpp composite write order). Implement Decode(TemplateModel, byte[]) walking
    template.Fields IN ORDER through an IffPayloadCursor: each FieldRecord reads its kernel type
    (dispatch on KernelType); structs recurse over StructFields; arrays read per RepeatKind —
    FixedCount (constant), CountFromField (read the prior named field's decoded value as the count;
    bound it — never allocate on the attacker count, loop-read with Need so over-claim throws
    Truncated), UntilEnd (loop until cursor.Remaining == 0). Implement Encode(TemplateModel,
    decoded-values) into a single MemoryStream mirroring ClefFieldCodec: write each field; when a
    field IS the count-source for a later array, write elements.Count (the CURRENT count), NEVER the
    stale decoded value (D-10 — the core encode-parity mechanism, RESEARCH Pattern 2). Apply the
    NamedValueMap only at display (enum/flags are int on the wire — D-11; encode writes the raw int).
    Throw a typed TemplateException when a count field references an undefined prior field or a width
    overruns (mirror ClefFieldCodec's VersionFieldMismatch throw discipline; UI-SPEC error copy).
    The kernel produces ONLY leaf payload bytes — NEVER touch IFF tag/length framing or the no-pad
    quirk (that is IffWriter's job, already free). Do NOT model on TrnFieldEncoder (fixed-span,
    rejects length changes — the wrong tool).
  </action>
  <verify>
    <automated>MSBuild then `dotnet test Utinni.Cli.Tests --no-build --filter "Template&CountRecompute&Roundtrip"` and `--filter "Template&KernelCodec"` and `--filter "Template&Array&Roundtrip"` and `--filter "Template&Preset"` all pass (the 23-01 RED goldens go GREEN)</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion (CRITICAL): `--filter "Template&CountRecompute&Roundtrip"` passes — count field auto-recomputed on grow AND shrink, byte-exact.
    - Behavior assertion: all kernel-type + array-kind + preset round-trip goldens pass.
    - Source assertion: the encoder writes elements.Count (not a stored/decoded count value) for a CountFromField array's source field.
    - Behavior assertion: over-claimed count throws Truncated rather than over-reading.
  </acceptance_criteria>
  <done>KernelCodec decode+encode byte-exact across all kernel types, 3 array kinds, and D-09 presets; the count-recompute golden is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FitReport pure function + TypePlausibility predicate library</name>
  <files>UtinniCoreDotNet/Formats/Template/KernelCodec.cs, UtinniCoreDotNet/Formats/Template/FitReport.cs, UtinniCoreDotNet/Formats/Template/TypePlausibility.cs, UtinniCoreDotNet/Formats/Decoders/IffPayloadCursor.cs</files>
  <read_first>
    - .planning/phases/23-user-definable-iff-chunk-templates/23-RESEARCH.md (§D-17.2 fit-check pure fn, §D-17.3 plausibility predicates, §D-09 layouts for float plausibility ranges)
    - .planning/phases/23-user-definable-iff-chunk-templates/23-PATTERNS.md (§FitReport — Remaining/Length consumed-exactly accounting; §"No Analog Found" — TypePlausibility is net-new)
    - UtinniCoreDotNet/Formats/Template/KernelCodec.cs (the decode entry FitReport drives)
  </read_first>
  <behavior>
    - Test: FitReport on a template that consumes the whole payload reports ConsumedExactly=true, BytesConsumed==BytesTotal. Filter: "Template&Fit".
    - Test: FitReport on a template that leaves trailing bytes reports ConsumedExactly=false with the residual count. Filter: "Template&Fit&Partial".
    - Test: looksLikeFloat rejects NaN/denormal-heavy garbage and accepts a plausible f32; looksLikeCStringRun detects an ASCII+NUL run; looksLikeCount accepts a small non-negative int. Filter: "Template&Plausibility".
  </behavior>
  <action>
    Create FitReport.cs as a PURE function FitCheck(TemplateModel, byte[] payload) -> FitReport: run
    the kernel decode catching Truncated, set ConsumedExactly = (cursor.Remaining == 0 after a clean
    decode), BytesConsumed/BytesTotal from the cursor, and PerField plausibility by running each
    decoded field's value through TypePlausibility. No side effects, no I/O (D-17.2 — reused by
    Tier-B's live indicator AND future Tier-C scoring). Create TypePlausibility.cs as a standalone
    static predicate library (D-17.3 — the one genuinely new piece with no analog): looksLikeFloat
    (finite, not absurd magnitude), looksLikeCStringRun (printable ASCII then NUL), looksLikeCount
    (non-negative, below a sane cap). Keep it dependency-light so Tier C reuses it verbatim. Both
    files headless; MIT header + provenance comment.
  </action>
  <verify>
    <automated>MSBuild then `dotnet test Utinni.Cli.Tests --no-build --filter "Template&Fit"` and `--filter "Template&Plausibility"` pass</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: FitReport reports consumed-exactly true on a full-consume template, false + residual on a partial.
    - Source assertion: FitCheck has no file I/O and no mutation of inputs (pure).
    - Behavior assertion: the three plausibility predicates pass their accept/reject tests.
  </acceptance_criteria>
  <done>FitReport is a pure (template,bytes)->report fn; TypePlausibility is a standalone predicate lib; both green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-authored template + chunk payload → KernelCodec.Decode | both the template (counts/widths) and the payload bytes are attacker-controllable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03-OOM | Denial of Service | array decode on a huge fixed/count value | mitigate | Never pre-allocate on the attacker count; loop-read through IffPayloadCursor.Need so an over-claim throws Truncated before any large allocation (RESEARCH V5 idiom). Cap fixed-count arrays at a sane bound. |
| T-23-03-OOB | Information Disclosure | reading past end of payload | mitigate | Every cursor read calls Need(n) first → DecoderException(Truncated); surfaced as a FitReport "does not fit", never an out-of-bounds read. |
| T-23-03-INT | Tampering | negative/overflowing field width | mitigate | Reject negative/overflowing widths in Decode/Encode with a typed TemplateException. |
| T-23-SC | Tampering | NuGet installs | accept | Zero new packages (in-repo only). |
</threat_model>

<verification>
- MSBuild solution build green (x86 Release).
- All 23-01 RED kernel/preset/array/count-recompute goldens are now GREEN.
- `git status` shows Generated/UtinniCore.cs unchanged.
</verification>

<success_criteria>
- KernelCodec byte-exact across all kernel types + 3 array kinds + D-09 presets.
- CRITICAL count-from-prior grow/shrink round-trip golden GREEN (DEC-C3).
- FitReport pure; TypePlausibility standalone (D-17.2/.3 Tier-C substrate).
- All engine code headless (no WinForms) — D-17.1 preserved.
</success_criteria>

<output>
Create `.planning/phases/23-user-definable-iff-chunk-templates/23-03-SUMMARY.md` when done.
</output>
