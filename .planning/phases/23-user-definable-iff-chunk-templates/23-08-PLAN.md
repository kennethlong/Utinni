---
phase: 23-user-definable-iff-chunk-templates
plan: 08
type: execute
wave: 5
depends_on: ["23-05", "23-07"]
files_modified:
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs"
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/TemplateBuilderPane.cs"
autonomous: false
requirements: [PROD-IFFT-03]

must_haves:
  truths:
    - "A modder can save a built template into a writable pack, select among scanned packs, and import/export a template"
    - "The shipped pack is read-only (shown disabled); overwrite + delete route through FormSaveConfirmDialog with candid copy"
    - "A live IFF Editor smoke confirms: create a template on a hex chunk, edit a field, save, byte-exact re-encode round-trips"
  artifacts:
    - path: "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/TemplateBuilderPane.cs"
      provides: "pack save/select + import/export UI over TemplatePackStore"
      min_lines: 40
  key_links:
    - from: "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/TemplateBuilderPane.cs"
      to: "UtinniCoreDotNet.Formats.Template.TemplatePackStore / TemplateJson"
      via: "save serializes via TemplateJson into a writable pack dir; select lists scanned packs"
      pattern: "TemplatePackStore|TemplateJson"
---

<objective>
Template management from the IFF Editor UI (PROD-IFFT-03 manage half): save a built template into a
writable pack, select among scanned packs, import/export a pack/template — then a maintainer
live-smoke proving the end-to-end create→edit→save→byte-exact-re-encode loop in a real injected
session (the one manual verification 23-VALIDATION.md flags).

Purpose: closes criterion 3 (create / edit / save / select from the IFF Editor UI) and provides the
candor-gated, fail-safe management surface. The live smoke is the manual UAT the headless test
matrix cannot cover (WinForms SubPanel, no CI UIA harness).
Output: pack management UI in TemplateBuilderPane + a maintainer live-smoke sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-user-definable-iff-chunk-templates/23-UI-SPEC.md
@.planning/phases/23-user-definable-iff-chunk-templates/23-VALIDATION.md
@.planning/phases/23-user-definable-iff-chunk-templates/23-PATTERNS.md

<interfaces>
<!-- Engine (23-04/23-02): TemplatePackStore (resolved roots + LoadAll + writable-pack write), TemplateJson.Serialize. -->
<!-- Host: FormSaveConfirmDialog (heading/body/accept/cancel, body emphasis Color.Red); OpenFileDialog/SaveFileDialog. -->
<!-- UI-SPEC copy strings: "Save template to pack…", "User templates", "Project pack ({name})", "Shipped (read-only)", -->
<!--   "Import template pack…", "Export this template…", overwrite/delete confirm headings/bodies. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pack save / select / import / export UI</name>
  <files>D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs, D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/TemplateBuilderPane.cs</files>
  <read_first>
    - .planning/phases/23-user-definable-iff-chunk-templates/23-UI-SPEC.md (§Copywriting Contract — Save/Import/Export/overwrite/delete rows; §Reuse Manifest — FormSaveConfirmDialog, OpenFileDialog/SaveFileDialog)
    - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/TemplateBuilderPane.cs" (the pane from 23-07 to extend)
    - UtinniCoreDotNet/Formats/Template/TemplatePackStore.cs (resolved roots + writable-pack semantics + load-order)
    - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormSaveConfirmDialog.cs" (the existing confirm dialog to reuse for overwrite/delete)
  </read_first>
  <action>
    Extend TemplateBuilderPane with the pack management surface. A "Save template to pack…" menu
    (UtinniContextMenuStrip) with a sub-item per scanned writable dir (D-12): "User templates",
    "Project pack ({name})"; the shipped pack shows disabled "Shipped (read-only)". Save serializes
    the current built TemplateModel via TemplateJson into the chosen pack dir (atomic write within the
    pack root — reuse the containment idiom). A select affordance lists templates across the scanned
    packs (load-order) and applies one to the current leaf. Import: "Import template pack…"
    (OpenFileDialog → copy into the user/project pack). Export: "Export this template…"
    (SaveFileDialog). Overwrite an existing same-key template and delete a template both route through
    FormSaveConfirmDialog with the EXACT UI-SPEC headings/bodies/accept verbs ("Overwrite", "Delete";
    body emphasis Color.Red). Applying a template + editing values stay NON-destructive (undoable;
    nothing on disk until an explicit Save▾). Use the exact copy strings; Colors.*() only; MEF-safe.
  </action>
  <verify>
    <automated>MSBuild the TJT solution + paired UtinniCore build, x86 Release; MEF-compose lane green; grep confirms the UI-SPEC copy strings ("Save template to pack", "Shipped (read-only)", "Import template pack") are present</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: TJT builds + MEF-composes; the pack menu lists writable packs and disables the shipped pack.
    - Source assertion: overwrite/delete reuse FormSaveConfirmDialog with the UI-SPEC copy; save writes via TemplateJson within the pack root.
    - Source assertion: import/export use OpenFileDialog/SaveFileDialog (no new file widget).
  </acceptance_criteria>
  <done>Pack save/select/import/export ships in the pane; destructive ops are confirm-gated; shipped pack is read-only.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Maintainer live-smoke — create to edit to save to byte-exact in the live IFF Editor</name>
  <action>Maintainer-only live verification (no headless path for the WinForms SubPanel). Build + inject + drive TJT per how-to-verify below; set the result honestly.</action>
  <what-built>
    The full Tier-B template feature in the live IFF Editor: a Template leaf-pane mode (select byte
    range → assign type+name → live decode + continuous byte-exact round-trip indicator;
    un-annotated bytes stay raw), auto-apply surfacing, and pack save/select/import/export. The
    headless engine (kernel codec + count-from-prior auto-recompute + presets + resolver) and the
    verbs/MCP are already golden-green; this confirms the WinForms surface end-to-end (the one manual
    verification 23-VALIDATION.md flags — no CI UIA harness for a WinForms SubPanel).
  </what-built>
  <how-to-verify>
    1. Build the Utinni solution (VS2026 MSBuild, x86 Release) + the TJT/UtinniPlugins solution; inject into a live SWGEmu session per the standing live-smoke path (windows-mcp drive of TJT).
    2. Open The Jawa Toolbox → IFF Editor → open an `.iff` whose root-FORM Utinni does NOT decode (so leaves are template-eligible), or a hex chunk under such a FORM.
    3. Select a leaf chunk → click the "Template" mode toggle. Confirm the raw-byte grid renders and the status strip shows "No template for this chunk…" with all bytes plain/un-boxed.
    4. Click-drag a byte range → right-click → "Assign type & name…" → assign (e.g.) a u32 count then an array Count-from-field. Confirm the span boxes+tags, a typed row appears, and the round-trip indicator updates ("N of M consumed").
    5. Continue until the indicator reads green "Round-trip OK · M of M bytes consumed".
    6. Edit a field value inline (commit on Leave) → confirm the length-changing feedback "payload {old}→{new} bytes · count field recomputed" appears and the indicator stays green.
    7. Save the template to "User templates"; re-open the chunk and confirm it AUTO-APPLIES (green, silent).
    8. Save the document via the Save▾ loose-override matrix; re-open the saved file and confirm the edited field round-trips byte-exact (the chunk decodes to the edited value; no corruption of sibling leaves).
    Report PASS, or describe the first step that diverged.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (which step, observed vs expected).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| imported pack file → user/project pack dir | an imported pack is attacker-controllable; the copy target must stay within the writable pack root |
| template save → disk | writes must stay within the chosen pack root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-08-PATH | Tampering | pack save / import write path | mitigate | Save/import resolve within the writable pack root (reuse the TemplatePackStore containment); shipped pack is read-only (disabled in UI + rejected at the store). |
| T-23-08-DESTRUCT | Tampering | overwrite/delete a template | mitigate | Both route through FormSaveConfirmDialog (explicit accept verb); applying/editing stay non-destructive until an explicit Save▾. |
| T-23-08-LIVE | (process) | live injection smoke | accept | Maintainer-only human checkpoint (no headless path for the WinForms SubPanel); the only manual UAT per VALIDATION.md. |
| T-23-SC | Tampering | NuGet installs | accept | Zero new packages. |
</threat_model>

<verification>
- MSBuild the TJT + paired UtinniCore build, x86 Release; MEF-compose lane green.
- UI-SPEC copy strings present; Colors.*()-only (plus the sanctioned directs).
- Maintainer live-smoke PASS (create→assign→green→edit→save→byte-exact re-encode + auto-apply).
</verification>

<success_criteria>
- Pack save/select/import/export from the IFF Editor; shipped pack read-only; confirm-gated destructive ops.
- Live-smoke confirms the end-to-end create→edit→save→byte-exact loop (PROD-IFFT-03 manual gate).
</success_criteria>

<output>
Create `.planning/phases/23-user-definable-iff-chunk-templates/23-08-SUMMARY.md` when done.
</output>
