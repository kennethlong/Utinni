---
phase: 23-user-definable-iff-chunk-templates
plan: 06
type: execute
wave: 5
depends_on: ["23-05"]
files_modified:
  - Utinni.Mcp/Tools/ReadTools.cs
  - Utinni.Mcp.Tests/Template/TemplateMcpToolTests.cs
autonomous: true
requirements: [PROD-IFFT-03]

must_haves:
  truths:
    - "An MCP read tool decodes a template-eligible chunk by shelling decode-with-template, with ZERO format logic in the MCP process"
    - "The agent-supplied path is resolved under the pinned root (escape → tool error)"
  artifacts:
    - path: "Utinni.Mcp/Tools/ReadTools.cs"
      provides: "one thin summarize_with_template tool shelling utinni-cli"
      contains: "summarize_with_template"
  key_links:
    - from: "Utinni.Mcp/Tools/ReadTools.cs"
      to: "Utinni.Mcp.Server.CliDispatcher"
      via: "RunAsync(\"decode-with-template\", ...) shell-out, verbatim envelope pass-through"
      pattern: "decode-with-template"
---

<objective>
The thin MCP read tool (D-16, DEC-V2-MCP-OOP): one `summarize_with_template` tool that shells
`utinni-cli decode-with-template`, resolves the path under the pinned root, and passes the envelope
through verbatim — ZERO format logic in the net10 MCP process. The named-pipe/subprocess boundary
to the x86 `utinni-cli` IS the architecture boundary.

Purpose: an AI agent can read an unknown chunk via a template through the same engine the CLI + UI
use — schema-driven decode is MCP-friendly (an agent can derive + apply a template).
Output: one new tool method in ReadTools.cs + a shell-out shape test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-user-definable-iff-chunk-templates/23-RESEARCH.md
@.planning/phases/23-user-definable-iff-chunk-templates/23-PATTERNS.md
@Utinni.Mcp/Tools/ReadTools.cs

<interfaces>
<!-- From 23-05: the `decode-with-template` verb (path [Value(0)], optional --template/--templates-dir). -->
<!-- Existing MCP contract (ReadTools.cs): -->
<!--   [McpServerTool(Name=..., ReadOnly=true, Idempotent=true)] -->
<!--   static async Task<CallToolResult> Fn(ResolvedRoot root, CliDispatcher cli, [Description(PathParamDescription)] string relativePath) -->
<!--   string abs = root.Resolve(relativePath);  // throws on escape → SDK tool error -->
<!--   CliInvocationResult r = await cli.RunAsync(<verb>, new[]{abs}).ConfigureAwait(false); -->
<!--   return CliResultMapper.ToCallToolResult(r);  // verbatim envelope pass-through -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: summarize_with_template MCP tool + shell-out test</name>
  <files>Utinni.Mcp/Tools/ReadTools.cs, Utinni.Mcp.Tests/Template/TemplateMcpToolTests.cs</files>
  <read_first>
    - Utinni.Mcp/Tools/ReadTools.cs (SummarizeClientEffect lines 129-144 — copy the 4-line body + DI signature + Description idiom verbatim)
    - .planning/phases/23-user-definable-iff-chunk-templates/23-PATTERNS.md (§ReadTools.cs — the exact thin-tool body to clone)
    - .planning/phases/23-user-definable-iff-chunk-templates/23-RESEARCH.md (§"Verb + MCP Surface" MCP bullet)
    - existing Utinni.Mcp.Tests MCP-tool shell-out tests (the test idiom for asserting the dispatched verb + ZERO local logic)
  </read_first>
  <behavior>
    - Test: summarize_with_template resolves the relative path under the root and dispatches the `decode-with-template` verb (asserted via the CliDispatcher seam); the envelope is returned verbatim. Filter: "Template" (in Utinni.Mcp.Tests).
    - Test: a path escaping the root throws (→ SDK tool error), no verb dispatched. Filter: "Template" + the escape-case name.
  </behavior>
  <action>
    Add ONE method to ReadTools.cs: `summarize_with_template`
    ([McpServerTool(Name="summarize_with_template", ReadOnly=true, Idempotent=true)] +
    [Description(...)] stating ZERO format logic / the MCP-OOP boundary, mirroring the
    SummarizeClientEffect Description). Keep the exact DI signature (ResolvedRoot root, CliDispatcher
    cli, [Description(PathParamDescription)] string relativePath). Body is the verbatim 3 lines:
    `string abs = root.Resolve(relativePath);` then `await cli.RunAsync("decode-with-template",
    new[]{abs})` then `return CliResultMapper.ToCallToolResult(r);`. No template/format logic in this
    process (MCP-OOP, D-16). Add Utinni.Mcp.Tests/Template/TemplateMcpToolTests.cs cloning the
    existing MCP shell-out test idiom: assert the dispatched verb is `decode-with-template` and the
    envelope passes through; assert a root-escape path throws.
  </action>
  <verify>
    <automated>MSBuild then `dotnet test Utinni.Mcp.Tests --no-build --filter "FullyQualifiedName~Template"` passes (net10 lane)</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: the tool dispatches `decode-with-template` and passes the envelope through verbatim.
    - Source assertion: the method body contains no template/format parsing — only root.Resolve + cli.RunAsync + ToCallToolResult.
    - Behavior assertion: a root-escape path raises a tool error without dispatching.
  </acceptance_criteria>
  <done>summarize_with_template ships as a thin shell-out tool; the MCP test confirms ZERO local format logic and the path-escape guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-supplied relativePath → ResolvedRoot.Resolve | the MCP client (an AI agent) is untrusted; the path must stay under the pinned root |
| MCP process ↔ x86 utinni-cli subprocess | the subprocess boundary IS the architecture boundary (MCP-OOP) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06-PATH | Tampering / Elevation | summarize_with_template path | mitigate | root.Resolve throws on escape → SDK maps to a tool error; no absolute paths accepted (PathParamDescription). |
| T-23-06-LOGIC | Tampering | format logic leaking into the MCP process | mitigate | Tool body is the verbatim 3-line shell-out; the test asserts no local parsing (MCP-OOP boundary preserved). |
| T-23-SC | Tampering | NuGet installs | accept | Zero new packages; ModelContextProtocol already referenced. |
</threat_model>

<verification>
- MSBuild solution build green; net10 MCP test lane green.
- `dotnet test Utinni.Mcp.Tests --no-build --filter "FullyQualifiedName~Template"` passes.
</verification>

<success_criteria>
- One thin summarize_with_template tool shelling decode-with-template, ZERO format logic (MCP-OOP).
- Path-escape guard verified; envelope passes through verbatim.
</success_criteria>

<output>
Create `.planning/phases/23-user-definable-iff-chunk-templates/23-06-SUMMARY.md` when done.
</output>
