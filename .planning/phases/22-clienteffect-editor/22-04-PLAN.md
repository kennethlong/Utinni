---
phase: 22-clienteffect-editor
plan: 04
type: execute
wave: 3
depends_on: ["22-01", "22-02"]
files_modified:
  - The Jawa Toolbox/TheJawaToolboxDotNet/UI/SubPanels/EffectsSubPanel.cs
  - The Jawa Toolbox/TheJawaToolboxDotNet/UI/SubPanels/EffectsSubPanel.Designer.cs
  - The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormClientEffectEditor.cs
  - The Jawa Toolbox/TheJawaToolboxDotNet/Saving/ClientEffectSaveTargets.cs
  - The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs
  - The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj
autonomous: false
requirements: [PROD-W2-CFX-01]
user_setup: []

must_haves:
  truths:
    - "A modder sees a docked 'Effects' SubPanel inside TJT that launches a roomy standalone FormClientEffectEditor (the TerrainSubPanel → FormTerrainEditor idiom, D-04)"
    - "The editor opens a ClientEffect .iff read-only from the TRE Browser, AND opens an existing loose-override .iff directly (D-09)"
    - "The editor shows a flat command list + a per-command typed field editor; the 5 known commands display typed at the file's CLEF version; unknown tags/versions degrade to a raw/hex field list — never a hard failure (D-06/D-13)"
    - "Editing a string/scalar/flag and add/remove/reorder commands marks dirty; Save writes a byte-exact loose override under <root>/loose/ (D-10) and the source TRE is never modified"
    - "The 'Preview in client' button is honest-candor-only this build — enabled when a doc is open, but on click with no reachable hook it surfaces the locked degraded copy and performs NO retrigger (D-07, RESEARCH finding #2)"
    - "A throwing SubPanel/Form ctor never cascades the whole IEditorPlugin out of MEF compose (Pitfall 5/8)"
  artifacts:
    - path: "The Jawa Toolbox/TheJawaToolboxDotNet/UI/SubPanels/EffectsSubPanel.cs"
      provides: "thin docked launcher (clone TerrainSubPanel; MEF-safe ctor; TRE hand-off + direct-open entries)"
      contains: "EffectsSubPanel"
    - path: "The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormClientEffectEditor.cs"
      provides: "roomy host: command list + field grid + Save▾ + honest-candor Preview"
      contains: "FormClientEffectEditor"
    - path: "The Jawa Toolbox/TheJawaToolboxDotNet/Saving/ClientEffectSaveTargets.cs"
      provides: "off-UI-thread loose-override save under <root>/loose/ via LooseOverridePath.Resolve (atomic, Flush(true))"
      contains: "looseOverrideSubDir"
  key_links:
    - from: "Plugin.cs SubPanelContainer(\"Controls\", …) array + forms list"
      to: "new EffectsSubPanel(this, hotkeyManager, ini) + FormClientEffectEditor"
      via: "the established docked-SubPanel + GetForms registration (GetSubPanels stays null)"
      pattern: "new EffectsSubPanel"
    - from: "FormClientEffectEditor edit/save"
      to: "ClientEffectDocument/MutableClientEffect (Plan 01) + ClientEffectSaveTargets → LooseOverridePath.Resolve(root,\"loose\")"
      via: "codec compose + fail-closed atomic save"
      pattern: "LooseOverridePath\\.Resolve"
    - from: "FormClientEffectEditor Preview button"
      to: "Game.IsRunning + IsRetriggerHookReachable() honest-candor branch (no retrigger this build)"
      via: "ClientReloadDispatcher PendingNextSceneChange tier (D-07/D-08)"
      pattern: "IsRetriggerHookReachable"
---

<objective>
Ship the TJT editor surface for ClientEffect (DEC-C4): a thin docked `EffectsSubPanel` that launches
a roomy standalone `FormClientEffectEditor`, plus an off-UI-thread `ClientEffectSaveTargets` that
writes byte-exact loose overrides under `<root>/loose/`. This is the in-app half of PROD-W2-CFX-01,
composing the Plan 01 codec.

Reuse-by-composition: clone `TerrainSubPanel` for the launcher (D-04 idiom), clone the
`FormTerrainEditor` shell + the `FormParticleEditor` preview seam for the host, clone `IffSaveTargets`
+ `TerrainSaveTargets`'s `looseOverrideSubDir` convention for the save target. The Preview action is
HONEST-CANDOR-ONLY — Phase 15's spike found NO reachable native hot-retrigger hook
(`IsRetriggerHookReachable()` hardcoded `false`); the button surfaces the locked degraded copy and
performs no retrigger (D-07, RESEARCH Pitfall 3). NEVER over-promise.

This is a cross-repo plan (sibling `D:/Code/UtinniPlugins` — standing write authority; paired commit,
no human checkpoint EXCEPT the maintainer live smoke). The codec is in `UtinniCoreDotNet.dll`; this
plan adds files only to `TheJawaToolboxDotNet`. `Generated/UtinniCore.cs` is NOT touched.

Purpose: PROD-W2-CFX-01's in-app editor + loose-override save. Output: `EffectsSubPanel`,
`FormClientEffectEditor`, `ClientEffectSaveTargets`, `Plugin.cs` registration, csproj wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-clienteffect-editor/22-CONTEXT.md
@.planning/phases/22-clienteffect-editor/22-RESEARCH.md
@.planning/phases/22-clienteffect-editor/22-PATTERNS.md
@.planning/phases/22-clienteffect-editor/22-UI-SPEC.md
@.planning/phases/22-clienteffect-editor/22-01-SUMMARY.md
@.planning/phases/22-clienteffect-editor/22-02-SUMMARY.md

<interfaces>
<!-- Reuse templates, read in full in 22-PATTERNS.md / 22-UI-SPEC.md. Do NOT re-explore. -->

SubPanel launcher (clone near-verbatim):
- D:/Code/UtinniPlugins/.../UI/SubPanels/TerrainSubPanel.cs — ctor (IEditorPlugin, HotkeyManager, UtINI) : base("Terrain"); MEF-safe try/catch (a throwing ctor cascades the whole IEditorPlugin out of compose, Pitfall 5/8) + SurfaceBuildFailure; BuildContent dock order (footer Bottom → banner Top → 2px accent → body, Pitfall 4); singleton hide-not-dispose LaunchHost; OpenFromTre(payload, archivePath, logicalPath) + OpenLooseOverride(loosePath) entries; the editorPlugin?.ClearUndoStack?.Invoke() null-checked undo seam.
- D:/Code/UtinniPlugins/.../UI/SubPanels/TerrainSubPanel.Designer.cs — the hand-authored partial.
- D:/Code/UtinniPlugins/.../Plugin.cs:139-148 — the SubPanelContainer("Controls", new SubPanel[]{...}) array (add `new EffectsSubPanel(this, hotkeyManager, ini)`); GetSubPanels() STAYS null (MEF SPI not widened, CON-M-01/02). Forms register via the `forms` list / GetForms() (cf. FormParticleEditor at :130).
- D:/Code/UtinniPlugins/.../UI/Forms/FormTreBrowser.cs:384-415 — CollapsiblePanel.WrappedSubPanel discovery for the TRE-Browser hand-off (D-09).

Host editor (clone shell + preview seam):
- D:/Code/UtinniPlugins/.../UI/Forms/FormTerrainEditor.cs — UtinniForm + IEditorForm host shell, SingletonFormClosePolicy.ShouldHideInsteadOfDispose, open/save flow, ResolveLooseOverrideSubDir() → "loose".
- D:/Code/UtinniPlugins/.../UI/Forms/FormParticleEditor.cs:700-783 — the Preview seam: IsRetriggerHookReachable() returns hardcoded false; OnPreviewClicked branches on Game.IsRunning (ALWAYS try/catch the P/Invoke) && IsRetriggerHookReachable(); honest degraded path surfaces distinct no-client vs no-hook copy in Colors.FontDisabled(), NO retrigger. Also the undoStack/redoStack + Ctrl+Z/Ctrl+Y editor-local undo, lblDirty ● marker (Colors.Secondary()), MarkRawPreservedRow (Consolas raw-fallback), SetFailure (Color.Red).

Save target (clone + looseOverrideSubDir convention):
- D:/Code/UtinniPlugins/.../Saving/IffSaveTargets.cs — async Task<SaveResult> off-UI-thread save; looseOverrideSubDir default "loose"; WriteAtomic with Flush(true) (MEDIUM-9 stale-bytes reload-race barrier).
- D:/Code/UtinniPlugins/.../Saving/TerrainSaveTargets.cs:283-297 — the two-step compose: overrideBase = Resolve(root, looseSubDir); fullPath = Resolve(overrideBase, relAsset).
- D:/Code/UtinniPlugins/.../Saving/ClientReloadDispatcher.cs — Game.IsRunning-gated, GameCallbacks.AddMainLoopCall game-thread dispatch; .iff/CLEF → ReloadTier.PendingNextSceneChange (no live binding) → honest candor.

Codec API (Plan 01, in UtinniCoreDotNet.dll):
- ClientEffectDocument.FromBytes(byte[]) → MutableClientEffect (.Commands, .Version, .IsRawPreserved, add/remove/reorder + field-edit ops, .Serialize()).
- ClientEffectParseException (caught → red status, NEVER thrown out of a MEF ctor).

UI contract (22-UI-SPEC.md — LOCKED): two-surface (thin 417px SubPanel → roomy Form); Colors dark theme (no ARGB literals; Color.Red only for failure); Segoe UI 8.25pt base, Consolas 9pt for raw bytes, Bold only on banner/title; flat command list (NO tree) + field grid in a SplitContainer (Size BEFORE SplitterDistance or the ctor throws and MEF drops the plugin); the LOCKED copy strings (Preview honest-disabled, raw-fallback hint, version-locked hint, save success/failure, add/remove/reorder labels, DEC-A3 footer) as `private const string` so grep gates verify them verbatim.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ClientEffectSaveTargets + EffectsSubPanel launcher</name>
  <files>The Jawa Toolbox/TheJawaToolboxDotNet/Saving/ClientEffectSaveTargets.cs, The Jawa Toolbox/TheJawaToolboxDotNet/UI/SubPanels/EffectsSubPanel.cs, The Jawa Toolbox/TheJawaToolboxDotNet/UI/SubPanels/EffectsSubPanel.Designer.cs</files>
  <read_first>
    - D:/Code/UtinniPlugins/.../Saving/IffSaveTargets.cs (async save + looseOverrideSubDir default "loose" + WriteAtomic Flush(true))
    - D:/Code/UtinniPlugins/.../Saving/TerrainSaveTargets.cs:283-297 (two-step loose compose)
    - D:/Code/UtinniPlugins/.../UI/SubPanels/TerrainSubPanel.cs (the whole file — ctor, MEF guard, BuildContent dock order, LaunchHost, OpenFromTre/OpenLooseOverride)
    - D:/Code/UtinniPlugins/.../UI/SubPanels/TerrainSubPanel.Designer.cs (hand-authored partial)
    - D:/Code/UtinniPlugins/.../UI/Forms/FormTreBrowser.cs:384-415 (CollapsiblePanel.WrappedSubPanel hand-off)
    - 22-UI-SPEC.md § Two-surface structure + § Layout containers + § Copywriting Contract (the SubPanel-side copy + DEC-A3 footer)
    - 22-01-SUMMARY.md (ClientEffectDocument API)
  </read_first>
  <action>
    Create `The Jawa Toolbox/TheJawaToolboxDotNet/Saving/ClientEffectSaveTargets.cs` cloning
    `IffSaveTargets` (NOT terrain's typed-field save): an off-UI-thread `async Task<SaveResult>` that
    takes the serialized CLEF bytes (`MutableClientEffect.Serialize()`), the `OpenSource`, the resolved
    root, and a `looseOverrideSubDir` (default `"loose"`). Compose the destination via the two-step
    `LooseOverridePath.Resolve(resolvedRoot, looseOverrideSubDir)` then `Resolve(overrideBase,
    relAsset)` (the `TerrainSaveTargets` shape, D-10) and write atomically with `Flush(true)`. The
    source TRE is NEVER modified (loose override only, D-09).

    Create `The Jawa Toolbox/TheJawaToolboxDotNet/UI/SubPanels/EffectsSubPanel.cs` (+ a
    `.Designer.cs` partial) cloning `TerrainSubPanel` near-verbatim:
    - Ctor `(IEditorPlugin editorPlugin, HotkeyManager hotkeyManager, UtINI ini) : base("Effects")` with
      the WHOLE build wrapped in try/catch → `SurfaceBuildFailure(ex)` (Pitfall 5/8 — a throwing ctor
      cascades the entire IEditorPlugin out of MEF compose).
    - `BuildContent` honoring the Pitfall-4 dock order (DEC-A3 candor footer `Dock.Bottom` FIRST →
      Bold "Effects" banner `Dock.Top` + 2px `Colors.Secondary()` accent rule → body), pinned to the
      417px width. Surfaces ONLY: an "Open Effects Editor" launch button, an "Open ClientEffect
      Override…" direct-open button, a `Colors.FontDisabled()` TRE-source hint, and the dimmed DEC-A3
      footer ("Preview is live in the real SWG client — Utinni never renders effects itself."). ZERO
      format/save/reload logic in the panel.
    - A singleton hide-not-dispose `LaunchHost()` returning the `FormClientEffectEditor`.
    - `OpenFromTre(payload, archivePath, logicalPath)` (read-only, the `CollapsiblePanel.WrappedSubPanel`
      hand-off, D-09) and `OpenLooseOverride(loosePath)` (the host owns the file dialog) — both launch
      the singleton and forward, wrapped in try/catch (never tear down the panel). Null-check the
      `editorPlugin?.ClearUndoStack?.Invoke()` undo seam.
    Use the LOCKED copy strings from 22-UI-SPEC.md as `private const string`.
  </action>
  <verify>
    <automated>"D:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe" Utinni.sln /p:Configuration=Release /p:Platform=x86 /v:m</automated>
  </verify>
  <acceptance_criteria>
    - `ClientEffectSaveTargets.cs` exists; its save composes the destination via a two-step `LooseOverridePath.Resolve` with `looseOverrideSubDir` defaulting to `"loose"` (source assertion) and writes with `Flush(true)`.
    - `EffectsSubPanel.cs` + `.Designer.cs` exist; the ctor signature is `(IEditorPlugin, HotkeyManager, UtINI)` and the whole build body is inside a try/catch with a `SurfaceBuildFailure` path (source assertion).
    - `EffectsSubPanel` adds NO format/save logic: grep `ClientEffectDocument` / `IffWriter` in `EffectsSubPanel.cs` returns zero (filtering comments) — the Form owns it.
    - The DEC-A3 footer copy "Utinni never renders effects itself" is present verbatim as a const.
    - `Utinni.sln` MSBuild (Release/x86) is clean; `git status` (both repos) shows `Generated/UtinniCore.cs` unmodified.
  </acceptance_criteria>
  <done>The save target writes byte-exact loose overrides under <root>/loose/; the thin EffectsSubPanel launches the host with a MEF-safe ctor, the TRE hand-off + direct-open entries, and the DEC-A3 candor footer.</done>
</task>

<task type="auto">
  <name>Task 2: FormClientEffectEditor host + Plugin.cs registration + csproj wiring</name>
  <files>The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormClientEffectEditor.cs, The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs, The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj</files>
  <read_first>
    - D:/Code/UtinniPlugins/.../UI/Forms/FormTerrainEditor.cs (host shell, SingletonFormClosePolicy.ShouldHideInsteadOfDispose, open/save flow, ResolveLooseOverrideSubDir)
    - D:/Code/UtinniPlugins/.../UI/Forms/FormParticleEditor.cs:700-783 (the Preview honest-candor seam; undoStack/redoStack; lblDirty ● marker; MarkRawPreservedRow; SetFailure red)
    - D:/Code/UtinniPlugins/.../Plugin.cs:130-148,175-185 (the forms list + GetForms; the SubPanelContainer array; GetSubPanels stays null)
    - D:/Code/UtinniPlugins/.../TheJawaToolboxDotNet.csproj (Compile item conventions for new .cs/.Designer.cs)
    - 22-UI-SPEC.md (full — Region inventory, Per-command typed field sets, Interaction states, Copywriting Contract, Threading/safety)
    - 22-01-SUMMARY.md + 22-02-SUMMARY.md (codec API + the effect-* verbs/save convention)
    - ClientEffectSaveTargets.cs + EffectsSubPanel.cs (from Task 1)
  </read_first>
  <action>
    Create `The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormClientEffectEditor.cs`
    (`: UtinniForm, IEditorForm`, singleton, `SingletonFormClosePolicy.ShouldHideInsteadOfDispose`)
    cloning the `FormTerrainEditor` shell + the `FormParticleEditor` preview seam. Per the 22-UI-SPEC
    Region inventory (control class is planner's discretion per D-06; the invariants are LOCKED):
    - Title band (`Dock.Top`, Bold + 2px `Colors.Secondary()` accent): "ClientEffect — <name>.iff
      (v<NNNN>)" + source (TRE vs override).
    - Toolbar (`Dock.Top`): Open / Save▾ (Save in place / Save as loose override / Save As…; in-place
      DISABLED when opened read-only from TRE, D-09) / Undo / Redo / Add command / Remove command /
      Move up / Move down / Preview in client. List-mutation buttons gate on a selected command.
    - A `SplitContainer` (`Dock.Fill`, front-most): left = flat command list (NO tree — CLEF has no
      nesting; `ListView` Details `# · Tag · Summary` or `ThemedDataGridView`); right = per-command
      field grid (`ThemedDataGridView` Field/Value/Type). SET `Size` BEFORE `SplitterDistance` at
      construction (or the ctor throws → MEF drops the plugin); persist `SplitterDistance` to the INI.
    - Status + candor strip (`Dock.Bottom`): status `UtinniLabel` + dimmed reload-candor +
      dimmed DEC-A3 footer.
    - Field rendering (D-03 version-aware, from the CLEF field table): CPAP shows name+time at v0001,
      +softParticleTerminate at v0002, +min/max scale/rate at v0003; PSND/CLGT/CAMS/FFBK stable.
      String fields (appearance/sound/FF path) are free-text editable (the variable-length D-01 edit).
      Unknown command tag / unknown CLEF version → a generic raw/hex field list in Consolas, dimmed,
      read-only, with the LOCKED hint — NEVER a hard failure (D-06/D-13).
    - Dirty tracking: field edit / add / remove / reorder → the `●` `Colors.Secondary()` marker +
      "Unsaved changes"; Save enabled. Editor-local undo/redo (`undoStack`/`redoStack`, Ctrl+Z/Ctrl+Y),
      INDEPENDENT of the scene `UndoRedoManager` (CON-M-05); null-check the `IEditorPlugin` undo seam.
    - Save: serialize `MutableClientEffect.Serialize()`, call `ClientEffectSaveTargets` on a background
      `Task`, marshal the result back via `BeginInvoke`; on success "Saved → <override-path>"
      (`Colors.Font()`); on failure / path-escape "Save failed: <reason> — your edits are kept in the
      editor." (`Color.Red`).
    - Preview in client: clone the `FormParticleEditor` HONEST-CANDOR seam EXACTLY. The button is
      ENABLED whenever a doc is open. `IsRetriggerHookReachable()` returns hardcoded `false` (single
      seam; no native hook this build — RESEARCH Pitfall 3). On click: read `Game.IsRunning` inside a
      try/catch (P/Invoke can throw outside an injected client); if `!(clientUp &&
      IsRetriggerHookReachable())` surface the LOCKED degraded copy in `Colors.FontDisabled()` — the
      no-hook string ("Live preview isn't wired this build — edits show on the next scene change or
      relog.") and the no-client string ("No live client — start SWG to preview in-scene.") stay
      DISTINCT — and perform NO retrigger. The live-capable "Re-triggered live effect." copy + the 1s
      accent pulse stay in the seam, gated behind the (false) reachability, for the future hook (D-08).
      Wrap the WHOLE constructor in the MEF-safe discipline; decode exceptions
      (`ClientEffectParseException`) are caught → red status, NEVER thrown out of a ctor.
    Keep ALL contracted copy as `private const string`.

    Modify `Plugin.cs`: add `new EffectsSubPanel(this, hotkeyManager, ini)` to the
    `SubPanelContainer("Controls", new SubPanel[]{...})` array (next to `TerrainSubPanel`), and create
    + add the `FormClientEffectEditor` to the `forms` list inside the existing try/catch isolation
    block (cf. the `FormParticleEditor` registration at :130) so `GetForms()` exposes it; `GetSubPanels()`
    STAYS null (MEF SPI not widened, CON-M-01/02).

    Modify `TheJawaToolboxDotNet.csproj`: add the `<Compile>` items for the three new `.cs` files +
    the `EffectsSubPanel.Designer.cs` (following the `TerrainSubPanel` / `FormTerrainEditor` csproj
    entries; net4.7.2, no new package).
  </action>
  <verify>
    <automated>"D:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe" Utinni.sln /p:Configuration=Release /p:Platform=x86 /v:m</automated>
  </verify>
  <acceptance_criteria>
    - `FormClientEffectEditor.cs` exists (`: UtinniForm, IEditorForm`, singleton hide-not-dispose) and compiles into `TheJawaToolboxDotNet.dll`.
    - `IsRetriggerHookReachable()` returns `false` (source assertion — the honest-candor seam, no over-promise); the no-hook and no-client Preview copy are present verbatim as distinct const strings.
    - The SplitContainer sets `Size` before `SplitterDistance` (source assertion — Pitfall 4 / MEF-drop guard).
    - `Plugin.cs` contains `new EffectsSubPanel(this, hotkeyManager, ini)` in the `SubPanelContainer("Controls", …)` array AND adds `FormClientEffectEditor` to the `forms` list; `GetSubPanels()` still returns null.
    - `TheJawaToolboxDotNet.csproj` has Compile items for `EffectsSubPanel.cs`, `EffectsSubPanel.Designer.cs`, `FormClientEffectEditor.cs`, `ClientEffectSaveTargets.cs`.
    - `Utinni.sln` MSBuild (Release/x86) is clean; full `dotnet test --no-build` is green; `git status` (both repos) shows `Generated/UtinniCore.cs` unmodified.
  </acceptance_criteria>
  <done>The roomy FormClientEffectEditor hosts the flat command list + version-aware typed field grid + Save▾ + honest-candor Preview; it is registered via GetForms + the docked EffectsSubPanel; the build is clean and the full test suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Maintainer live-SWG smoke (open/edit/save/preview)</name>
  <what-built>
    The ClientEffect editor end-to-end: the docked EffectsSubPanel → FormClientEffectEditor inside TJT,
    opening/editing/byte-exact-saving a ClientEffect .iff to a `<root>/loose/` override, and the
    honest-candor "Preview in client" action. Live-SWG inject + WinForms eyeball is the only path that
    can verify the in-client save→reload pickup and the Preview candor — there is no headless path
    (AGENTS.md: live-SWG verification is a maintainer-only human checkpoint). All automated gates
    (codec roundtrip, verb goldens, build) have already passed before this checkpoint.
  </what-built>
  <how-to-verify>
    1. Build `Utinni.sln` (Release/x86) and inject into a live SWG session (the maintainer's standard flow).
    2. Open the TJT "Effects" docked panel → click "Open Effects Editor"; confirm the roomy Form opens
       (no MEF drop, no empty body — Pitfall 4/8).
    3. From the TRE Browser, "Open in Effects Editor" on a CLEF .iff → confirm read-only open, the flat
       command list populates, and selecting a command shows version-appropriate typed fields.
    4. Edit an appearance/sound string (the variable-length D-01 edit) + a scalar; confirm the ● dirty
       marker; Save as loose override; confirm the "Saved → <root>/loose/<logical>" status and that the
       file lands UNDER `<root>/loose/` (NOT `<root>/<logical>` — the folded-todo consistency).
    5. Trigger a TJT-driven scene change; confirm the edited effect loads (or the honest
       "next scene change / relog" candor — either is a PASS; landing naked is baseline, not a regression).
    6. Click "Preview in client"; confirm the HONEST-CANDOR messaging (the no-hook degraded copy in
       dimmed text, NO effect fires, no crash on the game-thread dispatch path) — the no-client vs
       no-hook copy is distinct. Over-promising or a claimed-success-but-nothing-fires is a FAIL.
    7. Open a malformed / unknown-version CLEF; confirm the raw/hex degrade (dimmed, Consolas, hint) —
       no hard crash of the panel or Form.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. MEF drop, save lands off <root>/loose, Preview over-promises, raw-fallback crashes).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor edit + Save → loose-override save target | A relAsset derived from an opened file's logical path must not escape `<root>/loose/` (path traversal). |
| untrusted `.iff` (TRE or loose) → FormClientEffectEditor decode | A malformed/unknown CLEF opened in the editor must not crash the panel or cascade out of the MEF ctor. |
| Preview action → game-thread dispatch | A retrigger path must never call a binding from the UI thread or allocate on the hot path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-path | Tampering / Elevation | `ClientEffectSaveTargets` save destination | mitigate | Compose the destination only through the two-step `LooseOverridePath.Resolve` (fail-closed `..`/rooted/prefix-match rejection, the same as IFF/Datatable/STF/OT); a rejected path surfaces as a "Save failed" red status with NO write. Lands under `<root>/loose/` (D-10). |
| T-22-mef-crash | Denial of Service | EffectsSubPanel / FormClientEffectEditor ctor | mitigate | Wrap the whole SubPanel build + the Form ctor in try/catch (Pitfall 5/8) — a throwing ctor would silently drop the entire IEditorPlugin out of MEF compose. Surface a read-only state label / red status instead. |
| T-22-malformed | Denial of Service | editor decode of a malformed/unknown CLEF | mitigate | Decode exceptions (`ClientEffectParseException`) are caught → red status; unknown version/command raw-falls-back to a read-only hex view (D-06/D-13) — one bad file never crashes the panel or Form, never thrown out of a ctor. |
| T-22-preview-overpromise | Repudiation / (UX integrity) | Preview in client action | mitigate | The honest-candor seam: `IsRetriggerHookReachable()` is `false` this build; the button performs NO retrigger and surfaces distinct no-client vs no-hook degraded copy. Never claims success when nothing fires (RESEARCH Pitfall 3). Any future retrigger routes through `ClientReloadDispatcher` → `GameCallbacks.AddMainLoopCall`, `Game.IsRunning`-gated, heap-free (the `0x0051fb0a` guard, D-08). |
| T-22-SC | Tampering | npm/pip/cargo/NuGet installs | accept | No external packages installed (RESEARCH § Package Legitimacy Audit: N/A — pure WinForms composition of in-repo assemblies + shared TJT controls). |
</threat_model>

<verification>
- `Utinni.sln` MSBuild (Release/x86) clean across both repos; full `dotnet test --no-build` green.
- `Generated/UtinniCore.cs` unmodified in both repos.
- Maintainer live-smoke checkpoint approved (open/edit/save→<root>/loose/ pickup + honest-candor Preview + raw-fallback degrade).
</verification>

<success_criteria>
- A docked "Effects" SubPanel launches a roomy FormClientEffectEditor (DEC-C4, D-04).
- Open read-only from TRE Browser + direct open of a loose override (D-09).
- Flat command list + version-aware typed field editor; unknown tags/versions degrade to raw/hex, never a hard failure (D-06/D-13).
- Edit string/scalar/flag + add/remove/reorder → byte-exact loose-override save under `<root>/loose/` (D-10); source TRE never modified.
- Preview in client is honest-candor-only this build (no over-promise, D-07).
- PROD-W2-CFX-01 in-app editor delivered.
</success_criteria>

<output>
Create `.planning/phases/22-clienteffect-editor/22-04-SUMMARY.md` when done.
</output>
