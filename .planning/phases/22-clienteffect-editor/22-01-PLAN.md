---
phase: 22-clienteffect-editor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - UtinniCoreDotNet/Formats/ClientEffect/ClientEffectDocument.cs
  - UtinniCoreDotNet/Formats/ClientEffect/MutableClientEffect.cs
  - UtinniCoreDotNet/Formats/ClientEffect/ClientEffectCommand.cs
  - UtinniCoreDotNet/Formats/ClientEffect/ClefFieldCodec.cs
  - UtinniCoreDotNet/Formats/ClientEffect/ClientEffectParseException.cs
  - UtinniCoreDotNet/Formats/Decoders/ClefDecoder.cs
  - UtinniCoreDotNet.Tests/Fixtures/ClientEffect/clef_v0001_cpap.iff
  - UtinniCoreDotNet.Tests/Fixtures/ClientEffect/clef_v0002_cpap.iff
  - UtinniCoreDotNet.Tests/Fixtures/ClientEffect/clef_v0003_cpap.iff
  - UtinniCoreDotNet.Tests/Fixtures/ClientEffect/clef_v0003_all5.iff
  - UtinniCoreDotNet.Tests/Fixtures/ClientEffect/clef_v0001_all5.iff
  - UtinniCoreDotNet.Tests/Fixtures/ClientEffect/clef_unknown_version.iff
  - UtinniCoreDotNet.Tests/Fixtures/ClientEffect/clef_unknown_command.iff
  - UtinniCoreDotNet.Tests/Fixtures/ClientEffect/clef_empty.iff
  - UtinniCoreDotNet.Tests/ClientEffect/ClefFixtureBuilder.cs
  - UtinniCoreDotNet.Tests/ClientEffect/ClientEffectCodecTests.cs
autonomous: true
requirements: [PROD-W2-CFX-01]

must_haves:
  truths:
    - "A ClientEffect .iff (CLEF FORM, versions 0001/0002/0003) decodes to a typed flat command list (CPAP/PSND/CLGT/CAMS/FFBK)"
    - "An unknown CLEF version or unknown command tag raw-falls-back and re-emits byte-identical — never a hard decode failure"
    - "A no-edit roundtrip of every committed fixture is byte-exact (loaded.SequenceEqual(serialized))"
    - "A length-changing string edit (appearance/sound/FF) re-parses, untouched commands stay byte-identical, and the edited command decodes to the new value"
    - "Add / remove / reorder a command re-rolls the version FORM + CLEF FORM lengths and re-parses"
    - "The source CLEF version is preserved verbatim on save (a v0001 file never becomes v0003)"
  artifacts:
    - path: "UtinniCoreDotNet/Formats/ClientEffect/MutableClientEffect.cs"
      provides: "Typed model over MutableIffDocument with raw-fallback discipline; Serialize() == IffWriter.Write(SourceIff)"
      contains: "FromMutableIff"
    - path: "UtinniCoreDotNet/Formats/ClientEffect/ClefFieldCodec.cs"
      provides: "Variable-length LE-scalar + NUL-C-string encode/decode per the CLEF version field table"
      contains: "CPAP"
    - path: "UtinniCoreDotNet/Formats/Decoders/ClefDecoder.cs"
      provides: "LooksLikeClientEffect detection (root SubTypeId == CLEF)"
      contains: "CLEF"
    - path: "UtinniCoreDotNet.Tests/ClientEffect/ClientEffectCodecTests.cs"
      provides: "roundtrip / string-edit / list-mutation / field-edit / raw-fallback / load-order coverage"
      min_lines: 120
  key_links:
    - from: "ClientEffectDocument.FromBytes"
      to: "IffReader.Read + MutableIffDocument.FromDocument + MutableClientEffect.FromMutableIff"
      via: "compose-never-reframe (ParticleEffectDocument analog)"
      pattern: "MutableIffDocument\\.FromDocument"
    - from: "MutableClientEffect string/scalar edit"
      to: "MutableIffNode.SetPayload -> MarkDirtyAndInvalidateAncestors -> IffWriter FORM-length re-roll"
      via: "length-ripple DOM (already shipped)"
      pattern: "SetPayload"
---

<objective>
Build the pure-managed CLEF codec — the foundation every other Phase 22 plan composes on — and
commit the synthesized both-lineage golden fixture corpus + the codec test suite (Wave 0).

This is reuse-by-composition: clone the Particle three-layer stack (`ClientEffectDocument` /
`MutableClientEffect` / per-command view + parse exception) near-1:1, add the ONE genuinely new
asset (`ClefFieldCodec` — variable-length LE/cstring encode, unlike fixed-span `TrnFieldEncoder`),
and EXERCISE the already-shipped `MutableIffNode`/`IffWriter` length-ripple with goldens (it is
verified in research — the planner's job is to prove it with tests, not build it).

Purpose: PROD-W2-CFX-01's byte-exact open/edit/save core. No new external packages; pure net4.7.2,
compiles into the existing `UtinniCoreDotNet.dll`; `Generated/UtinniCore.cs` is NOT touched.
Output: `Formats/ClientEffect/*` codec, `Decoders/ClefDecoder.cs`, 8 committed `.iff` goldens,
`ClientEffectCodecTests.cs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-clienteffect-editor/22-CONTEXT.md
@.planning/phases/22-clienteffect-editor/22-RESEARCH.md
@.planning/phases/22-clienteffect-editor/22-PATTERNS.md
@.planning/phases/22-clienteffect-editor/22-VALIDATION.md

<interfaces>
<!-- Contracts the executor reuses verbatim. Do NOT re-explore the codebase for these. -->

Particle analog (clone shape, change type names):
- UtinniCoreDotNet/Formats/Particle/ParticleEffectDocument.cs  — FromBytes/FromIff entry methods (the codec front door).
- UtinniCoreDotNet/Formats/Particle/MutableParticleEffect.cs   — model over MutableIffDocument; MaxNodeCount forged-count cap; IsRawPreserved flag; KnownEffectVersions HashSet; FromMutableIff dispatch (throw only on wrong root / forged count, never on unknown version); Serialize() == IffWriter.Write(SourceIff).
- UtinniCoreDotNet/Formats/Particle/ParticleParseException.cs  — enum + ctor to clone.
- UtinniCoreDotNet/Formats/Decoders/TgenDecoder.cs             — LooksLikeTerrain shape for ClefDecoder.LooksLikeClientEffect.

LE/cstring primitives (read side — every CLEF primitive already exists):
- UtinniCoreDotNet/Formats/Decoders/IffPayloadCursor.cs:62-153 — ReadInt32Le / ReadInt8 / ReadFloatLe / ReadCString(Encoding). ReadCString scans to 0x00, consumes the terminator, throws DecoderException(Truncated) on an unterminated string (bounds-safe).

The verified length-ripple DOM (EXERCISE, do not build):
- UtinniCoreDotNet/Formats/Iff/MutableIffNode.cs — SetPayload (any length) / AddLeaf / Remove / ReorderUp / ReorderDown all call MarkDirtyAndInvalidateAncestors (clears capturedSlice on the node + every ancestor).
- UtinniCoreDotNet/Formats/Iff/IffWriter.cs:144-187 — WriteContainerFresh recomputes innerLen = 4 + Sigma child sizes from ACTUAL child bytes under `checked`; clean nodes re-emit their captured slice verbatim.
- UtinniCoreDotNet/Formats/Iff/MutableIffDocument.cs — FromDocument(doc, sourceBytes) captures the verbatim slice per node.
</interfaces>

<clef_spec>
<!-- The format truth (22-RESEARCH.md § CLEF Codec Spec). Container: FORM CLEF { FORM 0001|0002|0003 { N flat command chunks } }. -->
Endianness split (Pitfall 6): IFF tags + lengths are BIG-endian (IffReader handles framing);
chunk PAYLOAD scalars are LITTLE-endian. Strings are NUL-terminated C-strings (strlen+1 on disk),
NEVER length-prefixed (Pitfall 2). bool8 = 1 byte; uint8 = 1 byte; float = 4 bytes IEEE-754;
int32 = 4 bytes.

CPAP (CreateAppearance — the ONLY version-variant command), read in THIS order:
  appearanceTemplateName: C-string ; timeInSeconds: float  [v0001 ends here]
  + softParticleTerminate: bool8                            [v0002+]
  + minScale,maxScale,minPlaybackRate,maxPlaybackRate: 4 floats [v0003]
PSND (PlaySound): soundTemplateName: C-string. (stable)
CLGT (CreateLight): r,g,b: uint8 ; timeInSeconds,constantAtt,linearAtt,quadraticAtt,range: 5 floats = 23 bytes odd → no-pad quirk matters. (stable)
CAMS (CameraShake): magnitude,frequency,time,falloffRadius: 4 floats = 16 bytes. (stable)
FFBK (ForceFeedback), read in THIS order: forceFeedbackFile: C-string ; iterations: int32 ; range: float. (stable)
Unknown command tag → raw-preserve the leaf (never edited → re-emits captured slice for free).
Unknown version (4th+, e.g. 9999) → raw-preserve the WHOLE CLEF FORM (KnownEffectVersions = {0001,0002,0003}).
</clef_spec>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CLEF model + decoder + variable-length field codec</name>
  <files>UtinniCoreDotNet/Formats/ClientEffect/ClientEffectDocument.cs, UtinniCoreDotNet/Formats/ClientEffect/MutableClientEffect.cs, UtinniCoreDotNet/Formats/ClientEffect/ClientEffectCommand.cs, UtinniCoreDotNet/Formats/ClientEffect/ClefFieldCodec.cs, UtinniCoreDotNet/Formats/ClientEffect/ClientEffectParseException.cs, UtinniCoreDotNet/Formats/Decoders/ClefDecoder.cs</files>
  <read_first>
    - UtinniCoreDotNet/Formats/Particle/ParticleEffectDocument.cs (FromBytes/FromIff entry shape to clone)
    - UtinniCoreDotNet/Formats/Particle/MutableParticleEffect.cs (model skeleton: MaxNodeCount, IsRawPreserved, KnownEffectVersions, FromMutableIff, Serialize)
    - UtinniCoreDotNet/Formats/Particle/ParticleParseException.cs (enum + ctor to clone)
    - UtinniCoreDotNet/Formats/Particle/ParticleEmitterDescription.cs (per-command typed-view role analog)
    - UtinniCoreDotNet/Formats/Decoders/TgenDecoder.cs (LooksLikeTerrain shape)
    - UtinniCoreDotNet/Formats/Decoders/IffPayloadCursor.cs (ReadInt32Le/ReadInt8/ReadFloatLe/ReadCString — read side)
    - UtinniCoreDotNet/Formats/Iff/MutableIffNode.cs (SetPayload/AddLeaf/Remove/Reorder + MarkDirtyAndInvalidateAncestors)
    - UtinniCoreDotNet/Formats/Iff/IffWriter.cs (WriteContainerFresh length re-roll)
    - 22-RESEARCH.md § CLEF Codec Spec + § The Length-Ripple Mechanism + § Common Pitfalls (1,2,6)
    - 22-PATTERNS.md (ClientEffectDocument / MutableClientEffect / ClefFieldCodec / ClefDecoder assignments)
  </read_first>
  <behavior>
    - Decode a FORM CLEF with a version 0001/0002/0003 FORM into a flat ordered list of typed ClientEffectCommand views (one per command leaf); CPAP exposes exactly the fields its file version defines (D-03).
    - Read CPAP and FFBK payloads in the exact SOE field order (string THEN scalars) so offsets land correctly.
    - An unknown version FORM tag (e.g. 9999) sets IsRawPreserved=true and the whole CLEF re-emits verbatim — no throw.
    - An unknown command tag (e.g. XXXX) yields a raw command view whose leaf payload is never touched — re-emits its captured slice.
    - ClefFieldCodec encodes a command-chunk payload from a fresh MemoryStream: NUL-terminated C-string (Encoding.ASCII bytes + 0x00, no length prefix) then LE scalars via BitConverter.GetBytes + reverse-on-BE-host; version-conditional CPAP fields emitted at the SOURCE version only (never upgraded).
    - A wrong root (not FORM CLEF) throws ClientEffectParseException; a forged child count beyond MaxNodeCount throws before allocation (V5 input-validation).
    - Serialize() returns IffWriter.Write(SourceIff) — there is NO bespoke serializer; FORM lengths re-roll from child bytes automatically.
  </behavior>
  <action>
    Create `UtinniCoreDotNet/Formats/ClientEffect/` mirroring `Formats/Particle/`. Clone the Particle
    shapes verbatim, changing type names and the format-specific payload logic only:

    `ClientEffectParseException.cs` — clone `ParticleParseException` (enum `ClientEffectParseError`
    { UnexpectedForm, ForgedCount, Truncated, ... } + ctor carrying a `Kind` property).

    `ClientEffectCommand.cs` — a per-command typed view: a `CommandTag` (CPAP/PSND/CLGT/CAMS/FFBK or
    the raw 4cc), a back-reference to its `MutableIffNode` leaf, a `IsRaw` flag (true for
    unknown-tag leaves), and typed accessors per the `<clef_spec>` field table. Decode each command's
    fields through `IffPayloadCursor` over `leaf.GetPayloadCopy()` in the exact SOE read order.

    `ClefFieldCodec.cs` — the ONE new asset. Do NOT reuse `TrnFieldEncoder` (it is fixed-span and
    rejects length changes — wrong tool for the variable-length string edit that is the point of CFX).
    Expose static encode helpers that build a whole command-chunk payload via a `MemoryStream`:
    a NUL-terminated C-string writer (`Encoding.ASCII.GetBytes(s)` + `WriteByte(0x00)`), an LE float
    writer (`BitConverter.GetBytes(f)` + `Array.Reverse` when `!BitConverter.IsLittleEndian`), an LE
    int32 writer, a uint8/bool8 writer. Provide a per-version CPAP encoder (emit only the fields the
    SOURCE version defines, D-03), and PSND/CLGT/CAMS/FFBK encoders. Expose a matching decode side
    that reads via `IffPayloadCursor` (or delegate decode to `ClientEffectCommand`).

    `MutableClientEffect.cs` — clone `MutableParticleEffect`: `MaxNodeCount` forged-count cap;
    `IsRawPreserved` getter; `KnownEffectVersions = { "0001","0002","0003" }`; `Version` getter (the
    version FORM tag); a `SourceIff` holding the `MutableIffDocument`; `Serialize()` returning
    `IffWriter.Write(SourceIff)`. `FromMutableIff(mutable)` asserts root is `FORM CLEF` (else throw
    `ClientEffectParseException(UnexpectedForm)`), finds the first container child (version FORM); if
    its tag is NOT in `KnownEffectVersions` set `IsRawPreserved` and return (whole sub-tree re-emits
    verbatim); else walk the FLAT command list with a single `while (more children)` loop (NO count
    chunk, unlike PEFT) building a `ClientEffectCommand` per leaf — known tag → typed view, unknown
    tag → raw view. Expose `Commands` (ordered list) + add/remove/reorder operations that delegate to
    `MutableIffNode.AddLeaf`/`Remove`/`ReorderUp`/`ReorderDown` on the version FORM, plus a
    field-edit path that calls `leaf.SetPayload(ClefFieldCodec...)` for length-changing string/scalar
    edits — the DOM re-rolls FORM lengths automatically (verified, RESEARCH § Length-Ripple).

    `ClientEffectDocument.cs` — clone `ParticleEffectDocument`: `FromBytes(byte[])` (IffReader.Read →
    MutableIffDocument.FromDocument → MutableClientEffect.FromMutableIff) and `FromIff(IffDocument,
    byte[])` (skips the re-read, used by the decode-iff CLEF branch in Plan 02). Fold `Serialize()`
    into `MutableClientEffect` (no separate writer class — `IffWriter.Write(SourceIff)`).

    `Decoders/ClefDecoder.cs` — clone `TgenDecoder` shape: `public static bool
    LooksLikeClientEffect(IffChunk root) => (root as IffContainerChunk)?.SubTypeId == "CLEF";`
    (kept for symmetry; Plan 02 may inline the same check in the decode-iff branch).

    Honor the byte-exact gate: never reserialize the version FORM from scratch with a chosen version
    (Pitfall 1 — the SOE `ClientEffectTemplateRW::save` hard-stamps TAG_0003; Utinni must NOT mirror
    it). Never length-prefix a string (Pitfall 2). Never read a payload scalar big-endian (Pitfall 6).
  </action>
  <verify>
    <automated>"D:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe" Utinni.sln /t:UtinniCoreDotNet /p:Configuration=Release /p:Platform=x86 /v:m</automated>
  </verify>
  <acceptance_criteria>
    - `UtinniCoreDotNet/Formats/ClientEffect/{ClientEffectDocument,MutableClientEffect,ClientEffectCommand,ClefFieldCodec,ClientEffectParseException}.cs` all exist and compile into `UtinniCoreDotNet.dll`.
    - `git status` shows `UtinniCoreDotNet/Generated/UtinniCore.cs` is NOT modified (pure-managed; `git checkout --` it if CppSharp churned it during build).
    - `MutableClientEffect.KnownEffectVersions` contains exactly "0001","0002","0003" (source assertion).
    - `MutableClientEffect.Serialize()` body is `return IffWriter.Write(SourceIff);` — no bespoke byte writer (source assertion).
    - `ClefFieldCodec` contains a NUL-terminator write (`WriteByte(0x00)` / `0x00`) and an LE-reverse guard (`!BitConverter.IsLittleEndian` + `Array.Reverse`); it does NOT reference `TrnFieldEncoder` (grep `TrnFieldEncoder` in `ClefFieldCodec.cs` returns zero, filtering comments: `grep -v '^[[:space:]]*//' | grep -c`).
    - `ClefDecoder.LooksLikeClientEffect` compares `SubTypeId == "CLEF"`.
  </acceptance_criteria>
  <done>The CLEF codec compiles clean; model exposes typed commands + raw-fallback + add/remove/reorder; the field codec writes variable-length payloads via the LE/cstring idiom; Generated/UtinniCore.cs untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Synthesized golden fixtures + codec test suite (Wave 0)</name>
  <files>UtinniCoreDotNet.Tests/ClientEffect/ClefFixtureBuilder.cs, UtinniCoreDotNet.Tests/ClientEffect/ClientEffectCodecTests.cs, UtinniCoreDotNet.Tests/Fixtures/ClientEffect/clef_v0001_cpap.iff, UtinniCoreDotNet.Tests/Fixtures/ClientEffect/clef_v0002_cpap.iff, UtinniCoreDotNet.Tests/Fixtures/ClientEffect/clef_v0003_cpap.iff, UtinniCoreDotNet.Tests/Fixtures/ClientEffect/clef_v0003_all5.iff, UtinniCoreDotNet.Tests/Fixtures/ClientEffect/clef_v0001_all5.iff, UtinniCoreDotNet.Tests/Fixtures/ClientEffect/clef_unknown_version.iff, UtinniCoreDotNet.Tests/Fixtures/ClientEffect/clef_unknown_command.iff, UtinniCoreDotNet.Tests/Fixtures/ClientEffect/clef_empty.iff</files>
  <read_first>
    - 22-VALIDATION.md § Both-Lineage Golden Fixture Matrix (the 8 fixtures) + § Per-Task Verification Map
    - 22-RESEARCH.md § Code Examples (roundtrip byte-exact gate; CPAP read-by-version) + § The Length-Ripple Mechanism (the 4 assertions to make on a length-changing edit)
    - UtinniCoreDotNet/Formats/Iff/IffWriter.cs (hand-emit fixtures via the same writer the codec serializes through)
    - UtinniCoreDotNet/Formats/Iff/MutableIffDocument.cs + MutableIffNode.cs (build the FORM CLEF tree for the builder)
    - Utinni.Cli/Commands/RoundtripParticleCommand.cs (the load→serialize→re-parse→SequenceEqual gate shape)
    - The Task 1 codec files (under test)
  </read_first>
  <behavior>
    - `ClefFixtureBuilder` hand-emits each of the 8 fixtures deterministically via `IffWriter` (tiny, byte-stable) and the test writes them to `UtinniCoreDotNet.Tests/Fixtures/ClientEffect/` so they are committed goldens.
    - Roundtrip (no edit): for EVERY fixture, `ClientEffectDocument.FromBytes(bytes).Serialize()` equals the source bytes (length + SequenceEqual) — the strongest byte-exact gate (covers PROD-W2-CFX-01).
    - String edit: editing CPAP appearanceTemplateName to a LONGER and a SHORTER string → output re-parses; the edited command decodes to the new value; ALL untouched command chunks are byte-identical; the version FORM + CLEF FORM lengths equal the recomputed totals.
    - List mutation: add CPAP / remove a command / reorder up+down → output re-parses; version FORM length re-rolls; reorder is byte-reversible (down-then-up returns to identity for a 2-command list).
    - Field edit: editing a CLGT uint8 channel / a CAMS float → byte-exact except the target field's bytes; re-parses to the new value.
    - Raw-fallback: `clef_unknown_version.iff` decodes with IsRawPreserved=true and roundtrips byte-exact; `clef_unknown_command.iff` preserves the XXXX chunk verbatim and roundtrips byte-exact — neither throws.
    - Load-order: a v0003 CPAP decodes name,time,softTerminate,minScale,maxScale,minRate,maxRate in that order matching the SOE field order (reference-validation against the load order).
  </behavior>
  <action>
    Create `UtinniCoreDotNet.Tests/ClientEffect/ClefFixtureBuilder.cs` — a deterministic builder that
    composes each fixture's FORM CLEF { FORM vNNNN { commands } } tree and serializes via `IffWriter`,
    emitting the 8 files named in `<files_modified>` into
    `UtinniCoreDotNet.Tests/Fixtures/ClientEffect/`. Use small fixed strings/scalars (e.g. appearance
    name "appearance/test.prt", floats like 1.0f/2.5f) so the bytes are stable and reviewable. Mark
    the fixture files as committed test assets (Content / CopyToOutputDirectory in the .csproj if the
    test reads from the output dir — follow the existing Particle/Terrain fixture csproj convention).

    Create `UtinniCoreDotNet.Tests/ClientEffect/ClientEffectCodecTests.cs` with xUnit `[Fact]`/
    `[Theory]` methods whose names match the VALIDATION filters: `Roundtrip*ClientEffect*` (Theory over
    all 8 fixtures), `ClefStringEdit*` (longer + shorter), `ClefListMutation*` (add/remove/reorder),
    `ClefFieldEdit*` (CLGT/CAMS scalar), `ClefRawFallback*` (unknown version + unknown command),
    `ClefLoadOrder*` (v0003 CPAP field order). Clone the roundtrip assertion shape from
    `RoundtripParticleCommand` (load → Serialize → re-parse → length + SequenceEqual). For the
    string-edit/list-mutation tests assert the 4 structural conditions from RESEARCH § Length-Ripple
    (re-parses, edited value matches, untouched chunks byte-identical, FORM lengths recomputed).

    All test names MUST contain "ClientEffect" so the quick subset filter
    `--filter "FullyQualifiedName~ClientEffect"` catches them.
  </action>
  <verify>
    <automated>cd /d/Code/Utinni && dotnet test UtinniCoreDotNet.Tests --no-build -c Release --filter "FullyQualifiedName~ClientEffect"</automated>
  </verify>
  <acceptance_criteria>
    - All 8 fixture `.iff` files exist under `UtinniCoreDotNet.Tests/Fixtures/ClientEffect/` and are git-tracked (`git status --porcelain` lists them as added).
    - `dotnet test --no-build --filter "FullyQualifiedName~ClientEffect"` exits 0 with the roundtrip Theory passing for all 8 fixtures (0 failed).
    - A test named matching `ClefStringEdit` proves a LONGER and a SHORTER appearance-string edit both re-parse, with untouched commands byte-identical (asserts `SequenceEqual` on the non-target leaves).
    - A test named matching `ClefListMutation` proves add + remove + reorder each re-parse and re-roll the version FORM length.
    - A test named matching `ClefRawFallback` proves `clef_unknown_version.iff` (IsRawPreserved=true) and `clef_unknown_command.iff` both roundtrip byte-exact and neither throws.
    - A test named matching `ClefLoadOrder` asserts the v0003 CPAP field decode order (name,time,softTerminate,minScale,maxScale,minRate,maxRate).
    - `git status` shows `UtinniCoreDotNet/Generated/UtinniCore.cs` unmodified.
  </acceptance_criteria>
  <done>8 committed goldens exist; the codec test suite is green under the ClientEffect filter; byte-exact roundtrip + length-ripple + raw-fallback + load-order are all asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted `.iff` bytes → `ClefDecoder` / `ClientEffectCommand` | A malformed/forged/truncated ClientEffect file crosses into the variable-length field-decode path (IffPayloadCursor). Files may be hand-crafted or corrupted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-malformed | Denial of Service / Tampering | `ClefFieldCodec` / `ClientEffectCommand` variable-length decode | mitigate | Read every primitive through `IffPayloadCursor`, whose `Need()` bounds-checks each read and `ReadCString` throws `DecoderException(Truncated)` on an unterminated string — no unchecked buffer walk. A truncated CPAP/FFBK string surfaces as a caught parse exception (red status / exit 2 in consumers), never an out-of-bounds read. Covered by `ClefRawFallback`/truncation tests. |
| T-22-forged-count | Denial of Service | `MutableClientEffect.FromMutableIff` flat command loop | mitigate | Enforce the `MaxNodeCount` cap (cloned from `MutableParticleEffect`, T-15-02) before allocating the command list; reject a forged child count. `IffReader`'s existing 64MB chunk cap bounds the outer parse. |
| T-22-version-abort | Denial of Service | unknown CLEF version / unknown command tag | mitigate | Raw-fallback is the contract (D-13): an unknown version raw-preserves the whole CLEF FORM, an unknown command tag raw-preserves the leaf — neither throws. A malformed file degrades, never hard-aborts the future MEF-hosted editor. Covered by `ClefRawFallback`. |

(Path-containment and save-target threats are out of this plan's scope — the codec writes no files. They are modeled in 22-02 / 22-04.)
</threat_model>

<verification>
- MSBuild of `UtinniCoreDotNet` (Release/x86) is clean.
- `dotnet test --no-build --filter "FullyQualifiedName~ClientEffect"` is green (the quick subset, <30s).
- `git status` shows `Generated/UtinniCore.cs` unmodified.
- All 8 golden fixtures are committed.
</verification>

<success_criteria>
- CLEF decode → typed flat command list across versions 0001/0002/0003.
- Byte-exact no-edit roundtrip on all 8 fixtures.
- Length-changing string edit + add/remove/reorder re-parse with untouched chunks byte-identical and FORM lengths re-rolled.
- Source CLEF version preserved verbatim (no v0001→v0003 upgrade).
- Unknown version + unknown command raw-fallback, never a hard failure.
- PROD-W2-CFX-01 codec core delivered (the verb/MCP/UI surfaces ride this in later plans).
</success_criteria>

<output>
Create `.planning/phases/22-clienteffect-editor/22-01-SUMMARY.md` when done.
</output>
