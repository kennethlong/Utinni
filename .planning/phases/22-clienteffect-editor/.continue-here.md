# Phase 22 (ClientEffect Editor) — Continue Here

**Written:** 2026-06-18 (mid-execution pause for restart)
**Status:** 3 of 4 plans complete. Plan 22-04 is paused at its blocking human-verify checkpoint (maintainer live-SWG smoke). All automated work is committed.

---

## TL;DR — how to resume

1. **Bring up the local test server + client** (see "Live-smoke environment" below). The server is the local **Core3 in WSL2 Debian**; launch the client via **Utinni's `bin/Release/Launcher.exe`** (NOT the SWGEmu launcher).
2. **Run the 22-04 maintainer live-SWG smoke** (checklist below).
3. **If it passes:** re-run `/gsd-execute-phase 22`. The orchestrator will hit the safe-resume gate for 22-04 (production commits exist, no SUMMARY yet) — tell it **Tasks 1 & 2 are DONE and committed** (SHAs below) and to spawn a continuation that does ONLY Task 3 (record smoke outcome) → write `22-04-SUMMARY.md` → run the phase-completion flow (code review → verification → `phase.complete`).
   - If it reports issues instead: route to gap closure (`/gsd-plan-phase 22 --gaps`).

> Do NOT let a fresh executor redo 22-04 Tasks 1 & 2 — they are already committed across both repos. Resume at Task 3 only.

---

## Execution state

**Waves (worktrees OFF → executed inline/sequential, model: opus):**

| Wave | Plan | Status | SUMMARY |
|------|------|--------|---------|
| 1 | 22-01 CLEF codec | ✅ complete | yes |
| 1 | 22-03 terrain loose-subdir save fix | ✅ complete | yes |
| 2 | 22-02 effect-* verbs + MCP read tool | ✅ complete | yes |
| 3 | 22-04 TJT EffectsSubPanel + editor form | ⏸ **at Task 3 checkpoint** | **no (intentional)** |

**Commits — Utinni repo (master):**
- `7646dee` feat(22-01) codec model + decoder + ClefFieldCodec
- `ee8acf0` test(22-01) golden fixtures + 43-test suite
- `081667d` docs(22-01) SUMMARY
- `132bae2` feat(22-03) apply-save-trn loose-subdir parity
- `589b21f` docs(22-03) SUMMARY
- `9d734a7` feat(22-02) effect-* verbs + decode-iff CLEF branch + MCP tool
- `494dd33` test(22-02) effect verb suite
- `3077027` docs(22-02) SUMMARY
- `6361c80` test(22-04) in-proc↔CLI save-parity  ← **Task 2 (Utinni half)**

**Commits — UtinniPlugins repo (D:/Code/UtinniPlugins):**
- `b30245d` feat(22-04) ClientEffectSaveTargets + EffectsSubPanel + FormClientEffectEditor  ← **Task 1**
- `4298e48` feat(22-04) register ClientEffect editor in Plugin.cs (GetForms + docked SubPanel)  ← **Task 2 (plugins half)**

**22-04 task breakdown:** Task 1 (SubPanel/Form/SaveTargets) ✅ committed · Task 2 (in-proc save-parity test + Plugin.cs) ✅ committed · **Task 3 (maintainer live-SWG smoke) ⏸ PENDING**.

**Automated gates before the checkpoint — all GREEN:**
- `Utinni.sln` + `TheJawaToolbox.sln` build clean (Release/x86).
- ClientEffect tests: 53/53 (43 codec + 10 in-proc save-parity).
- All acceptance grep gates pass; `Generated/UtinniCore.cs` unmodified in both repos.

**Uncommitted:** `.planning/phases/22-clienteffect-editor/deferred-items.md` (untracked) — fold into the 22-04 SUMMARY commit on resume.

---

## 22-04 Task 3 — maintainer live-SWG smoke checklist

(PASS = type "approved" on resume; else describe the issue.)

1. Build `Utinni.sln` + `TheJawaToolbox.sln` (Release/x86), inject via Utinni `Launcher.exe`.
2. TJT **"Effects"** docked panel → **"Open Effects Editor"** → Form opens (no MEF drop, no empty body).
3. TRE Browser → **"Open in Effects Editor"** on a CLEF `.iff` → read-only open, command list populates, version-appropriate typed fields on select.
4. Edit an appearance/sound string (variable-length) + a scalar → ● dirty marker → **Save as loose override** → confirms `"Saved → <root>/loose/<logical>"` and file lands **under** `<root>/loose/`.  ← needs loose searchPath (see below)
5. TJT-driven scene change → edited effect loads, OR honest "next scene change / relog" candor (either = PASS; landing naked is baseline, not a regression).  ← needs loose searchPath
6. **"Preview in client"** → honest-candor messaging (dimmed no-hook copy, no effect fires, no crash); no-client vs no-hook copy distinct. Over-promising = FAIL.
7. Open a malformed/unknown-version/truncated-known CLEF → raw/hex degrade (dimmed, Consolas, hint), no hard crash.
8. Dogfood real-asset extract (D-14): `utinni-cli` TRE verbs → extract one real CLEF per reachable lineage → `roundtrip-effect` byte-exact check. If a small unencrypted real CLEF is recoverable, drop under `UtinniCoreDotNet.Tests/Fixtures/ClientEffect/real/` for the optional `RoundtripEffect_RealAsset_WhenPresent` test; if v6000 encryption blocks all, caveat the "both lineages" (PROD-W2-CFX-02) CI gap in the SUMMARY.

Steps 1–3, 6, 7 are testable without the loose searchPath. Steps 4–5 (in-client `<root>/loose` pickup) need it enabled (currently OFF — see below).

---

## Live-smoke environment

### Server: Core3 in WSL2 (local, on this dev box)
- Distro **Debian** (run user `kenny`); source/runbook `D:\Code\Core3\wsl2\`.
- **Start:** `wsl -d Debian` then `core3-start` (ensures MariaDB, launches core3 in a `screen` session; idempotent). Ready when log shows `[LoginDatagramService] INFO - started on port 44453`. Boot ~90–285s.
- **Status/log:** `wsl -d Debian -u kenny -- ss -uln` (SWG ports are **UDP**: 44453 login / 44462 ping / 44463 zone) · `tail -F ~/workspace/Core3/MMOCoreORB/bin/log/core3.log`.
- As of this pause: **healthy, 5.5-day uptime**, all 3 UDP ports up. WSL IP = `172.21.29.63`.

### Client login config — `D:\SWGEmu-Client\SWGEmu\swgemu_login.cfg`
Currently:
```
[ClientGame]
loginServerAddress0=172.21.29.63   ← the WSL IP (volatile; re-check on reboot)
loginServerPort0=44453
```
- **If login spins after a reboot:** the WSL NAT IP changed. Get the new IP (`wsl -d Debian -u kenny -- hostname -I`), then update BOTH this file AND the DB galaxy row:
  `wsl -d Debian -u kenny -- mysql -uswgemu -p123456 swgemu -e "UPDATE galaxy SET address='NEW.IP' WHERE galaxy_id=2;"`
- `192.168.1.200` was a **different VirtualBox server** — a red herring, do not use it.

### Launcher target — `D:\Code\Utinni\bin\Release\ut.ini` `[Launcher]`
Currently set to **SWGEmu** (correct for this smoke):
```
swgClientName = SWGEmu.exe
swgClientPath = D:\SWGEmu-Client\SWGEmu\
```
(DX11 target `SwgClient_r.exe` @ `swg-client-v2\stage\` preserved in the comment for one-line revert.)
- Utinni's `Launcher.exe` is an inject tool (CreateProcess suspended + inject), no UI of its own — "no window" = the launched client didn't come up. It waits 30s for the in-process editor ready signal, then shows a "timed out" dialog if injection failed.

### Loose searchPath (in-client `<root>/loose` pickup) — `D:\SWGEmu-Client\SWGEmu\swgemu_live.cfg`
- `searchPath_00_27` is **commented out** (left OFF deliberately — a stray priority-27 loose path shadows retail data machine-wide; caused the 06-12 phantom-walk).
- To run smoke steps 4–5, uncomment it pointed at `<root>\loose` for the `<root>` you pick in the editor's Save dialog, then **re-comment it after** the smoke.

---

## Critical Anti-Patterns

| # | Anti-pattern | Severity | Why |
|---|--------------|----------|-----|
| 1 | Re-running 22-04 Tasks 1 & 2 on resume | blocking | Already committed across both repos (`b30245d`,`4298e48`,`6361c80`). Resume at Task 3 only. |
| 2 | Using the official **SWGEmu launcher** to start the client | blocking | It rewrites `swgemu_login.cfg` to `login.swgemu.com` every run. Use Utinni's `Launcher.exe`. |
| 3 | Diagnosing SWG connectivity with TCP tools (`ss -tln`, `Test-NetConnection -Port`) | blocking | SWG login/zone are **UDP**. TCP tools test the wrong protocol and mislead (cost ~1h on 06-18). Use `ss -uln`. |
| 4 | Re-blessing the ABI baseline for `AbiSurfaceTests` failure | blocking | Pre-existing Phase-17 harness artifact (incremental build skips `UtinniCoreDotNetGen.exe`); 22-04 touches zero native/bridge surface. See `deferred-items.md`. Do NOT re-bless. |
| 5 | Committing `UtinniCoreDotNet/Generated/UtinniCore.cs` | blocking | CppSharp reorders it every build (symmetric no-op churn). Always `git checkout --` it. |
| 6 | Spawning executors with worktree isolation | blocking | Worktrees are OFF for this repo; run build waves inline on the main tree. |

---

## Open / deferred after this phase
- `deferred-items.md` — pre-existing `AbiSurfaceTests` ABI-baseline failure (out of scope; fold note into 22-04 SUMMARY).
- PROD-W2-CFX-02 "both lineages" CI coverage may be caveated if v6000 encryption blocks real-asset extraction (smoke step 8).
- After phase completion: STATE/ROADMAP/REQUIREMENTS update via `gsd-sdk query phase.complete 22` (watch the next-phase checkbox bug — next is Phase 23).
