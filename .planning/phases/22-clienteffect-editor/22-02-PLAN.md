---
phase: 22-clienteffect-editor
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]   # REVIEWS HIGH #3: does NOT depend on 22-03 — the --loose-subdir convention is self-contained per verb and lands IN THIS plan (ApplySaveEffectOptions), so ClefLooseOverrideTests is not flaky against 22-03's ordering. 22-02 and 22-03 each own their verb's loose-subdir flag independently.
files_modified:
  - Utinni.Cli/Commands/DecodeEffectCommand.cs
  - Utinni.Cli/Commands/RoundtripEffectCommand.cs
  - Utinni.Cli/Commands/ApplySaveEffectCommand.cs
  - Utinni.Cli/Commands/DecodeIffCommand.cs
  - Utinni.Cli/Program.cs
  - Utinni.Mcp/Tools/ReadTools.cs
  - Utinni.Cli.Tests/ClientEffect/DecodeEffectTests.cs
  - Utinni.Cli.Tests/ClientEffect/RoundtripEffectCommandTests.cs
  - Utinni.Cli.Tests/ClientEffect/ApplySaveEffectCommandTests.cs
  - Utinni.Cli.Tests/ClientEffect/ClefLooseOverrideTests.cs
  - Utinni.Cli.Tests/ClientEffect/CliHelpEnumeratesEffectVerbsTests.cs
autonomous: true
requirements: [PROD-W2-CFX-02]

must_haves:
  truths:
    - "`utinni-cli decode-effect <path>` and `decode-iff` on a CLEF root emit the same JSON envelope describing the command list INCLUDING a `stableId` per command (the SAME node.StableIdPath key the terrain envelope emits) — required for --leaf addressing and the editor (REVIEWS HIGH #1) — plus the thin MCP read tool surface (D-11)"
    - "`utinni-cli roundtrip-effect <path>` reports bytesIdentical=true on a clean fixture and uses the locked 0/2/3 exit-code contract"
    - "`apply-save-effect` defines its OWN `--loose-subdir` flag (default \"loose\") composing `<root>/loose/` via the SAME two-step LooseOverridePath.Resolve as apply-save-trn — the convention lands in THIS plan, not borrowed from 22-03 (REVIEWS HIGH #3)"
    - "`utinni-cli apply-save-effect` applies a length-changing field/string edit and verifies STRUCTURALLY BY MUTATION MODE — field-edit: untouched leaves byte-identical by stableId (excluding the edited leaf); reorder: count-unchanged + multiset of (tag,payload-bytes) equal (NOT stableId equality); add: count+1 + all original payloads present + new command decodes with locked defaults; remove: count-1 + removed payload absent + survivors byte-identical by CONTENT not id — then atomically commits under <root>/loose with fail-closed containment (REVIEWS HIGH #2)"
    - "`apply-save-effect` asserts canonical payload on the edited command: exact encoder length for the command/version, all fields decode, no trailing bytes, and the reparsed CLEF version equals the original (REVIEWS HIGH #2 / #7)"
    - "`--add-command <tag>` uses LOCKED default field values per command/version (shared with ClefFixtureBuilder + the Form) so goldens are deterministic and editor/CLI match (REVIEWS MEDIUM #5); apply-save-effect proves D-03 against v0001/v0002/v0003 fixtures (CLI is a separate save path, REVIEWS MEDIUM-Codex)"
    - "`apply-save-effect` refuses an edit addressed at a raw-fallback / non-editable / truncated-known node (no half-understood rewrite)"
    - "The MCP `summarize_clienteffect` tool shells `utinni-cli decode-iff` with ZERO format logic"
    - "`utinni-cli --help` enumerates the three new effect-* verbs"
  artifacts:
    - path: "Utinni.Cli/Commands/RoundtripEffectCommand.cs"
      provides: "byte-exact roundtrip gate verb (clone of RoundtripParticleCommand)"
      contains: "roundtrip-effect"
    - path: "Utinni.Cli/Commands/ApplySaveEffectCommand.cs"
      provides: "variable-length structural-verify save verb (adapts ApplySaveTrnCommand; fixed-length guard removed; verify split by mutation mode; own --loose-subdir)"
      contains: "loose-subdir"
    - path: "Utinni.Cli/Commands/DecodeIffCommand.cs"
      provides: "CLEF root-FORM auto-dispatch branch + BuildClefResult emitting per-command stableId + ClientEffectParseException catch rung"
      contains: "stableId"
    - path: "Utinni.Mcp/Tools/ReadTools.cs"
      provides: "summarize_clienteffect read tool (shells utinni-cli, zero format logic)"
      contains: "summarize_clienteffect"
  key_links:
    - from: "Program.cs ParseArguments Type[] + Dispatch switch"
      to: "DecodeEffectOptions / RoundtripEffectOptions / ApplySaveEffectOptions"
      via: "both lists updated (D-12; CLI already on the Type[] overload)"
      pattern: "EffectOptions"
    - from: "DecodeIffCommand CLEF branch / BuildClefResult"
      to: "ClientEffectDocument.FromIff + each command's StableId (DeriveStableId)"
      via: "root SubTypeId == CLEF (PEFT branch template); stableId key per command (terrain envelope parity)"
      pattern: "stableId"
    - from: "ApplySaveEffectCommand --root + --loose-subdir + relAsset"
      to: "LooseOverridePath.Resolve(root, looseSubDir) then Resolve(base, relAsset)"
      via: "two-step compose under <root>/loose (the apply-save-trn / TerrainSaveTargets convention, D-10)"
      pattern: "LooseOverridePath\\.Resolve"
    - from: "ReadTools.summarize_clienteffect"
      to: "cli.RunAsync(\"decode-iff\", ...)"
      via: "MCP-OOP subprocess boundary (DEC-V2-MCP-OOP)"
      pattern: "RunAsync\\(\"decode-iff\""
---

<objective>
Expose the Plan 01 CLEF codec as the verbs-first surface (DEC-V2-VERBS-FIRST): three `effect-*`
`utinni-cli` verbs (`decode-effect` / `roundtrip-effect` / `apply-save-effect`), a `decode-iff` CLEF
auto-dispatch branch (which gives the existing MCP `decode_iff` tool CLEF routing for free), and a
thin MCP `summarize_clienteffect` read tool that shells `utinni-cli` with ZERO format logic
(DEC-V2-MCP-OOP). Plus the integration test suite (the Wave-0 tests-first tier) that golden-tests them.

This revision (`--reviews`) lands the three contracts both reviewers flagged as the MEDIUM→LOW risk
delta on the verb surface: (1) `BuildClefResult` SURFACES `stableId` per command — the SAME
`node.StableIdPath` key the terrain envelope emits — so `--leaf` addressing and the editor have a
real contract (HIGH #1); (2) `apply-save-effect` defines its OWN `--loose-subdir` flag (default
"loose") composing `<root>/loose/` via the same two-step `LooseOverridePath.Resolve` — the convention
is self-contained in THIS plan, removing the implicit 22-03 dependency (HIGH #3); (3) the structural
verify is SPLIT BY MUTATION MODE (field-edit / reorder / add / remove) plus canonical-payload
assertions, because the ordinal-based stableId shifts under reorder/add and a naive
"untouched-by-stableId" compare would false-fail or false-pass (HIGH #2). Plus locked `--add-command`
defaults (MEDIUM #5) and per-version apply-save D-03 tests (MEDIUM-Codex).

The ONE design delta: `apply-save-effect` CANNOT reuse `apply-save-trn`'s fixed-span verify (which
rejects length changes). CLEF's primary edit IS a length change (D-01).

Purpose: PROD-W2-CFX-02 (golden-tested verbs + MCP read tool, reference-validated against the load
order). Output: 3 verbs + decode-iff branch + Program.cs wiring + MCP tool + 5 test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-clienteffect-editor/22-CONTEXT.md
@.planning/phases/22-clienteffect-editor/22-RESEARCH.md
@.planning/phases/22-clienteffect-editor/22-PATTERNS.md
@.planning/phases/22-clienteffect-editor/22-VALIDATION.md
@.planning/phases/22-clienteffect-editor/22-REVIEWS.md
@.planning/phases/22-clienteffect-editor/22-01-SUMMARY.md

<interfaces>
<!-- Reuse targets, read in full in 22-PATTERNS.md. Do NOT re-explore. -->

CLI verb reuse templates (clone shapes, change type names):
- Utinni.Cli/Commands/DecodeTrnCommand.cs        — alias-delegation precedent (delegates to DecodeIffCommand.BuildTerrainResult); the exact decode-effect shape.
- Utinni.Cli/Commands/RoundtripParticleCommand.cs — load → Serialize → re-parse → length+SequenceEqual JSON envelope; the roundtrip-effect shape.
- Utinni.Cli/Commands/ApplySaveTrnCommand.cs      — load → --root containment (LooseOverridePath.Resolve) → decode → locate-leaf → reject-non-editable → mutate → re-parse → atomic write. COPY the scaffold, REPLACE the verify, ADD --loose-subdir (the 22-03 revision adds the same flag to apply-save-trn; mirror it here).
- Utinni.Cli/Commands/DecodeIffCommand.cs:330-360 — the terrain envelope emits `stableId = node.StableIdPath` per child; BuildClefResult must emit the SAME `stableId` key per command (REVIEWS HIGH #1).
- Utinni.Cli/Commands/DecodeIffCommand.cs         — root-FORM auto-dispatch (PEFT/TGEN branches at the SubTypeId compares) + the catch ladder; add a CLEF branch + a ClientEffectParseException rung.
- Utinni.Cli/Program.cs:48-77 (Type[] ParseArguments) + :83-111 (Dispatch switch) — already at 26 verbs on the Type[] overload (D-12 solved). Add each new options type to BOTH lists.
- Utinni.Mcp/Tools/ReadTools.cs (SummarizeTerrain ~:119-127) — copy verbatim, change Name/Description to summarize_clienteffect, dispatch decode-iff (CLEF root auto-routes).

The stableId contract (from Plan 01, REVIEWS HIGH #1):
- ClientEffectCommand.StableId — set from MutableIffDocument.DeriveStableId (ordinal-path id). BuildClefResult reads it directly; do NOT recompute an index/path id.
- MutableIffDocument.DeriveStableId is ORDINAL-BASED — stable under field-edit, but CHANGES under reorder/add-at-front. This is exactly why the apply-save-effect verify splits by mutation mode (below).
- MutableClientEffect.RemoveByStableId / ReorderByStableId (Plan 01 shims) — the CLI --remove-leaf / --reorder addressing path.

CLI exit-code contract (LOCKED, shared by every verb): 0 success; 1 UsageError; 2 parse/decode/IO/verify/path-containment; 3 FileNotFound. Catch <Format>ParseException / DecoderException / IffParseException / IOException; generic Exception is intentionally NOT caught.

ApplySaveTrnCommand current verify (the fixed-length guard to REMOVE for CLEF, :178-183):
  if (newPayload.Length != originalPayload.Length) -> VerifyFailed.  // CLEF must NOT keep this.

Loose-subdir composition (the SAME two-step compose 22-03 adds to apply-save-trn — REVIEWS HIGH #3):
  var overrideBase = LooseOverridePath.Resolve(o.Root, o.LooseSubDir);   // <root>/loose
  destPath        = LooseOverridePath.Resolve(overrideBase, o.RelAsset); // <root>/loose/<relAsset>
  (empty LooseSubDir → legacy single-step Resolve(o.Root, o.RelAsset); both legs fail-closed.)

Codec entry (from Plan 01):
- ClientEffectDocument.FromBytes(byte[]) / FromIff(IffDocument, byte[]) -> MutableClientEffect (.Commands[].StableId, .Version, .IsRawPreserved, .Serialize(), add/remove/reorder by stableId + field-edit ops).
- ClientEffectParseException (.Kind).
</interfaces>

<verb_decisions>
<!-- D-11 + RESEARCH Open Question 2 — planner decisions, fixed here so the executor does not re-decide. -->
- Verb names: `decode-effect`, `roundtrip-effect`, `apply-save-effect` (the trn/particle family idiom).
- `decode-effect` is a thin ALIAS delegating to a new `DecodeIffCommand.BuildClefResult` (cf. DecodeTrnCommand → BuildTerrainResult). The `decode-iff` CLEF branch is required regardless.
- `apply-save-effect` flag shape:
    --root <client root>
    --loose-subdir <subdir>  (OPTIONAL, default "loose" — REVIEWS HIGH #3; composes <root>/loose/<relAsset> via the two-step LooseOverridePath.Resolve, mirroring apply-save-trn's revised flag. Empty string = legacy <root>/<relAsset> for a pre-composing caller.)
    --leaf <stableId of the command DATA leaf>  (the contained loose-override destination is the positional relAsset, as in apply-save-trn)
    Field edits:   --field <name>  --value <new value>   (variable-length string edits + scalar/flag/color edits — the fixed-length guard is REMOVED).
    List mutation: --add-command <CPAP|PSND|CLGT|CAMS|FFBK>  (emits at the file's existing version, D-03)
                   --remove-leaf <stableId>
                   --reorder <stableId> <up|down>
  Exactly ONE mutation per invocation (field edit XOR add XOR remove XOR reorder); reject combinations with exit 1.
- `--add-command` LOCKED DEFAULT FIELD VALUES (REVIEWS MEDIUM #5) — the SAME constants ClefFixtureBuilder (Plan 01) and the Form "Add command" path (Plan 04) MUST use, so goldens are deterministic and editor/CLI match. Emit at the file's existing CLEF version (D-03):
    CPAP: appearanceTemplateName = "appearance/default.prt", timeInSeconds = 1.0f
          [v0002+] softParticleTerminate = 0 (false)
          [v0003]  minScale = 1.0f, maxScale = 1.0f, minPlaybackRate = 1.0f, maxPlaybackRate = 1.0f
    PSND: soundTemplateName = "sound/default.snd"
    CLGT: r = 255, g = 255, b = 255 ; timeInSeconds = 1.0f, constantAtt = 1.0f, linearAtt = 0.0f, quadraticAtt = 0.0f, range = 1.0f
    CAMS: magnitude = 1.0f, frequency = 1.0f, time = 1.0f, falloffRadius = 1.0f
    FFBK: forceFeedbackFile = "forcefeedback/default.ffe", iterations = 1, range = 1.0f
  (These should live as shared `const`/`static readonly` defaults referenced by all three call sites — the executor places them in the Plan 01 codec namespace so Plan 04 can reuse them.)
- decode-effect / decode-iff CLEF JSON envelope: follow the particle/terrain `BuildXxxResult` JObject conventions (rootType "CLEF", version, rawPreserved, an ordered commands array of {index, stableId, tag, isRaw, fields}). `stableId` is MANDATORY per command (REVIEWS HIGH #1). Keep it descriptive (read tool); no byte dumps.
</verb_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: decode-effect + roundtrip-effect verbs + decode-iff CLEF branch (stableId envelope) + Program.cs + MCP tool</name>
  <files>Utinni.Cli/Commands/DecodeEffectCommand.cs, Utinni.Cli/Commands/RoundtripEffectCommand.cs, Utinni.Cli/Commands/DecodeIffCommand.cs, Utinni.Cli/Program.cs, Utinni.Mcp/Tools/ReadTools.cs</files>
  <read_first>
    - Utinni.Cli/Commands/DecodeTrnCommand.cs (alias-delegation precedent)
    - Utinni.Cli/Commands/RoundtripParticleCommand.cs (roundtrip envelope + catch ladder)
    - Utinni.Cli/Commands/DecodeIffCommand.cs (PEFT/TGEN branch + BuildParticleResult/BuildTerrainResult + catch ladder) + :330-360 (the terrain envelope's `stableId = node.StableIdPath` per child — the key BuildClefResult mirrors, REVIEWS HIGH #1)
    - Utinni.Cli/Program.cs (the Type[] ParseArguments list + the Dispatch switch — both at :48-113)
    - Utinni.Mcp/Tools/ReadTools.cs (SummarizeTerrain/SummarizeParticle — the zero-logic shell template)
    - 22-01-SUMMARY.md (the exact ClientEffectDocument / MutableClientEffect API surface + ClientEffectCommand.StableId)
    - 22-REVIEWS.md § Consensus Concern 1 (stableId in the envelope)
    - 22-PATTERNS.md (DecodeEffectCommand / RoundtripEffectCommand / DecodeIffCommand / ReadTools assignments)
  </read_first>
  <behavior>
    - `decode-effect <path>` reads bytes, builds `ClientEffectDocument.FromBytes`, emits `JsonOutput.EmitSuccess("decode-effect", DecodeIffCommand.BuildClefResult(...))`; FileNotFound → exit 3; ClientEffectParseException / DecoderException / IffParseException / IOException → exit 2; generic Exception NOT caught.
    - `BuildClefResult` emits an ordered commands array where EACH command object includes `stableId` (from `ClientEffectCommand.StableId`, the DeriveStableId ordinal-path), `index`, `tag`, `isRaw`, `fields` — `stableId` is MANDATORY (REVIEWS HIGH #1).
    - `decode-iff` on a `FORM CLEF` root auto-routes to `BuildClefResult` (the PEFT branch template) and the catch ladder gains a `ClientEffectParseException` rung — so the MCP `decode_iff` tool gets CLEF routing for free.
    - `roundtrip-effect <path>` loads → `Serialize()` → re-parses → reports `bytesIdentical`, `rawPreserved`, `rootType:"CLEF"`, `version`, `source`; exit 0 on success, 2 on parse/IO, 3 on FileNotFound.
    - `Program.cs` registers `DecodeEffectOptions`, `RoundtripEffectOptions`, `ApplySaveEffectOptions` in BOTH the `Type[]` ParseArguments list and the `Dispatch` switch.
    - `summarize_clienteffect` MCP tool resolves the path under the root, shells `utinni-cli decode-iff`, and passes the envelope through `CliResultMapper.ToCallToolResult` — ZERO format logic in `Utinni.Mcp`.
  </behavior>
  <action>
    Create `Utinni.Cli/Commands/DecodeEffectCommand.cs` (+ `DecodeEffectOptions` `[Verb("decode-effect")]`)
    cloning `DecodeTrnCommand` verbatim: FileNotFound guard → `ClientEffectDocument.FromBytes` →
    `JsonOutput.EmitSuccess("decode-effect", DecodeIffCommand.BuildClefResult(model, o.Path))` with the
    locked catch ladder (`ClientEffectParseException` → exit 2 in place of `TerrainParseException`).

    Create `Utinni.Cli/Commands/RoundtripEffectCommand.cs` (+ `RoundtripEffectOptions`
    `[Verb("roundtrip-effect")]`) cloning `RoundtripParticleCommand`: load → `model.Serialize()` →
    re-parse → `bytesEqual` JObject (`rootType` "CLEF") → `EmitSuccess("roundtrip-effect", ...)`; same
    catch ladder.

    Modify `Utinni.Cli/Commands/DecodeIffCommand.cs`: add a CLEF branch right after the PEFT branch —
    `if ((doc.Root as IffContainerChunk)?.SubTypeId == "CLEF") return EmitSuccess("decode-iff",
    BuildClefResult(ClientEffectDocument.FromIff(doc, bytes), o.Path));` — and add a
    `BuildClefResult(MutableClientEffect, string)` builder following `BuildParticleResult`'s JObject
    shape (rootType, version, rawPreserved, commands array of {index, stableId, tag, isRaw, fields}).
    The `stableId` value comes from each `ClientEffectCommand.StableId` — mirror the terrain
    `stableId = node.StableIdPath` precedent at DecodeIffCommand.cs:339 (REVIEWS HIGH #1). Add a
    `catch (ClientEffectParseException ex)` rung (exit 2) to the existing catch ladder.

    Modify `Utinni.Cli/Program.cs`: add the three new options types to the `Type[]` `ParseArguments`
    call AND a `case Commands.<X>Options o: return Commands.<X>Command.Run(o);` to the `Dispatch`
    switch. (D-12: the CLI already uses the no-arity-cap `Type[]` overload — this is wiring, not
    new infrastructure.)

    Modify `Utinni.Mcp/Tools/ReadTools.cs`: add `summarize_clienteffect` by copying `SummarizeTerrain`
    verbatim — change `[McpServerTool(Name = "summarize_clienteffect", ...)]` + the `[Description]`,
    keep `cli.RunAsync("decode-iff", new[] { abs })`. ZERO format logic (MCP-OOP lock).
  </action>
  <verify>
    <automated>"D:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe" Utinni.sln /t:Utinni_Cli;Utinni_Mcp /p:Configuration=Release /p:Platform=x86 /v:m</automated>
  </verify>
  <acceptance_criteria>
    - `Utinni.Cli/Commands/{DecodeEffectCommand,RoundtripEffectCommand}.cs` exist; `DecodeIffCommand.cs` has a `BuildClefResult` method and a `SubTypeId == "CLEF"` branch and a `catch (ClientEffectParseException` rung.
    - `BuildClefResult` emits a `stableId` key per command (grep `stableId` in `DecodeIffCommand.cs` within the CLEF builder returns ≥1, filtering comments) sourced from `ClientEffectCommand.StableId` (REVIEWS HIGH #1).
    - `Program.cs` references `DecodeEffectOptions`, `RoundtripEffectOptions`, `ApplySaveEffectOptions` in both the `ParseArguments` Type[] list and the `Dispatch` switch (grep all three options type names returns matches in both regions).
    - `ReadTools.cs` contains `summarize_clienteffect` and its body calls `cli.RunAsync("decode-iff"` with no CLEF/IFF format parsing in the method (source assertion: zero references to `ClientEffectDocument` / `IffReader` in `Utinni.Mcp`).
    - The catch ladders in all three CLI commands do NOT catch generic `System.Exception` (source assertion).
    - MSBuild of `Utinni.Cli` + `Utinni.Mcp` (Release/x86) is clean; `git status` shows `Generated/UtinniCore.cs` unmodified.
  </acceptance_criteria>
  <done>decode-effect / roundtrip-effect verbs build; decode-iff routes CLEF and BuildClefResult surfaces a per-command stableId; Program.cs wires all three effect verbs; the MCP summarize_clienteffect tool shells the CLI with zero format logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: apply-save-effect (own --loose-subdir + mutation-mode structural verify + canonical-payload + locked add defaults) + verb test suite</name>
  <files>Utinni.Cli/Commands/ApplySaveEffectCommand.cs, Utinni.Cli.Tests/ClientEffect/DecodeEffectTests.cs, Utinni.Cli.Tests/ClientEffect/RoundtripEffectCommandTests.cs, Utinni.Cli.Tests/ClientEffect/ApplySaveEffectCommandTests.cs, Utinni.Cli.Tests/ClientEffect/ClefLooseOverrideTests.cs, Utinni.Cli.Tests/ClientEffect/CliHelpEnumeratesEffectVerbsTests.cs</files>
  <read_first>
    - Utinni.Cli/Commands/ApplySaveTrnCommand.cs (the full scaffold: --root containment :99-113, reject-non-editable :149-154, the fixed-length guard to REMOVE :178-183, re-parse + atomic commit :185+, TestPerturbSerialized seam) + its revised --loose-subdir two-step compose (added in 22-03 — mirror the SAME flag/compose here)
    - Utinni.Cli/Commands/ApplySaveIffCommand.cs (the atomic-write + path-containment idiom)
    - UtinniCoreDotNet.PathContainment/LooseOverridePath.cs (Resolve fail-closed containment)
    - 22-RESEARCH.md § The Length-Ripple Mechanism ("the single design delta" — the structural verify) + § Security Domain
    - 22-REVIEWS.md § Consensus Concerns 1,2,3,5,7 + Cursor Suggestion #1 (the mutation-mode verify split) + Codex Suggestion (per-version apply-save D-03 tests)
    - 22-01-SUMMARY.md (MutableClientEffect add/remove/reorder by stableId + field-edit API + the locked --add-command default constants in the codec namespace)
    - 22-PATTERNS.md (ApplySaveEffectCommand assignment — the var-length delta)
    - Utinni.Cli.Tests existing *CommandTests.cs (test project conventions, temp-dir + fixture-copy helpers)
  </read_first>
  <behavior>
    - `apply-save-effect` resolves the positional relAsset under `--root` via the TWO-STEP `LooseOverridePath.Resolve(o.Root, o.LooseSubDir)` then `Resolve(overrideBase, o.RelAsset)` (default LooseSubDir "loose" → `<root>/loose/<relAsset>`; empty → legacy single-step). A traversal / rooted / `..` in EITHER leg → exit 2 PathContainment; FileNotFound → exit 3 (REVIEWS HIGH #3).
    - Exactly one mutation per invocation: a field edit (`--leaf --field --value`, length-changing allowed), OR `--add-command <tag>`, OR `--remove-leaf <id>`, OR `--reorder <id> up|down`. A combination → exit 1 UsageError.
    - A field edit addressed at a raw-fallback / unknown-command / truncated-known / non-editable leaf is rejected before any write (exit 1) — no half-understood rewrite (V5/Tampering mitigation).
    - The fixed-length guard (`newPayload.Length != originalPayload.Length → VerifyFailed`) is NOT present — a length change is the point (D-01).
    - `--add-command` uses the LOCKED default field values from the codec namespace (the SAME constants ClefFixtureBuilder + the Form use) — deterministic goldens, editor/CLI parity (REVIEWS MEDIUM #5). Added commands emit at the file's existing version (D-03).
    - STRUCTURAL VERIFY SPLIT BY MUTATION MODE (REVIEWS HIGH #2; the ordinal stableId shifts under reorder/add so a single "untouched-by-stableId" compare is unsound):
        * field-edit: re-parse; every UNTOUCHED leaf is byte-identical compared BY STABLEID (excluding the edited leaf); the edited command decodes to the requested value; CANONICAL-PAYLOAD asserts: edited payload length == exact encoder length for the command/version, all fields decode, no trailing bytes, reparsed CLEF version == original (REVIEWS HIGH #2 / #7).
        * reorder: re-parse; command COUNT unchanged; the MULTISET of (tag, payload-bytes) is equal pre/post (NOT stableId equality — ids deliberately shift); reorder is reversible.
        * add: re-parse; count == original+1; ALL original command payloads still present (by content); the new command decodes to the LOCKED defaults for its tag/version.
        * remove: re-parse; count == original-1; the removed payload is absent; ALL surviving payloads are byte-identical BY CONTENT (not by id).
      Only on a clean verify does it atomically write (Flush(true)) under the contained path. The `TestPerturbSerialized` seam still forces a verify failure in tests.
    - Source CLEF version is preserved on save (added commands emit at the file's version, D-03) — proven against v0001/v0002/v0003 fixtures through the CLI path (REVIEWS MEDIUM-Codex).
  </behavior>
  <action>
    Create `Utinni.Cli/Commands/ApplySaveEffectCommand.cs` (+ `ApplySaveEffectOptions`
    `[Verb("apply-save-effect")]` with the `<verb_decisions>` flag shape INCLUDING the
    `[Option("loose-subdir", Default = "loose", ...)] public string LooseSubDir`). Copy the
    `ApplySaveTrnCommand` scaffold: positional `relAsset`, FileNotFound guard,
    `ClientEffectDocument.FromBytes`, locate the addressed leaf/command by stableId. ADAPT:
    - Containment: compose `destPath` via the TWO-STEP `LooseOverridePath.Resolve` —
      `var overrideBase = string.IsNullOrEmpty(o.LooseSubDir) ? o.Root : LooseOverridePath.Resolve(o.Root, o.LooseSubDir); destPath = LooseOverridePath.Resolve(overrideBase, o.RelAsset);` — wrap both legs in the `catch (ArgumentException)` → exit 2 PathContainment (REVIEWS HIGH #3; mirrors apply-save-trn's revised compose, D-10).
    - Mutation-mode dispatch enforcing exactly-one-of {field-edit, add, remove, reorder};
      combinations → `EmitError(..., "UsageError", exitCode: 1)`.
    - For a field edit: reject a raw/truncated/non-editable target leaf (a command whose `IsRaw` is
      true is non-editable). Build the new payload via the Plan 01 `ClefFieldCodec` /
      `MutableClientEffect` field-edit op and `leaf.SetPayload(...)` — REMOVE the
      `newPayload.Length != originalPayload.Length` guard.
    - For add: use the LOCKED `--add-command` default constants from the codec namespace (REVIEWS #5).
      For add/remove/reorder: call the `MutableClientEffect` list-mutation ops by stableId (which
      delegate to `MutableIffNode.AddLeaf`/`Remove`/`Reorder*` / `RemoveByStableId`).
    - Replace the `OnlyTargetSpanDiffers` verify with the MUTATION-MODE STRUCTURAL verify described in
      <behavior> (field-edit = untouched-by-stableId + canonical-payload; reorder = count + multiset of
      (tag,payload); add = count+1 + originals present + new decodes to defaults; remove = count-1 +
      removed absent + survivors byte-identical by content). Keep the `TestPerturbSerialized` seam and
      the atomic-write-under-contained-path commit. Keep the locked exit-code contract.

    Create the Wave-0 test files in `Utinni.Cli.Tests/ClientEffect/`:
    - `DecodeEffectTests.cs` — `decode-effect` + `decode-iff` CLEF branch emit the expected JSON
      envelope (rootType "CLEF", commands array, version) AND assert a `stableId` is present per command
      (REVIEWS HIGH #1). Names contain "DecodeEffect".
    - `RoundtripEffectCommandTests.cs` — exit 0 / bytesIdentical on a clean fixture; exit 3 on a
      missing path; exit 2 on a corrupt file. Names contain "RoundtripEffectCommand".
    - `ApplySaveEffectCommandTests.cs` — (a) a length-changing string edit commits + re-parses +
      untouched chunks identical by stableId + canonical-payload (exact length, version unchanged);
      (b) reorder commits + multiset-of-(tag,payload) preserved (NOT stableId equality); (c) add commits
      with the LOCKED defaults + count+1 + originals present; (d) remove commits + count-1 + survivors
      byte-identical by content; (e) a field edit on v0001/v0002/v0003 fixtures preserves the version
      (D-03 through the CLI, REVIEWS MEDIUM-Codex); (f) a raw/truncated/non-editable target → exit 1;
      (g) a path-escape relAsset → exit 2; (h) `TestPerturbSerialized` forces VerifyFailed → exit 2
      (no write). Names contain "ApplySaveEffect".
    - `ClefLooseOverrideTests.cs` — assert the resolved destination is under `<root>\loose\` (the
      apply-save-effect `--loose-subdir` default), an empty `--loose-subdir` lands at `<root>\<rel>`,
      and a traversal escape through EITHER leg is rejected (mirror `TerrainLooseOverridePathTests`
      shape, REVIEWS HIGH #3). Names contain "ClefLooseOverride".
    - `CliHelpEnumeratesEffectVerbsTests.cs` — assert `utinni-cli --help` output enumerates
      `decode-effect`, `roundtrip-effect`, `apply-save-effect` (D-12 smoke). Names contain
      "CliHelpEnumerates".
    Reuse the existing test project's CLI-invocation / temp-dir / fixture-copy helpers (do not invent a
    new harness). Reference the Plan 01 fixtures (copy into the test temp dir).
  </action>
  <verify>
    <automated>cd /d/Code/Utinni && dotnet test Utinni.Cli.Tests --no-build -c Release --filter "FullyQualifiedName~ClientEffect"</automated>
  </verify>
  <acceptance_criteria>
    - `Utinni.Cli/Commands/ApplySaveEffectCommand.cs` exists; it does NOT contain a `newPayload.Length != originalPayload.Length` VerifyFailed guard (grep returns zero, filtering comments).
    - `ApplySaveEffectOptions` has a `--loose-subdir` option defaulting to `"loose"` (grep `loose-subdir` in `ApplySaveEffectCommand.cs` returns ≥1, filtering comments) and `Run` composes via a TWO-STEP `LooseOverridePath.Resolve` (base then relAsset) — the convention lands in THIS plan (REVIEWS HIGH #3).
    - `apply-save-effect` returns exit 2 `PathContainment` on a `..` relAsset; a test in `ClefLooseOverride` asserts the default destination is under `<root>\loose\` and an empty `--loose-subdir` lands at `<root>\<rel>`.
    - A test named matching `ApplySaveEffect` proves all four mutation-mode verifies: (a) a LONGER appearance-string edit commits, re-parses, untouched chunks byte-identical by stableId, canonical-payload + version-unchanged; (b) reorder commits with the (tag,payload) multiset preserved and NOT relying on stableId equality; (c) `--add-command CPAP` commits with the LOCKED defaults, count+1, originals present; (d) remove commits, count-1, survivors byte-identical by content (REVIEWS HIGH #2 / #5).
    - A test asserts a field edit on a v0001 AND a v0003 fixture preserves the source version through the CLI save path (D-03, REVIEWS MEDIUM-Codex).
    - A test asserts a field edit on a raw/truncated/non-editable leaf returns exit 1, and `TestPerturbSerialized` forces exit 2 with no file written.
    - A test named matching `CliHelpEnumerates` asserts all three `effect-*` verb names appear in `--help`.
    - `dotnet test --no-build --filter "FullyQualifiedName~ClientEffect"` (Utinni.Cli.Tests) exits 0 (0 failed).
    - `git status` shows `Generated/UtinniCore.cs` unmodified.
  </acceptance_criteria>
  <done>apply-save-effect owns its --loose-subdir default "loose", applies length-changing edits + add/remove/reorder with a MUTATION-MODE structural verify + canonical-payload + locked add-defaults and fail-closed <root>/loose containment; the verb test suite (decode-stableId / roundtrip / apply-save-modes / per-version-D-03 / loose-override / help) is green under the ClientEffect filter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI arg `--root` + `--loose-subdir` + relAsset → loose-override save target | An attacker-controlled or mistyped path could escape the intended client-root directory (path traversal) through either resolve leg. |
| untrusted `.iff` bytes → `decode-effect` / `roundtrip-effect` / `apply-save-effect` | A malformed/forged CLEF file crosses into the codec from the CLI. |
| MCP client → `summarize_clienteffect` → subprocess | A relative path from an MCP caller must not escape the MCP `ResolvedRoot`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-path | Tampering / Elevation | `apply-save-effect` save target | mitigate | Resolve the destination only through the TWO-STEP `LooseOverridePath.Resolve(root, looseSubDir)` then `Resolve(base, relAsset)` (fail-closed: rejects null/empty, rooted paths, any `..` segment, prefix-match escapes; final ordinal-ignore-case `StartsWith(root + sep)`). A rejected path in EITHER leg returns exit 2 `PathContainment` with NO write. The `--loose-subdir` convention lands in THIS plan (REVIEWS HIGH #3). Asserted by `ClefLooseOverrideTests`. |
| T-22-misaddress | Tampering | `apply-save-effect` structural verify under list mutation | mitigate | The verify is SPLIT BY MUTATION MODE (REVIEWS HIGH #2): only field-edit compares untouched leaves by the ordinal stableId; reorder/add/remove compare by CONTENT (multiset / presence / byte-identity) because the ordinal stableId deliberately shifts. Plus canonical-payload (exact encoder length + consume-all-bytes + version-unchanged) so a "successful" edit cannot silently rewrite the wrong bytes — the exact failure mode both reviewers flagged. |
| T-22-halfunderstood | Tampering | `apply-save-effect` field edit on a raw/truncated/non-editable leaf | mitigate | Reject the edit before any write when the addressed command is raw-fallback / unknown-tag / truncated-known (IsRaw=true) — mirrors `ApplySaveTrnCommand`'s `TargetTypedNodeIsEditable` gate (RESEARCH § Security Domain #4). Exit 1; never rewrites a half-understood payload. |
| T-22-malformed | Denial of Service | all three verbs decoding | mitigate | The codec's `IffPayloadCursor` bounds-checks + raw-fallback (from Plan 01) means a malformed file surfaces as a caught parse exception → exit 2, never an unhandled throw. Generic `Exception` is intentionally NOT caught (the locked contract) so a true bug is loud, but every expected malformation is a typed catch. |
| T-22-mcp-escape | Tampering / Information Disclosure | `summarize_clienteffect` path | mitigate | The MCP `ResolvedRoot.Resolve(relativePath)` (T-14-01) rejects path escape before the subprocess shell; the tool passes only the resolved absolute path to `utinni-cli`. ZERO format logic in-process keeps the attack surface at the proven CLI boundary. |
| T-22-SC | Tampering | npm/pip/cargo/NuGet installs | accept | No external packages are installed this phase (RESEARCH § Package Legitimacy Audit: N/A — pure-managed composition of in-repo assemblies). No slopcheck surface. |
</threat_model>

<verification>
- MSBuild of `Utinni.Cli` + `Utinni.Mcp` (Release/x86) is clean.
- `dotnet test --no-build --filter "FullyQualifiedName~ClientEffect"` is green across `Utinni.Cli.Tests`.
- `utinni-cli --help` enumerates the three `effect-*` verbs.
- `git status` shows `Generated/UtinniCore.cs` unmodified.
</verification>

<success_criteria>
- `decode-effect` / `roundtrip-effect` / `apply-save-effect` verbs build and golden-test green; the decode envelope carries a per-command stableId (REVIEWS HIGH #1).
- `decode-iff` auto-routes a CLEF root; the MCP `decode_iff` tool gets CLEF for free.
- `summarize_clienteffect` MCP read tool shells the CLI with zero format logic (MCP-OOP lock).
- `apply-save-effect` owns its `--loose-subdir` default "loose" (REVIEWS HIGH #3), applies length-changing edits + add/remove/reorder with a MUTATION-MODE structural verify + canonical-payload (REVIEWS HIGH #2) + locked add-defaults (REVIEWS #5) and fail-closed `<root>/loose` containment; D-03 proven per-version through the CLI.
- PROD-W2-CFX-02 verb + MCP surface delivered, reference-validated against the SOE load order.
</success_criteria>

<output>
Create `.planning/phases/22-clienteffect-editor/22-02-SUMMARY.md` when done.
</output>
