---
phase: 05-tier-1-c-unit-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - external/catch2/catch_amalgamated.hpp
  - external/catch2/catch_amalgamated.cpp
  - external/catch2/LICENSE.txt
  - UtinniCore.Tests/UtinniCore.Tests.vcxproj
  - UtinniCore.Tests/main_smoke.cpp
  - UtinniCore.Tests/StringUtilityTests.cpp
  - Utinni.sln
  - .github/workflows/ci.yml
# NOTE: UtinniCore.Tests/StringUtilityTests.cpp is created by 05-01 as a
# minimal placeholder (MIT header + one comment line). Its real content
# (the 6 max-harness TEST_CASEs) is owned by 05-02. The placeholder must
# exist now so the vcxproj's <ClCompile Include="StringUtilityTests.cpp"/>
# resolves at 05-01 build time.
autonomous: true
requirements: [TEST-02]
tags: [catch2, c++, msbuild, ci]

must_haves:
  truths:
    - "Catch2 v3.15.0 is vendored at external/catch2/ as catch_amalgamated.{hpp,cpp} (per D-02; SHA-256 captured in commit message per Security Domain V14)"
    - "UtinniCore.Tests.vcxproj declares Debug + Release + RelWithDbgInfo configurations end-to-end (per D-03 / D-07; CON-T-02 triple-config preserved)"
    - "UtinniCore.Tests is registered in Utinni.sln with full triple-config postSolution mappings (NOT the LoaderLockHarness RelWithDbgInfo->Release collapse pattern; per D-07)"
    - "main_smoke.cpp contains 3 TEST_CASEs proving (a) the amalgamated build links and runs, (b) Catch2 exception machinery is functional, (c) SECTION re-entry produces fresh state (per D-05 smoke set in RESEARCH.md §D-05)"
    - "ci.yml gains a third lane invoking bin\\Release\\UtinniCore.Tests.exe with stacked --reporter console + junit::out=... (per D-04; mitigates RESEARCH.md §Pitfall 5 exit-code swallow)"
    - "CI lane gates master from day one — no warn-only posture (per D-04)"
    - "Triple-config gate (CON-T-02 / D-07) is enforced as an explicit acceptance criterion at scaffold time"
  artifacts:
    - path: "external/catch2/catch_amalgamated.hpp"
      provides: "Catch2 v3.15.0 amalgamated header"
      contains: "Catch2 v3.15.0"
    - path: "external/catch2/catch_amalgamated.cpp"
      provides: "Catch2 v3.15.0 amalgamated translation unit (provides main())"
      contains: "Catch2 v3.15.0"
    - path: "UtinniCore.Tests/UtinniCore.Tests.vcxproj"
      provides: "Sibling MSBuild test exe project, triple-config Application/Console"
      contains: "RelWithDbgInfo|Win32"
    - path: "UtinniCore.Tests/main_smoke.cpp"
      provides: "Vendor-drop sanity smoke tests"
      min_lines: 30
      contains: "TEST_CASE(\"Smoke:"
    - path: ".github/workflows/ci.yml"
      provides: "Third CI lane: Run native unit tests (UtinniCore.Tests.exe)"
      contains: "UtinniCore.Tests.exe"
  key_links:
    - from: "UtinniCore.Tests/UtinniCore.Tests.vcxproj"
      to: "external/catch2/catch_amalgamated.cpp"
      via: "<ClCompile Include=\"..\\external\\catch2\\catch_amalgamated.cpp\" />"
      pattern: "catch_amalgamated\\.cpp"
    - from: "UtinniCore.Tests/UtinniCore.Tests.vcxproj"
      to: "UtinniCore/UtinniCore.vcxproj"
      via: "<ProjectReference> with LinkLibraryDependencies=false (build-order only, per LoaderLockHarness precedent)"
      pattern: "LinkLibraryDependencies>false"
    - from: "Utinni.sln"
      to: "UtinniCore.Tests.vcxproj"
      via: "Project entry + 6-line ProjectConfigurationPlatforms block (Debug+Release+RelWithDbgInfo, each with .ActiveCfg + .Build.0)"
      pattern: "RelWithDbgInfo\\|x86\\.Build\\.0 = RelWithDbgInfo\\|Win32"
    - from: ".github/workflows/ci.yml"
      to: "bin\\Release\\UtinniCore.Tests.exe"
      via: "PowerShell step invoking the exe with stacked --reporter flags"
      pattern: "UtinniCore\\.Tests\\.exe"
---

<objective>
Land the **scaffold + smoke** half of Phase 5: vendor Catch2 v3.15.0, create the `UtinniCore.Tests.vcxproj` sibling MSBuild project with full triple-config (Debug + Release + RelWithDbgInfo), register it in `Utinni.sln` honouring CON-T-02 end-to-end, add 3 smoke tests proving the vendor drop works, and wire a third lane into `.github/workflows/ci.yml` gating `master` from day one.

Purpose: Prove the build + CI green-on-master pipeline before any coverage value (D-05). Plan 05-02 is CI-gated — it does not start until this plan's CI lane is green on `master`.

Output: 6 new/modified files; 1 new vendored dependency; 1 new MSBuild project; 1 new CI lane.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-tier-1-c-unit-tests/05-CONTEXT.md
@.planning/phases/05-tier-1-c-unit-tests/05-RESEARCH.md
@.planning/phases/05-tier-1-c-unit-tests/05-PATTERNS.md
@.planning/phases/05-tier-1-c-unit-tests/05-VALIDATION.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/STRUCTURE.md

<interfaces>
<!-- Key contracts the executor needs. All quoted verbatim in PATTERNS.md / RESEARCH.md. -->

From UtinniCore/utility/string_utility.h (lines 1-23): 23-line MIT header block that every new C++ source/header file in this repo MUST carry. Apply to main_smoke.cpp (NOT to the vendored Catch2 files — those keep their upstream BSL-1.0 header per PATTERNS.md "Shared Patterns > MIT Header").

From Utinni.LoaderLockHarness/Utinni.LoaderLockHarness.vcxproj — closest sibling console-exe precedent: ConfigurationType=Application, PlatformToolset=v142, SubSystem=Console, ProjectReference on UtinniCore with LinkLibraryDependencies=false, OutDir=$(SolutionDir)bin\$(Configuration)\. **Deficiency to fix:** only declares 2 of 3 configs; Phase 5 must add RelWithDbgInfo|Win32 (graft from UtinniCore.vcxproj lines 24-45). Sln mapping must be the UtinniCore pattern (sln lines 82-87, real RelWithDbgInfo|Win32 with .Build.0), NOT the LoaderLockHarness pattern (sln line 104 collapses RelWithDbgInfo→Release).

From Utinni.sln (read in full): 13 existing project entries. Well-known C++ project type GUID is `{8BC9CEB8-8B4A-11D0-8D11-00A0C91BC942}`. UtinniCore's own GUID is `{AEFED7F6-4BA9-44FC-A353-71A463A82FDE}` (used in the new vcxproj's ProjectReference and in the new sln entry's ProjectDependencies block). Recommended insertion point: after the existing Utinni.LegacyPlugin entry (ends at line 54), before Utinni.Cli entry (begins at line 55), per RESEARCH.md §D-03.

From .github/workflows/ci.yml (lines 94-108): Phase 4 D-11 step pair — "Run CLI golden tests" + "Upload CLI test artifacts (on failure)". The new Phase 5 pair inserts AFTER line 108. CI builds only `Release|x86` (line 78); the new lane invokes `bin\Release\UtinniCore.Tests.exe`. Triple-config in the vcxproj is for local-dev parity per CON-T-02, not for the CI build matrix.

Composite skeleton for UtinniCore.Tests.vcxproj is in RESEARCH.md lines 762-909 (verbatim XML the executor copies, replacing only the `{REPLACE-WITH-FRESH-GUID}` placeholder). The skeleton already includes:
  - 3-config ProjectConfigurations block (Debug + Release + RelWithDbgInfo)
  - 3 Configuration PropertyGroups with ConfigurationType=Application
  - 3 ItemDefinitionGroups (the third — RelWithDbgInfo — has DebugInformationFormat=ProgramDatabase, Optimization=Disabled, IntrinsicFunctions=false; lines 863-882)
  - AdditionalIncludeDirectories: `$(SolutionDir);$(SolutionDir)external;$(SolutionDir)UtinniCore;$(ProjectDir);%(AdditionalIncludeDirectories)`
  - ConformanceMode=true on ALL three configs (mitigates RESEARCH.md §Pitfall 2)
  - <ClCompile Include="..\external\catch2\catch_amalgamated.cpp" />
  - <ClCompile Include="main_smoke.cpp" />
  - <ClCompile Include="StringUtilityTests.cpp" /> (file will be created by 05-02; declared up front so the vcxproj is stable across both plans)
  - <ClCompile Include="..\UtinniCore\utility\string_utility.cpp" /> (compiled directly into the test exe; no link against UtinniCore.dll for the stringUtility seed — see 05-02)
  - <ProjectReference Include="..\UtinniCore\UtinniCore.vcxproj"> with LinkLibraryDependencies=false
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor Catch2 v3.15.0 at external/catch2/</name>
  <files>external/catch2/catch_amalgamated.hpp, external/catch2/catch_amalgamated.cpp, external/catch2/LICENSE.txt</files>
  <read_first>
    PATTERNS.md §"external/catch2/catch_amalgamated.hpp + .cpp (vendor drop, flat layout)" — recommended layout (flat, with LICENSE.txt) mirrors `external/spdlog/`. Do NOT add a Catch2 vcxproj (unlike `external/imgui/imgui.vcxproj`); the amalgamated `.cpp` compiles as a `<ClCompile>` item inside `UtinniCore.Tests.vcxproj`.
    RESEARCH.md §"Installation" (lines 53-72) — exact download URLs for the v3.15.0 release assets (`catch_amalgamated.hpp` and `catch_amalgamated.cpp`). Tag verified live via GitHub releases API on 2026-05-23: `tag_name=v3.15.0`, `published_at=2026-05-12T11:19:23Z`.
    RESEARCH.md §"Security Domain" — V14 Configuration: capture SHA-256 of both files in the commit message that adds them (lightweight tamper-detection trail for future tag bumps).
  </read_first>
  <action>
    Create `external/catch2/` directory. Download the Catch2 v3.15.0 release assets from `https://github.com/catchorg/Catch2/releases/download/v3.15.0/catch_amalgamated.hpp` and `.cpp` into the new directory. Also fetch the Catch2 BSL-1.0 LICENSE.txt from the v3.15.0 source tree at `https://raw.githubusercontent.com/catchorg/Catch2/v3.15.0/LICENSE.txt` (recommended polish; matches `external/imgui/LICENSE.txt` + `external/spdlog/LICENSE` precedent — per PATTERNS.md "Notable difference").

    Do NOT graft an MIT header onto these files — they keep upstream BSL-1.0 (per PATTERNS.md "Shared Patterns > MIT Header" — `external/imgui/imgui.cpp` and `external/spdlog/spdlog.h` set the precedent).

    Do NOT modify the amalgamated files. Do NOT add a Catch2 vcxproj.

    On Windows PowerShell:
    - `New-Item -ItemType Directory -Path external/catch2 -Force`
    - `Invoke-WebRequest -Uri 'https://github.com/catchorg/Catch2/releases/download/v3.15.0/catch_amalgamated.hpp' -OutFile external/catch2/catch_amalgamated.hpp -UseBasicParsing`
    - `Invoke-WebRequest -Uri 'https://github.com/catchorg/Catch2/releases/download/v3.15.0/catch_amalgamated.cpp' -OutFile external/catch2/catch_amalgamated.cpp -UseBasicParsing`
    - `Invoke-WebRequest -Uri 'https://raw.githubusercontent.com/catchorg/Catch2/v3.15.0/LICENSE.txt' -OutFile external/catch2/LICENSE.txt -UseBasicParsing`

    After download, compute SHA-256 of both files (`Get-FileHash external/catch2/catch_amalgamated.hpp -Algorithm SHA256` and same for `.cpp`) and **record both hashes in the commit message body** when this task is committed. Format suggestion: `catch2 v3.15.0 amalgamated — sha256(hpp)=<...>, sha256(cpp)=<...>` (per Security Domain V14 / D-02).

    Implements: D-02 (Catch2 vendored amalgamated at specific tag), Security Domain V14 (pinned tag + integrity hash).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if ((Test-Path external/catch2/catch_amalgamated.hpp) -and (Test-Path external/catch2/catch_amalgamated.cpp) -and (Select-String -Path external/catch2/catch_amalgamated.hpp -Pattern 'Catch v3\.15\.0' -Quiet)) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>
    Three files exist under `external/catch2/`: `catch_amalgamated.hpp`, `catch_amalgamated.cpp`, `LICENSE.txt`. The `.hpp` contains the string `Catch v3.15.0` (the version banner Catch2 embeds in the header). SHA-256 hashes of the two amalgamated files are recorded in the commit message body. No Catch2 vcxproj exists under `external/catch2/`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create UtinniCore.Tests.vcxproj (triple-config Application) + main_smoke.cpp + register in Utinni.sln</name>
  <files>UtinniCore.Tests/UtinniCore.Tests.vcxproj, UtinniCore.Tests/main_smoke.cpp, UtinniCore.Tests/StringUtilityTests.cpp, Utinni.sln</files>
  <read_first>
    RESEARCH.md §D-03 (lines 762-909) — verbatim composite skeleton for `UtinniCore.Tests.vcxproj`. Copy this verbatim; replace only `{REPLACE-WITH-FRESH-GUID}` (4 occurrences) with a single fresh GUID generated via `[System.Guid]::NewGuid().ToString().ToUpper()`.
    RESEARCH.md §D-03 "Utinni.sln registration block" (lines 914-941) — verbatim project entry pattern + verbatim 6-line ProjectConfigurationPlatforms block. **The `RelWithDbgInfo|x86.ActiveCfg = RelWithDbgInfo|Win32` mapping (NOT `Release|Win32`) is load-bearing per D-07 / CON-T-02 / RESEARCH.md §Pitfall 1.**
    RESEARCH.md §"Code Examples > Standalone smoke test (05-01)" (lines 307-343) — verbatim 3 TEST_CASEs for `main_smoke.cpp`. Each TEST_CASE mitigates a specific Pitfall: REQUIRE(1+1==2) proves the amalgamated TU linked (Pitfall 1 — triple-config trap if it failed under a specific config); REQUIRE_THROWS_AS proves exception machinery works (Pitfall 3 — `SPDLOG_NO_EXCEPTIONS` confusion is not contagious); SECTION re-entry proves the BDD runner is sound (otherwise every multi-section seed test in 05-02 is unreliable).
    PATTERNS.md §"UtinniCore.Tests/UtinniCore.Tests.vcxproj" — composite analog: LoaderLockHarness for shape, UtinniCore for triple-config. Critical notable differences flagged: (1) add third RelWithDbgInfo config; (2) extend AdditionalIncludeDirectories with `$(SolutionDir)UtinniCore;` so tests can `#include "utility/string_utility.h"` directly; (3) WholeProgramOptimization=false (match UtinniCore.vcxproj, not LoaderLockHarness's true).
    PATTERNS.md §"Utinni.sln (modify — register new project + triple-config postSolution mappings)" — flags `Utinni.LoaderLockHarness` mapping (sln line 100-104) as the precedent NOT to follow. Five of the seven existing native projects in the sln mis-collapse RelWithDbgInfo→Release. Phase 5 follows the UtinniCore mapping (sln lines 82-87) which is the only correct precedent.
    PATTERNS.md §"UtinniCore.Tests/main_smoke.cpp" — apply 23-line MIT header per CONVENTIONS.md (copy verbatim from UtinniCore/utility/string_utility.h lines 1-23).

    Verify Pitfall 4 (test exe runs from wrong working dir in CI) mitigation is built in: the `--out` path in 05-01's CI lane (Task 3) is RELATIVE TO REPO ROOT (`UtinniCore.Tests\TestResults\junit-results.xml`), not relative to `bin\Release\`. This is intentional.
  </read_first>
  <action>
    **Sub-step A — Generate a fresh project GUID:**

    Run `[System.Guid]::NewGuid().ToString().ToUpper()` in PowerShell once and reuse the result everywhere in this task. Record the literal GUID in the commit message body so reviewers can audit.

    **Sub-step B — Create `UtinniCore.Tests/` directory + `UtinniCore.Tests.vcxproj`:**

    Copy the composite skeleton from RESEARCH.md lines 762-909 verbatim into `UtinniCore.Tests/UtinniCore.Tests.vcxproj`. Replace all 4 instances of `{REPLACE-WITH-FRESH-GUID}` with the GUID from sub-step A. Do not change anything else from the skeleton.

    Critical correctness checks (verify by reading the written file):
    - All three ProjectConfiguration entries exist (Debug|Win32, Release|Win32, RelWithDbgInfo|Win32) — per D-07 / CON-T-02.
    - All three Configuration PropertyGroups have `<ConfigurationType>Application</ConfigurationType>` (the canonical UtinniCore.vcxproj uses DynamicLibrary; this is the deliberate swap for a console-exe).
    - All three ItemDefinitionGroups have `<ConformanceMode>true</ConformanceMode>` — mitigates Pitfall 2 (UtinniCore.vcxproj sets Release to `false`, the test vcxproj follows LoaderLockHarness precedent of `true` everywhere; RESEARCH.md §Pitfall 2 confirms Catch2 v3 is clean under `/permissive-`).
    - AdditionalIncludeDirectories contains `$(SolutionDir)UtinniCore;` so tests can `#include "utility/string_utility.h"` (PATTERNS.md notable difference #2).
    - `<ClCompile Include="..\external\catch2\catch_amalgamated.cpp" />` is present.
    - `<ClCompile Include="..\UtinniCore\utility\string_utility.cpp" />` is present (compiled directly into the test exe so 05-02 can `#include "utility/string_utility.h"` without linking against UtinniCore.dll).
    - `<ClCompile Include="main_smoke.cpp" />` and `<ClCompile Include="StringUtilityTests.cpp" />` are BOTH present (StringUtilityTests.cpp is forward-declared in the vcxproj even though its source arrives in 05-02 — keeps the vcxproj stable across both plans; 05-02 creates the file).
    - `<ClInclude Include="..\external\catch2\catch_amalgamated.hpp" />` is present.
    - `<ProjectReference Include="..\UtinniCore\UtinniCore.vcxproj">` exists with `<LinkLibraryDependencies>false</LinkLibraryDependencies>` and the literal UtinniCore GUID `{AEFED7F6-4BA9-44FC-A353-71A463A82FDE}` in the `<Project>` element.
    - OutDir is `$(SolutionDir)bin\$(Configuration)\` for ALL THREE configs (PATTERNS.md "Shared Patterns > $(SolutionDir)bin\$(Configuration)\ OutDir convention").

    **Sub-step C — Add a placeholder `StringUtilityTests.cpp` so the Wave-1 build is clean:**

    Because the vcxproj references `StringUtilityTests.cpp` but 05-02 creates the actual file, add a one-line placeholder to `UtinniCore.Tests/StringUtilityTests.cpp` containing only the 23-line MIT header followed by `// Placeholder: real coverage lands in Plan 05-02.` and nothing else. This is the minimum to keep the vcxproj compilable in 05-01.

    Note: This placeholder is INSIDE 05-01's `files_modified` (explicitly listed in the frontmatter) by the by-product of the vcxproj's source list. 05-02 owns the file's content. Document this hand-off in the commit message body.

    **Sub-step D — Create `UtinniCore.Tests/main_smoke.cpp`:**

    Open `UtinniCore/utility/string_utility.h` and copy lines 1-23 (the 23-line MIT header block) verbatim as the new file's prologue.

    Then append the three TEST_CASEs verbatim from RESEARCH.md §"Code Examples > Standalone smoke test (05-01)" (lines 313-342):
    - `TEST_CASE("Smoke: vendored Catch2 runs", "[smoke]")` with `REQUIRE(1 + 1 == 2)` — proves amalgamated TU linked (Pitfall 1 mitigation).
    - `TEST_CASE("Smoke: exception machinery works", "[smoke]")` with `REQUIRE_THROWS_AS(...)` — proves Catch2's exception infrastructure works in this TU (Pitfall 3 mitigation).
    - `TEST_CASE("Smoke: SECTION re-entry produces fresh state", "[smoke]")` with two SECTIONs sharing a `counter` local — proves the BDD runner re-enters from the TEST_CASE top per SECTION (otherwise multi-section tests in 05-02 are unreliable).

    Include directive at the top of the source body (after the MIT header): `#include <catch2/catch_amalgamated.hpp>`. Note: the AdditionalIncludeDirectories `$(SolutionDir)external` plus the `<catch2/...>` form resolves to `external/catch2/catch_amalgamated.hpp` — this matches the file layout the executor created in Task 1.

    **Sub-step E — Register the new project in `Utinni.sln`:**

    Insert the new project entry block AFTER the existing `Utinni.LegacyPlugin` `EndProject` line (sln line 54) and BEFORE the `Utinni.Cli` project entry (line 55). The entry follows the Utinni.LoaderLockHarness shape (sln lines 40-44) with:
    - Project type GUID `{8BC9CEB8-8B4A-11D0-8D11-00A0C91BC942}` (well-known C++ type)
    - Project name `UtinniCore.Tests`
    - Path `UtinniCore.Tests\UtinniCore.Tests.vcxproj`
    - Project's own GUID (from sub-step A)
    - `ProjectSection(ProjectDependencies) = postProject` block with `{AEFED7F6-4BA9-44FC-A353-71A463A82FDE} = {AEFED7F6-4BA9-44FC-A353-71A463A82FDE}` (UtinniCore dep)

    Insert the 6-line ProjectConfigurationPlatforms entries for the new GUID into the `GlobalSection(ProjectConfigurationPlatforms) = postSolution` block. Mirror the **UtinniCore** mapping (sln lines 82-87), NOT the LoaderLockHarness mapping (sln line 104). The six lines are:
    ```
    {NEW-GUID}.Debug|x86.ActiveCfg = Debug|Win32
    {NEW-GUID}.Debug|x86.Build.0 = Debug|Win32
    {NEW-GUID}.Release|x86.ActiveCfg = Release|Win32
    {NEW-GUID}.Release|x86.Build.0 = Release|Win32
    {NEW-GUID}.RelWithDbgInfo|x86.ActiveCfg = RelWithDbgInfo|Win32
    {NEW-GUID}.RelWithDbgInfo|x86.Build.0 = RelWithDbgInfo|Win32
    ```
    Insert location for these 6 lines: a natural place is after the last existing `Utinni.Cli.Tests` block (currently ending at sln line 153). Order within the block does not affect MSBuild but matches sln-file convention if grouped near other native (Win32) project mappings.

    **Sub-step F — Build all three configs locally to verify CON-T-02 / D-07:**

    From the repo root, run:
    - `msbuild Utinni.sln /m /restore /p:Configuration=Debug /p:Platform=x86 /t:UtinniCore.Tests`
    - `msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86 /t:UtinniCore.Tests`
    - `msbuild Utinni.sln /m /restore /p:Configuration=RelWithDbgInfo /p:Platform=x86 /t:UtinniCore.Tests`

    All three must exit 0 and produce `bin\Debug\UtinniCore.Tests.exe`, `bin\Release\UtinniCore.Tests.exe`, `bin\RelWithDbgInfo\UtinniCore.Tests.exe` respectively. Run the Release exe with no args and verify the 3 smoke TEST_CASEs report `All tests passed`.

    Implements: D-03 (sibling vcxproj, no CMake/ctest), D-07 / CON-T-02 (triple-config preserved end-to-end), D-05 smoke set (3 TEST_CASEs proving build + exception + SECTION sanity).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86 /t:UtinniCore.Tests; if ($LASTEXITCODE -ne 0) { exit 1 }; msbuild Utinni.sln /m /restore /p:Configuration=Debug /p:Platform=x86 /t:UtinniCore.Tests; if ($LASTEXITCODE -ne 0) { exit 1 }; msbuild Utinni.sln /m /restore /p:Configuration=RelWithDbgInfo /p:Platform=x86 /t:UtinniCore.Tests; if ($LASTEXITCODE -ne 0) { exit 1 }; & 'bin\Release\UtinniCore.Tests.exe' --reporter console; if ($LASTEXITCODE -ne 0) { exit 1 }; if (-not (Select-String -Path Utinni.sln -Pattern 'UtinniCore\.Tests\\UtinniCore\.Tests\.vcxproj' -Quiet)) { exit 1 }; $relMatches = (Select-String -Path Utinni.sln -Pattern '\.RelWithDbgInfo\|x86\.ActiveCfg = RelWithDbgInfo\|Win32').Count; if ($relMatches -lt 2) { Write-Error \"Expected >=2 RelWithDbgInfo|x86.ActiveCfg = RelWithDbgInfo|Win32 mappings (UtinniCore + UtinniCore.Tests); found $relMatches. The LoaderLockHarness collapse pattern (RelWithDbgInfo->Release in postSolution) would silently let msbuild succeed but produce a Release-configured artifact — violating D-07 / CON-T-02.\"; exit 1 }; if (-not ((Test-Path 'UtinniCore.Tests\bin\Release\UtinniCore.Tests.pdb') -or (Test-Path 'bin\Release\UtinniCore.Tests.pdb'))) { Write-Error 'No Release pdb found at expected OutDir'; exit 1 }; if (-not ((Test-Path 'UtinniCore.Tests\bin\RelWithDbgInfo\UtinniCore.Tests.pdb') -or (Test-Path 'bin\RelWithDbgInfo\UtinniCore.Tests.pdb'))) { Write-Error 'No RelWithDbgInfo pdb found at expected OutDir'; exit 1 }; $relPdb = if (Test-Path 'bin\Release\UtinniCore.Tests.pdb') { 'bin\Release\UtinniCore.Tests.pdb' } else { 'UtinniCore.Tests\bin\Release\UtinniCore.Tests.pdb' }; $rwdiPdb = if (Test-Path 'bin\RelWithDbgInfo\UtinniCore.Tests.pdb') { 'bin\RelWithDbgInfo\UtinniCore.Tests.pdb' } else { 'UtinniCore.Tests\bin\RelWithDbgInfo\UtinniCore.Tests.pdb' }; $relHash = (Get-FileHash $relPdb -Algorithm SHA256).Hash; $rwdiHash = (Get-FileHash $rwdiPdb -Algorithm SHA256).Hash; if ($relHash -eq $rwdiHash) { Write-Error \"Release and RelWithDbgInfo pdbs are identical (sha256=$relHash). This means RelWithDbgInfo silently collapsed to Release — D-07 / CON-T-02 violated. Check Utinni.sln postSolution mappings for the LoaderLockHarness collapse anti-pattern.\"; exit 1 }; exit 0"</automated>
  </verify>
  <done>
    All four file artifacts exist and are well-formed:
    1. `UtinniCore.Tests/UtinniCore.Tests.vcxproj` — declares all three configs, ConfigurationType=Application for each, ConformanceMode=true everywhere, ProjectReference on UtinniCore with LinkLibraryDependencies=false.
    2. `UtinniCore.Tests/main_smoke.cpp` — 23-line MIT header + `#include <catch2/catch_amalgamated.hpp>` + 3 smoke TEST_CASEs from RESEARCH.md §"Standalone smoke test (05-01)".
    3. `UtinniCore.Tests/StringUtilityTests.cpp` — placeholder file with only the 23-line MIT header + a single comment line (`// Placeholder: real coverage lands in Plan 05-02.`). 05-02 replaces the content; this file must exist now so the vcxproj's `<ClCompile Include="StringUtilityTests.cpp" />` resolves.
    4. `Utinni.sln` — project entry inserted between Utinni.LegacyPlugin (line ~54) and Utinni.Cli (line ~55); the new GUID appears in 6 lines inside ProjectConfigurationPlatforms with full Debug+Release+RelWithDbgInfo mapping (each with both `.ActiveCfg` and `.Build.0`), with `RelWithDbgInfo|x86.ActiveCfg = RelWithDbgInfo|Win32` (NOT `Release|Win32`). The sln contains AT LEAST 2 lines matching `.RelWithDbgInfo|x86.ActiveCfg = RelWithDbgInfo|Win32` (one for UtinniCore, one for the new UtinniCore.Tests project).
    All three local MSBuild configs produce a working `UtinniCore.Tests.exe` at `bin\{Configuration}\`. The Release and RelWithDbgInfo pdbs are distinct (sha256 mismatch) — proving RelWithDbgInfo did not silently collapse to Release. `bin\Release\UtinniCore.Tests.exe --reporter console` exits 0 and reports `All tests passed (5 assertions in 3 test cases)` (3 smoke TEST_CASEs, 5 REQUIRE assertions — three REQUIREs in tests 1+3 plus two REQUIRE_THROWS_AS in test 2).
  </done>
</task>

<task type="auto">
  <name>Task 3: Add third CI lane in .github/workflows/ci.yml + verify on master</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    RESEARCH.md §D-04 "Proposed new Phase 5 step" (lines 980-998) — verbatim YAML for the two new steps to add to `ci.yml`. Insert AFTER the existing "Upload CLI test artifacts" step (currently ends at ci.yml line 108).
    PATTERNS.md §".github/workflows/ci.yml (modify — third CI lane)" — verbatim quote of Phase 4 D-11 step pair (lines 94-108) is the analog. Phase 5 pair mirrors structure exactly; only `name:`, `shell:`, `run:`, and artifact `name:` + `path:` differ. Critical differences flagged:
      1. `shell: pwsh` is explicit (Phase 4 doesn't set it because `dotnet test` is shell-agnostic; native exe invocation with backtick line-continuation needs PowerShell explicitly).
      2. NOT `dotnet test` — direct exe invocation (`& bin\Release\UtinniCore.Tests.exe ...`).
      3. Stacked `--reporter` flags (console + junit) per RESEARCH.md §Pitfall 5. The console reporter sets the exit code on failure (mitigates exit-code-swallow); the junit reporter writes XML for failure triage. **Do not drop either.**
      4. Artifact `name: native-test-results` (NOT `cli-test-results` — would collide with the Phase 4 artifact). RESEARCH.md §"Naming match with Phase 4" line 1009.
    RESEARCH.md §Pitfall 4 — `--out` path is relative to the exe's working directory in CI (repo root via PowerShell `&`). Use the absolute-from-repo-root form `UtinniCore.Tests\TestResults\junit-results.xml` so the artifact upload step can find it under that exact relative path.
    RESEARCH.md §"CI builds" (line 1003) — CI currently builds only `Release|x86` (ci.yml line 78). The Phase 5 lane MUST invoke `bin\Release\UtinniCore.Tests.exe`. Do NOT add separate Debug or RelWithDbgInfo build steps in CI; triple-config is exercised locally + at release-time per D-07.
  </read_first>
  <action>
    **Sub-step A — Append the two new steps to `.github/workflows/ci.yml`:**

    Insert the following YAML block immediately after the existing "Upload CLI test artifacts (on failure)" step (which currently ends at line 108). Match the existing two-space indentation under `steps:` exactly (each step starts with `      - name:`).

    ```yaml
          - name: Run native unit tests (UtinniCore.Tests.exe)
            shell: pwsh
            run: |
              # Phase 5 D-04: third CI lane gates `master` on the native test suite.
              # Direct exe invocation (not `dotnet test`) because the runner is Catch2 self-runner.
              # Stacked --reporter flags: console sets the exit code on failure (RESEARCH §Pitfall 5);
              # junit writes XML for triage. Output dir is created implicitly by Catch2 v3.
              & bin\Release\UtinniCore.Tests.exe `
                --reporter console `
                --reporter "junit::out=UtinniCore.Tests\TestResults\junit-results.xml"

          - name: Upload native test artifacts (on failure)
            if: failure()
            uses: actions/upload-artifact@v4
            with:
              name: native-test-results
              path: UtinniCore.Tests/TestResults/junit-results.xml
              if-no-files-found: warn
    ```

    Do NOT modify any existing CI lane. Do NOT add a Debug or RelWithDbgInfo build step to CI (D-04 keeps the existing single-config Release|x86 build matrix per RESEARCH.md §"CI builds").

    **Sub-step B — Soak the lane on `master` per D-04:**

    D-04 calls for "the new third lane is required for `master` green from day one" plus an implicit soak window before 05-02 builds on it. Push the changes to a feature branch first, open a PR, confirm the new step appears and the run is green. Then merge to master. After the master push, verify ONE green run on the master ci.yml page before considering this plan done.

    A **two-consecutive-green-pushes** soak (CONTEXT.md D-05 implicit, RESEARCH.md §"Smoke-test acceptance criteria" §6) is implicitly satisfied because 05-02 will be a second push to master that re-runs the same lane. If 05-02 fires the lane green, the soak is complete. If for any reason a second master push between 05-01 and 05-02 is desirable (e.g., a planned no-op edit to test the gate), the executor may add one — but this is NOT required.

    Implements: D-04 (CI lane required for master green from day one), RESEARCH.md §Pitfall 4 mitigation (relative-to-repo-root --out path), §Pitfall 5 mitigation (stacked reporters).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ciPath = '.github/workflows/ci.yml'; if (-not ((Select-String -Path $ciPath -Pattern 'UtinniCore\.Tests\.exe' -Quiet) -and (Select-String -Path $ciPath -Pattern '--reporter console' -Quiet) -and (Select-String -Path $ciPath -Pattern 'junit::out=UtinniCore\.Tests' -Quiet) -and (Select-String -Path $ciPath -Pattern 'name: native-test-results' -Quiet))) { Write-Error 'Required ci.yml patterns missing'; exit 1 }; $nativeLine = (Select-String -Path $ciPath -Pattern 'name: native-test-results' | Select-Object -First 1).LineNumber; $nativeContext = Get-Content $ciPath | Select-Object -Skip ($nativeLine - 6) -First 5; if (-not ($nativeContext -join \"`n\" -match 'if: failure\(\)')) { Write-Error 'native-test-results upload step missing if: failure() guard in the 5 lines preceding the artifact name'; exit 1 }; $cliLine = (Select-String -Path $ciPath -Pattern 'name: cli-test-results' | Select-Object -First 1).LineNumber; if (-not $cliLine) { Write-Error 'Could not locate existing cli-test-results upload step for ordering check'; exit 1 }; if ($nativeLine -le $cliLine) { Write-Error \"native-test-results step at line $nativeLine must come AFTER existing cli-test-results step at line $cliLine\"; exit 1 }; exit 0"</automated>
    <human-check>After merging this plan's PR to master, open the GitHub Actions tab and confirm the most-recent master commit's CI run shows the "Run native unit tests (UtinniCore.Tests.exe)" step finished green. This is the D-04 / D-05 CI-gate before 05-02 may begin.</human-check>
  </verify>
  <done>
    `.github/workflows/ci.yml` contains the two new steps in the correct location (after the existing Phase 4 "Upload CLI test artifacts" step — verified by line-number ordering: native-test-results step appears at a higher line number than cli-test-results). The "Run native unit tests" step uses `shell: pwsh`, invokes `bin\Release\UtinniCore.Tests.exe` directly (NOT `dotnet test`), uses stacked `--reporter console --reporter "junit::out=..."` flags, and writes XML to `UtinniCore.Tests\TestResults\junit-results.xml`. The "Upload native test artifacts" step uses `name: native-test-results` (no collision with Phase 4's `cli-test-results`), `if: failure()` (verified by context-aware regex within the 5 lines preceding the artifact name), and `if-no-files-found: warn`. The lane has run green on master at least once after the PR merges (human-verified on the GitHub Actions page).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Phase 5 has zero security surface — test-only Win32 console exe, runs on CI runner + dev machines, no network input, no untrusted data parsing, no public API (per RESEARCH.md §Security Domain). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Vendored Catch2 v3.15.0 amalgamated source (external/catch2/) | mitigate | ASVS V14 Configuration: pin to v3.15.0 tag in the commit message; capture SHA-256 of both catch_amalgamated.{hpp,cpp} in the commit message body for future tamper-detection (Task 1). Catchorg is the canonical org (Phil Nash, ~19k stars, two-decade-old project; not a slop risk). PGP signature verification deferred to Phase 6 STAB-03 (project does not yet have a key-trust chain). |
</threat_model>

<verification>
## Phase Checks

After this plan completes:

1. `external/catch2/catch_amalgamated.{hpp,cpp}` exist; `.hpp` banner contains `Catch v3.15.0`.
2. `msbuild Utinni.sln /p:Configuration={Debug,Release,RelWithDbgInfo} /p:Platform=x86 /t:UtinniCore.Tests` succeeds for all three configs locally.
3. `bin\Release\UtinniCore.Tests.exe --reporter console` exits 0 and reports `All tests passed (5 assertions in 3 test cases)`.
4. `Utinni.sln` contains a `UtinniCore.Tests` project entry with full Debug+Release+RelWithDbgInfo postSolution mappings (each with both `.ActiveCfg` and `.Build.0`; `RelWithDbgInfo|x86.ActiveCfg = RelWithDbgInfo|Win32`, NOT `Release|Win32`). Total count of `.RelWithDbgInfo|x86.ActiveCfg = RelWithDbgInfo|Win32` lines in the sln is >= 2.
5. The Release and RelWithDbgInfo pdbs for UtinniCore.Tests are distinct (sha256 mismatch) — confirms RelWithDbgInfo did not silently collapse to Release per the LoaderLockHarness anti-pattern.
6. `.github/workflows/ci.yml` contains "Run native unit tests" + "Upload native test artifacts" steps after the existing Phase 4 CLI artifact-upload step; native-test-results step's line number > cli-test-results step's line number; native-test-results upload step has `if: failure()` guard in its preceding 5 lines.
7. After PR merges to master, the new CI lane runs green (GitHub Actions page, human-verified).
</verification>

<success_criteria>
**05-01 succeeds when:**

- Catch2 v3.15.0 amalgamated files are vendored at `external/catch2/` with SHA-256 captured in the commit message body (D-02 + Security Domain V14).
- `UtinniCore.Tests.vcxproj` exists with all three configs (Debug + Release + RelWithDbgInfo), ConfigurationType=Application, ConformanceMode=true on all configs (D-03 / D-07 / CON-T-02 / Pitfall 2 mitigation).
- `Utinni.sln` registers the new project with the **UtinniCore-pattern** triple-config mapping (NOT the LoaderLockHarness collapsed pattern) (D-07 / CON-T-02). Positive assertion: >=2 lines matching `.RelWithDbgInfo|x86.ActiveCfg = RelWithDbgInfo|Win32` in the sln (UtinniCore + UtinniCore.Tests).
- The local triple-config build produces distinct Release and RelWithDbgInfo pdbs (sha256 mismatch) — proves RelWithDbgInfo is real, not silently collapsed.
- `main_smoke.cpp` ships 3 smoke TEST_CASEs that exercise the amalgamated build, exception machinery, and SECTION re-entry (D-05 smoke set / Pitfall 1+3 mitigations).
- `.github/workflows/ci.yml` contains a third lane with stacked `--reporter console --reporter junit::out=...` flags (D-04 / Pitfall 5 mitigation); the new step lands AFTER the existing Phase 4 cli-test-results upload step (verified by line-number ordering); the new upload step has `if: failure()` guard.
- The new CI lane has run green on master at least once (D-04 day-one gate; CI-gated boundary before 05-02 begins per D-05).
- Commit message body records: (a) the fresh project GUID generated for `UtinniCore.Tests.vcxproj`, (b) SHA-256 hashes of `external/catch2/catch_amalgamated.{hpp,cpp}`.
</success_criteria>

<output>
Create `.planning/phases/05-tier-1-c-unit-tests/05-01-SUMMARY.md` when done.

The SUMMARY.md should record:
- Fresh project GUID used for `UtinniCore.Tests.vcxproj`
- SHA-256 hashes of `external/catch2/catch_amalgamated.{hpp,cpp}` (per Security Domain V14)
- CI lane run URL on master (human-verified green)
- Any deviations from the verbatim RESEARCH.md / PATTERNS.md skeletons (expected: none)
- Hand-off note: `StringUtilityTests.cpp` placeholder created; 05-02 owns its content.
</output>
