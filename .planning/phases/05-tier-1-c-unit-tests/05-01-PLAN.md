---
phase: 05-tier-1-c-unit-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - external/catch2/catch_amalgamated.hpp
  - external/catch2/catch_amalgamated.cpp
  - external/catch2/LICENSE.txt
  - external/catch2/README.md
  - UtinniCore.Tests/UtinniCore.Tests.vcxproj
  - UtinniCore.Tests/main_smoke.cpp
  - UtinniCore.Tests/StringUtilityTests.cpp
  - Utinni.sln
  - .github/workflows/ci.yml
# NOTE: UtinniCore.Tests/StringUtilityTests.cpp is created by 05-01 as a
# minimal placeholder (MIT header + one comment line). Its real content
# (the max-harness TEST_CASEs — 4 top-level cases covering 6 conceptual
# coverage cases via Catch2 SECTIONs) is owned by 05-02. The placeholder
# must exist now so the vcxproj's <ClCompile Include="StringUtilityTests.cpp"/>
# resolves at 05-01 build time.
autonomous: true
requirements: [TEST-02]
tags: [catch2, c++, msbuild, ci]

must_haves:
  truths:
    - "Catch2 v3.15.0 is vendored at external/catch2/ as catch_amalgamated.{hpp,cpp} (per D-02; SHA-256 captured in commit message AND in external/catch2/README.md per Security Domain V14 + 05-REVIEWS.md item U6)"
    - "external/catch2/README.md exists with durable vendor metadata (version, source URL, license=BSL-1.0, vendored-date, SHA-256 of both amalgamated files) per 05-REVIEWS.md item U6 — commit-message hashes alone are fragile across squash/rewrite"
    - "UtinniCore.Tests.vcxproj declares Debug + Release + RelWithDbgInfo configurations end-to-end (per D-03 / D-07; CON-T-02 triple-config preserved)"
    - "UtinniCore.Tests is registered in Utinni.sln with full triple-config postSolution mappings (NOT the LoaderLockHarness RelWithDbgInfo->Release collapse pattern; per D-07) — verified by GUID-scoped 6-mapping match (per 05-REVIEWS.md item U5)"
    - "string_utility.cpp is NOT compiled into UtinniCore.Tests.vcxproj — header-only inline helpers only, eliminates the UTINNI_API/EXPORT_UTINNI dllimport conflict (per 05-REVIEWS.md item U1 / cursor-agent HIGH finding)"
    - "main_smoke.cpp (or a dedicated compile_smoke.cpp) #includes utility/string_utility.h to prove the header's include graph compiles cleanly under test-project preprocessor defines (per 05-REVIEWS.md item U1 compile-smoke check)"
    - "main_smoke.cpp contains 3 TEST_CASEs proving (a) the amalgamated build links and runs, (b) Catch2 exception machinery is functional, (c) SECTION re-entry produces fresh state (per D-05 smoke set in RESEARCH.md §D-05) — plus one extra include-smoke REQUIRE on stringUtility::toBool(\"true\") per 05-REVIEWS.md item U1"
    - "ci.yml gains a third lane invoking bin\\Release\\UtinniCore.Tests.exe with stacked --reporter console + junit::out=... (per D-04; mitigates RESEARCH.md §Pitfall 5 exit-code swallow)"
    - "CI lane gates master from day one — no warn-only posture (per D-04)"
    - "CI builds the test exe under all three configs (Debug + Release + RelWithDbgInfo) — Release runs the exe; Debug and RelWithDbgInfo are build-only verification per literal reading of ROADMAP success criterion #1 and 05-REVIEWS.md item C3"
    - "CI step creates UtinniCore.Tests\\TestResults\\ before exe invocation (New-Item -ItemType Directory -Force) — Catch2 std::ofstream reporters do not create parent dirs (per 05-REVIEWS.md item U4 / Codex HIGH finding)"
    - "Native-lane red-run validation is documented in 05-01-SUMMARY.md template: throwaway branch with REQUIRE(false), confirmed CI red + artifact upload, then revert (per 05-REVIEWS.md item U2 / Phase 1 precedent)"
    - "Triple-config gate (CON-T-02 / D-07) is enforced as an explicit acceptance criterion at scaffold time via GUID-scoped sln verification AND PDB SHA-256 mismatch check"
  artifacts:
    - path: "external/catch2/catch_amalgamated.hpp"
      provides: "Catch2 v3.15.0 amalgamated header"
      contains: "Catch2 v3.15.0"
    - path: "external/catch2/catch_amalgamated.cpp"
      provides: "Catch2 v3.15.0 amalgamated translation unit (provides main())"
      contains: "Catch2 v3.15.0"
    - path: "external/catch2/README.md"
      provides: "Durable vendor metadata: version, source URL, license, SHA-256 hashes, vendored-date (per 05-REVIEWS.md item U6)"
      contains: "v3.15.0"
    - path: "UtinniCore.Tests/UtinniCore.Tests.vcxproj"
      provides: "Sibling MSBuild test exe project, triple-config Application/Console"
      contains: "RelWithDbgInfo|Win32"
    - path: "UtinniCore.Tests/main_smoke.cpp"
      provides: "Vendor-drop sanity smoke tests + stringUtility include-graph compile smoke (per 05-REVIEWS.md item U1)"
      min_lines: 30
      contains: "TEST_CASE(\"Smoke:"
    - path: ".github/workflows/ci.yml"
      provides: "Third CI lane: Run native unit tests (UtinniCore.Tests.exe) with triple-config build verification (per 05-REVIEWS.md item C3)"
      contains: "UtinniCore.Tests.exe"
  key_links:
    - from: "UtinniCore.Tests/UtinniCore.Tests.vcxproj"
      to: "external/catch2/catch_amalgamated.cpp"
      via: "<ClCompile Include=\"..\\external\\catch2\\catch_amalgamated.cpp\" />"
      pattern: "catch_amalgamated\\.cpp"
    - from: "UtinniCore.Tests/UtinniCore.Tests.vcxproj"
      to: "UtinniCore/UtinniCore.vcxproj"
      via: "<ProjectReference> with LinkLibraryDependencies=false (build-order only, per LoaderLockHarness precedent)"
      pattern: "LinkLibraryDependencies>false"
    - from: "Utinni.sln"
      to: "UtinniCore.Tests.vcxproj"
      via: "Project entry + 6-line ProjectConfigurationPlatforms block (Debug+Release+RelWithDbgInfo, each with .ActiveCfg + .Build.0) — GUID-scoped 6-mapping verification per 05-REVIEWS.md item U5"
      pattern: "RelWithDbgInfo\\|x86\\.Build\\.0 = RelWithDbgInfo\\|Win32"
    - from: ".github/workflows/ci.yml"
      to: "bin\\Release\\UtinniCore.Tests.exe"
      via: "PowerShell step invoking the exe with stacked --reporter flags, preceded by New-Item directory-creation step and Debug/RelWithDbgInfo /t:UtinniCore.Tests build steps"
      pattern: "UtinniCore\\.Tests\\.exe"
---

<objective>
Land the **scaffold + smoke** half of Phase 5: vendor Catch2 v3.15.0, create the `UtinniCore.Tests.vcxproj` sibling MSBuild project with full triple-config (Debug + Release + RelWithDbgInfo), register it in `Utinni.sln` honouring CON-T-02 end-to-end, add 3 smoke tests proving the vendor drop works (plus one include-graph compile smoke for `utility/string_utility.h`), and wire a third lane into `.github/workflows/ci.yml` gating `master` from day one — with the third lane building the test exe under all three configs to satisfy ROADMAP success criterion #1 (per 05-REVIEWS.md item C3).

Purpose: Prove the build + CI green-on-master pipeline before any coverage value (D-05). Plan 05-02 is CI-gated — it does not start until this plan's CI lane is green on `master`.

Output: 7 new/modified files (Catch2 hpp/cpp/LICENSE/README, vcxproj, main_smoke.cpp, StringUtilityTests.cpp placeholder, sln, ci.yml); 1 new vendored dependency; 1 new MSBuild project; 1 new CI lane (with triple-config build verification + directory pre-creation per 05-REVIEWS.md items C3 + U4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-tier-1-c-unit-tests/05-CONTEXT.md
@.planning/phases/05-tier-1-c-unit-tests/05-RESEARCH.md
@.planning/phases/05-tier-1-c-unit-tests/05-PATTERNS.md
@.planning/phases/05-tier-1-c-unit-tests/05-VALIDATION.md
@.planning/phases/05-tier-1-c-unit-tests/05-REVIEWS.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/STRUCTURE.md

<interfaces>
<!-- Key contracts the executor needs. All quoted verbatim in PATTERNS.md / RESEARCH.md. -->

From UtinniCore/utility/string_utility.h (lines 1-23): 23-line MIT header block that every new C++ source/header file in this repo MUST carry. Apply to main_smoke.cpp + StringUtilityTests.cpp placeholder (NOT to the vendored Catch2 files — those keep their upstream BSL-1.0 header per PATTERNS.md "Shared Patterns > MIT Header").

From UtinniCore/utility/string_utility.h:35 — `UTINNI_API extern std::string toString(const std::wstring& wstr);`. **CRITICAL per 05-REVIEWS.md item U1:** This `UTINNI_API extern` declaration is the source of the dllimport linkage trap. When `EXPORT_UTINNI` is NOT defined in the test project, `UTINNI_API` expands to `__declspec(dllimport)`. If `string_utility.cpp` (which defines this overload locally) is compiled INTO the test exe, the link step sees both a dllimport declaration and a local definition for the same symbol — C4273 / inconsistent DLL linkage error. **Fix:** drop `string_utility.cpp` from the test vcxproj entirely; the seed tests only touch header-only inline helpers (`toBool`, `toString(int)`, `toHexString`, `trim/trimStart/trimEnd`). The wstring overload is NOT exercised by the seed.

From Utinni.LoaderLockHarness/Utinni.LoaderLockHarness.vcxproj — closest sibling console-exe precedent: ConfigurationType=Application, PlatformToolset=v142, SubSystem=Console, ProjectReference on UtinniCore with LinkLibraryDependencies=false, OutDir=$(SolutionDir)bin\$(Configuration)\. **Deficiency to fix:** only declares 2 of 3 configs; Phase 5 must add RelWithDbgInfo|Win32 (graft from UtinniCore.vcxproj lines 24-45). Sln mapping must be the UtinniCore pattern (sln lines 82-87, real RelWithDbgInfo|Win32 with .Build.0), NOT the LoaderLockHarness pattern (sln line 104 collapses RelWithDbgInfo→Release).

From Utinni.sln (read in full): 13 existing project entries. Well-known C++ project type GUID is `{8BC9CEB8-8B4A-11D0-8D11-00A0C91BC942}`. UtinniCore's own GUID is `{AEFED7F6-4BA9-44FC-A353-71A463A82FDE}` (used in the new vcxproj's ProjectReference and in the new sln entry's ProjectDependencies block). Recommended insertion point: after the existing Utinni.LegacyPlugin entry (ends at line 54), before Utinni.Cli entry (begins at line 55), per RESEARCH.md §D-03.

From .github/workflows/ci.yml (lines 94-108): Phase 4 D-11 step pair — "Run CLI golden tests" + "Upload CLI test artifacts (on failure)". The new Phase 5 step set inserts AFTER line 108. CI currently builds only `Release|x86` (line 78); the new lane invokes `bin\Release\UtinniCore.Tests.exe` BUT also adds targeted `msbuild ... /t:UtinniCore.Tests` build steps for Debug and RelWithDbgInfo (per 05-REVIEWS.md item C3 — satisfies ROADMAP success criterion #1 literal reading without adding test execution overhead). And a `New-Item -ItemType Directory -Force -Path UtinniCore.Tests\TestResults` step (per 05-REVIEWS.md item U4 — Catch2 std::ofstream reporters do NOT create parent dirs).

Composite skeleton for UtinniCore.Tests.vcxproj is in RESEARCH.md lines 762-909 (verbatim XML the executor copies, replacing only the `{REPLACE-WITH-FRESH-GUID}` placeholder). The skeleton already includes:
  - 3-config ProjectConfigurations block (Debug + Release + RelWithDbgInfo)
  - 3 Configuration PropertyGroups with ConfigurationType=Application
  - 3 ItemDefinitionGroups (the third — RelWithDbgInfo — has DebugInformationFormat=ProgramDatabase, Optimization=Disabled, IntrinsicFunctions=false; lines 863-882)
  - AdditionalIncludeDirectories: `$(SolutionDir);$(SolutionDir)external;$(SolutionDir)UtinniCore;$(ProjectDir);%(AdditionalIncludeDirectories)`
  - ConformanceMode=true on ALL three configs (mitigates RESEARCH.md §Pitfall 2)
  - <ClCompile Include="..\external\catch2\catch_amalgamated.cpp" />
  - <ClCompile Include="main_smoke.cpp" />
  - <ClCompile Include="StringUtilityTests.cpp" /> (file will be created by 05-02; declared up front so the vcxproj is stable across both plans)
  - **NOTE per 05-REVIEWS.md item U1:** Any `<ClCompile Include="..\UtinniCore\utility\string_utility.cpp" />` line in the RESEARCH.md skeleton MUST BE REMOVED before writing the vcxproj. The seed tests are header-only; including `string_utility.cpp` triggers UTINNI_API/EXPORT_UTINNI dllimport conflict.
  - <ProjectReference Include="..\UtinniCore\UtinniCore.vcxproj"> with LinkLibraryDependencies=false
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor Catch2 v3.15.0 at external/catch2/ + write README.md vendor metadata</name>
  <files>external/catch2/catch_amalgamated.hpp, external/catch2/catch_amalgamated.cpp, external/catch2/LICENSE.txt, external/catch2/README.md</files>
  <read_first>
    Per 05-REVIEWS.md item U6 (MEDIUM, Codex unique): SHA-256 in commit message is fragile across squash/rewrite. Add `external/catch2/README.md` with durable vendor metadata so the audit trail survives history rewrites.

    PATTERNS.md §"external/catch2/catch_amalgamated.hpp + .cpp (vendor drop, flat layout)" — recommended layout (flat, with LICENSE.txt) mirrors `external/spdlog/`. Do NOT add a Catch2 vcxproj (unlike `external/imgui/imgui.vcxproj`); the amalgamated `.cpp` compiles as a `<ClCompile>` item inside `UtinniCore.Tests.vcxproj`.
    RESEARCH.md §"Installation" (lines 53-72) — exact download URLs for the v3.15.0 release assets (`catch_amalgamated.hpp` and `catch_amalgamated.cpp`). Tag verified live via GitHub releases API on 2026-05-23: `tag_name=v3.15.0`, `published_at=2026-05-12T11:19:23Z`.
    RESEARCH.md §"Security Domain" — V14 Configuration: capture SHA-256 of both files in the commit message AND in external/catch2/README.md (lightweight tamper-detection trail for future tag bumps; README is more durable than commit-message metadata).
  </read_first>
  <action>
    Create `external/catch2/` directory. Download the Catch2 v3.15.0 release assets from `https://github.com/catchorg/Catch2/releases/download/v3.15.0/catch_amalgamated.hpp` and `.cpp` into the new directory. Also fetch the Catch2 BSL-1.0 LICENSE.txt from the v3.15.0 source tree at `https://raw.githubusercontent.com/catchorg/Catch2/v3.15.0/LICENSE.txt` (recommended polish; matches `external/imgui/LICENSE.txt` + `external/spdlog/LICENSE` precedent — per PATTERNS.md "Notable difference").

    Do NOT graft an MIT header onto these files — they keep upstream BSL-1.0 (per PATTERNS.md "Shared Patterns > MIT Header" — `external/imgui/imgui.cpp` and `external/spdlog/spdlog.h` set the precedent).

    Do NOT modify the amalgamated files. Do NOT add a Catch2 vcxproj.

    On Windows PowerShell:
    - `New-Item -ItemType Directory -Path external/catch2 -Force`
    - `Invoke-WebRequest -Uri 'https://github.com/catchorg/Catch2/releases/download/v3.15.0/catch_amalgamated.hpp' -OutFile external/catch2/catch_amalgamated.hpp -UseBasicParsing`
    - `Invoke-WebRequest -Uri 'https://github.com/catchorg/Catch2/releases/download/v3.15.0/catch_amalgamated.cpp' -OutFile external/catch2/catch_amalgamated.cpp -UseBasicParsing`
    - `Invoke-WebRequest -Uri 'https://raw.githubusercontent.com/catchorg/Catch2/v3.15.0/LICENSE.txt' -OutFile external/catch2/LICENSE.txt -UseBasicParsing`

    After download, compute SHA-256 of both amalgamated files (`Get-FileHash external/catch2/catch_amalgamated.hpp -Algorithm SHA256` and same for `.cpp`). Record both hashes in (a) the commit message body AND (b) the new `external/catch2/README.md` file (per 05-REVIEWS.md item U6 — commit-message hashes alone are fragile across squash/rewrite).

    **Create `external/catch2/README.md`** with the following exact content template (replace `{HASH_HPP}` and `{HASH_CPP}` with the captured SHA-256 hashes; replace `{DATE}` with the actual vendoring date in `yyyy-MM-dd` format via `Get-Date -Format yyyy-MM-dd`):

    ```markdown
    # Catch2 — Vendored Dependency

    **Version:** v3.15.0
    **Source URL:** https://github.com/catchorg/Catch2/releases/tag/v3.15.0
    **Amalgamated files:**
    - https://github.com/catchorg/Catch2/releases/download/v3.15.0/catch_amalgamated.hpp
    - https://github.com/catchorg/Catch2/releases/download/v3.15.0/catch_amalgamated.cpp
    **License:** Boost Software License 1.0 (BSL-1.0) — see `LICENSE.txt`
    **Vendored:** {DATE}
    **SHA-256 (catch_amalgamated.hpp):** {HASH_HPP}
    **SHA-256 (catch_amalgamated.cpp):** {HASH_CPP}
    **Why vendored, not vcpkg:** matches existing `external/` posture (CppSharp, DetourXS, ImGuizmo, LeksysINI, imgui, nvapi, spdlog all vendored); vcpkg adoption deferred to Phase 6 STAB-03 per 05-CONTEXT.md D-02.

    ## Integrity Verification

    To re-verify the vendored amalgamated files against the upstream tag:

    ```powershell
    Get-FileHash external/catch2/catch_amalgamated.hpp -Algorithm SHA256
    Get-FileHash external/catch2/catch_amalgamated.cpp -Algorithm SHA256
    ```

    Compare against the SHA-256 values above. Mismatch indicates tampering or accidental modification.
    ```

    Implements: D-02 (Catch2 vendored amalgamated at specific tag), Security Domain V14 (pinned tag + integrity hash), 05-REVIEWS.md item U6 (durable vendor metadata).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (-not (Test-Path external/catch2/catch_amalgamated.hpp)) { Write-Error 'missing catch_amalgamated.hpp'; exit 1 }; if (-not (Test-Path external/catch2/catch_amalgamated.cpp)) { Write-Error 'missing catch_amalgamated.cpp'; exit 1 }; if (-not (Test-Path external/catch2/LICENSE.txt)) { Write-Error 'missing LICENSE.txt'; exit 1 }; if (-not (Test-Path external/catch2/README.md)) { Write-Error 'missing README.md (05-REVIEWS.md item U6)'; exit 1 }; if (-not (Select-String -Path external/catch2/catch_amalgamated.hpp -Pattern 'Catch v3\.15\.0' -Quiet)) { Write-Error 'amalgamated.hpp missing version banner'; exit 1 }; $readme = Get-Content external/catch2/README.md -Raw; foreach ($pat in @('v3\.15\.0', 'BSL-1\.0', 'SHA-256')) { if (-not ($readme -match $pat)) { Write-Error \"README.md missing required pattern: $pat\"; exit 1 } }; exit 0"</automated>
  </verify>
  <done>
    Four files exist under `external/catch2/`: `catch_amalgamated.hpp`, `catch_amalgamated.cpp`, `LICENSE.txt`, `README.md`. The `.hpp` contains the string `Catch v3.15.0` (the version banner Catch2 embeds in the header). README.md contains the literal strings `v3.15.0`, `BSL-1.0`, `SHA-256`, and records the captured SHA-256 hashes + vendored date. SHA-256 hashes of the two amalgamated files are also recorded in the commit message body. No Catch2 vcxproj exists under `external/catch2/`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create UtinniCore.Tests.vcxproj (triple-config Application; string_utility.cpp EXCLUDED) + main_smoke.cpp + StringUtilityTests.cpp placeholder + register in Utinni.sln with GUID-scoped 6-mapping verification</name>
  <files>UtinniCore.Tests/UtinniCore.Tests.vcxproj, UtinniCore.Tests/main_smoke.cpp, UtinniCore.Tests/StringUtilityTests.cpp, Utinni.sln</files>
  <read_first>
    Per 05-REVIEWS.md item U1 (HIGH, cursor-agent unique): the `string_utility.h:35` wstring overload is `UTINNI_API extern`. Without `EXPORT_UTINNI` defined in the test project, `UTINNI_API` becomes `dllimport` while `string_utility.cpp` (if compiled locally) defines the symbol → C4273 / inconsistent DLL linkage at link time. **Decision (locked):** drop `string_utility.cpp` from `UtinniCore.Tests.vcxproj`. The seed tests cover header-only inline helpers; the wstring overload is not exercised.

    Per 05-REVIEWS.md item U1 compile-smoke check: `main_smoke.cpp` MUST `#include "utility/string_utility.h"` (with a single `REQUIRE(stringUtility::toBool("true") == true)` test case) to prove the header's include graph compiles cleanly under test-project preprocessor defines (no EXPORT_UTINNI, default `_DEBUG/_CONSOLE` or `NDEBUG/_CONSOLE`).

    Per 05-REVIEWS.md item U5 (MEDIUM, Codex unique): the sln verification must GUID-scope the 6-mapping match (capture the new project's GUID via regex from the project entry line, then assert all 6 `{GUID}.(Debug|Release|RelWithDbgInfo)|x86.(ActiveCfg|Build.0) = (Debug|Release|RelWithDbgInfo)|Win32` lines exist). The earlier "count >= 2 RelWithDbgInfo|Win32 mappings" approach can be REMOVED in favor of this stricter check, but the PDB SHA-256 mismatch check is RETAINED (it targets a different failure mode — silent RelWithDbgInfo→Release config collapse at build time).

    RESEARCH.md §D-03 (lines 762-909) — verbatim composite skeleton for `UtinniCore.Tests.vcxproj`. Copy this verbatim with TWO modifications: (1) replace `{REPLACE-WITH-FRESH-GUID}` (4 occurrences) with a single fresh GUID generated via `[System.Guid]::NewGuid().ToString().ToUpper()`; (2) per 05-REVIEWS.md item U1, REMOVE any `<ClCompile Include="..\UtinniCore\utility\string_utility.cpp" />` line and add an explanatory XML comment in its place.

    RESEARCH.md §D-03 "Utinni.sln registration block" (lines 914-941) — verbatim project entry pattern + verbatim 6-line ProjectConfigurationPlatforms block. **The `RelWithDbgInfo|x86.ActiveCfg = RelWithDbgInfo|Win32` mapping (NOT `Release|Win32`) is load-bearing per D-07 / CON-T-02 / RESEARCH.md §Pitfall 1.** The LoaderLockHarness collapse pattern at sln line 104 is what NOT to follow.

    RESEARCH.md §"Code Examples > Standalone smoke test (05-01)" (lines 307-343) — verbatim 3 TEST_CASEs for `main_smoke.cpp`. Each TEST_CASE mitigates a specific Pitfall: REQUIRE(1+1==2) proves the amalgamated TU linked (Pitfall 1 — triple-config trap if it failed under a specific config); REQUIRE_THROWS_AS proves exception machinery works (Pitfall 3 — `SPDLOG_NO_EXCEPTIONS` confusion is not contagious); SECTION re-entry proves the BDD runner is sound (otherwise every multi-section seed test in 05-02 is unreliable). Add a fourth TEST_CASE per 05-REVIEWS.md item U1: `TEST_CASE("Smoke: utility/string_utility.h include graph compiles", "[smoke]")` with `REQUIRE(stringUtility::toBool("true") == true)` — proves the test-project preprocessor defines do not break the `UTINNI_API`/dllimport include graph.

    PATTERNS.md §"UtinniCore.Tests/UtinniCore.Tests.vcxproj" — composite analog: LoaderLockHarness for shape, UtinniCore for triple-config. Critical notable differences flagged: (1) add third RelWithDbgInfo config; (2) extend AdditionalIncludeDirectories with `$(SolutionDir)UtinniCore;` so tests can `#include "utility/string_utility.h"` directly; (3) WholeProgramOptimization=false (match UtinniCore.vcxproj, not LoaderLockHarness's true).

    PATTERNS.md §"Utinni.sln (modify — register new project + triple-config postSolution mappings)" — flags `Utinni.LoaderLockHarness` mapping (sln line 100-104) as the precedent NOT to follow. Five of the seven existing native projects in the sln mis-collapse RelWithDbgInfo→Release. Phase 5 follows the UtinniCore mapping (sln lines 82-87) which is the only correct precedent.

    PATTERNS.md §"UtinniCore.Tests/main_smoke.cpp" — apply 23-line MIT header per CONVENTIONS.md (copy verbatim from UtinniCore/utility/string_utility.h lines 1-23).

    Verify Pitfall 4 (test exe runs from wrong working dir in CI) mitigation is built in: the `--out` path in 05-01's CI lane (Task 3) is RELATIVE TO REPO ROOT (`UtinniCore.Tests\TestResults\junit-results.xml`), not relative to `bin\Release\`. This is intentional.
  </read_first>
  <action>
    **Sub-step A — Generate a fresh project GUID:**

    Run `[System.Guid]::NewGuid().ToString().ToUpper()` in PowerShell once and reuse the result everywhere in this task. Record the literal GUID in the commit message body so reviewers can audit.

    **Sub-step B — Create `UtinniCore.Tests/` directory + `UtinniCore.Tests.vcxproj`:**

    Copy the composite skeleton from RESEARCH.md lines 762-909 into `UtinniCore.Tests/UtinniCore.Tests.vcxproj`. Apply two modifications:
    1. Replace all 4 instances of `{REPLACE-WITH-FRESH-GUID}` with the GUID from sub-step A.
    2. **Per 05-REVIEWS.md item U1:** REMOVE any `<ClCompile Include="..\UtinniCore\utility\string_utility.cpp" />` line if it appears in the RESEARCH.md skeleton. Replace with the following XML comment to make the exclusion intentional and reviewable:
       ```xml
       <!-- Header-only inline helpers; string_utility.cpp NOT included to avoid UTINNI_API/EXPORT_UTINNI link conflict (see 05-REVIEWS.md item U1). -->
       ```

    Critical correctness checks (verify by reading the written file):
    - All three ProjectConfiguration entries exist (Debug|Win32, Release|Win32, RelWithDbgInfo|Win32) — per D-07 / CON-T-02.
    - All three Configuration PropertyGroups have `<ConfigurationType>Application</ConfigurationType>`.
    - All three ItemDefinitionGroups have `<ConformanceMode>true</ConformanceMode>` — mitigates Pitfall 2.
    - AdditionalIncludeDirectories contains `$(SolutionDir)UtinniCore;` so tests can `#include "utility/string_utility.h"` (PATTERNS.md notable difference #2).
    - `<ClCompile Include="..\external\catch2\catch_amalgamated.cpp" />` is present.
    - **`<ClCompile Include="..\UtinniCore\utility\string_utility.cpp" />` is ABSENT** (per 05-REVIEWS.md item U1). The exclusion comment is present in its place.
    - `<ClCompile Include="main_smoke.cpp" />` and `<ClCompile Include="StringUtilityTests.cpp" />` are BOTH present (StringUtilityTests.cpp is forward-declared in the vcxproj even though its real source arrives in 05-02 — keeps the vcxproj stable across both plans; 05-01 creates a placeholder per sub-step C below).
    - `<ClInclude Include="..\external\catch2\catch_amalgamated.hpp" />` is present.
    - `<ProjectReference Include="..\UtinniCore\UtinniCore.vcxproj">` exists with `<LinkLibraryDependencies>false</LinkLibraryDependencies>` and the literal UtinniCore GUID `{AEFED7F6-4BA9-44FC-A353-71A463A82FDE}` in the `<Project>` element.
    - OutDir is `$(SolutionDir)bin\$(Configuration)\` for ALL THREE configs.

    **Sub-step C — Add a placeholder `StringUtilityTests.cpp` so the Wave-1 build is clean:**

    Because the vcxproj references `StringUtilityTests.cpp` but 05-02 creates the real content, add a placeholder to `UtinniCore.Tests/StringUtilityTests.cpp` containing only the 23-line MIT header followed by `// Placeholder: real coverage lands in Plan 05-02.` and nothing else. This is the minimum to keep the vcxproj compilable in 05-01. 05-02 owns the file's content.

    **Sub-step D — Create `UtinniCore.Tests/main_smoke.cpp` with 4 TEST_CASEs (3 smoke + 1 include-graph compile smoke per 05-REVIEWS.md item U1):**

    Open `UtinniCore/utility/string_utility.h` and copy lines 1-23 (the 23-line MIT header block) verbatim as the new file's prologue.

    Then add the includes (after the MIT header):
    ```cpp
    #include <catch2/catch_amalgamated.hpp>
    #include "utility/string_utility.h"   // 05-REVIEWS.md item U1 compile-smoke: verifies test-project preprocessor defines do not break the UTINNI_API include graph
    ```

    Then append the four TEST_CASEs:
    - `TEST_CASE("Smoke: vendored Catch2 runs", "[smoke]")` with `REQUIRE(1 + 1 == 2)` — proves amalgamated TU linked (Pitfall 1 mitigation). Verbatim from RESEARCH.md §"Code Examples > Standalone smoke test (05-01)" lines 313-318.
    - `TEST_CASE("Smoke: exception machinery works", "[smoke]")` with `REQUIRE_THROWS_AS(...)` — proves Catch2's exception infrastructure works in this TU (Pitfall 3 mitigation). Verbatim from RESEARCH.md lines 320-329.
    - `TEST_CASE("Smoke: SECTION re-entry produces fresh state", "[smoke]")` with two SECTIONs sharing a `counter` local — proves the BDD runner re-enters from the TEST_CASE top per SECTION. Verbatim from RESEARCH.md lines 331-342.
    - **NEW per 05-REVIEWS.md item U1:** `TEST_CASE("Smoke: utility/string_utility.h include graph compiles", "[smoke]")` with `REQUIRE(stringUtility::toBool("true") == true)` — proves the header's transitive includes (utinni.h, DetourXS, UtINI, log.h, memory.h, utility.h) compile under test-project preprocessor defines. **Why a single REQUIRE on toBool("true") only:** "true" parses as defined behavior in the current implementation (boolalpha extraction succeeds, result=true); the garbage-input case `toBool("garbage")` is UB until 05-02 patches `string_utility.h:44` per 05-REVIEWS.md item C1 — therefore not tested here. This TEST_CASE is purely an include-graph compile/link sanity check.

    **Sub-step E — Register the new project in `Utinni.sln`:**

    Insert the new project entry block AFTER the existing `Utinni.LegacyPlugin` `EndProject` line (sln line 54) and BEFORE the `Utinni.Cli` project entry (line 55). The entry follows the Utinni.LoaderLockHarness shape (sln lines 40-44) with:
    - Project type GUID `{8BC9CEB8-8B4A-11D0-8D11-00A0C91BC942}` (well-known C++ type)
    - Project name `UtinniCore.Tests`
    - Path `UtinniCore.Tests\UtinniCore.Tests.vcxproj`
    - Project's own GUID (from sub-step A)
    - `ProjectSection(ProjectDependencies) = postProject` block with `{AEFED7F6-4BA9-44FC-A353-71A463A82FDE} = {AEFED7F6-4BA9-44FC-A353-71A463A82FDE}` (UtinniCore dep)

    Insert the 6-line ProjectConfigurationPlatforms entries for the new GUID into the `GlobalSection(ProjectConfigurationPlatforms) = postSolution` block. Mirror the **UtinniCore** mapping (sln lines 82-87), NOT the LoaderLockHarness mapping (sln line 104). The six lines are:
    ```
    {NEW-GUID}.Debug|x86.ActiveCfg = Debug|Win32
    {NEW-GUID}.Debug|x86.Build.0 = Debug|Win32
    {NEW-GUID}.Release|x86.ActiveCfg = Release|Win32
    {NEW-GUID}.Release|x86.Build.0 = Release|Win32
    {NEW-GUID}.RelWithDbgInfo|x86.ActiveCfg = RelWithDbgInfo|Win32
    {NEW-GUID}.RelWithDbgInfo|x86.Build.0 = RelWithDbgInfo|Win32
    ```
    Insert location for these 6 lines: a natural place is after the last existing `Utinni.Cli.Tests` block (currently ending at sln line 153). Order within the block does not affect MSBuild but matches sln-file convention if grouped near other native (Win32) project mappings.

    **Sub-step F — Build all three configs locally to verify CON-T-02 / D-07:**

    From the repo root, run:
    - `msbuild Utinni.sln /m /restore /p:Configuration=Debug /p:Platform=x86 /t:UtinniCore.Tests`
    - `msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86 /t:UtinniCore.Tests`
    - `msbuild Utinni.sln /m /restore /p:Configuration=RelWithDbgInfo /p:Platform=x86 /t:UtinniCore.Tests`

    All three must exit 0 and produce `bin\Debug\UtinniCore.Tests.exe`, `bin\Release\UtinniCore.Tests.exe`, `bin\RelWithDbgInfo\UtinniCore.Tests.exe` respectively. Run the Release exe with no args and verify all smoke TEST_CASEs PASS (exit code 0, Catch2 reports `All tests passed`). Per 05-REVIEWS.md Codex LOW item: do NOT assert a specific assertion count — exit code 0 + `All tests passed` is the success signal.

    Implements: D-03 (sibling vcxproj, no CMake/ctest), D-07 / CON-T-02 (triple-config preserved end-to-end), D-05 smoke set (3 smoke TEST_CASEs + 1 include-graph compile smoke per 05-REVIEWS.md item U1).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86 /t:UtinniCore.Tests; if ($LASTEXITCODE -ne 0) { exit 1 }; msbuild Utinni.sln /m /restore /p:Configuration=Debug /p:Platform=x86 /t:UtinniCore.Tests; if ($LASTEXITCODE -ne 0) { exit 1 }; msbuild Utinni.sln /m /restore /p:Configuration=RelWithDbgInfo /p:Platform=x86 /t:UtinniCore.Tests; if ($LASTEXITCODE -ne 0) { exit 1 }; $exeOutput = & 'bin\Release\UtinniCore.Tests.exe' --reporter console 2>&1; if ($LASTEXITCODE -ne 0) { Write-Error \"Smoke exe non-zero exit: $LASTEXITCODE\"; exit 1 }; if (-not ($exeOutput -match 'All tests passed')) { Write-Error 'Smoke exe did not report All tests passed (per 05-REVIEWS Codex LOW item)'; exit 1 }; $vcxproj = Get-Content UtinniCore.Tests/UtinniCore.Tests.vcxproj -Raw; if ($vcxproj -match 'ClCompile\s+Include=\"\.\.\\UtinniCore\\utility\\string_utility\.cpp\"') { Write-Error 'string_utility.cpp is included in vcxproj (per 05-REVIEWS.md item U1 it MUST be excluded)'; exit 1 }; if (-not ($vcxproj -match '05-REVIEWS\.md item U1')) { Write-Error 'vcxproj missing 05-REVIEWS.md item U1 exclusion comment'; exit 1 }; $smoke = Get-Content UtinniCore.Tests/main_smoke.cpp -Raw; if (-not ($smoke -match '#include\s+\"utility/string_utility\.h\"')) { Write-Error 'main_smoke.cpp missing #include \"utility/string_utility.h\" (per 05-REVIEWS.md item U1 compile-smoke check)'; exit 1 }; if (-not ($smoke -match 'stringUtility::toBool\(\"true\"\)')) { Write-Error 'main_smoke.cpp missing stringUtility::toBool(\"true\") include-graph REQUIRE (per 05-REVIEWS.md item U1)'; exit 1 }; $sln = Get-Content Utinni.sln -Raw; if (-not ($sln -match 'UtinniCore\.Tests\\UtinniCore\.Tests\.vcxproj')) { Write-Error 'sln missing UtinniCore.Tests project entry'; exit 1 }; $entryMatch = [regex]::Match($sln, 'Project\(\"\{[0-9A-F-]+\}\"\) = \"UtinniCore\.Tests\", \"UtinniCore\.Tests\\\\UtinniCore\.Tests\.vcxproj\", \"(\{[0-9A-F-]+\})\"'); if (-not $entryMatch.Success) { Write-Error 'Could not extract UtinniCore.Tests project GUID from sln entry'; exit 1 }; $guid = $entryMatch.Groups[1].Value; $guidEsc = [regex]::Escape($guid); $expected = @(\"$guid.Debug|x86.ActiveCfg = Debug|Win32\", \"$guid.Debug|x86.Build.0 = Debug|Win32\", \"$guid.Release|x86.ActiveCfg = Release|Win32\", \"$guid.Release|x86.Build.0 = Release|Win32\", \"$guid.RelWithDbgInfo|x86.ActiveCfg = RelWithDbgInfo|Win32\", \"$guid.RelWithDbgInfo|x86.Build.0 = RelWithDbgInfo|Win32\"); $missing = @(); foreach ($line in $expected) { if (-not $sln.Contains($line)) { $missing += $line } }; if ($missing.Count -gt 0) { Write-Error (\"Missing GUID-scoped sln mappings (per 05-REVIEWS.md item U5): \" + ($missing -join '; ')); exit 1 }; if (-not ((Test-Path 'bin\Release\UtinniCore.Tests.pdb') -or (Test-Path 'UtinniCore.Tests\bin\Release\UtinniCore.Tests.pdb'))) { Write-Error 'No Release pdb found at expected OutDir'; exit 1 }; if (-not ((Test-Path 'bin\RelWithDbgInfo\UtinniCore.Tests.pdb') -or (Test-Path 'UtinniCore.Tests\bin\RelWithDbgInfo\UtinniCore.Tests.pdb'))) { Write-Error 'No RelWithDbgInfo pdb found at expected OutDir'; exit 1 }; $relPdb = if (Test-Path 'bin\Release\UtinniCore.Tests.pdb') { 'bin\Release\UtinniCore.Tests.pdb' } else { 'UtinniCore.Tests\bin\Release\UtinniCore.Tests.pdb' }; $rwdiPdb = if (Test-Path 'bin\RelWithDbgInfo\UtinniCore.Tests.pdb') { 'bin\RelWithDbgInfo\UtinniCore.Tests.pdb' } else { 'UtinniCore.Tests\bin\RelWithDbgInfo\UtinniCore.Tests.pdb' }; $relHash = (Get-FileHash $relPdb -Algorithm SHA256).Hash; $rwdiHash = (Get-FileHash $rwdiPdb -Algorithm SHA256).Hash; if ($relHash -eq $rwdiHash) { Write-Error \"Release and RelWithDbgInfo pdbs are identical (sha256=$relHash). RelWithDbgInfo silently collapsed to Release; D-07 / CON-T-02 violated.\"; exit 1 }; exit 0"</automated>
  </verify>
  <done>
    All four file artifacts exist and are well-formed:
    1. `UtinniCore.Tests/UtinniCore.Tests.vcxproj` — declares all three configs, ConfigurationType=Application for each, ConformanceMode=true everywhere, ProjectReference on UtinniCore with LinkLibraryDependencies=false. **`string_utility.cpp` is NOT in the source list** (per 05-REVIEWS.md item U1); an XML comment referencing `05-REVIEWS.md item U1` documents the deliberate exclusion.
    2. `UtinniCore.Tests/main_smoke.cpp` — 23-line MIT header + `#include <catch2/catch_amalgamated.hpp>` + `#include "utility/string_utility.h"` (per 05-REVIEWS.md item U1 compile-smoke check) + 4 smoke TEST_CASEs (3 from RESEARCH.md §"Standalone smoke test (05-01)" + 1 include-graph compile smoke with `REQUIRE(stringUtility::toBool("true") == true)`).
    3. `UtinniCore.Tests/StringUtilityTests.cpp` — placeholder file with only the 23-line MIT header + a single comment line (`// Placeholder: real coverage lands in Plan 05-02.`). 05-02 replaces the content.
    4. `Utinni.sln` — project entry inserted between Utinni.LegacyPlugin and Utinni.Cli; the new GUID appears in **all 6 expected postSolution lines** (Debug|x86.ActiveCfg/Build.0 = Debug|Win32; Release|x86.ActiveCfg/Build.0 = Release|Win32; RelWithDbgInfo|x86.ActiveCfg/Build.0 = RelWithDbgInfo|Win32). The GUID-scoped 6-mapping verification (per 05-REVIEWS.md item U5) replaces the earlier "count >= 2" check.
    All three local MSBuild configs produce a working `UtinniCore.Tests.exe` at `bin\{Configuration}\`. The Release and RelWithDbgInfo pdbs are distinct (sha256 mismatch) — proving RelWithDbgInfo did not silently collapse to Release. `bin\Release\UtinniCore.Tests.exe --reporter console` exits 0 and reports `All tests passed` (per 05-REVIEWS.md Codex LOW: no hardcoded assertion count).
  </done>
</task>

<task type="auto">
  <name>Task 3: Add third CI lane in .github/workflows/ci.yml (triple-config build + directory pre-creation + Release-only exe invocation) + verify on master + document red-run validation</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    Per 05-REVIEWS.md item C3 (HIGH-MEDIUM, both reviewers): ROADMAP success criterion #1 says "builds in CI under all three native configs". The new CI lane MUST add explicit `msbuild .../t:UtinniCore.Tests` steps for Debug AND RelWithDbgInfo (in addition to the existing Release|x86 solution build) to satisfy the literal reading. Only Release invokes the `.exe`; Debug + RelWithDbgInfo are build-only verification. Pattern: 3 msbuild steps + 1 directory-creation step + 1 exe-invocation step + 1 artifact-upload step.

    Per 05-REVIEWS.md item U4 (HIGH, Codex unique): Catch2's `std::ofstream` reporter does NOT create parent directories. The CI step MUST prepend a `New-Item -ItemType Directory -Force -Path UtinniCore.Tests\TestResults` step BEFORE the `.exe` invocation.

    Per 05-REVIEWS.md item U2 (HIGH, cursor-agent unique): Phase 1 set the precedent for red-on-failure validation; this lane MUST be exercised once with a deliberate `REQUIRE(false)` on a throwaway branch to confirm CI goes red + artifact uploads. This is a documented human procedure (NOT an autonomous task), recorded in 05-01-SUMMARY.md template.

    Per 05-REVIEWS.md Codex LOW item: don't hardcode smoke-test assertion counts ("5 assertions in 3 test cases"). Success criterion is exit 0 + `All tests passed` — not a specific number.

    RESEARCH.md §D-04 "Proposed new Phase 5 step" (lines 980-998) — verbatim YAML; insert AFTER the existing "Upload CLI test artifacts" step (currently ends at ci.yml line 108). The Phase 5 plan ADDS to this skeleton: triple-config build steps + directory pre-creation step.
    PATTERNS.md §".github/workflows/ci.yml (modify — third CI lane)" — verbatim quote of Phase 4 D-11 step pair (lines 94-108) is the analog. Critical differences flagged:
      1. `shell: pwsh` is explicit (native exe invocation with backtick line-continuation needs PowerShell explicitly).
      2. NOT `dotnet test` — direct exe invocation (`& bin\Release\UtinniCore.Tests.exe ...`).
      3. Stacked `--reporter` flags (console + junit) per RESEARCH.md §Pitfall 5. **Do not drop either.**
      4. Artifact `name: native-test-results` (NOT `cli-test-results` — collision with Phase 4).
    RESEARCH.md §Pitfall 4 — `--out` path is relative to the exe's working directory in CI (repo root via PowerShell `&`). Use the absolute-from-repo-root form `UtinniCore.Tests\TestResults\junit-results.xml`.
  </read_first>
  <action>
    **Sub-step A — Append the following step block to `.github/workflows/ci.yml`** immediately after the existing "Upload CLI test artifacts (on failure)" step (currently ends at line 108). Match the existing two-space indentation under `steps:` exactly (each step starts with `      - name:`):

    ```yaml
          - name: Build native tests (Debug|x86) — triple-config verification per 05-REVIEWS.md item C3
            shell: pwsh
            run: msbuild Utinni.sln /m /p:Configuration=Debug /p:Platform=x86 /t:UtinniCore.Tests
            # Targeted build (only the test exe project), not full solution rebuild.
            # Satisfies ROADMAP Phase 5 success criterion #1 literal reading: "builds in CI under all three native configs."

          - name: Build native tests (RelWithDbgInfo|x86) — triple-config verification per 05-REVIEWS.md item C3
            shell: pwsh
            run: msbuild Utinni.sln /m /p:Configuration=RelWithDbgInfo /p:Platform=x86 /t:UtinniCore.Tests
            # Targeted build (only the test exe project). RelWithDbgInfo is the CON-T-02 / D-07 config most prone to
            # silent collapse-to-Release at the sln-mapping layer; building it in CI catches regressions early.

          - name: Create native test results directory (per 05-REVIEWS.md item U4)
            working-directory: ${{ github.workspace }}
            shell: pwsh
            run: New-Item -ItemType Directory -Force -Path UtinniCore.Tests\TestResults | Out-Null
            # Catch2's std::ofstream-based junit reporter does NOT create parent dirs. Codex review (HIGH) caught this.

          - name: Run native unit tests (UtinniCore.Tests.exe, Release|x86)
            shell: pwsh
            run: |
              # Phase 5 D-04: third CI lane gates `master` on the native test suite.
              # Direct exe invocation (not `dotnet test`) because the runner is Catch2 self-runner.
              # Stacked --reporter flags: console sets the exit code on failure (RESEARCH §Pitfall 5);
              # junit writes XML for triage. Output dir was created in the preceding step.
              # Only Release runs the exe; Debug + RelWithDbgInfo are build-only verification per 05-REVIEWS.md item C3.
              & bin\Release\UtinniCore.Tests.exe `
                --reporter console `
                --reporter "junit::out=UtinniCore.Tests\TestResults\junit-results.xml"

          - name: Upload native test artifacts (on failure)
            if: failure()
            uses: actions/upload-artifact@v4
            with:
              name: native-test-results
              path: UtinniCore.Tests/TestResults/junit-results.xml
              if-no-files-found: warn
    ```

    Do NOT modify any existing CI lane.

    **Sub-step B — Soak the lane on `master` per D-04:**

    D-04 calls for "the new third lane is required for `master` green from day one". Push the changes to a feature branch first, open a PR, confirm the new steps appear and the run is green. Then merge to master. After the master push, verify ONE green run on the master ci.yml page before considering this plan done.

    **Sub-step C — Red-run validation procedure (documented in 05-01-SUMMARY.md template per 05-REVIEWS.md item U2):**

    This is a one-shot manual procedure executed by the user AFTER the autonomous tasks complete and the initial green CI run is confirmed on master. It is NOT an autonomous task; the lane initially lands green. The procedure:

    1. Create a throwaway branch from master (e.g., `phase05-redrun-validation`).
    2. Edit `UtinniCore.Tests/main_smoke.cpp` — add a single line `REQUIRE(false);` inside the first TEST_CASE body.
    3. Commit + push the throwaway branch (do NOT open a PR to master; this branch must NEVER merge).
    4. Open the GitHub Actions run for the throwaway branch push.
    5. **Confirm three observations:**
       - The "Run native unit tests (UtinniCore.Tests.exe, Release|x86)" step is RED (exit non-zero).
       - The "Upload native test artifacts (on failure)" step ran and uploaded `native-test-results` artifact containing `junit-results.xml`.
       - The workflow's overall conclusion is "failure".
    6. Delete the throwaway branch (no merge; revert is unnecessary since master never touched the failure).
    7. Record the throwaway branch name + the red CI run URL + the green-after-revert master CI run URL in `05-01-SUMMARY.md` under a "Red-run validation" section. Mark the section "✅ confirmed".

    This procedure is the documented `<human-check>` for Task 3 below; the task itself remains `autonomous: true` because the autonomous portion (msbuild + exe invocation + artifact upload wiring) is fully Claude-executable.

    Implements: D-04 (CI lane required for master green from day one), 05-REVIEWS.md item C3 (triple-config CI build), 05-REVIEWS.md item U4 (directory pre-creation), 05-REVIEWS.md item U2 (red-run validation documented), 05-REVIEWS.md Codex LOW (no hardcoded assertion count), RESEARCH.md §Pitfall 4 + §Pitfall 5 mitigations.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ciPath = '.github/workflows/ci.yml'; $ci = Get-Content $ciPath -Raw; $required = @{ 'UtinniCore.Tests.exe' = 'exe invocation present'; '--reporter console' = 'stacked console reporter present'; 'junit::out=UtinniCore\.Tests' = 'junit reporter present'; 'name: native-test-results' = 'native artifact upload present'; 'Configuration=Debug' = '05-REVIEWS C3: Debug build step present'; 'Configuration=RelWithDbgInfo' = '05-REVIEWS C3: RelWithDbgInfo build step present'; '/t:UtinniCore\.Tests' = '05-REVIEWS C3: targeted test build present'; 'New-Item -ItemType Directory -Force -Path UtinniCore\.Tests\\TestResults' = '05-REVIEWS U4: directory pre-creation present' }; foreach ($pat in $required.Keys) { if (-not ($ci -match $pat)) { Write-Error \"Missing: $pat ($($required[$pat]))\"; exit 1 } }; $nativeLine = (Select-String -Path $ciPath -Pattern 'name: native-test-results' | Select-Object -First 1).LineNumber; $nativeContext = Get-Content $ciPath | Select-Object -Skip ($nativeLine - 6) -First 5; if (-not ($nativeContext -join \"`n\" -match 'if: failure\(\)')) { Write-Error 'native-test-results upload step missing if: failure() guard'; exit 1 }; $cliLine = (Select-String -Path $ciPath -Pattern 'name: cli-test-results' | Select-Object -First 1).LineNumber; if (-not $cliLine) { Write-Error 'Could not locate existing cli-test-results upload step'; exit 1 }; if ($nativeLine -le $cliLine) { Write-Error \"native-test-results step at line $nativeLine must come AFTER cli-test-results step at line $cliLine\"; exit 1 }; $debugStepLine = (Select-String -Path $ciPath -Pattern 'Configuration=Debug.*UtinniCore\.Tests' | Select-Object -First 1).LineNumber; if ($debugStepLine -le $cliLine) { Write-Error \"Debug build step must come AFTER existing cli-test-results step (Phase 5 lane ordering)\"; exit 1 }; $dirCreateLine = (Select-String -Path $ciPath -Pattern 'New-Item -ItemType Directory' | Select-Object -First 1).LineNumber; $exeLine = (Select-String -Path $ciPath -Pattern 'bin\\\\Release\\\\UtinniCore\.Tests\.exe' | Select-Object -First 1).LineNumber; if ($dirCreateLine -ge $exeLine) { Write-Error \"Directory creation step (line $dirCreateLine) must come BEFORE exe invocation (line $exeLine) per 05-REVIEWS.md item U4\"; exit 1 }; exit 0"</automated>
    <human-check>After merging this plan's PR to master, open the GitHub Actions tab and confirm the most-recent master commit's CI run shows all four new Phase 5 steps (Debug build, RelWithDbgInfo build, directory creation, native test run) finished green. This is the D-04 / D-05 CI-gate before 05-02 may begin. Separately, after green is confirmed, perform the red-run validation procedure described in 05-01-PLAN.md Task 3 Sub-step C: create a throwaway branch with `REQUIRE(false);` in main_smoke.cpp, push, confirm CI goes RED with artifact upload, delete the branch. Record both green and red CI run URLs in 05-01-SUMMARY.md per 05-REVIEWS.md item U2.</human-check>
  </verify>
  <done>
    `.github/workflows/ci.yml` contains the five new steps (in this order, after the existing Phase 4 "Upload CLI test artifacts" step):
    1. "Build native tests (Debug|x86)" — `msbuild ... /p:Configuration=Debug /t:UtinniCore.Tests` (05-REVIEWS C3)
    2. "Build native tests (RelWithDbgInfo|x86)" — `msbuild ... /p:Configuration=RelWithDbgInfo /t:UtinniCore.Tests` (05-REVIEWS C3)
    3. "Create native test results directory" — `New-Item -ItemType Directory -Force -Path UtinniCore.Tests\TestResults` (05-REVIEWS U4)
    4. "Run native unit tests (UtinniCore.Tests.exe, Release|x86)" — direct exe invocation, stacked reporters (D-04 + Pitfall 5)
    5. "Upload native test artifacts (on failure)" — `name: native-test-results`, `if: failure()`, `if-no-files-found: warn`

    Verified by:
    - Line-number ordering: all five steps appear after the Phase 4 `cli-test-results` step.
    - Directory pre-creation step appears BEFORE exe invocation.
    - Artifact upload step has `if: failure()` guard in the preceding 5 lines.
    - Required regex patterns present: `Configuration=Debug`, `Configuration=RelWithDbgInfo`, `/t:UtinniCore.Tests`, `New-Item -ItemType Directory -Force -Path UtinniCore.Tests\\TestResults`.

    The lane has run green on master at least once after the PR merges (human-verified on the GitHub Actions page). The red-run validation procedure (per 05-REVIEWS.md item U2) has been performed and its results recorded in 05-01-SUMMARY.md under "Red-run validation" with both the red CI run URL (throwaway branch with REQUIRE(false)) and the green master CI run URL.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Phase 5 has zero security surface — test-only Win32 console exe, runs on CI runner + dev machines, no network input, no untrusted data parsing, no public API (per RESEARCH.md §Security Domain). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Vendored Catch2 v3.15.0 amalgamated source (external/catch2/) | mitigate | ASVS V14 Configuration: pin to v3.15.0 tag in the commit message; capture SHA-256 of both catch_amalgamated.{hpp,cpp} in the commit message body AND in external/catch2/README.md (per 05-REVIEWS.md item U6 — durable across squash/rewrite). Catchorg is the canonical org (Phil Nash, ~19k stars, two-decade-old project; not a slop risk). PGP signature verification deferred to Phase 6 STAB-03. |
</threat_model>

<verification>
## Phase Checks

After this plan completes:

1. `external/catch2/catch_amalgamated.{hpp,cpp}` exist; `.hpp` banner contains `Catch v3.15.0`. `external/catch2/README.md` exists with `v3.15.0`, `BSL-1.0`, `SHA-256` literal strings (per 05-REVIEWS.md item U6).
2. `msbuild Utinni.sln /p:Configuration={Debug,Release,RelWithDbgInfo} /p:Platform=x86 /t:UtinniCore.Tests` succeeds for all three configs locally.
3. `bin\Release\UtinniCore.Tests.exe --reporter console` exits 0 and reports `All tests passed` (no hardcoded assertion count per 05-REVIEWS.md Codex LOW).
4. `Utinni.sln` contains a `UtinniCore.Tests` project entry whose GUID appears in ALL 6 expected postSolution lines (Debug|x86.ActiveCfg/Build.0 = Debug|Win32; Release|x86.ActiveCfg/Build.0 = Release|Win32; RelWithDbgInfo|x86.ActiveCfg/Build.0 = RelWithDbgInfo|Win32) — GUID-scoped match per 05-REVIEWS.md item U5.
5. `UtinniCore.Tests/UtinniCore.Tests.vcxproj` does NOT include `string_utility.cpp` as a `<ClCompile>` item; an explanatory XML comment referencing `05-REVIEWS.md item U1` documents the exclusion.
6. `UtinniCore.Tests/main_smoke.cpp` `#includes "utility/string_utility.h"` and has a REQUIRE on `stringUtility::toBool("true")` (compile-smoke per 05-REVIEWS.md item U1).
7. The Release and RelWithDbgInfo pdbs for UtinniCore.Tests are distinct (sha256 mismatch) — confirms RelWithDbgInfo did not silently collapse to Release.
8. `.github/workflows/ci.yml` contains five new Phase 5 steps in the correct order: Debug build, RelWithDbgInfo build, directory creation, native test run, artifact upload (per 05-REVIEWS.md items C3 + U4).
9. After PR merges to master, the new CI lane runs green (GitHub Actions page, human-verified).
10. Red-run validation per 05-REVIEWS.md item U2 has been performed: throwaway branch with REQUIRE(false) confirmed CI red + artifact upload, branch deleted, master ran green after PR merge. Both CI run URLs recorded in 05-01-SUMMARY.md.
</verification>

<success_criteria>
**05-01 succeeds when:**

- Catch2 v3.15.0 amalgamated files are vendored at `external/catch2/` with SHA-256 captured in both the commit message body AND `external/catch2/README.md` (D-02 + Security Domain V14 + 05-REVIEWS.md item U6).
- `UtinniCore.Tests.vcxproj` exists with all three configs (Debug + Release + RelWithDbgInfo), ConfigurationType=Application, ConformanceMode=true on all configs, and does NOT include `string_utility.cpp` as a `<ClCompile>` item (per 05-REVIEWS.md item U1).
- `Utinni.sln` registers the new project with full GUID-scoped 6-mapping triple-config (Debug+Release+RelWithDbgInfo, each with .ActiveCfg + .Build.0) verified by extracting the project's GUID from the sln entry and asserting all 6 expected postSolution lines exist (per 05-REVIEWS.md item U5).
- The local triple-config build produces distinct Release and RelWithDbgInfo pdbs (sha256 mismatch) — proves RelWithDbgInfo is real, not silently collapsed.
- `main_smoke.cpp` ships 4 TEST_CASEs: 3 smoke from RESEARCH.md (amalgamated linkage, exception machinery, SECTION re-entry) + 1 include-graph compile smoke with `REQUIRE(stringUtility::toBool("true") == true)` (per 05-REVIEWS.md item U1).
- `.github/workflows/ci.yml` contains FIVE new Phase 5 steps in the correct order (Debug build, RelWithDbgInfo build, directory creation, native test run, artifact upload) — per 05-REVIEWS.md items C3 + U4. The triple-config build steps satisfy ROADMAP success criterion #1 literal reading.
- The new CI lane has run green on master at least once (D-04 day-one gate).
- Red-run validation has been performed and documented (per 05-REVIEWS.md item U2): throwaway branch with REQUIRE(false), confirmed CI red + artifact upload, branch deleted. Both CI run URLs recorded in 05-01-SUMMARY.md.
- Commit message body records: (a) the fresh project GUID generated for `UtinniCore.Tests.vcxproj`, (b) SHA-256 hashes of `external/catch2/catch_amalgamated.{hpp,cpp}`.
- Smoke-test verification uses `All tests passed` + exit code 0 — NO hardcoded assertion count (per 05-REVIEWS.md Codex LOW item).
</success_criteria>

<output>
Create `.planning/phases/05-tier-1-c-unit-tests/05-01-SUMMARY.md` when done.

The SUMMARY.md template should include:

```markdown
## Phase 5 / Plan 01 Summary

- **Fresh project GUID used for `UtinniCore.Tests.vcxproj`:** {GUID}
- **SHA-256 hashes:**
  - `external/catch2/catch_amalgamated.hpp`: {HASH_HPP}
  - `external/catch2/catch_amalgamated.cpp`: {HASH_CPP}
  - (also recorded in `external/catch2/README.md` per 05-REVIEWS.md item U6)
- **Triple-config build (per 05-REVIEWS.md item C3):**
  - CI Debug build (UtinniCore.Tests target): {green/red + run URL}
  - CI RelWithDbgInfo build (UtinniCore.Tests target): {green/red + run URL}
  - CI Release build + exe invocation: {green/red + run URL}
- **CI master green run URL:** {URL}
- **Red-run validation (per 05-REVIEWS.md item U2):**
  - Status: ❌ pending / ✅ confirmed
  - Throwaway branch name: {name}
  - Red CI run URL (REQUIRE(false) confirmed CI red + artifact upload): {URL}
  - Green master CI run URL (post-merge, baseline): {URL}
- **Deviations from RESEARCH.md / PATTERNS.md skeletons:** (expected per 05-REVIEWS.md item U1: `string_utility.cpp` excluded from vcxproj; document any others)
- **Hand-off note:** `StringUtilityTests.cpp` placeholder created; 05-02 owns its content.
```
</output>
