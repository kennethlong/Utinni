---
phase: 05-tier-1-c-unit-tests
plan: 02
type: execute
wave: 2
depends_on: ['05-01']
files_modified:
  - UtinniCore/utility/string_utility.h
  - UtinniCore.Tests/StringUtilityTests.cpp
  - docs/ai/test-harness-plan.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [TEST-02]
tags: [catch2, c++, stringUtility, seed-coverage, max-harness]

must_haves:
  truths:
    - "stringUtility seed coverage is the locked D-01 pick — chosen because Phase 4 D-06 moved TRE/IFF parsers to managed (per CONTEXT.md D-01 deferred-to-research, resolved in RESEARCH.md §D-01 decision table)"
    - "string_utility.h:44 is patched to initialize `bool result = false;` (was uninitialized) BEFORE the garbage-input TEST_CASEs land — eliminates the toBool UB that both reviewers flagged (per 05-REVIEWS.md item C1 / HIGH). The patch is Wave-1 within 05-02 so the test source is never in a state where it asserts UB."
    - "StringUtilityTests.cpp ships 4 top-level TEST_CASEs covering 6 conceptual coverage cases via Catch2 SECTIONs — toBool / toString(int,fillCount) / toHexString / trim+trimStart+trimEnd (per 05-REVIEWS.md item C2 normalization: language is consistent across frontmatter / objective / action / verify / source comments / success criteria)"
    - "Each TEST_CASE has a documented failure mode that would silently corrupt PluginManager::loadPlugins if reverted — max-harness per D-06 (per RESEARCH.md §Code Examples > Failure modes table lines 425-432)"
    - "After the C1 patch, the `toBool` garbage-input assertions (e.g., `toBool(\"garbage\") == false`) are real regression coverage — they catch the (now-defined) extraction-fails-returns-false behavior, not undefined behavior"
    - "Tests #include 'utility/string_utility.h' directly (header-only inline; string_utility.cpp is NOT compiled into the test exe per 05-REVIEWS.md item U1). No link against UtinniCore.dll required."
    - "docs/ai/test-harness-plan.md 'Tier 1 — C++ side' row is dispositioned as 'Closed by Phase 5' with references to D-01..D-04 (per CONTEXT.md domain 'In scope' line 19)"
    - ".planning/REQUIREMENTS.md §TEST-02 acceptance text is updated to remove the `ctest` reference (per 05-REVIEWS.md item U3 / Cursor MEDIUM finding) — D-03 superseded ctest with MSBuild + direct-exe; the requirement text should match the locked decision"
    - "Code review (/gsd:code-review 05) is invoked at phase end to confirm no new critical findings (per CONTEXT.md domain 'In scope' line 20)"
    - "TEST-02 ROADMAP success criterion #2 ('at least one UtinniCore parser or helper has Catch2 coverage') is satisfied by the 4 TEST_CASEs (6 conceptual cases via SECTIONs) landing in this plan"
  artifacts:
    - path: "UtinniCore/utility/string_utility.h"
      provides: "Patched `bool result = false;` initializer at line 44 — eliminates toBool UB (per 05-REVIEWS.md item C1)"
      contains: "bool result = false"
    - path: "UtinniCore.Tests/StringUtilityTests.cpp"
      provides: "Native seed coverage for stringUtility helpers — 4 top-level TEST_CASEs covering 6 conceptual cases via Catch2 SECTIONs (boolalpha round-trip, zero-pad, hex, trim)"
      min_lines: 80
      contains: "TEST_CASE(\"stringUtility::"
    - path: "docs/ai/test-harness-plan.md"
      provides: "Tier 1 — C++ side row updated to closed-by-Phase-5"
      contains: "Closed in Phase 5"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TEST-02 acceptance text updated to remove ctest reference (per 05-REVIEWS.md item U3)"
      contains: "MSBuild"
  key_links:
    - from: "UtinniCore.Tests/StringUtilityTests.cpp"
      to: "UtinniCore/utility/string_utility.h"
      via: "#include \"utility/string_utility.h\""
      pattern: "#include \"utility/string_utility\\.h\""
    - from: "UtinniCore.Tests/StringUtilityTests.cpp"
      to: "external/catch2/catch_amalgamated.hpp"
      via: "#include <catch2/catch_amalgamated.hpp>"
      pattern: "#include <catch2/catch_amalgamated\\.hpp>"
    - from: "docs/ai/test-harness-plan.md"
      to: ".planning/phases/05-tier-1-c-unit-tests/"
      via: "Cross-reference in the closed-out row + Suggested phase order item 3"
      pattern: "Phase 5"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/05-tier-1-c-unit-tests/05-CONTEXT.md"
      via: "TEST-02 acceptance text references D-03 / MSBuild + direct-exe (no ctest)"
      pattern: "MSBuild"
---

<objective>
Land the **seed coverage** half of Phase 5: patch `UtinniCore/utility/string_utility.h:44` to initialize `bool result = false;` (per 05-REVIEWS.md item C1 / HIGH — eliminates the toBool UB both reviewers caught), then add 4 top-level TEST_CASEs covering 6 conceptual coverage cases via Catch2 SECTIONs in `UtinniCore.Tests/StringUtilityTests.cpp`. Coverage spans `stringUtility::toBool`, `toString(int, fillCount)`, `toHexString`, and `trim`/`trimStart`/`trimEnd`. Each test catches a specific reversion failure mode that would silently corrupt `PluginManager::loadPlugins` — D-06 max-harness posture. Close out the `docs/ai/test-harness-plan.md` "Tier 1 — C++ side" row, update `.planning/REQUIREMENTS.md` §TEST-02 to remove the stale `ctest` reference (per 05-REVIEWS.md item U3), and trigger phase-end code review.

Purpose: Satisfy ROADMAP Phase 5 success criterion #2 ("at least one UtinniCore parser or helper has Catch2 coverage") with max-harness rigor — every TEST_CASE would fail if the covered function were reverted to a broken state, not ceremonial smoke (per D-06 + RESEARCH.md §D-01 candidate table verdict). The C1 patch makes the garbage-input assertions test DEFINED behavior (boolalpha extraction failure → return false), turning them from UB-assertions into real regression coverage.

Output: 1 production code patch (string_utility.h:44 — 4-char change per 05-REVIEWS.md item C1), 1 new test file (~80-100 lines), 1 docs update, 1 requirements update, 1 phase-end code review checkpoint.

**Wave-gate prerequisite:** 05-01's third CI lane is green on `master` AND the red-run validation has been performed (per 05-REVIEWS.md item U2). This plan does not start until both gates are satisfied (per CONTEXT.md D-05 / D-04 "CI-gated plan boundary").

**Intra-plan wave ordering (per 05-REVIEWS.md item C1):** Task 1 (the C1 patch to string_utility.h) MUST land BEFORE Task 2 (the TEST_CASE additions). The test source must never be in a state where it asserts UB against the un-patched header.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-tier-1-c-unit-tests/05-CONTEXT.md
@.planning/phases/05-tier-1-c-unit-tests/05-RESEARCH.md
@.planning/phases/05-tier-1-c-unit-tests/05-PATTERNS.md
@.planning/phases/05-tier-1-c-unit-tests/05-VALIDATION.md
@.planning/phases/05-tier-1-c-unit-tests/05-REVIEWS.md
@.planning/phases/05-tier-1-c-unit-tests/05-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@docs/ai/test-harness-plan.md

<interfaces>
<!-- Quoted production code (verified live in RESEARCH.md §"Recommended Pick" lines 598-624). -->

From UtinniCore/utility/string_utility.h (header-only inline) — the API under test:

```cpp
// line 37-42:
inline std::string toString(bool b)
{
    std::ostringstream oss;
    oss << std::boolalpha << b;
    return oss.str();
}

// line 44-49 — PRE-PATCH (current state, has UB on extraction failure):
inline bool toBool(const std::string& input)
{
    bool result;                                          // <-- UB: uninitialized
    std::istringstream(input) >> std::boolalpha >> result;
    return result;                                        // <-- returns indeterminate value if extraction failed
}

// line 44-49 — POST-PATCH (after Task 1 lands per 05-REVIEWS.md item C1):
inline bool toBool(const std::string& input)
{
    bool result = false;                                  // <-- initialized: extraction failure now deterministically returns false
    std::istringstream(input) >> std::boolalpha >> result;
    return result;
}

// line 51-56 (overloaded — int version):
inline std::string toString(int i, int digitFillCount = 0)
{
    std::ostringstream oss;
    oss << std::setw(digitFillCount) << std::setfill('0') << i;
    return oss.str();
}

// Additional functions covered by the seed (all inline in header):
// - toHexString(uint32_t, int width)            — lowercase hex with zero padding
// - trim(std::string&)                          — strips both ends, IN-PLACE mutation
// - trimStart(std::string&)                     — strips leading whitespace
// - trimEnd(std::string&)                       — strips trailing whitespace
//
// NOT covered (out-of-scope for Phase 5; surfaced as Phase 6 STAB-03 input by Cursor LOW review):
// - trim_copy / trim_start_copy / trim_end_copy in string_utility.h:89-102 — names appear swapped
//   or logic appears incomplete (trim_copy only calls trimStart; trimStart_copy only calls trimEnd; trimEnd_copy
//   calls full trim). Out-of-scope for this seed because PluginManager idiom uses the non-_copy variants which
//   behave correctly. See 05-02-SUMMARY.md "Phase 6 STAB-03 Inputs" section for the deferred finding.
```

From UtinniCore/plugin_framework/plugin_manager.cpp:121-138 — the load-bearing consumer that motivates D-06 max-harness selection:

```cpp
// PluginManager::loadPlugins parses lines like:
//   [Plugins]
//   plugin_00 = true , MyPlugin
//   plugin_01 = false, OtherPlugin
// Uses both stringUtility::toBool and stringUtility::trim:
//   stringUtility::toBool(stringUtility::trim(isEnabled))
//   stringUtility::trim(directoryName)
// And the writer side uses toString(int, 2) to zero-pad the ordinal:
//   key = "plugin_" + stringUtility::toString(i, 2)
// A regression in any of toBool / toString / trim silently corrupts the [Plugins] block on write
// and/or read, dropping every plugin from the priority list.
```

From .planning/REQUIREMENTS.md §TEST-02 (lines 50-54) — pre-update text:

```
- [ ] **TEST-02 — Tier 1 C++ unit-test scaffold**
  - **Source:** test-harness-plan.md "Tier 1"
  - **Statement:** Catch2 (header-only) wired through `ctest` or `vcpkg` for UtinniCore. Folds in once UtinniCore has the refactored seams produced by STAB-02 (R-A..R-H).
  - **Acceptance:** Catch2 builds in CI; at least one native parser or helper has coverage; CI runs both `dotnet test` and `ctest` and is green on master.
  - **Milestone:** V1
```

Per 05-REVIEWS.md item U3: the `ctest` references must be removed. D-03 chose MSBuild + direct-exe; the acceptance text should match.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch UtinniCore/utility/string_utility.h:44 — initialize `bool result = false;` (per 05-REVIEWS.md item C1 / HIGH)</name>
  <files>UtinniCore/utility/string_utility.h</files>
  <read_first>
    Per 05-REVIEWS.md item C1 (HIGH, both reviewers): `UtinniCore/utility/string_utility.h:44` currently declares `bool result;` (uninitialized) and `std::istringstream(input) >> std::boolalpha >> result;` leaves `result` indeterminate on extraction failure. The 05-02 seed tests for garbage inputs (e.g., `toBool("garbage") == false`) currently assert UB. The fix is to add the `= false` initializer: `bool result = false;`. This turns garbage-input behavior from UB into DEFINED ("extraction fails → return false") — making the garbage-input TEST_CASEs in Task 2 real regression coverage instead of UB-assertions.

    **Why this is Wave-1 within 05-02:** the patch MUST land before Task 2 adds the garbage-input TEST_CASEs to StringUtilityTests.cpp, so the test source is never in a state where it asserts UB against the un-patched header.

    The current code (verified live in `UtinniCore/utility/string_utility.h`):

    ```cpp
    // Lines 44-49 (verbatim):
    inline bool toBool(const std::string& input)
    {
        bool result;
        std::istringstream(input) >> std::boolalpha >> result;
        return result;
    }
    ```

    The post-patch code (single change — 4 characters added: ` = false`):

    ```cpp
    inline bool toBool(const std::string& input)
    {
        bool result = false;
        std::istringstream(input) >> std::boolalpha >> result;
        return result;
    }
    ```

    The acceptance criteria below assert BOTH a positive grep (the initialized form exists) AND a negative grep (the uninitialized form `bool result;` does NOT exist) to prevent accidental partial application.
  </read_first>
  <action>
    Open `UtinniCore/utility/string_utility.h`. Locate line 46 (currently: `    bool result;`). Replace it with `    bool result = false;` (same indentation; 4-space indent preserved).

    Do NOT modify any other lines. Do NOT add comments justifying the change (the commit message body is the appropriate place for that explanation; in-line comments noise up the production header).

    Implements: 05-REVIEWS.md item C1 (HIGH, both reviewers — eliminates toBool UB).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$h = Get-Content UtinniCore/utility/string_utility.h -Raw; if (-not ($h -match 'bool result = false;')) { Write-Error 'Positive check failed: UtinniCore/utility/string_utility.h does NOT contain `bool result = false;` (05-REVIEWS.md item C1 patch not applied)'; exit 1 }; if ($h -match '(?m)^\s*bool result;\s*$') { Write-Error 'Negative check failed: UtinniCore/utility/string_utility.h still contains an uninitialized `bool result;` line (05-REVIEWS.md item C1 patch incomplete or partially reverted)'; exit 1 }; msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86 /t:UtinniCore.Tests; if ($LASTEXITCODE -ne 0) { Write-Error 'Test exe rebuild failed after C1 patch'; exit 1 }; exit 0"</automated>
  </verify>
  <done>
    `UtinniCore/utility/string_utility.h` line containing the `toBool` local declaration reads `    bool result = false;` (initialized). The unmodified form (`    bool result;` on its own line) is no longer present in the file. The `UtinniCore.Tests` target rebuilds clean under Release|x86 (the production code is header-only inline; this change affects every TU that includes `string_utility.h`, but `toBool` is also called only by `PluginManager::loadPlugins` and the smoke test's include-graph compile check from 05-01 — both will compile cleanly with the new initializer).
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace placeholder StringUtilityTests.cpp with 4 top-level TEST_CASEs covering 6 conceptual cases via SECTIONs (per 05-REVIEWS.md item C2 count normalization)</name>
  <files>UtinniCore.Tests/StringUtilityTests.cpp</files>
  <read_first>
    Per 05-REVIEWS.md item C2 (HIGH, both reviewers): all language must be normalized to "**4 top-level TEST_CASEs covering 6 conceptual coverage cases via Catch2 SECTIONs**". The prior plan iteration drifted between "6 TEST_CASEs" (frontmatter) and "4 top-level TEST_CASEs" (action). This task's action text, the embedded source comments, and the verify regex MUST all use the normalized language.

    Per 05-REVIEWS.md item C1: Task 1 has already patched `string_utility.h:44` to `bool result = false;`. The garbage-input TEST_CASEs in this task (e.g., `toBool("garbage") == false`) now test DEFINED behavior: boolalpha extraction failure → return false. Add an inline comment in the relevant TEST_CASE body explaining: "// Per 05-REVIEWS.md item C1, string_utility.h:44 initializes `bool result = false;` so extraction failure deterministically returns false. This SECTION catches a regression that drops the initializer."

    RESEARCH.md §"Code Examples > Seed coverage (05-02) — stringUtility round-trip" (lines 346-422) — verbatim 4 top-level TEST_CASEs (the term "6 TEST_CASEs" used in earlier RESEARCH.md prose refers to the 6 conceptual coverage cases distributed across the 4 TEST_CASEs via Catch2 SECTIONs). Copy verbatim into the new file body.

    RESEARCH.md §"Code Examples > Failure modes each test catches (per D-06 max-harness)" (lines 425-432) — the 4-row table mapping each TEST_CASE to the specific PluginManager regression it catches. **Embed this table as a comment block at the top of the test source** so reviewers can audit max-harness compliance without leaving the file (per CONTEXT.md D-06 explicit requirement).

    PATTERNS.md §"UtinniCore.Tests/StringUtilityTests.cpp" — tag convention `[utility][string]` per RESEARCH.md §"Tag Taxonomy" line 1024. Apply 23-line MIT header per CONVENTIONS.md.

    PATTERNS.md §"Shared Patterns > MIT Header" — copy the 23-line block verbatim from `UtinniCore/utility/string_utility.h:1-23`. This was already done for `main_smoke.cpp` in 05-01; mirror exactly.

    Note on file state: 05-01 created `UtinniCore.Tests/StringUtilityTests.cpp` as a placeholder containing only the MIT header + a single `// Placeholder: ...` comment line. 05-02 REPLACES the file content — the vcxproj's `<ClCompile Include="StringUtilityTests.cpp" />` already wires it into the build, no vcxproj edit is needed.

    Note on `trim_*_copy` helpers (per 05-REVIEWS.md Cursor LOW): `string_utility.h:89-102` has `trim_copy` (calls only trimStart), `trimStart_copy` (calls only trimEnd), `trimEnd_copy` (calls full trim) — names appear swapped or logic incomplete. This is OUT-OF-SCOPE for the 05-02 seed (PluginManager idiom uses the non-`_copy` variants which behave correctly). The deferred finding is recorded in 05-02-SUMMARY.md (Task 3 output section) for Phase 6 STAB-03 follow-up.
  </read_first>
  <action>
    **Sub-step A — Overwrite `UtinniCore.Tests/StringUtilityTests.cpp` with the seed coverage:**

    Open the existing placeholder file (created by 05-01 Task 2 — contains only the 23-line MIT header + one placeholder comment). Replace its entire content with:

    1. **Lines 1-23: 23-line MIT header block** — copy verbatim from `UtinniCore/utility/string_utility.h:1-23`. This matches what 05-01's `main_smoke.cpp` carries.

    2. **Blank line, then header comment block (~18 lines) documenting D-06 max-harness compliance + the 05-REVIEWS.md item C2 count normalization:**

       ```
       // D-06 max-harness compliance: each TEST_CASE below catches a specific
       // PluginManager::loadPlugins regression. If any covered helper were reverted
       // to the broken state in column 2, the corresponding TEST_CASE would FAIL.
       //
       // Per 05-REVIEWS.md item C2: this file contains 4 top-level TEST_CASEs
       // covering 6 conceptual coverage cases via Catch2 SECTIONs. Do NOT
       // describe this file as "6 TEST_CASEs" anywhere; the count language is
       // normalized to "4 TEST_CASEs / 6 conceptual cases via SECTIONs".
       //
       // | Test                                                | Failure caught                                                                                                                                                                              |
       // |-----------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
       // | toBool("true") == true                              | std::boolalpha regression -> every plugin treated as disabled                                                                                                                               |
       // | toString(0, 2) == "00"                              | std::setfill('0') regression -> writes "plugin_ 0", next-startup read fails to find "plugin_00", drops every plugin from priority list                                                       |
       // | toHexString(0xDEADBEEF, 8) == "deadbeef"            | std::hex regression -> writes decimal "3735928559" for every logged RVA; log lines become un-greppable                                                                                       |
       // | trim(" hello ") == "hello"                          | find_first_not_of operator inversion -> trim(" hello ") returns " "; PluginManager's trim(directoryName) corrupts every plugin dir string                                                    |
       //
       // Source: 05-RESEARCH.md §"Code Examples > Failure modes each test catches" (lines 425-432).
       // Consumer (PluginManager::loadPlugins): UtinniCore/plugin_framework/plugin_manager.cpp:121-138.
       ```

    3. **Includes (after the documentation comment):**

       ```cpp
       #include <catch2/catch_amalgamated.hpp>
       #include "utility/string_utility.h"
       ```

    4. **The 4 top-level TEST_CASEs — copy verbatim from RESEARCH.md lines 356-421:**

       - `TEST_CASE("stringUtility::toBool round-trip via boolalpha", "[utility][string]")` — 3 SECTIONs: canonical true/false; case-sensitivity (uppercase rejected); non-matching defaults to false. The non-matching SECTION includes the inline comment `// Per 05-REVIEWS.md item C1, string_utility.h:44 initializes \`bool result = false;\` so extraction failure deterministically returns false. This SECTION catches a regression that drops the initializer.`
       - `TEST_CASE("stringUtility::toString(int, fillCount) zero-pads correctly", "[utility][string]")` — 4 REQUIREs across `(0,2)`, `(7,2)`, `(42,2)`, `(100,2)` (the last verifies overflow is not truncated).
       - `TEST_CASE("stringUtility::toHexString lowercase hex with zero padding", "[utility][string]")` — 3 REQUIREs across `(0,4)`, `(0xAB,4)`, `(static_cast<uint32_t>(0xDEADBEEF), 8)`.
       - `TEST_CASE("stringUtility::trim / trimStart / trimEnd strip default whitespace", "[utility][string]")` — 4 SECTIONs: trim strips both ends + in-place mutation; trimStart strips only leading; trimEnd strips only trailing; "PluginManager [Plugins] line idiom" SECTION reproducing the `toBool(trim(isEnabled))` and `trim(dirName)` calls from plugin_manager.cpp:121-138.

       **Count summary embedded as a final comment in the file:** `// Total: 4 top-level TEST_CASEs / 6 conceptual coverage cases via SECTIONs (toBool 3 sections, toString 1 case, toHexString 1 case, trim 4 sections grouped as 1 conceptual case + PluginManager idiom). Per 05-REVIEWS.md item C2.`

    Two of the TEST_CASEs use SECTION re-entry. Each section must observe **fresh state** (verified by the smoke test in 05-01). No SECTION may depend on side effects from a sibling SECTION.

    **Sub-step B — Build + run locally to verify:**

    From the repo root:
    - `msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86 /t:UtinniCore.Tests` (must exit 0)
    - `bin\Release\UtinniCore.Tests.exe "[utility][string]" --reporter console` (must exit 0; must invoke 4 top-level TEST_CASEs)
    - `bin\Release\UtinniCore.Tests.exe --reporter console` (full suite — must report green for all smoke + seed TEST_CASEs; success language is `All tests passed`, no hardcoded count per 05-REVIEWS.md Codex LOW)

    Implements: D-01 (locked seed: stringUtility), D-06 (max-harness with documented failure modes), 05-REVIEWS.md item C2 (count normalization), 05-REVIEWS.md item C1 (post-patch garbage-input tests are real coverage), TEST-02 ROADMAP success criterion #2.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86 /t:UtinniCore.Tests; if ($LASTEXITCODE -ne 0) { exit 1 }; $exeFilter = & 'bin\Release\UtinniCore.Tests.exe' '[utility][string]' --reporter console 2>&1; if ($LASTEXITCODE -ne 0) { Write-Error \"Filter run non-zero exit: $LASTEXITCODE\"; exit 1 }; if (-not ($exeFilter -match 'All tests passed')) { Write-Error 'Filter run did not report All tests passed (per 05-REVIEWS Codex LOW: success = exit 0 + All tests passed)'; exit 1 }; $exeFull = & 'bin\Release\UtinniCore.Tests.exe' --reporter console 2>&1; if ($LASTEXITCODE -ne 0) { Write-Error \"Full-suite run non-zero exit: $LASTEXITCODE\"; exit 1 }; if (-not ($exeFull -match 'All tests passed')) { Write-Error 'Full-suite run did not report All tests passed'; exit 1 }; $content = Get-Content UtinniCore.Tests/StringUtilityTests.cpp -Raw; $required = @('TEST_CASE\(\"stringUtility::toBool', 'TEST_CASE\(\"stringUtility::toString\(int', 'TEST_CASE\(\"stringUtility::toHexString', 'TEST_CASE\(\"stringUtility::trim', '\[utility\]\[string\]', 'D-06 max-harness', 'PluginManager::loadPlugins', '4 top-level TEST_CASEs', '6 conceptual'); foreach ($pat in $required) { if (-not ($content -match $pat)) { Write-Error \"Missing required pattern: $pat\"; exit 1 } }; $caseCount = ([regex]::Matches($content, 'TEST_CASE\(\"stringUtility::')).Count; if ($caseCount -ne 4) { Write-Error \"Expected 4 top-level TEST_CASE(\"\"stringUtility::...\"\") patterns per 05-REVIEWS.md item C2; found $caseCount\"; exit 1 }; if ($content -match '6 TEST_CASEs') { Write-Error 'Source still uses normalize-prohibited phrase `6 TEST_CASEs` (per 05-REVIEWS.md item C2 must say `4 top-level TEST_CASEs covering 6 conceptual coverage cases via SECTIONs`)'; exit 1 }; if (-not ($content -match '05-REVIEWS\.md item C1')) { Write-Error 'Source missing 05-REVIEWS.md item C1 cross-reference in the garbage-input SECTION (audit trail for the toBool UB fix)'; exit 1 }; exit 0"</automated>
  </verify>
  <done>
    `UtinniCore.Tests/StringUtilityTests.cpp` exists with:
    - 23-line MIT header (verbatim from UtinniCore/utility/string_utility.h:1-23).
    - D-06 failure-mode table embedded as a comment block at the top (verbatim from RESEARCH.md lines 425-432, formatted as a C++ comment).
    - 05-REVIEWS.md item C2 normalization explicitly recorded in the comment block ("4 top-level TEST_CASEs covering 6 conceptual coverage cases via SECTIONs").
    - 2 `#include` directives: `<catch2/catch_amalgamated.hpp>` and `"utility/string_utility.h"`.
    - **Exactly 4** top-level `TEST_CASE("stringUtility::...` patterns (verified by regex count == 4), each tagged `[utility][string]`.
    - In the toBool garbage-input SECTION: an inline comment citing `05-REVIEWS.md item C1` to provide audit trail for the post-patch defined-behavior assertion.
    - The phrase `6 TEST_CASEs` does NOT appear anywhere in the file (the normalized phrasing `6 conceptual` does appear, but the unnormalized `6 TEST_CASEs` does not).

    Local build green under Release|x86, `[utility][string]` filter exits 0 with `All tests passed`, full suite exits 0 with `All tests passed`. The placeholder content from 05-01 (`// Placeholder: real coverage lands in Plan 05-02.`) is no longer present in the file.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close out docs/ai/test-harness-plan.md "Tier 1 — C++ side" row + update .planning/REQUIREMENTS.md §TEST-02 acceptance text (per 05-REVIEWS.md item U3)</name>
  <files>docs/ai/test-harness-plan.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    Per 05-REVIEWS.md item U3 (MEDIUM, Cursor unique): `.planning/REQUIREMENTS.md:53` (the TEST-02 row) still references `ctest`, but D-03 superseded this with MSBuild + direct-exe. The requirement text must match the locked decision so the phase doesn't close with stale acceptance language.

    Current `.planning/REQUIREMENTS.md` TEST-02 (lines 50-54):

    ```
    - [ ] **TEST-02 — Tier 1 C++ unit-test scaffold**
      - **Source:** test-harness-plan.md "Tier 1"
      - **Statement:** Catch2 (header-only) wired through `ctest` or `vcpkg` for UtinniCore. Folds in once UtinniCore has the refactored seams produced by STAB-02 (R-A..R-H).
      - **Acceptance:** Catch2 builds in CI; at least one native parser or helper has coverage; CI runs both `dotnet test` and `ctest` and is green on master.
      - **Milestone:** V1
    ```

    Both the **Statement** and **Acceptance** lines reference `ctest`. Both need updating.

    PATTERNS.md §"docs/ai/test-harness-plan.md (modify — close out Tier 1 C++ row)" — verbatim quote of the existing "Tier 1 — Pure unit tests" section (lines 23-33) and the "Suggested phase order" item 3 (line 73) that both need updating.

    Existing line 26 (currently): `**C++ side:** Catch2 (header-only, easy to drop into UtinniCore's solution) wired through `ctest` or a `vcpkg`-managed dependency.`

    Existing line 73 (currently): `3. **Tier 1 C++ unit tests** — fold in once UtinniCore has refactored seams (the prior audit flagged native code quality, so this likely pairs with cleanup).`

    CONTEXT.md domain "In scope" line 19 — explicit requirement: "docs/ai/test-harness-plan.md 'Tier 1 — C++ side' row gets dispositioned with the resolved D-01..D-04."
  </read_first>
  <action>
    **Sub-step A — Replace the "C++ side" bullet in `docs/ai/test-harness-plan.md` Tier 1 section:**

    Locate the line currently reading:

    > `- **C++ side:** Catch2 (header-only, easy to drop into UtinniCore's solution) wired through `ctest` or a `vcpkg`-managed dependency.`

    Replace it with:

    > `- **C++ side:** **[Closed in Phase 5, 2026-XX-XX]** Catch2 v3.15.0 vendored amalgamated at \`external/catch2/\` (D-02). Sibling MSBuild test exe \`UtinniCore.Tests.vcxproj\` produces \`UtinniCore.Tests.exe\` (D-03, no CMake/ctest). Third lane in \`.github/workflows/ci.yml\` gates master from day one (D-04). Seed coverage: 4 TEST_CASEs (6 conceptual cases via SECTIONs) across \`stringUtility::toBool/toString/toHexString/trim*\` with documented PluginManager-regression failure modes per D-06 max-harness (D-01). See \`.planning/phases/05-tier-1-c-unit-tests/\` for full details.`

    Replace `2026-XX-XX` with the actual commit date (`Get-Date -Format yyyy-MM-dd` in PowerShell).

    **Sub-step B — Refresh the stale "Targets" bullet list in `docs/ai/test-harness-plan.md`:**

    Replace the current list (`docs/ai/test-harness-plan.md` lines ~27-32) with:

    - **C# side:** TRE / IFF parsers (Phase 4 D-06 moved these from native to managed), settings serialization / migration, plugin manifest validation, hotkey parsing, pure data-model logic.
    - **C++ side (Phase 5 / closed):** `stringUtility::toBool` / `toString(int, fillCount)` / `toHexString` / `trim` round-trip — exercised under PluginManager's `[Plugins]` line idiom.
    - **C++ side (Phase 6 STAB-03 candidates, deferred):** `utility/log` R-A subscribe/unsubscribe registry (RESEARCH.md §D-01 "Backup pick"); broader `utility/utility.cpp` formatting helpers; `string_utility.h:89-102` `trim_*_copy` helpers (names appear swapped or logic incomplete per 05-REVIEWS.md Cursor LOW); math helpers if any seam emerges.
    - **C++ side (out-of-scope; Tier-3 V2):** `swg/*` shim (RVA-bound, untestable without a fake SWG process); `utility/memory::*` (VirtualProtect / live-process semantics).

    **Sub-step C — Update the "Suggested phase order" item 3 in `docs/ai/test-harness-plan.md`:**

    Locate the line:

    > `3. **Tier 1 C++ unit tests** — fold in once UtinniCore has refactored seams (the prior audit flagged native code quality, so this likely pairs with cleanup).`

    Replace with:

    > `3. **Tier 1 C++ unit tests** — **[Closed in Phase 5, 2026-XX-XX]** Catch2 v3.15.0 vendored, MSBuild + direct exe, third CI lane green on master. See `.planning/phases/05-tier-1-c-unit-tests/`.`

    **Sub-step D — Update `.planning/REQUIREMENTS.md` §TEST-02 (per 05-REVIEWS.md item U3):**

    Locate the TEST-02 block (lines 50-54) and replace:

    - **Statement** line — from: `Catch2 (header-only) wired through \`ctest\` or \`vcpkg\` for UtinniCore. Folds in once UtinniCore has the refactored seams produced by STAB-02 (R-A..R-H).`
      To: `Catch2 (header-only amalgamated) wired through MSBuild + direct exe invocation for UtinniCore — no \`ctest\` or \`vcpkg\` per Phase 5 D-03. Folds in once UtinniCore has the refactored seams produced by STAB-02 (R-A..R-H).`
    - **Acceptance** line — from: `Catch2 builds in CI; at least one native parser or helper has coverage; CI runs both \`dotnet test\` and \`ctest\` and is green on master.`
      To: `Catch2 builds in CI under all three native configs (Debug + Release + RelWithDbgInfo); at least one native parser or helper has coverage; CI runs both \`dotnet test\` and the native test exe (\`UtinniCore.Tests.exe\` via Catch2's self-runner, no \`ctest\`) and is green on master.`

    The TEST-02 checkbox `[ ]` may be checked `[x]` when the phase completes and a verification step confirms; this task leaves the checkbox state to the phase-completion workflow.

    Implements: CONTEXT.md domain "In scope" (line 19) — "docs/ai/test-harness-plan.md 'Tier 1 — C++ side' row gets dispositioned with the resolved D-01..D-04"; 05-REVIEWS.md item U3 (MEDIUM) — REQUIREMENTS.md TEST-02 acceptance language updated to remove `ctest` references.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$thp = Get-Content docs/ai/test-harness-plan.md -Raw; $required = @('Closed in Phase 5', 'Catch2 v3\.15\.0', 'D-01', 'D-02', 'D-03', 'D-04', 'stringUtility', 'external/catch2', '4 TEST_CASEs', '6 conceptual'); foreach ($pat in $required) { if (-not ($thp -match $pat)) { Write-Error \"docs/ai/test-harness-plan.md missing required pattern: $pat\"; exit 1 } }; if ($thp -match '(?i)Tier\s*1\s*[—-]\s*C\+\+\s*side[^\r\n]{0,200}\bctest\b') { Write-Error 'docs/ai/test-harness-plan.md Tier-1 C++ row still references ctest'; exit 1 }; $req = Get-Content .planning/REQUIREMENTS.md -Raw; $test02Block = if ($req -match '(?ms)\*\*TEST-02.*?\*\*Milestone:\*\*\s*V1') { $matches[0] } else { '' }; if (-not $test02Block) { Write-Error 'Could not locate TEST-02 block in REQUIREMENTS.md'; exit 1 }; if ($test02Block -match '\bctest\b') { Write-Error 'REQUIREMENTS.md TEST-02 block still contains `ctest` (per 05-REVIEWS.md item U3 it MUST be removed)'; exit 1 }; if (-not ($test02Block -match 'MSBuild')) { Write-Error 'REQUIREMENTS.md TEST-02 block missing MSBuild reference (per 05-REVIEWS.md item U3 the replacement text references MSBuild + direct-exe)'; exit 1 }; if (-not ($test02Block -match 'D-03')) { Write-Error 'REQUIREMENTS.md TEST-02 block missing D-03 cross-reference (audit trail to the locked Phase 5 decision)'; exit 1 }; if (-not ($test02Block -match 'UtinniCore\.Tests\.exe')) { Write-Error 'REQUIREMENTS.md TEST-02 Acceptance line should reference UtinniCore.Tests.exe (the actual artifact, not the abstract \"native test exe\")'; exit 1 }; exit 0"</automated>
  </verify>
  <done>
    `docs/ai/test-harness-plan.md` reflects Phase 5 closure:
    - The "Tier 1 — Pure unit tests" section's "C++ side" bullet is updated to "[Closed in Phase 5, YYYY-MM-DD]" and references D-01..D-04 explicitly. The phrase "4 TEST_CASEs (6 conceptual cases via SECTIONs)" appears (per 05-REVIEWS.md item C2 normalization).
    - The stale "Targets" bullet list is replaced with a Phase-4-D-06-aware split that also flags `trim_*_copy` helpers in `string_utility.h:89-102` as a Phase 6 STAB-03 candidate (per 05-REVIEWS.md Cursor LOW).
    - The "Suggested phase order" item 3 is marked closed with a link to `.planning/phases/05-tier-1-c-unit-tests/`.
    - The old "wired through `ctest` or a `vcpkg`-managed dependency" phrasing is gone from the Tier-1 C++ row.

    `.planning/REQUIREMENTS.md` §TEST-02 reflects the locked D-03 decision:
    - The **Statement** line no longer contains `ctest`; it references "MSBuild + direct exe invocation".
    - The **Acceptance** line no longer contains `ctest`; it references `UtinniCore.Tests.exe` via Catch2's self-runner.
    - Both lines explicitly cite "Phase 5 D-03" for the supersession audit trail.
    - Verified by a `\bctest\b` negative grep on the TEST-02 block AND positive greps for `MSBuild`, `D-03`, `UtinniCore.Tests.exe`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Phase-end code review — /gsd:code-review 05</name>
  <files>(no file edits — invokes /gsd:code-review 05 workflow; any resulting findings file lands at .planning/phases/05-tier-1-c-unit-tests/05-CODE-REVIEW.md)</files>
  <action>Phase-end checkpoint. The user runs /gsd:code-review 05 to spawn the GSD code-review workflow against Phase 5 (both plans). The workflow surfaces any new critical findings; blocking findings (STAB-04 preservation regressions) must be addressed before merging to master. Non-blocking findings are acceptable to fix inline or defer to Phase 6 STAB-03 cleanups. See <how-to-verify> below for the exact procedure.</action>
  <verify>
    <automated>powershell -NoProfile -Command "if (Test-Path .planning/phases/05-tier-1-c-unit-tests/05-CODE-REVIEW.md) { exit 0 } else { Write-Host 'No 05-CODE-REVIEW.md yet -- checkpoint pending human resume'; exit 0 }"</automated>
    <human-check>Run /gsd:code-review 05, then review the produced findings and respond with the resume-signal below.</human-check>
  </verify>
  <done>The code review has been run, all blocking findings (if any) are addressed, and the user has resumed with one of: approved / needs-fix: &lt;desc&gt; / deferred-fix: &lt;desc&gt;. The 05-02-SUMMARY.md records the findings disposition.</done>
  <what-built>
    Phase 5 deliverables across both plans (post 05-REVIEWS replan):
    - 05-01: Catch2 v3.15.0 vendored at `external/catch2/` (incl. README.md with durable vendor metadata per U6); `UtinniCore.Tests.vcxproj` (triple-config Application, `string_utility.cpp` excluded per U1); `Utinni.sln` registration with GUID-scoped 6-mapping verification (per U5); `main_smoke.cpp` with 4 TEST_CASEs (3 smoke + 1 include-graph compile smoke per U1); third CI lane with triple-config build + directory pre-creation + Release-only exe invocation (per C3 + U4); red-run validation documented (per U2); green on master.
    - 05-02: `string_utility.h:44` patched to `bool result = false;` (per C1); `StringUtilityTests.cpp` with 4 top-level TEST_CASEs covering 6 conceptual cases via SECTIONs (per C2 normalization) + embedded D-06 failure-mode table; `docs/ai/test-harness-plan.md` Tier 1 — C++ side row closed; `.planning/REQUIREMENTS.md` §TEST-02 updated to remove `ctest` (per U3).
  </what-built>
  <how-to-verify>
    Run the GSD code-review workflow against Phase 5:

    1. From the repo root, invoke `/gsd:code-review 05`.
    2. The workflow will surface any new critical findings introduced by 05-01 + 05-02.
    3. Review the produced findings file (typically `.planning/phases/05-tier-1-c-unit-tests/05-CODE-REVIEW.md`).
    4. **Expected (and acceptable):** Zero new critical findings; possibly minor stylistic notes that can be fixed in-place or deferred to Phase 6 STAB-03 cleanups.
    5. **Blocking:** Any critical finding tagged as a regression of CON-N-* / CON-M-* / CON-T-* preservation items (STAB-04 cross-cutting).

    Implements: CONTEXT.md domain "In scope" line 20 — "Code-review at phase end (`/gsd:code-review 05`) to confirm no new critical findings."
  </how-to-verify>
  <resume-signal>Type "approved" if the code review surfaced no blocking findings, "needs-fix: ..." with a brief description if blocking findings were surfaced and addressed, or "deferred-fix: ..." if a finding is acceptable to fix in Phase 6.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Phase 5 has zero security surface — test-only Win32 console exe, runs on CI runner + dev machines, no network input, no untrusted data parsing, no public API (per RESEARCH.md §Security Domain). Plan 05-02 adds no new boundary beyond 05-01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Vendored Catch2 v3.15.0 amalgamated source (external/catch2/) | mitigate (covered in 05-01 Task 1) | Inherited from 05-01: SHA-256 captured in commit message AND in external/catch2/README.md (per 05-REVIEWS.md item U6), tag pinned to v3.15.0 (ASVS V14). |
| T-05-02 | Tampering | string_utility.h:44 toBool initializer regression | mitigate | Per 05-REVIEWS.md item C1: the `bool result = false;` initializer landed in Task 1; the garbage-input SECTION in Task 2's `toBool` TEST_CASE catches a regression that drops the initializer (extraction failure → UB). Negative grep on `^\s*bool result;\s*$` in the verify step prevents accidental partial revert. |
</threat_model>

<verification>
## Phase Checks

After this plan completes:

1. `UtinniCore/utility/string_utility.h` line 46 reads `    bool result = false;` (initialized per 05-REVIEWS.md item C1); the un-initialized form `bool result;` does NOT appear anywhere in the file.
2. `UtinniCore.Tests/StringUtilityTests.cpp` contains EXACTLY 4 top-level TEST_CASEs (`toBool`, `toString(int,fillCount)`, `toHexString`, `trim*`) covering 6 conceptual coverage cases via Catch2 SECTIONs (per 05-REVIEWS.md item C2 — language normalized everywhere), all tagged `[utility][string]`.
3. The file includes the D-06 failure-mode table as a C++ comment block at the top, the 05-REVIEWS.md item C2 normalization note ("4 top-level TEST_CASEs covering 6 conceptual coverage cases via SECTIONs"), and a 05-REVIEWS.md item C1 cross-reference in the toBool garbage-input SECTION.
4. The phrase `6 TEST_CASEs` (unnormalized) does NOT appear in `StringUtilityTests.cpp` (the normalized phrasing `6 conceptual` does appear).
5. `bin\Release\UtinniCore.Tests.exe "[utility][string]"` exits 0 with `All tests passed` (no hardcoded assertion count per 05-REVIEWS.md Codex LOW).
6. `bin\Release\UtinniCore.Tests.exe` (full suite) exits 0 with `All tests passed` — 05-01 smoke + 05-02 seed coverage together pass.
7. `docs/ai/test-harness-plan.md` Tier 1 — C++ side row is closed with explicit references to D-01..D-04, the "4 TEST_CASEs (6 conceptual cases via SECTIONs)" language, and a date stamp. The `trim_*_copy` finding is noted as a Phase 6 STAB-03 candidate (per 05-REVIEWS.md Cursor LOW).
8. `.planning/REQUIREMENTS.md` §TEST-02 has NO `ctest` references; the Statement + Acceptance lines reference MSBuild + direct-exe + `UtinniCore.Tests.exe` + D-03 (per 05-REVIEWS.md item U3).
9. CI master push of 05-02 produces a second green run of the native test lane (implicit two-consecutive-green soak satisfied per CONTEXT.md D-04/D-05).
10. `/gsd:code-review 05` surfaces zero new critical findings (or any surfaced findings are addressed before merge).
</verification>

<success_criteria>
**05-02 succeeds when:**

- `UtinniCore/utility/string_utility.h:44` is patched to `bool result = false;` per 05-REVIEWS.md item C1; the un-initialized form is no longer present. (Task 1, Wave-1 within 05-02.)
- `StringUtilityTests.cpp` ships **4 top-level TEST_CASEs covering 6 conceptual coverage cases via Catch2 SECTIONs** (per 05-REVIEWS.md item C2 normalization — language consistent across frontmatter, objective, action, verify, source comments, success criteria, and SUMMARY).
- Each TEST_CASE has a documented failure mode in the file's header comment block, traceable to a specific PluginManager::loadPlugins regression — D-06 max-harness compliant.
- The toBool garbage-input SECTION cites 05-REVIEWS.md item C1 inline (audit trail for the post-patch defined-behavior assertion).
- The file includes `"utility/string_utility.h"` (header-only inline) and `<catch2/catch_amalgamated.hpp>` (vendored amalgamated). No link against UtinniCore.dll required (`string_utility.cpp` is NOT compiled into the test exe per 05-REVIEWS.md item U1).
- Local full-suite run produces green output: smoke (05-01) + seed (05-02) TEST_CASEs all pass with `All tests passed` exit message (no hardcoded count per 05-REVIEWS.md Codex LOW).
- `docs/ai/test-harness-plan.md` Tier 1 — C++ side row is dispositioned with references to D-01..D-04, a date stamp, and the "4 TEST_CASEs / 6 conceptual cases via SECTIONs" language.
- `.planning/REQUIREMENTS.md` §TEST-02 acceptance text is updated to remove all `ctest` references (per 05-REVIEWS.md item U3) and reference MSBuild + direct-exe + D-03.
- `/gsd:code-review 05` runs and either reports no blocking findings, or the findings are addressed before merging the second master push.
- Second master push (with this plan's contents) produces a green CI run on the third lane — implicit soak window closed.
- TEST-02 ROADMAP success criterion #2 ("At least one UtinniCore parser or helper has Catch2 coverage") is verified complete.
</success_criteria>

<output>
Create `.planning/phases/05-tier-1-c-unit-tests/05-02-SUMMARY.md` when done.

The SUMMARY.md template should include:

```markdown
## Phase 5 / Plan 02 Summary

- **Final Catch2 console-reporter output:** "All tests passed" + exit code 0 (no hardcoded assertion count per 05-REVIEWS.md Codex LOW).
- **C1 patch applied (per 05-REVIEWS.md item C1):** `UtinniCore/utility/string_utility.h:44` initialized to `bool result = false;`. Garbage-input TEST_CASEs (e.g., `toBool("garbage") == false`) now test defined behavior.
- **Test counts (per 05-REVIEWS.md item C2 normalization):**
  - Top-level TEST_CASEs: 4 (toBool, toString(int,fillCount), toHexString, trim*)
  - Conceptual coverage cases (via Catch2 SECTIONs): 6
- **Master CI run URL (second consecutive green on third lane):** {URL}
- **`/gsd:code-review 05` findings disposition:** none / addressed-inline / deferred-to-Phase-6
- **TEST-02 ROADMAP success criteria status:**
  - #1 (Catch2 test exe builds in CI under all three native configs per 05-REVIEWS.md item C3): {green/red}
  - #2 (At least one UtinniCore parser/helper has Catch2 coverage — 4 TEST_CASEs across stringUtility::toBool/toString(int,fillCount)/toHexString/trim*): {green/red}
  - #3 (CI gates `main` on `dotnet test` + CLI golden tests + Catch2 native exe): {green/red}

## Phase 6 STAB-03 Inputs

The following findings from Phase 5 review + execution are out-of-scope for Phase 5 but should be picked up in Phase 6 STAB-03:

- **`trim_*_copy` helpers in `string_utility.h:89-102` (per 05-REVIEWS.md Cursor LOW):** `trim_copy` only calls `trimStart`; `trimStart_copy` only calls `trimEnd`; `trimEnd_copy` calls full `trim`. Names appear swapped or logic appears incomplete. Out-of-scope for Phase 5 because the 05-02 seed tests use the non-`_copy` variants which behave correctly under the PluginManager idiom. Surfaces as a Phase 6 STAB-03 candidate (with first-test-then-fix discipline).
- {Any additional findings surfaced by `/gsd:code-review 05` tagged as deferred-to-Phase-6.}
```
</output>
