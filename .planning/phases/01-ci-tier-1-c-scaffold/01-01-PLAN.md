---
phase: 01-ci-tier-1-c-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .editorconfig
  - UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj
  - UtinniCoreDotNet.Tests/HotkeyTests.cs
  - UtinniCoreDotNet.Tests/packages.lock.json
  - Utinni.sln
autonomous: true
requirements:
  - TEST-01

must_haves:
  decisions_addressed:
    - "D-01: Sibling project at repo root — `UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj` lives next to `UtinniCoreDotNet` in `Utinni.sln`; no `tests/` subfolder, no parallel `Utinni.Tests.sln`."
    - "D-02: Test project targets `net472` + `x86` and references `UtinniCoreDotNet` via `<ProjectReference>` — same platform as the system under test."
    - "D-03: xUnit 2.x pin — packages `xunit 2.9.3`, `xunit.runner.visualstudio 3.1.5`, `Microsoft.NET.Test.Sdk 17.13.0`; test discovery via `dotnet test`."
    - "D-04: Test naming convention `[Method]_[Scenario]_[ExpectedOutcome]`; test classes named `<TypeName>Tests.cs` (e.g. `HotkeyTests.cs`)."
    - "D-05: First smoke-test target is `Hotkey.ProcessString` exercised via the string ctor; cases include valid single-key, modifier-chord, multi-modifier, and a malformed-input case for the C-08 regression guard."
    - "D-06: UndoRedoManager testability refactor explicitly deferred to Phase 2 — no UndoRedoManager tests, no `GameCallbacks.AddCleanupSceneCall` injection seam in this plan."
    - "D-11: Bare formatting `.editorconfig` at repo root (4-space indent, CRLF, UTF-8, trim trailing whitespace, final newline); no analyzer rules; vendored `external/imgui/.editorconfig` preserved byte-for-byte."
  truths:
    - "Running `msbuild Utinni.sln /restore /p:Configuration=Release /p:Platform=x86 /m` locally exits 0 and produces UtinniCoreDotNet.Tests.dll under UtinniCoreDotNet.Tests/bin/Release/net472/."
    - "Running `dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release` locally exits 0 with 3 passed + 1 skipped (the C-08 Skip)."
    - "The CON-T-01 post-build chain on UtinniCore.vcxproj runs during the solution build and regenerates UtinniCoreDotNet/Generated/UtinniCore.cs without error."
    - ".editorconfig at the repo root declares 4-space indentation, CRLF line endings, UTF-8 charset, trim trailing whitespace, and final newline, scoped via `[*]` + per-language sections."
    - "external/imgui/.editorconfig is byte-for-byte unchanged."
  artifacts:
    - path: ".editorconfig"
      provides: "Repo-root formatting rules (4-space indent, CRLF, UTF-8, per-language sections)"
      contains: "root = true"
    - path: "UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj"
      provides: "SDK-style net472/x86 xUnit test project with PackageReference for xunit 2.9.3, xunit.runner.visualstudio 3.1.5, Microsoft.NET.Test.Sdk 17.13.0, ProjectReference to UtinniCoreDotNet"
      contains: "Microsoft.NET.Sdk"
    - path: "UtinniCoreDotNet.Tests/HotkeyTests.cs"
      provides: "Three passing [Fact]/[Theory] tests against Hotkey string-ctor + one [Fact(Skip=\"C-08: ...\")] for the malformed-input regression guard"
      contains: "namespace UtinniCoreDotNet.Tests"
      min_lines: 50
    - path: "UtinniCoreDotNet.Tests/packages.lock.json"
      provides: "NuGet transitive-dependency pin generated by msbuild /restore"
      contains: "\"version\": 1"
    - path: "Utinni.sln"
      provides: "Solution entry for UtinniCoreDotNet.Tests (C# project GUID {FAE04EC0-301F-11D3-BF4B-00C04F79EFBC}), Debug|x86 + Release|x86 build mappings, RelWithDbgInfo|x86 maps to Release|x86 with no .Build.0 line, ProjectDependencies on {39AB8A43-B916-4C6E-87DD-928B438CAE68} (UtinniCoreDotNet)"
      contains: "UtinniCoreDotNet.Tests"
  key_links:
    - from: "UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj"
      to: "UtinniCoreDotNet/UtinniCoreDotNet.csproj"
      via: "ProjectReference"
      pattern: "ProjectReference Include=\"..\\\\UtinniCoreDotNet\\\\UtinniCoreDotNet.csproj\""
    - from: "Utinni.sln"
      to: "UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj"
      via: "Project entry + ProjectConfigurationPlatforms mappings"
      pattern: "UtinniCoreDotNet.Tests\\\\UtinniCoreDotNet.Tests.csproj"
    - from: "UtinniCoreDotNet.Tests/HotkeyTests.cs"
      to: "UtinniCoreDotNet/Hotkeys/Hotkey.cs"
      via: "string ctor exercises ProcessString"
      pattern: "new Hotkey\\("
---

<objective>
Stand up the Tier 1 C# unit-test scaffold required by TEST-01: a sibling `UtinniCoreDotNet.Tests` xUnit 2.x project at the repo root, registered in `Utinni.sln`, with one `HotkeyTests.cs` class containing three passing tests against `Hotkey.ProcessString` and one skipped malformed-input test that documents the C-08 contract for Phase 2 to honor. Also drop a bare formatting `.editorconfig` at the repo root that codifies de-facto CONVENTIONS.md rules without analyzer enforcement.

Purpose: Phase 1 IS the validation infrastructure for every subsequent phase. Without a buildable, runnable test project (and a NuGet lockfile for deterministic CI), Plan 02's CI workflow has nothing to invoke.

Output: Five committed files (four hand-authored, one generated lockfile) plus a modified `Utinni.sln`. Local `dotnet test` exits 0 against the new project.

This implements every locked decision D-01 through D-06 and D-11 from CONTEXT.md. D-07..D-10 (CI workflow + badge) land in Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-ci-tier-1-c-scaffold/01-CONTEXT.md
@.planning/phases/01-ci-tier-1-c-scaffold/01-RESEARCH.md
@.planning/phases/01-ci-tier-1-c-scaffold/01-PATTERNS.md
@.planning/phases/01-ci-tier-1-c-scaffold/01-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
@.planning/codebase/TESTING.md
@.planning/codebase/INTEGRATIONS.md

@UtinniCoreDotNet/Hotkeys/Hotkey.cs
@UtinniCoreDotNet/UtinniCoreDotNet.csproj
@Utinni.sln
@external/imgui/.editorconfig

<interfaces>
<!-- Hotkey.cs public surface the test must call. Verified by reading
     D:\Code\Utinni\UtinniCoreDotNet\Hotkeys\Hotkey.cs lines 31-92. -->

Class: public class Hotkey  (namespace UtinniCoreDotNet.Hotkeys)
Public fields the tests assert against:
  public Keys ModifierKeys;
  public Keys Key;

String ctor under test (Hotkey.cs:42):
  public Hotkey(string name, string text, string keyComboStr, Action onDownCallback,
                bool overrideGameInput, bool enabled = true, bool onGameFocusOnly = false);

  This ctor calls ProcessString(keyComboStr) at line 51. ProcessString is `private void`
  (line 66). The string-ctor is the only public seam for exercising it.

ProcessString behavior (Hotkey.cs:66-92):
  - Empty/null string -> Log.Warning(...) then return. DO NOT TEST this branch:
    Log.Warning P/Invokes into UtinniCore.dll which is not loaded in the test runner.
  - `keyComboStr` containing `+`:
      split at FIRST `+`; left side = modifiers, right side = key (trimmed).
      ModifierKeys = (Keys)Enum.Parse(typeof(Keys), modifiers, true);  // line 82 — THROWS on unknown token (C-08)
      Key          = (Keys)Enum.Parse(typeof(Keys), key, true);        // line 91 — THROWS on unknown token (C-08)
  - No `+`:
      ModifierKeys = Keys.None;
      Key          = (Keys)Enum.Parse(typeof(Keys), trimmedString, true);

Keys is System.Windows.Forms.Keys ([Flags] enum). "Control", "Shift", "Alt", "S", "Z", "F1"
are all valid token names. "Ctrl" is NOT valid (canonical name is "Control") — that is the
C-08 malformed-input case.

Existing managed C# project to mirror for naming/conventions (NOT for csproj structure —
that intentionally breaks per RESEARCH §State of the Art):
  - UtinniCoreDotNet/UtinniCoreDotNet.csproj — legacy non-SDK, but confirms TargetFrameworkVersion=v4.7.2, PlatformTarget=x86, LangVersion=7.3.
  - UtinniCoreDotNet GUID in Utinni.sln line 17: {39AB8A43-B916-4C6E-87DD-928B438CAE68}.
  - C# project type GUID in Utinni.sln lines 17, 23: {FAE04EC0-301F-11D3-BF4B-00C04F79EFBC}.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create `.editorconfig` at repo root (D-11) and verify imgui's vendored file is untouched</name>
  <files>.editorconfig</files>
  <read_first>
    - external/imgui/.editorconfig (vendored — structural reference only; MUST NOT be modified; CONTEXT.md D-11)
    - .planning/phases/01-ci-tier-1-c-scaffold/01-RESEARCH.md §Code Examples §Example 2 (canonical body for the new root .editorconfig)
    - .planning/phases/01-ci-tier-1-c-scaffold/01-PATTERNS.md §`.editorconfig` (deviations from imgui analog; per-language sections)
    - .planning/codebase/CONVENTIONS.md §Code Style (de-facto rules being codified: 4-space indent, Allman braces, CRLF norm, no tabs, no `_` prefix)
  </read_first>
  <action>
    Create `.editorconfig` at the repo root (D:\Code\Utinni\.editorconfig). Use the canonical body in RESEARCH.md §Example 2 verbatim. Required keys and section headers:

    - Leading comment block referencing http://editorconfig.org plus a one-line pointer to `.planning/codebase/CONVENTIONS.md` and a note that `external/imgui/.editorconfig` is preserved and overrides this file for files under that path.
    - `root = true` on its own line.
    - `[*]` default section: `indent_style = space`, `indent_size = 4`, `end_of_line = crlf` (per D-11), `charset = utf-8`, `trim_trailing_whitespace = true`, `insert_final_newline = true`.
    - `[*.{cs,cpp,h}]` section: `indent_style = space`, `indent_size = 4` (Allman braces enforced by file content, NOT by analyzer rules — per D-11 those are deferred to Phase 6).
    - `[*.md]` section: `trim_trailing_whitespace = false` (preserve Markdown two-space line breaks).
    - `[*.{yml,yaml}]` section: `indent_size = 2` (YAML de-facto convention; matches the GH Actions workflow added in Plan 02).
    - `[Makefile]` section: `indent_style = tab`.

    File MUST be written with CRLF line endings (per Pattern S-5; many editors default to LF — verify after write). Do NOT add the 23-line MIT header (CONVENTIONS.md File Headers applies to source files only, not config files — verified: existing `external/imgui/.editorconfig` has no MIT header).

    Do NOT add any analyzer-style rules (`csharp_prefer_var`, `dotnet_naming_*`, etc.) — deferred per D-11.

    Do NOT touch `external/imgui/.editorconfig` — preservation locked by D-11.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (-not (Test-Path .editorconfig)) { exit 1 }; $c = Get-Content -Raw .editorconfig; if ($c -notmatch 'root\s*=\s*true') { exit 2 }; if ($c -notmatch 'indent_size\s*=\s*4') { exit 3 }; if ($c -notmatch 'end_of_line\s*=\s*crlf') { exit 4 }; if ($c -notmatch '\[\*\.\{cs,cpp,h\}\]') { exit 5 }; if ($c -notmatch '\[\*\.md\]') { exit 6 }; if ($c -notmatch '\[\*\.\{yml,yaml\}\]') { exit 7 }; $bytes = [System.IO.File]::ReadAllBytes('.editorconfig'); $crlf = 0; for ($i=0; $i -lt $bytes.Length-1; $i++) { if ($bytes[$i] -eq 13 -and $bytes[$i+1] -eq 10) { $crlf++ } }; if ($crlf -lt 5) { Write-Error \"Expected CRLF line endings, found $crlf CRLF pairs\"; exit 8 }; git diff --quiet external/imgui/.editorconfig; if ($LASTEXITCODE -ne 0) { Write-Error 'external/imgui/.editorconfig modified — D-11 preservation violated'; exit 9 }; $imgui = Get-FileHash external/imgui/.editorconfig -Algorithm SHA256; Write-Output \"imgui hash: $($imgui.Hash)\"; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - File `.editorconfig` exists at repo root.
    - Contents include literal lines `root = true`, `indent_size = 4`, `end_of_line = crlf`, and the section headers `[*]`, `[*.{cs,cpp,h}]`, `[*.md]`, `[*.{yml,yaml}]`, `[Makefile]`.
    - File line endings are CRLF (at least 5 CRLF pairs detected by byte scan).
    - File `external/imgui/.editorconfig` is byte-for-byte unchanged. Verify by running `git diff --quiet external/imgui/.editorconfig` (exit code 0 = unchanged).
    - File contains NO `csharp_prefer_var`, NO `dotnet_naming_*`, NO `dotnet_diagnostic.*` keys (those are Phase 6 deferrals per D-11).
  </acceptance_criteria>
  <done>Repo-root `.editorconfig` codifies CONVENTIONS.md formatting defaults, uses CRLF, has per-language sections for .cs/.cpp/.h/.md/.yml/.yaml/Makefile, and the vendored imgui editorconfig is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create SDK-style xUnit test project (csproj + lockfile), wire into Utinni.sln, and author HotkeyTests.cs (D-01..D-06)</name>
  <files>UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj, UtinniCoreDotNet.Tests/HotkeyTests.cs, UtinniCoreDotNet.Tests/packages.lock.json, Utinni.sln</files>
  <behavior>
    HotkeyTests.cs contains exactly four `[Fact]`/`[Theory]` methods, three passing and one skipped:

    1. `Ctor_StringConstructor_SingleKey_SetsKeyAndNoModifier`
       - Input: `new Hotkey("test", "test", "F1", () => { }, overrideGameInput: false)`
       - Expect: `hk.ModifierKeys == Keys.None` AND `hk.Key == Keys.F1`. Both assertions pass today.

    2. `Ctor_StringConstructor_ModifierChord_SetsBoth`
       - Input: `new Hotkey("test", "test", "Control + S", () => { }, overrideGameInput: false)`
       - Expect: `hk.ModifierKeys == Keys.Control` AND `hk.Key == Keys.S`. Both pass today.

    3. `Ctor_StringConstructor_MultiModifierChord_ParsesFlags` — `[Theory]` with one `[InlineData]` row.
       - Input row: `("Shift + Alt + Z", Keys.Shift, Keys.Alt | Keys.Z)` — derived from the ACTUAL ProcessString behavior verified in Hotkey.cs lines 76-91: ProcessString splits at the FIRST `+`, so `"Shift + Alt + Z"` becomes modifiers=`"Shift"`, key=`"Alt + Z"`, and `Enum.Parse(typeof(Keys), "Alt + Z", true)` yields the bitwise-OR `Keys.Alt | Keys.Z` (Keys is a [Flags] enum). RESEARCH.md §Code Example 3 flags this "test data caveat" and instructs the planner/executor to match the actual production-code behavior.
       - Expect: `hk.ModifierKeys == Keys.Shift` AND `hk.Key == (Keys.Alt | Keys.Z)`. Passes today.

    4. `Ctor_StringConstructor_MalformedInput_DoesNotThrow` — `[Fact(Skip = "C-08: ...")]`.
       - Input: `new Hotkey("test", "test", "Ctrl + T", () => { }, overrideGameInput: false)` (`"Ctrl"` is NOT a valid Keys token; canonical name is `"Control"`).
       - Expect (when un-skipped after Phase 2 C-08 fix): `Record.Exception(...)` returns null. Today it would throw `ArgumentException` from Enum.Parse — which is exactly the C-08 bug — so we ship it `Skip`-ped per RESEARCH.md §Pitfall 5 recommendation.

    Do NOT test the empty-string path: that path calls `Log.Warning` which P/Invokes into `UtinniCore.dll` and would crash the test runner (Hotkey.cs:70).
  </behavior>
  <read_first>
    - UtinniCoreDotNet/Hotkeys/Hotkey.cs (full file — system under test; verify ProcessString behavior on line 66-92, ctor signature on line 42, public fields on lines 35-36)
    - UtinniCoreDotNet/UtinniCoreDotNet.csproj (analog for net472/x86 targeting — note it is legacy non-SDK; the NEW csproj is SDK-style per RESEARCH §State of the Art)
    - Utinni.sln (analog for project-entry shape; locate the UtinniCoreDotNet entry at line 17 and its config mappings at lines 48-53; verify the C# GUID `{FAE04EC0-301F-11D3-BF4B-00C04F79EFBC}` and UtinniCoreDotNet's GUID `{39AB8A43-B916-4C6E-87DD-928B438CAE68}`)
    - .planning/phases/01-ci-tier-1-c-scaffold/01-RESEARCH.md §Code Examples §Example 1 (canonical csproj body) AND §Example 3 (canonical HotkeyTests.cs body) AND §Pitfall 5 (verified ProcessString behavior + recommended Skip-with-comment posture)
    - .planning/phases/01-ci-tier-1-c-scaffold/01-PATTERNS.md §HotkeyTests.cs (license header, using-block ordering S-3, Allman braces S-2, naming S-4) AND §UtinniCoreDotNet.Tests.csproj (intentional deviation from legacy non-SDK analog) AND §Utinni.sln (project-entry + config-mapping copy-paste pattern, including the RelWithDbgInfo-maps-to-Release deviation)
    - .planning/codebase/CONVENTIONS.md §File Headers (verbatim 23-line MIT header from Hotkey.cs:1-23 — copyright `(c) 2020 Philip Klatt` preserved unchanged) AND §Code Style (Allman braces, 4-space indent) AND §Import Organization (System.* → third-party → UtinniCoreDotNet.*)
  </read_first>
  <action>
    Author the four artifacts in this order so that the lockfile generation succeeds:

    **(a) Create `UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj`** (D-01, D-02, D-03). SDK-style; copy the canonical body from RESEARCH.md §Code Examples §Example 1 (also reproduced verbatim in PATTERNS.md under "Canonical body"). Required elements (exact values, no substitutions):
      - `<Project Sdk="Microsoft.NET.Sdk">` root element. Omit the `<?xml version="1.0"?>` declaration (SDK-style convention; PATTERNS.md §Convention Drift Risks #6).
      - `<PropertyGroup>`:
        * `<TargetFramework>net472</TargetFramework>`
        * `<PlatformTarget>x86</PlatformTarget>`
        * `<Platforms>x86</Platforms>`
        * `<LangVersion>7.3</LangVersion>` (parity with UtinniCoreDotNet.csproj)
        * `<IsPackable>false</IsPackable>`
        * `<RootNamespace>UtinniCoreDotNet.Tests</RootNamespace>`
        * `<AssemblyName>UtinniCoreDotNet.Tests</AssemblyName>`
        * `<RestorePackagesWithLockFile>true</RestorePackagesWithLockFile>` (per Pitfall 7 recommendation — deterministic CI restores).
      - First `<ItemGroup>` with three `<PackageReference>` items (exact versions — RESEARCH.md §Standard Stack):
        * `Microsoft.NET.Test.Sdk` Version `17.13.0`
        * `xunit` Version `2.9.3`
        * `xunit.runner.visualstudio` Version `3.1.5`
      - Second `<ItemGroup>` with one `<ProjectReference>`: `..\UtinniCoreDotNet\UtinniCoreDotNet.csproj`
      - Do NOT add `<Reference Include="System" />`-style BCL refs (SDK auto-references).
      - Do NOT add per-config `<PropertyGroup Condition="...">` blocks (SDK defaults handle Debug/Release output paths).
      - Do NOT add `<Compile Include="...">` items (SDK globs `**/*.cs`).
      - Do NOT add the 23-line MIT header (csproj convention — verified absent from existing csproj files).
      - File written with CRLF line endings.

    **(b) Create `UtinniCoreDotNet.Tests/HotkeyTests.cs`** (D-04, D-05, plus the C-08-Skip posture per RESEARCH §Pitfall 5). Required structure:
      - Lines 1-23: 23-line MIT license header copied byte-for-byte from `UtinniCoreDotNet/Hotkeys/Hotkey.cs:1-23` (opens `/**`, closes `**/`). Preserve `Copyright (c) 2020 Philip Klatt` unchanged (this is a fork; the per-file header is upstream — CONVENTIONS.md File Headers).
      - Line 24: blank.
      - Lines 25-27: three using directives in S-3 order:
        * `using System.Windows.Forms;` (System.*)
        * `using UtinniCoreDotNet.Hotkeys;` (UtinniCoreDotNet.*)
        * `using Xunit;` (third-party — between System.* and UtinniCoreDotNet.*; the example in PATTERNS.md uses the order System → UtinniCoreDotNet → Xunit, but the CONVENTIONS.md rule is System → third-party → UtinniCoreDotNet — follow the CONVENTIONS rule and order is `System.Windows.Forms`, `Xunit`, `UtinniCoreDotNet.Hotkeys`).
      - `namespace UtinniCoreDotNet.Tests { ... }` (Allman braces, block-style, not file-scoped — C# 7.3 doesn't support file-scoped).
      - `public class HotkeyTests` inside the namespace, Allman braces.
      - Four test methods, exact names per the `<behavior>` block, exact bodies derived from RESEARCH.md §Code Example 3 with the `Theory` data corrected per the `<behavior>` block note. Use `Record.Exception(() => ...)` in the Skip test (per S-7: no try/catch in test code).
      - Skip parameter text exactly: `"C-08: expected to fail until Phase 2 fix lands (Enum.TryParse refactor on Hotkey.cs:82,91). When unskipped, this asserts that malformed input like 'Ctrl + T' (note 'Ctrl' is not a valid Keys enum name - should be 'Control') is gracefully handled instead of throwing ArgumentException."` (per PATTERNS.md §HotkeyTests.cs Skip-with-comment pattern).
      - 4-space indent, Allman braces, `var` for locals, named arg `overrideGameInput: false` on each `new Hotkey(...)` call, no `_`-prefix anywhere.
      - File written with CRLF line endings.

    **(c) Update `Utinni.sln`** (D-01). Modify the existing file (currently 80 lines — verified). Add (insert in BOTH the Project section AND the GlobalSection):
      - One new project entry AFTER line 24's `EndProject` (immediately after the existing `UtinniCoreDotNetGen` entry; before the `UtINI` entry on line 25), following the pattern from PATTERNS.md §Utinni.sln:
        ```
        Project("{FAE04EC0-301F-11D3-BF4B-00C04F79EFBC}") = "UtinniCoreDotNet.Tests", "UtinniCoreDotNet.Tests\UtinniCoreDotNet.Tests.csproj", "{NEW-GUID}"
        	ProjectSection(ProjectDependencies) = postProject
        		{39AB8A43-B916-4C6E-87DD-928B438CAE68} = {39AB8A43-B916-4C6E-87DD-928B438CAE68}
        	EndProjectSection
        EndProject
        ```
        where `{NEW-GUID}` is a freshly generated GUID (uppercase, brace-wrapped). Use `[System.Guid]::NewGuid().ToString().ToUpper()` in PowerShell or any reliable generator. The C# project type GUID `{FAE04EC0-301F-11D3-BF4B-00C04F79EFBC}` is copied verbatim from Utinni.sln line 17. The dependency GUID `{39AB8A43-B916-4C6E-87DD-928B438CAE68}` is UtinniCoreDotNet (verified at Utinni.sln line 17). Use TAB indentation in the ProjectSection body (matches existing convention — verified via byte inspection of existing entries).
      - Five new lines in `GlobalSection(ProjectConfigurationPlatforms) = postSolution` (insert before line 72's `EndGlobalSection`), per PATTERNS.md §Utinni.sln "Mappings to ADD":
        ```
        	{NEW-GUID}.Debug|x86.ActiveCfg = Debug|x86
        	{NEW-GUID}.Debug|x86.Build.0 = Debug|x86
        	{NEW-GUID}.Release|x86.ActiveCfg = Release|x86
        	{NEW-GUID}.Release|x86.Build.0 = Release|x86
        	{NEW-GUID}.RelWithDbgInfo|x86.ActiveCfg = Release|x86
        ```
        Five lines, not six. The intentional omission of `{NEW-GUID}.RelWithDbgInfo|x86.Build.0 = ...` is the "do not build the test project under the RelWithDbgInfo config" deviation documented in PATTERNS.md §Utinni.sln "Critical deviation from analog" and RESEARCH §Pattern 4 / Assumption A8.
      - Preserve CRLF line endings, tab indentation, and any UTF-8 BOM on the original file. Do NOT touch the `SolutionConfigurationPlatforms`, `SolutionProperties`, or `ExtensibilityGlobals` sections (preserve `SolutionGuid`).

    **(d) Generate `UtinniCoreDotNet.Tests/packages.lock.json`** by running a local restore from the repo root: `msbuild Utinni.sln /restore /p:Configuration=Release /p:Platform=x86 /m /p:RestorePackagesConfig=true` (the same command Plan 02's CI will run). The lockfile materializes at `UtinniCoreDotNet.Tests/packages.lock.json` because of `<RestorePackagesWithLockFile>true</RestorePackagesWithLockFile>`. Commit the generated file as-is — do NOT hand-edit it. If the local environment lacks MSBuild + v142 toolset, fall back to `dotnet restore UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --use-lock-file` which generates the same lockfile shape (this restore is project-targeted so it does not require the full C++ build to succeed).

    Do NOT create a `Properties/AssemblyInfo.cs` (SDK-style auto-generates assembly attributes). Do NOT create a `.runsettings` (per CONTEXT.md §Specifics).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (-not (Test-Path UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj)) { Write-Error 'csproj missing'; exit 1 }; $csproj = Get-Content -Raw UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj; if ($csproj -notmatch 'Sdk=\"Microsoft.NET.Sdk\"') { exit 2 }; if ($csproj -notmatch '<TargetFramework>net472</TargetFramework>') { exit 3 }; if ($csproj -notmatch '<PlatformTarget>x86</PlatformTarget>') { exit 4 }; if ($csproj -notmatch 'xunit\" Version=\"2\\.9\\.3\"') { exit 5 }; if ($csproj -notmatch 'xunit\\.runner\\.visualstudio\" Version=\"3\\.1\\.5\"') { exit 6 }; if ($csproj -notmatch 'Microsoft\\.NET\\.Test\\.Sdk\" Version=\"17\\.13\\.0\"') { exit 7 }; if ($csproj -notmatch 'RestorePackagesWithLockFile') { exit 8 }; if ($csproj -notmatch 'UtinniCoreDotNet\\.csproj') { exit 9 }; if (-not (Test-Path UtinniCoreDotNet.Tests/HotkeyTests.cs)) { exit 10 }; $hk = Get-Content -Raw UtinniCoreDotNet.Tests/HotkeyTests.cs; if ($hk -notmatch 'Copyright \\(c\\) 2020 Philip Klatt') { exit 11 }; if ($hk -notmatch 'namespace UtinniCoreDotNet\\.Tests') { exit 12 }; if ($hk -notmatch 'public class HotkeyTests') { exit 13 }; if ($hk -notmatch 'Ctor_StringConstructor_SingleKey_SetsKeyAndNoModifier') { exit 14 }; if ($hk -notmatch 'Ctor_StringConstructor_ModifierChord_SetsBoth') { exit 15 }; if ($hk -notmatch 'Ctor_StringConstructor_MultiModifierChord_ParsesFlags') { exit 16 }; if ($hk -notmatch 'Skip\\s*=\\s*\"C-08:') { exit 17 }; if ($hk -notmatch 'Record\\.Exception') { exit 18 }; if (-not (Test-Path Utinni.sln)) { exit 19 }; $sln = Get-Content -Raw Utinni.sln; if ($sln -notmatch 'UtinniCoreDotNet\\.Tests') { exit 20 }; if ($sln -notmatch '\\{FAE04EC0-301F-11D3-BF4B-00C04F79EFBC\\}\\) = \"UtinniCoreDotNet\\.Tests\"') { exit 21 }; if (-not (Test-Path UtinniCoreDotNet.Tests/packages.lock.json)) { Write-Error 'lockfile missing — local msbuild /restore (or dotnet restore --use-lock-file) must have been run'; exit 22 }; Write-Output 'static checks PASS'; & msbuild Utinni.sln /restore /p:Configuration=Release /p:Platform=x86 /m /p:RestorePackagesConfig=true /verbosity:minimal; if ($LASTEXITCODE -ne 0) { Write-Error \"msbuild failed: $LASTEXITCODE\"; exit 30 }; if (-not (Test-Path UtinniCoreDotNet.Tests/bin/Release/net472/UtinniCoreDotNet.Tests.dll)) { exit 31 }; & dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release; if ($LASTEXITCODE -ne 0) { Write-Error \"dotnet test failed: $LASTEXITCODE\"; exit 32 }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - `UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj` exists, is SDK-style (`Sdk="Microsoft.NET.Sdk"`), targets `net472` + `x86`, pins `xunit 2.9.3` + `xunit.runner.visualstudio 3.1.5` + `Microsoft.NET.Test.Sdk 17.13.0`, contains `<RestorePackagesWithLockFile>true</RestorePackagesWithLockFile>`, and ProjectReferences `..\UtinniCoreDotNet\UtinniCoreDotNet.csproj`.
    - `UtinniCoreDotNet.Tests/HotkeyTests.cs` exists, opens with the 23-line MIT header (`Copyright (c) 2020 Philip Klatt` literal), declares `namespace UtinniCoreDotNet.Tests`, declares `public class HotkeyTests`, contains the four exact method names from `<behavior>`, contains `Skip = "C-08:` literal, and uses `Record.Exception` in the Skip test.
    - `Utinni.sln` contains a Project entry `Project("{FAE04EC0-301F-11D3-BF4B-00C04F79EFBC}") = "UtinniCoreDotNet.Tests"`, a `ProjectDependencies` line `{39AB8A43-B916-4C6E-87DD-928B438CAE68} = {39AB8A43-B916-4C6E-87DD-928B438CAE68}`, and EXACTLY five `ProjectConfigurationPlatforms` lines for the new GUID (Debug ActiveCfg+Build.0, Release ActiveCfg+Build.0, RelWithDbgInfo ActiveCfg only — no RelWithDbgInfo Build.0). Existing entries for Launcher, UtinniCore, UtinniCoreDotNet, UtinniCoreDotNetGen, UtINI, UtinniCore-Symbols are unchanged.
    - `UtinniCoreDotNet.Tests/packages.lock.json` exists with `"version": 1` (or compatible — generated by NuGet).
    - `msbuild Utinni.sln /restore /p:Configuration=Release /p:Platform=x86 /m /p:RestorePackagesConfig=true` exits 0 from the repo root.
    - `UtinniCoreDotNet.Tests/bin/Release/net472/UtinniCoreDotNet.Tests.dll` exists after the build.
    - `dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release` exits 0 with output containing either `Passed: 3, Skipped: 1` OR `Total tests: 4. Passed: 3. Failed: 0. Skipped: 1.` (vstest output format varies by SDK version).
    - The CON-T-01 post-build chain runs: build log contains a successful `xcopy` line referencing `data` AND `UtinniCoreDotNetGen.exe` invocation with exit 0. (Verified by grepping the msbuild log for the post-build target output.)
  </acceptance_criteria>
  <done>The xUnit test project compiles into `bin/Release/net472/UtinniCoreDotNet.Tests.dll`, `dotnet test` discovers exactly 4 tests (3 pass, 1 skipped with the C-08 message), the solution file includes the new project with correct config mappings (RelWithDbgInfo has no Build.0 line), the NuGet lockfile is committed, and the CON-T-01 chain still fires.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer workstation → NuGet registry (api.nuget.org) | Plan 01 pulls `xunit`, `xunit.runner.visualstudio`, `Microsoft.NET.Test.Sdk` over TLS during `msbuild /restore`. Compromise vectors: typosquatted package, transitive dep drift, registry MITM. |
| developer workstation → committed lockfile | `packages.lock.json` becomes part of the repo and is consumed by every future CI run; tampering with it changes resolved versions silently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | NuGet supply chain (transitive dep drift between local and CI restore) | mitigate | `<RestorePackagesWithLockFile>true</RestorePackagesWithLockFile>` on the csproj plus commit of `packages.lock.json` pins exact transitive versions. CI (Plan 02) will restore against the lockfile. |
| T-01-02 | Tampering | Direct NuGet package compromise (slopsquatting / malicious upload) | accept | All three packages are first-party (Microsoft + xunit org), independently verified via NuGet v3 flatcontainer API and xunit.net official docs (RESEARCH §Package Legitimacy Audit). slopcheck does not cover NuGet ecosystem (0.6.1) — degraded gracefully per audit. No third-party-author packages introduced. |
| T-01-03 | Information Disclosure | MIT header copyright field on new test files | accept | Header preserves `Copyright (c) 2020 Philip Klatt` verbatim per CONVENTIONS.md File Headers + fork strategy memory. No new authorship lines added. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | N/A — no npm/pip/cargo installs in this plan (only NuGet, audited above as T-01-02). slopcheck blocking-human checkpoint pattern (gate="blocking-human") does not apply to NuGet ecosystem in slopcheck 0.6.1. |
</threat_model>

<verification>
## Phase-Level Verification (Plan 01 contribution)

After Plan 01 completes, the following must hold from the repo root:

1. `Test-Path .editorconfig` returns True.
2. `Test-Path UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj` returns True.
3. `Test-Path UtinniCoreDotNet.Tests/HotkeyTests.cs` returns True.
4. `Test-Path UtinniCoreDotNet.Tests/packages.lock.json` returns True.
5. `Select-String -Path Utinni.sln -Pattern 'UtinniCoreDotNet\.Tests'` returns at least 7 matches (project entry + 5 config lines + 1 dependency reference is in another project's section — actually verify ≥6).
6. `git diff --quiet external/imgui/.editorconfig` exits 0 (unchanged).
7. `msbuild Utinni.sln /restore /p:Configuration=Release /p:Platform=x86 /m /p:RestorePackagesConfig=true` exits 0.
8. `dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release` exits 0 with 3 Passed, 1 Skipped.

Plan 02 (CI workflow) will exercise these same commands on `windows-2022`.
</verification>

<success_criteria>
- All 5 artifacts in `files_modified` exist on disk after Plan 01 completes.
- Local `msbuild` + `dotnet test` round-trip is green (3 passed, 1 skipped with the C-08 message).
- `external/imgui/.editorconfig` is byte-identical to its pre-plan state (preservation guard-rail per D-11).
- CON-T-01 post-build chain executes during the local solution build (no refactor — STAB-04 preservation honored).
- No new files outside `files_modified` are created (no AssemblyInfo.cs, no .runsettings, no Properties/ folder).
</success_criteria>

<output>
Create `.planning/phases/01-ci-tier-1-c-scaffold/01-01-SUMMARY.md` when Plan 01 is complete. Record:
- Generated test-project GUID (the `{NEW-GUID}`) so Plan 02 and Phase 4/5 can cross-reference it.
- Exact transitive versions resolved into `packages.lock.json` (for STAB-04 audit traceability).
- Output of the local `dotnet test` run (timestamps + per-test pass/skip).
- Confirmation that `external/imgui/.editorconfig` checksum is unchanged.
</output>
