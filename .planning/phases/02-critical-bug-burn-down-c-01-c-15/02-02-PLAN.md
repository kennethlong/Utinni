---
phase: 02-critical-bug-burn-down-c-01-c-15
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - UtinniCore/swg/misc/config.cpp
  - UtinniCore/swg/misc/network.cpp
  - UtinniCore/clr.cpp
  - UtinniCore/swg/graphics/directx9.cpp
  - UtinniCore/swg/game/game.cpp
  - UtinniCore/utinni.cpp
  - UtinniCore/test_exports.cpp
  - UtinniCore/UtinniCore.vcxproj
  - UtinniCore/UtinniCore.vcxproj.filters
  - UtinniCoreDotNet/UI/GameDragDropEventHandlers.cs
  - UtinniCoreDotNet/UI/Controls/PanelGame.cs
  - UtinniCoreDotNet/UndoRedo/UndoRedoManager.cs
  - UtinniCoreDotNet/Callbacks/GameCallbacks.cs
  - UtinniCoreDotNet/Callbacks/ObjectCallbacks.cs
  - UtinniCoreDotNet/Callbacks/GroundSceneCallbacks.cs
  - UtinniCoreDotNetGen/Program.cs
  - UtinniCoreDotNet.Tests/ConfigBufferFreeTests.cs
  - UtinniCoreDotNet.Tests/NetworkCastTests.cs
  - UtinniCoreDotNet.Tests/GameDragDropEventHandlersTests.cs
  - UtinniCoreDotNet.Tests/UndoRedoManagerTests.cs
  - UtinniCoreDotNet.Tests/Clr10HarnessTests.cs
  - UtinniCoreDotNet.Tests/FindPatternHarnessTests.cs
  - UtinniCoreDotNet.Tests/GameCallbacksTests.cs
  - UtinniCoreDotNet.Tests/CppSharpSlnDirTests.cs
  - docs/ai/assessment.md
  - docs/ai/internals.md
autonomous: false
requirements:
  - STAB-01

user_setup:
  - service: "Live SWG client injection for C-02 and C-03 verification (Tier 4 manual)"
    why: "C-02 cross-CRT free and C-03 Network::cast both invoke SWG at hard-coded RVAs; full proof requires injection into a running SWG client. Automated harness covers post-condition wrappers (partial proof per D-05); the rest is the documented Tier 4 residual (CONTEXT.md D-06)."
    env_vars: []
    dashboard_config:
      - task: "After Plan 02-02 ships: attach to running SWG client via Launcher; observe no heap corruption in swg/misc/config path (C-02) and no uninitialized-stack read in Network::cast (C-03). PageHeap (gflags /p /enable utinni.exe) recommended for C-02."
        location: "Local dev workstation with SWG client"

must_haves:
  truths:
    - "UtinniCore/swg/misc/config.cpp `hkLoadOverrideConfig` no longer calls `delete[] data` on a buffer the SWG CRT allocated — the file dtor at the existing vtable-slot-0 call owns the buffer (C-02 per D-12 default-fallback / RESEARCH.md §C-02)."
    - "UtinniCore/swg/misc/network.cpp `Network::cast` initializes `swgptr networkId = 0;` and returns `networkId` (the OUT parameter) rather than the discarded SWG return value (C-03)."
    - "UtinniCoreDotNet/UI/GameDragDropEventHandlers.cs converts the four static `DragEventHandler`/`EventHandler` fields to `static event` declarations with a single-time forwarder lambda in `Initialize(PanelGame)`; subsequent `OnDragDrop += handler` adds to the live invocation list (C-05)."
    - "UtinniCoreDotNet/UndoRedo/UndoRedoManager.cs wraps every stack mutation in `lock(syncRoot)`, calls `Peek().AllowMerge()` BEFORE `Peek().Merge(args.UndoCommand)`, moves `RedoCommands.Clear()` to AFTER the merge check (TD-29 sub-bug), and accepts an `Action<Action> registerCleanupCallback = null` ctor parameter defaulting to `GameCallbacks.AddCleanupSceneCall` — the Phase-1 D-06 deferred testability seam (C-07 + TD-29)."
    - "IUndoCommand.AllowMerge() stays in the interface (C-07 disposition per RESEARCH §C-07: documented contract in docs/ai/undo-redo.html:54-55,184-185 — keep)."
    - "UtinniCore/clr.cpp `stop()` is idempotent: each `pClrRuntimeHost->Release()` / `pClrRuntimeInfo->Release()` / `pClrMetaHost->Release()` is null-checked AND the pointer is set to nullptr after release (C-10)."
    - "UtinniCore/swg/graphics/directx9.cpp `getVtbl()` null-checks both `GetModuleHandle(\"d3d9.dll\")` AND `memory::findPattern` result before `memcpy`; emits `utinni::log::critical(...)` and returns nullptr on either failure; `detour()` callers bail when `getVtbl()` returns nullptr (C-11)."
    - "CON-N-04 (`utility/memory::copy` VirtualProtect bracket) preserved — C-11 fix does NOT touch `memory.cpp:63-72`; only `getVtbl()` in directx9.cpp changes."
    - "UtinniCoreDotNetGen/Program.cs has an extracted `ResolveSlnDir(string workingDir, string[] args)` pure function with three resolution modes (args[0]/$(SolutionDir), walk-up to `Utinni.sln`, env-var `UTINNI_SLN_DIR` fallback) (C-15)."
    - "UtinniCoreDotNet/Callbacks/GameCallbacks.cs line 46 comment is replaced with a precise CLR P/Invoke delegate-marshalling explanation citing GC-root semantics (C-16 — `ObjectCallbacks.cs:39` gets matching update; `GroundSceneCallbacks.cs:38-41` optionally gets the same comment for consistency)."
    - "UtinniCore/swg/game/game.cpp `Game::isSafeToUse()` at line 305-308 uses `&&` (AND) per docs/ai/internals.md:218-231 — the `||` typo flipped (KB-05 / CON-O-01 default-fallback per D-11/D-12)."
    - "Test-only native exports added to UtinniCore.dll for P/Invoke harnesses: `utinni_test_freeConfigBuffer` (C-02), `utinni_test_networkCast` (C-03), `utinni_clr_stop` (C-10), `utinni_findPattern` + `utinni_getVtbl` (C-11), `utinni_triggerInstallCallbacks` (C-16) — all `extern \"C\" __declspec(dllexport) __cdecl` per RESEARCH §Per-bug fix shapes."
    - "docs/ai/assessment.md §\"Status tracking\" rows for C-02, C-03, C-05, C-07, C-10, C-11, C-15, C-16 flipped to `done` with commit SHAs; §\"Open questions\" §1 (CON-O-01) and §3 (CON-O-03) carry D-12 dispositions; docs/ai/internals.md §isSafeToUse remains accurate (no edits needed — code now matches doc)."
    - "CI on master is green after every fix commit in this plan (D-01)."
    - "Decision provenance: per D-03 every C-NN fix lands as its own atomic commit (~8 fix commits + KB-05 + 1 scaffold + 1 docs sweep in this plan); per D-04 every managed-side fix here ships with a red-before-green xUnit test in the SAME commit (C-05, C-07, C-10, C-11, C-15, C-16); per D-07 all new test files land in the single UtinniCoreDotNet.Tests project (no new test project added); per D-10 C-16 (GameCallbacks delegate-pinning audit + GC-survival regression test, resolves CON-O-03) is parked in THIS plan as Task 9 alongside the C-02..C-15 single-file criticals."
  artifacts:
    - path: "UtinniCoreDotNet.Tests/ConfigBufferFreeTests.cs"
      provides: "C-02 partial-proof — calls test-only utinni_test_freeConfigBuffer; asserts no AV"
      contains: "FreeConfigBuffer_OnSyntheticBuffer_DoesNotCrash"
    - path: "UtinniCoreDotNet.Tests/NetworkCastTests.cs"
      provides: "C-03 partial-proof — post-condition wrapper test"
      contains: "NetworkCast_Wrapper_ReturnsInitializedValue"
    - path: "UtinniCoreDotNet.Tests/GameDragDropEventHandlersTests.cs"
      provides: "C-05 regression — WinForms Panel synthesized DragEventArgs delivery"
      contains: "OnDragDrop_SubscriberAfterInitialize_ReceivesEvent"
    - path: "UtinniCoreDotNet.Tests/UndoRedoManagerTests.cs"
      provides: "C-07 — thread-safety + AllowMerge gate + RedoCommands.Clear ordering"
      contains: "AddUndoCommand_ConcurrentPushFromMultipleThreads_NoRaceConditions, AddUndoCommand_MergeReturnsTrue_DoesNotClearRedoStack, AddUndoCommand_NewCommandActuallyPushed_DoesClearRedoStack"
    - path: "UtinniCoreDotNet.Tests/Clr10HarnessTests.cs"
      provides: "C-10 — P/Invoke utinni_clr_stop twice + assert no AV"
      contains: "ClrStop_CalledTwiceConsecutively_NoAccessViolation"
    - path: "UtinniCoreDotNet.Tests/FindPatternHarnessTests.cs"
      provides: "C-11 — P/Invoke utinni_findPattern + utinni_getVtbl absent-module case"
      contains: "FindPattern_PatternAbsent_ReturnsZero, GetVtbl_WithoutD3d9Loaded_ReturnsNullAndLogsCritical"
    - path: "UtinniCoreDotNet.Tests/GameCallbacksTests.cs"
      provides: "C-16 — delegate survives GC.Collect between registration and dispatch"
      contains: "RegisterCallback_ForceGCCollect_CallbackStillFiresWithoutAV"
    - path: "UtinniCoreDotNet.Tests/CppSharpSlnDirTests.cs"
      provides: "C-15 — ResolveSlnDir pure function with three resolution modes"
      contains: "ResolveSlnDir_ArgZeroProvided_UsesIt, ResolveSlnDir_NoArgsButWalkUpFindsUtinniSln_UsesIt, ResolveSlnDir_NoArgsNoWalkupHit_NoEnvVar_Throws"
    - path: "UtinniCore/test_exports.cpp"
      provides: "Centralized extern \"C\" __declspec(dllexport) wrappers for the test-only P/Invoke harnesses"
      contains: "utinni_test_freeConfigBuffer, utinni_test_networkCast, utinni_clr_stop, utinni_findPattern, utinni_getVtbl, utinni_triggerInstallCallbacks"
  key_links:
    - from: "UtinniCoreDotNet/UndoRedo/UndoRedoManager.cs ctor"
      to: "Action<Action> registerCleanupCallback = null"
      via: "default parameter resolved to GameCallbacks.AddCleanupSceneCall in production; no-op lambda in tests"
      pattern: "Action<Action>\\s+registerCleanupCallback"
    - from: "UtinniCoreDotNet/UndoRedo/UndoRedoManager.cs"
      to: "private readonly object syncRoot"
      via: "lock(syncRoot) around every stack mutation in AddUndoCommand/Undo/Redo/OnCleanupCallback"
      pattern: "lock\\s*\\(\\s*syncRoot\\s*\\)"
    - from: "UtinniCore/test_exports.cpp"
      to: "UtinniCore.dll DllExport table"
      via: "extern \"C\" __declspec(dllexport) __cdecl wrappers"
      pattern: "extern \"C\" __declspec\\(dllexport\\)"
    - from: "UtinniCoreDotNet.Tests/Clr10HarnessTests.cs"
      to: "UtinniCore.dll!utinni_clr_stop"
      via: "[DllImport(\"UtinniCore\", CallingConvention=Cdecl, EntryPoint=\"utinni_clr_stop\")]"
      pattern: "DllImport\\(\"UtinniCore\""
    - from: "UtinniCore/swg/game/game.cpp:307"
      to: "docs/ai/internals.md:218-231 AND semantics"
      via: "operator change || → &&"
      pattern: "memory::read<bool>\\(0x01908858\\) && memory::read<bool>\\(0x01919410\\)"
---

<objective>
**Single-file criticals burn-down (Wave 2).** Close C-02 (cross-CRT delete[]), C-03 (Network::cast uninitialized stack), C-05 (drag-drop static field re-wire bug), C-07 (UndoRedoManager thread-safety + dead AllowMerge + RedoCommands.Clear ordering + Phase-1-deferred testability refactor — single atomic commit per CONTEXT.md "Claude's Discretion" and 02-RESEARCH.md), C-10 (clr::stop null deref + double-call idempotence), C-11 (DirectX9 findPattern + getVtbl null checks), C-15 (CppSharp slnDir brittle Substring(-1)), C-16 (GameCallbacks delegate-pinning audit + comment fix + GC-survival regression test — resolves CON-O-03 per D-10), KB-05 (`||`→`&&` in `Game::isSafeToUse` — folds into CON-O-01 disposition per D-11).

Adds Wave-0 scaffolding to UtinniCore for test-only `extern "C" __declspec(dllexport)` wrappers (`UtinniCore/test_exports.cpp` + UtinniCore.vcxproj entry — exports must be available before P/Invoke harnesses can run). Adds five new managed-side tests (C-05, C-07) and five P/Invoke harnesses (C-02, C-03, C-10, C-11, C-16) and one pure-function test (C-15).

Depends on Plan 02-01 (Wave 1) closing with green CI — Plan 02-02 does NOT start until 02-01's CI is green on master per D-01.

Purpose: Burn down the durability-critical single-file bugs after the trivial criticals are stable. Establishes the Phase-2 P/Invoke pattern (test-only `extern "C"` wrappers in `UtinniCore/test_exports.cpp` — first time UtinniCore exports unmangled C-linkage symbols for managed test consumption). Lands the Phase-1 deferred `UndoRedoManager` testability refactor co-located with its C-07 fix (single atomic commit per 02-RESEARCH.md §C-07 "Per CONTEXT.md 'Claude's discretion': the testability refactor lands as part of the C-07 fix task").

Output: ~9 fix commits + 1 Wave-0 scaffolding commit (UtinniCore test exports + vcxproj entries) + 1 status-tracking commit. Eight new test files; one new native source file (`UtinniCore/test_exports.cpp`); updates to seven managed files and four native files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-CONTEXT.md
@.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md
@.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-VALIDATION.md
@.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-PATTERNS.md
@.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-01-PLAN.md
@.planning/intel/constraints.md
@.planning/codebase/CONVENTIONS.md
@docs/ai/assessment.md
@docs/ai/internals.md
@docs/ai/undo-redo.html

<interfaces>
<!-- Key contracts for executors. -->

From `UtinniCoreDotNet/Callbacks/GameCallbacks.cs:39-58` (C-16 surface):
- Five `private static UtinniCore.Delegates.Action_ xxxAction;` fields — these ARE the GC roots; the bug shape changes to "fix the misleading comment" not "add GCHandle.Alloc". Per RESEARCH §C-16 audit, the field approach is already valid; the change is comment-only + a regression test.
- Line 46 currently reads: `// Storing this in a variable is somehow needed to prevent corruption on WinForms resize. Very odd bug that I still don't fully understand.`
- Replace with: `// The static field acts as a GC root for the delegate passed to native via Add*Callback. Without it, the GC can collect the managed delegate while native still holds its stub, causing AVs on later callback dispatch. See https://docs.microsoft.com/dotnet/standard/native-interop/best-practices#function-pointers`

From `UtinniCoreDotNet/UndoRedo/UndoRedoManager.cs:30-50` (C-07 surface):
- Ctor signature today: `public UndoRedoManager(Action onUpdateCommandsCallback, Action undoCallback, Action redoCallback)` — line 50 inside the ctor calls `GameCallbacks.AddCleanupSceneCall(OnCleanupCallback);` directly.
- Phase-1 D-06 deferred refactor (NOW LANDING per 02-RESEARCH.md §C-07): change to `public UndoRedoManager(Action onUpdateCommandsCallback, Action undoCallback, Action redoCallback, Action<Action> registerCleanupCallback = null)` and replace the direct `GameCallbacks.AddCleanupSceneCall(OnCleanupCallback)` call with `(registerCleanupCallback ?? GameCallbacks.AddCleanupSceneCall)(OnCleanupCallback);` — preserves production behavior (default = `GameCallbacks.AddCleanupSceneCall`) and enables tests to pass a no-op lambda.

From `UtinniCoreDotNet/UndoRedo/IUndoCommand.cs:36`:
- `bool AllowMerge();` — DISPOSITION per RESEARCH §C-07: KEEP this method (documented contract in docs/ai/undo-redo.html:54-55,184-185).

From `UtinniCore/clr.cpp:93-102` (C-10 surface):
- `void stop()` body: three raw `Release()` calls on `pClrRuntimeHost`, `pClrRuntimeInfo`, `pClrMetaHost` — none null-checked, none set to nullptr after release.
- Fix per RESEARCH §C-10: `if (pX) { pX->Release(); pX = nullptr; }` for each.

From `UtinniCore/swg/graphics/directx9.cpp:297-303` (C-11 surface):
- `getVtbl()` calls `findPattern` against `GetModuleHandle("d3d9.dll")` — no null check on either `GetModuleHandle` result or `findPattern` return; then `memcpy(&vtbl, (void*)(((swgptr)pDevice) + 2), 4)` which AVs if `pDevice == nullptr`.

From `UtinniCore/utinni.h:42-46` (UTINNI_API macro) and `UtinniCore/plugin_framework/utinni_plugin.h:50` (extern "C" precedent):
- The ONLY in-tree `extern "C" __declspec(dllexport)` is on `utinni_plugin.h:50`. New test-only exports follow this pattern (UNmangled C-linkage names like `utinni_clr_stop`) — NOT the `UTINNI_API` macro (which produces mangled C++ names CppSharp consumes).

From `UtinniCoreDotNet/Utility/Native.cs:67-77` (hand-written `[DllImport]` analog):
```csharp
[DllImport("user32.dll", EntryPoint = "ReleaseCapture")]
public static extern void ReleaseCapture();
```
For Phase 2: swap `"user32.dll"` → `"UtinniCore"` (no .dll extension); add `CallingConvention = CallingConvention.Cdecl`; explicit `EntryPoint = "utinni_xxx"`.

From `UtinniCoreDotNetGen/Program.cs:39-41` (C-15 surface):
- Current: `string slnDir = Directory.GetParent(workingDir.Substring(0, workingDir.LastIndexOf("\\bin\\"))).FullName + "\\";` — `Substring(0, -1)` throws when `\bin\` not in path.

From `UtinniCore/swg/game/game.cpp:305-308` (KB-05 surface):
```cpp
bool Game::isSafeToUse()
{
    return memory::read<bool>(0x01908858) || memory::read<bool>(0x01919410);
}
```
Change `||` to `&&` per docs/ai/internals.md:218-231 ("Both must be true" AND semantics).

From `UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj` (Phase-1 wiring):
- `[InternalsVisibleTo("UtinniCoreDotNet.Tests")]` was added to UtinniCoreDotNet/Properties/AssemblyInfo.cs in Plan 02-01 Task 1 — enables tests to invoke `internal` members on UtinniCoreDotNet.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 (Wave 0 scaffolding): Create UtinniCore/test_exports.cpp + wire into UtinniCore.vcxproj</name>
  <files>
    UtinniCore/test_exports.cpp,
    UtinniCore/UtinniCore.vcxproj,
    UtinniCore/UtinniCore.vcxproj.filters
  </files>
  <read_first>
    UtinniCore/utinni.cpp:1-30 (verify MIT header verbatim + standard include block + namespace pattern),
    UtinniCore/utinni.h (full file — `UTINNI_API` macro definition at :42-46, namespace conventions),
    UtinniCore/plugin_framework/utinni_plugin.h:1-52 (full file — only in-tree `extern "C" __declspec(dllexport)` precedent),
    UtinniCore/UtinniCore.vcxproj (full file — particularly the `<ItemGroup>` blocks listing `<ClCompile>` and `<ClInclude>` entries; understand the existing pattern for adding a new .cpp file),
    UtinniCore/UtinniCore.vcxproj.filters (full file — filter ItemGroups that mirror the .vcxproj source list),
    UtinniCore/clr.cpp:1-50 (verify `clr::` namespace + the `stop()` signature at :93),
    UtinniCore/swg/misc/network.cpp:60-69 (verify `Network::cast` and the pCast typedef on line 36),
    UtinniCore/swg/misc/config.cpp:50-80 (verify the buffer-allocation surface for the C-02 test-only wrapper),
    UtinniCore/swg/graphics/directx9.cpp:290-310 (verify `getVtbl()` + `memory::findPattern` signature),
    UtinniCore/utility/memory.h (or wherever `memory::findPattern` is declared — verify signature for the C-11 test wrapper),
    UtinniCore/swg/game/game.h (or wherever `Game::install`/install callback registry is declared — needed for `utinni_triggerInstallCallbacks` design),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-PATTERNS.md §4 "Native exports" (complete — has the test_exports.cpp template with all six functions)
  </read_first>
  <action>
    Create `UtinniCore/test_exports.cpp` as a new source file containing the test-only `extern "C" __declspec(dllexport) __cdecl` wrappers required by Tasks 2, 3, 5, 6, and 9 of this plan. File header is the 23-line MIT header verbatim from `UtinniCore/utinni.cpp:1-23`. Add `// NOTE: Test-only exports for UtinniCoreDotNet.Tests P/Invoke harnesses (Phase 2). Do not consume from production code; CppSharp generation skips this file because the symbols use C linkage, not mangled C++ linkage.` directly under the header.

    Include the necessary headers (`<Windows.h>`, the clr header, the network header, the config header, the directx9 header, and any UtinniCore namespace headers needed for `Game::triggerInstallCallbacks`). Then declare these wrappers, one per bug:

    - `utinni_clr_stop()` — calls `clr::stop()`. Signature: `extern "C" __declspec(dllexport) void __cdecl utinni_clr_stop()`.
    - `utinni_findPattern(const uint8_t* buffer, size_t bufferLen, const char* pattern, const char* mask)` — returns `uintptr_t`; forwards to `utinni::memory::findPattern(buffer, bufferLen, pattern, mask)` per the in-tree signature (verify exact return type and param order from utility/memory.h).
    - `utinni_getVtbl()` — returns `uintptr_t` (the vtbl pointer cast from `swgptr*`); forwards to `swg::graphics::directx9::getVtbl()` or the equivalent namespace location (check directx9.cpp:297 for the actual qualified name). Returns 0 if internal call returns nullptr.
    - `utinni_test_freeConfigBuffer(unsigned char* /*data*/, void* /*pFile*/)` — a no-op stub returning `bool` `true` if the call completes; its purpose is to assert "we removed the cross-CRT delete[]" partial-proof per RESEARCH §C-02. The wrapper exposes the affected fix surface as a testable function pointer — exact body: simply does nothing and returns true. The test then asserts the wrapper returns without crashing when called with a synthetic non-zero buffer pointer.
    - `utinni_test_networkCast(int id)` — calls `swg::network::cast(&out, id, (id >> 32))` where `out` is a local `swgptr` initialized to 0; returns `out` cast to `int64_t`. Mirrors the C-03 fixed `Network::cast` shape. Test asserts the returned value is `!= 0xCCCCCCCC` (MSVC debug-init pattern). NOTE: this wrapper calls SWG at hard RVA 0xAA4900; the test runs in `dotnet test` process where SWG is not loaded — the call WILL crash if invoked. Therefore the wrapper SHOULD be a static post-condition check on its own (initialize `out`, set it to a sentinel `0xDEADBEEF`, return it without calling SWG) so the test runs in the test process. Document this in the file header comment.
    - `utinni_triggerInstallCallbacks()` — walks the install-callback registry that `GameCallbacks.AddInstallCallback` registers into, and invokes each handler. For C-16 GC-survival test: register a handler from managed; force `GC.Collect()`; call this wrapper; assert the handler still fires. The implementation iterates whatever the existing callback-registry mechanism is in `UtinniCore` (read `swg/game/game.cpp` and related to find the install callback list — likely a `std::vector<pFn>` or similar). If the registry is private to the namespace, add a small friend export here.

    Update `UtinniCore/UtinniCore.vcxproj`: add a `<ClCompile Include="test_exports.cpp" />` entry in the same `<ItemGroup>` that lists the other `.cpp` files (preserve alphabetical ordering if the file uses it; otherwise append). Configure the entry for all three platform/config combinations the project supports.

    Update `UtinniCore/UtinniCore.vcxproj.filters`: add a `<ClCompile Include="test_exports.cpp"><Filter>...</Filter></ClCompile>` entry in the appropriate filter group (typically a root "Source Files" filter or similar — match the pattern used for `utinni.cpp` since the new file is at the same root level).

    Build verification: `msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86` must succeed. After build, `dumpbin /exports bin\Release\UtinniCore.dll | findstr utinni_` must list the six new symbols: `utinni_clr_stop`, `utinni_findPattern`, `utinni_getVtbl`, `utinni_test_freeConfigBuffer`, `utinni_test_networkCast`, `utinni_triggerInstallCallbacks`. If any symbol is mangled (starts with `?`), the `extern "C"` linkage on that wrapper is wrong — fix before proceeding.

    Note: Per CONTEXT.md §canonical_refs preservation guard, CON-N-04 (memory::copy VirtualProtect bracket) must remain intact — `test_exports.cpp` does NOT touch `memory::copy`; only `memory::findPattern` (pure read).

    Commit message: `chore(02-02): wave-0 scaffolding — UtinniCore/test_exports.cpp (extern "C" wrappers for P/Invoke harnesses)`.
  </action>
  <verify>
    <automated>msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86 2>&amp;1 | findstr /R "error\|Error\|FAILED" ; dumpbin /exports bin\Release\UtinniCore.dll | findstr utinni_</automated>
  </verify>
  <acceptance_criteria>
    - `dumpbin /exports bin/Release/UtinniCore.dll` lists `utinni_clr_stop`, `utinni_findPattern`, `utinni_getVtbl`, `utinni_test_freeConfigBuffer`, `utinni_test_networkCast`, `utinni_triggerInstallCallbacks` (six exports, all unmangled — no leading `?` or `@@YA` suffix).
    - `grep -nE 'extern "C" __declspec\(dllexport\)' UtinniCore/test_exports.cpp` returns at least six matches.
    - The UtinniCoreDotNet.Tests project still builds and the existing Plan 02-01 tests still pass.
    - `msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86` exits 0.
    - CI on master is green after this commit.
    - Commit message starts with `chore(02-02):`.
    - The 23-line MIT header in `test_exports.cpp` matches `UtinniCore/utinni.cpp:1-23` byte-for-byte.
  </acceptance_criteria>
  <done>
    `test_exports.cpp` exists and is wired into UtinniCore.vcxproj + filters; the six test-only exports are visible in dumpbin output; CI is green; downstream P/Invoke tasks (Tasks 2, 3, 5, 6, 9) can now reference these entry points.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: C-02 — Remove cross-CRT delete[] in config override; partial-proof test</name>
  <files>
    UtinniCore/swg/misc/config.cpp,
    UtinniCore/test_exports.cpp,
    UtinniCoreDotNet.Tests/ConfigBufferFreeTests.cs
  </files>
  <read_first>
    UtinniCore/swg/misc/config.cpp:50-80 (hkLoadOverrideConfig — line 59-76; specifically line 69 read-via-vtable-slot-36, line 71 delete[] data, line 72 close-via-vtable-slot-0),
    UtinniCore/test_exports.cpp (from Task 1 — utinni_test_freeConfigBuffer stub to verify the wrapper signature),
    UtinniCoreDotNet.Tests/HotkeyTests.cs:1-68 (xUnit analog),
    UtinniCoreDotNet/Utility/Native.cs:67-77 (P/Invoke shape),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §C-02 (fix shape — disposition is to NOT free at all; file dtor at vtable slot 0 owns the buffer; CON-B-04 cross-CRT discipline preserved),
    .planning/intel/constraints.md (CON-B-04 cross-CRT discipline; CON-H-01..-05 class-of-bug constraints)
  </read_first>
  <behavior>
    Test 1 (RED initially — but it goes green as soon as Task 1's stub wrapper compiles, since the wrapper is a no-op): `ConfigBufferFreeTests.FreeConfigBuffer_OnSyntheticBuffer_DoesNotCrash` — P/Invoke `utinni_test_freeConfigBuffer` from UtinniCore.dll with a non-zero buffer pointer; assert no exception thrown AND the wrapper returns true. This is a partial proof per D-05 — it asserts "the fixed code path (no delete[]) does not crash on a synthetic call"; the full CRT-mismatch detection remains Tier 4 manual (user_setup above).
  </behavior>
  <action>
    Edit `UtinniCore/swg/misc/config.cpp:71` to DELETE the `delete[] data;` line. Per RESEARCH §C-02 disposition: do NOT free at all — the file dtor (vtable slot 0 call on the next line) owns the buffer. Add a comment above the removed line: `// C-02: Do NOT delete[] data here — buffer is owned by the SWG TreeFile (allocated with SWG's CRT). The file dtor below (vtable slot 0 call) releases it. Per CON-B-04 cross-CRT discipline.`

    Verify Task 1's `utinni_test_freeConfigBuffer` stub still exists and matches the signature `extern "C" __declspec(dllexport) bool __cdecl utinni_test_freeConfigBuffer(unsigned char* data, void* pFile)`. The stub body simply returns `true` (it's a partial-proof harness, not a real free path — see RESEARCH §C-02 caveat about CRT-mismatch detection requiring a Tier-4 fixture).

    Create `UtinniCoreDotNet.Tests/ConfigBufferFreeTests.cs`: 23-line MIT header; `using System;` + `using System.Runtime.InteropServices;` + `using Xunit;`; namespace `UtinniCoreDotNet.Tests`; class `ConfigBufferFreeTests`. Private nested static `NativeBridge` class with `[DllImport("UtinniCore", CallingConvention = CallingConvention.Cdecl, EntryPoint = "utinni_test_freeConfigBuffer")] public static extern bool Utinni_TestFreeConfigBuffer(IntPtr data, IntPtr pFile);`. One test method: pin a `byte[16]` via `GCHandle.Alloc(buf, GCHandleType.Pinned)`, call the wrapper with `handle.AddrOfPinnedObject()` for both args, assert returns true, free handle. `Record.Exception` check around the call asserts no AV.

    Update `docs/ai/assessment.md` §"Status tracking" row C-02 → `done` with commit SHA.

    Commit message: `fix(C-02): drop cross-CRT delete[] in hkLoadOverrideConfig; TreeFile dtor owns the buffer (CON-B-04 preserved)`.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release --filter FullyQualifiedName~ConfigBufferFreeTests</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter FullyQualifiedName~ConfigBufferFreeTests` passes.
    - `grep -nE 'delete\[\]\s+data' UtinniCore/swg/misc/config.cpp` returns 0 matches (the delete[] is removed).
    - `grep -nE 'C-02:' UtinniCore/swg/misc/config.cpp` returns at least one match (the comment is present).
    - `grep -nE 'memory::copy' UtinniCore/utility/memory.cpp` is unchanged from pre-task (CON-N-04 preservation — code-review check; the C-02 fix must not touch memory.cpp).
    - `git diff HEAD~1 UtinniCore/utility/memory.cpp` returns empty for this commit.
    - CI on master is green after this commit.
    - Commit message starts with `fix(C-02):`.
  </acceptance_criteria>
  <done>
    Cross-CRT delete[] removed; partial-proof harness asserts no crash on the fixed surface; full CRT-mismatch proof remains in Tier 4 manual residual.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: C-03 — Network::cast initializes networkId + returns OUT param; post-condition wrapper test</name>
  <files>
    UtinniCore/swg/misc/network.cpp,
    UtinniCore/test_exports.cpp,
    UtinniCoreDotNet.Tests/NetworkCastTests.cs
  </files>
  <read_first>
    UtinniCore/swg/misc/network.cpp:60-69 (Network::cast surface — lines 65-69 — note the double semicolon `...;;` on line 68 + the discarded return value bug),
    UtinniCore/swg/misc/network.cpp:30-40 (pCast typedef on line 36 — `int64_t(__thiscall*)(swgptr*, int, int)` — confirms first param is OUT pointer),
    UtinniCore/test_exports.cpp (from Task 1 — `utinni_test_networkCast` stub),
    .planning/codebase/CONCERNS.md TD-03 (Network::cast bug history),
    UtinniCoreDotNet.Tests/HotkeyTests.cs:1-68 (xUnit analog),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §C-03 (three-line fix; OUT-param read pattern; hard RVA 0xAA4900 means full proof is Tier 4)
  </read_first>
  <behavior>
    Test 1: `NetworkCastTests.NetworkCast_Wrapper_ReturnsInitializedValue` — P/Invoke `utinni_test_networkCast` with a synthetic id (e.g. `0x12345678`); assert the returned `long` is `!= 0xCCCCCCCCL` (the MSVC debug-init pattern that indicated the original uninitialized-stack bug) AND `!= 0L` (the bug shape where SWG didn't write through `&networkId`). Per Task 1 the wrapper sets a sentinel `0xDEADBEEF` to avoid calling SWG; assert the returned value equals the sentinel (catches regression of any future "wrapper was rewired to call SWG without proper init" change).
  </behavior>
  <action>
    Edit `UtinniCore/swg/misc/network.cpp:65-69` per 02-RESEARCH.md §C-03. Replace the three-line body with:
    - Line A: `swgptr networkId = 0;`   (initialize to defined value — was uninitialized)
    - Line B: `swg::network::cast(&networkId, id, (id >> 32));`   (single semicolon — was `;;`; discard return value)
    - Line C: `return networkId;`   (return the OUT param, not the function return)

    Add a comment above the body: `// C-03: networkId must be initialized; SWG cast writes through &networkId; the function's int64_t return is unreliable per CONCERNS.md TD-03 — read networkId after the call.`

    Verify Task 1's `utinni_test_networkCast` stub returns the sentinel `0xDEADBEEF` so the test runs in the test process without invoking SWG.

    Create `UtinniCoreDotNet.Tests/NetworkCastTests.cs`: 23-line MIT header; using order; private `NativeBridge` with `[DllImport("UtinniCore", CallingConvention = CallingConvention.Cdecl, EntryPoint = "utinni_test_networkCast")] public static extern long Utinni_TestNetworkCast(int id);`. One test: assert `result != 0xCCCCCCCCL && result != 0L && result == 0xDEADBEEFL`.

    Update `docs/ai/assessment.md` §"Status tracking" row C-03 → `done` with commit SHA.

    Commit message: `fix(C-03): Network::cast initializes networkId + returns OUT param (was reading uninit stack)`.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release --filter FullyQualifiedName~NetworkCastTests</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter FullyQualifiedName~NetworkCastTests` passes.
    - `grep -nE 'swgptr networkId\s*=\s*0' UtinniCore/swg/misc/network.cpp` returns one match (initialization).
    - `grep -nE 'return networkId' UtinniCore/swg/misc/network.cpp` returns one match.
    - `grep -nE ';;' UtinniCore/swg/misc/network.cpp` returns 0 matches (double-semicolon typo gone).
    - CI on master is green.
    - Commit message starts with `fix(C-03):`.
  </acceptance_criteria>
  <done>
    Network::cast initializes its OUT param and returns it; partial-proof harness asserts the fixed signature; full live-SWG proof remains in Tier 4 manual residual.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: C-05 — GameDragDropEventHandlers converts to static events with single-time forwarder</name>
  <files>
    UtinniCoreDotNet/UI/GameDragDropEventHandlers.cs,
    UtinniCoreDotNet/UI/Controls/PanelGame.cs,
    UtinniCoreDotNet.Tests/GameDragDropEventHandlersTests.cs
  </files>
  <read_first>
    UtinniCoreDotNet/UI/GameDragDropEventHandlers.cs (full file — static field declarations + Initialize method),
    UtinniCoreDotNet/UI/Controls/PanelGame.cs:60-80 (Initialize call site at :68),
    UtinniCoreDotNet.Tests/HotkeyTests.cs:1-68 (xUnit analog; `using System.Windows.Forms;` already in place from Phase 1),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §C-05 (fix shape — convert static fields to `static event` + lambda forwarder pattern),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-PATTERNS.md §5 row "C-05"
  </read_first>
  <behavior>
    Test 1: `GameDragDropEventHandlersTests.OnDragDrop_SubscriberAfterInitialize_ReceivesEvent` — instantiate a `PanelGame`; call `GameDragDropEventHandlers.Initialize(panel)`; subscribe a handler via `GameDragDropEventHandlers.OnDragDrop += (s, e) => fired = true;`; synthesize a `DragEventArgs` (constructor `new DragEventArgs(dataObj, 0, 0, 0, DragDropEffects.None, DragDropEffects.None)`); invoke the panel's protected `OnDragDrop` via `typeof(Control).GetMethod("OnDragDrop", BindingFlags.NonPublic | BindingFlags.Instance).Invoke(panel, new object[] { args })`; assert `fired == true`.
    Test 2: `OnDragEnter_SubscriberAfterInitialize_ReceivesEvent` — same shape for `DragEnter`.
    Test 3: `OnDragDrop_MultipleSubscribers_AllReceive` — subscribe two handlers; trigger; both fire.
    Test 4: `OnDragDrop_NoSubscribers_DoesNotCrash` — call Initialize, invoke OnDragDrop with no handler subscribed; assert no NullReferenceException (the `?.Invoke` null-conditional handles it).
  </behavior>
  <action>
    Rewrite `UtinniCoreDotNet/UI/GameDragDropEventHandlers.cs` per 02-RESEARCH.md §C-05. Replace the four `public static DragEventHandler/EventHandler` field declarations with `public static event DragEventHandler OnDragDrop;` / `public static event DragEventHandler OnDragEnter;` / `public static event EventHandler OnDragLeave;` / `public static event DragEventHandler OnDragOver;`. Inside `Initialize(PanelGame panelGame)`, wire each panel-level handler to a lambda forwarder: `panelGame.DragDrop += (s, e) => OnDragDrop?.Invoke(s, e);` (etc.). The lambda captures the static event symbol — each call dereferences the live event field, so subsequent `OnDragDrop += handler` properly extends the invocation list.

    Verify call site at `UtinniCoreDotNet/UI/Controls/PanelGame.cs:68` continues to call `GameDragDropEventHandlers.Initialize(this)` — no change needed there.

    Create `UtinniCoreDotNet.Tests/GameDragDropEventHandlersTests.cs` with the four tests. Use `using System.Windows.Forms;` + `using System.Reflection;` + `using Xunit;`. For DragEventArgs synthesis use `new DragEventArgs(null, 0, 0, 0, DragDropEffects.None, DragDropEffects.None)` — the `IDataObject` arg can be null for the test. Use reflection to invoke the protected `OnDragDrop` (per the canonical pattern in 02-RESEARCH.md §C-05). Reset subscribers between tests by setting `OnDragDrop = null;` etc. in a teardown helper or by using xUnit `IClassFixture` / fresh `PanelGame` instances per test.

    Update `docs/ai/assessment.md` §"Status tracking" row C-05 → `done` with commit SHA.

    Commit message: `fix(C-05): GameDragDropEventHandlers uses static event + forwarder lambda (subscribers wired after Initialize now receive events)`.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release --filter FullyQualifiedName~GameDragDropEventHandlersTests</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter FullyQualifiedName~GameDragDropEventHandlersTests` passes all four tests.
    - `grep -nE 'public static event' UtinniCoreDotNet/UI/GameDragDropEventHandlers.cs` returns at least four matches.
    - `grep -nE 'public static DragEventHandler\s+\w+\s*;' UtinniCoreDotNet/UI/GameDragDropEventHandlers.cs` returns 0 matches (raw field declarations replaced).
    - `grep -nE '\?\.Invoke\(s, e\)' UtinniCoreDotNet/UI/GameDragDropEventHandlers.cs` returns at least four matches (forwarder lambdas).
    - `UtinniCoreDotNet/UI/Controls/PanelGame.cs:68` still calls `GameDragDropEventHandlers.Initialize(this)`.
    - CI on master is green.
    - Commit message starts with `fix(C-05):`.
  </acceptance_criteria>
  <done>
    Drag-drop event handlers now correctly re-fire on subscribers wired after `Initialize`; four WinForms-fixture tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: C-07 — UndoRedoManager thread-safety + AllowMerge + RedoCommands.Clear ordering + Phase-1 deferred testability seam (single atomic commit)</name>
  <files>
    UtinniCoreDotNet/UndoRedo/UndoRedoManager.cs,
    UtinniCoreDotNet/UndoRedo/IUndoCommand.cs,
    UtinniCoreDotNet.Tests/UndoRedoManagerTests.cs
  </files>
  <read_first>
    UtinniCoreDotNet/UndoRedo/UndoRedoManager.cs (full file — ctor at :30-50, AddUndoCommand at :60-74, Undo at :76-87, Redo at :97-108, OnCleanupCallback),
    UtinniCoreDotNet/UndoRedo/IUndoCommand.cs (full file — line 36 has AllowMerge() declaration),
    UtinniCoreDotNet/Callbacks/GameCallbacks.cs (full file — AddCleanupSceneCall signature; the ctor injects this as a default for the testability seam),
    docs/ai/undo-redo.html (lines 54-55 + 146-167 + 184-185 — AllowMerge contract reference; KEEP disposition per RESEARCH §C-07),
    UtinniCoreDotNet.Tests/HotkeyTests.cs:1-68 (xUnit analog),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §C-07 (complete fix shape — lock(syncRoot), AllowMerge before Merge, RedoCommands.Clear after merge check, Action<Action> testability seam),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-PATTERNS.md §5 rows "C-07" (both rows — UndoRedoManager.cs and IUndoCommand.cs),
    .planning/intel/constraints.md (CON-M-05 — UndoRedoManager.OnCleanupCallback clear-on-scene-cleanup must remain),
    .planning/phases/01-ci-tier-1-c-scaffold/01-CONTEXT.md (Phase-1 D-06 — the deferred testability refactor is the registerCleanupCallback parameter, lands here per RESEARCH recommendation)
  </read_first>
  <behavior>
    Test 1: `AddUndoCommand_ConcurrentPushFromMultipleThreads_NoRaceConditions` — spawn 10 threads, each push 100 fake `IUndoCommand`s via the editor-plugin event seam; await all complete; assert `UndoCommands.Count == 1000` AND no thrown exception in any thread.
    Test 2: `AddUndoCommand_MergeReturnsTrue_DoesNotClearRedoStack` — construct `UndoRedoManager(no-op callbacks, _ => {})` with the no-op `registerCleanupCallback`; push one initial `MergeableCommand` where `AllowMerge() => true` and `Merge(_) => true`; push three commands onto `RedoCommands` via reflection or direct stack access; trigger another `AddUndoCommand` that should merge into the top; assert `RedoCommands.Count == 3` (unchanged — merged command absorbed, no new push, no Clear).
    Test 3: `AddUndoCommand_NewCommandActuallyPushed_DoesClearRedoStack` — same setup but `MergeableCommand.Merge() => false`; push initial command; push three onto `RedoCommands`; trigger another `AddUndoCommand`; assert `RedoCommands.Count == 0` AND `UndoCommands.Count == 2`.
    Test 4: `AddUndoCommand_NoExistingTop_PushesAndClearsRedo` — first ever AddUndoCommand (UndoCommands empty); push three onto `RedoCommands` first; trigger AddUndoCommand; assert `RedoCommands.Count == 0` AND `UndoCommands.Count == 1`.
    Test 5: `OnCleanupCallback_ClearsBothStacks_PreservedCONM05` — push some commands onto both stacks via the test seam; trigger the injected cleanup callback (capture the Action passed to `registerCleanupCallback` lambda); assert both stacks are empty.
  </behavior>
  <action>
    Refactor `UtinniCoreDotNet/UndoRedo/UndoRedoManager.cs` per 02-RESEARCH.md §C-07 (the code shape is fully specified there). Changes:

    1. Add `private readonly object syncRoot = new object();` field.
    2. Change ctor signature to accept `Action<Action> registerCleanupCallback = null` as a new last parameter. Replace the direct `GameCallbacks.AddCleanupSceneCall(OnCleanupCallback);` call at line 50 with `(registerCleanupCallback ?? GameCallbacks.AddCleanupSceneCall)(OnCleanupCallback);`. This is the Phase-1 D-06 deferred testability seam — production code passes nothing (uses default); tests pass a no-op `_ => {}`.
    3. Refactor `AddUndoCommand` to lock `syncRoot` during stack mutation. Inside the lock: call `Peek().AllowMerge()` BEFORE attempting `Peek().Merge(args.UndoCommand)` (was: `Merge` was called directly without the gate per RESEARCH §C-07). Move `RedoCommands.Clear()` to AFTER the merge check — only clear when a new command is actually being pushed (TD-29 sub-bug). After releasing the lock, call `onUpdateCommandsCallback()` outside the lock.
    4. Refactor `Undo()` and `Redo()` similarly — lock during stack pop/push, release lock BEFORE calling `cmd.Undo()` / `cmd.Execute()` and `undoCallback()` / `redoCallback()` to avoid re-entrancy deadlock through `GroundSceneCallbacks.AddUpdateLoopCall`.
    5. `OnCleanupCallback`: wrap the `UndoCommands.Clear()` + `RedoCommands.Clear()` calls in `lock(syncRoot)`; call `onUpdateCommandsCallback()` outside the lock. **CON-M-05 preservation:** the cleanup behavior is preserved verbatim — only wrapped in the lock.

    Leave `UtinniCoreDotNet/UndoRedo/IUndoCommand.cs:36` `AllowMerge()` declaration UNCHANGED — disposition per RESEARCH §C-07 is KEEP (documented contract in undo-redo.html; canonical SetTimeOfDayCommand example demonstrates value; removing would be a breaking change for plugin authors). Add a comment above the AllowMerge declaration: `// Returns true if this command may absorb the next command into itself via Merge(). Per docs/ai/undo-redo.html §AllowMerge, called by UndoRedoManager.AddUndoCommand BEFORE Merge() to gate cheap merging of e.g. time-of-day slider drags.`

    Create `UtinniCoreDotNet.Tests/UndoRedoManagerTests.cs` with the five tests above. Define a private nested `MergeableCommand : IUndoCommand` fixture class in the test file with configurable `AllowMerge()` and `Merge()` return values (constructor params). Construct `UndoRedoManager` with `(() => {}, () => {}, () => {}, _ => {})` — the no-op `registerCleanupCallback` makes the test fully unit-isolated (no native GameCallbacks invocation). For Test 5, capture the cleanup action via a local variable: `Action capturedCleanup = null; var mgr = new UndoRedoManager(..., registerCleanupCallback: a => capturedCleanup = a);` then invoke `capturedCleanup()` in the test body.

    Update `docs/ai/assessment.md` §"Status tracking" row C-07 → `done` with commit SHA. Note in the row that this commit also closes the Phase-1 D-06 deferred work + the TD-29 RedoCommands.Clear ordering sub-bug.

    Commit message: `fix(C-07): UndoRedoManager thread-safety + AllowMerge gate + RedoCommands.Clear ordering + Phase-1 D-06 testability seam (single atomic commit per RESEARCH)`.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release --filter FullyQualifiedName~UndoRedoManagerTests</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter FullyQualifiedName~UndoRedoManagerTests` passes all five tests.
    - `grep -nE 'private readonly object syncRoot' UtinniCoreDotNet/UndoRedo/UndoRedoManager.cs` returns one match.
    - `grep -cE 'lock\s*\(\s*syncRoot\s*\)' UtinniCoreDotNet/UndoRedo/UndoRedoManager.cs` returns at least 4 (AddUndoCommand + Undo + Redo + OnCleanupCallback).
    - `grep -nE 'Action<Action>\s+registerCleanupCallback' UtinniCoreDotNet/UndoRedo/UndoRedoManager.cs` returns one match (the Phase-1 D-06 testability seam).
    - `grep -nE 'AllowMerge\(\)\s*&&' UtinniCoreDotNet/UndoRedo/UndoRedoManager.cs` returns one match (the gate is called before Merge).
    - `grep -B1 -A5 'AllowMerge' UtinniCoreDotNet/UndoRedo/IUndoCommand.cs | grep -c 'bool AllowMerge'` returns 1 (interface method preserved).
    - The order of statements in `AddUndoCommand`'s handler ensures `RedoCommands.Clear()` runs AFTER the merge check (verify by reading the method body — should see merge-attempt-block first, then Clear-then-Push block).
    - CI on master is green.
    - Commit message starts with `fix(C-07):` and references Phase-1 D-06 + TD-29 in the body.
  </acceptance_criteria>
  <done>
    UndoRedoManager is thread-safe, AllowMerge gate is honored, RedoCommands.Clear is ordered after merge check, and the Phase-1 deferred testability seam is shipped in the same atomic commit; CON-M-05 preserved; five tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: C-10 — clr::stop null-checked idempotent shutdown + P/Invoke double-call harness</name>
  <files>
    UtinniCore/clr.cpp,
    UtinniCoreDotNet.Tests/Clr10HarnessTests.cs
  </files>
  <read_first>
    UtinniCore/clr.cpp (full file — focus on stop() at :93-102 + start() at :40-90 to understand the failure-path null assignment + global pClr* pointer declarations),
    UtinniCore/utinni.cpp:130-150 (detatch + DllMain DLL_PROCESS_DETACH calling clr::stop()),
    UtinniCore/test_exports.cpp (from Task 1 — utinni_clr_stop wrapper),
    UtinniCoreDotNet.Tests/HotkeyTests.cs:1-68 (xUnit analog),
    UtinniCoreDotNet/Utility/Native.cs:67-77 (P/Invoke shape),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §C-10 (fix shape — three-line idempotent shutdown; null check + Release + set to nullptr)
  </read_first>
  <behavior>
    Test 1: `Clr10HarnessTests.ClrStop_CalledTwiceConsecutively_NoAccessViolation` — P/Invoke `utinni_clr_stop` twice in succession; wrap in `Record.Exception`; assert null. The first call may or may not have anything to release (depends on whether CLR was started in the test process — typically NOT); the second call definitely hits the null pointer path and would AV pre-fix.
  </behavior>
  <action>
    Edit `UtinniCore/clr.cpp:93-102`. Replace the three raw `Release()` calls with null-checked-release-and-null per 02-RESEARCH.md §C-10:
    - `if (pClrRuntimeHost) { pClrRuntimeHost->Release(); pClrRuntimeHost = nullptr; }`
    - `if (pClrRuntimeInfo) { pClrRuntimeInfo->Release(); pClrRuntimeInfo = nullptr; }`
    - `if (pClrMetaHost) { pClrMetaHost->Release(); pClrMetaHost = nullptr; }`

    Add a comment above the body: `// C-10: Idempotent shutdown. start() may have failed before allocating each pointer; detatch() (DLL_PROCESS_DETACH) calls stop() unconditionally so the body must handle null and survive multiple calls without AV.`

    Create `UtinniCoreDotNet.Tests/Clr10HarnessTests.cs` with the one test. Use `[DllImport("UtinniCore", CallingConvention = CallingConvention.Cdecl, EntryPoint = "utinni_clr_stop")] public static extern void Utinni_ClrStop();` in a private `NativeBridge` class.

    Update `docs/ai/assessment.md` §"Status tracking" row C-10 → `done` with commit SHA.

    Commit message: `fix(C-10): clr::stop is null-checked and idempotent (no AV on double-call from DllMain detach)`.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release --filter FullyQualifiedName~Clr10HarnessTests</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter FullyQualifiedName~Clr10HarnessTests` passes.
    - `grep -cE 'if\s*\(\s*pClr(RuntimeHost|RuntimeInfo|MetaHost)\s*\)' UtinniCore/clr.cpp` returns at least 3 matches.
    - `grep -cE 'pClr(RuntimeHost|RuntimeInfo|MetaHost)\s*=\s*nullptr' UtinniCore/clr.cpp` returns at least 3 matches.
    - CI on master is green.
    - Commit message starts with `fix(C-10):`.
  </acceptance_criteria>
  <done>
    clr::stop is null-safe and idempotent; calling it twice no longer AVs; harness asserts the property via P/Invoke double-call.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: C-11 — DirectX9 getVtbl null-checks for GetModuleHandle + findPattern; bail with log::critical</name>
  <files>
    UtinniCore/swg/graphics/directx9.cpp,
    UtinniCoreDotNet.Tests/FindPatternHarnessTests.cs
  </files>
  <read_first>
    UtinniCore/swg/graphics/directx9.cpp:290-320 (getVtbl + detour — full surrounding context),
    UtinniCore/swg/graphics/directx9.cpp:210-241 (hkPresent + the BlockPresent/IsPresentBlocked pair — for awareness; C-09 will touch this in Plan 02-04, do NOT change here),
    UtinniCore/utility/memory.h (memory::findPattern signature; verify return type is pointer/uintptr_t),
    UtinniCore/utility/memory.cpp:63-72 (memory::copy — DO NOT TOUCH; CON-N-04 preservation; verify in commit diff),
    UtinniCore/test_exports.cpp (from Task 1 — utinni_findPattern + utinni_getVtbl wrappers),
    UtinniCoreDotNet.Tests/HotkeyTests.cs:1-68 (xUnit analog),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §C-11 (fix shape — null-check GetModuleHandle + null-check findPattern; bail with log::critical; preservation note for CON-N-04),
    .planning/intel/constraints.md (CON-N-04 — utility/memory::copy VirtualProtect bracket must remain intact)
  </read_first>
  <behavior>
    Test 1: `FindPatternHarnessTests.FindPattern_PatternAbsent_ReturnsZero` — allocate a `byte[0x1000]` of zeros, pin it, call `utinni_findPattern` with a non-matching pattern (e.g. `"\xDE\xAD\xBE\xEF"` with mask `"xxxx"`); assert returned pointer/uintptr is 0.
    Test 2: `FindPatternHarnessTests.FindPattern_PatternPresent_ReturnsNonZero` — allocate a buffer where the pattern exists at a known offset; assert returned pointer points into the buffer (positive control).
    Test 3: `GetVtbl_WithoutD3d9Loaded_ReturnsNullAndLogsCritical` — call `utinni_getVtbl` in the test process where `d3d9.dll` is NOT loaded; assert return is 0; (optional) assert a log line was emitted via a captured log sink if the in-process logging is wired.
  </behavior>
  <action>
    Edit `UtinniCore/swg/graphics/directx9.cpp:297-303` (`getVtbl()`). Restructure per 02-RESEARCH.md §C-11:
    1. Capture `HMODULE hD3d9 = GetModuleHandle("d3d9.dll");` first.
    2. If `hD3d9 == nullptr`, emit `utinni::log::critical("DirectX9 hook installation failed: d3d9.dll not loaded yet");` and return `nullptr`.
    3. Call `memory::findPattern((swgptr)hD3d9, 0x128000, "\xC7\x06\x00\x00\x00\x00\x89\x86\x00\x00\x00\x00\x89\x86", "xx????xx????xx")` and store in `pDevice`.
    4. If `pDevice == nullptr`, emit `utinni::log::critical("DirectX9 hook installation failed: vtable pattern not found in d3d9.dll");` and return `nullptr`.
    5. Only if both null-checks pass: `memcpy(&vtbl, (void*)(((swgptr)pDevice) + 2), 4); return vtbl;`

    Also update `detour()` (or the caller of `getVtbl()`) to bail if `getVtbl()` returns nullptr — adding `auto vtbl = getVtbl(); if (vtbl == nullptr) return;` at the top.

    **DO NOT TOUCH `UtinniCore/utility/memory.cpp`** — CON-N-04 preservation. Verify by running `git diff HEAD UtinniCore/utility/memory.cpp` after the commit; it must show no changes.

    Create `UtinniCoreDotNet.Tests/FindPatternHarnessTests.cs` with the three tests. Use `GCHandle.Alloc(byteBuffer, GCHandleType.Pinned)` to pass managed arrays to native; pass `handle.AddrOfPinnedObject()` for the buffer pointer arg; free the handle in a `finally` block. Verify `utinni_findPattern` signature in `NativeBridge` matches the wrapper: `[DllImport("UtinniCore", CallingConvention = CallingConvention.Cdecl, EntryPoint = "utinni_findPattern")] public static extern UIntPtr Utinni_FindPattern(IntPtr buffer, UIntPtr bufferLen, [MarshalAs(UnmanagedType.LPStr)] string pattern, [MarshalAs(UnmanagedType.LPStr)] string mask);` (adjust types based on actual signature in test_exports.cpp from Task 1).

    Update `docs/ai/assessment.md` §"Status tracking" row C-11 → `done` with commit SHA.

    Commit message: `fix(C-11): directx9::getVtbl null-checks GetModuleHandle + findPattern; bail with log::critical (CON-N-04 preserved)`.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release --filter FullyQualifiedName~FindPatternHarnessTests &amp;&amp; git diff HEAD UtinniCore/utility/memory.cpp</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter FullyQualifiedName~FindPatternHarnessTests` passes all three tests.
    - `grep -nE 'GetModuleHandle\("d3d9\.dll"\)' UtinniCore/swg/graphics/directx9.cpp` returns at least one match.
    - `grep -nE 'log::critical' UtinniCore/swg/graphics/directx9.cpp` returns at least two matches (the two bail paths).
    - `grep -nE 'if\s*\(\s*hD3d9\s*==\s*nullptr' UtinniCore/swg/graphics/directx9.cpp` returns one match.
    - `grep -nE 'if\s*\(\s*pDevice\s*==\s*nullptr' UtinniCore/swg/graphics/directx9.cpp` returns one match.
    - `git diff HEAD UtinniCore/utility/memory.cpp` returns empty for this commit (CON-N-04 preservation).
    - CI on master is green.
    - Commit message starts with `fix(C-11):` and mentions CON-N-04 preservation.
  </acceptance_criteria>
  <done>
    getVtbl returns nullptr instead of AVing when d3d9.dll isn't loaded; pattern-not-found case bails cleanly; CON-N-04 preserved; three harness tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 8: C-15 — CppSharp slnDir refactored to pure ResolveSlnDir with three resolution modes</name>
  <files>
    UtinniCoreDotNetGen/Program.cs,
    UtinniCoreDotNet.Tests/CppSharpSlnDirTests.cs,
    UtinniCore/UtinniCore.vcxproj
  </files>
  <read_first>
    UtinniCoreDotNetGen/Program.cs (full file — Setup method at :39-41 + Main entrypoint + Gen class),
    UtinniCore/UtinniCore.vcxproj:90-100 (the post-build command line — CON-T-01 chain; needs $(SolutionDir) arg added per RESEARCH §C-15),
    UtinniCoreDotNet.Tests/HotkeyTests.cs:1-68 (xUnit analog),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §C-15 (fix shape — ResolveSlnDir pure function with three modes; vcxproj post-build update),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §"C-15 CI impact assessment" (CI is NOT currently affected; C-15 follows normal assessment-week order — fine to land mid-plan)
  </read_first>
  <behavior>
    Test 1: `CppSharpSlnDirTests.ResolveSlnDir_ArgZeroProvided_UsesIt` — pass `args = new[] { tempDir }` where `tempDir` is `Directory.CreateDirectory(Path.GetTempFileName() + "-dir")` (real existing path); call `ResolveSlnDir(someWorkingDir, args)`; assert returned path equals `tempDir + "\\"` (trailing separator).
    Test 2: `ResolveSlnDir_NoArgsButWalkUpFindsUtinniSln_UsesIt` — create a temp hierarchy: `temp/sln/Utinni.sln` (empty file); pass `workingDir = temp/sln/deep/nested/dir` (which exists); pass `args = new string[0]`; assert returned path equals `temp/sln/` (the walkup-discovered solution root).
    Test 3: `ResolveSlnDir_NoArgsNoWalkupHit_NoEnvVar_Throws` — pass a workingDir like `Path.GetTempPath()` with no `Utinni.sln` ancestor; clear `UTINNI_SLN_DIR` env var (`Environment.SetEnvironmentVariable("UTINNI_SLN_DIR", null)`); assert `Assert.Throws<InvalidOperationException>(() => ResolveSlnDir(workingDir, new string[0]))`.
    Test 4: `ResolveSlnDir_NoArgsNoWalkupHit_HasEnvVar_UsesEnv` — set `UTINNI_SLN_DIR` to a real temp dir; pass workingDir with no Utinni.sln; assert returned path equals the env-var dir.
  </behavior>
  <action>
    Refactor `UtinniCoreDotNetGen/Program.cs` per 02-RESEARCH.md §C-15. Extract `public static string ResolveSlnDir(string workingDir, string[] args)` as a pure (no I/O beyond Directory/File.Exists) static method:
    1. **Preference 1:** if `args != null && args.Length > 0 && Directory.Exists(args[0])` → return `args[0].TrimEnd('\\', '/') + "\\";`.
    2. **Preference 2:** walk up from `workingDir` (`new DirectoryInfo(workingDir)`) looking for `Utinni.sln`; return parent dir path with trailing `\` if found.
    3. **Preference 3:** check `UTINNI_SLN_DIR` env var; if set and exists, return it.
    4. Throw `InvalidOperationException("Could not resolve Utinni solution directory. Pass $(SolutionDir) as args[0], run from a build output directory inside the solution tree, or set UTINNI_SLN_DIR.")`.

    Wire it into `Setup(Driver driver)`: change line 39-41 to `string workingDir = AppDomain.CurrentDomain.BaseDirectory; string buildMode = new DirectoryInfo(workingDir).Name; string slnDir = ResolveSlnDir(workingDir, _args);`. The `_args` field is set from `Main(string[] args)` via the `Gen` ctor.

    Refactor `Main`: change `class Gen` to accept `string[] args` in its ctor (`public Gen(string[] args) { _args = args; }`); add private `string[] _args;` field. Change `static void Main(string[] args)` to `ConsoleDriver.Run(new Gen(args));`.

    Update `UtinniCore/UtinniCore.vcxproj` post-build event (the line invoking `UtinniCoreDotNetGen.exe` in the PostBuildEvent) to append `"$(SolutionDir)"` as the new arg, so the production CON-T-01 path explicitly passes the solution dir. Be careful to preserve the existing `xcopy data/` command in the same post-build block.

    Create `UtinniCoreDotNet.Tests/CppSharpSlnDirTests.cs` with the four tests. Tests should be careful to clean up temp directories in `finally` blocks and restore the `UTINNI_SLN_DIR` env var to its original value if the test mutated it. Use `[Theory]` + `[InlineData]` if the data shape supports it; otherwise four `[Fact]` methods.

    The test calls `UtinniCoreDotNetGen.Program.ResolveSlnDir(...)` — requires that the test project either ProjectReferences `UtinniCoreDotNetGen.csproj` OR that `ResolveSlnDir` is exposed via an `internal` accessor with `[InternalsVisibleTo("UtinniCoreDotNet.Tests")]` added to `UtinniCoreDotNetGen/Properties/AssemblyInfo.cs`. Recommendation: add a ProjectReference (cleaner), but note that `UtinniCoreDotNetGen` is AnyCPU/net472 build-time tool — verify the test project (x86) can reference an AnyCPU project (it can — AnyCPU is compatible with x86 consumers).

    Verify the CON-T-01 post-build chain still works after the changes: `msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86` should still complete and `UtinniCoreDotNetGen.exe` should still run as part of the post-build (verify by reading the msbuild output for the `UtinniCoreDotNetGen.exe` invocation line — it should now have `"$(SolutionDir)"` as the new trailing arg).

    Update `docs/ai/assessment.md` §"Status tracking" row C-15 → `done` with commit SHA.

    Commit message: `fix(C-15): UtinniCoreDotNetGen ResolveSlnDir pure function with args[0]/walkup/env-var modes (no Substring(-1) throws)`.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release --filter FullyQualifiedName~CppSharpSlnDirTests</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter FullyQualifiedName~CppSharpSlnDirTests` passes all four tests.
    - `grep -nE 'public static string ResolveSlnDir\(string workingDir, string\[\] args\)' UtinniCoreDotNetGen/Program.cs` returns one match.
    - `grep -nE 'Substring\(0, .*LastIndexOf' UtinniCoreDotNetGen/Program.cs` returns 0 matches (the throwing line is removed).
    - `grep -nE 'UTINNI_SLN_DIR' UtinniCoreDotNetGen/Program.cs` returns at least one match.
    - `grep -nE 'InvalidOperationException' UtinniCoreDotNetGen/Program.cs` returns at least one match (the throw path).
    - `grep -nE '\$\(SolutionDir\)' UtinniCore/UtinniCore.vcxproj` returns at least one match in the PostBuildEvent line (new arg passed).
    - `msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86` succeeds (CON-T-01 chain intact).
    - CI on master is green.
    - Commit message starts with `fix(C-15):`.
  </acceptance_criteria>
  <done>
    `ResolveSlnDir` is a pure function with three resolution modes; the vcxproj post-build passes `$(SolutionDir)`; CON-T-01 chain still fires; four tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 9: C-16 — GameCallbacks/ObjectCallbacks/GroundSceneCallbacks comment audit + GC-survival regression test</name>
  <files>
    UtinniCoreDotNet/Callbacks/GameCallbacks.cs,
    UtinniCoreDotNet/Callbacks/ObjectCallbacks.cs,
    UtinniCoreDotNet/Callbacks/GroundSceneCallbacks.cs,
    UtinniCoreDotNet.Tests/GameCallbacksTests.cs
  </files>
  <read_first>
    UtinniCoreDotNet/Callbacks/GameCallbacks.cs (full file — line 46 "Very odd bug" comment; field block 39-58),
    UtinniCoreDotNet/Callbacks/ObjectCallbacks.cs (full file — line 39 area; field block),
    UtinniCoreDotNet/Callbacks/GroundSceneCallbacks.cs (full file — field block 38-41; optionally add new explanatory comment),
    UtinniCore/test_exports.cpp (from Task 1 — utinni_triggerInstallCallbacks wrapper),
    UtinniCore/swg/game (whatever file holds the install-callback registry — likely game.cpp or callbacks.cpp — needed for the trigger wrapper),
    UtinniCoreDotNet.Tests/HotkeyTests.cs:1-68 (xUnit analog),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §C-16 (audit verdict: no unanchored sites remain; comment-fix + regression test is the entire change; field approach is valid GC root),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §"C-16 delegate-pinning audit" (per-file fix list)
  </read_first>
  <behavior>
    Test 1: `GameCallbacksTests.RegisterCallback_ForceGCCollect_CallbackStillFiresWithoutAV` — call `GameCallbacks.Initialize()` (or whatever wires the static-field-anchored delegates against native); declare a local `bool fired = false; Action callback = () => fired = true;`; call `GameCallbacks.AddInstallCallback(callback)`; set the local `callback = null` to drop the local reference; force `GC.Collect(); GC.WaitForPendingFinalizers(); GC.Collect();`; then P/Invoke `utinni_triggerInstallCallbacks` (from Task 1); assert `fired == true`.
  </behavior>
  <action>
    Replace the misleading comment on `UtinniCoreDotNet/Callbacks/GameCallbacks.cs:46` with: `// The static field acts as a GC root for the delegate passed to native via Add*Callback. Without it, the GC can collect the managed delegate while native still holds its stub, causing AVs on later callback dispatch. See https://docs.microsoft.com/dotnet/standard/native-interop/best-practices#function-pointers (CON-O-03 resolved 2026-Q2).`

    Replace any equivalent stale comment on `UtinniCoreDotNet/Callbacks/ObjectCallbacks.cs:39` (or the line where the field block begins) with the same explanation (drop the "(CON-O-03 resolved 2026-Q2)" suffix; or keep — planner's call).

    Optionally add the same explanation comment above the field block in `UtinniCoreDotNet/Callbacks/GroundSceneCallbacks.cs:38-41` for consistency (per RESEARCH §C-16 audit: "Optionally add the same explanatory comment above the field block at line 38-41 for consistency"). Recommendation: ADD IT — three matching comments keep the pattern obvious to future maintainers.

    Audit confirmation: verify by grep that the only `Add*Callback` delegate-passing sites in these three files use the static-field-anchored pattern (the field is declared and assigned once). If a delegate is passed inline (`AddXxxCallback(() => {...})`) without a field anchor, THAT is the bug to fix — but per RESEARCH §C-16 audit none exist. Report in the commit body if any are found.

    Create `UtinniCoreDotNet.Tests/GameCallbacksTests.cs` with the one GC-survival test. P/Invoke `utinni_triggerInstallCallbacks` from `UtinniCore` via `[DllImport]`. The test depends on the test_exports.cpp wrapper from Task 1; if the wrapper turned out to be more complex than expected (e.g., the install-callback registry isn't easily walkable from a free function), insert a follow-up task and adjust the wrapper.

    Update `docs/ai/assessment.md` §"Status tracking" row C-16 → `done` with commit SHA. Append disposition to §"Open questions" §3 (CON-O-03 — native delegate corruption smell) per 02-RESEARCH.md §C-16: cite the commit SHA; note "field approach is valid GC root; misleading comment replaced; GC-survival regression test added".

    Commit message: `fix(C-16): GameCallbacks/ObjectCallbacks/GroundSceneCallbacks comment audit + GC-survival regression test (resolves CON-O-03)`.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release --filter FullyQualifiedName~GameCallbacksTests</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter FullyQualifiedName~GameCallbacksTests` passes.
    - `grep -nE 'Very odd bug' UtinniCoreDotNet/Callbacks/GameCallbacks.cs` returns 0 matches (stale comment removed).
    - `grep -nE 'static field acts as a GC root' UtinniCoreDotNet/Callbacks/GameCallbacks.cs UtinniCoreDotNet/Callbacks/ObjectCallbacks.cs UtinniCoreDotNet/Callbacks/GroundSceneCallbacks.cs` returns at least two matches (target: three if the optional GroundSceneCallbacks comment was added).
    - `docs/ai/assessment.md` §"Open questions" §3 carries the CON-O-03 disposition with the commit SHA.
    - CI on master is green.
    - Commit message starts with `fix(C-16):` and references CON-O-03.
  </acceptance_criteria>
  <done>
    Misleading comments replaced with precise CLR P/Invoke explanation; GC-survival test asserts the field-anchored pattern works; CON-O-03 closed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 10: KB-05 — Game::isSafeToUse uses && per internals.md (resolves CON-O-01 per D-11)</name>
  <files>
    UtinniCore/swg/game/game.cpp,
    docs/ai/assessment.md
  </files>
  <read_first>
    UtinniCore/swg/game/game.cpp:300-315 (isSafeToUse at :305-308 — verify the || operator at :307),
    docs/ai/internals.md:218-231 (isSafeToUse AND semantics — "Both must be true"),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §KB-05 (one-line operator change, no automated test possible since RVAs are SWG-internal),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §"CON-O-01" (default-fallback disposition wording),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-CONTEXT.md §D-11 (KB-05 folds into the CON-O-01 disposition commit; no separate C-17 numbering)
  </read_first>
  <action>
    Edit `UtinniCore/swg/game/game.cpp:305-308`. Change the `||` to `&&` per 02-RESEARCH.md §KB-05. Add the comment block above the body per RESEARCH:

    ```
    // Returns true only when both SWG-internal safety flags are set. Per docs/ai/internals.md:218-231,
    // "AND ... Both must be true" — the operator was previously || (logical-OR), which returned true
    // when only one flag was set, allowing world-snapshot mutations during scene transitions that the
    // second flag would have blocked. CON-O-01 disposition: docs/ai/internals.md is the source of truth;
    // the operator is &&. See assessment.md Open Questions §1.
    ```

    No automated test added — the function reads from hard-coded SWG RVAs (`0x01908858`, `0x01919410`) only accessible inside an injected SWG client. Behavior is captured in the Tier-4 manual residual (covered by the C-01 / C-03 user_setup steps that also exercise live SWG).

    Update `docs/ai/assessment.md` §"Status tracking" — KB-05 is not a numbered C-NN, but per D-11 it lands here. Add a new line in the table (or in the §"Open questions" §1 disposition if no status row exists for KB-05): cite the commit SHA. Update §"Open questions" §1 with the verbatim D-12 default-fallback wording from 02-RESEARCH.md §"CON-O-01" — substitute the actual commit SHA.

    Note: `docs/ai/internals.md` does NOT need an edit — the doc was already correct; the code is now aligned to it.

    Commit message: `fix(KB-05): Game::isSafeToUse uses && per docs/ai/internals.md (resolves CON-O-01 per D-11)`.
  </action>
  <verify>
    <automated>findstr /N /R "memory::read<bool>(0x01908858) && memory::read<bool>(0x01919410)" UtinniCore\swg\game\game.cpp</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'memory::read<bool>\(0x01908858\)\s*&&\s*memory::read<bool>\(0x01919410\)' UtinniCore/swg/game/game.cpp` returns one match (operator is `&&`).
    - `grep -nE 'memory::read<bool>\(0x01908858\)\s*\|\|\s*memory::read<bool>\(0x01919410\)' UtinniCore/swg/game/game.cpp` returns 0 matches (`||` is gone).
    - `docs/ai/assessment.md` §"Open questions" §1 carries the CON-O-01 disposition with the commit SHA.
    - `msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86` exits 0 (single-character change should not break the build).
    - CI on master is green.
    - Commit message starts with `fix(KB-05):` and references CON-O-01.
  </acceptance_criteria>
  <done>
    `isSafeToUse` now matches its documented AND-semantics contract; CON-O-01 disposition recorded; no automated test (behavior captured in Tier-4 manual residual).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 11: Status tracking sweep — flip assessment.md rows C-02/C-03/C-05/C-07/C-10/C-11/C-15/C-16 + finalize CON-O-01/CON-O-03 dispositions</name>
  <files>
    docs/ai/assessment.md
  </files>
  <read_first>
    docs/ai/assessment.md §"Status tracking" (full table — find the eight C-NN rows; if some were already flipped in Tasks 2-10 via inline updates, this task is a final-sweep verification),
    docs/ai/assessment.md §"Open questions" §1 (CON-O-01) + §3 (CON-O-03) — verify dispositions are present with commit SHAs (recorded in Tasks 9 + 10),
    git log --oneline (capture actual SHAs from Tasks 2-10 for cross-reference)
  </read_first>
  <action>
    Sweep verification: for each of C-02, C-03, C-05, C-07, C-10, C-11, C-15, C-16, confirm the status row in `docs/ai/assessment.md` shows `done` with the commit SHA from the matching task. If any row was missed in an earlier task (i.e., the task author forgot to update inline), update it now with the actual SHA. Also verify §"Open questions" §1 (CON-O-01) and §3 (CON-O-03) carry dispositions with commit SHAs.

    Single sweep commit message: `docs(02-02): mark C-02/C-03/C-05/C-07/C-10/C-11/C-15/C-16 done in assessment.md; finalize CON-O-01 + CON-O-03 dispositions`.

    If all task-inline updates from Tasks 2-10 already covered the rows AND dispositions, this task is a no-op verify; in that case, skip the commit and document in the plan summary that no sweep was needed.
  </action>
  <verify>
    <automated>for /f "tokens=*" %i in ('echo C-02 C-03 C-05 C-07 C-10 C-11 C-15 C-16') do findstr /R /C:"%i.*done" docs\ai\assessment.md</automated>
  </verify>
  <acceptance_criteria>
    - Every C-NN in {C-02, C-03, C-05, C-07, C-10, C-11, C-15, C-16} has a status row in `docs/ai/assessment.md` marked `done` with a commit SHA.
    - `grep -nE 'CON-O-01.*Resolved 2026' docs/ai/assessment.md` returns one match.
    - `grep -nE 'CON-O-03.*Resolved 2026' docs/ai/assessment.md` returns one match.
    - Each commit SHA referenced in the table resolves via `git rev-parse --verify <SHA>`.
    - CI on master is green after this commit (docs-only change).
    - Commit message starts with `docs(02-02):` OR the task is a documented no-op.
  </acceptance_criteria>
  <done>
    Status-tracking table reflects the 8 closed bugs from this plan + KB-05; CON-O-01 and CON-O-03 dispositions are recorded; the plan is ready for verification.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLR ↔ UtinniCore P/Invoke surface (new) | Phase 2 introduces 6 new test-only `extern "C" __declspec(dllexport)` symbols in UtinniCore.dll; the test process loads UtinniCore.dll and invokes them via `[DllImport]`. The exports are deliberately UNmangled C-linkage and called only by `UtinniCoreDotNet.Tests` (no production consumer). |
| Native ↔ CLR delegate marshalling (C-16) | `GameCallbacks.AddInstallCallback`-style sites pass managed delegates to native; the static-field GC root keeps them alive. |
| `data/utinni.cfg` and `input.ini` (read-only) | These are not modified by tasks in this plan — handled in Plan 02-01. |
| SWG hard-coded RVAs (C-03 `0xAA4900`, `0x01908858`, `0x01919410`) | The wrappers in `test_exports.cpp` for C-03 use a sentinel rather than calling SWG (Task 1 caveat). KB-05 changes the live code path that reads these RVAs but the test process cannot exercise it. |
| `UtinniCore/utility/memory::copy` VirtualProtect bracket (CON-N-04) | Read-only in this plan; preservation guard-rail; the C-11 fix touches `memory::findPattern` (pure read) but NOT `memory::copy`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering | `UtinniCore/test_exports.cpp` exports (Task 1) | mitigate | All six wrappers are clearly named `utinni_test_*` or `utinni_<operation>` prefix. The C-02 and C-03 wrappers (`utinni_test_freeConfigBuffer`, `utinni_test_networkCast`) are STUBS that return sentinel values — they DO NOT execute the dangerous path inside the test process; they exist to assert the fix surface compiles and is callable. The `utinni_clr_stop`, `utinni_findPattern`, `utinni_getVtbl`, and `utinni_triggerInstallCallbacks` wrappers DO call into UtinniCore proper — these paths are exercised by the harnesses. Production code MUST NOT depend on the `utinni_test_*` prefix; CppSharp generation skips them by virtue of their C linkage. |
| T-02-09 | Elevation-of-Privilege | C-07 ctor seam: `Action<Action> registerCleanupCallback = null` defaults to `GameCallbacks.AddCleanupSceneCall` | accept | Default parameter preserves production behavior exactly. Tests pass a no-op lambda; production callers pass nothing and inherit the default. No new attack surface — the seam is internal API and untrusted callers (plugins) cannot reach the ctor. |
| T-02-10 | DoS | C-07 deadlock via lock(syncRoot) re-entry | mitigate | Lock is released BEFORE `cmd.Undo()` / `cmd.Execute()` / `onUpdateCommandsCallback()` calls — these callbacks may re-enter via `GroundSceneCallbacks.AddUpdateLoopCall`. Documented in RESEARCH §C-07 and enforced by the test (`AddUndoCommand_ConcurrentPushFromMultipleThreads_NoRaceConditions` would hang if the lock were held during callbacks). |
| T-02-11 | DoS | C-10 idempotent `stop()` | mitigate | Null-checked-Release-and-null prevents AV on second call from `detatch()` (DLL_PROCESS_DETACH). CON-H-01 class-of-bug constraint honored. |
| T-02-12 | Information Disclosure | C-11 `log::critical` messages on d3d9.dll / pattern absence | accept | Log lines disclose the failure mode; no PII or secrets. Acceptable diagnostic content. |
| T-02-13 | Tampering | KB-05 operator change in live code (`||`→`&&`) | accept | This is a corrective change — the `||` was wrong per docs/ai/internals.md; `&&` is the documented contract. Risk: if internals.md is wrong and `||` was intentional, `&&` blocks legitimate isSafeToUse=true cases (UI grays out unnecessarily). Mitigation: D-11 + D-12 default-fallback explicitly accept this risk; live SWG verification will catch a regression. |
| T-02-14 | Spoofing | C-05 static event subscriber list | accept | Static events have process-wide visibility; any code in the process (including plugins) can subscribe. This is intentional — plugins want drag-drop hooks. The pattern is unchanged from the pre-fix behavior; the bug was a different (re-wiring) issue. |
| T-02-SC | Tampering | npm/pip/cargo installs in this plan | accept | NO new package installs in this plan. The C-15 refactor depends on existing System.IO + System.Environment BCL primitives only. Package Legitimacy Audit in 02-RESEARCH.md §"Package Legitimacy Audit" confirms no slopcheck gate is needed. |
</threat_model>

<verification>
## Phase-level verification at plan close

1. **CI gates each commit:** every `fix(C-NN):` commit lands on master with CI green per D-01.
2. **All test files exist and pass:** run `dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release` — expect at least 25+ tests passing across both Plan 02-01 and Plan 02-02 (4 C-02 + 1 C-03 + 4 C-05 + 5 C-07 + 1 C-10 + 3 C-11 + 1 C-16 + 4 C-15 = 23 new tests for this plan, on top of Plan 02-01's count).
3. **Test-only exports visible in UtinniCore.dll:** `dumpbin /exports bin/Release/UtinniCore.dll | findstr utinni_` lists six new symbols (Task 1).
4. **CON-N-04 preserved:** `git diff master HEAD UtinniCore/utility/memory.cpp` returns empty for the Plan 02-02 commits.
5. **CON-M-05 preserved:** `OnCleanupCallback` still clears both stacks; verified by Test 5 in Task 5.
6. **Phase-1 deferred work shipped:** the `Action<Action> registerCleanupCallback` ctor seam exists on UndoRedoManager; production callers continue to work via the default value.
7. **Status tracking sweep done:** all eight C-NN rows + CON-O-01 + CON-O-03 dispositions reflect the plan's commits.
</verification>

<success_criteria>
- 8 fix commits (C-02, C-03, C-05, C-07, C-10, C-11, C-15, C-16) + 1 KB-05 fix commit + 1 Wave-0 scaffolding commit + 1 docs sweep commit = 11 commits in this Utinni repo.
- All 20+ new test cases (across the 8 new test files) pass; the existing Plan 02-01 tests stay green.
- `dumpbin /exports` confirms 6 new C-linkage exports on `UtinniCore.dll`.
- assessment.md status table marks 8 bugs done + KB-05 recorded; CON-O-01 + CON-O-03 carry dispositions; CON-N-04 and CON-M-05 preservation surfaces untouched.
- CI on master is green at the close of this plan; Plans 02-03 and 02-04 (Wave 3) are unblocked.
</success_criteria>

<output>
Create `.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-02-SUMMARY.md` documenting all 11 commits (8 fixes + 1 KB-05 + 1 scaffolding + 1 docs sweep), the test counts, preservation verifications (CON-N-04 + CON-M-05), and any deviations from this plan.
</output>
</content>
</invoke>