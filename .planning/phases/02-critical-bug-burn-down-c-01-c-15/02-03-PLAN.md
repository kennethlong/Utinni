---
phase: 02-critical-bug-burn-down-c-01-c-15
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - UtinniCore/utinni.cpp
  - UtinniCore/UtinniCore.vcxproj
  - UtinniCore/UtinniCore.vcxproj.filters
  - Launcher/main.cpp
  - Utinni.LoaderLockHarness/Utinni.LoaderLockHarness.vcxproj
  - Utinni.LoaderLockHarness/Utinni.LoaderLockHarness.vcxproj.filters
  - Utinni.LoaderLockHarness/main.cpp
  - UtinniCoreDotNet.Tests/LoaderLockHarnessTests.cs
  - Utinni.sln
  - docs/ai/assessment.md
autonomous: false
requirements:
  - STAB-01

user_setup:
  - service: "Live SWG client injection verification (C-01 full proof — Tier 4 manual)"
    why: "The LoaderLockHarness only catches the regression class 'someone moved heavy work back into DllMain' (timing under 50 ms). Full proof of 'no deadlock under loader-lock contention' requires injection into a live SWG client (CONTEXT.md D-06)."
    env_vars: []
    dashboard_config:
      - task: "After Plan 02-03 ships: inject UtinniCore.dll into SWG client via Launcher; observe game window appears within 2 s; no hang in Task Manager → Resource Monitor → CPU → Waiting Chain"
        location: "Local dev workstation with SWG client"
      - task: "Confirm plugins load and editor UI renders within the same launch (verifies CLR bring-up via utinni_init runs after DllMain returns)"
        location: "Local dev workstation"

must_haves:
  truths:
    - "UtinniCore/utinni.cpp `DllMain` `DLL_PROCESS_ATTACH` does ONLY `DisableThreadLibraryCalls(hinstDLL); return TRUE;` — no `CreateThread`, no `loadPlugins`, no `clr::load`. `DLL_PROCESS_DETACH` continues to call `detatch()` (the typo is Phase-6 cleanup territory — leave it)."
    - "UtinniCore exports an unmangled `utinni_init` symbol: `extern \"C\" __declspec(dllexport) DWORD WINAPI utinni_init(LPVOID lpThreadParam)` — body is the prior `main()` body (plugin manager init + CLR bring-up). Verified via `dumpbin /exports bin/Release/UtinniCore.dll | findstr utinni_init` (NO mangled `?utinni_init@@YA...` form — clean C linkage)."
    - "Launcher/main.cpp::inject() chases the existing `LoadLibraryA` remote thread with a second `CreateRemoteThread` against `utinni_init`. The remote address is computed via the standard injection idiom: local `LoadLibrary` → `GetProcAddress` → offset = `(BYTE*)localInit - (BYTE*)localCore`; `FreeLibrary(localCore)`; remote target = `(BYTE*)hDll + offset`."
    - "`Utinni.LoaderLockHarness/` exists as a NEW sibling C++ console-exe project at repo root (flat-root convention per 01-PATTERNS.md). vcxproj depends on UtinniCore so the CON-T-01 post-build chain fires first. Builds Release|Win32 and Debug|Win32 (skip RelWithDbgInfo per RESEARCH §C-01 timing harness)."
    - "`Utinni.LoaderLockHarness/main.cpp` brackets `LoadLibraryA(\"UtinniCore.dll\")` with `QueryPerformanceCounter`; exits 0 if elapsed < 50 ms; exits 1 otherwise; exits 2 on `LoadLibrary` failure. Prints `\"UtinniCore DllMain elapsed: %.3f ms\\n\"` to stdout."
    - "Utinni.sln has the new project entry with C++ project-type GUID `{8BC9CEB8-8B4A-11D0-8D11-00A0C91BC942}`, both Debug|Win32 and Release|Win32 configurations mapped, and `Utinni.LoaderLockHarness` depends on `UtinniCore`."
    - "xUnit `LoaderLockHarnessTests.cs` spawns `Utinni.LoaderLockHarness.exe` via `Process.Start`, asserts exit code 0 + stdout contains `\"elapsed:\"`. Test discovers harness exe at `Path.Combine(AppContext.BaseDirectory, \"..\", \"..\", \"..\", \"..\", \"Utinni.LoaderLockHarness\", \"bin\", \"$(Configuration)\", \"Utinni.LoaderLockHarness.exe\")`."
    - "CON-N-01 (detour-table pattern) is NOT touched — `utinni_init` is a plain export, not a new detour. Verified by code-review of the C-01 commit diff."
    - "docs/ai/assessment.md §\"Status tracking\" row C-01 flipped to `done` with commit SHAs (multiple — fix + harness scaffolding)."
    - "ROADMAP success criterion #4 met: DllMain no longer does heavy startup per CON-H-01; CLR bring-up deferred to the explicit `utinni_init` thread invoked by the launcher's second `CreateRemoteThread`."
    - "CI on master is green after every fix commit in this plan (D-01)."
    - "Decision provenance: per D-02 this plan is SPLIT from C-09 (Plan 02-04) — separate plans because the two architectural fixes share no code and each merits its own research/plan/execute lane; per D-03 every commit in this plan is atomic (scaffold commit + C-01 fix commit + docs sweep commit); per D-04 the C-01 fix lands with the LoaderLockHarness regression test in the SAME commit (red-before-green via the harness exe failing when DllMain does heavy work, passing when it returns <50ms); per D-07 the new Utinni.LoaderLockHarness sibling project follows Phase-1 flat-root precedent (sibling project in Utinni.sln, NOT a third test project — the xUnit harness wrapper lives in the existing UtinniCoreDotNet.Tests project); per D-08 path (a) `utinni_init` export + launcher CreateRemoteThread is the chosen architectural path — paths (b) defer-to-Game::install detour and (c) hybrid are explicitly rejected per 02-RESEARCH.md §C-01 architectural recommendation."
  artifacts:
    - path: "Utinni.LoaderLockHarness/Utinni.LoaderLockHarness.vcxproj"
      provides: "New native C++ console-exe project; depends on UtinniCore; outputs to $(SolutionDir)bin/$(Configuration)/Utinni.LoaderLockHarness.exe"
    - path: "Utinni.LoaderLockHarness/main.cpp"
      provides: "QueryPerformanceCounter-bracketed LoadLibraryA('UtinniCore.dll') with 50ms threshold; exit codes 0/1/2"
      min_lines: 30
    - path: "UtinniCoreDotNet.Tests/LoaderLockHarnessTests.cs"
      provides: "xUnit Process.Start wrapper invoking the harness exe; asserts exit code 0"
      contains: "LoaderLockHarness_LoadsUtinniCoreUnderThreshold"
    - path: "UtinniCore/utinni.cpp"
      provides: "Slimmed DllMain + utinni_init export + body extracted from main()"
      contains: "extern \"C\" __declspec(dllexport) DWORD WINAPI utinni_init"
    - path: "Launcher/main.cpp"
      provides: "Second CreateRemoteThread against utinni_init after LoadLibraryA returns"
      contains: "GetProcAddress(.*utinni_init"
  key_links:
    - from: "Launcher/main.cpp::inject()"
      to: "UtinniCore.dll!utinni_init in the remote SWG process"
      via: "remote = (BYTE*)hDll + (localInit - localCore) offset; CreateRemoteThread target = remote address"
      pattern: "CreateRemoteThread.*utinni_init|GetProcAddress\\(.*\\\"utinni_init\\\""
    - from: "Utinni.LoaderLockHarness/main.cpp"
      to: "UtinniCore.dll DllMain timing"
      via: "QueryPerformanceCounter-bracketed LoadLibraryA"
      pattern: "QueryPerformanceCounter.*LoadLibraryA\\(\"UtinniCore"
    - from: "UtinniCoreDotNet.Tests/LoaderLockHarnessTests.cs"
      to: "Utinni.LoaderLockHarness.exe process exit code"
      via: "Process.Start + WaitForExit + ExitCode == 0 assertion"
      pattern: "Process\\.Start.*LoaderLockHarness"
---

<objective>
**Architectural fix C-01 (Wave 3): DllMain loader-lock.** Move heavy startup OUT of `DllMain` (currently calls `CreateThread → main() → pluginManager.loadPlugins() + clr::load()`) into a new exported `utinni_init` function that the launcher fires via a second `CreateRemoteThread` after `LoadLibraryA` returns. Path (a) from 02-RESEARCH.md §"C-01 architectural recommendation" — chosen over (b) defer-to-Game::install (chicken-and-egg with early plugin callbacks) and (c) hybrid (double-init complexity).

Adds Wave-0 scaffolding: a new sibling C++ console-exe project `Utinni.LoaderLockHarness/` at repo root (flat-root convention) that times `LoadLibraryA("UtinniCore.dll")` and exits with a status code consumable from `dotnet test`. Adds a new xUnit test that spawns the harness and asserts exit-code 0.

Per CONTEXT.md D-02 + D-08, this plan is SPLIT from C-09 (Plan 02-04) because they share no code and each merits its own research/plan/execute lane. Per CONTEXT.md D-01, this plan depends on Plan 02-02 (single-file criticals) closing with green CI on master — Wave 3 does NOT start until Wave 2 is green.

Note on parallelism: Plans 02-03 (C-01) and 02-04 (C-09) share no code per RESEARCH §"Landmines / preservation surfaces" #8 — they CAN run in parallel within Wave 3 if executor permits, OR sequentially. This plan does not encode an order against 02-04.

Purpose: Close the only V1-blocking architectural-class bug related to DLL startup discipline (CON-H-01); land an automated regression guard against "someone moved heavy work back into DllMain" (the harness times DllMain duration); set up the sibling-project pattern for the C-09 signaller (Plan 02-04).

Output: 3 fix commits — (1) Wave-0 scaffolding (LoaderLockHarness vcxproj + sln entry + harness main.cpp + xUnit test stub that asserts the file exists / exe is present), (2) C-01 native+launcher fix (utinni.cpp DllMain slim + utinni_init export + Launcher inject extension; the harness regression test flips green when the build succeeds), (3) docs sweep (assessment.md status row C-01 → done).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-CONTEXT.md
@.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md
@.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-VALIDATION.md
@.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-PATTERNS.md
@.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-01-PLAN.md
@.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-02-PLAN.md
@.planning/intel/constraints.md
@.planning/codebase/CONVENTIONS.md
@docs/ai/assessment.md

<interfaces>
<!-- Key contracts for executors. -->

From `UtinniCore/utinni.cpp:99-151` (C-01 surface — current state):
- `void main()` body at :99-130 calls `pluginManager.loadPlugins()` (line 123) and `clr::load()` (line 129) — both forbidden inside DLL_PROCESS_ATTACH per docs.microsoft.com/windows/win32/dlls/dynamic-link-library-best-practices.
- `DllMain` at :138-151 does `CreateThread(... main, ...)` inside DLL_PROCESS_ATTACH — currently returns "fast" because CreateThread is async, but the spawned thread does the heavy work and is the actual loader-lock risk.

From `UtinniCore/plugin_framework/utinni_plugin.h:50` (only `extern "C" __declspec(dllexport)` precedent in repo):
```cpp
#define UTINNI_PLUGIN extern "C" __declspec(dllexport) utinni::UtinniPlugin* createPlugin()
```
The new `utinni_init` follows this linkage pattern — unmangled C-linkage symbol name visible via `dumpbin /exports`.

From `Launcher/main.cpp:174-210` (`inject` function — C-01 launcher-side surface):
- Line 181-186: existing LoadLibraryA injection via `VirtualAllocEx` + `WriteProcessMemory` + `CreateRemoteThread` against `kernel32.dll!LoadLibraryA`.
- Line 195: `WaitForSingleObject(hThread, INFINITE)` — blocks until remote LoadLibraryA returns.
- Line 200-209: `GetExitCodeThread(hThread, (LPDWORD)&hDll)` — `hDll` is now the remote HMODULE for UtinniCore.dll.
- Per 02-RESEARCH.md §"Path-(a) detailed steps", after this point the NEW code adds: local `LoadLibrary` → `GetProcAddress("utinni_init")` → compute offset → `FreeLibrary(localCore)` → second `CreateRemoteThread` at `(BYTE*)hDll + offset` → `WaitForSingleObject` → `CloseHandle`.

From `Launcher/Launcher.vcxproj:1-157` (C++ console-exe analog for Utinni.LoaderLockHarness per 02-PATTERNS.md §3):
- Three-config matrix: Debug|Win32, Release|Win32, RelWithDbgInfo|Win32 — harness keeps Debug|Win32 + Release|Win32 only (skip RelWithDbgInfo per RESEARCH).
- `<PlatformToolset>v142</PlatformToolset>`, `<CharacterSet>NotSet</CharacterSet>`, `<LanguageStandard>stdcpp17</LanguageStandard>`, `<SubSystem>Console</SubSystem>` (harness keeps Console subsystem; Launcher pragma-overrides to `/SUBSYSTEM:windows`).
- `<OutDir>$(SolutionDir)bin\$(Configuration)\</OutDir>` — same output convention; the xUnit test resolves this path.
- DROP: `Launcher`'s `<ProjectReference>` to `UtINI` (lines 141-143; harness doesn't read ini), `<ResourceCompile>` + `resource.h` + `Launcher.rc` + `TJT.ico` (harness has no resources).

From `Utinni.sln` project-type GUIDs:
- C++: `{8BC9CEB8-8B4A-11D0-8D11-00A0C91BC942}` (cite from any existing .vcxproj entry like `UtinniCore` or `Launcher`).

From `UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj` (Phase-1 wiring):
- `<AppendPlatformToOutputPath>false</AppendPlatformToOutputPath>` — test bin is at `bin/Release/net472/`.
- Test resolves harness exe via relative-path walk from `AppContext.BaseDirectory`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 (Wave 0 scaffolding): Create Utinni.LoaderLockHarness sibling project + sln entry + harness main.cpp + xUnit test stub</name>
  <files>
    Utinni.LoaderLockHarness/Utinni.LoaderLockHarness.vcxproj,
    Utinni.LoaderLockHarness/Utinni.LoaderLockHarness.vcxproj.filters,
    Utinni.LoaderLockHarness/main.cpp,
    Utinni.sln,
    UtinniCoreDotNet.Tests/LoaderLockHarnessTests.cs
  </files>
  <read_first>
    Launcher/Launcher.vcxproj (full file — closest in-tree analog per 02-PATTERNS.md §3; copy the three-config matrix shape minus the parts called out in `<interfaces>` deltas above),
    Launcher/main.cpp:1-23 (MIT header — copy verbatim to harness main.cpp),
    Launcher/main.cpp:174-210 (the LoadLibraryA pattern reference — harness simplifies this to in-process),
    Utinni.sln (full file — particularly the project entries for UtinniCore + Launcher to understand the Project/EndProject + GlobalSection(ProjectConfigurationPlatforms) shape),
    UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj (test csproj output convention; the LoaderLockHarnessTests will live here),
    UtinniCoreDotNet.Tests/HotkeyTests.cs:1-68 (xUnit analog),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-PATTERNS.md §3 "Sibling C++ project" (complete — has the vcxproj deltas table and the full main.cpp template),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §"C-01 timing harness — sibling project sketch" (full main.cpp template + xUnit caller template + discoverability options),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-VALIDATION.md §"Sibling project (Utinni.sln addition)" (the four checklist items)
  </read_first>
  <action>
    Create the new project directory `Utinni.LoaderLockHarness/` at the repo root (flat-root convention per S-6 in 02-PATTERNS.md).

    Create `Utinni.LoaderLockHarness/Utinni.LoaderLockHarness.vcxproj` by copying `Launcher/Launcher.vcxproj` and applying the deltas table from 02-PATTERNS.md §3:
    - Keep ProjectConfigurations matrix but drop `RelWithDbgInfo|Win32` (harness only needs Debug + Release).
    - New `<ProjectGuid>` (generate a fresh GUID — record it in the commit message for traceability).
    - `<RootNamespace>Utinni.LoaderLockHarness</RootNamespace>`.
    - Keep `<VCProjectVersion>16.0</VCProjectVersion>`, `<WindowsTargetPlatformVersion>10.0</WindowsTargetPlatformVersion>`, `<ConfigurationType>Application</ConfigurationType>`, `<PlatformToolset>v142</PlatformToolset>`, `<CharacterSet>NotSet</CharacterSet>`, `<LanguageStandard>stdcpp17</LanguageStandard>`, `<WarningLevel>Level3</WarningLevel>`, `<AdditionalIncludeDirectories>$(SolutionDir);$(SolutionDir)external;$(ProjectDir);</AdditionalIncludeDirectories>`, `<SubSystem>Console</SubSystem>`, `<OutDir>$(SolutionDir)bin\$(Configuration)\</OutDir>`.
    - `<ClCompile Include="main.cpp" />` only — drop Launcher's `UtINI` `<ProjectReference>`, drop `<ResourceCompile>` + `<Image>` + `<ClInclude>` blocks.
    - Add a `<ProjectReference Include="..\UtinniCore\UtinniCore.vcxproj">` block to force build ordering so `UtinniCore.dll` is fresh before the harness runs (mirrors UtinniCoreDotNet → UtinniCore dependency pattern; harness doesn't link against UtinniCore — it `LoadLibrary`s at runtime — so add `<Project>{guid}</Project>` and DO NOT set `<LinkLibraryDependencies>true</LinkLibraryDependencies>`).

    Create `Utinni.LoaderLockHarness/Utinni.LoaderLockHarness.vcxproj.filters` with a single `<ClCompile Include="main.cpp"><Filter>Source Files</Filter></ClCompile>` entry; mirror the filter shape of `Launcher/Launcher.vcxproj.filters`.

    Create `Utinni.LoaderLockHarness/main.cpp` using the 02-PATTERNS.md §3 template:
    - 23-line MIT header verbatim from `Launcher/main.cpp:1-23` (do NOT alter Copyright line).
    - `#include <Windows.h>` + `#include <cstdio>`.
    - Comment block: `// Harness for C-01: measure DllMain entry-exit timing of UtinniCore.dll.` + the rest from the template.
    - `int main(int /*argc*/, char* /*argv*/[])` body: `QueryPerformanceFrequency` + `QueryPerformanceCounter(&start)` + `LoadLibraryA("UtinniCore.dll")` + `QueryPerformanceCounter(&end)`; on `hDll == nullptr` print `[ERROR]` + return 2; compute `elapsedMs = (end - start) * 1000.0 / freq`; print `"UtinniCore DllMain elapsed: %.3f ms\n"`; `FreeLibrary(hDll)`; return `(elapsedMs < 50.0) ? 0 : 1`.

    Update `Utinni.sln`: add a new `Project("{8BC9CEB8-8B4A-11D0-8D11-00A0C91BC942}") = "Utinni.LoaderLockHarness", "Utinni.LoaderLockHarness\Utinni.LoaderLockHarness.vcxproj", "{NEW-GUID}"` entry. Add ProjectConfigurationPlatforms entries:
    - `{NEW-GUID}.Debug|x86.ActiveCfg = Debug|Win32`
    - `{NEW-GUID}.Debug|x86.Build.0 = Debug|Win32`
    - `{NEW-GUID}.Release|x86.ActiveCfg = Release|Win32`
    - `{NEW-GUID}.Release|x86.Build.0 = Release|Win32`
    - `{NEW-GUID}.RelWithDbgInfo|x86.ActiveCfg = Release|Win32` (or skip — planner's call; mapping to Release is safe).

    Create `UtinniCoreDotNet.Tests/LoaderLockHarnessTests.cs`: 23-line MIT header; `using System.Diagnostics; using System.IO; using Xunit;`; namespace `UtinniCoreDotNet.Tests`; class `LoaderLockHarnessTests`. One `[Fact]`:
    - Resolve harness path: `Path.Combine(AppContext.BaseDirectory, "..", "..", "..", "..", "bin", "Release", "Utinni.LoaderLockHarness.exe")` (the harness builds into `$(SolutionDir)bin\$(Configuration)\` per its vcxproj — same convention as Launcher.exe; verify the relative path resolves correctly from the test bin dir `bin/Release/net472/`).
    - Assert harness exe exists (`Assert.True(File.Exists(harnessPath), $"Harness exe not found at {harnessPath}")`).
    - `Process.Start` with `RedirectStandardOutput = true, UseShellExecute = false, CreateNoWindow = true`; `WaitForExit(5000)`; `Assert.Equal(0, proc.ExitCode)`; `Assert.Contains("elapsed:", proc.StandardOutput.ReadToEnd())`.

    Build verification: `msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86` must produce `bin/Release/Utinni.LoaderLockHarness.exe`. Run the harness manually: `bin\Release\Utinni.LoaderLockHarness.exe` — **note: pre-fix, the harness MAY pass or fail depending on whether the current DllMain's `CreateThread` to `main()` returns fast enough; per RESEARCH §"Why this works" caveat, the CreateThread itself returns quickly so the harness may currently pass even pre-fix.** That's acceptable — the harness's value is the regression guard going forward, not pre-fix red signal.

    Run `dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release --filter FullyQualifiedName~LoaderLockHarnessTests`. Test should PASS if the harness exe is present and runs cleanly.

    Commit message: `chore(02-03): wave-0 scaffolding — Utinni.LoaderLockHarness sibling project + xUnit Process.Start test`.
  </action>
  <verify>
    <automated>msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86 2>&amp;1 | findstr /R "error\|Error\|FAILED" ; dir bin\Release\Utinni.LoaderLockHarness.exe &amp;&amp; dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release --filter FullyQualifiedName~LoaderLockHarnessTests</automated>
  </verify>
  <acceptance_criteria>
    - `bin/Release/Utinni.LoaderLockHarness.exe` exists after `msbuild Utinni.sln /p:Configuration=Release /p:Platform=x86`.
    - `bin/Release/Utinni.LoaderLockHarness.exe` runs and exits with code 0 OR 1 (NOT 2 — exit 2 means LoadLibraryA failed). Code 1 means the timing exceeded 50ms which is the regression class we want to catch but is acceptable at this Wave-0 scaffolding step.
    - `dotnet test --filter FullyQualifiedName~LoaderLockHarnessTests` runs the harness via Process.Start; depending on current pre-fix DllMain behavior, it may pass or be near the threshold. If it fails at this Wave-0 step due to timing > 50ms, that confirms C-01 is the right next task — proceed regardless.
    - `Utinni.sln` parses cleanly (`msbuild Utinni.sln /t:Restore` exits 0).
    - `grep -nE 'Utinni\.LoaderLockHarness' Utinni.sln` returns at least 4 matches (Project entry + 3-4 ProjectConfigurationPlatforms lines).
    - 23-line MIT header in `Utinni.LoaderLockHarness/main.cpp` matches `Launcher/main.cpp:1-23` byte-for-byte.
    - Commit message starts with `chore(02-03):`.
  </acceptance_criteria>
  <done>
    LoaderLockHarness project compiles standalone; UtinniCoreDotNet.Tests has a Process.Start wrapper invoking the harness; the sln contains the new project entry; ready for the C-01 fix in Task 2.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: C-01 fix — slim DllMain + utinni_init export + Launcher second CreateRemoteThread</name>
  <files>
    UtinniCore/utinni.cpp,
    UtinniCore/UtinniCore.vcxproj,
    Launcher/main.cpp
  </files>
  <read_first>
    UtinniCore/utinni.cpp (full file — main at :99-130, DllMain at :138-151, detatch at :132-136; understand the existing structure end-to-end),
    UtinniCore/UtinniCore.vcxproj (full file — particularly LinkerOptions / ModuleDefinitionFile / ExportsFile entries if any; new `__declspec(dllexport)` should auto-export but verify),
    Launcher/main.cpp (full file — inject at :174-210, loadDll at :298-368, the OEP-park-then-resume pattern at :loadDll),
    UtinniCore/plugin_framework/utinni_plugin.h:1-52 (extern "C" __declspec(dllexport) precedent),
    UtinniCore/clr.cpp (verify clr::load + clr::stop signatures unchanged by Plan 02-02 Task 6 — the C-10 commit only touched stop()),
    UtinniCore/utility/memory.cpp (verify untouched — CON-N-04 preservation; this task does not need to read this, but verify the diff is empty for memory.cpp after the commit),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §"C-01 architectural recommendation" + §"Path-(a) detailed steps" (complete — has the verbatim code for utinni.cpp DllMain rewrite + utinni_init signature + Launcher second-CreateRemoteThread idiom),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §"Open question — defer for Plan 02-03 task ordering" (synchronous vs threaded utinni_init — recommendation: synchronous; document the choice),
    .planning/intel/constraints.md (CON-N-01 detour-table pattern — utinni_init is plain export, NOT a detour; CON-H-01 class-of-bug),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-PATTERNS.md §4 "Native exports" (extern "C" __declspec(dllexport) pattern; CallingConvention for utinni_init MUST be WINAPI per LPTHREAD_START_ROUTINE contract — not __cdecl)
  </read_first>
  <action>
    **Native side — UtinniCore/utinni.cpp:**

    1. Extract the body of `main()` (currently lines 100-130 per RESEARCH) into a new function with this exact signature: `extern "C" __declspec(dllexport) DWORD WINAPI utinni_init(LPVOID lpThreadParam)`. Body: COPY the entire existing `main()` body verbatim (plugin manager init at :123 + `clr::load()` at :129 + everything in between). End with `return 0;` per `LPTHREAD_START_ROUTINE` contract. **Synchronous, no internal CreateThread** — per RESEARCH §"Open question" recommendation. Document this choice with a comment: `// C-01: utinni_init runs synchronously on the launcher-spawned thread. Launcher's WaitForSingleObject blocks until this returns. Synchronous startup is easier to debug than fire-and-forget; bring-up is bounded (CLR init + plugin load).`

    2. Delete the orphaned `void main()` declaration (it's no longer reachable).

    3. Rewrite `DllMain`. New body for `DLL_PROCESS_ATTACH`: `DisableThreadLibraryCalls(hinstDLL); return TRUE;` — NOTHING else. Keep `DLL_PROCESS_DETACH` calling `detatch()` (the typo `detatch` is Phase-6 cleanup territory per CONTEXT.md §domain). Other DLL_THREAD_* messages just `return TRUE`. Add a comment above DllMain: `// C-01: DllMain MUST complete in microseconds. Heavy startup (CLR + plugin load) moved to utinni_init, fired by the launcher via a second CreateRemoteThread after this DLL is loaded.`

    4. Verify `extern "C" __declspec(dllexport)` produces an unmangled symbol via `dumpbin /exports bin/Release/UtinniCore.dll | findstr utinni_init` after the build — should show `utinni_init` (no `?utinni_init@@YA...` form). If the symbol IS mangled, the `extern "C"` linkage is wrong (rare — likely a stray namespace wrap).

    **UtinniCore.vcxproj:** No changes should be required if `__declspec(dllexport)` is honored. But verify there's no `<ModuleDefinitionFile>` element that would override exports — if one exists, either add `utinni_init` to the .def file OR remove the .def reference (the repo currently has no .def file per 02-PATTERNS.md §4 verified-absent list).

    **Launcher side — Launcher/main.cpp::inject() (lines 174-210):**

    1. After the existing `GetExitCodeThread(hThread, (LPDWORD)&hDll)` at lines 200-209 (where `hDll` is now the remote HMODULE for UtinniCore.dll), insert the path-(a) second-CreateRemoteThread block from 02-RESEARCH.md §"Path-(a) detailed steps":
    ```cpp
    // C-01: After LoadLibraryA returns, fire utinni_init on a fresh remote thread.
    HMODULE localCore = LoadLibraryA(dllFilename.c_str());   // load locally to resolve GetProcAddress
    if (!localCore) throwError("[ERROR] Local LoadLibraryA(UtinniCore.dll) failed for utinni_init resolution.");
    FARPROC localInit = GetProcAddress(localCore, "utinni_init");
    if (!localInit) { FreeLibrary(localCore); throwError("[ERROR] utinni_init export not found in UtinniCore.dll."); }
    SIZE_T initOffset = (BYTE*)localInit - (BYTE*)localCore;
    FreeLibrary(localCore);

    FARPROC remoteInit = (FARPROC)((BYTE*)hDll + initOffset);
    HANDLE hInitThread = CreateRemoteThread(procInfo.hProcess, nullptr, 0,
                                             (LPTHREAD_START_ROUTINE)remoteInit, nullptr, 0, nullptr);
    if (!hInitThread) throwError("[ERROR] Couldn't open utinni_init remote thread.");
    WaitForSingleObject(hInitThread, INFINITE);
    CloseHandle(hInitThread);
    ```

    The `throwError` (or whatever the existing error-bail function is called in Launcher — verify in `Launcher/main.cpp` for the actual name; alternate names might be `errorOut`, `fatalError`, or inline `MessageBox + exit`) calls match the existing pattern; if Launcher uses a different idiom, adapt to it.

    Build verification: `msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86` must succeed. After build:
    - `dumpbin /exports bin/Release/UtinniCore.dll | findstr utinni_init` → ONE matching line, unmangled.
    - `bin/Release/Utinni.LoaderLockHarness.exe` → exits 0 AND prints "UtinniCore DllMain elapsed: X.XXX ms" where X is well under 50.000 (post-fix, expect microseconds — much less than 1 ms; if you see > 5 ms, something heavy is still in DllMain).
    - `dotnet test --filter FullyQualifiedName~LoaderLockHarnessTests` PASSES (Task 1's test now flips definitively green if it was on the boundary).

    Update `docs/ai/assessment.md` §"Status tracking" row C-01 → `done` with commit SHA. Reference the LoaderLockHarness scaffolding commit from Task 1 as a co-located deliverable.

    Commit message: `fix(C-01): DllMain returns immediately; utinni_init exported + fired by launcher via second CreateRemoteThread (path a)`.

    Body of commit message should cite:
    - Path-(a) choice rationale (chicken-and-egg argument against path (b) per RESEARCH).
    - The new `utinni_init` symbol export.
    - The Launcher delta.
    - The LoaderLockHarness regression guard.
    - Preservation: CON-N-01 untouched (utinni_init is plain export, not a detour); CON-H-01 honored (DllMain does no heavy work).
  </action>
  <verify>
    <automated>msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86 2>&amp;1 | findstr /R "error\|Error\|FAILED" ; dumpbin /exports bin\Release\UtinniCore.dll | findstr utinni_init &amp;&amp; dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release --filter FullyQualifiedName~LoaderLockHarnessTests</automated>
  </verify>
  <acceptance_criteria>
    - `dumpbin /exports bin/Release/UtinniCore.dll | findstr "utinni_init"` returns one line with `utinni_init` as the visible export name (NOT a mangled `?utinni_init@@YA...` form).
    - `bin/Release/Utinni.LoaderLockHarness.exe` runs and exits 0 (timing < 50 ms post-fix — expected behavior is microseconds).
    - `dotnet test --filter FullyQualifiedName~LoaderLockHarnessTests` passes.
    - `grep -nE 'extern "C" __declspec\(dllexport\) DWORD WINAPI utinni_init' UtinniCore/utinni.cpp` returns one match.
    - `grep -nE 'DisableThreadLibraryCalls' UtinniCore/utinni.cpp` returns one match (DllMain slimmed).
    - `grep -cE 'pluginManager.loadPlugins|clr::load\(\)' UtinniCore/utinni.cpp` reflects the migration: the calls are still present (inside `utinni_init`) but NOT inside `DllMain`. Verify by line-range proximity: the calls are within the `utinni_init` body, not the `DllMain` body.
    - `grep -nE 'GetProcAddress\(.*utinni_init' Launcher/main.cpp` returns one match.
    - `grep -cE 'CreateRemoteThread' Launcher/main.cpp` returns at least 2 (the existing LoadLibraryA one + the new utinni_init one).
    - `git diff HEAD~1 UtinniCore/utility/memory.cpp` returns empty (CON-N-04 preservation).
    - CI on master is green after this commit.
    - Commit message starts with `fix(C-01):` and references path-(a) + CON-N-01 + CON-H-01.
  </acceptance_criteria>
  <done>
    DllMain is slim; `utinni_init` is exported and fired by the launcher; LoaderLockHarness regression test confirms < 50 ms DllMain timing; ROADMAP success criterion #4 met.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (checkpoint): Maintainer injects UtinniCore.dll into live SWG client and confirms editor comes up</name>
  <what-built>Task 2 moved heavy startup out of DllMain into `utinni_init`, fired by the launcher's second `CreateRemoteThread`. The LoaderLockHarness regression test confirms DllMain timing is under 50 ms. Per CONTEXT.md D-06, the full proof of "no deadlock under loader-lock contention" + "editor still functions end-to-end" requires injection into a live SWG client.</what-built>
  <how-to-verify>
    1. Build the launcher: `msbuild Utinni.sln /p:Configuration=Release /p:Platform=x86` — must produce `bin/Release/Launcher.exe` AND `bin/Release/UtinniCore.dll` AND `bin/Release/UtinniCoreDotNet.dll`.
    2. Start the SWG client via `bin/Release/Launcher.exe` (or the equivalent local startup workflow).
    3. Confirm:
       - SWG client window appears within ~2 seconds (no hang at "Loading...").
       - Utinni editor UI is visible alongside / overlaid on the SWG client (depending on the editor's normal positioning).
       - Open Task Manager → Resource Monitor → CPU tab → "Wait Chain Analysis" for `SwgClient.exe` (or whatever the actual SWG process name is) — confirm no thread is blocked on `ntdll!LdrpDrainWorkQueue` or similar loader-lock primitives.
       - At least one plugin loads (visible in the editor UI's plugin panel, or in `bin/Release/utinni.log` — confirms `utinni_init` ran the plugin loader).
       - Optionally: try minimize/restore the SWG client window to confirm general responsiveness (the C-09 fix in Plan 02-04 will address one specific minimize-related hang; minimize should work either way at this point).
    4. If any of the above fail (no plugins load, plugin loader produces "couldn't find Plugins/ directory", editor doesn't appear, SWG hangs at "Loading..."): the C-01 fix has a regression. Capture the `bin/Release/utinni.log` output and report; likely culprit is the offset calculation in `Launcher/main.cpp` (verify `localInit - localCore` is the correct relative offset by manual inspection of `dumpbin /exports`).

    If the test passes: leave a comment on the commit (or append to the commit message via follow-up `chore(C-01): verified-by` commit) recording:
    - The SWG client launched cleanly.
    - Editor UI appeared with N plugins loaded.
    - No loader-lock wait chains observed.
    - Approximate launch-to-editor-visible time.
  </how-to-verify>
  <resume-signal>Type "approved — editor came up with plugins loaded; no loader-lock hang" OR describe the failure mode (hang location, missing UI element, plugin count mismatch, etc.).</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Status tracking sweep — flip assessment.md row C-01 done + reference plan commits</name>
  <files>
    docs/ai/assessment.md
  </files>
  <read_first>
    docs/ai/assessment.md §"Status tracking" (find C-01 row and verify current state),
    git log --oneline (capture the SHAs from Tasks 1 + 2 — both contribute to C-01 closure)
  </read_first>
  <action>
    Update `docs/ai/assessment.md` §"Status tracking" row for C-01 from current state to `done — fix(C-01): ${TASK_2_SHA} (harness scaffolding: ${TASK_1_SHA})`. If Task 2 already inline-updated the row, this task is a verification — confirm the row reflects the actual SHA and is set to `done`.

    No other dispositions to record here (Plan 02-03 doesn't close any CON-O open question; those land in Plans 02-01 + 02-02).

    Commit message: `docs(02-03): mark C-01 done in assessment.md (DllMain loader-lock fix + LoaderLockHarness)` — OR skip entirely if Task 2 inline already wrote the SHA accurately.
  </action>
  <verify>
    <automated>findstr /R /C:"C-01.*done" docs\ai\assessment.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^\| C-01 .*\|.*done" docs/ai/assessment.md` returns one match (status row flipped).
    - The commit SHA(s) in the row resolve via `git rev-parse --verify <SHA>`.
    - CI on master is green after this commit (docs-only change).
    - Commit message starts with `docs(02-03):` OR this task is a documented no-op (commit not needed).
  </acceptance_criteria>
  <done>
    C-01 status row is `done`; ROADMAP success criterion #4 reflected in the doc trail; Plan 02-03 is ready for verification.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Launcher → SWG client process (CreateRemoteThread × 2) | Existing trust boundary (Launcher already injects via CreateRemoteThread); Plan 02-03 adds ONE more remote-thread invocation against the same already-injected DLL — same trust posture. |
| `LoadLibraryA("UtinniCore.dll")` on local launcher process | The launcher loads UtinniCore.dll LOCALLY (in the launcher process) for the brief moment of resolving `GetProcAddress("utinni_init")`, then `FreeLibrary`s it. This is a standard DLL-injection idiom. |
| `utinni_init` exported symbol | New unmangled C-linkage export; any in-process consumer could call it. In practice, the launcher is the sole intended caller; the harness LoadLibrarys UtinniCore.dll (which runs DllMain only, never invokes utinni_init). |
| LoaderLockHarness.exe regression test | New executable, runs in-process; loads UtinniCore.dll and times DllMain. Read-only with respect to UtinniCore.dll — only does LoadLibrary + FreeLibrary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Tampering | `utinni_init` export — any in-process caller (including a malicious DLL co-loaded into SWG) could invoke it directly | accept | The existing risk surface is broader: a malicious DLL co-loaded into SWG can already invoke ANY function in UtinniCore.dll via `GetProcAddress` + offset, regardless of `extern "C"` linkage. Adding one more named export does not materially change the threat surface. Mitigation: V2 plugin signing (SEC-04 / MCF-04) is the strategic fix; not in V1 scope. |
| T-02-16 | DoS | utinni_init runs synchronously on the launcher-spawned thread; the launcher `WaitForSingleObject(hInitThread, INFINITE)` will block forever if utinni_init hangs | mitigate | If utinni_init hangs (e.g., CLR bring-up stalls), the launcher hangs alongside it — visible to the user, not a silent failure. This is strictly BETTER than the pre-fix behavior where a DllMain CreateThread to main() could hang inside the loader lock with no diagnostic surface. The LoaderLockHarness regression test catches "DllMain grew heavy" specifically; "utinni_init hangs" would surface as launcher GUI not appearing — easier to diagnose. Mitigation: the regression guard plus the human-verify checkpoint (Task 3) are the appropriate gates. |
| T-02-17 | EoP | `LoadLibraryA(dllFilename.c_str())` in launcher for GetProcAddress resolution — could be intercepted by a malicious DLL search-order hijack | accept | The launcher already does `LoadLibraryA(dllFilename.c_str())` in the existing `inject` code path for the remote-process injection (line 181-186 of pre-fix `Launcher/main.cpp`); the additional LOCAL `LoadLibraryA` in Task 2 uses the same `dllFilename` path. No new search-order surface. The user's local install path determines hijack risk — out-of-band concern for V1. |
| T-02-18 | DoS | LoaderLockHarness running in CI as part of `dotnet test` could fail intermittently if Windows is mid-update or has high background load (timing-sensitive 50ms threshold) | accept | Per RESEARCH §"Why this works" caveat, post-fix DllMain timing should be microseconds (DisableThreadLibraryCalls + return TRUE is essentially free). A 50ms threshold has ~2000× headroom. Flakiness risk is minimal. Mitigation: if CI reports periodic flakes, raise threshold to 100ms in the harness — but that's a Phase-6 STAB-03 cleanup, not a Phase-2 cost. |
| T-02-19 | Tampering | New `Utinni.LoaderLockHarness/main.cpp` is in-tree code that could be modified to mask the C-01 regression class | accept | The harness is a regression guard — its honesty depends on the codebase. Same trust posture as any test file. Code review at PR time is the gate. |
| T-02-SC | Tampering | npm/pip/cargo installs in this plan | accept | NO new package-manager installs. The new vcxproj uses only Win32 BCL (`Windows.h`, `cstdio`). The xUnit test uses only `System.Diagnostics.Process` (net472 BCL). Package Legitimacy Audit in 02-RESEARCH.md confirms no slopcheck gate needed. |
</threat_model>

<verification>
## Phase-level verification at plan close

1. **CI green after every commit.** Each `chore(02-03):` and `fix(C-01):` lands on master with the gated CI workflow passing (D-01).
2. **DllMain is slim.** `dumpbin /exports bin/Release/UtinniCore.dll | findstr utinni_init` shows the unmangled export; reading `UtinniCore/utinni.cpp` DllMain body confirms `DisableThreadLibraryCalls + return TRUE` only.
3. **LoaderLockHarness regression test passes.** `dotnet test --filter FullyQualifiedName~LoaderLockHarnessTests` exits 0; harness exit code 0 means timing < 50 ms (post-fix, expect << 1 ms).
4. **Live SWG verification approved.** Task 3 human-verify checkpoint logged as approved with editor coming up + plugins loaded.
5. **Preservation:** CON-N-01 (detour-table pattern), CON-N-04 (memory::copy VirtualProtect bracket) untouched. Verify with `git diff master HEAD UtinniCore/utility/memory.cpp` and inspection of the C-01 commit diff (no new `Detour::Create` calls; `utinni_init` is a plain export).
6. **Status tracking sweep done:** `docs/ai/assessment.md` C-01 row shows `done` with the Task 2 commit SHA.
</verification>

<success_criteria>
- 3 commits in this Utinni repo: 1 scaffolding (Task 1) + 1 C-01 fix (Task 2) + 1 docs sweep (Task 4 — or skip if Task 2 inline already covered it).
- Live SWG launch verified by maintainer (Task 3 checkpoint approved).
- LoaderLockHarness regression test passes in CI.
- assessment.md C-01 row marked `done`.
- ROADMAP success criterion #4 ("DllMain no longer does heavy startup per CON-H-01; CLR bring-up deferred") met.
- CI on master is green at the close of this plan; Plan 02-04 (C-09) is unblocked (it does not depend on this plan, but they are co-routed in Wave 3).
</success_criteria>

<output>
Create `.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-03-SUMMARY.md` documenting: the 3 commits in this plan; the path-(a) architectural choice rationale and what the maintainer would need to know if path-(b) ever needs to be reopened; the LoaderLockHarness regression-guard scope (catches "DllMain grew heavy" but NOT "no deadlock under contention"); the live-SWG verification result from Task 3; preservation verifications (CON-N-01 + CON-N-04 + CON-H-01).
</output>
</content>
</invoke>