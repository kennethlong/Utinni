---
phase: 02-critical-bug-burn-down-c-01-c-15
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - UtinniCore/swg/graphics/directx9.cpp
  - UtinniCore/swg/graphics/directx9.h
  - UtinniCore/test_exports.cpp
  - UtinniCoreDotNet/UI/Forms/FormMain.cs
  - UtinniCoreDotNet.Tests/FormMainSignallerTests.cs
  - docs/ai/assessment.md
autonomous: false
requirements:
  - STAB-01

user_setup:
  - service: "Live SWG minimize/restore verification (Tier 4 manual)"
    why: "Mock-signaller harness asserts WaitOne timeout behavior, but the actual minimize/restore-no-deadlock path requires a live SWG client. Per CONTEXT.md D-06 the full proof is Tier-4 residual."
    env_vars: []
    dashboard_config:
      - task: "After Plan 02-04 ships: inject UtinniCore.dll into SWG client via Launcher; minimize the SWG client window from the taskbar; restore it; repeat 5x. Confirm no UI hang, no busy-wait CPU spike on the UI thread."
        location: "Local dev workstation with SWG client"

must_haves:
  truths:
    - "UtinniCoreDotNet/UI/Forms/FormMain.cs WndProc no longer spins on `while (!IsPresentBlocked()) Thread.Sleep(1)`. The wait is replaced with `EventWaitHandle.WaitOne(TimeSpan.FromMilliseconds(100))` on a Win32 event handle exported by UtinniCore.dll (C-09)."
    - "The wait helper is extracted into an `internal` method (e.g., `WaitForPresentBlock(TimeSpan timeout)`) on FormMain so xUnit can invoke it via InternalsVisibleTo (already in place from Plan 02-01 Task 1)."
    - "UtinniCore/swg/graphics/directx9.cpp exports `extern \"C\" __declspec(dllexport) HANDLE __cdecl getPresentBlockedEvent()` — creates a manual-reset Win32 event lazily on first call; subsequent calls return the same HANDLE."
    - "`hkPresent` in directx9.cpp (at line 210-241 per CONTEXT.md §canonical_refs surface) emits `SetEvent(hPresentBlockedEvent)` AFTER observing `blockPresentCall == true` and setting `isPresenting = false`."
    - "`blockPresent(bool value)` in directx9.cpp calls `ResetEvent(hPresentBlockedEvent)` when value is false (re-arming the event for the next minimize cycle)."
    - "Managed side wraps the Win32 HANDLE as a managed `EventWaitHandle` via `SafeWaitHandle(h, ownsHandle: false)` — net472 BCL primitives, NO new NuGet packages."
    - "xUnit `FormMainSignallerTests.cs` defines a mock signaller interface; one test asserts `WaitForPresentBlock` returns within timeout EVEN IF the signal never fires (no infinite spin); another test asserts that when the event IS signalled, the wait returns immediately."
    - "Timeout policy: 100 ms per CONTEXT.md D-05. If the game thread never signals, the UI thread proceeds anyway — minimize/restore is best-effort, not a correctness gate."
    - "CON-N-01 (detour-table pattern) NOT touched — the C-09 fix adds new code paths around the EXISTING `hkPresent` detour but does not modify the detour-table registration."
    - "CON-N-04 (memory::copy VirtualProtect bracket) NOT touched — verify with `git diff` on UtinniCore/utility/memory.cpp."
    - "docs/ai/assessment.md §\"Status tracking\" row C-09 flipped to `done` with commit SHA."
    - "CI on master is green after every fix commit in this plan (D-01)."
    - "Decision provenance: per D-02 this plan is SPLIT from C-01 (Plan 02-03) — separate plans because the two architectural fixes share no code; per D-03 every commit in this plan is atomic (C-09 fix commit + docs sweep commit); per D-04 the C-09 fix lands with the FormMainSignallerTests regression test in the SAME commit (red-before-green via the mock-signaller test failing when WaitForPresentBlock spins, passing when it uses EventWaitHandle.WaitOne); per D-07 all new test files land in the single UtinniCoreDotNet.Tests project — NO new test project added, NO new sibling project (this plan adds only one native export getPresentBlockedEvent to UtinniCore.dll plus managed code in FormMain.cs)."
  artifacts:
    - path: "UtinniCoreDotNet.Tests/FormMainSignallerTests.cs"
      provides: "C-09 — mock-signaller test asserting WaitForPresentBlock timeout + signalled-return behavior"
      contains: "WaitForPresentBlock_SignalNeverFires_ReturnsWithinTimeout, WaitForPresentBlock_SignalFires_ReturnsImmediately"
    - path: "UtinniCore/swg/graphics/directx9.cpp"
      provides: "getPresentBlockedEvent export + SetEvent on hkPresent observation + ResetEvent in blockPresent(false)"
      contains: "getPresentBlockedEvent, SetEvent(hPresentBlockedEvent), ResetEvent(hPresentBlockedEvent)"
    - path: "UtinniCore/swg/graphics/directx9.h"
      provides: "Forward declaration of getPresentBlockedEvent export (header for any UtinniCore-internal callers; the extern \"C\" export itself is in the .cpp)"
    - path: "UtinniCoreDotNet/UI/Forms/FormMain.cs"
      provides: "WaitForPresentBlock(TimeSpan timeout) internal helper that EventWaitHandle.WaitOnes on the native event handle"
      contains: "WaitForPresentBlock, EventWaitHandle, SafeWaitHandle"
  key_links:
    - from: "UtinniCore/swg/graphics/directx9.cpp::hkPresent"
      to: "Win32 manual-reset event"
      via: "SetEvent(hPresentBlockedEvent) after isPresenting = false transition"
      pattern: "SetEvent\\(hPresentBlockedEvent\\)"
    - from: "UtinniCoreDotNet/UI/Forms/FormMain.cs::WaitForPresentBlock"
      to: "UtinniCore.dll!getPresentBlockedEvent → SafeWaitHandle → EventWaitHandle"
      via: "P/Invoke + handle wrapping; WaitOne(TimeSpan) returns true on signal, false on timeout"
      pattern: "EventWaitHandle.*SafeWaitHandle|getPresentBlockedEvent"
    - from: "UtinniCoreDotNet/UI/Forms/FormMain.cs::WndProc"
      to: "BlockPresent(true) → WaitForPresentBlock(100ms) → base.WndProc"
      via: "WM_SYSCOMMAND handler for SC_MINIMIZE/SC_RESTORE/SC_MAXIMIZE"
      pattern: "WM_SYSCOMMAND.*BlockPresent\\(true\\)"
---

<objective>
**Architectural fix C-09 (Wave 3): UI/game-thread busy-wait deadlock.** Replace the `while (!IsPresentBlocked()) Thread.Sleep(1)` spin at `UtinniCoreDotNet/UI/Forms/FormMain.cs:65-73` with a `EventWaitHandle.WaitOne(TimeSpan.FromMilliseconds(100))` against a Win32 event signalled by the native `hkPresent` detour. Adds a new `getPresentBlockedEvent` export on UtinniCore.dll (also test-only-visible via the test_exports.cpp scaffolding established in Plan 02-02 — Plan 02-04 adds the production export in directx9.cpp directly).

Per CONTEXT.md D-02 + D-08, this plan is SPLIT from C-01 (Plan 02-03) because they share no code; per CONTEXT.md D-01, this plan depends on Plan 02-02 (single-file criticals) closing with green CI on master.

Note on parallelism: Plans 02-03 and 02-04 share no code per RESEARCH §"Landmines / preservation surfaces" #8 — they CAN run in parallel within Wave 3 if executor permits.

Purpose: Close the remaining V1-blocking architectural-class bug — the UI hang on minimize/restore. Establish the cross-language event-signaller pattern that future Wave-1 plugin work (Phase 7+) may need for similar UI ↔ game-thread synchronization.

Output: 2 fix commits — (1) C-09 native+managed fix (directx9.cpp adds SetEvent / ResetEvent / getPresentBlockedEvent export; FormMain.cs WndProc replaces busy-wait with EventWaitHandle.WaitOne) + (2) docs sweep (assessment.md C-09 → done). Plus 1 test file (FormMainSignallerTests.cs).

Total commits: 2 (the test file commits with the C-09 fix in a single atomic commit per D-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-CONTEXT.md
@.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md
@.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-VALIDATION.md
@.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-PATTERNS.md
@.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-01-PLAN.md
@.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-02-PLAN.md
@.planning/intel/constraints.md
@.planning/codebase/CONVENTIONS.md
@docs/ai/assessment.md

<interfaces>
<!-- Key contracts for executors. -->

From `UtinniCoreDotNet/UI/Forms/FormMain.cs:57-78` (C-09 surface — current state):
- WndProc handles `WM_SYSCOMMAND` with command bits `SC_MINIMIZE`, `SC_RESTORE`, `SC_MAXIMIZE`.
- Lines 65-73 (per CONTEXT.md §canonical_refs surface map):
```csharp
UtinniCore.DirectX.directx9.BlockPresent(true);
while (!UtinniCore.DirectX.directx9.IsPresentBlocked())
{
    Thread.Sleep(1);
}
// ... rest of WndProc
```
- The `BlockPresent(true)` / `IsPresentBlocked()` calls are CppSharp-generated P/Invokes against the native side.

From `UtinniCore/swg/graphics/directx9.cpp:210-241` (C-09 native surface — `hkPresent`):
- When `blockPresentCall == true`, sets `isPresenting = false` and skips `present()`.
- When `blockPresentCall == false`, sets `isPresenting = true` and calls `present()`.
- The two-flag dance is the protocol the UI thread is polling for; C-09 fix replaces "poll IsPresentBlocked" with "wait on signalled event".

From net472 BCL (no new NuGet packages per 02-RESEARCH.md §C-09 "No new NuGet packages"):
- `System.Threading.EventWaitHandle` — managed wrapper around a Win32 event HANDLE.
- `Microsoft.Win32.SafeHandles.SafeWaitHandle` — safe wrapper, can wrap a raw HANDLE with `ownsHandle: false`.
- `System.TimeSpan.FromMilliseconds(100)` — the timeout value per CONTEXT.md D-05.

From `UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj` (Phase-1 wiring + Plan 02-01 Task 1):
- `[InternalsVisibleTo("UtinniCoreDotNet.Tests")]` already added to UtinniCoreDotNet/Properties/AssemblyInfo.cs (from Plan 02-01 Task 1) — enables tests to invoke `internal` methods like `WaitForPresentBlock`.

From `UtinniCoreDotNet/Utility/Native.cs:67-77` (hand-written `[DllImport]` template):
```csharp
[DllImport("user32.dll", EntryPoint = "ReleaseCapture")]
public static extern void ReleaseCapture();
```

From `UtinniCore/test_exports.cpp` (created in Plan 02-02 Task 1):
- The plan 02-02 file already exists; this plan EXTENDS the production code (directx9.cpp) directly rather than adding a test-only wrapper, because `getPresentBlockedEvent` is BOTH production code (used by FormMain.cs at runtime) AND optionally testable (the mock-signaller test uses a fake HANDLE so it doesn't need to call the native export at all — see Task 2 below).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: C-09 fix — native event + getPresentBlockedEvent export + managed WaitForPresentBlock + mock-signaller test (single atomic commit per D-04)</name>
  <files>
    UtinniCore/swg/graphics/directx9.cpp,
    UtinniCore/swg/graphics/directx9.h,
    UtinniCoreDotNet/UI/Forms/FormMain.cs,
    UtinniCoreDotNet.Tests/FormMainSignallerTests.cs
  </files>
  <read_first>
    UtinniCore/swg/graphics/directx9.cpp (full file — focus on hkPresent at :210-241 + blockPresent at :250-260-ish + the existing exports near top),
    UtinniCore/swg/graphics/directx9.h (full file — verify what's exposed; the new `extern "C" __declspec(dllexport) HANDLE __cdecl getPresentBlockedEvent()` declaration may need a forward decl here for UtinniCore-internal callers; the export itself lives in the .cpp),
    UtinniCoreDotNet/UI/Forms/FormMain.cs:50-90 (WndProc context — Native.WM_SYSCOMMAND constant location, SC_MINIMIZE/SC_RESTORE/SC_MAXIMIZE handling, surrounding flow),
    UtinniCoreDotNet/Utility/Native.cs (where WM_SYSCOMMAND + SC_* constants live; verify naming conventions),
    UtinniCore/test_exports.cpp (from Plan 02-02 Task 1 — confirm the file exists and review the extern "C" export pattern that's already in use),
    UtinniCore/plugin_framework/utinni_plugin.h:50 (extern "C" __declspec(dllexport) precedent),
    UtinniCoreDotNet.Tests/HotkeyTests.cs:1-68 (xUnit analog),
    UtinniCoreDotNet/Utility/Native.cs:67-77 (P/Invoke shape),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §C-09 (complete fix shape — native side getPresentBlockedEvent + SetEvent in hkPresent + ResetEvent in blockPresent(false); managed side Lazy<EventWaitHandle> + SafeWaitHandle + WaitOne(timeout); injectable signaller for testability),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §"C-09 signaller design" (Win32 CreateEvent manual-reset, EventWaitHandle wrapping, 100ms timeout policy),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §"Landmines" #9 (isPresenting flag semantics — verify SetEvent ordering matches the flag protocol)
  </read_first>
  <behavior>
    Test 1 (RED first): `FormMainSignallerTests.WaitForPresentBlock_SignalNeverFires_ReturnsWithinTimeout` — construct an `EventWaitHandle(false, EventResetMode.ManualReset)` with NO signal source; pass it to `FormMain.WaitForPresentBlock(TimeSpan.FromMilliseconds(50))`; assert the method returns within ~60 ms (small slack for runner variance) AND returns `false` (the WaitOne return value indicating timeout). Use `Stopwatch` to measure elapsed.
    Test 2 (RED first): `WaitForPresentBlock_SignalFires_ReturnsImmediately` — construct the same EventWaitHandle; `Task.Run(() => { Thread.Sleep(10); ewh.Set(); });` (signal after 10ms); call `WaitForPresentBlock(TimeSpan.FromMilliseconds(500))`; assert the method returns within ~50 ms (well under the 500ms timeout) AND returns `true`.
    Test 3: `WaitForPresentBlock_AlreadySignalled_ReturnsImmediately` — construct an EventWaitHandle that's ALREADY signalled (`new EventWaitHandle(true, EventResetMode.ManualReset)`); call `WaitForPresentBlock(TimeSpan.FromMilliseconds(500))`; assert returns within ~5 ms AND returns `true`.

    NOTE: These tests pass a mocked/owned EventWaitHandle directly — they do NOT call into UtinniCore.dll for the signal. The injection seam in `WaitForPresentBlock` (described in <action>) lets tests bypass the native handle entirely. This avoids the "test process doesn't have SWG/d3d9 loaded" problem and proves the wait semantics independently of the native plumbing.
  </behavior>
  <action>
    **Native side — UtinniCore/swg/graphics/directx9.cpp:**

    1. Add a file-scope `static HANDLE hPresentBlockedEvent = nullptr;` near the existing static state declarations.

    2. Add a new export at the end of the file (or near other exports — match the existing file's structure): `extern "C" __declspec(dllexport) HANDLE __cdecl getPresentBlockedEvent()` body creates the event lazily on first call (`if (!hPresentBlockedEvent) hPresentBlockedEvent = CreateEvent(nullptr, TRUE, FALSE, nullptr);` — TRUE = manual-reset, FALSE = initially non-signalled), returns the handle. The CppSharp generation step skips this function (C linkage, not mangled C++).

    3. In `hkPresent` (at the surface 02-CONTEXT.md cites as :210-241, but verify exact lines in the current file — Plan 02-02 may have added lines via Task 7's C-11 fix), find the branch where `blockPresentCall == true` is observed and `isPresenting = false` is set. AFTER that assignment and BEFORE the `return` (or `present()` skip), insert `if (hPresentBlockedEvent) SetEvent(hPresentBlockedEvent);`. This is the signaller.

    4. In `blockPresent(bool value)`, when `value` is false (i.e., re-enable Present), reset the event: `if (hPresentBlockedEvent) ResetEvent(hPresentBlockedEvent);`. This re-arms for the next minimize cycle.

    **Optional: UtinniCore/swg/graphics/directx9.h:** Add a forward declaration if any UtinniCore-internal code needs to reference `getPresentBlockedEvent` (likely NO — the only consumer is the managed FormMain code via P/Invoke, which uses the export directly). Skip the header entry if no internal caller exists; the `__declspec(dllexport)` in the .cpp is sufficient for the P/Invoke surface.

    **Managed side — UtinniCoreDotNet/UI/Forms/FormMain.cs:**

    1. Add `using System; using System.Threading; using Microsoft.Win32.SafeHandles; using System.Runtime.InteropServices;` to the using block at the top of the file (preserve existing using order per S-3).

    2. Add a P/Invoke import at the class scope (or a private nested NativeBridge class — match the file's existing style): `[DllImport("UtinniCore", CallingConvention = CallingConvention.Cdecl, EntryPoint = "getPresentBlockedEvent")] private static extern IntPtr GetPresentBlockedEvent();`. (Note: `EntryPoint` is the unmangled symbol name, no leading `utinni_` prefix because this export is production code, not test-only.)

    3. Add a `private static readonly Lazy<EventWaitHandle> presentBlockedSignal = new Lazy<EventWaitHandle>(() => { IntPtr h = GetPresentBlockedEvent(); var ewh = new EventWaitHandle(false, EventResetMode.ManualReset); ewh.SafeWaitHandle = new SafeWaitHandle(h, ownsHandle: false); return ewh; });` field. The `ownsHandle: false` is critical — the native side owns the handle, the managed wrapper just observes it.

    4. Add an `internal bool WaitForPresentBlock(TimeSpan timeout)` method that returns `presentBlockedSignal.Value.WaitOne(timeout)`. This is the test seam — `internal` so `InternalsVisibleTo` lets tests reach it; the tests pass a different `EventWaitHandle` via a constructor-or-property-injection variant (see Task 1's testability seam below).

    5. **Testability seam:** Add a TEST-ONLY overload OR change the signal source to be injectable. Recommendation (matches the C-07 ctor-injection pattern from Plan 02-02 Task 5): add a static `internal static EventWaitHandle TestSignaller = null;` field. The Lazy lambda checks `if (TestSignaller != null) return TestSignaller;` before doing the native handle resolution. Tests set `FormMain.TestSignaller = mockEwh;` before calling `WaitForPresentBlock`; production never touches it (default null → native path).

    6. Rewrite `WndProc` lines 65-73 to replace the busy-wait loop with the new helper. New body (inside the existing `if (command == SC_MINIMIZE || ...)` block):
    ```csharp
    UtinniCore.DirectX.directx9.BlockPresent(true);
    // C-09: Wait up to 100 ms for the game thread to confirm Present is blocked.
    // If it never signals (game thread is wedged), fall through — minimize/restore is best-effort.
    WaitForPresentBlock(TimeSpan.FromMilliseconds(100));
    ```
    Remove the `Thread.Sleep(1)` spin entirely.

    **Test side — UtinniCoreDotNet.Tests/FormMainSignallerTests.cs:**

    Create the file with 23-line MIT header; using order; namespace `UtinniCoreDotNet.Tests`; class `FormMainSignallerTests`. Three `[Fact]` methods per `<behavior>` above. Tests set `FormMain.TestSignaller = myMockEwh;` before calling `formMain.WaitForPresentBlock(timeout)`; in `finally` blocks set `FormMain.TestSignaller = null;` to clean up state. Use `using` blocks for EventWaitHandle disposal; use `Stopwatch.StartNew()` to measure elapsed time and assert it's within the expected band.

    **Build verification:** `msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86` succeeds. `dumpbin /exports bin/Release/UtinniCore.dll | findstr getPresentBlockedEvent` returns one unmangled match. `dotnet test --filter FullyQualifiedName~FormMainSignallerTests` passes all three tests.

    Update `docs/ai/assessment.md` §"Status tracking" row C-09 → `done` with the commit SHA placeholder.

    Commit message: `fix(C-09): replace WndProc busy-wait with Win32 event signaller (CreateEvent + EventWaitHandle.WaitOne(100ms timeout))`.

    Body should cite:
    - Native: getPresentBlockedEvent export + SetEvent in hkPresent + ResetEvent in blockPresent(false).
    - Managed: Lazy<EventWaitHandle> + SafeWaitHandle wrapper (no new NuGet — net472 BCL).
    - Testability seam (TestSignaller static field).
    - Timeout policy: 100ms; minimize/restore is best-effort.
    - Preservation: CON-N-01 (detour-table) and CON-N-04 (memory::copy) untouched.
  </action>
  <verify>
    <automated>msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86 2>&amp;1 | findstr /R "error\|Error\|FAILED" ; dumpbin /exports bin\Release\UtinniCore.dll | findstr getPresentBlockedEvent &amp;&amp; dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release --filter FullyQualifiedName~FormMainSignallerTests</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter FullyQualifiedName~FormMainSignallerTests` passes all three tests.
    - `dumpbin /exports bin/Release/UtinniCore.dll | findstr getPresentBlockedEvent` returns one line with `getPresentBlockedEvent` as the unmangled export name.
    - `grep -nE 'SetEvent\(hPresentBlockedEvent\)' UtinniCore/swg/graphics/directx9.cpp` returns at least one match.
    - `grep -nE 'ResetEvent\(hPresentBlockedEvent\)' UtinniCore/swg/graphics/directx9.cpp` returns at least one match.
    - `grep -nE 'extern "C" __declspec\(dllexport\) HANDLE __cdecl getPresentBlockedEvent' UtinniCore/swg/graphics/directx9.cpp` returns one match.
    - `grep -cE 'Thread\.Sleep\(1\)' UtinniCoreDotNet/UI/Forms/FormMain.cs` returns 0 (busy-wait removed).
    - `grep -nE 'EventWaitHandle.*WaitOne' UtinniCoreDotNet/UI/Forms/FormMain.cs` returns at least one match.
    - `grep -nE 'internal bool WaitForPresentBlock\(TimeSpan' UtinniCoreDotNet/UI/Forms/FormMain.cs` returns one match.
    - `grep -nE 'SafeWaitHandle\(.*, ownsHandle:\s*false\)' UtinniCoreDotNet/UI/Forms/FormMain.cs` returns one match (managed wrapper does NOT own the native handle).
    - `git diff HEAD~1 UtinniCore/utility/memory.cpp` returns empty (CON-N-04 preservation).
    - `grep -nE 'Detour::Create' UtinniCore/swg/graphics/directx9.cpp` count is UNCHANGED from pre-task (CON-N-01 preservation — no new detours added; verify by reading the diff).
    - CI on master is green after this commit.
    - Commit message starts with `fix(C-09):` and references CON-N-01 + CON-N-04 preservation.
  </acceptance_criteria>
  <done>
    Busy-wait replaced with event-signalled wait; mock-signaller tests pass; getPresentBlockedEvent exported with C linkage; production code uses the SafeWaitHandle wrap; CON-N-01 + CON-N-04 preserved.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (checkpoint): Maintainer verifies SWG minimize/restore no longer hangs the UI</name>
  <what-built>Task 1 replaced the `Thread.Sleep(1)` spin in FormMain.WndProc with `EventWaitHandle.WaitOne(100ms timeout)` against a Win32 event signalled by the native `hkPresent` detour. Mock-signaller tests pass. Per CONTEXT.md D-06, the full proof of "minimize/restore no longer hangs" requires injection into a live SWG client.</what-built>
  <how-to-verify>
    1. Build: `msbuild Utinni.sln /p:Configuration=Release /p:Platform=x86` — produces `bin/Release/Launcher.exe` + `bin/Release/UtinniCore.dll` + `bin/Release/UtinniCoreDotNet.dll`.
    2. Start SWG client via `bin/Release/Launcher.exe` (or the equivalent local startup workflow).
    3. Wait for the editor UI to be visible alongside SWG (verifies Plan 02-03's C-01 fix continues to work — UI comes up cleanly).
    4. Minimize the SWG client window via its taskbar button (or Win+Down).
    5. Restore the SWG client window via its taskbar entry.
    6. Repeat steps 4-5 five times in quick succession.
    7. Confirm:
       - No UI hang during any minimize or restore — the window animates smoothly.
       - Open Task Manager → Performance → CPU; during the minimize/restore cycles confirm the SwgClient.exe CPU does NOT spike to 100% on a single thread (the busy-wait pre-fix would peg one core for the duration of the 1ms-sleep loop).
       - The editor UI remains responsive throughout — clicking a plugin tab during minimize/restore should not freeze.
    8. Optional stress test: minimize/restore 20 times in a row, no pauses. Confirm no degradation.
    9. Edge case: if SWG happens to be wedged (rare; might require pre-staging a hang scenario), confirm that the UI still responds within ~100ms (the timeout) even when the native signal never fires — this is the "best-effort minimize/restore" behavior per CONTEXT.md D-05.

    If any of the above fail (visible UI hang > 200ms, CPU pegged on UI thread, editor freezes): report the failure mode + capture `bin/Release/utinni.log`. Likely culprits: SetEvent ordering wrong in hkPresent (signal fires before isPresenting state is consistent); ResetEvent not firing when blockPresent(false) is called; SafeWaitHandle ownership flag mis-set.

    If the test passes: leave a Verified-by note on the C-09 commit (follow-up `chore(C-09): verified-by` commit acceptable to avoid SHA churn).
  </how-to-verify>
  <resume-signal>Type "approved — minimize/restore no hang, no CPU spike" OR describe the failure mode.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Status tracking sweep — flip assessment.md row C-09 done</name>
  <files>
    docs/ai/assessment.md
  </files>
  <read_first>
    docs/ai/assessment.md §"Status tracking" (find C-09 row),
    git log --oneline (capture Task 1 SHA)
  </read_first>
  <action>
    Update `docs/ai/assessment.md` §"Status tracking" row C-09 from current state to `done — fix(C-09): ${TASK_1_SHA}`. If Task 1 already inline-wrote the SHA, this task is a verify-only no-op.

    Commit message: `docs(02-04): mark C-09 done in assessment.md (WndProc busy-wait → EventWaitHandle.WaitOne)` — OR skip entirely if Task 1's inline update is accurate.
  </action>
  <verify>
    <automated>findstr /R /C:"C-09.*done" docs\ai\assessment.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^\| C-09 .*\|.*done" docs/ai/assessment.md` returns one match.
    - The commit SHA in the row resolves via `git rev-parse --verify <SHA>`.
    - CI on master is green after this commit (docs-only change).
    - Commit message starts with `docs(02-04):` OR this task is a documented no-op.
  </acceptance_criteria>
  <done>
    C-09 row marked `done`; Plan 02-04 ready for verification; Phase 2 burn-down is complete pending the cross-phase verify step.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UtinniCore → manage UI synchronization | New cross-language Win32 event handle exchange — native side signals; managed side waits. The handle ownership is on the native side (`ownsHandle: false` on the SafeWaitHandle wrapper). |
| `getPresentBlockedEvent` export | New unmangled C-linkage export on UtinniCore.dll. Any in-process caller can call it and obtain the HANDLE. This is intentional — the managed FormMain code is the intended consumer. |
| Test seam: `FormMain.TestSignaller` static field | Tests can inject a mock EventWaitHandle to bypass the native handle. Production code never sets this field (default null). |
| `hkPresent` detour modification (additive only) | The C-09 fix adds `SetEvent(hPresentBlockedEvent);` calls INSIDE the existing `hkPresent` body but does NOT modify the detour-table registration or the underlying Present hook. CON-N-01 preserved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-20 | Tampering | `getPresentBlockedEvent` export — any in-process caller could call it and (a) consume the HANDLE and (b) Set/Reset it, disrupting the UI/game-thread sync | accept | Same risk posture as any other UtinniCore export. A malicious DLL co-loaded into SWG could already disrupt the entire framework. Plugin signing (SEC-04 / MCF-04) is the strategic mitigation, V2 scope. |
| T-02-21 | DoS | The 100ms timeout means a wedged game thread DOES NOT hang the UI — but if the timeout is set incorrectly to a much larger value (or omitted), the behavior regresses to the original deadlock class | mitigate | The timeout is hard-coded in `WaitForPresentBlock(TimeSpan.FromMilliseconds(100))` at the WndProc call site. Code review at PR time catches accidental changes. The mock-signaller test asserts the timeout fires (`WaitForPresentBlock_SignalNeverFires_ReturnsWithinTimeout`) — any regression of the timeout to `TimeSpan.Zero` or `Timeout.InfiniteTimeSpan` flips that test red. |
| T-02-22 | Information Disclosure | The native handle value (HANDLE = void*) leaks into managed memory via the SafeWaitHandle wrapper | accept | The handle value is process-local and only meaningful inside the same process. No PII or secrets. Standard Win32 pattern. |
| T-02-23 | Spoofing | A malicious in-process actor could SetEvent on the handle prematurely, causing `WaitForPresentBlock` to return before the game thread actually blocks Present | accept | Same threat posture as any in-process state. The minimize/restore behavior is best-effort per CONTEXT.md D-05; a spurious signal causes one frame's worth of render artifact at worst. Plugin sandboxing is V2 scope. |
| T-02-24 | DoS | SafeWaitHandle with `ownsHandle: false` — if it accidentally gets `ownsHandle: true`, the managed GC could free the native event prematurely, causing wait failures across the codebase | mitigate | Explicit `ownsHandle: false` in code per `<action>`. Acceptance criteria `grep -nE 'SafeWaitHandle\(.*, ownsHandle:\s*false\)' UtinniCoreDotNet/UI/Forms/FormMain.cs returns one match` is the regression guard. |
| T-02-SC | Tampering | npm/pip/cargo installs in this plan | accept | NO new package-manager installs. The managed code uses only net472 BCL (`System.Threading`, `Microsoft.Win32.SafeHandles`, `System.Runtime.InteropServices`). Package Legitimacy Audit in 02-RESEARCH.md confirms no slopcheck gate needed. |
</threat_model>

<verification>
## Phase-level verification at plan close

1. **CI green after every commit.** Each `fix(C-09):` and `docs(02-04):` lands on master with the gated CI workflow passing (D-01).
2. **Native export visible.** `dumpbin /exports bin/Release/UtinniCore.dll | findstr getPresentBlockedEvent` shows the unmangled symbol.
3. **Busy-wait removed.** `grep -cE 'Thread\.Sleep\(1\)' UtinniCoreDotNet/UI/Forms/FormMain.cs` returns 0.
4. **Mock-signaller tests pass.** `dotnet test --filter FullyQualifiedName~FormMainSignallerTests` exits 0 with 3 tests passed.
5. **Live SWG minimize/restore approved.** Task 2 human-verify checkpoint logged as approved.
6. **Preservation:** CON-N-01 (detour-table) and CON-N-04 (memory::copy) untouched. Verify with `git diff` on the C-09 commit's `UtinniCore/utility/memory.cpp` (empty) and inspection of `Detour::Create` count in `directx9.cpp` (unchanged from pre-task).
7. **Status tracking sweep done:** `docs/ai/assessment.md` C-09 row marked `done` with the Task 1 SHA.

## Cross-phase verification (after both Plan 02-03 and Plan 02-04 close)

- ROADMAP success criterion #1: every C-01 through C-15 closed → flip status row check.
- ROADMAP success criterion #2: CI green across the burn-down → confirmed by D-01 plan-boundary gating.
- ROADMAP success criterion #3: data/utinni.cfg blank login → closed by Plan 02-01 Task 7.
- ROADMAP success criterion #4: DllMain no heavy startup; CLR deferred → closed by Plan 02-03 Task 2.

Plus C-16 (Plan 02-02 Task 9) + KB-05 (Plan 02-02 Task 10) — out-of-assessment-list bugs that also need their status / disposition rows.
</verification>

<success_criteria>
- 2 commits in this Utinni repo: 1 C-09 fix (Task 1) + 1 docs sweep (Task 3 — or skip if Task 1 inline already covered it).
- Live SWG minimize/restore verified by maintainer (Task 2 checkpoint approved).
- FormMainSignallerTests passes in CI (3 tests).
- assessment.md C-09 row marked `done`.
- Busy-wait pattern removed from UI thread; 100ms timeout governs minimize/restore behavior.
- CI on master is green at the close of this plan; ALL 15 critical bugs + KB-05 + C-16 + 4 CON-O dispositions closed → Phase 2 STAB-01 acceptance criteria met.
</success_criteria>

<output>
Create `.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-04-SUMMARY.md` documenting: the 2 commits in this plan; the cross-language event-signaller design and what it ENABLES for future Wave-1 plugin work (Phase 7+) where similar UI ↔ game-thread sync may be needed; the live-SWG verification result from Task 2; preservation verifications (CON-N-01 + CON-N-04); and a cross-phase note confirming Phase 2 closure — all 15 C-NN + KB-05 + C-16 closed, CI green, ready for `/gsd:verify-work` against ROADMAP Phase 2 success criteria.
</output>
</content>
</invoke>