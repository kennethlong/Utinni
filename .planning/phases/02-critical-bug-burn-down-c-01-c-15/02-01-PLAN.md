---
phase: 02-critical-bug-burn-down-c-01-c-15
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj
  - UtinniCoreDotNet.Tests/HotkeyTests.cs
  - UtinniCoreDotNet.Tests/GroundSceneCallbacksTests.cs
  - UtinniCoreDotNet.Tests/PluginLoaderTests.cs
  - UtinniCoreDotNet.Tests/VsixManifestTests.cs
  - UtinniCoreDotNet.Tests/UtinniCfgTests.cs
  - UtinniCoreDotNet.Tests/Fixtures/BrokenPlugin/BrokenPlugin.csproj
  - UtinniCoreDotNet.Tests/Fixtures/BrokenPlugin/BrokenPlugin.cs
  - UtinniCoreDotNet.Tests/Fixtures/GoodPlugin/GoodPlugin.csproj
  - UtinniCoreDotNet.Tests/Fixtures/GoodPlugin/GoodPlugin.cs
  - UtinniCoreDotNet/Properties/AssemblyInfo.cs
  - UtinniCoreDotNet/Callbacks/GroundSceneCallbacks.cs
  - UtinniCoreDotNet/Callbacks/GameCallbacks.cs
  - UtinniCoreDotNet/Callbacks/ObjectCallbacks.cs
  - UtinniCoreDotNet/PluginFramework/PluginLoader.cs
  - UtinniCoreDotNet/Hotkeys/Hotkey.cs
  - sdk/UtinniPluginTemplates/Vsix/source.extension.vsixmanifest
  - sdk/UtinniPluginTemplates/Vsix/Vsix.csproj
  - data/utinni.cfg
  - Utinni.sln
  - docs/ai/assessment.md
  - UtinniPlugins/The Jawa Toolbox/TheJawaToolbox/TheJawaToolbox.vcxproj
  - UtinniPlugins/The Jawa Toolbox/TheJawaToolbox.sln
autonomous: false
requirements:
  - STAB-01

user_setup:
  - service: "VS 2019 + VS 2022 IDE install verification (C-12)"
    why: "C-12 widens the VSIX InstallationTarget to [16.0,18.0); per D-06 the maintainer must install the built VSIX into both VS 2019 and VS 2022 and confirm both template wizards run. Cannot be automated this phase."
    env_vars: []
    dashboard_config:
      - task: "Build VSIX locally and install into VS 2019 IDE; create one C# plugin and one C++ plugin from the templates"
        location: "Local VS 2019 install"
      - task: "Build VSIX locally and install into VS 2022 IDE; create one C# plugin and one C++ plugin from the templates"
        location: "Local VS 2022 install"
  - service: "UtinniPlugins sister-repo Debug build verification (C-13)"
    why: "UtinniPlugins has no CI workflow in Phase 2 (D-09); C-13 cross-repo fix verified by manual local build."
    env_vars: []
    dashboard_config:
      - task: "After Plan 02-01 ships, clone UtinniPlugins at the C-13 fix commit, build TJT Debug|x86, and confirm output lands in Utinni/bin/Debug/Plugins/TheJawaToolbox/"
        location: "Local UtinniPlugins clone"

must_haves:
  truths:
    - "GroundSceneCallbacks.DequeuePostDrawLoopCalls drains the postDrawLoopCallQueue (not the preDrawLoopCallQueue), and a shared Drain(ConcurrentQueue<Action>) helper covers every queue-drain site in GameCallbacks/GroundSceneCallbacks/ObjectCallbacks (C-04 / CON-O-02 default-fallback)."
    - "PluginLoader.Load isolates per-plugin failures: when one [Export(typeof(IPlugin))] plugin throws during composition, surviving plugins still load AND the failing plugin's directory name plus loader-exception message appear in the captured log output (C-06)."
    - "Hotkey.ProcessString uses Enum.TryParse and split-on-`+` segment handling; malformed input ('Ctrl + T') no longer throws — instead Enabled is set to false and a warning is logged; 'Shift + Alt + Z' parses to ModifierKeys=Shift|Alt, Key=Z (C-08)."
    - "Both Phase-1 [Fact(Skip = \"C-08:...\")] / [Theory(Skip = \"C-08:...\")] markers on HotkeyTests.cs are removed; the multi-modifier InlineData is updated to (\"Shift + Alt + Z\", Keys.Shift | Keys.Alt, Keys.Z); all four prior tests pass plus a new MalformedModifier_DoesNotThrow_DisablesHotkey case passes."
    - "sdk/UtinniPluginTemplates/Vsix/source.extension.vsixmanifest has Version=\"[16.0,18.0)\" on all three <InstallationTarget> elements AND the <Prerequisite Id=\"Microsoft.VisualStudio.Component.CoreEditor\"> element (C-12 / CON-O-04 default-fallback)."
    - "Vsix.csproj's Microsoft.VisualStudio.SDK and Microsoft.VSSDK.BuildTools PackageReferences are bumped to a VS-2022-compatible version (17.x line) and the package restore succeeds (C-12)."
    - "UtinniPlugins TheJawaToolbox.vcxproj line 63 uses three `..\\` levels (`..\\..\\..\\Utinni\\bin\\Debug\\Plugins\\TheJawaToolbox\\`) and TheJawaToolbox.sln has both `Debug|x86.ActiveCfg = Debug|Win32` AND `Debug|x86.Build.0 = Debug|Win32` entries for the TJT project GUID (C-13)."
    - "data/utinni.cfg ships with `loginServerAddress0=` and `loginServerPort0=` blank (no SWGEmu defaults) per CON-D-01 (C-14)."
    - "docs/ai/assessment.md §\"Open questions\" §2 (AddPostDrawLoopCall usage) and §4 (VS 2019 pin rationale) carry the D-12 default-fallback disposition with commit SHAs."
    - "docs/ai/assessment.md §\"Status tracking\" rows for C-04, C-06, C-08, C-12, C-13, C-14 are flipped to `done` with PR/SHA links."
    - "[assembly: InternalsVisibleTo(\"UtinniCoreDotNet.Tests\")] is added to UtinniCoreDotNet/Properties/AssemblyInfo.cs (enables tests to invoke `internal` Drain helper)."
    - "CI on master is green after every task in this plan (per D-01: CI gates each plan boundary)."
    - "Decision provenance: per D-03 every C-NN fix lands as its own atomic commit (~6 fix commits + 1 scaffold + 1 docs sweep in this plan); per D-04 every managed-side fix in this plan ships with a red-before-green xUnit test in the SAME commit; per D-07 all new test files land in the single UtinniCoreDotNet.Tests project (no new test project) with Fixtures/BrokenPlugin and Fixtures/GoodPlugin sibling csprojs added to Utinni.sln; per D-09 C-13 is a separate-clone separate-commit separate-PR cross-repo task in UtinniPlugins with no CI workflow added to that repo this phase."
  artifacts:
    - path: "UtinniCoreDotNet.Tests/GroundSceneCallbacksTests.cs"
      provides: "C-04 regression test — DequeuePostDrawLoopCalls drains postDrawLoopCallQueue"
      contains: "DequeuePostDrawLoopCalls_DrainsPostQueue_NotPreQueue"
      min_lines: 40
    - path: "UtinniCoreDotNet.Tests/PluginLoaderTests.cs"
      provides: "C-06 regression test — broken plugin isolation"
      contains: "Load_WithOneBrokenAndOneGoodPlugin_LoadsGoodAndLogsBroken"
      min_lines: 50
    - path: "UtinniCoreDotNet.Tests/VsixManifestTests.cs"
      provides: "C-12 regression test — VSIX InstallationTarget XML assertion"
      contains: "InstallationTargets_AllUseVersionRange_16To18Exclusive"
      min_lines: 35
    - path: "UtinniCoreDotNet.Tests/UtinniCfgTests.cs"
      provides: "C-14 regression test — blank login fields in data/utinni.cfg"
      contains: "LoginServerAddress0_AndLoginServerPort0_AreBlank"
      min_lines: 35
    - path: "UtinniCoreDotNet.Tests/Fixtures/BrokenPlugin/BrokenPlugin.csproj"
      provides: "Deliberately-throwing [InheritedExport(typeof(IPlugin))] fixture for C-06 test"
    - path: "UtinniCoreDotNet.Tests/Fixtures/GoodPlugin/GoodPlugin.csproj"
      provides: "Working [InheritedExport(typeof(IPlugin))] fixture for C-06 test"
    - path: "UtinniCoreDotNet/Properties/AssemblyInfo.cs"
      provides: "InternalsVisibleTo attribute enabling test access to internal Drain helper"
      contains: "[assembly: InternalsVisibleTo(\"UtinniCoreDotNet.Tests\")]"
  key_links:
    - from: "UtinniCoreDotNet/Callbacks/GroundSceneCallbacks.cs"
      to: "Drain(ConcurrentQueue<Action>) helper"
      via: "method extraction; called by DequeueMainLoopCalls/DequeuePreDrawLoopCalls/DequeuePostDrawLoopCalls/DequeueUpdateLoopCalls"
      pattern: "while \\(.*\\.TryDequeue\\(out"
    - from: "UtinniCoreDotNet/PluginFramework/PluginLoader.cs"
      to: "per-plugin try/catch + Log.Error"
      via: "ReflectionTypeLoadException + generic Exception handlers around DirectoryCatalog.ComposeParts"
      pattern: "catch \\(ReflectionTypeLoadException"
    - from: "UtinniCoreDotNet/Hotkeys/Hotkey.cs:66-92"
      to: "Enum.TryParse + split-on-`+` segment handling"
      via: "ProcessString rewrite"
      pattern: "Enum\\.TryParse"
    - from: "sdk/UtinniPluginTemplates/Vsix/source.extension.vsixmanifest"
      to: "VS 2019 + VS 2022 InstallationTarget"
      via: "Version=\"[16.0,18.0)\" on all targets + prerequisite"
      pattern: "Version=\"\\[16\\.0,18\\.0\\)\""
    - from: "UtinniPlugins/TheJawaToolbox.vcxproj"
      to: "Utinni/bin/Debug/Plugins/TheJawaToolbox/"
      via: "<OutDir>..\\..\\..\\Utinni\\bin\\Debug\\Plugins\\TheJawaToolbox\\</OutDir> on Debug config"
      pattern: "OutDir.*\\.\\.\\\\\\.\\.\\\\\\.\\.\\\\Utinni"
    - from: "data/utinni.cfg"
      to: "CON-D-01 blank-login default"
      via: "key=value file content with blank RHS"
      pattern: "loginServerAddress0\\s*=\\s*$"
---

<objective>
**Trivial-criticals burn-down (Wave 1).** Close C-04 (queue drain typo), C-06 (plugin-loader exception swallowing), C-08 (Hotkey malformed-input throw), C-12 (VSIX VS-2019 pin widened to VS-2022), C-13 (TJT Debug path — cross-repo `UtinniPlugins`), and C-14 (utinni.cfg blank login). Each fix lands with a red-before-green xUnit regression test (D-04) — except C-12 which is XML assertion only, C-13 which is cross-repo manual verify (D-06), and C-14 which is file-content assertion. CI must stay green on `master` across every commit in this plan (D-01).

Purpose: Establish the "fix + commit + green CI" rhythm with the lowest-risk bugs first, prove the Phase-1 test infrastructure absorbs Phase-2 patterns (System.Windows.Forms ref already wired from Phase 1 commit `3b001bb`; new `/Fixtures` subdir + `InternalsVisibleTo` are the Phase-2 additions), and clear two of the four CON-O dispositions (CON-O-02 default-fallback, CON-O-04 default-fallback with audit-then-widen).

Output: Six fix commits + one Wave-0 scaffolding commit + one status-tracking commit. Two fixture csprojs in `UtinniCoreDotNet.Tests/Fixtures/`. New test files for C-04, C-06, C-12, C-14. Updated `HotkeyTests.cs` (two skips removed, one new test added). Updated `docs/ai/assessment.md` status table + Open Questions §2 / §4 dispositions. A cross-repo PR in `UtinniPlugins` for C-13.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-CONTEXT.md
@.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md
@.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-VALIDATION.md
@.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-PATTERNS.md
@.planning/phases/01-ci-tier-1-c-scaffold/01-CONTEXT.md
@.planning/phases/01-ci-tier-1-c-scaffold/01-02-SUMMARY.md
@.planning/intel/constraints.md
@.planning/codebase/CONVENTIONS.md
@docs/ai/assessment.md

<interfaces>
<!-- Key contracts for executors. All extracted from the working tree at planning time. -->

From `UtinniCoreDotNet.Tests/HotkeyTests.cs:1-68` (universal xUnit analog per 02-PATTERNS.md §1):
- 23-line MIT header opens `/**` closes `**/`, `Copyright (c) 2020 Philip Klatt` verbatim.
- `using System.Windows.Forms;` + `using Xunit;` + `using UtinniCoreDotNet.Hotkeys;` (System → third-party → project order).
- `namespace UtinniCoreDotNet.Tests` + `public class HotkeyTests` Allman-braced.
- Test naming `[Method]_[Scenario]_[ExpectedOutcome]`.
- Skip-with-comment pattern: `[Fact(Skip = "C-08: ...")]` (line 60-66) and `[Theory(Skip = "C-08: ...")]` (line 49-58).
- `Record.Exception(() => ...)` + `Assert.Null(ex)` for "did it throw" assertions (line 62-65).

From `UtinniCoreDotNet/PluginFramework/IPlugin.cs:30-49`:
```csharp
public class PluginInformation { /* Name, Description, Author etc. */ }

[InheritedExport(typeof(IPlugin))]
public interface IPlugin
{
    PluginInformation Information { get; }
    UtINI GetConfig();
}
```
Note: `[InheritedExport(typeof(IPlugin))]` is on the interface — MEF discovers any class implementing IPlugin without needing per-fixture `[Export]`.

From `UtinniCoreDotNet/Callbacks/GroundSceneCallbacks.cs:97-106` (C-04 surface — the bug):
```csharp
private static void DequeuePostDrawLoopCalls(IntPtr pGroundScene)
{
    while (preDrawLoopCallQueue.Count > 0)   // <-- BUG: wrong queue
    {
        if (preDrawLoopCallQueue.TryDequeue(out var func))   // <-- BUG: wrong queue
        {
            func();
        }
    }
}
```

From `UtinniCoreDotNet/Hotkeys/Hotkey.cs:66-92` (C-08 surface):
- `ProcessString(string keyComboStr)` does `Enum.Parse(typeof(Keys), modifiers, true)` and `Enum.Parse(typeof(Keys), key, true)`.
- Both `Enum.Parse` calls throw `ArgumentException` on malformed input.
- Splits on first ` + ` only (does not handle `Shift + Alt + Z` multi-modifier).
- Companion: `HotkeyManager.cs:106-114` `Load()` calls `Hotkey.UpdateKeys(string)` which routes through `ProcessString`.

From `sdk/UtinniPluginTemplates/Vsix/source.extension.vsixmanifest:9-17` (C-12 surface):
- Three `<InstallationTarget>` elements (Community / Pro / Enterprise) each with `Version="[16.0,17.0)"`.
- One `<Prerequisite Id="Microsoft.VisualStudio.Component.CoreEditor" Version="[16.0,17.0)" ... />`.
- XML namespace: `http://schemas.microsoft.com/developer/vsx-schema/2011`.

From `sdk/UtinniPluginTemplates/Vsix/Vsix.csproj:74-75`:
- `<PackageReference Include="Microsoft.VisualStudio.SDK" Version="16.0.206" ExcludeAssets="runtime" />`
- `<PackageReference Include="Microsoft.VSSDK.BuildTools" Version="16.8.3038" />`

From `data/utinni.cfg:1-7` (C-14 surface — current state):
```
[ClientGame]
    loginServerPort0=44453
    loginServerAddress0=login.swgemu.com
    skipIntro=1
```

From `UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj` (Phase-1 wiring):
- SDK-style csproj, `<TargetFramework>net472</TargetFramework>`, `<Platforms>x86</Platforms>`, `<AppendPlatformToOutputPath>false</AppendPlatformToOutputPath>`, `<RestorePackagesWithLockFile>true</RestorePackagesWithLockFile>`.
- `<Reference Include="System.Windows.Forms" />` already present (Phase 1 commit `3b001bb`).
- `<ProjectReference Include="..\UtinniCoreDotNet\UtinniCoreDotNet.csproj" />`.
- `packages.lock.json` checked in with xunit 2.9.3 / xunit.runner.visualstudio 3.1.5 / Microsoft.NET.Test.Sdk 17.13.0.

From `UtinniPlugins/The Jawa Toolbox/TheJawaToolbox/TheJawaToolbox.vcxproj:63` (C-13 surface):
- `<OutDir>..\..\..\..\Utinni\bin\Debug\Plugins\TheJawaToolbox\</OutDir>` — FOUR `..\` (one too many; siblings on lines 67 and 71 correctly use THREE).

From `UtinniPlugins/The Jawa Toolbox/TheJawaToolbox.sln:26-28` (C-13 surface):
- `{EA1E6CED-6315-486A-8AA7-D41DF9D34888}.Debug|x86.ActiveCfg = Debug|Win32` present.
- `{EA1E6CED-6315-486A-8AA7-D41DF9D34888}.Debug|x86.Build.0 = Debug|Win32` ABSENT (must be inserted between ActiveCfg and the Release block).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0 scaffolding): Add InternalsVisibleTo + create fixture project skeletons in solution</name>
  <files>
    UtinniCoreDotNet/Properties/AssemblyInfo.cs,
    UtinniCoreDotNet.Tests/Fixtures/BrokenPlugin/BrokenPlugin.csproj,
    UtinniCoreDotNet.Tests/Fixtures/BrokenPlugin/BrokenPlugin.cs,
    UtinniCoreDotNet.Tests/Fixtures/GoodPlugin/GoodPlugin.csproj,
    UtinniCoreDotNet.Tests/Fixtures/GoodPlugin/GoodPlugin.cs,
    Utinni.sln
  </files>
  <read_first>
    UtinniCoreDotNet/Properties/AssemblyInfo.cs (full file — current state of attributes),
    UtinniCoreDotNet/PluginFramework/IPlugin.cs:30-49 (IPlugin contract + [InheritedExport] discovery semantics),
    sdk/UtinniPluginTemplates/DotNetPluginTemplate/Plugin.cs:1-25 (IPlugin source shape reference),
    UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj (SDK-style csproj precedent — Platforms x86, AppendPlatformToOutputPath false, RestorePackagesWithLockFile true),
    UtinniCoreDotNet/Hotkeys/Hotkey.cs:1-23 (verbatim 23-line MIT header source — copy bytes-for-bytes),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-PATTERNS.md §2 "Fixture projects" (full section — has the SDK-style csproj template with Private=False, RootNamespace, etc.),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §"Test project layout" + §"Wave 0 Gaps" (scaffolding inventory),
    .planning/phases/01-ci-tier-1-c-scaffold/01-PATTERNS.md (Phase-1 sln-entry mapping pattern for new x86-only csprojs),
    Utinni.sln (full file — project entry shape, GlobalSection ProjectConfigurationPlatforms format)
  </read_first>
  <action>
    Add `[assembly: InternalsVisibleTo("UtinniCoreDotNet.Tests")]` to `UtinniCoreDotNet/Properties/AssemblyInfo.cs` (append to existing attribute block; add `using System.Runtime.CompilerServices;` if not present).

    Create `UtinniCoreDotNet.Tests/Fixtures/BrokenPlugin/BrokenPlugin.csproj` as an SDK-style csproj per 02-PATTERNS.md §2 template: TargetFramework `net472`; PlatformTarget + Platforms `x86`; `<AppendPlatformToOutputPath>false</AppendPlatformToOutputPath>`; LangVersion 7.3; `<IsPackable>false</IsPackable>`; RootNamespace `UtinniCoreDotNet.Tests.Fixtures.BrokenPlugin`; AssemblyName `BrokenPlugin`; single `<ProjectReference Include="..\..\..\UtinniCoreDotNet\UtinniCoreDotNet.csproj"><Private>False</Private></ProjectReference>` (Private=False prevents the fixture from re-bundling UtinniCoreDotNet.dll).

    Create `UtinniCoreDotNet.Tests/Fixtures/BrokenPlugin/BrokenPlugin.cs`: 23-line MIT header verbatim from `Hotkey.cs:1-23`; `using UtinniCore.Utinni;` + `using UtinniCoreDotNet.PluginFramework;`; namespace `UtinniCoreDotNet.Tests.Fixtures.BrokenPlugin`; class `BrokenPlugin : IPlugin` whose parameterless ctor throws `System.InvalidOperationException("BrokenPlugin deliberately throws during construction — exercises C-06 isolation.")`; implements `PluginInformation Information { get; }` (auto-property returning null is fine — never reached) and `UtINI GetConfig() => null;`.

    Create `UtinniCoreDotNet.Tests/Fixtures/GoodPlugin/GoodPlugin.csproj` identical to BrokenPlugin.csproj except `RootNamespace=UtinniCoreDotNet.Tests.Fixtures.GoodPlugin`, `AssemblyName=GoodPlugin`, and ProjectReference path stays `..\..\..\UtinniCoreDotNet\UtinniCoreDotNet.csproj`.

    Create `UtinniCoreDotNet.Tests/Fixtures/GoodPlugin/GoodPlugin.cs`: same header + usings + namespace shape; class `GoodPlugin : IPlugin` whose ctor sets `Information = new PluginInformation { /* leave name/desc empty if PluginInformation has settable properties; otherwise use whatever the type supports */ }` and `GetConfig() => null;`. The ctor must NOT throw.

    Add both fixture csprojs to `Utinni.sln`: C# project-type GUID `{FAE04EC0-301F-11D3-BF4B-00C04F79EFBC}`; relative path `UtinniCoreDotNet.Tests\Fixtures\BrokenPlugin\BrokenPlugin.csproj` (and Good variant). Wire ProjectConfigurationPlatforms with the same x86-only mapping as `UtinniCoreDotNet.Tests` (Debug|x86 → Debug|x86, Release|x86 → Release|x86; skip RelWithDbgInfo per Phase-1 precedent or map it to Release|x86 if needed). Verify both fixtures appear under a "Fixtures" solution folder (optional but consistent with the flat-root convention — planner's call if absent in Phase-1 sln).

    Build verification: `msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86` should succeed; both fixture DLLs must land at `UtinniCoreDotNet.Tests/Fixtures/BrokenPlugin/bin/Release/net472/BrokenPlugin.dll` and the GoodPlugin equivalent (per `AppendPlatformToOutputPath=false`).

    Commit message: `chore(02-01): wave-0 scaffolding — InternalsVisibleTo + BrokenPlugin/GoodPlugin fixture projects`.
  </action>
  <behavior>
    - The added `InternalsVisibleTo("UtinniCoreDotNet.Tests")` permits later Task 2 to call `internal` members on `UtinniCoreDotNet` from the test project without reflection.
    - BrokenPlugin.dll exists in the test output tree but the test project does NOT directly reference it (the C-06 test will load it via a `DirectoryCatalog` pointing at the fixture's output directory at test time).
    - Building BrokenPlugin in isolation does NOT throw (only construction throws — that happens during MEF composition).
    - The Utinni.sln Build pass produces three .dll/.exe artifacts beyond the existing solution outputs: BrokenPlugin.dll, GoodPlugin.dll (no error if PluginInformation initialization needs adjustment after reading the actual type).
  </behavior>
  <verify>
    <automated>msbuild Utinni.sln /m /restore /p:Configuration=Release /p:Platform=x86 2>&amp;1 | findstr /R "error\|Error\|FAILED" ; dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release</automated>
  </verify>
  <done>
    All six files exist; both fixture csprojs build their respective DLLs to `bin/Release/net472/`; `dotnet test` still passes the Phase-1 baseline (2 pass + 2 skipped); InternalsVisibleTo attribute added; commit on master with green CI confirmation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: C-04 — DequeuePostDrawLoopCalls drains the right queue + shared Drain helper</name>
  <files>
    UtinniCoreDotNet/Callbacks/GroundSceneCallbacks.cs,
    UtinniCoreDotNet/Callbacks/GameCallbacks.cs,
    UtinniCoreDotNet/Callbacks/ObjectCallbacks.cs,
    UtinniCoreDotNet.Tests/GroundSceneCallbacksTests.cs
  </files>
  <read_first>
    UtinniCoreDotNet/Callbacks/GroundSceneCallbacks.cs (full file — surface for C-04 + queue field declarations + Add*Call sites),
    UtinniCoreDotNet/Callbacks/GameCallbacks.cs:100-145 (DequeueMainLoopCalls + DequeuePreMainLoopCalls — apply same Drain refactor),
    UtinniCoreDotNet/Callbacks/ObjectCallbacks.cs (full file — DequeueOnTargetCalls — apply same Drain refactor),
    UtinniCoreDotNet.Tests/HotkeyTests.cs:1-68 (universal xUnit analog per 02-PATTERNS.md §1),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §C-04 (fix shape — Drain(ConcurrentQueue&lt;Action&gt;) helper, single-TryDequeue loop pattern),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-PATTERNS.md §5 row "C-04" (modify-in-place pattern reference),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-CONTEXT.md §specifics (the Drain helper is named in assessment.md so this category of bug cannot recur — applies to ALL three callback files)
  </read_first>
  <behavior>
    Test 1 (RED first): `GroundSceneCallbacksTests.DequeuePostDrawLoopCalls_DrainsPostQueue_NotPreQueue` — enqueue sentinel `Action`s into `postDrawLoopCallQueue` via `AddPostDrawLoopCall(...)`, enqueue different sentinels into `preDrawLoopCallQueue` via `AddPreDrawLoopCall(...)`, invoke `DequeuePostDrawLoopCalls` via reflection or via the `internal`-accessible static method (the `Drain` helper should be `internal` so the test reaches it via `InternalsVisibleTo`); assert post-queue sentinels ran AND pre-queue is unchanged.
    Test 2: `Drain_EmptyQueue_DoesNothing` — invoke `Drain` on an empty `ConcurrentQueue<Action>`; assert returns without throwing.
    Test 3: `DequeueUpdateLoopCalls_DrainsUpdateQueue_NotPreOrPostQueue` — symmetric check on `GroundSceneCallbacks.DequeueUpdateLoopCalls`.
  </behavior>
  <action>
    In `GroundSceneCallbacks.cs:97-106`, replace `preDrawLoopCallQueue` with `postDrawLoopCallQueue` in both the loop guard and the `TryDequeue` call. Then extract an `internal static void Drain(ConcurrentQueue&lt;Action&gt; q)` method that does `while (q.TryDequeue(out var func)) { func(); }` (single-condition loop per 02-RESEARCH.md §C-04 — drops the racey `q.Count > 0` outer check). Rewrite all four GroundSceneCallbacks dequeue methods (`DequeueUpdateLoopCalls`, `DequeuePreDrawLoopCalls`, `DequeuePostDrawLoopCalls`, and whichever camera-change dequeue exists at the same level) to call `Drain(thatQueue)`. Apply the identical refactor to `GameCallbacks.cs:100-120` (`DequeueMainLoopCalls`, `DequeuePreMainLoopCalls`) and `ObjectCallbacks.cs` (`DequeueOnTargetCalls`) — same `internal static void Drain(...)` helper duplicated per file (acceptable; do not factor across files for Phase 2 — that's R-A territory in Phase 3).

    Create `UtinniCoreDotNet.Tests/GroundSceneCallbacksTests.cs` with the three tests above. 23-line MIT header verbatim; using order System → Xunit → UtinniCoreDotNet; namespace `UtinniCoreDotNet.Tests`; class `GroundSceneCallbacksTests`. Use the `InternalsVisibleTo` seam (Task 1) to reach `Drain` directly; use `[Fact]` shape from `HotkeyTests.cs:33-39`. Reset queue state between tests via a private helper or rely on `[Collection]` xUnit isolation (planner's call — the queues are static, so `[Collection(... DisableParallelization=true ...)]` may be cleanest; alternatively drain at the start of each test).

    Disposition update: append to `docs/ai/assessment.md` §"Open questions" §2 with the D-12 default-fallback wording from 02-RESEARCH.md §CON-O-02 (Bounded archaeology — assume IS used; Drain helper introduced ensures this class of bug cannot recur; reference commit SHA after the commit is made — use a placeholder and resolve in the same commit via `git commit --amend` OR commit the code first then commit the disposition separately citing the prior SHA).

    Commit message: `fix(C-04): DequeuePostDrawLoopCalls drains postDrawLoopCallQueue + introduce Drain helper across GroundScene/Game/Object callbacks (closes CON-O-02 per D-12)`.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release --filter FullyQualifiedName~GroundSceneCallbacksTests</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter FullyQualifiedName~GroundSceneCallbacksTests` passes all three tests (`DequeuePostDrawLoopCalls_DrainsPostQueue_NotPreQueue`, `Drain_EmptyQueue_DoesNothing`, `DequeueUpdateLoopCalls_DrainsUpdateQueue_NotPreOrPostQueue`).
    - `grep -n "preDrawLoopCallQueue" UtinniCoreDotNet/Callbacks/GroundSceneCallbacks.cs` shows the identifier ONLY inside `DequeuePreDrawLoopCalls` / `AddPreDrawLoopCall` (NOT inside `DequeuePostDrawLoopCalls`).
    - `grep -nE "while \(.*\.TryDequeue\(out" UtinniCoreDotNet/Callbacks/GroundSceneCallbacks.cs` returns at least one match (the new `Drain` helper).
    - `grep -nE "internal static void Drain" UtinniCoreDotNet/Callbacks/GroundSceneCallbacks.cs UtinniCoreDotNet/Callbacks/GameCallbacks.cs UtinniCoreDotNet/Callbacks/ObjectCallbacks.cs` returns three matches (one per file).
    - `docs/ai/assessment.md` §"Open questions" §2 has a "Resolved 2026-Q2" disposition referencing the commit SHA.
    - CI on master is green after this commit (D-01).
    - Commit message starts with `fix(C-04):`.
  </acceptance_criteria>
  <done>
    All three new tests pass; the `Drain` helper exists in all three callback files; the C-04 typo is fixed; the assessment.md CON-O-02 disposition is recorded; CI is green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: C-06 — PluginLoader per-plugin isolation with try/catch + diagnostic logging</name>
  <files>
    UtinniCoreDotNet/PluginFramework/PluginLoader.cs,
    UtinniCoreDotNet.Tests/PluginLoaderTests.cs
  </files>
  <read_first>
    UtinniCoreDotNet/PluginFramework/PluginLoader.cs (full file — current single-AggregateCatalog composition + pluginConfig enumeration),
    UtinniCoreDotNet/PluginFramework/IPlugin.cs:30-49 (IPlugin + [InheritedExport] contract),
    UtinniCoreDotNet/Logging/Log.cs (or wherever Log.Error / Log.Info live — required for both production logging and test capture via Log.AddOutputSinkCallback per CON-M-07),
    UtinniCoreDotNet.Tests/HotkeyTests.cs:1-68 (universal xUnit analog),
    UtinniCoreDotNet.Tests/Fixtures/BrokenPlugin/BrokenPlugin.cs (Wave-0 fixture from Task 1 — throws on construction),
    UtinniCoreDotNet.Tests/Fixtures/GoodPlugin/GoodPlugin.cs (Wave-0 fixture from Task 1 — loads successfully),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §C-06 (fix shape — per-plugin loop, ReflectionTypeLoadException with LoaderExceptions enumeration, PerPluginLoader private nested class, optional pluginDir parameter as testability seam),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-PATTERNS.md §5 row "C-06"
  </read_first>
  <behavior>
    Test 1 (RED first): `PluginLoaderTests.Load_WithOneBrokenAndOneGoodPlugin_LoadsGoodAndLogsBroken` — point a `PluginLoader` at a test directory containing both `BrokenPlugin.dll` and `GoodPlugin.dll`; subscribe a log capture via `Log.AddOutputSinkCallback`; call `Load(pluginDir)`; assert (a) `Plugins.Count() == 1`, (b) the surviving plugin's type is `GoodPlugin`, (c) the captured log contains the string `"BrokenPlugin"` AND contains either `"InvalidOperationException"` or the exception message substring `"deliberately throws"`.
    Test 2: `Load_AllPluginsBroken_ResultsInZeroLoaded_AndAllLogged` — point at a directory containing only `BrokenPlugin.dll`; assert `Plugins.Count() == 0` AND log captured the broken-plugin name.
    Test 3: `Load_EmptyDirectory_LoadsZeroPlugins_NoExceptions` — point at an empty temp directory; assert `Plugins.Count() == 0` AND no exception thrown.
  </behavior>
  <action>
    Refactor `PluginLoader.Load` per 02-RESEARCH.md §C-06: replace the single `AggregateCatalog` build with a per-plugin `foreach` loop over `EnumeratePluginConfigs(pluginManager)` (the existing enumeration API at the current call site); for each enabled config, build a `new DirectoryCatalog(pluginPath)` + `new CompositionContainer(catalog)` + a private nested `PerPluginLoader` class with `[ImportMany(typeof(IPlugin))] public IEnumerable<IPlugin> Plugins`; call `container.ComposeParts(loader)`; on `catch (ReflectionTypeLoadException ex)` enumerate `ex.LoaderExceptions` and `Log.Error` each one; on `catch (Exception ex)` log the broken plugin's directory name + `ex.GetType().Name + ": " + ex.Message`; accumulate `loader.Plugins` into a top-level `List<IPlugin>` and assign to `Plugins` at the end. Add a testability seam: change `public void Load()` to `public void Load(string pluginDir = null)` defaulting to `utinni.GetPath() + "/Plugins/"` when null — the production call site keeps the no-arg call; the test passes an explicit directory.

    Create `UtinniCoreDotNet.Tests/PluginLoaderTests.cs` with the three tests above. The tests create a temp directory, copy the fixture DLLs into it (the fixture DLLs live at the fixture project's `bin/Release/net472/` per Task 1 — compute path via `AppContext.BaseDirectory` + relative walk), then construct a `PluginLoader` and call `Load(tempDir)`. Use `Log.AddOutputSinkCallback(sink)` where `sink` is `Action<string>` that appends to a `StringBuilder`; assert against the StringBuilder content.

    Update `docs/ai/assessment.md` §"Status tracking" row for C-06 to `done` with the commit SHA placeholder (resolve in same commit or follow-up).

    Commit message: `fix(C-06): PluginLoader.Load isolates per-plugin failures + logs broken plugin names`.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release --filter FullyQualifiedName~PluginLoaderTests</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter FullyQualifiedName~PluginLoaderTests` passes all three tests.
    - `grep -nE "catch \(ReflectionTypeLoadException" UtinniCoreDotNet/PluginFramework/PluginLoader.cs` returns at least one match.
    - `grep -nE "ImportMany\(typeof\(IPlugin\)\)" UtinniCoreDotNet/PluginFramework/PluginLoader.cs` returns at least one match (the new `PerPluginLoader` private nested class).
    - `grep -nE "public void Load\(string pluginDir" UtinniCoreDotNet/PluginFramework/PluginLoader.cs` returns one match (the optional-parameter testability seam).
    - The production call site of `PluginLoader.Load(...)` (somewhere in `UtinniCoreDotNet/main.cs` or `FormMain.cs`) still compiles without changes (default parameter preserves backward compat).
    - CI on master is green after this commit.
    - Commit message starts with `fix(C-06):`.
  </acceptance_criteria>
  <done>
    Per-plugin try/catch isolation works; broken plugin name surfaces in log output; surviving plugins still load; testability seam allows the test to point at a fixture directory.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: C-08 — Hotkey.ProcessString uses TryParse + multi-segment split; unskip both Phase-1 C-08 tests</name>
  <files>
    UtinniCoreDotNet/Hotkeys/Hotkey.cs,
    UtinniCoreDotNet.Tests/HotkeyTests.cs
  </files>
  <read_first>
    UtinniCoreDotNet/Hotkeys/Hotkey.cs (full file — ProcessString at :66-92, GetKeyComboString at :105-113 for round-trip contract, ctor + UpdateKeys callers),
    UtinniCoreDotNet/Hotkeys/HotkeyManager.cs:106-114 (Load() — same root cause via UpdateKeys),
    UtinniCoreDotNet.Tests/HotkeyTests.cs (full file — existing 2 passing + 2 skipped tests to update),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §C-08 (fix shape — Enum.TryParse, split-on-`+`, multi-modifier semantics: first N-1 segments are modifiers OR'd together; last segment is the single key),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §"C-08 Phase-1 handoff" (the multi-modifier InlineData must change to (Keys.Shift | Keys.Alt, Keys.Z) — NOT the current (Keys.Shift, Keys.Alt | Keys.Z)),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-PATTERNS.md §5 rows "C-08" (three rows — Hotkey.cs, HotkeyManager.cs, HotkeyTests.cs)
  </read_first>
  <behavior>
    Test 1 (already exists, currently `[Theory(Skip = "C-08:...")]`): `Ctor_StringConstructor_MultiModifierChord_ParsesFlags` — InlineData updated from `("Shift + Alt + Z", Keys.Shift, Keys.Alt | Keys.Z)` to `("Shift + Alt + Z", Keys.Shift | Keys.Alt, Keys.Z)`; Skip parameter removed; test flips green.
    Test 2 (already exists, currently `[Fact(Skip = "C-08:...")]`): `Ctor_StringConstructor_MalformedInput_DoesNotThrow` — Skip parameter removed; `Record.Exception(() => new Hotkey("test", "test", "Ctrl + T", () => { }, overrideGameInput: false))` returns null AND the constructed hotkey has `Enabled == false`.
    Test 3 (new): `Ctor_StringConstructor_MalformedModifier_DisablesHotkey` — pass `"Garbage + S"`; assert no throw AND `hk.Enabled == false`.
    Tests 4-5 (already pass): `Ctor_StringConstructor_SingleKey_SetsKeyAndNoModifier`, `Ctor_StringConstructor_ModifierChord_SetsBoth` — unchanged.
  </behavior>
  <action>
    Rewrite `Hotkey.ProcessString` in `UtinniCoreDotNet/Hotkeys/Hotkey.cs:66-92` per 02-RESEARCH.md §C-08: handle null/empty input by logging warning + setting `Enabled = false`; split on `'+'` and `.Trim()` each segment; for single segment use `Enum.TryParse<Keys>(seg, true, out k)` and set `ModifierKeys = Keys.None`, `Key = k` (warn-and-disable on parse failure); for 2+ segments, treat first N-1 segments as modifiers OR'd together via `mods |= m` per `Enum.TryParse<Keys>` call (warn-and-disable on any segment failure), treat the LAST segment as the single key (warn-and-disable on failure). Set `ModifierKeys = mods` and `Key = keyOnly` only when all segments parse.

    Update `HotkeyTests.cs`:
    - Remove the `Skip = "C-08:..."` parameter from `[Theory]` on `MultiModifierChord_ParsesFlags`.
    - Change `[InlineData("Shift + Alt + Z", Keys.Shift, Keys.Alt | Keys.Z)]` to `[InlineData("Shift + Alt + Z", Keys.Shift | Keys.Alt, Keys.Z)]`.
    - Remove the `Skip = "C-08:..."` parameter from `[Fact]` on `MalformedInput_DoesNotThrow`. Strengthen the assertion: after `Assert.Null(ex)`, also assert `hk.Enabled == false` (requires the Record.Exception lambda to capture the constructed hotkey — restructure as `Hotkey hk = null; var ex = Record.Exception(() => hk = new Hotkey(...)); Assert.Null(ex); Assert.NotNull(hk); Assert.False(hk.Enabled);`).
    - Add a new `[Fact] public void Ctor_StringConstructor_MalformedModifier_DisablesHotkey()` for input `"Garbage + S"`.

    `HotkeyManager.Load()` at line 106-114 needs no direct change — once `Hotkey.ProcessString` is non-throwing, the `Load` path through `Hotkey.UpdateKeys(string)` is automatically fixed (verify by reading `UpdateKeys` and confirming it calls `ProcessString`).

    Update `docs/ai/assessment.md` §"Status tracking" row C-08 to `done` with commit SHA placeholder.

    Commit message: `fix(C-08): Hotkey.ProcessString uses Enum.TryParse + multi-segment split; unskip Phase-1 C-08 tests`.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release --filter FullyQualifiedName~HotkeyTests</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter FullyQualifiedName~HotkeyTests` reports 5 passed, 0 skipped, 0 failed.
    - `grep -nE 'Skip = "C-08' UtinniCoreDotNet.Tests/HotkeyTests.cs` returns ZERO matches (both skips removed).
    - `grep -nE "Enum\.Parse\(typeof\(Keys\)" UtinniCoreDotNet/Hotkeys/Hotkey.cs` returns ZERO matches (all `Enum.Parse` calls in `ProcessString` replaced with `Enum.TryParse`).
    - `grep -nE "Enum\.TryParse" UtinniCoreDotNet/Hotkeys/Hotkey.cs` returns at least two matches.
    - The HotkeyTests.cs InlineData uses `Keys.Shift | Keys.Alt, Keys.Z` (not `Keys.Shift, Keys.Alt | Keys.Z`).
    - CI on master is green after this commit.
    - Commit message starts with `fix(C-08):`.
  </acceptance_criteria>
  <done>
    `Hotkey.ProcessString` is non-throwing; both Phase-1 skips are removed; the multi-modifier contract is `mods | mods + key`; HotkeyManager.Load benefits transitively; CI is green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: C-12 — VSIX widening to [16.0,18.0) + SDK + BuildTools bump to 17.x</name>
  <files>
    sdk/UtinniPluginTemplates/Vsix/source.extension.vsixmanifest,
    sdk/UtinniPluginTemplates/Vsix/Vsix.csproj,
    UtinniCoreDotNet.Tests/VsixManifestTests.cs
  </files>
  <read_first>
    sdk/UtinniPluginTemplates/Vsix/source.extension.vsixmanifest (full file — three InstallationTarget elements at lines 9-11, one Prerequisite at line 17, XML namespace at line 1-3),
    sdk/UtinniPluginTemplates/Vsix/Vsix.csproj (full file — line 4 MinimumVisualStudioVersion stays at 16.0; lines 74-75 PackageReference entries to bump),
    UtinniCoreDotNet.Tests/HotkeyTests.cs:1-68 (universal xUnit analog),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §C-12 (fix shape — four Version ranges to widen; SDK 17.0.32112.339 + VSSDK.BuildTools 17.13.2069 as candidate versions; verify at task time via NuGet),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §"CON-O-04 (VS 2019 pin rationale)" (D-12 default-fallback disposition — audit clean → widen),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-PATTERNS.md §5 row "C-12"
  </read_first>
  <behavior>
    Test 1 (RED first): `VsixManifestTests.InstallationTargets_AllUseVersionRange_16To18Exclusive` — load `sdk/UtinniPluginTemplates/Vsix/source.extension.vsixmanifest` as an `XDocument`; query all `<InstallationTarget>` elements via `XName.Get("InstallationTarget", "http://schemas.microsoft.com/developer/vsx-schema/2011")`; assert there are exactly three elements AND every `Version` attribute equals `"[16.0,18.0)"`.
    Test 2: `Prerequisite_CoreEditor_UsesVersionRange_16To18Exclusive` — query the `<Prerequisite Id="Microsoft.VisualStudio.Component.CoreEditor" ...>` element; assert its `Version` attribute equals `"[16.0,18.0)"`.
    Test 3: `Manifest_FileExistsAndIsValidXml` — guard test asserting the manifest file is readable and parses as `XDocument` without exception (defends against typos).
  </behavior>
  <action>
    Edit `sdk/UtinniPluginTemplates/Vsix/source.extension.vsixmanifest`: change all four `Version="[16.0,17.0)"` to `Version="[16.0,18.0)"` (three `<InstallationTarget>` elements at lines 9-11, one `<Prerequisite>` at line 17 with `Id="Microsoft.VisualStudio.Component.CoreEditor"`). Leave `<MinimumVisualStudioVersion>16.0</MinimumVisualStudioVersion>` in `Vsix.csproj:4` unchanged (minimum stays VS 2019). Leave `<Dependency Id="Microsoft.Framework.NDP" Version="[4.7.2,)" />` unchanged.

    Bump `Vsix.csproj` PackageReference versions per 02-RESEARCH.md §C-12: `Microsoft.VisualStudio.SDK` from `16.0.206` to `17.0.32112.339` (or the closest available 17.0.x — verify via `dotnet add package Microsoft.VisualStudio.SDK --version <X>` against a checkout); `Microsoft.VSSDK.BuildTools` from `16.8.3038` to `17.13.2069` (or closest 17.x). Keep the existing `ExcludeAssets="runtime"` attribute on `Microsoft.VisualStudio.SDK`. After version edits, run `dotnet restore sdk/UtinniPluginTemplates/Vsix/Vsix.csproj` and `msbuild sdk/UtinniPluginTemplates/Vsix/Vsix.csproj /p:Configuration=Release` to confirm the Vsix builds with the bumped packages. If the chosen `17.0.x` version doesn't exist or fails to restore, increment to the next available 17.x and re-try (record the chosen versions in the commit message).

    Create `UtinniCoreDotNet.Tests/VsixManifestTests.cs` with the three tests above. Use `System.Xml.Linq` (BCL in net472, no new package). Compute the manifest path relative to the test assembly: walk up from `AppContext.BaseDirectory` four levels (`../../../../`) then into `sdk/UtinniPluginTemplates/Vsix/source.extension.vsixmanifest`. Same path-walk approach as the C-14 test (Task 7).

    Update `docs/ai/assessment.md` §"Status tracking" row C-12 to `done` with the commit SHA placeholder. Also append a disposition note to §"Open questions" §4 per the D-12 default-fallback wording in 02-RESEARCH.md §"CON-O-04 (VS 2019 pin rationale)" — cite the commit SHA. This commit also closes CON-O-04.

    Commit message: `fix(C-12): widen VSIX InstallationTarget to [16.0,18.0) + bump Microsoft.VisualStudio.SDK and VSSDK.BuildTools to 17.x (resolves CON-O-04 per D-12)`.

    **Verified-by note (per D-06):** The C-12 task ships an automated XML/manifest assertion AND a `dotnet restore` proof that the bumped SDK packages restore cleanly. The truly-manual residual — actual install into VS 2019 IDE then VS 2022 IDE and create-from-template smoke test — is captured in the `user_setup` frontmatter and must be performed by the maintainer at human-verify time (next task).
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release --filter FullyQualifiedName~VsixManifestTests &amp;&amp; dotnet restore sdk/UtinniPluginTemplates/Vsix/Vsix.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter FullyQualifiedName~VsixManifestTests` passes all three tests.
    - `grep -cE 'Version="\[16\.0,17\.0\)"' sdk/UtinniPluginTemplates/Vsix/source.extension.vsixmanifest` returns 0.
    - `grep -cE 'Version="\[16\.0,18\.0\)"' sdk/UtinniPluginTemplates/Vsix/source.extension.vsixmanifest` returns 4 (three InstallationTarget + one Prerequisite).
    - `grep -nE 'Microsoft\.VisualStudio\.SDK.*Version="17\.' sdk/UtinniPluginTemplates/Vsix/Vsix.csproj` returns one match.
    - `grep -nE 'Microsoft\.VSSDK\.BuildTools.*Version="17\.' sdk/UtinniPluginTemplates/Vsix/Vsix.csproj` returns one match.
    - `dotnet restore sdk/UtinniPluginTemplates/Vsix/Vsix.csproj` exits 0.
    - `docs/ai/assessment.md` §"Open questions" §4 carries a "Resolved 2026-Q2" disposition referencing the commit SHA.
    - CI on master is green after this commit.
    - Commit message starts with `fix(C-12):`.
  </acceptance_criteria>
  <done>
    Manifest widened; SDK packages bumped to 17.x; manifest XML test asserts the four version ranges; `dotnet restore` confirms package availability; CON-O-04 disposition recorded.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6 (checkpoint): Maintainer installs the C-12 VSIX into VS 2019 + VS 2022 and confirms templates work</name>
  <what-built>Task 5 widened the VSIX `InstallationTarget` to `[16.0,18.0)` and bumped `Microsoft.VisualStudio.SDK` + `Microsoft.VSSDK.BuildTools` to 17.x. Automated XML assertion passes; `dotnet restore` succeeds. Per CONTEXT.md D-06, the truly-manual residual is actual install into both IDEs.</what-built>
  <how-to-verify>
    1. Build the VSIX: `msbuild sdk/UtinniPluginTemplates/Vsix/Vsix.csproj /p:Configuration=Release` — produces `sdk/UtinniPluginTemplates/Vsix/bin/Release/UtinniPluginTemplates.vsix`.
    2. **VS 2019:** double-click the `.vsix` (or run `VSIXInstaller.exe UtinniPluginTemplates.vsix`); confirm the installer accepts both VS 2019 and VS 2022 in its IDE list; install into VS 2019; open VS 2019; `File → New → Project`; search "Utinni"; create a project from BOTH the C# template AND the C++ template; confirm both wizards complete and the resulting projects build.
    3. **VS 2022:** install the same `.vsix` into VS 2022; repeat step 2 inside VS 2022.
    4. **Verified-by block:** paste the commit SHA + this verification's date into the `fix(C-12):...` commit message (use `git commit --amend` if needed — though prefer leaving it as a follow-up commit `chore(C-12): record VS 2019 + VS 2022 verification` to avoid SHA churn).

    If EITHER IDE fails (install rejected, template wizard errors, generated project doesn't build), the C-12 fix is incomplete; report the failure mode and we'll narrow the `[16.0,18.0)` range or pin a different SDK version per the D-12 default-fallback escape (last paragraph of 02-RESEARCH.md §"CON-O-04 disposition").
  </how-to-verify>
  <resume-signal>Type "approved — installed in both IDEs, both templates work" OR describe which IDE/template failed and how.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 7: C-14 — utinni.cfg blank login default + file-content regression test</name>
  <files>
    data/utinni.cfg,
    UtinniCoreDotNet.Tests/UtinniCfgTests.cs
  </files>
  <read_first>
    data/utinni.cfg (full file — current loginServerAddress0 + loginServerPort0 + ClientGame section structure),
    .planning/intel/constraints.md (CON-D-01 — blank login default),
    UtinniCoreDotNet.Tests/HotkeyTests.cs:1-68 (universal xUnit analog),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §C-14 (fix shape — blank values + comment citing CON-D-01; relative path walk-up `../../../../data/utinni.cfg` from `bin/Release/net472`),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-PATTERNS.md §5 row "C-14"
  </read_first>
  <action>
    Edit `data/utinni.cfg`: change `loginServerPort0=44453` to `loginServerPort0=` and `loginServerAddress0=login.swgemu.com` to `loginServerAddress0=` (RHS blank, keep the `=` sign and surrounding indent if any). Add an explanatory comment above the two keys: a line of the form `# Set your shard's login host and port here. Utinni intentionally ships these blank for the sovereign-fork per CON-D-01.` (use whatever comment syntax the existing cfg uses — likely `#` based on the ini-like format; verify by reading the file). Do NOT change `skipIntro=1` or other keys.

    Create `UtinniCoreDotNet.Tests/UtinniCfgTests.cs` with one test: `LoginServerAddress0_AndLoginServerPort0_AreBlank` — read `data/utinni.cfg` via `File.ReadAllText` using a path resolver that walks up from `AppContext.BaseDirectory` (`bin/Release/net472`) four levels to the repo root, then `data/utinni.cfg`. Assert via two `Assert.Matches` regex checks: (a) `@"loginServerAddress0\s*=\s*(\r?\n|$)"` (blank value followed by line break or EOF) and (b) `@"loginServerPort0\s*=\s*(\r?\n|$)"`. Optionally add a second test `Cfg_HasClientGameSection` as a guard against accidental whole-file deletion.

    Update `docs/ai/assessment.md` §"Status tracking" row C-14 to `done` with the commit SHA placeholder.

    Commit message: `fix(C-14): utinni.cfg ships blank login server address + port per CON-D-01`.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release --filter FullyQualifiedName~UtinniCfgTests</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter FullyQualifiedName~UtinniCfgTests` passes.
    - `grep -E 'loginServerAddress0\s*=\s*login' data/utinni.cfg` returns no match.
    - `grep -E 'loginServerPort0\s*=\s*[0-9]+' data/utinni.cfg` returns no match.
    - `grep -E '^\s*loginServerAddress0\s*=\s*$' data/utinni.cfg` returns one match (blank-RHS line).
    - `grep -E '^\s*loginServerPort0\s*=\s*$' data/utinni.cfg` returns one match.
    - CI on master is green after this commit.
    - Commit message starts with `fix(C-14):`.
  </acceptance_criteria>
  <done>
    Cfg ships blank login fields; file-content assertion in test project catches future regressions; CON-D-01 honored.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 8: C-13 — cross-repo TJT Debug path fix in UtinniPlugins (repo:UtinniPlugins)</name>
  <files>
    UtinniPlugins/The Jawa Toolbox/TheJawaToolbox/TheJawaToolbox.vcxproj,
    UtinniPlugins/The Jawa Toolbox/TheJawaToolbox.sln
  </files>
  <read_first>
    UtinniPlugins/The Jawa Toolbox/TheJawaToolbox/TheJawaToolbox.vcxproj (full file or at minimum lines 60-75 — Debug OutDir + Release OutDir + RelWithDbgInfo OutDir all visible for comparison),
    UtinniPlugins/The Jawa Toolbox/TheJawaToolbox.sln (full file or at minimum lines 20-32 — Debug|x86 + Release|x86 + RelWithDbgInfo|x86 ActiveCfg/Build.0 entries for the TJT project GUID),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §C-13 (fix shape — vcxproj line 63 changes `..\..\..\..\` → `..\..\..\`; sln inserts `Debug|x86.Build.0 = Debug|Win32` line),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-CONTEXT.md §D-09 (cross-repo direct commit; no CI workflow added to UtinniPlugins in Phase 2),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-PATTERNS.md §5 row "C-13"
  </read_first>
  <action>
    Work in the sister repo `D:\Code\UtinniPlugins` (NOT in this Utinni repo). Steps:

    1. Confirm working tree at `D:\Code\UtinniPlugins` is clean: `git -C "D:\Code\UtinniPlugins" status` — abort with a checkpoint if dirty.
    2. Edit `D:\Code\UtinniPlugins\The Jawa Toolbox\TheJawaToolbox\TheJawaToolbox.vcxproj` line 63: change `<OutDir>..\..\..\..\Utinni\bin\Debug\Plugins\TheJawaToolbox\</OutDir>` to `<OutDir>..\..\..\Utinni\bin\Debug\Plugins\TheJawaToolbox\</OutDir>` (drop one `..\` level so it matches the Release line at 67 and RelWithDbgInfo line at 71).
    3. Edit `D:\Code\UtinniPlugins\The Jawa Toolbox\TheJawaToolbox.sln` to insert a missing `Debug|x86.Build.0 = Debug|Win32` line between line 26 (`{EA1E6CED-...}.Debug|x86.ActiveCfg = Debug|Win32`) and the existing Release block. The inserted line has the exact same `{EA1E6CED-6315-486A-8AA7-D41DF9D34888}` GUID prefix as the ActiveCfg line above it.
    4. Commit in the UtinniPlugins repo with message: `fix(C-13): TJT Debug path uses ..\..\..\ instead of ..\..\..\..\` (per CONTEXT.md §specifics). Commit body cites this phase: `Phase 2 critical-bug burn-down — closes assessment.md C-13. See planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §C-13.`
    5. Push the commit to `kennethlong/UtinniPlugins` master (or open a PR if branch protection requires — the user owns the repo so direct push is acceptable per D-09).
    6. Capture the commit SHA in the sister-repo PR URL or commit URL.
    7. Back in this Utinni repo, update `docs/ai/assessment.md` §"Status tracking" row C-13 to `done` with the sister-repo commit SHA / PR URL (this is the ONLY in-repo change for the C-13 task).
    8. Commit the in-repo status-tracking update with: `docs(C-13): record TJT Debug path fix in UtinniPlugins (cross-repo)` and reference the sister-repo commit SHA.

    Note: there is NO automated regression test in this repo for C-13 (per D-09 / D-06). The verification is the human-verify Task 9 below.
  </action>
  <verify>
    <automated>git -C "D:/Code/UtinniPlugins" log --oneline -1 | findstr "fix(C-13)" &amp;&amp; git -C "D:/Code/Utinni" log --oneline -1 | findstr "docs(C-13)"</automated>
  </verify>
  <acceptance_criteria>
    - `git -C "D:/Code/UtinniPlugins" log --oneline | head -3` shows a commit titled exactly `fix(C-13): TJT Debug path uses ..\..\..\ instead of ..\..\..\..\` on master HEAD or one commit back.
    - `grep -cE 'OutDir>\.\.\\\.\.\\\.\.\\\.\.\\Utinni' "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolbox/TheJawaToolbox.vcxproj"` returns 0 (four-..\ path removed).
    - `grep -cE 'OutDir>\.\.\\\.\.\\\.\.\\Utinni\\bin\\Debug\\Plugins\\TheJawaToolbox' "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolbox/TheJawaToolbox.vcxproj"` returns 1 (correct three-..\ path present).
    - `grep -E '\{EA1E6CED-6315-486A-8AA7-D41DF9D34888\}\.Debug\|x86\.Build\.0 = Debug\|Win32' "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolbox.sln"` returns one match (Build.0 entry inserted).
    - `docs/ai/assessment.md` §"Status tracking" C-13 row links to the sister-repo commit.
    - CI on this Utinni repo's master stays green (no code in this repo changed except the docs row).
    - Both commit messages start with their expected prefixes (`fix(C-13):` in UtinniPlugins, `docs(C-13):` in Utinni).
  </acceptance_criteria>
  <done>
    Cross-repo fix committed and pushed to UtinniPlugins; in-repo status-tracking row updated; awaiting Task 9 manual TJT Debug build verification.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 9 (checkpoint): Maintainer builds TJT Debug locally and confirms output lands in Utinni/bin/Debug/Plugins/TheJawaToolbox/</name>
  <what-built>Task 8 fixed `TheJawaToolbox.vcxproj` `OutDir` (one too many `..\`) and inserted the missing `Debug|x86.Build.0 = Debug|Win32` line in `TheJawaToolbox.sln`. Both changes are now on `kennethlong/UtinniPlugins` master. Per CONTEXT.md D-06, the cross-repo build is manual-only because UtinniPlugins has no CI workflow this phase.</what-built>
  <how-to-verify>
    1. `cd D:\Code\UtinniPlugins` (or wherever the sister-repo clone lives).
    2. `git pull origin master` to land Task 8's commit.
    3. `msbuild "The Jawa Toolbox\TheJawaToolbox.sln" /p:Configuration=Debug /p:Platform=x86 /restore` — must complete without errors AND must invoke the linker for the TJT project (NOT silently skip the Build step).
    4. After the build, `Test-Path "D:\Code\Utinni\bin\Debug\Plugins\TheJawaToolbox\TheJawaToolbox.dll"` (or equivalent for the actual produced artifact name) — expect `True`.
    5. Confirm the path is exactly `Utinni/bin/Debug/Plugins/TheJawaToolbox/` and NOT one level up (`D:/bin/Debug/Plugins/TheJawaToolbox/`) — that's the bug we fixed.
  </how-to-verify>
  <resume-signal>Type "approved — TJT Debug build lands at Utinni/bin/Debug/Plugins/TheJawaToolbox/" OR describe the failure mode (build error, wrong output path, missing Build.0 entry, etc.).</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 10: Status tracking sweep — flip assessment.md C-04/C-06/C-08/C-12/C-13/C-14 rows + record CON-O-02 / CON-O-04 dispositions</name>
  <files>
    docs/ai/assessment.md
  </files>
  <read_first>
    docs/ai/assessment.md §"Status tracking" (full table — find the C-04, C-06, C-08, C-12, C-13, C-14 rows and the existing format),
    docs/ai/assessment.md §"Open questions" §2 + §4 (current text for CON-O-02 + CON-O-04 — required to phrase the dispositions),
    .planning/phases/02-critical-bug-burn-down-c-01-c-15/02-RESEARCH.md §"CON-O-01 / -02 / -04 archaeology findings" (exact disposition wording for §2 and §4),
    git log --oneline (capture the actual SHAs from Tasks 2, 3, 4, 5, 8 commits in this plan)
  </read_first>
  <action>
    For each of C-04, C-06, C-08, C-12, C-14: locate the row in `docs/ai/assessment.md §"Status tracking"`; change the `Status` column from whatever it currently says (likely `open` or `pending`) to `done`; append the commit SHA + short prefix to the row (e.g., `done — fix(C-04): ${SHA}`). Use the actual SHAs from `git log --oneline` for the commits made in Tasks 2-7 above. For C-13, link to the sister-repo commit (the Task 8 commit) — the in-repo Task 8 already wrote a placeholder; verify it has the real SHA now.

    Append/update §"Open questions" §2 ("Was `AddPostDrawLoopCall` ever actually used?") with the verbatim disposition text from 02-RESEARCH.md §"CON-O-02 (was `AddPostDrawLoopCall` ever used?)" — substitute the actual commit SHA from Task 2 for the `<SHA>` placeholder.

    Append/update §"Open questions" §4 ("VS 2019 pin rationale") with the verbatim disposition text from 02-RESEARCH.md §"CON-O-04 (VS 2019 pin rationale)" — substitute the actual commit SHA from Task 5 for the `<SHA>` placeholder.

    Commit message: `docs(02-01): mark C-04/C-06/C-08/C-12/C-13/C-14 done in assessment.md; record CON-O-02 + CON-O-04 dispositions`.
  </action>
  <verify>
    <automated>git diff HEAD~1 docs/ai/assessment.md | findstr /R "C-04\|C-06\|C-08\|C-12\|C-13\|C-14\|CON-O-02\|CON-O-04"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^\| C-04 .*\|.*done" docs/ai/assessment.md` returns one match (status flipped).
    - Similar matches for C-06, C-08, C-12, C-13, C-14 — all on `done`.
    - `grep -A2 "## 2\. Was \`AddPostDrawLoopCall\`" docs/ai/assessment.md` (or equivalent for whatever the actual heading reads) contains the string "Resolved 2026" or "Disposition:" with a commit SHA.
    - Similar for `## 4. VS 2019 pin rationale`.
    - Each commit-SHA referenced in the table exists in `git log` for the matching task (use `git rev-parse --verify <SHA>` to confirm).
    - CI on master is green after this commit (the change is docs-only; CI should not break).
    - Commit message starts with `docs(02-01):`.
  </acceptance_criteria>
  <done>
    Status-tracking table reflects the six closed bugs in this plan; CON-O-02 and CON-O-04 carry their D-12 dispositions; the plan is ready for verification and Plan 02-02 may begin.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MEF plugin discovery (`DirectoryCatalog`) | Untrusted DLLs in the plugins directory can run code via the `[InheritedExport(typeof(IPlugin))]` discovery + `CompositionContainer.ComposeParts` |
| `input.ini` parser (Hotkey load path) | User-supplied string content from `input.ini` flows through `Hotkey.UpdateKeys(string)` → `Hotkey.ProcessString` |
| `data/utinni.cfg` | User-editable config file consumed by the editor at startup (CON-D-01) |
| `source.extension.vsixmanifest` | Build-time artifact; not a runtime trust boundary, but a typo bricks the install-IDE matrix |
| `UtinniPlugins` sister repo (cross-repo) | Separate repo, no CI; commit + push is direct |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Plugin DLL in `Plugins/` directory | accept | C-06's per-plugin try/catch ISOLATES bad plugins from tearing down the editor; it does NOT prevent a malicious plugin from running its own code once loaded. Plugin signing is SEC-04 / MCF-04 (V2 scope per CONTEXT.md). Accept residual risk: the user explicitly installed the plugin. |
| T-02-02 | Denial-of-Service | Malformed hotkey in `input.ini` | mitigate | C-08 fix replaces `Enum.Parse` (throws on bad input) with `Enum.TryParse` + warn-and-disable; a malformed entry no longer aborts the hotkey load loop or crashes the editor at startup. |
| T-02-03 | Spoofing | `data/utinni.cfg` defaulting to `login.swgemu.com` could mislead a sovereign-fork user into connecting to SWGEmu | mitigate | C-14 blanks both fields per CON-D-01; user must explicitly enter their shard's host + port. |
| T-02-04 | Tampering | Bumped NuGet packages (`Microsoft.VisualStudio.SDK` + `Microsoft.VSSDK.BuildTools` 17.x) | mitigate | Both packages are first-party Microsoft (`microsoft` on NuGet); 02-RESEARCH.md §"Package Legitimacy Audit" approves the bump. No slopcheck gate needed. The bump only affects Vsix.csproj — VS-template-authoring tooling, not the runtime. |
| T-02-05 | Information Disclosure | `Log.AddOutputSinkCallback`-captured plugin-load errors might leak file paths in logs | accept | Log captures plugin directory names and exception messages — these are local file paths, low-sensitivity. The plugin author and Utinni maintainer share the local machine. Accept. |
| T-02-06 | Repudiation | C-13 cross-repo direct commit (no PR review) | accept | UtinniPlugins is a sovereign fork owned by the same user (`kennethlong/UtinniPlugins`); D-09 explicitly authorizes direct commit. Repudiation risk is low — author == reviewer == owner. |
| T-02-SC | Tampering | npm/pip/cargo installs in this plan | accept | NO new package-manager installs in this plan. The C-12 Vsix bump is a `<PackageReference>` version-only change, restored via existing `dotnet restore`. Package Legitimacy Audit in 02-RESEARCH.md §"Package Legitimacy Audit" approves Microsoft-published 17.x packages (Microsoft.VisualStudio.SDK + Microsoft.VSSDK.BuildTools); both are first-party, decade-old, high-download — no slopcheck gate required per the audit. T-{phase}-SC retained for register completeness. |
</threat_model>

<verification>
## Phase-level verification at plan close

1. **All six bug fix tasks committed + CI green after each.** Each `fix(C-NN):` commit lands on master with the gated CI workflow passing per D-01.
2. **New test files exist and pass:** `GroundSceneCallbacksTests.cs` (C-04), `PluginLoaderTests.cs` (C-06), `VsixManifestTests.cs` (C-12), `UtinniCfgTests.cs` (C-14), updated `HotkeyTests.cs` (C-08). Run `dotnet test UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj --no-build --configuration Release` — expect at least 11 tests passing (4 prior HotkeyTests now unskipped + 1 new + 3 GroundSceneCallbacks + 3 PluginLoader + 3 VsixManifest + 1+ UtinniCfgTests = 15+ tests).
3. **Cross-repo commit verified:** `git -C "D:/Code/UtinniPlugins" log --oneline -3` shows the `fix(C-13):` commit on master; in-repo `docs/ai/assessment.md` C-13 row links to it.
4. **Two human-verify checkpoints completed:** Task 6 (VS 2019 + VS 2022 IDE install) and Task 9 (local TJT Debug build verification).
5. **Status tracking sweep done:** `docs/ai/assessment.md` rows for C-04, C-06, C-08, C-12, C-13, C-14 are `done`; CON-O-02 and CON-O-04 carry D-12 dispositions.
6. **No CON-N-* / CON-M-* / CON-T-* preservation surface touched** (per STAB-04 cross-cutting). Verify by inspection: the Drain refactor preserves `ConcurrentQueue<Action>` semantics; PluginLoader still uses `DirectoryCatalog`; Hotkey type contract unchanged; the VSIX is build-time tooling; utinni.cfg is data; C-13 is cross-repo.
</verification>

<success_criteria>
- 6 fix commits in this Utinni repo (C-04, C-06, C-08, C-12, C-14 + the in-repo C-13 docs commit), 1 scaffolding commit (Task 1), 1 docs sweep commit (Task 10), plus 1 cross-repo fix commit in UtinniPlugins for C-13.
- All 11+ new test cases pass; both Phase-1 C-08 skips are removed.
- Both human-verify checkpoints (Tasks 6 + 9) approved.
- CI on master is green at the close of this plan; Plan 02-02 is unblocked.
- assessment.md status table reflects six closed bugs; CON-O-02 + CON-O-04 carry dispositions.
</success_criteria>

<output>
Create `.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-01-SUMMARY.md` documenting all 8 commits (6 fixes + 1 scaffolding + 1 docs sweep, plus the sister-repo SHA), the test counts before/after, the two human-verify approvals, and any deviations from this plan.
</output>
</content>
</invoke>