---
phase: 17-cppsharp-v145-hardening
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - .github/workflows/ci.yml
  - tools/allowed-cpp-stl-headers.txt
autonomous: true
requirements: [CPPS-03]
must_haves:
  truths:
    - "CI HARD-FAILS (throws) if UtinniCore C++ adds an #include of a C++23 STL header the 14.29 redirect cannot parse, scoped to UtinniCore/ #include lines only"
    - "CI WARNS LOUD (never blocks) when the observed latest CppSharp release differs from a committed clang-19 pin — a clang>=20 release is good news (native v145 reachable), not a regression"
    - "Both new steps fit the self-hosted, push-only, PowerShell-5.1, verify-only CI model with no pull_request-from-fork trigger added"
  artifacts:
    - path: "tools/allowed-cpp-stl-headers.txt"
      provides: "CPPS-03a allowlist/denylist the C++23-header scan reads (kept out of the scanned root)"
      min_lines: 5
    - path: ".github/workflows/ci.yml"
      provides: "CPPS-03a C++23-header HARD-FAIL scan + CPPS-03b clang-20 WARN-loud pin tripwire, slotted beside the redirect-verify steps"
      contains: "::warning::"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "tools/allowed-cpp-stl-headers.txt"
      via: "the C++23 scan step reads the allowlist file"
      pattern: "allowed-cpp-stl-headers"
    - from: ".github/workflows/ci.yml"
      to: "UtinniCore/ #include lines"
      via: "scoped Get-ChildItem scan excluding external/, Generated, docs"
      pattern: "UtinniCore"
---

<objective>
Add the two CPPS-03 fail-fast CI tripwires beside the existing redirect-verify steps: (a) a HARD-FAIL scan that throws if UtinniCore C++ adopts a C++23 STL header the 14.29 redirect cannot parse, and (b) a WARN-loud clang-20 pin tripwire that fires as good-news when a CppSharp release newer than the committed clang-19 pin appears. The severity split is asymmetric and locked by D-04: the header scan blocks master; the clang-20 pin never blocks master.

Purpose: Guard the two unblock/regression signals cheaply, without egress dependency or fork-PR RCE exposure.
Output: One new allowlist file + two new verify-only ci.yml steps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-cppsharp-v145-hardening/17-CONTEXT.md
@.planning/phases/17-cppsharp-v145-hardening/17-RESEARCH.md
@.planning/phases/17-cppsharp-v145-hardening/17-PATTERNS.md

# Source-of-truth (the model the new steps must fit):
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: CPPS-03a — C++23-STL-header HARD-FAIL scan step + allowlist</name>
  <files>tools/allowed-cpp-stl-headers.txt, .github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/17-cppsharp-v145-hardening/17-PATTERNS.md (§"ci.yml — two new verify-only steps" → CPPS-03a — exact include/exclude idiom + throw-on-fail mirror lines)
    - .github/workflows/ci.yml (lines 44-68 clang-format style gate — the Get-ChildItem -Recurse / -notmatch $excludePattern idiom; lines 141-185 the throw-on-fail verify-step shape; lines 7-15 the locked no-pull_request invariant)
    - .planning/phases/17-cppsharp-v145-hardening/17-RESEARCH.md (Pitfall 4 grep-gate self-trip, Pitfall 5 the 14.29-unparseable-header risk, §Security Domain T-V5 path injection)
    - .planning/phases/17-cppsharp-v145-hardening/17-CONTEXT.md (D-04 asymmetric severity; constraint 6 scope-to-UtinniCore/-#include-lines-only)
  </read_first>
  <action>
    Create tools/allowed-cpp-stl-headers.txt — a small allowlist/denylist that the scan reads. CRITICAL grep-gate hygiene (Pitfall 4): this file lives under tools/, which is OUTSIDE the scanned root (the scan is scoped to UtinniCore/ #include lines only), so naming C++23 header tokens here does NOT self-trip; but do NOT place any C++23 header token in a file under UtinniCore/. Add a new ci.yml step titled for the C++23 STL header scan, placed after the VS2019 14.29 verify step (after ci.yml:185, before "Setup MSBuild"). The step: enumerates #include lines in *.cpp/*.h under UtinniCore/ ONLY (mirror the clang-format step's Get-ChildItem -Recurse + a $excludePattern that excludes external/, Generated, bin/obj/Release/Debug, .vs, vcpkg_installed); matches each included STL header against the denylist of 14.29-unparseable C++23 headers (sourced from allowed-cpp-stl-headers.txt); and throws (HARD-FAIL, non-zero exit) if any disallowed header is found — mirroring the style-gate throw at ci.yml:65-67. Quote/escape all file paths (T-V5 path-injection mitigation). The current scan baseline is clean (RESEARCH Pitfall 5: zero C++23-risky headers in UtinniCore/ today) so the step must PASS on the current tree. Do NOT add a pull_request trigger. RED-PATH PROOF (mirror the negative-[Fact] discipline 17-03 Task 1 applies to the ABI diff — guard against a dead gate from a denylist regex typo): drop a THROWAWAY temp header under a scanned path (e.g. UtinniCore/.abi-scan-selftest.tmp.h) containing an #include of one denylisted C++23 STL header, run the scan logic, ASSERT it flags/throws on that file, then DELETE the temp file (do NOT commit it; it must never reach git). The committed scan must end green on the real tree.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -Command "$deny=Get-Content tools/allowed-cpp-stl-headers.txt; $hits=Get-ChildItem -Path UtinniCore -Recurse -Include *.cpp,*.h -File | Where-Object { $_.FullName -notmatch '\\(external|bin|obj|Release|Debug|x86|x64|\.vs|vcpkg_installed|Generated)\\' } | Select-String -Pattern '^\s*#include'; Write-Host 'scan executed OK'"</automated>
  </verify>
  <acceptance_criteria>
    - `tools/allowed-cpp-stl-headers.txt` exists with at least 5 lines (denylist of 14.29-unparseable C++23 STL headers + a comment header)
    - `.github/workflows/ci.yml` contains a step that throws on a disallowed C++23 STL header (HARD-FAIL); the scan is scoped to UtinniCore/ and excludes external/, Generated, and build-output dirs
    - The scan step PASSES on the current tree (no C++23-risky header exists in UtinniCore/ today)
    - A RED-PATH self-test proves the scan THROWS when a temp file under a scanned path includes a denylisted C++23 STL header (guards against a denylist-regex typo producing a dead gate); the temp file is deleted and never committed
    - No C++23 STL header token appears anywhere under UtinniCore/ (no self-trip introduced) — verified by the scan passing
    - The scan step is placed after the 14.29-verify step and before "Setup MSBuild"; no `pull_request:` key added to the `on:` block
  </acceptance_criteria>
  <done>CI hard-fails the build if UtinniCore C++ adopts a 14.29-unparseable C++23 STL header; the step is green on today's clean tree.</done>
</task>

<task type="auto">
  <name>Task 2: CPPS-03b — clang-20 CppSharp release WARN-loud pin tripwire</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/17-cppsharp-v145-hardening/17-PATTERNS.md (§"ci.yml" → CPPS-03b — copy the 14.29-verify step structure, assert a committed pin, ::warning:: not throw)
    - .github/workflows/ci.yml (lines 166-185 the 14.29-verify step to mirror; line 132 the existing `::warning::` emit idiom)
    - .planning/phases/17-cppsharp-v145-hardening/17-CONTEXT.md (D-03 committed pin not live probe; D-04 WARN-loud never block; A1 runner-egress note)
    - .planning/phases/17-cppsharp-v145-hardening/17-RESEARCH.md (§Security Domain — clang-20 probe must not trust a spoofable registry response; §Open Questions #1)
  </read_first>
  <action>
    Add a new ci.yml step for the CppSharp clang-20 release tripwire, immediately after the CPPS-03a step. Per D-03, assert a COMMITTED "last-known-latest" pin (CppSharp v1.2 / clang 19) declared inline in the step — NOT a live network probe (no self-hosted egress dependency, deterministic, unspoofable). The step asserts the committed pin against a committed expected value and, in the unblock path, emits Write-Host "::warning::..." (mirror the idiom at ci.yml:132) signaling that a CppSharp release newer than the clang-19 baseline would mean native-v145 just became reachable and the 14.29 redirect can be reviewed for retirement. It must NEVER throw — a clang>=20 release is an unblock/good-news signal and must not turn master red (D-04). Document inline that refreshing the pin is an out-of-band manual/scheduled job, not this step's job (D-03). Do NOT add network egress or a pull_request trigger.
  </action>
  <verify>
    <automated>grep -q "::warning::" .github/workflows/ci.yml && grep -iq "clang" .github/workflows/ci.yml && powershell -ExecutionPolicy Bypass -Command "$y=Get-Content .github/workflows/ci.yml -Raw; if ($y -match 'pull_request') { throw 'pull_request trigger present' }; Write-Host 'no pull_request trigger OK'"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains a clang-20 tripwire step that emits `::warning::` and NEVER calls `throw` (WARN-loud, never block per D-04)
    - The step asserts a COMMITTED pin (CppSharp v1.2 / clang 19) inline — no live network probe, no NuGet/GitHub-API call, no new egress
    - An inline comment states the pin is refreshed out-of-band by a separate manual/scheduled job (D-03)
    - `grep -c "pull_request" .github/workflows/ci.yml` returns 0 (locked no-fork-PR invariant preserved)
    - The step is placed beside the redirect-verify / CPPS-03a steps (before "Setup MSBuild")
  </acceptance_criteria>
  <done>CI emits a loud WARN — never a failure — when a clang-20-bearing CppSharp release would unblock retiring the redirect; the pin is committed, not probed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI scan → UtinniCore/ source files | Untrusted-shape file paths flow into the scan; must be quoted/scoped to a fixed root |
| CI clang-20 tripwire → version pin | The "latest CppSharp" signal is a committed pin, NOT a live (spoofable) registry response |
| public repo → self-hosted v145 runner | Only `master` pushes reach the runner; fork PRs must never run (RCE) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 (V14) | Elevation / RCE | Untrusted PR code reaching the self-hosted v145 runner | mitigate | Keep push-only trigger; both new steps add NO `pull_request` key (LOCKED invariant ci.yml:10-15) |
| T-17-05 (V5) | Tampering | Header-scan command injection via crafted file paths | mitigate | Quote/escape all paths; scope the scan to a fixed root (UtinniCore/) with an explicit exclude set |
| T-17-06 (V14) | Spoofing / Tampering | clang-20 tripwire trusting a spoofed/poisoned registry response | mitigate | Assert a committed pin (D-03) instead of a live network probe; no egress introduced |
</threat_model>

<verification>
- The C++23-header scan step exists, is scoped to UtinniCore/ #include lines, throws on a disallowed header, and PASSES on the current clean tree.
- The clang-20 tripwire step exists, asserts a committed pin, emits `::warning::`, and never throws.
- `grep -c "pull_request" .github/workflows/ci.yml` returns 0.
- No C++23 STL header token introduced anywhere under UtinniCore/ (no grep-gate self-trip).
</verification>

<success_criteria>
- CPPS-03a: CI fails fast (HARD-FAIL) on a newly-adopted 14.29-unparseable C++23 STL header in UtinniCore/.
- CPPS-03b: CI warns loud (never blocks) when a CppSharp release newer than the committed clang-19 pin appears.
- Both steps fit the self-hosted, push-only, PowerShell-5.1, verify-only model; no new dependency, no fork-PR trigger.
</success_criteria>

<output>
Create `.planning/phases/17-cppsharp-v145-hardening/17-02-SUMMARY.md` when done.
</output>
