---
phase: 17-cppsharp-v145-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/cppsharp-clang-capability-spike.ps1
  - docs/ai/cppsharp-v145-redirect.md
  - docs/ai/regen-bindings.md
  - UtinniCoreDotNetGen/Program.cs
autonomous: true
requirements: [CPPS-01, CPPS-02]
must_haves:
  truths:
    - "The repo contains a runnable spike script that tabulates each installed MSVC yvals_core.h clang gate vs CppSharp's bundled clang and concludes no released CppSharp parses the v145 STL"
    - "A discoverable in-repo doc records the spike negative result and names the VS2019-14.29 parser-include redirect as the SUPPORTED binding-generation config"
    - "regen-bindings.md no longer claims the generated file is ~5000 lines and no longer references a CppSharp version line in the banner"
    - "Program.cs::ConfigureCppSharpParserStl carries a self-describing header comment pointing at the supported-config doc, with its resolver logic unchanged"
  artifacts:
    - path: "tools/cppsharp-clang-capability-spike.ps1"
      provides: "CPPS-01 spike: per-MSVC yvals_core.h __clang_major__ tabulation + mechanical negative-result conclusion"
      contains: "__clang_major__"
    - path: "docs/ai/cppsharp-v145-redirect.md"
      provides: "CPPS-02 supported-config doc: spike result, why the 14.29 redirect is load-bearing, ABI-stability assumption, prerequisites, env overrides"
      min_lines: 40
    - path: "docs/ai/regen-bindings.md"
      provides: "Updated regen procedure: corrected line count, version-line claim removed, per-block-hash gate + --rebless procedure referenced"
  key_links:
    - from: "docs/ai/regen-bindings.md"
      to: "docs/ai/cppsharp-v145-redirect.md"
      via: "markdown link to the supported-config doc"
      pattern: "cppsharp-v145-redirect"
    - from: "UtinniCoreDotNetGen/Program.cs"
      to: "docs/ai/cppsharp-v145-redirect.md"
      via: "header-comment doc pointer above ConfigureCppSharpParserStl"
      pattern: "cppsharp-v145-redirect"
---

<objective>
Run and document the CPPS-01 clang-capability spike, then lift the parser-include redirect's rationale out of source comments into a discoverable in-repo doc (CPPS-02). The spike's documented negative result — no released CppSharp parses the MSVC v145 (14.5x) STL — is what formally re-sets the phase's acceptance from "retire the redirect" to "harden the redirect." This plan is Wave 1 and gates the two Wave-2 plans (the tripwires and the ABI gate both depend on the redirect being the accepted, documented config).

Purpose: Stop the binding-generation config being silently load-bearing; produce the empirical record that justifies the entire phase scope.
Output: A committed spike script + a new supported-config doc + an updated (de-staled) regen-bindings.md + a self-describing header comment in Program.cs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-cppsharp-v145-hardening/17-CONTEXT.md
@.planning/phases/17-cppsharp-v145-hardening/17-RESEARCH.md
@.planning/phases/17-cppsharp-v145-hardening/17-PATTERNS.md

# Source-of-truth (read, do not rewrite the resolver logic):
@UtinniCoreDotNetGen/Program.cs
@docs/ai/regen-bindings.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CPPS-01 clang-capability spike script + documented negative result</name>
  <files>tools/cppsharp-clang-capability-spike.ps1</files>
  <read_first>
    - .planning/phases/17-cppsharp-v145-hardening/17-PATTERNS.md (§"CPPS-01 spike script" — mechanism, evidence lines, analog ci.yml verify steps 141-185)
    - .planning/phases/17-cppsharp-v145-hardening/17-RESEARCH.md (§"Reading the v145 STL clang gate", Pitfall 1, Code Examples)
    - .github/workflows/ci.yml (lines 141-185 — the vswhere-resolved PowerShell-5.1 verify-step idiom to mirror)
    - .planning/research/cppsharp-msvc-14.5-upgrade.md (the yvals_core.h gate evidence per MSVC version)
  </read_first>
  <action>
    Create a PowerShell-5.1-safe spike script under tools/ that: (1) enumerates installed MSVC toolsets via vswhere (-products * -find "VC\Tools\MSVC\*") plus the VS2019 BuildTools candidate paths used in ci.yml's 14.29-verify step; (2) for each located yvals_core.h, extracts the `__clang_major__ < N` gate value; (3) tabulates each MSVC version's required-clang N against CppSharp's bundled clang (11 vendored, 19 latest released v1.2); (4) prints the mechanical conclusion that 11 < 20 and 19 < 20, therefore no released CppSharp parses the v145 (14.5x) STL → harden-the-redirect. The script is a reporting/tabulation tool: it must NOT throw and must NOT modify anything. Mirror the env-var/vswhere/default-probe resolver discipline from Program.cs — never hard-code an install path (per D-01 reuse-don't-regress). Expected evidence per 17-RESEARCH: v145 14.51/14.52 gate is `< 20`, VS2022 14.44 is `< 19`, VS2019 14.29 is `< 11` (the redirect target). Keep the literal C++23 header example tokens OUT of this script's comments (grep-gate self-trip hazard — Pitfall 4); refer to them only as "C++23 STL headers" in prose.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File tools/cppsharp-clang-capability-spike.ps1</automated>
  </verify>
  <acceptance_criteria>
    - `tools/cppsharp-clang-capability-spike.ps1` exists and runs to completion with exit code 0 (it never throws — it is a report)
    - Script output contains a per-MSVC table with the `__clang_major__` gate value for at least the v145 (14.5x) toolset and the VS2019 14.29 redirect target
    - Script output contains an explicit conclusion line stating no released CppSharp (clang 11 vendored / clang 19 latest) parses the v145 STL and that the supported config is the 14.29 redirect
    - `grep -n "__clang_major__" tools/cppsharp-clang-capability-spike.ps1` returns at least one match
    - The script contains NO hard-coded MSVC install path (resolution is via vswhere / candidate-probe), and NO literal C++23 STL header token in comments
  </acceptance_criteria>
  <done>The spike script is committed, runs green, and prints the documented negative result that re-sets phase acceptance to harden-the-redirect.</done>
</task>

<task type="auto">
  <name>Task 2: CPPS-02 supported-config doc + de-stale regen-bindings.md + Program.cs doc pointer</name>
  <files>docs/ai/cppsharp-v145-redirect.md, docs/ai/regen-bindings.md, UtinniCoreDotNetGen/Program.cs</files>
  <read_first>
    - .planning/phases/17-cppsharp-v145-hardening/17-PATTERNS.md (§"docs/ai/regen-bindings.md (UPDATE)" and §"supported-config doc / section" — the exact stale facts + content to lift)
    - docs/ai/regen-bindings.md (the file being de-staled — locate the "~5000+ lines" claim and the "CppSharp version line" references)
    - UtinniCoreDotNetGen/Program.cs (the ConfigureCppSharpParserStl region + resolvers — the rationale to lift into prose; header-comment touch ONLY)
    - .planning/phases/17-cppsharp-v145-hardening/17-RESEARCH.md (§"State of the Art" deprecated/outdated note; Pattern 1 ABI-safety rationale; Assumptions A3/A4; Runtime State Inventory re-bless procedure)
  </read_first>
  <action>
    Create docs/ai/cppsharp-v145-redirect.md as the CPPS-02 supported-config doc: record the Task-1 spike result; explain why the VS2019-14.29 parser-include redirect is load-bearing (clang 11 parses the 14.29 STL cleanly while the build uses v145); state the ABI-stability assumption (`_MSVC_STL_VERSION = 143` stable across 14.29 → 14.52, per RESEARCH Assumption A3, so 14.29-parsed bindings stay layout-correct for v145 builds); list the documented prerequisites (VS2019 BuildTools MSVC 14.29.30133, VS2026 v145 14.51/14.52, Win10 SDK 19041) and the `UTINNI_VS2019_ROOT` / `UTINNI_SLN_DIR` env overrides the resolver reads. Update docs/ai/regen-bindings.md: correct the stale "~5000+ lines" to the actual 27,659; remove the "CppSharp version line" claim (the auto-generated banner has NO version line — the ABI diff keys off structural surface, not a version string); add a markdown link to cppsharp-v145-redirect.md; add the re-bless procedure stub per D-01 (rebuild TJT in lockstep, regen the baseline block-hash via --rebless, rebuild+re-freeze the plugin fixture, commit all artifacts together) — point forward to Plan 03 which builds the --rebless tool. In Program.cs, add ONLY a self-describing header comment above ConfigureCppSharpParserStl pointing at docs/ai/cppsharp-v145-redirect.md; do NOT touch the resolver logic. Keep example C++23 STL header tokens out of any file the CPPS-03a scan will read (these docs are under docs/, which is excluded — but keep the hygiene anyway).
  </action>
  <verify>
    <automated>grep -L "5000" docs/ai/regen-bindings.md && grep -q "cppsharp-v145-redirect" docs/ai/regen-bindings.md && test -f docs/ai/cppsharp-v145-redirect.md && grep -q "cppsharp-v145-redirect" UtinniCoreDotNetGen/Program.cs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/ai/cppsharp-v145-redirect.md` exists, is at least 40 lines, and names the 14.29 redirect as the supported config + states the `_MSVC_STL_VERSION = 143` ABI-stability assumption + lists VS2019 14.29 / v145 / Win10 SDK 19041 prerequisites + the `UTINNI_VS2019_ROOT` env override
    - `grep -c "5000" docs/ai/regen-bindings.md` returns 0 (stale line-count claim removed)
    - `grep "cppsharp-v145-redirect" docs/ai/regen-bindings.md` returns at least one match (link added)
    - regen-bindings.md no longer references a "CppSharp version line" in the generated banner and describes the per-block-hash gate + --rebless re-bless procedure
    - `grep "cppsharp-v145-redirect" UtinniCoreDotNetGen/Program.cs` returns at least one match (doc-pointer comment added)
    - `git diff UtinniCoreDotNetGen/Program.cs` shows ONLY added comment lines — no change to AddSystemIncludeDirs / vswhere / resolver logic
  </acceptance_criteria>
  <done>The redirect is documented as the supported config in a discoverable doc, regen-bindings.md is de-staled and cross-links it, and Program.cs points at it without any resolver-logic change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike script → MSVC install dirs | Script reads yvals_core.h from machine-local toolchain paths; read-only, no execution of toolchain binaries |
| docs → repo | Documentation is committed trusted content; review-on-change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 (V1) | Information / Architecture | Redirect silently load-bearing in Program.cs comments only | mitigate | CPPS-02 lifts the rationale into a discoverable in-repo doc; the config stops being a hidden SPOF |
| T-17-02 (V5) | Tampering | Spike reading yvals_core.h via resolved paths | mitigate | Resolve via vswhere/candidate-probe (mirror Program.cs); never accept an unvalidated external path; read-only, never executes toolchain binaries |
| T-17-03 | Tampering | Grep-gate self-trip from C++23 header example tokens in committed text | mitigate | Keep literal C++23 STL header tokens out of the spike script and docs that any scan reads (Pitfall 4 / grep-gate hygiene) |
</threat_model>

<verification>
- `tools/cppsharp-clang-capability-spike.ps1` runs to completion (exit 0) and prints the per-MSVC clang-gate table + the harden-the-redirect conclusion.
- `docs/ai/cppsharp-v145-redirect.md` exists and documents the supported config.
- `docs/ai/regen-bindings.md` is de-staled (no "5000", no version-line claim) and links the new doc.
- `UtinniCoreDotNetGen/Program.cs` resolver logic is byte-identical except for an added doc-pointer comment.
</verification>

<success_criteria>
- CPPS-01: documented negative result exists (spike script + conclusion) — phase acceptance re-set to harden-the-redirect.
- CPPS-02: the 14.29 redirect is documented in-repo as the supported config, explicit and discoverable.
- No new external dependency added; no shipped-DLL behavior changed; resolver logic untouched.
</success_criteria>

<output>
Create `.planning/phases/17-cppsharp-v145-hardening/17-01-SUMMARY.md` when done.
</output>
