---
phase: 20-terrain-trn-codec-verbs-mcp
plan: 04
type: execute
wave: 3
depends_on: ["20-03"]
files_modified:
  - Utinni.Mcp/Tools/ReadTools.cs
  - Utinni.Mcp.Tests/Terrain/TerrainReadToolTests.cs
  - Utinni.Cli.Tests/Fixtures/trn/TgenFixtureSynthesizer.cs
  - Utinni.Cli.Tests/Terrain/RoundtripTrnTests.cs
autonomous: false
requirements: [PROD-W2-TRN-04]
user_setup: []
must_haves:
  truths:
    - "An MCP `summarize_terrain` read tool shells utinni-cli decode-iff (which auto-routes TGEN) and returns the JSON envelope — ZERO format logic in Utinni.Mcp (MCP-OOP)"
    - "The both-lineage byte-exact gate passes: a low-version (SWGEmu-era) AND a high-version (Restoration-era) fixture per Tier-1 tag roundtrip byte-identically"
    - "The real per-lineage .trn version pair is observed (via Utinni's own TRE verbs) and used to PIN the synthesized matrix versions — real assets stay OUT of the committed golden corpus"
  artifacts:
    - path: "Utinni.Mcp/Tools/ReadTools.cs"
      provides: "summarize_terrain [McpServerTool(ReadOnly=true)] thin shell over decode-iff"
      contains: "summarize_terrain"
    - path: "Utinni.Mcp.Tests/Terrain/TerrainReadToolTests.cs"
      provides: "MCP thin-wrapper dispatch test (envelope pass-through, both lineages)"
  key_links:
    - from: "Utinni.Mcp/Tools/ReadTools.cs"
      to: "utinni-cli decode-iff"
      via: "CliDispatcher.RunAsync(\"decode-iff\", [abs]) → CliResultMapper.ToCallToolResult"
      pattern: "decode-iff"
---

<objective>
Close the phase: add the thin MCP `summarize_terrain` read tool (MCP-OOP — shells `decode-iff`,
zero format logic), prove the both-lineage byte-exact gate (low + high FORM version per Tier-1
tag roundtrips identically, DEC-C3), and pin the synthesized matrix versions against a real
per-lineage `.trn` pair sourced via Utinni's own TRE verbs (D-13/D-14). Completes criterion 4
(TRN-04) and locks the DEC-C3 codec gate across both lineages.

Purpose: This is the MCP half of the verb/read surface plus the milestone gate. The maintainer
checkpoint sources the real-asset pair (the only step Utinni cannot fully self-serve — it needs
the live client archives) to confirm the synthesized version matrix matches what ships.
Output: `summarize_terrain` + the MCP test; pinned + green both-lineage roundtrip matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Build/test invariant:** MSBuild `Utinni.sln` x86 Release FIRST (CLI), build `Utinni.Mcp` (net10),
then `dotnet test --no-build`. Never `dotnet build` the WinForms-coupled lanes.
`Generated/UtinniCore.cs` NOT touched — `git checkout --` if reordered. MCP-OOP lock (DEC-V2-MCP-OOP):
ZERO format logic in `Utinni.Mcp`; the named pipe / process boundary is real.
</execution_context>

<context>
@.planning/phases/20-terrain-trn-codec-verbs-mcp/20-CONTEXT.md
@.planning/phases/20-terrain-trn-codec-verbs-mcp/20-RESEARCH.md
@.planning/phases/20-terrain-trn-codec-verbs-mcp/20-PATTERNS.md
@.planning/phases/20-terrain-trn-codec-verbs-mcp/20-VALIDATION.md

<interfaces>
<!-- MCP thin-shell copy template. Source: Utinni.Mcp/Tools/ReadTools.cs:97-110 (SummarizeParticle) -->
[McpServerTool(Name="summarize_particle", ReadOnly=true, Idempotent=true)] static async
Task<CallToolResult> Summarize...(ResolvedRoot root, CliDispatcher cli, string relativePath):
abs = root.Resolve(relativePath) [throws on escape → SDK tool error]; r = await
cli.RunAsync("<verb>", new[]{abs}); return CliResultMapper.ToCallToolResult(r). ZERO format logic.

<!-- Real-asset sourcing. Source: utinni-cli verbs (Phase 12/13) -->
`parse-tre` lists/extracts TRE archive records. terrain assets live at `terrain/<planet>.trn` inside the
client TRE load order. v6000+ Restoration TRE payloads are encrypted (enumerate-only) — prefer a
small/simple planet whose .trn is reachable.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MCP summarize_terrain thin-shell read tool (MCP-OOP)</name>
  <files>Utinni.Mcp/Tools/ReadTools.cs, Utinni.Mcp.Tests/Terrain/TerrainReadToolTests.cs</files>
  <read_first>
    - Utinni.Mcp/Tools/ReadTools.cs:97-110 (SummarizeParticle — copy verbatim, retarget to terrain)
    - 20-CONTEXT.md D-10 (summarize_terrain shells decode-iff, ZERO format logic) + D-08 (decode-iff routes TGEN — so the MCP tool gets terrain for free)
    - 20-PATTERNS.md § "Utinni.Mcp/Tools/ReadTools.cs" :225-238 (the exact copy template) + § "MCP thin-shell (MCP-OOP)" :278-281
    - AGENTS.md DEC-V2-MCP-OOP (named pipe IS the boundary; never host format logic in net10)
  </read_first>
  <action>
    Add `SummarizeTerrain` to `Utinni.Mcp/Tools/ReadTools.cs` by copying `SummarizeParticle` (:97-110)
    verbatim and retargeting: `[McpServerTool(Name = "summarize_terrain", ReadOnly = true, Idempotent =
    true)]` with a `[Description]` noting it dispatches `decode-iff` (which auto-routes a FORM TGEN root).
    Body: `string abs = root.Resolve(relativePath);` (throws on escape → SDK tool error) →
    `CliInvocationResult r = await cli.RunAsync("decode-iff", new[] { abs });` →
    `return CliResultMapper.ToCallToolResult(r);`. ZERO format logic — it is a pure envelope pass-through
    over the CLI (MCP-OOP, D-10). Then create `Utinni.Mcp.Tests/Terrain/TerrainReadToolTests.cs`: un-Skip
    the Plan-01 stub + assert the tool dispatches `decode-iff` and returns the terrain envelope
    (`type:terrain`/`rootType:TGEN`) for a TGEN fixture, and that a path-escape relativePath surfaces a
    tool error (no format work in net10). Use the committed `Utinni.Cli.Tests/Fixtures/trn/` fixtures (or
    a small MCP-side fixture copy) for both a low-version and a high-version case (both lineages).
  </action>
  <verify>
    <automated>Build Utinni.Mcp (net10) + Utinni.Cli (x86 Release via MSBuild), THEN `dotnet test Utinni.Mcp.Tests --no-build --filter Terrain` exits 0 — summarize_terrain dispatches decode-iff and returns the terrain envelope; path-escape surfaces a tool error</automated>
  </verify>
  <acceptance_criteria>
    - `ReadTools.cs` contains `summarize_terrain` as a `[McpServerTool(ReadOnly=true)]` with NO format-decode logic (grep: no TGEN/IFF parsing types referenced in the method body — it only calls `cli.RunAsync` + `CliResultMapper`).
    - The tool returns the `type:terrain`/`rootType:TGEN` envelope for a TGEN fixture (both a low and a high version).
    - A path-escape `relativePath` surfaces a tool error (no write, no decode).
    - `dotnet test Utinni.Mcp.Tests --no-build --filter Terrain` exits 0.
  </acceptance_criteria>
  <done>The MCP read tool routes terrain via the OOP boundary with zero format logic — criterion 4's MCP half (TRN-04) is met.</done>
</task>

<task type="auto">
  <name>Task 2: Both-lineage byte-exact gate — version-matrix roundtrip (DEC-C3)</name>
  <files>Utinni.Cli.Tests/Fixtures/trn/TgenFixtureSynthesizer.cs, Utinni.Cli.Tests/Terrain/RoundtripTrnTests.cs</files>
  <read_first>
    - 20-CONTEXT.md D-12 (matrix: low-version "SWGEmu-era" + high-version "Restoration-era" per Tier-1 tag) + D-14 (real pair pins versions; synthesized fixtures remain the committed goldens)
    - 20-RESEARCH.md § "SWGEmu vs Restoration version dispatch" :247-255 (LAYR 0000-0004, ShaderGroup 0000-0006, FloraGroup 0001-0008 — where lineages diverge) + § Validation Architecture :414-445
    - 20-VALIDATION.md § Per-Task Verification Map (TRN-04 both-lineage roundtrip) + § Wave 0 Requirements
    - Utinni.Cli.Tests/Terrain/RoundtripTrnTests.cs (the Plan-03 roundtrip assertions to extend across the full version matrix)
  </read_first>
  <action>
    Extend `TgenFixtureSynthesizer` so every Tier-1 tag (D-01) has BOTH a low-version and a high-version
    fixture, plus a palette-bearing TGEN exercising the wide-range palettes (ShaderGroup high version,
    FloraGroup high version) where lineages diverge (RESEARCH :247-255). Parameterize the synthesizer's
    version arguments from named constants (`SwgEmuEraVersions` / `RestorationEraVersions`) so Task 3 can
    PIN them to the observed real-asset versions. Extend `RoundtripTrnTests.cs` to a `[Theory]` over the
    FULL version matrix asserting whole-file byte identity (`SequenceEqual`) for each — this is the DEC-C3
    both-lineage gate. Keep all fixtures ≤200 bytes and committed; the real per-lineage `.trn` pair stays
    OUT of the committed corpus (D-14).
  </action>
  <verify>
    <automated>MSBuild x86 Release succeeds, THEN `dotnet test Utinni.Cli.Tests --no-build --filter RoundtripTrn` exits 0 across the full low+high version matrix (every Tier-1 tag both lineages roundtrips byte-identically)</automated>
  </verify>
  <acceptance_criteria>
    - Every Tier-1 tag (D-01) has a low-version AND a high-version committed fixture; both roundtrip byte-identically.
    - The synthesizer's version arguments are sourced from named `SwgEmuEraVersions`/`RestorationEraVersions` constants (pinnable in Task 3).
    - All fixtures are ≤200 bytes and committed; no real retail `.trn` is committed.
    - `dotnet test Utinni.Cli.Tests --no-build --filter RoundtripTrn` exits 0 (full matrix green) — the DEC-C3 gate.
  </acceptance_criteria>
  <done>The both-lineage byte-exact roundtrip matrix is green — DEC-C3 is satisfied across SWGEmu-era + Restoration-era versions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Pin synthesized matrix to a real per-lineage .trn version pair (D-13/D-14)</name>
  <action>Source the real per-lineage .trn pair via Utinni TRE verbs, confirm the observed FORM version pair, run roundtrip-trn on the real asset(s), and pin the synthesizer SwgEmuEraVersions/RestorationEraVersions constants to the observed versions (no committed real asset). See what-built/how-to-verify below.</action>
  <what-built>
    The full terrain codec + verbs + MCP tool are green against synthesized ≤200-byte fixtures whose
    FORM versions are currently assumed (low = SWGEmu-era, high = Restoration-era). This checkpoint sources
    a REAL per-lineage `.trn` pair via Utinni's own TRE verbs to (a) confirm the assumed version pair
    matches what each live client ships (resolves RESEARCH A5 / Open Q1) and (b) run `roundtrip-trn` on a
    real asset as an extra byte-exact cross-check. Real assets stay OUT of the committed corpus (D-14:
    retail `.trn` are large; v6000+ TRE payloads encrypted).
  </what-built>
  <how-to-verify>
    Automation has already run the synthesized matrix green. To pin versions against reality (maintainer
    has the live client archives — Utinni cannot self-serve this):
    1. Pick a small/simple planet and extract its terrain from EACH client via Utinni's own CLI, e.g.
       `utinni-cli parse-tre --extract terrain/<planet>.trn` against the SWGEmu archive, then the
       Restoration archive (dogfooding the revived TRE verbs — D-13). If the Restoration archive is
       v6000+ encrypted and the `.trn` is unreachable, note that and rely on the synthesized matrix.
    2. For each extracted `.trn`, run `utinni-cli decode-iff <path>` and record the observed FORM versions
       (TGEN, LAYR, the palettes, the Tier-1 tags present).
    3. Run `utinni-cli roundtrip-trn <path>` on each real asset; confirm it reports `bytesIdentical:true`
       (the extra real-asset byte-exact check, D-14).
    4. Report the observed SWGEmu vs Restoration version pair so the synthesizer constants
       (`SwgEmuEraVersions`/`RestorationEraVersions`) can be pinned to match. If they already match the
       assumed values, confirm; if they differ, provide the real numbers.
  </how-to-verify>
  <resume-signal>
    Reply with the observed version pair (or "synthesized matrix stands — real asset unreachable/encrypted"),
    and whether `roundtrip-trn` on the real asset(s) reported `bytesIdentical:true`. The executor then pins
    the synthesizer constants to the observed versions (no committed real asset) and re-runs the matrix.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → ReadTools.summarize_terrain | relativePath from the MCP client crosses into the resolver; must be contained by `root.Resolve` (V12) |
| net10 MCP process → utinni-cli (x86) | the named-pipe / subprocess boundary IS the security + architecture boundary (MCP-OOP); no format logic crosses into net10 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-08 | Tampering / Elevation | summarize_terrain path resolution | mitigate | `root.Resolve(relativePath)` throws on escape (→ SDK tool error, no decode), mirroring the existing read-tool containment — V12. Acceptance: the path-escape MCP test surfaces a tool error. |
| T-20-09 | Repudiation / Info-disclosure | MCP hosting format logic in net10 | mitigate | `summarize_terrain` is a pure envelope pass-through over `cli.RunAsync` (MCP-OOP, D-10) — no parsing/decoding in net10, keeping the codec's V5 bounds-checking on the CLI side of the boundary. Acceptance: the method body references only CliDispatcher + CliResultMapper (no IFF/TGEN types). |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure managed, zero new external packages. No legitimacy gate needed. |
</threat_model>

<verification>
- `dotnet test Utinni.Mcp.Tests --no-build --filter Terrain` exits 0 (summarize_terrain dispatch + path-escape).
- `dotnet test Utinni.Cli.Tests --no-build --filter RoundtripTrn` exits 0 across the full low+high version matrix (DEC-C3).
- Maintainer checkpoint: real per-lineage `.trn` version pair observed (or unreachable-noted); `roundtrip-trn` on the real asset(s) reports `bytesIdentical:true`; synthesizer constants pinned.
- Full managed suite green: `dotnet test --no-build` across CLI + MCP + CoreDotNet lanes.
</verification>

<success_criteria>
- TRN-04 (MCP half): `summarize_terrain` shells `decode-iff` with zero format logic; both lineages route.
- DEC-C3: both-lineage (low + high version per Tier-1 tag) byte-exact roundtrip matrix is green.
- Real per-lineage version pair pins the synthesized matrix (real assets stay out of the committed corpus).
</success_criteria>

<output>
Create `.planning/phases/20-terrain-trn-codec-verbs-mcp/20-04-SUMMARY.md` when done
</output>
