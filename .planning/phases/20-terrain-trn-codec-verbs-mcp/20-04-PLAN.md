---
phase: 20-terrain-trn-codec-verbs-mcp
plan: 04
type: execute
wave: 3
depends_on: ["20-03"]
files_modified:
  - Utinni.Mcp/Tools/ReadTools.cs
  - Utinni.Mcp.Tests/Terrain/TerrainReadToolTests.cs
  - Utinni.Cli.Tests/Fixtures/trn/TgenEraVersions.cs
  - Utinni.Cli.Tests/Fixtures/trn/LargeFixtures/TgenLargeFixtureSynthesizer.cs
  - Utinni.Cli.Tests/Terrain/RoundtripTrnTests.cs
  - Utinni.Cli.Tests/Terrain/PaletteLineageTests.cs
autonomous: false
requirements: [PROD-W2-TRN-04]
user_setup: []
must_haves:
  truths:
    - "An MCP summarize_terrain read tool shells utinni-cli decode-iff (which auto-routes TGEN) and returns the JSON envelope — ZERO format logic in Utinni.Mcp (MCP-OOP); the MCP test asserts schema compatibility, propagates nonzero CLI exit as a tool error, and exercises malformed input (review concern #5)"
    - "The both-lineage byte-exact gate passes across the FULL low+high matrix built in Plan 01 from TgenEraVersions; palette-heavy lineage-divergence is covered by a SEPARATE opt-in LargeFixtures set NOT subject to the ≤200-byte cap (review concern #11)"
    - "DEC-C3 is declared closed ONLY AFTER the real-asset version pin is confirmed — the pin is a PREREQUISITE, not a post-hoc checkpoint (review concern #1)"
    - "The Wave-0 observed real-asset versions (Plan 01 Task 3) are reconciled into TgenEraVersions and the matrix re-run; an encrypted/unreachable Restoration .trn fails clearly and is recorded as such (review concern #15)"
  decisions:
    - "D-10: MCP summarize_terrain is a [McpServerTool(ReadOnly=true)] that shells utinni-cli — ZERO format logic in Utinni.Mcp (MCP-OOP lock)"
    - "D-12: synthesized ≤200-byte TGEN fixtures remain the committed golden corpus for the core matrix; palette-heavy fixtures live in a separate opt-in LargeFixtures set WITHOUT the size cap (#11)"
    - "D-13: the real per-lineage .trn versions OBSERVED in Wave 0 (Plan 01 Task 3) are CONFIRMED/pinned here as the DEC-C3 prerequisite"
    - "D-14: real assets stay OUT of the committed corpus; an extra roundtrip-trn byte-exact check runs against them where reachable"
  artifacts:
    - path: "Utinni.Mcp/Tools/ReadTools.cs"
      provides: "summarize_terrain [McpServerTool(ReadOnly=true)] thin shell over decode-iff"
      contains: "summarize_terrain"
    - path: "Utinni.Cli.Tests/Fixtures/trn/LargeFixtures/TgenLargeFixtureSynthesizer.cs"
      provides: "opt-in palette-bearing fixtures (ShaderGroup/FloraGroup high version, CString family names) NOT subject to the ≤200-byte cap — lineage-divergence coverage (#11)"
      contains: "LargeFixtures"
    - path: "Utinni.Mcp.Tests/Terrain/TerrainReadToolTests.cs"
      provides: "MCP thin-wrapper dispatch test (envelope schema pass-through, exit-code → tool-error mapping, malformed input, both lineages)"
  key_links:
    - from: "Utinni.Mcp/Tools/ReadTools.cs"
      to: "utinni-cli decode-iff"
      via: "CliDispatcher.RunAsync(\"decode-iff\", [abs]) → CliResultMapper.ToCallToolResult"
      pattern: "decode-iff"
    - from: "Utinni.Cli.Tests/Fixtures/trn/TgenEraVersions.cs"
      to: "Utinni.Cli.Tests/Terrain/RoundtripTrnTests.cs"
      via: "the pinned era constants drive the full both-lineage roundtrip matrix (DEC-C3)"
      pattern: "TgenEraVersions"
---

<objective>
Close the phase: add the thin MCP `summarize_terrain` read tool (MCP-OOP — shells `decode-iff`, zero
format logic) with schema/exit-code/malformed-input tests (#5), add a SEPARATE opt-in `LargeFixtures`
palette-bearing set so palette-lineage divergence is covered without violating the ≤200-byte cap
(#11), CONFIRM the real-asset version pin observed in Wave 0 and reconcile it into `TgenEraVersions`,
then — ONLY after that pin is confirmed — declare the DEC-C3 both-lineage byte-exact gate closed
(#1). Completes criterion 4 (TRN-04) and locks DEC-C3 across both lineages on a grounded version pair.

Purpose: This is the MCP half of the verb/read surface plus the milestone gate. The reviewers'
loudest theme — "false byte-exact confidence" — is closed by making the real-asset pin a PREREQUISITE
to the DEC-C3 close (not a soft post-hoc checkpoint) and by covering palette-heavy lineage divergence
in an opt-in large-fixture set the ≤200-byte cap cannot accommodate.
Output: `summarize_terrain` + the MCP test (schema/exit-code/malformed); `LargeFixtures`; a reconciled
+ pinned both-lineage matrix; DEC-C3 closed on confirmed versions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Build/test invariant:** MSBuild `Utinni.sln` x86 Release FIRST (CLI), build `Utinni.Mcp` (net10),
then `dotnet test --no-build`. Never `dotnet build` the WinForms-coupled lanes.
`Generated/UtinniCore.cs` NOT touched — `git checkout --` if reordered. MCP-OOP lock (DEC-V2-MCP-OOP):
ZERO format logic in `Utinni.Mcp`; the named pipe / process boundary is real.
</execution_context>

<context>
@.planning/phases/20-terrain-trn-codec-verbs-mcp/20-CONTEXT.md
@.planning/phases/20-terrain-trn-codec-verbs-mcp/20-RESEARCH.md
@.planning/phases/20-terrain-trn-codec-verbs-mcp/20-PATTERNS.md
@.planning/phases/20-terrain-trn-codec-verbs-mcp/20-VALIDATION.md
@.planning/phases/20-terrain-trn-codec-verbs-mcp/20-REVIEWS.md

<interfaces>
<!-- MCP thin-shell copy template. Source: Utinni.Mcp/Tools/ReadTools.cs:97-110 (SummarizeParticle) -->
[McpServerTool(Name="summarize_particle", ReadOnly=true, Idempotent=true)] static async
Task<CallToolResult> Summarize...(ResolvedRoot root, CliDispatcher cli, string relativePath):
abs = root.Resolve(relativePath) [throws on escape → SDK tool error]; r = await
cli.RunAsync("<verb>", new[]{abs}); return CliResultMapper.ToCallToolResult(r). ZERO format logic.
CliResultMapper maps a nonzero CLI exit to a tool error — assert that mapping for a malformed input.

<!-- Real-asset sourcing. Source: utinni-cli verbs (Phase 12/13) -->
`parse-tre` lists/extracts TRE archive records. terrain assets live at `terrain/<planet>.trn` inside the
client TRE load order. v6000+ Restoration TRE payloads are encrypted (enumerate-only) — prefer a
small/simple planet whose .trn is reachable. Wave-0 (Plan 01 Task 3) already OBSERVED the version pair;
this plan CONFIRMS/pins it as the DEC-C3 prerequisite.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MCP summarize_terrain thin-shell read tool + schema/exit-code/malformed tests (MCP-OOP)</name>
  <files>Utinni.Mcp/Tools/ReadTools.cs, Utinni.Mcp.Tests/Terrain/TerrainReadToolTests.cs</files>
  <read_first>
    - Utinni.Mcp/Tools/ReadTools.cs:97-110 (SummarizeParticle — copy verbatim, retarget to terrain) + CliResultMapper.ToCallToolResult (the nonzero-exit → tool-error mapping to assert)
    - Utinni.Mcp.Tests/ (existing MCP read-tool test convention — how ParticleReadToolTests drive the dispatcher; mirror for terrain)
    - 20-CONTEXT.md D-10 (summarize_terrain shells decode-iff, ZERO format logic) + D-08 (decode-iff routes TGEN)
    - 20-PATTERNS.md § "Utinni.Mcp/Tools/ReadTools.cs" :225-238 (copy template) + § "MCP thin-shell (MCP-OOP)" :278-281
    - AGENTS.md DEC-V2-MCP-OOP (named pipe IS the boundary; never host format logic in net10)
    - 20-REVIEWS.md Consensus #5 (schema compatibility + exit-code mapping + malformed-input behavior tested)
  </read_first>
  <behavior>
    - summarize_terrain dispatches `decode-iff` for a TGEN fixture and returns the SAME navigable terrain envelope the CLI emits (type:terrain/rootType:TGEN/layers array) — schema pass-through, zero format logic.
    - A nonzero CLI exit (e.g. malformed .trn → exit 2) is propagated as an MCP TOOL ERROR by CliResultMapper, not a success envelope (concern #5).
    - A path-escape relativePath surfaces a tool error (no decode).
    - Both a low-version and a high-version fixture route correctly (both lineages).
  </behavior>
  <action>
    Add `SummarizeTerrain` to `Utinni.Mcp/Tools/ReadTools.cs` by copying `SummarizeParticle` (:97-110)
    verbatim and retargeting: `[McpServerTool(Name = "summarize_terrain", ReadOnly = true, Idempotent =
    true)]` with a `[Description]` noting it dispatches `decode-iff` (which auto-routes a FORM TGEN root).
    Body: `string abs = root.Resolve(relativePath);` (throws on escape → SDK tool error) →
    `CliInvocationResult r = await cli.RunAsync("decode-iff", new[] { abs });` →
    `return CliResultMapper.ToCallToolResult(r);`. ZERO format logic. Then create
    `Utinni.Mcp.Tests/Terrain/TerrainReadToolTests.cs` (filter `Terrain`): un-Skip the Plan-01 stub +
    assert (a) the tool dispatches `decode-iff` and returns the navigable terrain envelope
    (type:terrain/rootType:TGEN) for both a low and a high version fixture; (b) a malformed `.trn` causes
    CliResultMapper to surface a TOOL ERROR (nonzero exit propagated, not a success envelope — concern #5);
    (c) a path-escape relativePath surfaces a tool error. Use the committed `Utinni.Cli.Tests/Fixtures/trn/`
    fixtures (or a small MCP-side copy). The MCP integration test MUST resolve the just-built x86 Release
    `utinni-cli.exe` (the build output carrying the Plan-03 decode-iff TGEN branch), NOT a stale PATH copy.
  </action>
  <verify>
    <automated>Build Utinni.Mcp (net10) + Utinni.Cli (x86 Release via MSBuild), THEN `dotnet test Utinni.Mcp.Tests --no-build --filter Terrain` exits 0 — summarize_terrain dispatches decode-iff and returns the navigable terrain envelope (both versions); a malformed input is surfaced as a tool error (nonzero exit propagated); a path-escape surfaces a tool error. The test resolves the freshly-built x86 Release utinni-cli.exe (with the Plan-03 TGEN branch), not a stale PATH copy.</automated>
  </verify>
  <acceptance_criteria>
    - `ReadTools.cs` contains `summarize_terrain` as a `[McpServerTool(ReadOnly=true)]` with NO format-decode logic (the method body references only `cli.RunAsync` + `CliResultMapper` — no IFF/TGEN types).
    - The tool returns the navigable `type:terrain`/`rootType:TGEN` envelope for both a low and a high version fixture (schema pass-through).
    - A malformed input propagates the nonzero CLI exit as an MCP tool error (not a success envelope); a path-escape surfaces a tool error (#5).
    - The MCP test resolves the freshly-built `utinni-cli.exe` (x86 Release with the Plan-03 decode-iff TGEN branch).
    - `dotnet test Utinni.Mcp.Tests --no-build --filter Terrain` exits 0.
  </acceptance_criteria>
  <done>The MCP read tool routes terrain via the OOP boundary with zero format logic, schema pass-through, and exit-code/malformed mapping (#5) — criterion 4's MCP half (TRN-04) is met.</done>
</task>

<task type="auto">
  <name>Task 2: Opt-in LargeFixtures palette set (lineage divergence, no ≤200-byte cap) + full-matrix gate prep</name>
  <files>Utinni.Cli.Tests/Fixtures/trn/LargeFixtures/TgenLargeFixtureSynthesizer.cs, Utinni.Cli.Tests/Terrain/PaletteLineageTests.cs, Utinni.Cli.Tests/Terrain/RoundtripTrnTests.cs</files>
  <read_first>
    - Utinni.Cli.Tests/Fixtures/trn/TgenFixtureSynthesizer.cs (Plan 01 — the core ≤200-byte synthesizer + TgenEraVersions; LargeFixtures mirrors its build idiom WITHOUT the size cap)
    - UtinniCoreDotNet/Formats/Decoders/IffPayloadCursor.cs:136 (ReadCString — palette family names are NUL-terminated, variable-length; why palette fixtures exceed 200 bytes)
    - 20-RESEARCH.md § "The Six Shared Palettes" :232-245 (ShaderGroup 0000-0006, FloraGroup 0001-0008 — the wide-range palettes where lineages diverge) + § "SWGEmu vs Restoration version dispatch" :247-255
    - 20-CONTEXT.md D-12 (≤200-byte committed goldens) + D-14 (real assets out of corpus)
    - 20-REVIEWS.md Consensus #11 (≤200-byte cap conflicts with palette fixtures → separate opt-in LargeFixtures set, not subject to the cap) + #3 (palette absence / single-MGRP coverage)
  </read_first>
  <behavior>
    - TgenLargeFixtureSynthesizer emits palette-bearing TGEN fixtures (ShaderGroup high version with SFAM CString family names, FloraGroup high version with FFAM names, both MGRP palettes present) — these EXCEED 200 bytes and are NOT committed goldens subject to D-12's cap (concern #11).
    - It also emits a single-MGRP-present fixture (only the fractal-slot MGRP) and a missing-earlier-palette fixture to drive the palette state-machine absence tests (#3) at realistic size.
    - PaletteLineageTests (filter `PaletteLineage`) decode + roundtrip these large fixtures: positional palette assignment, family-name resolution, single-MGRP Ambiguous flag, and whole-file byte identity — proving palette lineage divergence the ≤200-byte core matrix cannot.
    - The core ≤200-byte RoundtripTrn matrix (Plan 01/03) remains the committed-golden gate; LargeFixtures are opt-in (generated at test time, not committed as golden blobs).
  </behavior>
  <action>
    Create `Utinni.Cli.Tests/Fixtures/trn/LargeFixtures/TgenLargeFixtureSynthesizer.cs` mirroring the
    Plan-01 synthesizer's `IffWriter`-based build idiom but WITHOUT the ≤200-byte assertion (concern #11) —
    it emits palette-bearing TGEN fixtures: a ShaderGroup high-version palette (SFAM family entries with
    `ReadCString`/insert-string NUL-terminated names), a FloraGroup high-version palette (FFAM), both MGRP
    palettes (fractal then bitmap by position), plus a single-MGRP-present fixture and a missing-earlier-slot
    fixture for the absence state-machine (#3). Version arguments come from `TgenEraVersions` (so they pin
    with the core matrix). Create `PaletteLineageTests.cs` (filter `PaletteLineage`): decode each large
    fixture and assert positional palette assignment + family-name resolution + the single-MGRP `Ambiguous`
    flag (#3), and assert whole-file byte identity (`SequenceEqual`) on roundtrip. Keep these OUT of the
    committed ≤200-byte golden corpus — they are generated at test time (D-12/D-14 preserved). Confirm the
    core RoundtripTrn matrix from Plan 01/03 still covers every Tier-1 tag low+high (no regression).
  </action>
  <verify>
    <automated>MSBuild x86 Release succeeds, THEN `dotnet test Utinni.Cli.Tests --no-build --filter "PaletteLineage|RoundtripTrn"` exits 0 — palette-bearing large fixtures decode positionally (single-MGRP Ambiguous), resolve family names, and roundtrip byte-identically; the core ≤200-byte low+high matrix still passes</automated>
  </verify>
  <acceptance_criteria>
    - `TgenLargeFixtureSynthesizer` emits palette-bearing fixtures (ShaderGroup/FloraGroup high version with CString names, both MGRP) that EXCEED 200 bytes and are NOT committed goldens (#11).
    - `PaletteLineageTests` prove positional palette assignment + family-name resolution + single-MGRP `Ambiguous` (#3) + whole-file byte identity on roundtrip.
    - The core ≤200-byte RoundtripTrn matrix (every Tier-1 tag low+high) still passes (no regression).
    - `dotnet test Utinni.Cli.Tests --no-build --filter "PaletteLineage|RoundtripTrn"` exits 0.
  </acceptance_criteria>
  <done>Palette lineage divergence is covered by an opt-in large-fixture set without violating the ≤200-byte committed-golden cap (#11/#3 resolved).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: CONFIRM the real-asset version pin — DEC-C3 PREREQUISITE (reconcile Wave-0 observation, then declare DEC-C3 closed)</name>
  <action>Reconcile the real-asset FORM versions OBSERVED in Wave 0 (Plan 01 Task 3) into TgenEraVersions, re-run the full both-lineage matrix + the extra real-asset roundtrip-trn check, and ONLY THEN declare DEC-C3 closed. The pin is a PREREQUISITE to the close, not a post-hoc checkpoint (review concern #1). See what-built/how-to-verify below.</action>
  <what-built>
    The full terrain codec + verbs + MCP tool are green against synthesized fixtures whose FORM versions
    come from `TgenEraVersions`. Wave 0 (Plan 01 Task 3) already OBSERVED the real per-lineage version pair.
    Review concern #1 (both reviewers, HIGH) is that DEC-C3 must NOT be declared closed against assumed
    versions. This checkpoint reconciles the Wave-0 observation into `TgenEraVersions`, re-runs the matrix,
    runs `roundtrip-trn` on the real asset(s) as an extra byte-exact cross-check, and gates the DEC-C3
    close on that confirmation. If a Restoration `.trn` is v6000+ encrypted/unreachable, that is recorded
    explicitly (concern #15) and the RestorationEra constants are documented as best-available-assumed —
    DEC-C3 is then declared closed for the reachable lineage with the limitation noted, not silently.
  </what-built>
  <how-to-verify>
    1. Apply the Wave-0 observed versions (Plan 01 Task 3 resume note) to `TgenEraVersions` (edit ONLY that
       file) so the synthesized matrix versions match what each live client ships. If Wave-0 reported
       "Restoration encrypted/unreachable," annotate `RestorationEra` as best-available-assumed and record
       the limitation (concern #15).
    2. Re-run the full matrix: `dotnet test Utinni.Cli.Tests --no-build --filter "RoundtripTrn|PaletteLineage"`
       — confirm GREEN on the pinned versions.
    3. Run `utinni-cli roundtrip-trn <real .trn>` on each reachable real asset; confirm `bytesIdentical:true`
       (the extra real-asset byte-exact check, D-14). Real assets stay OUT of the committed corpus.
    4. Declare DEC-C3 closed ONLY after steps 1-3 pass (or, for an unreachable lineage, after the limitation
       is explicitly recorded) — this checkpoint is the PREREQUISITE gate, not a post-hoc sign-off (#1).
  </how-to-verify>
  <resume-signal>
    Reply confirming the pinned version pair matches Wave-0 observation (or that the Restoration lineage is
    encrypted/unreachable and recorded as such), that the full matrix is GREEN on the pinned versions, and
    that `roundtrip-trn` on the reachable real asset(s) reported `bytesIdentical:true`. Only on this
    confirmation is DEC-C3 declared closed.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → ReadTools.summarize_terrain | relativePath from the MCP client crosses into the resolver; must be contained by `root.Resolve` (V12) |
| net10 MCP process → utinni-cli (x86) | the named-pipe / subprocess boundary IS the security + architecture boundary (MCP-OOP); no format logic crosses into net10; a nonzero CLI exit must surface as a tool error |
| real client TRE archive → utinni-cli TRE verbs | encrypted v6000+ Restoration payloads must fail clearly (enumerate-only); DEC-C3 records the limitation rather than claiming a false both-lineage pass (#15) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-08 | Tampering / Elevation | summarize_terrain path resolution | mitigate | `root.Resolve(relativePath)` throws on escape (→ SDK tool error, no decode), mirroring the existing read-tool containment — V12. Acceptance: the path-escape MCP test surfaces a tool error. |
| T-20-09 | Repudiation / Info-disclosure | MCP hosting format logic in net10 | mitigate | `summarize_terrain` is a pure envelope pass-through over `cli.RunAsync` (MCP-OOP, D-10) — no parsing/decoding in net10, keeping the codec's V5 bounds-checking on the CLI side. A nonzero CLI exit is propagated as a tool error (concern #5). Acceptance: the method body references only CliDispatcher + CliResultMapper; the malformed-input MCP test surfaces a tool error. |
| T-20-10 | Tampering | false both-lineage gate on assumed versions | mitigate | DEC-C3 close is gated on the confirmed real-asset version pin (prerequisite, #1); an encrypted/unreachable lineage is recorded explicitly (#15) rather than claimed green. Acceptance: the Task-3 checkpoint resume confirms pinned-version match + matrix green before the close. |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure managed, zero new external packages. No legitimacy gate needed. |
</threat_model>

<verification>
- `dotnet test Utinni.Mcp.Tests --no-build --filter Terrain` exits 0 (summarize_terrain dispatch + schema + exit-code/malformed mapping + path-escape).
- `dotnet test Utinni.Cli.Tests --no-build --filter "RoundtripTrn|PaletteLineage"` exits 0 across the full pinned low+high matrix + palette lineage (DEC-C3).
- Maintainer checkpoint: Wave-0 observed versions reconciled into `TgenEraVersions`; matrix GREEN on pinned versions; `roundtrip-trn` on reachable real asset(s) reports `bytesIdentical:true`; DEC-C3 declared closed ONLY after this confirmation.
- Full managed suite green: `dotnet test --no-build` across CLI + MCP + CoreDotNet lanes.
</verification>

<success_criteria>
- TRN-04 (MCP half): `summarize_terrain` shells `decode-iff` with zero format logic; schema/exit-code/malformed tested; both lineages route.
- DEC-C3: both-lineage (low + high version per Tier-1 tag) byte-exact roundtrip matrix is green ON THE CONFIRMED real-asset version pin (the pin is a prerequisite, #1); palette lineage divergence covered by opt-in LargeFixtures (#11).
- Encrypted/unreachable Restoration lineage fails clearly and is recorded, not falsely claimed green (#15).
</success_criteria>

<output>
Create `.planning/phases/20-terrain-trn-codec-verbs-mcp/20-04-SUMMARY.md` when done
</output>
</content>
</invoke>
