---
phase: 20-terrain-trn-codec-verbs-mcp
plan: 02
type: execute
wave: 1
depends_on: ["20-01"]
files_modified:
  - UtinniCoreDotNet/Formats/Terrain/TgenFieldLayouts.cs
  - UtinniCoreDotNet/Formats/Terrain/TerrainDocument.cs
  - UtinniCoreDotNet/Formats/Terrain/TerrainLayer.cs
  - UtinniCoreDotNet/Formats/Terrain/TerrainNode.cs
  - UtinniCoreDotNet/Formats/Terrain/TerrainPalettes.cs
  - UtinniCoreDotNet/Formats/Terrain/TerrainParseException.cs
  - UtinniCoreDotNet/Formats/Decoders/TgenDecoder.cs
  - Utinni.Cli.Tests/Terrain/TgenDecoderTests.cs
  - Utinni.Cli.Tests/Terrain/TgenFieldLayoutTests.cs
autonomous: true
requirements: [PROD-W2-TRN-01, PROD-W2-TRN-02]
must_haves:
  truths:
    - "A SINGLE shared TgenFieldLayouts descriptor table records, per Tier-1 tag × version, every field's {name, offset, width, signedness, endian, enum-storage-width, display-type, edit-parser, editable} — consumed by BOTH the decoder (this plan) and the encoder (Plan 03)"
    - "A modder can decode a .trn and navigate its TGEN→LYRS→LAYR tree (boundaries/filters/affectors/sub-layers) with each layer's name + active flag, exposed in a NAVIGABLE JSON shape (TRN-01 depth decision, review concern #9)"
    - "The six shared palettes decode read-only positionally via a sequential optional-slot state machine mirroring C++ load_0000 — absence of a slot does NOT shift later slots; an absent MGRP leaves palette identity AMBIGUOUS rather than guessed (review concern #3)"
    - "Tier-1 common tags decode to named typed fields ONLY when a complete descriptor exists AND consumed-length == payload-length; otherwise the node is raw-fallback and explicitly NON-EDITABLE (review concern #4)"
    - "Stable-ids are derived from the physical FORM/chunk path INCLUDING raw/dead siblings, so ordinals do not drift across decode modes (review concern #14)"
    - "DEAD/obsolete tags are recognized-and-skipped (display-only), never raw-fallback editable nodes; a DEAD tag adjacent to a typed sibling re-emits exact form framing (review concern #16)"
    - "TerrainDocument exposes Serialize() (IffWriter.Write over the held MutableIffDocument) so Plan 03 roundtrip/apply-save can re-emit (review concern #12)"
  decisions:
    - "D-01: v1 typed decode = the Tier-1 set; everything else is raw-fallback {tag, version, hex}"
    - "D-02: unknown FORM version → raw-fallback the whole chunk (version read FIRST; never guess field offsets)"
    - "D-03: DEAD/obsolete tags are recognized-and-skipped, re-emitted verbatim, never raw-fallback editable nodes"
    - "D-04: six shared palettes read-only, positional sequential-optional state machine (NOT tag lookup); never renumber familyIds"
    - "TRN-01 JSON depth (#9): the decode envelope IS navigable — it exposes the layer tree (layer names + active flags + typed/raw/dead children + palette family names), NOT summary-only counts"
    - "Active-flag read location (#10): TerrainLayer.Active is decoded FROM the int32 active flag on the IHDR layer-item-header leaf under LAYR (RESEARCH :186) — the SAME DOM leaf Plan 03 --field active mutates (read↔write location parity)"
  artifacts:
    - path: "UtinniCoreDotNet/Formats/Terrain/TgenFieldLayouts.cs"
      provides: "the SINGLE-SOURCE per-tag×version binary field-descriptor table (offset/width/signedness/endian/enum-width/display-type/parser/editable) consumed by decoder + encoder"
      contains: "TgenFieldDescriptor"
      min_lines: 80
    - path: "UtinniCoreDotNet/Formats/Decoders/TgenDecoder.cs"
      provides: "version-first dispatch + recursive layer-tree walk + descriptor-driven typed decode + raw-fallback (incl. truncated→non-editable) + positional palette state machine + DEAD-skip + LooksLikeTerrain"
      contains: "LooksLikeTerrain"
      min_lines: 140
    - path: "UtinniCoreDotNet/Formats/Terrain/TerrainDocument.cs"
      provides: "FromBytes/FromIff compose-on-DOM entry holding the MutableIffDocument + Serialize() for the edit/roundtrip path"
      contains: "Serialize"
    - path: "UtinniCoreDotNet/Formats/Terrain/TerrainNode.cs"
      provides: "typed-or-raw leaf node (tag, version, typed fields | IsRawPreserved hex; IsDeadSkipped; IsEditable gate; StableIdPath)"
      contains: "IsEditable"
    - path: "UtinniCoreDotNet/Formats/Terrain/TerrainPalettes.cs"
      provides: "six positional palette DTOs (familyId→name) + per-slot Present/Ambiguous flags, never renumbered on save"
  key_links:
    - from: "UtinniCoreDotNet/Formats/Decoders/TgenDecoder.cs"
      to: "UtinniCoreDotNet/Formats/Terrain/TgenFieldLayouts.cs"
      via: "decoder reads field offsets/widths from the shared descriptor table (no duplicated offset literals)"
      pattern: "TgenFieldLayouts"
    - from: "UtinniCoreDotNet/Formats/Decoders/TgenDecoder.cs"
      to: "UtinniCoreDotNet/Formats/Decoders/IffPayloadCursor.cs"
      via: "bounds-checked LE field reads (Need guard, V5 input validation)"
      pattern: "IffPayloadCursor"
    - from: "UtinniCoreDotNet/Formats/Terrain/TerrainDocument.cs"
      to: "UtinniCoreDotNet/Formats/Iff/IffReader.cs"
      via: "IffReader.Read → MutableIffDocument.FromDocument compose-on-DOM"
      pattern: "IffReader\\.Read"
---

<objective>
Build the read path on a SINGLE-SOURCE binary field-descriptor table: a `TgenFieldLayouts` table
(the reviewers' #2 ask — consumed by BOTH this decoder and Plan-03's encoder), a `TgenDecoder` that
recurses the `TGEN→LYRS→LAYR` tree and typed-decodes the Tier-1 tags THROUGH that table, a
positional palette state machine that mirrors C++ `load_0000` optional-slot semantics (#3), explicit
non-editable gating for raw/truncated nodes (#4), physical-path stable-ids that don't drift (#14),
and the `Terrain/` model (with `Serialize()`, #12). This satisfies criteria 1 + 2 (TRN-01/TRN-02)
with the TRN-01 JSON depth decision made: the envelope is NAVIGABLE, not summary-only (#9).

Purpose: This is the genuinely-new logic of the phase. The descriptor table is what makes Plan-03's
single-field re-encode byte-safe (the reviewers' central correctness concern), so it is built and
offset-parity-tested HERE before any encode exists.
Output: `TgenFieldLayouts` + `TgenDecoder` + six `Terrain/*` files; `TgenDecoderTests` +
`TgenFieldLayoutTests` turn green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Build/test invariant:** MSBuild `Utinni.sln` x86 Release FIRST, then `dotnet test --no-build`.
Never `dotnet build`. `Generated/UtinniCore.cs` NOT touched — `git checkout --` it if reordered.
The reference corpus `D:/Code/swg-client-v2/...` is READ-ONLY — port understanding only, never
`#include`/ProjectReference (DEC-V2-LIFT-SHIFT). Every new file carries the port-understanding header.
</execution_context>

<context>
@.planning/phases/20-terrain-trn-codec-verbs-mcp/20-CONTEXT.md
@.planning/phases/20-terrain-trn-codec-verbs-mcp/20-RESEARCH.md
@.planning/phases/20-terrain-trn-codec-verbs-mcp/20-PATTERNS.md
@.planning/phases/20-terrain-trn-codec-verbs-mcp/20-REVIEWS.md

<interfaces>
<!-- Read mirror to reuse for field reads. Source: UtinniCoreDotNet/Formats/Decoders/IffPayloadCursor.cs -->
IffPayloadCursor: bounds-checked LITTLE-endian payload cursor. ReadInt32Le() :62, ReadFloatLe() :113,
ReadInt8() :94, ReadCString() :136 (NUL-terminated, variable-length — Pitfall 7), Need(n) :155 (throws
DecoderException(Truncated) on over-read — V5 bounds check), Position/Remaining for the consumed-length
check. Payload scalars are LE (:25-27); FORM/chunk framing is BE (handled by IffReader).

<!-- Compose-on-DOM entry idiom + Serialize. Source: Particle/ParticleEffectDocument.cs:49-70 + MutableParticleEffect -->
FromBytes(bytes): IffReader.Read(MemoryStream) → MutableIffDocument.FromDocument(iffDoc, bytes) → decode.
FromIff(iffDoc, sourceBytes): same, no re-read. The document HOLDS the MutableIffDocument; Serialize() is
IffWriter.Write(heldMutable) — clean nodes re-emit captured slices verbatim.

<!-- Stable-id construction. Source: MutableIffDocument.DeriveStableId :161-177 -->
DeriveStableId builds a hierarchical ordinal path over the PHYSICAL DOM (every child, including raw/dead
siblings, occupies an ordinal slot). The decoder must record each TerrainNode's stable-id path from the
same physical walk so ordinals never drift vs the DOM (concern #14).

<!-- Dispatcher sniff + immutable-view DTO shapes. Source: ObjectTemplateDecoder.cs:38-113 -->
ObjectTemplateDecoder: version-form dispatch + raw-fallback + LooksLike* sniff (:103-113) + sealed
constructor-injected get-only view DTOs (:67-81). The structural precedent for TgenDecoder + Terrain models.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TgenFieldLayouts — the single-source binary field-descriptor table (+ Terrain model + Serialize)</name>
  <files>UtinniCoreDotNet/Formats/Terrain/TgenFieldLayouts.cs, UtinniCoreDotNet/Formats/Terrain/TerrainDocument.cs, UtinniCoreDotNet/Formats/Terrain/TerrainLayer.cs, UtinniCoreDotNet/Formats/Terrain/TerrainNode.cs, UtinniCoreDotNet/Formats/Terrain/TerrainPalettes.cs, UtinniCoreDotNet/Formats/Terrain/TerrainParseException.cs, Utinni.Cli.Tests/Terrain/TgenFieldLayoutTests.cs</files>
  <read_first>
    - UtinniCoreDotNet/Formats/Particle/ParticleEffectDocument.cs:24-70 (FromBytes/FromIff dual-entry idiom + provenance header — copy verbatim, retarget) and ParticleParseException.cs (exception shape)
    - UtinniCoreDotNet/Formats/Decoders/ObjectTemplateDecoder.cs:38-81 (ObjectTemplateField/ObjectTemplateView — the sealed, constructor-injected, get-only DTO shape to mirror)
    - UtinniCoreDotNet/Formats/Decoders/IffPayloadCursor.cs:24-28 (provenance header) + :62/:94/:113 (the read widths the descriptor's width/endian fields must match)
    - UtinniCoreDotNet/Formats/Iff/IffWriter.cs (Write — the Serialize() target) + MutableIffNode.cs:255,279 (GetPayloadCopy/SetPayload)
    - 20-RESEARCH.md § "Tag Taxonomy" :176-230 (Tier-1 set + DATA layouts) + § "SWGEmu vs Restoration version dispatch" :247-255 (which fields each version adds) + § Pitfall 5 :319-321
    - swg-client-v2 reference (READ-ONLY, port understanding): AffectorHeight.cpp:159-166 (AHCN = [int32 operation][float height]), AffectorShader.cpp:185 (familyId width), Boundary.cpp:215-273 (BCIR v0000 no feather; v0001+ feather), Filter.cpp (FHGT/FSLP fields)
    - 20-CONTEXT.md D-01/D-04/D-05 + 20-REVIEWS.md Consensus #2 (single-sourced descriptor with width/signedness/enum-width/endian/parser/editable) + #12 (Serialize task)
  </read_first>
  <behavior>
    - TgenFieldLayouts.For(tag, version) returns an ordered list of TgenFieldDescriptor for a Tier-1 tag at a recognized version, or null/empty for an unrecognized (tag,version) — never throws.
    - Each TgenFieldDescriptor exposes Name, Offset, Width, Signed, Endian (Little), EnumStorageWidth (where applicable), DisplayType, EditParser, and Editable.
    - The descriptor for AHCN includes operation (int32 enum, editable) at offset 0 and height (float32, editable) at offset 4; BCIR v0000 has center/radius (no feather), v0001 adds feather descriptors at the correct offsets.
    - The sum of descriptor widths for a (tag,version) equals that version's fixed DATA payload length (so consumed-length == payload-length can be asserted).
    - TerrainDocument.FromBytes(bytes) and FromIff(iffDoc, sourceBytes) both populate the document and hold the MutableIffDocument; Serialize() returns IffWriter.Write(heldMutable) bytes.
    - FromBytes on a non-TGEN root raises TerrainParseException (not a generic throw).
    - A TerrainNode is EITHER typed (Tag, Version, named TypedFields, IsEditable=true) OR IsRawPreserved (Tag, Version, raw hex, IsEditable=false) OR IsDeadSkipped (Tag, "obsolete, ignored", IsEditable=false) — mutually exclusive; and every node carries its physical StableIdPath.
    - TerrainPalettes exposes six familyId→name lists in fixed load order, each with a Present flag and an Ambiguous flag (familyIds preserved verbatim, no renumber).
  </behavior>
  <action>
    Create `UtinniCoreDotNet/Formats/Terrain/TgenFieldLayouts.cs`: a static table keyed by (tag, version)
    returning an ordered `IReadOnlyList<TgenFieldDescriptor>`. `TgenFieldDescriptor` is a sealed get-only
    record of `Name`, `Offset` (int), `Width` (int), `Signed` (bool), `Endian` (enum, Little), an optional
    `EnumStorageWidth` (for enum fields), `DisplayType` (scalar-float / int / enum / active-flag /
    familyId-ref), `EditParser` (how `--value` is parsed for this field), and `Editable` (bool). Populate
    ONLY the Tier-1 set (D-01) at the versions present in `TgenEraVersions` (Plan 01) — port the layouts
    from the reference source (AffectorHeight/AffectorShader/Boundary/Filter), porting UNDERSTANDING only
    (provenance header). This table is the SINGLE source of truth for both the decoder (Task 2) and the
    encoder (Plan 03). Add `TgenFieldLayoutTests.cs` (filter `TgenLayout`) asserting, per (tag,version),
    that the descriptor widths sum to the known fixed payload length and that offsets are contiguous +
    non-overlapping (this is the offset-parity foundation the encoder's parity test in Plan 03 builds on).

    Create the `Terrain/` model (mirrors `Particle/`), each file with the MIT + port-understanding header:
    `TerrainDocument.cs` (copy ParticleEffectDocument FromBytes/FromIff :49-70, retarget to TGEN; HOLD the
    MutableIffDocument; add `Serialize()` = `IffWriter.Write(heldMutable)` — concern #12 — both entries
    delegate tree-build to TgenDecoder in Task 2); `TerrainLayer.cs` (sealed get-only Name/Active/
    IReadOnlyList<TerrainNode> Children); `TerrainNode.cs` (Tag, Version, mutually-exclusive typed /
    IsRawPreserved / IsDeadSkipped, plus an `IsEditable` gate and a `StableIdPath` string — concern #4/#14);
    `TerrainPalettes.cs` (six familyId→name lists in fixed load order, each with Present + Ambiguous flags,
    never renumber — D-04); `TerrainParseException.cs` (model after ParticleParseException). NO edit/save
    logic here beyond Serialize() (the edit verb is Plan 03).
  </action>
  <verify>
    <automated>MSBuild x86 Release succeeds; `dotnet test Utinni.Cli.Tests --no-build --filter TgenLayout` exits 0 — descriptor widths sum to each (tag,version) payload length and offsets are contiguous/non-overlapping; `dotnet test Utinni.Cli.Tests --no-build --filter TgenDecode` compiles + collects (decode assertions may still Skip pending Task 2 wiring)</automated>
  </verify>
  <acceptance_criteria>
    - `TgenFieldLayouts.For(tag, version)` returns the ordered descriptor list for every Tier-1 (tag,version) in `TgenEraVersions`, recording offset/width/signedness/endian/enum-width/display-type/parser/editable per field.
    - `TgenFieldLayoutTests` (non-Skipped) prove descriptor widths sum to the fixed payload length and offsets are contiguous + non-overlapping per (tag,version).
    - `TerrainDocument` exposes `FromBytes` + `FromIff` + `Serialize()` and holds the `MutableIffDocument`.
    - `TerrainNode` enforces mutually-exclusive typed / `IsRawPreserved` / `IsDeadSkipped` states and carries `IsEditable` + `StableIdPath`.
    - `TerrainPalettes` stores six positional familyId→name lists with Present/Ambiguous flags; no familyId renumber path exists.
    - Six `Terrain/*` files + `TgenFieldLayouts.cs` lead with the port-understanding provenance header; solution builds MSBuild x86 Release; `Generated/UtinniCore.cs` unchanged.
  </acceptance_criteria>
  <done>The single-source descriptor table + Terrain model (with Serialize) exist and the offset-parity foundation is proven; the decoder (Task 2) and encoder (Plan 03) consume the SAME table (#2/#12 resolved).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TgenDecoder — descriptor-driven typed decode, palette state machine, non-editable gating, physical-path stable-ids, DEAD-skip</name>
  <files>UtinniCoreDotNet/Formats/Decoders/TgenDecoder.cs, Utinni.Cli.Tests/Terrain/TgenDecoderTests.cs</files>
  <read_first>
    - UtinniCoreDotNet/Formats/Decoders/ObjectTemplateDecoder.cs (version-form dispatch + raw-fallback + LooksLike* sniff :103-113 — the structural precedent)
    - UtinniCoreDotNet/Formats/Decoders/IffPayloadCursor.cs (ReadInt32Le :62 / ReadFloatLe :113 / ReadInt8 :94 / ReadCString :136 / Need :155 + Position/Remaining — reuse for ALL field reads + the consumed-length check)
    - UtinniCoreDotNet/Formats/Terrain/TgenFieldLayouts.cs (Task 1 — the descriptor table the decoder reads offsets/widths from; do NOT hardcode offset literals)
    - UtinniCoreDotNet/Formats/Iff/MutableIffDocument.cs:161-177 (DeriveStableId — derive each node's StableIdPath from the physical walk INCLUDING raw/dead siblings, concern #14)
    - 20-RESEARCH.md § "Tag Taxonomy" :176-230 + § "The Six Shared Palettes" :232-245 (fixed load order, MGRP collision) + § "Common Pitfalls" :299-329 (ALL seven) + § "Code Examples" :333-365
    - swg-client-v2/sharedTerrain reference (READ-ONLY): TerrainGenerator.cpp (load_0000 SEQUENTIAL OPTIONAL enterForm palette order :2174-2212 — mirror the enterForm(TAG,true) state machine EXACTLY; LAYR recursion :1451-1646), TerrainGeneratorLoader.cpp (tag→class dispatch + DEAD tags :68-74,226-264)
    - 20-CONTEXT.md D-01/D-02/D-03/D-04 + 20-REVIEWS.md Consensus #3 (palette absence state machine + ambiguous-not-guessed) + #4 (raw/truncated → non-editable) + #14 (physical-path stable-ids) + #16 (DEAD adjacency invariant) + #17 (negative battery) + #8 (raw-preserved ≠ parsed-field-list)
  </read_first>
  <behavior>
    - Decode MinimalTgen → a TerrainDocument with empty layers + all six palette slots Present=false (optional forms absent, no throw — Pitfall 3).
    - Decode WithAffector("AHCN", SwgEmuEra) AND ("AHCN", RestorationEra) → typed TerrainNode exposing operation (int32 enum) + height (float) named fields for both versions, fields sourced from TgenFieldLayouts (version-first — Pitfall 5).
    - Decode WithBoundary("BCIR", SwgEmuEra=v0000) → typed node WITHOUT feather; ("BCIR", RestorationEra=v0001) → typed node WITH feather (never read a field a version didn't write).
    - Decode WithUnknownTag("ZZZZ") → IsRawPreserved node {tag, version, hex}, IsEditable=false, NO exception (criterion 2 raw-fallback). The raw node is presented as raw-preserved {tag,version,hex}, NOT a parsed field list (concern #8).
    - Decode an unrecognized FORM version of a known tag → IsRawPreserved, IsEditable=false (D-02 whole-chunk raw-fallback).
    - Decode WithTruncatedKnownTag("AHCN") (cut mid-field) → IsRawPreserved, IsEditable=false, NO throw (a known tag whose consumed-length != payload-length falls back and is non-editable — concern #4).
    - Decode WithDeadTag("BALL") → IsDeadSkipped ("obsolete, ignored"), IsEditable=false, NOT raw-fallback (D-03 / Pitfall 2).
    - CompositionalLayer (AHCN + BALL DEAD + ZZZZ unknown + BCIR, adjacent) → all four children decode with NON-DRIFTING StableIdPath ordinals (DEAD/unknown siblings still occupy ordinal slots — concern #14/#16).
    - Palette state machine: a TGEN with BOTH MGRP palettes → fifth slot=Fractal, sixth slot=Bitmap by load-order POSITION (Pitfall 4). A TGEN with only ONE MGRP present → the present MGRP is bound to the EXPECTED next slot in sequence and the palette is marked Ambiguous (fractal-vs-bitmap not guessed — concern #3). Missing an earlier palette slot does NOT shift later slots.
    - A LAYR with sub-LAYR children → the nested layer appears under Children (recursion); each LAYR exposes Name + Active.
    - READ↔WRITE LOCATION PARITY (#10): `TerrainLayer.Active` is decoded FROM the int32 active-flag stored on the `IHDR` layer-item-header leaf under `LAYR` (RESEARCH §Tag Taxonomy :186 — `IHDR` = active flag + name, versions 0000/0001) — the SAME DOM leaf that Plan 03 `--field active` mutates. Decode-read location and edit-write location MUST be the same IHDR leaf so the displayed active flag and the edited bytes never diverge.
    - LAYR version progression (v1 envelope scope): the v1 navigable layer envelope exposes only Name + Active (TRN-01 requires name + active); the decoder version-dispatches LAYR/IHDR and reads ONLY the fields each version actually wrote (LAYR 0000–0004 add invertFilters@v0001 / expanded@v0002 / notes@v0003 progressively — RESEARCH :251; version read FIRST), never reading a field a lower version did not write.
    - Negative battery (concern #17): known tag with trailing bytes → raw-fallback non-editable; unknown version → raw-fallback; missing LYRS → empty layers no throw; missing each palette slot → that slot Present=false, later slots unshifted; malformed CString (empty / no terminator / embedded non-ASCII / long) → bounded read, no over-run into the next chunk.
  </behavior>
  <action>
    Create `TgenDecoder.cs` in `Formats/Decoders/` (static class, namespace
    `UtinniCoreDotNet.Formats.Decoders`) with the MIT + port-understanding header (PATTERNS :40-47).
    Provide `bool LooksLikeTerrain(IffChunk root)` (root `SubTypeId == "TGEN"`) and the main
    `Decode(MutableIffDocument)` / `Decode(IffDocument)` entry returning the populated `TerrainDocument`.
    Implement:
    (1) walk `TGEN → 0000`, deriving each visited node's `StableIdPath` from `DeriveStableId` over the
        PHYSICAL DOM so ordinals include raw/dead siblings and never drift (concern #14);
    (2) decode the six palettes via a SEQUENTIAL OPTIONAL-SLOT STATE MACHINE mirroring C++ `load_0000`
        enterForm(TAG, true) order (shader, flora, radial, environment, fractal, bitmap) — each slot
        optional (absent → Present=false, do NOT shift later slots); the two MGRP slots are disambiguated
        by POSITION not tag (Pitfall 4 / D-04); if only one MGRP is present, bind it to the expected next
        slot in sequence and mark it `Ambiguous` rather than guessing fractal-vs-bitmap (concern #3);
        familyIds preserved verbatim;
    (3) optional `LYRS` → recurse `LAYR*` into `TerrainLayer` (name + active + children), descending into
        sub-`LAYR`; read each LAYR's Active FROM its `IHDR` layer-item-header leaf's int32 active flag (the
        SAME IHDR leaf Plan 03 `--field active` mutates — read↔write location parity, #10);
    (4) for each boundary/filter/affector form, read the FORM version FIRST, then look up
        `TgenFieldLayouts.For(tag, version)`: if a complete descriptor exists, decode each field via
        `IffPayloadCursor` reads AT the descriptor offsets/widths, then assert `cursor.Position` (consumed)
        == payload length — on a match emit a typed node (`IsEditable=true`); on ANY mismatch (truncated,
        trailing bytes, descriptor incomplete) OR a `DecoderException(Truncated)` from `Need`, fall back to
        `RawFallback(tag, version, dataBytes)` with `IsEditable=false` (concern #4); a `default`/unknown
        tag OR an unrecognized version → `RawFallback` (D-02, never throw); the DEAD set
        (`BALL`,`BSPL`,`AHSM`,`AHBM`,`ACBM`,`ASBM`,`AFBM`) → an `IsDeadSkipped` node (D-03, never editable).
    Then fill `TgenDecoderTests.cs`: un-Skip the stubs (incl. the negative battery from Plan 01) and
    implement them against the `TgenFixtureSynthesizer` matrix per `<behavior>` (filters `TgenDecode` for
    navigation/typed/palette/compositional, `TgenRawFallback` for unknown/version/truncated/DEAD).
  </action>
  <verify>
    <automated>MSBuild x86 Release succeeds, THEN `dotnet test Utinni.Cli.Tests --no-build --filter "TgenDecode|TgenRawFallback"` exits 0 with ALL terrain decoder tests passing (navigation, typed Tier-1 both versions, positional palette + single-MGRP-ambiguous, raw-fallback unknown+unknown-version+truncated→non-editable, DEAD-skip non-editable, compositional non-drifting stable-ids, full negative battery)</automated>
  </verify>
  <acceptance_criteria>
    - Every Tier-1 tag in D-01 decodes to named typed fields for BOTH a low and a high version, with fields sourced from `TgenFieldLayouts` (no offset literals in the decoder).
    - A typed node is emitted ONLY when a complete descriptor exists AND consumed-length == payload-length; otherwise `IsRawPreserved` + `IsEditable=false` (truncated AHCN proves this).
    - An unknown FourCC AND an unrecognized version both yield `IsRawPreserved` non-editable nodes with NO exception; raw nodes are presented as raw-preserved {tag,version,hex}, not a parsed field list (#8).
    - A DEAD-set tag yields `IsDeadSkipped` (non-editable); a DEAD tag adjacent to typed siblings re-emits exact framing and the siblings keep non-drifting stable-ids (#14/#16).
    - `TerrainLayer.Active` is decoded FROM the IHDR active-flag int32 under LAYR — the same DOM leaf Plan 03 `--field active` mutates (read↔write location parity, #10).
    - The six palettes decode positionally; with one MGRP present, the palette is marked `Ambiguous` not guessed; absent slots do not shift later slots (#3).
    - The negative battery (#17) all passes; `dotnet test Utinni.Cli.Tests --no-build --filter "TgenDecode|TgenRawFallback"` exits 0.
  </acceptance_criteria>
  <done>The TGEN read path is complete and proven against the full synthesized matrix + negative battery — criteria 1 + 2 (TRN-01/02) hold, and the descriptor-driven / non-editable-gated decode de-risks the Plan-03 encoder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.trn` file bytes → TgenDecoder | untrusted/malformed file payload crosses into the typed decoder; the primary attack surface (V5 input validation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02 | Tampering / DoS | TgenDecoder typed field reads | mitigate | Every field read bounds-checks via `IffPayloadCursor.Need`; a `DecoderException(Truncated)` OR a consumed-length != payload-length mismatch raw-falls-back AND marks the node non-editable, rather than over-reading or exposing a half-understood payload as editable (Pitfall 5 / concern #4). Acceptance: the truncated-AHCN test asserts IsRawPreserved + IsEditable=false. |
| T-20-03 | Tampering | unknown tag / unrecognized version | mitigate | D-02 whole-chunk raw-fallback — version read FIRST, never guess field offsets; unknown tags degrade to {tag, version, hex}, never throw. Acceptance: `TgenRawFallback` green. |
| T-20-04 | Tampering | DEAD-tag re-emit drift | mitigate | DEAD tags recognized-and-skipped (display-only, non-editable), re-emitted verbatim via captured-slice; a DEAD tag adjacent to a typed sibling re-emits exact framing (concern #16). Acceptance: compositional fixture roundtrips byte-identical (Plan 03) + DEAD node non-editable. |
| T-20-04b | Tampering | palette mis-assignment on absence | mitigate | Positional sequential-optional state machine; a single MGRP is marked Ambiguous not guessed (concern #3), so an absent slot cannot silently mislabel fractal-vs-bitmap. Acceptance: single-MGRP test asserts Ambiguous. |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure managed, zero new external packages. No legitimacy gate needed. |
</threat_model>

<verification>
- `dotnet test Utinni.Cli.Tests --no-build --filter "TgenLayout|TgenDecode|TgenRawFallback"` exits 0.
- Descriptor offsets sum/contiguity proven; Tier-1 tags decode typed across both versions; truncated/unknown/unknown-version raw-fallback + non-editable; DEAD-skip non-editable; palettes positional with single-MGRP Ambiguous; stable-ids non-drifting.
- Solution builds with MSBuild x86 Release; `Generated/UtinniCore.cs` unchanged.
</verification>

<success_criteria>
- TRN-01: TGEN tree navigable (layers with name + active, six read-only palettes) exposed in a navigable model (#9 JSON depth realized in Plan 03's envelope over this model).
- TRN-02: Tier-1 typed fields; unknown/long-tail/truncated raw-fallback (non-editable); DEAD-skip — never a hard decode failure.
- The single-source `TgenFieldLayouts` table (offset-parity tested) + non-editable gating de-risk the Plan-03 byte-exact encoder (#2/#4).
</success_criteria>

<output>
Create `.planning/phases/20-terrain-trn-codec-verbs-mcp/20-02-SUMMARY.md` when done
</output>
</content>
</invoke>
