---
phase: 20-terrain-trn-codec-verbs-mcp
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - Utinni.Cli.Tests/Fixtures/trn/TgenFixtureSynthesizer.cs
  - Utinni.Cli.Tests/Fixtures/trn/TgenEraVersions.cs
  - Utinni.Cli.Tests/Terrain/TgenDecoderTests.cs
  - Utinni.Cli.Tests/Terrain/RoundtripTrnTests.cs
  - Utinni.Cli.Tests/Terrain/ApplySaveTrnTests.cs
  - Utinni.Mcp.Tests/Terrain/TerrainReadToolTests.cs
autonomous: false
requirements: [PROD-W2-TRN-04]
must_haves:
  truths:
    - "A fixture synthesizer can hand-emit a ≤200-byte FORM TGEN byte array via IffWriter for EVERY Tier-1 tag at BOTH a low (SWGEmu-era) and a high (Restoration-era) FORM version"
    - "The synthesizer's per-tag version arguments are sourced from named TgenEraVersions.SwgEmuEra / TgenEraVersions.RestorationEra constants so a later wave can PIN them to observed real-asset versions without touching fixture-build code"
    - "Four Skip-marked test stubs exist and are collected by the xUnit runner (TgenDecoder/RoundtripTrn/ApplySaveTrn/TerrainReadTool)"
    - "The CommandLineParser verb-count ceiling is confirmed clean by a TEST-ONLY dispatch-registration assertion (no throwaway no-op verb shipped in Program.cs)"
    - "A maintainer checkpoint records the OBSERVED FORM/tag versions of one real SWGEmu and one real Restoration .trn (or an encrypted/unreachable note) BEFORE the synthesized version constants are treated as facts"
  decisions:
    - "D-11: confirm the trn verb dispatch path registers cleanly through the existing Type[] CommandLineParser dispatch — via a test-only registration assertion, NOT a shipped no-op stub verb (review concern #7)"
    - "D-12: synthesized ≤200-byte TGEN fixtures (hand-emitted via IffWriter) are the committed golden corpus — this plan builds the FULL per-tag low+high synthesizer matrix (review concerns #11/#13)"
    - "D-13: real-asset version DISCOVERY is moved to Wave 0 (this plan) so the synthesized matrix is built from observed facts, not assumed versions (review concern #1)"
  artifacts:
    - path: "Utinni.Cli.Tests/Fixtures/trn/TgenFixtureSynthesizer.cs"
      provides: "IffWriter-based ≤200-byte TGEN fixture builder covering EVERY Tier-1 tag at low+high version, plus minimal/no-palette, unknown-tag, DEAD-tag, truncated, and compositional fixtures"
      contains: "TGEN"
    - path: "Utinni.Cli.Tests/Fixtures/trn/TgenEraVersions.cs"
      provides: "named SwgEmuEra/RestorationEra per-tag version constants (assumed now, pinnable in Wave 3) — the single source feeding the synthesizer's version arguments"
      contains: "RestorationEra"
    - path: "Utinni.Cli.Tests/Terrain/TgenDecoderTests.cs"
      provides: "navigation + typed-field + raw-fallback + DEAD-skip + negative-battery test stubs (Skip until Wave 1)"
    - path: "Utinni.Cli.Tests/Terrain/RoundtripTrnTests.cs"
      provides: "byte-exact whole-file roundtrip stubs over the full version matrix (Skip until Wave 2)"
    - path: "Utinni.Cli.Tests/Terrain/ApplySaveTrnTests.cs"
      provides: "single-field edit + active-toggle + exact-byte-span stubs (Skip until Wave 2)"
    - path: "Utinni.Mcp.Tests/Terrain/TerrainReadToolTests.cs"
      provides: "MCP thin-shell dispatch stub (Skip until Wave 3)"
  key_links:
    - from: "Utinni.Cli.Tests/Fixtures/trn/TgenFixtureSynthesizer.cs"
      to: "Utinni.Cli.Tests/Fixtures/trn/TgenEraVersions.cs"
      via: "synthesizer reads version arguments from the named era constants (pinnable)"
      pattern: "TgenEraVersions"
---

<objective>
Lay the Nyquist scaffold AND resolve the fixture-matrix ordering contradiction the reviewers
flagged (#11/#13): build the COMPLETE per-Tier-1-tag low+high synthesized fixture matrix HERE in
Wave 0 (not deferred to Wave 3), so every downstream plan can honestly pass its own acceptance
criteria in wave order. Source the synthesizer's version arguments from named, pinnable
`TgenEraVersions` constants. Confirm the verb-ceiling cleanly via a TEST-ONLY registration
assertion (no throwaway no-op verb — review concern #7). And move real-asset version DISCOVERY to a
Wave-0 maintainer checkpoint (#1) so the synthesized "both-lineage" matrix is built from observed
facts rather than assumptions BEFORE any DEC-C3 claim.

Purpose: The reviewers' loudest theme is "false byte-exact confidence" — the matrix going green
against assumed versions and the real pin landing last. This plan front-loads both the full matrix
and the real-asset discovery so the false-green window is closed at the source.
Output: A full `TgenFixtureSynthesizer`, the `TgenEraVersions` constants, four Skip stubs, a
test-only verb-registration assertion, and a recorded real-asset version observation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Build/test invariant (AGENTS.md):** Build `Utinni.sln` with VS2026 MSBuild
(`/p:Configuration=Release /p:Platform=x86`), THEN `dotnet test --no-build`. NEVER `dotnet build`
(MSB3823 on WinForms .resx). Worktrees OFF — build inline on the main tree.
`Generated/UtinniCore.cs` is NOT touched this phase (pure managed); if MSBuild reorders it,
`git checkout -- UtinniCoreDotNet/Generated/UtinniCore.cs`.
</execution_context>

<context>
@.planning/phases/20-terrain-trn-codec-verbs-mcp/20-CONTEXT.md
@.planning/phases/20-terrain-trn-codec-verbs-mcp/20-RESEARCH.md
@.planning/phases/20-terrain-trn-codec-verbs-mcp/20-PATTERNS.md
@.planning/phases/20-terrain-trn-codec-verbs-mcp/20-VALIDATION.md
@.planning/phases/20-terrain-trn-codec-verbs-mcp/20-REVIEWS.md

<interfaces>
<!-- Verb registration surface (test-only assertion target). Source: Utinni.Cli/Program.cs:48-107 -->
ParseArguments uses the `Type[]` overload (NOT the generic `ParseArguments<T...>` — the 16-verb
ceiling is solved at 23 verbs). The Wave-0 confirmation that the dispatch path admits a new verb is a
TEST that drives `CommandLine.Parser` over the existing `Type[]` plus one new sentinel options type and
asserts no `BadVerbSelectedError` / registration throw — it does NOT add a verb to `Program.cs` (review
concern #7: the no-op stub registered-then-removed is churn). The real `decode-trn`/`roundtrip-trn`/
`apply-save-trn` verbs are registered in Plan 03.

<!-- IFF emit primitive for the synthesizer. Source: UtinniCoreDotNet/Formats/Iff/IffWriter.cs -->
IffWriter serializes a MutableIffDocument → bytes (BE FORM/chunk framing, NO trailing pad byte at
IffWriter.cs:141). The synthesizer builds a MutableIffDocument tree (FORM TGEN → 0000 → optional
palettes/LYRS → LAYR → typed forms each wrapping a DATA leaf) and calls IffWriter.Write to emit
fixture bytes. DATA-payload scalars are LITTLE-endian (IffPayloadCursor convention); framing is BE.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Full per-tag low+high TGEN fixture synthesizer + pinnable era constants + Skip stubs</name>
  <files>Utinni.Cli.Tests/Fixtures/trn/TgenFixtureSynthesizer.cs, Utinni.Cli.Tests/Fixtures/trn/TgenEraVersions.cs, Utinni.Cli.Tests/Terrain/TgenDecoderTests.cs, Utinni.Cli.Tests/Terrain/RoundtripTrnTests.cs, Utinni.Cli.Tests/Terrain/ApplySaveTrnTests.cs, Utinni.Mcp.Tests/Terrain/TerrainReadToolTests.cs</files>
  <read_first>
    - UtinniCoreDotNet/Formats/Iff/IffWriter.cs (Write entry + no-pad at :141 — the emit primitive the synthesizer reuses)
    - UtinniCoreDotNet/Formats/Iff/MutableIffDocument.cs (FromDocument + how a mutable tree is built/serialized) and MutableIffNode.cs (NewLeaf :192, NewContainer :206, AddLeaf :306, AddContainer :319 — node construction)
    - UtinniCoreDotNet/Formats/Decoders/IffPayloadCursor.cs:24-28 (port-understanding provenance header to replicate) + :25-27 (LE payload convention the synthesizer must match)
    - Utinni.Cli.Tests/Fixtures/iff/ (existing committed synthesized .iff fixture convention to mirror under Fixtures/trn/)
    - 20-RESEARCH.md § "Tag Taxonomy" :176-230 (the FULL Tier-1 tag set D-01) + § "The Six Shared Palettes" :232-245 (positional load order, MGRP collision) + § "SWGEmu vs Restoration version dispatch" :247-255 (LAYR 0000-0004, ShaderGroup 0000-0006, FloraGroup 0001-0008, BCIR v0000-no-feather vs v0001+-feather) + § Wave 0 Gaps :444-451
    - 20-CONTEXT.md D-12 (synthesized ≤200-byte matrix) + D-01 (full Tier-1 set) + D-03 (DEAD tag set) + D-04 (six palettes)
    - 20-REVIEWS.md Consensus #11/#13 (move the per-tag low/high matrix to Wave 0/early-Wave-1) + #16/#17 (compositional + negative fixtures)
  </read_first>
  <behavior>
    - TgenEraVersions exposes SwgEmuEra and RestorationEra lookups returning a per-tag FORM version (e.g. SwgEmuEra["LAYR"], RestorationEra["LAYR"], same for AHCN/BCIR/SGRP/FGRP/...). Values are the CURRENTLY-ASSUMED versions, documented as "pinned in Plan 04 Task 3 against observed real assets."
    - TgenFixtureSynthesizer.MinimalTgen() returns a ≤200-byte FORM TGEN (version 0000) with NO palettes and NO LYRS (legal per Pitfall 3 optional forms).
    - For EVERY Tier-1 tag in D-01 (AHCN, AHTR, ACCN, ACRH, ASCN, ASRP, AFCN, AFSC, AFSN, BCIR, BREC, FHGT, FSLP), the synthesizer can emit BOTH a low-version fixture (version = TgenEraVersions.SwgEmuEra[tag]) and a high-version fixture (version = TgenEraVersions.RestorationEra[tag]) — the full both-lineage matrix.
    - WithBoundary("BCIR", SwgEmuEra) emits a v0000 BoundaryCircle WITHOUT feather; WithBoundary("BCIR", RestorationEra) emits a feather-bearing version (proves the version-divergence path).
    - WithUnknownTag("ZZZZ") emits a leaf with a FourCC outside the Tier-1 set (raw-fallback target).
    - WithDeadTag("BALL") emits a DEAD-tag form (recognized-and-skipped target).
    - WithTruncatedKnownTag("AHCN") emits a known-tag DATA leaf cut off mid-field (truncated raw-fallback / non-editable target — concern #4/#17).
    - CompositionalLayer() emits ONE LAYR containing, in order, an AHCN typed form, a BALL DEAD tag, a ZZZZ unknown tag, and a BCIR typed form (the adjacency fixture for concern #16).
    - Every emitted fixture re-parses through IffReader without throwing and is ≤200 bytes (committed-golden budget).
  </behavior>
  <action>
    Create `Utinni.Cli.Tests/Fixtures/trn/TgenEraVersions.cs`: two static read-only dictionaries
    `SwgEmuEra` and `RestorationEra` mapping each Tier-1 tag + each palette tag + `LAYR` to a FORM
    version `ushort`/`string`. Seed them with the assumed low/high versions from RESEARCH :247-255 (low =
    SWGEmu-era, high = Restoration-era), and add a class-level comment: "ASSUMED pending Plan 04 Task 3
    real-asset pin (D-13/D-14) — do not treat as confirmed facts until pinned." These are the SINGLE
    source the synthesizer reads, so Plan 04 pins versions by editing ONLY this file.

    Create `Utinni.Cli.Tests/Fixtures/trn/TgenFixtureSynthesizer.cs`: a static helper that builds FORM
    TGEN byte arrays by composing a `MutableIffDocument` tree (FORM TGEN → version-`0000` FORM → optional
    palette FORMs in the fixed load order shader/flora/radial/environment/fractal/bitmap → optional `LYRS`
    → `LAYR` → boundary/filter/affector forms each wrapping a `DATA` leaf) and emitting via
    `IffWriter.Write`. Expose the matrix builders named in `<behavior>` — `MinimalTgen`, a generic
    `WithTypedTag(tag, version, fields)`, plus convenience `WithAffector`/`WithBoundary`/`WithFilter`,
    `WithUnknownTag`, `WithDeadTag`, `WithTruncatedKnownTag`, and `CompositionalLayer` — each reading its
    version from `TgenEraVersions` where a low/high pair is needed. DATA payload scalars are LITTLE-endian
    (per IffPayloadCursor convention); FORM/chunk framing is big-endian via IffWriter. Keep every emitted
    fixture ≤200 bytes. Include a self-test method that asserts each emitted fixture re-parses via
    IffReader without throwing AND is ≤200 bytes (a non-Skipped synthesizer self-test). Replicate the
    port-understanding provenance header (IffPayloadCursor.cs:24-28).

    Create four xUnit test classes — `Utinni.Cli.Tests/Terrain/{TgenDecoderTests,RoundtripTrnTests,
    ApplySaveTrnTests}.cs` and `Utinni.Mcp.Tests/Terrain/TerrainReadToolTests.cs`. Each test method
    carries `[Fact(Skip = "Wave N — pending <component>")]` (or `[Theory(Skip=...)]` for the matrix) with
    `[Trait]`/naming so the VALIDATION filters resolve: TgenDecoder* (filter `TgenDecode`/`TgenRawFallback`),
    Roundtrip* (filter `RoundtripTrn`), ApplySave* (filter `ApplySaveTrn`), MCP class (filter `Terrain`).
    Stub one method per VALIDATION § Per-Task Verification Map row PLUS the negative-battery rows from
    review concern #17 (truncated known tag, known tag with trailing bytes, unknown version, missing LYRS,
    missing each palette slot, only one MGRP, DEAD adjacent to edited sibling, malformed CString) as
    `// TODO Wave N` Skip stubs so the count is visible. The Skip stubs MUST compile and be COLLECTED by
    the runner, not absent.
  </action>
  <verify>
    <automated>After MSBuild x86 Release: `dotnet test Utinni.Cli.Tests --no-build --filter "TgenDecode|TgenRawFallback|RoundtripTrn|ApplySaveTrn"` exits 0 with the terrain stubs reported as Skipped (collected, not failed) AND the synthesizer self-test (NON-Skipped) passing — every Tier-1 tag emits a low+high ≤200-byte fixture that re-parses; `dotnet test Utinni.Mcp.Tests --no-build --filter Terrain` exits 0 with the MCP stub Skipped</automated>
  </verify>
  <acceptance_criteria>
    - `TgenEraVersions` exposes `SwgEmuEra` + `RestorationEra` per-tag version maps covering every Tier-1 tag, every palette tag, and `LAYR`, with the "ASSUMED pending pin" comment.
    - `TgenFixtureSynthesizer` emits, for EVERY Tier-1 tag in D-01, both a low-version and a high-version ≤200-byte fixture (synthesizer self-test enumerates them and asserts byte-length ≤ 200 + re-parse).
    - The synthesizer additionally emits Minimal, WithUnknownTag, WithDeadTag, WithTruncatedKnownTag, and CompositionalLayer fixtures.
    - All four test files compile and are collected by xUnit; the terrain methods (including the negative-battery rows) report Skipped (not Failed, not absent) under their VALIDATION filters.
    - The synthesizer file carries the port-understanding provenance header.
    - `dotnet test Utinni.Cli.Tests --no-build` and `dotnet test Utinni.Mcp.Tests --no-build` both exit 0.
  </acceptance_criteria>
  <done>The FULL both-lineage fixture matrix + pinnable era constants + four Skip stubs exist; downstream plans can honestly assert against the complete matrix in wave order (#11/#13 resolved).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test-only verb-registration assertion (D-11 ceiling smoke, no shipped stub verb)</name>
  <files>Utinni.Cli.Tests/Terrain/TgenDecoderTests.cs</files>
  <read_first>
    - Utinni.Cli/Program.cs:48-107 (the Type[] ParseArguments list + Dispatch switch — the registration surface the test exercises WITHOUT modifying)
    - Utinni.Cli/Commands/RoundtripParticleCommand.cs:35-40 (Verb attribute + Options class shape — the shape the sentinel options type mirrors)
    - 20-CONTEXT.md D-11 (the ceiling is already solved at 23 verbs; this is a smoke check, NOT new infrastructure)
    - 20-REVIEWS.md Consensus #7 (drop the Wave-0 no-op stub verb — smoke via a test-only registration instead)
  </read_first>
  <behavior>
    - A test constructs a `Type[]` equal to the production verb-options set PLUS a single sentinel `[Verb("trn-smoke", Hidden=true)]` options type, drives `CommandLine.Parser.Default.ParseArguments(args, types)` with `new[] { "trn-smoke" }`, and asserts the result is NOT a `NotParsed` carrying a `BadVerbSelectedError` (i.e. the dispatch path admits an additional verb past the existing 23 without a registration throw).
    - The test references NO new production type in Program.cs — it proves the ceiling using a test-local sentinel options class only.
  </behavior>
  <action>
    Add a non-Skipped `[Fact]` `VerbDispatch_AdmitsAdditionalTrnVerb_NoCeilingError` to
    `TgenDecoderTests.cs` (filter `TgenDecode`). Define a test-local `[Verb("trn-smoke", Hidden = true)]`
    options class with a single optional flag. Build the `Type[]` the same way `Program.cs:48-71` does
    (the production verb-options types) and append the sentinel type; call
    `CommandLine.Parser.Default.ParseArguments(new[] { "trn-smoke" }, types)` and assert the parse does
    NOT yield a `BadVerbSelectedError` (verb is recognized) — proving D-11's "registers cleanly" without
    shipping a throwaway verb in `Program.cs`. This replaces the previously-planned no-op `trn` stub verb
    (review concern #7 — registered-then-removed churn). Do NOT modify `Program.cs` in this plan.
  </action>
  <verify>
    <automated>MSBuild x86 Release succeeds, THEN `dotnet test Utinni.Cli.Tests --no-build --filter TgenDecode` runs the verb-registration fact GREEN (not Skipped) — the dispatch path admits an additional trn verb with no BadVerbSelectedError; no Program.cs diff in this plan</automated>
  </verify>
  <acceptance_criteria>
    - A non-Skipped `[Fact]` proves an additional `trn-smoke` verb parses through the existing `Type[]` dispatch with no `BadVerbSelectedError` (D-11 ceiling smoke).
    - `Program.cs` is UNCHANGED by this plan (no shipped no-op verb — concern #7).
    - `dotnet test Utinni.Cli.Tests --no-build --filter TgenDecode` exits 0 with this fact reported Passed.
  </acceptance_criteria>
  <done>The verb-ceiling is confirmed clean by a test-only assertion with zero Program.cs churn (#7 resolved).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Real-asset version DISCOVERY (Wave 0) — observe SWGEmu + Restoration .trn FORM versions BEFORE the matrix is trusted</name>
  <action>Using Utinni's already-shipped TRE verbs (Phase 12/13), extract one small/simple planet's terrain/<planet>.trn from EACH live client and record the OBSERVED FORM/tag versions in a NON-committed note, so the Plan-01 TgenEraVersions constants are grounded in facts (or flagged assumed) BEFORE Wave 1 builds against them. See what-built/how-to-verify below.</action>
  <what-built>
    Plan 01 Task 1 seeded `TgenEraVersions` with ASSUMED low/high versions from the reference source.
    Review concern #1 (both reviewers, HIGH) is that the both-lineage gate can go green against invented
    versions. To close the false-green window at the SOURCE, this checkpoint observes the REAL shipped
    FORM versions NOW (Wave 0) — not as a post-hoc Wave-3 sign-off. The maintainer has the live client
    archives (Utinni cannot self-serve the asset). The observation feeds the assumed constants so Wave 1
    builds against grounded numbers; the final pin/confirm still happens in Plan 04 Task 3, but the
    discovery is front-loaded here.
  </what-built>
  <how-to-verify>
    1. Pick a small/simple planet. Extract its terrain from EACH client via Utinni's OWN already-shipped
       CLI, e.g. `utinni-cli parse-tre --extract terrain/<planet>.trn` against the SWGEmu archive, then
       the Restoration archive (dogfooding the revived TRE verbs — D-13).
    2. If the Restoration archive is v6000+ ENCRYPTED and the `.trn` is unreachable, the TRE verb MUST
       report that clearly (enumerate-only) and you note "Restoration .trn encrypted/unreachable —
       synthesized RestorationEra constants stay assumed" (review concern #15). Do NOT attempt a decode
       on an encrypted payload.
    3. For each extracted `.trn`, inspect the FORM versions with any available IFF dump (the terrain
       decoder does not exist yet — a generic `decode-iff` structural dump or a hex peek at the FORM
       version words is sufficient at this stage). Record observed TGEN/LAYR/palette/Tier-1-tag versions
       in a NON-committed scratch note (real assets stay OUT of the committed corpus — D-14).
    4. Report the observed SWGEmu-vs-Restoration version pair (or "encrypted/unreachable") so the executor
       can update `TgenEraVersions` to the observed numbers (or annotate them as still-assumed) before
       Wave 1 begins.
  </how-to-verify>
  <resume-signal>
    Reply with the observed per-tag SWGEmu vs Restoration FORM version pair (or "Restoration .trn
    encrypted/unreachable — RestorationEra stays assumed"). The executor then updates `TgenEraVersions`
    to the observed values (or annotates assumed) so Wave 1 builds against grounded facts. The final
    DEC-C3 pin/confirm is Plan 04 Task 3.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → IffReader | synthesized byte arrays cross into the parser; even fixtures must re-parse cleanly (proves the synthesizer emits well-framed IFF) |
| real client TRE archive → utinni-cli TRE verbs | the maintainer-supplied real `.trn` crosses the extractor; encrypted v6000+ payloads must fail clearly (enumerate-only), never silently decode garbage (concern #15) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | TgenFixtureSynthesizer emit framing | mitigate | Self-test asserts every emitted fixture re-parses via IffReader without throwing AND is ≤200 bytes — a mis-framed fixture would mask real codec bugs downstream. Test-only code, no production attack surface. |
| T-20-01b | Tampering / Info-disclosure | encrypted Restoration TRE during real-asset discovery | mitigate | The TRE verb reports v6000+ payloads as enumerate-only and the checkpoint records "encrypted/unreachable" rather than attempting a decode (concern #15). No decode on an encrypted blob. |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs this phase — pure managed, zero new external packages. No legitimacy gate needed. |
</threat_model>

<verification>
- The synthesizer self-test (non-Skipped) proves every Tier-1 tag emits a low+high ≤200-byte re-parsing fixture.
- The four terrain test files are collected by xUnit and report Skipped (negative-battery rows included).
- The verb-registration fact is GREEN; `Program.cs` is unchanged.
- The real-asset discovery checkpoint records the observed (or encrypted/unreachable) version pair.
- `dotnet test Utinni.Cli.Tests --no-build` + `dotnet test Utinni.Mcp.Tests --no-build` both exit 0.
</verification>

<success_criteria>
- The FULL both-lineage synthesized fixture matrix (low+high per Tier-1 tag) is built in Wave 0 (#11/#13).
- Version arguments are sourced from pinnable `TgenEraVersions` constants (single source for the Wave-3 pin).
- The verb-ceiling is confirmed via a test-only assertion with zero Program.cs churn (#7).
- Real-asset FORM versions are OBSERVED in Wave 0 so the matrix is grounded before Wave 1 (#1, #15).
- Four Skip-marked stubs (incl. the negative battery) exist so no downstream wave has a 3-task gap.
</success_criteria>

<output>
Create `.planning/phases/20-terrain-trn-codec-verbs-mcp/20-01-SUMMARY.md` when done
</output>
</content>
</invoke>
