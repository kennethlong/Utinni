---
phase: 20-terrain-trn-codec-verbs-mcp
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - Utinni.Cli/Commands/TrnVerbStubCommand.cs
  - Utinni.Cli/Program.cs
  - Utinni.Cli.Tests/Fixtures/trn/TgenFixtureSynthesizer.cs
  - Utinni.Cli.Tests/Terrain/TgenDecoderTests.cs
  - Utinni.Cli.Tests/Terrain/RoundtripTrnTests.cs
  - Utinni.Cli.Tests/Terrain/ApplySaveTrnTests.cs
  - Utinni.Mcp.Tests/Terrain/TerrainReadToolTests.cs
autonomous: true
requirements: [PROD-W2-TRN-04]
must_haves:
  truths:
    - "`utinni-cli --help` enumerates a `trn` verb and parses it without a CommandLineParser registration error"
    - "A fixture synthesizer can hand-emit a ≤200-byte FORM TGEN byte array via IffWriter"
    - "Four Skip-marked test stubs exist and are collected by the xUnit runner (TgenDecoder/RoundtripTrn/ApplySaveTrn/TerrainReadTool)"
  decisions:
    - "D-11: confirm the `trn` verb registers cleanly through the existing Type[] CommandLineParser dispatch (Wave-0 smoke, not new infra)"
    - "D-12: synthesized ≤200-byte TGEN fixtures (hand-emitted via IffWriter) are the committed golden corpus — this plan builds the synthesizer"
  artifacts:
    - path: "Utinni.Cli/Commands/TrnVerbStubCommand.cs"
      provides: "no-op `trn` verb proving clean registration past the 23-verb Type[] dispatch (D-11)"
      contains: "Verb(\"trn\""
    - path: "Utinni.Cli.Tests/Fixtures/trn/TgenFixtureSynthesizer.cs"
      provides: "IffWriter-based ≤200-byte TGEN fixture builder feeding the version matrix"
      contains: "TGEN"
    - path: "Utinni.Cli.Tests/Terrain/TgenDecoderTests.cs"
      provides: "navigation + typed-field + raw-fallback test stubs (Skip until Wave 1)"
    - path: "Utinni.Cli.Tests/Terrain/RoundtripTrnTests.cs"
      provides: "byte-exact whole-file roundtrip stubs (Skip until Wave 2)"
    - path: "Utinni.Cli.Tests/Terrain/ApplySaveTrnTests.cs"
      provides: "single-field edit + active-toggle stubs (Skip until Wave 2)"
    - path: "Utinni.Mcp.Tests/Terrain/TerrainReadToolTests.cs"
      provides: "MCP thin-shell dispatch stub (Skip until Wave 3)"
  key_links:
    - from: "Utinni.Cli/Program.cs"
      to: "Commands.TrnVerbStubCommand"
      via: "Type[] ParseArguments + Dispatch switch (D-11)"
      pattern: "TrnVerbStubOptions"
---

<objective>
Lay the Nyquist scaffold for the whole phase: prove the new `trn` verb registers cleanly
through the existing `Type[]` CommandLineParser dispatch (criterion 4 smoke check, D-11),
build the synthesized-fixture generator (the committed golden corpus per D-12), and create the
four Skip-marked test stubs so every downstream task has a failing-until-implemented automated
verify (no 3-consecutive-tasks-without-automated-verify gap).

Purpose: Without this wave, the codec/verb/MCP tasks would have no fixtures to assert against and
no test files to fill in — the Nyquist gate would be unsatisfiable. This wave creates the test
surface FIRST so implementation tasks turn Skip→green.
Output: A no-op `trn` verb, a `TgenFixtureSynthesizer`, and four Skip stubs collected by xUnit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Build/test invariant (AGENTS.md):** Build `Utinni.sln` with VS2026 MSBuild
(`/p:Configuration=Release /p:Platform=x86`), THEN `dotnet test --no-build`. NEVER `dotnet build`
(MSB3823 on WinForms .resx). Worktrees OFF — build inline on the main tree.
`Generated/UtinniCore.cs` is NOT touched this phase (pure managed); if MSBuild reorders it,
`git checkout -- UtinniCoreDotNet/Generated/UtinniCore.cs`.
</execution_context>

<context>
@.planning/phases/20-terrain-trn-codec-verbs-mcp/20-CONTEXT.md
@.planning/phases/20-terrain-trn-codec-verbs-mcp/20-RESEARCH.md
@.planning/phases/20-terrain-trn-codec-verbs-mcp/20-PATTERNS.md
@.planning/phases/20-terrain-trn-codec-verbs-mcp/20-VALIDATION.md

<interfaces>
<!-- Verb registration surface the executor extends. Source: Utinni.Cli/Program.cs:48-107 -->
ParseArguments uses the `Type[]` overload (NOT the generic `ParseArguments<T...>` — the 16-verb
ceiling is solved). To add a verb: append `typeof(Commands.XOptions)` to the Type[] list
(Program.cs:49-71) AND a `case Commands.XOptions o: return Commands.XCommand.Run(o);` to the
Dispatch switch (Program.cs:82-106). Currently 23 verbs registered.

<!-- IFF emit primitive for the synthesizer. Source: UtinniCoreDotNet/Formats/Iff/IffWriter.cs -->
IffWriter serializes a MutableIffDocument → bytes (BE FORM/chunk framing, NO trailing pad byte at
IffWriter.cs:141). The synthesizer builds a MutableIffDocument tree (FORM TGEN → 0000 → optional
palettes/LYRS) and calls IffWriter.Write to emit fixture bytes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: No-op `trn` verb + clean-registration smoke (D-11, criterion 4)</name>
  <files>Utinni.Cli/Commands/TrnVerbStubCommand.cs, Utinni.Cli/Program.cs</files>
  <read_first>
    - Utinni.Cli/Program.cs:48-107 (the Type[] ParseArguments list + Dispatch switch — the exact two-line-per-verb registration site, D-11)
    - Utinni.Cli/Commands/RoundtripParticleCommand.cs:35-40 (Verb attribute + Options class shape to mirror)
    - 20-CONTEXT.md D-11 (the ceiling is already solved; this is a smoke check, NOT new infrastructure)
    - 20-PATTERNS.md § "Utinni.Cli/Program.cs (modify — register verbs)" :210-221
  </read_first>
  <action>
    Create `TrnVerbStubCommand.cs` in `Utinni.Cli/Commands/` with the standard MIT header block
    (copy the header from `RoundtripParticleCommand.cs:1-24`). Define a `[Verb("trn", Hidden = true,
    HelpText = "...")] TrnVerbStubOptions` class with a single optional `--ping` flag, and a static
    `TrnVerbStubCommand.Run(TrnVerbStubOptions)` that emits a trivial `JsonOutput.EmitSuccess("trn",
    new { ok = true })` and returns exit 0. Register it: append `typeof(Commands.TrnVerbStubOptions)`
    to the `Type[]` in `Program.cs` (after `ValidateBundleOptions` at :71) and add
    `case Commands.TrnVerbStubOptions o: return Commands.TrnVerbStubCommand.Run(o);` to the Dispatch
    switch (after the last case ~:106). This is the Wave-0 registration smoke required by criterion 4
    ("confirm the CommandLineParser verb-count ceiling registers cleanly first"); the real
    `decode-trn`/`roundtrip-trn`/`apply-save-trn` verbs replace/supplement it in Wave 2. Keep the stub
    `Hidden = true` so it does not pollute the public help surface; it still appears under `--help`
    enumeration for the smoke assertion.
  </action>
  <verify>
    <automated>MSBuild Utinni.sln /p:Configuration=Release /p:Platform=x86 succeeds, THEN `utinni-cli --help` exit 0 and stdout contains "trn", AND `utinni-cli trn --ping` exit 0 — no CommandLineParser registration exception</automated>
  </verify>
  <acceptance_criteria>
    - `Utinni.Cli/Commands/TrnVerbStubCommand.cs` exists with `[Verb("trn"` and the MIT header.
    - `Program.cs` Type[] list contains `typeof(Commands.TrnVerbStubOptions)` and the Dispatch switch has its `case`.
    - The solution builds with MSBuild x86 Release with no CS errors.
    - `utinni-cli --help` exits 0 and its output enumerates the `trn` verb (no `BadVerbSelectedError`/registration throw).
  </acceptance_criteria>
  <done>The `trn` verb registers and parses cleanly through the existing Type[] dispatch — criterion 4's verb-ceiling smoke is satisfied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TGEN fixture synthesizer + four Skip-marked test stubs (Nyquist scaffold)</name>
  <files>Utinni.Cli.Tests/Fixtures/trn/TgenFixtureSynthesizer.cs, Utinni.Cli.Tests/Terrain/TgenDecoderTests.cs, Utinni.Cli.Tests/Terrain/RoundtripTrnTests.cs, Utinni.Cli.Tests/Terrain/ApplySaveTrnTests.cs, Utinni.Mcp.Tests/Terrain/TerrainReadToolTests.cs</files>
  <read_first>
    - UtinniCoreDotNet/Formats/Iff/IffWriter.cs (Write entry + no-pad at :141 — the emit primitive the synthesizer reuses)
    - UtinniCoreDotNet/Formats/Iff/MutableIffDocument.cs (FromDocument + how a mutable tree is built/serialized) and MutableIffNode.cs (node construction)
    - Utinni.Cli.Tests/Fixtures/iff/ (existing committed synthesized .iff fixture convention to mirror under Fixtures/trn/)
    - 20-RESEARCH.md § "Tag Taxonomy" :176-230 (the Tier-1 tag set D-01) + § "The Six Shared Palettes" :232-245 (positional load order, MGRP collision) + § Wave 0 Gaps :439-445
    - 20-CONTEXT.md D-12 (synthesized ≤200-byte matrix: low-version SWGEmu-era + high-version Restoration-era per Tier-1 tag, plus minimal/no-palette, unknown-tag raw-fallback, DEAD-tag skip) + D-03 (DEAD tag set)
  </read_first>
  <behavior>
    - TgenFixtureSynthesizer.MinimalTgen() returns a ≤200-byte FORM TGEN (version 0000) with NO palettes and NO LYRS (legal per Pitfall 3 optional forms).
    - TgenFixtureSynthesizer.WithAffector("AHCN", version, ...) emits a TGEN containing a LYRS→LAYR→AHCN→DATA leaf packing [int32 operation][float height] (the AffectorHeightConstant layout from RESEARCH).
    - TgenFixtureSynthesizer.WithBoundary("BCIR", version, ...) emits a Tier-1 BoundaryCircle.
    - TgenFixtureSynthesizer.WithUnknownTag("ZZZZ") emits a leaf with a FourCC outside the Tier-1 set (raw-fallback target).
    - TgenFixtureSynthesizer.WithDeadTag("BALL") emits a DEAD-tag form (recognized-and-skipped target).
    - Every emitted fixture re-parses through IffReader without throwing and is ≤200 bytes.
  </behavior>
  <action>
    Create `Utinni.Cli.Tests/Fixtures/trn/TgenFixtureSynthesizer.cs`: a static helper that builds
    FORM TGEN byte arrays by composing a `MutableIffDocument` tree (FORM TGEN → version-`0000` FORM →
    optional palette FORMs in the fixed load order shader/flora/radial/environment/fractal/bitmap →
    optional `LYRS` → `LAYR` → boundary/filter/affector forms each wrapping a `DATA` leaf) and emitting
    via `IffWriter.Write`. Expose the matrix builders named in `<behavior>` (Minimal, WithAffector,
    WithBoundary, WithUnknownTag, WithDeadTag), each parameterized by FORM version so a low ("SWGEmu-era")
    and high ("Restoration-era") version of each Tier-1 tag can be generated (D-12). DATA payload scalars
    are LITTLE-endian (per IffPayloadCursor convention); FORM/chunk framing is big-endian via IffWriter.
    Keep every fixture ≤200 bytes. Include a self-test method (or assert in the stub) that each emitted
    fixture re-parses via IffReader without throwing. Replicate the port-understanding provenance header
    (see IffPayloadCursor.cs:24-28) since the TGEN layout is understood from the reference corpus.

    Create four xUnit test classes — `Utinni.Cli.Tests/Terrain/{TgenDecoderTests,RoundtripTrnTests,
    ApplySaveTrnTests}.cs` and `Utinni.Mcp.Tests/Terrain/TerrainReadToolTests.cs`. Each test method
    carries `[Fact(Skip = "Wave N — pending <component>")]` with `[Trait]`/naming so the VALIDATION
    filters resolve: TgenDecoder* (filter `TgenDecode`/`TgenRawFallback`), Roundtrip* (filter
    `RoundtripTrn`), ApplySave* (filter `ApplySaveTrn`), and the MCP class (filter `Terrain`). Stub one
    method per VALIDATION § Per-Task Verification Map row (navigate-tree, typed+raw-fallback+DEAD-skip,
    byte-exact field edit + active toggle, MCP thin-shell dispatch), each with a `// TODO Wave N` body and
    an `Assert` shape that documents the intended assertion. The Skip stubs MUST compile and be COLLECTED
    by the runner (so the count is visible), not absent.
  </action>
  <verify>
    <automated>After MSBuild x86 Release: `dotnet test Utinni.Cli.Tests --no-build --filter "TgenDecode|TgenRawFallback|RoundtripTrn|ApplySaveTrn"` exits 0 with the terrain stubs reported as Skipped (collected, not failed); `dotnet test Utinni.Mcp.Tests --no-build --filter Terrain` exits 0 with the MCP stub Skipped</automated>
  </verify>
  <acceptance_criteria>
    - `TgenFixtureSynthesizer` emits each named fixture as a ≤200-byte array that re-parses via `IffReader` without throwing (assert byte-length ≤ 200 in a non-Skipped synthesizer self-test).
    - All four test files compile and are collected by xUnit; the terrain methods report Skipped (not Failed, not absent) under their VALIDATION filters.
    - The synthesizer file carries the port-understanding provenance header.
    - `dotnet test Utinni.Cli.Tests --no-build` and `dotnet test Utinni.Mcp.Tests --no-build` both exit 0 (no compile break, no red).
  </acceptance_criteria>
  <done>The fixture generator + four Skip stubs exist and are collected; every downstream codec/verb/MCP task now has a fixture to assert against and a test to turn green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → IffReader | synthesized byte arrays cross into the parser; even fixtures must re-parse cleanly (proves the synthesizer emits well-framed IFF) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | TgenFixtureSynthesizer emit framing | mitigate | Self-test asserts every emitted fixture re-parses via IffReader without throwing — a mis-framed fixture would mask real codec bugs downstream. No production attack surface in this wave (test-only code). |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs this phase — pure managed, zero new external packages (RESEARCH § Standard Stack "adds no new external packages"). No legitimacy gate needed. |
</threat_model>

<verification>
- `utinni-cli --help` enumerates the `trn` verb; `utinni-cli trn --ping` exits 0 (registration smoke).
- The four terrain test files are collected by xUnit and report Skipped (not absent / not Failed).
- `dotnet test Utinni.Cli.Tests --no-build` + `dotnet test Utinni.Mcp.Tests --no-build` both exit 0.
</verification>

<success_criteria>
- The `trn` verb registers cleanly through the existing Type[] dispatch (criterion 4 ceiling smoke).
- `TgenFixtureSynthesizer` produces the ≤200-byte fixture matrix (D-12) and each fixture re-parses.
- Four Skip-marked stubs exist for TRN-01..04 so no downstream wave has a 3-task automated-verify gap.
</success_criteria>

<output>
Create `.planning/phases/20-terrain-trn-codec-verbs-mcp/20-01-SUMMARY.md` when done
</output>
