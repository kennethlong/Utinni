---
phase: 20-terrain-trn-codec-verbs-mcp
plan: 03
type: execute
wave: 2
depends_on: ["20-02"]
files_modified:
  - UtinniCoreDotNet/Formats/Terrain/TrnFieldEncoder.cs
  - Utinni.Cli/Commands/DecodeTrnCommand.cs
  - Utinni.Cli/Commands/RoundtripTrnCommand.cs
  - Utinni.Cli/Commands/ApplySaveTrnCommand.cs
  - Utinni.Cli/Commands/DecodeIffCommand.cs
  - Utinni.Cli/Program.cs
  - Utinni.Cli.Tests/Terrain/RoundtripTrnTests.cs
  - Utinni.Cli.Tests/Terrain/ApplySaveTrnTests.cs
  - Utinni.Cli.Tests/Terrain/TrnVerbGoldenTests.cs
autonomous: true
requirements: [PROD-W2-TRN-03, PROD-W2-TRN-04]
must_haves:
  truths:
    - "TrnFieldEncoder consumes the SAME TgenFieldLayouts table the decoder uses (no duplicated offsets); an offset-parity test asserts decoder offsets == encoder offsets per (tag,version) (review concern #2)"
    - "apply-save-trn resolves the (tag, version) of a --leaf stable-id by walking the parent chain (ResolveFieldContext) — it does NOT assume the leaf's own FORM carries them (review concern #10)"
    - "The active-flag edit target is DEFINED in one decision block: --field active mutates the int32 active flag on the IHDR leaf under LAYR, located via ResolveFieldContext (review concern #10)"
    - "A single-field edit produces output that differs ONLY in the target field's exact byte span (width-specific replacement, asserted), not a wholesale reserialization (review concern #2)"
    - "Untouched float fields preserve exact bits (no parse/format canonicalization); --value float input format is defined and NaN/Infinity is rejected (review concern #6)"
    - "decode-iff TGEN branch + decode-trn alias emit a NAVIGABLE terrain envelope (layer tree + names + active + typed/raw/dead children + palette family names), schema-tested; roundtrip-trn + apply-save-trn have xUnit CLI golden tests (review concerns #5, #9, #18)"
    - "apply-save-trn --field active is round-trip-verified end-to-end: toggling active then re-decoding asserts TerrainLayer.Active reflects the edit, proving the read location (Plan 02 IHDR leaf) == the write location (review concern #10)"
    - "The DEC-C3 codec-level roundtrip matrix asserts whole-file byte identity across the FULL low+high version matrix built in Plan 01 from TgenEraVersions"
  decisions:
    - "D-05: apply-save-trn v1 supports fixed-length edits only (scalar float/int, enum, active-flag) — no FORM length ripple"
    - "D-06: variable-length name edits DEFERRED out of v1 — rejected with a clear error"
    - "D-07: save path uses the same fail-closed --root containment + atomic write as ApplySaveIffCommand"
    - "D-08: surface = decode-iff TGEN branch + decode-trn + roundtrip-trn + apply-save-trn"
    - "D-09: apply-save-trn is field-aware (--leaf/--field/--value), re-encodes ONE field, addressed by DeriveStableId ordinal path; (tag,version) recovered via ResolveFieldContext parent-walk (#10)"
    - "Active-flag location (#10): --field active mutates the int32 active flag on the IHDR leaf under LAYR (per RESEARCH research places active+name on IHDR), resolved via ResolveFieldContext — the SAME IHDR leaf Plan 02's decoder reads TerrainLayer.Active FROM (read↔write location parity)"
    - "Float bit policy (#6): untouched floats are copied byte-for-byte (no round-trip through parse/format); the edited float accepts invariant-culture decimal input and REJECTS NaN/Infinity"
  artifacts:
    - path: "UtinniCoreDotNet/Formats/Terrain/TrnFieldEncoder.cs"
      provides: "re-encode ONE field inside a packed DATA payload via the shared TgenFieldLayouts descriptor (exact byte-span replacement, untouched bytes copied verbatim), byte-exact"
      contains: "TgenFieldLayouts"
    - path: "Utinni.Cli/Commands/ApplySaveTrnCommand.cs"
      provides: "field-aware apply-save verb: ResolveFieldContext parent-walk + --root containment + atomic write + exact-byte-span verify"
      contains: "ResolveFieldContext"
    - path: "Utinni.Cli/Commands/RoundtripTrnCommand.cs"
      provides: "parse→serialize→re-parse whole-file byte-identity assertion"
    - path: "Utinni.Cli/Commands/DecodeIffCommand.cs"
      provides: "TGEN branch emitting the NAVIGABLE terrain envelope (MCP routing for free)"
      contains: "TGEN"
    - path: "Utinni.Cli.Tests/Terrain/TrnVerbGoldenTests.cs"
      provides: "xUnit CLI golden tests for decode-iff TGEN / decode-trn / roundtrip-trn envelopes + exit-code mapping (#5/#18)"
  key_links:
    - from: "UtinniCoreDotNet/Formats/Terrain/TrnFieldEncoder.cs"
      to: "UtinniCoreDotNet/Formats/Terrain/TgenFieldLayouts.cs"
      via: "encoder looks up the SAME descriptor table the decoder reads (offset-parity)"
      pattern: "TgenFieldLayouts"
    - from: "Utinni.Cli/Commands/ApplySaveTrnCommand.cs"
      to: "UtinniCoreDotNet/Formats/Terrain/TrnFieldEncoder.cs"
      via: "field-aware single-field re-pack → MutableIffNode.SetPayload"
      pattern: "TrnFieldEncoder"
    - from: "Utinni.Cli/Commands/ApplySaveTrnCommand.cs"
      to: "UtinniCoreDotNet/Saving/LooseOverridePath.cs"
      via: "LooseOverridePath.Resolve(root, relAsset) fail-closed containment"
      pattern: "LooseOverridePath\\.Resolve"
    - from: "Utinni.Cli/Commands/DecodeIffCommand.cs"
      to: "UtinniCoreDotNet/Formats/Terrain/TerrainDocument.cs"
      via: "if root SubTypeId == TGEN → TerrainDocument.FromIff"
      pattern: "TerrainDocument\\.FromIff"
---

<objective>
Build the edit path + the verb surface with the byte-exact correctness the reviewers demanded: a
`TrnFieldEncoder` that consumes the SAME `TgenFieldLayouts` table the decoder uses (offset-parity
test, #2) and replaces ONLY the target field's exact byte span (#2/#6), a field-aware
`apply-save-trn` that recovers (tag, version) by walking the parent chain via `ResolveFieldContext`
(#10) with the active-flag DOM location pinned (#10), a navigable `decode-iff` TGEN branch + a
`decode-trn` alias (TRN-01 JSON depth, #9), a whole-file `roundtrip-trn`, and xUnit CLI GOLDEN tests
for the TGEN verbs (#18) with schema/exit-code assertions (#5). Satisfies criterion 3 (TRN-03) and
the verb half of criterion 4 (TRN-04).

Purpose: This is the second genuinely-new piece (the single-field re-encoder) — the reviewers' core
correctness concern. It is built ON the offset-parity-tested descriptor table from Plan 02 and proven
with exact-byte-span assertions, not whole-leaf reserialization.
Output: `TrnFieldEncoder` + three verbs + the navigable decode-iff branch + Program.cs registration +
CLI golden tests; `RoundtripTrn` + `ApplySaveTrn` + `TrnVerbGolden` stubs turn green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Build/test invariant:** MSBuild `Utinni.sln` x86 Release FIRST, then `dotnet test --no-build`.
Never `dotnet build`. `Generated/UtinniCore.cs` NOT touched — `git checkout --` if reordered.
NOTE: Plan 01 ships NO no-op `trn` stub verb (the verb ceiling was confirmed by a test-only
assertion), so there is nothing to remove here — register the real verbs cleanly.
</execution_context>

<context>
@.planning/phases/20-terrain-trn-codec-verbs-mcp/20-CONTEXT.md
@.planning/phases/20-terrain-trn-codec-verbs-mcp/20-RESEARCH.md
@.planning/phases/20-terrain-trn-codec-verbs-mcp/20-PATTERNS.md
@.planning/phases/20-terrain-trn-codec-verbs-mcp/20-REVIEWS.md

<interfaces>
<!-- Byte-exact edit primitives. Source: MutableIffNode.cs -->
MutableIffNode.GetPayloadCopy() :255 (defensive copy), SetPayload(bytes) :279 (marks IsDirty +
ancestor-invalidate), Parent :147, TypeId :96, SubTypeId :127 (the parent-walk surface for
ResolveFieldContext). IffWriter.Write(mutable) re-emits clean nodes from captured slice verbatim
(:103-110), re-frames dirty nodes with NO pad (:141), recomputes ancestor FORM lengths.

<!-- Stable-id leaf addressing + apply-save scaffolding. Source: ApplySaveIffCommand.cs -->
LooseOverridePath.Resolve(root, relAsset) (UtinniCoreDotNet/Saving/LooseOverridePath.cs) — fail-closed
containment, throws ArgumentException on escape → map to exit 2 PathContainment. FindMutableLeafByStableId
/ FindMutableLeafRecursive :288-307 (walk MutableIffDocument.DeriveStableId :161-177). CompareUntouchedLeaves
:236-275 (untouched-leaf byte-identity verify). SaveCommandIo.WriteAtomic (Utinni.Cli/Commands/SaveCommandIo.cs).
IsTreMagic :227-230 (reject .tre up front). Exit codes: 0 ok / 1 usage / 2 verify|parse|path-containment / 3 file-not-found.

<!-- Shared descriptor + read mirror. Source: Terrain/TgenFieldLayouts.cs (Plan 02) + IffPayloadCursor.cs -->
TgenFieldLayouts.For(tag, version) → ordered TgenFieldDescriptor list (offset/width/signed/endian/
enum-width/display-type/parser/editable). IffPayloadCursor.ReadFloatLe :113-129 — invert the byte
assembly for the LE field write (BitConverter.GetBytes + reverse if !IsLittleEndian → Array.Copy at
the descriptor offset). Untouched bytes are NEVER re-derived — only the target field span is overwritten.

<!-- decode-iff dispatch + JSON envelope. Source: DecodeIffCommand.cs -->
PEFT branch :88-93 is the verbatim template — add a TGEN twin immediately after. JsonOutput.EmitSuccess
(JsonOutput.cs:50) emits the sorted-key schemaVersion:1 envelope. Generic Exception intentionally NOT caught.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TrnFieldEncoder — exact-byte-span single-field re-encode on the shared descriptor (byte-exact)</name>
  <files>UtinniCoreDotNet/Formats/Terrain/TrnFieldEncoder.cs, Utinni.Cli.Tests/Terrain/RoundtripTrnTests.cs</files>
  <read_first>
    - UtinniCoreDotNet/Formats/Terrain/TgenFieldLayouts.cs (Plan 02 — the SINGLE descriptor table the encoder MUST consume; do NOT duplicate offsets) + Utinni.Cli.Tests/Terrain/TgenFieldLayoutTests.cs (the offset-parity foundation to extend)
    - UtinniCoreDotNet/Formats/Decoders/IffPayloadCursor.cs:113-129 (ReadFloatLe — invert the byte assembly for the write) + :24-28 (provenance header)
    - UtinniCoreDotNet/Formats/Iff/MutableIffNode.cs:255 (GetPayloadCopy) + :279 (SetPayload) and IffWriter.cs:103-110,141 (verbatim re-emit + no-pad)
    - 20-RESEARCH.md § Pitfall 1 :301-305 (DATA packs 2-6 scalars) + § "Code Examples — Byte-exact single-field edit" :346-354
    - 20-CONTEXT.md D-05 (fixed-length scalar/int/enum + active) + D-06 (variable-length name edits REJECTED)
    - 20-REVIEWS.md Consensus #2 (single-sourced descriptor + offset-parity test + exact-byte-span assertion) + #6 (float bit policy: untouched floats exact bits, reject NaN/Inf)
  </read_first>
  <behavior>
    - Given an AHCN DATA payload [int32 operation][float height] and ("height", "12.5"), the encoder returns a SAME-LENGTH payload where ONLY bytes [4..8) (the height span from TgenFieldLayouts) differ; the operation bytes [0..4) are byte-identical to the input (copied, not re-derived).
    - Given ("operation", "3"), only bytes [0..4) change.
    - Untouched float fields elsewhere in the payload keep their EXACT input bytes (no parse/format canonicalization) — proven by editing one float and asserting another float field's bytes are unchanged bit-for-bit (#6).
    - --value "NaN" or "Infinity" for a float field → a deterministic rejection (ArgumentException), NOT a written NaN payload (#6); accepted input is invariant-culture decimal.
    - A field name not in the tag's TgenFieldLayouts descriptor → deterministic ArgumentException, not a silent no-op.
    - A name/variable-length field edit request is REJECTED (D-06).
    - An offset-parity test asserts that for each (tag,version) the encoder's field spans (offset+width) are IDENTICAL to the decoder's descriptor spans (same TgenFieldLayouts source — concern #2).
    - roundtrip-trn-level: every fixture in the FULL Plan-01 matrix asserts bytes-in == bytes-out (whole-file SequenceEqual) including unknown-tag, DEAD-tag, truncated, and compositional fixtures (clean nodes re-emit verbatim).
  </behavior>
  <action>
    Create `TrnFieldEncoder.cs` in `Formats/Terrain/` (MIT + port-understanding header). Signature takes a
    DATA payload byte[], the tag, the FORM version, a field name, and a string value. It looks up
    `TgenFieldLayouts.For(tag, version)` (the SAME table the decoder reads — single source of truth, #2),
    finds the target descriptor (by Name), validates `Editable`, parses the string `--value` per the
    descriptor's `EditParser` to the field type, and writes ONLY that field's exact byte span (offset..
    offset+width) into a COPY of the input payload (LE, inverting `IffPayloadCursor.ReadFloatLe:113-129`
    for floats; LE int of the descriptor width for scalar/enum). All OTHER bytes are copied verbatim from
    the input — the encoder NEVER reserializes the whole payload from a typed model (#2/#6). Return the
    same-length payload. For float fields, accept invariant-culture decimal and REJECT NaN/Infinity (#6).
    Support the `active` field target via the descriptor (int32 active flag, fixed length, D-05). REJECT
    name/variable-length edits (D-06) with a clear exception. The caller (`apply-save-trn`, Task 3) does
    `GetPayloadCopy → TrnFieldEncoder → SetPayload`.

    Extend `TgenFieldLayoutTests.cs` (or add to it) an OFFSET-PARITY test (filter `TgenLayout`) asserting
    the encoder and decoder resolve identical field spans per (tag,version) from `TgenFieldLayouts`
    (concern #2). Then implement `RoundtripTrnTests.cs` (filter `RoundtripTrn`): un-Skip + assert whole-file
    byte identity (`SequenceEqual`) across the FULL Plan-01 matrix — the DEC-C3 byte-exact gate at the
    codec level — PLUS the exact-byte-span assertions (edit height → only [4..8) differ; another float
    field bits unchanged; NaN/Inf rejected).
  </action>
  <verify>
    <automated>MSBuild x86 Release succeeds, THEN `dotnet test Utinni.Cli.Tests --no-build --filter "RoundtripTrn|TgenLayout"` exits 0 — whole-file byte identity across the FULL matrix; a one-field edit changes ONLY that field's exact byte span; an untouched float keeps exact bits; NaN/Inf rejected; encoder offsets == decoder offsets per (tag,version)</automated>
  </verify>
  <acceptance_criteria>
    - `TrnFieldEncoder` returns a same-length payload where ONLY the named field's exact byte span (from `TgenFieldLayouts`) differs; all other bytes are byte-identical to the input (asserted per-field for AHCN operation + height).
    - Untouched float fields keep exact bits; `--value NaN`/`Infinity` is rejected; accepted input is invariant-culture decimal (#6).
    - An offset-parity test proves encoder field spans == decoder descriptor spans per (tag,version) (#2).
    - An unknown field name throws; a name/variable-length edit is rejected (D-06).
    - `dotnet test Utinni.Cli.Tests --no-build --filter "RoundtripTrn|TgenLayout"` exits 0 (full-matrix byte identity + parity).
  </acceptance_criteria>
  <done>The single-field re-encoder is byte-exact via exact-span replacement on the shared descriptor, with float-bit policy and offset-parity proven — the reviewers' central correctness concern (#2/#6) is closed at the codec level.</done>
</task>

<task type="auto">
  <name>Task 2: decode-iff TGEN branch (navigable envelope) + decode-trn + roundtrip-trn verbs + CLI golden tests + registration</name>
  <files>Utinni.Cli/Commands/DecodeTrnCommand.cs, Utinni.Cli/Commands/RoundtripTrnCommand.cs, Utinni.Cli/Commands/DecodeIffCommand.cs, Utinni.Cli/Program.cs, Utinni.Cli.Tests/Terrain/TrnVerbGoldenTests.cs</files>
  <read_first>
    - Utinni.Cli/Commands/DecodeIffCommand.cs:84-169 (root-FORM dispatch; PEFT branch :88-93 verbatim template) + :225-259 (BuildParticleResult JSON envelope convention) + :54-120 (exit-code/exception taxonomy)
    - Utinni.Cli/Commands/RoundtripParticleCommand.cs:35-40,57-113 (Verb attribute + body: ReadAllBytes → FromBytes → Serialize → re-parse → SequenceEqual → JObject)
    - Utinni.Cli/Program.cs:48-107 (Type[] list + Dispatch switch registration sites)
    - Utinni.Cli/Output/JsonOutput.cs:50 (EmitSuccess sorted-key envelope)
    - Utinni.Cli.Tests/ (existing CLI golden-test convention — e.g. how RoundtripParticle / decode-iff verbs are golden-tested in xUnit, to mirror for the TGEN verbs)
    - 20-CONTEXT.md D-08 (decode-iff TGEN branch + decode-trn + roundtrip-trn) + Claude's Discretion (fold-or-alias)
    - 20-REVIEWS.md Consensus #5 (JSON envelope schema + exit-code mapping tested) + #9 (TRN-01 NAVIGABLE envelope, not summary-only) + #18 (CLI golden tests for the TGEN verbs)
  </read_first>
  <action>
    Add the TGEN branch to `DecodeIffCommand` immediately after the PEFT branch (:88-93): `if ((doc.Root
    as IffContainerChunk)?.SubTypeId == "TGEN") return JsonOutput.EmitSuccess("decode-iff",
    BuildTerrainResult(TerrainDocument.FromIff(doc, bytes), o.Path));`. Write `BuildTerrainResult` to emit
    a NAVIGABLE envelope per the TRN-01 depth decision (#9), NOT summary-only counts: sorted-key anonymous
    object with `type = "terrain"`, `rootType = "TGEN"`, `source`, `version`, a `palettes` array (each:
    slot name, present, ambiguous, family names), and a `layers` array recursively exposing each layer's
    `name`, `active`, and `children` (each child: `tag`, `version`, `kind` ∈ {typed, raw, dead},
    `stableId`, `editable`, and for typed children the named `fields`; for raw children the `{tag, version,
    hex}` form — concern #8). Keep counts (layerCount/paletteCounts/rawFallbackCount) as convenience
    fields alongside the navigable tree. Catch `TerrainParseException` (exit 2) alongside
    `IffParseException`; FileNotFound → exit 3; generic `Exception` intentionally NOT caught.

    Create `DecodeTrnCommand.cs` as the thin discoverability alias (`[Verb("decode-trn", ...)]`) delegating
    to the same `TerrainDocument.FromBytes` + `BuildTerrainResult` (alias, not a fork — Claude's Discretion).
    Create `RoundtripTrnCommand.cs` by copying `RoundtripParticleCommand`: `ReadAllBytes` →
    `TerrainDocument.FromBytes` → `Serialize()` (Plan-02 IffWriter.Write over the held mutable DOM) →
    re-parse for validity → `SequenceEqual` whole-file → `JObject { bytesIdentical, comparisonGranularity =
    "whole-file", rootType = "TGEN", source, version }`; exit codes 0/1/2/3; generic Exception NOT caught.
    Register all three in `Program.cs`: append `typeof(Commands.DecodeTrnOptions)`,
    `typeof(Commands.RoundtripTrnOptions)` to the Type[] and the matching `case ...: return ...Command.Run(o);`.

    Create `TrnVerbGoldenTests.cs` (filter `TrnVerbGolden`): xUnit GOLDEN tests that invoke the verb
    entry points (DecodeTrnCommand.Run / RoundtripTrnCommand.Run / DecodeIffCommand.Run over a TGEN
    fixture) and assert (a) the emitted JSON envelope matches a committed golden schema/shape (navigable
    tree present, sorted keys, `type:terrain`/`rootType:TGEN`), (b) the exit codes map correctly
    (valid → 0; FileNotFound → 3; malformed/parse → 2), and (c) stderr/malformed-input behavior (concern
    #5/#18). This closes the verb-golden coverage gap (the codec-level RoundtripTrnTests bypass the verb).
  </action>
  <verify>
    <automated>MSBuild x86 Release succeeds; `dotnet test Utinni.Cli.Tests --no-build --filter TrnVerbGolden` exits 0 (envelope schema + exit-code golden tests pass); `utinni-cli decode-iff <fixture.trn>` emits a navigable JSON envelope with `"type":"terrain"`, `"rootType":"TGEN"`, and a `layers` array (exit 0); `utinni-cli roundtrip-trn <fixture.trn>` reports `bytesIdentical:true` (exit 0); `utinni-cli --help` enumerates decode-trn + roundtrip-trn</automated>
  </verify>
  <acceptance_criteria>
    - `decode-iff` on a TGEN fixture emits a NAVIGABLE envelope (layers array with name/active/children, palette family names, typed fields, sorted keys) — not summary-only counts (#9), routed without an MCP-layer change.
    - `decode-trn` alias and `roundtrip-trn` verb both register and run (exit 0 on a valid fixture); `roundtrip-trn` reports `bytesIdentical:true` across the matrix.
    - `TrnVerbGoldenTests` assert the envelope schema/shape AND exit-code mapping (0/2/3) AND malformed-input behavior for the TGEN verbs (#5/#18).
    - `TerrainParseException` → exit 2; FileNotFound → exit 3; generic Exception NOT caught.
  </acceptance_criteria>
  <done>decode-iff routes TGEN with a navigable envelope (MCP-for-free, #9), decode-trn/roundtrip-trn exist, and CLI golden tests guard the verb wiring (#5/#18) — the read+roundtrip verb surface is live and regression-guarded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: apply-save-trn — ResolveFieldContext parent-walk + field-aware contained atomic save (exact-span verify)</name>
  <files>Utinni.Cli/Commands/ApplySaveTrnCommand.cs, Utinni.Cli/Program.cs, Utinni.Cli.Tests/Terrain/ApplySaveTrnTests.cs</files>
  <read_first>
    - Utinni.Cli/Commands/ApplySaveIffCommand.cs (FULL: options :38-55, path containment :97-105, FindMutableLeafByStableId/FindMutableLeafRecursive :288-307, CompareUntouchedLeaves :236-275, IsTreMagic :227-230, result JObject + exit codes :202-223,69, TestPerturbSerialized seam :74)
    - UtinniCoreDotNet/Formats/Iff/MutableIffNode.cs:147 (Parent), :96 (TypeId), :127 (SubTypeId) — the parent-walk surface for ResolveFieldContext
    - UtinniCoreDotNet/Formats/Terrain/TrnFieldEncoder.cs (Task 1) + TgenFieldLayouts.cs (the (tag,version) descriptor the resolved context feeds)
    - UtinniCoreDotNet/Saving/LooseOverridePath.cs (Resolve fail-closed, throws on escape — V12) + Utinni.Cli/Commands/SaveCommandIo.cs (WriteAtomic)
    - UtinniCoreDotNet/Formats/Iff/MutableIffDocument.cs:161-177 (DeriveStableId)
    - 20-RESEARCH.md § "The Six Shared Palettes"/§ Tag Taxonomy (IHDR carries active+name under LAYR — the active-flag location, #10)
    - 20-CONTEXT.md D-07 (containment + atomic write) + D-09 (field-aware, DeriveStableId addressing) + D-05 (scalar/enum/active only)
    - 20-REVIEWS.md Consensus #10 (ResolveFieldContext parent-walk + active-flag DOM location) + #2 (exact-byte-span verify) + #4 (reject edits on non-editable/raw nodes) + #17 (negative tests)
  </read_first>
  <behavior>
    - ResolveFieldContext(leaf): walks the leaf's MutableIffNode.Parent chain to recover the enclosing typed FORM's FourCC (tag) AND the version FORM (e.g. .../AHCN/0000/DATA → tag=AHCN, version=0000), returning (tag, version). It does NOT assume the DATA leaf's own node carries them.
    - `apply-save-trn --root <r> --asset <rel> --leaf <stable-id> --field height --value 12.5` resolves (tag,version) via ResolveFieldContext, edits ONE field via TrnFieldEncoder, verifies the output differs from source ONLY in the target field's exact byte span (and all untouched leaves byte-identical), writes atomically under the contained path, exit 0.
    - `--field active --value 0|1` resolves to the IHDR active-flag int32 under LAYR (the defined active-flag location, #10) and flips it byte-exact.
    - ROUND-TRIP PARITY (#10): after `--field active` toggles the flag and the file is saved, re-decoding the written bytes via `TerrainDocument.FromBytes` asserts the affected layer's `TerrainLayer.Active` reflects the edited value — proving Plan 02's read location (the IHDR active-flag leaf) == this verb's write location end-to-end (no read↔write divergence).
    - An edit targeting a node whose TerrainNode.IsEditable is false (raw-fallback / truncated / DEAD) is REJECTED → deterministic error, NO write (concern #4).
    - A `--root` escape (relAsset resolving outside root) → exit 2, NO write (V12).
    - A `.tre`-magic input → rejected up front (IsTreMagic), no write.
    - A bad `--leaf` stable-id (not found) → deterministic error exit, NO write.
    - After save, re-parsing the written bytes succeeds and ONLY the edited leaf's target span differs from the source.
  </behavior>
  <action>
    Create `ApplySaveTrnCommand.cs` modeled on `ApplySaveIffCommand.cs`, FIELD-AWARE (D-09): options
    `--root` (required, containment), positional `relAsset`, `--leaf <stable-id>`, `--field <name>`,
    `--value <v>`. Add a private `ResolveFieldContext(MutableIffNode leaf)` helper that walks
    `leaf.Parent` (MutableIffNode.cs:147) up the chain to recover the enclosing typed FORM FourCC and the
    version FORM, returning `(string tag, version)` (concern #10) — for `--field active`, resolve to the
    IHDR active-flag leaf under the LAYR (the pinned active-flag location, #10 — the SAME IHDR leaf Plan
    02's decoder reads `TerrainLayer.Active` from). Flow: reject `.tre` magic (IsTreMagic);
    `LooseOverridePath.Resolve(root, relAsset)` (throws on escape → exit 2 PathContainment, V12); read the
    asset, build `MutableIffDocument`, decode via `TerrainDocument.FromIff` to obtain the target
    `TerrainNode` and assert `IsEditable` (reject raw/truncated/DEAD edits → error, no write — concern #4);
    locate the target leaf via `FindMutableLeafByStableId` / `FindMutableLeafRecursive` (:288-307, reused
    verbatim); `ResolveFieldContext` → `(tag, version)`; `GetPayloadCopy` → `TrnFieldEncoder` (Task 1) to
    replace ONE field by name → `SetPayload`; `IffWriter.Write(mutable)`; re-parse for validity; verify the
    output differs from source ONLY in the target field's EXACT byte span (compute the span from
    `TgenFieldLayouts` + the leaf's file offset) AND `CompareUntouchedLeaves` (:236-275) confirms every
    untouched leaf byte-identical (concern #2); ONLY on a clean verify, `SaveCommandIo.WriteAtomic(destPath,
    mutatedBytes)`. Result JObject `{ bytesEqualUntouched, bytesWritten, fieldSpanOnly, mutationApplied,
    path, validated, written }`; exit codes 0/1/2/3 (verbatim from apply-save-iff). Register in `Program.cs`
    (`typeof(Commands.ApplySaveTrnOptions)` + Dispatch case).
    Then implement `ApplySaveTrnTests.cs` (filter `ApplySaveTrn`): un-Skip + assert single-field edit
    (exact-span), active toggle (IHDR), the round-trip parity test
    `ApplySaveTrn_ToggleActive_RoundtripDecodeReflectsEdit` (toggle `--field active`, save, re-decode via
    `TerrainDocument.FromBytes`, assert the layer's `TerrainLayer.Active` == the edited value — read↔write
    location parity, #10), untouched-leaf byte-identity, a non-editable-node edit rejection (concern #4), a
    `--root` escape rejection (exit 2, no write), `.tre` rejection, and bad-`--leaf` rejection.
  </action>
  <verify>
    <automated>MSBuild x86 Release succeeds, THEN `dotnet test Utinni.Cli.Tests --no-build --filter ApplySaveTrn` exits 0 — single-field exact-span edit, IHDR active toggle, ApplySaveTrn_ToggleActive_RoundtripDecodeReflectsEdit (re-decode reflects the edit), untouched-leaf byte-identity, non-editable-node rejection (no write), --root escape rejection (exit 2, no write), .tre + bad-leaf rejection all pass</automated>
  </verify>
  <acceptance_criteria>
    - `ResolveFieldContext` recovers (tag,version) by walking `MutableIffNode.Parent` (asserted: a DATA leaf under AHCN/0000 resolves to tag=AHCN, version=0000) (#10).
    - `--field active` mutates the IHDR active-flag int32 under LAYR (the defined location) byte-exact (#10).
    - Read↔write location parity is proven end-to-end: `ApplySaveTrn_ToggleActive_RoundtripDecodeReflectsEdit` toggles `--field active`, saves, re-decodes via `TerrainDocument.FromBytes`, and asserts the layer's `TerrainLayer.Active` reflects the edit — the decode-read IHDR leaf (Plan 02) and the edit-write IHDR leaf are the same DOM location (#10).
    - The output differs from source ONLY in the target field's exact byte span (verified) and all untouched leaves are byte-identical (#2).
    - An edit on a non-editable (raw/truncated/DEAD) node is rejected with no write (#4); a path-escape, `.tre`-magic, and not-found `--leaf` are all rejected with no write (exit 2 for escape).
    - `apply-save-trn` is registered in `Program.cs`; `dotnet test ... --filter ApplySaveTrn` exits 0.
  </acceptance_criteria>
  <done>Field-aware byte-exact contained save works with parent-chain context resolution, a pinned active-flag location, read↔write location parity proven by re-decode, exact-span verification, and non-editable rejection — criterion 3 (TRN-03) is met and the reviewers' edit-addressing concerns (#2/#4/#10) are closed; criterion 4's verb surface (TRN-04) is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args (relAsset, --root) → save path | untrusted relative path crosses into the filesystem; must be contained under --root fail-closed (V12) |
| --value string → field bytes | untrusted scalar value parsed + written into ONLY the descriptor's exact field span (V5) |
| --leaf stable-id → DOM node | a stable-id addressing a non-editable (raw/truncated/DEAD) node must be rejected, never half-rewritten (#4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Tampering | apply-save-trn save path | mitigate | `LooseOverridePath.Resolve` fail-closed containment under `--root` (throws on escape → exit 2, no write), atomic write only after a clean exact-span + untouched-leaf verify — verbatim reuse of apply-save-iff (V12). Acceptance: `--root` escape test asserts exit 2 + no file written. |
| T-20-06 | Tampering | length-ripple / wholesale-reserialize corruption | mitigate | v1 is fixed-length edits only (D-05); name/variable-length rejected (D-06). The encoder overwrites ONLY the target field's exact byte span and copies all other bytes verbatim (no reserialization), so untouched fields (incl. floats) keep exact bits (#2/#6). Acceptance: exact-span + untouched-float-bits tests green. |
| T-20-07 | Tampering | edit on a half-understood payload | mitigate | apply-save-trn rejects edits on any node with IsEditable=false (raw-fallback, truncated, DEAD) — it never rewrites a payload the decoder did not fully understand (concern #4). Acceptance: non-editable-node rejection test (no write). |
| T-20-07b | Tampering | wrong (tag,version) → wrong layout | mitigate | ResolveFieldContext recovers (tag,version) from the parent chain (#10) and the encoder uses the SAME TgenFieldLayouts the decoder validated — no mis-addressed field write. Acceptance: ResolveFieldContext test asserts AHCN/0000 recovery. |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure managed, zero new external packages. No legitimacy gate needed. |
</threat_model>

<verification>
- `dotnet test Utinni.Cli.Tests --no-build --filter "RoundtripTrn|TgenLayout|TrnVerbGolden|ApplySaveTrn"` exits 0.
- `utinni-cli decode-iff <fixture.trn>` emits a navigable `type:terrain` envelope; `roundtrip-trn` reports `bytesIdentical:true`.
- `apply-save-trn` exact-span field edit + IHDR active toggle save byte-exact; the active toggle re-decodes to the edited value (read↔write parity); non-editable / path-escape / .tre / bad-leaf rejected with no write.
- Solution builds with MSBuild x86 Release; `Generated/UtinniCore.cs` unchanged.
</verification>

<success_criteria>
- TRN-03: edit + save scalar/enum leaf + active-flag toggle, byte-exact (exact-span), via the loose-override matrix; non-editable nodes rejected.
- TRN-04 (verb half): decode-iff TGEN branch (navigable) + decode-trn + roundtrip-trn + apply-save-trn, with xUnit CLI golden tests.
- Reviewer concerns closed: #2 (single-source descriptor + exact-span), #6 (float bits), #9 (navigable envelope), #10 (ResolveFieldContext + active location + read↔write parity), #5/#18 (verb goldens + schema/exit-code).
</success_criteria>

<output>
Create `.planning/phases/20-terrain-trn-codec-verbs-mcp/20-03-SUMMARY.md` when done
</output>
</content>
</invoke>
