---
phase: 07-tjt-subpanel-tre-browser-read-only
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - "D:/Code/Utinni/UtinniCoreDotNet/Formats/Iff/IffChunk.cs"
  - "D:/Code/Utinni/UtinniCoreDotNet/Formats/Iff/IffReader.cs"
  - "D:/Code/Utinni/Utinni.Cli.Tests/Commands/InspectIffCommandTests.cs"
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs"
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.Designer.cs"
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/TreDetailPane.cs"
autonomous: false
requirements: [PROD-W1-TRE, PROD-01]

must_haves:
  truths:
    - "Selecting a tree entry shows a metadata header: virtual path, file size (bytes + human), source .tre archive, CRC (hex), compression kind (D-12)"
    - "The framework IffChunk exposes an OffsetBytes property (byte offset of the chunk header within the document) populated by IffReader, so the UI can honestly render @offset without a second parser (review consensus / codex MEDIUM: IffChunk had no offset)"
    - "When the payload is readable, a universal IFF chunk tree renders the FORM/chunk hierarchy (TAG, size, @offset) from the shared Formats/Iff reader (D-12)"
    - "A type/version banner states the decoded type and root FORM tag + version (e.g. 'Object Template (CREO / 0006)') with the reserved Colors.Secondary() 2px accent rule (D-12)"
    - "A collapsed raw-hex peek shows the first <=4 KB of readable payload in a monospace control (D-12)"
    - "For an enumerate-only archive the banner reads the CORRECT version — 'Encrypted payload (v6000) — enumerate-only' for v6000 and the v5000 equivalent for v5000 (not a hard-coded v6000 string for both) — and content sections are replaced by the info panel (D-07; review LOW: banner version accuracy)"
    - "A parse failure on one file shows the 'Could not decode this file' error panel and never takes down the browser (UI-SPEC States)"
    - "The detail pane reads payloads on demand via the shared TrePayloadResolver + IffReader — the same code path the inspect-iff CLI uses (D-08, success criterion #4)"
    - "The universal IFF chunk tree is structured as a reusable read-only surface (public LoadIff) that Phase 8's IFF editor makes editable with no rework — just add write + editing UI on top (D-13)"
  artifacts:
    - path: "D:/Code/Utinni/UtinniCoreDotNet/Formats/Iff/IffChunk.cs"
      provides: "IffChunk with new OffsetBytes property (chunk-header byte offset within the document)"
      contains: "OffsetBytes"
    - path: "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/TreDetailPane.cs"
      provides: "Detail-pane control: metadata header + type/version banner + universal IFF chunk TreeView (TAG·size·@offset) + raw hex peek + encrypted/parse-fail states"
      contains: "class TreDetailPane"
    - path: "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs"
      provides: "AfterSelect wiring that reconstructs the path, resolves the payload via TrePayloadResolver, and populates the detail pane"
      contains: "AfterSelect"
  key_links:
    - from: "TreDetailPane.cs"
      to: "UtinniCoreDotNet.Formats.Iff.IffReader"
      via: "universal chunk-tree parse of the selected readable payload, rendering OffsetBytes"
      pattern: "IffReader\\.Read"
    - from: "FormTreBrowser.cs"
      to: "UtinniCoreDotNet.Formats.Tre.TrePayloadResolver"
      via: "on-demand single-payload resolve on tree selection (shared facade)"
      pattern: "TrePayloadResolver"
    - from: "FormTreBrowser.cs"
      to: "TreDetailPane"
      via: "AfterSelect populates the detail pane in the SplitContainer Panel2"
      pattern: "TreDetailPane"
---

<objective>
Build the detail pane for the TRE Browser, and first add the framework `IffChunk.OffsetBytes` property the UI needs to render `@offset` honestly. On tree selection, populate a metadata header, a type/version banner, a universal IFF chunk tree (TAG · size · @offset, from the shared `Formats/Iff` reader), and a collapsed raw-hex peek — resolving the selected payload on demand via the shared `TrePayloadResolver`. Handle the encrypted/enumerate-only state (D-07) with the correct version banner, and the parse-failure state gracefully so one bad file never crashes the browser. Keep the read path identical to the `inspect-iff` CLI (D-08, success criterion #4).

Purpose: This is the "view individual file metadata" + "universal chunk tree" half of D-12, and the reusable IFF chunk-tree control Phase 8's IFF editor makes editable (D-13). It folds in the cross-AI review's IffChunk-offset finding (the chunk tree cannot show @offset without a framework offset property — added here, golden-covered by inspect-iff) and the v5000-vs-v6000 banner-accuracy fix. It completes the right region the plan-02 shell left empty.
Output: `IffChunk.OffsetBytes` + `IffReader` population (framework) with an inspect-iff golden bump, `TreDetailPane.cs` hosting the five sections, the `FormTreBrowser` AfterSelect wiring, and the Designer hookup of the detail pane into Panel2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-tjt-subpanel-tre-browser-read-only/07-CONTEXT.md
@.planning/phases/07-tjt-subpanel-tre-browser-read-only/07-UI-SPEC.md
@.planning/phases/07-tjt-subpanel-tre-browser-read-only/07-PATTERNS.md
@.planning/phases/07-tjt-subpanel-tre-browser-read-only/07-01-SUMMARY.md
@.planning/phases/07-tjt-subpanel-tre-browser-read-only/07-02-SUMMARY.md

<interfaces>
<!-- Shared-core contracts the detail pane consumes. -->

Formats/Iff reader (UtinniCoreDotNet/Formats/Iff):
  IffDocument IffReader.Read(string path)
  IffDocument IffReader.Read(Stream input)          // <-- use the Stream overload on a MemoryStream of the payload
  IffDocument { IffChunk Root; IReadOnlyList<IffChunk> AllNodesInPreorder }
  IffChunk (base) { string TypeId; int LengthBytes; string Id; /* NEW this plan: long OffsetBytes */ }
  IffContainerChunk : IffChunk { string SubTypeId; IReadOnlyList<IffChunk> Children }
  IffLeafChunk : IffChunk { byte[] Data }   // payload scalars are LITTLE-endian; tags already read BE
  IffParseException { IffParseError Kind; string Message }
  (IffReader.ReadChunk already tracks `long chunkStart = br.BaseStream.Position` internally —
   thread it into each IffChunk via the protected ctor as OffsetBytes; this is the ONLY framework change.)

Shared Tre facade from plan 01:
  TreArchiveIndex.TryGetDescriptor(path, out TreEntryDescriptor d)
  TreEntryDescriptor { Path, SourceArchive, Crc, Offset, CompressedLength, Length, Compressor, Version, EnumerateOnly }
  TrePayloadResolver.Resolve(TreEntryDescriptor) -> byte[] (readable) / enumerate-only signal
  TreVersion.IsEnumerateOnly(TreVersion)

CLI parity reference (Utinni.Cli/Commands/InspectIffCommand.cs):
  inspect-iff reads via IffReader.Read(path) — the detail pane reads via IffReader.Read(Stream) over the
  resolved payload; SAME parser, no new chunk-walker (Pitfall 7). The inspect-iff JSON is a LOCKED
  schemaVersion:1 contract; adding OffsetBytes to chunks MAY require bumping the flat/tree projection +
  the *.expected.json goldens — do that deliberately as part of Task 1, do NOT drop the flat view.

UI controls + theming (from plan 02 + UI-SPEC):
  CollapsiblePanel, UtinniLabel, UtinniButton, UtinniContextMenuStrip, System.Windows.Forms.TreeView,
  UtinniTextbox (Multiline, ReadOnly for hex), Colors.* accessors (no Color.FromArgb).
  Banner accent = Colors.Secondary() 2px top rule ONLY. Parse-fail heading = Color.Red.
  Bold = banner emphasis only. Hex = 9pt Consolas (the monospace exception).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add IffChunk.OffsetBytes (framework) + populate in IffReader + inspect-iff golden bump</name>
  <files>D:/Code/Utinni/UtinniCoreDotNet/Formats/Iff/IffChunk.cs, D:/Code/Utinni/UtinniCoreDotNet/Formats/Iff/IffReader.cs, D:/Code/Utinni/Utinni.Cli.Tests/Commands/InspectIffCommandTests.cs</files>
  <read_first>
    - D:/Code/Utinni/UtinniCoreDotNet/Formats/Iff/IffChunk.cs (the base class — TypeId/LengthBytes/Id + the protected ctor to extend with OffsetBytes)
    - D:/Code/Utinni/UtinniCoreDotNet/Formats/Iff/IffReader.cs (ReadChunk: `long chunkStart = br.BaseStream.Position` at line 138 is the offset to thread through; the container/leaf construction sites; the flat/tree projection is in InspectIffCommand, not here)
    - D:/Code/Utinni/UtinniCoreDotNet/Formats/Iff/IffContainerChunk.cs and IffLeafChunk.cs (the ctors that call base — must pass the new offset)
    - D:/Code/Utinni/Utinni.Cli/Commands/InspectIffCommand.cs (the LOCKED schemaVersion:1 flat+tree projection; if OffsetBytes is surfaced in the JSON, the goldens bump here)
    - D:/Code/Utinni/Utinni.Cli.Tests/Commands/InspectIffCommandTests.cs (the goldens to keep green / bump deliberately)
    - D:/Code/Utinni/Utinni.Cli.Tests/Fixtures/iff/*.expected.json (the golden files that may need regeneration)
  </read_first>
  <behavior>
    - Test: parsing a known fixture, the root chunk's OffsetBytes == 0 and each child's OffsetBytes equals the byte position of its 4-char TypeId header within the stream (verified against the synthetic-nested fixture's known layout).
    - Test: inspect-iff output stays a valid schemaVersion:1 envelope; if OffsetBytes is added to the chunk projection, the *.expected.json goldens are regenerated to match and remain byte-stable (flat view NOT dropped).
    - Test: an existing malformed/truncated IFF fixture still produces the same error kind (the offset addition does not change error behavior).
  </behavior>
  <action>
    Add `public long OffsetBytes { get; }` to `IffChunk` (the base class), documented as the byte offset of the chunk's 4-char TypeId header within the document stream. Extend the protected `IffChunk` ctor to accept the offset; update `IffContainerChunk` and `IffLeafChunk` ctors to pass it through. In `IffReader.ReadChunk`, thread the already-tracked `long chunkStart = br.BaseStream.Position` (line 138) into the constructed chunk as `OffsetBytes` — this is the ONLY framework change; do NOT re-parse or add a second pass. Keep the parser-purity rule (no JSON/console/file-write in Formats/Iff).

    Decide the inspect-iff exposure deliberately: if surfacing `OffsetBytes` in the inspect-iff JSON is the cleanest way to keep the UI and CLI on one golden-tested projection, add an `offset` key to BOTH the flat and tree chunk projections in `InspectIffCommand` and REGENERATE the `Utinni.Cli.Tests/Fixtures/iff/*.expected.json` goldens (do NOT drop the flat view — it is locked). If you instead keep OffsetBytes off the JSON (UI-only consumption of the in-memory model), add a unit test in `InspectIffCommandTests.cs` asserting `IffReader.Read` populates OffsetBytes correctly and leave the JSON goldens byte-identical. Either way, OffsetBytes must be populated on the in-memory model the UI consumes, and the inspect-iff lane must stay green.
  </action>
  <verify>
    <automated>cd D:/Code/Utinni; dotnet test Utinni.Cli.Tests --filter "InspectIff"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test Utinni.Cli.Tests --filter "InspectIff"` passes (goldens green — regenerated deliberately if OffsetBytes is surfaced in JSON, or byte-identical if UI-only).
    - `IffChunk.cs` contains `OffsetBytes` (grep) with the documenting summary; `IffContainerChunk` and `IffLeafChunk` ctors pass it through (grep base ctor calls).
    - A test asserts the root OffsetBytes == 0 and a child's OffsetBytes equals its known header position in the synthetic-nested fixture.
    - The flat view is still present in the inspect-iff projection (grep `flat` in InspectIffCommand — not dropped).
    - No second parse pass added to IffReader (grep confirms OffsetBytes comes from the existing `chunkStart`, not a new read).
  </acceptance_criteria>
  <done>IffChunk exposes a populated OffsetBytes sourced from the existing parser position; the inspect-iff lane stays green; the UI can now render @offset honestly from the shared model.</done>
</task>

<task type="auto">
  <name>Task 2: TreDetailPane control — metadata header + type/version banner + universal IFF chunk tree (TAG·size·@offset) + raw hex peek</name>
  <files>D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/TreDetailPane.cs, D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.Designer.cs</files>
  <read_first>
    - D:/Code/Utinni/UtinniCoreDotNet/Formats/Iff/IffReader.cs (the Read(Stream) seam; container/leaf model; parser-purity note)
    - D:/Code/Utinni/UtinniCoreDotNet/Formats/Iff/IffChunk.cs (TypeId, LengthBytes, OffsetBytes — added in Task 1)
    - D:/Code/Utinni/UtinniCoreDotNet/Formats/Iff/IffContainerChunk.cs (SubTypeId, Children)
    - D:/Code/Utinni/Utinni.Cli/Commands/InspectIffCommand.cs (CLI parity reference — same IffReader, the tree projection idiom)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormObjectBrowser.Designer.cs (themed TreeView declaration + ContextMenuStrip wiring to mirror)
    - .planning/phases/07-tjt-subpanel-tre-browser-read-only/07-UI-SPEC.md (Right region — Detail pane sections 1-5; States: Loading detail, Encrypted, Parse failure; Color: banner accent + Red heading; Typography: Bold banner, Consolas hex; Copywriting: encrypted/parse-fail/5000-note exact strings)
    - .planning/phases/07-tjt-subpanel-tre-browser-read-only/07-PATTERNS.md (themed TreeView reuse; CollapsiblePanel; UtinniContextMenuStrip Copy path/Copy CRC; theming caveat)
  </read_first>
  <action>
    Create `TreDetailPane.cs` (a `UserControl` in `TheJawaToolboxDotNet/UI/Controls/`) carrying the verbatim MIT header. It hosts a vertically-scrolling stack (`AutoScroll = true`, `BackColor = Colors.Primary()`) with five regions per UI-SPEC:

    1. Metadata header (always visible): `UtinniLabel` key/value rows at 16px pitch, label column x=3, value column x=140 — Virtual path, File size (bytes + human-readable e.g. "12.3 KB"), Source .tre archive, CRC (hex, e.g. "0x1A2B3C4D"), Compression (none / zlib / deflate). Attach a `UtinniContextMenuStrip` with "Copy path" and "Copy CRC" items writing to the clipboard (read-only — clipboard copy of text is allowed, NOT an asset write/export).

    2. Type + version banner (`CollapsiblePanel`, open by default): one prominent `UtinniLabel` (Bold — the only Bold use besides the search-node bold) stating decoded type + root FORM tag + version, e.g. `Datatable (DTII / 0001)` / `Object Template (CREO / 0006)`. Draw a 2px top accent rule in `Colors.Secondary()` (mirror UtinniForm.OnPaint's titlebar accent). Derive type from the root container `SubTypeId` (and extension fallback).

    3. Universal IFF chunk tree (`CollapsiblePanel`, open when readable): a themed `TreeView` rendering the FORM/chunk hierarchy from `IffDocument.Root`. Each node label: `TAG  ·  {LengthBytes} bytes  ·  @{OffsetBytes}` (containers show `TAG SubTypeId`, leaves show `TAG`) — use the NEW `IffChunk.OffsetBytes` from Task 1 for the `@offset` (no second parser). Build by recursing `IffContainerChunk.Children`. Keep this a standalone, reusable region (Phase 8 makes it editable — D-13). Expose a public `LoadIff(IffDocument doc)` method so Phase 8 can reuse it.

    4. Type-specific structured view (`CollapsiblePanel`): leave a placeholder host `Panel pnlStructured` here that plan 04b fills with the per-type ListViews. In this plan render nothing in it (hidden).

    5. Raw hex peek (`CollapsiblePanel`, collapsed by default): a read-only `UtinniTextbox` (Multiline) or fixed-pitch label in 9pt Consolas showing the first min(4096, payload.Length) bytes as a classic hex+ASCII dump. Hidden entirely when the payload is encrypted/enumerate-only.

    Add a public API the Form calls: `void ShowReadable(TreMetadata meta, byte[] payload)`, `void ShowEncrypted(TreMetadata meta)`, `void ShowParseFailure(TreMetadata meta, string reason)`, `void ShowEmpty()`, `void ShowDecoding(TreMetadata meta)` — where `TreMetadata` is a small struct {Path, SizeBytes, SourceArchive, Crc, CompressionKind, RootFormTag, Version (TreVersion)}. In `ShowReadable`, parse via `IffReader.Read(new MemoryStream(payload))` inside a try/catch — on `IffParseException` (or IOException) call the parse-failure rendering with `ex.Message` rather than throwing (UI-SPEC: one bad file must not crash the panel). NO chunk-walking beyond consuming `IffReader` output (Pitfall 7).

    Implement the three replacement states per UI-SPEC States:
    - Encrypted: metadata + banner render; sections 3-5 replaced by an info panel. The banner heading is version-accurate (review LOW): for V6000 `Encrypted payload (v6000)`, for V5000 use the 5000-note copy `Recognized TRE version 5000 — layout not yet verified; enumerate-only.` (UI-SPEC Copywriting) — do NOT hard-code "v6000" for a v5000 entry. Body for v6000: the exact `This archive's contents are obfuscated and cannot be decoded in-app. Metadata is available above. Extract with TreeFileExtractor.exe to inspect raw bytes.` (dimmed font, no accent, no red).
    - Parse failure: sections 3-5 replaced by an error panel: heading `Could not decode this file` in `Color.Red`, body `{reason}. The file may be truncated or use an unsupported layout. Other files are unaffected.`
    - Empty (no selection): a centered dimmed `UtinniLabel` heading `No file selected` + body `Select a file in the tree to inspect its structure and contents.`

    All colors via `Colors.*()` accessors — NO `Color.FromArgb` literals. In `FormTreBrowser.Designer.cs`, instantiate `TreDetailPane` docked-fill inside the SplitContainer Panel2 (replacing the empty `pnlDetail` from plan 02).
  </action>
  <verify>
    <automated>cd "D:/Code/UtinniPlugins/The Jawa Toolbox"; msbuild TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj /p:Configuration=Release /p:Platform=x86 /t:Build /v:minimal</automated>
  </verify>
  <acceptance_criteria>
    - TheJawaToolboxDotNet builds Release/x86 with no errors.
    - `TreDetailPane.cs` declares `class TreDetailPane : UserControl` and exposes `LoadIff(IffDocument)`, `ShowReadable`, `ShowEncrypted`, `ShowParseFailure`, `ShowEmpty`, `ShowDecoding` (grep each member).
    - The chunk tree is built from `IffReader.Read` output and renders `OffsetBytes` (grep `IffReader.Read` and `OffsetBytes`); grep finds NO new byte-scan/sentinel loop and no `OBJS` literal.
    - The exact encrypted body copy `Extract with TreeFileExtractor.exe to inspect raw bytes.` appears in source (grep); the v5000 banner uses the 5000-note copy (grep `layout not yet verified`), NOT a hard-coded v6000 string for v5000 (a code branch on `meta.Version` selects the banner text — grep the branch).
    - The parse-failure heading `Could not decode this file` and empty heading `No file selected` appear in source (grep).
    - `Color.Red` is used for the parse-failure heading ONLY; grep finds ZERO `Color.FromArgb` literals in TreDetailPane.cs.
    - The hex peek uses `Consolas` (grep) and caps at 4096 bytes (grep `4096`); the banner uses `Colors.Secondary()` for the 2px accent (grep).
  </acceptance_criteria>
  <done>The detail pane renders metadata + banner + universal IFF chunk tree (with honest @offset from the framework OffsetBytes) + hex peek, degrades cleanly for encrypted (version-accurate) and parse-failure cases, and exposes a reusable IFF-chunk-tree surface for Phase 8.</done>
</task>

<task type="auto">
  <name>Task 3: AfterSelect wiring — reconstruct path, resolve payload via TrePayloadResolver, dispatch to the detail pane</name>
  <files>D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs</files>
  <read_first>
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormObjectBrowser.cs (AfterSelect path reconstruction lines 240-252 — walk parents prepending Text + "/")
    - D:/Code/Utinni/.planning/phases/07-tjt-subpanel-tre-browser-read-only/07-01-SUMMARY.md (TreArchiveIndex.TryGetDescriptor + TrePayloadResolver.Resolve + TreEntryDescriptor.EnumerateOnly contract)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/TreDetailPane.cs (the public Show* API to call — created in Task 2)
    - .planning/phases/07-tjt-subpanel-tre-browser-read-only/07-UI-SPEC.md (States: Loading detail — metadata immediate, content "Decoding…"; Encrypted; Parse failure)
    - .planning/phases/07-tjt-subpanel-tre-browser-read-only/07-RESEARCH.md (Pitfall 4 v6000 obfuscation detection; on-demand single payload read)
  </read_first>
  <action>
    In `FormTreBrowser.cs`, implement `tvTre.AfterSelect`. When a LEAF node is selected: reconstruct the full virtual path by walking parents (FormObjectBrowser pattern), then resolve the entry via the shared facade `TreArchiveIndex.TryGetDescriptor(path, out var d)`. Populate `TreMetadata` from the descriptor (path, Length as size, SourceArchive, Crc hex, Compressor as compression kind, Version) and call `detailPane.ShowDecoding(meta)` immediately so the metadata header shows while the payload resolves (UI-SPEC Loading-detail — metadata is immediate from the already-enumerated descriptor).

    Determine readability: if `d.EnumerateOnly` (v6000/v5000) call `detailPane.ShowEncrypted(meta)` and stop (the banner picks v6000 vs v5000 copy from meta.Version per Task 2). Otherwise resolve the single payload on demand via the shared `TrePayloadResolver.Resolve(d)` (do NOT call TreFile.GetRecordData directly from the UI — go through the shared resolver so the CLI/browser path stays one; D-08, criterion #4). Also guard the Pitfall-4 case: if a non-enumerate-only payload does NOT begin with a printable-ASCII FORM/known tag, treat it as enumerate-only/obfuscated and call `ShowEncrypted(meta)`. Run the resolve + decode on a background `Task` so a large payload does not freeze the UI, then marshal `detailPane.ShowReadable(meta, payload)` via `Control.Invoke`. The detail pane's own try/catch surfaces parse failures via `ShowParseFailure`, so the Form does not catch IffParseException itself — but DO catch `TreParseException`/`IOException` from the resolve and route to `ShowParseFailure(meta, ex.Message)` (a corrupt descriptor must not crash the browser).

    When a directory (non-leaf) node is selected, call `detailPane.ShowEmpty()`. On initial open with nothing selected, the pane shows the empty state.
  </action>
  <verify>
    <automated>cd "D:/Code/UtinniPlugins/The Jawa Toolbox"; msbuild TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj /p:Configuration=Release /p:Platform=x86 /t:Build /v:minimal</automated>
  </verify>
  <acceptance_criteria>
    - TheJawaToolboxDotNet builds Release/x86 with no errors.
    - `FormTreBrowser.cs` wires `AfterSelect` and reconstructs the path by walking `Parent` (grep `AfterSelect` and `.Parent`).
    - The on-demand resolve calls `TrePayloadResolver.Resolve` (grep) and the decode runs off the UI thread (`Task` + `Control.Invoke`); grep finds NO direct `GetRecordData` call in the UI (the UI uses the shared resolver — `grep -c 'GetRecordData' FormTreBrowser.cs` returns 0).
    - Enumerate-only entries route to `ShowEncrypted` (grep `EnumerateOnly` and `ShowEncrypted`); the Pitfall-4 non-FORM-tag obfuscation guard exists (grep the printable-ASCII/FORM check).
    - `TreParseException` and `IOException` from the resolve are caught and routed to `ShowParseFailure` (grep both catch types).
    - No eager full-archive read is introduced in the AfterSelect path (grep confirms no loop over all entries resolving every payload).
  </acceptance_criteria>
  <done>Tree selection reconstructs the path, shows metadata immediately, resolves one payload on demand off-thread via the shared TrePayloadResolver, and dispatches to the readable / encrypted (version-accurate) / parse-failure detail-pane states without ever crashing the browser.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Live-SWG smoke — detail pane metadata + IFF chunk tree (@offset) + encrypted/parse-fail states (PROD-01)</name>
  <action>After tasks 1-3 are auto-complete and both the CLI inspect-iff lane and the TJT build are green, pause for the human to inject Utinni + TJT into a live SWGEmu client and verify the detail pane per the how-to-verify steps. This is the only manual step in this plan; all auto-verifiable work is done in tasks 1-3.</action>
  <what-built>The TRE Browser detail pane: selecting a file shows metadata (path/size/archive/CRC/compression), a type/version banner, the universal IFF chunk tree with TAG · size · @offset, and a collapsed raw-hex peek; encrypted v6000 entries show the enumerate-only info panel and v5000 entries show the 5000-note banner; corrupt files show the parse-failure panel without crashing. (Live-host smoke — PROD-01 "view individual file metadata" + universal chunk tree; no headless harness for the TJT UI host per VALIDATION.)</what-built>
  <how-to-verify>
    1. Build both repos and inject Utinni + TJT into a live SWGEmu client.
    2. Open the TRE Browser, select a readable IFF entry (an SWGEmu 0005/0006 archive entry); confirm the metadata header populates immediately and the IFF chunk tree renders the FORM/chunk hierarchy with TAG · size · @offset (the offsets are real byte positions, not zero/placeholder).
    3. Confirm the type/version banner reads the decoded type + root FORM + version (e.g. `Object Template (CREO / 0006)`) with the thin blue accent rule.
    4. Expand the raw-hex peek; confirm it shows a hex+ASCII dump of the first bytes.
    5. Select an entry from a v6000 Restoration archive; confirm the banner reads `Encrypted payload (v6000) — enumerate-only` and the content sections show the "Extract with TreeFileExtractor.exe" info panel (no crash, metadata still shown). If a v5000 entry is available, confirm its banner uses the 5000-note copy, NOT "v6000".
    6. (If a known-corrupt or truncated file is available) confirm the parse-failure panel shows `Could not decode this file` and the browser stays usable for other files.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. @offset shows 0 for all chunks, encrypted state not triggered, v5000 banner says v6000, panel crash on bad file).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| on-demand payload bytes → IffReader.Read(Stream) | Untrusted IFF binary from a .tre archive; bounds-checking + 64 MB cap enforced inside the shared Formats/Iff reader (no parsing added in the UI). |
| decoded payload → detail-pane rendering | A pathological chunk tree could nest deeply; the IFF reader's NestedChunkOverflow + cap handle depth/size; the UI renders the bounded output only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Denial of Service | pathological IFF nesting / huge chunk in the selected payload | mitigate | IffReader enforces 64 MB MaxChunkSize + NestedChunkOverflow + Truncated checks (existing); the detail pane consumes the bounded parse output only and wraps the parse in try/catch → ShowParseFailure. |
| T-07-10 | Denial of Service | large readable payload freezing the UI thread | mitigate | On-demand resolve + decode runs on a background Task, marshaled via Control.Invoke; metadata shows immediately, content shows "Decoding…" until ready. |
| T-07-11 | Tampering | a corrupt descriptor / truncated payload | mitigate | TreParseException / IOException from TrePayloadResolver caught and routed to ShowParseFailure; one bad file never takes down the browser (UI-SPEC States). |
| T-07-12 | Information Disclosure | clipboard "Copy path/CRC" | accept | Read-only clipboard copy of displayed text; no asset write/export; benign for a local desktop tool. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (BCL + in-repo only; RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- inspect-iff lane green (IffChunk.OffsetBytes populated; flat view preserved); TheJawaToolboxDotNet builds Release/x86.
- Detail pane consumes IffReader output only (no new chunk-walker; Pitfall 7 grep gate) and renders @offset from OffsetBytes.
- Encrypted state is version-accurate (v6000 vs v5000); parse-failure renders the exact UI-SPEC copy.
- On-demand resolve is off-thread, single-payload, via the shared TrePayloadResolver (no direct GetRecordData in UI).
- Live-host smoke (checkpoint) confirms PROD-01 metadata + universal chunk tree.
</verification>

<success_criteria>
- IffChunk.OffsetBytes added + populated; @offset rendered honestly (review consensus / codex MEDIUM).
- Metadata header + type/version banner + universal IFF chunk tree + raw hex peek render on selection (D-12).
- Encrypted v6000/v5000 degrades to the version-accurate enumerate-only info panel (D-07; review LOW banner-accuracy).
- Parse failure is isolated to one file (UI-SPEC States).
- Detail pane shares the inspect-iff read path + the TrePayloadResolver facade (D-08, criterion #4).
- The IFF chunk tree is a reusable surface for Phase 8 (D-13).
</success_criteria>

<output>
Create `.planning/phases/07-tjt-subpanel-tre-browser-read-only/07-03-SUMMARY.md` when done.
</output>
