---
phase: 07-tjt-subpanel-tre-browser-read-only
plan: 01
type: execute
wave: 1
depends_on: ["07-00"]
files_modified:
  - "D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TreFile.cs"
  - "D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TreHeader.cs"
  - "D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TreRecord.cs"
  - "D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TreParseException.cs"
  - "D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TreVersion.cs"
  - "D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/CotMasterIndex.cs"
  - "D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TreArchiveIndex.cs"
  - "D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TrePayloadResolver.cs"
  - "D:/Code/Utinni/Utinni.Cli/Commands/ParseTreCommand.cs"
  - "D:/Code/Utinni/Utinni.Cli/Commands/ListObjectsCommand.cs"
  - "D:/Code/Utinni/Utinni.Cli.Tests/Commands/ParseTreCommandTests.cs"
  - "D:/Code/Utinni/Utinni.Cli.Tests/Commands/CotMasterIndexTests.cs"
  - "D:/Code/Utinni/Utinni.Cli.Tests/Commands/ListObjectsCommandTests.cs"
  - "D:/Code/Utinni/Utinni.Cli.Tests/Commands/TreArchiveIndexTests.cs"
autonomous: true
requirements: [PROD-W1-TRE, PROD-01]

must_haves:
  truths:
    - "The TRE reader dispatches on the 4-char version tag and reads SWGEmu 0004/0005/0006 AND Restoration 6000/COT2000 archives (D-06)"
    - "A dedicated 0004-header golden (ParseTre_0004Header_EnumeratesRecords) asserts synthetic-0004-header.tre enumerates the builder's expected record count/names without throwing — the recognized 0004 tag is not a recognized-but-untested path (review item 8 / Cursor 0004 golden)"
    - "A 5000 version tag is recognized, sets Header.Version=V5000 and EnumerateOnly=true, exposes NO records, and is NEVER routed through the v6000 crc-first stride — a synthetic non-6000-layout 5000 file enumerates empty and does NOT throw (D-06b, review consensus #1)"
    - "v6000 archives enumerate (TOC/names/CRC) but content reads degrade gracefully to enumerate-only because payloads are encrypted (D-07)"
    - "v6000 TOC and name blocks ARE zlib-readable (the reader inflates them to enumerate); only v6000 file PAYLOADS are enumerate-only/obfuscated — the reader does NOT block v6000 compressed-block reads wholesale (review Codex MEDIUM: v6000 enumerate-vs-payload distinction)"
    - "Enumeration reads only TOC + names, never eager-reads all payloads; a single payload is read on demand by index; a test-only PayloadReadCount counter proves parse-tre performs ZERO payload reads (D-08, review consensus #2)"
    - "PayloadReadCount visibility is pinned to internal + [InternalsVisibleTo] the test assembly (NOT public) so it is a test seam, not shipping surface on TreFile (review LOW: PayloadReadCount visibility)"
    - "Open(Stream) lazy contract is explicit: a stream-backed instance with no stored sourcePath throws a documented InvalidOperationException on GetRecordData; an Open(string) instance reads the single record lazily by re-opening the path (review consensus #2)"
    - "Checked arithmetic guards every count/offset before allocation: recordCount > (streamLength - headerSize) / recordStride throws; offset+length and cumulative name offsets use the same division-form guard (review consensus #4 / divergent-view resolution)"
    - "Real zlib RFC1950-framed blocks (0x78 0x9c, %31==0) inflate correctly while existing raw-deflate fixtures still pass; an unknown compressor and a truncated/invalid zlib frame raise documented error kinds (the truncated-frame detection is inflate-side, not Adler-only — review Codex item 11)"
    - "The master-index reader detects BOTH master-index kinds documented in RESEARCH: COT2000 (first 8 bytes == ' COT2000') AND SearchTOC (bytes[0:4]==TAG_TOC 0x20434F54 'TOC ' + bytes[4:8]==TAG_0001), porting tre_reader.py detect_master_index_kind — SearchTOC is NOT advertised-but-untasked (review item 6 / Cursor SearchTOC finding)"
    - "TreEntryDescriptor carries the fields needed to resolve a COT2000/SearchTOC entry back to a physical .tre: master-index base directory, tree-file name + index, the resolved physical archive path, and archive-local offset/length semantics — not just SourceArchive (review consensus #2 / Codex descriptor finding)"
    - "TrePayloadResolver exposes ONE pinned behavior: bool TryResolve(TreEntryDescriptor d, out byte[] payload) returning false (payload null) for an enumerate-only entry and true with bytes for a readable one — NOT a dual return-and-raise; 07-03 AfterSelect aligns to TryResolve (review consensus #3 / resolver contract)"
    - "Shared framework types TreArchiveIndex + TrePayloadResolver are the ONE browse-indexing + payload-resolution path consumed by BOTH the CLI and (in 07-02..04) the browser — criterion #4 is mechanically enforced, not grep-for-classname (review consensus #7)"
    - "list-objects is migrated off the provisional OBJS byte-scan onto the shared IffReader + framework path, so success criterion #4's 'parse-tre AND list-objects' is genuinely lock-step (review consensus #7 / ROADMAP criterion #4 kept intact)"
    - "The browser and the parse-tre/list-objects CLI verbs share one Formats/Tre code path (D-08, success criterion #4)"
    - "swg-client-v2 (tre_reader.py + sample-tre-files.md) is used as a read-to-port format spec only — algorithm ported to C#, no code/identifiers copied, no runtime Python dependency (D-02)"
    - "The reader is a read-only enumeration surface — it exposes no write/authoring/export path; Maya-exporter parity stays a deferred milestone and DEC-A3 stays locked (D-01)"
  artifacts:
    - path: "D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TreVersion.cs"
      provides: "TreVersion enum {V0004,V0005,V0006,V5000,V6000} + Parse(tag) dispatcher + IsEnumerateOnly"
      contains: "enum TreVersion"
    - path: "D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/CotMasterIndex.cs"
      provides: "COT2000 + SearchTOC master-index reader producing the complete cross-archive path enumeration"
      contains: "class CotMasterIndex"
    - path: "D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TreArchiveIndex.cs"
      provides: "shared browse-index facade (flat path list + per-entry descriptor) over CotMasterIndex/TreFile consumed by CLI + browser"
      contains: "class TreArchiveIndex"
    - path: "D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TrePayloadResolver.cs"
      provides: "shared on-demand single-payload resolver (TryResolve(descriptor, out bytes)) consumed by CLI + browser"
      contains: "TryResolve"
    - path: "D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TreFile.cs"
      provides: "version-dispatched, zlib-aware, lazy TOC-only TRE reader with PayloadReadCount seam"
      contains: "PayloadReadCount"
    - path: "D:/Code/Utinni/Utinni.Cli.Tests/Commands/CotMasterIndexTests.cs"
      provides: "golden tests for v6000 + COT2000 + SearchTOC enumeration (in-repo synthetic + env-gated real)"
      contains: "[Fact]"
  key_links:
    - from: "Utinni.Cli/Commands/ParseTreCommand.cs"
      to: "UtinniCoreDotNet.Formats.Tre.TreFile"
      via: "TreFile.Open shared core call"
      pattern: "TreFile\\.Open"
    - from: "Utinni.Cli/Commands/ListObjectsCommand.cs"
      to: "UtinniCoreDotNet.Formats.Iff.IffReader"
      via: "OBJS chunk read via the shared IFF parser, replacing the byte-scan"
      pattern: "IffReader\\.Read"
    - from: "UtinniCoreDotNet/Formats/Tre/TreArchiveIndex.cs"
      to: "UtinniCoreDotNet.Formats.Tre.CotMasterIndex"
      via: "shared browse-index built from the master index / per-archive TOC"
      pattern: "CotMasterIndex|TreFile"
    - from: "UtinniCoreDotNet/Formats/Tre/TrePayloadResolver.cs"
      to: "UtinniCoreDotNet.Formats.Tre.TreFile"
      via: "TryResolve opens the descriptor's resolved physical archive at its archive-local offset"
      pattern: "TryResolve"
    - from: "UtinniCoreDotNet/Formats/Tre/TreFile.cs"
      to: "TreVersion.Parse"
      via: "version-tag dispatch replacing the 0005/0006 hard-fail gate"
      pattern: "TreVersion\\.Parse"
---

<objective>
Refactor the existing eager-read, 0005/0006-only `Formats/Tre` reader into a version-dispatching, zlib-aware, lazy/TOC-only enumerator that reads both client lineages (SWGEmu 0004/0005/0006 and Restoration 6000/COT2000), add the COT2000 + SearchTOC master-index reader (both kinds documented in RESEARCH), and introduce the shared `TreArchiveIndex` + `TrePayloadResolver` framework facade that BOTH the CLI and the browser consume so success criterion #4 is mechanically enforced. Pin the `TrePayloadResolver` contract to a single `TryResolve(descriptor, out byte[])` shape, and give `TreEntryDescriptor` the fields needed to resolve a COT2000/SearchTOC entry back to its physical `.tre` companion archive. Keep the Phase-4 `parse-tre` CLI byte-identical, and migrate `list-objects` off its provisional `OBJS` byte-scan onto the shared `IffReader` path so criterion #4's "parse-tre AND list-objects" clause is honestly met.

Purpose: This is the central engineering refactor of Phase 7 (RESEARCH Pattern 1 + Pitfalls 1-5) and the plan that closes the cross-AI review's HIGH-priority parser-contract concerns: 5000 enumerate-only (no v6000-stride routing), SearchTOC actually tasked (not advertised-but-untasked), a fully-specified COT2000 resolution descriptor, a single pinned resolver contract, the explicit Open(Stream) lazy contract with a deterministic PayloadReadCount counter, named checked-arithmetic guards, and a shared framework browse/payload path (not grep-for-classname) that mechanically ties the CLI and browser together. Everything downstream — the tree, the overlay, the detail pane, the per-type decoders — reads through these shared types.
Output: Extended `TreFile.cs` (+ `TreHeader`/`TreRecord`/`TreParseException`), new `TreVersion.cs` + `CotMasterIndex.cs` (COT2000 + SearchTOC) + `TreArchiveIndex.cs` + `TrePayloadResolver.cs` (TryResolve), extended `parse-tre` verb, migrated `list-objects` verb, and golden tests covering v6000/COT2000/SearchTOC/zlib/lazy/5000-enumerate-only/0004-header/malformed paths (against the 07-00 in-repo synthetic fixtures + env-gated real fixtures).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-tjt-subpanel-tre-browser-read-only/07-CONTEXT.md
@.planning/phases/07-tjt-subpanel-tre-browser-read-only/07-RESEARCH.md
@.planning/phases/07-tjt-subpanel-tre-browser-read-only/07-PATTERNS.md
@.planning/phases/07-tjt-subpanel-tre-browser-read-only/07-00-SUMMARY.md

<interfaces>
<!-- Existing contracts the executor extends. Do NOT explore the codebase to rediscover these. -->

Existing TreFile public surface (UtinniCoreDotNet/Formats/Tre/TreFile.cs):
  public static TreFile Open(string path)
  public static TreFile Open(Stream stream)
  public TreHeader Header { get; }
  public IReadOnlyList<TreRecord> Records { get; }
  public byte[] GetRecordData(int index)     // <-- the lazy-accessor seam to keep
  private const int RecordInfoSize = 24      // <-- must become version-dependent
  private static byte[] ReadBlock(...)       // deflate helper to make zlib-aware
  private static string ReadNullTerminatedAscii(byte[] namesBlock, int offset)
  private static void ValidateLength(long claimed, long maxBound, TreParseError kindOnNegative, TreParseError kindOnOverflow, string fieldName)
  (NOTE: the class XML docstring currently says "REVIEWS HIGH-4 eager-read path A" — that is now
   stale; this plan converts the reader to lazy TOC-only, so the docstring MUST be updated — review LOW.)

Existing version gate to REPLACE (TreFile.cs:130-135):
  if (version != "0005" && version != "0006") throw new TreParseException(TreParseError.UnsupportedVersion, ...)

Existing TreParseError enum (TreParseException.cs) — EXTEND with UnknownCompressor + InvalidZlibTrailer:
  BadMagic, UnsupportedVersion, Truncated, NegativeLength, ChunkLengthExceedsCap, DeflateExpansionExceedsCap

Existing parse-tre verb (Utinni.Cli/Commands/ParseTreCommand.cs):
  [Verb("parse-tre", ...)] -> ParseTreCommand.Run(ParseTreOptions o)
  exit codes: FileNotFound=3, TreParseException=2, IOException=2; generic NOT caught
  BuildResult emits LOCKED schemaVersion:1 keys: header{version,recordCount,infoOffset,infoCompression,
    infoCompressedSize,nameCompression,nameCompressedSize,nameSize}, records[]{id,name,offset,
    compressedSize,uncompressedSize,compressionKind,checksum}

Existing list-objects verb to MIGRATE (Utinni.Cli/Commands/ListObjectsCommand.cs):
  [Verb("list-objects", ...)] -> reads ws.iff, currently via a PROVISIONAL OBJS byte-scan (IndexOfObjs +
  ExtractNullTerminatedStrings). REVIEWS MEDIUM-13 debt. Output is the LOCKED schemaVersion:1 contract:
    { source, objects[]{ id, templateName } } sorted Ordinal by templateName.
  Existing golden fixture: Utinni.Cli.Tests/Fixtures/world-snapshot/synthesized-ws.iff (+ .expected.json)

Existing IFF reader to consume in the migration (UtinniCoreDotNet/Formats/Iff/IffReader.cs):
  IffDocument IffReader.Read(string path) / Read(Stream)
  IffDocument { IffChunk Root; IReadOnlyList<IffChunk> AllNodesInPreorder }
  IffContainerChunk { string TypeId; string SubTypeId; IReadOnlyList<IffChunk> Children }
  IffLeafChunk { string TypeId; byte[] Data }   // OBJS is a LEAF (PROP/OBJS not containers)
  (find the OBJS leaf via AllNodesInPreorder where TypeId == "OBJS"; read its Data as the
   null-terminated template-name block — same payload the byte-scan extracted, now via the parser)

07-00 in-repo synthetic fixtures available (Utinni.Cli.Tests/Fixtures/tre):
  synthetic-v6000-2record.tre, synthetic-cot2000-2tree.toc (+ companion cot2000/tree0.tre + cot2000/tree1.tre),
  synthetic-5000-header.tre, synthetic-0004-header.tre, zlib-framed-1record-v6000.tre,
  malformed-count-stride-overflow.tre, malformed-offset-length-overflow.tre,
  malformed-zlib-bad-adler.tre, malformed-unknown-compressor.tre
  FixturePath.SampleTreDir() / HasSampleTreDir() — env resolver for the real D:/Sample-TRE-Files set
  (the COT2000 master index is SELF-CONTAINED: its global TOC entries resolve to byte positions inside the
   committed companion archives, so TreArchiveIndexTests + TrePayloadResolver.TryResolve readable cases run
   in CI without SWG_SAMPLE_TRE_DIR — review consensus #2.)

VERIFIED v6000 per-TRE layout (07-00 fixture + decoded D:/Sample-TRE-Files/SwgRestoration_06.tre):
  Header(36): 0:"EERT" 4:"6000" 8:numFiles 12:tocOffset 16:tocCompressor(2=zlib)
    20:sizeOfTOC 24:blockCompressor(2=zlib) 28:sizeOfNameBlock 32:uncompSizeOfNameBlock
  TOC entry (32-byte stride, crc-first): 0:u32 crc 4:i32 length 8:i32 offset 12:i32 compressor
    16:i32 compressedLength 20:i32 fileNameOffset 24:8 pad

VERIFIED existing 0005/0006 layout (synthesized fixture, 24-byte stride, size-first):
  i32 dataSize, i32 dataOffset, i32 dataCompression, i32 dataCompressedSize, i32 checksum, i32 nameOffset

VERIFIED master-index detection (tre_reader.py detect_master_index_kind port, RESEARCH "Detecting master-index kind"):
  COT2000:   first 8 bytes == " COT2000" (leading space)
  SearchTOC: bytes[0:4] == TAG_TOC (0x20434F54, ascii "TOC ") AND bytes[4:8] == TAG_0001 (ascii "0001")
  COT2000 header offsets: 8:reserved 12:numFiles 16:sizeOfTocBlock 20:sizeOfNameBlock 24:dupNameBlock
    28:numTreeFiles 32:sizeOfTreeNameBlock 36:null-terminated .tre names (numTreeFiles) -> TOC -> name block
  COT2000 global TOC entry (32): 0:u8 compressor 1:u8 unused 2:u16 treeFileIndex 4:u32 crc 8:u32 fileNameLength
    12:u32 offset 16:u32 length 20:u32 compressedLength 24:8 pad
    (fileNameLength is a LENGTH; cumulative offset: off[0]=0, off[i]=off[i-1]+len[i-1]+1)
    (offset/length/compressedLength are ARCHIVE-LOCAL into the .tre named at treeFileIndex — see descriptor below)

HAZARD: Utinni.Cli.Tests/Fixtures/tre/unsupported-version.tre + its golden assert a tag that
  TreVersion.Parse must STILL reject (so the existing unsupported-version golden stays green).
  Read that fixture's 4-char tag first; if it is now a recognized tag (0004/5000/6000), re-point the
  golden to a tag that remains unknown (e.g. "9999") so the negative test stays meaningful.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Version-dispatch + zlib-aware framing + 5000 enumerate-only + 0004 golden + checked arithmetic (keep 0005/0006 green)</name>
  <files>D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TreVersion.cs, D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TreParseException.cs, D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TreFile.cs, D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TreHeader.cs, D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TreRecord.cs, D:/Code/Utinni/Utinni.Cli.Tests/Commands/ParseTreCommandTests.cs</files>
  <read_first>
    - D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TreFile.cs (the reader being refactored — read the class XML docstring ("eager-read path A" — now stale), the version gate at lines 130-135, the per-record loop at 220-255, the eager-read block at 258-290, the deflate path at 344-345 + ReadBlock at 397-450, ValidateLength at 476-488)
    - D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TreParseException.cs (the kind-enum to extend with UnknownCompressor + InvalidZlibTrailer)
    - D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TreHeader.cs (header model — add Version + EnumerateOnly fields)
    - D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TreRecord.cs (record model)
    - D:/Code/Utinni/Utinni.Cli.Tests/Commands/ParseTreCommandTests.cs (existing 0005/0006 goldens that MUST stay green)
    - D:/Code/Utinni/Utinni.Cli.Tests/Fixtures/tre/unsupported-version.tre (read its 4-char tag — the HAZARD note above)
    - D:/Code/Utinni/.planning/phases/07-tjt-subpanel-tre-browser-read-only/07-00-SUMMARY.md (the synthetic fixtures + their exact byte shapes this task asserts against — including the 0004 header fixture's expected record count/names)
    - .planning/phases/07-tjt-subpanel-tre-browser-read-only/07-RESEARCH.md (Pitfalls 1, 2, 3; "Per-TRE v6000 header")
    - .planning/phases/07-tjt-subpanel-tre-browser-read-only/07-PATTERNS.md (TreFile.cs EXTEND section, TreVersion.cs NEW section)
  </read_first>
  <behavior>
    - Test: TreVersion.Parse("0004"/"0005"/"0006"/"5000"/"6000") returns the matching enum value; an unknown tag (e.g. "9999") throws TreParseException(UnsupportedVersion). IsEnumerateOnly(V5000)==true and IsEnumerateOnly(V6000)==true; IsEnumerateOnly(V0005)==false.
    - Test: opening the existing synthesized 0005 fixture still produces the same Records (size-first order) and parse-tre JSON stays byte-identical to the current golden (no CLI contract change). The unsupported-version golden still fails on its (now-unknown) tag.
    - Test (review item 8 — dedicated 0004 golden): ParseTre_0004Header_EnumeratesRecords opens Fixtures/tre/synthetic-0004-header.tre, asserts it does NOT throw, and asserts the record count + names match the 07-00 builder's emitted values (even though the 0004 record layout is marked real-layout-unverified, the synthetic fixture's own shape is asserted) — so the recognized 0004 tag is a TESTED path.
    - Test: opening Fixtures/tre/synthetic-v6000-2record.tre enumerates with the crc-first 32-byte-stride TOC and resolves both record names from the zlib name block; opening the env-gated D:/Sample-TRE-Files/SwgRestoration_06.tre resolves record 0 to "playback/fire_projectiles_arc.pst" length 344 (Skip when SWG_SAMPLE_TRE_DIR unset).
    - Test: v6000 TOC + name blocks inflate (enumeration succeeds and names resolve) WHILE the v6000 Header.EnumerateOnly==true (payloads are enumerate-only) — proves the reader inflates v6000 metadata blocks but flags payloads as enumerate-only (review Codex enumerate-vs-payload distinction).
    - Test: opening Fixtures/tre/synthetic-5000-header.tre sets Header.Version==V5000 and Header.EnumerateOnly==true, exposes ZERO records, and does NOT throw — even though its record region is a deliberately non-6000 layout (proves 5000 is never routed through the v6000 stride; review consensus #1).
    - Test: a zlib-framed block (0x78 0x9c, %31==0) inflates correctly (synthetic-v6000 + zlib-framed-1record-v6000); a raw-deflate block (existing 0005 fixture) still inflates correctly; malformed-zlib-bad-adler raises Truncated/InvalidZlibTrailer via the inflate-side short-read path (NOT Adler-only validation; review Codex item 11); malformed-unknown-compressor raises UnknownCompressor.
    - Test: malformed-count-stride-overflow throws TreParseException via the division-form guard `recordCount > (streamLength - headerSize) / recordStride` (not OutOfMemory); malformed-offset-length-overflow throws via the `offset <= streamLength - length` guard (no negative-wrap).
  </behavior>
  <action>
    Create `TreVersion.cs` carrying the verbatim MIT + "Format understood by reading swg-client-v2... No code... copied... original to Utinni under MIT" provenance header (copy the block from TreParseException.cs lines 1-26). Define `public enum TreVersion { V0004, V0005, V0006, V5000, V6000 }` and `public static TreVersion Parse(string tag)` mapping the 4-char tag, throwing `TreParseException(TreParseError.UnsupportedVersion, ...)` on an unknown tag. Add `public static bool IsEnumerateOnly(TreVersion v)` returning true for V5000 and V6000 (encrypted/unverified payloads, D-07/D-06b).

    In `TreParseException.cs` add `UnknownCompressor` and `InvalidZlibTrailer` kinds to the enum.

    In `TreFile.cs`: FIRST update the class XML docstring (drop the stale "REVIEWS HIGH-4 eager-read path A" line; document the new lazy TOC-only contract — review LOW). Replace the hard-fail gate (lines 130-135) with `TreVersion version = TreVersion.Parse(versionTag)`. Add `Version` (TreVersion) and `EnumerateOnly` (bool) to `TreHeader`. Make `RecordInfoSize` version-dependent: 24 bytes (size-first) for V0004/V0005/V0006; 32 bytes (crc-first, +8 pad) for V6000. Version-dispatch the per-record decode loop: for V0004/V0005/V0006 keep the EXISTING size-first field order EXACTLY (dataSize, dataOffset, dataCompression, dataCompressedSize, checksum, nameOffset) — do NOT change the path the existing CLI goldens exercise (Open Q1 unresolved); for V6000 read crc-first (crc, length, offset, compressor, compressedLength, fileNameOffset, skip 8 pad bytes) and set `Header.EnumerateOnly = true` (v6000 metadata IS enumerated by inflating its TOC/name zlib blocks; only payload reads are enumerate-only).

    5000 handling (review consensus #1 — apply directly): for V5000 set `Header.EnumerateOnly = true`, expose `Records` as EMPTY, and DO NOT parse any record region — there is NO routing through the V6000 crc-first stride and NO layout assertion. Add a `// TODO(5000-fixture): no record layout — enumerate-empty until a real 5000 fixture/spec exists` marker. A synthetic non-6000-layout 5000 file MUST enumerate empty and never throw.

    Make the deflate path zlib-aware (Pitfall 2): in `ReadBlock` and `GetRecordData`, before constructing `DeflateStream`, inspect the first two bytes — if `b0 == 0x78 && ((b0 << 8) | b1) % 31 == 0`, validate the block has at least 2 header + 4 trailer bytes, skip the 2-byte zlib header, feed the body `[2..^4]` to `DeflateStream`, and treat a short/failed inflate or a body shorter than the framing minimum as `InvalidZlibTrailer`/`Truncated` (do NOT silently return garbage; the detection is the inflate-side short read, NOT an Adler32 comparison — .NET DeflateStream does not validate Adler, review Codex item 11); otherwise feed the bytes unchanged (raw deflate, existing fixtures). A `compressor` value that is neither none(0)/raw-deflate/zlib(2) raises `UnknownCompressor`. Keep both inflate paths so synthesized raw-deflate fixtures stay green and real zlib archives also inflate.

    Checked arithmetic by name (review consensus #4 / divergent-view resolution): before `new List<TreRecord>(recordCount)` (or any count-driven allocation), guard `recordCount < 0` (NegativeLength) and `recordCount > 0 && recordCount > (streamLength - headerSize) / recordStride` (ChunkLengthExceedsCap/Truncated) — the DIVISION form, which cannot overflow. For every record: guard `length >= 0`, `offset >= 0`, and `offset <= streamLength - length` (the subtraction form, never `offset + length`). Reuse/extend `ValidateLength` for these; name each guard's field in the thrown message.

    Add `ParseTreCommandTests.cs` cases for: version dispatch (each tag, including the unknown-tag reject), the dedicated `ParseTre_0004Header_EnumeratesRecords` 0004 golden (review item 8), the synthetic-v6000 + env-gated real enumeration, the v6000 enumerate-vs-payload distinction, the 5000 enumerate-empty/no-throw, the zlib-vs-raw inflate roundtrip, the bad-zlib-frame + unknown-compressor error kinds, and the count*stride + offset+length checked-arithmetic rejections. If `unsupported-version.tre`'s tag is now recognized, re-point its golden to a still-unknown tag. Do NOT modify the parse-tre JSON contract for 0005/0006.
  </action>
  <verify>
    <automated>cd D:/Code/Utinni; dotnet test Utinni.Cli.Tests --filter ParseTre</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test Utinni.Cli.Tests --filter ParseTre` passes including version-dispatch, the 0004-header golden, zlib, 5000-enumerate-only, and the malformed/checked-arithmetic cases.
    - The existing 0005/0006 parse-tre golden JSON is byte-identical (the size-first path is unchanged; `grep -v '^[[:space:]]*//' TreFile.cs | grep` confirms the `dataSize` ... `nameOffset` field order still present for the non-V6000 branch).
    - A dedicated `ParseTre_0004Header_EnumeratesRecords` test exists (grep the test name) and asserts synthetic-0004-header.tre enumerates the builder's expected record count/names without throwing (review item 8).
    - `TreVersion.cs` exists with `enum TreVersion` containing V0004,V0005,V0006,V5000,V6000, `Parse(string)`, and `IsEnumerateOnly`; carries the provenance header (grep "original to Utinni under MIT").
    - V5000 is enumerate-empty: the source has NO call routing V5000 through the V6000 record stride (grep confirms the V5000 branch sets EnumerateOnly and returns empty Records before any record decode); a `TODO(5000-fixture)` marker exists; the synthetic-5000 test asserts zero records + no throw.
    - The v6000 path sets Header.EnumerateOnly=true while STILL inflating TOC/name blocks to enumerate names (a test asserts v6000 names resolve AND EnumerateOnly==true).
    - The class XML docstring no longer says "eager-read path A" (grep `-c 'eager-read path A' TreFile.cs` returns 0; review LOW).
    - The zlib header check `% 31 == 0` and the body slice `[2..^4]` (or equivalent skip-2/drop-4) appear in TreFile.cs (grep excluding comment lines: `grep -v '^[[:space:]]*//' TreFile.cs | grep '% 31'`).
    - The division-form guard `(streamLength - headerSize) / recordStride` (or equivalently named) appears in TreFile.cs (grep, non-comment); a test asserts malformed-count-stride-overflow throws TreParseException not OutOfMemoryException.
    - `UnknownCompressor` and `InvalidZlibTrailer` exist in TreParseError (grep) and each is reachable via a malformed-fixture test.
  </acceptance_criteria>
  <done>TreVersion dispatch + zlib framing + 5000 enumerate-only + a dedicated 0004 golden + named checked arithmetic land; both real-archive and synthesized-fixture inflate paths work; existing CLI goldens unchanged; 5000 enumerate-empty with no v6000-stride routing; v6000 metadata enumerates while payloads stay enumerate-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lazy/Open(Stream) contract + PayloadReadCount + CotMasterIndex (COT2000 + SearchTOC) + TreArchiveIndex/TrePayloadResolver shared facade (resolution-complete descriptor + TryResolve)</name>
  <files>D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TreFile.cs, D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/CotMasterIndex.cs, D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TreArchiveIndex.cs, D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TrePayloadResolver.cs, D:/Code/Utinni/Utinni.Cli/Commands/ParseTreCommand.cs, D:/Code/Utinni/Utinni.Cli.Tests/Commands/CotMasterIndexTests.cs, D:/Code/Utinni/Utinni.Cli.Tests/Commands/TreArchiveIndexTests.cs</files>
  <read_first>
    - D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TreFile.cs (the eager-read loop at 258-290 to make lazy; Open(string)/Open(Stream) factories; GetRecordData at 311; ReadBlock at 397-450; ReadNullTerminatedAscii at 455-469; the AssemblyInfo/InternalsVisibleTo location for the PayloadReadCount seam)
    - D:/Code/Utinni/Utinni.Cli/Commands/ParseTreCommand.cs (the verb that must stay metadata-only — it must trigger ZERO payload reads)
    - D:/Code/Utinni/Utinni.Cli.Tests/Infrastructure/FixturePath.cs (the SampleTreDir/HasSampleTreDir env resolver from 07-00)
    - D:/Code/Utinni/.planning/phases/07-tjt-subpanel-tre-browser-read-only/07-00-SUMMARY.md (synthetic-cot2000-2tree.toc shape + the companion cot2000/tree0.tre+tree1.tre archives + env resolver)
    - .planning/phases/07-tjt-subpanel-tre-browser-read-only/07-RESEARCH.md ("Detecting master-index kind" — COT2000 AND SearchTOC; Anti-Patterns "Eager-reading all payloads"; Pitfall 5)
    - .planning/phases/07-tjt-subpanel-tre-browser-read-only/07-PATTERNS.md (CotMasterIndex.cs NEW section; "Eager-read block to make lazy" note)
  </read_first>
  <behavior>
    - Test: TreFile.Open(string) enumeration populates Records (TOC + names) and PayloadReadCount stays 0; calling GetRecordData(i) increments PayloadReadCount by exactly 1 and re-opens the file by stored sourcePath to read only that record's bytes.
    - Test: TreFile.Open(Stream) populates Records (metadata) with no stored sourcePath; calling GetRecordData throws a documented InvalidOperationException explaining stream-backed lazy reads are unsupported (the contract, review consensus #2) — it does NOT keep the whole stream and does NOT silently eager-read.
    - Test: parse-tre on a multi-record fixture leaves PayloadReadCount == 0 (DETERMINISTIC proof of "no eager read", replacing the timing/IO proxy; review consensus #2). PayloadReadCount is accessible to the test via InternalsVisibleTo, not as public TreFile surface (review LOW).
    - Test: CotMasterIndex.Open detects the COT2000 kind on synthetic-cot2000-2tree.toc (reports 2 TreeFileNames + >=2 Entries each with TreeFileIndex < numTreeFiles) AND detects the SearchTOC kind on a SearchTOC-shaped fixture (bytes[0:4]==TAG_TOC + bytes[4:8]==TAG_0001) — the kind detection is the ported detect_master_index_kind (review item 6).
    - Test: the env-gated real D:/Sample-TRE-Files set reports 213086 total paths and 45 tre names (Skip when unset).
    - Test: a crafted entry with treeFileIndex >= numTreeFiles throws TreParseException; a COT2000 header whose sizeOfTocBlock/sizeOfNameBlock exceeds the stream throws TreParseException, never OutOfMemory.
    - Test: TreArchiveIndex over the synthetic COT2000 exposes a flat IReadOnlyList<string> AllPaths and a TryGetDescriptor(path)->TreEntryDescriptor; each descriptor for a COT2000 entry carries a RESOLVED physical archive path (under cot2000/), its tree-file index + name, and archive-local offset/length — enough to open the companion archive directly.
    - Test: TrePayloadResolver.TryResolve(descriptor, out payload) returns TRUE with decompressed bytes for a readable COT2000 entry (opening cot2000/tree{idx}.tre at the descriptor's archive-local offset — proves the SELF-CONTAINED resolver path runs in CI without env vars, review consensus #2) and returns FALSE with a null payload for a v6000/5000 EnumerateOnly entry WITHOUT attempting decode (single pinned TryResolve contract, review consensus #3).
  </behavior>
  <action>
    Convert the eager-read block in `TreFile.Open` (lines 258-290, the `compressedCache = new byte[recordCount][]` loop) into TOC-only enumeration: parse the TOC + names blocks to build `Records`, but do NOT pre-read every payload. Add a private optional `sourcePath` field set by `Open(string)` and left null by `Open(Stream)`. Implement the explicit lazy contract (review consensus #2): `GetRecordData(int index)` — when `sourcePath != null` re-opens the file (FileShare.Read), seeks to the record offset, reads only that record's compressed bytes, inflates via the Task-1 zlib-aware path, and increments a test-visible `internal int PayloadReadCount { get; private set; }` by 1; when `sourcePath == null` (stream-backed) throws `InvalidOperationException` with a message documenting that stream-backed instances cannot perform lazy payload reads (open via Open(string) for payload access). Pin PayloadReadCount as `internal` and add `[assembly: InternalsVisibleTo("Utinni.Cli.Tests")]` (in the UtinniCoreDotNet AssemblyInfo or a dedicated InternalsVisibleTo.cs) so it is a TEST seam, not public shipping surface on TreFile (review LOW: PayloadReadCount visibility). Preserve the existing `GetRecordData` security caps (256 MB, declared-vs-actual length tamper check) and the WR-01 fresh-copy contract. The parse-tre CLI (metadata-only) MUST leave PayloadReadCount at 0.

    Create `CotMasterIndex.cs` carrying the provenance header. Implement `public static CotMasterIndex Open(string path)` that FIRST detects the master-index kind via a ported `detect_master_index_kind` (review item 6): COT2000 when `first 8 bytes == " COT2000"`; SearchTOC when `bytes[0:4] == 0x20434F54 (TAG_TOC "TOC ")` AND `bytes[4:8] == "0001" (TAG_0001)`. For COT2000, read the header (offsets at 8/12/16/20/24/28/32), the numTreeFiles null-terminated `.tre` names, the (zlib) TOC block of `numFiles` 32-byte global entries, the name block, and reconstruct each entry's full virtual path from `fileNameLength` cumulative offsets. For SearchTOC, port the documented SearchTOC layout from tre_reader.py (the second master-index shape) into the same `CotEntry` model — OR, if a SearchTOC fixture/spec cannot be sourced this session, implement SearchTOC detection that recognizes the kind and throws a documented `TreParseError.UnsupportedVersion`-style "SearchTOC master index not yet supported" error with a `// TODO(searchtoc-fixture)` marker AND a skipped, fixture-gated test — so SearchTOC is explicitly TASKED (recognized + dispositioned), not advertised-but-silent (review item 6). Prefer porting since the algorithm is researched; only fall back to the recognized-and-deferred path if a SearchTOC fixture cannot be produced. Expose `IReadOnlyList<CotEntry> Entries` where `CotEntry` carries `{ string Path, uint Crc, int TreeFileIndex, long Offset, int Length, int CompressedLength, int Compressor }`, `IReadOnlyList<string> TreeFileNames`, and `MasterIndexKind Kind`. Reuse `TreFile`'s block-read + null-terminated-name idioms (copy structure, do not share private members). Bounds-check every field with the same checked-arithmetic + ValidateLength pattern from Task 1: `treeFileIndex < numTreeFiles`, cumulative name offsets via the division-form guard, TOC/name block sizes within the stream length, cap inflate at 256 MB.

    Create the SHARED facade (review consensus #7 — mechanically enforce criterion #4). Define `TreEntryDescriptor` as a small immutable struct that carries the FULL resolution chain (review consensus #2 / Codex descriptor finding), NOT just SourceArchive:
      `{ string Path; uint Crc; TreVersion Version; bool EnumerateOnly; int Compressor;
         long ArchiveLocalOffset; int Length; int CompressedLength;
         string MasterIndexBaseDir; int TreeFileIndex; string TreeFileName;
         string ResolvedArchivePath }`
    where `ResolvedArchivePath` = the physical `.tre` path the resolver opens (for a COT2000 entry: `Path.Combine(MasterIndexBaseDir, TreeFileName)`; for a loose per-archive `.tre`: the archive's own path), and `ArchiveLocalOffset`/`CompressedLength` are the byte position + size INSIDE that resolved archive. `TreArchiveIndex.cs`: `public sealed class TreArchiveIndex` with `static TreArchiveIndex Build(string clientDirOrIndexPath)` that prefers a COT2000/SearchTOC master index when present (via CotMasterIndex, populating MasterIndexBaseDir from the .toc's directory + TreeFileName/Index from each entry) else enumerates per-archive `.tre` files (via TreFile, with ResolvedArchivePath == the archive path), exposing `IReadOnlyList<string> AllPaths` (the FLAT path list 07-02 filters over — paths only, no payloads) and `bool TryGetDescriptor(string path, out TreEntryDescriptor d)`. `TrePayloadResolver.cs`: `public static class TrePayloadResolver` with the SINGLE PINNED contract `public static bool TryResolve(TreEntryDescriptor d, out byte[] payload)` (review consensus #3): when `d.EnumerateOnly` is true, set `payload = null` and return `false` WITHOUT attempting decode; otherwise open `d.ResolvedArchivePath` (FileShare.Read), seek to `d.ArchiveLocalOffset`, read `d.CompressedLength` bytes, inflate via the same TreFile lazy + zlib path, set `payload` and return `true`. Do NOT also expose a throwing `Resolve` overload — TryResolve is the one contract the CLI tests (here) and the browser (07-03 AfterSelect) both call.

    Extend `parse-tre` only as needed to assert PayloadReadCount==0 (no contract change). Add `CotMasterIndexTests.cs` (COT2000 + SearchTOC kind-detection + synthetic + env-gated count goldens + the bounds-check cases) and `TreArchiveIndexTests.cs` (flat AllPaths + TryGetDescriptor with a resolution-complete descriptor + TrePayloadResolver TryResolve readable-TRUE-vs-enumerate-only-FALSE, all over the SELF-CONTAINED synthetic COT2000 fixture + its companion archives — no env var required).
  </action>
  <verify>
    <automated>cd D:/Code/Utinni; dotnet test Utinni.Cli.Tests --filter "ParseTre|CotMasterIndex|TreArchiveIndex"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test Utinni.Cli.Tests --filter "ParseTre|CotMasterIndex|TreArchiveIndex"` passes.
    - The eager `compressedCache = new byte[recordCount][]` loop no longer exists in TreFile.cs (`grep -v '^[[:space:]]*//' TreFile.cs | grep -c 'new byte\[recordCount\]\[\]'` returns 0).
    - `TreFile` exposes `internal int PayloadReadCount` (NOT public; grep `internal` on the property line) and `InternalsVisibleTo("Utinni.Cli.Tests")` exists in UtinniCoreDotNet (grep); a test asserts parse-tre leaves PayloadReadCount at 0 and a single GetRecordData increments it to exactly 1.
    - `Open(Stream)` GetRecordData throws `InvalidOperationException` (asserted in a test); `Open(string)` GetRecordData reads lazily by stored path (asserted).
    - `CotMasterIndex.cs`, `TreArchiveIndex.cs`, `TrePayloadResolver.cs` exist with their classes + provenance headers (grep each class name + "original to Utinni under MIT").
    - CotMasterIndex detects BOTH kinds: grep finds the COT2000 `" COT2000"` check AND the SearchTOC `TAG_TOC`/`0x20434F54`/`0001` check (review item 6); a test asserts SearchTOC is recognized (either parsed or recognized-and-deferred with a skipped fixture-gated test + TODO(searchtoc-fixture) marker — NOT silently dropped).
    - `TreEntryDescriptor` carries the resolution-complete fields (grep `ResolvedArchivePath`, `MasterIndexBaseDir`, `TreeFileIndex`, `TreeFileName`, `ArchiveLocalOffset` — review consensus #2); `SourceArchive`-only is NOT the descriptor.
    - `TrePayloadResolver` exposes exactly `TryResolve(TreEntryDescriptor, out byte[])` and NO throwing `Resolve` overload (grep `TryResolve`; `grep -c 'byte\[\] Resolve' TrePayloadResolver.cs` returns 0 — single pinned contract, review consensus #3).
    - A test builds TreArchiveIndex from synthetic-cot2000-2tree.toc and asserts AllPaths count == the fixture's entry count; TrePayloadResolver.TryResolve returns TRUE+bytes for a readable companion-archive entry (CI, no env var) and FALSE+null for an EnumerateOnly entry.
    - The env-gated COT2000 test asserts 213086 paths / 45 tre names when SWG_SAMPLE_TRE_DIR is set and Skips (not fails) when unset; `treeFileIndex >= numTreeFiles` and oversized-block inputs throw TreParseException (asserted), never OutOfMemoryException.
  </acceptance_criteria>
  <done>Lazy TOC-only enumeration with an explicit Open(Stream) contract + a deterministic internal PayloadReadCount replaces eager caching; CotMasterIndex builds the complete cross-archive set for COT2000 AND SearchTOC with full bounds-checking; the shared TreArchiveIndex/TrePayloadResolver facade gives CLI + browser one provable browse/payload path with a resolution-complete descriptor and a single TryResolve contract.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate list-objects off the OBJS byte-scan onto the shared IffReader path (criterion #4 lock-step)</name>
  <files>D:/Code/Utinni/Utinni.Cli/Commands/ListObjectsCommand.cs, D:/Code/Utinni/Utinni.Cli.Tests/Commands/ListObjectsCommandTests.cs</files>
  <read_first>
    - D:/Code/Utinni/Utinni.Cli/Commands/ListObjectsCommand.cs (the provisional OBJS byte-scan: IndexOfObjs + ExtractNullTerminatedStrings; the LOCKED schemaVersion:1 output contract { source, objects[]{id,templateName} } sorted Ordinal)
    - D:/Code/Utinni/UtinniCoreDotNet/Formats/Iff/IffReader.cs (Read(path)/Read(Stream); AllNodesInPreorder; IffLeafChunk.Data — OBJS is a LEAF chunk)
    - D:/Code/Utinni/UtinniCoreDotNet/Formats/Iff/IffLeafChunk.cs (Data byte[] access)
    - D:/Code/Utinni/Utinni.Cli.Tests/Commands/ListObjectsCommandTests.cs (the existing golden + Fixtures/world-snapshot/synthesized-ws.iff)
    - .planning/phases/07-tjt-subpanel-tre-browser-read-only/07-RESEARCH.md (Anti-Patterns "Naive OBJS byte-scan"; State of the Art "ListObjectsCommand OBJS byte-scan deprecated"; Pitfall 7)
    - .planning/phases/07-tjt-subpanel-tre-browser-read-only/07-PATTERNS.md (ListObjectsCommand anti-pattern note; CLI verb idiom)
  </read_first>
  <action>
    Migrate `list-objects` to read the OBJS template-name block via the shared `IffReader` instead of the provisional sentinel byte-scan (review consensus #7 / ROADMAP criterion #4 — keep "parse-tre AND list-objects" honestly lock-step; do NOT narrow the criterion). Replace `IndexOfObjs`/`ExtractNullTerminatedStrings` with: `IffReader.Read(path)` -> find the `OBJS` leaf chunk in `AllNodesInPreorder` (TypeId == "OBJS") -> extract the null-terminated ASCII template names from its `Data` payload (the SAME bytes the byte-scan read, now via the bounds-checked parser). Preserve the LOCKED schemaVersion:1 output contract EXACTLY: `{ source, objects[]{ id = templateName, templateName } }` sorted Ordinal by templateName, so the existing golden stays byte-identical. Keep the exit-code envelope (FileNotFound=3; IffParseException/IOException=2; NoObjsChunk -> exit 2 when no OBJS leaf is found; generic NOT caught). Delete the now-dead `IndexOfObjs`/`ExtractNullTerminatedStrings` byte-scan helpers and the REVIEWS-MEDIUM-13 debt comment (the debt is now paid).

    Update `ListObjectsCommandTests.cs` so the existing synthesized-ws.iff golden passes against the IffReader path (output unchanged), and add a malformed case (a non-IFF / OBJS-less file) asserting the documented error kind. This makes list-objects share the SAME `Formats/Iff` core the browser detail pane uses (07-03), closing the criterion #4 "list-objects" half.
  </action>
  <verify>
    <automated>cd D:/Code/Utinni; dotnet test Utinni.Cli.Tests --filter "ListObjects"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test Utinni.Cli.Tests --filter "ListObjects"` passes; the synthesized-ws.iff golden JSON is byte-identical to the pre-migration output (LOCKED contract preserved).
    - `ListObjectsCommand.cs` calls `IffReader.Read` and locates the OBJS leaf via the parser (grep `IffReader.Read` and `OBJS`); the byte-scan helpers `IndexOfObjs` and `ExtractNullTerminatedStrings` no longer exist (`grep -c 'IndexOfObjs' ListObjectsCommand.cs` returns 0).
    - The REVIEWS-MEDIUM-13 provisional-debt comment is removed (grep `-c 'MEDIUM-13'` in ListObjectsCommand.cs returns 0).
    - A malformed/OBJS-less input returns the documented exit-2 error kind (asserted).
  </acceptance_criteria>
  <done>list-objects reads through the shared IffReader, the provisional OBJS byte-scan is retired, the locked JSON contract is unchanged, and success criterion #4's "list-objects" half is genuinely lock-step with the browser's IFF path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk `.tre`/`.toc`/`ws.iff` file → TreFile/CotMasterIndex/IffReader parser | Untrusted attacker-influenceable binary (a mod file a user downloaded). All header counts, offsets, sizes, and compressed blocks are adversary-controlled. |
| CLI `parse-tre`/`list-objects` argv → reader | User-supplied path; existing FileNotFound (exit 3) + exception envelope (exit 2) contract. |
| TreEntryDescriptor.ResolvedArchivePath → TrePayloadResolver file open | Path is composed from the master-index base dir + a tree-file name read from the archive; the name is adversary-influenceable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Denial of Service | TreFile/CotMasterIndex header count*stride allocation | mitigate | Division-form checked guard `recordCount > (streamLength - headerSize) / stride` (cannot overflow) before any count-driven allocation; ValidateLength caps every count/size against stream length + 256 MB before allocation; exercised by malformed-count-stride-overflow fixture. |
| T-07-02 | Denial of Service | zlib/deflate inflate (decompression bomb) | mitigate | Inflate output capped at min(256 MB, declared uncompressed size); over-expansion throws DeflateExpansionExceedsCap. |
| T-07-03 | Tampering | out-of-bounds offsets (infoOffset, nameOffset, fileNameOffset, treeFileIndex, per-record offset, ArchiveLocalOffset) | mitigate | Subtraction-form guard `offset <= streamLength - length` (never `offset + length`); `treeFileIndex < numTreeFiles`; cumulative name offsets via division-form guard; the resolver re-validates ArchiveLocalOffset+CompressedLength against the opened companion archive's length before reading; exercised by malformed-offset-length-overflow fixture. |
| T-07-04 | Denial of Service | truncated / malformed archive / bad zlib frame | mitigate | Short-read detection (Truncated) on every block; zlib header validated (`%31==0`) before stripping; truncated/invalid frame raises InvalidZlibTrailer via the inflate-side short read (not Adler-only); unknown compressor raises UnknownCompressor; exercised by malformed-zlib-bad-adler + malformed-unknown-compressor fixtures. |
| T-07-05 | Tampering | path-traversal via crafted virtual path (`../`) OR a crafted tree-file name in the master index resolving ResolvedArchivePath outside the base dir | mitigate | Virtual paths surfaced as data only, never resolved against the filesystem. ResolvedArchivePath is built ONLY by combining the master-index base dir with the tree-file name; the resolver rejects a TreeFileName containing path separators / `..` and confirms the resolved path stays within MasterIndexBaseDir before opening. No extraction in V1. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH Package Legitimacy Audit: BCL + in-repo only); nothing to slopcheck. |
</threat_model>

<verification>
- `dotnet test Utinni.Cli.Tests --filter "ParseTre|CotMasterIndex|TreArchiveIndex|ListObjects"` is green.
- Existing 0005/0006 parse-tre golden + the list-objects golden are byte-identical (CLI contracts preserved); a dedicated 0004 golden + a SearchTOC kind-detection test exist.
- In-repo synthetic fixtures exercise v6000/COT2000(+companion resolution)/5000/0004/zlib/malformed on every CI run; env-gated real fixtures Skip cleanly when absent.
- PayloadReadCount (internal + InternalsVisibleTo) proves parse-tre does zero payload reads; Open(Stream) lazy contract is enforced; 5000 enumerate-empty with no v6000-stride routing.
- TreEntryDescriptor is resolution-complete; TrePayloadResolver exposes the single TryResolve contract; list-objects retired its byte-scan onto IffReader.
</verification>

<success_criteria>
- The TRE reader version-dispatches across 0004/0005/0006 + 5000(enumerate-only)/6000/COT2000 + SearchTOC (D-06/D-06b; review item 6).
- 5000 is recognized, enumerate-empty, never routed through the v6000 stride, never throws on a non-6000 layout (review consensus #1).
- A dedicated 0004 golden tests the recognized 0004 path (review item 8).
- Enumeration is lazy/TOC-only with a deterministic internal PayloadReadCount + explicit Open(Stream) contract (D-08, review consensus #2, LOW visibility).
- Checked arithmetic (division/subtraction form) guards every count/offset before allocation (review consensus #4).
- zlib-framed real archives inflate; raw-deflate fixtures still pass; unknown compressor + truncated zlib frame raise documented kinds (review item 11).
- TreEntryDescriptor resolves COT2000/SearchTOC entries to physical archives; TrePayloadResolver is a single TryResolve contract (review consensus #2/#3).
- CLI (parse-tre + list-objects) and browser share the same Formats/Tre + Formats/Iff core via TreArchiveIndex/TrePayloadResolver (D-08, success criterion #4; review consensus #7).
- The reader stays a read-only enumeration surface — no write/authoring/export path (D-01).
</success_criteria>

<output>
Create `.planning/phases/07-tjt-subpanel-tre-browser-read-only/07-01-SUMMARY.md` when done.
</output>
</objective>
</content>
