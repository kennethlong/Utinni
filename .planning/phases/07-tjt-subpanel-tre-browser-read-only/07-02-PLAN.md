---
phase: 07-tjt-subpanel-tre-browser-read-only
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs"
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.Designer.cs"
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs"
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"
autonomous: false
requirements: [PROD-W1-TRE, PROD-01]

must_haves:
  truths:
    - "The TRE Browser opens as a resizable UtinniForm registered via GetForms(), NOT a 417px SubPanel (D-03, UI-SPEC host decision)"
    - "GetSubPanels() stays null and GetStandalonePanels() keeps the existing control container — the MEF SPI is not widened (CON-M-01/02)"
    - "The user can navigate a SWG virtual-path tree (object/, appearance/, shader/, datatables/, string/...) covering ALL directories and ALL extensions (D-10)"
    - "The client .tre directory source is CONCRETE (review item 7 / CONTEXT line 100): primary = resolve from the injected SWG client (utility.GetWorkingDirectory() / the SWG process directory) and detect *.toc / *.tre; fallback = a documented [TreBrowser] clientDir ini key; if neither resolves, a first-run error state ('Could not locate the client .tre directory — set [TreBrowser] clientDir') is shown rather than a silent empty tree"
    - "Case-insensitive substring search/filter with a 250ms debounce operates on a FLAT path index (TreArchiveIndex.AllPaths) built ONCE at enumeration (paths only, no payloads), NOT a per-tick tree re-walk; matching ancestor chains are pruned-to and expanded (D-11, UI-SPEC, review consensus #5)"
    - "Filter scalability is guarded (review consensus #4): when the matched-path set exceeds a cap (5000), the tree is NOT rebuilt — a flat ListView results mode is shown instead with a status label ('N matches — refine filter'); below the cap, node mutations are batched per top-level branch (one Control.Invoke per branch, not per node); a broad-filter stress check ('.iff' / 'a') is in the acceptance + live smoke"
    - "Whole matching tree nodes are bolded (NodeFont bold) — NOT a substring within the label, which standard WinForms TreeView cannot style without owner-draw (review consensus / divergent-view: substring-bold infeasible)"
    - "The tree is populated lazily (top-level eager, children on BeforeExpand) and built on a background Task; node mutations are marshaled to the UI via Control.Invoke BATCHED per branch — never 213k nodes up front and never one Invoke per node (Pitfall 5, review consensus #4)"
    - "Game.Repository threading contract is EXPLICIT (review item 9): the background-Task overlay harvest reads Game.Repository the SAME deliberate way FormObjectBrowser.LoadRepo does (direct background read of the in-process harvested set), documented as the established pattern; the read is one-shot install-time-snapshot and is never concurrently re-harvested"
    - "Entries present in the live Game.Repository render at full color; on-disk-not-loaded entries render dimmed; the overlay is built via FilenameCount + GetFilenameAt(i) and is an install-time SNAPSHOT — trees mounted after construction show dimmed; this limitation is documented in the legend/copy (D-05 hybrid overlay; review consensus #6)"
    - "The browser reads via the shared TreArchiveIndex (disk truth) + TrePayloadResolver the CLI also uses — no browser-only enumeration path (D-08, success criterion #4)"
    - "Both repos build before the wave is marked complete (review item 10 cross-repo CI gap): the executor builds Utinni Release/x86 AND TheJawaToolboxDotNet Release/x86 against the pinned Utinni output, since Utinni's Actions lane does not compile TJT"
    - "The browser is a WinForms surface; the ImGui chromeless HUD-overlay presentation is deferred to optional later polish — Phase 7 is the explicit exception to the 06-01 HUD directive (D-04)"
  artifacts:
    - path: "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs"
      provides: "TRE Browser Form: virtual-path TreeView + debounced flat-index filter (capped + flat-ListView fallback) + Repository snapshot overlay + async disk load with concrete client-dir resolution"
      contains: "class FormTreBrowser"
    - path: "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.Designer.cs"
      provides: "SplitContainer + themed TreeView + filtered-results ListView + UtinniTextbox filter + status/legend labels"
      contains: "SplitContainer"
    - path: "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs"
      provides: "FormTreBrowser registration in the forms list"
      contains: "FormTreBrowser"
  key_links:
    - from: "FormTreBrowser.cs"
      to: "UtinniCoreDotNet.Formats.Tre.TreArchiveIndex"
      via: "shared-core flat enumeration + descriptor lookup (disk truth)"
      pattern: "TreArchiveIndex"
    - from: "FormTreBrowser.cs"
      to: "Game.Repository"
      via: "read-only loaded-overlay consumption via FilenameCount + GetFilenameAt (CON-N-02)"
      pattern: "Game\\.Repository\\.(FilenameCount|GetFilenameAt)"
    - from: "Plugin.cs"
      to: "forms list"
      via: "forms.Add(new FormTreBrowser(this))"
      pattern: "forms\\.Add\\(new FormTreBrowser"
---

<objective>
Build the TRE Browser shell in The Jawa Toolbox (sibling repo): a resizable `UtinniForm` with a two-region `SplitContainer`, a SWG virtual-path `TreeView` (lazy/on-demand expansion), a debounced case-insensitive substring filter that operates over the shared `TreArchiveIndex.AllPaths` FLAT index (built once, no per-tick re-walk) WITH a match-cap + flat-ListView fallback for broad filters, and the hybrid `Game.Repository` install-time-snapshot overlay (D-05). Resolve the client `.tre` directory CONCRETELY from the injected client (CONTEXT line 100), and read the tree through the shared `TreArchiveIndex` facade delivered by plan 01. Register it via `GetForms()` without widening the MEF SPI.

Purpose: This is the navigation surface PROD-W1-TRE criterion 2 demands and the host pattern Phases 8-11 inherit. It folds in the cross-AI review's #5 (flat path index for search), #4 (filter scalability cap + per-branch batching + broad-filter stress), #6 (Repository snapshot semantics + FilenameCount loop + documented limitation), #7 (concrete client-dir source per CONTEXT line 100), #9 (explicit Game.Repository threading contract), #10 (build both repos before wave complete), and the substring-bold infeasibility finding (bold whole matching nodes, not substrings). The detail pane (plans 03/04) plugs into this Form's right region.
Output: `FormTreBrowser.cs` + `.Designer.cs`, the `Plugin.cs` registration line, and the `.csproj` entry for the new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-tjt-subpanel-tre-browser-read-only/07-CONTEXT.md
@.planning/phases/07-tjt-subpanel-tre-browser-read-only/07-PATTERNS.md
@.planning/phases/07-tjt-subpanel-tre-browser-read-only/07-UI-SPEC.md
@.planning/phases/07-tjt-subpanel-tre-browser-read-only/07-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor consumes. The FormObjectBrowser analog is the direct template. -->

FormObjectBrowser pattern (D:/Code/UtinniPlugins/.../TheJawaToolboxDotNet/UI/Forms/FormObjectBrowser.cs):
  public partial class FormObjectBrowser : UtinniForm, IEditorForm
  ctor(IEditorPlugin editorPlugin): InitializeComponent; load TJT.ico guarded; ini = editorPlugin.GetConfig();
    CreateSettings(); ini.Load(); Width/Height from ini; theme via Colors.*; Task load = LoadRepo();
  CreateSettings(): ini.AddSetting("ObjectBrowser","width","525",UtINI.Value.Types.VtInt) ...
  async Task LoadRepo(): while(!Game.IsRunning) await Task.Delay(1); var dirInfo = Game.Repository.GetDirectoryInfo("object"); ...
    (NOTE review item 9: LoadRepo reads Game.Repository DIRECTLY from this background Task with no Control.Invoke
     marshaling — this is the ESTABLISHED, deliberate pattern for the in-process harvested set. Match it
     deliberately for the overlay harvest; the control mutations from the harvest result ARE marshaled.)
  string GetName() => this.Text;
  Form Create(IEditorPlugin, List<Form> parentChildren): if already-open Activate()+return null; else new+Show()+add
  tvDirectories_AfterSelect: walk parents to rebuild full path
  Tree-build: split dir.Key on '/', nest TreeNodes (lines 192-205)
  Filter (lines 271-295): synchronous Contains() on every TextChanged — ADAPT to debounce + flat index + cap

Client-path resolution analogs (read these to pick the CONCRETE source — review item 7 / CONTEXT line 100):
  UtinniCore `utility` binding (Generated/UtinniCore.cs): utility.GetWorkingDirectory() -> the injected
    client's working directory (where SWG loads .tre); the PRIMARY client-dir source.
  Game / Client bindings: the SWG process directory accessor if utility.GetWorkingDirectory() is unsuitable.
  Existing TJT ini accessor (editorPlugin.GetConfig() / UtINI): the FALLBACK [TreBrowser] clientDir key source.

Plugin.cs registration seam (line 57):
  forms.Add(new FormObjectBrowser(this));   // add: forms.Add(new FormTreBrowser(this));
  GetSubPanels() returns null (line 97) — keep null
  GetStandalonePanels() returns panels (line 92) — keep unchanged

Shared core from plan 01 (UtinniCoreDotNet.Formats.Tre):
  TreArchiveIndex.Build(string clientDirOrIndexPath) -> AllPaths (IReadOnlyList<string>, FLAT, paths only),
    TryGetDescriptor(path, out TreEntryDescriptor)
  TreEntryDescriptor { Path, Crc, Version, EnumerateOnly, Compressor, ArchiveLocalOffset, Length,
    CompressedLength, MasterIndexBaseDir, TreeFileIndex, TreeFileName, ResolvedArchivePath }
  TrePayloadResolver.TryResolve(TreEntryDescriptor, out byte[] payload) -> bool  (single pinned contract; 07-03 uses it)
  TreVersion / TreVersion.IsEnumerateOnly

Game.Repository binding (UtinniCoreDotNet/Generated/UtinniCore.cs):
  Game.Repository.FilenameCount                 // total harvested names — loop this for the overlay
  Game.Repository.GetFilenameAt(int i) -> string
  Game.Repository.GetDirectoryInfo(string dir) -> DirectoryInfo{ StartIndex, Size }  // top-level segment only
  (CON-N-02: consume read-only; the harvest is a DESTRUCTIVE one-shot install-time SNAPSHOT —
   never call getAllFilenames() a second time; trees mounted after install are NOT in the snapshot)

Theming (UtinniCoreDotNet.UI.Theme.Colors — NO raw Color.FromArgb):
  Colors.Primary() #282828, Colors.PrimaryHighlight() #404040, Colors.Font() WhiteSmoke,
  Colors.FontDisabled() #646464, Colors.Secondary() #007ACC (reserved accent)

Controls (UtinniCoreDotNet.UI.Controls): UtinniTextbox, UtinniLabel, UtinniButton, UtinniComboBox,
  UtinniContextMenuStrip, CollapsiblePanel; System.Windows.Forms.{TreeView, ListView, SplitContainer, Timer}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FormTreBrowser shell — UtinniForm + SplitContainer + themed TreeView + filtered-results ListView + debounced capped flat-index filter</name>
  <files>D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.Designer.cs, D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs, D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj</files>
  <read_first>
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormObjectBrowser.cs (the direct template — ctor, CreateSettings, GetName, Create already-open guard, AfterSelect path reconstruction, tree-build split-on-'/', the synchronous filter at 271-295 to ADAPT to debounce + flat index + cap)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormObjectBrowser.Designer.cs (themed TreeView declaration, AutoScaleMode.Font, control wiring, ContextMenuStrip; the Color.FromArgb literals flagged as the anti-pattern to avoid)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj (Compile Include entries for the existing forms — the new files must be registered the same way if not SDK-style globbing)
    - .planning/phases/07-tjt-subpanel-tre-browser-read-only/07-UI-SPEC.md (Window, Two-region split, Left region tree+search, Spacing, Typography — note Bold is "matched substring"; reword to whole-node per the review finding, Color sections)
    - .planning/phases/07-tjt-subpanel-tre-browser-read-only/07-PATTERNS.md (FormTreBrowser.cs + .Designer.cs sections; theming caveat re Color.FromArgb literals; Filter pattern to ADAPT)
  </read_first>
  <action>
    Create `FormTreBrowser.Designer.cs` and `FormTreBrowser.cs` modeled exactly on `FormObjectBrowser`, carrying the verbatim MIT header block. Class signature: `public partial class FormTreBrowser : UtinniForm, IEditorForm`. Window title `TRE Browser`; default size 1100x700; minimum size 760x480 (set `MinimumSize`). Persist `width`/`height`/`splitterDistance` under a `[TreBrowser]` ini section via `CreateSettings()` (mirror `ini.AddSetting("TreBrowser","width","1100",UtINI.Value.Types.VtInt)` etc.). Load the TJT icon with the same guarded path block as FormObjectBrowser (lines 84-90).

    Designer layout: a single `SplitContainer` (`Orientation = Vertical`, `BackColor = Colors.Primary()`, SplitterWidth 4, default `SplitterDistance` ~360, `Panel1MinSize = 240`, `Panel2MinSize = 420`) filling the client area. Panel1 (left): a `UtinniTextbox txtFilter` docked top (height 22, 3px inset, `BackColor = Colors.PrimaryHighlight()`), an optional `UtinniComboBox cbTypeFacet` ("All types") inline to its right (120px, V1-optional — single "All types" entry acceptable), a themed `TreeView tvTre` docked fill (`BackColor = Colors.PrimaryHighlight()`, `ForeColor = Colors.Font()`, `BorderStyle = None`, `HideSelection = false`, `ShowLines = true`), a themed `ListView lvFiltered` docked fill (Details, single `Path` column, `Visible = false` by default — the broad-filter results mode, review consensus #4), and a small `UtinniLabel lblStatus`/`UtinniLabel lblLegend` docked bottom for the match-count status + overlay legend. Panel2 (right): a `Panel pnlDetail` (`AutoScroll = true`, `BackColor = Colors.Primary()`) — left empty in this plan (plan 03 fills it) but present so the split is real. ALL colors via `Colors.*()` accessors — NO `Color.FromArgb` literals.

    Implement `GetName()` returning `this.Text`, and `Create(IEditorPlugin, List<Form> parentChildren)` copying the FormObjectBrowser already-open guard verbatim (typeof(FormTreBrowser) check -> Activate()+return null, else new+Show()+add).

    Implement the debounced, CAPPED filter over the FLAT shared index (review consensus #5 + #4): hold the `IReadOnlyList<string> _allPaths` reference from `TreArchiveIndex.AllPaths` (populated in Task 2). A single restarting `System.Windows.Forms.Timer dbFilter` (Interval = 250). `txtFilter.TextChanged` calls `dbFilter.Stop(); dbFilter.Start();`. `dbFilter.Tick` stops the timer and re-runs the filter: scan the FLAT `_allPaths` once with `IndexOf(text, StringComparison.OrdinalIgnoreCase) >= 0` (NOT a recursive tree re-walk per tick), collecting matches. Define `const int FilterMatchCap = 5000`. If the match count exceeds `FilterMatchCap`: do NOT rebuild the TreeView — hide `tvTre`, show `lvFiltered` populated (virtual-mode or capped to the first N) with the matching paths, and set `lblStatus` to `"{count} matches — refine filter"` (review consensus #4 broad-filter guard). If the match count is within the cap: show `tvTre`, hide `lvFiltered`, project the matching paths to ancestor chains, prune the tree to those chains, expand them, and set `NodeFont` Bold on the WHOLE matching leaf node (NOT a substring — standard WinForms TreeView cannot bold a substring without owner-draw; this is the only Bold use per UI-SPEC Typography, reworded to whole-node). When rebuilding the pruned tree, BATCH node additions per top-level branch (build each branch's nodes off-thread as plain models, then add the whole branch in one `tvTre.Nodes` mutation — not one mutation per node). Empty filter shows the full hierarchy, hides `lvFiltered`, clears bold, clears `lblStatus`. Persist split distance on `FormClosing`.

    If the project is non-SDK-style (`<Compile Include=...>` entries), add `FormTreBrowser.cs`, `FormTreBrowser.Designer.cs` (with `<DependentUpon>`) to `TheJawaToolboxDotNet.csproj`; if SDK-style globbing, no csproj edit is needed.
  </action>
  <verify>
    <automated>cd "D:/Code/UtinniPlugins/The Jawa Toolbox"; msbuild TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj /p:Configuration=Release /p:Platform=x86 /t:Build /v:minimal</automated>
  </verify>
  <acceptance_criteria>
    - The TheJawaToolboxDotNet project builds in Release/x86 with no errors.
    - `FormTreBrowser.cs` declares `class FormTreBrowser : UtinniForm, IEditorForm` and implements `GetName()` + `Create(...)` (grep both members).
    - `FormTreBrowser.Designer.cs` contains a `SplitContainer` with `Orientation` Vertical, a `TreeView`, and a `ListView lvFiltered` (the broad-filter results mode); grep confirms `Colors.Primary()` / `Colors.PrimaryHighlight()` usage and `grep -c 'Color.FromArgb' FormTreBrowser*.cs` returns 0.
    - A `Timer` with `Interval = 250` exists for filter debounce (grep `Interval` and `250`).
    - The filter scans a flat path collection (grep finds `_allPaths` or equivalent flat-list field used by the Tick handler) and sets `NodeFont` Bold on whole nodes (grep `NodeFont`); there is NO substring-highlight/owner-draw code.
    - A `FilterMatchCap` (= 5000) exists and the Tick handler switches to `lvFiltered` + a `"{count} matches — refine filter"` status when exceeded (grep `FilterMatchCap`, `matches — refine filter`); below the cap the tree rebuild batches per branch (grep a per-branch batch comment / one-Invoke-per-branch structure, not per-node).
    - `MinimumSize` is set to 760x480 (grep); the already-open guard checks `typeof(FormTreBrowser)` (grep).
  </acceptance_criteria>
  <done>The Form shell builds, hosts a real two-region split with a themed lazy-ready TreeView, a broad-filter ListView fallback, and a debounced CAPPED filter that scans the flat shared index (not a per-tick tree re-walk), bolds whole matching nodes, and switches to a flat results list past 5000 matches; conforms to UI-SPEC theming/spacing with no color literals.</done>
</task>

<task type="auto">
  <name>Task 2: Concrete client-dir resolution + shared-index disk enumeration + lazy tree population (per-branch batched) + Game.Repository snapshot overlay</name>
  <files>D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs, D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs</files>
  <read_first>
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormObjectBrowser.cs (LoadRepo async pattern lines 130-212; Game.Repository usage — NOTE the deliberate direct background read, review item 9; tree-build nesting; AfterSelect path reconstruction)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs (the forms.Add registration seam at line 57; GetSubPanels null at line 97)
    - D:/Code/Utinni/UtinniCoreDotNet/Generated/UtinniCore.cs (utility.GetWorkingDirectory() + the Game/Client process-dir accessor — the PRIMARY client-dir source, review item 7)
    - D:/Code/Utinni/.planning/phases/07-tjt-subpanel-tre-browser-read-only/07-01-SUMMARY.md (the TreArchiveIndex.Build / AllPaths / TryGetDescriptor + TrePayloadResolver.TryResolve shared facade delivered by plan 01)
    - .planning/phases/07-tjt-subpanel-tre-browser-read-only/07-RESEARCH.md (Pattern 4 virtual-path tree + async; Pitfall 5 100k+ entries; Runtime State — destructive one-shot install-time snapshot harvest)
    - .planning/phases/07-tjt-subpanel-tre-browser-read-only/07-PATTERNS.md (Live Repository overlay section — GetDirectoryInfo only indexes the first path segment; Game-thread vs UI-thread marshaling section)
    - .planning/phases/07-tjt-subpanel-tre-browser-read-only/07-UI-SPEC.md (Lazy/on-demand expansion REQUIRED; Loaded/resolvable overlay; States table: Loading tree, Overlay unavailable)
  </read_first>
  <action>
    CONCRETE client-dir resolution (review item 7 / CONTEXT line 100 — this is NOT a new product decision; it implements "TRE mount list comes from the injected client's config where SWG loads .tre"). Add a private `ResolveClientTreDir()` that returns the directory to hand to `TreArchiveIndex.Build`, in this priority order: (1) PRIMARY — `utility.GetWorkingDirectory()` (the injected SWG client's working directory) or, if that is empty/unsuitable, the SWG process directory; detect a `*.toc` master index or loose `*.tre` files there. (2) FALLBACK — a documented `[TreBrowser] clientDir` ini key (added via `CreateSettings()`); if set, use it. (3) If NEITHER resolves to a directory containing `*.toc`/`*.tre`, do NOT silently show an empty tree — set the tree's status/legend to the first-run error copy `Could not locate the client .tre directory — set [TreBrowser] clientDir` and leave the tree empty pending configuration. Document this resolution order in a code comment citing CONTEXT line 100.

    In `FormTreBrowser.cs`, add the shared-index disk enumeration + tree population. Build the enumeration via the shared `TreArchiveIndex.Build(ResolveClientTreDir())` facade from plan 01 (it internally prefers a COT2000/SearchTOC master index, else per-archive `.tre` enumeration) — do NOT call CotMasterIndex/TreFile directly from the UI; the UI consumes ONLY TreArchiveIndex/TrePayloadResolver so the browser and CLI provably share one path (D-08, criterion #4; review consensus #7). Run `TreArchiveIndex.Build` on a background `Task` (NOT the UI thread); show the `Loading archive index…` status label while it runs; keep the window responsive (no modal). Store `TreArchiveIndex.AllPaths` into the `_allPaths` field the Task-1 filter scans.

    Tree population MUST be lazy (Pitfall 5, UI-SPEC): on the background task, build only the top-level directory node set from `AllPaths`; for each directory node with children, add a single dummy child node so the expand glyph shows. Marshal `tvTre.Nodes` mutations to the UI thread via `Control.Invoke` BATCHED per top-level branch (one Invoke per branch, NOT one per node — review consensus #4 / Codex many-small-invokes finding). Implement `tvTre.BeforeExpand` to replace the dummy child with the real children for that directory on demand (split virtual paths on '/' using the FormObjectBrowser nesting algorithm, generalized to ALL directories and ALL extensions). Leaf nodes carry a bracketed type-tag suffix derived from extension (`[IFF] [TAB] [STF] [TMPL] [MESH] [SKEL] [ANIM] [SHDR] [-]`) per UI-SPEC; use the text-tag mechanism consistently.

    Implement the loaded/resolvable overlay (D-05) with the corrected snapshot semantics (review consensus #6) AND the explicit threading contract (review item 9): after `Game.IsRunning` becomes true, build a `HashSet<string>` of loaded paths by looping `for (int i = 0; i < Game.Repository.FilenameCount; i++) loaded.Add(Game.Repository.GetFilenameAt(i));` — read DIRECTLY on the background Task the SAME deliberate way `FormObjectBrowser.LoadRepo` reads the in-process harvested set (read-only, CON-N-02 — do NOT modify the native hook, do NOT call getAllFilenames() a second time, do NOT concurrently re-harvest). Add a code comment stating this matches the established FormObjectBrowser background-read pattern for the one-shot snapshot (review item 9 — pin the thread-safety story explicitly). Use `FilenameCount`+`GetFilenameAt`, NOT a top-level `GetDirectoryInfo` walk (which only indexes the first path segment and would under-harvest). Color tree leaf nodes (marshaled to UI per branch): present-in-loaded -> `ForeColor = Colors.Font()`; on-disk-not-loaded -> `ForeColor = Colors.FontDisabled()`. Set the legend label to `Dimmed = on disk, not currently loaded` when a live session exists; document the install-time-snapshot limitation in the legend/tooltip copy (entries mounted AFTER the client built its harvest snapshot show dimmed even if later resolvable — the overlay reflects the install-time snapshot). When `!Game.IsRunning`, render all nodes full-color and set the legend to `Overlay unavailable — no live client`. The overlay is best-effort — disk enumeration is source of truth; if the Repository read throws or is empty, fall back to all-full-color + the no-client legend (never crash the tree).

    Add the registration line to `Plugin.cs`: `forms.Add(new FormTreBrowser(this));` immediately after `forms.Add(new FormObjectBrowser(this));` (line 57). Leave `GetSubPanels()` returning null and `GetStandalonePanels()` unchanged (CON-M-01/02 — do not widen the SPI).
  </action>
  <verify>
    <automated>cd "D:/Code/UtinniPlugins/The Jawa Toolbox"; msbuild TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj /p:Configuration=Release /p:Platform=x86 /t:Build /v:minimal</automated>
  </verify>
  <acceptance_criteria>
    - The TheJawaToolboxDotNet project builds in Release/x86 with no errors.
    - The client-dir source is concrete: grep finds `ResolveClientTreDir` and `GetWorkingDirectory` (the primary source) AND the fallback `[TreBrowser] clientDir` ini key AND the first-run error copy `Could not locate the client .tre directory` (review item 7 / CONTEXT line 100 cited in a comment).
    - `Plugin.cs` contains `forms.Add(new FormTreBrowser(this));` and `GetSubPanels()` still returns null (grep both; no new GetSubPanels body change).
    - `FormTreBrowser.cs` consumes the shared facade: grep finds `TreArchiveIndex` (disk enumeration) and finds NO direct `CotMasterIndex` or `TreFile.Open` call in the UI (the UI goes through TreArchiveIndex only — `grep -c 'CotMasterIndex' FormTreBrowser.cs` returns 0).
    - The overlay loops `FilenameCount` + `GetFilenameAt`: grep finds `Game.Repository.FilenameCount` and `Game.Repository.GetFilenameAt`; grep finds NO `getAllFilenames` (`grep -c getAllFilenames` returns 0); a comment documents the deliberate FormObjectBrowser-matching background-read threading contract (grep `established` or `FormObjectBrowser` near the harvest, review item 9).
    - Enumeration runs off the UI thread: grep finds `Task` and `Control.Invoke` (or `BeginInvoke`) in the load path; node mutations are batched per branch (grep the per-branch batch structure, not a per-node Invoke loop); lazy expansion exists (grep `BeforeExpand`).
    - Both legend strings appear in source: `Overlay unavailable — no live client` and `Dimmed = on disk, not currently loaded` (grep); the install-time-snapshot limitation is documented in a code comment or tooltip copy (grep `snapshot`).
  </acceptance_criteria>
  <done>The tree populates lazily from disk via the shared TreArchiveIndex (client dir resolved concretely from the injected client per CONTEXT line 100, with a documented fallback + first-run error state) on a background thread, marshals safely to the UI batched per branch, covers all directories/extensions, and overlays live-loaded state best-effort via the read-only FilenameCount+GetFilenameAt install-time snapshot read the established FormObjectBrowser way, with the limitation documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Build BOTH repos + Live-SWG smoke — TRE Browser shell + tree + filter scale + overlay (PROD-W1-TRE criteria 1+2)</name>
  <action>After tasks 1-2 are auto-complete, the executor MUST build BOTH repos before pausing (review item 10 cross-repo CI gap — Utinni's Actions lane does not compile TJT): build Utinni Release/x86 AND TheJawaToolboxDotNet Release/x86 against the pinned Utinni output, confirming the new TreArchiveIndex/TrePayloadResolver framework surface compiles against the TJT consumer. THEN pause for the human to inject Utinni + TJT into a live SWGEmu client and verify the browser shell per the how-to-verify steps. This is the only manual step in this plan; all auto-verifiable work (both builds, grep gates, structural checks) is done in tasks 1-2 + the build gate above.</action>
  <what-built>The TRE Browser Form registered in TJT: a resizable window with a virtual-path tree (lazy expand, per-branch batched), debounced flat-index filter (whole-node bold, 5000-match cap → flat ListView results mode), type tags, and the Game.Repository install-time-snapshot loaded/dimmed overlay. Disk enumeration runs off-thread via the shared TreArchiveIndex facade with the client dir resolved concretely from the injected client. (PROD-W1-TRE criterion 1+2 live-host smoke — no headless harness for the TJT UI host, per VALIDATION Manual-Only Verifications.)</what-built>
  <how-to-verify>
    1. Build both repos (Utinni Release/x86 + TheJawaToolboxDotNet Release/x86) and deploy the TJT plugin alongside Utinni (the build-both gate above must already be green — review item 10).
    2. Inject Utinni + TJT into a live SWGEmu client (the cross-repo paired commit is pre-authorized; only this live smoke needs the human checkpoint per the cross-repo write-authority memory).
    3. Open the TRE Browser from the TJT forms menu. Confirm it opens as a resizable window (not a 417px fixed panel) titled `TRE Browser`, and the tree POPULATES WITHOUT MANUAL PATH ENTRY (the client dir resolved automatically per CONTEXT line 100; review item 7).
    4. Confirm the tree shows SWG virtual-path directories (object/, appearance/, shader/, datatables/, string/...); expand a few subtrees and confirm children load on expand without a multi-second freeze.
    5. Type a narrow substring in the filter; confirm the tree prunes/expands to matches within ~250ms and the whole matching nodes are bolded. THEN type a BROAD filter (`.iff` or `a`); confirm it does NOT freeze — it shows the flat results ListView with a `"{count} matches — refine filter"` status instead of rebuilding the whole tree (review consensus #4 stress check).
    6. Confirm loaded entries render at full color and on-disk-not-loaded entries render dimmed, with the legend `Dimmed = on disk, not currently loaded`. (Without a loaded scene, confirm `Overlay unavailable — no live client`.)
    7. Resize the window and the splitter; confirm size/splitter persist across close+reopen.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. tree empty / manual-path needed, broad-filter freeze, missing directories, overlay miscolor).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk `.tre`/COT2000 index → TreArchiveIndex (consumed by the Form) | Untrusted binary; all bounds-checking is enforced in the plan-01 framework core the Form consumes (no parsing logic added in the UI layer). |
| injected client working dir → ResolveClientTreDir | A path read from the injected client; used to locate `.tre`/`.toc`; the resolver only enumerates files in that dir, never executes anything. |
| live `Game.Repository` (in-process) → Form overlay read | In-process trusted data harvested by the native detour; read-only consumption via FilenameCount+GetFilenameAt, no native modification (CON-N-02). |
| game thread ↔ UI thread | Cross-thread access to WinForms controls and game state; node mutations marshaled via Control.Invoke (batched per branch); the harvest read matches the established FormObjectBrowser background-read pattern. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Denial of Service | UI freeze / OOM building 213k tree nodes OR a per-tick filter re-walk OR a broad-filter full-tree rebuild | mitigate | Lazy tree (top-level eager, BeforeExpand children, dummy-child glyph); enumeration on a background Task marshaled via Control.Invoke BATCHED per branch; filter scans the flat AllPaths index ONCE per debounced tick; broad filters (> 5000 matches) switch to a flat ListView results mode + a refine-filter status instead of rebuilding the tree — never build all nodes synchronously on the UI thread, never one Invoke per node. |
| T-07-07 | Tampering | crafted virtual path (`../`) surfaced in the tree | mitigate | Paths are displayed as TreeNode text only; never resolved against the filesystem (read-only browse, no extraction in V1). |
| T-07-08 | Information Disclosure / crash | stale or empty Repository harvest snapshot | accept/mitigate | Overlay is best-effort install-time snapshot; disk enumeration is source of truth; a failed/empty Repository read degrades to all-full-color + no-client legend rather than crashing (RESEARCH A5); the snapshot limitation is documented in the legend copy. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (BCL + in-repo controls only; RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- BOTH repos build Release/x86 (Utinni + TheJawaToolboxDotNet) before the wave is marked complete (review item 10).
- Plugin.cs registers FormTreBrowser via forms.Add; SPI unchanged (GetSubPanels null).
- Client dir resolved concretely (GetWorkingDirectory primary + [TreBrowser] clientDir fallback + first-run error); filter scans the flat shared index with a 5000-match cap + flat-ListView fallback; overlay uses FilenameCount+GetFilenameAt read the established FormObjectBrowser way; whole-node bold; off-thread lazy expansion batched per branch — all present (grep gates).
- Live-host smoke (checkpoint) confirms PROD-W1-TRE criteria 1+2 including auto-populate (no manual path) + broad-filter no-freeze.
</verification>

<success_criteria>
- TRE Browser opens as a resizable UtinniForm via GetForms() (D-03, UI-SPEC).
- The client .tre directory resolves concretely from the injected client (CONTEXT line 100; review item 7), with a documented fallback ini key + first-run error state.
- Tree covers all directories + extensions over the SWG virtual-path hierarchy (D-10), populated lazily, batched per branch.
- Search/filter is case-insensitive substring with 250ms debounce over the flat TreArchiveIndex.AllPaths index (D-11; review consensus #5); whole matching nodes bolded (review divergent-view); guarded at 5000 matches with a flat-ListView fallback (review consensus #4).
- Game.Repository overlay marks loaded vs on-disk entries via FilenameCount+GetFilenameAt as an install-time snapshot read the established FormObjectBrowser way (D-05; review consensus #6 + item 9), best-effort, read-only (CON-N-02).
- Browser and CLI share the TreArchiveIndex/TrePayloadResolver facade (D-08, criterion #4); both repos build before wave complete (review item 10).
</success_criteria>

<output>
Create `.planning/phases/07-tjt-subpanel-tre-browser-read-only/07-02-SUMMARY.md` when done.
</output>
</objective>
</content>
