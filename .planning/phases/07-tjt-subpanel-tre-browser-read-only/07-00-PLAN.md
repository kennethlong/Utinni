---
phase: 07-tjt-subpanel-tre-browser-read-only
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - "D:/Code/Utinni/Utinni.Cli.Tests/Infrastructure/FixturePath.cs"
  - "D:/Code/Utinni/Utinni.Cli.Tests/Fixtures/tre/zlib-framed-1record-v0006.tre"
  - "D:/Code/Utinni/Utinni.Cli.Tests/Fixtures/tre/synthetic-v6000-2record.tre"
  - "D:/Code/Utinni/Utinni.Cli.Tests/Fixtures/tre/synthetic-cot2000-2tree.toc"
  - "D:/Code/Utinni/Utinni.Cli.Tests/Fixtures/tre/synthetic-5000-header.tre"
  - "D:/Code/Utinni/Utinni.Cli.Tests/Fixtures/tre/synthetic-0004-header.tre"
  - "D:/Code/Utinni/Utinni.Cli.Tests/Fixtures/tre/malformed-count-stride-overflow.tre"
  - "D:/Code/Utinni/Utinni.Cli.Tests/Fixtures/tre/malformed-offset-length-overflow.tre"
  - "D:/Code/Utinni/Utinni.Cli.Tests/Fixtures/tre/malformed-zlib-bad-adler.tre"
  - "D:/Code/Utinni/Utinni.Cli.Tests/Fixtures/tre/malformed-unknown-compressor.tre"
  - "D:/Code/Utinni/Utinni.Cli.Tests/Infrastructure/TreFixtureBuilder.cs"
  - "D:/Code/Utinni/Utinni.Cli.Tests/Infrastructure/TreFixtureBuilderTests.cs"
autonomous: true
requirements: [PROD-W1-TRE, PROD-01]

must_haves:
  truths:
    - "Small in-repo synthetic TRE/COT2000 fixtures exist that exercise the v6000 crc-first zlib TOC/name-block, a COT2000 master index with >=2 tree files, a synthetic 5000 header, and a synthetic 0004 header — so the real-archive code paths run on CI without depending on the env-gated D:/Sample-TRE-Files set (review consensus #3)"
    - "Malformed fixtures exist for count*stride overflow, offset+length overflow, truncated/invalid-Adler zlib trailer, and unknown compressor — the bounds-check and zlib-validation paths are CI-exercised, not only env-gated (review consensus #3)"
    - "FixturePath exposes a SWG_SAMPLE_TRE_DIR resolver returning the env path or null, so the large real-fixture (213086-path COT2000) tests skip cleanly when the sample set is absent — the env-gate is a SUPPLEMENT to in-repo fixtures, not the only coverage (review consensus #3/#4)"
    - "The synthetic 5000 fixture is built with a deliberately NON-6000 record layout so 07-01's '5000 must not throw, only enumerate-empty' acceptance is provable against a real artifact (review consensus #1)"
    - "Fixtures are produced by a deterministic in-repo byte builder (TreFixtureBuilder), not source string literals, so they are reproducible and avoid encoding churn (review LOW: non-ASCII fixture guidance)"
    - "07-00 completion is the Wave-0 gate that 07-01 depends on; 07-VALIDATION Wave 0 items are reconciled from [ ] assumptions to artifacts this plan delivers (review consensus #4)"
    - "Fixtures are read-only test inputs only — no write/authoring/export surface is added; DEC-A3 stays locked (D-01)"
  artifacts:
    - path: "D:/Code/Utinni/Utinni.Cli.Tests/Infrastructure/TreFixtureBuilder.cs"
      provides: "deterministic byte builder for synthetic v6000/COT2000/5000/0004/malformed TRE fixtures"
      contains: "class TreFixtureBuilder"
    - path: "D:/Code/Utinni/Utinni.Cli.Tests/Infrastructure/FixturePath.cs"
      provides: "fixture-path helper + SWG_SAMPLE_TRE_DIR env resolver (path-or-null)"
      contains: "SWG_SAMPLE_TRE_DIR"
    - path: "D:/Code/Utinni/Utinni.Cli.Tests/Fixtures/tre/synthetic-v6000-2record.tre"
      provides: "in-repo v6000 crc-first zlib TOC/name-block fixture"
      contains: ""
    - path: "D:/Code/Utinni/Utinni.Cli.Tests/Fixtures/tre/synthetic-cot2000-2tree.toc"
      provides: "in-repo COT2000 master index with >=2 referenced tree files"
      contains: ""
    - path: "D:/Code/Utinni/Utinni.Cli.Tests/Fixtures/tre/synthetic-5000-header.tre"
      provides: "synthetic 5000 header with deliberately non-6000 record layout (enumerate-empty target)"
      contains: ""
  key_links:
    - from: "Utinni.Cli.Tests/Infrastructure/TreFixtureBuilder.cs"
      to: "Utinni.Cli.Tests/Fixtures/tre/*.tre"
      via: "deterministic byte emission of each synthetic fixture"
      pattern: "TreFixtureBuilder"
    - from: "Utinni.Cli.Tests/Infrastructure/FixturePath.cs"
      to: "SWG_SAMPLE_TRE_DIR"
      via: "env-var resolver returning the real-fixture dir or null for clean skip"
      pattern: "SWG_SAMPLE_TRE_DIR"
---

<objective>
Create the Wave-0 fixture foundation the rest of Phase 7 depends on: a deterministic in-repo byte builder (`TreFixtureBuilder`) plus the small synthetic TRE/COT2000 fixtures it emits (v6000 crc-first zlib TOC, COT2000 master index with >=2 tree files, a non-6000-layout 5000 header, a 0004 header, and four malformed-input fixtures), and the `FixturePath` `SWG_SAMPLE_TRE_DIR` env resolver so the large real-fixture goldens skip cleanly. This is the gate that closes the cross-AI review's #3/#4 consensus concern (CI permanently skips the real COT2000/v6000 path because everything was env-gated) and reconciles the `07-VALIDATION.md` Wave-0 `[ ]` items into real artifacts.

Purpose: Both reviewers rated this a HIGH-priority prerequisite — 07-01 assumed fixtures existed that were still unchecked Wave-0 assumptions, so executors would hit red CI or skip-heavy green. Producing the synthetic fixtures here, in their own Wave-0 plan, means 07-01's TDD tasks have real bytes to assert against on every CI run (not only when `D:/Sample-TRE-Files` is present).
Output: `TreFixtureBuilder.cs` + its self-test, nine synthetic `.tre`/`.toc` fixtures under `Fixtures/tre/`, and the `FixturePath` env resolver.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-tjt-subpanel-tre-browser-read-only/07-CONTEXT.md
@.planning/phases/07-tjt-subpanel-tre-browser-read-only/07-RESEARCH.md
@.planning/phases/07-tjt-subpanel-tre-browser-read-only/07-VALIDATION.md

<interfaces>
<!-- Existing fixture conventions the builder must match. Do NOT re-explore. -->

Existing fixture directory + golden convention (Utinni.Cli.Tests/Fixtures/tre):
  synthesized-3record-v0005.tre + .expected.json   (24-byte stride, size-first, raw-deflate payloads)
  synthesized-2record-v0006.tre + .expected.json
  malformed-magic.tre, truncated.tre, unsupported-version.tre (+ .expected.json each)
  GoldenTestRunner.cs runs a verb and compares stdout to the *.expected.json
  InProcessCliRunner.cs invokes a verb in-process

VERIFIED v6000 per-TRE layout to synthesize (RESEARCH "Per-TRE v6000 header"):
  Header(36): 0:"EERT" 4:"6000" 8:numFiles 12:tocOffset 16:tocCompressor(2=zlib)
    20:sizeOfTOC 24:blockCompressor(2=zlib) 28:sizeOfNameBlock 32:uncompSizeOfNameBlock
  TOC entry (32-byte stride, crc-first): 0:u32 crc 4:i32 length 8:i32 offset 12:i32 compressor
    16:i32 compressedLength 20:i32 fileNameOffset 24:8 pad

VERIFIED COT2000 master-index layout to synthesize (RESEARCH "Detecting master-index kind"):
  detect: first 8 bytes == " COT2000" (leading space)
  Header offsets: 8:reserved 12:numFiles 16:sizeOfTocBlock 20:sizeOfNameBlock 24:dupNameBlock
    28:numTreeFiles 32:sizeOfTreeNameBlock 36:null-terminated .tre names (numTreeFiles) -> TOC -> name block
  Global TOC entry (32): 0:u8 compressor 1:u8 unused 2:u16 treeFileIndex 4:u32 crc 8:u32 fileNameLength
    12:u32 offset 16:u32 length 20:u32 compressedLength 24:8 pad
    (fileNameLength is a LENGTH; cumulative offset: off[0]=0, off[i]=off[i-1]+len[i-1]+1)

zlib RFC1950 framing for a synthetic block (RESEARCH Pitfall 2):
  prepend 0x78 0x9c (two-byte header, (0x78<<8|0x9c) % 31 == 0), append the 4-byte big-endian Adler32
  trailer. System.IO.Compression.DeflateStream(CompressionMode.Compress) produces the RAW deflate body
  to wrap. Adler32 over the UNCOMPRESSED bytes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TreFixtureBuilder + FixturePath env resolver + valid synthetic fixtures (v6000, COT2000, 5000, 0004)</name>
  <files>D:/Code/Utinni/Utinni.Cli.Tests/Infrastructure/TreFixtureBuilder.cs, D:/Code/Utinni/Utinni.Cli.Tests/Infrastructure/FixturePath.cs, D:/Code/Utinni/Utinni.Cli.Tests/Infrastructure/TreFixtureBuilderTests.cs, D:/Code/Utinni/Utinni.Cli.Tests/Fixtures/tre/synthetic-v6000-2record.tre, D:/Code/Utinni/Utinni.Cli.Tests/Fixtures/tre/synthetic-cot2000-2tree.toc, D:/Code/Utinni/Utinni.Cli.Tests/Fixtures/tre/synthetic-5000-header.tre, D:/Code/Utinni/Utinni.Cli.Tests/Fixtures/tre/synthetic-0004-header.tre, D:/Code/Utinni/Utinni.Cli.Tests/Fixtures/tre/zlib-framed-1record-v0006.tre</files>
  <read_first>
    - D:/Code/Utinni/Utinni.Cli.Tests/Infrastructure/FixturePath.cs (the helper to extend with the env resolver — read its current path-resolution shape)
    - D:/Code/Utinni/Utinni.Cli.Tests/Infrastructure/GoldenTestRunner.cs (how a golden test loads a fixture + compares — the convention the builder output must be loadable by)
    - D:/Code/Utinni/Utinni.Cli.Tests/Commands/ParseTreCommandTests.cs (the existing TRE golden idiom + how synthesized-*-v000X.tre fixtures are referenced)
    - .planning/phases/07-tjt-subpanel-tre-browser-read-only/07-RESEARCH.md ("Per-TRE v6000 header" + "Detecting master-index kind" + Pitfall 2 zlib framing — the exact byte layouts to synthesize)
    - .planning/phases/07-tjt-subpanel-tre-browser-read-only/07-VALIDATION.md (Wave 0 Requirements list — the items this task reconciles)
  </read_first>
  <behavior>
    - Test: TreFixtureBuilder emits a synthetic v6000 archive whose 36-byte header has magic "EERT" + tag "6000", a zlib-framed (0x78 0x9c) crc-first 32-byte-stride TOC of 2 records, and a zlib-framed name block; re-reading the header byte-for-byte returns the written numFiles/tocOffset/sizeOfTOC.
    - Test: TreFixtureBuilder emits a synthetic COT2000 master index beginning with " COT2000" (leading space), numTreeFiles==2, two null-terminated .tre names, a zlib TOC of >=2 global entries each with treeFileIndex < numTreeFiles, and a name block reconstructable from cumulative fileNameLength.
    - Test: TreFixtureBuilder emits a synthetic 5000-header fixture whose record region is DELIBERATELY not a valid 32-byte crc-first 6000 TOC (e.g. a short/garbage record region) — the artifact exists so 07-01 can prove "5000 recognized, enumerate-empty, no throw" against a non-6000 layout.
    - Test: TreFixtureBuilder emits a synthetic 0004-header fixture using the existing size-first 24-byte stride (same family as the 0005/0006 synthesized fixtures), marked in a comment as real-layout-unverified.
    - Test: FixturePath.SampleTreDir() returns the SWG_SAMPLE_TRE_DIR env value when set and null when unset (so env-gated tests Skip rather than fail).
    - Test: a zlib-framed single-record v6000 fixture round-trips — its TOC/name zlib blocks inflate to the expected uncompressed bytes when fed through the strip-header+ignore-Adler path (mirrors what 07-01's reader will do).
  </behavior>
  <action>
    Create `TreFixtureBuilder.cs` (a `public static` test-infrastructure class, NOT in the shipping `Formats/` assembly — it lives in `Utinni.Cli.Tests/Infrastructure/`) carrying the standard test-file header. Give it deterministic emit methods that WRITE the synthetic fixtures to `Utinni.Cli.Tests/Fixtures/tre/`: `WriteV6000TwoRecord(path)`, `WriteCot2000TwoTree(path)`, `WriteSynthetic5000(path)`, `WriteSynthetic0004(path)`, `WriteZlibFramedV6000OneRecord(path)`. Implement a `ZlibFrame(byte[] uncompressed)` helper that deflate-compresses via `DeflateStream(CompressionMode.Compress)`, prepends `0x78 0x9c`, and appends the 4-byte big-endian Adler32 over the uncompressed bytes (compute Adler32 inline — small, no new dep). Build the v6000 header at the verified offsets (magic "EERT", tag "6000", numFiles, tocOffset, tocCompressor=2, sizeOfTOC, blockCompressor=2, sizeOfNameBlock, uncompSizeOfNameBlock) and a 2-record crc-first 32-byte TOC (crc, length, offset, compressor, compressedLength, fileNameOffset, +8 pad). Build the COT2000 index at the verified offsets with the " COT2000" magic, numTreeFiles=2, two `.tre` names, a >=2-entry global TOC (compressor, treeFileIndex<2, crc, fileNameLength, offset, length, compressedLength, +pad), and a name block keyed by cumulative `fileNameLength`. For the 5000 fixture, write a valid `5000` header tag but a record region that is NOT a coherent 32-byte crc-first TOC (so 07-01's enumerate-empty contract is provable). For the 0004 fixture, reuse the size-first 24-byte stride family.

    Commit the EMITTED fixture bytes to the repo (the builder is the generator of record; the committed `.tre`/`.toc` files are what the 07-01 golden tests load). Provide a `[Fact]` (or `[Trait("category","fixture-gen")]`) in `TreFixtureBuilderTests.cs` that regenerates each fixture into a temp path and asserts the committed fixture bytes are byte-identical (so the committed fixtures cannot silently drift from the builder).

    Extend `FixturePath.cs` with `public static string SampleTreDir()` returning `Environment.GetEnvironmentVariable("SWG_SAMPLE_TRE_DIR")` (or null), plus a `public static bool HasSampleTreDir()` convenience. Do NOT change the existing fixture-path resolution behavior; only ADD the env resolver. Document in a comment that the env-gated tests SUPPLEMENT the in-repo synthetic fixtures (they do not replace CI coverage).

    Register all new fixtures + the new infrastructure files in `Utinni.Cli.Tests.csproj` if it is non-SDK-style (Compile/Content includes with CopyToOutputDirectory) so they deploy alongside the test binaries the same way the existing `Fixtures/tre/*.tre` do.
  </action>
  <verify>
    <automated>cd D:/Code/Utinni; dotnet test Utinni.Cli.Tests --filter "TreFixtureBuilder"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test Utinni.Cli.Tests --filter "TreFixtureBuilder"` passes (the regenerate-and-compare self-test is green).
    - `TreFixtureBuilder.cs` exists with `class TreFixtureBuilder` and the five Write* emit methods (grep each method name).
    - The five committed valid fixtures exist on disk: `synthetic-v6000-2record.tre`, `synthetic-cot2000-2tree.toc`, `synthetic-5000-header.tre`, `synthetic-0004-header.tre`, `zlib-framed-1record-v0006.tre` (file-exists check each).
    - `synthetic-v6000-2record.tre` begins with bytes `45 45 52 54 36 30 30 30` (ASCII "EERT6000") (hex-dump assert) and `synthetic-cot2000-2tree.toc` begins with `20 43 4F 54 32 30 30 30` (" COT2000") (hex-dump assert).
    - `FixturePath.cs` contains `SampleTreDir` reading `SWG_SAMPLE_TRE_DIR` (grep both tokens); returns null when the env var is unset (asserted in a test).
    - The 5000 fixture's record region is asserted NOT to be a valid 32-byte crc-first TOC (the builder test documents/asserts the deliberate non-6000 layout).
  </acceptance_criteria>
  <done>The deterministic builder + the five valid synthetic fixtures + the env resolver land; 07-01 has real v6000/COT2000/5000/0004 bytes to assert against on every CI run, not only when D:/Sample-TRE-Files is present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Malformed/adversarial synthetic fixtures (count*stride, offset+length, bad-Adler zlib, unknown compressor)</name>
  <files>D:/Code/Utinni/Utinni.Cli.Tests/Infrastructure/TreFixtureBuilder.cs, D:/Code/Utinni/Utinni.Cli.Tests/Infrastructure/TreFixtureBuilderTests.cs, D:/Code/Utinni/Utinni.Cli.Tests/Fixtures/tre/malformed-count-stride-overflow.tre, D:/Code/Utinni/Utinni.Cli.Tests/Fixtures/tre/malformed-offset-length-overflow.tre, D:/Code/Utinni/Utinni.Cli.Tests/Fixtures/tre/malformed-zlib-bad-adler.tre, D:/Code/Utinni/Utinni.Cli.Tests/Fixtures/tre/malformed-unknown-compressor.tre</files>
  <read_first>
    - D:/Code/Utinni/Utinni.Cli.Tests/Infrastructure/TreFixtureBuilder.cs (the builder created in Task 1 — extend with the malformed emitters)
    - D:/Code/Utinni/Utinni.Cli.Tests/Fixtures/tre/malformed-magic.tre and truncated.tre (the existing malformed-fixture naming + size convention to match)
    - .planning/phases/07-tjt-subpanel-tre-browser-read-only/07-RESEARCH.md (Security caps: count*stride, offset+length, zlib %31 validation, unknown compressor; Pitfall 2)
    - .planning/phases/07-tjt-subpanel-tre-browser-read-only/07-01-PLAN.md (the T-07-01..04 threat register these fixtures must exercise — note the matching checked-arithmetic acceptance criteria)
  </read_first>
  <behavior>
    - Test: the count*stride-overflow fixture declares a numFiles whose product with the 32-byte stride exceeds the file length (an int-overflow-adjacent value), so 07-01's division-form guard `recordCount > (streamLength - headerSize) / stride` must reject it before allocation.
    - Test: the offset+length-overflow fixture has a TOC entry whose offset+length exceeds the stream length (and an offset that wraps when added to length on int math), so 07-01's `offset <= streamLength - length` guard must reject it.
    - Test: the bad-Adler fixture has a zlib-framed block with a valid 0x78 0x9c header but a corrupted/truncated 4-byte Adler trailer (and/or a body that inflates short), so 07-01's zlib path detects the truncated/invalid trailer rather than silently returning garbage.
    - Test: the unknown-compressor fixture has a TOC entry with a compressor value that is neither 0 (none), raw-deflate, nor 2 (zlib), so 07-01 raises a documented unknown-compressor error kind.
    - Test: TreFixtureBuilder regenerate-and-compare self-test stays green for all four malformed fixtures (deterministic).
  </behavior>
  <action>
    Extend `TreFixtureBuilder.cs` with four malformed emitters: `WriteMalformedCountStrideOverflow(path)`, `WriteMalformedOffsetLengthOverflow(path)`, `WriteMalformedZlibBadAdler(path)`, `WriteMalformedUnknownCompressor(path)`. Each starts from the valid v6000 base header so it is structurally a v6000 archive, then injects exactly ONE adversarial field so the failure mode is unambiguous: (1) numFiles set so `numFiles * 32` overflows or exceeds the file (pick a value near `int.MaxValue / 32` to exercise the int-overflow path AND a smaller-but-out-of-bounds value); (2) a TOC entry offset+length past the stream end with an offset chosen so naive `offset + length` int-arithmetic wraps negative; (3) a zlib block with the correct 2-byte header but a mangled Adler trailer / short body; (4) a TOC entry whose compressor byte is an unrecognized value (e.g. 7).

    Commit the four emitted malformed fixtures. Add their regenerate-and-compare cases to `TreFixtureBuilderTests.cs`. Do NOT author any reader code here — these fixtures are pure adversarial inputs consumed by 07-01's bounds-check tests (this plan only produces the bytes + proves they are the malformed shapes intended). Add a comment in the builder mapping each fixture to the 07-01 threat ID it exercises (T-07-01 count*stride, T-07-03 offset OOB, T-07-04 truncated/zlib, plus the unknown-compressor case) so the link is auditable.

    Register the four new fixtures in `Utinni.Cli.Tests.csproj` (Content + CopyToOutputDirectory) if non-SDK-style, matching Task 1.
  </action>
  <verify>
    <automated>cd D:/Code/Utinni; dotnet test Utinni.Cli.Tests --filter "TreFixtureBuilder"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test Utinni.Cli.Tests --filter "TreFixtureBuilder"` passes including the four malformed regenerate-and-compare cases.
    - The four committed malformed fixtures exist on disk: `malformed-count-stride-overflow.tre`, `malformed-offset-length-overflow.tre`, `malformed-zlib-bad-adler.tre`, `malformed-unknown-compressor.tre` (file-exists each).
    - The count*stride fixture's declared numFiles is asserted to be > `(fileLength - 36) / 32` (the builder test proves the overflow shape).
    - The offset+length fixture's chosen offset is asserted to wrap negative under int `offset + length` (the builder test proves the int-overflow shape).
    - The bad-Adler fixture begins with a valid `0x78 0x9c` zlib header but its Adler trailer is asserted corrupted/short (builder test).
    - The unknown-compressor fixture's TOC compressor byte is asserted to be a value other than {0, raw-deflate, 2} (builder test).
  </acceptance_criteria>
  <done>The four adversarial fixtures land with builder self-tests proving each is the exact malformed shape 07-01's checked-arithmetic and zlib-validation acceptance criteria assert against.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-fixture bytes on disk → consumed by 07-01 reader tests | The fixtures THEMSELVES are the adversarial inputs; this plan only emits them. No untrusted input crosses into shipping code here — the builder runs in the test assembly only. |
| SWG_SAMPLE_TRE_DIR env var → FixturePath resolver | A path hint, not a secret; resolver returns null when unset (clean skip), never throws on a missing/garbage path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-00-01 | Tampering | committed fixture drift from the builder | mitigate | The regenerate-and-compare self-test asserts committed fixture bytes are byte-identical to the builder output — a hand-edited fixture fails CI. |
| T-07-00-02 | Information Disclosure | SWG_SAMPLE_TRE_DIR pointing outside the repo | accept | Test-only path hint; the resolver returns the raw env value for env-gated test reads only; no shipping code consumes it. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH Package Legitimacy Audit: BCL + in-repo only; Adler32 computed inline, no new dep). Nothing to slopcheck. |
</threat_model>

<verification>
- `dotnet test Utinni.Cli.Tests --filter "TreFixtureBuilder"` is green (all valid + malformed regenerate-and-compare cases).
- Nine synthetic fixtures (5 valid + 4 malformed) committed under `Fixtures/tre/`.
- `FixturePath.SampleTreDir()` resolves the env var or null; existing fixture-path behavior unchanged.
- 07-VALIDATION Wave 0 items are now backed by real artifacts (reconcile the `[ ]` list).
</verification>

<success_criteria>
- In-repo synthetic v6000/COT2000/5000/0004 fixtures + four malformed fixtures exist and are deterministic (builder self-tested).
- The env resolver supplements (does not replace) in-repo CI coverage of the real-archive paths (review consensus #3).
- The 5000 fixture uses a non-6000 layout so 07-01's enumerate-empty/no-throw contract is provable (review consensus #1).
- This plan is the Wave-0 gate 07-01 depends on (review consensus #4).
- Fixtures are read-only test inputs; no write/authoring/export surface added (D-01).
</success_criteria>

<output>
Create `.planning/phases/07-tjt-subpanel-tre-browser-read-only/07-00-SUMMARY.md` when done.
</output>
