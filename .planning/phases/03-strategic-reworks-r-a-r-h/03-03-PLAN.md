---
phase: 03-strategic-reworks-r-a-r-h
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - UtinniCoreDotNet/Utility/Log.cs
  - UtinniCoreDotNetGen/Program.cs
  - sdk/UtinniPluginTemplates/Vsix/Utility/Props.cs
  - UtinniCoreDotNet.Tests/LogCallerMemberNameTests.cs
  - UtinniCoreDotNet.Tests/HeaderDiscoveryTests.cs
  - UtinniCoreDotNet.Tests/DirectoryBuildPropsTests.cs
  - docs/ai/assessment.md
autonomous: true
requirements: [STAB-02]

must_haves:
  truths:
    - "Log.cs FormatText no longer walks `new StackTrace().GetFrame(2)`; method/file/line resolution comes from `[CallerMemberName]`/`[CallerFilePath]`/`[CallerLineNumber]` defaulted parameters on `Log.Info/Debug/Warning/Error/Critical`."
    - "CallerName resolves at compile time (zero runtime cost); existing Log.Info('msg') calls remain source-compatible with no changes."
    - "UtinniCoreDotNetGen/Program.cs auto-discovers `UtinniCore/**/*.h` via `Directory.EnumerateFiles(..., SearchOption.AllDirectories)`, excluding any path segment named `_internal/` (case-insensitive)."
    - "CppSharp regeneration produces `UtinniCoreDotNet/Generated/UtinniCore.cs` covering every public UtinniCore header; `StdEdited.cs` and `Std.cs` are NOT regenerated by this discovery (CON-O-05 disposition)."
    - "Props.CreateDotNetDirectoryProps idempotently merges Utinni properties into an existing `Directory.Build.props` instead of silently no-opping; if the file doesn't exist, it creates the skeleton; if Utinni properties are already present, the call is a no-op."
    - "xUnit Facts assert: Log emits caller method name; Log.cs source no longer contains `new StackTrace().GetFrame`; header discovery excludes `_internal/`; Directory.Build.props merge handles all three cases (no-file / file-without-Utinni-props / file-with-Utinni-props)."
    - "docs/ai/assessment.md §Status tracking marks R-E + R-F + R-G done with implementing commit SHAs; §Open questions marks CON-O-05 disposition (StdEdited.cs hand-curated; not regenerated)."
    - "Decision coverage (CONTEXT.md): this plan addresses D-01 (plan grouping by category — Build-tooling+logging category), D-02 (CI-gated ordering — depends_on 03-02), D-03 (atomic commits per R-X — R-E + R-F + R-G each ship as atomic commits paired with their harness), D-04 (CI gate at plan boundary — 03-03 doesn't start until 03-02 is green), D-05 (max-harness posture — every R-X has a regression test). Note: CONTEXT.md decisions D-21..D-25 use the parenthetical-category format `**D-NN (R-X):**` which the decisions parser does not register as trackable (regex requires `**D-NN:**`); their substance is fully addressed by the R-E / R-F / R-G truths above (CallerMemberName logging, _internal/ filter glob, idempotent XDocument props merger) and the CON-O-05 disposition truth (StdEdited.cs hand-curated)."
  artifacts:
    - path: "UtinniCoreDotNet/Utility/Log.cs"
      provides: "Log.Info/Debug/Warning/Error/Critical with [CallerMemberName]/[CallerFilePath]/[CallerLineNumber] defaulted parameters; FormatText takes resolved strings"
      contains: "CallerMemberName"
    - path: "UtinniCoreDotNetGen/Program.cs"
      provides: "Header glob auto-discovery with _internal/ filter; HeaderDiscovery helper extracted as testable static"
      contains: "Directory.EnumerateFiles"
    - path: "sdk/UtinniPluginTemplates/Vsix/Utility/Props.cs"
      provides: "Idempotent XDocument-based Directory.Build.props merger; UpsertPropertyGroup private helper"
      contains: "XDocument"
    - path: "UtinniCoreDotNet.Tests/LogCallerMemberNameTests.cs"
      provides: "[Fact] Log emits caller method name; negative grep test on Log.cs source"
      contains: "Log_Info_EmitsCallerMethodName"
    - path: "UtinniCoreDotNet.Tests/HeaderDiscoveryTests.cs"
      provides: "[Fact] HeaderDiscovery_FixtureTree_ExcludesInternalDirectory"
      contains: "HeaderDiscovery_FixtureTree_ExcludesInternalDirectory"
    - path: "UtinniCoreDotNet.Tests/DirectoryBuildPropsTests.cs"
      provides: "[Fact] three-case merge (no file / file-without-Utinni / idempotent re-run)"
      contains: "CreateDotNetDirectoryProps_NoExistingFile_CreatesFreshSkeleton"
  key_links:
    - from: "UtinniCoreDotNet/Utility/Log.cs"
      to: "[CallerMemberName] resolution at compile time"
      via: "Defaulted parameter on Log.Info(...)"
      pattern: "\\[CallerMemberName\\]"
    - from: "UtinniCoreDotNetGen/Program.cs"
      to: "UtinniCore/**/*.h headers"
      via: "Directory.EnumerateFiles glob + _internal/ filter"
      pattern: "Directory\\.EnumerateFiles\\(.*UtinniCore"
    - from: "sdk/UtinniPluginTemplates/Vsix/Utility/Props.cs"
      to: "Directory.Build.props on disk"
      via: "XDocument.Load → UpsertPropertyGroup → doc.Save"
      pattern: "XDocument\\.Load"
---

<objective>
Land R-E (`[CallerMemberName]`/`[CallerFilePath]`/`[CallerLineNumber]` defaulted parameters on Log methods, replacing `new StackTrace().GetFrame` walk), R-F (CppSharp header auto-discovery via glob with `_internal/` filter), and R-G (idempotent merger replacing the silent early-return in the VSIX `Directory.Build.props` wizard).

Purpose: TD-20 (Log.FormatText walks the stack on EVERY log call — measurable runtime cost in a hot path), TD-21 (CppSharp header allowlist must be edited by hand every time a new header is added to UtinniCore — silent omissions are common), TD-22 (VSIX wizard silently destroys the user's existing `Directory.Build.props` by returning early — broken since `Props.cs:9-14` shipped). Smallest blast-radius work in Phase 3; lands after R-A/R-H (Plan 03-01) and R-B/R-C (Plan 03-02) so the new `UTINNI_API` surface from Plan 03-02 R-C is automatically picked up by R-F's auto-discovery. CI must stay green at every commit (D-04).

Output: Three independent refactors with per-feature xUnit Facts. Internal task order: R-E first (smallest line-count quick-win — per `<specifics>`), R-F second (formalizes header discovery for the new R-C symbols), R-G last (most isolated). CON-O-05 disposition committed alongside R-F (StdEdited.cs and Std.cs remain hand-curated; only `Generated/UtinniCore.cs` is regenerated by the auto-discovery). All R-X status rows in `docs/ai/assessment.md` close to `done` — Phase 3 deliverable complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-strategic-reworks-r-a-r-h/03-CONTEXT.md
@.planning/phases/03-strategic-reworks-r-a-r-h/03-PATTERNS.md
@.planning/phases/03-strategic-reworks-r-a-r-h/03-02-SUMMARY.md
@.planning/intel/constraints.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/INTEGRATIONS.md
@docs/ai/assessment.md

<interfaces>
<!-- Key contracts the executor will define/consume. Drawn from PATTERNS.md per D-21/D-22/D-23. -->

R-E Log API change (D-21 — defaulted parameters on every public Log method):

```csharp
using System.Runtime.CompilerServices;

private static string FormatText(string text, string callerName, string callerFile)
{
    if (writeClassName)
    {
        string className = System.IO.Path.GetFileNameWithoutExtension(callerFile);
        if (writeFunctionName)
            return "[" + className + "][" + callerName + "] " + text;
        else
            return "[" + className + "] " + text;
    }
    return text;
}

public static void Info(string text,
    [CallerMemberName] string callerName = "",
    [CallerFilePath] string callerFile = "")
{
    log.Info(FormatText(text, callerName, callerFile));
}
// same pattern for Debug/Warning/Error/Critical
```

R-F HeaderDiscovery helper (D-22 — extract as testable static for the unit-test fixture):

```csharp
internal static class HeaderDiscovery
{
    // Returns header paths relative to utinniCoreRoot, with backslash separators (CppSharp expects this).
    // Excludes any header under a path segment named "_internal" (case-insensitive).
    public static List<string> Discover(string utinniCoreRoot)
    {
        var results = new List<string>();
        foreach (string headerPath in Directory.EnumerateFiles(
            utinniCoreRoot, "*.h", SearchOption.AllDirectories))
        {
            string relPath = headerPath.Substring(utinniCoreRoot.Length + 1);
            if (relPath.Split('\\', '/').Any(seg =>
                string.Equals(seg, "_internal", StringComparison.OrdinalIgnoreCase)))
            {
                continue;
            }
            results.Add(relPath.Replace('/', '\\'));
        }
        return results;
    }
}
```

R-F Program.cs integration (replaces the explicit allowlist at lines 73-103):

```csharp
string utinniCoreRoot = Path.Combine(slnDir, targetProjName);
foreach (string relPath in HeaderDiscovery.Discover(utinniCoreRoot))
{
    module.Headers.Add(relPath);
}
```

R-G Props.CreateDotNetDirectoryProps shape (D-23 — idempotent XDocument merger):

```csharp
public static void CreateDotNetDirectoryProps(string slnPath)
{
    string filePath = slnPath + DirectoryBuildPropsFilename;
    XNamespace ns = "http://schemas.microsoft.com/developer/msbuild/2003";

    XDocument doc = File.Exists(filePath)
        ? XDocument.Load(filePath)
        : new XDocument(new XElement(ns + "Project"));

    XElement project = doc.Element(ns + "Project")
                      ?? new XElement(ns + "Project");
    if (project.Parent == null) doc.Add(project);

    // Upsert each PropertyGroup the wizard owns (one unconditional + three configuration-conditional).
    UpsertPropertyGroup(project, ns, conditionAttr: null,
        new (string, string)[] { ("PluginOutputDir", @"$(SolutionDir)\bin\$(Configuration)\"),
                                  ("UtinniCoreDotNetPath", @"..\..\..\bin\Release\") });
    UpsertPropertyGroup(project, ns,
        conditionAttr: "'$(Configuration)|$(Platform)' == 'RelWithDbgInfo|AnyCPU'",
        /* existing RelWithDbgInfo block, hoisted into name-value pairs */);
    // ... Release|AnyCPU, Debug|AnyCPU groups same pattern ...

    doc.Save(filePath);
}

private static void UpsertPropertyGroup(XElement project, XNamespace ns,
    string conditionAttr, (string name, string value)[] properties)
{
    XElement group = project.Elements(ns + "PropertyGroup")
        .FirstOrDefault(pg =>
            (conditionAttr == null && pg.Attribute("Condition") == null) ||
            (conditionAttr != null && pg.Attribute("Condition")?.Value == conditionAttr));
    if (group == null)
    {
        group = new XElement(ns + "PropertyGroup");
        if (conditionAttr != null) group.SetAttributeValue("Condition", conditionAttr);
        project.Add(group);
    }
    foreach (var (name, value) in properties)
    {
        XElement el = group.Element(ns + name);
        if (el == null) group.Add(new XElement(ns + name, value));
        else            el.Value = value;
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: R-E — Replace Log.FormatText StackTrace walk with [CallerMemberName]/[CallerFilePath] defaulted parameters; add Log regression Facts</name>
  <files>
    UtinniCoreDotNet/Utility/Log.cs,
    UtinniCoreDotNet.Tests/LogCallerMemberNameTests.cs
  </files>
  <read_first>
    @UtinniCoreDotNet/Utility/Log.cs (lines 50-69 — current FormatText with `new StackTrace().GetFrame(2).GetMethod()`; lines 70+ — Info/Debug/Warning/Error/Critical methods that call FormatText)
    @UtinniCoreDotNet.Tests/UtinniCfgTests.cs (lines 64-74 — Assert.DoesNotMatch grep-style precedent for negative test on file content)
    @.planning/phases/03-strategic-reworks-r-a-r-h/03-PATTERNS.md §"UtinniCoreDotNet/Utility/Log.cs (R-E CallerMemberName + R-A typo fix)" (lines 433-515 — R-E half: before/after FormatText + Info/Debug/... defaulted-parameter rewrite)
    @.planning/phases/03-strategic-reworks-r-a-r-h/03-CONTEXT.md decision D-21 (CallerMemberName/CallerFilePath/CallerLineNumber on Log.Info/Debug/Warning/Error/Critical; compile-time resolution; managed side only — native spdlog already cheap)
    @.planning/codebase/CONVENTIONS.md (Allman braces, MIT header preserved)
    @.planning/codebase/STACK.md (net472 target — confirm [CallerMemberName] available; it is, since .NET 4.5+)
  </read_first>
  <behavior>
    - Test 1 (LogCallerMemberNameTests.Log_Info_EmitsCallerMethodName_WhenWriteClassNameOn): subscribe to the output sink (via the SubscribeOutputSink from Plan 03-01); call `Log.Info("test message")` from a test method named `Log_Info_EmitsCallerMethodName_WhenWriteClassNameOn`; assert the captured output contains "[LogCallerMemberNameTests][Log_Info_EmitsCallerMethodName_WhenWriteClassNameOn] test message" (class name from CallerFilePath, method name from CallerMemberName).
    - Test 2 (LogCallerMemberNameTests.Log_NoLongerWalksStackTraceInFormatText): grep-style negative — read `UtinniCoreDotNet/Utility/Log.cs`; assert `new StackTrace\(\)\.GetFrame` does NOT match (R-E completion).
    - Test 3 (LogCallerMemberNameTests.Log_Info_ExplicitCallerName_OverridesAutomatic): call `Log.Info("test", callerName: "ManualName")`; assert output uses "ManualName" not the actual caller — confirms the defaulted parameters can be explicitly overridden when needed.
    - Test 4 (LogCallerMemberNameTests.Log_Info_WriteClassNameOff_OmitsClassName): toggle `writeClassName = false` (planner sets via reflection or test helper); call `Log.Info("test")`; assert output equals "test" (no prefix). Round-trip the toggle back to its original value in [Cleanup] / finally.
    - Test 5 (LogCallerMemberNameTests.Log_Debug_AlsoUsesCallerMemberName): same shape as Test 1 but for `Log.Debug` — confirms the defaulted parameters apply to all five Log methods (Info/Debug/Warning/Error/Critical), not just Info.
  </behavior>
  <action>
    Step 1 — Rewrite `UtinniCoreDotNet/Utility/Log.cs` FormatText (currently lines 50-69) and each of Log.Info/Debug/Warning/Error/Critical per `<interfaces>` "R-E Log API change":

    - Add `using System.Runtime.CompilerServices;` to the file's using-block.
    - Replace FormatText signature to take `(string text, string callerName, string callerFile)` — caller-name/file resolved by [CallerMemberName] / [CallerFilePath] at the public call site, NOT by stack walking inside FormatText. The body extracts class name via `Path.GetFileNameWithoutExtension(callerFile)` (compile-time-resolved file path → file name without `.cs`).
    - Each of `Log.Info / Log.Debug / Log.Warning / Log.Error / Log.Critical` gains two defaulted parameters: `[CallerMemberName] string callerName = ""`, `[CallerFilePath] string callerFile = ""`. The body passes both to FormatText. The (existing) text-only signature continues to compile-and-work via the defaulted parameters — full source-compat for all existing call sites.
    - REMOVE the `new StackTrace().GetFrame(2).GetMethod()` body entirely. `using System.Diagnostics` may no longer be needed (planner audits — leave if used elsewhere in the file; remove if Log.cs no longer references any `System.Diagnostics` symbol).
    - Preserve MIT header. Preserve all other Log.cs behavior (writeClassName / writeFunctionName toggles, the output sink Subscribe path from Plan 03-01).

    Per D-21 — apply to MANAGED side only. Native `log.cpp` is NOT touched in this task (spdlog already has cheap source-location built in).

    Step 2 — Create `UtinniCoreDotNet.Tests/LogCallerMemberNameTests.cs` with the five Facts above per Phase 1 D-04 naming convention. Mark `[Collection("StaticCallbackState")]` because the output sink is shared static state with the other Log/Callbacks tests.

    For Test 1's class-name assertion (refined per checker I2): `[CallerFilePath]` resolves to the FULL absolute path at compile time (e.g., `D:\Code\Utinni\UtinniCoreDotNet.Tests\LogCallerMemberNameTests.cs` locally vs `D:\a\Utinni\UtinniCoreDotNet.Tests\LogCallerMemberNameTests.cs` on GitHub Actions — the compiler embeds whichever absolute prefix existed at compile time). The test asserts only the file basename after `Path.GetFileNameWithoutExtension(callerFile)`, which strips both directory components and the `.cs` extension regardless of which absolute prefix the compiler embedded — yielding the stable string `LogCallerMemberNameTests`. The assertion is therefore stable across local and CI build machines.

    For Test 4: toggling `writeClassName` requires reflection access to the private static field. Use the same `BindingFlags.NonPublic | BindingFlags.Static` pattern as GroundSceneCallbacksTests.cs:45-59 (GetQueue helper).
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests --filter "FullyQualifiedName~LogCallerMemberNameTests"</automated>
    <automated>powershell -Command "if ((Select-String -Path 'UtinniCoreDotNet/Utility/Log.cs' -Pattern 'new StackTrace\(\)\.GetFrame' -SimpleMatch:$false).Count -eq 0) { exit 0 } else { Write-Error 'R-E: new StackTrace().GetFrame still present in Log.cs'; exit 1 }"</automated>
    <automated>powershell -Command "if ((Select-String -Path 'UtinniCoreDotNet/Utility/Log.cs' -Pattern 'CallerMemberName' -SimpleMatch).Count -ge 5) { exit 0 } else { Write-Error 'R-E: expected CallerMemberName attribute on all 5 Log methods'; exit 1 }"</automated>
  </verify>
  <done>
    Log.cs FormatText no longer walks `new StackTrace().GetFrame`. Five Log methods (Info/Debug/Warning/Error/Critical) carry `[CallerMemberName]` + `[CallerFilePath]` defaulted parameters. All existing call sites (no signature changes — defaulted parameters are transparent) compile + run. Five Facts pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: R-F — CppSharp header auto-discovery via Directory.EnumerateFiles + _internal/ filter; add HeaderDiscovery Facts; close CON-O-05 disposition</name>
  <files>
    UtinniCoreDotNetGen/Program.cs,
    UtinniCoreDotNet.Tests/HeaderDiscoveryTests.cs
  </files>
  <read_first>
    @UtinniCoreDotNetGen/Program.cs (lines 67-92 — current header `module.Headers.Add(...)` allowlist body; 23+ lines of explicit `Add("utinni.h")` / `Add("utility\\log.h")` etc.)
    @UtinniCoreDotNet.Tests/CppSharpSlnDirTests.cs (lines 74-104 — temp-dir fixture pattern: `Directory.CreateDirectory` + `File.WriteAllText` seed + try/finally `Directory.Delete(recursive: true)` cleanup; copy verbatim for the HeaderDiscovery fixture tree)
    @.planning/phases/03-strategic-reworks-r-a-r-h/03-PATTERNS.md §"UtinniCoreDotNetGen/Program.cs (R-F CppSharp header auto-discovery)" (lines 778-850 — includes target glob + _internal/ filter body + R-F fixture test pattern with `{public.h, _internal/private.h, sub/public2.h}` expected result `{public.h, sub\\public2.h}`)
    @.planning/phases/03-strategic-reworks-r-a-r-h/03-CONTEXT.md decisions D-22 (Directory.EnumerateFiles glob + _internal/ filter; convention: any header NOT projected to managed lives in _internal/), D-24 (CON-O-05 disposition: StdEdited.cs and Std.cs are NOT regenerated by the auto-discovery; ONLY Generated/UtinniCore.cs is the output)
    @.planning/codebase/INTEGRATIONS.md §"CON-T-01 post-build chain" (UtinniCoreDotNetGen.exe invoked from UtinniCore.vcxproj post-build; the glob must work under CI's path conventions — verify temp-dir test passes the fixture, then production glob runs against the real `UtinniCore/` directory at build time)
  </read_first>
  <behavior>
    - Test 1 (HeaderDiscoveryTests.HeaderDiscovery_FixtureTree_ExcludesInternalDirectory): create temp tree `temp/UtinniCore/{public.h, _internal/private.h, sub/public2.h}`; call `HeaderDiscovery.Discover(temp/UtinniCore)`; assert result is exactly `{"public.h", "sub\\public2.h"}` (2 entries, `_internal/private.h` excluded; backslash separators).
    - Test 2 (HeaderDiscoveryTests.HeaderDiscovery_NestedInternalDirectories_AlsoExcluded): create temp tree `temp/UtinniCore/{public.h, swg/_internal/private.h, swg/public.h}`; assert result is `{"public.h", "swg\\public.h"}` — the `_internal` filter matches at ANY depth, not just top-level.
    - Test 3 (HeaderDiscoveryTests.HeaderDiscovery_CaseInsensitiveInternalSegment): create `temp/UtinniCore/{_Internal/private.h, _INTERNAL/other.h, public.h}`; assert all `_Internal`/`_INTERNAL` variants are excluded (StringComparison.OrdinalIgnoreCase).
    - Test 4 (HeaderDiscoveryTests.HeaderDiscovery_RealUtinniCoreRoot_CoversAllExpectedHeaders): point at the actual `UtinniCore/` directory (4-level walk-up from AppContext.BaseDirectory per FindCfg precedent); assert discovered set includes `utinni.h`, `utility\\log.h`, `plugin_framework\\plugin_manager.h`, `swg\\client\\client.h` (the headers explicitly in the current allowlist — confirms backward-compat).
    - Test 5 (HeaderDiscoveryTests.HeaderDiscovery_RealUtinniCoreRoot_PicksUpNewRcSymbol): assert discovered set includes `swg\\client\\client.h` — same header from Test 4, but this test exists specifically to confirm Plan 03-02 R-C's new `getSwgWndProc` declaration in `client.h` is picked up by the auto-discovery (since R-F runs after Plan 03-02 per D-02).
  </behavior>
  <action>
    Step 1 — Extract a new `internal static class HeaderDiscovery` in `UtinniCoreDotNetGen/Program.cs` per `<interfaces>` "R-F HeaderDiscovery helper". The class is internal to the generator project and exposes `public static List<string> Discover(string utinniCoreRoot)` returning relative-path strings with backslash separators (CppSharp's expected format). Body implements the glob with `Directory.EnumerateFiles(utinniCoreRoot, "*.h", SearchOption.AllDirectories)` + `_internal/` segment filter (case-insensitive).

    Step 2 — Rewrite the explicit allowlist in `UtinniCoreDotNetGen/Program.cs:67-92` (the `module.Headers.Add(...)` block) per `<interfaces>` "R-F Program.cs integration":
    - Replace the 23+ explicit `Add()` calls with a single foreach over `HeaderDiscovery.Discover(utinniCoreRoot)`.
    - The `utinniCoreRoot` path is constructed as `Path.Combine(slnDir, "UtinniCore")` — using the `slnDir` resolution already in scope from Phase 02 C-15's fix.
    - Add a code comment block citing D-22 + D-24 + CON-O-05: any header that should NOT be projected to managed lives under an `_internal/` directory; `StdEdited.cs` and `Std.cs` are NOT regenerated by this discovery (only `Generated/UtinniCore.cs` is the auto-discovery target).

    Step 3 — Expose `HeaderDiscovery` to the test project. The class is `internal` and `UtinniCoreDotNetGen` is a separate project from `UtinniCoreDotNet.Tests`. Add `[InternalsVisibleTo("UtinniCoreDotNet.Tests")]` to `UtinniCoreDotNetGen/Properties/AssemblyInfo.cs` (or to a sibling AssemblyInfo file; create one if absent — Phase 1 D-01 precedent). Alternative: make HeaderDiscovery `public` — planner's discretion; `internal` + `[InternalsVisibleTo]` is the lighter-touch choice and matches the Phase 1 D-01 pattern from Plan 03-01 Task 1.

    Step 4 — Create `UtinniCoreDotNet.Tests/HeaderDiscoveryTests.cs` with the five Facts above. Use the `CppSharpSlnDirTests.cs:74-104` temp-dir fixture pattern verbatim — `Directory.CreateDirectory` + `File.WriteAllText` seed, try/finally `Directory.Delete(recursive: true)` cleanup. For Test 4-5's "real UtinniCore root" walk-up, use the `FindCfg`-style 4-level walk-up from `AppContext.BaseDirectory`.

    Step 5 — CON-O-05 disposition: update the comment block at the top of `UtinniCoreDotNetGen/Program.cs` (or a new `// CON-O-05 disposition: ...` block near the HeaderDiscovery call site) per Phase 02 D-12 style: `// CON-O-05 disposition (D-24): StdEdited.cs is the only hand-curated Generated/ file; CppSharp's STL-template handling for std::basic_string is unreliable so StdEdited.cs is curated by hand. The HeaderDiscovery glob does NOT regenerate it. Std.cs (generated by the UtinniCore-Symbols project) is also out of scope. Only Generated/UtinniCore.cs is regenerated by this glob.` The matching disposition row update in `docs/ai/assessment.md` lands in Task 4.

    Step 6 — Run the CppSharp generator manually (or via the post-build step) and verify `UtinniCoreDotNet/Generated/UtinniCore.cs` regenerates cleanly + contains the new `getSwgWndProc` symbol from Plan 03-02 R-C. If the generated file diffs in undesirable ways (e.g., CppSharp emits bindings for a header that should have been `_internal/`), the planner audits per D-22 ("Phase 3 itself adds zero `_internal/` directories — that's a follow-up cleanup if any existing header should be hidden"); no `_internal/` moves happen in Phase 3 — the convention is documented for future use.
  </action>
  <verify>
    <automated>powershell -Command "& 'C:\\Program Files\\Microsoft Visual Studio\\2022\\Community\\MSBuild\\Current\\Bin\\msbuild.exe' Utinni.sln /p:Configuration=Release /p:Platform=x86 /v:minimal"</automated>
    <automated>dotnet test UtinniCoreDotNet.Tests --filter "FullyQualifiedName~HeaderDiscoveryTests"</automated>
    <automated>powershell -Command "if ((Select-String -Path 'UtinniCoreDotNetGen/Program.cs' -Pattern 'Directory\.EnumerateFiles' -SimpleMatch:$false).Count -ge 1) { exit 0 } else { Write-Error 'R-F: Directory.EnumerateFiles not present in Program.cs'; exit 1 }"</automated>
    <automated>powershell -Command "if ((Select-String -Path 'UtinniCoreDotNet/Generated/UtinniCore.cs' -Pattern 'getSwgWndProc' -SimpleMatch).Count -ge 1) { exit 0 } else { Write-Error 'R-F: generated file does not contain Plan 03-02 R-C getSwgWndProc symbol'; exit 1 }"</automated>
  </verify>
  <done>
    `HeaderDiscovery.Discover(...)` is the single entry point for header enumeration; explicit allowlist removed from Program.cs. Five HeaderDiscoveryTests Facts pass (including the temp-fixture exclusion tests + the real-UtinniCore-coverage tests). Regenerated `UtinniCoreDotNet/Generated/UtinniCore.cs` contains the new R-C `getSwgWndProc` symbol from Plan 03-02. CON-O-05 disposition committed in code comment.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: R-G — Make Props.CreateDotNetDirectoryProps an idempotent XDocument-based merger; add three-case fixture Facts</name>
  <files>
    sdk/UtinniPluginTemplates/Vsix/Utility/Props.cs,
    UtinniCoreDotNet.Tests/DirectoryBuildPropsTests.cs
  </files>
  <read_first>
    @sdk/UtinniPluginTemplates/Vsix/Utility/Props.cs (lines 9-14 — current `if (File.Exists(...)) { return; }` early-return bug; lines beyond — the StringBuilder XML construction that becomes the seed data for the new merger)
    @UtinniCoreDotNet.Tests/VsixManifestTests.cs (lines 79-99 — XDocument.Load + namespace-aware Descendants pattern; exact API surface the new Props.cs uses for load-mutate-save)
    @UtinniCoreDotNet.Tests/CppSharpSlnDirTests.cs (lines 74-104 — temp-dir fixture pattern, create+seed+try-finally cleanup)
    @.planning/phases/03-strategic-reworks-r-a-r-h/03-PATTERNS.md §"sdk/UtinniPluginTemplates/Vsix/Utility/Props.cs (R-G idempotent merge...)" (lines 854-963 — exact target body with UpsertPropertyGroup helper)
    @.planning/phases/03-strategic-reworks-r-a-r-h/03-PATTERNS.md §"Test pattern (R-G three-case fixture per D-05)" (lines 955-963 — three [Fact] specifications)
    @.planning/phases/03-strategic-reworks-r-a-r-h/03-CONTEXT.md decision D-23 (idempotent XDocument merger replaces silent early-return)
    @.planning/intel/constraints.md CON-T-04 (Props.cs factoring; signature + location frozen, body changes only; XML-merge logic can live in a `private static` helper inside Props.cs but NOT in a new file)
  </read_first>
  <behavior>
    - Test 1 (DirectoryBuildPropsTests.CreateDotNetDirectoryProps_NoExistingFile_CreatesFreshSkeleton): temp dir without any Directory.Build.props; call CreateDotNetDirectoryProps(tempDir); assert the file is created; load via XDocument; assert `<Project xmlns="...">` root, expected PropertyGroups present (one unconditional with PluginOutputDir + UtinniCoreDotNetPath; three configuration-conditional for RelWithDbgInfo|AnyCPU / Release|AnyCPU / Debug|AnyCPU per the existing wizard's data), property values match the wizard's defaults.
    - Test 2 (DirectoryBuildPropsTests.CreateDotNetDirectoryProps_ExistingFileMissingUtinniProps_MergesIn): temp dir with a Directory.Build.props containing only `<Project xmlns="..."><PropertyGroup><UserProperty>foo</UserProperty></PropertyGroup></Project>`; call CreateDotNetDirectoryProps(tempDir); load result; assert `UserProperty=foo` is PRESERVED (not removed), Utinni properties are PRESENT (merged in), and the PropertyGroup ordering is sensible (planner's discretion; existing siblings before new ones is the conservative choice).
    - Test 3 (DirectoryBuildPropsTests.CreateDotNetDirectoryProps_AlreadyApplied_Idempotent): call CreateDotNetDirectoryProps twice in a row on the same temp dir; assert file contents are byte-identical (or value-stable — XDocument may reorder attributes, so the strict assertion is "load both, compare PropertyGroup name/Condition/child values"). Idempotency per D-23.
    - Test 4 (DirectoryBuildPropsTests.CreateDotNetDirectoryProps_ExistingFileWithDifferentUtinniValues_UpdatesValues): temp dir with a Directory.Build.props containing the Utinni PropertyGroup but with stale values (e.g., `<UtinniCoreDotNetPath>OLD\path</UtinniCoreDotNetPath>`); call CreateDotNetDirectoryProps; assert values are UPDATED to the wizard's defaults — covers the case where the user ran an OLDER VSIX wizard, then updates the wizard, then re-runs.
    - Test 5 (DirectoryBuildPropsTests.CreateDotNetDirectoryProps_NoXxeAttack_XDocumentSafe): create a Directory.Build.props with an XXE attempt (`<!DOCTYPE foo [<!ENTITY xxe SYSTEM "file:///c:/windows/win.ini">]><Project>&xxe;</Project>`); call CreateDotNetDirectoryProps; assert no exception (XDocument's default DtdProcessing=Prohibit handles this safely per threat-model note); assert the file is rewritten in a clean form (no DOCTYPE preserved).
  </behavior>
  <action>
    Step 1 — Rewrite `sdk/UtinniPluginTemplates/Vsix/Utility/Props.cs` `CreateDotNetDirectoryProps` body per `<interfaces>` "R-G Props.CreateDotNetDirectoryProps shape" + PATTERNS.md lines 872-922:

    - Replace the early-return + StringBuilder body with an XDocument-based load-modify-write flow.
    - Add `using System.Xml.Linq;` and `using System.Linq;` to the file's using-block.
    - Define namespace constant `XNamespace ns = "http://schemas.microsoft.com/developer/msbuild/2003"`.
    - If file exists: `XDocument.Load(filePath)`; else: `new XDocument(new XElement(ns + "Project"))`.
    - Resolve `<Project>` root; create-if-missing.
    - For each PropertyGroup the wizard owns (one unconditional with PluginOutputDir + UtinniCoreDotNetPath; three configuration-conditional groups for RelWithDbgInfo|AnyCPU / Release|AnyCPU / Debug|AnyCPU — hoist the property name-value pairs from the existing StringBuilder body), call `UpsertPropertyGroup(project, ns, conditionAttr, properties)`.
    - `doc.Save(filePath)`.

    Step 2 — Add the `private static void UpsertPropertyGroup(XElement project, XNamespace ns, string conditionAttr, (string name, string value)[] properties)` helper per `<interfaces>` "UpsertPropertyGroup":
    - Find the PropertyGroup whose `Condition` attribute matches `conditionAttr` (null = unconditional group with no Condition attribute).
    - Create the group if not found.
    - For each (name, value) pair, set or create the child element with that name and value.
    - Idempotency invariant: calling the method twice with the same args yields a value-equivalent result.

    CON-T-04 invariant: the method stays in `Props.cs`; do NOT factor `UpsertPropertyGroup` into a new file. Comment cites D-23 + CON-T-04.

    XML-safety per `<threat_model>` T-03-03-01: XDocument's default `DtdProcessing` is `Prohibit` in .NET 4.7.2 — DTDs (and therefore XXE) are rejected at parse time. The default behavior is sufficient for this threat. Test 5 verifies.

    Step 3 — Create `UtinniCoreDotNet.Tests/DirectoryBuildPropsTests.cs` with the five Facts above. Each Fact uses the temp-dir fixture pattern from `CppSharpSlnDirTests.cs:74-104` (Directory.CreateDirectory + File.WriteAllText seed + try/finally cleanup). The class does NOT need `[Collection("StaticCallbackState")]` (Props.cs is stateless — temp dirs isolate completely).

    Test-side wiring for `Props.CreateDotNetDirectoryProps` (committed to option (c) per checker I3): The test project (`UtinniCoreDotNet.Tests`) does NOT currently reference the VSIX project (`sdk/UtinniPluginTemplates/Vsix`). DEFAULT: option (c) linked-source-file — add `<Compile Include="..\..\sdk\UtinniPluginTemplates\Vsix\Utility\Props.cs" Link="Linked\Props.cs" />` to `UtinniCoreDotNet.Tests.csproj`. Lightest touch; respects CON-T-04 (Props.cs location is fixed in the VSIX project; we're testing BEHAVIOR, not adding a new home). Document in the commit message: "Props.cs linked into UtinniCoreDotNet.Tests via <Compile Link=...> per Plan 03-03 Task 3 default."

    FALLBACKS (only IF the default linked-source path hits a build error — e.g., Props.cs depends on a Vsix-only assembly reference not available to the Tests project):
    - (a) Add a ProjectReference from `UtinniCoreDotNet.Tests` to the VSIX project. Likely blocked because the VSIX project's `<ProjectTypeGuids>` marks it as a VSIX, not a regular library; net472 ProjectReference rules may reject it. If accepted, this is cleaner than (c).
    - (b) Extract `Props.cs` into a small helper library project that both VSIX and Tests reference. Heaviest change; only justified if (a) AND (c) both fail.

    Choose fallback only after attempting (c) and confirming a specific build error; do not pre-empt with the heavier paths.
  </action>
  <verify>
    <automated>powershell -Command "& 'C:\\Program Files\\Microsoft Visual Studio\\2022\\Community\\MSBuild\\Current\\Bin\\msbuild.exe' Utinni.sln /p:Configuration=Release /p:Platform=x86 /v:minimal"</automated>
    <automated>dotnet test UtinniCoreDotNet.Tests --filter "FullyQualifiedName~DirectoryBuildPropsTests"</automated>
    <automated>powershell -Command "if ((Select-String -Path 'sdk/UtinniPluginTemplates/Vsix/Utility/Props.cs' -Pattern 'XDocument' -SimpleMatch).Count -ge 1 -and (Select-String -Path 'sdk/UtinniPluginTemplates/Vsix/Utility/Props.cs' -Pattern 'UpsertPropertyGroup' -SimpleMatch).Count -ge 1) { exit 0 } else { Write-Error 'R-G: XDocument or UpsertPropertyGroup missing from Props.cs'; exit 1 }"</automated>
  </verify>
  <done>
    `Props.CreateDotNetDirectoryProps` is an idempotent XDocument-based merger; three-case fixture Facts pass + XXE-safety Fact passes + value-update Fact passes. CON-T-04 invariant verified: `Props.cs` is the single file changed; no new file under `sdk/UtinniPluginTemplates/Vsix/Utility/`. `UpsertPropertyGroup` is a private static helper inside Props.cs.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update docs/ai/assessment.md — mark R-E + R-F + R-G done; record CON-O-05 disposition (StdEdited.cs hand-curated); verify all R-X rows = done</name>
  <files>docs/ai/assessment.md</files>
  <read_first>
    @docs/ai/assessment.md §"Status tracking" (R-X table — Plan 03-01 set R-A + R-H done; Plan 03-02 set R-B + R-C done; this task sets R-E + R-F + R-G done, and asserts R-D was already done from Phase 1)
    @docs/ai/assessment.md §"Open questions" (CON-O-05 row — disposition pending; this task closes it with the SHA of Task 2)
    @.planning/phases/03-strategic-reworks-r-a-r-h/03-CONTEXT.md decision D-24 (CON-O-05 disposition: StdEdited.cs hand-curated for CppSharp-cannot-handle STL templates like std::basic_string; auto-discovery regenerates only Generated/UtinniCore.cs; StdEdited.cs and Std.cs are explicitly out of scope)
    @.planning/phases/03-strategic-reworks-r-a-r-h/03-CONTEXT.md §"Specifics" (CON-O-05 + CON-O-07 disposition wording = Phase 02 D-12 style: one-line comment + link to implementing commit SHA)
  </read_first>
  <action>
    Step 1 — Read `docs/ai/assessment.md` §"Status tracking". Locate R-E, R-F, R-G rows. For each, set status to `done` and append the implementing commit SHA from this plan's Tasks 1-3.

    Step 2 — Verify R-D (CI workflow) is ALREADY marked done from Phase 1 (per `<domain>` "Out of scope" — R-D was closed by Phase 1 Plan 01-02). If not marked done in the status table, update it now with the Phase 1 commit SHA. This ensures the Phase 3 deliverable (all 8 R-letters closed = STAB-02 satisfied) is fully reflected in the assessment table.

    Step 3 — Read `docs/ai/assessment.md` §"Open questions". Locate the CON-O-05 row. Append the disposition: "StdEdited.cs is the only hand-curated Generated/ file. CppSharp's STL-template handling for std::basic_string is unreliable so StdEdited.cs is curated by hand; the R-F header auto-discovery regenerates only Generated/UtinniCore.cs. Std.cs (from UtinniCore-Symbols) is also out of scope. Curation criteria: (a) CppSharp generates incorrect output for the symbol, OR (b) the symbol name is unstable across MSVC versions, OR (c) the binding requires marshaling logic CppSharp can't infer. Disposition resolved alongside R-F in commit `<sha-of-task-2>`." Use Phase 02 KB-05 / D-12 row formatting precedent.

    Step 4 — Sanity-check: after this commit, EVERY R-letter row (R-A through R-H) in §"Status tracking" should read `done`. R-D was Phase 1; R-A + R-H were Plan 03-01; R-B + R-C were Plan 03-02; R-E + R-F + R-G are this task. If any row is still `pending`/`open`, surface it as a blocker — the phase isn't done.

    Step 5 — If §"Status tracking" includes an aggregate counter (e.g., "remaining R-items: 7"), decrement to 0.
  </action>
  <verify>
    <automated>powershell -Command "$c = Get-Content 'docs/ai/assessment.md' -Raw; $re = $c -match 'R-E.*done'; $rf = $c -match 'R-F.*done'; $rg = $c -match 'R-G.*done'; $std = $c -match 'CON-O-05.*hand-curated|StdEdited.*hand'; $all = ($c | Select-String -Pattern 'R-[A-H].*done' -AllMatches).Matches.Count; if ($re -and $rf -and $rg -and $std -and $all -ge 8) { exit 0 } else { Write-Error \"R-E=$re R-F=$rf R-G=$rg CON-O-05=$std totalDone=$all\"; exit 1 }"</automated>
  </verify>
  <done>
    `docs/ai/assessment.md` §Status tracking shows R-A..R-H all = done with SHAs. §Open questions CON-O-05 row carries the "hand-curated, not regenerated" disposition. STAB-02 deliverable closure visible in the table. Phase 3 is functionally complete after this commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time → source tree | R-F's `Directory.EnumerateFiles("UtinniCore", "*.h", AllDirectories)` reads the source tree at build time. Inputs are repo files (operator-controlled), not network input. |
| `[CallerFilePath]` resolution → log sink | R-E exposes source file paths (compile-time-resolved) into log lines. Already-implicit — `StackTrace.GetFrame` walked the same data at runtime before. |
| VSIX wizard → user's solution dir | R-G `Directory.Build.props` load+write touches files in the user's solution directory. Wizard runs only on user-initiated New Project action; no untrusted network input. |
| XML parser → potentially-malicious Directory.Build.props | XDocument.Load on a Directory.Build.props that could contain XXE entities. Mitigated by .NET 4.7.2 default `DtdProcessing=Prohibit` — XDocument rejects DOCTYPE declarations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | Directory.Build.props with XXE attack | mitigate | XDocument default DtdProcessing=Prohibit in .NET 4.7.2; DOCTYPE declarations are rejected at parse time. Test 5 in Task 3 explicitly covers this case (parse + assert no exception + assert file rewritten without DOCTYPE). |
| T-03-03-02 | Information disclosure | [CallerFilePath] leaking source machine paths into log | accept | Build-machine paths are already implicit in compiled .pdb files and in StackTrace output before R-E. R-E doesn't expose anything new; it just shifts the resolution from runtime to compile time. V1 is operator-locally-built; no public log distribution. Documented as defensive disposition. |
| T-03-03-03 | Denial of service | R-F header glob scans the entire UtinniCore tree on every build | accept | UtinniCore has ~100 .h files; Directory.EnumerateFiles is a single OS call; cost is negligible (<10ms on any modern Windows machine). Build-time only, not at runtime. |
| T-03-03-04 | Tampering | A malicious header named `_internal/something.h` placed by an attacker | accept | The build is operator-driven on operator-controlled source. No network input. The filter convention is documentation of intent (which headers to project), not a security boundary. |
| T-03-03-05 | Information disclosure | R-G UpsertPropertyGroup preserves user's existing custom properties | mitigate (positive) | This is feature, not threat: the merger PRESERVES user properties instead of silently destroying them (the pre-R-G silent early-return was the actual data-loss bug). Tests 2 and 4 in Task 3 cover. |
| T-03-03-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan. Existing test deps already pinned. |

Block-on-high: none.
</threat_model>

<verification>
- `msbuild Utinni.sln /p:Configuration=Release /p:Platform=x86` exits 0; all touched targets (UtinniCoreDotNet, UtinniCoreDotNetGen, UtinniCoreDotNet.Tests, sdk VSIX project) build green.
- `dotnet test UtinniCoreDotNet.Tests` exits 0; all new Facts (LogCallerMemberNameTests + HeaderDiscoveryTests + DirectoryBuildPropsTests) + all prior Plan 03-01/03-02 tests + Phase 1/2/2.1 tests pass.
- `UtinniCoreDotNet/Utility/Log.cs` contains zero matches for `new StackTrace\(\)\.GetFrame` (R-E completion).
- `UtinniCoreDotNetGen/Program.cs` contains `Directory.EnumerateFiles` + a `HeaderDiscovery` class (R-F completion).
- `sdk/UtinniPluginTemplates/Vsix/Utility/Props.cs` contains `XDocument` + `UpsertPropertyGroup` (R-G completion).
- `UtinniCoreDotNet/Generated/UtinniCore.cs` regenerates cleanly and contains Plan 03-02 R-C's `getSwgWndProc` symbol (proves R-F integration with R-C).
- `docs/ai/assessment.md` §Status tracking shows all 8 R-letters = done; §Open questions CON-O-05 + CON-O-07 dispositions both present.
- CI Windows-2022 workflow stays green on master after every commit.
</verification>

<success_criteria>
- Log.cs replaces `new StackTrace().GetFrame` with `[CallerMemberName]`/`[CallerFilePath]` defaulted parameters on all five public Log methods; existing call sites compile unchanged; runtime cost = 0.
- UtinniCoreDotNetGen/Program.cs replaces the 23-entry explicit header allowlist with `HeaderDiscovery.Discover(...)` (glob + `_internal/` filter, case-insensitive).
- StdEdited.cs and Std.cs remain hand-curated (CON-O-05 disposition); only `Generated/UtinniCore.cs` is regenerated.
- Props.CreateDotNetDirectoryProps is an idempotent XDocument merger; preserves user properties; handles no-file / file-without-Utinni-props / already-applied cases; XXE-safe.
- New xUnit Facts in three test files pass.
- `docs/ai/assessment.md` §Status tracking: R-A..R-H all = done with SHAs. §Open questions: CON-O-05 + CON-O-07 dispositions both committed.
- STAB-02 requirement satisfied: "All 8 R-X items show done; plugin authoring is genuinely pleasant per assessment.md; CI catches regressions on every PR."
- CI stays green on master after every commit; Phase 3 is functionally complete.
</success_criteria>

<output>
Create `.planning/phases/03-strategic-reworks-r-a-r-h/03-03-SUMMARY.md` when done, following `@$HOME/.claude/get-shit-done/templates/summary.md`. Record:
- Final commit SHAs for each task (1-4)
- Confirmation that every R-letter row in `docs/ai/assessment.md` reads `done` (R-A..R-H, 8/8)
- Confirmation that CON-O-05 + CON-O-07 dispositions are both visible in §Open questions
- A line in the SUMMARY noting "Phase 3 (STAB-02) complete; Phase 4 (TEST-03 Tier 2 CLI shim) is the next phase per ROADMAP.md"
- Any deviations from the plan (e.g., Props.cs linked-file vs ProjectReference choice in Task 3)
</output>
