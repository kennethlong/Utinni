---
phase: 03-strategic-reworks-r-a-r-h
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - UtinniCore/plugin_framework/plugin_manager.cpp
  - UtinniCore/plugin_framework/plugin_manager.h
  - UtinniCore/plugin_framework/utinni_plugin.h
  - UtinniCore/swg/client/client.h
  - UtinniCore/swg/client/client.cpp
  - UtinniCoreDotNet/UI/Controls/PanelGame.cs
  - UtinniCoreDotNet/Utility/Native.cs
  - UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj
  - UtinniCoreDotNet.Tests/PluginManagerLifecycleTests.cs
  - UtinniCoreDotNet.Tests/GetSwgWndProcTests.cs
  - Utinni.CrtMatchPlugin/Utinni.CrtMatchPlugin.vcxproj
  - Utinni.CrtMatchPlugin/main.cpp
  - Utinni.LegacyPlugin/Utinni.LegacyPlugin.vcxproj
  - Utinni.LegacyPlugin/main.cpp
  - Utinni.sln
  - docs/ai/assessment.md
autonomous: false
requirements: [STAB-02]

must_haves:
  truths:
    - "UTINNI_PLUGIN macro declares BOTH `extern \"C\" __declspec(dllexport) createPlugin()` and `extern \"C\" __declspec(dllexport) destroyPlugin(UtinniPlugin*)`."
    - "PluginManager::loadPlugins runs two passes: pass 1 LoadLibrary+createPlugin all DLLs (HMODULE + plugin* + cached destroyPlugin captured); pass 2 calls init() on each with per-plugin try/catch isolation (Phase 02 C-06 precedent extended to init phase)."
    - "LoadLibrary failures are logged with GetLastError to the framework log sink (log::error), no MessageBox."
    - "~PluginManager invokes plugin's destroyPlugin (in plugin's CRT) THEN FreeLibrary (host's CRT) per registered plugin; legacy plugins with no destroyPlugin export fall back to virtual destructor."
    - "PluginManager::Impl tracks `vector<LoadedPlugin {HMODULE, UtinniPlugin*, destroyFn}>`; pImpl invariant (CON-N-08) preserved — plugin_manager.h public surface unchanged."
    - "Two new fixture DLL projects (`Utinni.CrtMatchPlugin` /MD, `Utinni.LegacyPlugin` /MT) exist at solution root; both build green; LegacyPlugin omits destroyPlugin export entirely (exercises fallback)."
    - "`UtinniCoreDotNet.Tests/Fixtures/CrtMatchPlugin/` + `Fixtures/LegacyPlugin/` directories are deployment landing zones; CopyNativeArtifactsForTests target copies both DLLs alongside UtinniCore.dll."
    - "PanelGame.cs no longer contains the literal `0x00AA0970`; the value is resolved once at ctor via Native.GetSwgWndProc() (P/Invoke into extern \"C\" getSwgWndProcExport) and cached as a field."
    - "Client::getSwgWndProc() declared in `swg/client/client.h` (in-namespace) + defined in `client.cpp`; `extern \"C\" __declspec(dllexport) IntPtr __cdecl getSwgWndProcExport()` shim mirrors the existing getSwgHwndExport at client.cpp:290-293."
    - "xUnit Facts assert: PluginManager two-phase init order; per-plugin init() exception isolation; LoadLibrary failure logged; destroyPlugin called on shutdown; legacy plugin fallback to virtual destructor; getSwgWndProc returns 0x00AA0970; PanelGame.cs source no longer contains the literal."
    - "Cross-repo commit lands in `kennethlong/UtinniPlugins` exporting destroyPlugin from TJT entry point per CON-T-05 *Impl separation."
    - "docs/ai/assessment.md §Status tracking marks R-B and R-C done; §Open questions marks CON-O-07 disposition (Sytner's plugin = legacy, no compat target) with the implementing commit SHA."
    - "Decision coverage (CONTEXT.md): this plan addresses D-01 (plan grouping by category — Lifecycle+RVAs category), D-02 (CI-gated ordering — depends_on 03-01), D-03 (atomic commits per R-X — R-B + R-C each ship as one or more atomic commits paired with their harness), D-04 (CI gate at plan boundary — 03-02 doesn't start until 03-01 is green), D-05 (max-harness — every R-X has a regression test), D-06 (Tier-4 manual carve-out — R-C live-SWG WndProc forwarding attested via 'Verified-by: live SWG window forwards WM_KEYDOWN end-to-end' commit-body line), D-07 (test home + sibling fixture-project pattern — Utinni.CrtMatchPlugin / Utinni.LegacyPlugin sibling DLLs), D-13 (symmetric exported createPlugin+destroyPlugin via UTINNI_PLUGIN macro), D-14 (two-phase init: all createPlugin then all init with per-plugin try/catch), D-15 (CON-O-07 disposition: Sytner = legacy, no compat target), D-16 (HMODULE tracked + FreeLibrary in ~PluginManager), D-17 (LoadLibrary failure logged with GetLastError), D-18 (R-C scope: only the actual duplicate WndProc — isSafeToUse stays native-only), D-19 (mechanism: Client::getSwgWndProc UTINNI_API + extern \"C\" getSwgWndProcExport shim), D-20 (resolution timing: PanelGame ctor cache once, hot-path reads from field), D-26 (cross-repo paired commit in kennethlong/UtinniPlugins for TJT destroyPlugin export)."
  artifacts:
    - path: "UtinniCore/plugin_framework/utinni_plugin.h"
      provides: "UTINNI_PLUGIN macro with both createPlugin and destroyPlugin declarations"
      contains: "destroyPlugin"
    - path: "UtinniCore/plugin_framework/plugin_manager.cpp"
      provides: "Two-phase loadPlugins + HMODULE tracking + LoadLibrary error log + destroyPlugin/FreeLibrary shutdown"
      contains: "GetLastError"
    - path: "UtinniCore/swg/client/client.cpp"
      provides: "Client::getSwgWndProc() definition + extern \"C\" getSwgWndProcExport shim"
      contains: "getSwgWndProcExport"
    - path: "UtinniCore/swg/client/client.h"
      provides: "static IntPtr Client::getSwgWndProc() declaration"
      contains: "getSwgWndProc"
    - path: "UtinniCoreDotNet/UI/Controls/PanelGame.cs"
      provides: "P/Invoke-cached swgWndProcAddr field; literal 0x00AA0970 removed"
    - path: "UtinniCoreDotNet/Utility/Native.cs"
      provides: "[DllImport] GetSwgWndProc declaration (EntryPoint=getSwgWndProcExport)"
      contains: "getSwgWndProcExport"
    - path: "Utinni.CrtMatchPlugin/main.cpp"
      provides: "Fixture plugin with both createPlugin and destroyPlugin exports; CRT match (/MD)"
      contains: "destroyPlugin"
    - path: "Utinni.LegacyPlugin/main.cpp"
      provides: "Fixture plugin with ONLY createPlugin export; CRT mismatch (/MT) — exercises legacy fallback"
    - path: "UtinniCoreDotNet.Tests/PluginManagerLifecycleTests.cs"
      provides: "R-B regression Facts (two-phase init, exception isolation, LoadLibrary error log, destroyPlugin invoked, legacy fallback)"
      contains: "LoadPlugins_TwoPhaseInit_BothInitsCalledAfterAllCreatePlugins"
    - path: "UtinniCoreDotNet.Tests/GetSwgWndProcTests.cs"
      provides: "R-C regression Facts (getter returns 0x00AA0970; PanelGame.cs source no longer contains the literal)"
      contains: "GetSwgWndProc_ReturnsLiteralConstant"
  key_links:
    - from: "UtinniCoreDotNet/UI/Controls/PanelGame.cs"
      to: "UtinniCoreDotNet/Utility/Native.cs"
      via: "Native.GetSwgWndProc() P/Invoke called in ctor"
      pattern: "Native\\.GetSwgWndProc\\(\\)"
    - from: "UtinniCoreDotNet/Utility/Native.cs"
      to: "UtinniCore/swg/client/client.cpp"
      via: "[DllImport(...)] EntryPoint = \"getSwgWndProcExport\""
      pattern: "EntryPoint = \"getSwgWndProcExport\""
    - from: "UtinniCore/plugin_framework/plugin_manager.cpp"
      to: "UtinniCore/plugin_framework/utinni_plugin.h"
      via: "GetProcAddress(hDll, \"destroyPlugin\") matches the macro-declared export"
      pattern: "GetProcAddress.*destroyPlugin"
    - from: "Utinni.CrtMatchPlugin/main.cpp"
      to: "UtinniCore/plugin_framework/utinni_plugin.h"
      via: "Includes utinni_plugin.h for UTINNI_PLUGIN macro + base class"
      pattern: "utinni_plugin\\.h"
---

<objective>
Land R-B (symmetric plugin lifecycle: `createPlugin`/`destroyPlugin` ABI, two-phase init, `LoadLibrary` failures logged, HMODULE tracking) and R-C (single-source-of-truth WndProc RVA `0x00AA0970` surfaced via `UTINNI_API` getter consumed via P/Invoke from `PanelGame.cs`).

Purpose: TD-17 (plugin `init()` silently never called; cross-CRT delete crash on plugin teardown) and TD-18 (RVAs duplicated across the native↔managed boundary; managed-side `new IntPtr(0x00AA0970)` literal at `PanelGame.cs:41` drifts independently from the native-side declaration at `client.cpp:43`). The plugin ABI change is intentionally breaking for legacy plugins (Sytner) per D-15 / CON-O-07 disposition — upstream is dormant per `project_fork_strategy` memory.

Output: `UTINNI_PLUGIN` macro extends to declare `destroyPlugin`; `PluginManager::loadPlugins` two-passes (createPlugin all → init all) with per-plugin try/catch around init; HMODULE tracked in pImpl (CON-N-08 preserved); LoadLibrary failures logged with GetLastError; two fixture plugin DLLs (`Utinni.CrtMatchPlugin` /MD, `Utinni.LegacyPlugin` /MT) added to `Utinni.sln`; deployment landing zones at `UtinniCoreDotNet.Tests/Fixtures/{CrtMatchPlugin,LegacyPlugin}/`; new R-B + R-C xUnit Facts (lifecycle behavior + native getter + grep negative). R-C surfaces `Client::getSwgWndProc()` in `UTINNI_API` + `extern "C" getSwgWndProcExport()` C-linkage shim (mirroring the Phase B `getSwgHwndExport` precedent at `client.cpp:290-293`); `PanelGame.cs` caches the address once at ctor. Cross-repo paired commit lands in `kennethlong/UtinniPlugins` for TJT `destroyPlugin` export. `docs/ai/assessment.md` updated: R-B + R-C status = done; CON-O-07 disposition row.

Note on effective task count (per checker W1): This plan has 5 tasks but the effective auto-execute count is 3. Tasks 1-3 are auto and carry the substantive R-B/R-C engineering work; Task 4 is a cross-repo human-checkpoint (TJT destroyPlugin export in kennethlong/UtinniPlugins, no CLI/API surface for cross-repo automation per planner authority limits); Task 5 is a small docs/ai/assessment.md status-tracking update (R-B + R-C status + CON-O-07 disposition row). Tasks 4 and 5 are kept separate from Tasks 1-3 to (a) preserve the human-checkpoint gate resume-signal protocol and (b) isolate the docs commit so it can land after the cross-repo work is confirmed green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-strategic-reworks-r-a-r-h/03-CONTEXT.md
@.planning/phases/03-strategic-reworks-r-a-r-h/03-PATTERNS.md
@.planning/phases/03-strategic-reworks-r-a-r-h/03-01-SUMMARY.md
@.planning/intel/constraints.md
@.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@docs/ai/assessment.md

<interfaces>
<!-- Key contracts the executor will define/consume. Drawn from PATTERNS.md per D-13/D-14/D-16/D-18/D-19. -->

UTINNI_PLUGIN macro (D-13; `UtinniCore/plugin_framework/utinni_plugin.h:50` rewrite):

```cpp
// Before (single line):
#define UTINNI_PLUGIN extern "C" __declspec(dllexport) utinni::UtinniPlugin* createPlugin()

// After (two declarations — both required of every plugin):
#define UTINNI_PLUGIN \
    extern "C" __declspec(dllexport) utinni::UtinniPlugin* createPlugin(); \
    extern "C" __declspec(dllexport) void destroyPlugin(utinni::UtinniPlugin* p)
```

PluginManager::Impl shape (D-16; CON-N-08 invariant says this stays inside pImpl):

```cpp
struct PluginManager::Impl
{
    std::vector<PluginConfig> pluginConfigs;
    struct LoadedPlugin
    {
        HMODULE hModule;
        UtinniPlugin* plugin;
        void(*destroyFn)(UtinniPlugin*);   // nullptr for legacy plugins (fallback to virtual dtor)
    };
    std::vector<LoadedPlugin> plugins;
};
```

Plugin loader pass shapes (D-14 + D-17 — exact body in PATTERNS.md lines 619-684):
- Pass 1: enumerate `.dll` files; `LoadLibrary` → on failure log::error with path + GetLastError + continue; `GetProcAddress(createPlugin)` → if null, FreeLibrary + continue; invoke createPlugin → if null, FreeLibrary + continue; `GetProcAddress(destroyPlugin)` (may be null for legacy); push `{hModule, plugin, destroyFn}` to Impl::plugins.
- Pass 2: foreach loaded plugin: try { plugin->init(); } catch (std::exception&) { log::error("Plugin init() threw: %s ...", ex.what(), plugin->getInformation().name); } catch (...) { log::error("Plugin init() threw unknown exception"); }

~PluginManager (D-13 + D-16 destruction order — destroyPlugin (plugin's CRT) → FreeLibrary (host's CRT)):

```cpp
PluginManager::~PluginManager()
{
    for (const auto& loaded : pImpl->plugins)
    {
        if (loaded.destroyFn != nullptr) { loaded.destroyFn(loaded.plugin); }
        else                              { delete loaded.plugin; }   // legacy fallback (CON-O-07 disposition)
        FreeLibrary(loaded.hModule);
    }
    delete pImpl;
}
```

R-C getter (D-18 + D-19 — `client.h` + `client.cpp` per PATTERNS.md §"UtinniCore/swg/client/client.h..."):

```cpp
// client.h (alongside existing static HWND getSwgHwnd() at lines 78-79):
static IntPtr getSwgWndProc();

// client.cpp (alongside existing Client::getSwgHwnd() at lines 122-125):
IntPtr Client::getSwgWndProc() { return (IntPtr)swg::client::wndProc; }

// extern "C" shim (alongside existing getSwgHwndExport at client.cpp:290-293):
extern "C" __declspec(dllexport) IntPtr __cdecl getSwgWndProcExport()
{
    return (IntPtr)swg::client::wndProc;
}
```

R-C consumer (D-20 — `PanelGame.cs:41` rewrite; cache once at ctor):

```csharp
private readonly IntPtr swgWndProcAddr;

public PanelGame(PluginLoader pluginLoader)
{
    // existing ctor body...
    swgWndProcAddr = Native.GetSwgWndProc();  // P/Invoke once
}

protected override void WndProc(ref Message m)
{
    Native.CallWindowProc(swgWndProcAddr, m.HWnd, m.Msg, m.WParam, m.LParam);
    base.WndProc(ref m);
}
```

P/Invoke declaration (Native.cs — mirror existing GetSwgHwnd at Native.cs:121-123):

```csharp
[DllImport("UtinniCore", CallingConvention = CallingConvention.Cdecl, EntryPoint = "getSwgWndProcExport")]
public static extern IntPtr GetSwgWndProc();
```

Fixture plugins (D-07 + PATTERNS.md §"Utinni.CrtMatchPlugin/...") — sibling vcxproj projects at solution root, DynamicLibrary, RuntimeLibrary diverges per fixture:

```cpp
// Utinni.CrtMatchPlugin/main.cpp (full plugin):
#include "plugin_framework/utinni_plugin.h"
class CrtMatchPlugin : public utinni::UtinniPlugin {
public:
    void init() override { s_initCount++; }
    const Information& getInformation() const override {
        static Information info = { "CrtMatchPlugin", "R-B fixture (CRT match)", "Phase 3" };
        return info;
    }
};
// Per checker W5 / Task 2 Test 1 two-phase ordering observability: increment a
// distinct counter inside createPlugin so the xUnit test can read BOTH counters
// at instrumented points and assert "all createPlugin returned before first init()".
static int s_createCount = 0;
static int s_initCount = 0;
extern "C" __declspec(dllexport) utinni::UtinniPlugin* createPlugin() { s_createCount++; return new CrtMatchPlugin(); }
extern "C" __declspec(dllexport) void destroyPlugin(utinni::UtinniPlugin* p) { delete p; }   // D-13 symmetric path
extern "C" __declspec(dllexport) int __cdecl crtmatch_getCreateCount() { return s_createCount; }
extern "C" __declspec(dllexport) int __cdecl crtmatch_getInitCount() { return s_initCount; }

// Utinni.LegacyPlugin/main.cpp: same shape MINUS the destroyPlugin export entirely (exercises fallback).
```

Native test bridge exports (added to `UtinniCore/utinni_exports.cpp`):

```cpp
// Constructs a PluginManager pointing at a temp Plugins/ dir, runs loadPlugins, returns observable state.
extern "C" __declspec(dllexport) int   __cdecl utinni_test_pluginManagerLoadFromDir(const char* dir);
extern "C" __declspec(dllexport) int   __cdecl utinni_test_pluginManagerLoadedCount();
extern "C" __declspec(dllexport) void  __cdecl utinni_test_pluginManagerDispose();   // forces dtor — triggers destroyPlugin
extern "C" __declspec(dllexport) int   __cdecl utinni_test_lastLoadLibraryError();   // 0 if no error captured during last load
```

Native test bridge for R-C (one export, no parameters):

```cpp
// (Existing) extern "C" __declspec(dllexport) IntPtr __cdecl getSwgWndProcExport(); — already declared by R-C task; reused by R-C test.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend UTINNI_PLUGIN macro for symmetric destroyPlugin + create the two R-B fixture plugin projects</name>
  <files>
    UtinniCore/plugin_framework/utinni_plugin.h,
    Utinni.CrtMatchPlugin/Utinni.CrtMatchPlugin.vcxproj,
    Utinni.CrtMatchPlugin/main.cpp,
    Utinni.LegacyPlugin/Utinni.LegacyPlugin.vcxproj,
    Utinni.LegacyPlugin/main.cpp,
    Utinni.sln,
    UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj
  </files>
  <read_first>
    @UtinniCore/plugin_framework/utinni_plugin.h (line 50 single-line macro — replace with two-declaration form per D-13)
    @UtinniCore/plugin_framework/plugin_manager.h (public PluginManager surface — CON-N-08 invariant; do NOT modify)
    @Utinni.LoaderLockHarness/Utinni.LoaderLockHarness.vcxproj (sibling-project vcxproj precedent for Phase 02 — lines 1-46 project shape; lines 46-53 OutDir pattern; lines 95-102 ProjectReference)
    @Utinni.LoaderLockHarness/main.cpp (MIT header + minimal Windows.h includes pattern — lines 1-23)
    @Utinni.sln (current sln entries — lines 30-44 for sibling-project pattern; lines 80-84 for configuration mappings)
    @UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj (existing CopyNativeArtifactsForTests Target at lines 57-66 — extend with the two new fixture DLLs)
    @.planning/phases/03-strategic-reworks-r-a-r-h/03-PATTERNS.md §"Utinni.CrtMatchPlugin/Utinni.CrtMatchPlugin.vcxproj + main.cpp (NEW R-B fixture pair)" (lines 102-189) — includes vcxproj knobs, MIT header, plugin body shapes, CRT discipline differences
    @.planning/phases/03-strategic-reworks-r-a-r-h/03-PATTERNS.md §"UtinniCoreDotNet.Tests/Fixtures/CrtMatchPlugin/ + ..." (lines 192-218) — deployment landing zone pattern (no in-fixture-dir csproj — vcxproj at solution root)
    @.planning/phases/03-strategic-reworks-r-a-r-h/03-CONTEXT.md decisions D-07, D-13, D-15
    @.planning/codebase/CONVENTIONS.md (MIT header on every C++ source file)
  </read_first>
  <behavior>
    - Both fixture projects (`Utinni.CrtMatchPlugin`, `Utinni.LegacyPlugin`) build green under Release|Win32 + Debug|Win32 + RelWithDbgInfo|Win32 (the Phase 02 D-09 sln entry convention).
    - `Utinni.CrtMatchPlugin.dll` exports BOTH `createPlugin` and `destroyPlugin` (verifiable via `dumpbin /exports` or programmatic GetProcAddress in test); `s_createCount` increments via `crtmatch_getCreateCount()` on createPlugin and `s_initCount` increments via `crtmatch_getInitCount()` on init — the two counters let Task 2 Test 1 observe two-phase ordering (per checker W5).
    - `Utinni.LegacyPlugin.dll` exports ONLY `createPlugin` (no `destroyPlugin` symbol present — exercises the loader's fallback-to-virtual-destructor path per D-13).
    - The CopyNativeArtifactsForTests Target picks up both new DLLs and copies them into `bin/$(Configuration)/UtinniCoreDotNet.Tests/` next to UtinniCore.dll.
    - The UTINNI_PLUGIN macro change is a compile break for any plugin that uses the macro but does not define a matching `destroyPlugin` (Sytner — dormant; D-15 disposition).
  </behavior>
  <action>
    Step 1 — Rewrite `UtinniCore/plugin_framework/utinni_plugin.h:50` per `<interfaces>` "UTINNI_PLUGIN macro" — replace the single-line `#define UTINNI_PLUGIN` with the two-declaration form. Add a comment block citing D-13 and the rationale (symmetric ABI, plugin-owns-alloc-and-free in its own CRT, eliminates CON-B-04 cross-CRT delete crash class).

    Step 2 — Create `Utinni.CrtMatchPlugin/Utinni.CrtMatchPlugin.vcxproj` per PATTERNS.md lines 106-139:
    - New GUID (use `New-Guid` PowerShell cmdlet or VS auto-generate)
    - `<ConfigurationType>DynamicLibrary</ConfigurationType>` (DLL not exe — overrides the LoaderLockHarness Application analog)
    - `<PlatformToolset>v142</PlatformToolset>` (CON-T-02 toolset)
    - `<WindowsTargetPlatformVersion>10.0</WindowsTargetPlatformVersion>`
    - Three solution configs: Debug|Win32, Release|Win32, RelWithDbgInfo|Win32
    - OutDir = `$(SolutionDir)bin\$(Configuration)\` (co-locates fixture DLL with UtinniCore.dll)
    - AdditionalIncludeDirectories = `$(SolutionDir);$(SolutionDir)external;$(ProjectDir);%(AdditionalIncludeDirectories)`
    - `<RuntimeLibrary>MultiThreadedDLL</RuntimeLibrary>` (`/MD`) for all three configs — matches UtinniCore.dll's CRT per D-07
    - ProjectReference to `..\UtinniCore\UtinniCore.vcxproj` with `<LinkLibraryDependencies>false</LinkLibraryDependencies>` (fixture LoadLibrary's at runtime; no static link)

    Step 3 — Create `Utinni.CrtMatchPlugin/main.cpp` per PATTERNS.md lines 142-189:
    - MIT header verbatim from `Utinni.LoaderLockHarness/main.cpp:1-23`
    - `#include "plugin_framework/utinni_plugin.h"` for the macro
    - `class CrtMatchPlugin : public utinni::UtinniPlugin` with `void init() override { s_initCount++; }` and `const Information& getInformation() const override { ... }`
    - `extern "C" __declspec(dllexport) utinni::UtinniPlugin* createPlugin() { return new CrtMatchPlugin(); }`
    - `extern "C" __declspec(dllexport) void destroyPlugin(utinni::UtinniPlugin* p) { delete p; }` — D-13 symmetric path
    - Diagnostic exports per the `getSwgHwndExport` precedent (PATTERNS.md lines 185-189), with a separate counter for createPlugin (per checker W5 — Task 2 Test 1 needs to compare both counters at instrumented points to assert "all createPlugin returned before first init()" two-phase ordering): `static int s_createCount = 0; static int s_initCount = 0; createPlugin() increments s_createCount BEFORE returning the new CrtMatchPlugin; init() increments s_initCount as before. Exports: extern "C" __declspec(dllexport) int __cdecl crtmatch_getCreateCount() { return s_createCount; } AND extern "C" __declspec(dllexport) int __cdecl crtmatch_getInitCount() { return s_initCount; }`

    Step 4 — Create `Utinni.LegacyPlugin/Utinni.LegacyPlugin.vcxproj` — same shape as Step 2 but with `<RuntimeLibrary>MultiThreaded</RuntimeLibrary>` (`/MT`) for all three configs — deliberately mismatches UtinniCore.dll's CRT per D-07 (exercises the cross-CRT delete crash class that D-13 destroyPlugin fixes).

    Step 5 — Create `Utinni.LegacyPlugin/main.cpp` — same shape as Step 3 BUT **omit `destroyPlugin` export entirely**. Add the diagnostic export with a distinct name: `static int s_initCount = 0; extern "C" __declspec(dllexport) int __cdecl legacy_getInitCount() { return s_initCount; }` so the test can distinguish the two fixtures' init counters.

    Step 6 — Update `Utinni.sln` per PATTERNS.md §"Utinni.sln sibling-project entry pattern" (lines 1000-1014). Add two `Project("{8BC9CEB8-...}")` entries (the vcxproj C++ project type GUID) for the two new fixtures with their new GUIDs, ProjectSection(ProjectDependencies) referencing UtinniCore's GUID `{AEFED7F6-4BA9-44FC-A353-71A463A82FDE}`. Add three configuration mappings per project per the Phase 02 precedent at sln lines 80-84 (`Debug|x86 → Debug|Win32`, `Release|x86 → Release|Win32`, `RelWithDbgInfo|x86 → Release|Win32` per the Phase 02 line-84 mapping that doesn't carry RelWithDbgInfo on harness projects).

    Step 7 — Update `UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj` CopyNativeArtifactsForTests Target (lines 57-66) per PATTERNS.md "Deployment pattern" (lines 200-214). Append two more `<_NativeTestArtifacts Include="$(SolutionDir)bin\$(Configuration)\{Utinni.CrtMatchPlugin,Utinni.LegacyPlugin}.dll" />` items.

    Step 8 — Create the deployment landing zones: empty directories `UtinniCoreDotNet.Tests/Fixtures/CrtMatchPlugin/` and `UtinniCoreDotNet.Tests/Fixtures/LegacyPlugin/`. Add a `.gitkeep` to each so git tracks the directory. The DLLs are NOT checked in; the CopyNativeArtifactsForTests Target deploys them post-build.
  </action>
  <verify>
    <automated>powershell -Command "& 'C:\\Program Files\\Microsoft Visual Studio\\2022\\Community\\MSBuild\\Current\\Bin\\msbuild.exe' Utinni.sln /p:Configuration=Release /p:Platform=x86 /t:Utinni_CrtMatchPlugin /v:minimal"</automated>
    <automated>powershell -Command "& 'C:\\Program Files\\Microsoft Visual Studio\\2022\\Community\\MSBuild\\Current\\Bin\\msbuild.exe' Utinni.sln /p:Configuration=Release /p:Platform=x86 /t:Utinni_LegacyPlugin /v:minimal"</automated>
    <automated>powershell -Command "& 'dumpbin.exe' /exports bin/Release/Utinni.CrtMatchPlugin.dll | Select-String -Pattern 'createPlugin|destroyPlugin'"</automated>
    <automated>powershell -Command "$exports = & 'dumpbin.exe' /exports bin/Release/Utinni.LegacyPlugin.dll | Out-String; if ($exports -match 'createPlugin' -and $exports -notmatch 'destroyPlugin') { exit 0 } else { Write-Error 'LegacyPlugin export shape wrong (expected createPlugin only)'; exit 1 }"</automated>
  </verify>
  <done>
    UTINNI_PLUGIN macro declares both createPlugin + destroyPlugin. Both fixture projects build green; CrtMatchPlugin exports both symbols, LegacyPlugin exports only createPlugin. CopyNativeArtifactsForTests Target deploys both DLLs alongside UtinniCore.dll. Fixture landing zones exist at `UtinniCoreDotNet.Tests/Fixtures/{CrtMatchPlugin,LegacyPlugin}/`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Land R-B PluginManager two-phase init + HMODULE tracking + LoadLibrary error logging + destroyPlugin/FreeLibrary shutdown; add R-B regression Facts</name>
  <files>
    UtinniCore/plugin_framework/plugin_manager.cpp,
    UtinniCore/utinni_exports.cpp,
    UtinniCoreDotNet.Tests/PluginManagerLifecycleTests.cs
  </files>
  <read_first>
    @UtinniCore/plugin_framework/plugin_manager.cpp (lines 33-49 — Impl struct + ~PluginManager; lines 129-150 — loadPlugins body; CON-N-08 invariant says public header surface is frozen — only the anonymous `struct PluginManager::Impl { ... }` in this .cpp changes)
    @UtinniCore/plugin_framework/plugin_manager.h (DO NOT MODIFY — CON-N-08 mandates the public surface is frozen; only the pImpl-hidden Impl struct in plugin_manager.cpp grows)
    @UtinniCore/utinni_exports.cpp (existing utinni_test_* C-linkage exports; add the four new R-B test-bridge exports per `<interfaces>`)
    @UtinniCoreDotNet.Tests/PluginLoaderTests.cs (lines 49-68 FindFixtureDll + lines 70-75 MakeTempDir + lines 77-107 multi-DLL test + lines 151-156 TryDeleteDir finally — exact pattern to mirror for the native-fixture-DLL variant; copy fixture DLLs into a temp dir, point PluginManager at it via P/Invoke, observe loaded count + GetLastError-style capture)
    @UtinniCoreDotNet.Tests/ExportResolutionTests.cs (lines 48-56 — one-export harness with EntryPoint = "utinni_test_*"; exact pattern for the new R-B test-bridge P/Invoke declarations)
    @.planning/phases/03-strategic-reworks-r-a-r-h/03-PATTERNS.md §"UtinniCore/plugin_framework/plugin_manager.cpp (R-B two-phase init...)" (lines 519-684) — includes Impl struct shape, ~PluginManager body, loadPlugins two-pass body with LoadLibrary error log + GetProcAddress(destroyPlugin) + per-plugin try/catch around init
    @.planning/phases/03-strategic-reworks-r-a-r-h/03-CONTEXT.md decisions D-13, D-14, D-15, D-16, D-17
    @.planning/intel/constraints.md CON-N-08 (PluginManager pImpl; public header unchanged), CON-N-02 (thin-wrapper firewall; PluginManager already in `namespace utinni`), CON-B-04 (cross-CRT delete crash class — eliminated by D-13)
    @.planning/phases/02-critical-bug-burn-down-c-01-c-15/02-CONTEXT.md D-04 max-harness posture + D-07 single-test-project (precedent: C-06 per-plugin try/catch isolation in PluginLoader is the disposition this task extends to the init phase)
  </read_first>
  <behavior>
    - Test 1 (PluginManagerLifecycleTests.LoadPlugins_TwoPhaseInit_BothInitsCalledAfterAllCreatePlugins): copy both fixture DLLs into a temp Plugins/ dir; call utinni_test_pluginManagerLoadFromDir; assert utinni_test_pluginManagerLoadedCount() == 2; assert crtmatch_getInitCount() == 1 AND legacy_getInitCount() == 1 (both inits called); assert via timing/instrumentation that all createPlugin returns precede the first init Two-phase ordering assertion (per checker W5): the loader between pass 1 (createPlugin) and pass 2 (init) MUST have crtmatch_getCreateCount() == legacy_getCreateCount() == 1 AND crtmatch_getInitCount() == legacy_getInitCount() == 0 — proving all createPlugin returned before any init ran. Implementation: extend utinni_exports.cpp with a small utinni_test_pluginManagerLoadFromDir_observable variant that exposes the between-passes observation point, OR have Task 2 split the load into utinni_test_pluginManagerCreateAllFromDir + utinni_test_pluginManagerInitAll exports; planner picks the lighter-touch shape during implementation.
    - Test 2 (PluginManagerLifecycleTests.LoadLibraryFailure_LoggedWithGetLastError): create a temp Plugins/ dir; write a deliberately-corrupt 0-byte file named `Broken.dll`; copy CrtMatchPlugin.dll alongside; load; assert CrtMatchPlugin loaded AND the framework log sink received an `error`-level entry matching `/Failed to load plugin DLL .*Broken\.dll: GetLastError=\d+/` (use the existing managed log capture infrastructure from Phase 02 or a fresh `Log.SubscribeOutputSink(...)` from Plan 03-01).
    - Test 3 (PluginManagerLifecycleTests.PluginInitThrows_OtherPluginsStillInit): create a third fixture variant inline (planner discretion — either a new tiny fixture or extend CrtMatchPlugin with a conditional throw controlled by a global) where init() throws std::runtime_error; assert all sibling plugins' init() still called AND log sink received `error`-level entry matching `/Plugin init\(\) threw/`. If planner prefers not to add a third fixture, use a runtime export `crtmatch_setInitShouldThrow(bool)` on CrtMatchPlugin to control its init behavior — simpler and isolated.
    - Test 4 (PluginManagerLifecycleTests.DestroyPlugin_InvokedOnShutdown_WhenPresent): load CrtMatchPlugin; call utinni_test_pluginManagerDispose; assert no crash + crtmatch_getDestroyCount() == 1 (add the destroy-counter export to the fixture per the same pattern as crtmatch_getInitCount).
    - Test 5 (PluginManagerLifecycleTests.LegacyPlugin_FallbackToVirtualDestructor_NoCrash): load LegacyPlugin (no destroyPlugin export); dispose PluginManager; assert no crash. Note: this exercises the `delete loaded.plugin` fallback in ~PluginManager — the test can't directly assert "virtual dtor invoked" without instrumentation, but the absence of crash on a /MT-built plugin freed via host's /MD CRT IS the regression case that D-13 fixes structurally (the legacy fallback is best-effort per D-15 CON-O-07 disposition; the structural fix is destroyPlugin existing for new plugins).
  </behavior>
  <action>
    Step 1 — Extend `UtinniCore/plugin_framework/plugin_manager.cpp` Impl struct (currently lines 33-37) to the LoadedPlugin shape per `<interfaces>`. The `Impl` struct stays anonymous inside the .cpp (CON-N-08 invariant). Add `#include <windows.h>` for HMODULE if not already present.

    Step 2 — Rewrite `loadPlugins` body (currently lines 129-150) per `<interfaces>` "Plugin loader pass shapes" and PATTERNS.md lines 619-684. Two passes:
    - Pass 1: `std::filesystem::recursive_directory_iterator(currentPluginDir)` → for each `.dll`: `LoadLibrary` → on failure `log::error("Failed to load plugin DLL '%s': GetLastError=%lu", path.c_str(), GetLastError())` (per D-17 + PATTERNS.md exact wording) + `continue`; `GetProcAddress(createPlugin)` → null = FreeLibrary + continue; invoke createPlugin → null = FreeLibrary + continue; `GetProcAddress(destroyPlugin)` (may be null); `pImpl->plugins.push_back({hModule, plugin, destroyFn})`.
    - Pass 2: foreach loaded plugin → `try { plugin->init(); } catch (const std::exception& ex) { log::error("Plugin init() threw: %s (plugin name=%s)", ex.what(), plugin->getInformation().name); } catch (...) { log::error("Plugin init() threw unknown exception"); }`. Per D-14 + Phase 02 C-06 isolation precedent.

    Step 3 — Rewrite `~PluginManager` (currently lines 41-49) per `<interfaces>` "~PluginManager" — destroyPlugin in plugin's CRT (when destroyFn != nullptr) OR virtual destructor fallback (legacy plugins); then FreeLibrary in host's CRT. Comment cites D-13 + D-15 + CON-O-07.

    Step 4 — Add the four new R-B test-bridge exports to `UtinniCore/utinni_exports.cpp` per `<interfaces>` "Native test bridge exports":
    - `utinni_test_pluginManagerLoadFromDir(const char* dir)` — constructs (or reuses a singleton) `PluginManager`, points it at `dir`, calls `loadPlugins`. Returns count loaded.
    - `utinni_test_pluginManagerLoadedCount()` — returns the size of the current Impl::plugins vector.
    - `utinni_test_pluginManagerDispose()` — destructs the PluginManager (triggers destroyPlugin + FreeLibrary path). Caller must not use any prior pointer after this.
    - `utinni_test_lastLoadLibraryError()` — returns the captured GetLastError from the most-recent LoadLibrary failure (resets on next loadPlugins call). For Test 2 assertion convenience; planner-discretion alternative: have Test 2 grep the log sink output instead (simpler if log capture is already wired from Phase 02).

    Step 5 — Create `UtinniCoreDotNet.Tests/PluginManagerLifecycleTests.cs` with the five Facts above. Use PATTERNS.md §"xUnit fixture-driven test (multi-DLL, fixture-resolver helper)" pattern — `FindFixtureDll` from `PluginLoaderTests.cs:49-68` (walk-up from AppContext.BaseDirectory looking for the deployed DLL), `MakeTempDir` from lines 70-75, `TryDeleteDir` finally from lines 151-156. P/Invoke declarations via NativeBridge nested static class per `ExportResolutionTests.cs:48-56` precedent.

    Convention: existing C++ style (Allman braces, `namespace utinni { ... }` wrapping the plugin_manager.cpp body — preserve). MIT header preserved. CON-N-08 verified: open `plugin_manager.h` after the change and confirm it's byte-identical to before.

    Document the CON-O-07 disposition in the commit message body (mention Sytner's plugin = legacy, no compat target preserved, upstream dormant per project_fork_strategy memory). The `docs/ai/assessment.md` §"Open questions" update for CON-O-07 lands in Task 5.
  </action>
  <verify>
    <automated>powershell -Command "& 'C:\\Program Files\\Microsoft Visual Studio\\2022\\Community\\MSBuild\\Current\\Bin\\msbuild.exe' Utinni.sln /p:Configuration=Release /p:Platform=x86 /v:minimal"</automated>
    <automated>dotnet test UtinniCoreDotNet.Tests --filter "FullyQualifiedName~PluginManagerLifecycleTests"</automated>
    <automated>powershell -Command "$header_before = Get-FileHash 'UtinniCore/plugin_framework/plugin_manager.h' -Algorithm SHA256; git log -1 --format='%H' UtinniCore/plugin_framework/plugin_manager.h; if ((Select-String -Path 'UtinniCore/plugin_framework/plugin_manager.h' -Pattern 'LoadedPlugin').Count -eq 0) { exit 0 } else { Write-Error 'CON-N-08 violation: LoadedPlugin leaked into public header'; exit 1 }"</automated>
  </verify>
  <done>
    `loadPlugins` runs two passes with LoadLibrary error logging; `~PluginManager` invokes destroyPlugin (when present) + FreeLibrary in correct order; `Impl` struct gained the LoadedPlugin shape but stays inside the .cpp (CON-N-08 invariant verified). Four `utinni_test_*` exports added. Five new R-B Facts pass. `msbuild Utinni.sln /p:Configuration=Release /p:Platform=x86` exits 0. `dotnet test UtinniCoreDotNet.Tests` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Land R-C single-source-of-truth WndProc RVA — Client::getSwgWndProc() + extern "C" shim + PanelGame.cs field-cache; add R-C regression Facts</name>
  <files>
    UtinniCore/swg/client/client.h,
    UtinniCore/swg/client/client.cpp,
    UtinniCoreDotNet/UI/Controls/PanelGame.cs,
    UtinniCoreDotNet/Utility/Native.cs,
    UtinniCoreDotNet.Tests/GetSwgWndProcTests.cs
  </files>
  <read_first>
    @UtinniCore/swg/client/client.h (lines 78-79 — existing `static HWND getSwgHwnd()` declaration; exact analog for the new `static IntPtr getSwgWndProc()` declaration)
    @UtinniCore/swg/client/client.cpp (line 43 — existing `0x00AA0970` constant declaration; lines 122-125 — existing `Client::getSwgHwnd()` definition; lines 290-293 — existing `extern "C" __declspec(dllexport) HWND __cdecl getSwgHwndExport()` C-linkage shim for the CppSharp-drops-pointer-getter workaround)
    @UtinniCoreDotNet/UI/Controls/PanelGame.cs (line 41 — current `IntPtr swgWndProc = new IntPtr(0x00AA0970)` literal; lines 71-73 — existing field-cache shape; lines 168, 207-208 — existing `Native.GetSwgHwnd()` consumer pattern that R-C mirrors)
    @UtinniCoreDotNet/Utility/Native.cs (lines 121-123 — existing `[DllImport(EntryPoint = "getSwgHwndExport")]` declaration; exact analog for the new `getSwgWndProcExport` declaration)
    @UtinniCoreDotNet.Tests/UtinniCfgTests.cs (lines 64-74 — Assert.Matches/DoesNotMatch precedent for grep-style negative test on file content)
    @UtinniCoreDotNet.Tests/FindPatternHarnessTests.cs (lines 42-67 NativeBridge nested static + GCHandle pin pattern; useful for the GetSwgWndProc P/Invoke test)
    @.planning/phases/03-strategic-reworks-r-a-r-h/03-PATTERNS.md §"UtinniCore/swg/client/client.h + client.cpp (R-C single-source WndProc RVA)" (lines 703-775)
    @.planning/phases/03-strategic-reworks-r-a-r-h/03-CONTEXT.md decisions D-18 (scope = just the actual duplicate, WndProc), D-19 (UTINNI_API IntPtr getSwgWndProc; CppSharp may drop pointer-return getter so include extern "C" shim per getSwgHwndExport precedent), D-20 (resolve once in ctor, cache as field)
    @.planning/intel/constraints.md CON-N-02 (thin-wrapper firewall — getSwgWndProc lives in `namespace utinni` inside Client::; extern "C" shim outside the namespace, mirroring getSwgHwndExport at client.cpp:290-293)
    @.planning/intel/constraints.md §CON-N-04 (memory VirtualProtect bracket — per checker W2: R-C is a read-only constant-returning getter; does NOT touch protected memory, does NOT call VirtualProtect. Citation only — no behavior change required.)
    @.planning/phases/03-strategic-reworks-r-a-r-h/03-CONTEXT.md decision D-06 (Tier-4 manual carve-out for R-C live-SWG side — per checker W3: the actually-calls-into-SWG WndProc forwarding path is verified manually, NOT in xUnit; the getter unit-test from D-05 covers everything testable without live SWG.)
  </read_first>
  <behavior>
    - Test 1 (GetSwgWndProcTests.GetSwgWndProc_ReturnsLiteralConstant): P/Invoke utinni-bridge `Native.GetSwgWndProc()` returns `new IntPtr(0x00AA0970)` exactly.
    - Test 2 (GetSwgWndProcTests.PanelGameSource_NoLongerContainsLiteralRVA): `grep -F "0x00AA0970" UtinniCoreDotNet/UI/Controls/PanelGame.cs` returns no match (grep-style negative test per PATTERNS.md §"grep-style negative test" lines 1060-1083). Uses `FindPanelGameSrc()` walk-up helper analog of `FindCfg()`.
    - Test 3 (GetSwgWndProcTests.PanelGameCtor_CachesAddress_WndProcReadsFromCache): instantiate PanelGame in a unit-test context (or read the source and assert via reflection that `swgWndProcAddr` is a `readonly IntPtr` field set in the ctor; planner picks the assertion shape). Lower-priority test; included as a paranoia check on the D-20 caching pattern.
  </behavior>
  <action>
    Step 1 — Add `static IntPtr getSwgWndProc()` declaration to `UtinniCore/swg/client/client.h` alongside the existing `static HWND getSwgHwnd()` at lines 78-79 (per `<interfaces>` "R-C getter"). Place in the same `class Client` `public:` block; comment cites D-18..D-20 and the analog precedent.

    Step 2 — Add `Client::getSwgWndProc()` definition to `UtinniCore/swg/client/client.cpp` alongside the existing `Client::getSwgHwnd()` at lines 122-125. Body: `return (IntPtr)swg::client::wndProc;` where `swg::client::wndProc` is the existing constant at `client.cpp:43`.

    Step 3 — Add the `extern "C"` C-linkage shim alongside the existing `getSwgHwndExport` at `client.cpp:290-293` (per `<interfaces>` "extern "C" shim" and PATTERNS.md lines 727-736). Body: `return (IntPtr)swg::client::wndProc;`. Comment cites D-18..D-20 and the CppSharp-drops-pointer-getter workaround that the getSwgHwndExport precedent established.

    Step 4 — Add the P/Invoke declaration to `UtinniCoreDotNet/Utility/Native.cs` per `<interfaces>` "P/Invoke declaration" — `[DllImport("UtinniCore", CallingConvention = CallingConvention.Cdecl, EntryPoint = "getSwgWndProcExport")] public static extern IntPtr GetSwgWndProc();`. Place immediately adjacent to the existing `GetSwgHwnd` declaration at lines 121-123.

    Step 5 — Rewrite `UtinniCoreDotNet/UI/Controls/PanelGame.cs:41` per `<interfaces>` "R-C consumer":
    - Replace the inline literal `new IntPtr(0x00AA0970)` with a `private readonly IntPtr swgWndProcAddr` field cached at ctor time (`swgWndProcAddr = Native.GetSwgWndProc();`).
    - Rewrite `WndProc(ref Message m)` to read `swgWndProcAddr` from the cache on the hot path (zero per-message overhead per D-20).
    - The literal `0x00AA0970` MUST NOT appear anywhere in this file after the change.

    Step 6 — Create `UtinniCoreDotNet.Tests/GetSwgWndProcTests.cs` with the three Facts above per PATTERNS.md §"xUnit P/Invoke harness" (lines 1038-1058) for Test 1 and §"grep-style negative test" (lines 1060-1083) for Test 2. The `FindPanelGameSrc()` helper follows the `FindCfg` precedent at `UtinniCfgTests.cs` (4-level walk-up from `AppContext.BaseDirectory` to the repo root then `Path.Combine(repoRoot, "UtinniCoreDotNet", "UI", "Controls", "PanelGame.cs")`).

    Convention: MIT header preserved; existing C++/C# style. CON-N-02 verified: `getSwgWndProc` defined inside `namespace utinni { ... }` block; `extern "C"` shim outside the namespace, exactly matching the getSwgHwndExport precedent. CON-N-04 verified: the getter is read-only constant-returning; touches no protected memory (per checker W2).

    Tier-4 manual attestation (per D-06 / checker W3): the commit body MUST carry the line `Verified-by: live SWG window forwards WM_KEYDOWN end-to-end` to attest the Tier-4 carve-out (the live-SWG WndProc forwarding path is operator-verified outside CI — the xUnit Facts above cover everything testable without live SWG). Add this line to the commit-message body alongside the implementing SHA reference.
  </action>
  <verify>
    <automated>powershell -Command "& 'C:\\Program Files\\Microsoft Visual Studio\\2022\\Community\\MSBuild\\Current\\Bin\\msbuild.exe' Utinni.sln /p:Configuration=Release /p:Platform=x86 /v:minimal"</automated>
    <automated>dotnet test UtinniCoreDotNet.Tests --filter "FullyQualifiedName~GetSwgWndProcTests"</automated>
    <automated>powershell -Command "if ((Select-String -Path 'UtinniCoreDotNet/UI/Controls/PanelGame.cs' -Pattern '0x00AA0970' -SimpleMatch).Count -eq 0) { exit 0 } else { Write-Error 'R-C: 0x00AA0970 literal still present in PanelGame.cs'; exit 1 }"</automated>
    <automated>powershell -Command "& 'dumpbin.exe' /exports bin/Release/UtinniCore.dll | Select-String -Pattern 'getSwgWndProcExport'"</automated>
  </verify>
  <done>
    `Client::getSwgWndProc()` declared in client.h + defined in client.cpp (in `namespace utinni`); `getSwgWndProcExport` C-linkage shim exported from UtinniCore.dll; Native.cs declares the P/Invoke; PanelGame.cs:41 literal replaced with `swgWndProcAddr` cached field. Three GetSwgWndProcTests Facts pass. `dumpbin /exports` confirms `getSwgWndProcExport` is exported.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking" repo="kennethlong/UtinniPlugins">
  <name>Task 4: Cross-repo paired commit — TJT exports destroyPlugin in kennethlong/UtinniPlugins (D-26)</name>
  <files>External repo: kennethlong/UtinniPlugins — TJT plugin entry point (likely TheJawaToolbox/main.cpp or TheJawaToolboxImpl.cpp per CON-T-05 *Impl separation pattern). NOT a file in this repo.</files>
  <read_first>
    @.planning/phases/03-strategic-reworks-r-a-r-h/03-CONTEXT.md decision D-26 (R-B UtinniPlugins-side migration: TJT exports destroyPlugin symmetrically with createPlugin; manual TJT build verifies; Phase 02 D-09 precedent — no UtinniPlugins CI yet)
    @.planning/phases/03-strategic-reworks-r-a-r-h/03-PATTERNS.md §"CON-T-05 — Jawa Toolbox `*Impl` separation (cross-repo R-B touch)" (the *Impl separation invariant the destroyPlugin export must respect — delete the public-API class through its virtual destructor, do NOT collapse the Impl separation)
    @UtinniCore/plugin_framework/utinni_plugin.h (per this plan's Task 1: macro now declares BOTH createPlugin and destroyPlugin; TJT must export both)
    @Utinni.CrtMatchPlugin/main.cpp (per this plan's Task 1: reference shape for destroyPlugin export body — `delete p` through virtual destructor)
  </read_first>
  <action>
    HUMAN-ACTION required: no CLI/API automation available for this cross-repo work. TJT lives in `kennethlong/UtinniPlugins` (separate Git repo, no shared CI yet per Phase 02 D-09 manual-verify posture). The user (Kenneth) executes these steps:

    1. `cd` into the local clone of `kennethlong/UtinniPlugins` (separate working directory).
    2. Locate TJT plugin entry point following CON-T-05 *Impl separation (`TheJawaToolbox.h` declares public-API class; `TheJawaToolboxImpl.h`/`.cpp` carries implementation). The export site is wherever `extern "C" __declspec(dllexport) utinni::UtinniPlugin* createPlugin()` lives today.
    3. Add the symmetric destroyPlugin export immediately adjacent to the existing createPlugin export. Match the pattern from this plan's Task 1 `Utinni.CrtMatchPlugin/main.cpp`:
       `extern "C" __declspec(dllexport) void destroyPlugin(utinni::UtinniPlugin* p) { delete p; }`
       The `delete p` invokes `utinni::UtinniPlugin`'s virtual destructor, which dispatches to `~TheJawaToolboxImpl` per CON-T-05 *Impl separation. PRECONDITION: `~UtinniPlugin` must be virtual in `UtinniCore/plugin_framework/utinni_plugin.h` — if it is not, surface this as a latent bug back to the planner BEFORE proceeding; the planner adds a sub-task to make it virtual.
    4. Build TJT manually in Visual Studio against the updated Utinni framework (built from this plan's Tasks 1-3). Confirm Release|Win32 builds green.
    5. Smoke-test: launch SWG with Utinni injection + TJT loaded; verify TJT panels open as before (no behavioral regression from the destroyPlugin addition; lifecycle shutdown path is exercised only on PluginManager destruction at process exit).
    6. Commit to UtinniPlugins. Commit message references the Utinni-side SHA of Plan 03-02 Task 1 (the UTINNI_PLUGIN macro change). Push to origin.
    7. Resume signal: type "approved" in chat once (a) UtinniPlugins commit lands, (b) TJT Release|Win32 builds green, (c) live SWG smoke is clean.

    If the build or smoke fails: type "blocked: <description>" in chat. Planner branches into a sub-task to fix forward — most-likely path is to make `~UtinniPlugin` virtual in UtinniCore (single-line edit + a re-build of TJT). Less-likely path is adjusting the TJT destructor pattern itself.

    Rationale this is checkpoint:human-action (rare per planner authority limits): cross-repo work has no CLI/API automation surface. TJT lives in a separate Git repo with no shared CI. The Visual Studio build of a vcxproj plugin DLL plus the live-SWG-injection smoke are operator-local operations per Phase 02 D-09 precedent. Claude has no way to drive any of these steps autonomously from the Utinni working tree.
  </action>
  <verify>
    <human-check>User confirms in chat that the kennethlong/UtinniPlugins commit lands + TJT builds green in Release|Win32 + live SWG smoke test loads TJT panels successfully.</human-check>
    <automated>powershell -Command "& 'C:\\Program Files\\Microsoft Visual Studio\\2022\\Community\\MSBuild\\Current\\Bin\\msbuild.exe' Utinni.sln /t:Utinni_CrtMatchPlugin /p:Configuration=Release /p:Platform=x86 /v:minimal"</automated>
  </verify>
  <done>
    User types "approved" in chat after: (a) UtinniPlugins commit pushed to origin with reference to this plan's Task 1 SHA; (b) TJT Release|Win32 manual build green; (c) live SWG smoke test confirms TJT panels open as before. The Utinni-side commit SHA for the macro change (Task 1) is recorded in the UtinniPlugins commit message. Acceptance is operator-confirmed per Phase 02 D-09 manual-verify posture; no automated assertion is possible from this side of the repo boundary.
  </done>
</task>

<task type="auto">
  <name>Task 5: Update docs/ai/assessment.md — mark R-B + R-C done with SHAs; record CON-O-07 disposition (Sytner = legacy, no compat target)</name>
  <files>docs/ai/assessment.md</files>
  <read_first>
    @docs/ai/assessment.md §"Status tracking" (the R-X table — Plan 03-01 set R-A + R-H to done; this task sets R-B + R-C to done)
    @docs/ai/assessment.md §"Open questions" (CON-O-07 row — disposition pending; this task closes it with the SHA of Task 2)
    @.planning/phases/03-strategic-reworks-r-a-r-h/03-CONTEXT.md decisions D-15 + D-25 (CON-O-07 = Sytner's plugin treated as legacy, no compat target through R-B; upstream dormant per project_fork_strategy memory)
    @.planning/phases/03-strategic-reworks-r-a-r-h/03-CONTEXT.md §"Specifics" (CON-O-05 + CON-O-07 disposition wording — Phase 02 D-12 style: one-line comment + link to implementing commit SHA)
  </read_first>
  <action>
    Read `docs/ai/assessment.md` §"Status tracking". Locate the R-B and R-C rows. For each, set status to `done` and append the implementing commit SHA from this plan's tasks. Use `git log --oneline -- UtinniCore/plugin_framework/plugin_manager.cpp UtinniCore/swg/client/client.cpp` to identify the SHAs of Tasks 2-3.

    Read `docs/ai/assessment.md` §"Open questions". Locate the CON-O-07 row. Append the disposition: "Sytner's plugin treated as legacy / no ABI-compat target preserved through R-B. Upstream `ptklatt/UtinniPlugins` dormant per project_fork_strategy memory; our `kennethlong/UtinniPlugins` does not actively ship Sytner. Disposition resolved alongside R-B in commit `<sha-of-task-2>`." Use Phase 02 KB-05 / D-12-style row formatting (find an already-closed CON-O-NN row earlier in the table to mirror).

    The CON-O-05 disposition row (StdEdited.cs curation criteria) is OWNED BY Plan 03-03 R-F — do NOT close it in this commit.
  </action>
  <verify>
    <automated>powershell -Command "$c = Get-Content 'docs/ai/assessment.md' -Raw; $rb = $c -match 'R-B.*done'; $rc = $c -match 'R-C.*done'; $sytner = $c -match 'CON-O-07.*legacy|Sytner.*legacy'; if ($rb -and $rc -and $sytner) { exit 0 } else { Write-Error \"R-B=$rb R-C=$rc CON-O-07=$sytner\"; exit 1 }"</automated>
  </verify>
  <done>
    `docs/ai/assessment.md` §Status tracking shows R-B + R-C = done with implementing commit SHAs. §Open questions CON-O-07 row carries the "Sytner = legacy, no compat target" disposition.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host (UtinniCore.dll) → plugin DLL (LoadLibrary) | Plugin DLL is third-party code loaded into the editor process. Plugin's createPlugin and destroyPlugin functions run inside the plugin's own CRT. |
| plugin's CRT vs host's CRT | Phase 3 R-B fixes CON-B-04 (cross-CRT delete) structurally by requiring symmetric createPlugin/destroyPlugin so plugin owns both alloc and free in its own CRT. |
| log sink → operator (Tier 4) | LoadLibrary failure path logs the configured Plugins/ directory (operator-controlled, not user-input) + GetLastError code. |
| native↔managed boundary (R-C P/Invoke) | `Native.GetSwgWndProc()` returns a raw IntPtr representing a process-internal code address (0x00AA0970). Not a network or user-input boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | Plugin DLL on disk | accept | Plugin DLL is in operator-controlled `[Plugins]` directory — defense-in-depth not required at V1. Existing fork-strategy mitigation (we only ship our own plugins) covers V1. |
| T-03-02-02 | Tampering | createPlugin returns malicious UtinniPlugin* | mitigate | Per-plugin try/catch around `init()` (D-14 + Phase 02 C-06 isolation precedent) prevents one bad plugin from killing the rest of the load sequence. Tests cover this (Task 2 Test 3). |
| T-03-02-03 | Denial of service | LoadLibrary failure path before R-B | mitigate (closed) | R-B itself: D-17 surfaces failures to the log sink instead of silently swallowing. Test 2 in Task 2 covers. |
| T-03-02-04 | Repudiation | "init() was never called" silent failure | mitigate (closed) | R-B itself: D-14 two-phase explicitly invokes init() with try/catch around it. Test 1 in Task 2 covers. |
| T-03-02-05 | Information disclosure | log::error of plugin DLL path + GetLastError | accept | Plugin path is operator-configured (not network input); GetLastError is a small int (no PII). Logged to the standard sink which is already operator-readable. |
| T-03-02-06 | Elevation of privilege | Plugin can crash host by exporting destroyPlugin that returns to a stack-corrupting pattern | accept | Trust model assumption: plugins are first-party. V2 plugin sandboxing is out of scope (deferred per ROADMAP V2 boundary). |
| T-03-02-07 | Tampering | Plugin uses /MT CRT and host uses /MD; cross-CRT delete | mitigate (closed) | R-B itself: D-13 destroyPlugin symmetric ABI means plugin frees its own allocations in its own CRT. Test 4 in Task 2 covers the present-destroyPlugin path; Test 5 covers the legacy fallback (where the crash CAN still occur — accepted for legacy plugins per CON-O-07 disposition). |
| T-03-02-08 | Tampering | R-C P/Invoke returns wrong address | accept | The exported constant 0x00AA0970 is hardcoded in client.cpp:43 — same source-of-truth as before, just surfaced via getter. Single source of truth structurally prevents drift. Test 1 in Task 3 covers. |
| T-03-02-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan. xUnit 2.9.x already pinned from Phase 1. |

Block-on-high: none. The Sytner legacy-plugin path (T-03-02-07 fallback case) is documented as accepted-risk per CON-O-07 disposition + D-15.
</threat_model>

<verification>
- `msbuild Utinni.sln /p:Configuration=Release /p:Platform=x86` builds all targets including the two new fixture plugins (`Utinni.CrtMatchPlugin`, `Utinni.LegacyPlugin`); exit 0.
- `dotnet test UtinniCoreDotNet.Tests` runs all new R-B Facts (PluginManagerLifecycleTests) + R-C Facts (GetSwgWndProcTests) + all prior Plan 03-01 + Phase 1/2/2.1 tests; exit 0.
- `dumpbin /exports bin/Release/Utinni.CrtMatchPlugin.dll` shows BOTH `createPlugin` and `destroyPlugin`; `dumpbin /exports bin/Release/Utinni.LegacyPlugin.dll` shows ONLY `createPlugin`.
- `dumpbin /exports bin/Release/UtinniCore.dll` shows `getSwgWndProcExport` (R-C extern "C" shim).
- `grep -F "0x00AA0970" UtinniCoreDotNet/UI/Controls/PanelGame.cs` returns no match.
- `UtinniCore/plugin_framework/plugin_manager.h` is byte-identical pre/post Task 2 (CON-N-08 verification).
- Cross-repo: `kennethlong/UtinniPlugins` master shows a new commit exporting `destroyPlugin` from TJT entry; manual TJT build verifies green.
- CI Windows-2022 workflow stays green on master after each commit.
</verification>

<success_criteria>
- UTINNI_PLUGIN macro declares both createPlugin + destroyPlugin.
- PluginManager runs two-phase init with per-plugin try/catch around init(); LoadLibrary failures logged with GetLastError; HMODULE tracked; ~PluginManager invokes destroyPlugin (when present) + FreeLibrary in correct order; legacy plugins fall back to virtual destructor.
- CON-N-08 invariant preserved (`plugin_manager.h` public surface unchanged).
- Two new fixture plugins (`Utinni.CrtMatchPlugin` /MD, `Utinni.LegacyPlugin` /MT) build green; CopyNativeArtifactsForTests deploys both.
- `Client::getSwgWndProc()` + `getSwgWndProcExport` C-linkage shim exported from UtinniCore.dll; `PanelGame.cs` no longer contains the literal `0x00AA0970`; P/Invoke caches the address at ctor.
- All new xUnit Facts (R-B lifecycle + R-C getter + R-C grep negative) pass; full test suite green.
- Cross-repo paired commit lands in `kennethlong/UtinniPlugins` for TJT destroyPlugin; manual build verifies (Phase 02 D-09 precedent).
- `docs/ai/assessment.md` §Status tracking shows R-B + R-C = done with SHAs; §Open questions CON-O-07 carries the disposition.
- CI stays green on master after every commit; Plan 03-03 may start.
</success_criteria>

<output>
Create `.planning/phases/03-strategic-reworks-r-a-r-h/03-02-SUMMARY.md` when done, following `@$HOME/.claude/get-shit-done/templates/summary.md`. Record:
- Final commit SHAs for each task (1-5; Task 4 is the cross-repo UtinniPlugins SHA)
- Confirmation of CON-N-08 byte-identity of plugin_manager.h
- Fixture-plugin build artifacts (DLL paths, sizes)
- TJT manual-build smoke result (pass/fail + any notes from Kenneth)
- Carry-overs: none expected; Plan 03-03 starts after this lands green
</output>
