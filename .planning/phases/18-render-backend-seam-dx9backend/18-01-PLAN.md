---
phase: 18-render-backend-seam-dx9backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - UtinniCore/swg/graphics/render_backend.h
  - UtinniCore/swg/graphics/render_backend.cpp
  - UtinniCore.Tests/Graphics/RenderBackendSeamTests.cpp
  - UtinniCore/UtinniCore.vcxproj
  - UtinniCore.Tests/UtinniCore.Tests.vcxproj
autonomous: true
requirements: [RNDR-01]
nyquist_compliant: true

must_haves:
  truths:
    - "A render_backend.{h,cpp} seam exists in UtinniCore/swg/graphics/ exposing an IRenderBackend pure-virtual ABC (D-01: runtime-polymorphic vtable, single global instance selected at setup(); Phase 19 adds Dx11Backend behind the same vtable without re-touching imgui_impl)."
    - "The IRenderBackend vtable is 10 pure virtuals (D-02 + A2 amendment): the 6 ROADMAP-named members (newFrame, renderDrawData, onPreResize, onPostResize, renderTargetWidth, renderTargetHeight) + the A2 sceneDepthTexture/sceneColorTexture accessors + the A2 sceneDepthStage get/set pair — color accessor + stage slider preserved API-neutrally so DrawColorWindow and the !enableUi stage slider survive the Plan-02 purge."
    - "A NON-VIRTUAL Dx9Backend::init(IDirect3DDevice9* device) is declared in render_backend.h and implemented ONLY in render_backend.cpp — it does the device-bearing GetCreationParameters + ImGui_ImplDX9_Init. It is deliberately OFF the vtable (Phase 19's Dx11 init differs); imgui_impl::setup() calls it (review amendment 1 / 6 / 9)."
    - "Dx9Backend wraps the existing directX:: free functions verbatim (wrap, don't rewrite) — no detour/device/vtable-harvest internals are re-implemented."
    - "onPreResize/onPostResize are honest {} no-ops in Dx9Backend (D-03) — no Reset is introduced and the third-party device is never destabilized."
    - "The backend singleton is static storage (no heap), and dispatch is a plain virtual call (heap-free per-frame hot path)."
    - "A render_backend::set()/get() accessor pair exists so a mock backend can be installed by the D-07 test without a live device; get() is documented as nullable (Plan 02 guards every call site)."
    - "renderTargetWidth/renderTargetHeight are forward-compat seam hooks for Phase 19 (D-04 clarified): they forward to utinni::Graphics::getCurrentRenderTargetWidth/Height(). They are UNUSED in Phase 18 — imgui_impl continues to read Graphics:: directly in its RT-space block; the seam exposes the dims, it does not relocate the math (review amendment 7 / resolves SC#3 literal-wording tension)."
    - "The D-07 Catch2 mock-dispatch test proves all 10 pure-virtual members route through the installed backend vtable, runnable in CI with no live D3D9 device, and asserts render_backend::get() == nullptr after the post-test restore (Codex leaked-global-state check, review amendment 2)."
  artifacts:
    - path: "UtinniCore/swg/graphics/render_backend.h"
      provides: "IRenderBackend pure-virtual ABC (10 members) + non-virtual Dx9Backend::init(IDirect3DDevice9*) decl + render_backend::get()/set()/dx9Singleton() accessor trio"
      contains: "class IRenderBackend"
    - path: "UtinniCore/swg/graphics/render_backend.cpp"
      provides: "Dx9Backend concrete impl forwarding to directX:: + init() doing GetCreationParameters+ImGui_ImplDX9_Init + static-storage singleton + get/set/dx9Singleton"
      contains: "class Dx9Backend"
    - path: "UtinniCore.Tests/Graphics/RenderBackendSeamTests.cpp"
      provides: "D-07 mock IRenderBackend dispatch test (all 10 members + post-restore get()==nullptr assert), tag [rndr01][graphics]"
      contains: "MockBackend"
  key_links:
    - from: "UtinniCore.Tests/Graphics/RenderBackendSeamTests.cpp"
      to: "render_backend::set/get"
      via: "install mock then drive each member through get(), then restore + assert get()==nullptr"
      pattern: "render_backend::(set|get)"
    - from: "UtinniCore/swg/graphics/render_backend.cpp"
      to: "directX::getDepthTexture / directX::getDevice / utinni::Graphics::getCurrentRenderTargetWidth/Height / ImGui_ImplDX9_*"
      via: "Dx9Backend members + init() forward verbatim"
      pattern: "directX::|ImGui_ImplDX9_"
---

<objective>
Create the `IRenderBackend` seam (the single runtime-polymorphic vtable the overlay will render
through) and the `Dx9Backend` that wraps the existing live-verified `directX::` D3D9 code verbatim.
This is the contract Plan 02 carves `imgui_impl` onto — define the interface FIRST (including the
non-virtual `Dx9Backend::init()` that Plan 02 calls from `setup()`) so the executor of Plan 02 receives
the seam shape rather than discovering it. Also lands the D-07 mock-dispatch Catch2 test (the
CI-enforceable proof that all 10 seam members route through the installed backend without a live device)
and wires both new files into the build.

Purpose: settle the seam shape (D-01 vtable, D-02 + A2 ten members, the off-vtable init contract) before
any consumer touches it, so Phase 19 can add `Dx11Backend` behind the same vtable without re-opening the
seam, and Plan 02 does not have to invent the init API mid-execution.
Output: `render_backend.{h,cpp}`, `RenderBackendSeamTests.cpp`, two `<ClCompile>`/`<ClInclude>` vcxproj
edits. The live D3D9 behavior is UNCHANGED by this plan — it only adds a new, not-yet-consumed seam.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-render-backend-seam-dx9backend/18-CONTEXT.md
@.planning/phases/18-render-backend-seam-dx9backend/18-RESEARCH.md
@.planning/phases/18-render-backend-seam-dx9backend/18-PATTERNS.md
@.planning/phases/18-render-backend-seam-dx9backend/18-REVIEWS.md

<interfaces>
<!-- Existing directX:: free functions the Dx9Backend wraps VERBATIM. Verified from directx9.h this session. -->
<!-- DO NOT change these exported signatures (managed/plugin consumers depend on them — CPPS-04 ABI gate). -->

From UtinniCore/swg/graphics/directx9.h:
```
namespace directX {
  void initPresentBlockedEvent();   // CR-04 — called from utinni_init BEFORE createDetours(); do NOT move
  void initDepthTexture();          // WR-03 — called from utinni_init BEFORE createDetours(); do NOT move
  void detour();
  void cleanup();
  DepthTexture* getDepthTexture();
  UTINNI_API void toggleWireframe();
  UTINNI_API void blockPresent(bool value);
  UTINNI_API bool isPresentBlocked();
  UTINNI_API IDirect3DDevice9* getDevice();
}
```

From UtinniCore/swg/graphics/directx9.cpp (the getter bodies the backend forwards to — lines 637-665):
```
DepthTexture* getDepthTexture() { return depthTexture; }                 // :637
IDirect3DDevice9* getDevice()   { return pDirectXDevice; }               // :662
```

From depth_texture.h (the DepthTexture members the seam surfaces API-neutrally):
```
// getTextureDepth(), getTextureColor(), getStage(), setStage(int)
```

ImTextureID is imgui's opaque void*-sized texture handle (the LCD type Dx11Backend will also satisfy
in Phase 19 with an ID3D11ShaderResourceView*). It is the cast already used at imgui_impl.cpp:364/390.

<!-- The non-virtual init contract that Plan 02's setup() requires (review amendments 1/6/9). -->
<!-- init() takes the live pDevice from hkPresent. The Dx9Backend OWNS GetCreationParameters +
     ImGui_ImplDX9_Init. directX::getDevice() is an ASSERT/FALLBACK only, never the primary source. -->
From the seam to be defined in render_backend.h:
```
// Non-virtual — NOT on the IRenderBackend vtable (Phase 19 Dx11 init signature differs):
class Dx9Backend {
  HWND init(IDirect3DDevice9* device);   // GetCreationParameters -> ImGui_ImplDX9_Init; returns hFocusWindow
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define IRenderBackend seam (10 members) + non-virtual Dx9Backend::init + Dx9Backend wrapper</name>
  <files>UtinniCore/swg/graphics/render_backend.h, UtinniCore/swg/graphics/render_backend.cpp</files>
  <read_first>
    - UtinniCore/swg/graphics/directx9.h (the directX:: façade the backend wraps; the MIT header + #pragma once pattern to copy verbatim from lines 1-25; the UTINNI_API export convention to deliberately NOT use)
    - UtinniCore/swg/graphics/directx9.cpp lines 249-251 (hkBeginScene captures pDirectXDevice), 359-382 (hkPresent + hkReset), 637-665 (the getter/state free-function bodies the backend forwards to)
    - UtinniCore/swg/graphics/depth_texture.h (DepthTexture::getTextureDepth/getTextureColor/getStage/setStage signatures)
    - UtinniCore/swg/ui/imgui_impl.cpp lines 257-281 (the current setup() body — the GetCreationParameters + ImGui_ImplDX9_Init that init() must absorb; the hwnd flow into ImGui_ImplWin32_Init and the WndProc subclass that STAY in imgui_impl)
    - .planning/phases/18-render-backend-seam-dx9backend/18-PATTERNS.md (the render_backend.h / render_backend.cpp copy-ready excerpts — use these as the body; do NOT re-derive)
    - .planning/phases/18-render-backend-seam-dx9backend/18-RESEARCH.md Pattern 3 (setup device-acquisition) + Pitfall 1 (heap-free virtual dispatch) + Pitfall 2 (do NOT move initPresentBlockedEvent/initDepthTexture into the ctor)
    - .planning/phases/18-render-backend-seam-dx9backend/18-REVIEWS.md Consensus Summary amendments 1, 2, 5, 6, 7
  </read_first>
  <behavior>
    - render_backend::set(mock) then render_backend::get() returns that same pointer; render_backend::set(nullptr) then get() returns nullptr (get() is nullable by contract — Plan 02 guards every call site).
    - Each of the 10 IRenderBackend members is a pure virtual (= 0) on the ABC and overridden on Dx9Backend; the 10 are: newFrame, renderDrawData, onPreResize, onPostResize, renderTargetWidth, renderTargetHeight, sceneDepthTexture, sceneColorTexture, sceneDepthStage, setSceneDepthStage.
    - Dx9Backend::init(device) is a NON-VIRTUAL member (NOT on the vtable): it calls device->GetCreationParameters(&cParam), then ImGui_ImplDX9_Init(device), and returns cParam.hFocusWindow. If device is null it early-returns nullptr (Plan 02 setup() then bails — no overlay install). directX::getDevice() is used only as an assert/fallback if the passed device is null, never the primary source.
    - Dx9Backend::onPreResize() and onPostResize() are empty {} no-ops (D-03).
    - Dx9Backend::sceneDepthTexture() returns (ImTextureID)0 when directX::getDepthTexture() is null or its color texture is null (folds the imgui_impl.cpp:350 null guard into the accessor); otherwise returns (ImTextureID)getTextureDepth().
    - Dx9Backend::sceneColorTexture() mirrors that guard and returns (ImTextureID)getTextureColor() (the imgui_impl.cpp:390 cast).
    - Dx9Backend::renderTargetWidth/Height() forward to utinni::Graphics::getCurrentRenderTargetWidth/Height() — forward-compat hooks, unused by imgui_impl in Phase 18 (D-04 clarified).
    - render_backend::dx9Singleton() returns the address of a static-storage Dx9Backend (never heap-allocated).
  </behavior>
  <action>
    Create render_backend.h: copy the 23-line MIT header from directx9.h:1-25, then `#pragma once` and
    `#include <imgui.h>` (for ImTextureID + ImDrawData) and `#include <Windows.h>` (for HWND on the init
    decl) — do NOT include `<d3d9.h>`, `<imgui_impl_dx9.h>`, `utinni.h`, or `depth_texture.h` (this header
    is what imgui_impl.cpp will include instead of the DX9 bindings; D-05). Forward-declare
    `struct IDirect3DDevice9;` so the non-virtual init() signature compiles WITHOUT pulling `<d3d9.h>`
    into the header (review amendment: header stays DX9-binding-free; the gate at acceptance enforces it).
    Declare `namespace render_backend` with `class IRenderBackend` exposing these 10 pure-virtual members
    (signatures exact, per the D-02 + A2 amendment): `virtual void newFrame() = 0`,
    `virtual void renderDrawData(ImDrawData* drawData) = 0`, `virtual void onPreResize() = 0`,
    `virtual void onPostResize() = 0`, `virtual int renderTargetWidth() = 0`,
    `virtual int renderTargetHeight() = 0`, `virtual ImTextureID sceneDepthTexture() = 0`,
    `virtual ImTextureID sceneColorTexture() = 0`, `virtual int sceneDepthStage() = 0`,
    `virtual void setSceneDepthStage(int stage) = 0`, and a `virtual ~IRenderBackend() = default`.
    Add a one-line comment on renderTargetWidth/Height that they are forward-compat hooks for Phase 19
    and UNUSED in Phase 18 (D-04 clarified — imgui_impl reads Graphics:: directly). Declare
    `class Dx9Backend final : public IRenderBackend` in the header with its 10 overrides AND the NON-VIRTUAL
    `HWND init(IDirect3DDevice9* device);` member (the init contract Plan 02 calls — comment it explicitly
    "non-virtual, OFF the vtable; Phase 19 Dx11 init differs"). Declare the free accessor trio
    `IRenderBackend* get();`, `void set(IRenderBackend* backend);`, `Dx9Backend* dx9Singleton();`. Document
    in a header comment that get() is NULLABLE (returns nullptr before setup() installs the backend — Plan 02
    guards every seam call site). Use the namespace-of-free-functions convention from directx9.h:30-42 but
    deliberately do NOT mark anything `UTINNI_API` (the seam is internal — links via project reference, no
    DLL export, no CppSharp binding surface). Header MUST compile under C++17 (no C++20/23 features — also
    satisfies the CPPS-03 C++23-header CI tripwire).
    Create render_backend.cpp: copy the MIT header + `#include "render_backend.h"`, then `#include <d3d9.h>`,
    `#include <imgui_impl_dx9.h>`, `#include "directx9.h"`, `#include "graphics.h"` — render_backend.cpp is
    the ONLY new file that may include the DX9 bindings. Implement the Dx9Backend overrides forwarding
    verbatim per the PATTERNS excerpt: newFrame -> ImGui_ImplDX9_NewFrame(); renderDrawData(d) ->
    ImGui_ImplDX9_RenderDrawData(d); onPreResize/onPostResize -> `{}` (D-03 honest no-op, NEVER add
    ->Reset(/.Reset(); renderTargetWidth/Height -> utinni::Graphics::getCurrentRenderTargetWidth/Height();
    sceneDepthTexture/sceneColorTexture/sceneDepthStage/setSceneDepthStage -> directX::getDepthTexture()
    with the null guards folded in. Implement the NON-VIRTUAL `HWND Dx9Backend::init(IDirect3DDevice9* device)`:
    if device is null, assert/fallback to directX::getDevice(); if STILL null, return nullptr (caller bails).
    Call device->GetCreationParameters(&cParam), then ImGui_ImplDX9_Init(device), then return
    cParam.hFocusWindow. This is the ONLY place ImGui_ImplDX9_Init appears in the codebase after Plan 02.
    Define a static-storage `static Dx9Backend s_dx9Backend;` and `static IRenderBackend* s_active = nullptr;`
    with `get()`/`set()`/`dx9Singleton()` bodies. Do NOT call initPresentBlockedEvent/initDepthTexture from
    the ctor (Pitfall 2 — they stay in utinni_init's pre-detour block); keep the singleton ctor trivial. Do
    NOT change any exported directX:: signature (CPPS-04 ABI gate). This plan does NOT wire setup()/render()
    to the seam — Plan 02 does. OUT OF SCOPE (do NOT add): any D3D11 detection, any resize-behavior logic in
    the no-op hooks, any hkReset/detour relocation.
  </action>
  <verify>
    <automated>grep -L 'IDirect3DDevice9 device\|#include <d3d9.h>\|#include <imgui_impl_dx9.h>\|ImGui_ImplDX9_' UtinniCore/swg/graphics/render_backend.h returns the file (header is DX9-binding-free apart from the forward-declared struct IDirect3DDevice9; on the init decl only the forward-declared pointer type appears); grep -c 'virtual' UtinniCore/swg/graphics/render_backend.h shows >= 11 (10 pure virtuals + dtor)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ImGui_ImplDX9_Init' UtinniCore/swg/graphics/render_backend.cpp` == 1 (init() owns the ONLY ImGui_ImplDX9_Init call — review amendment 1).
    - render_backend.h declares the non-virtual `init(` member on Dx9Backend taking an `IDirect3DDevice9*` (review amendment 1/6); render_backend.h does NOT `#include <d3d9.h>` or `#include <imgui_impl_dx9.h>` (only render_backend.cpp may — Codex/Cursor amendment).
    - `grep -c 'directX::getDepthTexture' UtinniCore/swg/graphics/render_backend.cpp` >= 1 (backend wraps, does not re-implement).
    - render_backend.cpp calls `GetCreationParameters` and references `directX::getDevice` as the assert/fallback (review amendment 6: pDevice from hkPresent is primary, getDevice() is fallback only).
    - `grep -E '\->Reset\(|\.Reset\(' UtinniCore/swg/graphics/render_backend.cpp` returns ZERO matches (no Reset introduced — no-Reset contract).
    - render_backend.h contains the literal tokens `sceneColorTexture`, `setSceneDepthStage`, and `sceneDepthStage` (A2 10-member amendment present, not dropped); a self-count of `= 0;` pure-virtual decls is exactly 10.
    - render_backend.h carries a comment marking renderTargetWidth/Height as forward-compat / unused-in-Phase-18 (D-04 clarified — review amendment 7).
    - render_backend.h declares ZERO `UTINNI_API` (internal seam, no new binding surface).
    - render_backend.cpp defines `static Dx9Backend s_dx9Backend;` (static storage, no heap) and contains no `std::function` / `make_unique` / `new Dx9Backend` (heap-free hot path, Pitfall 1).
    - render_backend.cpp does NOT contain `initPresentBlockedEvent` or `initDepthTexture` (ordering preserved in utinni_init — Pitfall 2).
  </acceptance_criteria>
  <done>render_backend.{h,cpp} exist; the header is DX9-binding-free (struct IDirect3DDevice9 forward-declared only) and declares the 10-member ABC + the non-virtual Dx9Backend::init(IDirect3DDevice9*) + get/set/dx9Singleton; Dx9Backend wraps directX:: verbatim with no-op resize hooks, an init() that owns GetCreationParameters+ImGui_ImplDX9_Init (pDevice primary, getDevice() fallback), and a static-storage singleton.</done>
</task>

<task type="auto">
  <name>Task 2: D-07 Catch2 mock-dispatch test (10 members + post-restore null assert) + build wiring</name>
  <files>UtinniCore.Tests/Graphics/RenderBackendSeamTests.cpp, UtinniCore/UtinniCore.vcxproj, UtinniCore.Tests/UtinniCore.Tests.vcxproj</files>
  <read_first>
    - UtinniCore.Tests/Graphics/NoDeviceResetTests.cpp (Catch2 file structure: MIT header, #include <catch2/catch_all.hpp>, anon-namespace helpers, TEST_CASE(..., "[tag]")) — do NOT modify this file (review amendment 10)
    - UtinniCore/swg/graphics/render_backend.h (the interface the mock implements — created in Task 1; 10 pure virtuals)
    - .planning/phases/18-render-backend-seam-dx9backend/18-RESEARCH.md "D-07 mock IRenderBackend dispatch test" code example (the MockBackend shape — extend to all 10 members)
    - .planning/phases/18-render-backend-seam-dx9backend/18-REVIEWS.md amendment 2 (post-restore get()==nullptr assert) + amendment 5 (10-member counters)
    - UtinniCore/UtinniCore.vcxproj lines 205-214 + 286-291 (the <ClCompile>/<ClInclude> neighbor entries: directx9.cpp at ~210, directx9.h at ~289)
    - UtinniCore.Tests/UtinniCore.Tests.vcxproj lines 130-147 (NoDeviceResetTests.cpp registration at ~135 + the ProjectReference with LinkLibraryDependencies=true at 138-146)
  </read_first>
  <action>
    Create RenderBackendSeamTests.cpp: copy the MIT header + `#include <catch2/catch_all.hpp>` +
    `#include "swg/graphics/render_backend.h"`. In an anonymous namespace define
    `struct MockBackend final : render_backend::IRenderBackend` with a call counter per member
    (newFrameCalls, renderDrawCalls, preResize, postResize, rtwCalls, rthCalls, depthCalls, colorCalls,
    stageGetCalls, stageSetCalls) and override ALL 10 members to bump their counter (renderTargetWidth
    returns 1920, renderTargetHeight returns 1080, sceneDepthStage returns a sentinel, sceneDepthTexture/
    sceneColorTexture return (ImTextureID)0). Add `TEST_CASE("D-07 seam routes all 10 members through the
    installed backend", "[rndr01][graphics]")`: `render_backend::set(&mock)`, REQUIRE get() == &mock, drive
    each of the 10 members via `render_backend::get()->...`, REQUIRE each counter == 1 (and the width/height
    return values match 1920/1080), then `render_backend::set(nullptr)` to restore, then
    `REQUIRE(render_backend::get() == nullptr)` (Codex leaked-global-state check — review amendment 2). No
    live device needed.
    Edit UtinniCore/UtinniCore.vcxproj: add `<ClCompile Include="swg\graphics\render_backend.cpp" />`
    adjacent to the existing `swg\graphics\directx9.cpp` entry (~line 210) and
    `<ClInclude Include="swg\graphics\render_backend.h" />` adjacent to `swg\graphics\directx9.h` (~line 289).
    Edit UtinniCore.Tests/UtinniCore.Tests.vcxproj: add a phase-tagged
    `<!-- Phase 18 / RNDR-01 D-07: IRenderBackend mock-dispatch seam test. -->` comment then
    `<ClCompile Include="Graphics\RenderBackendSeamTests.cpp" />` next to the NoDeviceResetTests.cpp entry
    (~line 135), matching the phase/req/decision-tagged comment convention. After building, run
    `git checkout -- UtinniCoreDotNet/Generated/UtinniCore.cs` (CppSharp reorder churn — never commit it).
    No .vcxproj.filters edit (no such file exists). OUT OF SCOPE: do NOT refactor or touch
    NoDeviceResetTests.cpp (review amendment 10).
  </action>
  <verify>
    <automated>"D:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe" Utinni.sln /m /p:Configuration=Release /p:Platform=x86 exits 0, then bin\Release\UtinniCore.Tests.exe "[rndr01]" passes (the D-07 case green)</automated>
  </verify>
  <acceptance_criteria>
    - MSBuild `Utinni.sln /p:Configuration=Release /p:Platform=x86` exits 0 (render_backend.cpp compiles + links into both UtinniCore.dll and the test exe).
    - `bin\Release\UtinniCore.Tests.exe "[rndr01]"` exits 0 with the D-07 mock-dispatch case passing.
    - MockBackend overrides all 10 members: `grep -c 'override' UtinniCore.Tests/Graphics/RenderBackendSeamTests.cpp` >= 10.
    - The test asserts `render_backend::get() == nullptr` after `set(nullptr)` (review amendment 2 — leaked-global-state check): `grep -c 'get() == nullptr' UtinniCore.Tests/Graphics/RenderBackendSeamTests.cpp` >= 1.
    - `grep -c 'render_backend::set\|render_backend::get' UtinniCore.Tests/Graphics/RenderBackendSeamTests.cpp` >= 3 (install + assert + restore).
    - UtinniCore.vcxproj contains `render_backend.cpp` and `render_backend.h`; UtinniCore.Tests.vcxproj contains `RenderBackendSeamTests.cpp`.
    - `git diff --stat UtinniCore.Tests/Graphics/NoDeviceResetTests.cpp` shows ZERO changes (review amendment 10 — existing [resid04] gate untouched).
    - `git status --porcelain UtinniCoreDotNet/Generated/UtinniCore.cs` shows no staged change (CppSharp churn reverted).
  </acceptance_criteria>
  <done>The solution builds Release/x86; the D-07 Catch2 mock-dispatch test passes proving all 10 seam members route through the installed backend with no live device and that get() is null after restore; both vcxproj files reference the new files; NoDeviceResetTests.cpp is untouched; Generated/UtinniCore.cs is not committed.</done>
</task>

</tasks>

<threat_model>
This phase introduces NO new external attack surface (internal render-API refactor). The only trust
boundary is the injected DLL ↔ the third-party SWG D3D9 render thread; this plan does not touch the live
render path (it only ADDS a not-yet-consumed seam + a CI test). The threats are regression/destabilization
classes the carve could introduce.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UtinniCore.dll → SWG's D3D9 device | Untrusted/foreign device owned by SWG.exe; Utinni must never Reset or destabilize it. No new attack surface — in-process injected code into a live third-party render thread. |
| Per-frame render-thread hot path | Allocator-sensitive; heap churn here fragments SWG's allocator (R-H crash class) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering / DoS | Dx9Backend::onPreResize/onPostResize | mitigate | Honest `{}` no-ops (D-03); acceptance gate `grep -E '->Reset\(|\.Reset\(' render_backend.cpp` == 0; the existing NoDeviceResetTests.cpp continues to gate the whole tree |
| T-18-02 | DoS (allocator fragmentation) | per-frame seam dispatch | mitigate | Static-storage singleton + plain virtual call; acceptance gate forbids `std::function`/`make_unique`/`new Dx9Backend` in render_backend.cpp (Pitfall 1) |
| T-18-03 | DoS (device/render-thread race) | init ordering | mitigate | render_backend.cpp must NOT contain initPresentBlockedEvent/initDepthTexture (kept in utinni_init pre-detour block per Pitfall 2/CR-04/WR-03); acceptance gate asserts their absence; the singleton ctor stays trivial |
| T-18-10 | DoS (null device at init) | Dx9Backend::init(IDirect3DDevice9*) | mitigate | init() guards null device (assert/fallback to directX::getDevice(); returns nullptr if still null so Plan 02 setup() bails); pDevice from hkPresent is the live primary source (review amendment 6) |
| T-18-04 | (binary compat) | exported directX:: symbols | mitigate | Wrap, don't replace — no exported `UTINNI_API directX::*` signature changes; render_backend declares zero UTINNI_API (no new binding surface, no CPPS-04 ABI gate trip) |
| T-18-SC | Tampering | npm/pip/cargo/vcpkg installs | accept | Zero packages installed this phase (RESEARCH Package Legitimacy Audit: N/A); no install task exists |

No threat rises to `high` after these mitigations: this plan only adds a new, not-yet-consumed seam +
a CI test. The live D3D9 path is byte-for-byte unchanged until Plan 02 carves imgui_impl. The final
RNDR-01 acceptance (overlay renders + takes input live) is the Plan-02 D-08 maintainer live-smoke.
</threat_model>

<verification>
- MSBuild `Utinni.sln /p:Configuration=Release /p:Platform=x86` exits 0 (build INLINE on main tree — worktrees OFF).
- `bin\Release\UtinniCore.Tests.exe "[rndr01]"` passes (D-07 mock-dispatch green; 10 members + post-restore null assert).
- `bin\Release\UtinniCore.Tests.exe "[resid04]"` still passes (NoDeviceResetTests.cpp — no Reset regression; file untouched).
- D-06 source gate is NOT expected to pass yet — imgui_impl still holds DX9 symbols until Plan 02. This plan only adds the seam.
- `git status` shows no staged `Generated/UtinniCore.cs`.
</verification>

<success_criteria>
- render_backend.h declares the 10-member IRenderBackend ABC (D-02 + A2 amendment) + the non-virtual Dx9Backend::init(IDirect3DDevice9*) + get/set/dx9Singleton, with ZERO DX9 bindings (struct IDirect3DDevice9 forward-declared) and ZERO UTINNI_API.
- render_backend.cpp implements Dx9Backend wrapping directX:: verbatim, with honest no-op resize hooks, an init() that owns GetCreationParameters+ImGui_ImplDX9_Init (the single ImGui_ImplDX9_Init in the tree), and a static-storage singleton (heap-free).
- The D-07 Catch2 test proves all 10 members route through the installed backend with no live device and that get() is null after restore.
- The solution builds Release/x86 and links the seam into both the DLL and the test exe.
- No Reset introduced; no init-ordering disturbed; no exported signature changed; NoDeviceResetTests.cpp untouched; Generated churn not committed.
- OUT OF SCOPE confirmed absent: no D3D11 detection, no resize-behavior change, no detour/hkReset relocation.
</success_criteria>

<output>
Create `.planning/phases/18-render-backend-seam-dx9backend/18-01-SUMMARY.md` when done
</output>
</content>
</invoke>
