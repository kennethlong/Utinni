---
phase: 18-render-backend-seam-dx9backend
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - UtinniCore/swg/ui/imgui_impl.cpp
  - UtinniCore/swg/ui/imgui_impl.h
  - UtinniCore/swg/graphics/directx9.cpp
  - UtinniCore.Tests/Graphics/SourceGateUtil.h
  - UtinniCore.Tests/Graphics/ImguiApiNeutralityTests.cpp
  - UtinniCore.Tests/UtinniCore.Tests.vcxproj
autonomous: false
requirements: [RNDR-01]
nyquist_compliant: true

must_haves:
  truths:
    - "imgui_impl.{cpp,h} contain ZERO #include <d3d9.h>, ZERO IDirect3DDevice9, ZERO ImGui_ImplDX9_, ZERO directX:: reach-ins after the carve (D-05 full purge)."
    - "The six DX9 touch-points in imgui_impl.cpp (Init line 274, NewFrame line 419, RenderDrawData line 562, and the three getDepthTexture reach-ins lines 349/376/489) route through the IRenderBackend seam instead."
    - "imgui_impl::setup() takes HWND (Win32, not a DX9 type) — the backend self-sources the device via directX::getDevice() and does GetCreationParameters + ImGui_ImplDX9_Init internally; Client::setSwgHwnd(hFocusWindow) still happens (Issue #10 PanelGame reparent survives)."
    - "DrawColorWindow and the !enableUi depth-stage slider survive verbatim via the A2 sceneColorTexture()/sceneDepthStage get-set accessors — behavior preserved, not dropped (A2 amendment to D-02)."
    - "The API-neutral overlay logic stays single-sourced and untouched: WndProc subclass + Issue #11 chat-context routing, RT-space AddMousePosEvent input map, DirectInput arbitration, renderCallbacks dispatchSnapshot, gizmo namespace."
    - "The no-Reset / Present-stretch D3D9 contract is preserved verbatim — no Reset introduced; hkReset's InvalidateDeviceObjects/CreateDeviceObjects stay in directx9.cpp (not imgui_impl)."
    - "A D-06 structural Catch2 source-gate fails the build if any DX9 symbol reappears in imgui_impl.{cpp,h}, gated on concrete symbol forms (not the bare string D3D9), with a comment-stripping self-check."
    - "The maintainer live-smoke (D-08) confirms the overlay still renders + takes input + Issue #11 chat-context routing works in a live D3D9 SWG session — the final RNDR-01 acceptance gate."
  artifacts:
    - path: "UtinniCore/swg/ui/imgui_impl.cpp"
      provides: "API-neutral overlay logic routing the six former DX9 touch-points through render_backend::get()"
      contains: "render_backend::get()"
    - path: "UtinniCore/swg/ui/imgui_impl.h"
      provides: "DX9-binding-free header; setup(HWND) signature; includes render_backend.h"
      contains: "setup(HWND"
    - path: "UtinniCore.Tests/Graphics/ImguiApiNeutralityTests.cpp"
      provides: "D-06 source gate asserting zero DX9 symbols in imgui_impl.{cpp,h}, tag [rndr01][graphics]"
      contains: "ImGui_ImplDX9_"
    - path: "UtinniCore.Tests/Graphics/SourceGateUtil.h"
      provides: "shared stripComments/readFile/repoRootFromThisFile helpers lifted from NoDeviceResetTests.cpp"
      contains: "stripComments"
  key_links:
    - from: "UtinniCore/swg/ui/imgui_impl.cpp"
      to: "render_backend::get()"
      via: "newFrame / renderDrawData / sceneDepthTexture / sceneColorTexture / sceneDepthStage dispatch through the seam"
      pattern: "render_backend::get\\(\\)->"
    - from: "UtinniCore/swg/graphics/directx9.cpp hkPresent"
      to: "imgui_impl::setup(HWND)"
      via: "extract hFocusWindow from the captured device then call setup with the HWND"
      pattern: "imgui_impl::setup"
---

<objective>
Carve `imgui_impl.{cpp,h}` onto the Plan-01 seam: route the six DX9 touch-points through
`render_backend::get()`, change `setup()` to take an `HWND` (backend self-sources the device), and
fully purge `<d3d9.h>` / `IDirect3DDevice9` / `ImGui_ImplDX9_*` / `directX::` from imgui_impl (D-05).
Add the D-06 structural source-gate (clone of `NoDeviceResetTests.cpp`) that fails the build if any DX9
symbol reappears in imgui_impl. Then gate RNDR-01 on the maintainer live-smoke (D-08).

Purpose: single-source the ~1000-line API-neutral overlay logic so Phase 19's Dx11Backend slots behind
the same vtable without re-touching imgui_impl. Behavior is PRESERVED, not changed.
Output: carved imgui_impl.{cpp,h}, the setup-callsite update in directx9.cpp's hkPresent, the D-06 gate
+ shared SourceGateUtil.h, and the live-smoke sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-render-backend-seam-dx9backend/18-CONTEXT.md
@.planning/phases/18-render-backend-seam-dx9backend/18-RESEARCH.md
@.planning/phases/18-render-backend-seam-dx9backend/18-PATTERNS.md
@.planning/phases/18-render-backend-seam-dx9backend/18-01-SUMMARY.md

<interfaces>
<!-- The seam created by Plan 01 (18-01). imgui_impl now includes render_backend.h INSTEAD of <d3d9.h>. -->

From UtinniCore/swg/graphics/render_backend.h:
```
namespace render_backend {
  class IRenderBackend {
    virtual void newFrame() = 0;                            // replaces imgui_impl.cpp:419 ImGui_ImplDX9_NewFrame()
    virtual void renderDrawData(ImDrawData* drawData) = 0;  // replaces imgui_impl.cpp:562
    virtual void onPreResize() = 0;                         // D-03 no-op (unused this phase)
    virtual void onPostResize() = 0;                        // D-03 no-op (unused this phase)
    virtual int  renderTargetWidth() = 0;                   // D-04
    virtual int  renderTargetHeight() = 0;                  // D-04
    virtual ImTextureID sceneDepthTexture() = 0;            // replaces directX::getDepthTexture()->getTextureDepth() at :364
    virtual ImTextureID sceneColorTexture() = 0;            // replaces ...->getTextureColor() at :390 (A2)
    virtual int  sceneDepthStage() = 0;                     // replaces ...->getStage() at :499-503 (A2)
    virtual void setSceneDepthStage(int stage) = 0;         // replaces ...->setStage() (A2)
  };
  IRenderBackend* get();
  void set(IRenderBackend* backend);
  Dx9Backend* dx9Singleton();   // setup() installs this once
}
```

<!-- The six touch-points to carve (all line numbers VERIFIED against source). -->
| Line | Today | Post-carve |
|------|-------|-----------|
| imgui_impl.cpp:274 | `ImGui_ImplDX9_Init(pDevice);` | moved into Dx9Backend (Plan 01); setup() calls `render_backend::set(render_backend::dx9Singleton())` + the backend init |
| imgui_impl.cpp:419 | `ImGui_ImplDX9_NewFrame();` | `render_backend::get()->newFrame();` |
| imgui_impl.cpp:562 | `ImGui_ImplDX9_RenderDrawData(ImGui::GetDrawData());` | `render_backend::get()->renderDrawData(ImGui::GetDrawData());` |
| imgui_impl.cpp:349 | `directX::getDepthTexture()` (DrawDepthWindow, getTextureColor guard + getTextureDepth at :364) | `render_backend::get()->sceneDepthTexture()` |
| imgui_impl.cpp:376 | `directX::getDepthTexture()` (DrawColorWindow, getTextureColor at :390) | `render_backend::get()->sceneColorTexture()` |
| imgui_impl.cpp:489 | `directX::getDepthTexture()` (Tests window, getStage/setStage :499-503) | `render_backend::get()->sceneDepthStage()` / `setSceneDepthStage()` |

<!-- setup() carve (RESEARCH Pattern 3 / A3). Verified at imgui_impl.cpp:257-281. -->
Today: `void setup(IDirect3DDevice9* pDevice)` does GetCreationParameters(&cParam) (:264),
Client::setSwgHwnd(cParam.hFocusWindow) (:269), ImGui_ImplWin32_Init(cParam.hFocusWindow) (:273),
ImGui_ImplDX9_Init(pDevice) (:274), SetWindowLongPtr(...hkWndProcHandler) (:281).
Post-carve: `void setup(HWND hwnd)`. The GetCreationParameters + ImGui_ImplDX9_Init move into the
backend (Plan 01 Dx9Backend init / its sourcing of directX::getDevice()). Client::setSwgHwnd MUST still
fire. ImGui_ImplWin32_Init(hwnd) + the WndProc subclass STAY in imgui_impl (Win32, not DX9 — do not trip
the gate). The caller hkPresent (directx9.cpp:361) extracts hFocusWindow and calls setup(hwnd).

<!-- D-06 gate analog. -->
From UtinniCore.Tests/Graphics/NoDeviceResetTests.cpp: repoRootFromThisFile() (69-87), readFile() (89-96),
stripComments() (106-145), self-check SECTION (174-188) — clone verbatim into SourceGateUtil.h.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Carve imgui_impl onto the seam + full D-05 purge</name>
  <files>UtinniCore/swg/ui/imgui_impl.cpp, UtinniCore/swg/ui/imgui_impl.h, UtinniCore/swg/graphics/directx9.cpp</files>
  <read_first>
    - UtinniCore/swg/ui/imgui_impl.h (lines 27 #include <d3d9.h> + 38 setup(IDirect3DDevice9*) — the two header edits)
    - UtinniCore/swg/ui/imgui_impl.cpp lines 255-281 (setup body), 415-425 (NewFrame), 555-565 (RenderDrawData), 345-400 (DrawDepthWindow + DrawColorWindow), 480-515 (Tests window depth-stage slider), 144-254 (WndProc subclass — MUST stay untouched), 447-463 (RT-space AddMousePosEvent — MUST stay)
    - UtinniCore/swg/graphics/directx9.cpp lines 359-382 (hkPresent setup callsite + hkReset Invalidate/Create which STAY here)
    - UtinniCore/swg/graphics/render_backend.h (the seam — created in Plan 01)
    - .planning/phases/18-render-backend-seam-dx9backend/18-PATTERNS.md "imgui_impl.cpp (the six-touch-point carve)" + "imgui_impl.h (header purge)" + "setup() device-acquisition pattern" sections
    - .planning/phases/18-render-backend-seam-dx9backend/18-RESEARCH.md Pitfall 3 (hkReset stays in directx9.cpp) + Pitfall 4 (Issue #11 / RT-space must NOT move)
  </read_first>
  <action>
    Edit imgui_impl.h: REMOVE `#include <d3d9.h>` (line 27). ADD `#include "swg/graphics/render_backend.h"`.
    CHANGE `extern void setup(IDirect3DDevice9* pDevice);` (line 38) to `extern void setup(HWND hwnd);`
    (HWND is Win32, not DX9 — does not trip the gate; setup is non-UTINNI_API so this is ABI-safe).
    Edit imgui_impl.cpp: change `setup(IDirect3DDevice9* pDevice)` to `setup(HWND hwnd)` and rework its
    body per the A3 pattern — drop the local `D3DDEVICE_CREATION_PARAMETERS`/`GetCreationParameters` and
    `ImGui_ImplDX9_Init(pDevice)`; install the backend with `render_backend::set(render_backend::dx9Singleton())`
    and trigger its init (the backend self-sources the device via directX::getDevice() and does
    GetCreationParameters + ImGui_ImplDX9_Init internally per Plan 01). Keep `Client::setSwgHwnd(hwnd)`
    (Issue #10 PanelGame reparent MUST survive — the hwnd now arrives as the param). Keep
    `ImGui_ImplWin32_Init(hwnd)` (:273) and the `SetWindowLongPtr(hwnd, GWL_WNDPROC, hkWndProcHandler)`
    subclass install (:281) verbatim. Replace the per-frame touch-points: `ImGui_ImplDX9_NewFrame()` (:419)
    -> `render_backend::get()->newFrame();`; `ImGui_ImplDX9_RenderDrawData(ImGui::GetDrawData())` (:562) ->
    `render_backend::get()->renderDrawData(ImGui::GetDrawData());`. Replace the three depth reach-ins:
    DrawDepthWindow (:349/:364) -> `render_backend::get()->sceneDepthTexture()` (the accessor folds the
    null guard); DrawColorWindow (:376/:390) -> `render_backend::get()->sceneColorTexture()`; the !enableUi
    Tests-window stage slider (:489/:499-503) -> `render_backend::get()->sceneDepthStage()` /
    `setSceneDepthStage()`. Do NOT touch: the WndProc subclass + Issue #11 chat-context routing (144-254),
    the RT-space AddMousePosEvent map (447-463), DirectInput arbitration (537-550), renderCallbacks
    dispatchSnapshot, the gizmo namespace (631-1005). Edit directx9.cpp hkPresent (the setup callsite ~:361):
    extract hFocusWindow from the captured device (it already has the device) and call `imgui_impl::setup(hwnd)`
    with the HWND instead of the device pointer. Do NOT move hkReset's ImGui_ImplDX9_InvalidateDeviceObjects/
    CreateDeviceObjects (directx9.cpp:373/375) — they stay in the DX9 tier (Pitfall 3). NEVER add ->Reset(/.Reset().
    After building, `git checkout -- UtinniCoreDotNet/Generated/UtinniCore.cs`.
  </action>
  <verify>
    <automated>"D:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe" Utinni.sln /m /p:Configuration=Release /p:Platform=x86 exits 0; grep -L 'IDirect3DDevice9\|#include <d3d9.h>\|ImGui_ImplDX9_\|directX::' UtinniCore/swg/ui/imgui_impl.cpp returns the file (zero DX9-symbol matches after comment text is incidental — confirmed by the Task 2 gate)</automated>
  </verify>
  <acceptance_criteria>
    - MSBuild `Utinni.sln /p:Configuration=Release /p:Platform=x86` exits 0 (the carve compiles + links).
    - `grep -c 'render_backend::get()->' UtinniCore/swg/ui/imgui_impl.cpp` >= 5 (the five seam dispatch sites: newFrame, renderDrawData, sceneDepthTexture, sceneColorTexture, sceneDepthStage/set).
    - imgui_impl.h contains `setup(HWND` and does NOT contain `#include <d3d9.h>` or `IDirect3DDevice9`.
    - imgui_impl.cpp contains `Client::setSwgHwnd` (Issue #10 reparent preserved) and `ImGui_ImplWin32_Init` (Win32 init stays).
    - `grep -E '\->Reset\(|\.Reset\(' UtinniCore/swg/ui/imgui_impl.cpp UtinniCore/swg/graphics/render_backend.cpp` returns ZERO matches.
    - directx9.cpp hkPresent calls `imgui_impl::setup(` with an HWND; the InvalidateDeviceObjects/CreateDeviceObjects calls remain present in directx9.cpp (`grep -c 'ImGui_ImplDX9_InvalidateDeviceObjects' UtinniCore/swg/graphics/directx9.cpp` >= 1).
    - The WndProc subclass token `hkWndProcHandler` and the RT-space `AddMousePosEvent` token are still present in imgui_impl.cpp (Issue #11 + RT-space NOT swept out).
    - `git status --porcelain UtinniCoreDotNet/Generated/UtinniCore.cs` shows no staged change.
  </acceptance_criteria>
  <done>imgui_impl.{cpp,h} are DX9-binding-free and route the six former touch-points through the seam; setup takes HWND; hkPresent passes the HWND; the no-Reset contract and Issue #11 / RT-space / gizmo logic are preserved verbatim; the solution builds Release/x86.</done>
</task>

<task type="auto">
  <name>Task 2: D-06 API-neutrality source gate + shared SourceGateUtil.h</name>
  <files>UtinniCore.Tests/Graphics/SourceGateUtil.h, UtinniCore.Tests/Graphics/ImguiApiNeutralityTests.cpp, UtinniCore.Tests/UtinniCore.Tests.vcxproj</files>
  <read_first>
    - UtinniCore.Tests/Graphics/NoDeviceResetTests.cpp lines 63-170 (repoRootFromThisFile, readFile, stripComments — the helpers to lift) + 174-188 (the planted-in-comment self-check SECTION)
    - .planning/phases/18-render-backend-seam-dx9backend/18-RESEARCH.md "D-06 API-neutrality source gate" code example + Pitfall 5 (gate on concrete symbol forms, never bare "D3D9")
    - .planning/phases/18-render-backend-seam-dx9backend/18-PATTERNS.md "ImguiApiNeutralityTests.cpp" + "SourceGateUtil.h" + "Phase-tagged vcxproj <ClCompile> registration" sections
    - UtinniCore.Tests/UtinniCore.Tests.vcxproj lines 130-147 (NoDeviceResetTests.cpp + the new RenderBackendSeamTests.cpp entry added in 18-01; phase-tagged comment convention)
  </read_first>
  <action>
    Create SourceGateUtil.h: copy the MIT header + `#pragma once`, then lift `repoRootFromThisFile()`,
    `readFile()`, `stripComments()` (and a small `countSubstr`) from NoDeviceResetTests.cpp:63-170 into a
    `namespace source_gate` (or inline namespace) so both NoDeviceResetTests.cpp and the new gate can share
    them without an ODR clash. (RESEARCH Wave-0 recommendation (a) — single-source the gate plumbing.) Leave
    NoDeviceResetTests.cpp working: either include the new header there too, or leave its file-local copies
    intact — do NOT break the existing [resid04] gate.
    Create ImguiApiNeutralityTests.cpp: MIT header + `#include <catch2/catch_all.hpp>` +
    `#include "SourceGateUtil.h"`. Add `TEST_CASE("D-06 imgui_impl is fully DX9-API-neutral after the carve",
    "[rndr01][graphics]")` with: a `SECTION("stripper hygiene self-check")` planting `IDirect3DDevice9` and
    `#include <d3d9.h>` inside comments and asserting the stripped count is 0 while the raw count is non-zero
    (proves the stripper does real work — mirror NoDeviceResetTests.cpp:174-188); a `SECTION("imgui_impl.cpp")`
    asserting `gatedCount(...,"#include <d3d9.h>")==0`, `IDirect3DDevice9 ==0`, `ImGui_ImplDX9_ ==0`,
    `directX:: ==0`; a `SECTION("imgui_impl.h")` asserting `#include <d3d9.h> ==0` and `IDirect3DDevice9 ==0`.
    Gate ONLY on these concrete symbol forms — NEVER the bare string "D3D9"/"DX9" (comments at imgui_impl.cpp
    52-55, 422-446 mention D3D9/Reset; Pitfall 5). Strip comments before counting.
    Edit UtinniCore.Tests/UtinniCore.Tests.vcxproj: add a phase-tagged
    `<!-- Phase 18 / RNDR-01 D-06: imgui_impl DX9-API-neutrality source gate. -->` comment then
    `<ClCompile Include="Graphics\ImguiApiNeutralityTests.cpp" />` next to the NoDeviceResetTests.cpp /
    RenderBackendSeamTests.cpp entries (~line 135). SourceGateUtil.h is header-only — if it needs an explicit
    `<ClInclude>`, add one next to the test entries; otherwise it is pulled by #include. After building,
    `git checkout -- UtinniCoreDotNet/Generated/UtinniCore.cs`.
  </action>
  <verify>
    <automated>"D:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe" Utinni.sln /m /p:Configuration=Release /p:Platform=x86 exits 0, then bin\Release\UtinniCore.Tests.exe "[rndr01],[resid04]" passes (D-06 gate + D-07 seam test + the existing no-Reset gate all green)</automated>
  </verify>
  <acceptance_criteria>
    - `bin\Release\UtinniCore.Tests.exe "[rndr01]"` exits 0 with the D-06 gate case passing (imgui_impl.{cpp,h} contain zero DX9 symbols).
    - `bin\Release\UtinniCore.Tests.exe "[resid04]"` still passes (NoDeviceResetTests.cpp not broken by the SourceGateUtil.h refactor).
    - ImguiApiNeutralityTests.cpp gates on `#include <d3d9.h>`, `IDirect3DDevice9`, `ImGui_ImplDX9_`, `directX::` and contains a stripper self-check SECTION; `grep -c 'D3D9"' UtinniCore.Tests/Graphics/ImguiApiNeutralityTests.cpp` shows the gate does NOT assert against the bare string "D3D9".
    - SourceGateUtil.h contains `stripComments` and `repoRootFromThisFile`; the UtinniCore.Tests.vcxproj references `ImguiApiNeutralityTests.cpp`.
    - `git status --porcelain UtinniCoreDotNet/Generated/UtinniCore.cs` shows no staged change.
  </acceptance_criteria>
  <done>The D-06 structural gate passes (and would fail the build if any DX9 symbol reappeared in imgui_impl), shares its comment-stripping helpers with the existing no-Reset gate, and gates on concrete symbol forms only; the existing [resid04] gate still passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: D-08 maintainer live-smoke (final RNDR-01 acceptance)</name>
  <what-built>
    The render-backend seam carve is complete and CI-green: the ImGui overlay now renders through the
    IRenderBackend vtable (Dx9Backend wrapping directX:: verbatim), imgui_impl is fully DX9-API-neutral
    (D-06 gate green), and the D-07 mock-dispatch + [resid04] no-Reset gates pass. CI cannot inject into a
    live SWG.exe, so the final RNDR-01 acceptance is this maintainer-only live-smoke (D-08).
  </what-built>
  <how-to-verify>
    1. Build Release/x86 if not already current: MSBuild `Utinni.sln /p:Configuration=Release /p:Platform=x86`.
    2. Inject `UtinniCore.dll` into a live 32-bit SWG session (SWGEmu or Restoration) as usual.
    3. Confirm the ImGui overlay RENDERS (the internal UI / gizmo draws on top of the 3D scene).
    4. Confirm input works: mouse moves the cursor in render-target space (no right-edge offset regression
       beyond the known RESID-04 deadzone), keyboard reaches the overlay.
    5. Confirm Issue #11 chat-context routing still works: pressing Enter in-game opens chat (openChat),
       not the input-mode chatEnter — verify via TJT's chat-command parser path.
    6. Trigger a TJT-driven scene change and a window resize; confirm NO crash and NO device Reset
       (landing naked after a scene change is the documented baseline, NOT a regression).
    7. Confirm the overlay behavior is INDISTINGUISHABLE from pre-carve (this is a pure refactor).
  </how-to-verify>
  <resume-signal>Type "approved" (overlay renders + input + Issue #11 routing all work, no Reset/crash), or describe the observed regression.</resume-signal>
</task>

</tasks>

<threat_model>
This phase introduces NO new external attack surface. The threats are regression/destabilization classes
the carve could introduce while moving the six DX9 touch-points behind the seam.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UtinniCore.dll → SWG's D3D9 device | Foreign device owned by SWG.exe; never Reset / destabilize it (no-Reset/Present-stretch contract) |
| Per-frame render-thread hot path | Allocator-sensitive (R-H crash class); seam dispatch must stay heap-free |
| imgui_impl WndProc subclass → SWG input | Issue #11 chat-context routing + RT-space input map must survive the carve unchanged |
| imgui_impl::setup signature | Non-UTINNI_API internal; changing to setup(HWND) is ABI-safe (no managed/plugin consumer) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Tampering / DoS | the carve introduces a Reset or destabilizes the device | mitigate | onPreResize/onPostResize stay no-ops (Plan 01); acceptance gate `grep -E '->Reset\(|\.Reset\(' imgui_impl.cpp render_backend.cpp` == 0; hkReset's Invalidate/Create stay in directx9.cpp; NoDeviceResetTests.cpp [resid04] continues to gate; live-smoke step 6 confirms no Reset/crash through resize |
| T-18-06 | DoS (input regression) | Issue #11 chat-context routing / RT-space map accidentally swept out | mitigate | Pitfall 4 — WndProc subclass + AddMousePosEvent explicitly NOT moved; acceptance gate asserts hkWndProcHandler + AddMousePosEvent tokens still present in imgui_impl.cpp; live-smoke step 4/5 confirms input + Issue #11 routing |
| T-18-07 | DoS (null device / missed hook) | setup() HWND carve breaks the hkPresent → setup ordering | mitigate | Backend self-sources device via directX::getDevice() (live when hkPresent fires per A3); Client::setSwgHwnd preserved (acceptance gate); init ordering untouched (initPresentBlockedEvent/initDepthTexture stay in utinni_init — Plan 01 T-18-03); live-smoke step 3 confirms overlay renders |
| T-18-08 | (binary compat) | exported symbol signature change → MissingMethodException at MEF compose | accept | imgui_impl::setup is NOT UTINNI_API-exported (verified imgui_impl.h:38, plain extern, called only from directx9.cpp) — its signature change is ABI-safe; no exported directX:: signature changes; no TJT/plugin rebuild needed |
| T-18-09 | DoS (allocator fragmentation) | per-frame seam dispatch on the carved render path | mitigate | Plain virtual call through Plan-01 static-storage singleton; no new container/std::function added to imgui_impl render path |

After mitigations, no threat rises to `high`: every regression class has a CI-enforceable structural gate
(D-06 source gate, [resid04] no-Reset gate, D-07 seam dispatch) PLUS the maintainer live-smoke as the
final RNDR-01 acceptance. T-18-08 is accepted because setup is provably non-exported (ABI-safe). The
live D3D9 behavior is preserved, not changed.
</threat_model>

<verification>
- MSBuild `Utinni.sln /p:Configuration=Release /p:Platform=x86` exits 0 (build INLINE on main tree — worktrees OFF).
- `bin\Release\UtinniCore.Tests.exe "[rndr01]"` passes (D-06 API-neutrality gate + D-07 seam dispatch green).
- `bin\Release\UtinniCore.Tests.exe "[resid04]"` passes (no-Reset contract preserved).
- `dotnet test --no-build` PreservationAudit lane green (CON-* structural Facts unbroken).
- D-08 maintainer live-smoke: overlay renders + takes input + Issue #11 routing works in a live D3D9 session; no Reset/crash through scene change + resize.
- `git status` shows no staged `Generated/UtinniCore.cs`.
</verification>

<success_criteria>
- imgui_impl.{cpp,h} are fully DX9-API-neutral (D-05 full purge): zero <d3d9.h>, IDirect3DDevice9, ImGui_ImplDX9_, directX:: — enforced by the D-06 gate.
- The six former DX9 touch-points route through render_backend::get(); setup takes HWND; hkPresent passes the HWND.
- DrawColorWindow + the !enableUi stage slider survive via the A2 sceneColorTexture/sceneDepthStage accessors (behavior preserved, not dropped).
- The API-neutral overlay logic (Issue #11 routing, RT-space map, gizmo, renderCallbacks) is single-sourced and untouched; the no-Reset contract is verbatim.
- The D-06 + D-07 + [resid04] gates are CI-green; the maintainer live-smoke confirms live RNDR-01 acceptance.
</success_criteria>

<output>
Create `.planning/phases/18-render-backend-seam-dx9backend/18-02-SUMMARY.md` when done
</output>
