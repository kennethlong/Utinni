---
phase: 18-render-backend-seam-dx9backend
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - UtinniCore/swg/ui/imgui_impl.cpp
  - UtinniCore/swg/ui/imgui_impl.h
  - UtinniCore/swg/graphics/directx9.cpp
  - UtinniCore.Tests/Graphics/SourceGateUtil.h
  - UtinniCore.Tests/Graphics/ImguiApiNeutralityTests.cpp
  - UtinniCore.Tests/UtinniCore.Tests.vcxproj
autonomous: false
requirements: [RNDR-01]
nyquist_compliant: true

must_haves:
  truths:
    - "imgui_impl.{cpp,h} contain ZERO #include <d3d9.h>, ZERO #include <imgui_impl_dx9.h>, ZERO #include \"swg/graphics/directx9.h\", ZERO IDirect3DDevice9, ZERO LPDIRECT3DDEVICE9/LPDIRECT3D, ZERO D3DDEVICE_CREATION_PARAMETERS, ZERO ImGui_ImplDX9_, ZERO directX:: reach-ins after the carve (D-05 full purge, extended token set per review amendment 3)."
    - "The six DX9 touch-points in imgui_impl.cpp (Init line 274, NewFrame line 419, RenderDrawData line 562, and the three getDepthTexture reach-ins lines 349/376/489) route through the IRenderBackend seam instead."
    - "imgui_impl::setup() takes HWND (Win32, not a DX9 type). The locked setup() step order is: render_backend::set(dx9Singleton()) -> backend->init(pDevice_from_hkPresent) -> Client::setSwgHwnd(hwnd) -> ImGui_ImplWin32_Init(hwnd) [Win32 BEFORE DX9] -> backend init completes ImGui_ImplDX9_Init -> SetWindowLongPtr WndProc subclass -> isSetup=true LAST (review amendments 2/9)."
    - "hkPresent extracts HWND + the live device from its pDevice argument and passes pDevice into Dx9Backend::init() (review amendment 6) — directX::getDevice() is assert/fallback only. If GetCreationParameters fails / hFocusWindow is null / device is null, setup() guards and early-returns (does not assume live)."
    - "Every seam call site (newFrame, renderDrawData, DrawDepth/Color, Tests window) is null-guarded: set() happens BEFORE isSetup=true, and each dispatch uses an `if (auto* b = render_backend::get())` guard / early-return (review amendment 2). The existing hkPresent render()-before-setup() ordering is PRESERVED intentionally (the isSetup gate protects it) — executors MUST NOT reorder/merge it."
    - "DrawColorWindow and the !enableUi depth-stage slider survive via the A2 sceneColorTexture()/sceneDepthStage get-set accessors. The !enableUi Tests-window block guard is the STRICTER sceneDepthTexture() != 0 (an intentional dev-only behavior change, NOT verbatim — documented per review amendment 4). Release (enableInternalUi=true) is unaffected: the block is behind if (!enableUi)."
    - "The API-neutral overlay logic stays single-sourced and untouched: WndProc subclass + Issue #11 chat-context routing, RT-space AddMousePosEvent input map, DirectInput arbitration, renderCallbacks dispatchSnapshot, gizmo namespace."
    - "The no-Reset / Present-stretch D3D9 contract is preserved verbatim — no Reset introduced; hkReset's InvalidateDeviceObjects/CreateDeviceObjects stay in directx9.cpp (not imgui_impl)."
    - "A D-06 structural Catch2 source-gate fails the build if any DX9 symbol (extended token set) reappears in imgui_impl.{cpp,h}, gated on concrete symbol forms (not the bare string D3D9), with a comment-stripping self-check; PLUS a seam-dispatch-presence assertion that render_backend::get()-> appears >= 5 times in imgui_impl.cpp (review amendment 8 — proves the carve actually wired the seam in)."
    - "The maintainer live-smoke (D-08) confirms the overlay still renders + takes input + Issue #11 chat-context routing works in a live D3D9 SWG session, and (dev-config) the Tests window + stage slider behave as expected post-guard-change — the final RNDR-01 acceptance gate."
  artifacts:
    - path: "UtinniCore/swg/ui/imgui_impl.cpp"
      provides: "API-neutral overlay logic routing the six former DX9 touch-points through render_backend::get() with null guards"
      contains: "render_backend::get()"
    - path: "UtinniCore/swg/ui/imgui_impl.h"
      provides: "DX9-binding-free header; setup(HWND) signature; includes render_backend.h"
      contains: "setup(HWND"
    - path: "UtinniCore.Tests/Graphics/ImguiApiNeutralityTests.cpp"
      provides: "D-06 source gate asserting zero DX9 symbols (extended set) in imgui_impl.{cpp,h} + a seam-dispatch-presence assert, tag [rndr01][graphics]"
      contains: "ImGui_ImplDX9_"
    - path: "UtinniCore.Tests/Graphics/SourceGateUtil.h"
      provides: "shared stripComments/readFile/repoRootFromThisFile helpers for the NEW D-06 gate (NoDeviceResetTests.cpp keeps its own file-local copies untouched)"
      contains: "stripComments"
  key_links:
    - from: "UtinniCore/swg/ui/imgui_impl.cpp"
      to: "render_backend::get()"
      via: "null-guarded newFrame / renderDrawData / sceneDepthTexture / sceneColorTexture / sceneDepthStage dispatch through the seam"
      pattern: "render_backend::get\\(\\)->"
    - from: "UtinniCore/swg/graphics/directx9.cpp hkPresent"
      to: "imgui_impl::setup(HWND)"
      via: "extract hFocusWindow + the live device from the captured pDevice; pass pDevice into Dx9Backend::init() inside setup()"
      pattern: "imgui_impl::setup"
---

<objective>
Carve `imgui_impl.{cpp,h}` onto the Plan-01 seam: route the six DX9 touch-points through
`render_backend::get()` (null-guarded), change `setup()` to take an `HWND` and call the Plan-01
`Dx9Backend::init(pDevice)` in a LOCKED step order (Win32-before-DX9, set-before-isSetup), and fully
purge the extended DX9 token set (`<d3d9.h>`, `<imgui_impl_dx9.h>`, `"swg/graphics/directx9.h"`,
`IDirect3DDevice9`, `LPDIRECT3DDEVICE9`/`LPDIRECT3D`, `D3DDEVICE_CREATION_PARAMETERS`, `ImGui_ImplDX9_*`,
`directX::`) from imgui_impl (D-05). Add the D-06 structural source-gate (clone of `NoDeviceResetTests.cpp`)
that fails the build if any DX9 symbol reappears AND that proves the seam was actually wired in. Then gate
RNDR-01 on the maintainer live-smoke (D-08).

Purpose: single-source the ~1000-line API-neutral overlay logic so Phase 19's Dx11Backend slots behind
the same vtable without re-touching imgui_impl. Behavior is PRESERVED, not changed (with one documented
dev-only exception: the !enableUi Tests-window guard becomes stricter — see truth above and amendment 4).
Output: carved imgui_impl.{cpp,h}, the setup-callsite update in directx9.cpp's hkPresent, the D-06 gate
+ shared SourceGateUtil.h, and the live-smoke sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-render-backend-seam-dx9backend/18-CONTEXT.md
@.planning/phases/18-render-backend-seam-dx9backend/18-RESEARCH.md
@.planning/phases/18-render-backend-seam-dx9backend/18-PATTERNS.md
@.planning/phases/18-render-backend-seam-dx9backend/18-REVIEWS.md
@.planning/phases/18-render-backend-seam-dx9backend/18-01-SUMMARY.md

<interfaces>
<!-- The seam created by Plan 01 (18-01). imgui_impl now includes render_backend.h INSTEAD of the DX9 bindings. -->

From UtinniCore/swg/graphics/render_backend.h:
```
namespace render_backend {
  class IRenderBackend {                                    // 10 pure virtuals (D-02 + A2)
    virtual void newFrame() = 0;                            // replaces imgui_impl.cpp:419 ImGui_ImplDX9_NewFrame()
    virtual void renderDrawData(ImDrawData* drawData) = 0;  // replaces imgui_impl.cpp:562
    virtual void onPreResize() = 0;                         // D-03 no-op (unused this phase)
    virtual void onPostResize() = 0;                        // D-03 no-op (unused this phase)
    virtual int  renderTargetWidth() = 0;                   // D-04 forward-compat hook (UNUSED in Phase 18)
    virtual int  renderTargetHeight() = 0;                  // D-04 forward-compat hook (UNUSED in Phase 18)
    virtual ImTextureID sceneDepthTexture() = 0;            // replaces directX::getDepthTexture()->getTextureDepth() at :364
    virtual ImTextureID sceneColorTexture() = 0;            // replaces ...->getTextureColor() at :390 (A2)
    virtual int  sceneDepthStage() = 0;                     // replaces ...->getStage() at :499-503 (A2)
    virtual void setSceneDepthStage(int stage) = 0;         // replaces ...->setStage() (A2)
  };
  // NON-VIRTUAL init contract (OFF the vtable) that setup() calls:
  class Dx9Backend { HWND init(IDirect3DDevice9* device); /* GetCreationParameters -> ImGui_ImplDX9_Init; returns hFocusWindow */ };
  IRenderBackend* get();        // NULLABLE — null before setup() installs the backend; guard every call site
  void set(IRenderBackend* backend);
  Dx9Backend* dx9Singleton();   // setup() installs this once
}
```

<!-- The six touch-points to carve (all line numbers VERIFIED against source). -->
| Line | Today | Post-carve |
|------|-------|-----------|
| imgui_impl.cpp:274 | `ImGui_ImplDX9_Init(pDevice);` | moved into Dx9Backend::init() (Plan 01); setup() calls `render_backend::set(render_backend::dx9Singleton())` then `dx9Singleton()->init(pDevice_from_hkPresent)` |
| imgui_impl.cpp:419 | `ImGui_ImplDX9_NewFrame();` | `if (auto* b = render_backend::get()) b->newFrame();` |
| imgui_impl.cpp:562 | `ImGui_ImplDX9_RenderDrawData(ImGui::GetDrawData());` | `if (auto* b = render_backend::get()) b->renderDrawData(ImGui::GetDrawData());` |
| imgui_impl.cpp:349 | `directX::getDepthTexture()` (DrawDepthWindow, getTextureColor guard + getTextureDepth at :364) | `render_backend::get()->sceneDepthTexture()` (guarded) |
| imgui_impl.cpp:376 | `directX::getDepthTexture()` (DrawColorWindow, getTextureColor at :390) | `render_backend::get()->sceneColorTexture()` (guarded) |
| imgui_impl.cpp:489 | `directX::getDepthTexture()` (Tests window, getStage/setStage :499-503) | block gated on `render_backend::get()->sceneDepthTexture() != 0`; slider uses `sceneDepthStage()`/`setSceneDepthStage()` |

<!-- setup() carve — LOCKED step order (RESEARCH Pattern 3 / A3; review amendments 2/6/9). Verified at imgui_impl.cpp:257-281. -->
Today: `void setup(IDirect3DDevice9* pDevice)` does GetCreationParameters(&cParam) (:264),
Client::setSwgHwnd(cParam.hFocusWindow) (:269), ImGui_ImplWin32_Init(cParam.hFocusWindow) (:273),
ImGui_ImplDX9_Init(pDevice) (:274), SetWindowLongPtr(...hkWndProcHandler) (:281).
Post-carve `void setup(HWND hwnd)` — the GetCreationParameters + ImGui_ImplDX9_Init move into
Dx9Backend::init(pDevice) (Plan 01). hkPresent (directx9.cpp:361) extracts hFocusWindow + the live device
from its pDevice and calls setup(hwnd); setup() then runs the LOCKED order:
  1. render_backend::set(render_backend::dx9Singleton());
  2. dx9Singleton()->init(pDevice_from_hkPresent);   // NOT getDevice() alone — pDevice is primary
  3. Client::setSwgHwnd(hwnd);                        // Issue #10 PanelGame reparent MUST survive
  4. ImGui_ImplWin32_Init(hwnd);                      // Win32 platform backend BEFORE the DX9 renderer
  5. (init in step 2 already completed ImGui_ImplDX9_Init internally — DX9 renderer init)
  6. SetWindowLongPtr(hwnd, GWL_WNDPROC, hkWndProcHandler);  // WndProc subclass install
  7. isSetup = true;                                  // LAST — set() in step 1 precedes this

<!-- D-06 gate analog. -->
From UtinniCore.Tests/Graphics/NoDeviceResetTests.cpp: repoRootFromThisFile() (69-87), readFile() (89-96),
stripComments() (106-145), self-check SECTION (174-188) — clone into SourceGateUtil.h for the NEW gate ONLY.
Do NOT modify NoDeviceResetTests.cpp (its file-local copies stay; the [resid04] gate is untouched).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Carve imgui_impl onto the seam (null-guarded, locked setup order) + full D-05 purge</name>
  <files>UtinniCore/swg/ui/imgui_impl.cpp, UtinniCore/swg/ui/imgui_impl.h, UtinniCore/swg/graphics/directx9.cpp</files>
  <read_first>
    - UtinniCore/swg/ui/imgui_impl.h (lines 27 #include <d3d9.h> + 38 setup(IDirect3DDevice9*) — the two header edits)
    - UtinniCore/swg/ui/imgui_impl.cpp lines 255-281 (setup body — the LOCKED order target), 415-425 (NewFrame), 555-565 (RenderDrawData), 345-400 (DrawDepthWindow + DrawColorWindow), 480-515 (Tests window depth-stage slider, behind if (!enableUi) at :480), 144-254 (WndProc subclass — MUST stay untouched), 447-463 (RT-space AddMousePosEvent — MUST stay; reads Graphics:: not DX9), 403 (the isSetup render gate)
    - UtinniCore/swg/graphics/directx9.cpp lines 249-251 (hkBeginScene captures pDirectXDevice), 320-382 (hkPresent: render() at ~325 BEFORE setup() at ~361 — the ordering to PRESERVE; hkReset Invalidate/Create at 373/375 which STAY here)
    - UtinniCore/swg/graphics/render_backend.h (the seam + the non-virtual Dx9Backend::init — created in Plan 01)
    - .planning/phases/18-render-backend-seam-dx9backend/18-PATTERNS.md "imgui_impl.cpp (the six-touch-point carve)" + "imgui_impl.h (header purge)" + "setup() device-acquisition pattern" sections
    - .planning/phases/18-render-backend-seam-dx9backend/18-RESEARCH.md Pattern 3 (setup) + Pitfall 3 (hkReset stays in directx9.cpp) + Pitfall 4 (Issue #11 / RT-space must NOT move)
    - .planning/phases/18-render-backend-seam-dx9backend/18-REVIEWS.md Consensus Summary amendments 2, 3, 4, 6, 9
  </read_first>
  <action>
    Edit imgui_impl.h: REMOVE `#include <d3d9.h>` (line 27). ADD `#include "swg/graphics/render_backend.h"`.
    CHANGE `extern void setup(IDirect3DDevice9* pDevice);` (line 38) to `extern void setup(HWND hwnd);`
    (HWND is Win32, not DX9 — does not trip the gate; setup is non-UTINNI_API so this is ABI-safe).
    Edit imgui_impl.cpp: change `setup(IDirect3DDevice9* pDevice)` to `setup(HWND hwnd)` and rework its
    body to the LOCKED step order (review amendments 2/6/9), in EXACTLY this sequence:
    (1) `render_backend::set(render_backend::dx9Singleton());`
    (2) `render_backend::dx9Singleton()->init(pDevice)` where pDevice is the live device passed from hkPresent
        (see directx9.cpp edit below) — NOT directX::getDevice() alone; init() (Plan 01) does
        GetCreationParameters + ImGui_ImplDX9_Init internally and returns hFocusWindow;
    (3) `Client::setSwgHwnd(hwnd);` (Issue #10 PanelGame reparent MUST survive — the hwnd arrives as the param);
    (4) `ImGui_ImplWin32_Init(hwnd);` — Win32 platform backend init, which MUST come BEFORE the DX9 renderer
        init (ImGui backend docs require platform-before-renderer; the DX9 renderer init happened inside
        init() in step 2 — keep step 2 before any frame is drawn, never reorder Win32 ahead of set/init);
    (6) `SetWindowLongPtr(hwnd, GWL_WNDPROC, (LONG)hkWndProcHandler);` WndProc subclass install (verbatim);
    (7) `isSetup = true;` LAST. Because set() in step 1 precedes isSetup=true, no seam call site can fire
        with a null backend through the isSetup gate. Drop the local `D3DDEVICE_CREATION_PARAMETERS`/
        `GetCreationParameters` and `ImGui_ImplDX9_Init(pDevice)` from setup() entirely (they live in init()).
    NULL-GUARD every seam call site (review amendment 2). Replace the per-frame touch-points:
    `ImGui_ImplDX9_NewFrame()` (:419) -> `if (auto* b = render_backend::get()) b->newFrame();`;
    `ImGui_ImplDX9_RenderDrawData(ImGui::GetDrawData())` (:562) ->
    `if (auto* b = render_backend::get()) b->renderDrawData(ImGui::GetDrawData());`. Replace the three depth
    reach-ins: DrawDepthWindow (:349/:364) -> `render_backend::get()->sceneDepthTexture()` (the accessor
    folds the null guard); DrawColorWindow (:376/:390) -> `render_backend::get()->sceneColorTexture()`; the
    !enableUi Tests-window stage slider (:489/:499-503) -> `render_backend::get()->sceneDepthStage()` /
    `setSceneDepthStage()`.
    TESTS-WINDOW GUARD (review amendment 4 — INTENTIONAL stricter behavior, NOT verbatim): today the
    `if (depthTex != nullptr)` guard at :490 wraps the WHOLE Tests-window block; `sceneDepthStage()` returns
    an int and cannot signal "no depth texture". Gate the block on `render_backend::get()->sceneDepthTexture()
    != 0`. This is STRICTER than today (the accessor also requires a live color texture), so the !enableUi
    Tests block stays hidden in more cases than before — this is a DELIBERATE dev-only behavior change. Add a
    code comment at the guard documenting it as intentional (not verbatim). Release (enableInternalUi=true)
    is UNAFFECTED — the block is behind `if (!enableUi)` at :480. (Do NOT add an 11th seam member; the seam
    is locked at 10 — the stricter guard is the chosen resolution.)
    PRESERVE the existing hkPresent render()-before-setup() ordering (Cursor): the isSetup gate at :403
    protects render() running before setup() in the first frame. Do NOT reorder/merge setup() ahead of
    render() in hkPresent, and do NOT "clean up" that ordering.
    Do NOT touch: the WndProc subclass + Issue #11 chat-context routing (144-254), the RT-space
    AddMousePosEvent map (447-463), DirectInput arbitration (537-550), renderCallbacks dispatchSnapshot,
    the gizmo namespace (631-1005). imgui_impl keeps reading Graphics:: directly in the RT-space block —
    renderTargetWidth/Height seam members are NOT consumed here (D-04 forward-compat, review amendment 7).
    Edit directx9.cpp hkPresent (the setup callsite ~:361): from the captured pDevice extract hFocusWindow
    (GetCreationParameters lives in Plan-01 init(), so here just pass the device through) and call
    `imgui_impl::setup(hwnd)` after obtaining hwnd — BUT route the live device into the backend: pass the
    same live pDevice that hkPresent holds into the Plan-01 init() (the setup() body calls
    dx9Singleton()->init(pDevice)). The cleanest shape: hkPresent calls setup(hwnd) and setup() reads the
    device via the Plan-01 init() which receives it; if the planner/executor prefers, hkPresent may stash the
    live pDevice for init() to consume — but pDevice from hkPresent (NOT directX::getDevice() alone) MUST be
    init()'s primary source (review amendment 6). Guard: if pDevice is null or GetCreationParameters fails or
    hFocusWindow is null, setup() early-returns without installing (no overlay rather than a crash).
    Do NOT move hkReset's ImGui_ImplDX9_InvalidateDeviceObjects/CreateDeviceObjects (directx9.cpp:373/375) —
    they stay in the DX9 tier (Pitfall 3). NEVER add ->Reset(/.Reset(). OUT OF SCOPE: no D3D11 detection, no
    resize-behavior change, no detour relocation, no hkReset seam routing. After building,
    `git checkout -- UtinniCoreDotNet/Generated/UtinniCore.cs`.
  </action>
  <verify>
    <automated>"D:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe" Utinni.sln /m /p:Configuration=Release /p:Platform=x86 exits 0 (the structural truth — zero DX9 symbols — is the Task 2 D-06 gate, the real acceptance check; raw grep is comment-noisy)</automated>
  </verify>
  <acceptance_criteria>
    - MSBuild `Utinni.sln /p:Configuration=Release /p:Platform=x86` exits 0 (the carve compiles + links).
    - `grep -c 'render_backend::get()->' UtinniCore/swg/ui/imgui_impl.cpp` >= 5 (the five seam dispatch sites: newFrame, renderDrawData, sceneDepthTexture, sceneColorTexture, sceneDepthStage/set) — the authoritative presence check is the Task 2 D-06 seam-dispatch assertion (review amendment 8).
    - Each per-frame seam dispatch is null-guarded: `grep -c 'if (auto\* b = render_backend::get())' UtinniCore/swg/ui/imgui_impl.cpp` >= 2 (newFrame + renderDrawData guarded — review amendment 2).
    - setup() locked order present: `set(` / `dx9Singleton()->init(` / `ImGui_ImplWin32_Init` / `isSetup = true` appear in imgui_impl.cpp in the documented order, with set() before isSetup=true and Win32 init present (review amendments 2/9).
    - The !enableUi Tests-window block is gated on `sceneDepthTexture() != 0` with an adjacent code comment documenting it as an INTENTIONAL stricter dev-only behavior change (review amendment 4).
    - imgui_impl.h contains `setup(HWND` and does NOT contain `#include <d3d9.h>` or `IDirect3DDevice9`.
    - imgui_impl.cpp contains `Client::setSwgHwnd` (Issue #10 reparent preserved) and `ImGui_ImplWin32_Init` (Win32 init stays).
    - `grep -E '\->Reset\(|\.Reset\(' UtinniCore/swg/ui/imgui_impl.cpp UtinniCore/swg/graphics/render_backend.cpp` returns ZERO matches.
    - directx9.cpp hkPresent calls `imgui_impl::setup(` with an HWND and routes the live pDevice into init (review amendment 6); the InvalidateDeviceObjects/CreateDeviceObjects calls remain present in directx9.cpp (`grep -c 'ImGui_ImplDX9_InvalidateDeviceObjects' UtinniCore/swg/graphics/directx9.cpp` >= 1).
    - The WndProc subclass token `hkWndProcHandler` and the RT-space `AddMousePosEvent` token are still present in imgui_impl.cpp (Issue #11 + RT-space NOT swept out).
    - `git status --porcelain UtinniCoreDotNet/Generated/UtinniCore.cs` shows no staged change.
  </acceptance_criteria>
  <done>imgui_impl.{cpp,h} are DX9-binding-free and route the six former touch-points through the seam with null guards; setup takes HWND and runs the locked set->init(pDevice)->setSwgHwnd->Win32-init->WndProc->isSetup order; hkPresent passes the live device into init and the HWND into setup; the render()-before-setup() ordering is preserved; the no-Reset contract and Issue #11 / RT-space / gizmo logic are preserved verbatim; the Tests-window stricter guard is documented; the solution builds Release/x86.</done>
</task>

<task type="auto">
  <name>Task 2: D-06 API-neutrality source gate (extended tokens + seam-dispatch-presence) + SourceGateUtil.h</name>
  <files>UtinniCore.Tests/Graphics/SourceGateUtil.h, UtinniCore.Tests/Graphics/ImguiApiNeutralityTests.cpp, UtinniCore.Tests/UtinniCore.Tests.vcxproj</files>
  <read_first>
    - UtinniCore.Tests/Graphics/NoDeviceResetTests.cpp lines 63-170 (repoRootFromThisFile, readFile, stripComments — the helpers to COPY into SourceGateUtil.h; do NOT modify this file) + 174-188 (the planted-in-comment self-check SECTION pattern)
    - .planning/phases/18-render-backend-seam-dx9backend/18-RESEARCH.md "D-06 API-neutrality source gate" code example + Pitfall 5 (gate on concrete symbol forms, never bare "D3D9")
    - .planning/phases/18-render-backend-seam-dx9backend/18-PATTERNS.md "ImguiApiNeutralityTests.cpp" + "SourceGateUtil.h" + "Phase-tagged vcxproj <ClCompile> registration" sections
    - .planning/phases/18-render-backend-seam-dx9backend/18-REVIEWS.md amendments 3 (extended token set), 8 (seam-dispatch-presence), 10 (do not refactor NoDeviceResetTests.cpp)
    - UtinniCore.Tests/UtinniCore.Tests.vcxproj lines 130-147 (NoDeviceResetTests.cpp + the RenderBackendSeamTests.cpp entry added in 18-01; phase-tagged comment convention)
  </read_first>
  <action>
    Create SourceGateUtil.h: copy the MIT header + `#pragma once`, then COPY `repoRootFromThisFile()`,
    `readFile()`, `stripComments()` (and a small `countSubstr`) from NoDeviceResetTests.cpp:63-170 into a
    `namespace source_gate`. Use this header for the NEW gate ONLY — leave NoDeviceResetTests.cpp's
    file-local helper copies INTACT and untouched (review amendment 10: zero churn to the proven [resid04]
    gate; do NOT include the new header there, do NOT lift its helpers out).
    Create ImguiApiNeutralityTests.cpp: MIT header + `#include <catch2/catch_all.hpp>` +
    `#include "SourceGateUtil.h"`. Add `TEST_CASE("D-06 imgui_impl is fully DX9-API-neutral after the carve",
    "[rndr01][graphics]")` with:
    - a `SECTION("stripper hygiene self-check")` planting `IDirect3DDevice9` and `#include <d3d9.h>` inside
      comments and asserting the stripped count is 0 while the raw count is non-zero (proves the stripper does
      real work — mirror NoDeviceResetTests.cpp:174-188);
    - a `SECTION("imgui_impl.cpp")` asserting EACH of the EXTENDED token set counts to 0 after comment-strip
      (review amendment 3): `#include <d3d9.h>`, `#include <imgui_impl_dx9.h>` (and the `imgui_impl_dx9`
      symbol form), `#include "swg/graphics/directx9.h"` (and the `<...>` angle-bracket variant),
      `IDirect3DDevice9`, `LPDIRECT3DDEVICE9`, `LPDIRECT3D`, `D3DDEVICE_CREATION_PARAMETERS`, `ImGui_ImplDX9_`,
      `directX::`;
    - a `SECTION("imgui_impl.h")` asserting the same extended set counts to 0;
    - a `SECTION("seam dispatch present")` asserting `countSubstr(stripComments(imgui_impl.cpp), "render_backend::get()->") >= 5`
      (review amendment 8 — proves the carve actually wired the seam in; D-06 absence + this presence together
      prove the carve, not just empty stubs).
    Gate ONLY on these concrete symbol forms — NEVER the bare string "D3D9"/"DX9" (comments at imgui_impl.cpp
    52-55, 422-446 mention D3D9/Reset; Pitfall 5). Strip comments before counting; the Catch2 gate (not a raw
    grep) is the real acceptance check.
    Edit UtinniCore.Tests/UtinniCore.Tests.vcxproj: add a phase-tagged
    `<!-- Phase 18 / RNDR-01 D-06: imgui_impl DX9-API-neutrality source gate. -->` comment then
    `<ClCompile Include="Graphics\ImguiApiNeutralityTests.cpp" />` next to the NoDeviceResetTests.cpp /
    RenderBackendSeamTests.cpp entries (~line 135). SourceGateUtil.h is header-only — if it needs an explicit
    `<ClInclude>`, add one next to the test entries; otherwise it is pulled by #include. After building,
    `git checkout -- UtinniCoreDotNet/Generated/UtinniCore.cs`.
  </action>
  <verify>
    <automated>"D:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe" Utinni.sln /m /p:Configuration=Release /p:Platform=x86 exits 0, then bin\Release\UtinniCore.Tests.exe "[rndr01],[resid04]" passes (D-06 extended gate + seam-presence + D-07 seam test + the existing no-Reset gate all green)</automated>
  </verify>
  <acceptance_criteria>
    - `bin\Release\UtinniCore.Tests.exe "[rndr01]"` exits 0 with the D-06 gate case passing (imgui_impl.{cpp,h} contain zero DX9 symbols across the EXTENDED token set).
    - The D-06 gate asserts the EXTENDED token set: `grep -c 'imgui_impl_dx9' UtinniCore.Tests/Graphics/ImguiApiNeutralityTests.cpp` >= 1 AND `grep -c 'D3DDEVICE_CREATION_PARAMETERS' UtinniCore.Tests/Graphics/ImguiApiNeutralityTests.cpp` >= 1 AND `grep -c 'directx9.h' UtinniCore.Tests/Graphics/ImguiApiNeutralityTests.cpp` >= 1 (review amendment 3).
    - The D-06 gate asserts seam-dispatch presence: `grep -c 'render_backend::get()->' UtinniCore.Tests/Graphics/ImguiApiNeutralityTests.cpp` >= 1 and the assertion requires >= 5 (review amendment 8).
    - `bin\Release\UtinniCore.Tests.exe "[resid04]"` still passes (NoDeviceResetTests.cpp NOT broken — and `git diff --stat UtinniCore.Tests/Graphics/NoDeviceResetTests.cpp` shows ZERO changes; review amendment 10).
    - ImguiApiNeutralityTests.cpp contains a stripper self-check SECTION; `grep -c '"D3D9"' UtinniCore.Tests/Graphics/ImguiApiNeutralityTests.cpp` shows the gate does NOT assert against the bare string "D3D9".
    - SourceGateUtil.h contains `stripComments` and `repoRootFromThisFile`; the UtinniCore.Tests.vcxproj references `ImguiApiNeutralityTests.cpp`.
    - `git status --porcelain UtinniCoreDotNet/Generated/UtinniCore.cs` shows no staged change.
  </acceptance_criteria>
  <done>The D-06 structural gate passes on the EXTENDED token set (and would fail the build if any DX9 include/symbol — including imgui_impl_dx9.h, directx9.h, or D3DDEVICE_CREATION_PARAMETERS — reappeared in imgui_impl), proves the seam was actually wired in via the >=5 dispatch-presence assertion, uses its own copied helpers in SourceGateUtil.h without touching the existing [resid04] gate, and gates on concrete symbol forms only.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: D-08 maintainer live-smoke (final RNDR-01 acceptance)</name>
  <what-built>
    The render-backend seam carve is complete and CI-green: the ImGui overlay now renders through the
    IRenderBackend vtable (Dx9Backend wrapping directX:: verbatim, init()-driven from setup() in the locked
    Win32-before-DX9 order), imgui_impl is fully DX9-API-neutral across the extended token set (D-06 gate
    green) and provably dispatches through the seam (>=5 sites), and the D-07 mock-dispatch + [resid04]
    no-Reset gates pass. CI cannot inject into a live SWG.exe, so the final RNDR-01 acceptance is this
    maintainer-only live-smoke (D-08).
  </what-built>
  <how-to-verify>
    1. Build Release/x86 if not already current: MSBuild `Utinni.sln /p:Configuration=Release /p:Platform=x86`.
    2. Inject `UtinniCore.dll` into a live 32-bit SWG session (SWGEmu or Restoration) as usual.
    3. Confirm the FIRST overlay open after injection RENDERS (the internal UI / gizmo draws on top of the
       3D scene) — this is the most likely place a setup-order/null-backend bug would surface.
    4. Confirm input works: mouse moves the cursor in render-target space (no right-edge offset regression
       beyond the known RESID-04 deadzone), keyboard reaches the overlay.
    5. Confirm Issue #11 chat-context routing still works: pressing Enter in-game opens chat (openChat),
       not the input-mode chatEnter — verify via TJT's chat-command parser path.
    6. Trigger a TJT-driven scene change AND the FIRST frame after a scene load; confirm NO crash and NO
       device Reset (landing naked after a scene change is the documented baseline, NOT a regression).
    7. Trigger a window resize; confirm NO crash and NO device Reset.
    8. DEV-CONFIG check (review amendment 4): if you run with `enableInternalUi=false`, confirm the Tests
       window + depth-stage slider behave as expected under the new stricter `sceneDepthTexture() != 0`
       guard (the block now appears only when a live depth+color texture exists — an intentional change).
       If you only run the shipped Release `enableInternalUi=true`, this block is skipped and unaffected.
    9. Confirm the overlay behavior is otherwise INDISTINGUISHABLE from pre-carve (this is a pure refactor
       apart from the documented dev-only Tests-window guard).
  </how-to-verify>
  <resume-signal>Type "approved" (overlay renders + input + Issue #11 routing all work, no Reset/crash, dev-config Tests-window behavior acceptable), or describe the observed regression.</resume-signal>
</task>

</tasks>

<threat_model>
This phase introduces NO new external attack surface. The only trust boundary is the injected DLL ↔ the
live third-party SWG D3D9 render thread; this carve moves the six DX9 touch-points behind the seam without
adding any new input/parse/network surface. The threats are regression/destabilization classes the carve
could introduce.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UtinniCore.dll → SWG's D3D9 device | Foreign device owned by SWG.exe; never Reset / destabilize it (no-Reset/Present-stretch contract). In-process injected code into a live third-party render thread; no new attack surface introduced. |
| Per-frame render-thread hot path | Allocator-sensitive (R-H crash class); seam dispatch must stay heap-free; null-guarded so a not-yet-installed backend never deref-crashes the render thread |
| imgui_impl WndProc subclass → SWG input | Issue #11 chat-context routing + RT-space input map must survive the carve unchanged |
| imgui_impl::setup signature | Non-UTINNI_API internal; changing to setup(HWND) is ABI-safe (no managed/plugin consumer) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Tampering / DoS | the carve introduces a Reset or destabilizes the device | mitigate | onPreResize/onPostResize stay no-ops (Plan 01); acceptance gate `grep -E '->Reset\(|\.Reset\(' imgui_impl.cpp render_backend.cpp` == 0; hkReset's Invalidate/Create stay in directx9.cpp; NoDeviceResetTests.cpp [resid04] continues to gate (untouched); live-smoke steps 6/7 confirm no Reset/crash through scene change + resize |
| T-18-06 | DoS (input regression) | Issue #11 chat-context routing / RT-space map accidentally swept out | mitigate | Pitfall 4 — WndProc subclass + AddMousePosEvent explicitly NOT moved; acceptance gate asserts hkWndProcHandler + AddMousePosEvent tokens still present in imgui_impl.cpp; live-smoke step 4/5 confirms input + Issue #11 routing |
| T-18-07 | DoS (null backend / null device / first-frame race) | setup() HWND carve + first-frame seam dispatch | mitigate | Locked setup() order: set() BEFORE isSetup=true so no seam call site fires with a null backend through the isSetup gate; every per-frame dispatch is `if (auto* b = get())` guarded (review amendment 2); init() receives the live pDevice from hkPresent (primary) with directX::getDevice() fallback and a null-device early-return (review amendment 6); Win32-before-DX9 init order locked (review amendment 9); render()-before-setup() ordering preserved by the isSetup gate; live-smoke step 3/6 confirms first-overlay-open + first-frame-after-scene-load render |
| T-18-08 | (binary compat) | exported symbol signature change → MissingMethodException at MEF compose | accept | imgui_impl::setup is NOT UTINNI_API-exported (verified imgui_impl.h:38, plain extern, called only from directx9.cpp) — its signature change is ABI-safe; no exported directX:: signature changes; no TJT/plugin rebuild needed |
| T-18-09 | DoS (allocator fragmentation) | per-frame seam dispatch on the carved render path | mitigate | Plain virtual call through Plan-01 static-storage singleton; no new container/std::function added to imgui_impl render path |

After mitigations, no threat rises to `high`: every regression class has a CI-enforceable structural gate
(D-06 extended source gate + seam-dispatch-presence, [resid04] no-Reset gate, D-07 seam dispatch) PLUS the
maintainer live-smoke as the final RNDR-01 acceptance. T-18-08 is accepted because setup is provably
non-exported (ABI-safe). The live D3D9 behavior is preserved, not changed (except the documented dev-only
Tests-window guard).
</threat_model>

<verification>
- MSBuild `Utinni.sln /p:Configuration=Release /p:Platform=x86` exits 0 (build INLINE on main tree — worktrees OFF).
- `bin\Release\UtinniCore.Tests.exe "[rndr01]"` passes (D-06 extended API-neutrality gate + seam-dispatch presence + D-07 seam dispatch green).
- `bin\Release\UtinniCore.Tests.exe "[resid04]"` passes (no-Reset contract preserved; NoDeviceResetTests.cpp untouched).
- `dotnet test --no-build` PreservationAudit lane green (CON-* structural Facts unbroken).
- D-08 maintainer live-smoke: overlay renders + takes input + Issue #11 routing works in a live D3D9 session; first-overlay-open + first-frame-after-scene-load render; no Reset/crash through scene change + resize; dev-config Tests-window guard behaves as documented.
- `git status` shows no staged `Generated/UtinniCore.cs`.
</verification>

<success_criteria>
- imgui_impl.{cpp,h} are fully DX9-API-neutral (D-05 full purge, extended token set): zero <d3d9.h>, <imgui_impl_dx9.h>, "swg/graphics/directx9.h", IDirect3DDevice9, LPDIRECT3DDEVICE9/LPDIRECT3D, D3DDEVICE_CREATION_PARAMETERS, ImGui_ImplDX9_, directX:: — enforced by the D-06 gate.
- The six former DX9 touch-points route through render_backend::get() (null-guarded); setup takes HWND and runs the locked set->init(pDevice)->setSwgHwnd->Win32-init->WndProc->isSetup order; hkPresent passes the live device into init and the HWND into setup.
- DrawColorWindow + the !enableUi stage slider survive via the A2 sceneColorTexture/sceneDepthStage accessors; the Tests-window block uses the documented stricter sceneDepthTexture()!=0 guard (intentional dev-only change).
- The API-neutral overlay logic (Issue #11 routing, RT-space map, gizmo, renderCallbacks) is single-sourced and untouched; the no-Reset contract is verbatim; the render()-before-setup() ordering is preserved.
- The D-06 (extended + seam-presence) + D-07 + [resid04] gates are CI-green; the maintainer live-smoke confirms live RNDR-01 acceptance.
- OUT OF SCOPE confirmed absent: no D3D11 detection, no resize-behavior change, no detour relocation, no hkReset seam routing, no NoDeviceResetTests.cpp refactor.
</success_criteria>

<output>
Create `.planning/phases/18-render-backend-seam-dx9backend/18-02-SUMMARY.md` when done
</output>
</content>
