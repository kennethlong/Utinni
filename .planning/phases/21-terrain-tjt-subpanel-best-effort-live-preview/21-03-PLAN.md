---
phase: 21-terrain-tjt-subpanel-best-effort-live-preview
plan: 03
type: execute
wave: 3
depends_on: ["21-01", "21-02"]
files_modified:
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/SubPanels/TerrainSubPanel.cs"
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/SubPanels/TerrainSubPanel.Designer.cs"
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs"
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs"
autonomous: true
requirements: [PROD-W2-TRN-05]

must_haves:
  truths:
    - "A docked TerrainSubPanel (IEditorPlugin surface) ships inside TJT, registered via the existing SubPanelContainer(\"Controls\", ...) array — NOT by widening GetSubPanels()."
    - "The SubPanel opens a .trn from the TRE Browser (read-only → Save As Override) and from a loose override (direct), launching the roomy FormTerrainEditor host (singleton, hide-not-dispose)."
    - "The TerrainSubPanel ctor is guarded against MEF silent-reject; the undo seam is null-checked."
  artifacts:
    - path: "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/SubPanels/TerrainSubPanel.cs"
      provides: "Docked entry SubPanel: banner + Open/Open-Override + scene-availability gating, launches FormTerrainEditor"
      contains: "FormTerrainEditor"
    - path: "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs"
      provides: "TerrainSubPanel added to the SubPanelContainer(\"Controls\", ...) array"
      contains: "TerrainSubPanel"
  key_links:
    - from: "Plugin.cs"
      to: "UI/SubPanels/TerrainSubPanel.cs"
      via: "new TerrainSubPanel(this, hotkeyManager, ini) in the SubPanelContainer(\"Controls\", ...) array"
      pattern: "new TerrainSubPanel"
    - from: "FormTreBrowser.cs"
      to: "TerrainSubPanel/FormTerrainEditor open entry"
      via: "\"Open in Terrain Editor\" context-menu hand-off on a .trn entry"
      pattern: "Open in Terrain Editor"
    - from: "TerrainSubPanel.cs"
      to: "UI/Forms/FormTerrainEditor.cs"
      via: "singleton launch (hide-not-dispose) + OpenFromTreEntry/OpenLooseOverride"
      pattern: "FormTerrainEditor"
---

<objective>
Ship the D-01 docked entry point: a thin `TerrainSubPanel` (banner + Open / Open Override + scene-aware
button gating that fits the 417px column) that launches the roomy `FormTerrainEditor` from Plan 02,
register it via the existing `SubPanelContainer("Controls", ...)` array in `Plugin.cs` (NOT by widening
`GetSubPanels()` — CON-M-01/02), and add the "Open in Terrain Editor" hand-off to the TRE Browser.

Purpose: This wires the editor into TJT's compose surface and both D-08 entry points (TRE read-only →
Save As Override, and direct loose-override open), completing the user-reachable path. It is the last
autonomous plan; Plan 04 is the maintainer live-smoke.

Output: `TerrainSubPanel.cs` (+ Designer), the `Plugin.cs` registration, and the `FormTreBrowser` menu item.
</objective>

<planner_decisions>
- **Registration via `GetStandalonePanels()`'s `SubPanelContainer("Controls", ...)` array**, exactly where
  `ScenePanel`/`SnapshotPanel` live (Plugin.cs:133-141). `GetSubPanels()` STAYS returning null — the MEF
  SPI is NOT widened (CON-M-01/02, STAB-04), matching every shipped SubPanel.
- **TRE-Browser hand-off target = the launched `FormTerrainEditor`** (consistent with the D-02
  thin-SubPanel-launches-Form choice). The hand-off reaches the editor via the SubPanel instance in the
  plugin's `GetStandalonePanels()` list (the docked SubPanel owns the singleton Form), mirroring the
  "find the editor and call its open entry" idiom.
</planner_decisions>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-terrain-tjt-subpanel-best-effort-live-preview/21-RESEARCH.md
@.planning/phases/21-terrain-tjt-subpanel-best-effort-live-preview/21-PATTERNS.md
@.planning/phases/21-terrain-tjt-subpanel-best-effort-live-preview/21-UI-SPEC.md

<interfaces>
<!-- Plug-in host contract + precedent surfaces -->
SubPanel base ctor: SubPanel(string name)  // sets collapsible header text; PINS width to 417px (OnResize)
TerrainSubPanel(IEditorPlugin editorPlugin, HotkeyManager hotkeyManager, UtINI ini) : base("Terrain")
Plugin.cs GetStandalonePanels() returns: panels = [ new SubPanelContainer("Controls", new SubPanel[]{ ScenePanel, SnapshotPanel, PlayerPanel, FreeCamPanel, GraphicsPanel, MiscPanel }) ]
Plugin.cs GetSubPanels() => null   // DO NOT widen
IEditorPlugin.Undo/Redo/ClearUndoStack/AddUndoCommand  // null until FormMain wires; null-check every call
FormTerrainEditor(IEditorPlugin) + .OpenFromTreEntry(byte[] payload, string archivePath, string logicalPath) + .OpenLooseOverride(string loosePath)   // Plan 02
SnapshotPanel singleton-launch idiom (SnapshotPanel.cs:120-139): hide-not-dispose, Activate-if-visible-else-Show
SnapshotPanel.UpdateSceneAvailability(bool) (SnapshotPanel.cs:211-229): idempotent transition gate
FormTreBrowser BuildTvTreContextMenu / OnTvTreContextMenuOpening (FormTreBrowser.cs:146-199): the .trn-entry context hand-off
UtinniCoreDotNet.UI.Theme.Colors.*
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: TerrainSubPanel docked entry (fits 417px) — banner, Open / Open Override, scene-availability gating, singleton FormTerrainEditor launch, MEF-safe ctor + null-checked undo seam</name>
  <files>D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/SubPanels/TerrainSubPanel.cs, D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/SubPanels/TerrainSubPanel.Designer.cs</files>
  <read_first>
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/SubPanels/SnapshotPanel.cs (the docked-SubPanel precedent: ctor : base("Snapshot") at :50,67-82; singleton companion-window launch at :120-139; UpdateSceneAvailability at :211-229; detach/reattach idiom at :191-195)
    - UtinniCoreDotNet/UI/Controls/SubPanel.cs (the 417px width pin at :36,63 enforced in OnResize — the banner+buttons MUST fit this)
    - UtinniCoreDotNet/PluginFramework/IEditorPlugin.cs (the undo seam — null until FormMain wires)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormParticleEditor.cs (the loose-file open picker path at :311 + the Game.IsRunning try/catch button gate)
    - .planning/phases/21-terrain-tjt-subpanel-best-effort-live-preview/21-UI-SPEC.md (the "Open Terrain Override…" copy, empty-state copy, the DEC-A3 footer, the 417px form-factor lock)
  </read_first>
  <action>
    Create `TJT.UI.SubPanels.TerrainSubPanel : SubPanel` with ctor
    `TerrainSubPanel(IEditorPlugin editorPlugin, HotkeyManager hotkeyManager, UtINI ini) : base("Terrain")`
    (copy the SnapshotPanel ctor shape; store `editorPlugin` for the undo seam). The docked content must
    fit the 417px column: a `Dock.Top` banner (`UtinniLabel`), an action row with "Open in Terrain
    Editor" intent surfaced as an "Open Terrain Override…" file-picker button (loose `.trn` direct open)
    plus a short hint that TRE-sourced opens come from the TRE Browser, and a dimmed DEC-A3 footer. The
    "Open Terrain Override…" button shows a `.trn` file picker, reads the bytes, and routes to the
    launched host. Implement the singleton launch (hide-not-dispose) of `FormTerrainEditor` exactly like
    SnapshotPanel's companion-window pattern (`if (form == null || form.IsDisposed) form = new
    FormTerrainEditor(editorPlugin); if (form.Visible) form.Activate(); else form.Show();`) and call its
    `OpenLooseOverride(path)` (or `OpenFromTreEntry(...)` from the TRE hand-off in Task 3). Add a public
    `OpenFromTre(byte[] payload, string archivePath, string logicalPath)` that launches the host and
    forwards to `FormTerrainEditor.OpenFromTreEntry(...)` (the entry point Task 3's hand-off calls).
    Implement `UpdateSceneAvailability(bool isSceneActive)` (idempotent on transition) to gate any
    live-only controls, mirroring SnapshotPanel; read `Game.IsRunning` only inside try/catch (Pitfall 4).
    Wire the whole ctor + control build inside a try/catch and null-check every undo-seam call
    (`editorPlugin.Undo?.Invoke()`) — a throwing SubPanel ctor silently drops the WHOLE IEditorPlugin
    from MEF compose (D-09 / Pitfall 8). Theme via `Colors.*`; base 8.25pt, Bold only on the banner.
  </action>
  <verify>
    <automated>cd "/d/Code/UtinniPlugins/The Jawa Toolbox" && "$MSBUILD" TheJawaToolbox.sln /p:Configuration=Release /p:Platform=x86 /t:Build /v:m</automated>
  </verify>
  <acceptance_criteria>
    - `TerrainSubPanel : SubPanel` with ctor signature `(IEditorPlugin, HotkeyManager, UtINI) : base("Terrain")`; compiles into `TheJawaToolboxDotNet`.
    - The launch uses the singleton hide-not-dispose pattern (grep confirms `IsDisposed` guard + `Activate`/`Show`), not a `new ... .ShowDialog()` per click.
    - The ctor + control build is wrapped in try/catch; every undo-seam call is null-conditional (grep shows `?.Invoke()` only).
    - `Game.IsRunning` is read only inside try/catch (grep confirms no bare `Game.IsRunning` outside a try).
    - No hard-coded color literals except `Color.Red` for error status (grep filtered with `grep -v '^ *//'`).
  </acceptance_criteria>
  <done>The thin docked entry point launches the roomy host, handles the direct loose-override open, gates on scene availability, and is MEF-safe + undo-seam-null-safe.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Register TerrainSubPanel in Plugin.cs (SubPanelContainer "Controls" array, NOT GetSubPanels) + add "Open in Terrain Editor" hand-off to FormTreBrowser</name>
  <files>D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs, D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs</files>
  <read_first>
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs (the SubPanelContainer("Controls", new SubPanel[]{...}) array at :133-141; GetSubPanels()=>null at :178; the try/catch form-registration idiom at :62-69)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs (BuildTvTreContextMenu :146-174; OnTvTreContextMenuOpening visibility gating :189-199; the existing "Open in <Editor>" hand-off that finds the editor + calls its open entry)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/SubPanels/TerrainSubPanel.cs (Task 1 — the OpenFromTre entry to call)
    - .planning/phases/21-terrain-tjt-subpanel-best-effort-live-preview/21-UI-SPEC.md (the "Open in Terrain Editor" context-action copy + the .trn-only enablement)
  </read_first>
  <action>
    In `Plugin.cs`, add `new TerrainSubPanel(this, hotkeyManager, ini)` to the existing
    `SubPanelContainer("Controls", new SubPanel[]{ ... })` array (after `SnapshotPanel`), keeping
    `GetSubPanels()` returning null (do NOT widen — CON-M-01/02). Because a throwing SubPanel ctor in that
    array would fail the whole compose, ensure the SubPanel's own ctor try/catch (Task 1) is the guard;
    do not wrap the array element in a statement-level try (the array literal can't host one) — rely on
    the Task-1 ctor guard, matching the established SubPanel registration. In `FormTreBrowser.cs`, add an
    "Open in Terrain Editor" `ToolStripMenuItem` to `BuildTvTreContextMenu` (copy the Particle-editor
    entry idiom), gate its visibility in `OnTvTreContextMenuOpening` to a resolvable `.trn` entry, and on
    click resolve the editor by locating the `TerrainSubPanel` instance in the plugin's
    `GetStandalonePanels()` "Controls" container and calling its `OpenFromTre(payload, archivePath,
    logicalPath)` (Task 1). The first commit from a TRE source is "Save As Override…" (D-08 — the host
    enforces this; the TRE is never edited in place).
  </action>
  <verify>
    <automated>cd "/d/Code/UtinniPlugins/The Jawa Toolbox" && "$MSBUILD" TheJawaToolbox.sln /p:Configuration=Release /p:Platform=x86 /t:Build /v:m</automated>
  </verify>
  <acceptance_criteria>
    - `Plugin.cs` adds `new TerrainSubPanel(this, hotkeyManager, ini)` to the `SubPanelContainer("Controls", ...)` array; `GetSubPanels()` still returns null (grep confirms `return null` unchanged).
    - `FormTreBrowser.cs` has an "Open in Terrain Editor" menu item gated to `.trn` entries (grep confirms the literal + the visibility gate).
    - The hand-off reaches the `TerrainSubPanel` via `GetStandalonePanels()` (the "Controls" container), not via the forms list (consistent with D-02).
    - `TheJawaToolbox.sln` builds clean (VS2026 MSBuild x86/Release).
  </acceptance_criteria>
  <done>The editor is reachable from the TJT panel column and from the TRE Browser; the MEF SPI is not widened; both D-08 entry points work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MEF compose → plugin surface | A throwing SubPanel/child ctor crosses into the compose pipeline and can silently drop the whole IEditorPlugin. |
| TRE Browser → editor | A `.trn` payload + provenance crosses from the (read-only) TRE Browser into the editor open path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-mef-reject | Repudiation / silent failure | `TerrainSubPanel` ctor in the SubPanelContainer array | mitigate | Whole ctor + control build in try/catch (D-09); the array element relies on that ctor guard — a partial-load surfaces a state panel, never throws the compose. |
| T-21-tre-inplace | Tampering | TRE-sourced open | mitigate | TRE opened read-only; first commit is "Save As Override…" (host-enforced, D-08); the source archive is never written. |
| T-21-spi-widen | Tampering (invariant) | `GetSubPanels()` | mitigate | Stays `return null`; registration via the existing `SubPanelContainer("Controls", ...)` (CON-M-01/02 not violated). |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero packages installed (RESEARCH audit: none). |
</threat_model>

<verification>
- `TheJawaToolbox.sln` builds clean (VS2026 MSBuild x86/Release; NOT `dotnet build`).
- Grep-asserted invariants: `GetSubPanels()` still null; `TerrainSubPanel` in the "Controls" array; "Open in Terrain Editor" gated to `.trn`; singleton launch; null-safe undo seam.
- Plan 01 framework suites stay green (no framework change here).
- Live/visual confirmation deferred to Plan 04 (D-07 maintainer smoke).
</verification>

<success_criteria>
- A docked `TerrainSubPanel` ships inside TJT via the existing SubPanelContainer array (MEF SPI unchanged).
- Both D-08 entry points work: TRE read-only → Save As Override, and direct loose-override open.
- The SubPanel launches the roomy `FormTerrainEditor` singleton; ctor MEF-safe; undo seam null-checked.
</success_criteria>

<output>
Create `.planning/phases/21-terrain-tjt-subpanel-best-effort-live-preview/21-03-SUMMARY.md` when done.
Cross-repo commit on UtinniPlugins — no human checkpoint (standing authority; live smoke is Plan 04).
</output>
