---
phase: 21-terrain-tjt-subpanel-best-effort-live-preview
plan: 04
type: execute
wave: 4
depends_on: ["21-01", "21-02", "21-03"]
files_modified:
  - ".planning/phases/21-terrain-tjt-subpanel-best-effort-live-preview/21-SMOKE-LOG.md"
  - UtinniCoreDotNet/Saving/TerrainReloadCandor.cs
autonomous: false
requirements: [PROD-W2-TRN-05]

must_haves:
  truths:
    - "A maintainer has observed, in a live injected SWG session, whether an in-session edited .trn visibly re-reads via GroundScene.ReloadTerrain immediately, on next scene change, or only on relog."
    - "The shipped reload-tier copy reflects the OBSERVED disposition: the ReloadedTerrain-tier copy is upgraded to \"Reloaded (terrain)\" ONLY if in-session re-read was observed; otherwise the honest PendingNextSceneChange default stands."
    - "The observation + its disposition are recorded in a SMOKE-LOG with the searchPath prerequisite noted."
  artifacts:
    - path: ".planning/phases/21-terrain-tjt-subpanel-best-effort-live-preview/21-SMOKE-LOG.md"
      provides: "The maintainer live-smoke observation + the final candor disposition"
  key_links:
    - from: "21-SMOKE-LOG.md"
      to: "UtinniCoreDotNet/Saving/TerrainReloadCandor.cs"
      via: "the observed disposition sets TerrainReloadCandor.LivePreviewObserved"
      pattern: "LivePreviewObserved"
---

<objective>
Resolve the ONE genuine unknown of this phase (D-07): does the live SWG client's
`GroundScene::ReloadTerrain` visibly re-read a procedurally-edited `.trn` in-session, or only on a
scene change / relog? This is statically unknowable (the native body is not in our source) and is the
lone non-automatable gate. The maintainer observes it in a live injected session and the build's reload
candor copy is set to match what was observed — never over-promising.

Purpose: Until observed, the build ships the honest `PendingNextSceneChange` copy for the
`ReloadedTerrain` tier (Plan 01 `TerrainReloadCandor.LivePreviewObserved == false`). This plan either
confirms that honest default or upgrades it to "live" based on a real observation. Precedent:
`phase10-stringtable-sc3-live-reload-residual` (observe-then-honestly-label).

Output: a `21-SMOKE-LOG.md` recording the observation + disposition, and (only if in-session re-read was
observed) the one-line flip of `TerrainReloadCandor.LivePreviewObserved` to true.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-terrain-tjt-subpanel-best-effort-live-preview/21-RESEARCH.md
@.planning/phases/21-terrain-tjt-subpanel-best-effort-live-preview/21-VALIDATION.md
@.planning/todos/pending/phase10-stringtable-sc3-live-reload-residual.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Maintainer live-smoke — observe in-session terrain reload disposition (D-07)</name>
  <files>.planning/phases/21-terrain-tjt-subpanel-best-effort-live-preview/21-SMOKE-LOG.md</files>
  <read_first>
    - .planning/phases/21-terrain-tjt-subpanel-best-effort-live-preview/21-RESEARCH.md (Open Questions §D-07 + Runtime State Inventory: the disabled loose searchPath prerequisite)
    - .planning/todos/pending/phase10-stringtable-sc3-live-reload-residual.md (the observe-then-honestly-label precedent + the disabled-searchPath / useSwgOverrideCfg=false blocker)
  </read_first>
  <action>
    MAINTAINER-ONLY live smoke (no headless/subagent path exists — AGENTS.md build-wave constraint). The
    maintainer injects a live SWG session, opens a planet `.trn` in the Terrain editor, edits a
    fixed-length leaf, Saves + manual-Previews, and observes whether the terrain visibly re-reads
    in-session / on scene change / on relog. Record the disposition in `21-SMOKE-LOG.md`. See
    <how-to-verify> for the exact steps and the disabled-searchPath prerequisite. Task 2 consumes the
    recorded disposition; do NOT label preview "live" without an observed in-session re-read (D-07).
  </action>
  <what-built>
    Plans 01-03 shipped the full Terrain editor: in-proc byte-exact save (parity-tested vs apply-save-trn),
    a tree+field editor host, the docked SubPanel entry, TRE + loose-override open, and the on-save +
    manual-Preview reload routing through the existing `ClientReloadDispatcher` → `GroundScene.Get().ReloadTerrain()`.
    The reload-tier candor copy currently ships the honest D-07 default (`PendingNextSceneChange` wording for
    the `ReloadedTerrain` tier).
  </what-built>
  <how-to-verify>
    PREREQUISITE (note explicitly): the maintainer's loose-override `searchPath` is currently DISABLED in
    the client cfg (`swgemu_live.cfg` priority-27 searchPath commented out; `ut.ini useSwgOverrideCfg=false`)
    per the 2026-06-12 phantom-walk mitigation. Without re-enabling it, the client will NOT pick up a saved
    loose `.trn` and the smoke observes nothing. The maintainer decides whether to re-enable for this smoke.

    With searchPath enabled:
    1. Build UtinniCore + TheJawaToolboxDotNet (VS2026 MSBuild, x86/Release) and inject into a live SWG
       session; load a planet scene via the TJT chat-command parser path.
    2. Open that planet's `.trn` in the Terrain editor (from the TRE Browser → Save As Override, or a loose
       override directly).
    3. Edit a fixed-length scalar/enum leaf (or toggle a layer active flag) and Save. Observe whether the
       terrain visibly changes: (a) immediately in-session, (b) only after a TJT-driven scene change, or
       (c) only after relog.
    4. Repeat using the manual Preview button (no disk commit path).
    5. Record in `21-SMOKE-LOG.md`: the observed disposition (a/b/c), whether searchPath was re-enabled,
       any crash/anomaly (there must be NO `0x0051fb0a` scene-change crash — D-06 guard), and the resulting
       honest copy decision (`ReloadedTerrain`-live vs `PendingNextSceneChange`-pending).
  </how-to-verify>
  <verify>
    <automated>MISSING — maintainer-only live smoke; no automatable command exists (no headless inject path). Disposition recorded in 21-SMOKE-LOG.md and consumed by Task 2.</automated>
  </verify>
  <acceptance_criteria>
    - `21-SMOKE-LOG.md` exists and records: disposition (a/b/c), searchPath state, crash observations, and the candor decision.
    - No `0x0051fb0a` (or any) scene-change crash was triggered by the save/preview dispatch (D-06 heap-free guard held).
    - The candor decision is explicit and matches the observation (never labels "live" without observing in-session re-read).
  </acceptance_criteria>
  <done>21-SMOKE-LOG.md records the observed disposition (live / pending / relog-only / deferred), the searchPath state, and any crash anomaly; no scene-change crash occurred.</done>
  <resume-signal>Type "observed: live" / "observed: pending" / "observed: relog-only" (plus any anomaly), or "deferred — searchPath stays disabled" to ship the honest PendingNextSceneChange default.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Set the shipped candor disposition from the observation</name>
  <files>UtinniCoreDotNet/Saving/TerrainReloadCandor.cs, .planning/phases/21-terrain-tjt-subpanel-best-effort-live-preview/21-SMOKE-LOG.md</files>
  <read_first>
    - UtinniCoreDotNet/Saving/TerrainReloadCandor.cs (Plan 01 — the LivePreviewObserved const guarding the ReloadedTerrain branch)
    - UtinniCoreDotNet.Tests/Saving/TerrainReloadCandorTests.cs (Plan 01 — the string-assert test to update if the disposition flips)
    - .planning/phases/21-terrain-tjt-subpanel-best-effort-live-preview/21-SMOKE-LOG.md (Task 1 — the recorded disposition)
  </read_first>
  <action>
    Based ONLY on the Task-1 observation recorded in the SMOKE-LOG: if in-session re-read was observed
    ("observed: live"), flip `TerrainReloadCandor.LivePreviewObserved` to `true` so the `ReloadedTerrain`
    tier renders "Reloaded (terrain)", and update the corresponding assertion in `TerrainReloadCandorTests`
    to expect "Reloaded (terrain)" for that tier. Otherwise (pending / relog-only / deferred), leave
    `LivePreviewObserved == false` — the honest `PendingNextSceneChange` copy stands; make no code change
    beyond confirming the SMOKE-LOG records the disposition. Either way, append the final disposition note
    to the SMOKE-LOG and reference the phase10 SC3 precedent. If the smoke was deferred (searchPath stays
    disabled), record the residual as a follow-up todo mirroring `phase10-stringtable-sc3-live-reload-residual`.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~TerrainReloadCandor"</automated>
  </verify>
  <acceptance_criteria>
    - `TerrainReloadCandor.LivePreviewObserved` and `TerrainReloadCandorTests` agree with the SMOKE-LOG disposition (live → "Reloaded (terrain)"; otherwise the PendingNextSceneChange copy).
    - `dotnet test --no-build` green on the candor tests.
    - The SMOKE-LOG contains the final disposition; if deferred, a residual todo is recorded.
  </acceptance_criteria>
  <done>The shipped reload candor reflects the observed (or honestly-deferred) disposition — no over-promise.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor save → live client | A saved loose `.trn` crosses into the running client's asset-resolution + terrain regen path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-over-promise | Repudiation (honesty) | shipped reload-tier copy | mitigate | The copy is set from a real observation; "live" wording requires observed in-session re-read, else the honest pending default ships (D-07). |
| T-21-scene-crash | Denial of Service | save/preview dispatch under a live scene | mitigate | The smoke explicitly checks for the `0x0051fb0a` scene-change crash; the dispatch is heap-free, one `AddMainLoopCall` per action (D-06 guard, verified live). |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero packages installed. |
</threat_model>

<verification>
- `21-SMOKE-LOG.md` records the observation + disposition with the searchPath prerequisite noted.
- `dotnet test UtinniCoreDotNet.Tests --no-build --filter TerrainReloadCandor` green and consistent with the disposition.
- No scene-change crash observed during the live save/preview.
</verification>

<success_criteria>
- The D-07 live-vs-pending disposition is observed (or honestly deferred) and recorded.
- The shipped candor copy matches the observation — never over-promising.
- The phase ships fully automation-verified except this one maintainer gate, exactly per the VALIDATION strategy.
</success_criteria>

<output>
Create `.planning/phases/21-terrain-tjt-subpanel-best-effort-live-preview/21-04-SUMMARY.md` when done.
If the smoke is deferred, record the residual todo under `.planning/todos/pending/` mirroring the phase10 SC3 precedent.
</output>
