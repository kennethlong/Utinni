---
phase: 21-terrain-tjt-subpanel-best-effort-live-preview
plan: 06
type: execute
wave: 2
depends_on: ["21-05"]
gap_closure: true
files_modified:
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/TerrainSaveTargets.cs"
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTerrainEditor.cs"
  - UtinniCoreDotNet.Tests/SavingTests/TerrainLooseOverridePathTests.cs
autonomous: true
requirements: [PROD-W2-TRN-05]

must_haves:
  truths:
    - "A terrain override for a TRE asset 'terrain/naboo.trn' resolves to a destination UNDER '<root>/loose/' (e.g. <root>/loose/terrain/naboo.trn), matching IFF/Datatable/STF/OT — NOT <root>/terrain/naboo.trn."
    - "The overwrite-confirm prediction (PredictOverridePath) points at the SAME loose/-composed destination the save writes, so File.Exists checks the real override."
    - "The composed path is byte-content-identical to what apply-save-trn --root <root> <loose/terrain/naboo.trn> would write (same single-source codec; only the destination differs)."
  artifacts:
    - path: "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/TerrainSaveTargets.cs"
      provides: "SaveLooseOverride threads a looseOverrideSubDir (default 'loose') and composes <root>/loose/<logical>"
      contains: "looseOverrideSubDir"
    - path: "UtinniCoreDotNet.Tests/SavingTests/TerrainLooseOverridePathTests.cs"
      provides: "A test asserting the resolved terrain-override destination is under <root>/loose/"
      contains: "loose"
  key_links:
    - from: "TerrainSaveTargets.SaveLooseOverride"
      to: "<root>/loose/<logical-asset-path>"
      via: "overrideBase = LooseOverridePath.Resolve(resolvedRoot, looseOverrideSubDir) then Resolve(overrideBase, relAssetPath)"
      pattern: "looseOverrideSubDir"
    - from: "FormTerrainEditor.PredictOverridePath"
      to: "the same loose/-composed destination"
      via: "the same two-step LooseOverridePath.Resolve composition the save uses"
      pattern: "PredictOverridePath"
---

<objective>
Close R2: `TerrainSaveTargets.SaveLooseOverride` resolves the destination as
`LooseOverridePath.Resolve(resolvedRoot, relAssetPath)` with NO `loose/` sub-directory, so a terrain override
for `terrain/naboo.trn` lands at `<root>/terrain/naboo.trn` instead of `<root>/loose/terrain/naboo.trn` like
every other editor (IFF/Datatable/STF/OT). The documented loose-`searchPath` convention (`<root>/loose`,
the one the phantom-walk mitigation toggles) therefore never covers terrain overrides — the 21-04 smoke had
to relocate the file by hand for the client to pick it up.

Purpose: make terrain overrides land on the documented loose searchPath like every other editor, with the
overwrite-confirm prediction pointing at the same destination.

Output: a `looseOverrideSubDir` (default `"loose"`) threaded through the save + the predict path using the
exact two-step composition `IffSaveTargets.SaveLooseOverride` uses, plus a framework-side test asserting the
destination is under `<root>/loose/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-terrain-tjt-subpanel-best-effort-live-preview/21-SMOKE-LOG.md
@.planning/todos/pending/phase21-terrain-override-loose-subdir.md

<interfaces>
<!-- The authoritative analog (IffSaveTargets) and the resolver contract. Use directly. -->

LooseOverridePath.Resolve (UtinniCoreDotNet.PathContainment/LooseOverridePath.cs) — IMPORTANT:
  - public static string Resolve(string resolvedRoot, string relAssetPath)  // TWO args only.
  - There is NO 3-arg overload taking a looseOverrideSubDir. The loose/ subdir is composed by CALLING
    Resolve TWICE: first to make the override base (root -> root/loose), then to place the asset under it.
  - Rejects rooted paths, '..' segments, escapes; OrdinalIgnoreCase StartsWith containment gate.

IffSaveTargets.SaveLooseOverride (UtinniPlugins .../Saving/IffSaveTargets.cs ~line 88) — THE ANALOG:
  - signature: SaveLooseOverride(MutableIffDocument doc, OpenSource source, string resolvedRoot, string looseOverrideSubDir)
  - composes: overrideBase = string.IsNullOrEmpty(looseOverrideSubDir) ? resolvedRoot : LooseOverridePath.Resolve(resolvedRoot, looseOverrideSubDir);
              fullPath    = LooseOverridePath.Resolve(overrideBase, relAssetPath);
  - each Resolve in its own try/catch returning SaveResult.Failure("Invalid loose-override sub-directory: ...")
    / SaveResult.Failure("Invalid loose-override asset path: ...").

TerrainSaveTargets.SaveLooseOverride (UtinniPlugins .../Saving/TerrainSaveTargets.cs ~line 256) — R2 site:
  - current signature (NO subdir): SaveLooseOverride(TerrainDocument terrain, OpenSource source, string resolvedRoot,
    string stableLeafId, string tag, string version, string fieldName, string value)
  - current resolve: fullPath = LooseOverridePath.Resolve(resolvedRoot, relAssetPath);   // <-- omits loose/
  - relAssetPath derivation (TRE -> tre.LogicalPath; loose -> LogicalAssetPath.TryFromAbsolute or filename) is correct — keep it.

FormTerrainEditor (UtinniPlugins .../UI/Forms/FormTerrainEditor.cs) — caller + predict (R2 site):
  - call site ~line 1047: await TerrainSaveTargets.SaveLooseOverride(document, source, resolvedRoot, pendingEdit.StableLeafId, pendingEdit.Tag, pendingEdit.Version, pendingEdit.FieldName, pendingEdit.Value);
  - PredictOverridePath(resolvedRoot) ~line 1181: returns LooseOverridePath.Resolve(resolvedRoot, tre.LogicalPath);  // <-- also omits loose/
  - WritePreviewTemp uses Path.Combine(resolvedRoot, "loose_preview") — a SEPARATE preview temp dir; do NOT change it.

apply-save-trn (Utinni.Cli/Commands/ApplySaveTrnCommand.cs ~line 102) — byte-parity reference:
  - destPath = LooseOverridePath.Resolve(o.Root, o.RelAsset);  // takes relAsset AS-GIVEN, does NOT prepend loose/.
  - Byte-parity = file CONTENT identical (same single-source codec). The CLI achieves the loose/ destination by
    being INVOKED with relAsset = "loose/terrain/naboo.trn". So the editor's two-step composition produces the
    SAME absolute path the CLI would for that relAsset — content parity is already guaranteed by the shared codec.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread looseOverrideSubDir ("loose") through TerrainSaveTargets.SaveLooseOverride</name>
  <files>D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/TerrainSaveTargets.cs</files>
  <read_first>
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/TerrainSaveTargets.cs (SaveLooseOverride ~line 256, esp. the relAssetPath derivation + the single LooseOverridePath.Resolve at ~line 312)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/IffSaveTargets.cs (the SaveLooseOverride two-step composition ~lines 88-159 — the authoritative analog)
    - .planning/todos/pending/phase21-terrain-override-loose-subdir.md
  </read_first>
  <behavior>
    - SaveLooseOverride with looseOverrideSubDir="loose" places a TRE asset "terrain/naboo.trn" at <root>/loose/terrain/naboo.trn.
    - An empty/null looseOverrideSubDir falls back to <root>/<logical> (preserves the old behavior for callers that pass empty).
    - A bad subdir ('..' / rooted) returns SaveResult.Failure with the "loose-override sub-directory" message, never throws.
  </behavior>
  <action>
    Add a `string looseOverrideSubDir` parameter to TerrainSaveTargets.SaveLooseOverride (append it after `value`,
    or place it adjacent to `resolvedRoot` — choose the position that keeps the call site readable; document the choice).
    Replace the single `LooseOverridePath.Resolve(resolvedRoot, relAssetPath)` with the EXACT two-step composition from
    IffSaveTargets.SaveLooseOverride: compute `overrideBase = string.IsNullOrEmpty(looseOverrideSubDir) ? resolvedRoot
    : LooseOverridePath.Resolve(resolvedRoot, looseOverrideSubDir)` inside a try/catch returning
    SaveResult.Failure("Invalid loose-override sub-directory: " + ex.Message), then `fullPath =
    LooseOverridePath.Resolve(overrideBase, relAssetPath)` inside the existing try/catch (keep its
    SaveResult.Failure mapping, optionally reword to "Invalid loose-override asset path:" to match IffSaveTargets).
    Do NOT change the relAssetPath derivation (TRE -> LogicalPath; loose -> TryFromAbsolute/filename) or the
    ApplyFieldEdit / Serialize / WriteAtomic sequence. Update the XML-doc to state the destination is
    `<resolvedRoot>/<looseOverrideSubDir>/<logical>` mirroring IffSaveTargets.
  </action>
  <verify>
    <automated>cd /d/Code/Utinni && grep -v '^[[:space:]]*//' "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/TerrainSaveTargets.cs" | grep -c "looseOverrideSubDir"  # >= 2 (param + composition)</automated>
  </verify>
  <acceptance_criteria>
    - SaveLooseOverride's signature includes a `string looseOverrideSubDir` parameter.
    - The body composes overrideBase via LooseOverridePath.Resolve(resolvedRoot, looseOverrideSubDir) (when non-empty) then Resolve(overrideBase, relAssetPath) — matching IffSaveTargets verbatim in shape.
    - An empty looseOverrideSubDir preserves the legacy <root>/<logical> destination (no regression for empty).
    - Build of TheJawaToolboxDotNet (VS2026 MSBuild, x86 Release) succeeds.
  </acceptance_criteria>
  <done>Terrain overrides resolve under <root>/loose/<logical> via the same two-step composition every other editor uses.</done>
</task>

<task type="auto">
  <name>Task 2: Pass "loose" at the call site + align PredictOverridePath; assert <root>/loose/ destination</name>
  <files>D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTerrainEditor.cs, UtinniCoreDotNet.Tests/SavingTests/TerrainLooseOverridePathTests.cs</files>
  <read_first>
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTerrainEditor.cs (the SaveLooseOverride call ~line 1047 and PredictOverridePath ~line 1181)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/IffSaveTargets.cs (how FormIffEditor passes its looseOverrideSubDir — confirm the literal "loose" and any ini override key it reads, e.g. [IffEditor] looseOverrideDir)
    - UtinniCoreDotNet.Tests/SavingTests/DatatableSaveTargetsTests.cs (the SaveLooseOverride_UsesLooseOverridePathResolve_StaysUnderRoot test shape ~line 135 — the framework-side analog the new test mirrors)
    - UtinniCoreDotNet.Tests/SavingTests/LooseOverridePathTests.cs (LooseOverridePath.Resolve usage patterns)
  </read_first>
  <action>
    In FormTerrainEditor, pass the loose subdir to the SaveLooseOverride call (~line 1047). Use the literal `"loose"`
    unless FormIffEditor resolves its subdir from an ini key (e.g. `[IffEditor] looseOverrideDir`) — if it does, mirror
    that resolution for the terrain editor (its own ini section) so the two editors agree; otherwise hardcode `"loose"`
    matching IffSaveTargets' default. Update `PredictOverridePath(resolvedRoot)` (~line 1181) to compose the SAME
    two-step destination (`LooseOverridePath.Resolve(LooseOverridePath.Resolve(resolvedRoot, "loose"), tre.LogicalPath)`,
    inside its existing try/catch returning null on rejection) so the overwrite-confirm File.Exists checks the real
    loose/ destination. Do NOT touch WritePreviewTemp (its separate loose_preview temp dir is correct).

    Add a framework-side test `UtinniCoreDotNet.Tests/SavingTests/TerrainLooseOverridePathTests.cs` (xUnit) that does
    NOT reference UtinniPlugins (the test project cannot). Mirror DatatableSaveTargetsTests'
    SaveLooseOverride_UsesLooseOverridePathResolve_StaysUnderRoot: compose the two-step destination directly via
    LooseOverridePath.Resolve(LooseOverridePath.Resolve(root, "loose"), "terrain/naboo.trn") and assert the result
    (a) StartsWith Path.GetFullPath(Path.Combine(root, "loose")) (OrdinalIgnoreCase) and (b) contains the OS-native
    "loose" + separator segment, proving the destination lands under <root>/loose/. Add a second [Fact] asserting a
    "../../escape.trn" relAsset still throws ArgumentException through the composition (containment preserved).
  </action>
  <verify>
    <automated>cd /d/Code/Utinni && dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~TerrainLooseOverridePath" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The FormTerrainEditor SaveLooseOverride call passes a non-empty loose subdir ("loose" or the ini-resolved value).
    - PredictOverridePath composes the SAME <root>/loose/<logical> destination as the save (two-step Resolve), so the overwrite confirm and the actual write agree.
    - TerrainLooseOverridePathTests passes: the resolved destination StartsWith <root>/loose/ and the escape case still throws.
    - `grep -v '^[[:space:]]*//' "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTerrainEditor.cs" | grep -c 'SaveLooseOverride'` >= 1 and the matched call includes a loose-subdir argument.
  </acceptance_criteria>
  <done>The editor saves terrain overrides under <root>/loose/, the overwrite prediction matches, and a framework test pins the destination under loose/ + the escape rejection.</done>
</task>

</tasks>

<verification>
- Build the managed solution + TheJawaToolboxDotNet (VS2026 MSBuild, x86 Release) in this wave (cross-repo paired commit).
- `dotnet test UtinniCoreDotNet.Tests --no-build` green (incl. the new TerrainLooseOverridePathTests).
- `git checkout -- UtinniCoreDotNet/Generated/UtinniCore.cs` before committing (CppSharp churn).
- Byte-content parity with apply-save-trn is unchanged (same single-source codec); R2 only relocates the destination under loose/.
</verification>

<success_criteria>
R2 closed: terrain loose overrides land at <root>/loose/<logical> like every other editor, on the documented
loose searchPath; the overwrite-confirm prediction points at the same destination; a framework test asserts the
destination is under <root>/loose/ and that traversal escapes are still rejected.
</success_criteria>

<output>
Create `.planning/phases/21-terrain-tjt-subpanel-best-effort-live-preview/21-06-SUMMARY.md` when done.
Paired cross-repo commit (Utinni + UtinniPlugins) — no human checkpoint required.
</output>
