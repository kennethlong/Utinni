---
phase: 16-live-injected-mcp-bridge-blender-ecosystem-boundary
plan: 03
type: execute
wave: 2
depends_on: [16-02]
files_modified:
  - Utinni.Mcp/Server/LivePipeClient.cs
  - Utinni.Mcp/Tools/LiveTools.cs
  - Utinni.Mcp/Program.cs
  - Utinni.Mcp.Tests/LivePipeProtocolTests.cs
  - Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj
  - Utinni.Mcp/MCP-SECURITY.md
autonomous: true
requirements: [MCP-03]
user_setup: []

must_haves:
  truths:
    - "live_ping over a loopback pipe round-trips to a stub server and maps to a clean MCP result; when no server listens it returns { listening: false } and never hangs"
    - "live_reload_asset resolves its path under the fail-closed ResolvedRoot BEFORE any pipe send, sends a well-formed envelope, and maps the ack tier"
    - "When --enable-live is OFF (default), the live_* tools are NOT registered/advertised (fail-closed by absence, D-04); when ON they are registered via WithTools<LiveTools>()"
    - "Pipe framing edge cases (partial read / oversize / server-close mid-message) return a hard error, never a hang"
    - "The net10 duplicated PipeName literal byte-equals the canonical 16-02 fixture (Utinni.Cli.Tests/Fixtures/live/pipe-name.txt) — proven by a test, so the cross-plan duplicate cannot drift from the in-client server"
  artifacts:
    - path: "Utinni.Mcp/Server/LivePipeClient.cs"
      provides: "Named-pipe client dispatch target (twin of CliDispatcher; injectable-timeout dual ctor, never-hang discipline)"
      contains: "NamedPipeClientStream"
    - path: "Utinni.Mcp/Tools/LiveTools.cs"
      provides: "live_ping + live_reload_asset thin tools (NOT McpServerToolType-decorated; registered only via WithTools-of-T)"
      contains: "live_ping"
    - path: "Utinni.Mcp/Program.cs"
      provides: "Conditional WithTools-of-LiveTools gated on serverArgs.EnableLive"
      contains: "WithTools"
  key_links:
    - from: "Utinni.Mcp/Program.cs"
      to: "WithTools-of-LiveTools"
      via: "if (serverArgs.EnableLive) ... (fail-closed by absence)"
      pattern: "serverArgs\\.EnableLive"
    - from: "Utinni.Mcp/Tools/LiveTools.cs"
      to: "ResolvedRoot.Resolve"
      via: "live_reload_asset path resolution before pipe send"
      pattern: "root\\.Resolve"
    - from: "Utinni.Mcp/Server/LivePipeClient.cs"
      to: "LiveBridgeProtocol.PipeName"
      via: "shared pipe-name constant + framing (duplicated literal, fixture-asserted)"
      pattern: "PipeName"
    - from: "Utinni.Mcp.Tests/LivePipeProtocolTests.cs"
      to: "Utinni.Cli.Tests/Fixtures/live/pipe-name.txt"
      via: "byte-equality assertion on the net10 duplicated PipeName literal"
      pattern: "pipe-name\\.txt"
---

<objective>
Build the out-of-proc MCP host half of the live bridge (MCP-03): the LivePipeClient named-pipe dispatch target (structural twin of CliDispatcher with the injectable-timeout, never-hang discipline), the live_ping + live_reload_asset tools (the D-02 minimal surface), and the D-04 fail-closed gating in Program.cs (conditional WithTools registration of LiveTools gated on serverArgs.EnableLive, NOT the assembly scan). Prove the whole protocol with a loopback NamedPipe test (no live client) and extend MCP-SECURITY.md with the live-tier threat surface.

Because net10 `Utinni.Mcp` cannot project-ref net472 `UtinniCoreDotNet`, this plan DUPLICATES the canonical pipe-name literal `utinni-live-bridge`. To make the `depends_on: [16-02]` edge enforce real agreement (BLOCKER fix), this plan links the single canonical fixture committed by 16-02 (`Utinni.Cli.Tests/Fixtures/live/pipe-name.txt`) and a test asserts the net10 duplicated literal byte-equals fixture line 1 — drift between the two ends now FAILS a Tier-2 test instead of surfacing only at the non-gating Tier-4 smoke.

Purpose: Close MCP-03's automated (Tier-2) deliverable — the agent-facing tools, the pipe client, the fail-closed gate, the cross-plan pipe-name agreement, and the loopback proof — leaving only the Tier-4 manual live-injection confirmation (non-gating per D-01).
Output: LivePipeClient, LiveTools, conditional registration, loopback protocol tests (incl. the fixture byte-equality assertion), MCP-SECURITY.md live-tier addendum.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-CONTEXT.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-RESEARCH.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-PATTERNS.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-VALIDATION.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-02-PLAN.md

<interfaces>
From UtinniCoreDotNet/Live/LiveBridgeProtocol.cs (16-02):
  static class LiveBridgeProtocol  // PipeName constant; request/ack envelope DTOs; 4-byte LE length-prefix framing; bounded max size
  // CANONICAL pipe name value = "utinni-live-bridge" (16-02 fixture line 1, written verbatim there).
  // Lives in UtinniCoreDotNet (net472). net10 Utinni.Mcp cannot project-ref net472, so duplicate the
  // EXACT literal "utinni-live-bridge" + the framing in Utinni.Mcp, cite LiveBridgeProtocol as the single
  // documented source-of-truth in a code comment, AND bind the duplicate to the canonical fixture
  // Utinni.Cli.Tests/Fixtures/live/pipe-name.txt via the byte-equality test in Task 3 (BLOCKER fix —
  // the duplicate cannot silently drift from the in-client server's PipeName).

From Utinni.Mcp/Server/CliDispatcher.cs (:56-74, :121-138) — the twin to clone:
  public class CliDispatcher { public static readonly TimeSpan DefaultTimeout = TimeSpan.FromSeconds(60);
    public CliDispatcher(string cliExePath) : this(cliExePath, DefaultTimeout) {}
    public CliDispatcher(string cliExePath, TimeSpan timeout) {...}
    public virtual async Task RunAsync(string verb, IReadOnlyList args) {...} }
  // CTS(_timeout) -> on cancel return a hard-error result object, never throw, never hang.

From Utinni.Mcp/Tools/ReadTools.cs (:54-70) — the thin-tool shape:
  [McpServerTool(Name="read_tre", ReadOnly=true, Idempotent=true)]
  public static async Task ReadTre(ResolvedRoot root, CliDispatcher cli, string relativePath)
  { string abs = root.Resolve(relativePath); ... return CliResultMapper.ToCallToolResult(r); }

From Utinni.Mcp/Program.cs (:65-74):
  var serverArgs = ServerArgs.Parse(args);
  builder.Services.AddSingleton(new CliDispatcher(cliExePath));
  builder.Services.AddMcpServer().WithStdioServerTransport().WithToolsFromAssembly();
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave-0 spike — confirm WithTools-of-LiveTools gating excludes the assembly scan (Assumption A1)</name>
  <files>Utinni.Mcp/Tools/LiveTools.cs</files>
  <read_first>
    - Utinni.Mcp/Program.cs (the current WithToolsFromAssembly() block, lines 71-74; the scan registers every McpServerToolType)
    - Utinni.Mcp/Tools/ReadTools.cs (a McpServerToolType class the scan DOES pick up — the contrast case)
    - .planning/phases/16-.../16-RESEARCH.md A5 + Pitfall 1 + Assumptions A1 (the ~10-line spike: a non-McpServerToolType class registered only via WithTools-of-T must NOT be grabbed by WithToolsFromAssembly())
  </read_first>
  <action>
    Create LiveTools.cs as a static class that does NOT carry the McpServerToolType attribute (RESEARCH Pitfall 1 — if it did, WithToolsFromAssembly() would register it unconditionally and defeat the D-04 gate). Add the two McpServerTool-attributed method stubs (live_ping, live_reload_asset) returning a placeholder CallToolResult for now (Task 2 fills the bodies). Run the ~10-line confirmation as an assertion in LivePipeProtocolTests (added in Task 3): with WithToolsFromAssembly() alone (no conditional WithTools), the live_* tools are NOT advertised; with the conditional WithTools they ARE. If the SDK 1.4.0 scan grabs the class regardless of the missing McpServerToolType attribute, fall back to RESEARCH option (b): scope the assembly scan to exclude the live class (sub-namespace / separate registration) — document whichever lock you land in a code comment citing A1.
    Lock the outcome: exactly one registration path for live_* = the conditional WithTools registration; the assembly scan must not also register it.
  </action>
  <verify>
    <automated>cd D:/Code/Utinni; dotnet build Utinni.Mcp</automated>
  </verify>
  <acceptance_criteria>
    - LiveTools.cs exists and is NOT decorated with McpServerToolType (the attribute is absent from the file), OR a documented code comment explains the scoped-scan exclusion fallback (option b) citing Assumption A1
    - Utinni.Mcp builds clean
    - The chosen single-registration-path lock is recorded in a LiveTools.cs comment referencing D-04 + A1
  </acceptance_criteria>
  <done>The D-04 gating mechanism is confirmed: live_* tools reach the SDK only via the conditional WithTools registration, never the assembly scan.</done>
</task>

<task type="auto">
  <name>Task 1: LivePipeClient — named-pipe dispatch target (twin of CliDispatcher, never-hang)</name>
  <files>Utinni.Mcp/Server/LivePipeClient.cs</files>
  <read_first>
    - Utinni.Mcp/Server/CliDispatcher.cs (the twin: non-sealed class, dual ctor with injectable timeout lines 56-74, CTS(_timeout) non-throwing hard-error result lines 121-138; PATTERNS lines 67-81)
    - Utinni.Mcp.Tests/DispatcherTests.cs (the stub-target + injectable-timeout + non-throwing-result test discipline the loopback test reuses)
    - UtinniCoreDotNet/Live/LiveBridgeProtocol.cs (16-02 — the pipe name + framing + envelope to mirror on the net10 side; PATTERNS A3)
    - .planning/phases/16-.../16-RESEARCH.md A5 (LivePipeClient as a new dispatch target) + Pitfall 3 (no-client maps to clean listening-false, never hang)
  </read_first>
  <action>
    Create LivePipeClient.cs in Utinni.Mcp/Server/ as the named-pipe twin of CliDispatcher: a non-sealed class with LivePipeClient(string pipeName) and LivePipeClient(string pipeName, TimeSpan timeout) dual ctors (a SHORT connect timeout default, much smaller than CliDispatcher's 60s — a no-client ping must return fast). Make the connect/send method virtual so the loopback test can substitute a recording/stub.
    Declare the DUPLICATED canonical pipe name as a single named const in this file — `public const string PipeName = "utinni-live-bridge";` — written byte-identical to the 16-02 canonical value, with a code comment naming UtinniCoreDotNet/Live/LiveBridgeProtocol.cs as the documented source-of-truth and Utinni.Cli.Tests/Fixtures/live/pipe-name.txt as the agreement anchor the Task-3 test asserts against (net10 cannot project-ref net472). Default the (string pipeName)-less convenience path, if any, to this const.
    Mirror the never-hang discipline (CliDispatcher.cs:121-138): wrap NamedPipeClientStream Connect + send + read in a CancellationTokenSource(_timeout); on OperationCanceledException, a connect failure, or a server-close mid-message, return a hard-error/absent result object (listening-false for ping) — NEVER throw, NEVER hang (Pitfall 3). Use the LiveBridgeProtocol framing (4-byte LE length prefix + UTF-8 JSON). Expose PingAsync() returning the ping result and ReloadAssetAsync(absolutePath, ext) returning the ack result. Both return result objects the tools map; neither throws on transport failure. Enforce the same bounded max read size as the server (reject oversize ack frames).
  </action>
  <verify>
    <automated>cd D:/Code/Utinni; dotnet build Utinni.Mcp</automated>
  </verify>
  <acceptance_criteria>
    - LivePipeClient.cs contains NamedPipeClientStream, CancellationTokenSource, and a `PipeName` const literal `"utinni-live-bridge"` (byte-identical to the 16-02 canonical value) with a comment citing LiveBridgeProtocol + the pipe-name.txt fixture as the source-of-truth
    - Dual ctors exist; the default connect timeout is short (sub-second to a few seconds, well under CliDispatcher's 60s)
    - The connect/send method is virtual (substitutable by the loopback test)
    - No-server-listening, timeout, and server-close paths return a result object and never throw/hang (asserted in Task 3)
    - Utinni.Mcp builds clean
  </acceptance_criteria>
  <done>The pipe client mirrors CliDispatcher's never-hang discipline, speaks the shared LiveBridgeProtocol framing, and pins the duplicated pipe-name literal for the Task-3 fixture-equality assertion.</done>
</task>

<task type="auto">
  <name>Task 2: LiveTools bodies (live_ping + live_reload_asset) + conditional registration in Program.cs</name>
  <files>Utinni.Mcp/Tools/LiveTools.cs, Utinni.Mcp/Program.cs</files>
  <read_first>
    - Utinni.Mcp/Tools/ReadTools.cs (the thin-tool shape: DI params, root.Resolve FIRST, dispatch, map; lines 54-70)
    - Utinni.Mcp/Tools/RepackTool.cs (the non-readonly attribute shape ReadOnly=false, Idempotent=false; line ~59-74)
    - Utinni.Mcp/Program.cs (singleton-registration line 67 + the WithToolsFromAssembly() block lines 71-74 — the two edit sites)
    - Utinni.Mcp/Server/ResolvedRoot.cs (Resolve throws on escape; the fail-closed containment the live tier must NOT bypass)
    - .planning/phases/16-.../16-PATTERNS.md (Tool shapes lines 55-59; Program.cs conditional-registration lines 116-125)
  </read_first>
  <action>
    Fill LiveTools.cs (the non-McpServerToolType class from Task 0) with the D-02 minimal surface:
    - live_ping — McpServerTool(Name = "live_ping", ReadOnly = true, Idempotent = true), takes LivePipeClient pipe (DI), no path arg. Calls pipe.PingAsync(), maps to a CallToolResult reporting listening/gameRunning/pid (or listening-false when no client). Honest absence reporting — never an error/hang (Pitfall 3).
    - live_reload_asset — McpServerTool(Name = "live_reload_asset", ReadOnly = false, Idempotent = false), takes ResolvedRoot root, LivePipeClient pipe, string relativePath. MUST call root.Resolve(relativePath) FIRST (the live tier must not bypass the fail-closed root — RESEARCH A5 / Security V4), derive the ext, then pipe.ReloadAssetAsync(abs, ext), map to accepted/tier/queued/path. The tier comes verbatim from the in-client ReloadAssetClassifier via the ack.
    In Program.cs: register the client singleton alongside the CliDispatcher one — builder.Services.AddSingleton(new LivePipeClient(LivePipeClient.PipeName)). Capture the MCP builder and conditionally add the live tier: keep WithToolsFromAssembly() for the existing read/save/repack tools, then add a guarded line that registers LiveTools via WithTools only when serverArgs.EnableLive is true — registered ONLY when --enable-live (fail-closed by absence, D-04). Do NOT register LiveTools via the assembly scan (Task 0 lock).
  </action>
  <verify>
    <automated>cd D:/Code/Utinni; dotnet build Utinni.Mcp</automated>
  </verify>
  <acceptance_criteria>
    - LiveTools.cs defines live_ping (ReadOnly=true) and live_reload_asset (ReadOnly=false); live_reload_asset calls root.Resolve BEFORE any pipe send
    - Program.cs registers the LivePipeClient singleton and contains a guard on serverArgs.EnableLive around the WithTools registration of LiveTools
    - LiveTools is registered ONLY under the EnableLive guard (not via WithToolsFromAssembly)
    - Utinni.Mcp builds clean
  </acceptance_criteria>
  <done>The agent-facing live_* tools exist, route through the pipe client, honor the fail-closed root, and are gated on --enable-live.</done>
</task>

<task type="auto">
  <name>Task 3: Loopback protocol test (no live client) + D-04 gating assertion + pipe-name fixture-equality + MCP-SECURITY.md live-tier addendum</name>
  <files>Utinni.Mcp.Tests/LivePipeProtocolTests.cs, Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj, Utinni.Mcp/MCP-SECURITY.md</files>
  <read_first>
    - Utinni.Mcp.Tests/DispatcherTests.cs (the stub-target + injectable-timeout + non-throwing-result test shape to clone; lines 50-70; PATTERNS lines 170-184)
    - Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj (the test project to add the linked-fixture Content entry to)
    - Utinni.Mcp/Server/LivePipeClient.cs (Task 1 — the client under test, incl. the duplicated PipeName const)
    - Utinni.Mcp/MCP-SECURITY.md (the Phase-14 threat register to extend with a Phase-16 live-tier section)
    - .planning/phases/16-.../16-RESEARCH.md A6 (loopback harness design) + Security Domain (the exact ASVS rows + STRIDE table + accepted-risk analog AR-14-03/04 to fold in)
  </read_first>
  <action>
    Link the canonical fixture into this test project so the net10 lane can read it: in Utinni.Mcp.Tests.csproj add `<Content Include="..\Utinni.Cli.Tests\Fixtures\live\pipe-name.txt" Link="Fixtures\live\pipe-name.txt" CopyToOutputDirectory="PreserveNewest" />` (Link form — the SAME committed file 16-02 created and UtinniCoreDotNet.Tests also links; no fork). This is the cross-plan agreement anchor.
    Write LivePipeProtocolTests.cs in Utinni.Mcp.Tests (name the class so --filter LivePipe selects it). Stand up a NamedPipeServerStream test-stub on a randomized pipe name implementing the ping + reload-asset protocol (mirror the LiveBridgeProtocol framing), point a LivePipeClient at it, and assert:
    - live_ping round-trips: stub replies listening-true with gameRunning/pid; the client maps to the expected result.
    - live_reload_asset: the client sends a well-formed request envelope (assert the bytes the client SENT parse to op=reload-asset + the resolved path + ext), the stub replies an ack with a tier, the client maps accepted/tier/queued/path.
    - Framing edge cases: a partial-read frame, an oversize body (exceeds the bounded max), and a stub that closes mid-message — each returns a hard error result, never a hang (use the injectable short timeout; assert the call returns within the timeout window).
    - No-client: point the client at a pipe name no server owns; assert PingAsync returns listening-false quickly and never throws/hangs (Pitfall 3).
    - Pipe-name fixture-equality (BLOCKER agreement anchor): read Fixtures/live/pipe-name.txt from the test output dir, parse line 1, and assert it byte-equals LivePipeClient.PipeName (the net10 duplicated literal). This is the host half of the cross-plan agreement — 16-02 asserts the SAME fixture line equals the in-client LiveBridgeProtocol.PipeName, so both ends are transitively proven byte-identical. Drift in either literal FAILS a Tier-2 test, making the wave-2 depends_on edge load-bearing.
    - D-04 gating assertion (the Task-0 spike outcome): assert that building the host WITHOUT EnableLive does not advertise live_ping/live_reload_asset, and WITH EnableLive does. Use the MCP SDK's tool-enumeration surface (or a registration-introspection equivalent) consistent with the SDK 1.4.0 API; if direct enumeration is impractical in-test, assert the registration code path is taken only under the guard.
    Extend MCP-SECURITY.md with a Phase-16 live-tier section folding in RESEARCH Security Domain verbatim: the ASVS rows (V1/V4/V5/V13/V7), the STRIDE table (unauthorized-pipe-connect, agent-reaches-live-when-off, path-escape, frame-DoS, wrong-thread-crash, pipe-squatting, visible-render-over-promise), each mitigate row citing the proving test/file, and the accepted-risk row (single-user local desktop; --enable-live/pipe-name are PATHs not secrets; analog AR-14-03/04). Note no new packages (Package Legitimacy Gate not triggered).
  </action>
  <verify>
    <automated>cd D:/Code/Utinni; dotnet test Utinni.Mcp.Tests --no-build --filter LivePipe</automated>
  </verify>
  <acceptance_criteria>
    - Utinni.Mcp.Tests.csproj links ..\Utinni.Cli.Tests\Fixtures\live\pipe-name.txt (same committed file, Link form)
    - dotnet test Utinni.Mcp.Tests --filter LivePipe exits 0
    - Tests cover: ping round-trip, reload-asset well-formed-envelope + ack-tier mapping, partial-read/oversize/server-close hard-error (no hang), no-client listening-false, the pipe-name fixture byte-equality (line 1 == LivePipeClient.PipeName), and the EnableLive on/off gating
    - Every framing-edge-case test completes within the injected short timeout (proves never-hang)
    - MCP-SECURITY.md contains a Phase-16 live-tier section with the ASVS rows, the STRIDE table, per-row test citations, and the accepted-risk analog
  </acceptance_criteria>
  <done>The full pipe protocol + envelope mapping + D-04 gate + cross-plan pipe-name agreement are proven without a live client; the security register documents the live-tier surface.</done>
</task>

</tasks>

<threat_model>
**Gate verdict: ASVS L1; no HIGH-severity threats; security gate passes.** (All threats below are mitigate or accept; none rated HIGH. Block-on-HIGH posture: had any threat been HIGH it would gate the wave — none are.)

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI agent -> MCP host (stdio) | Untrusted tool-call args (relativePath) cross into the host |
| MCP host -> named pipe | The host's pipe client crosses the OS named-pipe boundary into the injected client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-20 | Elevation | Agent reaches live_* when the tier is not intended to be on | mitigate | D-04 fail-closed gate: live_* are UNREGISTERED unless --enable-live; the pipe name is not advertised. Acceptance: Task 3 gating assertion (off => tools absent). |
| T-16-21 | Tampering | Path-escape via live_reload_asset path arg | mitigate | ResolvedRoot.Resolve runs BEFORE any pipe send (throws on escape -> SDK tool error). Acceptance: live_reload_asset calls root.Resolve first (Task 2 key_link). |
| T-16-22 | DoS | Malformed/oversize/partial ack frame hangs the MCP call | mitigate | Injectable short connect/read timeout + bounded max frame size; transport failure returns a result object, never a hang. Acceptance: Task 3 framing-edge-case tests complete within the timeout. |
| T-16-23 | Spoofing | No client listening -> naive client hangs the MCP call | mitigate | Short connect timeout maps no-server to listening-false (Pitfall 3). Acceptance: Task 3 no-client test returns quickly. |
| T-16-24 | (candor) | Visible-render over-promise | mitigate (correctness) | Ack reports the honest ReloadAssetClassifier tier; render is best-effort, NOT a success gate (D-01). must_haves do NOT gate on visible render. |
| T-16-AR16 | (accepted risk) | Single-user local desktop pipe; --enable-live / pipe-name are identifiers not secrets | accept | Local-only named pipe + current-user ACL (server side, 16-02); analog AR-14-03/04; logged in MCP-SECURITY.md. |
| T-16-SC | Tampering | npm/pip/cargo installs | n/a | No new packages — BCL System.IO.Pipes + System.Text.Json + already-pinned ModelContextProtocol 1.4.0 only (RESEARCH Package Legitimacy Audit: gate not triggered). |
</threat_model>

<verification>
- dotnet test Utinni.Mcp.Tests --filter LivePipe green (loopback ping + reload-asset + framing edge cases + no-client + pipe-name fixture-equality + D-04 gating).
- Utinni.Mcp builds clean with the conditional WithTools registration.
- MCP-SECURITY.md carries the Phase-16 live-tier section.
- Tier-4 manual: live-injected confirmation (launch with --enable-live, live_ping then live_reload_asset against an edited asset) — non-gating per D-01, documented in 16-VALIDATION.md.
</verification>

<success_criteria>
- MCP-03: an agent can drive the live-injected client over the MCP bridge (named-pipe IPC); the ack round-trip is proven over a loopback pipe (Tier-2). Visible re-render is best-effort and explicitly NOT a Phase-16 success gate (D-01).
- The live tier is fail-closed by absence (D-04) and the minimal surface (ping + reload-asset only, D-02).
- The net10 duplicated pipe-name literal is proven byte-identical to the in-client server's via the shared canonical fixture (cross-plan agreement; the wave-2 depends_on edge is load-bearing).
- The out-of-proc anti-pattern holds: the SDK never enters SWG.exe; only the narrow named pipe crosses.
</success_criteria>

<output>
Create `.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-03-SUMMARY.md` when done.
</output>
