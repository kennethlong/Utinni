---
phase: 16-live-injected-mcp-bridge-blender-ecosystem-boundary
plan: 03
type: execute
wave: 2
depends_on: [16-02]
files_modified:
  - Utinni.Mcp/Server/LivePipeClient.cs
  - Utinni.Mcp/Tools/LiveTools.cs
  - Utinni.Mcp/Program.cs
  - Utinni.Mcp.Tests/LivePipeProtocolTests.cs
  - Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj
  - UtinniCoreDotNet.Tests/Live/LiveBridgeIntegrationTests.cs
  - UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj
  - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/MCP-SECURITY.md
autonomous: true
requirements: [MCP-03]
user_setup:
  - service: "Live bridge (two-flag operator contract — CUR-NEW-6)"
    why: "The live tier requires BOTH --enable-live (this plan) AND the in-client [Live] enableLiveBridge flag (16-02). Recorded here so operators discover that --enable-live alone yields live_ping listening:false."
    env_vars: []
    dashboard_config:
      - task: "Launch Utinni.Mcp with --enable-live (or UTINNI_MCP_ENABLE_LIVE=1) so the live_* tools are advertised + the LivePipeClient is registered."
        location: "Utinni.Mcp launch args"
      - task: "ALSO set the in-client [Live] enableLiveBridge=true (16-02) so the listener runs. Neither flag alone produces a working live path."
        location: "Utinni.ini in-client config + documented in MCP-SECURITY.md live-tier section"

must_haves:
  truths:
    - "live_ping over a loopback pipe round-trips to a stub server and maps to a clean MCP result; when no server listens it returns { listening: false } and never hangs"
    - "live_reload_asset resolves its path under the fail-closed ResolvedRoot BEFORE any pipe send, sends a well-formed envelope carrying the RELATIVE asset path (the in-client server resolves it under its OWN pinned client root — root reconciliation, CUR-NEW-1), and maps the ack tier"
    - "A REAL cross-implementation integration test (net472) spins up the real 16-02 LivePipeServer on a randomized pipe and round-trips ping + reload-asset over the ACTUAL wire using the shared LiveBridgeProtocol framing/serialization — proving wire-protocol agreement, not just pipe-name literal equality (C-02)"
    - "The net10 client asserts byte-EXACT against the SAME committed golden wire byte-vector fixtures the net472 server asserts (ping/reload request+ack) — closing the cross-TFM wire gap so a green net472-on-net472 test cannot hide a net10 camelCase/tier/protocolVersion mismatch (CDX-NEW-1=CUR-NEW-2/CUR-NEW-4/CUR-NEW-9)"
    - "When --enable-live is OFF (default), the live_* tools are NOT advertised (asserted through the REAL host-building / tool-enumeration path mirroring RoundTripTests.Handshake_ListsExactlyTheTwelveNamedTools' McpClient.ListToolsAsync, not a branch/comment — C-10/CDX-NEW-5/6/CUR-NEW-8); when ON they ARE advertised"
    - "Pipe framing edge cases (partial read / oversize / server-close mid-message) return a hard error, never a hang; a protocolVersion-skew ack is surfaced as a structured error, not silently parsed (CUR-NEW-5)"
    - "The net10 duplicated PipeName literal byte-equals canonical fixture line 1 AND the net10 framing constants byte-equal fixture line 2 (name + framing both pinned — C-05), proven by a test"
    - "The live_reload_asset tool DESCRIPTION + ack make clear that queued != visible reload (transport-only until RESID-03; reloadAttempted=false) — C-14 candor"
  artifacts:
    - path: "Utinni.Mcp/Server/LivePipeClient.cs"
      provides: "Named-pipe client dispatch target (twin of CliDispatcher; injectable-timeout dual ctor, never-hang discipline); net10 System.Text.Json configured camelCase + string-encoded tier to match the net472 JavaScriptSerializer wire"
      contains: "NamedPipeClientStream"
    - path: "Utinni.Mcp/Tools/LiveTools.cs"
      provides: "live_ping + live_reload_asset thin tools (NOT McpServerToolType-decorated; registered only via WithTools-of-T); reload tool description carries the transport-only candor"
      contains: "live_ping"
    - path: "Utinni.Mcp/Program.cs"
      provides: "Conditional WithTools-of-LiveTools gated on serverArgs.EnableLive; LivePipeClient singleton registered only when EnableLive (C-13)"
      contains: "WithTools"
    - path: "UtinniCoreDotNet.Tests/Live/LiveBridgeIntegrationTests.cs"
      provides: "REAL LivePipeServer round-trip over an actual pipe via LiveBridgeProtocol (C-02 cross-impl proof; net472 — the only side that can reference the real server)"
      contains: "LivePipeServer"
  key_links:
    - from: "Utinni.Mcp/Program.cs"
      to: "WithTools-of-LiveTools"
      via: "if (serverArgs.EnableLive) ... (fail-closed by absence)"
      pattern: "serverArgs\\.EnableLive"
    - from: "Utinni.Mcp/Tools/LiveTools.cs"
      to: "ResolvedRoot.Resolve"
      via: "live_reload_asset path resolution before pipe send (resolves for validation; the relative path is sent on the wire — CUR-NEW-1)"
      pattern: "root\\.Resolve"
    - from: "Utinni.Mcp/Server/LivePipeClient.cs"
      to: "LiveBridgeProtocol.PipeName + framing + golden wire vectors"
      via: "shared pipe-name constant + framing (duplicated, fixture-asserted line 1 + line 2 + golden bytes)"
      pattern: "PipeName"
    - from: "UtinniCoreDotNet.Tests/Live/LiveBridgeIntegrationTests.cs"
      to: "UtinniCoreDotNet.Live.LivePipeServer + LiveBridgeProtocol"
      via: "real server + real protocol round-trip over an actual NamedPipe (C-02)"
      pattern: "new LivePipeServer"
    - from: "Utinni.Mcp.Tests/LivePipeProtocolTests.cs"
      to: "Utinni.Cli.Tests/Fixtures/live/{pipe-name.txt, wire-*.json}"
      via: "byte-equality on the net10 duplicated PipeName (line 1) + framing (line 2) + byte-exact golden wire-vector produce/consume on the net10 side"
      pattern: "wire-reload-ack\\.json"
---

<objective>
Build the out-of-proc MCP host half of the live bridge (MCP-03): the LivePipeClient named-pipe dispatch target (structural twin of CliDispatcher with the injectable-timeout, never-hang discipline), the live_ping + live_reload_asset tools (the D-02 minimal surface), and the D-04 fail-closed gating in Program.cs (conditional WithTools registration of LiveTools gated on serverArgs.EnableLive, NOT the assembly scan). Prove the whole protocol with (a) a loopback NamedPipe test (net10, no live client) that ALSO asserts the net10 client produces/consumes the SAME committed golden wire byte-vectors the net472 server does, AND (b) a REAL cross-implementation integration test (net472) that round-trips the real 16-02 LivePipeServer over the actual wire — and extend the Phase-14 MCP-SECURITY.md (at its real on-disk path) with the live-tier threat surface.

Because net10 `Utinni.Mcp` cannot project-ref net472 `UtinniCoreDotNet`, this plan DUPLICATES the canonical pipe-name literal `utinni-live-bridge` AND the framing constants. To make the `depends_on: [16-02]` edge enforce real agreement (BLOCKER fix + C-02 + C-05 + CDX-NEW-1), this plan: (1) links the canonical fixture + golden wire byte-vectors committed by 16-02 and asserts the net10 duplicated literal byte-equals fixture LINE 1, the net10 framing constants byte-equal fixture LINE 2, AND the net10 System.Text.Json (camelCase + string-tier) produces/consumes the wire-*.json goldens byte-exact; AND (2) adds a real-wire integration test on the net472 side (the only side that can reference the real LivePipeServer) exercising the real server against a LiveBridgeProtocol-driven client. Drift in the literal, the framing, the JSON casing/tier-encoding, or the wire shape now FAILS a Tier-2 test instead of surfacing only at the non-gating Tier-4 smoke.

Purpose: Close MCP-03's automated (Tier-2) deliverable — the agent-facing tools, the pipe client, the fail-closed gate (proven via real tool enumeration), the cross-plan pipe-name + framing + golden-wire agreement, the real cross-impl wire round-trip, and the loopback proof — leaving only the Tier-4 manual live-injection confirmation (non-gating per D-01).
Output: LivePipeClient (camelCase + string-tier-aligned net10 JSON), LiveTools (with candid reload description), conditional registration, loopback protocol tests (incl. golden-wire byte-equality + fixture byte-equality on name+framing + real-tool-enumeration gating assertion), the net472 cross-impl integration test, MCP-SECURITY.md live-tier addendum at the correct path.

REVIEW-DRIVEN EDITS (from 16-REVIEWS.md): C-02 (real client↔server cross-impl integration test — placed net472 due to the TFM wall), C-05 (framing line-2 enforced on the net10 side), C-10 (D-04 gating asserted via the real host-building / tool-enumeration path), **C-11** (MCP-SECURITY.md is at .planning/phases/14-.../ — CONFIRMED; not Utinni.Mcp/), C-12 (protocolVersion in the request envelope), C-13 (LivePipeClient registered only under the EnableLive guard), C-14 (reload tool description + ack candor), C-03-alignment (net10 System.Text.Json configured camelCase field-for-field to the net472 JavaScriptSerializer wire).

REVIEW-DRIVEN EDITS (Round 2, from 16-REVIEWS.md): **CDX-NEW-1 = CUR-NEW-2 (HIGH)** — the net10 client asserts byte-EXACT against the SAME committed golden wire byte-vectors the net472 server asserts (Task 3 (A)) — closing the cross-TFM wire gap. **CUR-NEW-1 (HIGH) root reconciliation** — live_reload_asset resolves the path under ResolvedRoot for VALIDATION but sends the RELATIVE asset path on the wire; the in-client server resolves it under its OWN pinned client root (16-02), so the two roots are coherent (Tasks 2/3). **CUR-NEW-4** — the net10 STJ is configured camelCase to match the wire, PROVEN by the golden byte-vectors (Task 3). **CUR-NEW-5** — a protocolVersion-skew ack is surfaced as a structured error (client-side), with a skew test (Tasks 1/3). **CUR-NEW-9** — ReloadTier is decoded from its STRING wire encoding (Task 1). **CDX-NEW-5/6 = CUR-NEW-8** — the D-04 real-enumeration gating proof is locked in Task 0 (not Task 3) as a failing→passing test mirroring the EXISTING `RoundTripTests.Handshake_ListsExactlyTheTwelveNamedTools` (McpClient.ListToolsAsync) precedent, AND Task 0 explicitly verifies whether `[McpServerTool]` methods in a class WITHOUT `[McpServerToolType]` are skipped by `WithToolsFromAssembly()` (fall back to scoped-scan exclusion if not). **CUR-NEW-6** — the dual-flag operator contract is documented in user_setup + MCP-SECURITY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-CONTEXT.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-RESEARCH.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-PATTERNS.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-VALIDATION.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-02-PLAN.md

<interfaces>
From UtinniCoreDotNet/Live/LiveBridgeProtocol.cs (16-02):
  static class LiveBridgeProtocol  // PipeName constant; ProtocolVersion=1; MaxFrameBytes=65536;
  // a framing-descriptor const == fixture line 2; request/ack envelope DTOs (JavaScriptSerializer, camelCase,
  // STRING-encoded ReloadTier); 4-byte LE length-prefix framing; bounded max size.
  // CANONICAL pipe name value = "utinni-live-bridge" (16-02 fixture line 1). Framing descriptor = fixture line 2
  // ("frame=len32le+utf8json;maxframe=65536;protocol=1;json=camelCase;tier=stringnames"). net10 Utinni.Mcp
  // cannot project-ref net472, so duplicate the EXACT literal + framing constants in Utinni.Mcp, cite
  // LiveBridgeProtocol as the single documented source-of-truth in a code comment, AND bind both the name
  // (line 1) and the framing (line 2) to the canonical fixture Utinni.Cli.Tests/Fixtures/live/pipe-name.txt
  // AND assert byte-exact against the wire-*.json golden vectors (CDX-NEW-1) via the Task-3 tests.
  // The request reload `path` is RELATIVE (the in-client server resolves it under its own client root — CUR-NEW-1).
  // The ack reload result carries reloadAttempted (always false) + a transport-only note (C-14) + string tier.

From Utinni.Mcp/Server/CliDispatcher.cs (:56-74, :121-138) — the twin to clone:
  public class CliDispatcher { public static readonly TimeSpan DefaultTimeout = TimeSpan.FromSeconds(60);
    public CliDispatcher(string cliExePath) : this(cliExePath, DefaultTimeout) {}
    public CliDispatcher(string cliExePath, TimeSpan timeout) {...}
    public virtual async Task RunAsync(string verb, IReadOnlyList args) {...} }
  // CTS(_timeout) -> on cancel return a hard-error result object, never throw, never hang.

From Utinni.Mcp/Tools/ReadTools.cs (:54-70) — the thin-tool shape:
  [McpServerTool(Name="read_tre", ReadOnly=true, Idempotent=true)]
  public static async Task ReadTre(ResolvedRoot root, CliDispatcher cli, string relativePath)
  { string abs = root.Resolve(relativePath); ... return CliResultMapper.ToCallToolResult(r); }

From Utinni.Mcp/Program.cs (:65-74):
  var serverArgs = ServerArgs.Parse(args);  // now exposes serverArgs.EnableLive (16-02 Task 2)
  builder.Services.AddSingleton(new CliDispatcher(cliExePath));
  builder.Services.AddMcpServer().WithStdioServerTransport().WithToolsFromAssembly();

ENUMERATION PRECEDENT (C-10 / CDX-NEW-5/6 / CUR-NEW-8 — the concrete fallback Codex asked for, found by Cursor):
  Utinni.Mcp.Tests/RoundTripTests.cs  — Handshake_ListsExactlyTheTwelveNamedTools already calls
  McpClient.ListToolsAsync() to enumerate the ADVERTISED tool list end-to-end. Mirror this pattern
  with/without --enable-live for the D-04 gating assertion. Lock it in Task 0.

ON-DISK FACT (C-11, CONFIRMED): MCP-SECURITY.md exists ONLY at
  .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/MCP-SECURITY.md
  (there is NO Utinni.Mcp/MCP-SECURITY.md). Extend the Phase-14 file in place.

TFM WALL (C-02 placement rationale): UtinniCoreDotNet.Tests = net472 (refs UtinniCoreDotNet → can see the real
  LivePipeServer + LiveBridgeProtocol); Utinni.Mcp.Tests = net10 (refs Utinni.Mcp → can see the real LivePipeClient).
  NEITHER can reference both. So the cross-impl real-server round-trip (C-02) lives in UtinniCoreDotNet.Tests (net472);
  the net10 LivePipeClient's agreement with that wire is held by the GOLDEN WIRE BYTE-VECTORS (wire-*.json) BOTH
  sides assert byte-exact (CDX-NEW-1) + the field-for-field camelCase/string-tier JSON policy + the framing fixture.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave-0 spike + LOCK — confirm WithTools-of-LiveTools gating excludes the assembly scan (Assumption A1), with a REAL failing→passing tool-enumeration test mirroring RoundTripTests (C-10/CDX-NEW-5/6/CUR-NEW-8)</name>
  <files>Utinni.Mcp/Tools/LiveTools.cs, Utinni.Mcp.Tests/LivePipeProtocolTests.cs</files>
  <read_first>
    - Utinni.Mcp/Program.cs (the current WithToolsFromAssembly() block, lines 71-74; the scan registers every McpServerToolType)
    - Utinni.Mcp/Tools/ReadTools.cs (a McpServerToolType class the scan DOES pick up — the contrast case)
    - Utinni.Mcp.Tests/RoundTripTests.cs (Handshake_ListsExactlyTheTwelveNamedTools — the EXISTING McpClient.ListToolsAsync() enumeration precedent to mirror with/without --enable-live; CDX-NEW-5/6/CUR-NEW-8)
    - .planning/phases/16-.../16-RESEARCH.md A5 + Pitfall 1 + Assumptions A1 (a non-McpServerToolType class registered only via WithTools-of-T must NOT be grabbed by WithToolsFromAssembly())
  </read_first>
  <action>
    Create LiveTools.cs as a static class that does NOT carry the McpServerToolType attribute (RESEARCH Pitfall 1 — if it did, WithToolsFromAssembly() would register it unconditionally and defeat the D-04 gate). Add the two McpServerTool-attributed method stubs (live_ping, live_reload_asset) returning a placeholder CallToolResult for now (Task 2 fills the bodies).
    LOCK THE GATING IN TASK 0 (not Task 3) via a REAL failing→passing enumeration test (CDX-NEW-5/6/CUR-NEW-8): in LivePipeProtocolTests.cs, add a gating fact that MIRRORS RoundTripTests.Handshake_ListsExactlyTheTwelveNamedTools — build/connect the actual MCP host through the real Program.cs registration path TWICE (without --enable-live and with --enable-live) and call McpClient.ListToolsAsync() (the same enumeration surface the existing handshake test uses). Assert live_ping/live_reload_asset are ABSENT from the advertised list when off and PRESENT when on. This is the real-enumeration proof (NOT a branch/comment) — write it now so the lock is verified at the Task-0 boundary, not deferred to Task 3. (Task 3 may extend it with the loopback round-trips, but the gating assertion itself lands here.)
    EXPLICITLY VERIFY the WithToolsFromAssembly behavior (CDX-NEW-6): confirm in this spike whether `[McpServerTool]` methods in a class WITHOUT `[McpServerToolType]` are SKIPPED by `WithToolsFromAssembly()` (the assumption D-04 rests on). The off-state enumeration assert (live_* absent with WithToolsFromAssembly() and no conditional WithTools) IS this verification. If the methods ARE picked up by the assembly scan (assumption false), fall back to RESEARCH option (b): scope the assembly scan to exclude the live class (sub-namespace / separate registration) so live_* never registers unless --enable-live — document whichever lock you land in a LiveTools.cs code comment citing A1, and keep the real-enumeration assert green.
    Lock the outcome: exactly one registration path for live_* = the conditional WithTools registration; the assembly scan must not also register it — PROVEN by the off-state enumeration assert.
  </action>
  <verify>
    <automated>cd D:/Code/Utinni; dotnet test Utinni.Mcp.Tests --filter LivePipe</automated>
  </verify>
  <acceptance_criteria>
    - LiveTools.cs exists and is NOT decorated with McpServerToolType (the attribute is absent from the file), OR a documented code comment explains the scoped-scan exclusion fallback (option b) citing Assumption A1
    - A real failing→passing enumeration test in LivePipeProtocolTests.cs calls McpClient.ListToolsAsync() (mirroring RoundTripTests.Handshake_ListsExactlyTheTwelveNamedTools) and asserts live_ping/live_reload_asset are ABSENT without --enable-live and PRESENT with it — the D-04 lock is verified at the TASK-0 boundary, not a branch/comment (C-10/CDX-NEW-5/6/CUR-NEW-8)
    - The off-state enumeration assert doubles as the explicit verification that WithToolsFromAssembly() skips a no-McpServerToolType class (CDX-NEW-6); the fallback (option b) is documented if the assumption proves false
    - `dotnet test Utinni.Mcp.Tests --filter LivePipe` exits 0 (the gating fact passes; other LivePipe facts may still be RED/stubbed until Tasks 1-3 — name the gating fact so it is independently runnable and green here)
    - The chosen single-registration-path lock is recorded in a LiveTools.cs comment referencing D-04 + A1
  </acceptance_criteria>
  <done>The D-04 gating mechanism is LOCKED in Task 0 via real McpClient.ListToolsAsync enumeration (RoundTripTests precedent): live_* tools reach the SDK only via the conditional WithTools registration, never the assembly scan — verified failing→passing now, not deferred.</done>
</task>

<task type="auto">
  <name>Task 1: LivePipeClient — named-pipe dispatch target (twin of CliDispatcher, never-hang, camelCase + string-tier-aligned to the net472 wire, protocolVersion-skew aware)</name>
  <files>Utinni.Mcp/Server/LivePipeClient.cs</files>
  <read_first>
    - Utinni.Mcp/Server/CliDispatcher.cs (the twin: non-sealed class, dual ctor with injectable timeout lines 56-74, CTS(_timeout) non-throwing hard-error result lines 121-138; PATTERNS lines 67-81)
    - Utinni.Mcp.Tests/DispatcherTests.cs (the stub-target + injectable-timeout + non-throwing-result test discipline the loopback test reuses)
    - .planning/phases/16-.../16-02-PLAN.md interfaces block (LiveBridgeProtocol: PipeName, ProtocolVersion, MaxFrameBytes, framing const, camelCase JSON policy, STRING-encoded tier, ack reloadAttempted/note, RELATIVE request path — the net472 wire the net10 client must mirror FIELD-FOR-FIELD)
    - Utinni.Cli.Tests/Fixtures/live/wire-*.json (the committed golden byte-vectors the net10 client must produce/consume byte-exact — CDX-NEW-1)
    - .planning/phases/16-.../16-RESEARCH.md A5 (LivePipeClient as a new dispatch target) + Pitfall 3 (no-client maps to clean listening-false, never hang)
  </read_first>
  <action>
    Create LivePipeClient.cs in Utinni.Mcp/Server/ as the named-pipe twin of CliDispatcher: a non-sealed class with LivePipeClient(string pipeName) and LivePipeClient(string pipeName, TimeSpan timeout) dual ctors (a SHORT connect timeout default, much smaller than CliDispatcher's 60s — a no-client ping must return fast). Make the connect/send method virtual so the loopback test can substitute a recording/stub.
    Declare the DUPLICATED canonical constants as single named consts in this file — `public const string PipeName = "utinni-live-bridge";`, `public const int ProtocolVersion = 1;`, `public const int MaxFrameBytes = 65536;`, and a framing-descriptor const string byte-identical to fixture line 2 — all written byte-identical to the 16-02 canonical values, with a code comment naming UtinniCoreDotNet/Live/LiveBridgeProtocol.cs as the documented source-of-truth and Utinni.Cli.Tests/Fixtures/live/{pipe-name.txt, wire-*.json} as the agreement anchors the Task-3 tests assert against (net10 cannot project-ref net472).
    C-03 ALIGNMENT + CUR-NEW-4/CUR-NEW-9: serialize requests / deserialize acks with System.Text.Json (in-box on net10) configured to the SAME wire shape the net472 JavaScriptSerializer emits — `JsonNamingPolicy.CamelCase` for property names AND the ReloadTier decoded from its STRING name (a `JsonStringEnumConverter`, or a string-typed `tier` property parsed via Enum.Parse — NOT the default integer). The request envelope carries `protocolVersion` (C-12) and the reload request's `path` is the RELATIVE asset path (CUR-NEW-1 — the server resolves it under its own client root). The wire bytes the net10 client emits MUST byte-exact-match the committed wire-request goldens and the acks it parses MUST match the wire-ack goldens — the Task-3 golden-vector assertions + cross-impl test prove this.
    PROTOCOLVERSION SKEW (CUR-NEW-5): if a received ack's `protocolVersion != ProtocolVersion`, surface it as a structured error result (not a silent parse / ignored fields) so the tool maps it to a clear MCP error rather than a misleading success.
    Mirror the never-hang discipline (CliDispatcher.cs:121-138): wrap NamedPipeClientStream Connect + send + read in a CancellationTokenSource(_timeout); on OperationCanceledException, a connect failure, or a server-close mid-message, return a hard-error/absent result object (listening-false for ping) — NEVER throw, NEVER hang (Pitfall 3). Use the LiveBridgeProtocol framing (4-byte LE length prefix + UTF-8 JSON), enforce MaxFrameBytes on read (reject oversize ack frames). Expose PingAsync() returning the ping result and ReloadAssetAsync(relativePath, ext) returning the ack result (incl. the reloadAttempted/note candor fields). Both return result objects the tools map; neither throws on transport failure.
  </action>
  <verify>
    <automated>cd D:/Code/Utinni; dotnet build Utinni.Mcp</automated>
  </verify>
  <acceptance_criteria>
    - LivePipeClient.cs contains NamedPipeClientStream, CancellationTokenSource, a `PipeName` const `"utinni-live-bridge"`, `ProtocolVersion`, `MaxFrameBytes`, and a framing-descriptor const byte-identical to fixture line 2, with a comment citing LiveBridgeProtocol + the pipe-name.txt + wire-*.json fixtures as the source-of-truth
    - System.Text.Json is configured `JsonNamingPolicy.CamelCase` AND decodes ReloadTier from its STRING name (JsonStringEnumConverter or string-typed property) to match the net472 wire (C-03 alignment / CUR-NEW-4 / CUR-NEW-9); the request envelope includes protocolVersion (C-12) and a RELATIVE reload `path` (CUR-NEW-1)
    - A received ack with a mismatched protocolVersion is surfaced as a structured error, not silently parsed (CUR-NEW-5)
    - Dual ctors exist; the default connect timeout is short (sub-second to a few seconds, well under CliDispatcher's 60s)
    - The connect/send method is virtual (substitutable by the loopback test)
    - No-server-listening, timeout, and server-close paths return a result object and never throw/hang (asserted in Task 3)
    - Utinni.Mcp builds clean
  </acceptance_criteria>
  <done>The pipe client mirrors CliDispatcher's never-hang discipline, speaks the shared LiveBridgeProtocol framing with camelCase + string-tier JSON aligned to the net472 wire, carries protocolVersion + a relative path, surfaces version skew, and pins the duplicated name + framing constants for the Task-3 fixture-equality + golden-vector + cross-impl assertions.</done>
</task>

<task type="auto">
  <name>Task 2: LiveTools bodies (live_ping + live_reload_asset, candid description, root reconciliation) + conditional registration in Program.cs (EnableLive-gated, incl. the client singleton — C-13)</name>
  <files>Utinni.Mcp/Tools/LiveTools.cs, Utinni.Mcp/Program.cs</files>
  <read_first>
    - Utinni.Mcp/Tools/ReadTools.cs (the thin-tool shape: DI params, root.Resolve FIRST, dispatch, map; lines 54-70)
    - Utinni.Mcp/Tools/RepackTool.cs (the non-readonly attribute shape ReadOnly=false, Idempotent=false; line ~59-74)
    - Utinni.Mcp/Program.cs (singleton-registration line 67 + the WithToolsFromAssembly() block lines 71-74 — the two edit sites)
    - Utinni.Mcp/Server/ResolvedRoot.cs (Resolve throws on escape; delegates to LooseOverridePath.Resolve — the SAME helper the in-client server uses, so the two roots are coherent — CUR-NEW-1; the fail-closed containment the live tier must NOT bypass)
    - .planning/phases/16-.../16-PATTERNS.md (Tool shapes lines 55-59; Program.cs conditional-registration lines 116-125)
  </read_first>
  <action>
    Fill LiveTools.cs (the non-McpServerToolType class from Task 0) with the D-02 minimal surface:
    - live_ping — McpServerTool(Name = "live_ping", ReadOnly = true, Idempotent = true), takes LivePipeClient pipe (DI), no path arg. Calls pipe.PingAsync(), maps to a CallToolResult reporting listening/gameRunning/pid (or listening-false when no client). Honest absence reporting — never an error/hang (Pitfall 3).
    - live_reload_asset — McpServerTool(Name = "live_reload_asset", ReadOnly = false, Idempotent = false), takes ResolvedRoot root, LivePipeClient pipe, string relativePath. ROOT RECONCILIATION (CUR-NEW-1): call root.Resolve(relativePath) FIRST to VALIDATE the path is contained under the MCP-pinned root (the fail-closed boundary — RESEARCH A5 / Security V4 / T-16-21; throws on escape → SDK tool error). Then derive the ext and send the RELATIVE path (NOT the resolved absolute) over the wire via pipe.ReloadAssetAsync(relativePath, ext) — the in-client server resolves the SAME relative path under its OWN pinned client root (16-02), which has the SAME LooseOverridePath semantics, so the two roots are coherent and an absolute path never crosses the pipe (closing the injectRoot-vs-client-root mismatch). Map to accepted/tier/queued/reloadAttempted/path/note; the tier comes verbatim from the in-client ReloadAssetClassifier via the ack.
      CANDOR (C-14): the McpServerTool Description (and the mapped result) MUST state explicitly that this QUEUES a best-effort reload over the bridge and that `queued:true` does NOT mean a visible reload occurred — it is transport-only until RESID-03 (reloadAttempted is always false this phase). The description an agent reads must not let `queued` be misread as "preview happened."
    In Program.cs: gate the live tier on serverArgs.EnableLive. Register the LivePipeClient singleton ONLY under the EnableLive guard (C-13 — do not register it unconditionally when the tier is off): `if (serverArgs.EnableLive) { builder.Services.AddSingleton(new LivePipeClient(LivePipeClient.PipeName)); ...WithTools<LiveTools>()... }`. Keep WithToolsFromAssembly() for the existing read/save/repack tools OUTSIDE the guard. Add LiveTools via WithTools ONLY inside the EnableLive guard (Task 0 lock — never via the assembly scan; fail-closed by absence, D-04).
  </action>
  <verify>
    <automated>cd D:/Code/Utinni; dotnet build Utinni.Mcp</automated>
  </verify>
  <acceptance_criteria>
    - LiveTools.cs defines live_ping (ReadOnly=true) and live_reload_asset (ReadOnly=false); live_reload_asset calls root.Resolve(relativePath) for validation BEFORE any pipe send AND sends the RELATIVE path on the wire (NOT the resolved absolute — CUR-NEW-1 root reconciliation)
    - The live_reload_asset tool Description states queued != visible reload (transport-only until RESID-03; reloadAttempted=false) — C-14
    - Program.cs registers the LivePipeClient singleton AND the WithTools<LiveTools> registration BOTH only under the serverArgs.EnableLive guard (C-13); LiveTools is never registered via WithToolsFromAssembly
    - Utinni.Mcp builds clean
  </acceptance_criteria>
  <done>The agent-facing live_* tools exist with candid reload semantics, validate against the MCP root then send the RELATIVE path so the in-client server's own-root resolution is coherent (CUR-NEW-1), route through the pipe client, and (client singleton + tool registration) are both gated on --enable-live.</done>
</task>

<task type="auto">
  <name>Task 3: Loopback protocol test + GOLDEN WIRE byte-equality (net10) + REAL cross-impl integration test (net472) + pipe-name/framing fixture-equality + MCP-SECURITY.md live-tier addendum (correct path)</name>
  <files>Utinni.Mcp.Tests/LivePipeProtocolTests.cs, Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj, UtinniCoreDotNet.Tests/Live/LiveBridgeIntegrationTests.cs, UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj, .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/MCP-SECURITY.md</files>
  <read_first>
    - Utinni.Mcp.Tests/DispatcherTests.cs (the stub-target + injectable-timeout + non-throwing-result test shape to clone; lines 50-70; PATTERNS lines 170-184)
    - Utinni.Mcp.Tests/RoundTripTests.cs (the McpClient.ListToolsAsync enumeration pattern — the Task-0 gating fact lives here; this task may extend it but the gating lock already landed in Task 0)
    - Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj (the net10 test project to add the linked-fixture Content entries to: pipe-name.txt + wire-*.json)
    - UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj (the net472 test project that ALREADY refs UtinniCoreDotNet — the home for the real-server cross-impl test; it also already links the fixtures via 16-02 Task 1)
    - Utinni.Mcp/Server/LivePipeClient.cs (Task 1 — the client under test, incl. the duplicated PipeName + framing consts + camelCase/string-tier STJ)
    - UtinniCoreDotNet/Live/LivePipeServer.cs + LiveBridgeProtocol.cs (16-02 — the real server + protocol the net472 integration test drives)
    - Utinni.Cli.Tests/Fixtures/live/wire-*.json (the golden byte-vectors both sides assert byte-exact — CDX-NEW-1)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/MCP-SECURITY.md (the Phase-14 threat register to extend with a Phase-16 live-tier section — THIS is the real on-disk path, C-11)
    - .planning/phases/16-.../16-RESEARCH.md A6 (loopback harness design) + Security Domain (the exact ASVS rows + STRIDE table + accepted-risk analog AR-14-03/04 to fold in)
  </read_first>
  <action>
    (A) net10 loopback + golden-wire byte-equality + fixture-equality (Utinni.Mcp.Tests):
    Link the canonical fixture + golden vectors into the net10 test project: in Utinni.Mcp.Tests.csproj add `<Content Include="..\Utinni.Cli.Tests\Fixtures\live\pipe-name.txt" Link="Fixtures\live\pipe-name.txt" CopyToOutputDirectory="PreserveNewest" />` AND the four `wire-*.json` files (Link form — the SAME committed files 16-02 created; no fork).
    Extend LivePipeProtocolTests.cs (the Task-0 gating fact already lives here; --filter LivePipe selects the class). Stand up a NamedPipeServerStream test-stub on a randomized pipe name implementing the ping + reload-asset protocol (mirror the LiveBridgeProtocol framing), point a LivePipeClient at it, and assert:
    - GOLDEN WIRE byte-equality (CDX-NEW-1=CUR-NEW-2): assert the net10 client SERIALIZES the canonical ping/reload request envelopes to bytes byte-EXACT matching the committed wire-ping-request.json / wire-reload-request.json, AND DESERIALIZES the committed wire-ping-ack.json / wire-reload-ack.json into the expected result objects (camelCase keys, string-encoded tier "PendingNextSceneChange", reloadAttempted=false). These are the SAME files the net472 server asserts in 16-02 — so a net10 camelCase/tier/protocolVersion drift fails HERE.
    - live_ping round-trips: stub replies listening-true with gameRunning/pid; the client maps to the expected result.
    - live_reload_asset: the client sends a well-formed request envelope (assert the bytes the client SENT parse to op=reload-asset + protocolVersion + the RELATIVE path + ext — CUR-NEW-1), the stub replies an ack with a string tier + reloadAttempted=false + note, the client maps accepted/tier/queued/reloadAttempted/path/note.
    - Framing edge cases: partial-read frame, oversize body (exceeds MaxFrameBytes), stub closes mid-message — each returns a hard error result, never a hang (injectable short timeout; assert the call returns within the timeout window).
    - protocolVersion skew (CUR-NEW-5): the stub replies an ack with protocolVersion=999; assert the client surfaces a structured error, not a silent success.
    - No-client: point the client at a pipe name no server owns; assert PingAsync returns listening-false quickly and never throws/hangs (Pitfall 3).
    - Pipe-name + framing fixture-equality (C-05): read Fixtures/live/pipe-name.txt; assert line 1 byte-equals LivePipeClient.PipeName AND line 2 byte-equals LivePipeClient's framing-descriptor const (name AND framing/maxframe/protocol/json-policy/tier-encoding pinned on the net10 side).
    (The D-04 real-enumeration gating assertion already landed in Task 0 — do not duplicate; reference it.)

    (B) REAL cross-implementation integration test (UtinniCoreDotNet.Tests, net472 — C-02):
    Write LiveBridgeIntegrationTests.cs in UtinniCoreDotNet.Tests/Live/ (name so --filter LiveBridgeIntegration selects it). This is the only side that can reference the real LivePipeServer (TFM wall). Spin up the REAL LivePipeServer (16-02) on a RANDOMIZED pipe name with a pinned temp CLIENT root + an injected Func<bool> gameIsRunning, drive it with a client built on the shared LiveBridgeProtocol framing/serialization (the same wire the net10 LivePipeClient mirrors field-for-field), and assert over the ACTUAL pipe:
    - ping round-trips: real server replies listening-true (+ gameRunning per the injected scalar / pid).
    - reload-asset round-trips: send a RELATIVE path CONTAINED under the temp client root → the real server resolves it under ITS OWN pinned root (CUR-NEW-1), returns a real ReloadAssetClassifier tier (string-encoded), queued=true (gameIsRunning=true) / Unavailable+queued=false (false), reloadAttempted=false, and enqueues exactly one Action (assert via the 16-02 injectable enqueue sink).
    - reload-asset with an absolute / `..`-escaping path → the real server returns accepted=false + Rejected and NO enqueue (the C-04/CUR-NEW-1 server-side client-root containment, proven over the wire).
    - root reconciliation (CUR-NEW-1): a case where the MCP-resolved root and the server's pinned root are the SAME temp client tree → a contained relative reload succeeds; a case where they differ → the server rejects/does-not-resolve a path that isn't contained under ITS root (predictable rejection), proving the server uses its own pinned root not a wire-supplied absolute.
    This makes depends_on:[16-02] PROTOCOL-load-bearing (real server + real framing round-trip), not just pipe-name literal equality. (net10 agreement with this exact wire is held by the shared golden byte-vectors + framing fixture both sides assert in part (A) + Task 1.)

    (C) MCP-SECURITY.md addendum at the CORRECT path (C-11):
    Extend .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/MCP-SECURITY.md (the real on-disk Phase-14 deliverable — there is NO Utinni.Mcp/MCP-SECURITY.md) with a Phase-16 live-tier section folding in RESEARCH Security Domain verbatim: the ASVS rows (V1/V4/V5/V13/V7), the STRIDE table (unauthorized-pipe-connect with the exact current-user ACL construction, agent-reaches-live-when-off, MCP-side path-escape AND in-client client-root containment, the wire-golden + protocolVersion-skew protocol-drift mitigation, frame-DoS incl. server-side partial-read + bounded timeouts, wrong-thread/native-deref crash with the Func-injected game-state rule, pipe-squatting, visible-render-over-promise candor), each mitigate row citing the proving test/file (incl. the net472 cross-impl test + the golden-wire byte-vectors + the in-client client-root containment test + the C-06 in-client start gate). DOCUMENT THE DUAL-FLAG OPERATOR CONTRACT (CUR-NEW-6): both --enable-live AND in-client [Live] enableLiveBridge are required; neither alone is sufficient; --enable-live/pipe-name are identifiers not secrets. Add the accepted-risk row (single-user local desktop; analog AR-14-03/04). Note no new packages (net472 server uses in-box JavaScriptSerializer — C-03; net10 uses in-box System.Text.Json; Package Legitimacy Gate not triggered). Optionally leave a one-line pointer from Utinni.Mcp (a comment in Program.cs) to the doc's real location.
  </action>
  <verify>
    <automated>cd D:/Code/Utinni; dotnet test Utinni.Mcp.Tests --no-build --filter LivePipe && dotnet test UtinniCoreDotNet.Tests --no-build --filter LiveBridgeIntegration</automated>
  </verify>
  <acceptance_criteria>
    - Utinni.Mcp.Tests.csproj links ..\Utinni.Cli.Tests\Fixtures\live\pipe-name.txt AND the four wire-*.json files (same committed files, Link form)
    - `dotnet test Utinni.Mcp.Tests --filter LivePipe` exits 0; covers GOLDEN-WIRE byte-equality (net10 produces the request goldens + consumes the ack goldens byte-exact, camelCase + string-tier — CDX-NEW-1), ping round-trip, reload-asset well-formed-envelope (RELATIVE path + protocolVersion) + ack mapping (reloadAttempted/note), partial-read/oversize/server-close hard-error (no hang), protocolVersion-skew structured-error (CUR-NEW-5), no-client listening-false, and fixture byte-equality on BOTH line 1 (PipeName) and line 2 (framing incl. tier-encoding)
    - `dotnet test UtinniCoreDotNet.Tests --filter LiveBridgeIntegration` exits 0; the REAL LivePipeServer round-trips ping + reload-asset (contained relative + rejected absolute/escape) over an actual pipe via the shared LiveBridgeProtocol, with the server resolving the relative path under ITS OWN pinned client root + the same/different-root reconciliation case (C-02 + CUR-NEW-1) — the depends_on:[16-02] edge is now a wire-protocol proof
    - Every framing-edge-case test completes within the injected short timeout (proves never-hang)
    - The D-04 gating assertion (real McpClient.ListToolsAsync enumeration) is present and green — landed in Task 0, referenced here (C-10/CDX-NEW-5/6)
    - .planning/phases/14-.../MCP-SECURITY.md (the real path — C-11) contains a Phase-16 live-tier section with the ASVS rows, the STRIDE table (incl. in-client client-root containment + server-side partial-read + bounded timeouts + Func-injected game-state + wire-golden/protocolVersion-skew drift mitigation + exact ACL), per-row test citations, the DUAL-FLAG operator contract (CUR-NEW-6), and the accepted-risk analog; no Utinni.Mcp/MCP-SECURITY.md orphan is created
  </acceptance_criteria>
  <done>The full pipe protocol + envelope mapping + D-04 real-enumeration gate (Task 0) + cross-plan name/framing agreement + GOLDEN-WIRE byte-equality across the TFM wall + the REAL cross-impl wire round-trip with client-root reconciliation are proven without a live client; the Phase-14 security register (correct path) documents the live-tier surface + the dual-flag operator contract.</done>
</task>

</tasks>

<threat_model>
**Gate verdict: ASVS L1; no HIGH-severity threats; security gate passes.** (All threats below are mitigate or accept; none rated HIGH. Block-on-HIGH posture: had any threat been HIGH it would gate the wave — none are.)

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI agent -> MCP host (stdio) | Untrusted tool-call args (relativePath) cross into the host |
| MCP host -> named pipe | The host's pipe client crosses the OS named-pipe boundary into the injected client; only a RELATIVE path crosses (CUR-NEW-1), resolved server-side under the in-client client root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-20 | Elevation | Agent reaches live_* when the tier is not intended to be on | mitigate | D-04 fail-closed gate: live_* tools AND the LivePipeClient singleton are UNREGISTERED unless --enable-live (C-13); the pipe name is not advertised. Acceptance: Task 0 gating assertion via REAL McpClient.ListToolsAsync enumeration (off => tools absent — C-10/CDX-NEW-5/6, locked in Task 0). |
| T-16-21 | Tampering | Path-escape via live_reload_asset path arg (MCP side) | mitigate | ResolvedRoot.Resolve runs BEFORE any pipe send (throws on escape -> SDK tool error). The RELATIVE path is then sent on the wire and re-resolved server-side under the in-client client root via the SAME LooseOverridePath helper (root reconciliation — CUR-NEW-1), so the MCP root and the server root are coherent and no absolute crosses the pipe. Acceptance: Task 2 root.Resolve-then-send-relative + Task 3 (B) same/different-root reconciliation. NOTE: the in-client server ALSO independently contains the path (T-16-14, 16-02) so a same-user direct-pipe process is covered too. |
| T-16-22 | DoS | Malformed/oversize/partial ack frame hangs the MCP call | mitigate | Injectable short connect/read timeout + bounded MaxFrameBytes; transport failure returns a result object, never a hang. Acceptance: Task 3 framing-edge-case tests complete within the timeout. |
| T-16-23 | Spoofing | No client listening -> naive client hangs the MCP call | mitigate | Short connect timeout maps no-server to listening-false (Pitfall 3). Acceptance: Task 3 no-client test returns quickly. |
| T-16-24 | (candor) | Visible-render over-promise | mitigate (correctness) | Ack reports the honest ReloadAssetClassifier tier (string-encoded) with reloadAttempted=false + a transport-only note; the tool DESCRIPTION states queued != visible reload (C-14); render is best-effort, NOT a success gate (D-01). |
| T-16-25 | Tampering (protocol drift) | net10/net472 wire shapes silently diverge (framing, JSON casing, tier encoding, field names, protocol version) → a green net472-on-net472 test hides a broken net10 path | mitigate | Committed GOLDEN WIRE BYTE-VECTORS (ping/reload request+ack) asserted byte-EXACT on BOTH the net472 (16-02) and net10 (Task 3 A) sides — CDX-NEW-1=CUR-NEW-2; net10 STJ camelCase + string-tier aligned (C-03/CUR-NEW-4/CUR-NEW-9); protocolVersion in the envelope + a skew-surfaces-structured-error test (C-12/CUR-NEW-5); AND a real net472 cross-impl round-trip (C-02). Acceptance: Task 3 (A) golden-wire byte-equality + skew + fixture-equality + (B) LiveBridgeIntegration. |
| T-16-AR16 | (accepted risk) | Single-user local desktop pipe; --enable-live / pipe-name are identifiers not secrets | accept | Local-only named pipe + current-user ACL (server side, 16-02, exact construction) + in-client start gate (C-06, 16-02 Task 3); DUAL-flag operator contract documented (CUR-NEW-6); analog AR-14-03/04; logged in MCP-SECURITY.md. |
| T-16-SC | Tampering | npm/pip/cargo installs | n/a | No new packages — net10 uses in-box System.Text.Json; net472 uses in-box JavaScriptSerializer (C-03); already-pinned ModelContextProtocol 1.4.0 only (RESEARCH Package Legitimacy Audit: gate not triggered). |
</threat_model>

<verification>
- `dotnet test Utinni.Mcp.Tests --filter LivePipe` green (golden-wire byte-equality + loopback ping + reload-asset relative-path-envelope + framing edge cases + protocolVersion skew + no-client + name/framing fixture-equality + the Task-0 D-04 real-enumeration gating).
- `dotnet test UtinniCoreDotNet.Tests --filter LiveBridgeIntegration` green (REAL LivePipeServer cross-impl round-trip over an actual pipe via the shared LiveBridgeProtocol, with server-own-root resolution + root reconciliation — C-02/CUR-NEW-1).
- Utinni.Mcp builds clean with the EnableLive-gated WithTools registration (+ gated client singleton).
- .planning/phases/14-.../MCP-SECURITY.md (correct path, C-11) carries the Phase-16 live-tier section + the dual-flag operator contract.
- Tier-4 manual: live-injected confirmation (launch with BOTH the in-client [Live] enableLiveBridge ON AND --enable-live, live_ping then live_reload_asset against an edited asset) — non-gating per D-01, documented in 16-VALIDATION.md.
</verification>

<success_criteria>
- MCP-03: an agent can drive the live-injected client over the MCP bridge (named-pipe IPC); the ack round-trip is proven over BOTH a loopback pipe (net10 Tier-2, incl. byte-exact golden-wire agreement) AND a real cross-impl round-trip against the actual 16-02 server (net472 Tier-2, C-02). Visible re-render is best-effort and explicitly NOT a Phase-16 success gate (D-01).
- The live tier is fail-closed by absence (D-04, gated client + tools, proven via real McpClient.ListToolsAsync enumeration locked in Task 0 — C-10/CDX-NEW-5/6) and the minimal surface (ping + reload-asset only, D-02) with candid reload semantics (C-14).
- The net10 duplicated pipe-name literal AND framing constants are proven byte-identical to the canonical fixture (name + framing), AND the actual JSON wire bytes match the golden vectors byte-exact across the TFM wall (CDX-NEW-1); the cross-stack JSON is camelCase + string-tier-aligned (C-03/CUR-NEW-4/CUR-NEW-9); protocolVersion skew is surfaced (CUR-NEW-5); root reconciliation is coherent (relative-on-wire, server-own-root resolution — CUR-NEW-1); the wave-2 depends_on edge is a wire-protocol proof.
- The out-of-proc anti-pattern holds: the SDK never enters SWG.exe; only the narrow named pipe crosses, carrying only a relative path. The dual-flag operator contract is documented (CUR-NEW-6).
</success_criteria>

<output>
Create `.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-03-SUMMARY.md` when done.
</output>
