---
phase: 16-live-injected-mcp-bridge-blender-ecosystem-boundary
plan: 03
type: execute
wave: 2
depends_on: [16-02]
files_modified:
  - Utinni.Mcp/Server/LivePipeClient.cs
  - Utinni.Mcp/Tools/LiveTools.cs
  - Utinni.Mcp/Program.cs
  - Utinni.Mcp.Tests/LivePipeProtocolTests.cs
  - Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj
  - UtinniCoreDotNet.Tests/Live/LiveBridgeIntegrationTests.cs
  - UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj
  - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/MCP-SECURITY.md
autonomous: true
requirements: [MCP-03]
user_setup: []

must_haves:
  truths:
    - "live_ping over a loopback pipe round-trips to a stub server and maps to a clean MCP result; when no server listens it returns { listening: false } and never hangs"
    - "live_reload_asset resolves its path under the fail-closed ResolvedRoot BEFORE any pipe send, sends a well-formed envelope, and maps the ack tier"
    - "A REAL cross-implementation integration test (net472) spins up the real 16-02 LivePipeServer on a randomized pipe and round-trips ping + reload-asset over the ACTUAL wire using the shared LiveBridgeProtocol framing/serialization — proving wire-protocol agreement, not just pipe-name literal equality (C-02)"
    - "When --enable-live is OFF (default), the live_* tools are NOT advertised (asserted through the REAL host-building / tool-enumeration path, not a branch/comment — C-10); when ON they ARE advertised"
    - "Pipe framing edge cases (partial read / oversize / server-close mid-message) return a hard error, never a hang"
    - "The net10 duplicated PipeName literal byte-equals canonical fixture line 1 AND the net10 framing constants byte-equal fixture line 2 (name + framing both pinned — C-05), proven by a test, so the cross-plan duplicate cannot drift from the in-client server"
    - "The live_reload_asset tool DESCRIPTION + ack make clear that queued != visible reload (transport-only until RESID-03; reloadAttempted=false) — C-14 candor"
  artifacts:
    - path: "Utinni.Mcp/Server/LivePipeClient.cs"
      provides: "Named-pipe client dispatch target (twin of CliDispatcher; injectable-timeout dual ctor, never-hang discipline); net10 System.Text.Json configured camelCase to match the net472 JavaScriptSerializer wire"
      contains: "NamedPipeClientStream"
    - path: "Utinni.Mcp/Tools/LiveTools.cs"
      provides: "live_ping + live_reload_asset thin tools (NOT McpServerToolType-decorated; registered only via WithTools-of-T); reload tool description carries the transport-only candor"
      contains: "live_ping"
    - path: "Utinni.Mcp/Program.cs"
      provides: "Conditional WithTools-of-LiveTools gated on serverArgs.EnableLive; LivePipeClient singleton registered only when EnableLive (C-13)"
      contains: "WithTools"
    - path: "UtinniCoreDotNet.Tests/Live/LiveBridgeIntegrationTests.cs"
      provides: "REAL LivePipeServer round-trip over an actual pipe via LiveBridgeProtocol (C-02 cross-impl proof; net472 — the only side that can reference the real server)"
      contains: "LivePipeServer"
  key_links:
    - from: "Utinni.Mcp/Program.cs"
      to: "WithTools-of-LiveTools"
      via: "if (serverArgs.EnableLive) ... (fail-closed by absence)"
      pattern: "serverArgs\\.EnableLive"
    - from: "Utinni.Mcp/Tools/LiveTools.cs"
      to: "ResolvedRoot.Resolve"
      via: "live_reload_asset path resolution before pipe send"
      pattern: "root\\.Resolve"
    - from: "Utinni.Mcp/Server/LivePipeClient.cs"
      to: "LiveBridgeProtocol.PipeName (duplicated literal + framing)"
      via: "shared pipe-name constant + framing (duplicated, fixture-asserted line 1 + line 2)"
      pattern: "PipeName"
    - from: "UtinniCoreDotNet.Tests/Live/LiveBridgeIntegrationTests.cs"
      to: "UtinniCoreDotNet.Live.LivePipeServer + LiveBridgeProtocol"
      via: "real server + real protocol round-trip over an actual NamedPipe (C-02)"
      pattern: "new LivePipeServer"
    - from: "Utinni.Mcp.Tests/LivePipeProtocolTests.cs"
      to: "Utinni.Cli.Tests/Fixtures/live/pipe-name.txt"
      via: "byte-equality on the net10 duplicated PipeName (line 1) + framing constants (line 2)"
      pattern: "pipe-name\\.txt"
---

<objective>
Build the out-of-proc MCP host half of the live bridge (MCP-03): the LivePipeClient named-pipe dispatch target (structural twin of CliDispatcher with the injectable-timeout, never-hang discipline), the live_ping + live_reload_asset tools (the D-02 minimal surface), and the D-04 fail-closed gating in Program.cs (conditional WithTools registration of LiveTools gated on serverArgs.EnableLive, NOT the assembly scan). Prove the whole protocol with (a) a loopback NamedPipe test (net10, no live client) AND (b) a REAL cross-implementation integration test (net472) that round-trips the real 16-02 LivePipeServer over the actual wire — and extend the Phase-14 MCP-SECURITY.md (at its real on-disk path) with the live-tier threat surface.

Because net10 `Utinni.Mcp` cannot project-ref net472 `UtinniCoreDotNet`, this plan DUPLICATES the canonical pipe-name literal `utinni-live-bridge` AND the framing constants. To make the `depends_on: [16-02]` edge enforce real agreement (BLOCKER fix + C-02 + C-05), this plan: (1) links the canonical fixture committed by 16-02 and asserts the net10 duplicated literal byte-equals fixture LINE 1 and the net10 framing constants byte-equal fixture LINE 2; AND (2) adds a real-wire integration test on the net472 side (the only side that can reference the real LivePipeServer) exercising the real server against a LiveBridgeProtocol-driven client. Drift in the literal, the framing, or the wire shape now FAILS a Tier-2 test instead of surfacing only at the non-gating Tier-4 smoke.

Purpose: Close MCP-03's automated (Tier-2) deliverable — the agent-facing tools, the pipe client, the fail-closed gate (proven via real tool enumeration), the cross-plan pipe-name + framing agreement, the real cross-impl wire round-trip, and the loopback proof — leaving only the Tier-4 manual live-injection confirmation (non-gating per D-01).
Output: LivePipeClient (camelCase-aligned net10 JSON), LiveTools (with candid reload description), conditional registration, loopback protocol tests (incl. fixture byte-equality on name+framing + real-tool-enumeration gating assertion), the net472 cross-impl integration test, MCP-SECURITY.md live-tier addendum at the correct path.

REVIEW-DRIVEN EDITS (from 16-REVIEWS.md): C-02 (real client↔server cross-impl integration test — placed net472 due to the TFM wall), C-05 (framing line-2 enforced on the net10 side), C-10 (D-04 gating asserted via the real host-building / tool-enumeration path), **C-11** (MCP-SECURITY.md is at .planning/phases/14-.../ — CONFIRMED; not Utinni.Mcp/), C-12 (protocolVersion in the request envelope), C-13 (LivePipeClient registered only under the EnableLive guard), C-14 (reload tool description + ack candor), C-03-alignment (net10 System.Text.Json configured camelCase field-for-field to the net472 JavaScriptSerializer wire).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-CONTEXT.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-RESEARCH.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-PATTERNS.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-VALIDATION.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-02-PLAN.md

<interfaces>
From UtinniCoreDotNet/Live/LiveBridgeProtocol.cs (16-02):
  static class LiveBridgeProtocol  // PipeName constant; ProtocolVersion=1; MaxFrameBytes=65536;
  // a framing-descriptor const == fixture line 2; request/ack envelope DTOs (JavaScriptSerializer, camelCase);
  // 4-byte LE length-prefix framing; bounded max size.
  // CANONICAL pipe name value = "utinni-live-bridge" (16-02 fixture line 1). Framing descriptor = fixture line 2
  // ("frame=len32le+utf8json;maxframe=65536;protocol=1;json=camelCase"). net10 Utinni.Mcp cannot project-ref net472,
  // so duplicate the EXACT literal + framing constants in Utinni.Mcp, cite LiveBridgeProtocol as the single
  // documented source-of-truth in a code comment, AND bind both the name (line 1) and the framing (line 2) to the
  // canonical fixture Utinni.Cli.Tests/Fixtures/live/pipe-name.txt via the byte-equality tests in Task 3.
  // The ack reload result carries reloadAttempted (always false this phase) + a transport-only note (C-14).

From Utinni.Mcp/Server/CliDispatcher.cs (:56-74, :121-138) — the twin to clone:
  public class CliDispatcher { public static readonly TimeSpan DefaultTimeout = TimeSpan.FromSeconds(60);
    public CliDispatcher(string cliExePath) : this(cliExePath, DefaultTimeout) {}
    public CliDispatcher(string cliExePath, TimeSpan timeout) {...}
    public virtual async Task RunAsync(string verb, IReadOnlyList args) {...} }
  // CTS(_timeout) -> on cancel return a hard-error result object, never throw, never hang.

From Utinni.Mcp/Tools/ReadTools.cs (:54-70) — the thin-tool shape:
  [McpServerTool(Name="read_tre", ReadOnly=true, Idempotent=true)]
  public static async Task ReadTre(ResolvedRoot root, CliDispatcher cli, string relativePath)
  { string abs = root.Resolve(relativePath); ... return CliResultMapper.ToCallToolResult(r); }

From Utinni.Mcp/Program.cs (:65-74):
  var serverArgs = ServerArgs.Parse(args);  // now exposes serverArgs.EnableLive (16-02 Task 2)
  builder.Services.AddSingleton(new CliDispatcher(cliExePath));
  builder.Services.AddMcpServer().WithStdioServerTransport().WithToolsFromAssembly();

ON-DISK FACT (C-11, CONFIRMED): MCP-SECURITY.md exists ONLY at
  .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/MCP-SECURITY.md
  (there is NO Utinni.Mcp/MCP-SECURITY.md). Extend the Phase-14 file in place.

TFM WALL (C-02 placement rationale): UtinniCoreDotNet.Tests = net472 (refs UtinniCoreDotNet → can see the real
  LivePipeServer + LiveBridgeProtocol); Utinni.Mcp.Tests = net10 (refs Utinni.Mcp → can see the real LivePipeClient).
  NEITHER can reference both. So the cross-impl real-server round-trip (C-02) lives in UtinniCoreDotNet.Tests (net472),
  exercising the real LivePipeServer against a LiveBridgeProtocol-driven client; the net10 LivePipeClient's agreement
  with that wire is held by the field-for-field camelCase JSON policy + the framing fixture (line 2) both sides assert.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave-0 spike — confirm WithTools-of-LiveTools gating excludes the assembly scan (Assumption A1), with a REAL tool-enumeration probe (C-10)</name>
  <files>Utinni.Mcp/Tools/LiveTools.cs</files>
  <read_first>
    - Utinni.Mcp/Program.cs (the current WithToolsFromAssembly() block, lines 71-74; the scan registers every McpServerToolType)
    - Utinni.Mcp/Tools/ReadTools.cs (a McpServerToolType class the scan DOES pick up — the contrast case)
    - .planning/phases/16-.../16-RESEARCH.md A5 + Pitfall 1 + Assumptions A1 (a non-McpServerToolType class registered only via WithTools-of-T must NOT be grabbed by WithToolsFromAssembly())
  </read_first>
  <action>
    Create LiveTools.cs as a static class that does NOT carry the McpServerToolType attribute (RESEARCH Pitfall 1 — if it did, WithToolsFromAssembly() would register it unconditionally and defeat the D-04 gate). Add the two McpServerTool-attributed method stubs (live_ping, live_reload_asset) returning a placeholder CallToolResult for now (Task 2 fills the bodies).
    Verify the gating mechanism via the REAL host-building / tool-enumeration path (C-10), not a branch/comment: build the actual MCP host two ways (with WithToolsFromAssembly() alone, and with the conditional WithTools<LiveTools>() added) and enumerate the ADVERTISED tool list through the SDK's tool-enumeration surface (the same registration path Program.cs uses). Assert live_ping/live_reload_asset are ABSENT from the assembly-scan-only build and PRESENT in the conditional-WithTools build. (This assertion is added/finalized in Task 3's test, but Task 0 confirms the enumeration approach is feasible at SDK 1.4.0 in the spike.) If SDK 1.4.0 cannot enumerate advertised tools in-test, fall back to RESEARCH option (b): scope the assembly scan to exclude the live class (sub-namespace / separate registration) — document whichever lock you land in a code comment citing A1, and STILL assert via the closest-to-real registration introspection available (not a bare branch/comment).
    Lock the outcome: exactly one registration path for live_* = the conditional WithTools registration; the assembly scan must not also register it.
  </action>
  <verify>
    <automated>cd D:/Code/Utinni; dotnet build Utinni.Mcp</automated>
  </verify>
  <acceptance_criteria>
    - LiveTools.cs exists and is NOT decorated with McpServerToolType (the attribute is absent from the file), OR a documented code comment explains the scoped-scan exclusion fallback (option b) citing Assumption A1
    - The spike confirms the tool list can be enumerated through the real host-building path (or documents the closest-to-real introspection fallback) — the D-04 gating assertion in Task 3 will use it, NOT a branch/comment (C-10)
    - Utinni.Mcp builds clean
    - The chosen single-registration-path lock is recorded in a LiveTools.cs comment referencing D-04 + A1
  </acceptance_criteria>
  <done>The D-04 gating mechanism is confirmed via real tool enumeration: live_* tools reach the SDK only via the conditional WithTools registration, never the assembly scan.</done>
</task>

<task type="auto">
  <name>Task 1: LivePipeClient — named-pipe dispatch target (twin of CliDispatcher, never-hang, camelCase-aligned to the net472 wire)</name>
  <files>Utinni.Mcp/Server/LivePipeClient.cs</files>
  <read_first>
    - Utinni.Mcp/Server/CliDispatcher.cs (the twin: non-sealed class, dual ctor with injectable timeout lines 56-74, CTS(_timeout) non-throwing hard-error result lines 121-138; PATTERNS lines 67-81)
    - Utinni.Mcp.Tests/DispatcherTests.cs (the stub-target + injectable-timeout + non-throwing-result test discipline the loopback test reuses)
    - .planning/phases/16-.../16-02-PLAN.md interfaces block (LiveBridgeProtocol: PipeName, ProtocolVersion, MaxFrameBytes, framing const, camelCase JSON policy, ack reloadAttempted/note — the net472 wire the net10 client must mirror FIELD-FOR-FIELD)
    - UtinniCoreDotNet/Formats/ObjectTemplate/ObjectTemplateSchemaLoader.cs (:81 — the net472 side uses JavaScriptSerializer; the net10 client must serialize/deserialize the SAME camelCase field names via System.Text.Json with a matching naming policy — C-03 alignment)
    - .planning/phases/16-.../16-RESEARCH.md A5 (LivePipeClient as a new dispatch target) + Pitfall 3 (no-client maps to clean listening-false, never hang)
  </read_first>
  <action>
    Create LivePipeClient.cs in Utinni.Mcp/Server/ as the named-pipe twin of CliDispatcher: a non-sealed class with LivePipeClient(string pipeName) and LivePipeClient(string pipeName, TimeSpan timeout) dual ctors (a SHORT connect timeout default, much smaller than CliDispatcher's 60s — a no-client ping must return fast). Make the connect/send method virtual so the loopback test can substitute a recording/stub.
    Declare the DUPLICATED canonical constants as single named consts in this file — `public const string PipeName = "utinni-live-bridge";`, `public const int ProtocolVersion = 1;`, `public const int MaxFrameBytes = 65536;`, and a framing-descriptor const string byte-identical to fixture line 2 — all written byte-identical to the 16-02 canonical values, with a code comment naming UtinniCoreDotNet/Live/LiveBridgeProtocol.cs as the documented source-of-truth and Utinni.Cli.Tests/Fixtures/live/pipe-name.txt as the agreement anchor the Task-3 tests assert against (net10 cannot project-ref net472).
    C-03 ALIGNMENT: serialize requests / deserialize acks with System.Text.Json (in-box on net10) configured to the SAME field-naming policy the net472 JavaScriptSerializer emits — camelCase property names (JsonNamingPolicy.CamelCase), matching the documented policy in fixture line 2. The request envelope carries `protocolVersion` (C-12) so a future skew is detectable. The wire bytes the net10 client emits MUST parse as the net472 server's request DTO and vice-versa for the ack — the Task-3 cross-impl test (net472) and the loopback test together prove this.
    Mirror the never-hang discipline (CliDispatcher.cs:121-138): wrap NamedPipeClientStream Connect + send + read in a CancellationTokenSource(_timeout); on OperationCanceledException, a connect failure, or a server-close mid-message, return a hard-error/absent result object (listening-false for ping) — NEVER throw, NEVER hang (Pitfall 3). Use the LiveBridgeProtocol framing (4-byte LE length prefix + UTF-8 JSON), enforce MaxFrameBytes on read (reject oversize ack frames). Expose PingAsync() returning the ping result and ReloadAssetAsync(absolutePath, ext) returning the ack result (incl. the reloadAttempted/note candor fields). Both return result objects the tools map; neither throws on transport failure.
  </action>
  <verify>
    <automated>cd D:/Code/Utinni; dotnet build Utinni.Mcp</automated>
  </verify>
  <acceptance_criteria>
    - LivePipeClient.cs contains NamedPipeClientStream, CancellationTokenSource, a `PipeName` const `"utinni-live-bridge"`, `ProtocolVersion`, `MaxFrameBytes`, and a framing-descriptor const byte-identical to fixture line 2, with a comment citing LiveBridgeProtocol + the pipe-name.txt fixture as the source-of-truth
    - System.Text.Json is configured camelCase (JsonNamingPolicy.CamelCase) to match the net472 JavaScriptSerializer wire (C-03 alignment); the request envelope includes protocolVersion (C-12)
    - Dual ctors exist; the default connect timeout is short (sub-second to a few seconds, well under CliDispatcher's 60s)
    - The connect/send method is virtual (substitutable by the loopback test)
    - No-server-listening, timeout, and server-close paths return a result object and never throw/hang (asserted in Task 3)
    - Utinni.Mcp builds clean
  </acceptance_criteria>
  <done>The pipe client mirrors CliDispatcher's never-hang discipline, speaks the shared LiveBridgeProtocol framing with camelCase JSON aligned to the net472 wire, carries protocolVersion, and pins the duplicated name + framing constants for the Task-3 fixture-equality + cross-impl assertions.</done>
</task>

<task type="auto">
  <name>Task 2: LiveTools bodies (live_ping + live_reload_asset, candid description) + conditional registration in Program.cs (EnableLive-gated, incl. the client singleton — C-13)</name>
  <files>Utinni.Mcp/Tools/LiveTools.cs, Utinni.Mcp/Program.cs</files>
  <read_first>
    - Utinni.Mcp/Tools/ReadTools.cs (the thin-tool shape: DI params, root.Resolve FIRST, dispatch, map; lines 54-70)
    - Utinni.Mcp/Tools/RepackTool.cs (the non-readonly attribute shape ReadOnly=false, Idempotent=false; line ~59-74)
    - Utinni.Mcp/Program.cs (singleton-registration line 67 + the WithToolsFromAssembly() block lines 71-74 — the two edit sites)
    - Utinni.Mcp/Server/ResolvedRoot.cs (Resolve throws on escape; the fail-closed containment the live tier must NOT bypass)
    - .planning/phases/16-.../16-PATTERNS.md (Tool shapes lines 55-59; Program.cs conditional-registration lines 116-125)
  </read_first>
  <action>
    Fill LiveTools.cs (the non-McpServerToolType class from Task 0) with the D-02 minimal surface:
    - live_ping — McpServerTool(Name = "live_ping", ReadOnly = true, Idempotent = true), takes LivePipeClient pipe (DI), no path arg. Calls pipe.PingAsync(), maps to a CallToolResult reporting listening/gameRunning/pid (or listening-false when no client). Honest absence reporting — never an error/hang (Pitfall 3).
    - live_reload_asset — McpServerTool(Name = "live_reload_asset", ReadOnly = false, Idempotent = false), takes ResolvedRoot root, LivePipeClient pipe, string relativePath. MUST call root.Resolve(relativePath) FIRST (the live tier must not bypass the fail-closed root — RESEARCH A5 / Security V4 / T-16-21), derive the ext, then pipe.ReloadAssetAsync(abs, ext), map to accepted/tier/queued/reloadAttempted/path/note. The tier comes verbatim from the in-client ReloadAssetClassifier via the ack.
      CANDOR (C-14): the McpServerTool Description (and the mapped result) MUST state explicitly that this QUEUES a best-effort reload over the bridge and that `queued:true` does NOT mean a visible reload occurred — it is transport-only until RESID-03 (reloadAttempted is always false this phase). The description an agent reads must not let `queued` be misread as "preview happened."
    In Program.cs: gate the live tier on serverArgs.EnableLive. Register the LivePipeClient singleton ONLY under the EnableLive guard (C-13 — do not register it unconditionally when the tier is off): `if (serverArgs.EnableLive) { builder.Services.AddSingleton(new LivePipeClient(LivePipeClient.PipeName)); ...WithTools<LiveTools>()... }`. Keep WithToolsFromAssembly() for the existing read/save/repack tools OUTSIDE the guard. Add LiveTools via WithTools ONLY inside the EnableLive guard (Task 0 lock — never via the assembly scan; fail-closed by absence, D-04).
  </action>
  <verify>
    <automated>cd D:/Code/Utinni; dotnet build Utinni.Mcp</automated>
  </verify>
  <acceptance_criteria>
    - LiveTools.cs defines live_ping (ReadOnly=true) and live_reload_asset (ReadOnly=false); live_reload_asset calls root.Resolve BEFORE any pipe send
    - The live_reload_asset tool Description states queued != visible reload (transport-only until RESID-03; reloadAttempted=false) — C-14
    - Program.cs registers the LivePipeClient singleton AND the WithTools<LiveTools> registration BOTH only under the serverArgs.EnableLive guard (C-13); LiveTools is never registered via WithToolsFromAssembly
    - Utinni.Mcp builds clean
  </acceptance_criteria>
  <done>The agent-facing live_* tools exist with candid reload semantics, route through the pipe client, honor the fail-closed root, and (client singleton + tool registration) are both gated on --enable-live.</done>
</task>

<task type="auto">
  <name>Task 3: Loopback protocol test + REAL cross-impl integration test (net472) + D-04 real-enumeration gating + pipe-name/framing fixture-equality + MCP-SECURITY.md live-tier addendum (correct path)</name>
  <files>Utinni.Mcp.Tests/LivePipeProtocolTests.cs, Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj, UtinniCoreDotNet.Tests/Live/LiveBridgeIntegrationTests.cs, UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj, .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/MCP-SECURITY.md</files>
  <read_first>
    - Utinni.Mcp.Tests/DispatcherTests.cs (the stub-target + injectable-timeout + non-throwing-result test shape to clone; lines 50-70; PATTERNS lines 170-184)
    - Utinni.Mcp.Tests/Utinni.Mcp.Tests.csproj (the net10 test project to add the linked-fixture Content entry to)
    - UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj (the net472 test project that ALREADY refs UtinniCoreDotNet — the home for the real-server cross-impl test; it also already links the fixture via 16-02 Task 1)
    - Utinni.Mcp/Server/LivePipeClient.cs (Task 1 — the client under test, incl. the duplicated PipeName + framing consts)
    - UtinniCoreDotNet/Live/LivePipeServer.cs + LiveBridgeProtocol.cs (16-02 — the real server + protocol the net472 integration test drives)
    - .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/MCP-SECURITY.md (the Phase-14 threat register to extend with a Phase-16 live-tier section — THIS is the real on-disk path, C-11; there is NO Utinni.Mcp/MCP-SECURITY.md)
    - .planning/phases/16-.../16-RESEARCH.md A6 (loopback harness design) + Security Domain (the exact ASVS rows + STRIDE table + accepted-risk analog AR-14-03/04 to fold in)
  </read_first>
  <action>
    (A) net10 loopback + gating + fixture-equality (Utinni.Mcp.Tests):
    Link the canonical fixture into the net10 test project: in Utinni.Mcp.Tests.csproj add `<Content Include="..\Utinni.Cli.Tests\Fixtures\live\pipe-name.txt" Link="Fixtures\live\pipe-name.txt" CopyToOutputDirectory="PreserveNewest" />` (Link form — the SAME committed file 16-02 created; no fork).
    Write LivePipeProtocolTests.cs (name the class so --filter LivePipe selects it). Stand up a NamedPipeServerStream test-stub on a randomized pipe name implementing the ping + reload-asset protocol (mirror the LiveBridgeProtocol framing), point a LivePipeClient at it, and assert:
    - live_ping round-trips: stub replies listening-true with gameRunning/pid; the client maps to the expected result.
    - live_reload_asset: the client sends a well-formed request envelope (assert the bytes the client SENT parse to op=reload-asset + protocolVersion + the resolved path + ext), the stub replies an ack with a tier + reloadAttempted=false + note, the client maps accepted/tier/queued/reloadAttempted/path/note.
    - Framing edge cases: partial-read frame, oversize body (exceeds MaxFrameBytes), stub closes mid-message — each returns a hard error result, never a hang (injectable short timeout; assert the call returns within the timeout window).
    - No-client: point the client at a pipe name no server owns; assert PingAsync returns listening-false quickly and never throws/hangs (Pitfall 3).
    - Pipe-name + framing fixture-equality (C-05): read Fixtures/live/pipe-name.txt; assert line 1 byte-equals LivePipeClient.PipeName AND line 2 byte-equals LivePipeClient's framing-descriptor const (name AND framing/maxframe/protocol/json-policy pinned on the net10 side).
    - D-04 gating via REAL tool enumeration (C-10): build the host WITHOUT EnableLive and WITH EnableLive through the real Program.cs registration path and enumerate the advertised tool list via the SDK's tool-enumeration surface (the Task-0 approach); assert live_ping/live_reload_asset are ABSENT when off and PRESENT when on. Do NOT settle for a branch/comment assertion unless SDK enumeration is genuinely impossible (then use the closest registration introspection and note why).

    (B) REAL cross-implementation integration test (UtinniCoreDotNet.Tests, net472 — C-02):
    Write LiveBridgeIntegrationTests.cs in UtinniCoreDotNet.Tests/Live/ (name so --filter LiveBridgeIntegration selects it). This is the only side that can reference the real LivePipeServer (TFM wall — net10 cannot ref net472). Spin up the REAL LivePipeServer (16-02) on a RANDOMIZED pipe name with gameIsRunning injected, drive it with a client built on the shared LiveBridgeProtocol framing/serialization (the same wire the net10 LivePipeClient mirrors field-for-field via camelCase), and assert over the ACTUAL pipe:
    - ping round-trips: real server replies listening-true (+ gameRunning per the injected scalar / pid).
    - reload-asset round-trips: send a CONTAINED path → the real server returns a real ReloadAssetClassifier tier, queued=true (gameIsRunning=true) / Unavailable+queued=false (false), reloadAttempted=false, and enqueues exactly one Action (assert via the 16-02 injectable enqueue sink).
    - reload-asset with an absolute / `..`-escaping path → the real server returns accepted=false + Rejected and NO enqueue (the C-04 server-side containment, proven over the wire).
    This makes depends_on:[16-02] PROTOCOL-load-bearing (real server + real framing round-trip), not just pipe-name literal equality. (net10 LivePipeClient agreement with this exact wire is held by the shared fixture line-2 framing + camelCase policy both sides assert in part (A) + Task 1.)

    (C) MCP-SECURITY.md addendum at the CORRECT path (C-11):
    Extend .planning/phases/14-headless-mcp-server-utinni-mcp-the-centerpiece/MCP-SECURITY.md (the real on-disk Phase-14 deliverable — there is NO Utinni.Mcp/MCP-SECURITY.md) with a Phase-16 live-tier section folding in RESEARCH Security Domain verbatim: the ASVS rows (V1/V4/V5/V13/V7), the STRIDE table (unauthorized-pipe-connect, agent-reaches-live-when-off, MCP-side path-escape AND in-client path-escape/containment, frame-DoS incl. server-side partial-read, wrong-thread/native-deref crash, pipe-squatting, visible-render-over-promise candor), each mitigate row citing the proving test/file (incl. the net472 cross-impl test + the in-client containment test + the C-06 in-client start gate), and the accepted-risk row (single-user local desktop; --enable-live/pipe-name are PATHs not secrets; analog AR-14-03/04). Note no new packages (net472 server uses in-box JavaScriptSerializer — C-03; net10 uses in-box System.Text.Json; Package Legitimacy Gate not triggered). If desired, leave a one-line pointer from Utinni.Mcp (e.g. a comment in Program.cs) to the doc's real location so future readers find it.
  </action>
  <verify>
    <automated>cd D:/Code/Utinni; dotnet test Utinni.Mcp.Tests --no-build --filter LivePipe && dotnet test UtinniCoreDotNet.Tests --no-build --filter LiveBridgeIntegration</automated>
  </verify>
  <acceptance_criteria>
    - Utinni.Mcp.Tests.csproj links ..\Utinni.Cli.Tests\Fixtures\live\pipe-name.txt (same committed file, Link form)
    - `dotnet test Utinni.Mcp.Tests --filter LivePipe` exits 0; covers ping round-trip, reload-asset well-formed-envelope (incl. protocolVersion) + ack mapping (incl. reloadAttempted/note), partial-read/oversize/server-close hard-error (no hang), no-client listening-false, fixture byte-equality on BOTH line 1 (PipeName) and line 2 (framing), and the EnableLive on/off gating via REAL tool enumeration (C-10)
    - `dotnet test UtinniCoreDotNet.Tests --filter LiveBridgeIntegration` exits 0; the REAL LivePipeServer round-trips ping + reload-asset (contained + rejected) over an actual pipe via the shared LiveBridgeProtocol (C-02) — the depends_on:[16-02] edge is now a wire-protocol proof
    - Every framing-edge-case test completes within the injected short timeout (proves never-hang)
    - The D-04 gating assertion inspects the actually-advertised tool list through the real host-building path (not a branch/comment), or documents why SDK 1.4.0 enumeration is impossible and uses the closest registration introspection (C-10)
    - .planning/phases/14-.../MCP-SECURITY.md (the real path — C-11) contains a Phase-16 live-tier section with the ASVS rows, the STRIDE table (incl. in-client containment + server-side partial-read + in-client start gate), per-row test citations, and the accepted-risk analog; no Utinni.Mcp/MCP-SECURITY.md orphan is created
  </acceptance_criteria>
  <done>The full pipe protocol + envelope mapping + D-04 real-enumeration gate + cross-plan name/framing agreement + the REAL cross-impl wire round-trip are proven without a live client; the Phase-14 security register (correct path) documents the live-tier surface.</done>
</task>

</tasks>

<threat_model>
**Gate verdict: ASVS L1; no HIGH-severity threats; security gate passes.** (All threats below are mitigate or accept; none rated HIGH. Block-on-HIGH posture: had any threat been HIGH it would gate the wave — none are.)

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI agent -> MCP host (stdio) | Untrusted tool-call args (relativePath) cross into the host |
| MCP host -> named pipe | The host's pipe client crosses the OS named-pipe boundary into the injected client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-20 | Elevation | Agent reaches live_* when the tier is not intended to be on | mitigate | D-04 fail-closed gate: live_* tools AND the LivePipeClient singleton are UNREGISTERED unless --enable-live (C-13); the pipe name is not advertised. Acceptance: Task 3 gating assertion via REAL tool enumeration (off => tools absent — C-10). |
| T-16-21 | Tampering | Path-escape via live_reload_asset path arg (MCP side) | mitigate | ResolvedRoot.Resolve runs BEFORE any pipe send (throws on escape -> SDK tool error). Acceptance: live_reload_asset calls root.Resolve first (Task 2 key_link). NOTE: the in-client server ALSO independently contains the path (T-16-14, 16-02) so a same-user direct-pipe process is covered too. |
| T-16-22 | DoS | Malformed/oversize/partial ack frame hangs the MCP call | mitigate | Injectable short connect/read timeout + bounded MaxFrameBytes; transport failure returns a result object, never a hang. Acceptance: Task 3 framing-edge-case tests complete within the timeout. |
| T-16-23 | Spoofing | No client listening -> naive client hangs the MCP call | mitigate | Short connect timeout maps no-server to listening-false (Pitfall 3). Acceptance: Task 3 no-client test returns quickly. |
| T-16-24 | (candor) | Visible-render over-promise | mitigate (correctness) | Ack reports the honest ReloadAssetClassifier tier with reloadAttempted=false + a transport-only note; the tool DESCRIPTION states queued != visible reload (C-14); render is best-effort, NOT a success gate (D-01). must_haves do NOT gate on visible render. |
| T-16-25 | Tampering (protocol drift) | net10/net472 wire shapes silently diverge (framing, JSON casing, field names) | mitigate | Shared fixture line 1 (name) + line 2 (framing/maxframe/protocol/json policy) asserted on BOTH sides (C-05); net10 System.Text.Json camelCase aligned to the net472 JavaScriptSerializer wire (C-03); protocolVersion in the envelope (C-12); AND a real net472 cross-impl round-trip (C-02). Acceptance: Task 3 (A) fixture-equality + (B) LiveBridgeIntegration. |
| T-16-AR16 | (accepted risk) | Single-user local desktop pipe; --enable-live / pipe-name are identifiers not secrets | accept | Local-only named pipe + current-user ACL (server side, 16-02) + in-client start gate (C-06, 16-02 Task 3); analog AR-14-03/04; logged in MCP-SECURITY.md. |
| T-16-SC | Tampering | npm/pip/cargo installs | n/a | No new packages — net10 uses in-box System.Text.Json; net472 uses in-box JavaScriptSerializer (C-03); already-pinned ModelContextProtocol 1.4.0 only (RESEARCH Package Legitimacy Audit: gate not triggered). |
</threat_model>

<verification>
- `dotnet test Utinni.Mcp.Tests --filter LivePipe` green (loopback ping + reload-asset + framing edge cases + no-client + name/framing fixture-equality + D-04 real-enumeration gating).
- `dotnet test UtinniCoreDotNet.Tests --filter LiveBridgeIntegration` green (REAL LivePipeServer cross-impl round-trip over an actual pipe — C-02).
- Utinni.Mcp builds clean with the EnableLive-gated WithTools registration (+ gated client singleton).
- .planning/phases/14-.../MCP-SECURITY.md (correct path, C-11) carries the Phase-16 live-tier section.
- Tier-4 manual: live-injected confirmation (launch with the in-client live flag ON + --enable-live, live_ping then live_reload_asset against an edited asset) — non-gating per D-01, documented in 16-VALIDATION.md.
</verification>

<success_criteria>
- MCP-03: an agent can drive the live-injected client over the MCP bridge (named-pipe IPC); the ack round-trip is proven over BOTH a loopback pipe (net10 Tier-2) AND a real cross-impl round-trip against the actual 16-02 server (net472 Tier-2, C-02). Visible re-render is best-effort and explicitly NOT a Phase-16 success gate (D-01).
- The live tier is fail-closed by absence (D-04, gated client + tools, proven via real enumeration C-10) and the minimal surface (ping + reload-asset only, D-02) with candid reload semantics (C-14).
- The net10 duplicated pipe-name literal AND framing constants are proven byte-identical to the canonical fixture (name + framing, C-05); the cross-stack JSON is camelCase-aligned (C-03); the wave-2 depends_on edge is a wire-protocol proof.
- The out-of-proc anti-pattern holds: the SDK never enters SWG.exe; only the narrow named pipe crosses.
</success_criteria>

<output>
Create `.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-03-SUMMARY.md` when done.
</output>
</content>
