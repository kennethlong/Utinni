---
phase: 16-live-injected-mcp-bridge-blender-ecosystem-boundary
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - UtinniCoreDotNet/Live/LiveBridgeProtocol.cs
  - UtinniCoreDotNet/Live/LivePipeServer.cs
  - UtinniCoreDotNet/UtinniCoreDotNet.csproj
  - UtinniCoreDotNet.Tests/Live/LivePipeServerTests.cs
  - UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj
  - Utinni.Cli.Tests/Fixtures/live/pipe-name.txt
  - Utinni.Mcp/Server/ServerArgs.cs
  - Utinni.Mcp.Tests/ServerArgsTests.cs
autonomous: true
requirements: [MCP-03]
user_setup: []

must_haves:
  truths:
    - "The in-client LivePipeServer parses a request envelope, classifies a reload-asset request via ReloadAssetClassifier, and enqueues exactly one Action onto the game-thread queue — never touching game state off the game thread"
    - "When gameIsRunning is injected false, the server's reload disposition is the honest Unavailable tier (no enqueue side-effect required)"
    - "ServerArgs.Parse recognizes --enable-live (space, equals-irrelevant boolean presence) and exposes EnableLive; absence leaves EnableLive false (fail-closed default)"
    - "A shared pipe-name constant and the request/ack envelope shape are defined once and referenced by both ends"
    - "The canonical pipe name + framing descriptor live in one committed fixture; the in-client LiveBridgeProtocol.PipeName byte-equals that fixture (the agreement the Wave-2 host duplicate is asserted against)"
  artifacts:
    - path: "UtinniCoreDotNet/Live/LivePipeServer.cs"
      provides: "Managed net472 named-pipe server inside the injected host; background-thread accept loop + game-thread marshal"
      contains: "NamedPipeServerStream"
    - path: "UtinniCoreDotNet/Live/LiveBridgeProtocol.cs"
      provides: "Shared pipe name constant + envelope (de)serialization + framing (4-byte LE length prefix)"
      contains: "PipeName"
    - path: "Utinni.Cli.Tests/Fixtures/live/pipe-name.txt"
      provides: "Single canonical pipe-name + framing-descriptor fixture both pipe ends assert against (cross-plan agreement anchor)"
      contains: "utinni-live-bridge"
    - path: "Utinni.Mcp/Server/ServerArgs.cs"
      provides: "EnableLive flag parse (fail-closed default false)"
      contains: "EnableLive"
  key_links:
    - from: "UtinniCoreDotNet/Live/LivePipeServer.cs"
      to: "GameCallbacks.AddMainLoopCall"
      via: "enqueue one Action for game-thread reload"
      pattern: "AddMainLoopCall"
    - from: "UtinniCoreDotNet/Live/LivePipeServer.cs"
      to: "ReloadAssetClassifier.Classify"
      via: "ack tier derivation"
      pattern: "ReloadAssetClassifier\\.Classify"
    - from: "UtinniCoreDotNet.Tests/Live/LivePipeServerTests.cs"
      to: "Utinni.Cli.Tests/Fixtures/live/pipe-name.txt"
      via: "fixture-equality assertion on LiveBridgeProtocol.PipeName"
      pattern: "pipe-name\\.txt"
---

<objective>
Build the in-client (injected-host) half of the MCP-03 live bridge plus the `--enable-live` flag plumbing. The named-pipe SERVER lives in managed net472 `UtinniCoreDotNet` (the CLR is already hosted in-proc per clr.cpp — no C++, no cross-arch marshaling). It parses a request envelope on a background thread, derives the honest ack tier via the shipped `ReloadAssetClassifier`, and marshals reload work onto the shipped `GameCallbacks.mainLoopCallQueue` (heap-free hot path, honoring project_rh_snapshot_no_heap_alloc). This plan also extends the tested `ServerArgs` parser with `EnableLive` (fail-closed default) so the Wave-2 host plan can gate registration.

This plan also commits the **single canonical pipe-name/framing fixture** (`Utinni.Cli.Tests/Fixtures/live/pipe-name.txt`) that BOTH pipe ends are asserted against. Because net10 `Utinni.Mcp` cannot project-ref net472 `UtinniCoreDotNet`, the Wave-2 host (16-03) must duplicate the pipe-name literal; this fixture is the agreement-enforcing anchor that makes the duplicated literals provably byte-identical (BLOCKER fix — the `depends_on: [16-02]` edge now MEANS something).

Purpose: Land the hardest new mechanism (pipe server + game-thread marshal + ack candor) behind pure-managed unit tests, and define the shared protocol/pipe-name constant the Wave-2 client consumes.
Output: LiveBridgeProtocol (shared constant + framing/envelope), LivePipeServer (in-client), the canonical pipe-name fixture, pure-managed server tests (incl. the fixture-equality assertion), ServerArgs.EnableLive + extended ServerArgsTests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-CONTEXT.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-RESEARCH.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-PATTERNS.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-VALIDATION.md

<interfaces>
<!-- Shipped seams the server reuses (do NOT rebuild). Extracted from codebase. -->

From UtinniCoreDotNet/Callbacks/GameCallbacks.cs (:63, :254-257):
  private static readonly ConcurrentQueue<Action> mainLoopCallQueue = new ConcurrentQueue<Action>();
  public static void AddMainLoopCall(Action call)   // enqueue; drained on game thread each frame

From UtinniCoreDotNet/Saving/ReloadAssetClassifier.cs (:124, :78-80):
  public static ReloadTier Classify(string extension, string rootTypeIdOrNull)
  // → ReloadedTextures | ReloadedTerrain | PendingNextSceneChange | Unavailable
  // !Game.IsRunning maps to Unavailable upstream

From UtinniCoreDotNet/Editing/LivePatchValidator.cs (:135-147) — purity precedent:
  public static LivePatchValidation Validate(IntPtr targetAddr, int originalMappedLength,
      int rewrittenLength, bool gameIsRunning)   // game state injected as a scalar for unit-testability
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — shared protocol + canonical pipe-name fixture + pure-managed LivePipeServer (background accept loop + game-thread marshal) + tests</name>
  <files>UtinniCoreDotNet/Live/LiveBridgeProtocol.cs, UtinniCoreDotNet/Live/LivePipeServer.cs, UtinniCoreDotNet/UtinniCoreDotNet.csproj, UtinniCoreDotNet.Tests/Live/LivePipeServerTests.cs, UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj, Utinni.Cli.Tests/Fixtures/live/pipe-name.txt</files>
  <read_first>
    - UtinniCoreDotNet/Saving/ReloadAssetClassifier.cs (Classify signature + the 4 tiers + !Game.IsRunning→Unavailable; reuse verbatim for the ack tier — PATTERNS lines 144-149)
    - UtinniCoreDotNet/Callbacks/GameCallbacks.cs (mainLoopCallQueue + AddMainLoopCall — the marshal seam to reuse, NOT rebuild; PATTERNS lines 136-141)
    - UtinniCoreDotNet/Editing/LivePatchValidator.cs (the gameIsRunning-injected-as-scalar purity pattern; lines 135-147)
    - UtinniCoreDotNet.Tests/Editing/LivePatchValidatorTests.cs (the injected-state pure-fn test shape to clone)
    - Utinni.Cli.Tests/Utinni.Cli.Tests.csproj (the `<Content Include="Fixtures\**\*" CopyToOutputDirectory="PreserveNewest" />` pattern — the fixture lives under this project's Fixtures tree and is auto-copied to its output; the net472 test project links it)
    - .planning/phases/16-.../16-RESEARCH.md §A2/A3/A4 (threading model, wire format, ack reuse) + §Security Domain V13 (PipeSecurity current-user ACL) + Pitfall 2 (worker thread never touches game state)
  </read_first>
  <action>
    Commit the CANONICAL pipe-name fixture FIRST: create Utinni.Cli.Tests/Fixtures/live/pipe-name.txt as the single source of truth for the cross-plan pipe-name/framing agreement (BLOCKER fix). It is two lines: line 1 = the canonical pipe name `utinni-live-bridge` (verbatim, no surrounding whitespace); line 2 = a framing descriptor token `frame=len32le+utf8json` naming the 4-byte little-endian length-prefix + UTF-8 JSON framing. This file is the byte-anchor BOTH pipe ends assert equality against (this plan's net472 server, and 16-03's net10 duplicate). It lives under Utinni.Cli.Tests/Fixtures so it is auto-copied by that project's existing `Content Include="Fixtures\**\*"`; the two protocol test projects link it (this plan links it into UtinniCoreDotNet.Tests; 16-03 links it into Utinni.Mcp.Tests). Write the canonical value `utinni-live-bridge` verbatim — do NOT paraphrase or change casing/separators.

    Create LiveBridgeProtocol.cs in UtinniCoreDotNet/Live/: a shared static class defining the canonical pipe name constant `public const string PipeName = "utinni-live-bridge";` (byte-identical to fixture line 1), the request/ack envelope DTOs, and the framing helpers. Frame = 4-byte little-endian length prefix + UTF-8 JSON body (RESEARCH A3 — transmission-mode-independent, trivially loopback-testable). Request envelope: `{ "op": "ping" }` or `{ "op": "reload-asset", "path": ..., "ext": ... }`. Ack envelope mirrors the CLI sorted-key shape `{ schemaVersion, op, result }` — ping result `{ listening, gameRunning, pid }`; reload-asset result `{ accepted, tier, queued, path }`. Enforce a bounded max envelope size (reject oversize frames). Keep this class BCL-only (System.Text.Json) so it is referenced by both ends and unit-testable without UtinniCore.dll. Add a code comment on PipeName naming Utinni.Cli.Tests/Fixtures/live/pipe-name.txt as the canonical agreement anchor that 16-03's net10 duplicate also asserts against.
    Create LivePipeServer.cs in UtinniCoreDotNet/Live/: a background-thread NamedPipeServerStream accept/read loop (the worker thread NEVER touches game state — Pitfall 2). Apply a PipeSecurity/PipeAccessRule restricting the pipe to the current user (Security V13). On a parsed request, route to a PURE decision method that takes gameIsRunning as a scalar (LivePatchValidator purity pattern): ping → build the ping result; reload-asset → call ReloadAssetClassifier.Classify(ext, null) for the tier, and IF gameIsRunning enqueue exactly one Action onto the marshal seam via GameCallbacks.AddMainLoopCall(() => /* reload binding placeholder; best-effort, D-01 */). The pure decision method returns the ack envelope (accepted/queued/tier) WITHOUT performing the enqueue itself, so it unit-tests; the public Handle method does the enqueue side-effect. Map !gameIsRunning to the Unavailable tier and queued=false. The ack returns IMMEDIATELY (no completion signal — D-01, RESEARCH Open Q2).

    The body of the enqueued Action is an INTENTIONAL best-effort placeholder this phase (visible render is NON-gating per D-01; rides on RESID-03 whenever it lands — NOT wired reload). Mark it explicitly so it is never mistaken for functional reload: place an inline `// TODO(RESID-03): best-effort placeholder per D-01 — wired reload deferred; NOT functional re-render` comment ON the enqueued Action body, and keep the Action a deliberate no-op/best-effort stub. The "exactly one Action enqueued with gameIsRunning=true" assertion still holds (the placeholder must still be the one Action enqueued).
    Wire the listener start as a method invokable from the CLR entrypoint (do NOT couple it to a hard always-on start here — expose StartListener so the host decides; a config-gated start is acceptable defense-in-depth per RESEARCH Open Q1, but D-04's tool gate is the authoritative control). Add the two new files to UtinniCoreDotNet.csproj <Compile Include> entries (old-style csproj).
    In UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj, link the canonical fixture so the net472 test project can read it: add `<Content Include="..\Utinni.Cli.Tests\Fixtures\live\pipe-name.txt" Link="Fixtures\live\pipe-name.txt" CopyToOutputDirectory="PreserveNewest" />` (Link form so the SAME committed file — not a copy — is the asserted source; both test projects link the one file).
    Write LivePipeServerTests.cs in UtinniCoreDotNet.Tests/Live/ cloning LivePatchValidatorTests: assert envelope-parse→tier→ack with gameIsRunning injected both true and false; assert a reload-asset with gameIsRunning=true yields a tier from ReloadAssetClassifier and queued=true; with false yields Unavailable + queued=false and NO enqueue; assert oversize-frame rejection; assert ping result shape. Inject the enqueue target (e.g. an Action sink) so the test verifies "exactly one Action enqueued" without a live game thread. ALSO add a fixture-equality fact: read Fixtures/live/pipe-name.txt from the test output dir, parse line 1, and assert it byte-equals LiveBridgeProtocol.PipeName (this is the in-client half of the cross-plan agreement; 16-03 asserts its net10 duplicate equals the same fixture line). Name the class so `--filter LivePipeServer` selects it.
  </action>
  <verify>
    <automated>cd D:/Code/Utinni && dotnet test UtinniCoreDotNet.Tests --no-build --filter LivePipeServer</automated>
  </verify>
  <acceptance_criteria>
    - Utinni.Cli.Tests/Fixtures/live/pipe-name.txt exists, committed, line 1 is exactly `utinni-live-bridge`, line 2 names the framing (`frame=len32le+utf8json`)
    - LiveBridgeProtocol.cs defines `public const string PipeName = "utinni-live-bridge";` (byte-identical to fixture line 1), the request/ack envelope DTOs, 4-byte LE length-prefix framing, and a bounded max envelope size; BCL-only (no UtinniCore.dll dependency)
    - LivePipeServerTests.cs contains a fact reading Fixtures/live/pipe-name.txt and asserting line 1 byte-equals LiveBridgeProtocol.PipeName (the agreement anchor; the assertion FAILS if the literal and fixture drift)
    - UtinniCoreDotNet.Tests.csproj links ..\Utinni.Cli.Tests\Fixtures\live\pipe-name.txt (the same committed file, not a fork)
    - LivePipeServer.cs contains `NamedPipeServerStream`, a PipeSecurity current-user ACL, `GameCallbacks.AddMainLoopCall`, and `ReloadAssetClassifier.Classify`
    - The enqueued Action body carries the `// TODO(RESID-03): best-effort placeholder per D-01 — wired reload deferred; NOT functional re-render` marker so it is not mistaken for wired reload; it remains the single enqueued Action
    - The reload-decision method takes `bool gameIsRunning` as a scalar (purity) and does NOT read Game.IsRunning directly inside the unit-tested path
    - The worker/accept loop contains no direct game-state dereference (Pitfall 2): the only game-thread crossing is a single enqueued Action
    - `dotnet test UtinniCoreDotNet.Tests --filter LivePipeServer` exits 0; tests cover gameIsRunning true/false, the exactly-one-enqueue assertion, oversize-frame rejection, the ping result shape, and the fixture-equality assertion
    - UtinniCoreDotNet.csproj includes both new files; the managed suite builds net472 (no regression in the full UtinniCoreDotNet.Tests count)
  </acceptance_criteria>
  <done>The in-client pipe server + shared protocol exist, reuse the shipped marshal seam + classifier, keep the decision path pure, pin the canonical pipe-name fixture both ends assert against, mark the reload Action as a D-01 placeholder, and pass pure-managed tests with no live client.</done>
</task>

<task type="auto">
  <name>Task 2: ServerArgs.EnableLive flag (fail-closed default) + extended ServerArgsTests</name>
  <files>Utinni.Mcp/Server/ServerArgs.cs, Utinni.Mcp.Tests/ServerArgsTests.cs</files>
  <read_first>
    - Utinni.Mcp/Server/ServerArgs.cs (the self-analog: --root/--cli-path property + const + parse-loop, lines 43-94; the tolerant/first-wins/equals-form contract in the XML doc lines 28-39)
    - Utinni.Mcp.Tests/ServerArgsTests.cs (the existing parser test class to extend)
    - .planning/phases/16-.../16-RESEARCH.md §A5 (--enable-live is a BOOLEAN presence flag, optional UTINNI_MCP_ENABLE_LIVE env alias mirroring UTINNI_MCP_ROOT)
  </read_first>
  <action>
    Extend ServerArgs.cs: add `public bool EnableLive { get; private set; }` and `private const string EnableLiveFlag = "--enable-live";`. In the Parse loop, add a branch BEFORE the value-flag branches: `if (token == EnableLiveFlag) { result.EnableLive = true; continue; }` — it is a presence flag, NOT value-taking (differs from --root/--cli-path). Default is false (fail-closed by absence, D-04). Optionally honor a UTINNI_MCP_ENABLE_LIVE env var (any non-empty/truthy value) mirroring the UTINNI_MCP_ROOT precedent — if added, document it in the class XML doc. Do not break the existing tolerant/first-wins/equals-form contract for the other flags.
    Extend ServerArgsTests.cs: add facts asserting (1) `--enable-live` present → EnableLive true; (2) absent → EnableLive false (the fail-closed default); (3) --enable-live alongside --root/--cli-path parses all three correctly; (4) (if env alias added) the env var sets EnableLive. Name the additions so `--filter ServerArgs` selects them.
  </action>
  <verify>
    <automated>cd D:/Code/Utinni && dotnet test Utinni.Mcp.Tests --no-build --filter ServerArgs</automated>
  </verify>
  <acceptance_criteria>
    - ServerArgs.cs contains `public bool EnableLive` and `EnableLiveFlag = "--enable-live"`; EnableLive defaults to false when the flag is absent
    - `--enable-live` is parsed as a presence flag (no following value consumed)
    - ServerArgsTests asserts present→true, absent→false, and co-existence with --root/--cli-path
    - `dotnet test Utinni.Mcp.Tests --filter ServerArgs` exits 0
  </acceptance_criteria>
  <done>EnableLive parses fail-closed-by-absence with test coverage; the Wave-2 host plan can gate WithTools<LiveTools>() on it.</done>
</task>

</tasks>

<threat_model>
**Gate verdict: ASVS L1; no HIGH-severity threats; security gate passes.** (All threats below are mitigate or accept; none rated HIGH. Block-on-HIGH posture: had any threat been HIGH it would gate the wave — none are.)

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local process → named pipe (in-client server) | Any local process that knows the pipe name can connect; the server runs inside the injected x86 client |
| pipe worker thread → game thread | Reload work must cross from the background worker onto the game thread only via the marshal queue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | Spoofing/Elevation | Unauthorized local process connects to the pipe and triggers reloads | mitigate | PipeSecurity/PipeAccessRule restricts the pipe to the current user; NamedPipeServerStream is local-machine only (no network). Acceptance: server constructs the pipe with a current-user ACL. |
| T-16-11 | Tampering (integrity) | Reload work executed on the wrong thread → client AV/crash | mitigate | All reload work marshals via GameCallbacks.AddMainLoopCall; the worker thread never dereferences game state (Pitfall 2). Acceptance: the accept/decision path has no direct game-state access; only one Action crosses. |
| T-16-12 | DoS | Malformed/oversize wire frame on the in-client server thread | mitigate | Bounded max envelope size + op allow-list ({ping,reload-asset}); a bad frame closes that connection only, never the game thread. Acceptance: oversize-frame-rejection test. |
| T-16-13 | Spoofing | Pipe name collision / squatting (another process pre-creates the pipe) | accept | Sufficiently-specific pipe name; server owns creation (NamedPipeServerStream). Single-user local desktop trust assumption (analog AR-14-03/04). |
| T-16-SC | Tampering | npm/pip/cargo installs | n/a | No new packages this phase — BCL System.IO.Pipes + System.Text.Json only; Package Legitimacy Gate not triggered (RESEARCH §Package Legitimacy Audit). |
</threat_model>

<verification>
- `dotnet test UtinniCoreDotNet.Tests --filter LivePipeServer` green (envelope→tier→enqueue with injected game state; oversize rejection; pipe-name fixture-equality).
- `dotnet test Utinni.Mcp.Tests --filter ServerArgs` green (EnableLive fail-closed default).
- Full UtinniCoreDotNet.Tests + Utinni.Mcp.Tests lanes build with no regression.
</verification>

<success_criteria>
- The in-client pipe server reuses the shipped marshal seam (mainLoopCallQueue) and ReloadAssetClassifier; no per-frame heap allocation introduced (project_rh_snapshot_no_heap_alloc honored).
- The ack reports the honest reload tier; visible re-render is NOT gated (D-01); the enqueued reload Action is an explicit D-01 placeholder, not wired reload.
- The canonical pipe-name fixture is committed and the in-client PipeName asserts equal to it (the cross-plan agreement anchor the Wave-2 duplicate also binds to).
- EnableLive defaults closed; the Wave-2 host gates tool registration on it.
</success_criteria>

<output>
Create `.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-02-SUMMARY.md` when done.
</output>
