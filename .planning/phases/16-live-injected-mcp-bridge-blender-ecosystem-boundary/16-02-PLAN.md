---
phase: 16-live-injected-mcp-bridge-blender-ecosystem-boundary
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - UtinniCoreDotNet/Live/LiveBridgeProtocol.cs
  - UtinniCoreDotNet/Live/LivePipeServer.cs
  - UtinniCoreDotNet/main.cs
  - UtinniCoreDotNet/UtinniCoreDotNet.csproj
  - UtinniCoreDotNet.Tests/Live/LivePipeServerTests.cs
  - UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj
  - Utinni.Cli.Tests/Fixtures/live/pipe-name.txt
  - Utinni.Mcp/Server/ServerArgs.cs
  - Utinni.Mcp.Tests/ServerArgsTests.cs
autonomous: true
requirements: [MCP-03]
user_setup: []

must_haves:
  truths:
    - "The in-client LivePipeServer parses a request envelope, classifies a reload-asset request via ReloadAssetClassifier, and enqueues exactly one Action onto the game-thread queue — never touching native game state off the game thread"
    - "When gameIsRunning is injected false, the server's reload disposition is the honest Unavailable tier (no enqueue side-effect required)"
    - "gameIsRunning is captured at the marshal/enqueue boundary (or read inside the enqueued game-thread Action), NEVER dereferenced on the background worker thread — Game.IsRunning is native-backed (Pitfall 2)"
    - "LivePipeServer.StartListener() is WIRED into Startup.EntryPoint (main.cs), invoked AFTER the *Callbacks.Initialize() block and BEFORE the blocking Application.Run, gated on an in-client enable flag (defense-in-depth, C-06); StartListener is idempotent (no double-spawned accept loop)"
    - "The in-client reload path is root-contained: the server resolves the received path against a pinned in-client root and REJECTS absolute / escaping paths (C-04) — a same-user local process on the pipe cannot target arbitrary filesystem paths"
    - "The named-pipe server lifecycle is specified and tested: cancel/dispose stops the accept loop, one request per connection, the loop survives a malformed/partial frame on one connection and accepts the next, restart-after-disconnect, accept-loop-throw recovery"
    - "ServerArgs.Parse recognizes --enable-live and exposes EnableLive; absence leaves EnableLive false (fail-closed default)"
    - "The canonical pipe name + framing descriptor live in one committed fixture; the in-client LiveBridgeProtocol.PipeName byte-equals fixture line 1 AND the implemented framing constants byte-equal fixture line 2 (C-05 framing enforcement)"
    - "The net472 server JSON (de)serialization uses JavaScriptSerializer (System.Web.Extensions, already referenced — genuinely in-box on net472); NO System.Text.Json package is added (C-03 honesty)"
  artifacts:
    - path: "UtinniCoreDotNet/Live/LivePipeServer.cs"
      provides: "Managed net472 named-pipe server inside the injected host; background-thread accept loop + game-thread marshal + lifecycle (cancel/dispose/restart) + root containment"
      contains: "NamedPipeServerStream"
    - path: "UtinniCoreDotNet/Live/LiveBridgeProtocol.cs"
      provides: "Shared pipe name constant + protocolVersion + envelope (JavaScriptSerializer) + framing (4-byte LE length prefix + UTF-8 JSON) + bounded max size constant"
      contains: "PipeName"
    - path: "UtinniCoreDotNet/main.cs"
      provides: "StartListener wiring into Startup.EntryPoint (post-Callbacks.Initialize, pre-Application.Run), in-client-flag gated"
      contains: "StartListener"
    - path: "Utinni.Cli.Tests/Fixtures/live/pipe-name.txt"
      provides: "Single canonical pipe-name + framing-descriptor fixture both pipe ends assert against (cross-plan agreement anchor — line 1 name, line 2 framing)"
      contains: "utinni-live-bridge"
    - path: "Utinni.Mcp/Server/ServerArgs.cs"
      provides: "EnableLive flag parse (fail-closed default false)"
      contains: "EnableLive"
  key_links:
    - from: "UtinniCoreDotNet/Live/LivePipeServer.cs"
      to: "GameCallbacks.AddMainLoopCall"
      via: "enqueue one Action for game-thread reload"
      pattern: "AddMainLoopCall"
    - from: "UtinniCoreDotNet/Live/LivePipeServer.cs"
      to: "ReloadAssetClassifier.Classify"
      via: "ack tier derivation"
      pattern: "ReloadAssetClassifier\\.Classify"
    - from: "UtinniCoreDotNet/main.cs"
      to: "LivePipeServer.StartListener"
      via: "Startup.EntryPoint wiring (post-Callbacks.Initialize, pre-Application.Run), in-client-flag gated"
      pattern: "StartListener"
    - from: "UtinniCoreDotNet.Tests/Live/LivePipeServerTests.cs"
      to: "Utinni.Cli.Tests/Fixtures/live/pipe-name.txt"
      via: "fixture-equality assertion on LiveBridgeProtocol.PipeName (line 1) AND framing constants (line 2)"
      pattern: "pipe-name\\.txt"
---

<objective>
Build the in-client (injected-host) half of the MCP-03 live bridge plus the `--enable-live` flag plumbing. The named-pipe SERVER lives in managed net472 `UtinniCoreDotNet` (the CLR is already hosted in-proc per clr.cpp — no C++, no cross-arch marshaling). It parses a request envelope on a background thread, derives the honest ack tier via the shipped `ReloadAssetClassifier`, contains the received path under a pinned in-client root, and marshals reload work onto the shipped `GameCallbacks.mainLoopCallQueue` (heap-free hot path, honoring project_rh_snapshot_no_heap_alloc). This plan also WIRES the listener start into `Startup.EntryPoint` (main.cs) so the bridge actually runs in a real injected client, and extends the tested `ServerArgs` parser with `EnableLive` (fail-closed default) so the Wave-2 host plan can gate registration.

This plan also commits the **single canonical pipe-name/framing fixture** (`Utinni.Cli.Tests/Fixtures/live/pipe-name.txt`) that BOTH pipe ends are asserted against — BOTH line 1 (pipe name) and line 2 (framing descriptor). Because net10 `Utinni.Mcp` cannot project-ref net472 `UtinniCoreDotNet`, the Wave-2 host (16-03) must duplicate the pipe-name literal AND the framing; this fixture is the agreement-enforcing anchor that makes the duplicated literals + framing constants provably byte-identical.

Purpose: Land the hardest new mechanism (pipe server + lifecycle + game-thread marshal + path containment + ack candor + the main.cs wiring that makes it real) behind pure-managed unit tests, and define the shared protocol/pipe-name/framing constant the Wave-2 client consumes.
Output: LiveBridgeProtocol (shared constant + protocolVersion + JavaScriptSerializer envelope + framing), LivePipeServer (in-client, with lifecycle + containment), the main.cs StartListener wiring, the canonical pipe-name/framing fixture, pure-managed server tests (incl. fixture-equality on name+framing, lifecycle, partial-read, containment, idempotent-start), ServerArgs.EnableLive + extended ServerArgsTests.

REVIEW-DRIVEN EDITS (from 16-REVIEWS.md — orchestrator-VERIFIED facts in bold): **C-01** (StartListener WIRED into main.cs — confirmed missing; new Task 3), **C-03** (net472 JSON = JavaScriptSerializer, NOT System.Text.Json — confirmed no STJ ref; package-gate honesty), C-04 (server-side path containment), C-05 (framing line-2 enforcement), C-06 (in-client start gate defense-in-depth), C-07/C-08 (lifecycle + partial-read tests), C-09 (gameIsRunning capture boundary, native-backed), C-12 (protocolVersion envelope field), C-14 (reload ack candor: reloadAttempted=false), Codex (document supported reload asset classes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-CONTEXT.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-RESEARCH.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-PATTERNS.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-VALIDATION.md

<interfaces>
<!-- Shipped seams the server reuses (do NOT rebuild). Extracted from codebase. -->

From UtinniCoreDotNet/Callbacks/GameCallbacks.cs (:63, :254-257):
  private static readonly ConcurrentQueue<Action> mainLoopCallQueue = new ConcurrentQueue<Action>();
  public static void AddMainLoopCall(Action call)   // enqueue; drained on game thread each frame

From UtinniCoreDotNet/Saving/ReloadAssetClassifier.cs (:124, :78-80):
  public static ReloadTier Classify(string extension, string rootTypeIdOrNull)
  // → ReloadedTextures | ReloadedTerrain | PendingNextSceneChange | Unavailable
  // !Game.IsRunning maps to Unavailable upstream (the DISPATCHER does this; the helper is game-state-free)

From UtinniCoreDotNet/Editing/LivePatchValidator.cs (:135-147) — purity precedent:
  public static LivePatchValidation Validate(IntPtr targetAddr, int originalMappedLength,
      int rewrittenLength, bool gameIsRunning)   // game state injected as a scalar for unit-testability

From UtinniCoreDotNet/Generated/UtinniCore.cs (:11143) — game-state read (NATIVE-BACKED, C-09):
  public static bool Game.IsRunning { get; }   // P/Invoke ?isRunning@Game@utinni@@SA_NXZ — DO NOT call off the game thread

From UtinniCoreDotNet/main.cs (Startup.EntryPoint, :43-114) — the wiring site (C-01):
  if (!initialized) { initialized = true; ... AssemblyResolve handler (15-12) ...
    GameCallbacks.Initialize(); GroundSceneCallbacks/ObjectCallbacks/CuiCallbacks.Initialize();  // :90-93
    Native.SignalLauncherReady();  // :106
    if (...enableEditorMode) Application.Run(new FormMain(pluginLoader));  // :108-110 — BLOCKS for editor lifetime
  }
  // StartListener must be invoked AFTER :93 and BEFORE :108 (background thread, non-blocking)

From UtinniCoreDotNet/UtinniCoreDotNet.csproj (:60):
  <Reference Include="System.Web.Extensions" />   // JavaScriptSerializer lives here — ALREADY referenced (C-03)
  // (There is NO System.Text.Json reference; adding STJ would pull a NuGet package + Package Legitimacy Gate.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — shared protocol (JavaScriptSerializer envelope + framing) + canonical pipe-name/framing fixture + pure-managed LivePipeServer (background accept loop + lifecycle + path containment + game-thread marshal) + tests</name>
  <files>UtinniCoreDotNet/Live/LiveBridgeProtocol.cs, UtinniCoreDotNet/Live/LivePipeServer.cs, UtinniCoreDotNet/UtinniCoreDotNet.csproj, UtinniCoreDotNet.Tests/Live/LivePipeServerTests.cs, UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj, Utinni.Cli.Tests/Fixtures/live/pipe-name.txt</files>
  <read_first>
    - UtinniCoreDotNet/Saving/ReloadAssetClassifier.cs (Classify signature + the 4 tiers; the helper is GAME-STATE-FREE — !Game.IsRunning→Unavailable is the DISPATCHER's job; reuse Classify for the tier — PATTERNS lines 144-149)
    - UtinniCoreDotNet/Callbacks/GameCallbacks.cs (mainLoopCallQueue + AddMainLoopCall — the marshal seam to reuse, NOT rebuild; PATTERNS lines 136-141)
    - UtinniCoreDotNet/Editing/LivePatchValidator.cs (the gameIsRunning-injected-as-scalar purity pattern; lines 135-147)
    - UtinniCoreDotNet/Formats/ObjectTemplate/ObjectTemplateSchemaLoader.cs (:81 — the EXISTING JavaScriptSerializer usage in this assembly; the in-box net472 JSON precedent to follow for C-03)
    - Utinni.Mcp/Server/ResolvedRoot.cs (the Phase-14 canonicalize-then-reject-escape root containment to MIRROR for the in-client reload path — C-04)
    - UtinniCoreDotNet.Tests/Editing/LivePatchValidatorTests.cs (the injected-state pure-fn test shape to clone)
    - Utinni.Cli.Tests/Utinni.Cli.Tests.csproj (the `<Content Include="Fixtures\**\*" CopyToOutputDirectory="PreserveNewest" />` pattern — the fixture lives under this project's Fixtures tree and is auto-copied to its output; the net472 test project links it)
    - .planning/phases/16-.../16-RESEARCH.md §A2/A3/A4 (threading model, wire format, ack reuse) + §Security Domain V13 (PipeSecurity current-user ACL) + Pitfall 2 (worker thread never touches game state)
  </read_first>
  <action>
    Commit the CANONICAL pipe-name/framing fixture FIRST: create Utinni.Cli.Tests/Fixtures/live/pipe-name.txt as the single source of truth for the cross-plan agreement. It is two lines: line 1 = the canonical pipe name `utinni-live-bridge` (verbatim, no surrounding whitespace); line 2 = the framing descriptor `frame=len32le+utf8json;maxframe=65536;protocol=1;json=camelCase` naming the 4-byte little-endian length-prefix + UTF-8 JSON framing, the bounded max frame size (bytes), the protocol version, and the JSON field-naming policy. Line 2 is now a small PROTOCOL descriptor (C-05): both ends parse it and assert their implemented constants match (length-prefix width/endianness, encoding, max-frame size, protocol version, field-naming) — it is no longer documentation-only. This file is the byte-anchor BOTH pipe ends assert equality against (this plan's net472 server, and 16-03's net10 duplicate). It lives under Utinni.Cli.Tests/Fixtures so it is auto-copied by that project's existing `Content Include="Fixtures\**\*"`; the two protocol test projects link it. Write the canonical values verbatim — do NOT paraphrase or change casing/separators.

    Create LiveBridgeProtocol.cs in UtinniCoreDotNet/Live/: a shared static class defining:
      - `public const string PipeName = "utinni-live-bridge";` (byte-identical to fixture line 1).
      - `public const int ProtocolVersion = 1;` and `public const int MaxFrameBytes = 65536;` (byte-identical to fixture line 2's protocol/maxframe tokens). Add a public framing-descriptor string/const that equals fixture line 2 so the test can byte-compare it.
      - the request/ack envelope DTOs (plain classes with public properties; serialized via JavaScriptSerializer) and the framing helpers. Frame = 4-byte little-endian length prefix + UTF-8 JSON body (RESEARCH A3 — transmission-mode-independent, trivially loopback-testable). Request envelope: `{ protocolVersion, op }` where op="ping" or op="reload-asset" with `{ path, ext }`. Ack envelope mirrors the CLI sorted-key shape `{ schemaVersion, protocolVersion, op, result }` — ping result `{ listening, gameRunning, pid }`; reload-asset result `{ accepted, tier, queued, reloadAttempted, path, note }` where `reloadAttempted` is ALWAYS false this phase and `note` carries the transport-only candor string (C-14, see Action body below).
      - Enforce the bounded MaxFrameBytes (reject oversize frames on read).
    C-03 (CONFIRMED): use **JavaScriptSerializer** (System.Web.Extensions — ALREADY referenced in UtinniCoreDotNet.csproj :60, the same serializer ObjectTemplateSchemaLoader.cs:81 uses). Do NOT use System.Text.Json (NOT in-box on net472; would add a NuGet package + trip the Package Legitimacy Gate the threat model declares untriggered). Keep this class dependency-light (BCL + System.Web.Extensions) so it is referenced by both the server and the test without UtinniCore.dll. Document the JSON field-naming policy (camelCase) so the net10 (System.Text.Json) side in 16-03 can be configured FIELD-FOR-FIELD to match what net472 emits/expects (cross-stack serialization mismatch is the C-02/C-05 amplifier). Add a code comment on PipeName + the framing const naming Utinni.Cli.Tests/Fixtures/live/pipe-name.txt as the canonical agreement anchor that 16-03's net10 duplicate also asserts against.

    Create LivePipeServer.cs in UtinniCoreDotNet/Live/:
      - LIFECYCLE (C-07): a background-thread NamedPipeServerStream accept/read loop with an explicit StartListener()/Stop()/Dispose() surface. StartListener is IDEMPOTENT (a guard flag / Interlocked so a second call does NOT spawn a second accept loop). The loop processes ONE request per connection then closes that NamedPipeServerStream and loops to WaitForConnection for the next client (sequential single-client model — simplest correct shape, no MaxAllowedServerInstances fan-out). A malformed/partial/oversize frame closes ONLY that connection and the loop continues to the next. The accept loop is wrapped so an unexpected exception on one connection is caught + logged and the loop recovers (never tears down the listener). Stop/Dispose cancels the accept/read (CancellationTokenSource + closing the pipe) and joins/ends the worker cleanly. The worker thread NEVER touches native game state (Pitfall 2).
      - SECURITY: apply a PipeSecurity/PipeAccessRule restricting the pipe to the current user (Security V13).
      - PATH CONTAINMENT (C-04): the server is constructed with a pinned in-client root (the resolved client/inject root; reuse the same root the editor save path uses, or accept it via ctor for testability). On a reload-asset request, canonicalize the received `path` against that root via Path.GetFullPath and verify it is rooted under it (StartsWith guard mirroring ResolvedRoot.Resolve). An absolute / `..`-escaping / out-of-root path is REJECTED — the ack returns accepted=false + a `Rejected` disposition and NO enqueue. (Defense-in-depth even though 16-03 resolves on the MCP side: a same-user local process can hit the pipe directly — T-16-21 MCP-side guard does not cover that.)
      - PURE DECISION + GAME-STATE BOUNDARY (C-09): route a parsed request to a PURE decision method that takes `bool gameIsRunning` as a scalar (LivePatchValidator purity pattern). The decision method: ping → build the ping result; reload-asset → first run the containment check; if contained, call ReloadAssetClassifier.Classify(ext, null) for the tier; if gameIsRunning → tier as classified + queued=true; if !gameIsRunning → Unavailable tier + queued=false. The decision method returns the ack envelope WITHOUT performing the enqueue (so it unit-tests). The public Handle method captures gameIsRunning at the MARSHAL/ENQUEUE BOUNDARY — Game.IsRunning is NATIVE-BACKED (P/Invoke, confirmed at UtinniCore.cs:11143) so it must NOT be read on the background worker (Pitfall 2). Capture it once at the boundary (the single allowed crossing) and pass the scalar into the pure decision; the actual reload-binding read, if any, lives inside the enqueued Action which runs on the game thread. Add a code comment at the capture site documenting this (worker-thread-safe: the bool is captured at the boundary, not dereferenced in the read loop).
      - MARSHAL: on a contained reload-asset with gameIsRunning=true, enqueue exactly one Action via GameCallbacks.AddMainLoopCall.
      - CANDOR (C-14): the enqueued Action body is an INTENTIONAL best-effort placeholder this phase (visible render is NON-gating per D-01; rides on RESID-03). Mark it `// TODO(RESID-03): best-effort placeholder per D-01 — wired reload deferred; NOT functional re-render`. The ack ALWAYS sets `reloadAttempted=false` and a `note` like "transport-only until RESID-03; queued != visible reload" so an agent/user cannot read `queued:true` as "the preview happened". The "exactly one Action enqueued with gameIsRunning=true + contained path" assertion still holds (the placeholder is the one Action).
      - Document the supported reload asset classes this phase (Codex): a code comment / XML doc on the reload path stating which extensions return a real tier vs PendingNextSceneChange vs Unavailable per ReloadAssetClassifier, so Classify(ext, null) does not over-promise (note that rootType is null this phase → object-template root-type-specific tiers may under-classify to PendingNextSceneChange; that is the honest minimal-scope disposition).
      - The ack returns IMMEDIATELY (no completion signal — D-01, RESEARCH Open Q2).
    Add the two new files to UtinniCoreDotNet.csproj <Compile Include> entries (old-style csproj).
    In UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj, link the canonical fixture: add `<Content Include="..\Utinni.Cli.Tests\Fixtures\live\pipe-name.txt" Link="Fixtures\live\pipe-name.txt" CopyToOutputDirectory="PreserveNewest" />` (Link form so the SAME committed file — not a copy — is the asserted source).
    Write LivePipeServerTests.cs in UtinniCoreDotNet.Tests/Live/ cloning LivePatchValidatorTests. Cover:
      - envelope-parse→tier→ack with gameIsRunning injected both true and false; reload-asset with gameIsRunning=true + a CONTAINED path yields a tier from ReloadAssetClassifier + queued=true + reloadAttempted=false; with false yields Unavailable + queued=false + NO enqueue. Inject the enqueue target (an Action sink) so the test verifies "exactly one Action enqueued" without a live game thread.
      - path containment (C-04): a reload-asset with an ABSOLUTE path and one with a `..`-escaping relative path each yield accepted=false + Rejected + NO enqueue (server-side rejection, distinct from the 16-03 MCP-side guard).
      - lifecycle (C-07): StartListener is idempotent (calling it twice does not spawn a second accept loop — assert via a single-instance guard or a connection-count probe); Stop/Dispose ends the accept loop without leaking the worker; the accept loop survives a malformed-frame connection and accepts a subsequent valid connection on the SAME running server (two sequential connections: one malformed, one valid → the valid one gets a correct ack).
      - server-side partial-read / mid-message disconnect (C-08): a connection that writes a length prefix then disconnects mid-body closes only that connection (no hang, no crash, loop continues) — mirror the 16-03 client-side oversize/partial coverage on the server side.
      - oversize-frame rejection (MaxFrameBytes); ping result shape.
      - fixture-equality (line 1 + line 2, C-05): read Fixtures/live/pipe-name.txt from the test output dir; assert line 1 byte-equals LiveBridgeProtocol.PipeName AND line 2 byte-equals LiveBridgeProtocol's framing-descriptor const (so name AND framing/maxframe/protocol/json-policy are pinned, not just the name).
    Name the class so `--filter LivePipeServer` selects it. (Lifecycle/partial-read tests may use a real loopback NamedPipe on a randomized pipe name within the test process — this is still Tier-2, no live SWG client.)
  </action>
  <verify>
    <automated>cd D:/Code/Utinni && dotnet test UtinniCoreDotNet.Tests --no-build --filter LivePipeServer</automated>
  </verify>
  <acceptance_criteria>
    - Utinni.Cli.Tests/Fixtures/live/pipe-name.txt exists, committed, line 1 is exactly `utinni-live-bridge`, line 2 is the protocol descriptor (`frame=len32le+utf8json;maxframe=65536;protocol=1;json=camelCase`)
    - LiveBridgeProtocol.cs defines `public const string PipeName = "utinni-live-bridge";`, `ProtocolVersion`, `MaxFrameBytes`, a framing-descriptor const equal to fixture line 2, the request/ack envelope DTOs, and 4-byte LE length-prefix framing; JSON via **JavaScriptSerializer** (System.Web.Extensions) — NO System.Text.Json reference/package added (C-03)
    - The reload-asset ack DTO carries `reloadAttempted` (always false this phase) and a transport-only `note` (C-14 candor)
    - LivePipeServer.cs contains `NamedPipeServerStream`, a PipeSecurity current-user ACL, `GameCallbacks.AddMainLoopCall`, `ReloadAssetClassifier.Classify`, a `Path.GetFullPath` + StartsWith containment guard against a pinned root, and idempotent StartListener + Stop/Dispose lifecycle
    - The reload-decision method takes `bool gameIsRunning` as a scalar (purity); Game.IsRunning is captured at the marshal boundary, NOT read on the background worker loop (C-09 — a code comment documents this and the read does not appear in the accept/read loop body)
    - The enqueued Action body carries the `// TODO(RESID-03): best-effort placeholder per D-01 — wired reload deferred; NOT functional re-render` marker; it remains the single enqueued Action
    - The worker/accept loop contains no direct native game-state dereference (Pitfall 2): the only game-thread crossing is a single enqueued Action
    - `dotnet test UtinniCoreDotNet.Tests --filter LivePipeServer` exits 0; tests cover gameIsRunning true/false + exactly-one-enqueue, path containment (absolute + `..` rejected, no enqueue), lifecycle (idempotent StartListener, Stop/Dispose, malformed-then-valid sequential connections, accept-loop recovery), server-side partial-read / mid-message disconnect, oversize-frame rejection, ping result shape, and the fixture-equality on BOTH line 1 (PipeName) and line 2 (framing)
    - UtinniCoreDotNet.csproj includes both new files; the managed suite builds net472 (no regression in the full UtinniCoreDotNet.Tests count)
  </acceptance_criteria>
  <done>The in-client pipe server + shared protocol exist with a specified lifecycle, server-side path containment, native-safe game-state capture, in-box JavaScriptSerializer framing pinned to the fixture (name + framing), the D-01 placeholder + reloadAttempted=false candor, and pass pure-managed/loopback tests with no live client.</done>
</task>

<task type="auto">
  <name>Task 2: ServerArgs.EnableLive flag (fail-closed default) + extended ServerArgsTests</name>
  <files>Utinni.Mcp/Server/ServerArgs.cs, Utinni.Mcp.Tests/ServerArgsTests.cs</files>
  <read_first>
    - Utinni.Mcp/Server/ServerArgs.cs (the self-analog: --root/--cli-path property + const + parse-loop, lines 43-94; the tolerant/first-wins/equals-form contract in the XML doc lines 28-39)
    - Utinni.Mcp.Tests/ServerArgsTests.cs (the existing parser test class to extend)
    - .planning/phases/16-.../16-RESEARCH.md §A5 (--enable-live is a BOOLEAN presence flag, optional UTINNI_MCP_ENABLE_LIVE env alias mirroring UTINNI_MCP_ROOT)
  </read_first>
  <action>
    Extend ServerArgs.cs: add `public bool EnableLive { get; private set; }` and `private const string EnableLiveFlag = "--enable-live";`. In the Parse loop, add a branch BEFORE the value-flag branches: `if (token == EnableLiveFlag) { result.EnableLive = true; continue; }` — it is a presence flag, NOT value-taking (differs from --root/--cli-path). Default is false (fail-closed by absence, D-04). Optionally honor a UTINNI_MCP_ENABLE_LIVE env var mirroring the UTINNI_MCP_ROOT precedent — if added, require an EXPLICIT truthy value (e.g. "1"/"true"/"yes", case-insensitive), NOT "any non-empty" (Codex LOW: keep enablement explicit/visible), and document it in the class XML doc. Do not break the existing tolerant/first-wins/equals-form contract for the other flags.
    Extend ServerArgsTests.cs: add facts asserting (1) `--enable-live` present → EnableLive true; (2) absent → EnableLive false (the fail-closed default); (3) --enable-live alongside --root/--cli-path parses all three correctly; (4) (if env alias added) an explicit truthy env value sets EnableLive AND a non-truthy/garbage env value does NOT. Name the additions so `--filter ServerArgs` selects them.
  </action>
  <verify>
    <automated>cd D:/Code/Utinni && dotnet test Utinni.Mcp.Tests --no-build --filter ServerArgs</automated>
  </verify>
  <acceptance_criteria>
    - ServerArgs.cs contains `public bool EnableLive` and `EnableLiveFlag = "--enable-live"`; EnableLive defaults to false when the flag is absent
    - `--enable-live` is parsed as a presence flag (no following value consumed)
    - If the env alias is added, it requires an explicit truthy value (not any-non-empty), with a test asserting a garbage value does NOT enable
    - ServerArgsTests asserts present→true, absent→false, and co-existence with --root/--cli-path
    - `dotnet test Utinni.Mcp.Tests --filter ServerArgs` exits 0
  </acceptance_criteria>
  <done>EnableLive parses fail-closed-by-absence with test coverage; the Wave-2 host plan can gate WithTools<LiveTools>() on it.</done>
</task>

<task type="auto">
  <name>Task 3: Wire LivePipeServer.StartListener() into Startup.EntryPoint (main.cs) — in-client-gated, post-Callbacks, pre-Application.Run (C-01, CONFIRMED)</name>
  <files>UtinniCoreDotNet/main.cs, UtinniCoreDotNet.Tests/Live/LivePipeServerTests.cs</files>
  <read_first>
    - UtinniCoreDotNet/main.cs (Startup.EntryPoint :43-114 — the one-time init block; the *Callbacks.Initialize() block :90-93; Native.SignalLauncherReady() :106; the BLOCKING Application.Run :108-110 — the listener start must land between :93 and :108)
    - UtinniCoreDotNet/Live/LivePipeServer.cs (Task 1 — StartListener()/Stop()/Dispose() + ctor that takes the pinned in-client root)
    - UtinniCore.Utinni.utinni.GetConfig() usage at main.cs:108 (the GetConfig().GetBool("Editor","enableEditorMode") precedent — the SAME config surface to read an in-client live-enable flag, C-06)
    - .planning/phases/16-.../16-RESEARCH.md Open Q1 (in-client listener gating as defense-in-depth) + §Security Domain V13
  </read_first>
  <action>
    C-01 (CONFIRMED MISSING): wire the listener start so the bridge actually runs in a real injected client. In Startup.EntryPoint, inside the `if (!initialized)` one-time block, AFTER the four `*Callbacks.Initialize()` calls (main.cs:93) and BEFORE `Native.SignalLauncherReady()` / the blocking `Application.Run` (main.cs:108), construct the LivePipeServer (passing the pinned in-client/inject root — reuse `injectRoot` already computed at :62, or the resolved client root the save path uses) and call StartListener(). StartListener spawns a BACKGROUND thread, so placing it before Application.Run is correct (non-blocking) and it is live for both editor-mode and headless-injected sessions.
    C-06 (defense-in-depth — in-client start gate): gate the StartListener call on its OWN in-client flag, read from the existing config surface (e.g. `utinni.GetConfig().GetBool("Live", "enableLiveBridge")`, mirroring the enableEditorMode read at :108) and/or an env var. The MCP-host `--enable-live` flag (16-02 Task 2 / 16-03) gates TOOL ADVERTISEMENT on the out-of-proc side; this in-client flag gates the LISTENER itself so a same-user process cannot reach the pipe when the in-client tier is off. Default the in-client flag to OFF (fail-closed). Wrap the construct+start in a try/catch that logs and never throws into the init path (the AssemblyResolve/PluginLoader precedent — a live-bridge failure must not abort injection).
    Add a code comment citing C-01 + D-04/D-06 explaining the placement (post-Callbacks so the marshal seam is wired; pre-Application.Run because Run blocks; background thread so non-blocking) and the two-flag model (MCP `--enable-live` = tool gate, in-client flag = listener gate).
    Add a pure-managed test (extend LivePipeServerTests.cs from Task 1, or a sibling fact): assert StartListener is IDEMPOTENT — calling StartListener() twice on one LivePipeServer instance does NOT spawn a second accept loop (single-instance guard), so the main.cs one-time `if (!initialized)` placement plus any defensive re-entry stays safe. (The live in-client confirmation that ping returns listening:true is Tier-4 manual / non-gating per D-01; this task's GATED requirement is that the WIRING exists in main.cs and is code-asserted.)
  </action>
  <verify>
    <automated>cd D:/Code/Utinni && dotnet test UtinniCoreDotNet.Tests --no-build --filter LivePipeServer</automated>
  </verify>
  <acceptance_criteria>
    - main.cs Startup.EntryPoint constructs a LivePipeServer and calls `StartListener()` within the `if (!initialized)` block, positioned AFTER the *Callbacks.Initialize() block and BEFORE Application.Run (a source-assertion: `StartListener` appears in main.cs between the GameCallbacks.Initialize and Application.Run lines)
    - The StartListener call is gated on an in-client enable flag (config/env, default OFF — C-06) AND wrapped in a try/catch that logs and does not throw into the injection init path
    - UtinniCoreDotNet/main.cs is listed in this plan's files_modified (it is)
    - LivePipeServerTests asserts StartListener idempotency (a second call does not double-spawn the accept loop)
    - `dotnet test UtinniCoreDotNet.Tests --filter LivePipeServer` exits 0; full UtinniCoreDotNet.Tests builds net472 with no regression
    - NOTE recorded in the SUMMARY: live in-client ping=listening:true is Tier-4 manual / non-gating (D-01); the GATED deliverable here is the wired + idempotent listener start, code-asserted
  </acceptance_criteria>
  <done>StartListener is wired into main.cs (post-Callbacks, pre-Application.Run, background thread), gated on an in-client flag (defense-in-depth, C-06), failure-isolated, and idempotency-tested — so live_ping can return listening:true in a real injected client (the C-01 delivery gap is closed).</done>
</task>

</tasks>

<threat_model>
**Gate verdict: ASVS L1; no HIGH-severity threats; security gate passes.** (All threats below are mitigate or accept; none rated HIGH. Block-on-HIGH posture: had any threat been HIGH it would gate the wave — none are.)

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local process → named pipe (in-client server) | Any same-user local process that knows the pipe name can connect; the server runs inside the injected x86 client. Both the current-user ACL AND the in-client enable flag (C-06) gate exposure; the reload path is root-contained (C-04). |
| pipe worker thread → game thread | Reload work must cross from the background worker onto the game thread only via the marshal queue; native game state (Game.IsRunning, P/Invoke) is read only at the boundary / on the game thread (C-09, Pitfall 2). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | Spoofing/Elevation | Unauthorized local process connects to the pipe and triggers reloads | mitigate | PipeSecurity/PipeAccessRule restricts the pipe to the current user; NamedPipeServerStream is local-machine only (no network); AND the listener only starts when the in-client enable flag is ON (C-06 defense-in-depth — fail-closed default OFF). Acceptance: current-user ACL + Task-3 in-client-flag gate. |
| T-16-11 | Tampering (integrity) | Reload work executed on the wrong thread / native game-state deref off the game thread → client AV/crash | mitigate | All reload work marshals via GameCallbacks.AddMainLoopCall; gameIsRunning (native-backed P/Invoke) is captured at the marshal boundary, never in the worker read loop (C-09); the worker never dereferences game state (Pitfall 2). Acceptance: the accept/decision path has no direct game-state access; only one Action crosses. |
| T-16-12 | DoS | Malformed/oversize/partial wire frame on the in-client server thread | mitigate | Bounded MaxFrameBytes + op allow-list ({ping,reload-asset}); a bad/partial frame closes that connection only, the accept loop recovers, the game thread is never touched. Acceptance: oversize + server-side partial-read/mid-disconnect + malformed-then-valid-sequential tests (C-07/C-08). |
| T-16-13 | Spoofing | Pipe name collision / squatting (another process pre-creates the pipe) | accept | Sufficiently-specific pipe name; server owns creation (NamedPipeServerStream). Single-user local desktop trust assumption (analog AR-14-03/04). |
| T-16-14 | Tampering | In-client reload-asset targets an arbitrary absolute / out-of-root path (same-user process bypassing the MCP-side root) | mitigate | The server canonicalizes the received path against a pinned in-client root (Path.GetFullPath + StartsWith) and REJECTS absolute / `..`-escaping / out-of-root paths (accepted=false, no enqueue) — independent of the 16-03 MCP-side ResolvedRoot (C-04, T-16-21 covered the MCP side only). Acceptance: Task-1 path-containment tests. |
| T-16-SC | Tampering | npm/pip/cargo installs | n/a | No new packages this phase — net472 server uses **JavaScriptSerializer** (System.Web.Extensions, ALREADY referenced — C-03); BCL System.IO.Pipes only; Package Legitimacy Gate not triggered (no System.Text.Json package added). |
</threat_model>

<verification>
- `dotnet test UtinniCoreDotNet.Tests --filter LivePipeServer` green (envelope→tier→enqueue with injected game state; path containment; lifecycle incl. idempotent StartListener + sequential malformed-then-valid + Stop/Dispose; server-side partial-read; oversize rejection; pipe-name + framing fixture-equality; StartListener idempotency for the main.cs wiring).
- `dotnet test Utinni.Mcp.Tests --filter ServerArgs` green (EnableLive fail-closed default).
- Full UtinniCoreDotNet.Tests + Utinni.Mcp.Tests lanes build with no regression.
- Source assertion: `StartListener` is present in main.cs between the GameCallbacks.Initialize and Application.Run lines (C-01 wiring).
</verification>

<success_criteria>
- The in-client pipe server reuses the shipped marshal seam (mainLoopCallQueue) and ReloadAssetClassifier; no per-frame heap allocation introduced (project_rh_snapshot_no_heap_alloc honored).
- The listener is actually WIRED into main.cs (C-01) so live_ping can return listening:true in a real injected client; the wiring is in-client-flag gated (C-06) and idempotency-tested.
- The in-client reload path is root-contained (C-04); the server lifecycle is specified + tested (C-07/C-08); game-state reads are native-safe (C-09).
- The ack reports the honest reload tier with reloadAttempted=false candor; visible re-render is NOT gated (D-01); the enqueued reload Action is an explicit D-01 placeholder, not wired reload.
- The canonical pipe-name/framing fixture is committed and the in-client PipeName (line 1) + framing constants (line 2) assert equal to it (the cross-plan agreement anchor; C-05); net472 JSON is in-box JavaScriptSerializer (C-03), no new package.
- EnableLive defaults closed; the Wave-2 host gates tool registration on it.
</success_criteria>

<output>
Create `.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-02-SUMMARY.md` when done.
</output>
</content>
