---
phase: 16-live-injected-mcp-bridge-blender-ecosystem-boundary
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - UtinniCoreDotNet/Live/LiveBridgeProtocol.cs
  - UtinniCoreDotNet/Live/CanonicalJson.cs
  - UtinniCoreDotNet/Live/LivePipeServer.cs
  - UtinniCoreDotNet/main.cs
  - UtinniCoreDotNet/UtinniCoreDotNet.csproj
  - UtinniCoreDotNet.Tests/Live/LiveBridgeProtocolTests.cs
  - UtinniCoreDotNet.Tests/Live/LivePipeServerTests.cs
  - UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj
  - Utinni.Cli.Tests/Fixtures/live/pipe-name.txt
  - Utinni.Cli.Tests/Fixtures/live/wire-ping-request.json
  - Utinni.Cli.Tests/Fixtures/live/wire-ping-ack.json
  - Utinni.Cli.Tests/Fixtures/live/wire-reload-request.json
  - Utinni.Cli.Tests/Fixtures/live/wire-reload-ack.json
  - Utinni.Mcp/Server/ServerArgs.cs
  - Utinni.Mcp.Tests/ServerArgsTests.cs

autonomous: true
requirements: [MCP-03]
user_setup:
  - service: "Live bridge (two-flag operator contract — CUR-NEW-6)"
    why: "The live tier requires BOTH flags ON; neither alone is sufficient. This is operator config, not a third-party account — recorded here so it is operator-discoverable, not in code only."
    env_vars: []
    dashboard_config:
      - task: "Enable the in-client listener: set the in-client config flag [Live] enableLiveBridge=true (default OFF/fail-closed) so LivePipeServer.StartListener actually runs inside the injected client (16-02 Task 4)."
        location: "Utinni.ini / the in-client config surface (utinni.GetConfig().GetBool(\"Live\",\"enableLiveBridge\"))"
      - task: "Enable the MCP tool tier: launch Utinni.Mcp with --enable-live (default OFF/fail-closed) so the live_* tools are advertised + the LivePipeClient is registered (16-03)."
        location: "Utinni.Mcp launch args (or UTINNI_MCP_ENABLE_LIVE=1)"
      - task: "BOTH are required: --enable-live alone advertises the tools but the listener never started → live_ping returns listening:false; the in-client flag alone starts the listener but the agent never sees the tools. Neither alone produces a working live path. The live_ping ack also reports the in-client resolved clientRoot (R3-3) so a --root mismatch is operator-visible."
        location: "documented in MCP-SECURITY.md live-tier section (16-03 Task 3) + 16-VALIDATION.md Tier-4 checklist"

must_haves:
  truths:
    - "The in-client LivePipeServer parses a request envelope, classifies a reload-asset request via ReloadAssetClassifier, and enqueues exactly one Action onto the game-thread queue — never touching native game state off the game thread"
    - "When gameIsRunning is the cached false, the server's reload disposition is the honest Unavailable tier (no enqueue side-effect required)"
    - "gameIsRunning is a GAME-THREAD-UPDATED CACHED scalar (volatile/Interlocked snapshot refreshed on the game thread); the pipe worker/accept loop reads ONLY that cached field and NEVER invokes a delegate that P/Invokes native Game.IsRunning (Game.IsRunning is native-backed — Pitfall 2 / CDX-NEW-2 / R3-2): the native read/refresh happens on the game-thread path only"
    - "LivePipeServer.StartListener() is WIRED into Startup.EntryPoint (main.cs), invoked AFTER the *Callbacks.Initialize() block and BEFORE the blocking Application.Run, gated on an in-client enable flag (defense-in-depth, C-06); StartListener is idempotent (no double-spawned accept loop); the game-state cache refresh is wired to a game-thread callback (main-loop tick)"
    - "The in-client reload path is root-contained against the SWG CLIENT ROOT (same semantics as the editor save path / MCP --root — NOT injectRoot/the Utinni DLL dir, CUR-NEW-1): the server resolves the received relative path against the pinned client root via the shipped LooseOverridePath.Resolve and REJECTS absolute / escaping paths (C-04)"
    - "The named-pipe server lifecycle is specified and tested with EXPLICIT bounded timeouts (CDX-NEW-7/8): cancel/dispose unblocks a blocking WaitForConnection within a bounded window; one request per connection; server-side connect/read timeouts close a stalled/slow/oversize-declared connection without starving the accept loop; the loop survives a malformed/partial frame and accepts the next; restart-after-disconnect; accept-loop-throw recovery"
    - "ServerArgs.Parse recognizes --enable-live and exposes EnableLive; absence leaves EnableLive false (fail-closed default)"
    - "The live wire JSON is produced by a CANONICAL JSON writer Utinni OWNS (CanonicalJson — hand-rolled, deterministic: FIXED documented key order, camelCase keys, no insignificant whitespace, explicit ReloadTier enum encoded as its exact pinned string name, consistent null handling, invariant-culture numbers); byte-exactness comes from the writer's fixed field emission, NOT serializer reflection order (R3-1). The net472 side may internally use JavaScriptSerializer/StringBuilder but the determinism is the canonical writer's. ALL FOUR golden wire byte-vectors (ping/reload request+ack) are asserted byte-EXACT on the net472 server side (produce AND consume); variable ack fields (pid/gameRunning) use stable pinned placeholder values so the bytes are deterministic"
    - "Byte-exact is the SOLE wire contract for every message type incl. the nested acks (no 'key-order-independent equality' — R3-7); the golden files hold JSON PAYLOAD bytes only (no 4-byte length prefix), the 4-byte LE prefix framing being covered by the framing-descriptor fixture line 2 + the integration test (R3-6)"
    - "protocolVersion mismatch is handled: the server returns a structured reject ack (or hard error envelope) when a request's protocolVersion != LiveBridgeProtocol.ProtocolVersion, with a skew test (CUR-NEW-5)"
    - "The ReloadTier wire encoding is pinned EXPLICITLY by the canonical writer (exact string names, e.g. \"PendingNextSceneChange\" — pinned casing, not a serializer auto-policy that could lowercase it); it appears in the golden byte-vectors (CUR-NEW-9 / R3-1)"
    - "The live_ping ack carries a clientRoot (or clientRootHash) diagnostic so a --root vs in-client-resolved-client-root MISMATCH is visible at runtime (R3-3 root reconciliation diagnostic)"
    - "The net472 server JSON OUTPUT uses the canonical writer (no System.Text.Json package added — C-03 honesty; a hand-rolled writer needs no package); INPUT parsing may use JavaScriptSerializer/manual parse"
  artifacts:
    - path: "UtinniCoreDotNet/Live/CanonicalJson.cs"
      provides: "Utinni-owned hand-rolled CANONICAL JSON writer + reader: deterministic byte output (fixed key order, camelCase, no whitespace, pinned-string enum, invariant numbers) for the four live-wire payloads (R3-1)"
      contains: "CanonicalJson"
    - path: "UtinniCoreDotNet/Live/LivePipeServer.cs"
      provides: "Managed net472 named-pipe server inside the injected host; background-thread accept loop + bounded lifecycle + game-thread marshal + client-root containment + cached game-state read"
      contains: "NamedPipeServerStream"
    - path: "UtinniCoreDotNet/Live/LiveBridgeProtocol.cs"
      provides: "Shared pipe name constant + protocolVersion + envelope DTOs + framing (4-byte LE length prefix + UTF-8 JSON) + bounded max size; emits/parses via CanonicalJson"
      contains: "PipeName"
    - path: "UtinniCoreDotNet/main.cs"
      provides: "StartListener wiring into Startup.EntryPoint (post-Callbacks.Initialize, pre-Application.Run), in-client-flag gated, pinned to the resolved CLIENT root, with the game-thread game-state cache refresh wired"
      contains: "StartListener"
    - path: "Utinni.Cli.Tests/Fixtures/live/pipe-name.txt"
      provides: "Single canonical pipe-name + framing-descriptor fixture both pipe ends assert against (line 1 name, line 2 framing+json-policy+tier-encoding)"
      contains: "utinni-live-bridge"
    - path: "Utinni.Cli.Tests/Fixtures/live/wire-reload-ack.json"
      provides: "Golden wire byte-vector for the reload ack (camelCase, string-encoded tier, reloadAttempted=false) produced by CanonicalJson — the net472 server asserts byte-exact produce/consume; net10 asserts the same bytes in 16-03 (cross-TFM wire proof CDX-NEW-1/R3-1)"
    - path: "Utinni.Mcp/Server/ServerArgs.cs"
      provides: "EnableLive flag parse (fail-closed default false)"
      contains: "EnableLive"
  key_links:
    - from: "UtinniCoreDotNet/Live/LiveBridgeProtocol.cs"
      to: "UtinniCoreDotNet/Live/CanonicalJson.cs"
      via: "envelope serialize/deserialize routes through the canonical writer/reader (R3-1)"
      pattern: "CanonicalJson"
    - from: "UtinniCoreDotNet/Live/LivePipeServer.cs"
      to: "GameCallbacks.AddMainLoopCall"
      via: "enqueue one Action for game-thread reload"
      pattern: "AddMainLoopCall"
    - from: "UtinniCoreDotNet/Live/LivePipeServer.cs"
      to: "ReloadAssetClassifier.Classify"
      via: "ack tier derivation"
      pattern: "ReloadAssetClassifier\\.Classify"
    - from: "UtinniCoreDotNet/Live/LivePipeServer.cs"
      to: "LooseOverridePath.Resolve (against the pinned CLIENT root)"
      via: "server-side reload-path containment (CUR-NEW-1: client root, not injectRoot)"
      pattern: "LooseOverridePath\\.Resolve"
    - from: "UtinniCoreDotNet/main.cs"
      to: "LivePipeServer.StartListener"
      via: "Startup.EntryPoint wiring (post-Callbacks.Initialize, pre-Application.Run), in-client-flag gated, client-root-pinned, game-thread cache-refresh wired"
      pattern: "StartListener"
    - from: "UtinniCoreDotNet.Tests/Live/LiveBridgeProtocolTests.cs"
      to: "Utinni.Cli.Tests/Fixtures/live/{pipe-name.txt, wire-*.json}"
      via: "fixture-equality on PipeName+framing (line 1/2) AND byte-exact golden wire-vector produce/consume via CanonicalJson on the net472 side"
      pattern: "wire-reload-ack\\.json"
---

<objective>
Build the in-client (injected-host) half of the MCP-03 live bridge plus the `--enable-live` flag plumbing. The named-pipe SERVER lives in managed net472 `UtinniCoreDotNet` (the CLR is already hosted in-proc per clr.cpp — no C++, no cross-arch marshaling). It parses a request envelope on a background thread, derives the honest ack tier via the shipped `ReloadAssetClassifier`, contains the received path under the pinned SWG CLIENT root, reads a GAME-THREAD-CACHED game-state snapshot (never a worker-thread native read), and marshals reload work onto the shipped `GameCallbacks.mainLoopCallQueue` (heap-free hot path, honoring project_rh_snapshot_no_heap_alloc). This plan also WIRES the listener start into `Startup.EntryPoint` (main.cs) so the bridge actually runs in a real injected client, and extends the tested `ServerArgs` parser with `EnableLive` (fail-closed default) so the Wave-2 host plan can gate registration.

This plan defines a **Utinni-owned canonical JSON writer/reader** (`CanonicalJson`) that produces DETERMINISTIC bytes for all four live-wire payloads (ping request/ack, reload request/ack) so the cross-TFM wire proof is byte-exact by construction — not dependent on `JavaScriptSerializer` reflection order. It commits the **single canonical pipe-name/framing fixture** (`Utinni.Cli.Tests/Fixtures/live/pipe-name.txt`) that BOTH pipe ends assert against — BOTH line 1 (pipe name) and line 2 (framing descriptor incl. json-policy + tier-encoding) — AND a set of **golden wire byte-vector fixtures** (`wire-{ping,reload}-{request,ack}.json`) that the net472 server produces/consumes byte-exact via CanonicalJson. Because net10 `Utinni.Mcp` cannot project-ref net472 `UtinniCoreDotNet`, the Wave-2 host (16-03) re-implements CanonicalJson field-for-field against the SAME documented contract; the fixture + golden byte-vectors are the agreement-enforcing anchors that make the duplicated literals + the actual JSON bytes provably byte-identical across the TFM wall — closing the round-2 fragility where a green net472-on-net472 test could hide a net10 casing/encoding mismatch (R3-1).

Purpose: Land the hardest new mechanism (canonical wire writer + pipe server + bounded lifecycle + game-thread marshal + client-root path containment + cached game-state + ack candor + protocolVersion handling + the main.cs wiring that makes it real) behind pure-managed unit tests, and define the shared protocol/pipe-name/framing/golden-wire constants the Wave-2 client consumes.
Output: CanonicalJson (deterministic writer/reader), LiveBridgeProtocol (shared constant + protocolVersion + envelope DTOs + framing), LivePipeServer (in-client, with bounded lifecycle + client-root containment + cached gameIsRunning + protocolVersion skew handling + clientRoot diagnostic), the main.cs StartListener wiring (client-root-pinned + game-thread cache refresh), the canonical pipe-name/framing fixture + golden wire byte-vectors, pure-managed server/protocol tests, ServerArgs.EnableLive + extended ServerArgsTests.

REVIEW-DRIVEN EDITS (from 16-REVIEWS.md — orchestrator-VERIFIED facts in bold): **C-01** (StartListener WIRED into main.cs — confirmed missing; Task 4), **C-03** (net472 JSON OUTPUT = Utinni-owned canonical writer, NOT System.Text.Json — confirmed no STJ ref; package-gate honesty preserved), C-04 (server-side path containment), C-05 (framing line-2 enforcement), C-06 (in-client start gate defense-in-depth), C-07/C-08 (lifecycle + partial-read tests), C-09 (gameIsRunning capture boundary, native-backed), C-12 (protocolVersion envelope field), C-14 (reload ack candor: reloadAttempted=false), Codex (document supported reload asset classes).

REVIEW-DRIVEN EDITS (Round 2, from 16-REVIEWS.md): CDX-NEW-1 = CUR-NEW-2 (committed GOLDEN WIRE BYTE-VECTOR fixtures), CUR-NEW-1 (containment root = SWG CLIENT ROOT, relative-on-wire), CUR-NEW-4 (camelCase on the wire), CUR-NEW-5 (protocolVersion skew reject ack), CUR-NEW-6 (dual-flag operator contract), CUR-NEW-9 (ReloadTier pinned to string names), CDX-NEW-7/8 (bounded server timeouts + bounded dispose), CDX-NEW-2 (gameIsRunning never a native read on a non-game thread), CDX-NEW-4 (exact current-user PipeSecurity ACL).

REVIEW-DRIVEN EDITS (Round 3, from 16-REVIEWS.md TOP section): **R3-1 (HIGH, both)** — the live-wire JSON is produced by a Utinni-owned hand-rolled CANONICAL writer/reader (`CanonicalJson`, Task 1a) so byte-exactness is deterministic by construction (fixed key order, camelCase, no insignificant whitespace, pinned-string ReloadTier, consistent null handling, invariant-culture numbers); NO System.Text.Json package on net472 (C-03 honesty preserved — a hand-rolled writer needs no package). ALL FOUR golden byte-vectors are asserted byte-exact on the net472 side (produce + consume); the round-2 "key-order-independent equality" language is DELETED — byte-exact is the SOLE contract for every message incl. nested acks (R3-7); variable ack fields use stable pinned placeholders. **R3-2 (HIGH Cursor / MEDIUM Codex)** — gameIsRunning is a GAME-THREAD-UPDATED cached scalar (volatile/Interlocked); the worker reads ONLY the cache and NEVER invokes a delegate that P/Invokes native Game.IsRunning; the production refresh is wired to a game-thread callback (Task 4); acceptance asserts (a) no production accept/read-thread path invokes Game.IsRunning, (b) the worker reads the cache field while the refresh is game-thread-wired (Tasks 1b/4). **R3-3 (both MEDIUM)** — the live_ping ack carries a clientRoot/clientRootHash diagnostic so a --root vs in-client-resolved-client-root mismatch is visible at runtime (Tasks 1a/1b; documented in 16-03 Task 3 C + the ping golden vector). **R3-4 (both MEDIUM)** — the oversized round-2 Task 1 is SPLIT: Task 1a = wire contract (LiveBridgeProtocol + CanonicalJson + fixtures + golden + protocol tests, stable before server complexity); Task 1b = LivePipeServer (ACL, lifecycle, containment, cached game-state, skew, marshal, candor) + server tests; the remaining tasks (ServerArgs flag, main.cs wiring) renumbered. **R3-3-Codex (MEDIUM)** — task-level `--no-build` replaced with full `dotnet test` for any task that creates/edits .cs/.csproj (so the gate compiles new code). **R3-6 (Codex LOW)** — the golden files hold JSON PAYLOAD bytes only (no 4-byte length prefix); the prefix framing is covered by fixture line 2 + the integration test (stated, so "golden byte-vector" is not overstated). **R3-8 (Cursor LOW)** — where FormIffEditor.cs:1596 (ResolveClientRoot, UtinniPlugins repo) is cited, the executor MUST grep for `ResolveClientRoot` rather than trust line numbers (cross-repo source may have drifted — confirmed present at FormIffEditor.cs:1596 at plan time but grep to be safe).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-CONTEXT.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-RESEARCH.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-PATTERNS.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-VALIDATION.md

<interfaces>
<!-- Shipped seams the server reuses (do NOT rebuild). Extracted from codebase. -->

From UtinniCoreDotNet/Callbacks/GameCallbacks.cs (:63, :254-257):
  private static readonly ConcurrentQueue<Action> mainLoopCallQueue = new ConcurrentQueue<Action>();
  public static void AddMainLoopCall(Action call)   // enqueue; drained on game thread each frame
  // NOTE (R3-2): AddMainLoopCall runs the Action ON THE GAME THREAD — this is also the seam used to
  // refresh the cached game-state snapshot on the game thread (a recurring/per-tick refresh action).

From UtinniCoreDotNet/Saving/ReloadAssetClassifier.cs (:124, :78-80):
  public static ReloadTier Classify(string extension, string rootTypeIdOrNull)
  // → ReloadedTextures | ReloadedTerrain | PendingNextSceneChange | Unavailable
  // !Game.IsRunning maps to Unavailable upstream (the DISPATCHER does this; the helper is game-state-free)

From UtinniCoreDotNet.PathContainment/LooseOverridePath.cs (:81-175) — the SHARED containment helper (reuse, do NOT reimplement):
  public static string Resolve(string resolvedRoot, string relAssetPath)
  // canonicalizes root; rejects rooted relAssetPath; rejects any '..' segment; trailing-separator
  // OrdinalIgnoreCase StartsWith prefix-attack guard; returns the contained absolute path or throws
  // ArgumentException on escape. netstandard2.0 lib, referenced by net472 UtinniCoreDotNet.

From UtinniCoreDotNet/Editing/LivePatchValidator.cs (:135-147) — purity precedent:
  public static LivePatchValidation Validate(IntPtr targetAddr, int originalMappedLength,
      int rewrittenLength, bool gameIsRunning)   // game state injected as a scalar for unit-testability

From UtinniCoreDotNet/Generated/UtinniCore.cs (:11143) — game-state read (NATIVE-BACKED, C-09/CDX-NEW-2/R3-2):
  public static bool Game.IsRunning { get; }   // P/Invoke ?isRunning@Game@utinni@@SA_NXZ — DO NOT call off the game thread
  // R3-2: this native read happens ONLY inside a game-thread refresh action; the worker never calls it.

From the EDITOR SAVE PATH client-root semantics (UtinniPlugins FormIffEditor.cs ResolveClientRoot — the root the
loose-override save composes under; CUR-NEW-1 source-of-truth. R3-8: GREP for `ResolveClientRoot` rather than trust
the line number — confirmed at FormIffEditor.cs:1596 at plan time, but cross-repo source may drift):
  (1) Path.GetDirectoryName(Process.GetCurrentProcess().MainModule.FileName)  // = SWGEmu.exe dir under injection (the SWG CLIENT ROOT)
  (2) UtinniCore.Utility.utility.GetWorkingDirectory()
  (3) ini.GetString("TreBrowser","clientDir")
  // This is the SWG client root the MCP --root ALSO points at — NOT injectRoot (the Utinni DLL dir).

From UtinniCoreDotNet/main.cs (Startup.EntryPoint, :43-114) — the wiring site (C-01):
  string injectRoot = Path.GetDirectoryName(Assembly.GetExecutingAssembly().Location);  // :62 = the Utinni DLL dir — NOT the client root (CUR-NEW-1)
  if (!initialized) { initialized = true; ... AssemblyResolve handler (15-12) ...
    GameCallbacks.Initialize(); GroundSceneCallbacks/ObjectCallbacks/CuiCallbacks.Initialize();  // :90-93
    Native.SignalLauncherReady();  // :106
    if (...GetConfig().GetBool("Editor","enableEditorMode")) Application.Run(new FormMain(pluginLoader));  // :108-110 — BLOCKS for editor lifetime
  }
  // StartListener must be invoked AFTER :93 and BEFORE :108 (background thread, non-blocking),
  // pinned to the resolved CLIENT root (mirror ResolveClientRoot), NOT injectRoot; the game-state
  // cache refresh wired to a game-thread callback (R3-2).

From UtinniCoreDotNet/UtinniCoreDotNet.csproj (:60):
  <Reference Include="System.Web.Extensions" />   // JavaScriptSerializer lives here — ALREADY referenced (C-03)
  // (There is NO System.Text.Json reference; the canonical writer is hand-rolled BCL-only, so no NuGet package /
  //  Package Legitimacy Gate is triggered. JavaScriptSerializer may be used for INPUT parsing only.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Wire CONTRACT — CanonicalJson (deterministic writer/reader) + LiveBridgeProtocol (envelope DTOs + framing) + canonical pipe-name/framing fixture + four GOLDEN WIRE BYTE-VECTORS + golden produce/consume + camelCase/string-tier/skew protocol tests (R3-1, R3-4)</name>
  <files>UtinniCoreDotNet/Live/CanonicalJson.cs, UtinniCoreDotNet/Live/LiveBridgeProtocol.cs, UtinniCoreDotNet/UtinniCoreDotNet.csproj, UtinniCoreDotNet.Tests/Live/LiveBridgeProtocolTests.cs, UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj, Utinni.Cli.Tests/Fixtures/live/pipe-name.txt, Utinni.Cli.Tests/Fixtures/live/wire-ping-request.json, Utinni.Cli.Tests/Fixtures/live/wire-ping-ack.json, Utinni.Cli.Tests/Fixtures/live/wire-reload-request.json, Utinni.Cli.Tests/Fixtures/live/wire-reload-ack.json</files>
  <read_first>
    - UtinniCoreDotNet/Saving/ReloadAssetClassifier.cs (the ReloadTier enum member names — the EXACT pinned strings the canonical writer emits, e.g. "PendingNextSceneChange"; the writer encodes ReloadTier.ToString() verbatim, NOT a lowercased/auto-policy form — R3-1)
    - UtinniCoreDotNet/Formats/ObjectTemplate/ObjectTemplateSchemaLoader.cs (:81 — the EXISTING JavaScriptSerializer usage in this assembly; in-box net472 JSON precedent for C-03. NOTE: it may be used for INPUT parse only; OUTPUT goes through CanonicalJson for determinism — R3-1)
    - Utinni.Cli/Output/JsonOutput.cs (the CLI's sorted-key ack envelope shape `{ schemaVersion, command/op, result }` the live ack mirrors so an agent sees a familiar shape)
    - Utinni.Cli.Tests/Utinni.Cli.Tests.csproj (the `<Content Include="Fixtures\**\*" CopyToOutputDirectory="PreserveNewest" />` pattern — the fixtures live under this project's Fixtures tree and are auto-copied to its output; the net472 test project links them)
    - .planning/phases/16-.../16-RESEARCH.md §A3 (wire format) + §A2 (threading model context)
  </read_first>
  <action>
    Commit the CANONICAL pipe-name/framing fixture FIRST: create Utinni.Cli.Tests/Fixtures/live/pipe-name.txt as the single source of truth for the cross-plan agreement. Two lines: line 1 = the canonical pipe name `utinni-live-bridge` (verbatim, no surrounding whitespace); line 2 = the framing descriptor `frame=len32le+utf8json;maxframe=65536;protocol=1;json=camelCase;tier=stringnames` naming the 4-byte little-endian length-prefix + UTF-8 JSON framing, the bounded max frame size (bytes), the protocol version, the JSON field-naming policy (camelCase), AND the ReloadTier wire encoding (stringnames — CUR-NEW-9). Line 2 is a PROTOCOL descriptor (C-05): both ends parse it and assert their implemented constants match. Write the canonical values verbatim — do NOT paraphrase or change casing/separators.

    Create CanonicalJson.cs in UtinniCoreDotNet/Live/ (R3-1 — the HIGH fix): a small Utinni-owned HAND-ROLLED canonical JSON writer + reader, BCL-only (StringBuilder + Encoding.UTF8 + a minimal tokenizer for the reader; NO NuGet package — preserves C-03 honesty). The writer's DETERMINISM contract, documented in a header comment and enforced by emission order:
      - FIXED, DECLARED key order per message type (document the order in the comment; the writer emits fields in that order ALWAYS — determinism is from this fixed emission, NOT serializer reflection order).
      - camelCase keys (the writer emits the camelCase literal key strings directly).
      - NO insignificant whitespace (no spaces/newlines between tokens).
      - ReloadTier encoded as its EXACT enum NAME string (ReloadTier.ToString(), pinned casing e.g. "PendingNextSceneChange") — NOT an integer, NOT a lowercased/auto-policy form.
      - Explicit, consistent null handling (document: a null string field is emitted as `null` OR omitted — pick ONE and apply it uniformly; the goldens reflect the choice).
      - Invariant-culture number formatting (CultureInfo.InvariantCulture for any numeric/bool emission; bools as `true`/`false`).
      - String escaping per JSON spec (quotes, backslash, control chars) — deterministic.
      The reader parses the same canonical bytes back into the DTOs (tolerant enough to consume the committed goldens; the produce path is the byte-exact authority). Provide CanonicalJson.Serialize(envelope) → byte[] (UTF-8) and CanonicalJson.Deserialize<T>(byte[]) → DTO. State in a comment that 16-03 re-implements this writer field-for-field against the same documented contract (net10 cannot project-ref net472) and the golden byte-vectors are the cross-TFM proof.

    Create LiveBridgeProtocol.cs in UtinniCoreDotNet/Live/: a shared static class defining:
      - `public const string PipeName = "utinni-live-bridge";` (byte-identical to fixture line 1).
      - `public const int ProtocolVersion = 1;` and `public const int MaxFrameBytes = 65536;` (byte-identical to fixture line 2's protocol/maxframe tokens). Add a public framing-descriptor const string equal to fixture line 2 so the test can byte-compare it.
      - the request/ack envelope DTOs (plain classes/structs). Request: `{ protocolVersion, op }` where op="ping" or op="reload-asset" with `{ path, ext }` (path RELATIVE — CUR-NEW-1). Ack mirrors the CLI sorted-key shape `{ schemaVersion, protocolVersion, op, result }` — ping result `{ listening, gameRunning, pid, clientRoot }` (R3-3 — clientRoot is the in-client resolved client root, a string; OR clientRootHash if hashing is preferred — pick one and document it); reload-asset result `{ accepted, tier, queued, reloadAttempted, path, note }`.
      - Serialize/deserialize the envelopes EXCLUSIVELY via CanonicalJson (the OUTPUT path; INPUT may also go through CanonicalJson.Deserialize). Frame = 4-byte little-endian length prefix + the CanonicalJson UTF-8 body (RESEARCH A3); expose framing helpers (write-frame / read-frame) enforcing MaxFrameBytes on read.
      - PROTOCOLVERSION enforcement helper: a request whose `protocolVersion != ProtocolVersion` routes to a structured reject (used by the server in Task 1b, CUR-NEW-5).
    C-03 (CONFIRMED): OUTPUT bytes come from CanonicalJson (hand-rolled, no package). JavaScriptSerializer (System.Web.Extensions, ALREADY referenced :60) MAY be used for INPUT parse if convenient, but NOT for OUTPUT (determinism). Do NOT use System.Text.Json. Add a code comment on PipeName + the framing const naming the fixture files (pipe-name.txt + wire-*.json) as the canonical agreement anchors that 16-03's net10 re-implementation also asserts against.

    Commit the GOLDEN WIRE BYTE-VECTOR fixtures (R3-1/CDX-NEW-1): create four files under Utinni.Cli.Tests/Fixtures/live/ holding the EXACT JSON BODY bytes (UTF-8, no BOM, NO framing prefix — JSON payload only, so both TFMs can assert it independent of framing; R3-6 — the 4-byte LE prefix is covered separately by fixture line 2 + the Task-1b/integration tests):
      - wire-ping-request.json: `{ protocolVersion, op }` (op="ping"), camelCase keys.
      - wire-ping-ack.json: `{ schemaVersion, protocolVersion, op, result:{ listening, gameRunning, pid, clientRoot } }` — use STABLE PINNED placeholder values for the variable fields (e.g. listening:true, gameRunning:true, pid:0, clientRoot:"C:/swg/client" or a fixed clientRootHash) so the byte-vector is DETERMINISTIC and asserted BYTE-EXACT (R3-7 — no key-order-independent fallback; R3-3 — clientRoot present).
      - wire-reload-request.json: `{ protocolVersion, op, path, ext }` (op="reload-asset"), where `path` is a RELATIVE asset path (CUR-NEW-1).
      - wire-reload-ack.json: `{ schemaVersion, protocolVersion, op, result:{ accepted, tier, queued, reloadAttempted, path, note } }` where `tier` is a STRING name (e.g. "PendingNextSceneChange" — CUR-NEW-9, NOT an integer), `reloadAttempted` is false (C-14), `note` carries the transport-only candor string.
    Generate these by running CanonicalJson.Serialize on the canonical DTO instances, then commit the output verbatim. The test re-serializes and asserts byte-exact against the committed files.

    Add CanonicalJson.cs + LiveBridgeProtocol.cs to UtinniCoreDotNet.csproj <Compile Include> entries (old-style csproj).
    In UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj, link the canonical fixture + golden vectors: add `<Content Include="..\Utinni.Cli.Tests\Fixtures\live\pipe-name.txt" Link="Fixtures\live\pipe-name.txt" CopyToOutputDirectory="PreserveNewest" />` and the four `wire-*.json` files (Link form so the SAME committed files are the asserted source).
    Write LiveBridgeProtocolTests.cs in UtinniCoreDotNet.Tests/Live/ (name so `--filter LiveBridgeProtocol` selects it). Cover:
      - GOLDEN WIRE BYTE-VECTORS (R3-1/CDX-NEW-1): CanonicalJson.Serialize the canonical ping request, ping ack (fixed/pinned-value fields), reload request, and reload ack; assert the produced UTF-8 bytes byte-EXACT match each committed wire-*.json fixture; AND CanonicalJson.Deserialize each committed fixture back into the DTOs and assert the round-trip. ALL FOUR are byte-exact (incl. the nested acks — R3-7; no key-order-independent fallback).
      - canonical-writer ORDER STABILITY (R3-1): serialize the SAME DTO twice and assert identical bytes; serialize DTOs whose fields were SET in a different source order and assert the OUTPUT key order is still the fixed declared order (proves determinism is from fixed emission, not input order / reflection).
      - camelCase + string-tier (CUR-NEW-4/CUR-NEW-9): assert the serialized reload-ack bytes contain `"tier":"PendingNextSceneChange"` (string, not `"tier":2`) and camelCase keys (`"protocolVersion"`, not `"ProtocolVersion"`).
      - clientRoot diagnostic (R3-3): assert the ping-ack bytes contain the `clientRoot` (or `clientRootHash`) key and that it round-trips.
      - protocolVersion skew helper (CUR-NEW-5): the LiveBridgeProtocol version-check helper flags protocolVersion=999 as a reject (the structured-reject-ack production is asserted in Task 1b against the server).
      - fixture-equality (line 1 + line 2, C-05): read Fixtures/live/pipe-name.txt; assert line 1 byte-equals LiveBridgeProtocol.PipeName AND line 2 byte-equals the framing-descriptor const.
      - framing round-trip: write-frame(body) then read-frame returns the same body; an oversize declared length (> MaxFrameBytes) is rejected on read.
  </action>
  <verify>
    <automated>cd D:/Code/Utinni && dotnet test UtinniCoreDotNet.Tests --filter LiveBridgeProtocol</automated>
  </verify>
  <acceptance_criteria>
    - The Task-1a verify BUILDS UtinniCoreDotNet + UtinniCoreDotNet.Tests (full `dotnet test`, no --no-build) and passes (R3-3-Codex — the gate compiles the new .cs/.csproj)
    - CanonicalJson.cs exists, is BCL-only (no NuGet package / no System.Text.Json), and its header comment documents the deterministic contract (fixed key order per message, camelCase, no whitespace, pinned-string ReloadTier, consistent null handling, invariant-culture numbers); a test asserts order-stability (same DTO → same bytes; field-set order does not change output key order) — R3-1
    - Utinni.Cli.Tests/Fixtures/live/pipe-name.txt exists, committed, line 1 is exactly `utinni-live-bridge`, line 2 is `frame=len32le+utf8json;maxframe=65536;protocol=1;json=camelCase;tier=stringnames`
    - The four wire-*.json golden byte-vectors exist, committed, holding JSON PAYLOAD bytes only (NO 4-byte length prefix — R3-6); the reload-ack vector contains `"tier":"PendingNextSceneChange"` (string) + `"reloadAttempted":false` + camelCase keys; the reload-request carries a RELATIVE `path`; the ping-ack carries `clientRoot`/`clientRootHash` (R3-3)
    - LiveBridgeProtocolTests serializes the DTOs via CanonicalJson and asserts byte-EXACT equality against EACH committed wire-*.json (all four, incl. the nested acks — R3-7, no key-order-independent path), AND deserializes each fixture back (produce + consume); a test asserts camelCase keys + string-encoded tier + the clientRoot diagnostic
    - LiveBridgeProtocol.cs defines PipeName/ProtocolVersion/MaxFrameBytes, the framing-descriptor const equal to fixture line 2, the envelope DTOs (ping result carries clientRoot — R3-3), 4-byte LE length-prefix framing, and routes OUTPUT through CanonicalJson — NO System.Text.Json reference/package (C-03)
    - The reload-asset ack DTO carries `reloadAttempted` (always false) + a transport-only `note` (C-14)
    - A protocolVersion-skew check helper flags version!=1; framing round-trips and rejects an oversize declared length
    - UtinniCoreDotNet.csproj includes CanonicalJson.cs + LiveBridgeProtocol.cs; the managed suite builds net472 with no regression in the full UtinniCoreDotNet.Tests count
    - `dotnet test UtinniCoreDotNet.Tests --filter LiveBridgeProtocol` exits 0
  </acceptance_criteria>
  <done>The deterministic wire contract is stable and proven before any server complexity: a Utinni-owned canonical writer/reader, the pinned pipe-name/framing fixture, four byte-exact golden wire-vectors (produce + consume, all four, no key-order-independent fallback), camelCase + string-tier + clientRoot-diagnostic + skew-check + framing, all BCL-only (no STJ package — C-03), ready for the server to consume in Task 1b and the net10 side to mirror in 16-03.</done>
</task>

<task type="auto">
  <name>Task 1b: LivePipeServer — bounded accept loop + bounded dispose + exact current-user ACL + CLIENT-ROOT containment + GAME-THREAD-CACHED game-state (worker reads cache only) + protocolVersion skew reject + marshal + D-01 candor + server tests (R3-2, R3-4)</name>
  <files>UtinniCoreDotNet/Live/LivePipeServer.cs, UtinniCoreDotNet/UtinniCoreDotNet.csproj, UtinniCoreDotNet.Tests/Live/LivePipeServerTests.cs, UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj</files>
  <read_first>
    - UtinniCoreDotNet/Live/LiveBridgeProtocol.cs + CanonicalJson.cs (Task 1a — the envelopes, framing, version-check helper, and the deterministic writer/reader the server consumes)
    - UtinniCoreDotNet/Saving/ReloadAssetClassifier.cs (Classify signature + the 4 tiers; the helper is GAME-STATE-FREE — !Game.IsRunning→Unavailable is the DISPATCHER's job; reuse Classify for the tier — PATTERNS lines 144-149)
    - UtinniCoreDotNet/Callbacks/GameCallbacks.cs (mainLoopCallQueue + AddMainLoopCall — the marshal seam to reuse AND the game-thread seam used to refresh the cached game-state snapshot; PATTERNS lines 136-141)
    - UtinniCoreDotNet/Editing/LivePatchValidator.cs (the gameIsRunning-injected-as-scalar purity pattern; lines 135-147)
    - UtinniCoreDotNet.PathContainment/LooseOverridePath.cs (:81-175 — the SHARED containment helper to call directly for the server-side reload-path containment against the CLIENT root; reuse, do not reimplement — CUR-NEW-1/CUR-NEW-7)
    - UtinniCoreDotNet.Tests/Editing/LivePatchValidatorTests.cs (the injected-state pure-fn test shape to clone)
    - .planning/phases/16-.../16-RESEARCH.md §A2/A4 (threading model, ack reuse) + §Security Domain V13 (PipeSecurity current-user ACL) + Pitfall 2 (worker thread never touches game state)
  </read_first>
  <action>
    Create LivePipeServer.cs in UtinniCoreDotNet/Live/:
      - LIFECYCLE WITH BOUNDED TIMEOUTS (C-07 / CDX-NEW-7 / CDX-NEW-8): a background-thread NamedPipeServerStream accept/read loop with StartListener()/Stop()/Dispose(). StartListener is IDEMPOTENT (Interlocked guard so a second call does NOT spawn a second accept loop). One request per connection then close that stream and loop to accept the next (sequential single-client model). SERVER-SIDE TIMEOUTS: bounded connect-wait + bounded per-read timeout so a client that connects then stalls, sends only the length prefix, drips the body slowly, or declares an oversize frame is closed within a bounded window WITHOUT starving the accept loop (async pipe APIs with CancellationTokenSource(readTimeout) or a guard timer). A malformed/partial/oversize frame closes ONLY that connection; the loop recovers and continues. An unexpected exception on one connection is caught + logged; the listener is never torn down. BOUNDED DISPOSE: Stop/Dispose must unblock a blocking WaitForConnection within a bounded time — use WaitForConnectionAsync with a linked CTS OR close/dispose the NamedPipeServerStream to force the wait to throw; the worker thread joins within a bounded window. The worker thread NEVER touches native game state (Pitfall 2 / R3-2).
      - SECURITY — EXACT ACL (CDX-NEW-4): construct the NamedPipeServerStream with a PipeSecurity that adds a single PipeAccessRule using `WindowsIdentity.GetCurrent().User` as the IdentityReference, `PipeAccessRights.FullControl`, `AccessControlType.Allow`. Add NO other rules (deny-others-by-omission). State this construction in a code comment. (Non-current-user-denial is Tier-4; the construction is pinned and the rule-count/identity asserted in-test.)
      - PATH CONTAINMENT AGAINST THE CLIENT ROOT (C-04 / CUR-NEW-1): the server is constructed with a pinned SWG CLIENT root (the SAME root the editor save path resolves — mirror FormIffEditor.ResolveClientRoot: MainModule dir → GetWorkingDirectory → [TreBrowser] clientDir — accept it via ctor for testability; NOT injectRoot). The wire carries a RELATIVE path; on a reload-asset request the server calls `LooseOverridePath.Resolve(clientRoot, relativePath)` (the shared helper — reuse, do not reimplement). A rooted/absolute or `..`-escaping path makes Resolve throw ArgumentException → the ack returns accepted=false + a `Rejected` disposition and NO enqueue.
      - GAME-THREAD-CACHED GAME-STATE (R3-2 — the HIGH fix): the server holds a CACHED game-state snapshot — a `private volatile bool _gameIsRunningCache;` (or an Interlocked-updated int) — that is refreshed ONLY on the game thread. Expose a public `RefreshGameStateOnGameThread()` method that reads native `Game.IsRunning` and writes the cache; this method is intended to be invoked from a game-thread context (wired via GameCallbacks in Task 4). For UNIT testability, keep an injected SNAPSHOT SOURCE: the ctor takes a `Func<bool> gameStateProbe` that production wires to read Game.IsRunning, but RefreshGameStateOnGameThread is the ONLY place that probe is invoked, and it is documented as game-thread-only. The accept/read loop + the pure decision read ONLY `_gameIsRunningCache` — they NEVER invoke the probe Func and contain NO `Game.IsRunning` reference. (This makes Pitfall-2 SEMANTIC: the worker cannot trigger a native read on its thread; the native read is structurally confined to the refresh path.)
      - PURE DECISION (C-09 / CDX-NEW-2): route a parsed request to a PURE decision method that takes the resolved-or-rejected path + a `bool gameIsRunning` SCALAR (the cached value is passed in). The decision: protocolVersion mismatch → structured reject ack (CUR-NEW-5); ping → ping result incl. the pinned/resolved clientRoot diagnostic (R3-3); reload-asset → containment check first; if contained, ReloadAssetClassifier.Classify(ext, null) for the tier; if gameIsRunning → tier as classified + queued=true; if !gameIsRunning → Unavailable + queued=false. The decision returns the ack WITHOUT performing the enqueue (unit-testable).
      - MARSHAL: on a contained reload-asset with the cached gameIsRunning=true, enqueue exactly one Action via GameCallbacks.AddMainLoopCall.
      - CANDOR (C-14): the enqueued Action body is an INTENTIONAL best-effort placeholder this phase (visible render NON-gating per D-01; rides on RESID-03). Mark it `// TODO(RESID-03): best-effort placeholder per D-01 — wired reload deferred; NOT functional re-render`. The ack ALWAYS sets `reloadAttempted=false` and a `note` like "transport-only until RESID-03; queued != visible reload".
      - CLIENTROOT DIAGNOSTIC (R3-3): the ping result reports the server's pinned/resolved client root (clientRoot string OR clientRootHash) so a --root vs in-client-root mismatch is runtime-visible.
      - Document the supported reload asset classes this phase (Codex): a code comment stating which extensions return a real tier vs PendingNextSceneChange vs Unavailable per ReloadAssetClassifier; rootType is null this phase → object-template root-type-specific tiers may under-classify to PendingNextSceneChange.
      - The ack returns IMMEDIATELY (no completion signal — D-01, RESEARCH Open Q2). All OUTPUT bytes go through CanonicalJson (Task 1a).
    Add LivePipeServer.cs to UtinniCoreDotNet.csproj <Compile Include>.
    Write LivePipeServerTests.cs in UtinniCoreDotNet.Tests/Live/ cloning LivePatchValidatorTests (name so `--filter LivePipeServer` selects it). Cover:
      - decision→tier→ack with the CACHED gameIsRunning set true and false: reload-asset with cache=true + a CONTAINED relative path yields a tier from ReloadAssetClassifier + queued=true + reloadAttempted=false; with cache=false yields Unavailable + queued=false + NO enqueue. Inject the enqueue target (an Action sink) so the test verifies "exactly one Action enqueued" without a live game thread.
      - CACHED GAME-STATE SEMANTICS (R3-2): (a) a SOURCE-assertion test (grep-style over the LivePipeServer.cs accept/read-loop + decision region) that NO production accept/read-thread code path contains `Game.IsRunning` NOR invokes the injected probe Func — only `_gameIsRunningCache` is read off-game-thread; (b) a behavioral test asserting the probe Func is invoked ONLY by RefreshGameStateOnGameThread (call Refresh, observe the cache updates from the probe; run a request on the worker and assert the probe invocation count did NOT increase — the worker read the cache field, not the probe). This proves the native read is game-thread-confined SEMANTICALLY, not just token-absent.
      - path containment against the CLIENT root (C-04/CUR-NEW-1): construct the server with a temp client-root tree; a reload-asset with a CONTAINED relative path resolves + (cache=true) enqueues; a reload-asset with an ABSOLUTE path and one with a `..`-escaping relative path each yield accepted=false + Rejected + NO enqueue. Add a cross-root case: server root = temp tree A; a relative path resolves under A (not a different root) — proving the server uses its OWN pinned root, not a wire-supplied absolute.
      - clientRoot diagnostic (R3-3): the ping ack result carries the server's pinned clientRoot (or hash); assert it equals the ctor-pinned root (or its hash).
      - protocolVersion skew (CUR-NEW-5): a request with protocolVersion=999 yields a structured reject ack (not a silent partial parse, not a crash).
      - lifecycle with BOUNDED timeouts (C-07/CDX-NEW-7/8): StartListener idempotent (twice → one accept loop); Stop/Dispose unblocks a blocking WaitForConnection and the worker joins within a bounded time (assert dispose returns within N ms); a connected-then-stalled client is closed within the read-timeout window without blocking a subsequent valid connection; the accept loop survives a malformed-frame connection and accepts a subsequent valid connection on the SAME running server.
      - server-side partial-read / mid-message disconnect (C-08): a connection that writes a length prefix then disconnects mid-body closes only that connection (no hang, no crash, loop continues).
      - oversize-frame rejection (MaxFrameBytes); ping result shape (incl. clientRoot).
      - ACL construction (CDX-NEW-4): assert the server's PipeSecurity has exactly one access rule whose IdentityReference resolves to WindowsIdentity.GetCurrent().User with FullControl/Allow.
    (Lifecycle/timeout tests may use a real loopback NamedPipe on a randomized pipe name within the test process — still Tier-2, no live SWG client.)
  </action>
  <verify>
    <automated>cd D:/Code/Utinni && dotnet test UtinniCoreDotNet.Tests --filter LivePipeServer</automated>
  </verify>
  <acceptance_criteria>
    - The Task-1b verify is a full `dotnet test` (no --no-build) so it compiles LivePipeServer.cs + the new test file (R3-3-Codex)
    - LivePipeServer.cs contains `NamedPipeServerStream`, a PipeSecurity rule built from `WindowsIdentity.GetCurrent().User` + FullControl/Allow (deny-others-by-omission, CDX-NEW-4), `GameCallbacks.AddMainLoopCall`, `ReloadAssetClassifier.Classify`, a `LooseOverridePath.Resolve(clientRoot, relativePath)` containment call against the pinned CLIENT root (NOT injectRoot — CUR-NEW-1), idempotent StartListener + bounded Stop/Dispose, and bounded server read/connect timeouts (CDX-NEW-7/8)
    - GAME-STATE is a game-thread-updated cached scalar (volatile/Interlocked): a public RefreshGameStateOnGameThread reads native Game.IsRunning + writes the cache; the accept/read loop + decision read ONLY the cache field and contain NO `Game.IsRunning` reference and do NOT invoke the injected probe Func (R3-2 — source-assertion test (a)); a behavioral test proves the probe is invoked ONLY via Refresh, not by serving a worker request (R3-2 test (b))
    - The reload-asset ack DTO carries `reloadAttempted` (always false) + a transport-only `note` (C-14); the ping ack carries the clientRoot/clientRootHash diagnostic (R3-3)
    - A CONTAINED relative path enqueues (cache=true); an ABSOLUTE + a `..`-escaping path each yield accepted=false + Rejected + NO enqueue; a cross-root case proves own-root resolution (C-04/CUR-NEW-1)
    - A protocolVersion-skew request yields a structured reject ack (CUR-NEW-5)
    - The enqueued Action body carries the `// TODO(RESID-03): ...` marker and remains the single enqueued Action; all OUTPUT bytes go through CanonicalJson (no STJ — C-03)
    - `dotnet test UtinniCoreDotNet.Tests --filter LivePipeServer` exits 0; tests cover cached gameIsRunning true/false + exactly-one-enqueue, cached-game-state semantics (source + behavioral), client-root containment (contained/absolute/escape/cross-root), clientRoot diagnostic, protocolVersion skew, bounded lifecycle (idempotent StartListener, dispose-within-N-ms, stalled-client-closed, malformed-then-valid sequential, accept-loop recovery), server-side partial-read, oversize rejection, ping result shape, ACL construction
    - UtinniCoreDotNet.csproj includes LivePipeServer.cs; the managed suite builds net472 with no regression
  </acceptance_criteria>
  <done>The in-client pipe server exists with a bounded lifecycle/dispose, server-side CLIENT-ROOT containment via the shared LooseOverridePath, a GAME-THREAD-CACHED game-state the worker reads but never natively probes (R3-2 semantic guarantee), CanonicalJson OUTPUT, protocolVersion-skew handling, exact current-user ACL, the clientRoot diagnostic, the D-01 placeholder + reloadAttempted=false candor, passing pure-managed/loopback tests with no live client.</done>
</task>

<task type="auto">
  <name>Task 2: ServerArgs.EnableLive flag (fail-closed default) + extended ServerArgsTests</name>
  <files>Utinni.Mcp/Server/ServerArgs.cs, Utinni.Mcp.Tests/ServerArgsTests.cs</files>
  <read_first>
    - Utinni.Mcp/Server/ServerArgs.cs (the self-analog: --root/--cli-path property + const + parse-loop, lines 43-94; the tolerant/first-wins/equals-form contract in the XML doc lines 28-39)
    - Utinni.Mcp.Tests/ServerArgsTests.cs (the existing parser test class to extend)
    - .planning/phases/16-.../16-RESEARCH.md §A5 (--enable-live is a BOOLEAN presence flag, optional UTINNI_MCP_ENABLE_LIVE env alias mirroring UTINNI_MCP_ROOT)
  </read_first>
  <action>
    Extend ServerArgs.cs: add `public bool EnableLive { get; private set; }` and `private const string EnableLiveFlag = "--enable-live";`. In the Parse loop, add a branch BEFORE the value-flag branches: `if (token == EnableLiveFlag) { result.EnableLive = true; continue; }` — it is a presence flag, NOT value-taking. Default is false (fail-closed by absence, D-04). Optionally honor a UTINNI_MCP_ENABLE_LIVE env var mirroring the UTINNI_MCP_ROOT precedent — if added, require an EXPLICIT truthy value (e.g. "1"/"true"/"yes", case-insensitive), NOT "any non-empty" (Codex LOW), and document it in the class XML doc. In the class XML doc, also state the DUAL-FLAG operator contract (CUR-NEW-6): --enable-live gates only the MCP tool tier; the in-client `[Live] enableLiveBridge` flag (16-02 Task 4) gates the LISTENER; BOTH must be ON, neither alone is sufficient. Do not break the existing tolerant/first-wins/equals-form contract for the other flags.
    Extend ServerArgsTests.cs: add facts asserting (1) `--enable-live` present → EnableLive true; (2) absent → EnableLive false (the fail-closed default); (3) --enable-live alongside --root/--cli-path parses all three correctly; (4) (if env alias added) an explicit truthy env value sets EnableLive AND a non-truthy/garbage env value does NOT. Name the additions so `--filter ServerArgs` selects them.
  </action>
  <verify>
    <automated>cd D:/Code/Utinni && dotnet test Utinni.Mcp.Tests --filter ServerArgs</automated>
  </verify>
  <acceptance_criteria>
    - The verify is a full `dotnet test` (no --no-build) since this task edits ServerArgs.cs + ServerArgsTests.cs (R3-3-Codex)
    - ServerArgs.cs contains `public bool EnableLive` and `EnableLiveFlag = "--enable-live"`; EnableLive defaults to false when the flag is absent
    - `--enable-live` is parsed as a presence flag (no following value consumed)
    - The class XML doc states the dual-flag operator contract (--enable-live + in-client [Live] enableLiveBridge, both required — CUR-NEW-6)
    - If the env alias is added, it requires an explicit truthy value (not any-non-empty), with a test asserting a garbage value does NOT enable
    - ServerArgsTests asserts present→true, absent→false, and co-existence with --root/--cli-path
    - `dotnet test Utinni.Mcp.Tests --filter ServerArgs` exits 0
  </acceptance_criteria>
  <done>EnableLive parses fail-closed-by-absence with test coverage and a documented dual-flag operator contract; the Wave-2 host plan can gate WithTools<LiveTools>() on it.</done>
</task>

<task type="auto">
  <name>Task 3: Wire LivePipeServer.StartListener() into Startup.EntryPoint (main.cs) — in-client-gated, CLIENT-ROOT-pinned, game-thread game-state-cache-refresh-wired, post-Callbacks, pre-Application.Run (C-01; CUR-NEW-1; R3-2)</name>
  <files>UtinniCoreDotNet/main.cs, UtinniCoreDotNet.Tests/Live/LivePipeServerTests.cs</files>
  <read_first>
    - UtinniCoreDotNet/main.cs (Startup.EntryPoint :43-114 — the one-time init block; `injectRoot` at :62 is the Utinni DLL dir, NOT the client root — CUR-NEW-1; the *Callbacks.Initialize() block :90-93; Native.SignalLauncherReady() :106; the BLOCKING Application.Run :108-110 — the listener start must land between :93 and :108)
    - UtinniCoreDotNet/Live/LivePipeServer.cs (Task 1b — StartListener()/Stop()/Dispose() + ctor that takes the pinned CLIENT root + the injected gameStateProbe + the public RefreshGameStateOnGameThread the game-thread refresh calls)
    - UtinniCoreDotNet/Callbacks/GameCallbacks.cs (AddMainLoopCall — the game-thread seam to wire the recurring game-state cache refresh through, R3-2)
    - UtinniPlugins "The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs" ResolveClientRoot (R3-8: GREP for `ResolveClientRoot` rather than trust the line number — confirmed at :1596 at plan time; the EXACT chain to mirror: MainModule dir → GetWorkingDirectory → [TreBrowser] clientDir; CUR-NEW-1. Reuse this resolution, NOT injectRoot.)
    - UtinniCore.Utinni.utinni.GetConfig() usage at main.cs:108 (the GetConfig().GetBool("Editor","enableEditorMode") precedent — the SAME config surface to read the in-client live-enable flag, C-06)
    - .planning/phases/16-.../16-RESEARCH.md Open Q1 (in-client listener gating as defense-in-depth) + §Security Domain V13
  </read_first>
  <action>
    C-01 (CONFIRMED MISSING): wire the listener start so the bridge actually runs in a real injected client. In Startup.EntryPoint, inside the `if (!initialized)` one-time block, AFTER the four `*Callbacks.Initialize()` calls (main.cs:93) and BEFORE `Native.SignalLauncherReady()` / the blocking `Application.Run` (main.cs:108), construct the LivePipeServer and call StartListener(). StartListener spawns a BACKGROUND thread, so placing it before Application.Run is correct (non-blocking).
    CLIENT-ROOT PINNING (CUR-NEW-1): do NOT pass `injectRoot` (main.cs:62). Resolve the SWG CLIENT root the SAME way the editor save path does (mirror FormIffEditor.ResolveClientRoot — grep it, R3-8: (1) Path.GetDirectoryName(Process.GetCurrentProcess().MainModule.FileName); (2) UtinniCore.Utility.utility.GetWorkingDirectory(); (3) the [TreBrowser] clientDir ini fallback) and pass THAT as the server's containment root, so the in-client containment root matches the MCP `--root` semantics. (Factor a tiny client-root resolver or inline the three-step chain in main.cs.)
    GAME-STATE CACHE WIRING (R3-2 — the HIGH fix's wiring half): inject the production gameStateProbe (a `() => Game.IsRunning` delegate) into the LivePipeServer ctor BUT wire the actual refresh to the GAME THREAD: register a recurring game-thread refresh that calls `server.RefreshGameStateOnGameThread()` via GameCallbacks (e.g. AddMainLoopCall re-enqueuing itself each tick, or the existing per-frame drain hook). The probe is therefore invoked ONLY on the game thread; the pipe worker reads the cached snapshot. Add a code comment stating the native Game.IsRunning read is confined to the game-thread refresh (R3-2).
    C-06 (defense-in-depth — in-client start gate): gate the StartListener call on its OWN in-client flag, read from the existing config surface (`utinni.GetConfig().GetBool("Live", "enableLiveBridge")`, mirroring enableEditorMode at :108). Default OFF (fail-closed). Wrap the construct+start+refresh-wiring in a try/catch that logs and never throws into the init path (the AssemblyResolve/PluginLoader precedent).
    Add a code comment citing C-01 + CUR-NEW-1 + R3-2 + D-04/D-06 explaining: the placement (post-Callbacks so the marshal seam is wired; pre-Application.Run because Run blocks; background thread so non-blocking), the client-root-not-injectRoot pinning, the game-thread-only native game-state read, and the two-flag model.
    Add a pure-managed test (extend LivePipeServerTests.cs from Task 1b): assert StartListener is IDEMPOTENT (a second call does not spawn a second accept loop). (The live in-client confirmation that ping returns listening:true is Tier-4 manual / non-gating per D-01; this task's GATED requirement is that the WIRING exists in main.cs, pins the CLIENT root, wires the game-thread cache refresh, and is code-asserted.)
  </action>
  <verify>
    <automated>cd D:/Code/Utinni && dotnet test UtinniCoreDotNet.Tests --filter LivePipeServer</automated>
  </verify>
  <acceptance_criteria>
    - The verify is a full `dotnet test` (no --no-build) since this task edits main.cs + a test file (R3-3-Codex)
    - main.cs Startup.EntryPoint constructs a LivePipeServer and calls `StartListener()` within the `if (!initialized)` block, positioned AFTER the *Callbacks.Initialize() block and BEFORE Application.Run (source-assertion: `StartListener` appears in main.cs between the GameCallbacks.Initialize and Application.Run lines)
    - The server's containment root is resolved via the editor-save-path client-root chain (MainModule dir → GetWorkingDirectory → [TreBrowser] clientDir), NOT `injectRoot` (CUR-NEW-1 — source-assertion: the StartListener construction does not pass `injectRoot` as the containment root)
    - The game-state cache refresh is wired to a game-thread callback (GameCallbacks) so the probe/native Game.IsRunning read runs ONLY on the game thread (R3-2 — source-assertion: RefreshGameStateOnGameThread is invoked from a GameCallbacks/AddMainLoopCall context, not the accept loop; a code comment documents the game-thread-only rule)
    - The StartListener call is gated on an in-client enable flag (`[Live] enableLiveBridge`, default OFF — C-06) AND wrapped in a try/catch that logs and does not throw into the injection init path
    - A code comment documents the two-flag operator contract (CUR-NEW-6), the client-root-not-injectRoot rationale (CUR-NEW-1), and the game-thread-only game-state read (R3-2)
    - LivePipeServerTests asserts StartListener idempotency
    - `dotnet test UtinniCoreDotNet.Tests --filter LivePipeServer` exits 0; full UtinniCoreDotNet.Tests builds net472 with no regression
    - NOTE recorded in the SUMMARY: live in-client ping=listening:true is Tier-4 manual / non-gating (D-01); the GATED deliverable here is the wired + client-root-pinned + game-thread-refresh-wired + idempotent listener start, code-asserted
  </acceptance_criteria>
  <done>StartListener is wired into main.cs (post-Callbacks, pre-Application.Run, background thread), pinned to the resolved SWG CLIENT root (NOT injectRoot — CUR-NEW-1), with the game-state cache refresh wired to a game-thread callback so the native read is game-thread-confined (R3-2), gated on an in-client flag (C-06), failure-isolated, and idempotency-tested.</done>
</task>

</tasks>

<threat_model>
**Gate verdict: ASVS L1; no HIGH-severity threats; security gate passes.** (All threats below are mitigate or accept; none rated HIGH. Block-on-HIGH posture: had any threat been HIGH it would gate the wave — none are.)

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local process → named pipe (in-client server) | Any same-user local process that knows the pipe name can connect; the server runs inside the injected x86 client. Both the current-user ACL AND the in-client enable flag (C-06) gate exposure; the reload path is contained against the SWG CLIENT root (C-04/CUR-NEW-1). |
| pipe worker thread → game thread | Reload work crosses from the background worker onto the game thread only via the marshal queue; native game state (Game.IsRunning, P/Invoke) is read ONLY on the game thread (via RefreshGameStateOnGameThread); the worker reads a cached snapshot, never the native API (C-09/CDX-NEW-2/R3-2, Pitfall 2). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | Spoofing/Elevation | Unauthorized local process connects to the pipe and triggers reloads | mitigate | PipeSecurity with a single PipeAccessRule for `WindowsIdentity.GetCurrent().User` + FullControl/Allow, deny-others-by-omission (CDX-NEW-4, exact construction pinned); NamedPipeServerStream is local-machine only; AND the listener only starts when the in-client enable flag is ON (C-06 defense-in-depth — fail-closed default OFF). Acceptance: ACL-construction assert (Task 1b) + Task-3 in-client-flag gate. |
| T-16-11 | Tampering (integrity) | Reload work executed on the wrong thread / native game-state deref off the game thread → client AV/crash | mitigate | All reload work marshals via GameCallbacks.AddMainLoopCall; game-state is a GAME-THREAD-UPDATED cached scalar (volatile/Interlocked) refreshed only on the game thread via RefreshGameStateOnGameThread; the worker/accept loop reads ONLY the cache and contains no Game.IsRunning reference and never invokes the probe Func (R3-2 — acceptance asserts both the source-absence AND the behavioral game-thread-only invocation); the worker never dereferences native game state (Pitfall 2). |
| T-16-12 | DoS | Malformed/oversize/partial/slow wire frame on the in-client server thread | mitigate | Bounded MaxFrameBytes + op allow-list + EXPLICIT server-side connect/read timeouts (CDX-NEW-7) so a stalled/slow/oversize-declared client is closed within a bounded window without starving the accept loop; a bad/partial frame closes that connection only, the loop recovers. Acceptance: Task-1b oversize + server-side partial-read/mid-disconnect + stalled-client-closed + malformed-then-valid-sequential tests. |
| T-16-13 | Spoofing | Pipe name collision / squatting | accept | Sufficiently-specific pipe name; server owns creation. Single-user local desktop trust assumption (analog AR-14-03/04). |
| T-16-14 | Tampering | In-client reload-asset targets an arbitrary absolute / out-of-root path (same-user process bypassing the MCP-side root) | mitigate | The wire carries a RELATIVE path; the server resolves it via the SHARED LooseOverridePath.Resolve against the pinned SWG CLIENT root (CUR-NEW-1 — the SAME root the editor save path + MCP --root use, NOT injectRoot; reuses the battle-tested helper) and REJECTS absolute / `..`-escaping paths (accepted=false, no enqueue). The live_ping ack reports the resolved clientRoot diagnostic (R3-3) so a --root mismatch is operator-visible. Acceptance: Task-1b client-root containment + cross-root + clientRoot-diagnostic tests. |
| T-16-15 | Tampering (protocol drift) | net10/net472 wire shapes silently diverge (framing, JSON casing, tier encoding, field names, protocol version) → a green net472-on-net472 test hides a broken net10 path | mitigate | Wire JSON OUTPUT is produced by a Utinni-owned CANONICAL writer (CanonicalJson) with fixed key order, camelCase, no whitespace, pinned-string ReloadTier, invariant numbers (R3-1) — byte-exactness is deterministic by construction, NOT serializer reflection order. ALL FOUR golden wire byte-vectors (ping/reload request+ack, incl. nested acks — no key-order-independent fallback, R3-7) are asserted byte-exact on the net472 side (produce+consume) and the net10 side (16-03) re-implements CanonicalJson field-for-field and asserts the SAME goldens; a protocolVersion-skew request returns a structured reject ack (CUR-NEW-5). Acceptance: Task-1a golden-vector produce+consume + order-stability + camelCase + string-tier tests; Task-1b skew test. |
| T-16-SC | Tampering | npm/pip/cargo installs | n/a | No new packages this phase — net472 OUTPUT uses the hand-rolled CanonicalJson (BCL-only — C-03); INPUT may use JavaScriptSerializer (System.Web.Extensions, ALREADY referenced); BCL System.IO.Pipes only; Package Legitimacy Gate not triggered (no System.Text.Json package added). |
</threat_model>

<verification>
- `dotnet test UtinniCoreDotNet.Tests --filter LiveBridgeProtocol` green (CanonicalJson order-stability; four golden wire byte-vectors produce+consume byte-exact incl. nested acks; camelCase + string-tier; clientRoot diagnostic; framing round-trip + oversize reject; pipe-name + framing fixture-equality; skew-check helper).
- `dotnet test UtinniCoreDotNet.Tests --filter LivePipeServer` green (decision→tier→enqueue with cached game state; cached-game-state semantics source+behavioral; client-root containment incl. cross-root; clientRoot diagnostic; protocolVersion skew; bounded lifecycle incl. idempotent StartListener + dispose-within-N-ms + stalled-client-closed + sequential malformed-then-valid; server-side partial-read; oversize rejection; ACL construction; StartListener idempotency for the main.cs wiring).
- `dotnet test Utinni.Mcp.Tests --filter ServerArgs` green (EnableLive fail-closed default + dual-flag doc).
- Full UtinniCoreDotNet.Tests + Utinni.Mcp.Tests lanes build with no regression.
- Source assertion: `StartListener` is present in main.cs between the GameCallbacks.Initialize and Application.Run lines (C-01 wiring), pinned to the resolved CLIENT root (not injectRoot — CUR-NEW-1), with the game-state cache refresh wired to a game-thread callback (R3-2).
</verification>

<success_criteria>
- The in-client pipe server reuses the shipped marshal seam (mainLoopCallQueue) and ReloadAssetClassifier; no per-frame heap allocation introduced (project_rh_snapshot_no_heap_alloc honored).
- The listener is actually WIRED into main.cs (C-01), pinned to the SWG CLIENT root (CUR-NEW-1), with the game-state cache refresh on the game thread (R3-2), in-client-flag gated (C-06), and idempotency-tested.
- The in-client reload path is contained against the client root via the shared LooseOverridePath (C-04/CUR-NEW-1/CUR-NEW-7); the server lifecycle is specified with bounded timeouts + a proven bounded dispose (C-07/C-08/CDX-NEW-7/8); game-state is a game-thread-updated cached scalar the worker reads but never natively probes (R3-2).
- The ack reports the honest reload tier (string-encoded via CanonicalJson — CUR-NEW-9) with reloadAttempted=false candor; the ping ack reports a clientRoot diagnostic (R3-3); visible re-render is NOT gated (D-01).
- The wire JSON is produced by the Utinni-owned canonical writer (R3-1) so the four golden wire byte-vectors are byte-exact by construction on the net472 side (produce+consume, all four incl. nested acks — R3-7); the pipe-name (line 1) + framing (line 2) fixture asserts byte-exact (C-05); protocolVersion skew is handled (CUR-NEW-5); net472 OUTPUT is hand-rolled, no new package (C-03).
- EnableLive defaults closed; the dual-flag operator contract is documented (CUR-NEW-6); the Wave-2 host gates tool registration on it.
</success_criteria>

<output>
Create `.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-02-SUMMARY.md` when done.
</output>
