---
phase: 16-live-injected-mcp-bridge-blender-ecosystem-boundary
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - UtinniCoreDotNet/Live/LiveBridgeProtocol.cs
  - UtinniCoreDotNet/Live/LivePipeServer.cs
  - UtinniCoreDotNet/main.cs
  - UtinniCoreDotNet/UtinniCoreDotNet.csproj
  - UtinniCoreDotNet.Tests/Live/LivePipeServerTests.cs
  - UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj
  - Utinni.Cli.Tests/Fixtures/live/pipe-name.txt
  - Utinni.Cli.Tests/Fixtures/live/wire-ping-request.json
  - Utinni.Cli.Tests/Fixtures/live/wire-ping-ack.json
  - Utinni.Cli.Tests/Fixtures/live/wire-reload-request.json
  - Utinni.Cli.Tests/Fixtures/live/wire-reload-ack.json
  - Utinni.Mcp/Server/ServerArgs.cs
  - Utinni.Mcp.Tests/ServerArgsTests.cs
autonomous: true
requirements: [MCP-03]
user_setup:
  - service: "Live bridge (two-flag operator contract — CUR-NEW-6)"
    why: "The live tier requires BOTH flags ON; neither alone is sufficient. This is operator config, not a third-party account — recorded here so it is operator-discoverable, not in code only."
    env_vars: []
    dashboard_config:
      - task: "Enable the in-client listener: set the in-client config flag [Live] enableLiveBridge=true (default OFF/fail-closed) so LivePipeServer.StartListener actually runs inside the injected client (16-02 Task 3)."
        location: "Utinni.ini / the in-client config surface (utinni.GetConfig().GetBool(\"Live\",\"enableLiveBridge\"))"
      - task: "Enable the MCP tool tier: launch Utinni.Mcp with --enable-live (default OFF/fail-closed) so the live_* tools are advertised + the LivePipeClient is registered (16-03)."
        location: "Utinni.Mcp launch args (or UTINNI_MCP_ENABLE_LIVE=1)"
      - task: "BOTH are required: --enable-live alone advertises the tools but the listener never started → live_ping returns listening:false; the in-client flag alone starts the listener but the agent never sees the tools. Neither alone produces a working live path."
        location: "documented in MCP-SECURITY.md live-tier section (16-03 Task 3) + 16-VALIDATION.md Tier-4 checklist"

must_haves:
  truths:
    - "The in-client LivePipeServer parses a request envelope, classifies a reload-asset request via ReloadAssetClassifier, and enqueues exactly one Action onto the game-thread queue — never touching native game state off the game thread"
    - "When gameIsRunning is injected false, the server's reload disposition is the honest Unavailable tier (no enqueue side-effect required)"
    - "gameIsRunning is captured via an injected scalar/Func, NEVER a direct native Game.IsRunning read on the background worker / accept loop (Game.IsRunning is native-backed P/Invoke — Pitfall 2 / CDX-NEW-2)"
    - "LivePipeServer.StartListener() is WIRED into Startup.EntryPoint (main.cs), invoked AFTER the *Callbacks.Initialize() block and BEFORE the blocking Application.Run, gated on an in-client enable flag (defense-in-depth, C-06); StartListener is idempotent (no double-spawned accept loop)"
    - "The in-client reload path is root-contained against the SWG CLIENT ROOT (same semantics as the editor save path / MCP --root — NOT injectRoot/the Utinni DLL dir, CUR-NEW-1): the server resolves the received relative path against the pinned client root via the shipped LooseOverridePath.Resolve and REJECTS absolute / escaping paths (C-04)"
    - "The named-pipe server lifecycle is specified and tested with EXPLICIT bounded timeouts (CDX-NEW-7/8): cancel/dispose unblocks a blocking WaitForConnection within a bounded window; one request per connection; server-side connect/read timeouts close a stalled/slow/oversize-declared connection without starving the accept loop; the loop survives a malformed/partial frame and accepts the next; restart-after-disconnect; accept-loop-throw recovery"
    - "ServerArgs.Parse recognizes --enable-live and exposes EnableLive; absence leaves EnableLive false (fail-closed default)"
    - "The canonical pipe name + framing descriptor live in one committed fixture AND there are committed GOLDEN WIRE BYTE-VECTOR fixtures (ping request/ack, reload request/ack) that the net472 server's JavaScriptSerializer produces/consumes byte-exact (CDX-NEW-1=CUR-NEW-2); the net472 envelope DTOs emit camelCase on the wire (CUR-NEW-4)"
    - "protocolVersion mismatch is handled: the server returns a structured reject ack (or hard error envelope) when a request's protocolVersion != LiveBridgeProtocol.ProtocolVersion, with a skew test (CUR-NEW-5)"
    - "The ReloadTier wire encoding is pinned EXPLICITLY (string names, not default-integer) and both serializers emit it that way; it appears in the golden byte-vectors (CUR-NEW-9)"
    - "The net472 server JSON (de)serialization uses JavaScriptSerializer (System.Web.Extensions, already referenced — genuinely in-box on net472); NO System.Text.Json package is added (C-03 honesty)"
  artifacts:
    - path: "UtinniCoreDotNet/Live/LivePipeServer.cs"
      provides: "Managed net472 named-pipe server inside the injected host; background-thread accept loop + bounded lifecycle + game-thread marshal + client-root containment"
      contains: "NamedPipeServerStream"
    - path: "UtinniCoreDotNet/Live/LiveBridgeProtocol.cs"
      provides: "Shared pipe name constant + protocolVersion + camelCase envelope (JavaScriptSerializer) + string-encoded ReloadTier + framing (4-byte LE length prefix + UTF-8 JSON) + bounded max size"
      contains: "PipeName"
    - path: "UtinniCoreDotNet/main.cs"
      provides: "StartListener wiring into Startup.EntryPoint (post-Callbacks.Initialize, pre-Application.Run), in-client-flag gated, pinned to the resolved CLIENT root"
      contains: "StartListener"
    - path: "Utinni.Cli.Tests/Fixtures/live/pipe-name.txt"
      provides: "Single canonical pipe-name + framing-descriptor fixture both pipe ends assert against (line 1 name, line 2 framing+json-policy+tier-encoding)"
      contains: "utinni-live-bridge"
    - path: "Utinni.Cli.Tests/Fixtures/live/wire-reload-ack.json"
      provides: "Golden wire byte-vector for the reload ack (camelCase, string-encoded tier, reloadAttempted=false) — the net472 server asserts byte-exact produce/consume; net10 asserts the same bytes in 16-03 (cross-TFM wire proof CDX-NEW-1)"
    - path: "Utinni.Mcp/Server/ServerArgs.cs"
      provides: "EnableLive flag parse (fail-closed default false)"
      contains: "EnableLive"
  key_links:
    - from: "UtinniCoreDotNet/Live/LivePipeServer.cs"
      to: "GameCallbacks.AddMainLoopCall"
      via: "enqueue one Action for game-thread reload"
      pattern: "AddMainLoopCall"
    - from: "UtinniCoreDotNet/Live/LivePipeServer.cs"
      to: "ReloadAssetClassifier.Classify"
      via: "ack tier derivation"
      pattern: "ReloadAssetClassifier\\.Classify"
    - from: "UtinniCoreDotNet/Live/LivePipeServer.cs"
      to: "LooseOverridePath.Resolve (against the pinned CLIENT root)"
      via: "server-side reload-path containment (CUR-NEW-1: client root, not injectRoot)"
      pattern: "LooseOverridePath\\.Resolve"
    - from: "UtinniCoreDotNet/main.cs"
      to: "LivePipeServer.StartListener"
      via: "Startup.EntryPoint wiring (post-Callbacks.Initialize, pre-Application.Run), in-client-flag gated, client-root-pinned"
      pattern: "StartListener"
    - from: "UtinniCoreDotNet.Tests/Live/LivePipeServerTests.cs"
      to: "Utinni.Cli.Tests/Fixtures/live/{pipe-name.txt, wire-*.json}"
      via: "fixture-equality on PipeName+framing (line 1/2) AND byte-exact golden wire-vector produce/consume on the net472 side"
      pattern: "wire-reload-ack\\.json"
---

<objective>
Build the in-client (injected-host) half of the MCP-03 live bridge plus the `--enable-live` flag plumbing. The named-pipe SERVER lives in managed net472 `UtinniCoreDotNet` (the CLR is already hosted in-proc per clr.cpp — no C++, no cross-arch marshaling). It parses a request envelope on a background thread, derives the honest ack tier via the shipped `ReloadAssetClassifier`, contains the received path under the pinned SWG CLIENT root, and marshals reload work onto the shipped `GameCallbacks.mainLoopCallQueue` (heap-free hot path, honoring project_rh_snapshot_no_heap_alloc). This plan also WIRES the listener start into `Startup.EntryPoint` (main.cs) so the bridge actually runs in a real injected client, and extends the tested `ServerArgs` parser with `EnableLive` (fail-closed default) so the Wave-2 host plan can gate registration.

This plan also commits the **single canonical pipe-name/framing fixture** (`Utinni.Cli.Tests/Fixtures/live/pipe-name.txt`) that BOTH pipe ends are asserted against — BOTH line 1 (pipe name) and line 2 (framing descriptor incl. json-policy + tier-encoding) — AND a set of **golden wire byte-vector fixtures** (`wire-{ping,reload}-{request,ack}.json`) that the net472 server produces/consumes byte-exact. Because net10 `Utinni.Mcp` cannot project-ref net472 `UtinniCoreDotNet`, the Wave-2 host (16-03) must duplicate the pipe-name literal AND the framing; the fixture + golden byte-vectors are the agreement-enforcing anchors that make the duplicated literals provably byte-identical AND prove the actual JSON bytes (camelCase, string-encoded tier, protocolVersion) match across the TFM wall — closing the gap where a fully-green net472-on-net472 test could hide a net10 casing/encoding mismatch (CDX-NEW-1=CUR-NEW-2/CUR-NEW-4/CUR-NEW-9).

Purpose: Land the hardest new mechanism (pipe server + bounded lifecycle + game-thread marshal + client-root path containment + ack candor + protocolVersion handling + the main.cs wiring that makes it real) behind pure-managed unit tests, and define the shared protocol/pipe-name/framing/golden-wire constants the Wave-2 client consumes.
Output: LiveBridgeProtocol (shared constant + protocolVersion + camelCase JavaScriptSerializer envelope + string-encoded tier + framing), LivePipeServer (in-client, with bounded lifecycle + client-root containment + Func-injected gameIsRunning + protocolVersion skew handling), the main.cs StartListener wiring (client-root-pinned), the canonical pipe-name/framing fixture + golden wire byte-vectors, pure-managed server tests, ServerArgs.EnableLive + extended ServerArgsTests.

REVIEW-DRIVEN EDITS (from 16-REVIEWS.md — orchestrator-VERIFIED facts in bold): **C-01** (StartListener WIRED into main.cs — confirmed missing; Task 3), **C-03** (net472 JSON = JavaScriptSerializer, NOT System.Text.Json — confirmed no STJ ref; package-gate honesty), C-04 (server-side path containment), C-05 (framing line-2 enforcement), C-06 (in-client start gate defense-in-depth), C-07/C-08 (lifecycle + partial-read tests), C-09 (gameIsRunning capture boundary, native-backed), C-12 (protocolVersion envelope field), C-14 (reload ack candor: reloadAttempted=false), Codex (document supported reload asset classes).

REVIEW-DRIVEN EDITS (Round 2, from 16-REVIEWS.md): **CDX-NEW-1 = CUR-NEW-2 (HIGH)** — committed GOLDEN WIRE BYTE-VECTOR fixtures (ping/reload request+ack) that the net472 server produces/consumes byte-exact (Task 1), asserted byte-exact in 16-03 by the net10 side too — closing the cross-TFM wire gap. **CUR-NEW-1 (HIGH)** — the in-client containment root is the SWG CLIENT ROOT (same semantics as the editor save path's ResolveClientRoot: Process.MainModule dir → GetWorkingDirectory → [TreBrowser] clientDir ini) and the MCP `--root`, NOT `injectRoot` (the Utinni DLL dir at main.cs:62); the wire carries a RELATIVE path the server resolves under its OWN pinned client root via LooseOverridePath.Resolve (Tasks 1/3). **CUR-NEW-4** — the net472 envelope DTOs emit camelCase on the wire (JavaScriptSerializer with camelCase C# property names / a custom converter — stated explicitly), proven by the golden byte-vectors (Task 1). **CUR-NEW-5** — protocolVersion mismatch returns a structured reject ack, with a skew test (Task 1). **CUR-NEW-6** — the dual-flag operator contract is documented in user_setup + the contract/security doc + the Tier-4 checklist. **CUR-NEW-9** — ReloadTier wire encoding pinned to STRING names, configured on the serializer, included in the golden vectors (Task 1). **CDX-NEW-7/CDX-NEW-8** — explicit bounded server read/connect timeouts + a proven bounded Stop/Dispose that unblocks WaitForConnection (use the async overload or pipe-close-to-unblock; TEST dispose returns within N ms) (Task 1). **CDX-NEW-2 (HIGH, Codex)** — gameIsRunning is captured via an injected scalar/Func, NEVER a direct native read on any non-game thread; the acceptance criterion asserts the worker/accept-loop body contains no Game.IsRunning reference (Tasks 1/3). **CDX-NEW-4** — the PipeSecurity/PipeAccessRule construction is pinned exactly (current-user SID via WindowsIdentity.GetCurrent().User, FullControl, deny-others-by-omission) (Task 1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-CONTEXT.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-RESEARCH.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-PATTERNS.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-VALIDATION.md

<interfaces>
<!-- Shipped seams the server reuses (do NOT rebuild). Extracted from codebase. -->

From UtinniCoreDotNet/Callbacks/GameCallbacks.cs (:63, :254-257):
  private static readonly ConcurrentQueue<Action> mainLoopCallQueue = new ConcurrentQueue<Action>();
  public static void AddMainLoopCall(Action call)   // enqueue; drained on game thread each frame

From UtinniCoreDotNet/Saving/ReloadAssetClassifier.cs (:124, :78-80):
  public static ReloadTier Classify(string extension, string rootTypeIdOrNull)
  // → ReloadedTextures | ReloadedTerrain | PendingNextSceneChange | Unavailable
  // !Game.IsRunning maps to Unavailable upstream (the DISPATCHER does this; the helper is game-state-free)

From UtinniCoreDotNet.PathContainment/LooseOverridePath.cs (:81-175) — the SHARED containment helper (reuse, do NOT reimplement):
  public static string Resolve(string resolvedRoot, string relAssetPath)
  // canonicalizes root; rejects rooted relAssetPath; rejects any '..' segment; trailing-separator
  // OrdinalIgnoreCase StartsWith prefix-attack guard; returns the contained absolute path or throws
  // ArgumentException on escape. netstandard2.0 lib, referenced by net472 UtinniCoreDotNet.

From UtinniCoreDotNet/Editing/LivePatchValidator.cs (:135-147) — purity precedent:
  public static LivePatchValidation Validate(IntPtr targetAddr, int originalMappedLength,
      int rewrittenLength, bool gameIsRunning)   // game state injected as a scalar for unit-testability

From UtinniCoreDotNet/Generated/UtinniCore.cs (:11143) — game-state read (NATIVE-BACKED, C-09/CDX-NEW-2):
  public static bool Game.IsRunning { get; }   // P/Invoke ?isRunning@Game@utinni@@SA_NXZ — DO NOT call off the game thread

From the EDITOR SAVE PATH client-root semantics (UtinniPlugins FormIffEditor.cs:1596-1630 ResolveClientRoot — the root the loose-override save composes under; CUR-NEW-1 source-of-truth):
  (1) Path.GetDirectoryName(Process.GetCurrentProcess().MainModule.FileName)  // = SWGEmu.exe dir under injection (the SWG CLIENT ROOT)
  (2) UtinniCore.Utility.utility.GetWorkingDirectory()
  (3) ini.GetString("TreBrowser","clientDir")
  // This is the SWG client root the MCP --root ALSO points at — NOT injectRoot (the Utinni DLL dir).

From UtinniCoreDotNet/main.cs (Startup.EntryPoint, :43-114) — the wiring site (C-01):
  string injectRoot = Path.GetDirectoryName(Assembly.GetExecutingAssembly().Location);  // :62 = the Utinni DLL dir — NOT the client root (CUR-NEW-1)
  if (!initialized) { initialized = true; ... AssemblyResolve handler (15-12) ...
    GameCallbacks.Initialize(); GroundSceneCallbacks/ObjectCallbacks/CuiCallbacks.Initialize();  // :90-93
    Native.SignalLauncherReady();  // :106
    if (...GetConfig().GetBool("Editor","enableEditorMode")) Application.Run(new FormMain(pluginLoader));  // :108-110 — BLOCKS for editor lifetime
  }
  // StartListener must be invoked AFTER :93 and BEFORE :108 (background thread, non-blocking),
  // pinned to the resolved CLIENT root (mirror ResolveClientRoot), NOT injectRoot.

From UtinniCoreDotNet/UtinniCoreDotNet.csproj (:60):
  <Reference Include="System.Web.Extensions" />   // JavaScriptSerializer lives here — ALREADY referenced (C-03)
  // (There is NO System.Text.Json reference; adding STJ would pull a NuGet package + Package Legitimacy Gate.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — shared protocol (camelCase JavaScriptSerializer envelope + string-encoded tier + framing) + canonical pipe-name/framing fixture + GOLDEN WIRE BYTE-VECTORS + pure-managed LivePipeServer (bounded accept loop + bounded dispose + client-root containment + Func-injected gameIsRunning + protocolVersion skew) + tests</name>
  <files>UtinniCoreDotNet/Live/LiveBridgeProtocol.cs, UtinniCoreDotNet/Live/LivePipeServer.cs, UtinniCoreDotNet/UtinniCoreDotNet.csproj, UtinniCoreDotNet.Tests/Live/LivePipeServerTests.cs, UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj, Utinni.Cli.Tests/Fixtures/live/pipe-name.txt, Utinni.Cli.Tests/Fixtures/live/wire-ping-request.json, Utinni.Cli.Tests/Fixtures/live/wire-ping-ack.json, Utinni.Cli.Tests/Fixtures/live/wire-reload-request.json, Utinni.Cli.Tests/Fixtures/live/wire-reload-ack.json</files>
  <read_first>
    - UtinniCoreDotNet/Saving/ReloadAssetClassifier.cs (Classify signature + the 4 tiers; the helper is GAME-STATE-FREE — !Game.IsRunning→Unavailable is the DISPATCHER's job; reuse Classify for the tier — PATTERNS lines 144-149)
    - UtinniCoreDotNet/Callbacks/GameCallbacks.cs (mainLoopCallQueue + AddMainLoopCall — the marshal seam to reuse, NOT rebuild; PATTERNS lines 136-141)
    - UtinniCoreDotNet/Editing/LivePatchValidator.cs (the gameIsRunning-injected-as-scalar purity pattern; lines 135-147)
    - UtinniCoreDotNet/Formats/ObjectTemplate/ObjectTemplateSchemaLoader.cs (:81 — the EXISTING JavaScriptSerializer usage in this assembly; the in-box net472 JSON precedent for C-03. NOTE: default JavaScriptSerializer emits PascalCase — see CUR-NEW-4 below for the camelCase mechanism)
    - UtinniCoreDotNet.PathContainment/LooseOverridePath.cs (:81-175 — the SHARED containment helper to call directly for the server-side reload-path containment against the CLIENT root; reuse, do not reimplement — CUR-NEW-1/CUR-NEW-7)
    - UtinniPlugins "The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs":1596-1630 (ResolveClientRoot — the EXACT client-root resolution chain the server-side containment root must mirror: MainModule dir → GetWorkingDirectory → [TreBrowser] clientDir; CUR-NEW-1)
    - UtinniCoreDotNet.Tests/Editing/LivePatchValidatorTests.cs (the injected-state pure-fn test shape to clone)
    - Utinni.Cli.Tests/Utinni.Cli.Tests.csproj (the `<Content Include="Fixtures\**\*" CopyToOutputDirectory="PreserveNewest" />` pattern — the fixtures live under this project's Fixtures tree and are auto-copied to its output; the net472 test project links them)
    - .planning/phases/16-.../16-RESEARCH.md §A2/A3/A4 (threading model, wire format, ack reuse) + §Security Domain V13 (PipeSecurity current-user ACL) + Pitfall 2 (worker thread never touches game state)
  </read_first>
  <action>
    Commit the CANONICAL pipe-name/framing fixture FIRST: create Utinni.Cli.Tests/Fixtures/live/pipe-name.txt as the single source of truth for the cross-plan agreement. It is two lines: line 1 = the canonical pipe name `utinni-live-bridge` (verbatim, no surrounding whitespace); line 2 = the framing descriptor `frame=len32le+utf8json;maxframe=65536;protocol=1;json=camelCase;tier=stringnames` naming the 4-byte little-endian length-prefix + UTF-8 JSON framing, the bounded max frame size (bytes), the protocol version, the JSON field-naming policy (camelCase), AND the ReloadTier wire encoding (stringnames — CUR-NEW-9). Line 2 is a PROTOCOL descriptor (C-05): both ends parse it and assert their implemented constants match (length-prefix width/endianness, encoding, max-frame size, protocol version, field-naming, tier-encoding) — it is not documentation-only. Write the canonical values verbatim — do NOT paraphrase or change casing/separators.

    Commit the GOLDEN WIRE BYTE-VECTOR fixtures (CDX-NEW-1=CUR-NEW-2): create four files under Utinni.Cli.Tests/Fixtures/live/ holding the EXACT JSON body bytes (UTF-8, no BOM, no framing prefix — the JSON payload only, so both TFMs can assert it independent of framing) for the canonical messages:
      - wire-ping-request.json: the ping request envelope `{ protocolVersion, op }` (op="ping"), camelCase keys.
      - wire-ping-ack.json: the ping ack `{ schemaVersion, protocolVersion, op, result:{ listening, gameRunning, pid } }` — use STABLE placeholder values for the variable fields (e.g. pid:0, gameRunning:true, listening:true) so the byte-vector is deterministic; the test compares structure+casing+key-order-independent equality but ALSO asserts a byte-exact serialization for the FIXED-shape fields (see test note below).
      - wire-reload-request.json: the reload request `{ protocolVersion, op, path, ext }` (op="reload-asset"), where `path` is a RELATIVE asset path (CUR-NEW-1 — the wire carries a relative path the server resolves under its own client root; an optional `resolvedHint` debug field MAY be present but is not load-bearing).
      - wire-reload-ack.json: the reload ack `{ schemaVersion, protocolVersion, op, result:{ accepted, tier, queued, reloadAttempted, path, note } }` where `tier` is a STRING name (e.g. "PendingNextSceneChange" — CUR-NEW-9, NOT an integer), `reloadAttempted` is false (C-14), and `note` carries the transport-only candor string.
    These golden bytes are the contract both serializers must produce/consume. (Generate them once by serializing the DTOs with the configured camelCase + string-tier JavaScriptSerializer, then commit the output verbatim; the test re-serializes and asserts byte-exact against the committed file so a casing/encoding/tier-format drift fails.)

    Create LiveBridgeProtocol.cs in UtinniCoreDotNet/Live/: a shared static class defining:
      - `public const string PipeName = "utinni-live-bridge";` (byte-identical to fixture line 1).
      - `public const int ProtocolVersion = 1;` and `public const int MaxFrameBytes = 65536;` (byte-identical to fixture line 2's protocol/maxframe tokens). Add a public framing-descriptor const string that equals fixture line 2 so the test can byte-compare it.
      - the request/ack envelope DTOs (plain classes with public properties; serialized via JavaScriptSerializer) and the framing helpers. Frame = 4-byte little-endian length prefix + UTF-8 JSON body (RESEARCH A3). Request: `{ protocolVersion, op }` where op="ping" or op="reload-asset" with `{ path, ext }` (path RELATIVE — CUR-NEW-1). Ack mirrors the CLI sorted-key shape `{ schemaVersion, protocolVersion, op, result }` — ping result `{ listening, gameRunning, pid }`; reload-asset result `{ accepted, tier, queued, reloadAttempted, path, note }`.
      - CAMELCASE ON THE WIRE (CUR-NEW-4): default JavaScriptSerializer emits the C# property NAME verbatim (PascalCase). To emit camelCase on the wire, give the DTO public properties camelCase identifiers (e.g. `public int protocolVersion;`/`public string op;`) OR register a JavaScriptConverter that lowercases the first char — STATE the concrete mechanism chosen in a code comment (do NOT assume default casing matches). The golden byte-vectors prove the actual emitted casing.
      - STRING-ENCODED ReloadTier (CUR-NEW-9): serialize/deserialize the tier as its enum NAME string (e.g. "PendingNextSceneChange"), NOT the default integer. Configure this explicitly (a JavaScriptConverter or a string-typed `tier` DTO property set from `ReloadTier.ToString()` and parsed via Enum.Parse on read). The golden reload-ack vector carries the string form.
      - PROTOCOLVERSION + MaxFrameBytes enforcement: reject oversize frames on read; expose a helper that a request whose `protocolVersion != ProtocolVersion` routes to a structured reject (used by the server, CUR-NEW-5).
    C-03 (CONFIRMED): use **JavaScriptSerializer** (System.Web.Extensions — ALREADY referenced in UtinniCoreDotNet.csproj :60). Do NOT use System.Text.Json. Keep this class dependency-light (BCL + System.Web.Extensions). Add a code comment on PipeName + the framing const naming the fixture files (pipe-name.txt + wire-*.json) as the canonical agreement anchors that 16-03's net10 duplicate also asserts against.

    Create LivePipeServer.cs in UtinniCoreDotNet/Live/:
      - LIFECYCLE WITH BOUNDED TIMEOUTS (C-07 / CDX-NEW-7 / CDX-NEW-8): a background-thread NamedPipeServerStream accept/read loop with an explicit StartListener()/Stop()/Dispose() surface. StartListener is IDEMPOTENT (Interlocked guard so a second call does NOT spawn a second accept loop). One request per connection then close that stream and loop to accept the next (sequential single-client model). SERVER-SIDE TIMEOUTS: apply a bounded connect-wait + a bounded per-read timeout so a client that connects then stalls (sends nothing), sends only the length prefix, drips the body slowly, or declares an oversize frame is closed within a bounded window WITHOUT starving the accept loop (use the async pipe APIs with a CancellationTokenSource(readTimeout), or a guard timer that closes the stream). A malformed/partial/oversize frame closes ONLY that connection; the loop recovers and continues. An unexpected exception on one connection is caught + logged; the listener is never torn down. BOUNDED DISPOSE: Stop/Dispose must unblock a blocking WaitForConnection within a bounded time — the synchronous WaitForConnection may not honor a CTS, so use the ASYNC WaitForConnectionAsync with a linked CTS, OR close/dispose the NamedPipeServerStream to force the wait to throw; whichever is chosen, the worker thread joins within a bounded window. The worker thread NEVER touches native game state (Pitfall 2).
      - SECURITY — EXACT ACL (CDX-NEW-4): construct the NamedPipeServerStream with a PipeSecurity that adds a single PipeAccessRule for the current-user SID — `new SecurityIdentifier(WellKnownSidType...)` is NOT precise enough; use `WindowsIdentity.GetCurrent().User` as the IdentityReference, `PipeAccessRights.FullControl`, `AccessControlType.Allow`. Add NO other rules (deny-others-by-omission). State this construction in a code comment. (A non-current-user-denial test is hard locally — note it Tier-4; the construction is pinned and the rule-count/identity is asserted in-test.)
      - PATH CONTAINMENT AGAINST THE CLIENT ROOT (C-04 / CUR-NEW-1): the server is constructed with a pinned SWG CLIENT root (the SAME root the editor save path resolves — mirror FormIffEditor.ResolveClientRoot: MainModule dir → GetWorkingDirectory → [TreBrowser] clientDir — accept it via ctor for testability; this is NOT injectRoot/the Utinni DLL dir). The wire carries a RELATIVE path; on a reload-asset request the server calls `LooseOverridePath.Resolve(clientRoot, relativePath)` (the shared helper — reuse, do not reimplement, CUR-NEW-7). A rooted/absolute or `..`-escaping path makes LooseOverridePath.Resolve throw ArgumentException → the ack returns accepted=false + a `Rejected` disposition and NO enqueue. (Defense-in-depth: a same-user local process can hit the pipe directly; T-16-21 MCP-side guard does not cover that.)
      - PURE DECISION + Func-INJECTED GAME-STATE (C-09 / CDX-NEW-2): route a parsed request to a PURE decision method that takes the resolved-or-rejected path + a `bool gameIsRunning` SCALAR (LivePatchValidator purity pattern). The decision: protocolVersion mismatch → structured reject ack (CUR-NEW-5); ping → ping result; reload-asset → containment check first; if contained, ReloadAssetClassifier.Classify(ext, null) for the tier; if gameIsRunning → tier as classified + queued=true; if !gameIsRunning → Unavailable + queued=false. The decision returns the ack WITHOUT performing the enqueue (unit-testable). The public Handle/Server is constructed with an injected `Func<bool> getGameIsRunning` (CDX-NEW-2 — the cleanest mechanism: a game-thread-owned cached bool OR a delegate read inside the enqueued game-thread Action; in production this Func reads Game.IsRunning, but it is INVOKED at the marshal boundary, never inside the accept/read loop body). The worker/accept loop body MUST contain NO `Game.IsRunning` reference at all — capture is via the injected scalar/Func only. Add a code comment at the capture site.
      - MARSHAL: on a contained reload-asset with gameIsRunning=true, enqueue exactly one Action via GameCallbacks.AddMainLoopCall.
      - CANDOR (C-14): the enqueued Action body is an INTENTIONAL best-effort placeholder this phase (visible render NON-gating per D-01; rides on RESID-03). Mark it `// TODO(RESID-03): best-effort placeholder per D-01 — wired reload deferred; NOT functional re-render`. The ack ALWAYS sets `reloadAttempted=false` and a `note` like "transport-only until RESID-03; queued != visible reload".
      - Document the supported reload asset classes this phase (Codex): a code comment stating which extensions return a real tier vs PendingNextSceneChange vs Unavailable per ReloadAssetClassifier; note rootType is null this phase → object-template root-type-specific tiers may under-classify to PendingNextSceneChange (the honest minimal-scope disposition).
      - The ack returns IMMEDIATELY (no completion signal — D-01, RESEARCH Open Q2).
    Add the two new files to UtinniCoreDotNet.csproj <Compile Include> entries (old-style csproj).
    In UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj, link the canonical fixture + golden vectors: add `<Content Include="..\Utinni.Cli.Tests\Fixtures\live\pipe-name.txt" Link="Fixtures\live\pipe-name.txt" CopyToOutputDirectory="PreserveNewest" />` and the four `wire-*.json` files (Link form so the SAME committed files are the asserted source).
    Write LivePipeServerTests.cs in UtinniCoreDotNet.Tests/Live/ cloning LivePatchValidatorTests. Cover:
      - envelope-parse→tier→ack with gameIsRunning injected (via the Func) both true and false; reload-asset with gameIsRunning=true + a CONTAINED relative path yields a tier from ReloadAssetClassifier + queued=true + reloadAttempted=false; with false yields Unavailable + queued=false + NO enqueue. Inject the enqueue target (an Action sink) so the test verifies "exactly one Action enqueued" without a live game thread.
      - GOLDEN WIRE BYTE-VECTORS (CDX-NEW-1): serialize the canonical ping request, ping ack (fixed-shape fields), reload request, and reload ack with the configured camelCase + string-tier serializer and assert the produced UTF-8 bytes byte-EXACT match the committed wire-*.json fixtures; AND deserialize each committed fixture back into the DTOs and assert the round-trip (so the server provably produces AND consumes the goldens). This is the net472 half of the cross-TFM proof; 16-03 asserts the SAME files on the net10 side.
      - camelCase + string-tier (CUR-NEW-4/CUR-NEW-9): assert the serialized reload-ack bytes contain `"tier":"PendingNextSceneChange"` (string, not `"tier":2`) and camelCase keys (`"protocolVersion"`, not `"ProtocolVersion"`).
      - protocolVersion skew (CUR-NEW-5): a request with protocolVersion=999 yields a structured reject ack (not a silent partial parse, not a crash).
      - path containment against the CLIENT root (C-04/CUR-NEW-1): construct the server with a temp client-root tree; a reload-asset with a CONTAINED relative path resolves + (gameIsRunning=true) enqueues; a reload-asset with an ABSOLUTE path and one with a `..`-escaping relative path each yield accepted=false + Rejected + NO enqueue (LooseOverridePath.Resolve throws → mapped to Rejected). Add a cross-root case: server root = temp tree A, the relative path that would be valid under A resolves under A (not under a different root) — proving the server uses its OWN pinned root, not a wire-supplied absolute.
      - lifecycle with BOUNDED timeouts (C-07/CDX-NEW-7/CDX-NEW-8): StartListener idempotent (twice → one accept loop); Stop/Dispose unblocks a blocking WaitForConnection and the worker joins within a bounded time (assert dispose returns within N ms); a connected-then-stalled client (connects, sends nothing) is closed within the read-timeout window without blocking a subsequent valid connection; the accept loop survives a malformed-frame connection and accepts a subsequent valid connection on the SAME running server.
      - server-side partial-read / mid-message disconnect (C-08): a connection that writes a length prefix then disconnects mid-body closes only that connection (no hang, no crash, loop continues).
      - oversize-frame rejection (MaxFrameBytes); ping result shape.
      - fixture-equality (line 1 + line 2, C-05): read Fixtures/live/pipe-name.txt; assert line 1 byte-equals LiveBridgeProtocol.PipeName AND line 2 byte-equals the framing-descriptor const (name AND framing/maxframe/protocol/json-policy/tier-encoding pinned).
      - ACL construction (CDX-NEW-4): assert the server's PipeSecurity has exactly one access rule whose IdentityReference resolves to WindowsIdentity.GetCurrent().User with FullControl/Allow.
    Name the class so `--filter LivePipeServer` selects it. (Lifecycle/timeout tests may use a real loopback NamedPipe on a randomized pipe name within the test process — still Tier-2, no live SWG client.)
  </action>
  <verify>
    <automated>cd D:/Code/Utinni && dotnet test UtinniCoreDotNet.Tests --no-build --filter LivePipeServer</automated>
  </verify>
  <acceptance_criteria>
    - Utinni.Cli.Tests/Fixtures/live/pipe-name.txt exists, committed, line 1 is exactly `utinni-live-bridge`, line 2 is `frame=len32le+utf8json;maxframe=65536;protocol=1;json=camelCase;tier=stringnames` (incl. the tier-encoding token — CUR-NEW-9)
    - The four wire-*.json golden byte-vectors exist, committed; the reload-ack vector contains `"tier":"PendingNextSceneChange"` (string) + `"reloadAttempted":false` + camelCase keys; the reload-request carries a RELATIVE `path` (CUR-NEW-1)
    - LivePipeServerTests serializes the DTOs and asserts byte-EXACT equality against each committed wire-*.json, AND deserializes each fixture back (produce + consume — CDX-NEW-1); a test asserts camelCase keys + string-encoded tier (CUR-NEW-4/CUR-NEW-9)
    - LiveBridgeProtocol.cs defines PipeName/ProtocolVersion/MaxFrameBytes, the framing-descriptor const equal to fixture line 2, the camelCase-emitting envelope DTOs (the concrete camelCase mechanism stated in a comment), string-encoded ReloadTier, and 4-byte LE length-prefix framing; JSON via **JavaScriptSerializer** — NO System.Text.Json reference/package (C-03)
    - The reload-asset ack DTO carries `reloadAttempted` (always false) + a transport-only `note` (C-14)
    - LivePipeServer.cs contains `NamedPipeServerStream`, a PipeSecurity rule built from `WindowsIdentity.GetCurrent().User` + FullControl/Allow (deny-others-by-omission, CDX-NEW-4), `GameCallbacks.AddMainLoopCall`, `ReloadAssetClassifier.Classify`, a `LooseOverridePath.Resolve(clientRoot, relativePath)` containment call against the pinned CLIENT root (NOT injectRoot — CUR-NEW-1), idempotent StartListener + bounded Stop/Dispose, and bounded server read/connect timeouts (CDX-NEW-7/8)
    - The reload-decision method takes `bool gameIsRunning` as a scalar; the server is constructed with an injected `Func<bool> getGameIsRunning`; the accept/read loop body contains NO `Game.IsRunning` reference at all (CDX-NEW-2 — source-asserted; a code comment documents the capture-at-boundary rule)
    - A protocolVersion-skew request yields a structured reject ack (CUR-NEW-5)
    - The enqueued Action body carries the `// TODO(RESID-03): ...` marker and remains the single enqueued Action
    - `dotnet test UtinniCoreDotNet.Tests --filter LivePipeServer` exits 0; tests cover gameIsRunning true/false + exactly-one-enqueue, golden wire byte-vectors (produce+consume), camelCase+string-tier, protocolVersion skew, client-root containment (contained relative enqueues; absolute + `..` rejected; cross-root uses-own-root), bounded lifecycle (idempotent StartListener, dispose-returns-within-N-ms, stalled-client-closed, malformed-then-valid sequential, accept-loop recovery), server-side partial-read, oversize rejection, ping result shape, fixture-equality (line 1 + line 2), and the ACL construction
    - UtinniCoreDotNet.csproj includes both new files; the managed suite builds net472 (no regression in the full UtinniCoreDotNet.Tests count)
  </acceptance_criteria>
  <done>The in-client pipe server + shared protocol exist with a bounded lifecycle/dispose, server-side CLIENT-ROOT path containment via the shared LooseOverridePath, Func-injected (never-on-worker) game-state, in-box camelCase JavaScriptSerializer framing with string-encoded tier pinned to the fixture + golden wire byte-vectors, protocolVersion-skew handling, exact current-user ACL, the D-01 placeholder + reloadAttempted=false candor, and pass pure-managed/loopback tests with no live client.</done>
</task>

<task type="auto">
  <name>Task 2: ServerArgs.EnableLive flag (fail-closed default) + extended ServerArgsTests</name>
  <files>Utinni.Mcp/Server/ServerArgs.cs, Utinni.Mcp.Tests/ServerArgsTests.cs</files>
  <read_first>
    - Utinni.Mcp/Server/ServerArgs.cs (the self-analog: --root/--cli-path property + const + parse-loop, lines 43-94; the tolerant/first-wins/equals-form contract in the XML doc lines 28-39)
    - Utinni.Mcp.Tests/ServerArgsTests.cs (the existing parser test class to extend)
    - .planning/phases/16-.../16-RESEARCH.md §A5 (--enable-live is a BOOLEAN presence flag, optional UTINNI_MCP_ENABLE_LIVE env alias mirroring UTINNI_MCP_ROOT)
  </read_first>
  <action>
    Extend ServerArgs.cs: add `public bool EnableLive { get; private set; }` and `private const string EnableLiveFlag = "--enable-live";`. In the Parse loop, add a branch BEFORE the value-flag branches: `if (token == EnableLiveFlag) { result.EnableLive = true; continue; }` — it is a presence flag, NOT value-taking (differs from --root/--cli-path). Default is false (fail-closed by absence, D-04). Optionally honor a UTINNI_MCP_ENABLE_LIVE env var mirroring the UTINNI_MCP_ROOT precedent — if added, require an EXPLICIT truthy value (e.g. "1"/"true"/"yes", case-insensitive), NOT "any non-empty" (Codex LOW: keep enablement explicit/visible), and document it in the class XML doc. In the class XML doc, also state the DUAL-FLAG operator contract (CUR-NEW-6): --enable-live gates only the MCP tool tier; the in-client `[Live] enableLiveBridge` flag (16-02 Task 3) gates the LISTENER; BOTH must be ON for a working live path, neither alone is sufficient. Do not break the existing tolerant/first-wins/equals-form contract for the other flags.
    Extend ServerArgsTests.cs: add facts asserting (1) `--enable-live` present → EnableLive true; (2) absent → EnableLive false (the fail-closed default); (3) --enable-live alongside --root/--cli-path parses all three correctly; (4) (if env alias added) an explicit truthy env value sets EnableLive AND a non-truthy/garbage env value does NOT. Name the additions so `--filter ServerArgs` selects them.
  </action>
  <verify>
    <automated>cd D:/Code/Utinni && dotnet test Utinni.Mcp.Tests --no-build --filter ServerArgs</automated>
  </verify>
  <acceptance_criteria>
    - ServerArgs.cs contains `public bool EnableLive` and `EnableLiveFlag = "--enable-live"`; EnableLive defaults to false when the flag is absent
    - `--enable-live` is parsed as a presence flag (no following value consumed)
    - The class XML doc states the dual-flag operator contract (--enable-live + in-client [Live] enableLiveBridge, both required — CUR-NEW-6)
    - If the env alias is added, it requires an explicit truthy value (not any-non-empty), with a test asserting a garbage value does NOT enable
    - ServerArgsTests asserts present→true, absent→false, and co-existence with --root/--cli-path
    - `dotnet test Utinni.Mcp.Tests --filter ServerArgs` exits 0
  </acceptance_criteria>
  <done>EnableLive parses fail-closed-by-absence with test coverage and a documented dual-flag operator contract; the Wave-2 host plan can gate WithTools<LiveTools>() on it.</done>
</task>

<task type="auto">
  <name>Task 3: Wire LivePipeServer.StartListener() into Startup.EntryPoint (main.cs) — in-client-gated, CLIENT-ROOT-pinned, post-Callbacks, pre-Application.Run (C-01, CONFIRMED; CUR-NEW-1 root fix)</name>
  <files>UtinniCoreDotNet/main.cs, UtinniCoreDotNet.Tests/Live/LivePipeServerTests.cs</files>
  <read_first>
    - UtinniCoreDotNet/main.cs (Startup.EntryPoint :43-114 — the one-time init block; `injectRoot` at :62 is the Utinni DLL dir, NOT the client root — CUR-NEW-1; the *Callbacks.Initialize() block :90-93; Native.SignalLauncherReady() :106; the BLOCKING Application.Run :108-110 — the listener start must land between :93 and :108)
    - UtinniCoreDotNet/Live/LivePipeServer.cs (Task 1 — StartListener()/Stop()/Dispose() + ctor that takes the pinned CLIENT root + the injected Func<bool> getGameIsRunning)
    - UtinniPlugins "The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs":1596-1630 (ResolveClientRoot — the EXACT chain to mirror for the server's client root: MainModule dir → GetWorkingDirectory → [TreBrowser] clientDir; CUR-NEW-1. The wiring must reuse this resolution, NOT injectRoot.)
    - UtinniCore.Utinni.utinni.GetConfig() usage at main.cs:108 (the GetConfig().GetBool("Editor","enableEditorMode") precedent — the SAME config surface to read the in-client live-enable flag, C-06)
    - .planning/phases/16-.../16-RESEARCH.md Open Q1 (in-client listener gating as defense-in-depth) + §Security Domain V13
  </read_first>
  <action>
    C-01 (CONFIRMED MISSING): wire the listener start so the bridge actually runs in a real injected client. In Startup.EntryPoint, inside the `if (!initialized)` one-time block, AFTER the four `*Callbacks.Initialize()` calls (main.cs:93) and BEFORE `Native.SignalLauncherReady()` / the blocking `Application.Run` (main.cs:108), construct the LivePipeServer and call StartListener(). StartListener spawns a BACKGROUND thread, so placing it before Application.Run is correct (non-blocking).
    CLIENT-ROOT PINNING (CUR-NEW-1 — the HIGH fix): do NOT pass `injectRoot` (main.cs:62, the Utinni DLL dir). Resolve the SWG CLIENT root the SAME way the editor save path does (mirror FormIffEditor.ResolveClientRoot: (1) Path.GetDirectoryName(Process.GetCurrentProcess().MainModule.FileName) — the SWGEmu.exe dir under injection; (2) UtinniCore.Utility.utility.GetWorkingDirectory(); (3) the [TreBrowser] clientDir ini fallback) and pass THAT as the server's containment root, so the in-client containment root matches the MCP `--root` semantics the agent resolves under. (Factor a tiny client-root resolver — or inline the three-step chain — in main.cs; it is the same root the loose-override save composes against.) Inject the production `Func<bool>` that reads Game.IsRunning (invoked only at the marshal boundary per Task 1's CDX-NEW-2 contract).
    C-06 (defense-in-depth — in-client start gate): gate the StartListener call on its OWN in-client flag, read from the existing config surface (`utinni.GetConfig().GetBool("Live", "enableLiveBridge")`, mirroring the enableEditorMode read at :108). The MCP-host `--enable-live` flag gates TOOL ADVERTISEMENT on the out-of-proc side; this in-client flag gates the LISTENER itself. Default the in-client flag to OFF (fail-closed). Wrap the construct+start in a try/catch that logs and never throws into the init path (the AssemblyResolve/PluginLoader precedent).
    Add a code comment citing C-01 + CUR-NEW-1 + D-04/D-06 explaining: the placement (post-Callbacks so the marshal seam is wired; pre-Application.Run because Run blocks; background thread so non-blocking), the client-root-not-injectRoot pinning, and the two-flag model (MCP `--enable-live` = tool gate, in-client `[Live] enableLiveBridge` = listener gate — CUR-NEW-6).
    Add a pure-managed test (extend LivePipeServerTests.cs from Task 1): assert StartListener is IDEMPOTENT (a second call does not spawn a second accept loop). (The live in-client confirmation that ping returns listening:true is Tier-4 manual / non-gating per D-01; this task's GATED requirement is that the WIRING exists in main.cs, pins the CLIENT root, and is code-asserted.)
  </action>
  <verify>
    <automated>cd D:/Code/Utinni && dotnet test UtinniCoreDotNet.Tests --no-build --filter LivePipeServer</automated>
  </verify>
  <acceptance_criteria>
    - main.cs Startup.EntryPoint constructs a LivePipeServer and calls `StartListener()` within the `if (!initialized)` block, positioned AFTER the *Callbacks.Initialize() block and BEFORE Application.Run (source-assertion: `StartListener` appears in main.cs between the GameCallbacks.Initialize and Application.Run lines)
    - The server's containment root is resolved via the editor-save-path client-root chain (MainModule dir → GetWorkingDirectory → [TreBrowser] clientDir), NOT `injectRoot` (CUR-NEW-1 — source-assertion: the StartListener construction does not pass `injectRoot` as the containment root)
    - The StartListener call is gated on an in-client enable flag (`[Live] enableLiveBridge`, default OFF — C-06) AND wrapped in a try/catch that logs and does not throw into the injection init path
    - A code comment documents the two-flag operator contract (CUR-NEW-6) and the client-root-not-injectRoot rationale (CUR-NEW-1)
    - UtinniCoreDotNet/main.cs is listed in this plan's files_modified (it is)
    - LivePipeServerTests asserts StartListener idempotency
    - `dotnet test UtinniCoreDotNet.Tests --filter LivePipeServer` exits 0; full UtinniCoreDotNet.Tests builds net472 with no regression
    - NOTE recorded in the SUMMARY: live in-client ping=listening:true is Tier-4 manual / non-gating (D-01); the GATED deliverable here is the wired + client-root-pinned + idempotent listener start, code-asserted
  </acceptance_criteria>
  <done>StartListener is wired into main.cs (post-Callbacks, pre-Application.Run, background thread), pinned to the resolved SWG CLIENT root (NOT injectRoot — CUR-NEW-1), gated on an in-client flag (defense-in-depth, C-06), failure-isolated, and idempotency-tested — so a legitimate MCP-resolved reload is contained correctly in production and live_ping can return listening:true (the C-01 delivery gap is closed without the CUR-NEW-1 root mismatch).</done>
</task>

</tasks>

<threat_model>
**Gate verdict: ASVS L1; no HIGH-severity threats; security gate passes.** (All threats below are mitigate or accept; none rated HIGH. Block-on-HIGH posture: had any threat been HIGH it would gate the wave — none are.)

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local process → named pipe (in-client server) | Any same-user local process that knows the pipe name can connect; the server runs inside the injected x86 client. Both the current-user ACL AND the in-client enable flag (C-06) gate exposure; the reload path is contained against the SWG CLIENT root (C-04/CUR-NEW-1). |
| pipe worker thread → game thread | Reload work crosses from the background worker onto the game thread only via the marshal queue; native game state (Game.IsRunning, P/Invoke) is read only via the injected Func at the boundary, never in the worker/accept loop (C-09/CDX-NEW-2, Pitfall 2). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | Spoofing/Elevation | Unauthorized local process connects to the pipe and triggers reloads | mitigate | PipeSecurity with a single PipeAccessRule for `WindowsIdentity.GetCurrent().User` + FullControl/Allow, deny-others-by-omission (CDX-NEW-4, exact construction pinned); NamedPipeServerStream is local-machine only; AND the listener only starts when the in-client enable flag is ON (C-06 defense-in-depth — fail-closed default OFF). Acceptance: ACL-construction assert + Task-3 in-client-flag gate. |
| T-16-11 | Tampering (integrity) | Reload work executed on the wrong thread / native game-state deref off the game thread → client AV/crash | mitigate | All reload work marshals via GameCallbacks.AddMainLoopCall; gameIsRunning is read only via the injected Func at the marshal boundary, never in the worker/accept loop (C-09/CDX-NEW-2 — acceptance asserts the loop body contains no Game.IsRunning reference); the worker never dereferences game state (Pitfall 2). |
| T-16-12 | DoS | Malformed/oversize/partial/slow wire frame on the in-client server thread | mitigate | Bounded MaxFrameBytes + op allow-list + EXPLICIT server-side connect/read timeouts (CDX-NEW-7) so a stalled/slow/oversize-declared client is closed within a bounded window without starving the accept loop; a bad/partial frame closes that connection only, the loop recovers. Acceptance: oversize + server-side partial-read/mid-disconnect + stalled-client-closed + malformed-then-valid-sequential tests. |
| T-16-13 | Spoofing | Pipe name collision / squatting | accept | Sufficiently-specific pipe name; server owns creation. Single-user local desktop trust assumption (analog AR-14-03/04). |
| T-16-14 | Tampering | In-client reload-asset targets an arbitrary absolute / out-of-root path (same-user process bypassing the MCP-side root) | mitigate | The wire carries a RELATIVE path; the server resolves it via the SHARED LooseOverridePath.Resolve against the pinned SWG CLIENT root (CUR-NEW-1 — the SAME root the editor save path + MCP --root use, NOT injectRoot; reuses the battle-tested helper per CUR-NEW-7) and REJECTS absolute / `..`-escaping paths (accepted=false, no enqueue) — independent of the 16-03 MCP-side ResolvedRoot. Acceptance: Task-1 client-root containment tests incl. the cross-root case. |
| T-16-15 | Tampering (protocol drift) | net10/net472 wire shapes silently diverge (framing, JSON casing, tier encoding, field names, protocol version) → a green net472-on-net472 test hides a broken net10 path | mitigate | Committed GOLDEN WIRE BYTE-VECTORS (ping/reload request+ack) the net472 server produces/consumes byte-exact (CDX-NEW-1) and the net10 side asserts identically in 16-03; camelCase emitted on the net472 wire (CUR-NEW-4); ReloadTier pinned to string names (CUR-NEW-9); a protocolVersion-skew request returns a structured reject ack (CUR-NEW-5). Acceptance: Task-1 golden-vector produce+consume + camelCase + string-tier + skew tests. |
| T-16-SC | Tampering | npm/pip/cargo installs | n/a | No new packages this phase — net472 server uses **JavaScriptSerializer** (System.Web.Extensions, ALREADY referenced — C-03); BCL System.IO.Pipes only; Package Legitimacy Gate not triggered (no System.Text.Json package added). |
</threat_model>

<verification>
- `dotnet test UtinniCoreDotNet.Tests --filter LivePipeServer` green (envelope→tier→enqueue with Func-injected game state; client-root containment incl. cross-root; golden wire byte-vectors produce+consume; camelCase+string-tier; protocolVersion skew; bounded lifecycle incl. idempotent StartListener + dispose-within-N-ms + stalled-client-closed + sequential malformed-then-valid; server-side partial-read; oversize rejection; pipe-name + framing fixture-equality; ACL construction; StartListener idempotency for the main.cs wiring).
- `dotnet test Utinni.Mcp.Tests --filter ServerArgs` green (EnableLive fail-closed default + dual-flag doc).
- Full UtinniCoreDotNet.Tests + Utinni.Mcp.Tests lanes build with no regression.
- Source assertion: `StartListener` is present in main.cs between the GameCallbacks.Initialize and Application.Run lines (C-01 wiring), pinned to the resolved CLIENT root (not injectRoot — CUR-NEW-1).
</verification>

<success_criteria>
- The in-client pipe server reuses the shipped marshal seam (mainLoopCallQueue) and ReloadAssetClassifier; no per-frame heap allocation introduced (project_rh_snapshot_no_heap_alloc honored).
- The listener is actually WIRED into main.cs (C-01), pinned to the SWG CLIENT root (CUR-NEW-1) so a legitimate MCP-resolved reload is contained correctly in production, in-client-flag gated (C-06), and idempotency-tested.
- The in-client reload path is contained against the client root via the shared LooseOverridePath (C-04/CUR-NEW-1/CUR-NEW-7); the server lifecycle is specified with bounded timeouts + a proven bounded dispose (C-07/C-08/CDX-NEW-7/8); game-state reads are Func-injected and never on the worker (C-09/CDX-NEW-2).
- The ack reports the honest reload tier (string-encoded — CUR-NEW-9) with reloadAttempted=false candor; visible re-render is NOT gated (D-01); the enqueued reload Action is an explicit D-01 placeholder.
- The canonical pipe-name/framing fixture + golden wire byte-vectors are committed; the net472 PipeName (line 1) + framing (line 2) + the actual JSON bytes (camelCase, string-tier, protocolVersion) assert byte-exact (the cross-TFM agreement anchors; CDX-NEW-1/C-05/CUR-NEW-4/CUR-NEW-9); protocolVersion skew is handled (CUR-NEW-5); net472 JSON is in-box JavaScriptSerializer (C-03), no new package.
- EnableLive defaults closed; the dual-flag operator contract is documented (CUR-NEW-6); the Wave-2 host gates tool registration on it.
</success_criteria>

<output>
Create `.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-02-SUMMARY.md` when done.
</output>
