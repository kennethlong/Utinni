---
phase: 16-live-injected-mcp-bridge-blender-ecosystem-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ai/blender-boundary-contract.md
  - Utinni.Cli/Commands/ValidateBundleCommand.cs
  - Utinni.Cli/Program.cs
  - Utinni.Cli.Tests/Commands/ValidateBundleTests.cs
  - Utinni.Cli.Tests/Commands/BlenderBoundaryGoldenTests.cs
  - Utinni.Cli.Tests/Fixtures/blender/frn_all_bed_sm_s1_l0.msh
  - Utinni.Cli.Tests/Fixtures/blender/retail_mini_0005.tre
  - Utinni.Cli.Tests/Fixtures/blender/data_compressed_mesh_static.rsp
  - Utinni.Cli.Tests/Fixtures/blender/client_search_paths.cfg
  - Utinni.Cli.Tests/Fixtures/blender/swg_export_manifest.json
  - Utinni.Cli.Tests/Fixtures/blender/fixture-hashes.txt
  - D:/Code/swg-blender-plugin/REFERENCES.md
autonomous: false
requirements: [ECO-01]
user_setup: []

must_haves:
  truths:
    - "A modder reading docs/ai/blender-boundary-contract.md learns the exact .rsp line format (incl. that the `@` RHS is an ABSOLUTE explicit path per rsp_builder.py), bucket rules, the TRE version matrix (5000 readable, 6000 enumerate-only), the bundle layout, the swg_export_manifest.json schema (required/optional fields), and the no-runtime-coupling rule"
    - "utinni-cli validate-bundle parses a Blender export bundle (manifest + .rsp + .cfg), existence-checks referenced assets whose canonical path is CONTAINED under the bundle root (a CONTAINED absolute ref is allowed and checked — matching real Blender output; only refs that ESCAPE the bundle root via `..`/drive/UNC, or are absent, are rejected, and an escaping ref is NEVER File.Exists-probed), and emits a JSON envelope with an explicit `valid`/`hasRejectedRefs` field (success or typed error)"
    - "utinni-cli parse-tre opens a pinned Blender-written .tre and decode-iff summarizes a pinned Blender-written .msh (count-only, no geometry)"
    - "The pinned Blender goldens are SHA-256-pinned and a golden test fails if the fixture bytes change (silent fixture-refresh drift is caught — Codex provenance finding)"
    - "validate-bundle rejects a malformed manifest AND a malformed .rsp with the exit-2 ParseError taxonomy (coverage beyond success + missing-manifest — C-16)"
    - "validate-bundle's containment delegates to / mirrors the shipped LooseOverridePath.Resolve algorithm (explicit `..` segment scan + canonicalize + trailing-separator OrdinalIgnoreCase StartsWith), not a hand-rolled subset (CDX-NEW-11/CUR-NEW-7)"
    - "The bucket-filename set used by ValidateBundleCommand matches the bucket sections documented in blender-boundary-contract.md (doc↔verb parity — C-17)"
    - "The swg-blender-plugin repo carries a pointer note referencing the Utinni-authoritative contract doc"
  artifacts:
    - path: "docs/ai/blender-boundary-contract.md"
      provides: "The D-06 four-part Utinni-authoritative boundary contract (incl. the manifest schema + contained-absolute .rsp rule)"
      min_lines: 80
    - path: "Utinni.Cli/Commands/ValidateBundleCommand.cs"
      provides: "validate-bundle verb (text .rsp/.cfg/manifest parse + CONTAINED-ref existence check incl. contained absolutes; NO binary codec)"
      contains: "validate-bundle"
    - path: "Utinni.Cli.Tests/Fixtures/blender/frn_all_bed_sm_s1_l0.msh"
      provides: "Pinned Blender golden mesh (cross-validation source bytes)"
    - path: "Utinni.Cli.Tests/Fixtures/blender/fixture-hashes.txt"
      provides: "SHA-256 hashes of the pinned goldens, asserted by the golden test (drift guard)"
  key_links:
    - from: "Utinni.Cli/Program.cs"
      to: "Commands.ValidateBundleCommand.Run"
      via: "ParseArguments Type[] + Dispatch switch case"
      pattern: "ValidateBundleOptions"
    - from: "Utinni.Cli.Tests/Commands/BlenderBoundaryGoldenTests.cs"
      to: "parse-tre / decode-iff"
      via: "InProcessCliRunner.Run over pinned fixtures"
      pattern: "FixturePath.Resolve\\(\"blender/"
    - from: "Utinni.Cli/Commands/ValidateBundleCommand.cs"
      to: "LooseOverridePath.Resolve containment algorithm"
      via: "canonicalize-and-reject-escape (delegated/mirrored) before any File.Exists"
      pattern: "GetFullPath"
---

<objective>
Formalize the Utinni ↔ `swg-blender-plugin` file-format boundary (ECO-01). Deliver the Utinni-authoritative contract doc covering all four D-06 surfaces, one thin `validate-bundle` CLI verb (pure text/manifest parsing with explicit path-traversal containment that ALLOWS contained absolute refs — NO 3D decode, honoring DEC-A3), pinned in-repo Blender golden fixtures with SHA-256 provenance pinning, a cross-validation golden test proving Utinni reads Blender-written bytes (D-08, SC4), and a one-line pointer note in the Blender repo (D-05).

Purpose: Lock the file-format seam so Blender writes and Utinni reads without runtime coupling. Reuse existing readers (`parse-tre`, `decode-iff`, `inspect-iff`); add only the thin text-validation verb the current reader set lacks.
Output: contract doc, `validate-bundle` verb + registration, fixtures + hash manifest, golden + verb tests (incl. malformed + path-traversal + contained-absolute cases), cross-repo pointer note.

REVIEW-DRIVEN EDITS (from 16-REVIEWS.md): C-15 (path-traversal containment explicit in action + tests), C-16 (malformed-manifest / malformed-rsp tests), C-17 (doc↔verb bucket parity check), Codex fixture-provenance (SHA-256 hashes asserted in the golden test), C-19 (Windows-safe verify — Git Bash / dotnet-friendly).

REVIEW-DRIVEN EDITS (Round 2, from 16-REVIEWS.md): **CUR-NEW-3 = CDX-NEW-12 (HIGH)** — `validate-bundle` must ALLOW an absolute ref whose canonical path is CONTAINED under the bundle root (real Blender `.rsp` emits `{treefile} @ {abspath}` where the RHS is an ABSOLUTE explicit path — VERIFIED in rsp_builder.py:58 `bucket_map[rel] = str(abspath)` + :62-64 `format_rsp_line`); reject ONLY refs that ESCAPE the bundle root (`..`, drive/UNC outside bundle) or are absent — a Blender-faithful absolute-`.rsp` fixture is added to the happy path (Tasks 1/2/3). **CDX-NEW-11 = CUR-NEW-7** — delegate containment to the shipped `UtinniCoreDotNet.Saving.LooseOverridePath.Resolve` algorithm (explicit `..` segment scan + root re-canonicalization + trailing-separator OrdinalIgnoreCase StartsWith prefix-attack guard) rather than a hand-rolled `StartsWith(root + sep)`; reuse it directly for relative refs and mirror its exact algorithm for the contained-absolute branch; document the symlink/junction policy (Task 2). **CDX-NEW-9** — `validate-bundle` emits an explicit `valid` / `hasRejectedRefs` envelope field so an exit-0 transport success cannot be misread as "validated clean" (Tasks 2/3). **CDX-NEW-10** — the manifest schema (required/optional fields, `rsp_files` shape, `client_cfg`, unknown-field handling, version behavior) is defined in the contract doc + tests so the validator is a stable contract not fixture-shaped (Tasks 2/3). **CUR-NEW-10** — Task 4's pointer-note verify is made Windows-safe (PowerShell/dotnet-friendly, consistent with the C-19 fix already applied to Task 3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-CONTEXT.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-RESEARCH.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-PATTERNS.md
@.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — pin Blender golden fixtures (+ SHA-256 manifest) + write the cross-validation golden test scaffold (+ malformed/traversal/contained-absolute RED cases)</name>
  <files>Utinni.Cli.Tests/Fixtures/blender/frn_all_bed_sm_s1_l0.msh, Utinni.Cli.Tests/Fixtures/blender/retail_mini_0005.tre, Utinni.Cli.Tests/Fixtures/blender/data_compressed_mesh_static.rsp, Utinni.Cli.Tests/Fixtures/blender/client_search_paths.cfg, Utinni.Cli.Tests/Fixtures/blender/swg_export_manifest.json, Utinni.Cli.Tests/Fixtures/blender/fixture-hashes.txt, Utinni.Cli.Tests/Commands/BlenderBoundaryGoldenTests.cs, Utinni.Cli.Tests/Commands/ValidateBundleTests.cs</files>
  <read_first>
    - Utinni.Cli.Tests/Commands/ParseTreCommandTests.cs (the in-process golden-test pattern + MaskPath sentinel, PATTERNS analog lines 251-263)
    - Utinni.Cli.Tests/Commands/DecoderTests.cs (the `InProcessCliRunner.Run("decode-iff", path)` STRING-runner pattern — invokes a verb by string name through CLI dispatch, so an unregistered verb fails at RUNTIME not compile-time; lines ~233, 256)
    - Utinni.Cli.Tests/Infrastructure/FixturePath.cs (FixturePath.Resolve resolves Fixtures/<rel> under AppContext.BaseDirectory; lines 37-50)
    - D:/Code/swg-blender-plugin/tests/golden/ (fixture origin — copy frn_all_bed_sm_s1_l0.msh and the .tre from here; D-08)
    - D:/Code/swg-blender-plugin/swg_pipeline/rsp_builder.py (THE .rsp line format + bucket rules — CONFIRM: line 58 `bucket_map[rel] = str(abspath)` and lines 62-64 `format_rsp_line(treefile_path, explicit_path)` emit `{treefile_path} @ {abspath}` where the RHS is the `.resolve()`-d ABSOLUTE path forward-slashed; the sample .rsp fixture MUST mirror this absolute-RHS shape — RESEARCH B2 / CUR-NEW-3)
    - D:/Code/swg-blender-plugin/swg_pipeline/export_bundle.py (manifest + cfg fragment shape for the sample fixtures — RESEARCH B3)
    - UtinniCoreDotNet.PathContainment/LooseOverridePath.cs (the shipped containment algorithm the verb will reuse/mirror in Task 2 — read so the test's expectations match its semantics: rooted relAssetPath rejected, `..` segment rejected, trailing-separator OrdinalIgnoreCase StartsWith)
    - .planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-VALIDATION.md (Wave 0 list + per-task test commands)
  </read_first>
  <action>
    Copy the two tiny Blender goldens VERBATIM from D:/Code/swg-blender-plugin/tests/golden/ into Utinni.Cli.Tests/Fixtures/blender/: frn_all_bed_sm_s1_l0.msh (~6099 B, FORM...MESH...0005) and retail_mini_0005.tre (~119 B, EERT0005). These are the cross-validation source bytes (D-08) — in-repo, NO Git LFS (CON-O-09 resolved). The existing Utinni.Cli.Tests.csproj `Fixtures\**\*` recursive glob (CopyToOutputDirectory="PreserveNewest") already copies the new blender/ subdir to output automatically — do NOT add a new/duplicate Content entry; just verify the fixtures are present under AppContext.BaseDirectory/Fixtures/blender/ after build (FixturePath.Resolve reads AppContext.BaseDirectory/Fixtures/...).
    Compute the SHA-256 of each pinned BINARY golden (the .msh and the .tre) and record them in Utinni.Cli.Tests/Fixtures/blender/fixture-hashes.txt as `{lowercase-hex-sha256}  {filename}` lines (one per binary golden). This is the Codex fixture-provenance fix: a future fixture refresh that silently changes the cross-validation bytes will mismatch the recorded hash and fail the golden test. (Hash only the binary goldens; the text fixtures below are human-authored and may be edited deliberately.)
    Author three small TEXT fixtures matching the rsp_builder.py / export_bundle.py reference shapes:
      - data_compressed_mesh_static.rsp: author it Blender-FAITHFULLY (CUR-NEW-3). Per rsp_builder.py format_rsp_line, each line is `{treefile_path} @ {explicit_path}` where the LHS treefile_path is the logical relative path (e.g. appearance/mesh/frn_all_bed_sm_s1_l0.msh, forward slashes) and the RHS explicit_path is an ABSOLUTE forward-slashed path that, in this fixture, points to the on-disk golden CONTAINED UNDER the bundle root (so the happy-path test exercises the contained-absolute branch, not just relative refs). Include at least one such contained-absolute line.
      - client_search_paths.cfg: a `searchPath_00_12=` SWGSource-form line (sku 0, priority 12, per rsp_builder.client_search_path_snippet).
      - swg_export_manifest.json: bundle_type=static, output_dir, an assets block listing the .msh + rsp_files (matching the export_bundle.py shape — see Task 3's documented schema), and client_cfg. The manifest's own asset refs are CONTAINED under the bundle root (the happy-path fixture has zero ESCAPING refs; contained absolutes are permitted and exercised).
    Write BlenderBoundaryGoldenTests.cs cloning ParseTreCommandTests.cs's InProcessCliRunner.Run + MaskPath + GoldenTestRunner shape: (1) `parse-tre` over retail_mini_0005.tre exits 0 + matches a golden; (2) `decode-iff` over frn_all_bed_sm_s1_l0.msh exits 0, emits a MESH AppearanceSummary with non-zero counts and no geometry payload (count-only); (3) a SHA-256 provenance fact: re-hash the on-disk .msh + .tre fixtures, parse fixture-hashes.txt, assert each computed hash byte-equals the recorded hash (drift guard). Use FixturePath.Resolve("blender/...") + MaskPath path masking. Name the test class so `--filter Blender` selects it. These tests exercise ONLY already-shipped verbs (parse-tre, decode-iff) + a pure SHA-256 compare so they compile AND pass at the Task-1 boundary.
    Write ValidateBundleTests.cs as the Wave-0 RED scaffold for Task 2's verb, exercising the verb via the in-process CLI STRING runner — `InProcessCliRunner.Run("validate-bundle", <args>)` (the DecoderTests.cs pattern) — NOT a direct `ValidateBundleCommand` type reference (WARNING-3 fix: keep the assembly COMPILING at the Task-1 boundary; an unregistered verb returns non-zero at RUNTIME, RED, not a build break). Author these cases (all go GREEN once Task 2 registers + implements the verb):
      (a) success: the pinned-bundle manifest path (which includes the contained-absolute `.rsp` line) → exit 0 + a success envelope with `valid:true` + `hasRejectedRefs:false` (CDX-NEW-9);
      (b) missing-manifest: a non-existent path → exit 3;
      (c) malformed manifest: a fixture or temp file with invalid JSON → exit 2 ParseError (C-16);
      (d) malformed .rsp: a manifest pointing at a .rsp whose lines don't match `{path} @ {path}` → exit 2 ParseError (C-16);
      (e) ESCAPING-ref (path-traversal): a manifest/.rsp whose asset ref canonicalizes OUTSIDE the bundle root — both an absolute path on a DIFFERENT drive/UNC AND a `..`-escaping relative ref → the verb reports each as a validation FINDING (a `rejectedRefs` entry) and NEVER File.Exists-probes the escaping path; assert the envelope flags the ref (`valid:false`/`hasRejectedRefs:true`) rather than silently existence-checking it (C-15). Build the escaping manifest in-test (Path.Combine with `..\..\..\..\` or an absolute path on another drive) so the malicious ref is synthetic, not a committed fixture.
      (f) CONTAINED-absolute (CUR-NEW-3): a manifest/.rsp whose asset ref is an ABSOLUTE path whose canonical form is rooted UNDER the bundle root (mirroring rsp_builder.py's absolute RHS pointing at a contained file) → the verb ALLOWS it, File.Exists-checks it, and does NOT add it to rejectedRefs; assert it is treated as a normal contained asset (this is the case that catches "reject ALL absolutes" regressions). Build it in-test pointing at the committed pinned .msh under the bundle-root fixture tree.
    Name the class so `--filter ValidateBundle` selects it (the Task-1 `--filter Blender` verify excludes it while the assembly still builds). Do NOT reference any type Task 2 creates from this file.
    Do NOT add any mesh/geometry decode. .rsp/.cfg/manifest are TEXT only.
  </action>
  <verify>
    <automated>cd D:/Code/Utinni && dotnet test Utinni.Cli.Tests --filter Blender</automated>
  </verify>
  <acceptance_criteria>
    - The Task-1 verify BUILDS the whole Utinni.Cli.Tests assembly (no --no-build) and passes — ValidateBundleTests.cs must NOT reference any Task-2 type, so the assembly compiles cleanly at this boundary (WARNING-3 fix)
    - Fixtures/blender/ contains frn_all_bed_sm_s1_l0.msh, retail_mini_0005.tre, data_compressed_mesh_static.rsp, client_search_paths.cfg, swg_export_manifest.json, fixture-hashes.txt (all committed, no LFS pointer)
    - data_compressed_mesh_static.rsp contains at least one `{treefile_path} @ {absolute_explicit_path}` line whose RHS is an ABSOLUTE forward-slashed path (Blender-faithful per rsp_builder.py) pointing at a file CONTAINED under the bundle-root fixture tree (CUR-NEW-3 happy-path coverage)
    - fixture-hashes.txt records a lowercase-hex SHA-256 for each binary golden (.msh + .tre); BlenderBoundaryGoldenTests re-hashes the on-disk fixtures and asserts equality (drift guard — Codex provenance fix)
    - `dotnet test Utinni.Cli.Tests --filter Blender` selects and runs BlenderBoundaryGoldenTests; the parse-tre + decode-iff + SHA-256 asserts pass against the pinned bytes
    - BlenderBoundaryGoldenTests.cs contains `FixturePath.Resolve("blender/` for each pinned fixture
    - The .msh decode assert verifies a MESH AppearanceSummary count-only output (no geometry/vertex array in the JSON)
    - ValidateBundleTests.cs exists, drives `validate-bundle` via InProcessCliRunner.Run (string verb name, no ValidateBundleCommand type reference), with cases (a) success+valid:true, (b) missing-manifest exit-3, (c) malformed-manifest exit-2, (d) malformed-rsp exit-2, (e) ESCAPING-ref reported-as-finding-not-probed (valid:false/hasRejectedRefs:true), (f) CONTAINED-absolute allowed+checked-not-rejected (filterable via `--filter ValidateBundle`; RED until Task 2 implements the verb)
  </acceptance_criteria>
  <done>Pinned Blender goldens committed in-repo with SHA-256 provenance; parse-tre + decode-iff + hash cross-validation green; the test assembly compiles at the Task-1 boundary; validate-bundle string-runner scaffold present incl. malformed + escaping + contained-absolute cases (RED until Task 2, never a compile break).</done>
</task>

<task type="auto">
  <name>Task 2: validate-bundle CLI verb (thin text/manifest parse + LooseOverridePath-delegated containment that allows contained absolutes + explicit valid/hasRejectedRefs envelope) + register in dispatch</name>
  <files>Utinni.Cli/Commands/ValidateBundleCommand.cs, Utinni.Cli/Program.cs</files>
  <read_first>
    - Utinni.Cli/Commands/InspectIffCommand.cs (exact analog: [Verb] options class + Run with FileNotFound→exit-3, parse→exit-2, IoError→exit-2, JsonOutput emit; this verb set uses Newtonsoft.Json — match it; PATTERNS lines 196-221)
    - Utinni.Cli/Commands/ParseTreCommand.cs (envelope discipline: result object never pre-adds command/schemaVersion; lines 71-75)
    - Utinni.Cli/Program.cs (ParseArguments Type[] overload lines 48-70 + Dispatch switch lines 79-104 — the registration sites)
    - UtinniCoreDotNet.PathContainment/LooseOverridePath.cs (THE shipped containment algorithm to DELEGATE to for relative refs and MIRROR for the contained-absolute branch — CDX-NEW-11/CUR-NEW-7. Note its semantics: it canonicalizes the root, rejects a ROOTED relAssetPath, rejects any `..` segment, appends a trailing separator, and does an OrdinalIgnoreCase StartsWith. It is in the netstandard2.0 UtinniCoreDotNet.PathContainment lib referenced by net472 — reuse it directly if the CLI assembly can reference it; otherwise mirror its exact algorithm and cite it.)
    - Utinni.Mcp/Server/ResolvedRoot.cs (the Phase-14 boundary that ALSO delegates to LooseOverridePath.Resolve — the precedent for reusing the shared helper rather than reimplementing)
    - D:/Code/swg-blender-plugin/swg_pipeline/rsp_builder.py (the `{treefile_path} @ {abspath}` line format with ABSOLUTE RHS + the 6 suffix→bucket rules to validate; RESEARCH B2 / CUR-NEW-3)
    - D:/Code/swg-blender-plugin/swg_pipeline/export_bundle.py (swg_export_manifest.json shape to consume as the bundle index; RESEARCH B3)
    - .planning/phases/16-.../16-RESEARCH.md §B1 (reachability verdict — reuse parse-tre/decode-iff/inspect-iff for IFF/TRE assets; do NOT re-open them in this verb)
  </read_first>
  <action>
    Create ValidateBundleCommand.cs mirroring InspectIffCommand.cs exactly: a `[Verb("validate-bundle", HelpText = ...)]` ValidateBundleOptions with a `[Value(0, MetaName = "path", Required = true)]` path that accepts a bundle root OR a swg_export_manifest.json. The static `ValidateBundleCommand.Run(ValidateBundleOptions)` follows the InspectIff error taxonomy: missing path → JsonOutput.EmitError("validate-bundle", "FileNotFound", ..., exitCode: 3); a malformed manifest/.rsp/.cfg → "ParseError" exit 2; IOException → "IoError" exit 2; generic Exception intentionally NOT caught (matches the shipped verbs).
    Body (thin, TEXT only — DEC-A3 clean): load swg_export_manifest.json as the authoritative bundle index (a JSON parse failure → ParseError exit 2); existence-check each referenced asset whose canonical path is CONTAINED under the bundle root; parse each rsp/*.rsp line as `{treefile_path} @ {explicit_path}` (a line that does not match the two-field `@`-separated grammar → ParseError exit 2) and validate the suffix→bucket→filename classification against the RESEARCH B2 rules (.mp3→music, .wav→sample, .dds→texture, .ans→animation, .mgn→mesh_skeletal, .msh→mesh_static, else other); validate the client_search_paths.cfg `searchPath{priority}=` / `searchPath_{sku:02d}_{priority}=` fragment is parseable. Determine the bundle root as the directory containing the manifest (or the bundle-root arg).
    PATH CONTAINMENT (C-15 / T-16-01 / CDX-NEW-11 / CUR-NEW-3 — explicit, DELEGATED): for EVERY asset ref drawn from the manifest or the .rsp RHS, classify it through the shipped LooseOverridePath containment algorithm rather than a hand-rolled StartsWith:
      - RELATIVE ref → call LooseOverridePath.Resolve(bundleRoot, ref) directly (it rejects rooted/`..` and returns the contained absolute path or throws ArgumentException on escape — catch the escape and record a rejectedRefs finding). Reuse the shipped helper as-is.
      - ABSOLUTE ref (the real-Blender `.rsp` RHS case — CUR-NEW-3) → LooseOverridePath.Resolve rejects rooted inputs by design, so it CANNOT be called on an absolute directly. Instead MIRROR its tail algorithm for the absolute: canonicalize the absolute via Path.GetFullPath, re-canonicalize the bundle root, append the OS trailing separator to the root, and OrdinalIgnoreCase-StartsWith-check the canonical absolute against `bundleRootWithSep` (the EXACT prefix-attack-safe gate LooseOverridePath uses at its lines 136-172). If CONTAINED → it is an allowed asset ref: File.Exists-check it like any contained ref. If it ESCAPES (different drive, UNC outside bundle, or `..`-walks out) → record a rejectedRefs finding and do NOT File.Exists-probe it.
      Add a one-line code comment citing LooseOverridePath.cs as the algorithm source-of-truth (CDX-NEW-11). Document the symlink/junction policy in a comment: the verb does NOT follow symlinks/junctions — containment is decided on the lexical canonical path (Path.GetFullPath), and a contained-but-symlinked target that points off-bundle is OUT OF SCOPE for this text validator (the live tier's runtime root containment is the enforcement boundary; validate-bundle is advisory text validation). State this so the policy is explicit, not implicit.
    Emit a JsonOutput success result object summarizing {valid, hasRejectedRefs, assetsChecked, missingAssets, rejectedRefs, rspFilesValidated, bucketMismatches} where `valid` is true ONLY when rejectedRefs is empty AND missingAssets is empty AND bucketMismatches is empty, and `hasRejectedRefs` is true when rejectedRefs is non-empty (CDX-NEW-9 — so a downstream agent reading exit-0 ALSO sees `valid:false` when there are findings). DECISION (documented in Task 3 contract doc): exit code stays 0 for a STRUCTURALLY-valid bundle (parseable manifest/.rsp/.cfg) even when it has missing/rejected refs — those are findings carried in the `valid`/`hasRejectedRefs`/`rejectedRefs`/`missingAssets` envelope fields, NOT parse failures; only a structurally-unparseable manifest/.rsp is exit 2. (Agents MUST read `valid`, not exit code alone — stated in the contract doc.) The result object must NOT pre-add schemaVersion/command — JsonOutput wraps it. Do NOT open .tre/.iff binaries here (reachability via parse-tre/decode-iff/inspect-iff is documented in the contract doc, not re-implemented).
    Define the suffix→bucket→filename map as a single named table in this file (e.g. a `static readonly` dictionary) so the Task-3 doc↔verb parity test (C-17) can assert the bucket filenames here match the documented sections; the bucket filenames the verb knows MUST be the same literal set the contract doc lists.
    Register the verb: add `typeof(Commands.ValidateBundleOptions)` to the ParseArguments Type[] in Program.cs and `case Commands.ValidateBundleOptions o: return Commands.ValidateBundleCommand.Run(o);` to the Dispatch switch. The Type[] overload has no 16-verb arity cap (memory project_phase13_cli_verbs) so adding one more is safe.
  </action>
  <verify>
    <automated>cd D:/Code/Utinni && dotnet test Utinni.Cli.Tests --no-build --filter ValidateBundle</automated>
  </verify>
  <acceptance_criteria>
    - ValidateBundleCommand.cs contains `[Verb("validate-bundle"` and emits via JsonOutput (no bespoke JSON)
    - The verb classifies every manifest/.rsp asset ref through the shipped LooseOverridePath algorithm — RELATIVE refs via a direct `LooseOverridePath.Resolve(bundleRoot, ref)` call, ABSOLUTE refs via the mirrored canonicalize + trailing-separator OrdinalIgnoreCase StartsWith gate — with a code comment citing LooseOverridePath.cs as the source-of-truth (CDX-NEW-11/CUR-NEW-7)
    - A CONTAINED absolute ref (canonical path under the bundle root) is ALLOWED and File.Exists-checked like a contained ref (CUR-NEW-3); an ESCAPING ref (different drive/UNC, or `..`-walk-out) is recorded in rejectedRefs and is NEVER File.Exists-probed (C-15, T-16-01)
    - The success envelope contains an explicit `valid` boolean (false when any rejectedRefs/missingAssets/bucketMismatches present) and a `hasRejectedRefs` boolean (CDX-NEW-9)
    - The symlink/junction policy is documented in a code comment (containment decided lexically via Path.GetFullPath; symlinked contained targets pointing off-bundle are out of scope for the text validator)
    - Program.cs contains `typeof(Commands.ValidateBundleOptions)` in the ParseArguments Type[] AND `case Commands.ValidateBundleOptions o:` in Dispatch
    - `dotnet test Utinni.Cli.Tests --filter ValidateBundle` exits 0 (the Task-1 success+valid:true, missing-manifest exit-3, malformed-manifest exit-2, malformed-rsp exit-2, ESCAPING-ref-reported-not-probed, and CONTAINED-absolute-allowed cases all go GREEN)
    - The verb body contains no binary mesh/IFF decode — only text/JSON manifest + .rsp + .cfg parsing and File.Exists checks on CONTAINED refs
    - The suffix→bucket→filename map is a single named table so the Task-3 parity test can reference its bucket filenames
  </acceptance_criteria>
  <done>validate-bundle verb compiles, is dispatched, validates a Blender bundle's text contract surface with the shipped CLI error taxonomy, delegates/mirrors LooseOverridePath containment (allowing contained absolutes per real Blender output, rejecting only escapes), and emits an explicit valid/hasRejectedRefs envelope; ValidateBundleTests green incl. malformed + escaping + contained-absolute cases.</done>
</task>

<task type="auto">
  <name>Task 3: Write the Utinni-authoritative boundary contract doc (all four D-06 surfaces + manifest schema + contained-absolute .rsp rule + exit/valid semantics) + doc↔verb bucket-parity test</name>
  <files>docs/ai/blender-boundary-contract.md, Utinni.Cli.Tests/Commands/ValidateBundleTests.cs</files>
  <read_first>
    - docs/ai/toolchain-inventory.md (structural/home-directory model for a docs/ai Utinni-authoritative reference doc; PATTERNS analog line 237)
    - UtinniCoreDotNet/Formats/Tre/TreVersion.cs (lines 35-106 — the AUTHORITATIVE version matrix the doc must mirror EXACTLY: TreVersions.Parse accepts {0004,0005,0006,5000,6000})
    - Utinni.Cli/Commands/ValidateBundleCommand.cs (Task 2 — the suffix→bucket→filename table whose bucket filenames the doc must list verbatim, for the C-17 parity check; AND the valid/hasRejectedRefs envelope + exit-0-with-findings semantics to document)
    - D:/Code/swg-blender-plugin/swg_pipeline/rsp_builder.py + export_bundle.py (the reference impl the doc documents — read-only, NOT imported; rsp_builder.py:62-64 confirms the `@` RHS is an ABSOLUTE path; export_bundle.py confirms the manifest field shape)
    - .planning/phases/16-.../16-RESEARCH.md §B2/B3/B4/B5 (the verified .rsp contract, bundle layout, version matrix, anti-coupling rules)
  </read_first>
  <action>
    Write docs/ai/blender-boundary-contract.md as the single source of truth (D-05). Document all four D-06 surfaces sourced from verified RESEARCH:
    (1) .rsp search-path contract (RESEARCH B2): the `{treefile_path} @ {explicit_path}` line format. STATE EXPLICITLY (CUR-NEW-3) that the LHS treefile_path is the logical relative asset path (forward slashes) and the `@` RHS explicit_path is an ABSOLUTE forward-slashed filesystem path (rsp_builder.py:62-64 `format_rsp_line` writes `str(abspath).replace("\\","/")`) — so a conforming bundle's .rsp RHS is normally absolute, and validate-bundle ALLOWS an absolute ref whose canonical path is CONTAINED under the bundle root, rejecting only refs that ESCAPE it. Document the 6 suffix→bucket→filename rules (.mp3→music/data_uncompressed_music.rsp … .msh→mesh_static/data_compressed_mesh_static.rsp, else other); earlier-roots-win-on-duplicate priority; the `searchPath{priority}=` (legacy) and `searchPath_{sku:02d}_{priority}=` (SWGSource) cfg dialects, noting the Phase-7 bundle writes priority 12 / sku 0. State that the loose searchPath is currently DISABLED in the user environment (project_swg_client_loose_overrides) so live render through it is best-effort (RESID-03 dependency, NOT a Phase-16 gate). List the concrete bucket filenames EXACTLY as the Task-2 suffix→bucket table names them (the parity-test contract — the doc's bucket-filename list and the verb's table are the same literal set).
    (1b) swg_export_manifest.json SCHEMA (CDX-NEW-10): define the manifest as a stable contract, not a fixture shape. Sourced from export_bundle.py, document: required fields (e.g. bundle_type, output_dir, assets), optional fields (client_cfg), the `rsp_files` shape (array of filenames/paths), the assets-block shape, allowed bundle_type values, the version field behavior (if present), and the UNKNOWN-FIELD policy (unknown top-level fields are tolerated/ignored, not a ParseError — so the validator is forward-compatible). State which fields validate-bundle requires vs. tolerates.
    (1c) validate-bundle exit / valid semantics (CDX-NEW-9): document that a STRUCTURALLY-valid bundle exits 0 EVEN WITH missing/rejected refs, and that agents MUST read the envelope `valid` (false when any findings) and `hasRejectedRefs` fields — exit-0 alone does NOT mean "validated clean". Only an unparseable manifest/.rsp is exit 2 (ParseError) and a missing path is exit 3.
    (2) Format-version matrix (RESEARCH B4): mirror TreVersion.cs EXACTLY — {0004,0005,0006,5000} READABLE (size-first, 5000 zlib TOC), 6000 ENUMERATE-ONLY (Restoration, encrypted payloads), any other version → UnsupportedVersion. CRITICAL Pitfall 4: do NOT write "5000 = encrypted" and do NOT call "COT2000" a TRE version — V5000 is readable and COT2000 is a master-index concept (CotMasterIndex), NOT a TreVersion enum member. The stale project_tre_version_support_gap memory is superseded by the shipped code. State that swg-blender-plugin writes within the Pre-CU 0004/0005/0006 family Utinni reads fully (no version mismatch on the bundle path).
    (3) Directory/bundle layout (RESEARCH B3): the `<bundle_root>/appearance/{mesh,skeleton,animation}/ + shader/ + texture/ + rsp/data_*.rsp + client_search_paths.cfg + swg_export_manifest.json` tree from export_bundle.py.
    (4) Anti-coupling rules (RESEARCH B5): Utinni READS, Blender WRITES; neither repo imports the other; no runtime dependency in either direction; Utinni owns format+injection, Blender owns DCC authoring; cites DEC-A3 + project_swg_toolchain_crosswalk; Utinni adds NO mesh/skeleton/animation/texture geometry codec (the decode-iff summaries stop at structural counts).
    Add a short section stating the open/preview reachability (D-07): parse-tre opens the .tre, decode-iff summarizes .msh/.mgn/.skt/.sht (count-only), inspect-iff gives any IFF chunk tree, and validate-bundle validates the text .rsp/.cfg/manifest (with bundle-root path containment that allows contained absolutes) — no new codec needed.
    Then ADD the doc↔verb parity test (C-17) to ValidateBundleTests.cs: a fact that reads the bucket-filename set from ValidateBundleCommand's named suffix→bucket table (reflection over the public/internal static table, OR a small public accessor exposing the filenames) and asserts every bucket filename appears as a literal in blender-boundary-contract.md (read the doc file from the repo root via a stable relative path; resolve up from AppContext.BaseDirectory or a repo-root probe). This fails if the doc drifts from the verb's bucket set. (This single parity fact may reference the now-shipped ValidateBundleCommand type — by Task 3 the type exists, so the WARNING-3 compile-boundary concern no longer applies; keep the Task-1 string-runner cases unchanged.)
  </action>
  <verify>
    <automated>cd D:/Code/Utinni && dotnet test Utinni.Cli.Tests --no-build --filter "Blender|ValidateBundle"</automated>
  </verify>
  <acceptance_criteria>
    - docs/ai/blender-boundary-contract.md exists and covers all four D-06 surfaces (.rsp contract, version matrix, bundle layout, anti-coupling) as distinct sections
    - The .rsp section states the `@` RHS explicit_path is an ABSOLUTE path and that validate-bundle allows a CONTAINED absolute ref while rejecting only escapes (CUR-NEW-3)
    - The doc defines the swg_export_manifest.json schema: required/optional fields, rsp_files shape, client_cfg, allowed bundle_type values, version behavior, and the unknown-field tolerance policy (CDX-NEW-10)
    - The doc documents the validate-bundle exit/valid semantics: exit-0-with-findings is possible; agents must read `valid`/`hasRejectedRefs` not exit code alone (CDX-NEW-9)
    - The version matrix lists 5000 as READABLE and 6000 as enumerate-only; the doc does NOT describe 5000 as encrypted and does NOT list "COT2000" as a TRE version (Pitfall 4)
    - The doc cites DEC-A3 and states Utinni adds no geometry codec
    - The doc names the concrete .rsp bucket filenames (e.g. data_compressed_mesh_static.rsp) — the SAME literal set as ValidateBundleCommand's suffix→bucket table — and the searchPath cfg dialects
    - ValidateBundleTests contains a doc↔verb parity fact asserting every bucket filename in ValidateBundleCommand's table appears in blender-boundary-contract.md; the fact passes (C-17)
    - `dotnet test Utinni.Cli.Tests --filter "Blender|ValidateBundle"` exits 0
  </acceptance_criteria>
  <done>The four-part boundary contract is documented (incl. the absolute-RHS .rsp rule, the manifest schema, and the exit/valid semantics), authoritative in Utinni, matches the shipped TreVersion.cs reality, and is kept in lockstep with the verb's bucket set by an automated parity test (replacing the prior bash grep gate — Windows-safe via dotnet, C-19).</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 4: Cross-repo pointer note in the Blender repo (D-05)</name>
  <what-built>
    The Utinni-authoritative contract doc at docs/ai/blender-boundary-contract.md (Task 3). This task adds a one-line pointer note in the SEPARATE swg-blender-plugin repo so the two never drift (D-05). swg-blender-plugin is a THIRD repo (not Utinni, not UtinniPlugins) — standing write authority does NOT extend here, so a human checkpoint confirms the cross-repo write/commit.
  </what-built>
  <how-to-verify>
    1. Review the proposed one-line addition to D:/Code/swg-blender-plugin/REFERENCES.md: a row in the existing "External references" table pointing at Utinni's docs/ai/blender-boundary-contract.md as the authoritative boundary contract (Utinni owns format+injection; Blender owns DCC authoring).
    2. Confirm you want this committed to the swg-blender-plugin repo. If approved, the executor adds the row and commits with a message referencing ECO-01.
    3. This is a pointer note ONLY — no code, no runtime coupling introduced.
  </how-to-verify>
  <read_first>
    - D:/Code/swg-blender-plugin/REFERENCES.md (the existing "External references" table to append a row to; PATTERNS analog lines 269-271)
  </read_first>
  <files>D:/Code/swg-blender-plugin/REFERENCES.md</files>
  <action>
    After human approval (resume-signal), append one row to the existing "External references" table in D:/Code/swg-blender-plugin/REFERENCES.md pointing at Utinni's docs/ai/blender-boundary-contract.md as the authoritative boundary contract (Utinni owns format+injection; Blender owns DCC authoring; neither repo imports the other). Pointer note ONLY — no code, no runtime coupling. Commit in the swg-blender-plugin repo with a message referencing ECO-01. Do NOT proceed without the approval signal (this is a third repo outside standing write authority).
  </action>
  <verify>
    <automated>cd D:/Code/swg-blender-plugin && git grep -q "blender-boundary-contract" -- REFERENCES.md && echo POINTER_OK</automated>
  </verify>
  <acceptance_criteria>
    - D:/Code/swg-blender-plugin/REFERENCES.md contains a row referencing docs/ai/blender-boundary-contract.md (the verify uses `git grep` — Windows-safe via the repo's bundled Git, no bash-only `grep`/`test -f` dependency; CUR-NEW-10. A PowerShell `Select-String REFERENCES.md -Pattern blender-boundary-contract` is an equivalent acceptable check if git grep is unavailable.)
    - The change is a one-line pointer note only (no code, no import of either repo into the other)
    - The note + commit happened only after the human approval signal
  </acceptance_criteria>
  <done>The Blender repo points at the Utinni-authoritative contract doc; the two cannot silently drift (D-05).</done>
  <resume-signal>Type "approved" to add + commit the pointer note, or describe a different placement/wording.</resume-signal>
</task>

</tasks>

<threat_model>
**Gate verdict: ASVS L1; no HIGH-severity threats; security gate passes.** (All threats below are mitigate or accept; none rated HIGH. Block-on-HIGH posture: had any threat been HIGH it would gate the wave — none are.)

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → validate-bundle | Blender-written bundle files (manifest/.rsp/.cfg) are read locally; untrusted path content (incl. absolute / `..`-traversal asset refs) crosses into the verb |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | ValidateBundleCommand path traversal in manifest / .rsp asset refs | mitigate | EVERY asset ref is classified through the shipped LooseOverridePath containment algorithm (Round-2 CDX-NEW-11/CUR-NEW-7): relative refs via a direct `LooseOverridePath.Resolve(bundleRoot, ref)` call; absolute refs via the mirrored canonicalize + trailing-separator OrdinalIgnoreCase StartsWith gate. A CONTAINED absolute (real-Blender `.rsp` RHS, CUR-NEW-3) is allowed + checked; an ESCAPING ref (`..`, drive/UNC outside bundle) is recorded as a rejectedRefs finding and NEVER File.Exists-probed. Symlink/junction targets are decided lexically (out of scope for the text validator; documented). Verb is read-only — no write, no execution. Acceptance: Task 2 containment criteria + Task 1 escaping (e) + contained-absolute (f) tests. |
| T-16-02 | DoS | Oversize/malformed manifest or .rsp | mitigate | Text parse with bounded line handling; a JSON-parse or .rsp-grammar failure returns exit-2 ParseError (no hang, no crash) per the InspectIff taxonomy. Acceptance: Task 1 malformed-manifest (c) + malformed-rsp (d) tests. |
| T-16-03 | Info disclosure | Contract doc accidentally states a false version capability (5000 encrypted / COT2000 as version) | mitigate (correctness) | Doc mirrors TreVersion.cs exactly (acceptance criterion in Task 3). |
| T-16-04 | Tampering | Silent fixture-refresh changes the cross-validation bytes | mitigate | SHA-256 of each pinned binary golden recorded in fixture-hashes.txt and re-asserted by the golden test; a byte change fails the test. Acceptance: Task 1 SHA-256 provenance fact (Codex finding). |
| T-16-05 | Info disclosure (correctness) | exit-0-with-findings misread by an agent as "validated clean" | mitigate | The success envelope carries explicit `valid` (false on any finding) + `hasRejectedRefs` fields; the contract doc states agents must read `valid`, not exit code alone (CDX-NEW-9). Acceptance: Task 2 envelope criterion + Task 1 (a)/(e) valid-field asserts + Task 3 doc statement. |
| T-16-AR | (accepted risk) | Local file reads of a maintainer-supplied bundle | accept | Single-user local desktop tool; the bundle is maintainer-authored Blender output (analog of AR-14-03). No network surface. |
</threat_model>

<verification>
- `dotnet test Utinni.Cli.Tests --filter Blender` green (parse-tre + decode-iff + SHA-256 cross-validation over pinned Blender bytes; assembly builds at the Task-1 boundary).
- `dotnet test Utinni.Cli.Tests --filter ValidateBundle` green (verb success+valid:true + error taxonomy + malformed manifest/rsp + ESCAPING-ref-reported-not-probed + CONTAINED-absolute-allowed + doc↔verb parity).
- docs/ai/blender-boundary-contract.md present, version-matrix-correct, manifest-schema-defined, absolute-RHS-rule + exit/valid semantics documented, bucket-filename-parity-asserted (dotnet test, not bash grep — C-19).
- Blender-repo pointer note added after human approval (Task 4; Windows-safe git grep verify — CUR-NEW-10).
</verification>

<success_criteria>
- ECO-01: the boundary is a documented file-format / .rsp search-path contract; Utinni opens/previews what Blender exports (reuse readers + one thin text verb with explicit path containment that allows contained absolutes); no runtime coupling (neither repo imports the other).
- Cross-validation proven: Blender writes, Utinni reads the same pinned bytes (D-08 / SC4), pinned by SHA-256 against silent drift.
- validate-bundle is traversal-safe (LooseOverridePath-delegated/mirrored containment; contained absolutes allowed, escapes rejected), emits an explicit valid/hasRejectedRefs envelope, and the doc↔verb bucket set stays in lockstep by test.
</success_criteria>

<output>
Create `.planning/phases/16-live-injected-mcp-bridge-blender-ecosystem-boundary/16-01-SUMMARY.md` when done.
</output>
