---
phase: 08-tjt-subpanel-iff-editor-read-write
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - Utinni.Cli/Commands/RoundtripIffCommand.cs
  - Utinni.Cli/Program.cs
  - Utinni.Cli.Tests/Commands/RoundtripIffCommandTests.cs
  - Utinni.Cli.Tests/Fixtures/iff/roundtrip/synthetic-nested.iff
  - Utinni.Cli.Tests/Fixtures/iff/roundtrip/synthetic-nested.expected.json
  - Utinni.Cli.Tests/Fixtures/iff/roundtrip/odd-chunk-no-pad.iff
  - Utinni.Cli.Tests/Fixtures/iff/roundtrip/odd-chunk-no-pad.expected.json
  - Utinni.Cli.Tests/Fixtures/iff/roundtrip/mutation-leaf-edit.iff
  - Utinni.Cli.Tests/Fixtures/iff/roundtrip/mutation-leaf-edit.expected.json
  - Utinni.Cli.Tests/Fixtures/iff/roundtrip/mutation-leaf-removed.iff
  - Utinni.Cli.Tests/Fixtures/iff/roundtrip/mutation-leaf-removed.expected.json
autonomous: true
requirements: [PROD-W1-IFF]
assumes:
  - "csproj coverage (round-2 HIGH-A): BOTH `Utinni.Cli.csproj` AND `Utinni.Cli.Tests.csproj` are SDK-style (`<Project Sdk=\"Microsoft.NET.Sdk\">` at line 1 of each). The default Compile glob (`**/*.cs`) picks up new files under each project tree automatically — therefore RoundtripIffCommand.cs (under `Utinni.Cli/Commands/`) and RoundtripIffCommandTests.cs (under `Utinni.Cli.Tests/Commands/`) need NO explicit `<Compile Include>` entries. Fixture files (`*.iff`, `*.expected.json`) are picked up by the existing `<Content Include=\"Fixtures\\**\\*\" CopyToOutputDirectory=\"PreserveNewest\" />` at `Utinni.Cli.Tests.csproj:31` — also no csproj edit needed."
  - "Same-wave file collisions (round-2 HIGH-A check): 08-02 (wave 2) touches `Utinni.Cli.csproj` (no edit needed) + `Utinni.Cli.Tests.csproj` (no edit needed); 08-03 (wave 2) touches `TheJawaToolboxDotNet.csproj`. ZERO csproj overlap — safe to parallel-execute."
must_haves:
  truths:
    - "A `roundtrip-iff` CLI verb parses -> builds the mutable DOM -> serializes -> emits a JSON envelope reporting byteExact identity — the CLI round-trip max-harness (D-02)"
    - "For an unmutated file the verb reports byteExact: true (the automated gate for Success Criterion 4)"
    - "An odd-length-chunk fixture round-trips byte-identical (no pad regression) through the verb"
    - "A `--mutate-leaf <leafId> <hex>` mode mutates exactly one leaf, asserts every OTHER serialized byte range is byte-identical to the original, and emits the per-byte-range identity report — strengthens Criterion 4 beyond the pure-identity case (08-REVIEWS folded-in: 08-02 leaf-edit mutation golden)"
    - "A `--remove-leaf <leafId>` mode removes exactly one leaf, asserts (a) byteExact: false at the top-level (structure changed), (b) byteExactExceptRemovedLeaf: true (every untouched leaf's byte range is unchanged), (c) the removed leaf's TypeID and parent container's child count is decremented by 1 — strengthens Criterion 4 across STRUCTURAL paths, not just payload-edit (round-2 MEDIUM item 8 / cursor N-M4: structural-op golden)"
    - "The verb follows the established exit-code contract (FileNotFound->3, IffParseException/IO->2, usage->1)"
  artifacts:
    - path: "Utinni.Cli/Commands/RoundtripIffCommand.cs"
      provides: "roundtrip-iff verb: parse -> FromDocument -> (optional --mutate-leaf | --remove-leaf) -> Write -> assert SequenceEqual / untouched-range identity / structural-removal identity; JSON envelope"
      contains: "roundtrip-iff"
    - path: "Utinni.Cli.Tests/Commands/RoundtripIffCommandTests.cs"
      provides: "Golden harness coverage incl. odd-length no-pad + one-leaf payload mutation + one-leaf STRUCTURAL removal golden (round-2 MEDIUM 8)"
      contains: "RoundtripIffCommandTests"
    - path: "Utinni.Cli.Tests/Fixtures/iff/roundtrip/odd-chunk-no-pad.iff"
      provides: "Odd-length-chunk no-pad round-trip golden fixture"
    - path: "Utinni.Cli.Tests/Fixtures/iff/roundtrip/mutation-leaf-edit.iff"
      provides: "Mutation golden: one-leaf payload edit; untouched byte ranges must remain identical"
    - path: "Utinni.Cli.Tests/Fixtures/iff/roundtrip/mutation-leaf-removed.iff"
      provides: "Structural-op golden: one-leaf removal; untouched leaves' byte ranges must remain identical (round-2 MEDIUM 8)"
  key_links:
    - from: "RoundtripIffCommand.Run"
      to: "IffWriter.Write(MutableIffDocument.FromDocument(...))"
      via: "the shared framework write path"
      pattern: "IffWriter\\.Write"
    - from: "Program.cs MapResult"
      to: "RoundtripIffCommand.Run"
      via: "verb dispatch"
      pattern: "RoundtripIffOptions"
---

<objective>
Add the D-02 max-harness: a `roundtrip-iff` CLI verb plus golden fixtures that parse -> (no-mutation OR
deterministic one-leaf payload mutation via `--mutate-leaf` OR deterministic one-leaf structural removal
via `--remove-leaf`) serialize -> re-parse and assert byte-exact identity on the untouched subtrees. This
is the automated gate for Success Criterion 4 ("no corruption of unedited chunks") and exercises the
SAME framework write path (from 08-01) the editor will use.

Round-2 MEDIUM item 8 (cursor N-M4) fold: the prior plan covered only one-leaf payload edit; the
mutation golden was therefore tight on payload paths but silent on structural-op paths. This revision
adds a `--remove-leaf` mode and a `mutation-leaf-removed.iff` + `.expected.json` golden so the gate
covers a structural op (D-03's remove) end-to-end through the CLI harness.

Purpose: live load/save/reload is Tier-4 manual residual; the CLI round-trip harness makes the
write-path correctness regression-testable in CI without a game client. The mutation goldens make the
gate cover the editor-only structural path AND the payload-edit path, not just pure identity.

Output: `RoundtripIffCommand.cs`, the `Program.cs` dispatch wiring, golden tests, round-trip
fixtures (including the odd-length no-pad regression case, the one-leaf payload mutation golden, AND the
one-leaf structural-removal golden).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-RESEARCH.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-PATTERNS.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-01-SUMMARY.md

<interfaces>
<!-- New framework write API from 08-01 (UtinniCoreDotNet/Formats/Iff): -->
MutableIffDocument.FromDocument(IffDocument doc, byte[] sourceBytes) -> MutableIffDocument
IffWriter.Write(MutableIffDocument doc) -> byte[]
IffReader.Read(Stream input) -> IffDocument   (existing)
// Leaf payload set (08-01): MutableIffNode.SetPayload(byte[] bytes)  — defensive copy on set
// Structural ops (08-01): MutableIffDocument.RemoveByStableId(string leafId) or equivalent on the parent container

<!-- Existing CLI verb pattern to mirror exactly (InspectIffCommand.cs): -->
[Verb("inspect-iff", HelpText="...")] public class InspectIffOptions { [Value(0, MetaName="path", Required=true)] public string Path { get; set; } }
public static int Run(InspectIffOptions o):
  if (!File.Exists(o.Path)) return JsonOutput.EmitError("inspect-iff","FileNotFound", "...", exitCode: 3);
  try { var doc = IffReader.Read(o.Path); return JsonOutput.EmitSuccess("inspect-iff", BuildResult(...)); }
  catch (IffParseException ex) { return JsonOutput.EmitError("inspect-iff", ex.Kind.ToString(), ex.Message, exitCode: 2); }
  catch (IOException ex) { return JsonOutput.EmitError("inspect-iff", "IoError", ex.Message, exitCode: 2); }
  // generic Exception intentionally NOT caught
BuildResult returns a JObject that NEVER pre-adds schemaVersion/command (those go at root via JsonOutput; REVIEWS HIGH-6); keys auto-sorted.

<!-- Program.cs dispatch (lines 43-55): add one type to ParseArguments<...> and one lambda to MapResult -->

<!-- csproj reference (round-2 HIGH-A — both CLI csproj are SDK-style; verified): -->
<!-- Utinni.Cli/Utinni.Cli.csproj line 1: <Project Sdk="Microsoft.NET.Sdk"> -->
<!-- Utinni.Cli.Tests/Utinni.Cli.Tests.csproj line 1: <Project Sdk="Microsoft.NET.Sdk"> -->
<!-- Default Compile glob auto-includes new .cs files; <Content Include="Fixtures\**\*"> at line 31 auto-copies new fixture files. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: roundtrip-iff verb + Program.cs dispatch + --mutate-leaf + --remove-leaf options</name>
  <read_first>
    - D:/Code/Utinni/Utinni.Cli/Commands/InspectIffCommand.cs (verb+options shape lines 35-40; Run + exit-code contract lines 56-80; BuildResult envelope discipline lines 82-99)
    - D:/Code/Utinni/Utinni.Cli/Program.cs (lines 43-55 ParseArguments + MapResult)
    - D:/Code/Utinni/Utinni.Cli/Output/JsonOutput.cs (EmitSuccess/EmitError; sorted-key + LF normalization)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-01-SUMMARY.md (final IffWriter / MutableIffDocument API)
    - D:/Code/Utinni/Utinni.Cli/Utinni.Cli.csproj (confirm SDK-style: line 1 = `<Project Sdk="Microsoft.NET.Sdk">`; default Compile glob picks up new files; NO csproj edit needed for the new verb file)
  </read_first>
  <action>
    Create `Utinni.Cli/Commands/RoundtripIffCommand.cs` (verbatim MIT banner) with a
    `[Verb("roundtrip-iff", HelpText = "Parse -> [optional mutate-leaf | remove-leaf] -> serialize -> re-parse an IFF; assert byte-exact untouched chunks.")]`
    `RoundtripIffOptions` (single `[Value(0, MetaName="path", Required=true)] string Path` + OPTIONAL
    `[Option("mutate-leaf")] string MutateLeafId` + OPTIONAL `[Option("mutate-hex")] string MutateHex` +
    OPTIONAL `[Option("remove-leaf")] string RemoveLeafId`). `--mutate-leaf` and `--remove-leaf` are
    mutually exclusive — usage error (exit 1) if both are supplied. `--mutate-hex` is required when
    `--mutate-leaf` is supplied; ignored otherwise.

    Static `RoundtripIffCommand.Run(RoundtripIffOptions o)` mirroring InspectIffCommand's structure:
    FileNotFound -> exit 3; `byte[] original = File.ReadAllBytes(o.Path)` ->
    `IffReader.Read(new MemoryStream(original))` -> `MutableIffDocument.FromDocument(doc, original)`. THEN
    branch:

    (a) **No mutation**: straight `IffWriter.Write(mutable)` -> compare `rewritten.SequenceEqual(original)`;
        result `{ byteExact: true|false, originalLength, rewrittenLength, source }`.

    (b) **`--mutate-leaf <leafId> --mutate-hex <hex>`**: locate the leaf by stable Id (the IffChunk `Id`
        format from 08-01) -> SetPayload with the parsed hex bytes -> `IffWriter.Write(mutable)` -> NOT
        byte-exact overall, but assert every untouched leaf's serialized byte range in `rewritten` is
        byte-identical to its range in `original` (use the leaf's original OffsetBytes + 8 + LengthBytes
        range from `IffDocument.AllNodesInPreorder` filtered to leaves minus the mutated leaf; for
        containers, the captured original slice is necessarily different because its length rolled up —
        DON'T compare container ranges; only untouched LEAF ranges). Result adds
        `mutatedLeafId, untouchedLeafRangesIdentical, untouchedLeafCount`.

    (c) **`--remove-leaf <leafId>`** (round-2 MEDIUM 8 / cursor N-M4): locate the leaf by stable Id;
        capture its TypeID and parent container's child count BEFORE removal; invoke the structural
        remove op on `MutableIffDocument` (RemoveByStableId or equivalent) -> `IffWriter.Write(mutable)`
        -> re-parse the rewritten bytes back into an `IffDocument`. Assert/report:
          - `byteExact: false` (always true — structure changed; the whole-file SequenceEqual obviously
            fails)
          - `byteExactExceptRemovedLeaf: true` IFF every leaf in the re-read tree whose stable Id
            matches an original UNTOUCHED leaf has the same Data bytes AND the same TypeID as the
            corresponding original leaf
          - `removedLeafId` = the supplied leaf id; `removedLeafTypeId` = the TypeID captured before
            removal
          - `parentContainerChildCountBefore` and `parentContainerChildCountAfter` (after - before == -1
            for a valid removal)
          - `untouchedLeafCount` = N - 1 where N was the original tree's leaf count
        If `byteExactExceptRemovedLeaf == false`, the verb still exits 0 (the report is the value); the
        test asserts equality against the expected.json.

    Emit `JsonOutput.EmitSuccess("roundtrip-iff", result)`. Keys auto-sort. Catch `IffParseException` ->
    exit 2 with `ex.Kind.ToString()`; catch `IOException` -> exit 2 "IoError"; usage error (unknown leaf
    id; both --mutate-leaf and --remove-leaf supplied; --mutate-leaf without --mutate-hex) -> exit 1;
    do NOT catch generic Exception.

    Then edit `Program.cs`: add `Commands.RoundtripIffOptions,` to the `ParseArguments<...>` type list
    and `(Commands.RoundtripIffOptions o) => Commands.RoundtripIffCommand.Run(o),` to the `MapResult`
    chain (PATTERNS lines 195-200).

    NO csproj edit: `Utinni.Cli.csproj` is SDK-style (line 1 verified) with the default `**/*.cs` glob
    — `RoundtripIffCommand.cs` auto-includes.
  </action>
  <verify>
    <automated>grep -c "roundtrip-iff" Utinni.Cli/Commands/RoundtripIffCommand.cs</automated>
  </verify>
  <acceptance_criteria>
    - RoundtripIffCommand.cs contains the literal `[Verb("roundtrip-iff"` and `public static int Run(RoundtripIffOptions`
    - RoundtripIffCommand.cs calls `IffWriter.Write` on a `MutableIffDocument.FromDocument(...)` result
    - The result JObject contains keys byteExact, originalLength, rewrittenLength, source and does NOT pre-add schemaVersion/command
    - When `--mutate-leaf` is supplied, the result also contains mutatedLeafId, untouchedLeafRangesIdentical, untouchedLeafCount
    - When `--remove-leaf` is supplied, the result also contains removedLeafId, removedLeafTypeId, byteExactExceptRemovedLeaf, parentContainerChildCountBefore, parentContainerChildCountAfter, untouchedLeafCount
    - Both --mutate-leaf and --remove-leaf supplied returns exit code 1 (usage error)
    - Program.cs lists `RoundtripIffOptions` in ParseArguments and maps it to `RoundtripIffCommand.Run`
    - Exit codes: FileNotFound->3, IffParseException/IOException->2; usage (unknown leaf id; mutually-exclusive options) -> 1; generic Exception not caught
    - `Utinni.Cli.csproj` is NOT modified (SDK-style glob handles new .cs); `git status --porcelain Utinni.Cli/Utinni.Cli.csproj` shows the file is not in the working tree changes
    - VS2026 MSBuild of Utinni.Cli compiles clean
  </acceptance_criteria>
  <done>The roundtrip-iff verb exists, is dispatched from Program.cs, supports no-mutation byteExact mode + the one-leaf payload mutation mode + the one-leaf structural removal mode, and reports each via the standard JSON envelope using the shared 08-01 write path.</done>
</task>

<task type="auto">
  <name>Task 2: Round-trip golden fixtures + tests (identity, no-pad, payload-mutation, AND structural-removal)</name>
  <read_first>
    - D:/Code/Utinni/Utinni.Cli.Tests/Commands/InspectIffCommandTests.cs (Theory + golden compare lines 48-73; InProcessCliRunner + MaskPath)
    - D:/Code/Utinni/Utinni.Cli.Tests/Infrastructure/GoldenTestRunner.cs (Matches lines 40-52, DeepEquals + mismatch dump)
    - D:/Code/Utinni/Utinni.Cli.Tests/Infrastructure/IffBuilder.cs (Form/Leaf builders lines 88-131 — Leaf emits no trailing pad; Container adds a parent-counted pad ONLY for reader fixtures)
    - D:/Code/Utinni/Utinni.Cli.Tests/Fixtures/iff/odd-chunk-no-pad.iff (the no-pad model fixture to mirror)
    - D:/Code/Utinni/Utinni.Cli.Tests/Utinni.Cli.Tests.csproj (confirm SDK-style line 1; default `**/*.cs` glob handles new test .cs; existing `<Content Include="Fixtures\**\*" CopyToOutputDirectory="PreserveNewest" />` at line 31 handles new fixture files — NO csproj edit needed)
  </read_first>
  <action>
    Create `Utinni.Cli.Tests/Commands/RoundtripIffCommandTests.cs` (verbatim banner) mirroring
    `InspectIffCommandTests`: run in-process via `InProcessCliRunner.Run("roundtrip-iff", fixturePath, ...)`,
    `MaskPath` the absolute path, assert exit code 0, then
    `GoldenTestRunner.Matches("iff/roundtrip/" + name, masked)`. Add fixtures under
    `Utinni.Cli.Tests/Fixtures/iff/roundtrip/`:

    (1) a nested no-mutation fixture (`synthetic-nested.iff`) synthesized via `IffBuilder.Form`/`.Leaf`
        encoding SWG no-pad input, with a `synthetic-nested.expected.json` golden showing `byteExact: true`;

    (2) an odd-length-chunk fixture (`odd-chunk-no-pad.iff`, modeled on the existing
        Fixtures/iff/odd-chunk-no-pad.iff) round-tripped to byte-identity with its `expected.json`;

    (3) (FOLDED FROM ROUND-1 08-REVIEWS) a `mutation-leaf-edit.iff` (a nested fixture with several
        leaves) + `mutation-leaf-edit.expected.json` golden: invoke the verb with
        `--mutate-leaf <one-known-leaf-id> --mutate-hex <some-deterministic-hex>`; the golden shows
        `byteExact: false`, `untouchedLeafRangesIdentical: true`, `untouchedLeafCount: <N-1>`. This
        proves Criterion 4 holds across an edited PAYLOAD write, not just the pure-identity case.

    (4) (NEW: round-2 MEDIUM 8 / cursor N-M4) a `mutation-leaf-removed.iff` (a nested fixture with at
        least 3 leaves under one container so the parent's child count goes from 3->2) +
        `mutation-leaf-removed.expected.json` golden: invoke the verb with
        `--remove-leaf <one-known-leaf-id>`; the golden shows `byteExact: false`,
        `byteExactExceptRemovedLeaf: true`, `removedLeafId: <id>`, `removedLeafTypeId: <4-char tag>`,
        `parentContainerChildCountBefore: 3`, `parentContainerChildCountAfter: 2`, `untouchedLeafCount`
        equal to the original leaf count minus 1. This proves Criterion 4 holds across a STRUCTURAL op,
        not just payload edits — closing the cursor N-M4 gap that the prior plan left open.

    Mark fixtures `Content`/`CopyToOutputDirectory` consistent with the existing iff fixtures so the test
    runner resolves them via `FixturePath.Resolve`. The existing
    `<Content Include="Fixtures\**\*" CopyToOutputDirectory="PreserveNewest" />` line in
    `Utinni.Cli.Tests.csproj` (line 31) ALREADY globs the new `roundtrip/` subdirectory — NO csproj edit
    needed.

    NO csproj edit: `Utinni.Cli.Tests.csproj` is SDK-style (line 1 verified) with the default `**/*.cs`
    glob — `RoundtripIffCommandTests.cs` auto-includes; fixture files are picked up by the existing
    `<Content Include="Fixtures\**\*">` line.
  </action>
  <verify>
    <automated>dotnet test Utinni.Cli.Tests --no-build --filter "FullyQualifiedName~Roundtrip"</automated>
  </verify>
  <acceptance_criteria>
    - RoundtripIffCommandTests.cs exists and exercises the verb in-process via InProcessCliRunner
    - Fixtures/iff/roundtrip/ contains synthetic-nested.{iff,expected.json}, odd-chunk-no-pad.{iff,expected.json}, mutation-leaf-edit.{iff,expected.json}, AND mutation-leaf-removed.{iff,expected.json}
    - The odd-chunk golden proves originalLength == rewrittenLength and byteExact: true (no pad regression)
    - The payload-mutation golden proves byteExact: false AND untouchedLeafRangesIdentical: true AND untouchedLeafCount == N-1
    - The structural-removal golden proves byteExact: false AND byteExactExceptRemovedLeaf: true AND parentContainerChildCountAfter == parentContainerChildCountBefore - 1 AND untouchedLeafCount == N-1 (round-2 MEDIUM 8 closed)
    - `dotnet test Utinni.Cli.Tests --no-build --filter "FullyQualifiedName~Roundtrip"` exits 0 after a VS2026 MSBuild
    - The expected.json files mask the absolute path (MaskPath) so they are machine-independent
    - `Utinni.Cli.Tests.csproj` is NOT modified (SDK-style globs handle .cs + fixture files)
  </acceptance_criteria>
  <done>Golden round-trip tests pass, proving byte-exact identity for untouched chunks, no pad regression for odd-length chunks, one-leaf payload mutation preserves every other leaf's byte range, AND a one-leaf structural removal preserves every untouched leaf's byte range — the automated Criterion 4 gate, strengthened across BOTH payload-edit and structural paths (round-2 MEDIUM 8 closed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crafted .iff path argument -> RoundtripIffCommand | untrusted file path + binary content crosses into the CLI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Denial of Service | roundtrip-iff parsing a malformed/huge IFF | mitigate | Reuses IffReader's bounds checks + 64 MB cap (08-01); IffParseException -> exit 2, never a crash |
| T-08-05 | Information Disclosure | absolute path leaking into golden JSON | mitigate | MaskPath the path in the test envelope (the established InspectIff test pattern) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No external packages added this plan (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- VS2026 MSBuild of Utinni.Cli + Utinni.Cli.Tests compiles clean.
- `dotnet test Utinni.Cli.Tests --no-build --filter "FullyQualifiedName~Roundtrip"` exits 0.
- `roundtrip-iff <fixture>` reports byteExact: true for an unmutated nested fixture.
- `roundtrip-iff <fixture> --mutate-leaf <id> --mutate-hex <hex>` reports byteExact: false but untouchedLeafRangesIdentical: true.
- `roundtrip-iff <fixture> --remove-leaf <id>` reports byteExact: false but byteExactExceptRemovedLeaf: true (round-2 MEDIUM 8).
- Neither CLI csproj is modified (both SDK-style; default globs handle new files).
</verification>

<success_criteria>
- roundtrip-iff verb dispatched and emitting the standard JSON envelope.
- byteExact: true for untouched files; odd-length chunks round-trip byte-identical; payload-mutation golden proves untouched-leaf-range identity on an edited write; structural-removal golden proves untouched-leaf-range identity on a remove (round-2 MEDIUM 8).
- Golden tests run in CI (the existing Utinni.Cli.Tests lane).
</success_criteria>

<output>
Create `.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-02-SUMMARY.md` when done (include a one-line confirmation that the structural-removal golden closes round-2 MEDIUM 8 / cursor N-M4).
</output>
</content>
