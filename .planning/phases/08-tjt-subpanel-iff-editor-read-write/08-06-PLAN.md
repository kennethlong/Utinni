---
phase: 08-tjt-subpanel-iff-editor-read-write
plan: 06
type: execute
wave: 5
depends_on: ["08-05"]
files_modified:
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/LivePatchSaveTarget.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormSaveConfirmDialog.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormSaveConfirmDialog.Designer.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"
  - UtinniCoreDotNet/Editing/LivePatchValidator.cs
  - UtinniCoreDotNet/UtinniCoreDotNet.csproj
  - UtinniCoreDotNet.Tests/EditingTests/LivePatchValidatorTests.cs
autonomous: false
requirements: [PROD-W1-IFF]
assumes:
  - "Shrink-safety V1 disposition: SAME-LENGTH-ONLY patches (08-REVIEWS HIGH-3 recommended option). Refuse `newLength != originalMappedLength`. Documents the follow-up phase for shrink-safe patches. CONTEXT D-05.3 already uplifted 2026-05-28 (commit 33cd3b9) to capture this — no further uplift needed."
  - "Provenance: live patch is gated on `FormIffEditor.Source is OpenSource.ClientMemory` (08-REVIEWS HIGH-2). Since no current open path in 08-04/08-05 constructs ClientMemory, the menu item REMAINS DISABLED in this phase. The infrastructure ships ready for a follow-up phase that wires a discovery path. The phase still satisfies D-05.3 as a hard V1 must-have at the IMPLEMENTATION-COMPLETE + UNIT-TESTED-BOUNDS-GATE level (round-2 HIGH-B fold: LivePatchValidator pure function + xUnit test)."
  - "Cross-repo (round-2 MEDIUM 3): this plan edits both Utinni (`UtinniCoreDotNet/Editing/LivePatchValidator.cs` + its csproj entry + its test) AND UtinniPlugins (LivePatchSaveTarget.cs, FormSaveConfirmDialog.cs/.Designer.cs, FormIffEditor.cs + their csproj entries). Execute in the same commit chain; verify `UtinniCoreDotNet.dll` is rebuilt before TheJawaToolbox builds against it. The `UtinniCoreDotNet/Generated/UtinniCore.cs` file shows as `M` in the working tree at planning time — the executor MUST confirm it's committed OR explicitly note the working-tree state in the SUMMARY before referencing any binding line citations (`Memory.memory.Copy` at line 702, etc.)."
  - "csproj coverage (round-2 HIGH-A): `UtinniCoreDotNet.csproj` is OLD-STYLE explicit-compile — `Editing\\LivePatchValidator.cs` requires an explicit `<Compile Include>` entry adjacent to 08-04's `Editing\\IffEditController.cs` entry. `TheJawaToolboxDotNet.csproj` is OLD-STYLE — `Saving\\LivePatchSaveTarget.cs` AND the two FormSaveConfirmDialog files (.cs with `<SubType>Form</SubType>` + .Designer.cs with `<DependentUpon>FormSaveConfirmDialog.cs</DependentUpon>`) ALL require explicit `<Compile Include>` entries. `UtinniCoreDotNet.Tests.csproj` is SDK-style — the new `LivePatchValidatorTests.cs` auto-globs (under `EditingTests/` — not in the `Fixtures\\**` exclusion); NO test-csproj edit needed."
  - "ROUND-2 HIGH-B (LivePatchValidator + unit tests): CONTEXT D-05.3 explicitly claims the live-patch infra is 'implementation-complete and unit-tested for its bounds gate.' The prior plan had no xUnit task — only grep gates + Tier-4 smoke. This revision extracts the validation logic from `LivePatchSaveTarget` into a pure function (`UtinniCoreDotNet.Editing.LivePatchValidator.Validate(...)`) and adds `LivePatchValidatorTests.cs` covering the five required cases (no-client, zero-target, growth-length, shrink-length, same-length-happy-path). This makes the CONTEXT claim true and matches it with plan-level verification."
must_haves:
  truths:
    - "**D-05.3 reduced-mode acceptance** (round-2 MEDIUM 11 / cursor N-M7): D-05.3 is **infra-ready, user-disabled** in this phase. `LivePatchSaveTarget` + `LivePatchValidator` are implemented and xUnit-tested for the bounds gate; the menu item ships **disabled** with the tooltip 'Live patch requires opening from client memory — not wired in this phase.' Full user-reachable acceptance is deferred to a follow-up phase that wires the `ClientMemory` open path. PROD-W1-IFF Criterion 2 is acceptance-tested against modes 1/2/4 in Phase 8 with mode 3 documented as infra-ready."
    - "The editor can patch the loaded IFF bytes directly in mapped client memory (D-05.3 implementation complete) — when `FormIffEditor.Source is OpenSource.ClientMemory cm`, gated behind a confirm dialog and Game.IsRunning"
    - "Every mapped-memory write goes through Memory.memory.Copy on the game thread (CON-N-04 VirtualProtect bracket; never the UI thread)"
    - "SAME-LENGTH-ONLY (V1, 08-REVIEWS HIGH-3): refuse `rewritten.Length != cm.OriginalMappedLength` with the candid copy 'Live patch requires the rewritten IFF to be the same length as the original. Save to file/repack instead.' — refuses BOTH growth AND shrink (closes the stale-tail-bytes corruption vector)"
    - "The patch length/bounds and `targetAddr != IntPtr.Zero` are validated BEFORE the write (no over-/under-/zero-target write into client memory)"
    - "Bounds-gate validation is EXTRACTED into a pure function `UtinniCoreDotNet.Editing.LivePatchValidator.Validate(IntPtr targetAddr, int originalMappedLength, int rewrittenLength, bool gameIsRunning)` returning a `LivePatchValidation` enum (Ok / RefusedNoClient / RefusedZeroTarget / RefusedSameLength). `LivePatchSaveTarget` consumes this pure function and only proceeds to `AddMainLoopCall` + `Memory.memory.Copy` if the result is `Ok`. Round-2 HIGH-B fold."
    - "The bounds gate is unit-tested in `UtinniCoreDotNet.Tests/EditingTests/LivePatchValidatorTests.cs` with ≥ 5 test cases: Validate_NoClient_RefusedNoClient (gameIsRunning=false), Validate_ZeroTarget_RefusedZeroTarget (targetAddr=0), Validate_GrowthLength_RefusedSameLength (rewrittenLength > originalMappedLength), Validate_ShrinkLength_RefusedSameLength (rewrittenLength < originalMappedLength), Validate_SameLengthHappyPath_Ok (all conditions met). Round-2 HIGH-B fold."
    - "The live-patch confirm dialog uses explicit-verb buttons with Color.Red emphasis and states the change is volatile (lost on reload)"
    - "The menu item is DISABLED this phase pending a follow-up open path that constructs OpenSource.ClientMemory; the disabled tooltip is honest ('Live patch requires opening from client memory — not wired in this phase')"
    - "UtinniCoreDotNet.csproj contains a `<Compile Include=\"Editing\\LivePatchValidator.cs\" />` entry — round-2 HIGH-A"
    - "TheJawaToolboxDotNet.csproj contains explicit `<Compile Include>` entries for LivePatchSaveTarget.cs + FormSaveConfirmDialog.cs (Form SubType) + FormSaveConfirmDialog.Designer.cs (DependentUpon) — round-2 HIGH-A"
  artifacts:
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/LivePatchSaveTarget.cs"
      provides: "In-memory live patch via game-thread Memory.memory.Copy (CON-N-04); consumes LivePatchValidator.Validate for the bounds gate; same-length-only V1"
      contains: "AddMainLoopCall"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormSaveConfirmDialog.cs"
      provides: "Risk-proportional confirm modal for the live patch (and reused by repack)"
      contains: "class FormSaveConfirmDialog"
    - path: "UtinniCoreDotNet/Editing/LivePatchValidator.cs"
      provides: "Pure function bounds-gate for the in-memory live patch (round-2 HIGH-B): no WinForms, no native interop, no plugin-only refs — same disposition as IffEditController's framework-side placement (08-REVIEWS MEDIUM-5)"
      contains: "Validate"
    - path: "UtinniCoreDotNet.Tests/EditingTests/LivePatchValidatorTests.cs"
      provides: "xUnit coverage of all five required bounds-gate cases (round-2 HIGH-B). Auto-globbed by SDK-style test csproj."
      contains: "LivePatchValidatorTests"
    - path: "UtinniCoreDotNet/UtinniCoreDotNet.csproj"
      provides: "Old-style explicit-compile project: MUST list `Editing\\LivePatchValidator.cs` — round-2 HIGH-A coverage gate"
      contains: "Editing\\LivePatchValidator.cs"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"
      provides: "Old-style explicit-compile project: MUST list LivePatchSaveTarget.cs + FormSaveConfirmDialog.cs (Form SubType) + FormSaveConfirmDialog.Designer.cs (DependentUpon) — round-2 HIGH-A coverage gates"
      contains: "Saving\\LivePatchSaveTarget.cs"
  key_links:
    - from: "LivePatchSaveTarget"
      to: "Memory.memory.Copy"
      via: "game-thread CON-N-04 bracketed write"
      pattern: "memory\\.Copy"
    - from: "LivePatchSaveTarget"
      to: "UtinniCoreDotNet.Editing.LivePatchValidator.Validate"
      via: "pure function bounds-gate before AddMainLoopCall (round-2 HIGH-B)"
      pattern: "LivePatchValidator\\.Validate"
    - from: "FormIffEditor Save▾ -> Patch live client"
      to: "Source is OpenSource.ClientMemory cm -> FormSaveConfirmDialog -> LivePatchSaveTarget"
      via: "provenance-gated confirmed live patch"
      pattern: "OpenSource\\.ClientMemory"
---

<objective>
Add the isolated, higher-risk in-memory live-patch save mode (D-05.3): write the loaded IFF's edited
bytes straight into mapped client memory via the CON-N-04 `Memory.memory.Copy` VirtualProtect bracket
on the game thread, behind a risk-proportional confirm dialog AND gated on
`FormIffEditor.Source is OpenSource.ClientMemory` (08-REVIEWS HIGH-2). V1 enforces SAME-LENGTH-ONLY
patches (08-REVIEWS HIGH-3) — refusing both growth AND shrink closes the stale-tail-bytes corruption
vector and is the simplest defensible V1 constraint per Codex/Cursor consensus. This is the 8b isolated
plan — it touches live client memory and is validated by live-SWG smoke only.

**Round-2 HIGH-B fold (CONTEXT D-05.3 claim alignment):** CONTEXT D-05.3 explicitly claims the live-patch
infra is "implementation-complete and unit-tested for its bounds gate." The prior plan had no xUnit
task — only grep gates + Tier-4 smoke. This revision extracts the validation logic from
`LivePatchSaveTarget` into a pure function (`UtinniCoreDotNet.Editing.LivePatchValidator.Validate(...)`)
and adds `LivePatchValidatorTests.cs` covering five required cases. This makes the CONTEXT claim true
and matches it with plan-level verification.

**Round-2 MEDIUM 11 fold (D-05.3 reduced-mode completion semantics):** the `must_haves` now explicitly
records D-05.3 as **infra-ready, user-disabled** — the menu ships disabled with the honest tooltip
"Live patch requires opening from client memory — not wired in this phase." PROD-W1-IFF Criterion 2
is acceptance-tested against modes 1/2/4 in Phase 8 with mode 3 documented as infra-ready. This avoids
the "100% D-05" false claim cursor N-M7 flagged.

Purpose: D-05.3 is a hard V1 must-have (instant in-session feedback; volatile, lost on reload). It is
isolated per the D-05 PLAN-SPLIT FLAG because the mapped-memory write carries distinct failure modes
(AV / device-lost / client crash) from the file save modes.

**Note on phase scope:** No current Phase-8 open path constructs `OpenSource.ClientMemory` (acknowledged
in 08-01 Task 3 + 08-REVIEWS HIGH-2(b)). The implementation is COMPLETE and unit-tested for its bounds
gate (round-2 HIGH-B), but the menu remains DISABLED in this phase until a follow-up phase wires a
discovery path. The phase is otherwise satisfied at the implementation-ready level for D-05.3.

Output: `LivePatchValidator.cs` (framework-side, pure-managed — round-2 HIGH-B) + csproj entry,
`LivePatchValidatorTests.cs` (xUnit, auto-globbed by SDK-style test csproj), `LivePatchSaveTarget.cs`
(consumes `LivePatchValidator.Validate`), `FormSaveConfirmDialog.cs`/`.Designer.cs`, the `Save ▾`
wiring + provenance gate in `FormIffEditor`, csproj entries for all UtinniPlugins additions, and a
live-SWG smoke checkpoint that is currently exercised only when a ClientMemory open path exists.
Cross-repo: validator + test + csproj in Utinni; save target + dialog + form wiring + csproj in
UtinniPlugins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-CONTEXT.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-RESEARCH.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-PATTERNS.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-UI-SPEC.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-REVIEWS.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-05-SUMMARY.md

<interfaces>
<!-- CON-N-04 mapped-memory write binding (UtinniCore.Memory.memory): -->
UtinniCore.Memory.memory.Copy(uint pDest, uint pSource, uint length)   // Generated/UtinniCore.cs ~702; native ?copy@memory@@YAXIII@Z
  // The VirtualProtect save/restore bracket is INSIDE the native memory::copy — do NOT hand-roll a write that skips it.
GameCallbacks.AddMainLoopCall(Action)                                  // game-thread marshaling (GameCallbacks.cs 254-257)
Game.IsRunning                                                         // gate before any live-client touch

<!-- 08-01 write path produces the bytes to patch + OpenSource provenance: -->
IffWriter.Write(MutableIffDocument) -> byte[]
OpenSource.ClientMemory { IntPtr TargetAddr; int OriginalMappedLength; string LogicalPath; }

<!-- Round-2 HIGH-B pure-function bounds gate (framework-side, UtinniCoreDotNet/Editing/): -->
namespace UtinniCoreDotNet.Editing {
  public enum LivePatchValidation { Ok, RefusedNoClient, RefusedZeroTarget, RefusedSameLength }
  public static class LivePatchValidator {
    public static LivePatchValidation Validate(IntPtr targetAddr, int originalMappedLength, int rewrittenLength, bool gameIsRunning);
  }
}
// Same disposition as IffEditController (framework-side, pure-managed, no WinForms, no native interop)
// — eligible for framework placement so it's testable on the CI runner without an injected client.

<!-- Small-modal analog: UtinniCoreDotNet/UI/Forms/FormHotkeyEditorDialog.cs (partial UtinniForm + buttons + public result) -->
<!-- UI-SPEC Destructive: live-patch confirm heading "Patch the live client in memory?" buttons "Patch live"/"Cancel", Color.Red emphasis. -->

<!-- csproj insertion-point references (round-2 HIGH-A — both csproj are OLD-STYLE): -->
<!-- UtinniCoreDotNet.csproj: 08-04 Task 1 added `<Compile Include="Editing\IffEditController.cs" />` adjacent to lines 204-205 UndoRedo cluster. -->
<!--                          New `<Compile Include="Editing\LivePatchValidator.cs" />` goes immediately adjacent.                                  -->
<!-- TheJawaToolboxDotNet.csproj: 08-05 Task 2 added Saving\IffSaveTargets.cs; 08-05 Task 3 added Saving\ClientReloadDispatcher.cs. -->
<!--                              New `<Compile Include="Saving\LivePatchSaveTarget.cs" />` joins that cluster.                       -->
<!--                              FormSaveConfirmDialog mirrors the FormTreBrowser pattern (lines 75-80): `.cs` with `<SubType>Form</SubType>`, `.Designer.cs` with `<DependentUpon>FormSaveConfirmDialog.cs</DependentUpon>`. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FormSaveConfirmDialog — risk-proportional confirm modal + TheJawaToolboxDotNet.csproj entries (round-2 HIGH-A)</name>
  <read_first>
    - D:/Code/Utinni/UtinniCoreDotNet/UI/Forms/FormHotkeyEditorDialog.cs (small UtinniForm modal: InitializeComponent, public result field, explicit buttons)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-UI-SPEC.md (Destructive/risky actions table; Color.Red emphasis; explicit-verb buttons — never bare OK/Cancel; optional repack "back up first" checkbox)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj (OLD-STYLE explicit-compile; mirror the FormTreBrowser cluster pattern at lines 75-80 for the form .cs + .Designer.cs entries)
  </read_first>
  <action>
    PART A — Create the form files.
    Create `UI/Forms/FormSaveConfirmDialog.cs` + `.Designer.cs` (verbatim MIT banner) as a small
    `UtinniForm` modal parameterized by heading, body, and the two explicit verb-button captions
    (returns a `DialogResult`-style outcome via a public field). Render the body emphasis in `Color.Red`
    per UI-SPEC. For the live patch it is invoked with heading `Patch the live client in memory?`, body
    `This writes your edits straight into the running client. The change is temporary (lost on reload)
    and can destabilize the session. Continue?`, buttons `Patch live` / `Cancel`. Build it generically so
    08-07 can reuse it for the repack confirm (heading/body/buttons + an optional "back up first"
    checkbox). Use `Colors.*()` accessors plus the documented `Color.Red` destructive exception only.

    PART B — csproj coverage (round-2 HIGH-A).
    Edit `../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj`. Inside
    the existing `<ItemGroup>` block at lines 64-132, add TWO new entries (mirror the FormTreBrowser
    cluster pattern at lines 75-80):

      <Compile Include="UI\Forms\FormSaveConfirmDialog.cs">
        <SubType>Form</SubType>
      </Compile>
      <Compile Include="UI\Forms\FormSaveConfirmDialog.Designer.cs">
        <DependentUpon>FormSaveConfirmDialog.cs</DependentUpon>
      </Compile>

    Both `<SubType>Form</SubType>` and `<DependentUpon>` are REQUIRED — they mirror the FormTreBrowser
    pattern exactly. Do NOT add a `<EmbeddedResource>` entry (no .resx, same as FormTreBrowser to avoid
    MSB3823).
  </action>
  <verify>
    <automated>grep -c "class FormSaveConfirmDialog" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormSaveConfirmDialog.cs" && grep -c '<Compile Include="UI\\\\Forms\\\\FormSaveConfirmDialog.cs"' "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"</automated>
  </verify>
  <acceptance_criteria>
    - FormSaveConfirmDialog.cs + .Designer.cs exist with the MIT banner and `class FormSaveConfirmDialog`
    - The dialog is parameterized by heading/body/two explicit verb captions and exposes the chosen outcome
    - Body emphasis uses Color.Red; buttons are explicit verbs (no bare OK/Cancel)
    - It is reusable for the repack confirm (08-07) with an optional checkbox
    - `TheJawaToolboxDotNet.csproj` contains BOTH `<Compile Include="UI\Forms\FormSaveConfirmDialog.cs"><SubType>Form</SubType></Compile>` AND `<Compile Include="UI\Forms\FormSaveConfirmDialog.Designer.cs"><DependentUpon>FormSaveConfirmDialog.cs</DependentUpon></Compile>` (grep gates: each returns 1; round-2 HIGH-A)
    - No `<EmbeddedResource>` entry is added (no .resx — mirrors FormTreBrowser; avoids MSB3823)
    - VS2026 MSBuild of TheJawaToolbox compiles clean
  </acceptance_criteria>
  <done>A reusable, risk-proportional confirm modal exists with explicit verb buttons and Color.Red emphasis; explicit csproj entries with correct SubType + DependentUpon added (round-2 HIGH-A).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LivePatchValidator pure function + xUnit bounds-gate tests (ROUND-2 HIGH-B fold) + UtinniCoreDotNet.csproj entry (round-2 HIGH-A)</name>
  <read_first>
    - D:/Code/Utinni/UtinniCoreDotNet/Editing/IffEditController.cs (from 08-04 Task 1 — framework-side pure-managed placement reference)
    - D:/Code/Utinni/UtinniCoreDotNet/Formats/Iff/OpenSource.cs (08-01 Task 3 — ClientMemory case; the validator parameters come from a ClientMemory descriptor)
    - D:/Code/Utinni/UtinniCoreDotNet/Formats/Iff/IffChunk.cs (verbatim MIT+provenance banner — first 27 lines)
    - D:/Code/Utinni/UtinniCoreDotNet/UtinniCoreDotNet.csproj (OLD-STYLE; 08-04 Task 1 added `<Compile Include="Editing\IffEditController.cs" />` adjacent to lines 204-205. New `<Compile Include="Editing\LivePatchValidator.cs" />` goes immediately adjacent.)
    - D:/Code/Utinni/UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj (SDK-style; the new test file under `EditingTests/` auto-globs — NO csproj edit needed)
  </read_first>
  <behavior>
    - `LivePatchValidator.Validate(IntPtr targetAddr, int originalMappedLength, int rewrittenLength, bool gameIsRunning)` returns a `LivePatchValidation` enum value:
      - `RefusedNoClient` when `gameIsRunning == false`
      - `RefusedZeroTarget` when `gameIsRunning == true && targetAddr == IntPtr.Zero` (treat IntPtr.Zero as the only zero-target; do NOT also check `(long)targetAddr == 0` separately — `IntPtr.Zero` already equals `new IntPtr(0)`)
      - `RefusedSameLength` when `gameIsRunning == true && targetAddr != IntPtr.Zero && rewrittenLength != originalMappedLength` (refuses BOTH growth AND shrink — 08-REVIEWS HIGH-3 lock)
      - `Ok` when `gameIsRunning == true && targetAddr != IntPtr.Zero && rewrittenLength == originalMappedLength`
    - The validator is a PURE FUNCTION: no I/O, no native interop, no WinForms, no GameCallbacks reference. It just inspects its four parameters and returns one of the four enum values.
    - Determinism: the order of refusal checks is fixed (no-client first, then zero-target, then length) so a `RefusedNoClient` result on a zero-target-and-wrong-length input is unambiguously NoClient (the caller learns the most-blocking failure mode first).
  </behavior>
  <action>
    PART A — Create the validator file.
    Create `UtinniCoreDotNet/Editing/LivePatchValidator.cs` (verbatim MIT+provenance banner). Define the
    `LivePatchValidation` enum and the static `LivePatchValidator.Validate(...)` method per
    `<behavior>`. The validator MUST NOT reference `System.Windows.Forms`, `UtinniCore.Memory`,
    `GameCallbacks`, or any TJT type — it is pure-managed and tested against the CI runner. Same
    disposition reasoning as IffEditController's framework-side placement (08-REVIEWS MEDIUM-5 / checker
    B-1): pure-managed, BCL-only, no plugin-only references → framework-side placement avoids the
    cross-repo linked-source CI break.

    PART B — csproj coverage (round-2 HIGH-A).
    Edit `UtinniCoreDotNet/UtinniCoreDotNet.csproj`. Inside the existing main `<ItemGroup>` at lines
    62-208, add ONE new entry immediately adjacent to the `Editing\IffEditController.cs` line added by
    08-04 Task 1:

      <Compile Include="Editing\LivePatchValidator.cs" />

    No `<SubType>` needed (plain class).

    PART C — Test file.
    Create `UtinniCoreDotNet.Tests/EditingTests/LivePatchValidatorTests.cs` (verbatim MIT banner) with
    the FIVE required [Fact]s:

      [Fact] Validate_NoClient_RefusedNoClient — call Validate(targetAddr: any non-zero, originalMappedLength: any, rewrittenLength: any, gameIsRunning: false); assert result == LivePatchValidation.RefusedNoClient.

      [Fact] Validate_ZeroTarget_RefusedZeroTarget — call Validate(targetAddr: IntPtr.Zero, originalMappedLength: any, rewrittenLength: any, gameIsRunning: true); assert result == LivePatchValidation.RefusedZeroTarget.

      [Fact] Validate_GrowthLength_RefusedSameLength — call Validate(targetAddr: any non-zero, originalMappedLength: N, rewrittenLength: N + 1, gameIsRunning: true); assert result == LivePatchValidation.RefusedSameLength.

      [Fact] Validate_ShrinkLength_RefusedSameLength — call Validate(targetAddr: any non-zero, originalMappedLength: N, rewrittenLength: N - 1, gameIsRunning: true); assert result == LivePatchValidation.RefusedSameLength.

      [Fact] Validate_SameLengthHappyPath_Ok — call Validate(targetAddr: any non-zero, originalMappedLength: N, rewrittenLength: N, gameIsRunning: true); assert result == LivePatchValidation.Ok.

    The test consumes the validator via the existing `ProjectReference` to `UtinniCoreDotNet.csproj`
    (line 33 of UtinniCoreDotNet.Tests.csproj) — `using UtinniCoreDotNet.Editing;` — NO linked-source
    entry. The new test file under `UtinniCoreDotNet.Tests/EditingTests/` auto-includes via the SDK-style
    default `**/*.cs` glob (the only excluded subtree is `Fixtures\**`).

    NO csproj edit needed for the test file.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~LivePatchValidator" && grep -c '<Compile Include="Editing\\\\LivePatchValidator.cs"' UtinniCoreDotNet/UtinniCoreDotNet.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `UtinniCoreDotNet/Editing/LivePatchValidator.cs` exists with the verbatim MIT+provenance banner and `namespace UtinniCoreDotNet.Editing; public enum LivePatchValidation { Ok, RefusedNoClient, RefusedZeroTarget, RefusedSameLength } public static class LivePatchValidator { public static LivePatchValidation Validate(IntPtr, int, int, bool); }`
    - The validator references NEITHER `System.Windows.Forms` NOR `UtinniCore.Memory` NOR `GameCallbacks` NOR any TJT type — grep returns 0 for all four
    - `UtinniCoreDotNet.csproj` contains `<Compile Include="Editing\LivePatchValidator.cs" />` (grep gate: returns 1; round-2 HIGH-A)
    - `UtinniCoreDotNet.Tests/EditingTests/LivePatchValidatorTests.cs` exists with the verbatim MIT banner and FIVE [Fact]s with the exact names from `<action>` Part C
    - Each [Fact] asserts the expected `LivePatchValidation` enum value from `<behavior>`
    - `dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~LivePatchValidator"` exits 0 with ≥ 5 tests run, all passing
    - The CONTEXT D-05.3 claim "unit-tested for its bounds gate" is now factually accurate (round-2 HIGH-B closed)
    - VS2026 MSBuild of UtinniCoreDotNet + UtinniCoreDotNet.Tests compiles clean
  </acceptance_criteria>
  <done>The bounds-gate validation is extracted into a pure-managed framework-side function and unit-tested with five [Fact]s covering refusal + happy path; CONTEXT D-05.3's "unit-tested for its bounds gate" claim is now factually accurate; explicit csproj entry added (round-2 HIGH-A); LivePatchSaveTarget consumes this function for its bounds gate (Task 3).</done>
</task>

<task type="auto">
  <name>Task 3: LivePatchSaveTarget — game-thread CON-N-04 mapped-memory write — consumes LivePatchValidator (round-2 HIGH-B) + TheJawaToolboxDotNet.csproj entry (round-2 HIGH-A)</name>
  <read_first>
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-PATTERNS.md (in-memory live-patch helper analog; Memory.memory.Copy binding lines 660-662/702-705; Pattern 3 / Pitfall 3 / CON-N-04)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-RESEARCH.md (Pattern 3 anti-pattern: never write mapped memory off the game thread or bypassing memory::copy; Runtime State Inventory — volatile by design)
    - D:/Code/Utinni/UtinniCoreDotNet/Callbacks/GameCallbacks.cs (AddMainLoopCall 254-257)
    - D:/Code/Utinni/UtinniCoreDotNet/Formats/Iff/OpenSource.cs (from 08-01 Task 3 — ClientMemory case)
    - D:/Code/Utinni/UtinniCoreDotNet/Editing/LivePatchValidator.cs (from Task 2 — the pure-function bounds gate to consume)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj (OLD-STYLE; new `<Compile Include="Saving\LivePatchSaveTarget.cs" />` joins the existing Saving cluster from 08-05)
  </read_first>
  <action>
    PART A — Create the save target.
    Create `Saving/LivePatchSaveTarget.cs` in TheJawaToolboxDotNet (verbatim MIT banner). Public surface:
    `public static LivePatchResult Apply(OpenSource.ClientMemory target, byte[] rewritten)` where
    `LivePatchResult` is a small enum or struct (Applied / RefusedSameLength / RefusedZeroTarget /
    RefusedNoClient — mirror `LivePatchValidation` from Task 2 1:1; document for the caller in
    08-04/08-05's UI handling).

    Add `using UtinniCoreDotNet.Editing;` (or fully-qualify as
    `UtinniCoreDotNet.Editing.LivePatchValidator.Validate(...)`) — the validator now lives framework-side
    per round-2 HIGH-B, consumed via the existing UtinniCoreDotNet.dll reference.

    BOUNDS VALIDATION BEFORE the write (round-2 HIGH-B fold — consume LivePatchValidator instead of
    inlining the checks):
      - Call `var v = LivePatchValidator.Validate(target.TargetAddr, target.OriginalMappedLength, rewritten?.Length ?? 0, Game.IsRunning);`
      - If `v == LivePatchValidation.RefusedNoClient` -> return `LivePatchResult.RefusedNoClient`.
      - If `v == LivePatchValidation.RefusedZeroTarget` -> return `LivePatchResult.RefusedZeroTarget`.
      - If `v == LivePatchValidation.RefusedSameLength` -> return `LivePatchResult.RefusedSameLength`.
        The UI surfaces the candid copy 'Live patch requires the rewritten IFF to be the same length as
        the original. Save to file/repack instead.'
      - If `v == LivePatchValidation.Ok` -> proceed.

    Note on rewritten-null safety: pass `rewritten?.Length ?? 0` so a null rewritten (defensive — caller
    bug) is treated as length-0, which (for any nonzero originalMappedLength) refuses as
    `RefusedSameLength`. The pure validator never dereferences the rewritten array (it only knows the
    length); the dereference happens HERE in the save target, AFTER the gate.

    If `v == Ok`: perform the patch entirely inside a `GameCallbacks.AddMainLoopCall` lambda: pin the
    source bytes (`GCHandle.Alloc(rewritten, GCHandleType.Pinned)`), get its `AddrOfPinnedObject`, cast
    to `(uint)`, then call `UtinniCore.Memory.memory.Copy((uint)(long)target.TargetAddr,
    (uint)pinnedAddr, (uint)rewritten.Length)` — the VirtualProtect bracket is inside the native fn; do
    NOT hand-roll a write that skips it. Free the pin in a `finally`. NEVER perform the write on the UI
    thread.

    Document that the patch is volatile (lost on reload/scene change) — the intended D-05.3 behavior.
    Document that a follow-up phase MAY relax the same-length-only constraint by zero-filling the tail
    (08-REVIEWS HIGH-3 alternate); V1 ships the strict gate.

    PART B — csproj coverage (round-2 HIGH-A).
    Edit `../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj`. Add:

      <Compile Include="Saving\LivePatchSaveTarget.cs" />

    Adjacent to the existing Saving cluster (IffSaveTargets.cs + ClientReloadDispatcher.cs from 08-05).
    No `<SubType>` needed.
  </action>
  <verify>
    <automated>grep -c "AddMainLoopCall\|LivePatchValidator\.Validate" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/LivePatchSaveTarget.cs" && grep -c '<Compile Include="Saving\\\\LivePatchSaveTarget.cs"' "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"</automated>
  </verify>
  <acceptance_criteria>
    - LivePatchSaveTarget.cs exists with the MIT banner
    - The file imports `UtinniCoreDotNet.Editing` AND calls `LivePatchValidator.Validate(...)` BEFORE any AddMainLoopCall queue or Memory.memory.Copy — grep proves `LivePatchValidator.Validate` is referenced
    - The Memory.memory.Copy call is inside a GameCallbacks.AddMainLoopCall lambda (never the UI thread)
    - SAME-LENGTH-ONLY: a patch with `rewritten.Length != target.OriginalMappedLength` is REFUSED (both growth AND shrink); returns `LivePatchResult.RefusedSameLength` via the validator's enum mapping
    - `target.TargetAddr == IntPtr.Zero` returns `LivePatchResult.RefusedZeroTarget`; the whole path is gated on Game.IsRunning (returns RefusedNoClient otherwise)
    - The pinned source GCHandle is freed in a `finally`
    - Implementation accepts `OpenSource.ClientMemory` (08-REVIEWS HIGH-2 provenance gate)
    - `TheJawaToolboxDotNet.csproj` contains `<Compile Include="Saving\LivePatchSaveTarget.cs" />` (grep gate: returns 1; round-2 HIGH-A)
    - VS2026 MSBuild of TheJawaToolbox compiles clean
  </acceptance_criteria>
  <done>The in-memory live patch writes mapped client memory only via the game-thread CON-N-04 bracket, with same-length-only bounds validation EXTRACTED to LivePatchValidator (consumed via the framework-side pure-function call — round-2 HIGH-B), refuses zero-target / no-client / wrong-length patches, stays purely volatile by design; explicit csproj entry added (round-2 HIGH-A).</done>
</task>

<task type="auto">
  <name>Task 4: Wire Patch live client into Save▾ (provenance + confirm + disabled-this-phase tooltip)</name>
  <read_first>
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs (Save▾ drop-down + the disabled live-patch placeholder from 08-05 + the Source property from 08-04)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/LivePatchSaveTarget.cs (from Task 3)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormSaveConfirmDialog.cs (from Task 1)
    - D:/Code/Utinni/UtinniCoreDotNet/Formats/Iff/OpenSource.cs (08-01 Task 3 — ClientMemory)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-UI-SPEC.md (Save-target menu copy; live-patch confirm; status states; volatility caveat)
  </read_first>
  <action>
    In `FormIffEditor`, enable the `Patch live client (in memory)` `Save ▾` item ONLY when
    `this.Source is OpenSource.ClientMemory cm` AND `Game.IsRunning` (08-REVIEWS HIGH-2). Otherwise keep
    it disabled with the HONEST tooltip "Live patch requires opening from client memory — not wired in
    this phase." (round-2 MEDIUM 11 wording: matches the must_haves entry for D-05.3 reduced-mode
    completion semantics.)

    On click (when enabled): show `FormSaveConfirmDialog` (the live-patch heading/body/buttons from
    Task 1); on `Patch live`, serialize via `IffWriter.Write(mutableDoc)`, then
    `LivePatchSaveTarget.Apply(cm, rewritten)`. Translate the result to status copy:
      - `Applied` -> `Saving (live patch)… Applied.` (volatile — does NOT clear the dirty marker; a
        live patch is not a file save)
      - `RefusedSameLength` -> `Live patch requires the rewritten IFF to be the same length as the
        original. Save to file/repack instead.` (UI-SPEC candid copy)
      - `RefusedZeroTarget` -> `Live patch target address is invalid. Save to file/repack instead.`
      - `RefusedNoClient` -> button should not have been clickable; defensive copy `No live client.`
    On a refused patch, the status guidance directs the user to a file save mode. Do not alter the file
    save modes from 08-05.

    NO csproj edit in this task — FormIffEditor.cs already has a Compile entry (added by 08-04 Task 2).
  </action>
  <verify>
    <automated>grep -c "LivePatchSaveTarget\|OpenSource\.ClientMemory" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs"</automated>
  </verify>
  <acceptance_criteria>
    - The `Patch live client (in memory)` item is enabled ONLY when `this.Source is OpenSource.ClientMemory cm` AND Game.IsRunning
    - When disabled, the tooltip honestly states "Live patch requires opening from client memory — not wired in this phase." (round-2 MEDIUM 11)
    - Clicking it (when enabled) shows FormSaveConfirmDialog and only patches on the explicit `Patch live` verb
    - A successful patch (`Applied`) shows the live-patch status and respects the volatile-edit semantics (does not falsely mark the file as saved)
    - A refused patch surfaces the matching candid copy (same-length / zero-target / no-client) directing to file save
    - VS2026 MSBuild of TheJawaToolbox compiles clean
  </acceptance_criteria>
  <done>The confirmed in-memory live patch is reachable from Save▾ ONLY when provenance is ClientMemory, gated correctly, and conveys its volatile / same-length-only nature; otherwise the menu is honestly disabled with the round-2 MEDIUM 11 wording.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Live-SWG smoke — in-memory live patch (D-05.3) — SAME-LENGTH-ONLY recipe</name>
  <files>(no files modified — Tier-4 manual verification checkpoint; see <how-to-verify>)</files>
  <action>
    Pause for the maintainer to run the live-SWG smoke in <how-to-verify>. **NOTE:** If no current open
    path in 08-05 constructs `OpenSource.ClientMemory` (the expected V1 state per 08-REVIEWS HIGH-2(b)
    + round-2 MEDIUM 11: D-05.3 ships infra-ready, user-disabled), the maintainer's Tier-4 verification
    is REDUCED to: (a) confirm the menu item is correctly DISABLED with the honest tooltip; (b) confirm
    a manual / temporary test-only construction of `OpenSource.ClientMemory` (e.g. via a debug-only
    command, NOT shipped) enables the menu and applies a same-length live patch without crash; (c)
    confirm an attempted same-different-length patch is refused with the candid copy. Document the
    verification mode (full vs reduced) in the SUMMARY.

    Note: the `LivePatchValidator` xUnit tests (Task 2) already cover the bounds gate in pure-managed
    CI without an injected client — so the bounds gate is automation-gated regardless of Tier-4
    coverage (round-2 HIGH-B).
  </action>
  <verify>
    <human-check>EITHER (full) the in-memory live patch applies (where observable) without crashing/AV, is lost on reload/scene change (volatile), AND a wrong-length patch is refused with the same-length copy; OR (reduced, expected this phase per 08-REVIEWS HIGH-2(b) + round-2 MEDIUM 11) the menu is HONESTLY DISABLED with the future-phase tooltip and a maintainer-only debug construction proves the patch applies same-length and refuses wrong-length without crash. Stability observation + verification mode recorded in the SUMMARY.</human-check>
  </verify>
  <what-built>
    The in-memory live-patch save mode: open an IFF that resolves to a known client-memory region, edit
    a SAME-LENGTH leaf payload, confirm the live-patch dialog, and write the bytes into mapped client
    memory via the CON-N-04 bracket on the game thread. Volatile by design; same-length-only V1.
    Bounds gate extracted to framework-side LivePatchValidator and unit-tested (round-2 HIGH-B).
  </what-built>
  <how-to-verify>
    1. Build TheJawaToolbox + UtinniCoreDotNet with VS2026 MSBuild (Release), inline on the main tree.
    2. Inject into a live SWGEmu/Restoration client; open The Jawa Toolbox + the IFF Editor.
    3. **Disabled-state check (this-phase expected):** open any IFF; confirm the `Patch live client (in memory)` Save▾ item is DISABLED with the honest tooltip ("Live patch requires opening from client memory — not wired in this phase."). Round-2 MEDIUM 11 wording.
    4. **Reduced functional check (requires maintainer-only debug construction):** temporarily construct a `OpenSource.ClientMemory` and SET it on `FormIffEditor.Source` (via a debug command or a one-off probe build — NOT shipped); confirm the menu enables.
    5. Edit a leaf payload SAME-LENGTH (replace bytes via Replace bytes from file… with a same-size file).
    6. Save ▾ -> `Patch live client (in memory)`; confirm the dialog reads as specified and the warning is in red.
    7. Click `Patch live`; observe the in-session effect (if observable for the chosen asset) and confirm NO crash / no access violation.
    8. Trigger a reload/scene change; confirm the patch is lost (volatile — the documented D-05.3 behavior).
    9. Attempt a DIFFERENT-LENGTH patch (grow OR shrink a payload); confirm it is refused with the SAME-LENGTH candid copy and NOT a crash.
    10. Record in the SUMMARY: verification mode (full vs reduced), live-patch stability observation, same-length refusal observation (Tier-4 residual), AND confirmation that the LivePatchValidator xUnit tests (Task 2) ran green in CI (round-2 HIGH-B).
  </how-to-verify>
  <resume-signal>Type "approved" with the verification mode (full / reduced) + live-patch stability observation + same-length refusal observation, or describe issues (esp. any AV/crash).</resume-signal>
  <done>The maintainer has approved with the verification mode + live-patch stability observation + same-length refusal observation recorded in the SUMMARY; no crash/AV, volatile behavior confirmed, wrong-length patch refused; the framework-side LivePatchValidator bounds-gate tests (Task 2) ran green in CI confirming the round-2 HIGH-B / CONTEXT D-05.3 "unit-tested" claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor bytes -> mapped client process memory | the highest-risk write surface; attacker-influenced bytes + length must be bounded before crossing |
| ClientMemory provenance descriptor -> live-patch enable | provenance gate is the only thing that lets the menu enable; OpenSource type is the load-bearing check |
| LivePatchValidator inputs -> LivePatchValidation result | the pure-function bounds gate is the source of truth for refusal; LivePatchSaveTarget MUST consume it, not inline checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-14 | Tampering / DoS | mapped-memory write off the game thread or bypassing memory::copy | mitigate | Write only inside GameCallbacks.AddMainLoopCall via Memory.memory.Copy (CON-N-04 bracket); never the UI thread (Task 3) |
| T-08-15 | DoS | wrong-length write (grow or shrink) into client memory -> AV/corruption/stale-tail | mitigate | SAME-LENGTH-ONLY V1 gate enforced by LivePatchValidator (pure-function, framework-side, unit-tested with FIVE [Fact]s — round-2 HIGH-B): refuses if `rewritten.Length != cm.OriginalMappedLength`; also refuses `IntPtr.Zero` target (Task 2 + Task 3 — 08-REVIEWS HIGH-3 + round-2 HIGH-B) |
| T-08-16 | Tampering | user unaware the patch is volatile / destabilizing | mitigate | Risk-proportional confirm dialog with Color.Red emphasis + explicit `Patch live` verb (Task 1/4) |
| T-08-16b | Tampering | menu enabled on wrong provenance (e.g. file-opened IFF) -> writing to arbitrary/unknown address | mitigate | Provenance gate: enabled only when `Source is OpenSource.ClientMemory` AND Game.IsRunning (Task 4, 08-REVIEWS HIGH-2) |
| T-08-16c | Tampering | bounds gate inlined in LivePatchSaveTarget cannot be unit-tested without an injected client -> regressions slip past CI | mitigate | Bounds gate EXTRACTED to LivePatchValidator (pure function, framework-side, no native interop); unit-tested with FIVE [Fact]s in CI without a live client (Task 2 — round-2 HIGH-B) |
| T-08-16d | Tampering | new .cs files silently fail to compile into UtinniCoreDotNet.dll / TheJawaToolboxDotNet.dll because the OLD-STYLE csproj files do not glob them | mitigate | Explicit `<Compile Include>` entries for LivePatchValidator.cs (UtinniCoreDotNet.csproj) AND LivePatchSaveTarget.cs + FormSaveConfirmDialog.cs/Designer.cs (TheJawaToolboxDotNet.csproj); grep-gated acceptance (Tasks 1, 2, 3 — round-2 HIGH-A) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No external packages added this plan (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- VS2026 MSBuild of TheJawaToolbox + UtinniCoreDotNet + UtinniCoreDotNet.Tests (Release) compiles clean (inline; worktrees off).
- The mapped-memory write is only ever inside AddMainLoopCall (grep gate) via Memory.memory.Copy.
- LivePatchSaveTarget consumes `LivePatchValidator.Validate` before queuing AddMainLoopCall (grep gate: `LivePatchValidator.Validate` appears in LivePatchSaveTarget.cs).
- `dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~LivePatchValidator"` exits 0 with FIVE tests passing (round-2 HIGH-B — CONTEXT D-05.3 "unit-tested" claim verified).
- Live-SWG smoke (Task 5) confirms no crash + volatile behavior + same-length refusal (full or reduced mode).
- UtinniCoreDotNet.csproj contains the `Editing\LivePatchValidator.cs` Compile entry (round-2 HIGH-A grep gate).
- TheJawaToolboxDotNet.csproj contains the three new Compile entries: Saving\LivePatchSaveTarget.cs + UI\Forms\FormSaveConfirmDialog.cs (Form SubType) + .Designer.cs (DependentUpon) (round-2 HIGH-A grep gates).
</verification>

<success_criteria>
- In-memory live patch writes mapped client memory only via the game-thread CON-N-04 bracket, bounds-validated by the framework-side pure-function LivePatchValidator, SAME-LENGTH-ONLY (08-REVIEWS HIGH-3).
- Bounds gate is unit-tested in CI with FIVE [Fact]s — CONTEXT D-05.3's "unit-tested for its bounds gate" claim is now factually accurate (round-2 HIGH-B).
- D-05.3 is recorded in the plan's must_haves as **infra-ready, user-disabled** — the menu ships disabled with the honest tooltip; PROD-W1-IFF Criterion 2 is acceptance-tested against modes 1/2/4 in this phase with mode 3 documented as infra-ready (round-2 MEDIUM 11).
- The confirm dialog conveys volatility + risk with explicit verbs and red emphasis.
- The menu item is provenance-gated on `OpenSource.ClientMemory` (08-REVIEWS HIGH-2); disabled with an honest tooltip otherwise.
- Live smoke confirms the patch applies (where observable) without crashing and is lost on reload — or, in reduced mode, the disabled state is correct and a maintainer-only debug construction proves the patch contract.
- BOTH old-style csproj files contain the explicit Compile entries for every new file (round-2 HIGH-A).
</success_criteria>

<output>
Create `.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-06-SUMMARY.md` when done (include the verification mode, live-patch stability observation, same-length refusal observation, AND a one-line confirmation that (a) the LivePatchValidator xUnit suite closes round-2 HIGH-B / CONTEXT D-05.3 "unit-tested" claim and (b) the four new csproj entries close round-2 HIGH-A for this plan).
</output>
</content>
