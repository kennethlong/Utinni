---
phase: 08-tjt-subpanel-iff-editor-read-write
plan: 06
type: execute
wave: 5
depends_on: ["08-05"]
files_modified:
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/LivePatchSaveTarget.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormSaveConfirmDialog.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormSaveConfirmDialog.Designer.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs"
autonomous: false
requirements: [PROD-W1-IFF]
must_haves:
  truths:
    - "The editor can patch the loaded IFF bytes directly in mapped client memory (D-05.3), gated behind a confirm dialog and Game.IsRunning"
    - "Every mapped-memory write goes through Memory.memory.Copy on the game thread (CON-N-04 VirtualProtect bracket; never the UI thread)"
    - "The patch length/bounds are validated before the write (no over-cap / out-of-range write into client memory)"
    - "The live-patch confirm dialog uses explicit-verb buttons with Color.Red emphasis and states the change is volatile (lost on reload)"
  artifacts:
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/LivePatchSaveTarget.cs"
      provides: "In-memory live patch via game-thread Memory.memory.Copy (CON-N-04)"
      contains: "AddMainLoopCall"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormSaveConfirmDialog.cs"
      provides: "Risk-proportional confirm modal for the live patch (and reused by repack)"
      contains: "class FormSaveConfirmDialog"
  key_links:
    - from: "LivePatchSaveTarget"
      to: "Memory.memory.Copy"
      via: "game-thread CON-N-04 bracketed write"
      pattern: "memory\\.Copy"
    - from: "FormIffEditor Save▾ -> Patch live client"
      to: "FormSaveConfirmDialog -> LivePatchSaveTarget"
      via: "confirmed live patch"
      pattern: "LivePatchSaveTarget"
---

<objective>
Add the isolated, higher-risk in-memory live-patch save mode (D-05.3): write the loaded IFF's edited
bytes straight into mapped client memory via the CON-N-04 `Memory.memory.Copy` VirtualProtect bracket
on the game thread, behind a risk-proportional confirm dialog. This is the 8b isolated plan — it
touches live client memory and is validated by live-SWG smoke only.

Purpose: D-05.3 is a hard V1 must-have (instant in-session feedback; volatile, lost on reload). It is
isolated per the D-05 PLAN-SPLIT FLAG because the mapped-memory write carries distinct failure modes
(AV / device-lost / client crash) from the file save modes.

Output: `LivePatchSaveTarget.cs`, `FormSaveConfirmDialog.cs`/`.Designer.cs`, the `Save ▾` wiring in
`FormIffEditor`, and a live-SWG smoke checkpoint. Cross-repo: entirely in `D:/Code/UtinniPlugins`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-RESEARCH.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-PATTERNS.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-UI-SPEC.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-05-SUMMARY.md

<interfaces>
<!-- CON-N-04 mapped-memory write binding (UtinniCore.Memory.memory): -->
UtinniCore.Memory.memory.Copy(uint pDest, uint pSource, uint length)   // Generated/UtinniCore.cs 702; native ?copy@memory@@YAXIII@Z
  // The VirtualProtect save/restore bracket is INSIDE the native memory::copy — do NOT hand-roll a write that skips it.
GameCallbacks.AddMainLoopCall(Action)                                  // game-thread marshaling (GameCallbacks.cs 254-257)
Game.IsRunning                                                         // gate before any live-client touch

<!-- 08-01 write path produces the bytes to patch: -->
IffWriter.Write(MutableIffDocument) -> byte[]

<!-- Small-modal analog: UtinniCoreDotNet/UI/Forms/FormHotkeyEditorDialog.cs (partial UtinniForm + buttons + public result) -->
<!-- UI-SPEC Destructive: live-patch confirm heading "Patch the live client in memory?" buttons "Patch live"/"Cancel", Color.Red emphasis. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FormSaveConfirmDialog — risk-proportional confirm modal</name>
  <read_first>
    - D:/Code/Utinni/UtinniCoreDotNet/UI/Forms/FormHotkeyEditorDialog.cs (small UtinniForm modal: InitializeComponent, public result field, explicit buttons)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-UI-SPEC.md (Destructive/risky actions table; Color.Red emphasis; explicit-verb buttons — never bare OK/Cancel; optional repack "back up first" checkbox)
  </read_first>
  <action>
    Create `UI/Forms/FormSaveConfirmDialog.cs` + `.Designer.cs` (verbatim MIT banner) as a small
    `UtinniForm` modal parameterized by heading, body, and the two explicit verb-button captions
    (returns a `DialogResult`-style outcome via a public field). Render the body emphasis in `Color.Red`
    per UI-SPEC. For the live patch it is invoked with heading `Patch the live client in memory?`, body
    `This writes your edits straight into the running client. The change is temporary (lost on reload)
    and can destabilize the session. Continue?`, buttons `Patch live` / `Cancel`. Build it generically so
    08-07 can reuse it for the repack confirm (heading/body/buttons + an optional "back up first"
    checkbox). Use `Colors.*()` accessors plus the documented `Color.Red` destructive exception only.
  </action>
  <verify>
    <automated>grep -c "class FormSaveConfirmDialog" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormSaveConfirmDialog.cs"</automated>
  </verify>
  <acceptance_criteria>
    - FormSaveConfirmDialog.cs + .Designer.cs exist with the MIT banner and `class FormSaveConfirmDialog`
    - The dialog is parameterized by heading/body/two explicit verb captions and exposes the chosen outcome
    - Body emphasis uses Color.Red; buttons are explicit verbs (no bare OK/Cancel)
    - It is reusable for the repack confirm (08-07) with an optional checkbox
    - VS2026 MSBuild of TheJawaToolbox compiles clean
  </acceptance_criteria>
  <done>A reusable, risk-proportional confirm modal exists with explicit verb buttons and Color.Red emphasis.</done>
</task>

<task type="auto">
  <name>Task 2: LivePatchSaveTarget — game-thread CON-N-04 mapped-memory write</name>
  <read_first>
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-PATTERNS.md (in-memory live-patch helper analog; Memory.memory.Copy binding lines 660-662/702-705; Pattern 3 / Pitfall 3 / CON-N-04)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-RESEARCH.md (Pattern 3 anti-pattern: never write mapped memory off the game thread or bypassing memory::copy; Runtime State Inventory — volatile by design)
    - D:/Code/Utinni/UtinniCoreDotNet/Callbacks/GameCallbacks.cs (AddMainLoopCall 254-257)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-05-SUMMARY.md (how the loaded IFF's source address/length are tracked when opened from memory)
  </read_first>
  <action>
    Create `Saving/LivePatchSaveTarget.cs` in TheJawaToolboxDotNet (verbatim MIT banner). Given the target
    address + the original mapped length of the loaded IFF in client memory and the freshly-serialized
    bytes (`IffWriter.Write`), perform the patch entirely inside a `GameCallbacks.AddMainLoopCall` lambda:
    pin the source bytes (`GCHandle.Alloc(..., Pinned)`), then call
    `UtinniCore.Memory.memory.Copy((uint)targetAddr, (uint)srcAddr, (uint)length)` — the VirtualProtect
    bracket is inside the native fn; do NOT hand-roll a write that skips it. BOUNDS VALIDATION BEFORE the
    write (Security): refuse the patch if the new byte length exceeds the original mapped length (an
    in-memory patch cannot safely grow a region — only same-size-or-smaller payload replacement is
    sanctioned for V1; surface a status message directing the user to a file save mode instead), and
    refuse if `targetAddr == 0`. Gate the whole thing behind `Game.IsRunning`. Free the pin in a finally.
    Document that the patch is volatile (lost on reload/scene change) — the intended D-05.3 behavior.
    NEVER perform the write on the UI thread.
  </action>
  <verify>
    <automated>grep -c "AddMainLoopCall" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/LivePatchSaveTarget.cs"</automated>
  </verify>
  <acceptance_criteria>
    - LivePatchSaveTarget.cs exists with the MIT banner
    - The Memory.memory.Copy call is inside a GameCallbacks.AddMainLoopCall lambda (never the UI thread)
    - The new length is validated against the original mapped length BEFORE the write; an over-length patch is refused with a status directing to a file save
    - targetAddr == 0 is refused; the whole path is gated on Game.IsRunning
    - The pinned source GCHandle is freed in a finally
    - VS2026 MSBuild of TheJawaToolbox compiles clean
  </acceptance_criteria>
  <done>The in-memory live patch writes mapped client memory only via the game-thread CON-N-04 bracket, with bounds validation, and refuses unsafe (growing/out-of-range) patches.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Patch live client into Save▾ (behind the confirm)</name>
  <read_first>
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs (Save▾ drop-down + the disabled live-patch placeholder from 08-05)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/LivePatchSaveTarget.cs (from Task 2)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormSaveConfirmDialog.cs (from Task 1)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-UI-SPEC.md (Save-target menu copy; live-patch confirm; status states; volatility caveat)
  </read_first>
  <action>
    In `FormIffEditor`, enable the `Patch live client (in memory)` `Save ▾` item but ONLY when the
    document was opened from a known client-memory address AND `Game.IsRunning` (otherwise keep it
    disabled with a tooltip). On click: show `FormSaveConfirmDialog` (the live-patch heading/body/buttons
    from Task 1); on `Patch live`, serialize via `IffWriter.Write` and invoke `LivePatchSaveTarget`; render
    the `Saving (live patch)…` -> outcome status (success keeps the editor dirty-state semantics per
    UI-SPEC since the patch is volatile — it does NOT clear the unsaved marker the way a file save does).
    On a refused/over-length patch, show the status directing the user to a file save mode. Do not alter
    the file save modes from 08-05.
  </action>
  <verify>
    <automated>grep -c "LivePatchSaveTarget" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs"</automated>
  </verify>
  <acceptance_criteria>
    - The `Patch live client (in memory)` item is enabled only when the doc has a client-memory source AND Game.IsRunning
    - Clicking it shows FormSaveConfirmDialog and only patches on the explicit `Patch live` verb
    - A successful patch shows the live-patch status and respects the volatile-edit semantics (does not falsely mark the file as saved)
    - A refused patch surfaces the file-save-instead guidance
    - VS2026 MSBuild of TheJawaToolbox compiles clean
  </acceptance_criteria>
  <done>The confirmed in-memory live patch is reachable from Save▾, gated correctly, and conveys its volatile nature.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live-SWG smoke — in-memory live patch (D-05.3)</name>
  <files>(no files modified — Tier-4 manual verification checkpoint; see <how-to-verify>)</files>
  <action>
    Pause for the maintainer to run the live-SWG smoke in <how-to-verify>: build inline, inject into a
    live client, open an IFF that resolves to a known client-memory region, edit a same-size-or-smaller
    leaf payload, confirm the live-patch dialog, and write the bytes into mapped client memory on the
    game thread. The maintainer confirms no crash/AV, that the patch is volatile (lost on reload), and
    that an over-length patch is refused (not a crash); they record the stability observation in the
    SUMMARY. Do NOT auto-advance — mapped-memory writes have no CI fallback (Tier-4 residual).
  </action>
  <verify>
    <human-check>The in-memory live patch applies (where observable) without crashing/AV, is lost on reload/scene change (volatile), and an over-length patch is refused with file-save guidance. Stability observation recorded in the SUMMARY.</human-check>
  </verify>
  <what-built>
    The in-memory live-patch save mode: open an IFF that resolves to a known client-memory region, edit
    a same-size-or-smaller leaf payload, confirm the live-patch dialog, and write the bytes into mapped
    client memory via the CON-N-04 bracket on the game thread. Volatile by design.
  </what-built>
  <how-to-verify>
    1. Build TheJawaToolbox + UtinniCoreDotNet with VS2026 MSBuild (Release), inline on the main tree.
    2. Inject into a live SWGEmu/Restoration client; open The Jawa Toolbox + the IFF Editor.
    3. Open an IFF that the editor tracks to a client-memory address; edit a leaf payload (same size or smaller).
    4. Save ▾ -> `Patch live client (in memory)`; confirm the dialog reads as specified and the warning is in red.
    5. Click `Patch live`; observe the in-session effect (if observable for the chosen asset) and confirm NO crash / no access violation.
    6. Trigger a reload/scene change; confirm the patch is lost (volatile — the documented D-05.3 behavior).
    7. Attempt an over-length patch (grow a payload); confirm it is refused with the file-save guidance, NOT a crash.
    8. Record the outcome + any client stability observations in the SUMMARY (Tier-4 residual).
  </how-to-verify>
  <resume-signal>Type "approved" with the live-patch stability observation, or describe issues (esp. any AV/crash).</resume-signal>
  <done>The maintainer has approved with the live-patch stability observation recorded in the SUMMARY; no crash/AV, volatile behavior confirmed, over-length patch refused.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor bytes -> mapped client process memory | the highest-risk write surface; attacker-influenced bytes + length must be bounded before crossing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-14 | Tampering / DoS | mapped-memory write off the game thread or bypassing memory::copy | mitigate | Write only inside GameCallbacks.AddMainLoopCall via Memory.memory.Copy (CON-N-04 bracket); never the UI thread (Task 2) |
| T-08-15 | DoS | over-length / out-of-range write into client memory -> AV/corruption | mitigate | Validate new length <= original mapped length and targetAddr != 0 BEFORE the write; refuse otherwise (Task 2) |
| T-08-16 | Tampering | user unaware the patch is volatile / destabilizing | mitigate | Risk-proportional confirm dialog with Color.Red emphasis + explicit `Patch live` verb (Task 1/3) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No external packages added this plan (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- VS2026 MSBuild of TheJawaToolbox (Release) compiles clean (inline; worktrees off).
- The mapped-memory write is only ever inside AddMainLoopCall (grep gate) via Memory.memory.Copy.
- Bounds validation refuses over-length / null-target patches (unit-checkable on the validation method if extracted).
- Live-SWG smoke (Task 4) confirms no crash + volatile behavior.
</verification>

<success_criteria>
- In-memory live patch writes mapped client memory only via the game-thread CON-N-04 bracket, bounds-validated.
- The confirm dialog conveys volatility + risk with explicit verbs and red emphasis.
- Live smoke confirms the patch applies (where observable) without crashing, and is lost on reload.
</success_criteria>

<output>
Create `.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-06-SUMMARY.md` when done.
</output>
