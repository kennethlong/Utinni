---
phase: 08-tjt-subpanel-iff-editor-read-write
plan: 07
type: execute
wave: 6
depends_on: ["08-05", "08-06"]
files_modified:
  - UtinniCoreDotNet/Formats/Tre/TreFile.cs
  - UtinniCoreDotNet/Formats/Tre/TreWriter.cs
  - UtinniCoreDotNet/UtinniCoreDotNet.csproj
  - UtinniCoreDotNet.Tests/FormatsTests/Tre/TreWriterTests.cs
  - UtinniCoreDotNet.Tests/FormatsTests/Tre/TreFileGetRecordCompressedBytesTests.cs
  - UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/TreRepackSaveTarget.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"
autonomous: false
requirements: [PROD-W1-IFF]
assumes:
  - "Repack gating: provenance-gated on `FormIffEditor.Source is OpenSource.TreArchive ta` (08-REVIEWS HIGH-2). 08-05 populates this from the TRE Browser hand-off (trePath = descriptor.ResolvedArchivePath, recordIndex resolved from ArchiveLocalOffset, logicalPath = descriptor.Path)."
  - "Byte-identity redefined per 08-REVIEWS HIGH-1: not 'archive bytes identical' as a full-file claim. Two separate guarantees: (a) logical payload identity for ALL untouched entries (GetRecordData equality); (b) raw compressed slice preservation for untouched entries (verbatim bytes from the source .tre at [rec.Offset, rec.Offset+rec.CompressedSize)). Only the edited entry recompresses; its compressor flag may differ (V1 picks 'deflate' as default for the edited entry; document explicitly)."
  - "Name block byte-layout preservation (round-2 MEDIUM 6 / codex unique): preserving `TreRecord.NameOffset` is only meaningful if the name block IS itself byte-identical or rebuilt byte-identically for untouched entries. Task 2's writer MUST copy each untouched record's name bytes (from `rec.NameOffset` for the recorded name string length, including the terminator if present) VERBATIM from the source .tre. The new TOC's `NameOffset` field for each untouched record points at the correct offset in the new name block — preserving BOTH the logical name AND the byte layout of the name string itself. Edited entries: name unchanged (path is unchanged); reuse the source name bytes."
  - "Cross-repo (round-2 MEDIUM 3): this plan edits both Utinni (TreFile.cs, TreWriter.cs, csproj, two test files + test-csproj linked-source addition) AND UtinniPlugins (TreRepackSaveTarget.cs, FormIffEditor.cs + csproj). Execute in the same commit chain; verify `UtinniCoreDotNet.dll` is rebuilt before TheJawaToolbox builds against it. The `UtinniCoreDotNet/Generated/UtinniCore.cs` file shows as `M` in the working tree at planning time — the executor MUST confirm it's committed OR explicitly note the working-tree state in the SUMMARY before referencing any binding line citations."
  - "csproj coverage (round-2 HIGH-A): `UtinniCoreDotNet.csproj` is OLD-STYLE explicit-compile — `Formats\\Tre\\TreWriter.cs` requires an explicit `<Compile Include>` entry adjacent to the existing Formats\\Tre cluster (lines 69-76). `TheJawaToolboxDotNet.csproj` is OLD-STYLE — `Saving\\TreRepackSaveTarget.cs` requires an explicit `<Compile Include>` entry adjacent to the existing Saving cluster from 08-05/08-06. `UtinniCoreDotNet.Tests.csproj` IS SDK-style — the two new test files auto-glob; the linked-source `TreFixtureBuilder` entry from Task 2 is a SEPARATE pattern (it points OUTSIDE the project tree, into `..\\Utinni.Cli.Tests\\Infrastructure\\`) and is the SDK-style csproj's existing linked-source convention (lines 44-55 of UtinniCoreDotNet.Tests.csproj — TODO: confirm this addition is the right placement in Task 2)."
  - "Cursor HIGH ACK-only items (round-2): N-H1 (edited-entry CRC unproven against live client — Task 4 smoke step 7 exercises this); N-H2 (TreWriter blast-radius — wave-6 isolation + Tier-4 gate + locked-archive fallback + timestamped backups already in plan). Acknowledged here per round-2 process."
must_haves:
  truths:
    - "TreFile exposes `public byte[] GetRecordCompressedBytes(int index)` returning the raw compressed slice from the source .tre verbatim (08-REVIEWS HIGH-1) — copies bytes [rec.Offset, rec.Offset+rec.CompressedSize); reuses TreFile's existing source-path / lazy-read infrastructure"
    - "TreFile exposes `public byte[] GetRecordNameBytes(int index)` returning the raw name-block slice from the source .tre verbatim (round-2 MEDIUM 6) — used by Task 2's writer to preserve name block byte layout for untouched entries AND by the per-record TOC invariant test"
    - "TreWriter repacks a .tre archive by full rebuild — copies raw compressed bytes for untouched entries (NOT decompress/recompress); only the edited entry recompresses"
    - "Untouched entries preserve EVERY TOC invariant byte-for-byte: Checksum, NameOffset, NameString, Compressor flag, UncompressedSize, CompressedSize, AND the raw compressed payload bytes (08-REVIEWS HIGH-1 TOC invariant test); ALSO the raw name-block bytes (round-2 MEDIUM 6: NameOffset preservation is meaningful only if the name block IS byte-identical for untouched entries)"
    - "Logical-payload identity: for every untouched entry, GetRecordData on the repacked archive equals GetRecordData on the original (the original behavioral claim, retained as guarantee (a))"
    - "Raw-compressed-slice identity: for every untouched entry, the [rec.Offset, rec.Offset+rec.CompressedSize) byte range in the repacked archive equals the same byte range in the original (the new guarantee (b))"
    - "Raw-name-bytes identity (round-2 MEDIUM 6): for every untouched entry, GetRecordNameBytes(new, i) SequenceEqual GetRecordNameBytes(old, i)"
    - "The repack acceptance NO LONGER claims 'archive bytes identical' as a full-file property — that was overstated (08-REVIEWS HIGH-1) and is replaced by the two guarantees above plus the name-block byte-layout requirement (round-2 MEDIUM 6)"
    - "zlib framing on write inverts TreFile.Inflate (RFC1950 header + DeflateStream body + Adler32 trailer) — ONLY for the edited entry"
    - "The editor can repack the source .tre (D-05.4) when `Source is OpenSource.TreArchive ta`, behind a confirm dialog with TIMESTAMPED backup default-on (08-REVIEWS MEDIUM-10: <name>.tre.<yyyyMMdd-HHmmss>.bak — never overwrite a previous backup)"
    - "On atomic-replace failure (most likely cause: SWG client holds the archive open), the repack falls back to 'Save loose override of edited entry only' WITHOUT partial-write (08-REVIEWS MEDIUM-10)"
    - "UtinniCoreDotNet.csproj contains explicit `<Compile Include>` entry for `Formats\\Tre\\TreWriter.cs` — round-2 HIGH-A"
    - "TheJawaToolboxDotNet.csproj contains explicit `<Compile Include>` entry for `Saving\\TreRepackSaveTarget.cs` — round-2 HIGH-A"
  artifacts:
    - path: "UtinniCoreDotNet/Formats/Tre/TreFile.cs"
      provides: "Add `public byte[] GetRecordCompressedBytes(int index)` AND `public byte[] GetRecordNameBytes(int index)` APIs — verbatim raw slices for payload (HIGH-1) and name (round-2 MEDIUM 6) preservation"
      contains: "GetRecordCompressedBytes"
    - path: "UtinniCoreDotNet/Formats/Tre/TreWriter.cs"
      provides: "Full .tre repack copying raw compressed bytes AND raw name bytes for untouched entries; only the edited entry recompresses"
      contains: "class TreWriter"
    - path: "UtinniCoreDotNet.Tests/FormatsTests/Tre/TreWriterTests.cs"
      provides: "Repack two-guarantee harness (logical payload identity + raw compressed slice identity + raw name byte identity + TOC invariant per-record)"
      contains: "TreWriterTests"
    - path: "UtinniCoreDotNet.Tests/FormatsTests/Tre/TreFileGetRecordCompressedBytesTests.cs"
      provides: "Unit coverage of the new TreFile APIs (GetRecordCompressedBytes AND GetRecordNameBytes): bounds, equality to actual file bytes, fresh-copy contract"
      contains: "GetRecordCompressedBytesTests"
    - path: "UtinniCoreDotNet/UtinniCoreDotNet.csproj"
      provides: "Old-style explicit-compile project: MUST list `Formats\\Tre\\TreWriter.cs` — round-2 HIGH-A coverage gate"
      contains: "Formats\\Tre\\TreWriter.cs"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"
      provides: "Old-style explicit-compile project: MUST list `Saving\\TreRepackSaveTarget.cs` — round-2 HIGH-A coverage gate"
      contains: "Saving\\TreRepackSaveTarget.cs"
  key_links:
    - from: "TreWriter (untouched entries)"
      to: "TreFile.GetRecordCompressedBytes(i)"
      via: "raw compressed bytes copied verbatim from source .tre"
      pattern: "GetRecordCompressedBytes"
    - from: "TreWriter (untouched entries, name block)"
      to: "TreFile.GetRecordNameBytes(i)"
      via: "raw name bytes copied verbatim from source .tre (round-2 MEDIUM 6: name block byte-layout preservation)"
      pattern: "GetRecordNameBytes"
    - from: "TreWriter (untouched entries)"
      to: "TreRecord.Checksum / Compressor / NameOffset / UncompressedSize / CompressedSize"
      via: "preserve TOC fields byte-for-byte"
      pattern: "Checksum"
    - from: "FormIffEditor Save▾ -> Repack into source .tre"
      to: "Source is OpenSource.TreArchive ta -> TreRepackSaveTarget -> TreWriter"
      via: "provenance-gated, confirmed repack with timestamped backup + locked-archive fallback"
      pattern: "TreWriter"
---

<objective>
Add the highest-risk save mode (D-05.4): a framework-side `TreWriter` that repacks a `.tre` archive by
full rebuild, plus the editor wiring behind a confirm dialog with an opt-in TIMESTAMPED backup and a
locked-archive fallback. Per 08-REVIEWS HIGH-1 the byte-identity claim is rewritten: untouched entries
are guaranteed under TWO distinct properties — (a) **logical payload identity** (GetRecordData equality)
AND (b) **raw compressed slice preservation** (verbatim bytes at [rec.Offset, rec.Offset+rec.CompressedSize)
copied from the source archive). Only the edited entry recompresses.

**Round-2 MEDIUM 6 fold (Codex unique — name block byte-layout preservation):** preserving
`TreRecord.NameOffset` is only meaningful if the name block IS byte-identical for untouched entries.
Task 2's writer MUST copy each untouched record's name bytes VERBATIM from the source .tre. The new
TOC's `NameOffset` field for each untouched record points at the correct offset in the new name block
— preserving BOTH the logical name AND the byte layout of the name string itself. Edited entries: name
unchanged (path is unchanged); reuse the source name bytes. A new `GetRecordNameBytes(int)` API on
TreFile makes this achievable AND testable. The acceptance test asserts
`dst.GetRecordNameBytes(i).SequenceEqual(src.GetRecordNameBytes(i))` for every untouched record `i`.

The new `GetRecordCompressedBytes` API on `TreFile` makes (b) achievable; the original "untouched
entries are byte-identical to the original archive bytes" full-file claim is dropped as misleading.

The repack menu is provenance-gated per 08-REVIEWS HIGH-2: enabled only when `FormIffEditor.Source is
OpenSource.TreArchive ta`. 08-05 populates this from the TRE Browser hand-off. This resolves the
logical-path-only handoff gap.

Backups are TIMESTAMPED per 08-REVIEWS MEDIUM-10 (`<name>.tre.<yyyyMMdd-HHmmss>.bak`) so a failed second
attempt cannot destroy the prior known-good backup. Atomic replace is via `File.Replace` to a temp
sibling on the same volume; on failure (likely cause: client holds the archive open while injected), the
UI falls back to "Save loose override of edited entry only" — no partial-write.

**Round-2 HIGH-A note (csproj coverage):** This plan adds production .cs files across BOTH old-style
explicit-compile csproj files. `Formats\Tre\TreWriter.cs` MUST get an explicit `<Compile Include>`
entry in `UtinniCoreDotNet.csproj` (Task 2); `Saving\TreRepackSaveTarget.cs` MUST get an explicit
`<Compile Include>` entry in `TheJawaToolboxDotNet.csproj` (Task 3). The two new test files
auto-glob via the SDK-style test csproj — but Task 2 also adds a LINKED-SOURCE entry for
`TreFixtureBuilder` (existing precedent at UtinniCoreDotNet.Tests.csproj lines 44-55).

**Round-2 cursor HIGH ACK items acknowledged:**
- **N-H1 (edited-entry CRC unproven):** Task 4 (live smoke) explicitly exercises this — smoke step 7
  verifies the client RESOLVES the edited record's path under its original path (not a stale cached
  payload). If the client fails to resolve, the path-CRC-only assumption (Open Q1/A1) is invalidated
  and a follow-up phase MUST add payload-CRC recomputation. This is the LITERAL Tier-4 gate cursor
  N-H1 demanded.
- **N-H2 (TreWriter blast-radius):** already isolated as wave 6 (singleton; depends_on 08-05 + 08-06
  for both logical and file-collision serialization), gated behind Tier-4 smoke + locked-archive
  fallback + timestamped backups. Acknowledged.

Purpose: D-05.4 is a hard V1 must-have (closest to edit-in-place). SWG does NOT support in-place `.tre`
patching (swg-client-v2 reference); the only sanctioned model is loose override (08-05) or full rebuild
(this plan).

Output: `TreFile.GetRecordCompressedBytes` + `TreFile.GetRecordNameBytes` (NEW) + their unit suite,
`TreWriter.cs` (framework) + csproj entry + the two-guarantee + name-byte-identity repack test, and
`TreRepackSaveTarget.cs` + csproj entry + `Save ▾` wiring + live smoke (UtinniPlugins repo).

**Wave/dependency note:** Plan 07 declares `depends_on: ["08-05", "08-06"]` (wave: 6). The dependency on
08-06 is a FILE-OWNERSHIP dependency, not a logical one: 08-06 (wave 5) and 08-07 (wave 6) both modify
`../UtinniPlugins/.../UI/Forms/FormIffEditor.cs`. Explicit `depends_on` prevents a depends_on-only
scheduler from parallel-executing them and colliding on the same file. Same applies to the
`TheJawaToolboxDotNet.csproj` (modified by 08-05, 08-06, AND 08-07) — wave serialization ensures only
one wave at a time edits it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-RESEARCH.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-PATTERNS.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-UI-SPEC.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-REVIEWS.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-05-SUMMARY.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-06-SUMMARY.md

<interfaces>
<!-- TreFile read path to invert (UtinniCoreDotNet/Formats/Tre/TreFile.cs): -->
public static TreFile Open(string path) / Open(Stream)              // lazy TOC-only
public byte[] GetRecordData(int index)                              // on-demand DECOMPRESSED payload read (lines 363-425); re-read untouched entries for guarantee (a)
public byte[] GetRecordCompressedBytes(int index)                   // NEW API ADDED IN THIS PLAN (Task 1): raw compressed slice at [rec.Offset, rec.Offset+rec.CompressedSize) — for guarantee (b)
public byte[] GetRecordNameBytes(int index)                         // NEW API ADDED IN THIS PLAN (Task 1, round-2 MEDIUM 6): raw name bytes at [rec.NameOffset, rec.NameOffset + nameByteLength) — for name-block byte-layout preservation
private const int MaxBlockSize = 256 * 1024 * 1024;                 // keep this inflate/expansion cap on repack
TreRecord {
  long Offset; int CompressedSize; int UncompressedSize;
  int Compressor;                       // 0=none, 1=deflate, 2=zlib — PRESERVE for untouched entries
  uint Checksum;                        // stored path CRC (line 337) — PRESERVE for untouched entries
  int NameOffset; string Name;          // name-block offset + ASCII name — PRESERVE for untouched entries
}
TreVersions.RecordStride(version)                                   // 24- or 32-byte TOC stride per version (lines 194,210)
private static byte[] Inflate(byte[] raw, int compression, ...)     // lines 486-562: strips RFC1950 [2..len-4]; invert for write of the edited entry only
// header: 36-byte "EERT" + version tag + 7 u32 fields (magic at offset 0; Parse lines 139-187)

<!-- 08-01 provenance + write path: -->
OpenSource.TreArchive { string TrePath; int RecordIndex; string LogicalPath; }
IffWriter.Write(MutableIffDocument) -> byte[]

<!-- Repack fixture harness: -->
Utinni.Cli.Tests/Infrastructure/TreFixtureBuilder.cs               // synthesize a deterministic .tre fixture to repack + byte-compare
FormSaveConfirmDialog (08-06)                                       // reuse for the repack confirm + "back up first" checkbox

<!-- Linked-source pattern (precedent in UtinniCoreDotNet.Tests.csproj lines 44-55):
     <Compile Include="..\UtinniCoreDotNetGen\SlnDirResolver.cs" Link="SlnDirResolver.cs" />
     <Compile Include="..\UtinniCoreDotNetGen\HeaderDiscovery.cs" Link="HeaderDiscovery.cs" />
     <Compile Include="..\sdk\UtinniPluginTemplates\Vsix\Utility\Props.cs" Link="Vsix\Props.cs" />
     The TreFixtureBuilder linkage below mirrors this exact ItemGroup convention.
     Note: UtinniCoreDotNet.Tests.csproj is SDK-style but uses explicit `<Compile Include>` for
     linked-source files (outside the project tree). This is the canonical pattern for linking files
     from sibling projects without taking a ProjectReference. -->

<!-- csproj insertion-point references (round-2 HIGH-A): -->
<!-- UtinniCoreDotNet.csproj is OLD-STYLE; line 73 is `<Compile Include="Formats\Tre\TrePayloadResolver.cs" />`. -->
<!-- The new `<Compile Include="Formats\Tre\TreWriter.cs" />` goes adjacent to the Tre cluster (lines 69-76).   -->
<!-- TheJawaToolboxDotNet.csproj is OLD-STYLE; existing Saving entries are IffSaveTargets.cs (08-05),            -->
<!--   ClientReloadDispatcher.cs (08-05), LivePatchSaveTarget.cs (08-06).                                        -->
<!-- The new `<Compile Include="Saving\TreRepackSaveTarget.cs" />` joins that cluster.                           -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TreFile.GetRecordCompressedBytes(int) AND TreFile.GetRecordNameBytes(int) — verbatim raw-slice APIs (08-REVIEWS HIGH-1 + round-2 MEDIUM 6)</name>
  <read_first>
    - D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TreFile.cs (GetRecordData lines 363-425 — the existing lazy-on-demand read path to mirror; `_sourcePath` invariant; PayloadReadCount; the FileStream(FileMode.Open, FileAccess.Read, FileShare.Read) discipline; ReadFully helper; T-04-DoS / Truncated error patterns; AND the name-block layout — examine how rec.NameOffset is parsed and how rec.Name is built; the byte length of each record's name is the length-of-the-decoded-string + null-terminator if present)
    - D:/Code/Utinni/UtinniCoreDotNet.Tests/FormatsTests/Tre/TreFileTests.cs (existing test patterns + assertion style for the new tests file)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-REVIEWS.md (HIGH-1 disposition specifying GetRecordCompressedBytes; round-2 MEDIUM 6 disposition specifying GetRecordNameBytes — codex unique)
  </read_first>
  <behavior>
    GetRecordCompressedBytes:
    - `public byte[] GetRecordCompressedBytes(int index)` opens `_sourcePath` (the same lazy contract as GetRecordData — throws InvalidOperationException for stream-backed instances) and reads exactly `rec.CompressedSize` bytes starting at `rec.Offset`; returns a fresh byte[] the caller may mutate freely
    - For `rec.CompressedSize == 0`, returns `new byte[0]` (consistent with GetRecordData's empty-payload behavior)
    - Throws `ArgumentOutOfRangeException` on bad index (mirror GetRecordData line 367)
    - Throws `TreParseException(TreParseError.Truncated, ...)` if the file is shorter than expected at that offset (mirror GetRecordData lines 391-395)
    - Does NOT decompress; does NOT validate the compressed payload's framing (this is the raw slice — TreWriter writes it verbatim)
    - Does NOT touch `PayloadReadCount` (or DOES, implementer's choice — but must be documented; it is observable test state)

    GetRecordNameBytes (round-2 MEDIUM 6):
    - `public byte[] GetRecordNameBytes(int index)` opens `_sourcePath` and reads the raw name-block slice for record `i` starting at `rec.NameOffset`. Length = byte length of the name string AS STORED IN THE FILE (this includes the null terminator if the SWG format writes one; mirror what TreFile.Parse consumes when materializing `rec.Name`).
    - For `rec.NameOffset` pointing at an empty name (length 0), returns `new byte[0]`.
    - Throws `ArgumentOutOfRangeException` on bad index.
    - Throws `TreParseException(TreParseError.Truncated, ...)` if the file is shorter than expected.
    - Returns a fresh byte[] the caller may mutate freely.
    - The byte length is determined the same way TreFile.Parse determines it (look at the existing parser at TreFile.cs to find how rec.Name's byte length is derived — typically `Encoding.ASCII.GetByteCount(rec.Name) + (hasNullTerminator ? 1 : 0)`, or by scanning for null in the name block). Be consistent with the parser.
  </behavior>
  <action>
    Edit `UtinniCoreDotNet/Formats/Tre/TreFile.cs`. Add BOTH new methods immediately after the existing
    `GetRecordData(int)` method (keep adjacent for readability):

    1) `public byte[] GetRecordCompressedBytes(int index)` — mirror GetRecordData's structural pattern:
       bounds-check the index; reject stream-backed instances with InvalidOperationException citing
       `_sourcePath`; for zero-length records return `new byte[0]`; else open
       `new FileStream(_sourcePath, FileMode.Open, FileAccess.Read, FileShare.Read)`, Seek to
       `rec.Offset`, ReadFully `rec.CompressedSize` bytes into a fresh byte[]; throw
       `TreParseException(TreParseError.Truncated, ...)` if the read returns fewer bytes than requested.
       Add XML docs explaining the verbatim contract and that TreWriter uses this to preserve untouched
       entries (08-REVIEWS HIGH-1).

    2) `public byte[] GetRecordNameBytes(int index)` — same lazy / stream-backed / bounds discipline as
       (1). The byte length is computed by examining the SAME logic TreFile.Parse uses to materialize
       `rec.Name`. Common case: if the format writes a null terminator, the length is
       `Encoding.ASCII.GetByteCount(rec.Name) + 1`; if not, just the byte count. CRITICAL: the byte
       length MUST match what TreFile.Parse consumed from the name block at `rec.NameOffset` — otherwise
       the writer's name-block reconstruction will be off by one. The implementer may either: (a) cache
       the byte length during Parse (store on TreRecord as a new internal int `NameByteLength`), OR
       (b) re-derive it at GetRecordNameBytes call time by reading from `rec.NameOffset` and scanning
       for null. Option (a) is cleaner; option (b) avoids touching the record schema. Pick one and
       document on the method's XML comment. Add XML docs explaining the round-2 MEDIUM 6 disposition
       (name-block byte-layout preservation makes NameOffset round-trip meaningful).

    Then create `UtinniCoreDotNet.Tests/FormatsTests/Tre/TreFileGetRecordCompressedBytesTests.cs`
    (verbatim MIT banner) covering BOTH new methods:

    For GetRecordCompressedBytes: (1) bounds — negative + out-of-range throws ArgumentOutOfRangeException;
    (2) stream-backed instance throws InvalidOperationException; (3) returned bytes EQUAL the actual file
    bytes at [rec.Offset, rec.Offset+rec.CompressedSize) — read the test fixture file independently and
    compare; (4) zero-CompressedSize record returns `new byte[0]`; (5) fresh-copy contract — calling the
    method twice returns two distinct arrays (one can be mutated without affecting the other); (6)
    truncated file (synthesize a fixture truncated mid-record) throws TreParseException.Truncated.

    For GetRecordNameBytes: (round-2 MEDIUM 6) (7) bounds — negative + out-of-range throws
    ArgumentOutOfRangeException; (8) stream-backed instance throws InvalidOperationException; (9)
    returned bytes EQUAL the actual file bytes at [rec.NameOffset, rec.NameOffset + nameByteLength) —
    read the test fixture file's name block independently and compare; (10) the byte length matches
    what TreFile.Parse uses (decode the returned bytes as ASCII, strip a trailing null if present, and
    compare to `rec.Name`); (11) fresh-copy contract.

    NOTE: this is a NEW public API surface on TreFile. Adding it is binary-safe (no existing signature
    changes). Phase 7 read path is untouched.

    NO csproj edits needed for TreFile.cs (already listed at line 69) or for the test file (SDK-style
    auto-glob). The new test file under `FormatsTests/Tre/` auto-includes.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~GetRecordCompressedBytes"</automated>
  </verify>
  <acceptance_criteria>
    - TreFile.cs gains `public byte[] GetRecordCompressedBytes(int index)` AND `public byte[] GetRecordNameBytes(int index)` adjacent to GetRecordData with full XML docs
    - GetRecordCompressedBytes mirrors GetRecordData's lazy / stream-backed / bounds / fresh-copy / Truncated contracts; does NOT decompress; returns raw bytes
    - GetRecordNameBytes mirrors the same disciplines; returns raw name-block bytes; byte length is consistent with what TreFile.Parse consumes (round-2 MEDIUM 6)
    - TreFileGetRecordCompressedBytesTests covers ALL behavior cases for BOTH methods (bounds, stream-backed, equality to file bytes, zero-length, fresh-copy, truncated; AND for GetRecordNameBytes: bounds, stream-backed, equality to file name-block bytes, length-matches-Parse, fresh-copy)
    - `dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~GetRecordCompressedBytes"` exits 0
    - Existing TreFile public API is unchanged (binary-safe addition)
  </acceptance_criteria>
  <done>TreFile gains verbatim raw-compressed-slice AND raw-name-bytes APIs the repack consumes to preserve untouched entries byte-for-byte (including the name block, round-2 MEDIUM 6).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TreWriter — full .tre repack copying raw compressed AND raw name bytes for untouched entries (08-REVIEWS HIGH-1 + round-2 MEDIUM 6) + two-guarantee + name-byte-identity + TOC invariant test + UtinniCoreDotNet.csproj entry (round-2 HIGH-A) + linked-source TreFixtureBuilder</name>
  <read_first>
    - D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TreFile.cs (Parse header 139-187; TOC stride 194-297; Checksum field 337; GetRecordData 363-425; GetRecordCompressedBytes + GetRecordNameBytes from Task 1; Inflate 486-562; MaxBlockSize 62)
    - D:/Code/Utinni/UtinniCoreDotNet.Tests/FormatsTests/Tre/TreFileTests.cs + TreFileFixtures.cs (test + fixture patterns)
    - D:/Code/Utinni/Utinni.Cli.Tests/Infrastructure/TreFixtureBuilder.cs (synthesize a deterministic .tre fixture)
    - D:/Code/Utinni/UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj (the linked-source `<Compile Include="..\Utinni.Cli.Tests\Infrastructure\TreFixtureBuilder.cs" Link="Infrastructure\TreFixtureBuilder.cs" />` is added in this task — mirrors the existing SlnDirResolver/HeaderDiscovery/Props linked-source ItemGroup at lines 44-55)
    - D:/Code/Utinni/UtinniCoreDotNet/UtinniCoreDotNet.csproj (OLD-STYLE; line 73 is `<Compile Include="Formats\Tre\TrePayloadResolver.cs" />` — the new TreWriter entry goes adjacent to the Tre cluster at lines 69-76)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-RESEARCH.md (Pattern 4 repack steps; Pitfall 5 CRC; Don't-Hand-Roll zlib framing + Adler32; Open Questions Q1 RESOLVED)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-PATTERNS.md (TreWriter section lines 120-145)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-REVIEWS.md (HIGH-1 disposition: raw-slice copy + TOC invariant test fields; round-2 MEDIUM 6: name-block byte-layout preservation)
  </read_first>
  <behavior>
    - TreWriter.Repack(TreFile original, IDictionary<int, byte[]> edits) -> byte[] new archive
    - For an UNTOUCHED entry: copy raw compressed bytes via `original.GetRecordCompressedBytes(i)` (Task 1) AND raw name bytes via `original.GetRecordNameBytes(i)` (Task 1, round-2 MEDIUM 6) and write them VERBATIM to the new archive (compressed bytes go to the payload region; name bytes go to the name block). Preserve the original TreRecord.Compressor flag, Checksum, NameOffset (re-computed to point at the new name block's correct offset for this entry's name bytes), NameString, UncompressedSize, AND CompressedSize EXACTLY (Codex unique: TOC invariants beyond CRC). The KEY insight (round-2 MEDIUM 6): NameOffset preservation is meaningful ONLY because the name bytes are byte-identical at the offset they point to.
    - For an EDITED entry (index in `edits`): take the new payload bytes; recompress (V1 picks 'deflate' (compressor=1) as default for the edited entry — documented explicitly); compute new CompressedSize / UncompressedSize; the Checksum (path CRC) is preserved from the original because the path is unchanged (Open Q1 / A1); reuse the original name bytes (path unchanged → name unchanged); the NameOffset for the edited entry's NAME points at the position in the new name block where its (unchanged) name bytes live.
    - Two-guarantee + name-byte-identity acceptance (08-REVIEWS HIGH-1 + round-2 MEDIUM 6):
      (a) Logical payload identity: for every untouched entry, GetRecordData on the repacked archive equals GetRecordData on the original
      (b) Raw compressed slice identity: for every untouched entry, the byte range [newRec.Offset, newRec.Offset+newRec.CompressedSize) in the repacked archive equals [oldRec.Offset, oldRec.Offset+oldRec.CompressedSize) in the original
      (c) (round-2 MEDIUM 6) Raw name bytes identity: for every untouched entry, GetRecordNameBytes(repacked, i) SequenceEqual GetRecordNameBytes(original, i)
    - For the EDITED entry: only guarantee that GetRecordData on the repacked archive returns the new payload bytes; the compressed slice WILL differ from the original; the name bytes are identical (path unchanged)
    - zlib framing on write (ONLY for the edited entry, when compressor=1 or 2): RFC1950 2-byte header (0x78 0x9c) + DeflateStream body + 4-byte Adler32 trailer (computed manually; BCL DeflateStream emits no Adler)
    - The TOC stride matches TreVersions.RecordStride(version); the 36-byte header offsets/sizes are written last once block sizes are known
    - Inflate/expansion during repack stays capped at MaxBlockSize (256 MB) — relevant only for the edited entry (no decompress of untouched entries; we copy raw)
    - TOC invariant test (per-record assertion, applied to every untouched entry — 08-REVIEWS HIGH-1 + round-2 MEDIUM 6):
      - new.Checksum == old.Checksum
      - new.NameOffset == old.NameOffset OR the new offset, computed correctly, points at byte-identical name bytes
      - new.NameString == old.NameString
      - new.Compressor == old.Compressor
      - new.UncompressedSize == old.UncompressedSize
      - new.CompressedSize == old.CompressedSize
      - GetRecordData(new, i).SequenceEqual(GetRecordData(old, i))
      - GetRecordCompressedBytes(new, i).SequenceEqual(GetRecordCompressedBytes(old, i))
      - **GetRecordNameBytes(new, i).SequenceEqual(GetRecordNameBytes(old, i))** (round-2 MEDIUM 6 — new assertion)
  </behavior>
  <action>
    PART A — Create the writer.
    Create `UtinniCoreDotNet/Formats/Tre/TreWriter.cs` (verbatim banner). For each entry, branch on
    edited vs untouched:

      - UNTOUCHED: `byte[] rawCompressed = original.GetRecordCompressedBytes(i);` (Task 1) AND
        `byte[] rawName = original.GetRecordNameBytes(i);` (Task 1, round-2 MEDIUM 6) — write the
        compressed slice into the new archive's payload region verbatim; write the name bytes into the
        new name block verbatim; preserve TreRecord.Compressor, Checksum, NameOffset (re-computed
        relative to the new name block layout — but since the name bytes are byte-identical, the
        offset arithmetic just sums prior records' byte lengths), NameString, UncompressedSize,
        CompressedSize EXACTLY in the rebuilt TOC. NO decompress, NO recompress, NO name reformatting.
      - EDITED: take `edits[i]` as the new payload bytes; compute compressed bytes by inverting Inflate
        (PATTERNS lines 120-145 / RESEARCH Pattern 4): RFC1950 header + `DeflateStream` + a
        manually-computed Adler32 trailer; set Compressor = 1 (deflate) as V1 default for the edited
        entry (documented); preserve the original Checksum (path unchanged); reuse the original name
        bytes (path unchanged); set UncompressedSize / CompressedSize from the new bytes.

    Lay out: 36-byte header (`EERT` + version tag + 7 u32 fields) -> payload blobs -> TOC block
    (`RecordStride` per version) -> name block, writing header offsets/sizes last. LE header/TOC fields
    (TRE is little-endian — do not cross with IFF's big-endian). Keep the `MaxBlockSize` 256 MB cap on
    any decompress (only relevant for the edited entry).

    PART B — csproj coverage (round-2 HIGH-A).
    Edit `UtinniCoreDotNet/UtinniCoreDotNet.csproj`. Inside the existing main `<ItemGroup>` at lines
    62-208, add ONE new entry adjacent to the Tre cluster (lines 69-76):

      <Compile Include="Formats\Tre\TreWriter.cs" />

    Recommended placement: immediately after `<Compile Include="Formats\Tre\TreRecord.cs" />` (line 76).
    No `<SubType>` needed.

    PART C — Linked-source for the fixture builder.
    Edit `UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj` — add a linked-source `<Compile>` entry
    to the existing ItemGroup at lines 44-55 (the SlnDirResolver / HeaderDiscovery / Props linked-source
    block):

      <Compile Include="..\Utinni.Cli.Tests\Infrastructure\TreFixtureBuilder.cs" Link="Infrastructure\TreFixtureBuilder.cs" />

    This mirrors the existing pattern verbatim (relative `..\` path + `Link=` for the project-local
    folder layout) and avoids a `ProjectReference` to `Utinni.Cli.Tests` (which would couple two test
    assemblies). `TreFixtureBuilder.cs` is dependency-free (System.IO + System.IO.Compression +
    System.Text only) — same constraint that lets `Props.cs` link cleanly.

    PART D — Test file.
    Create `UtinniCoreDotNet.Tests/FormatsTests/Tre/TreWriterTests.cs`: synthesize a deterministic
    fixture via the now-linked `TreFixtureBuilder`. Three tests:
      [Fact 1] Edit-free repack: assert (a) the output re-opens to an equal TOC; (b) for EVERY record,
               every TOC invariant field matches the original (Checksum, NameOffset, NameString,
               Compressor, UncompressedSize, CompressedSize); (c) GetRecordData is equal for every
               record; (d) GetRecordCompressedBytes is equal for every record; (e) (round-2 MEDIUM 6)
               GetRecordNameBytes is equal for every record.
      [Fact 2] Payload-only edit: edit exactly ONE record's payload via the edits dictionary; assert
               for the EDITED record GetRecordData equals the new payload (and the compressed slice
               WILL differ — that's fine); assert for EVERY OTHER record the full TOC invariant set +
               GetRecordData equality + GetRecordCompressedBytes equality + (round-2 MEDIUM 6)
               GetRecordNameBytes equality holds.
      [Fact 3] (08-REVIEWS HIGH-1 explicit + round-2 MEDIUM 6 explicit) Full TOC invariant per-record
               assertion table: implement the NINE TOC invariant comparisons listed in `<behavior>` as a
               single helper applied per-record; assert it passes for every untouched record in both
               Fact 1 and Fact 2.

    NO csproj edit for the test file itself — SDK-style auto-glob picks it up. The linked-source entry
    in PART C is for the FIXTURE BUILDER, not for the test file.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~TreWriter" && grep -c '<Compile Include="Formats\\\\Tre\\\\TreWriter.cs"' UtinniCoreDotNet/UtinniCoreDotNet.csproj</automated>
  </verify>
  <acceptance_criteria>
    - TreWriter.cs exists with the MIT+provenance banner and `class TreWriter`
    - TreWriter copies raw compressed bytes via TreFile.GetRecordCompressedBytes AND raw name bytes via TreFile.GetRecordNameBytes for untouched entries — grep proves both APIs are referenced
    - TreWriter preserves each untouched entry's TreRecord.Checksum AND Compressor flag AND NameOffset AND NameString AND UncompressedSize AND CompressedSize (verified by the TOC invariant test)
    - zlib write framing emits RFC1950 header + DeflateStream body + a computed Adler32 trailer ONLY for the edited entry
    - `UtinniCoreDotNet/UtinniCoreDotNet.csproj` contains `<Compile Include="Formats\Tre\TreWriter.cs" />` (grep gate: returns 1; round-2 HIGH-A)
    - `UtinniCoreDotNet.Tests.csproj` contains `<Compile Include="..\Utinni.Cli.Tests\Infrastructure\TreFixtureBuilder.cs" Link="Infrastructure\TreFixtureBuilder.cs" />` inside the existing linked-source ItemGroup (lines 44-55)
    - `dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~TreWriter"` exits 0 after a VS2026 MSBuild
    - The two-guarantee test proves (a) logical payload identity AND (b) raw compressed slice identity for untouched entries after an edit-free repack AND after a payload-only edit
    - (round-2 MEDIUM 6) The name-byte-identity assertion is part of the per-record TOC invariant test: `dst.GetRecordNameBytes(i).SequenceEqual(src.GetRecordNameBytes(i))` for every untouched record `i`
    - The TOC invariant per-record test proves every named TOC field matches for every untouched record (NINE invariants total)
    - The repack acceptance NO LONGER references the misleading "archive bytes identical" full-file claim (08-REVIEWS HIGH-1)
    - MaxBlockSize 256 MB cap retained on decompress
  </acceptance_criteria>
  <done>TreWriter repacks .tre archives by full rebuild, copying raw compressed bytes AND raw name bytes for untouched entries (TOC invariants byte-for-byte preserved INCLUDING the name block layout — round-2 MEDIUM 6); only the edited entry recompresses. Two-guarantee + name-byte-identity + TOC invariant per-record test gate it; explicit csproj entries added (round-2 HIGH-A).</done>
</task>

<task type="auto">
  <name>Task 3: TreRepackSaveTarget + Save▾ wiring — provenance + timestamped backup + locked-archive fallback (08-REVIEWS MEDIUM-10) + TheJawaToolboxDotNet.csproj entry (round-2 HIGH-A)</name>
  <read_first>
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs (Save▾ drop-down + the disabled repack placeholder from 08-05 + the Source property from 08-04)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormSaveConfirmDialog.cs (reusable confirm + checkbox, from 08-06)
    - D:/Code/Utinni/UtinniCoreDotNet/Formats/Iff/OpenSource.cs (08-01 Task 3 — TreArchive)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-UI-SPEC.md (repack confirm copy: heading "Repack <archive>.tre?", buttons "Repack"/"Cancel", opt-in "back up the .tre first" checkbox defaulted on)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-REVIEWS.md (MEDIUM-10: timestamped backup + locked-archive fallback)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-05-SUMMARY.md (how the editor tracks the source .tre + entry index for an IFF opened from the TRE Browser via OpenSource.TreArchive)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj (OLD-STYLE; new `<Compile Include="Saving\TreRepackSaveTarget.cs" />` joins the existing Saving cluster from 08-05/08-06)
  </read_first>
  <action>
    PART A — Create the save target.
    Create `Saving/TreRepackSaveTarget.cs` in TheJawaToolboxDotNet (verbatim MIT banner). Public surface:
    `public static TreRepackResult Apply(OpenSource.TreArchive target, byte[] rewrittenIffBytes, bool createBackup)`
    where `TreRepackResult` is a small enum/struct: `Replaced` / `BackedUpThenReplaced` /
    `RefusedClientHoldsArchive_LooseOverrideRecommended` / `Failed`.

    Steps on a background `Task`:
      1. **Open + repack:** `TreFile.Open(target.TrePath)`; build an edits dictionary
         `{ target.RecordIndex: rewrittenIffBytes }`; call `TreWriter.Repack(original, edits)` ->
         repacked bytes.
      2. **Timestamped backup (if createBackup is on, 08-REVIEWS MEDIUM-10):**
         `backupPath = target.TrePath + "." + DateTime.UtcNow.ToString("yyyyMMdd-HHmmss") + ".bak"`.
         If `File.Exists(backupPath)` (extremely unlikely sub-second collision), append a `-N` suffix
         until a non-existing name is found. `File.Copy(target.TrePath, backupPath, overwrite: false)`.
         NEVER overwrite a prior backup.
      3. **Temp-write + atomic replace:**
         `tempPath = target.TrePath + ".tmp-" + Guid.NewGuid().ToString("N")` on the SAME volume as
         `target.TrePath`. `File.WriteAllBytes(tempPath, repacked)`; then `File.Replace(tempPath,
         target.TrePath, destinationBackupFileName: null, ignoreMetadataErrors: true)`.
      4. **Locked-archive fallback (08-REVIEWS MEDIUM-10):** if step 3 throws `IOException`
         (HResult 0x80070020 ERROR_SHARING_VIOLATION, OR more broadly any IOException whose Message /
         InnerException suggests the file is in use), do NOT partial-write. Delete the temp file.
         Return `RefusedClientHoldsArchive_LooseOverrideRecommended`. The UI surfaces the candid
         copy: "The client appears to hold <archive>.tre open. Save a loose override of the edited
         entry instead, or close the client and retry."
      5. Otherwise success: return `Replaced` (or `BackedUpThenReplaced` if createBackup was on).

    In `FormIffEditor`, enable the `Repack into source .tre…` `Save ▾` item ONLY when
    `this.Source is OpenSource.TreArchive ta` (08-REVIEWS HIGH-2 provenance gate). On click show
    `FormSaveConfirmDialog` with heading `Repack <Path.GetFileName(ta.TrePath)>?`, body explaining the
    repack writes the full archive + replaces atomically, an opt-in `Create a timestamped backup
    (<name>.tre.<yyyyMMdd-HHmmss>.bak) first` checkbox DEFAULTED ON, buttons `Repack` / `Cancel`. On
    `Repack`:
      - Serialize via `IffWriter.Write(mutableDoc)` -> `rewrittenIffBytes`.
      - Call `TreRepackSaveTarget.Apply(ta, rewrittenIffBytes, createBackup: checkboxState)`.
      - Translate result to status copy:
        - `Replaced` / `BackedUpThenReplaced` -> success; clear dirty markers; offer the tiered reload
          (08-05 ClientReloadDispatcher).
        - `RefusedClientHoldsArchive_LooseOverrideRecommended` -> status with the candid copy AND
          automatically pre-select the `Save as loose override` Save▾ item so a single click completes
          the recovery (do NOT auto-save without user consent).
        - `Failed` -> status "Repack failed — your edits are retained. See log."

    Do not alter the other save modes.

    PART B — csproj coverage (round-2 HIGH-A).
    Edit `../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj`. Add:

      <Compile Include="Saving\TreRepackSaveTarget.cs" />

    Adjacent to the existing Saving cluster (IffSaveTargets.cs + ClientReloadDispatcher.cs from 08-05;
    LivePatchSaveTarget.cs from 08-06). No `<SubType>` needed.

    NO csproj edit for FormIffEditor.cs — already listed (added by 08-04 Task 2). Modification ≠ addition.
  </action>
  <verify>
    <automated>grep -c "TreWriter\|TreRepackSaveTarget\|yyyyMMdd-HHmmss" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/TreRepackSaveTarget.cs" && grep -c '<Compile Include="Saving\\\\TreRepackSaveTarget.cs"' "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"</automated>
  </verify>
  <acceptance_criteria>
    - TreRepackSaveTarget.cs exists with the MIT banner and calls TreWriter.Repack on a background Task
    - It TIMESTAMPS backups (`<name>.tre.<yyyyMMdd-HHmmss>.bak`) — grep proves `yyyyMMdd-HHmmss` is referenced AND code does NOT overwrite an existing backup of the same name
    - It writes to a sibling temp file on the same volume and uses `File.Replace` for atomicity
    - On atomic-replace failure (IOException matching sharing-violation), returns `RefusedClientHoldsArchive_LooseOverrideRecommended` and the temp file is deleted (no partial-write)
    - The `Repack into source .tre…` item is enabled ONLY for `Source is OpenSource.TreArchive ta`; it shows FormSaveConfirmDialog with the timestamped-backup checkbox DEFAULTED ON and only repacks on the `Repack` verb
    - A successful repack offers the tiered reload; a locked-archive fallback pre-selects the loose-override save mode but does NOT auto-save; a generic failure retains dirty state and never truncates the archive
    - `TheJawaToolboxDotNet.csproj` contains `<Compile Include="Saving\TreRepackSaveTarget.cs" />` (grep gate: returns 1; round-2 HIGH-A)
    - VS2026 MSBuild of TheJawaToolbox compiles clean
  </acceptance_criteria>
  <done>The confirmed .tre repack is reachable from Save▾ ONLY when provenance is TreArchive, backs up with a TIMESTAMPED non-overwriting name, atomically replaces, falls back honestly when the client holds the archive open, and offers an in-session tiered reload on success; explicit csproj entry added (round-2 HIGH-A).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live-SWG smoke — .tre repack (D-05.4) + V1-criterion sign-off — locked-archive smoke + edited-entry CRC live-resolution check (cursor N-H1 ACK)</name>
  <files>(no files modified — Tier-4 manual verification + phase sign-off checkpoint; see <how-to-verify>)</files>
  <action>
    Pause for the maintainer to run the live-SWG repack smoke in <how-to-verify>: build inline + run the
    full managed suite, inject into a live client, edit an IFF opened from a packed `.tre` (Source =
    OpenSource.TreArchive), repack with TIMESTAMPED backup on + atomically replace the source, and
    reload in client (tiered) — confirming the client resolves the repacked archive AND every untouched
    entry (no CRC/TOC resolution failure, Pitfall 5) AND specifically that the EDITED record's path
    still resolves under its original path (cursor N-H1 ACK — verifies the path-CRC-only assumption).
    ALSO test the locked-archive fallback: deliberately attempt a repack WHILE the client holds the
    archive open and confirm the candid copy surfaces with the loose-override recommendation
    (08-REVIEWS MEDIUM-10). The maintainer then signs off the consolidated Phase 8 success criteria
    (1-5 with tiered Criterion 2) and records the observation in the SUMMARY. Do NOT auto-advance —
    the `.tre` repack has the largest blast radius and no CI fallback for live resolution.
  </action>
  <verify>
    <human-check>The client resolves the repacked archive incl. all untouched entries AND the edited record under its original path (cursor N-H1 ACK — path-CRC-only assumption verified) and reflects the edit on tiered reload; a TIMESTAMPED backup was created and the original replaced without truncation; the locked-archive fallback surfaces the loose-override recommendation candidly (no partial-write); all 5 Phase 8 success criteria (Criterion 2 evaluated under the tiered acceptance) are confirmed and recorded in the SUMMARY. If the edited record's path FAILS to resolve, the path-CRC-only assumption is INVALIDATED and a follow-up phase MUST add payload-CRC recomputation — note this in the SUMMARY.</human-check>
  </verify>
  <what-built>
    The full `.tre` repack save mode: edit an IFF opened from a packed `.tre` (Source = OpenSource.TreArchive),
    repack the archive (TIMESTAMPED backup on), atomically replace the source, and reload in client
    (tiered). Plus a consolidated read of all Phase 8 success criteria. Plus the cursor N-H1 ACK live
    check that the edited record's path still resolves (path-CRC-only assumption).
  </what-built>
  <how-to-verify>
    1. Build TheJawaToolbox + UtinniCoreDotNet with VS2026 MSBuild (Release), inline; run `dotnet test --no-build` (full managed suite green, incl. roundtrip-iff + TreWriter + GetRecordCompressedBytes + GetRecordNameBytes + LooseOverridePath + TreHandoffFallback + LivePatchValidator + IffEditController + OpenSource + IffWriter).
    2. Inject into a live SWGEmu/Restoration client; open TJT + the IFF Editor.
    3. Open an IFF from the TRE Browser (packed `.tre` source); confirm Source is OpenSource.TreArchive with trePath / recordIndex / logicalPath populated correctly; edit a leaf payload.
    4. Save ▾ -> `Repack into source .tre…`; confirm the dialog copy includes the timestamped-backup checkbox (DEFAULTED ON) and the heading shows the archive name; click `Repack`.
    5. Confirm a `<name>.tre.<yyyyMMdd-HHmmss>.bak` backup was created (timestamped, NOT `.tre.bak`) and the original was replaced without truncation; `fc /b` the untouched entries against the backup to confirm raw-compressed-slice byte-identity AND raw-name-bytes byte-identity (or rely on the automated TreWriter two-guarantee + name-byte-identity tests + a spot check).
    6. Reload in client (tiered: if the edited asset is a texture/terrain, expect in-session reload; otherwise expect PendingNextSceneChange and trigger a TJT scene change to verify); confirm the client resolves the archive and reflects the edit.
    7. **Cursor N-H1 ACK check (path-CRC-only verification):** explicitly verify the EDITED record IS RESOLVED BY THE CLIENT under its ORIGINAL path (not a stale cached payload from a different lookup). If the client fails to resolve the edited record's path, the path-CRC-only assumption (Open Q1/A1) is INVALIDATED and a follow-up phase MUST add payload-CRC recomputation. Record the result in the SUMMARY: PATH-CRC-OK or PATH-CRC-INVALIDATED.
    8. Confirm the client still loads ALL other files from the repacked archive (no CRC/TOC resolution failure for untouched entries — Pitfall 5).
    9. **Locked-archive smoke (08-REVIEWS MEDIUM-10):** while the client is actively running and holding the archive open, attempt the repack again; confirm the result is `RefusedClientHoldsArchive_LooseOverrideRecommended` with the candid copy, the loose-override Save▾ item is pre-selected (but NOT auto-saved), and the original .tre is UNCHANGED (compare hash/size to before the attempt).
    10. Consolidated Phase 8 criteria check: (1) editor loads in TJT against a live client; (2) open/view/edit/save->reload works under tiered acceptance (texture/terrain in-session, others on scene change — 08-REVIEWS HIGH-4 disposition); (3) inspect-iff golden still green; (4) roundtrip-iff byte-exact + payload-mutation golden + structural-removal golden (round-2 MEDIUM 8) gates green; (5) primitives live in UtinniCoreDotNet/Formats/Iff consumed by direct reference (ROADMAP amended).
  </how-to-verify>
  <resume-signal>Type "approved" with the repack + archive-resolution observation + edited-entry path resolution observation (cursor N-H1 ACK) + locked-archive fallback observation, or describe issues (esp. any entry the client can no longer resolve OR any partial-write on the locked-archive case OR a PATH-CRC-INVALIDATED result from step 7).</resume-signal>
  <done>The maintainer has approved with the repack + archive-resolution + edited-entry path resolution (cursor N-H1) + locked-archive fallback observations recorded in the SUMMARY; the client resolves the repacked archive incl. untouched entries (TOC invariants AND name-block byte-layout preserved per round-2 MEDIUM 6); the edited entry's path still resolves (cursor N-H1 ACK PATH-CRC-OK — or a PATH-CRC-INVALIDATED follow-up is documented); locked-archive fallback is honest with no partial-write; and all 5 Phase 8 success criteria (Criterion 2 under tiered acceptance) are signed off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor -> rewritten .tre archive on disk | a shared archive the client loads; a corrupt repack breaks resolution for many files |
| crafted .tre payloads during repack | malformed/decompression-bomb input during the rebuild (relevant only for the edited entry now, since untouched entries are copied raw) |
| OpenSource.TreArchive descriptor -> repack enable | provenance gate is the only thing that lets the menu enable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-17 | Tampering | repack recomputing a wrong path CRC -> client can't resolve entries | mitigate | Preserve each untouched entry's stored Checksum; never recompute the unverified path CRC (Task 2, Open Q1/A1); verified at Tier-4 in Task 4 step 7 (cursor N-H1 ACK) |
| T-08-17b | Tampering | repack changing compressor flag / name-offset / TOC stride / record stride for untouched entries -> client can't resolve | mitigate | Per-record TOC invariant test asserts Checksum + NameOffset + NameString + Compressor + UncompressedSize + CompressedSize byte-for-byte for every untouched entry (Task 2 — 08-REVIEWS HIGH-1 Codex unique) |
| T-08-17b2 | Tampering | repack changing the name block byte layout for untouched entries -> NameOffset preservation is misleading (round-2 MEDIUM 6 / codex unique) | mitigate | Per-record TOC invariant test ALSO asserts `dst.GetRecordNameBytes(i).SequenceEqual(src.GetRecordNameBytes(i))` for every untouched entry (Task 2 — round-2 MEDIUM 6) |
| T-08-17c | Tampering | repack decompress/recompress producing different compressed bytes than original (deflate non-determinism) | mitigate | Copy raw compressed slices via GetRecordCompressedBytes for untouched entries — no decompress/recompress (Task 1 / Task 2 — 08-REVIEWS HIGH-1) |
| T-08-18 | DoS | decompression bomb during repack | mitigate | Keep TreFile's MaxBlockSize 256 MB inflate cap on the repack path; only relevant for the edited entry now (untouched copied raw) (Task 2) |
| T-08-19 | Tampering / data loss | a failed repack truncating/corrupting the shared archive | mitigate | Timestamped backup (default on, never overwrite) + atomic temp-then-replace; locked-archive fallback returns refused result with no partial-write (Task 3 — 08-REVIEWS MEDIUM-10) |
| T-08-19b | Repudiation | backup naming `<name>.tre.bak` overwriting prior known-good backup | mitigate | Timestamped backup naming `<name>.tre.<yyyyMMdd-HHmmss>.bak` — never overwrites a prior backup (Task 3 — 08-REVIEWS MEDIUM-10 Codex unique) |
| T-08-19c | Repudiation | menu enabled on wrong provenance -> repack runs on the wrong archive | mitigate | Provenance gate: enabled only when `Source is OpenSource.TreArchive ta` (Task 3 — 08-REVIEWS HIGH-2) |
| T-08-19d | Tampering | new .cs files silently fail to compile into UtinniCoreDotNet.dll / TheJawaToolboxDotNet.dll because the OLD-STYLE csproj files do not glob them | mitigate | Explicit `<Compile Include>` entries for TreWriter.cs (UtinniCoreDotNet.csproj) AND TreRepackSaveTarget.cs (TheJawaToolboxDotNet.csproj); linked-source entry for TreFixtureBuilder.cs in test csproj; grep-gated acceptance (Tasks 2, 3 — round-2 HIGH-A) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No external packages added this plan (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- VS2026 MSBuild of UtinniCoreDotNet + UtinniCoreDotNet.Tests + TheJawaToolbox compiles clean.
- `dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~GetRecordCompressedBytes"` exits 0 (covers both new TreFile APIs).
- `dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~TreWriter"` exits 0 (two-guarantee + name-byte-identity + TOC invariant).
- Repack two-guarantee + per-record TOC invariant + name-byte-identity test passes for the synthetic fixture (automated; round-2 MEDIUM 6).
- UtinniCoreDotNet.csproj contains the `Formats\Tre\TreWriter.cs` Compile entry (round-2 HIGH-A grep gate).
- TheJawaToolboxDotNet.csproj contains the `Saving\TreRepackSaveTarget.cs` Compile entry (round-2 HIGH-A grep gate).
- UtinniCoreDotNet.Tests.csproj contains the linked-source `<Compile Include="..\Utinni.Cli.Tests\Infrastructure\TreFixtureBuilder.cs" Link="Infrastructure\TreFixtureBuilder.cs" />` entry (Task 2 PART C).
- Live-SWG smoke (Task 4) confirms the client resolves the repacked archive incl. untouched entries AND the edited entry under its original path (cursor N-H1 ACK), the timestamped backup created, the locked-archive fallback is honest, AND the consolidated Phase 8 criteria (Criterion 2 under tiered acceptance).
</verification>

<success_criteria>
- TreFile.GetRecordCompressedBytes AND TreFile.GetRecordNameBytes expose the verbatim raw-slice APIs (08-REVIEWS HIGH-1 + round-2 MEDIUM 6).
- TreWriter repacks .tre by full rebuild, copying raw compressed bytes AND raw name bytes for untouched entries (TOC invariants byte-for-byte preserved INCLUDING the name block layout); only the edited entry recompresses.
- The editor repack is provenance-gated (`Source is OpenSource.TreArchive ta`), backs up with TIMESTAMPED non-overwriting names, atomically replaces, and falls back honestly when the archive is locked.
- BOTH old-style csproj files (UtinniCoreDotNet.csproj + TheJawaToolboxDotNet.csproj) explicitly list every new production file (round-2 HIGH-A).
- Live smoke confirms the client resolves the repacked archive AND the edited entry's path under its original path (cursor N-H1 ACK), reflects the edit (tiered); all 5 Phase 8 criteria verified under the 08-REVIEWS HIGH-4 tiered acceptance for Criterion 2.
</success_criteria>

<output>
Create `.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-07-SUMMARY.md` when done (include the consolidated Phase 8 criteria sign-off, the locked-archive fallback observation, the verified repack matrix, the cursor N-H1 ACK edited-entry path resolution observation (PATH-CRC-OK or PATH-CRC-INVALIDATED follow-up), AND a one-line confirmation that the THREE csproj entries (UtinniCoreDotNet.csproj + TheJawaToolboxDotNet.csproj + the linked-source TreFixtureBuilder entry in the test csproj) close round-2 HIGH-A for this plan AND the name-block byte-layout preservation closes round-2 MEDIUM 6).
</output>
</content>
