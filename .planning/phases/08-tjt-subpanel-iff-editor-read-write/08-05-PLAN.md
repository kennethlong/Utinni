---
phase: 08-tjt-subpanel-iff-editor-read-write
plan: 05
type: execute
wave: 4
depends_on: ["08-04"]
files_modified:
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/IffSaveTargets.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/ClientReloadDispatcher.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"
  - UtinniCoreDotNet/Saving/LooseOverridePath.cs
  - UtinniCoreDotNet/Formats/Tre/TreRecordIndexResolver.cs
  - UtinniCoreDotNet/UtinniCoreDotNet.csproj
  - UtinniCoreDotNet.Tests/SavingTests/LooseOverridePathTests.cs
  - UtinniCoreDotNet.Tests/SavingTests/ClientReloadDispatcherTests.cs
  - UtinniCoreDotNet.Tests/FormatsTests/Iff/TreHandoffFallbackTests.cs
autonomous: false
requirements: [PROD-W1-IFF]
assumes:
  - "CONTEXT D-05/D-06 wording uplifted 2026-05-28 (commit 33cd3b9); plan now matches CONTEXT — no further uplift needed. D-06 disposition: TIERED ACCEPTANCE — PROD-W1-IFF Criterion 2 'client reloads correctly' is split (a) textures/terrain in-session via `Graphics.ReloadTextures()` / `GroundScene.Get().ReloadTerrain()` — pass; (b) datatable/STF/object-template — pass on NEXT scene change (user-triggered via the existing TJT scene-change parser callback path, the documented scene-change-via-TJT pattern); (c) no live client — Unavailable."
  - "Terrain reload call-site instance pattern (round-2 MEDIUM 7 / cursor N-M2): `GroundScene.ReloadTerrain()` is bound as an INSTANCE ThisCall, NOT static. The correct call is `GroundScene.Get().ReloadTerrain()` (verified at `D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/SWG/GroundSceneImpl.cs:98`). ClientReloadDispatcher Task 3 MUST use the `.Get()` instance pattern; the bare `GroundScene.ReloadTerrain()` form (no `.Get()`) is FORBIDDEN and grep-gated zero."
  - "Asset-class routing extension-sniff table (round-2 MEDIUM 9 / cursor N-M3): the routing table is pinned in Task 3's action — TextureExtensions {.dds .tga .png .jpg .jpeg}; TerrainExtensions {.trn}; DatatableExtensions {.iff with sub-detect TypeId 'DTII'}; StringtableExtensions {.stf}; ObjectTemplateExtensions {.iff with sub-detect TypeId 'SHOT'/'STOT'/'SBOT'}; UnknownExtensions {everything else}. A parameterized routing table test (`ClientReloadDispatcherTests.cs`) covers each (extension, expectedTier) pair."
  - "Loose-override subdirectory Q2 (round-2 MEDIUM 10 / cursor N-M5): Open Q2 (which subdirectory under the client root holds loose overrides) is NOT resolved at plan time. The Tier-4 smoke (Task 5) determines the verified loose-override directory. If smoke fails, the editor falls back to Save As… (the user picks the path explicitly) and `IffSaveTargets` records the actual path the user picked for the next session in the `[IffEditor] looseOverrideDir` ini key. This fallback is explicit in Task 2's action."
  - "TreRecordIndexResolver linear-scan documentation (round-2 MEDIUM 12 / cursor N-M8): Linear O(n) scan over `TreFile.Records` is acceptable for V1 — typical `.tre` files have ≤ 50k records, scan completes in < 50ms. Duplicate-offset edge case (extremely rare; would only happen with archive corruption): scan returns the FIRST matching record; degraded fallback to `OpenSource.Unknown` if the caller cannot disambiguate. A future phase MAY add an indexed lookup if profiling shows scan time > 100ms on a representative archive. Task 4b documents this in the resolver's XML comment."
  - "Cross-repo (round-2 MEDIUM 3): this plan edits both Utinni (`LooseOverridePath.cs`, `TreRecordIndexResolver.cs`, csproj, two test files) AND UtinniPlugins (the four UI/save files, csproj). Execute in the same commit chain; verify `UtinniCoreDotNet.dll` is rebuilt before TheJawaToolbox builds against it. The `UtinniCoreDotNet/Generated/UtinniCore.cs` file shows as `M` in the working tree at planning time — the executor MUST confirm it's committed OR explicitly note the working-tree state in the SUMMARY before referencing any binding line citations (`Graphics.ReloadTextures` at line 12060, `GroundScene.Get().ReloadTerrain` at line 14428 etc.)."
  - "csproj coverage (round-2 HIGH-A / corrects the previous-round MEDIUM 4 default-glob misconception): `UtinniCoreDotNet.csproj` is OLD-STYLE explicit-compile (verified — no SDK attribute; explicit `<Compile Include>` entries at lines 63-208). The PRIOR-ROUND assumption that 'the default Compile glob covers Saving/*.cs' was WRONG (no default glob in old-style projects). Therefore `Saving\\LooseOverridePath.cs` AND `Formats\\Tre\\TreRecordIndexResolver.cs` BOTH require EXPLICIT `<Compile Include>` entries (Task 1 + Task 4b). `TheJawaToolboxDotNet.csproj` is OLD-STYLE too — `Saving\\IffSaveTargets.cs` and `Saving\\ClientReloadDispatcher.cs` BOTH need explicit `<Compile Include>` entries (Task 2 + Task 3). `UtinniCoreDotNet.Tests.csproj` is SDK-style — new test files auto-glob via `**/*.cs`; NO test-csproj edits needed."
must_haves:
  truths:
    - "The editor's open path constructs the correct OpenSource (LooseFile / TreArchive) at open time and sets FormIffEditor.Source — 08-REVIEWS HIGH-2 design hole closed at the source"
    - "The editor can save as a loose override file under the resolved client load path (D-05.1)"
    - "The editor can Save / Save-As to an arbitrary path, and save-in-place when opened from a loose file (D-05.2)"
    - "Both file saves serialize via the shared IffWriter and produce a byte-exact file for untouched chunks"
    - "Loose-override path construction goes through a root-containment helper (LooseOverridePath.Resolve) that normalizes the relative asset path, rejects rooted paths and `..`, Path.Combine + Path.GetFullPath, and verifies the result StartsWith the resolved root (trailing-separator-guarded, OrdinalIgnoreCase on Windows) — 08-REVIEWS MEDIUM-8 fold. The helper lives FRAMEWORK-SIDE in `UtinniCoreDotNet/Saving/` (NOT the plugin) — same reasoning as IffEditController's framework-side placement (08-REVIEWS MEDIUM-5 / checker B-1): pure-managed, BCL-only, no plugin-only references → eligible for framework placement, which (a) avoids the cross-repo linked-source CI break and (b) makes the helper trivially reusable by Phases 9-11."
    - "File writes flush-to-disk (FileStream.Flush(true)) BEFORE queuing the forced reload onto the game thread, preventing a stale-bytes reload race; the Reload button is DISABLED while the save Task is in flight — 08-REVIEWS MEDIUM-9 fold"
    - "After a file-based save the editor offers a TIERED forced reload (textures -> `Graphics.ReloadTextures()`; terrain -> `GroundScene.Get().ReloadTerrain()` (INSTANCE call per round-2 MEDIUM 7); datatable/STF/object-template -> 'reloads on next scene change' candid copy + an optional 'Trigger scene change now' shortcut that calls the documented TJT addCreateCommandParserCallback scene path) — D-06 tiered acceptance (08-REVIEWS HIGH-4)"
    - "The TRE Browser offers an 'Open in IFF Editor' hand-off that opens the resolved payload in FormIffEditor AND constructs OpenSource.TreArchive(trePath=descriptor.ResolvedArchivePath, recordIndex=resolved-from-offset, logicalPath=descriptor.Path) — populates the provenance model for downstream 08-07 repack (D-09). On record-index resolution FAILURE, falls back to OpenSource.Unknown.Instance (NOT OpenSource.LooseFile(descriptor.Path) — the descriptor's logical path is virtual, not a filesystem path; per checker W-3)."
    - "When `Source is OpenSource.Unknown`, the THREE provenance-dependent save modes (Save → In Place, Save → Loose Override, Save → Repack TRE, Save → Live Patch) are DISABLED; Save As… REMAINS ENABLED as the explicit user-chosen escape hatch (round-2 MEDIUM 5: chosen rule — Save As is the only enabled save mode on Unknown; user picks the path so no logical path is needed)"
    - "FormIffEditor is registered in Plugin.cs GetForms() inside the try/catch isolation block; the MEF SPI is not widened"
    - "UtinniCoreDotNet.csproj contains explicit `<Compile Include>` entries for `Saving\\LooseOverridePath.cs` AND `Formats\\Tre\\TreRecordIndexResolver.cs` — round-2 HIGH-A (no default glob in old-style projects)"
    - "TheJawaToolboxDotNet.csproj contains explicit `<Compile Include>` entries for `Saving\\IffSaveTargets.cs` AND `Saving\\ClientReloadDispatcher.cs` — round-2 HIGH-A"
  artifacts:
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/IffSaveTargets.cs"
      provides: "Loose-override + Save/Save-As file writers (off-UI-thread), using IffWriter + ResolveClientTreDir + LooseOverridePath.Resolve + Flush(true) before signaling completion"
      contains: "looseOverride"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/ClientReloadDispatcher.cs"
      provides: "Tiered D-06 forced reload on the game thread (textures / terrain via `GroundScene.Get().ReloadTerrain()` instance call / scene-change-pending)"
      contains: "ReloadTextures"
    - path: "UtinniCoreDotNet/Saving/LooseOverridePath.cs"
      provides: "Root-containment helper for loose-override paths (08-REVIEWS MEDIUM-8) — framework-side, pure-managed, BCL-only; consumed by the plugin-side IffSaveTargets via the existing UtinniCoreDotNet.dll reference (checker B-1: no linked-source path)"
      contains: "Resolve"
    - path: "UtinniCoreDotNet/Formats/Tre/TreRecordIndexResolver.cs"
      provides: "Static helper to resolve a TRE record index from a descriptor offset; returns OpenSource.TreArchive on match or OpenSource.Unknown.Instance on miss (checker W-3 degraded fallback). Linear O(n) scan documented as acceptable for V1 (round-2 MEDIUM 12)."
      contains: "ResolveOrUnknown"
    - path: "UtinniCoreDotNet/UtinniCoreDotNet.csproj"
      provides: "Old-style explicit-compile project: MUST list `Saving\\LooseOverridePath.cs` AND `Formats\\Tre\\TreRecordIndexResolver.cs` — round-2 HIGH-A coverage gates"
      contains: "Saving\\LooseOverridePath.cs"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"
      provides: "Old-style explicit-compile project: MUST list `Saving\\IffSaveTargets.cs` AND `Saving\\ClientReloadDispatcher.cs` — round-2 HIGH-A coverage gates"
      contains: "Saving\\IffSaveTargets.cs"
    - path: "UtinniCoreDotNet.Tests/SavingTests/LooseOverridePathTests.cs"
      provides: "Path traversal regression: crafted `..`, drive-rooted, alt-separator inputs all reject. Consumes LooseOverridePath via the existing ProjectReference to UtinniCoreDotNet.csproj — NO linked-source <Compile Include> entry (checker B-1). Auto-globbed by SDK-style test csproj."
      contains: "LooseOverridePathTests"
    - path: "UtinniCoreDotNet.Tests/SavingTests/ClientReloadDispatcherTests.cs"
      provides: "Routing-table unit test (round-2 MEDIUM 9): parameterized `[Theory]` over (extension, expectedTier) pairs — texture exts → ReloadedTextures; terrain ext → ReloadedTerrain; stf/datatable/object-template → PendingNextSceneChange; unknown → PendingNextSceneChange (conservative). Pure-managed (no live client needed). Auto-globbed by SDK-style test csproj."
      contains: "ClientReloadDispatcherTests"
  key_links:
    - from: "FormTreBrowser.DispatchDetail (existing) -> Open in IFF Editor"
      to: "FormIffEditor.Source = new OpenSource.TreArchive(...)"
      via: "TRE Browser hand-off populates provenance"
      pattern: "OpenSource\\.TreArchive"
    - from: "FormIffEditor Save▾"
      to: "IffWriter.Write -> File.WriteAllBytes (with Flush(true))"
      via: "off-UI-thread file save"
      pattern: "IffWriter\\.Write"
    - from: "ClientReloadDispatcher"
      to: "GameCallbacks.AddMainLoopCall"
      via: "game-thread reload dispatch"
      pattern: "AddMainLoopCall"
    - from: "ClientReloadDispatcher (terrain branch)"
      to: "GroundScene.Get().ReloadTerrain()"
      via: "INSTANCE call per round-2 MEDIUM 7 (cursor N-M2); never the bare static form"
      pattern: "GroundScene\\.Get\\(\\)\\.ReloadTerrain"
    - from: "FormTreBrowser"
      to: "FormIffEditor"
      via: "Open in IFF Editor hand-off"
      pattern: "IFF Editor"
    - from: "IffSaveTargets (plugin-side)"
      to: "UtinniCoreDotNet.Saving.LooseOverridePath.Resolve (framework-side)"
      via: "existing UtinniCoreDotNet.dll reference (no linked-source); checker B-1"
      pattern: "LooseOverridePath\\.Resolve"
---

<objective>
Wire the two low-risk file save modes — loose override (D-05.1) and Save / Save-As (D-05.2) — to the
`Save ▾` toolbar, add the TIERED D-06 forced reload (08-REVIEWS HIGH-4 disposition: textures/terrain
in-session pass via `Graphics.ReloadTextures()` / `GroundScene.Get().ReloadTerrain()` instance call;
datatable/STF/object-template = next scene change), wire the "Open in IFF Editor" hand-off from the TRE
Browser, populate `FormIffEditor.Source` with the correct OpenSource at open time (08-REVIEWS HIGH-2
design-hole closure — and on degraded hand-off, fall back to `OpenSource.Unknown.Instance` per checker
W-3 so the three provenance-dependent save modes stay honestly disabled and Save As… remains as the
user's escape hatch — round-2 MEDIUM 5), register `FormIffEditor` in `Plugin.cs`, and add a path
root-containment helper + a flush-before-reload barrier (08-REVIEWS MEDIUM-8 + MEDIUM-9 folds). Per
checker B-1, the root-containment helper ships FRAMEWORK-SIDE in `UtinniCoreDotNet/Saving/`
(pure-managed, BCL-only) — this eliminates the cross-repo linked-source CI break that linked-source
path would otherwise reintroduce (ci.yml has only the Utinni checkout step). This completes the 8a
primary slice; the higher-risk live patch (08-06) and `.tre` repack (08-07) follow in isolated plans.

**Round-2 fixes (this revision):**
- Stale `CONTEXT-UPLIFT-NEEDED` tag stripped from `assumes` (round-2 LOW 13).
- `GroundScene.Get().ReloadTerrain()` instance pattern explicitly named in Task 3 (round-2 MEDIUM 7).
- `OpenSource.Unknown` Save-As wording disambiguated (round-2 MEDIUM 5): on Unknown, Save → In Place,
  Save → Loose Override, Save → Repack TRE, and Save → Live Patch are ALL disabled; **Save As…
  REMAINS ENABLED** as the user-chosen escape hatch.
- Asset-class routing extension-sniff table pinned in Task 3's action (round-2 MEDIUM 9) + routing-table
  test (`ClientReloadDispatcherTests.cs`).
- Loose-override subdir Q2 (round-2 MEDIUM 10): documented as Tier-4-only; Save As… fallback path
  + `looseOverrideDir` ini-key persistence explicit in Task 2.
- `TreRecordIndexResolver` linear-scan + duplicate-offset semantics documented in the resolver's XML
  comment (round-2 MEDIUM 12).
- csproj coverage (round-2 HIGH-A — CORRECTS the prior-round MEDIUM 4 default-glob misconception):
  `UtinniCoreDotNet.csproj` is OLD-STYLE — `LooseOverridePath.cs` AND `TreRecordIndexResolver.cs` both
  require EXPLICIT `<Compile Include>` entries. `TheJawaToolboxDotNet.csproj` is OLD-STYLE — both
  `IffSaveTargets.cs` and `ClientReloadDispatcher.cs` require EXPLICIT `<Compile Include>` entries.
- Cross-repo coupling note (round-2 MEDIUM 3) recorded in `assumes`.

Purpose: PROD-W1-IFF Criterion 2 — "save modifications back to a file the live client reloads
correctly" — via the standard SWG modder loop (loose override) + a Save/Save-As baseline, plus the
tiered reload acceptance. Criterion 1 (loads in TJT against a live client) is verified here.

Output: `IffSaveTargets.cs`, `ClientReloadDispatcher.cs` (UtinniPlugins + their csproj entries),
`UtinniCoreDotNet/Saving/LooseOverridePath.cs` + `UtinniCoreDotNet/Formats/Tre/TreRecordIndexResolver.cs`
(framework-side per checker B-1 + their csproj entries), the framework-side path test + routing-table
test + degraded-hand-off test, the `Save ▾` + `Reload in client` wiring + the OpenSource open-time
construction in `FormIffEditor`, the `FormTreBrowser` hand-off populating `OpenSource.TreArchive` (with
the `OpenSource.Unknown` degraded fallback per W-3), `Plugin.cs` registration, and a live-SWG smoke
checkpoint. Cross-repo: framework path helper + resolver + test in Utinni; UI/save targets in
UtinniPlugins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-CONTEXT.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-RESEARCH.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-PATTERNS.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-UI-SPEC.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-REVIEWS.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-04-SUMMARY.md

<interfaces>
<!-- 08-01 write path + provenance model (FOUR cases after W-3): -->
IffWriter.Write(MutableIffDocument) -> byte[]
OpenSource.LooseFile(string path)
OpenSource.TreArchive(string trePath, int recordIndex, string logicalPath)
OpenSource.ClientMemory(IntPtr addr, int mappedLen, string logicalPath)   // not constructed this phase; live patch disabled
OpenSource.Unknown.Instance                                                // sealed singleton; constructed by 08-05 when the TRE hand-off cannot resolve recordIndex (degraded fallback per checker W-3). The three provenance-dependent save modes stay disabled; Save As… remains enabled (round-2 MEDIUM 5).

<!-- LooseOverridePath (framework-side, UtinniCoreDotNet/Saving/) — checker B-1: -->
namespace UtinniCoreDotNet.Saving { public static class LooseOverridePath {
  public static string Resolve(string resolvedRoot, string relAssetPath);   // returns full path or rejects per the <behavior> rules
} }

<!-- TreRecordIndexResolver (framework-side, UtinniCoreDotNet/Formats/Tre/) — round-2 MEDIUM 12: -->
namespace UtinniCoreDotNet.Formats.Tre { public static class TreRecordIndexResolver {
  public static OpenSource ResolveOrUnknown(string trePath, long offset, string logicalPath);
  // Linear O(n) scan; documented < 50ms for typical ≤ 50k records.
  // Duplicate-offset edge case: returns first match; degraded to Unknown if disambiguation needed.
} }

<!-- FormTreBrowser.cs (D:/Code/UtinniPlugins) — what we extend, NOT what we change beyond context-menu addition: -->
private string ResolveClientTreDir()                       // lines 312-379: process-module-dir primary -> GetWorkingDirectory() -> [..]Dir ini fallback
private void tvTre_AfterSelect(object, TreeViewEventArgs)  // lines 452-503: selection plumbing; descriptor d carries ResolvedArchivePath + ArchiveLocalOffset
private void DispatchDetail(TreMetadata meta, bool resolved, byte[] payload)  // lines 505-536: resolved payload bytes
Game.IsRunning                                             // gate before any live-client touch (line 393)

<!-- TreEntryDescriptor (UtinniCoreDotNet/Formats/Tre/TreArchiveIndex.cs lines 40-66): -->
struct TreEntryDescriptor {
  string Path;                       // virtual/logical path inside the archive — NOT a filesystem path; never use as a fallback OpenSource.LooseFile arg (checker W-3)
  string ResolvedArchivePath;        // the physical .tre path the resolver opens
  long ArchiveLocalOffset;           // payload offset inside that .tre
  int CompressedLength;
  // ... other fields
}
// Record-index resolution: TreFile.Open(ResolvedArchivePath).Records is an IList<TreRecord> where each rec.Offset == ArchiveLocalOffset for the descriptor's entry. A helper that finds-by-offset closes the gap.

<!-- Game-thread + reload bindings (UtinniCoreDotNet): -->
GameCallbacks.AddMainLoopCall(Action)                      // marshal onto the SWG game thread (GameCallbacks.cs 254-257)
Graphics.ReloadTextures()                                  // Generated/UtinniCore.cs ~12060 -> textureListReloadTextures (STATIC binding)
GroundScene.Get().ReloadTerrain()                          // Generated/UtinniCore.cs ~14428 -> INSTANCE ThisCall (round-2 MEDIUM 7: NEVER the bare `GroundScene.ReloadTerrain()` static form). Verified call-site: D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/SWG/GroundSceneImpl.cs:98 = `GroundScene.Get().ReloadTerrain();`
Graphics.flushResources(bool)                              // Generated/UtinniCore.cs ~11769 (broader GPU-resource flush)
Game.AddSetSceneCallback(...)                              // existing notification hook — NOT a setup-scene trigger; do not call as a reload path

<!-- D-06 tiered acceptance — concrete fallback path: the SCENE CHANGE is USER-TRIGGERED via the existing TJT chat-command parser registered via addCreateCommandParserCallback (project memory: project_scene_change_via_tjt.md). The editor SURFACES this candidly ("Reloads on next scene change") and OPTIONALLY exposes a `Trigger scene change now` button that posts the same chat command the user would type; if no such command is exposed by the current TJT build, the button is HIDDEN, not faked. -->

<!-- Plugin.cs registration (try/catch isolation block, lines 62-69): forms.Add(new FormX(this)); GetSubPanels() stays null. -->

<!-- csproj insertion-point references (round-2 HIGH-A — both csproj are OLD-STYLE): -->
<!-- UtinniCoreDotNet.csproj line 73: `<Compile Include="Formats\Tre\TrePayloadResolver.cs" />` — the new TreRecordIndexResolver entry goes here. -->
<!--                                  Saving\LooseOverridePath.cs is the FIRST file under a new top-level Saving/ folder — place anywhere in the main ItemGroup (e.g. adjacent to the Formats\Tre cluster lines 69-76). -->
<!-- TheJawaToolboxDotNet.csproj has no existing Saving/ entries — both IffSaveTargets.cs and ClientReloadDispatcher.cs need new entries in the main ItemGroup (lines 64-132). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LooseOverridePath root-containment helper (FRAMEWORK-SIDE per checker B-1) + xUnit traversal regression (08-REVIEWS MEDIUM-8) + UtinniCoreDotNet.csproj entry (round-2 HIGH-A; default-glob misconception corrected)</name>
  <read_first>
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs (ResolveClientTreDir 312-379 — the "resolved root" semantic to mirror)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-RESEARCH.md (Pitfall 7 loose-override dir derivation; Open Q2)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-REVIEWS.md (MEDIUM-8 root-containment + StartsWith + trailing-separator + OrdinalIgnoreCase)
    - D:/Code/Utinni/UtinniCoreDotNet/Formats/Iff/IffChunk.cs (verbatim MIT+provenance banner — first 27 lines to mirror on the new file)
    - D:/Code/Utinni/UtinniCoreDotNet/UtinniCoreDotNet.csproj (OLD-STYLE explicit-compile — NOT SDK-style; there is NO default Compile glob. CORRECTS the prior-round MEDIUM 4 misconception. `Saving\LooseOverridePath.cs` MUST get an explicit `<Compile Include>` entry. Recommended insertion: inside the existing main `<ItemGroup>` at lines 62-208, adjacent to the Formats cluster — e.g. after line 76 `<Compile Include="Formats\Tre\TreRecord.cs" />`)
    - D:/Code/Utinni/UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj (SDK-style at line 1; new test file under `UtinniCoreDotNet.Tests/SavingTests/` auto-includes via the default `**/*.cs` glob — NO test-csproj edit needed; the existing ProjectReference at line 33 carries `UtinniCoreDotNet.Saving.LooseOverridePath` into the test assembly)
  </read_first>
  <behavior>
    - Input: resolvedRoot (absolute, already canonical-normalized), relAssetPath (the entry's virtual/logical path from the .tre)
    - Reject (return null OR throw a specific Exception per implementer choice) if relAssetPath:
      (a) is null/empty
      (b) is rooted (Path.IsPathRooted true)
      (c) contains `..` segments (after splitting on both `/` and `\\`)
      (d) contains alternate-separator inputs after normalization that would escape resolvedRoot
    - Otherwise: Path.Combine(resolvedRoot, normalizedRel) -> Path.GetFullPath -> the result MUST start with `resolvedRoot` PLUS a trailing directory separator (prevents `C:\swg-client` prefix-matching `C:\swg-clientx`); use OrdinalIgnoreCase on Windows
    - Returns the full path on success; rejects on any check failure
    - The helper uses ONLY BCL types (System.IO + System) — NO WinForms, NO plugin-only references. This is what makes framework-side placement clean (checker B-1 disposition: same reasoning as IffEditController's framework move in 08-REVIEWS MEDIUM-5).
  </behavior>
  <action>
    PART A — Create the helper.
    Create `UtinniCoreDotNet/Saving/LooseOverridePath.cs` (verbatim MIT+provenance banner — mirror IffChunk.cs lines 1-27) — `namespace UtinniCoreDotNet.Saving { public static class LooseOverridePath { public static string Resolve(string resolvedRoot, string relAssetPath); } }`, implementing the `<behavior>` rules. Normalize separators to the OS-native form before splitting; explicitly check for `..` segments (after Split('/', '\\')) regardless of `Path.GetFullPath`'s canonicalization (defense in depth); on the final `StartsWith` check, append `Path.DirectorySeparatorChar` to `resolvedRoot` if missing before comparing (`OrdinalIgnoreCase` on Windows).

    PART B — csproj coverage (round-2 HIGH-A; CORRECTS the prior-round MEDIUM 4 misconception).
    Edit `UtinniCoreDotNet/UtinniCoreDotNet.csproj`. The PRIOR-ROUND assumption that the project has a
    "default Compile glob" covering `Saving/*.cs` was WRONG — the project is OLD-STYLE explicit-compile
    (verified: line 1 = `<Project ToolsVersion="15.0" xmlns="...">` — NOT SDK-style; explicit
    `<Compile Include>` entries at lines 63-208). Add ONE new entry inside the existing main
    `<ItemGroup>` at lines 62-208:

      <Compile Include="Saving\LooseOverridePath.cs" />

    Recommended placement: adjacent to the Formats cluster (e.g. immediately after line 76
    `<Compile Include="Formats\Tre\TreRecord.cs" />`). No `<SubType>` needed (plain class).

    Do NOT add any linked-source `<Compile Include="..\..\UtinniPlugins\..." />` entry anywhere in this
    plan — that is the exact pattern checker B-1 prohibits (MED-5 precedent: ci.yml only checks out
    Utinni, NOT UtinniPlugins).

    PART C — Test file.
    Create `UtinniCoreDotNet.Tests/SavingTests/LooseOverridePathTests.cs` (verbatim banner) covering the
    08-REVIEWS MEDIUM-8 cases: crafted `..` (relative + absolute drive-rooted form), drive-rooted inputs
    (`C:\\evil.iff`, `D:foo`), alt-separator-only inputs that resolve to escape (`..\\..\\evil`),
    prefix-match attack (`C:\\swg-clientx\\loot` against root `C:\\swg-client`), and a happy-path normal
    relative input that resolves under the root. The test file consumes the helper via the existing
    `ProjectReference` to `UtinniCoreDotNet.csproj` (line 33 of UtinniCoreDotNet.Tests.csproj) —
    `using UtinniCoreDotNet.Saving;` — NO linked-source `<Compile Include>` entry; the SDK-style test
    csproj's default `**/*.cs` glob auto-includes the new test file under `SavingTests/` (the only
    excluded subtree is `Fixtures\**`).

    On the plugin side, in 08-05 Task 2, `IffSaveTargets.cs` consumes the helper via
    `using UtinniCoreDotNet.Saving;` and calls `LooseOverridePath.Resolve(resolvedRoot, relAssetPath)` —
    the plugin already references `UtinniCoreDotNet.dll` (TheJawaToolboxDotNet.csproj HintPath) so no
    new reference is needed.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~LooseOverridePath" && grep -c '<Compile Include="Saving\\\\LooseOverridePath.cs"' UtinniCoreDotNet/UtinniCoreDotNet.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `UtinniCoreDotNet/Saving/LooseOverridePath.cs` exists (FRAMEWORK-SIDE per checker B-1; NOT under `../UtinniPlugins/...`) with the verbatim MIT+provenance banner and `namespace UtinniCoreDotNet.Saving; public static class LooseOverridePath { public static string Resolve(string resolvedRoot, string relAssetPath); }`
    - `UtinniCoreDotNet.Tests/SavingTests/LooseOverridePathTests.cs` exists with the MIT banner and covers ALL the listed cases (each as a separate [Fact]): `..` rejection, drive-rooted rejection, alt-separator escape rejection, prefix-match attack rejection, happy-path success
    - `UtinniCoreDotNet.csproj` contains the entry `<Compile Include="Saving\LooseOverridePath.cs" />` (grep gate: `grep -c '<Compile Include="Saving\\\\LooseOverridePath.cs"' UtinniCoreDotNet/UtinniCoreDotNet.csproj` returns 1; correcting the prior-round MEDIUM 4 default-glob misconception)
    - The new csproj entry lives inside the existing main `<ItemGroup>` (between lines 62 and 208)
    - `UtinniCoreDotNet.Tests.csproj` does NOT contain a `<Compile Include="..\..\UtinniPlugins\..." Link="Saving\LooseOverridePath.cs" />` entry (grep confirms zero linked-source matches into UtinniPlugins): `grep -c 'UtinniPlugins' UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj` returns 0
    - The test consumes the helper via the existing `ProjectReference` to `UtinniCoreDotNet.csproj` (no new reference; no new csproj entries in the test csproj — SDK-style auto-glob picks it up)
    - The compiled `UtinniCoreDotNet.dll` exports `UtinniCoreDotNet.Saving.LooseOverridePath.Resolve`
    - `dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~LooseOverridePath"` exits 0
    - VS2026 MSBuild of `UtinniCoreDotNet` (Release) compiles clean; VS2026 MSBuild of TheJawaToolbox (Release) compiles clean
  </acceptance_criteria>
  <done>The loose-override path resolver ships framework-side under `UtinniCoreDotNet/Saving/`, rejects every documented traversal class, the regression suite proves it without any linked-source path into UtinniPlugins, CI's Utinni-only checkout will not break (checker B-1 closed), AND the csproj entry is explicit (round-2 HIGH-A; corrects prior-round MEDIUM 4).</done>
</task>

<task type="auto">
  <name>Task 2: IffSaveTargets — loose-override + Save/Save-As (D-05.1/2) with Flush(true) barrier (08-REVIEWS MEDIUM-9) + Save As… fallback for unresolved loose-override subdir (round-2 MEDIUM 10) + TheJawaToolboxDotNet.csproj entry (round-2 HIGH-A)</name>
  <read_first>
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs (ResolveClientTreDir 312-379; off-UI-thread Task.Run + marshaling 225-265,486-503; CreateSettings clientDir analog 179-187)
    - D:/Code/Utinni/UtinniCoreDotNet/Saving/LooseOverridePath.cs (from Task 1 — framework-side; consumed via `using UtinniCoreDotNet.Saving;`)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-RESEARCH.md (Pitfall 7 loose-override dir derivation; Open Q2 — UNRESOLVED at plan time, Tier-4 only; round-2 MEDIUM 10 records this)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-04-SUMMARY.md (FormIffEditor [IffEditor] looseOverrideDir ini key, MutableIffDocument + IffEditController access, OpenSource Source property)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj (OLD-STYLE explicit-compile; the main `<ItemGroup>` at lines 64-132 has no existing Saving/ entries — IffSaveTargets is the first under a new top-level Saving/ folder. Add `<Compile Include="Saving\IffSaveTargets.cs" />` inside the same ItemGroup.)
  </read_first>
  <action>
    PART A — Create the IffSaveTargets file.
    Create `Saving/IffSaveTargets.cs` in TheJawaToolboxDotNet (verbatim MIT banner) with two file save
    paths, both serializing via `IffWriter.Write(mutableDoc)` and writing on a background `Task`
    (UI mutation marshals back via the captured `SynchronizationContext` / `BeginInvoke`).

    Add `using UtinniCoreDotNet.Saving;` to the file (or fully-qualify the call as
    `UtinniCoreDotNet.Saving.LooseOverridePath.Resolve(...)`) — the helper now lives framework-side per
    checker B-1, consumed via the existing UtinniCoreDotNet.dll reference (TheJawaToolboxDotNet.csproj
    HintPath; no new reference needed).

    (1) `SaveLooseOverride(MutableIffDocument doc, OpenSource source, string clientRoot, string looseOverrideSubDir)`
        — derive the override base directory by reusing the `ResolveClientTreDir` approach
        (process-module-dir primary), then the loose-override sub-directory from the
        `[IffEditor] looseOverrideDir` ini fallback. **Round-2 MEDIUM 10:** Open Q2 (which exact
        sub-directory) is UNRESOLVED at plan time and gated to Tier-4 (Task 5). FALLBACK behavior when
        the resolved sub-directory is wrong (the client never re-reads from it):
          - The first run uses the planner's best-guess subdir (e.g. `<client>/tre/loose/` — the
            implementer picks ONE deterministic default).
          - On Tier-4 smoke (Task 5), the maintainer records the ACTUAL directory the client re-reads.
          - If the smoke confirms the default works, persist that path in the
            `[IffEditor] looseOverrideDir` ini key for future sessions.
          - If the smoke FAILS (the client did NOT pick up the loose-override), the editor's UX falls
            back to recommending Save As… — the user picks the path explicitly, and `IffSaveTargets`
            records the user's chosen DIRECTORY (parent of the chosen file) into
            `[IffEditor] looseOverrideDir` for the next session, so the next loose-override save
            defaults to the correct location.

        Derive the logical path from `source`: for `OpenSource.TreArchive`, use `logicalPath`; for
        `OpenSource.LooseFile`, derive from the original file name; for `OpenSource.Unknown` (degraded
        fallback per W-3), throw / return failure with the message "Cannot resolve archive record — use
        Save As to write to a chosen file." (Unknown has no usable logical path; round-2 MEDIUM 5 picks
        Save As as the only enabled save mode).

        Then `LooseOverridePath.Resolve(resolvedRoot, relAssetPath)` to get the final path (path
        traversal rejected at the helper). Create parent directories under the resolved root; use BCL
        path APIs only.

    (2) `SaveToPath(MutableIffDocument doc, string path)` / `SaveInPlace(MutableIffDocument doc, OpenSource source)`
        — write to a `SaveFileDialog` path, or overwrite the original loose file when
        `source is OpenSource.LooseFile` (`SaveInPlace` requires that exact pattern-match; `OpenSource.Unknown`
        does NOT match, so SaveInPlace is correctly disabled on the degraded fallback per W-3).
        `SaveToPath` is the implementation behind Save As… and works for ANY source (including
        OpenSource.Unknown — round-2 MEDIUM 5: Save As… is the only enabled save mode on Unknown
        because it doesn't need a logical path; the user picks the destination).

    FLUSH BARRIER (08-REVIEWS MEDIUM-9): both paths use a `FileStream` opened for write and call
    `fs.Write(bytes, 0, bytes.Length); fs.Flush(true);` (the `true` arg flushes to disk, not just the
    OS buffer) BEFORE the Task completes. The Task's awaiter is the signal the caller uses to enable
    the Reload button — the Reload button MUST be DISABLED while the save Task is in flight (Task 4
    wires the UI side).

    Both paths report success/failure for the FormIffEditor status strip (Saved /
    Save-failed-edits-retained copy). Use the writer's 64 MB cap behavior (08-01) — surface a save-time
    validation rather than crash on an over-cap chunk. NEVER attempt to use the LooseOverridePath helper
    for SaveToPath/SaveInPlace — those operate on user-chosen / known-good paths.

    PART B — csproj coverage (round-2 HIGH-A).
    Edit `../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj`. Inside the
    existing main `<ItemGroup>` at lines 64-132, add:

      <Compile Include="Saving\IffSaveTargets.cs" />

    No `<SubType>` needed (plain class). Recommended placement: at the end of the ItemGroup or
    alphabetically adjacent to the SWG/*.cs entries at lines 67-68 / 127-131 (no fixed convention).
  </action>
  <verify>
    <automated>grep -c "Flush(true)\|IffWriter.Write\|LooseOverridePath.Resolve\|UtinniCoreDotNet.Saving" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/IffSaveTargets.cs" && grep -c '<Compile Include="Saving\\\\IffSaveTargets.cs"' "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"</automated>
  </verify>
  <acceptance_criteria>
    - IffSaveTargets.cs exists with the MIT banner and methods SaveLooseOverride / SaveToPath / SaveInPlace
    - The file imports `UtinniCoreDotNet.Saving` (grep `using UtinniCoreDotNet.Saving` OR `UtinniCoreDotNet.Saving.LooseOverridePath` returns ≥ 1)
    - All paths call IffWriter.Write
    - SaveLooseOverride goes through `UtinniCoreDotNet.Saving.LooseOverridePath.Resolve(resolvedRoot, relAssetPath)` — verified by grep (`LooseOverridePath.Resolve` appears)
    - Every file write calls `FileStream.Flush(true)` BEFORE the awaiter completes — grep proves `Flush(true)` is present
    - The loose-override base dir derives from the ResolveClientTreDir approach + the [IffEditor] looseOverrideDir ini fallback
    - On Save As… completion, the chosen file's parent directory is persisted to `[IffEditor] looseOverrideDir` (round-2 MEDIUM 10: explicit Save-As fallback path for unresolved Open Q2)
    - SaveInPlace requires the source to match `OpenSource.LooseFile` (throws/returns failure on `OpenSource.TreArchive` AND on `OpenSource.Unknown` — the W-3 degraded fallback)
    - SaveLooseOverride on `OpenSource.Unknown` returns failure with the "Cannot resolve archive record — use Save As to write to a chosen file." message (round-2 MEDIUM 5 wording)
    - SaveToPath (Save As…) accepts ANY OpenSource including Unknown — it operates on a user-chosen path
    - `TheJawaToolboxDotNet.csproj` contains `<Compile Include="Saving\IffSaveTargets.cs" />` (grep gate: `grep -c '<Compile Include="Saving\\\\IffSaveTargets.cs"' TheJawaToolboxDotNet.csproj` returns 1; round-2 HIGH-A)
    - VS2026 MSBuild of TheJawaToolbox compiles clean
  </acceptance_criteria>
  <done>The editor can persist edits as a loose override (via root-containment-protected paths, helper consumed from `UtinniCoreDotNet.Saving`) and via Save/Save-As, byte-exact for untouched chunks, off the UI thread, with a flush-to-disk barrier before the awaiter completes; the degraded `OpenSource.Unknown` fallback (W-3) correctly fails loose-override / SaveInPlace but allows Save As (round-2 MEDIUM 5); the loose-override subdir Q2 fallback uses Save As + ini persistence when the resolved subdir is wrong (round-2 MEDIUM 10); explicit csproj entry added (round-2 HIGH-A).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ClientReloadDispatcher — TIERED D-06 forced reload (08-REVIEWS HIGH-4) + GroundScene.Get().ReloadTerrain() instance pattern (round-2 MEDIUM 7) + pinned routing-table (round-2 MEDIUM 9) + routing-table unit test + TheJawaToolboxDotNet.csproj entry (round-2 HIGH-A)</name>
  <read_first>
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-RESEARCH.md (Pitfall 4 tiered reload + no general IFF reload hook; Pattern 3; Open Q3; naked-after-scene-change baseline note)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-PATTERNS.md (forced-reload helper analog + binding line numbers)
    - D:/Code/Utinni/UtinniCoreDotNet/Callbacks/GameCallbacks.cs (AddMainLoopCall 254-257)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-REVIEWS.md (HIGH-4 tiered acceptance disposition; scene change is user-triggered via the documented TJT chat-command parser callback path)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/SWG/GroundSceneImpl.cs (line 98 — verified call-site for `GroundScene.Get().ReloadTerrain();` — INSTANCE pattern per round-2 MEDIUM 7)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj (OLD-STYLE explicit-compile; add `<Compile Include="Saving\ClientReloadDispatcher.cs" />` adjacent to the IffSaveTargets entry from Task 2)
  </read_first>
  <action>
    PART A — Create the dispatcher file.
    Create `Saving/ClientReloadDispatcher.cs` in TheJawaToolboxDotNet (verbatim MIT banner) implementing
    the D-06 TIERED reload, ALL on the game thread via `GameCallbacks.AddMainLoopCall`. Classify the saved
    asset by extension (and where needed, IFF sub-detection via the root container's TypeId) and
    dispatch:

    **PINNED ROUTING TABLE (round-2 MEDIUM 9 / cursor N-M3 — no more "planner's discretion"):**

      TextureExtensions   := { .dds, .tga, .png, .jpg, .jpeg }
      TerrainExtensions   := { .trn }
      DatatableExtensions := { .iff } with sub-detection on root TypeId "DTII"
      StringtableExtensions := { .stf }
      ObjectTemplateExtensions := { .iff } with sub-detection on root TypeId "SHOT" / "STOT" / "SBOT"
      UnknownExtensions   := everything else

    **Routing rule:**

      - ext in TextureExtensions → tier (a) `Graphics.ReloadTextures()` → outcome `ReloadedTextures`.
        **In-session pass per tiered acceptance.**

      - ext in TerrainExtensions → tier (a) `GroundScene.Get().ReloadTerrain()` (round-2 MEDIUM 7:
        INSTANCE ThisCall — NEVER the bare static form) → outcome `ReloadedTerrain`. **In-session
        pass per tiered acceptance.**

      - ext in StringtableExtensions OR (ext == .iff AND root TypeId in DatatableExtensions sub-detect
        OR ObjectTemplateExtensions sub-detect) → tier (b) `PendingNextSceneChange`.

      - ext == .iff AND root container TypeId NOT in any recognized sub-detect set → tier (b)
        `PendingNextSceneChange` (CONSERVATIVE fallback).

      - All other extensions (`UnknownExtensions`) → tier (b) `PendingNextSceneChange` (CONSERVATIVE).

      - `!Game.IsRunning` → tier (c) `Unavailable`.

    Tier (b) does NOT attempt speculative scene re-setup via `AddSetSceneCallback` (that is a
    notification hook, NOT a trigger — 08-REVIEWS HIGH-4 / Codex HIGH). Instead, return outcome
    `PendingNextSceneChange` with the candid UI copy "Reloads on next scene change. Datatables,
    string tables, and object templates can't be hot-swapped — the running scene caches them."
    FormIffEditor surfaces this candidly per UI-SPEC (Task 4 wires it). **This is the documented
    TIERED-ACCEPTANCE pass per PROD-W1-IFF Criterion 2 — NOT a failure.**

    EXPOSE the outcome enum publicly. NEVER call any reload binding off the game thread. NEVER fabricate
    a scene-change trigger (refused at planning per 08-REVIEWS HIGH-4 to avoid texture-only fallback
    shipping silently). Note in a code comment that post-scene-change "naked" is the documented baseline,
    not a reload failure.

    **Grep gate (round-2 MEDIUM 7):** the file MUST contain `GroundScene.Get().ReloadTerrain()` and MUST
    NOT contain the bare `GroundScene.ReloadTerrain()` form (no `.Get()`). Acceptance criteria includes
    BOTH the positive (`Get().ReloadTerrain` ≥ 1) AND negative (bare `GroundScene.ReloadTerrain` with
    no preceding `.Get()` == 0) grep gates.

    Optional helper (do NOT block on it for V1): if the TJT chat-command-parser callback path exposes a
    documented scene-change command name (project memory: scene-change-via-TJT), FormIffEditor MAY show
    a `Trigger scene change now` button that posts that command — but ClientReloadDispatcher itself MUST
    NOT do this implicitly (user intent must be explicit; an automatic scene change while the user is in
    a session is destabilizing).

    PART B — Routing-table unit test (round-2 MEDIUM 9).
    Create `UtinniCoreDotNet.Tests/SavingTests/ClientReloadDispatcherTests.cs` (verbatim banner) as a
    pure-managed unit test. NOTE: ClientReloadDispatcher lives in UtinniPlugins (`TheJawaToolboxDotNet`)
    and that assembly is NOT a ProjectReference from `UtinniCoreDotNet.Tests`. To make the routing-table
    test PURE-MANAGED and runnable in CI without a live client AND without coupling the test assembly to
    UtinniPlugins, extract the EXTENSION-CLASSIFICATION step into a small static helper in
    `UtinniCoreDotNet` (e.g. `UtinniCoreDotNet/Saving/ReloadAssetClassifier.cs` returning a `ReloadTier`
    enum with values { Textures, Terrain, PendingNextSceneChange }), and have `ClientReloadDispatcher`
    call it. The test then exercises `ReloadAssetClassifier.Classify(extension, rootTypeIdOrNull)` as a
    pure function parameterized by `[Theory]` over the routing table from PART A's pinned rules.

    If extracting a framework-side classifier is preferred, ALSO add that file to the
    `UtinniCoreDotNet.csproj` Compile entries (Task 1 already establishes the pattern for new
    `Saving\*.cs` entries — add `<Compile Include="Saving\ReloadAssetClassifier.cs" />` next to
    `LooseOverridePath.cs`). If extraction is rejected (implementer's choice; cite reason in SUMMARY),
    skip the routing-table unit test and rely solely on grep gates against the dispatcher's source
    — this trades automation coverage for fewer moving parts.

    PART C — csproj coverage (round-2 HIGH-A).
    Edit `../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj`. Add:

      <Compile Include="Saving\ClientReloadDispatcher.cs" />

    Adjacent to the IffSaveTargets entry from Task 2 (same `<ItemGroup>`). No `<SubType>` needed.

    If the routing-table classifier is extracted framework-side (PART B option A), ALSO add
    `<Compile Include="Saving\ReloadAssetClassifier.cs" />` to `UtinniCoreDotNet/UtinniCoreDotNet.csproj`
    in the same place as Task 1's `Saving\LooseOverridePath.cs` entry.
  </action>
  <verify>
    <automated>grep -c "AddMainLoopCall\|PendingNextSceneChange\|GroundScene\.Get()\.ReloadTerrain" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/ClientReloadDispatcher.cs" && grep -c '<Compile Include="Saving\\\\ClientReloadDispatcher.cs"' "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"</automated>
  </verify>
  <acceptance_criteria>
    - ClientReloadDispatcher.cs exists with the MIT banner
    - Every reload binding call (ReloadTextures / Get().ReloadTerrain) is inside a `GameCallbacks.AddMainLoopCall` lambda
    - The terrain branch uses `GroundScene.Get().ReloadTerrain()` (INSTANCE pattern) — grep `grep -c 'GroundScene\.Get()\.ReloadTerrain' ClientReloadDispatcher.cs` returns ≥ 1 (round-2 MEDIUM 7)
    - The file does NOT contain the bare `GroundScene.ReloadTerrain` form (without preceding `.Get()`) — negative grep: a context-aware check such as `grep -E 'GroundScene\\.ReloadTerrain' ClientReloadDispatcher.cs | grep -v 'Get\\(\\)\\.ReloadTerrain' | wc -l` returns 0
    - The pinned routing table (round-2 MEDIUM 9) is implemented as named constants/sets in the source: `TextureExtensions`, `TerrainExtensions`, `DatatableExtensions`, `StringtableExtensions`, `ObjectTemplateExtensions` — grep finds each name at least once
    - The dispatcher gates on Game.IsRunning and returns one of FOUR distinct outcomes: ReloadedTextures, ReloadedTerrain, PendingNextSceneChange, Unavailable
    - For non-texture / non-terrain assets the dispatcher returns PendingNextSceneChange — does NOT attempt speculative AddSetSceneCallback re-setup (08-REVIEWS HIGH-4)
    - For unknown extensions, the dispatcher returns PendingNextSceneChange (CONSERVATIVE fallback)
    - No reload binding is called directly from the UI thread
    - `TheJawaToolboxDotNet.csproj` contains `<Compile Include="Saving\ClientReloadDispatcher.cs" />` (grep gate: returns 1; round-2 HIGH-A)
    - (IF classifier extracted framework-side) `ClientReloadDispatcherTests.cs` exists under `UtinniCoreDotNet.Tests/SavingTests/` and exercises the routing table as a pure function; `dotnet test --filter "FullyQualifiedName~ClientReloadDispatcherTests"` exits 0
    - VS2026 MSBuild of TheJawaToolbox compiles clean
  </acceptance_criteria>
  <done>A tiered, game-thread-marshaled forced reload dispatches by asset class with HONEST outcomes; texture/terrain pass in-session (terrain via the INSTANCE call per round-2 MEDIUM 7); datatable/STF/object-template return PendingNextSceneChange with the candid copy; the asset-class routing table is pinned in source (round-2 MEDIUM 9) and unit-tested where extraction is viable; explicit csproj entry added (round-2 HIGH-A).</done>
</task>

<task type="auto">
  <name>Task 4: Wire Save▾ + Reload + OpenSource at open time (incl. Unknown fallback per W-3; Save As enabled on Unknown per round-2 MEDIUM 5), the TRE Browser hand-off, Plugin.cs registration</name>
  <read_first>
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs (Save▾/Reload placeholders + status strip + dirty state + Source property from 08-04)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs (tvTre_AfterSelect 452-503 + descriptor `d` carrying ResolvedArchivePath + ArchiveLocalOffset; DispatchDetail 505-536; metadata context-menu pattern)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs (forms.Add try/catch block 62-69; GetForms/GetSubPanels 99-109)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-UI-SPEC.md (Save-target menu labels + Copywriting; Reload states; single-document discard-confirm)
    - D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TreFile.cs (Records[i].Offset == descriptor.ArchiveLocalOffset for the matching record — how to derive recordIndex from descriptor)
    - D:/Code/Utinni/UtinniCoreDotNet/Formats/Iff/OpenSource.cs (08-01 Task 3 — the FOUR cases LooseFile / TreArchive / ClientMemory / Unknown; Unknown is a sealed singleton via Instance, per checker W-3)
  </read_first>
  <action>
    In `FormIffEditor`: populate the `Save ▾` drop-down with the FIVE save items (most disabled in this
    plan):
      - `Save (in place)` — enabled only when `Source is OpenSource.LooseFile` (calls
        `IffSaveTargets.SaveInPlace`)
      - `Save as loose override` — enabled when `Source is OpenSource.LooseFile` OR
        `Source is OpenSource.TreArchive` (calls `IffSaveTargets.SaveLooseOverride`)
      - `Save As…` — **ALWAYS ENABLED** (calls `IffSaveTargets.SaveToPath` with a SaveFileDialog; the
        user picks the destination — round-2 MEDIUM 5: this is the only enabled save mode when
        `Source is OpenSource.Unknown`)
      - `Patch live client (in memory)` — DISABLED placeholder (08-06 enables when
        `Source is OpenSource.ClientMemory cm`; no current open path constructs ClientMemory)
      - `Repack into source .tre…` — DISABLED placeholder (08-07 enables when
        `Source is OpenSource.TreArchive ta`)

    **Per round-2 MEDIUM 5 (Save-As wording disambiguation):** when `Source is OpenSource.Unknown`:
      - `Save (in place)` → DISABLED (requires LooseFile)
      - `Save as loose override` → DISABLED (no logical path derivable)
      - `Save As…` → **ENABLED** (user-chosen path; the explicit escape hatch — the user knows where
        they want the bytes to go even when provenance is degraded)
      - `Patch live client (in memory)` → DISABLED (requires ClientMemory)
      - `Repack into source .tre…` → DISABLED (requires TreArchive)

    Tooltip on each disabled save item when `Source is OpenSource.Unknown`: "Cannot resolve archive
    record — use Save As to write to a chosen file." (matches round-2 MEDIUM 5's prose.) The Save As…
    item, since it is enabled, gets the neutral tooltip "Save the current edits to a path you choose."

    On a successful file-based save, clear dirty markers, set the `Saved <name> (<mode>)` status, **THEN
    enable Reload in client** (it MUST have been disabled while the save Task was in flight —
    08-REVIEWS MEDIUM-9). On Reload click -> `ClientReloadDispatcher` with the UI-SPEC reload states +
    tooltips:
      - `ReloadedTextures` -> status `Reloaded (textures)`
      - `ReloadedTerrain` -> status `Reloaded (terrain)`
      - `PendingNextSceneChange` -> status `Reloads on next scene change` + tooltip with the candid copy
      - `Unavailable` -> button disabled with tooltip `No live client`

    Implement the single-document discard-confirm (open-replace / close while dirty) per UI-SPEC.

    Add an `Open…` toolbar action (OpenFileDialog -> read bytes -> IffReader.Read -> MutableIffDocument.FromDocument
    -> bind). On open from `Open…`, SET `this.Source = new OpenSource.LooseFile(chosenPath)`.

    In `FormTreBrowser`, add an `Open in IFF Editor` entry to the selected-leaf context menu (extend the
    metadata-menu pattern) that:
      (a) passes the resolved payload bytes (already produced in `DispatchDetail`),
      (b) the descriptor's `ResolvedArchivePath` (the physical .tre path),
      (c) the descriptor's `Path` (logical path inside the archive),
      (d) the descriptor's `ArchiveLocalOffset`,
    to a new `FormIffEditor` (find-or-create the existing instance from the plugin's forms list).
    `FormIffEditor` then derives the recordIndex by calling `TreRecordIndexResolver.ResolveOrUnknown`
    (round-2 MEDIUM 12 — Task 4b's static helper). On success: the resolver returns
    `OpenSource.TreArchive(trePath, recordIndex, logicalPath)` — assign to `this.Source`.

    On FAILURE to resolve the record index (defensive — should not happen with a well-formed
    descriptor), the resolver returns `OpenSource.Unknown.Instance`; assign that to `this.Source` (per
    checker W-3). Do NOT fall back to `OpenSource.LooseFile(descriptor.Path)` — `descriptor.Path` is the
    virtual/logical path inside the archive, NOT a filesystem path, so falling back to LooseFile would
    incorrectly enable Save → In-Place against a path string that doesn't exist on disk. Surface the
    disabled-save tooltip "Cannot resolve archive record — use Save As to write to a chosen file."
    (round-2 MEDIUM 5 wording.) 08-06 live patch stays disabled (Source is not `OpenSource.ClientMemory`);
    08-07 repack stays disabled (Source is not `OpenSource.TreArchive`); and Save As remains AVAILABLE
    as the user's escape hatch (round-2 MEDIUM 5). Keep the browser read-only.

    In `Plugin.cs`, register `FormIffEditor` inside the SAME try/catch isolation block the TRE Browser
    uses (`try { forms.Add(new FormIffEditor(this)); } catch (Exception ex) { Log.Info("Failed to create
    FormIffEditor; IFF Editor will be unavailable: " + ex); }`). `GetSubPanels()` stays null; do NOT
    widen the MEF SPI (CON-M-01/02, STAB-04).

    NO csproj edits in this task — `Plugin.cs`, `FormIffEditor.cs`, `FormTreBrowser.cs` already have
    Compile entries (Plugin.cs at line 65; FormTreBrowser.cs at lines 75-80; FormIffEditor.cs was added
    by 08-04 Task 2). Modification ≠ addition.
  </action>
  <verify>
    <automated>grep -c "OpenSource\.\|FormIffEditor\|OpenSource\.Unknown" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs"</automated>
  </verify>
  <acceptance_criteria>
    - Save▾ offers the FIVE menu items (Save in place / Save as loose override / Save As… / Patch live client / Repack into source .tre); live-patch + repack are disabled placeholders
    - Save As… is ALWAYS ENABLED — including when `Source is OpenSource.Unknown` (round-2 MEDIUM 5)
    - When `Source is OpenSource.Unknown`: Save (in place), Save as loose override, Patch live client, AND Repack into source .tre are ALL disabled; Save As… is the ONLY enabled save mode; the disabled tooltip reads "Cannot resolve archive record — use Save As to write to a chosen file."
    - The Reload button is DISABLED while the save Task is in flight; enabled only after Flush(true) completes
    - A successful save clears dirty state and shows the `Saved <name> (<mode>)` status; a failure retains dirty state with the UI-SPEC copy
    - Reload in client calls ClientReloadDispatcher and reflects the four UI-SPEC reload states (ReloadedTextures / ReloadedTerrain / PendingNextSceneChange / Unavailable); disabled when !Game.IsRunning
    - FormTreBrowser has an `Open in IFF Editor` context-menu entry passing resolved payload bytes + ResolvedArchivePath + logical path + ArchiveLocalOffset; the browser stays read-only
    - FormIffEditor sets `this.Source` to `OpenSource.LooseFile` on Open… and uses `TreRecordIndexResolver.ResolveOrUnknown(...)` on TRE-browser hand-off — grep proves `OpenSource.` and `TreRecordIndexResolver.ResolveOrUnknown` are referenced
    - On `recordIndex` resolution FAILURE (defensive), `Source is OpenSource.Unknown` (NOT `OpenSource.LooseFile(descriptor.Path)`) — grep `OpenSource.Unknown` appears in FormIffEditor.cs or the hand-off code path. Per checker W-3.
    - Plugin.cs registers FormIffEditor in the try/catch block; GetSubPanels() stays null (SPI not widened)
    - VS2026 MSBuild of TheJawaToolbox compiles clean
  </acceptance_criteria>
  <done>Save▾ (file modes + Save As as the explicit Unknown escape hatch per round-2 MEDIUM 5), Reload in client (tiered outcomes), the TRE Browser hand-off (populating OpenSource.TreArchive on success, OpenSource.Unknown.Instance on resolution failure per W-3), Plugin.cs registration, and Open… (populating OpenSource.LooseFile) are wired; the IFF Editor's provenance model is correct end-to-end (including degraded fallback) and the save -> reload barrier is enforced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4b: TreRecordIndexResolver framework-side static helper (round-2 MEDIUM 12 docs) + unit test — degraded TRE hand-off disables both Save → In-Place and Save → Repack (checker W-3) + UtinniCoreDotNet.csproj entry (round-2 HIGH-A)</name>
  <read_first>
    - D:/Code/Utinni/UtinniCoreDotNet/Formats/Iff/OpenSource.cs (08-01 Task 3 — four-case enumeration incl. Unknown sentinel)
    - D:/Code/Utinni/UtinniCoreDotNet/Saving/LooseOverridePath.cs (from Task 1 — framework-side helper; not directly under test here but the same SavingTests directory)
    - D:/Code/Utinni/UtinniCoreDotNet.Tests/FormatsTests/Iff/OpenSourceTests.cs (from 08-01 Task 3 — analog test file for OpenSource value-equality / IsX predicates; mirror style)
    - D:/Code/Utinni/UtinniCoreDotNet/Formats/Tre/TreFile.cs (TreFile.Open + Records: IList<TreRecord> with rec.Offset == ArchiveLocalOffset)
    - D:/Code/Utinni/UtinniCoreDotNet/UtinniCoreDotNet.csproj (OLD-STYLE; line 73 is `<Compile Include="Formats\Tre\TrePayloadResolver.cs" />` — the new TreRecordIndexResolver entry goes immediately adjacent)
  </read_first>
  <behavior>
    - `public static OpenSource ResolveOrUnknown(string trePath, long offset, string logicalPath)` opens `TreFile.Open(trePath)`, performs a LINEAR O(n) scan over `Records`, returns the FIRST `rec` whose `rec.Offset == offset` as `OpenSource.TreArchive(trePath, recordIndex, logicalPath)`
    - On NO match, returns `OpenSource.Unknown.Instance` (per checker W-3)
    - DUPLICATE-OFFSET edge case (round-2 MEDIUM 12 / cursor N-M8): if multiple records share an offset (extremely rare; archive corruption only), the FIRST match wins. Document on the XML comment: "If multiple records share `rec.Offset == offset` (archive-corruption edge case), the first match wins. Degraded resolution scenarios where the caller cannot disambiguate fall through to `OpenSource.Unknown.Instance` via the higher-level call site."
    - LINEAR SCAN PERFORMANCE (round-2 MEDIUM 12 / cursor N-M8): document on the XML comment: "Linear O(n) scan. Typical `.tre` files have ≤ 50k records and complete in < 50 ms. A future phase MAY add an indexed lookup if profiling shows scan time > 100 ms on a representative archive."
    - Given a TRE descriptor whose ArchiveLocalOffset does NOT match any record's Offset in the source TreFile, the resolver yields `Source == OpenSource.Unknown.Instance` (NOT `OpenSource.LooseFile(descriptor.Path)`)
    - Test assertions on the Unknown branch: `Source is OpenSource.LooseFile == false`; `Source is OpenSource.TreArchive == false`; `Source is OpenSource.Unknown == true`
    - Test assertions on the match branch: `Source is OpenSource.TreArchive == true`; the TreArchive case carries the correct trePath, recordIndex, and logicalPath
  </behavior>
  <action>
    PART A — Create the resolver.
    Create `UtinniCoreDotNet/Formats/Tre/TreRecordIndexResolver.cs` (verbatim banner) with the static
    method per `<behavior>`. The XML comment MUST include the round-2 MEDIUM 12 documentation: the
    linear-scan rationale (acceptable for V1 — typical ≤ 50k records, < 50 ms) AND the
    duplicate-offset semantics (first match wins; degraded fallback to Unknown for ambiguous cases).

    PART B — csproj coverage (round-2 HIGH-A).
    Edit `UtinniCoreDotNet/UtinniCoreDotNet.csproj`. Inside the existing main `<ItemGroup>` at lines
    62-208, add adjacent to the Formats/Tre cluster (line 73):

      <Compile Include="Formats\Tre\TreRecordIndexResolver.cs" />

    Recommended placement: immediately after `<Compile Include="Formats\Tre\TrePayloadResolver.cs" />`
    (line 73) or anywhere in the Formats\Tre cluster (lines 69-76). No `<SubType>` needed.

    PART C — Test file.
    Create `UtinniCoreDotNet.Tests/FormatsTests/Iff/TreHandoffFallbackTests.cs` (verbatim banner) with
    [Fact]s asserting the degraded-fallback contract from `<behavior>`:
      - [Fact 1] `ResolveOrUnknown_OffsetNotInArchive_ReturnsUnknownInstance` — supply a fixture TreFile
        and an offset that doesn't exist in any record; assert the result is
        `OpenSource.Unknown.Instance` AND the three `is OpenSource.X` assertions from `<behavior>` pass.
      - [Fact 2] `ResolveOrUnknown_OffsetMatchesARecord_ReturnsTreArchiveWithRightIndex` — supply a
        fixture TreFile and the offset of a known record; assert the result is
        `OpenSource.TreArchive(trePath, expectedIndex, logicalPath)` with the correct fields.

    Use one of the existing Phase-7 TRE fixtures (`UtinniCoreDotNet.Tests/Fixtures/Tre/...`) or a
    purpose-built tiny fixture if needed. Keep the test ≤ 50 lines.

    FormIffEditor (Task 4) calls this helper from its hand-off path. The test exercises both branches
    directly without going through the form.

    NO csproj edit needed for the test file — SDK-style auto-glob picks it up.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~TreHandoffFallback" && grep -c '<Compile Include="Formats\\\\Tre\\\\TreRecordIndexResolver.cs"' UtinniCoreDotNet/UtinniCoreDotNet.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `UtinniCoreDotNet/Formats/Tre/TreRecordIndexResolver.cs` exists with `public static OpenSource ResolveOrUnknown(string trePath, long offset, string logicalPath)` returning either TreArchive (match) or `OpenSource.Unknown.Instance` (no match)
    - The XML comment on `ResolveOrUnknown` documents BOTH the linear-scan performance disposition (≤ 50k records, < 50ms) AND the duplicate-offset semantics (first match wins; degraded to Unknown for ambiguous cases) — round-2 MEDIUM 12
    - `UtinniCoreDotNet.csproj` contains `<Compile Include="Formats\Tre\TreRecordIndexResolver.cs" />` (grep gate: returns 1; round-2 HIGH-A)
    - `UtinniCoreDotNet.Tests/FormatsTests/Iff/TreHandoffFallbackTests.cs` contains a [Fact] for each branch (match -> TreArchive, mismatch -> Unknown) asserting the `is OpenSource.X` pattern-match contract from `<behavior>`
    - The test runs via `dotnet test --no-build --filter "FullyQualifiedName~TreHandoffFallback"` and exits 0
    - FormIffEditor's TRE hand-off path (Task 4) calls `TreRecordIndexResolver.ResolveOrUnknown(...)` — grep `TreRecordIndexResolver.ResolveOrUnknown` returns ≥ 1 in `FormIffEditor.cs` or the call-site file
  </acceptance_criteria>
  <done>The degraded TRE hand-off contract — recordIndex resolution failure yields `OpenSource.Unknown.Instance`, which DISABLES both Save → In-Place and Save → Repack — is provably enforced by a pure-managed unit test (round-2 MEDIUM 12 docs included on the resolver); checker W-3 closed; explicit csproj entry added (round-2 HIGH-A).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Live-SWG smoke — open, edit, loose-override save, tiered reload, KNOWN-SAFE fixture recipe (08-REVIEWS Codex unique smoke-recipe fold)</name>
  <files>(no files modified — Tier-4 manual verification checkpoint; see <how-to-verify>)</files>
  <action>
    Pause for the maintainer to run the live-SWG smoke in <how-to-verify>: build inline, inject into a
    live SWGEmu/Restoration client, open an IFF in the editor (via the TRE Browser hand-off or file
    picker), edit a known-SAFE leaf payload (08-REVIEWS Codex unique: smoke recipe), save as a loose
    override under the resolved client load path, and trigger the tiered in-session reload. The
    maintainer records the verified loose-override directory the client actually re-reads (resolves
    Open Q2) and the per-asset-class reload matrix (feeds TESTING.md / Open Q3) in the SUMMARY. Do NOT
    auto-advance — this is the documented Tier-4 residual the CLI round-trip harness (08-02) cannot
    cover.

    KNOWN-SAFE SMOKE RECIPE (per 08-REVIEWS Codex unique):
      - Choose a TEXTURE asset (e.g. a recognizable in-world texture); replace its payload with a
        same-format texture file via `Replace bytes from file…` — texture replacement is the LEAST
        likely to produce a semantically invalid asset out of the V1 editing surface.
      - Loose-save it; Reload in client; the in-world texture should swap. Outcome:
        `ReloadedTextures`. Tier-4 pass.
      - Then choose a NON-texture asset (e.g. a datatable); make a trivial structural edit (duplicate
        a chunk); loose-save; Reload in client; expect `PendingNextSceneChange` and the candid copy.
        Trigger a TJT scene change manually (per scene-change-via-TJT project memory); verify the edit
        is reflected post-scene-change. Note landing "naked" post-scene-change is documented baseline
        (project_tjt_scene_change_naked_baseline.md) — NOT a failure.

    OPEN Q2 RESOLUTION (round-2 MEDIUM 10): if the planner's-best-guess loose-override sub-directory
    DOES work, record it; if it does NOT, exercise the Save As… fallback path — pick the actual
    directory, save there, confirm the client picks it up, then verify `IffSaveTargets` persisted the
    user-chosen directory into the `[IffEditor] looseOverrideDir` ini key for next session's default.
  </action>
  <verify>
    <human-check>Criteria 1+2 confirmed against a live client (tiered acceptance): texture-class loose-override + ReloadTextures applies in-session; datatable-class loose-override + PendingNextSceneChange + manual TJT scene change applies after the scene change. The verified loose-override dir + reload matrix are recorded in the SUMMARY. If Open Q2 was NOT resolved by the default, the Save As… fallback + ini persistence was exercised and recorded.</human-check>
  </verify>
  <what-built>
    The IFF Editor opens inside TJT against a live injected SWG client: open an IFF from the TRE Browser
    hand-off (which populates OpenSource.TreArchive) or file picker (OpenSource.LooseFile), edit a
    known-safe leaf payload + a structural op, save as a loose override under the resolved client load
    path (through the framework-side LooseOverridePath root-containment helper + Flush(true) barrier),
    and trigger the tiered in-session reload. This is the Tier-4 manual residual the CLI round-trip
    harness (08-02) cannot cover.
  </what-built>
  <how-to-verify>
    1. Build TheJawaToolbox + UtinniCoreDotNet with VS2026 MSBuild (Release), inline on the main tree.
    2. Launch Utinni and inject into a live SWGEmu/Restoration client; open The Jawa Toolbox.
    3. Open the IFF Editor; verify it loads as a resizable window with the toolbar/tree/leaf-editor layout (Criterion 1).
    4. In the TRE Browser, right-click a resolvable IFF entry -> `Open in IFF Editor`; confirm the chunk tree + payloads load.
       Confirm by inspection (e.g. enable a debug log) that FormIffEditor.Source is now OpenSource.TreArchive with the right trePath / recordIndex / logicalPath.
    5. **Texture-class smoke (in-session pass):** select a texture IFF; right-click the leaf -> Replace bytes from file… with a same-format texture; loose-save; click Reload in client. Expect status `Reloaded (textures)`.
    6. Record the loose-override directory that was written; that directory IS the client's actual reload dir (Open Q2 resolution). If the default subdir was wrong and you used Save As… instead, record the user-chosen path AND confirm it persisted to the `[IffEditor] looseOverrideDir` ini key for next session (round-2 MEDIUM 10).
    7. **Datatable-class smoke (next-scene-change tiered pass):** select a datatable IFF; perform one structural op (e.g. duplicate a chunk); loose-save; click Reload in client. Expect status `Reloads on next scene change`. Trigger a TJT scene change manually; verify the edit is reflected. (Landing "naked" is baseline, not a failure.)
    8. **Negative case — over-cap or invalid:** attempt a Replace-bytes-from-file with a > 64 MB file; expect the save-time validation message, no crash.
    9. **OpenSource.Unknown happy-path:** if reachable in this build (defensive — depends on whether you can construct a malformed descriptor), confirm that on Unknown source, ONLY Save As… is enabled and the other four save items show the "Cannot resolve archive record — use Save As…" tooltip (round-2 MEDIUM 5).
    10. Record in the SUMMARY: which loose-override dir the client actually re-reads, the per-asset-class reload matrix observed (feeds TESTING.md, Open Q3), confirmation that OpenSource was constructed correctly on both open paths, AND (if exercised) confirmation of the Unknown Save-As-only behavior.
  </how-to-verify>
  <resume-signal>Type "approved" with the verified loose-override dir + tiered reload matrix + OpenSource construction confirmation, or describe issues.</resume-signal>
  <done>The maintainer has approved with the verified loose-override directory + per-asset tiered reload matrix + OpenSource construction confirmation recorded in the SUMMARY; Criteria 1 + 2 (tiered) confirmed against a live client.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor -> loose-override / Save-As path on disk | a user-driven local write into the client load path; must not traverse outside the resolved dir |
| editor -> game thread reload bindings | live-client touch must be marshaled to the game thread |
| save Task completion -> Reload button enable | a stale-bytes reload race must be prevented by Flush(true) + button-disabled-while-in-flight |
| degraded TRE hand-off (recordIndex resolution failure) -> save mode gating | fallback must not silently enable Save-In-Place or Save-Repack on a virtual/logical path string (checker W-3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Tampering | path traversal via a crafted loose-override / Save-As path | mitigate | LooseOverridePath.Resolve enforces normalization + rooted/.. rejection + StartsWith(resolvedRoot + sep) (Task 1, framework-side per checker B-1); BCL path APIs throughout (Task 2) |
| T-08-12 | Denial of Service | reload binding called off the game thread -> AV/crash | mitigate | All reload calls inside GameCallbacks.AddMainLoopCall; gate on Game.IsRunning (Task 3) |
| T-08-12b | Tampering | stale-bytes reload race (Reload runs before Write fully on disk) | mitigate | FileStream.Flush(true) before awaiter completes; Reload button disabled while save Task in flight (Task 2 / Task 4) |
| T-08-12c | Repudiation | speculative scene re-setup masquerading as a general IFF reload | mitigate | Tiered acceptance per 08-REVIEWS HIGH-4: refuse to fabricate scene triggers; PendingNextSceneChange outcome surfaces candidly (Task 3) |
| T-08-12d | Tampering | degraded TRE hand-off enables Save-In-Place against a virtual/logical path string | mitigate | On recordIndex resolution failure, Source = OpenSource.Unknown.Instance (NOT OpenSource.LooseFile(descriptor.Path)); both `Source is OpenSource.LooseFile` and `Source is OpenSource.TreArchive` evaluate false; Save-In-Place + Save-Repack stay disabled (Task 4 / Task 4b, checker W-3) |
| T-08-12e | Tampering | terrain reload using the bare static form `GroundScene.ReloadTerrain()` instead of the INSTANCE call `GroundScene.Get().ReloadTerrain()` -> binding fails / no-op | mitigate | Pinned `GroundScene.Get().ReloadTerrain()` call site + positive AND negative grep gates (Task 3 — round-2 MEDIUM 7) |
| T-08-13 | Tampering | widening the MEF SPI when registering the editor | mitigate | Register via existing GetForms() try/catch; GetSubPanels() stays null (Task 4, STAB-04) |
| T-08-13b | Tampering | new .cs files silently fail to compile into UtinniCoreDotNet.dll / TheJawaToolboxDotNet.dll because the OLD-STYLE csproj files do not glob them | mitigate | Explicit `<Compile Include>` entries for `Saving\LooseOverridePath.cs`, `Formats\Tre\TreRecordIndexResolver.cs` in UtinniCoreDotNet.csproj AND `Saving\IffSaveTargets.cs`, `Saving\ClientReloadDispatcher.cs` in TheJawaToolboxDotNet.csproj; grep-gated acceptance (Tasks 1, 2, 3, 4b — round-2 HIGH-A) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No external packages added this plan (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- VS2026 MSBuild of TheJawaToolbox + UtinniCoreDotNet (Release) compiles clean (inline; worktrees off).
- File saves serialize via IffWriter and are byte-exact for untouched chunks (covered automated by 08-02 roundtrip; spot-check fc /b on a saved file).
- Reload bindings only ever called inside AddMainLoopCall (grep gate); terrain branch uses the `.Get()` INSTANCE form (round-2 MEDIUM 7).
- LooseOverridePath unit suite green (Task 1) — framework-side, consumed via the existing ProjectReference; NO linked-source path into UtinniPlugins (checker B-1).
- TreHandoffFallback unit suite green (Task 4b) — degraded recordIndex fallback yields OpenSource.Unknown.Instance, disabling both Save-In-Place + Save-Repack (checker W-3).
- (IF extracted) ClientReloadDispatcher routing-table unit test green (Task 3, round-2 MEDIUM 9).
- UtinniCoreDotNet.csproj contains explicit Compile entries for `Saving\LooseOverridePath.cs` AND `Formats\Tre\TreRecordIndexResolver.cs` (round-2 HIGH-A grep gates).
- TheJawaToolboxDotNet.csproj contains explicit Compile entries for `Saving\IffSaveTargets.cs` AND `Saving\ClientReloadDispatcher.cs` (round-2 HIGH-A grep gates).
- Live-SWG smoke (Task 5) confirms Criteria 1 + 2 (tiered) and resolves Open Q2/Q3.
</verification>

<success_criteria>
- Loose-override + Save/Save-As save modes function and round-trip byte-exact for untouched chunks; root-containment enforced via the framework-side helper.
- Save -> Flush(true) -> Reload barrier prevents stale-bytes race; Reload button disabled while save in flight.
- Tiered forced reload dispatches by asset class on the game thread, with honest PendingNextSceneChange outcome for non-texture/terrain (TIERED ACCEPTANCE per 08-REVIEWS HIGH-4); terrain branch uses `GroundScene.Get().ReloadTerrain()` INSTANCE call (round-2 MEDIUM 7); routing table pinned in source (round-2 MEDIUM 9).
- OpenSource is constructed at open time: LooseFile via Open…, TreArchive via TRE Browser hand-off (with recordIndex resolved from descriptor offset by `TreRecordIndexResolver`), Unknown on the degraded resolution-failure fallback (checker W-3).
- Degraded TRE hand-off disables three save modes but leaves Save As… enabled as the explicit user escape hatch (round-2 MEDIUM 5); provably unit-tested.
- The TRE Browser hand-off opens files in the editor; Plugin.cs registers it without widening the SPI.
- CI's Utinni-only checkout (ci.yml) builds and tests the framework-side LooseOverridePath + TreRecordIndexResolver without any UtinniPlugins checkout step (checker B-1: no linked-source path into UtinniPlugins).
- BOTH old-style csproj files (UtinniCoreDotNet.csproj + TheJawaToolboxDotNet.csproj) explicitly list every new production file (round-2 HIGH-A; corrects prior-round MEDIUM 4 default-glob misconception).
- Live smoke confirms the editor loads and saves+reloads against a live client (Criteria 1, 2 tiered); Open Q2 resolved either by the default subdir OR by the Save As… fallback with ini persistence (round-2 MEDIUM 10).
</success_criteria>

<output>
Create `.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-05-SUMMARY.md` when done (include the verified loose-override dir + per-asset tiered reload matrix + OpenSource construction confirmation, AND a one-line confirmation that the framework-side LooseOverridePath + TreRecordIndexResolver paths + the OpenSource.Unknown degraded fallback + the four new csproj entries close checker B-1 + W-3 + round-2 HIGH-A respectively. Also note whether Open Q2 resolved via the default subdir or via the Save As… fallback path + ini persistence — round-2 MEDIUM 10).
</output>
</content>
