---
phase: 08-tjt-subpanel-iff-editor-read-write
plan: 05
type: execute
wave: 4
depends_on: ["08-04"]
files_modified:
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/IffSaveTargets.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/ClientReloadDispatcher.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs"
autonomous: false
requirements: [PROD-W1-IFF]
must_haves:
  truths:
    - "The editor can save as a loose override file under the resolved client load path (D-05.1)"
    - "The editor can Save / Save-As to an arbitrary path, and save-in-place when opened from a loose file (D-05.2)"
    - "Both file saves serialize via the shared IffWriter and produce a byte-exact file for untouched chunks"
    - "After a file-based save the editor offers a tiered forced reload (textures -> ReloadTextures; terrain -> ReloadTerrain; else scene-change-style / candid fallback copy) on the game thread (D-06)"
    - "The TRE Browser offers an 'Open in IFF Editor' hand-off that opens the resolved payload in FormIffEditor (D-09)"
    - "FormIffEditor is registered in Plugin.cs GetForms() inside the try/catch isolation block; the MEF SPI is not widened"
  artifacts:
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/IffSaveTargets.cs"
      provides: "Loose-override + Save/Save-As file writers (off-UI-thread), using IffWriter + ResolveClientTreDir"
      contains: "looseOverride"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/ClientReloadDispatcher.cs"
      provides: "Tiered D-06 forced reload on the game thread"
      contains: "ReloadTextures"
  key_links:
    - from: "FormIffEditor Save▾"
      to: "IffWriter.Write -> File.WriteAllBytes"
      via: "off-UI-thread file save"
      pattern: "IffWriter\\.Write"
    - from: "ClientReloadDispatcher"
      to: "GameCallbacks.AddMainLoopCall"
      via: "game-thread reload dispatch"
      pattern: "AddMainLoopCall"
    - from: "FormTreBrowser"
      to: "FormIffEditor"
      via: "Open in IFF Editor hand-off"
      pattern: "IFF Editor"
---

<objective>
Wire the two low-risk file save modes — loose override (D-05.1) and Save / Save-As (D-05.2) — to the
`Save ▾` toolbar, add the tiered D-06 forced reload, wire the "Open in IFF Editor" hand-off from the
TRE Browser, and register `FormIffEditor` in `Plugin.cs`. This completes the 8a primary slice; the
higher-risk live patch (08-06) and `.tre` repack (08-07) follow in isolated plans.

Purpose: PROD-W1-IFF Criterion 2 — "save modifications back to a file the live client reloads
correctly" — via the standard SWG modder loop (loose override) + a Save/Save-As baseline, plus the
graceful tiered reload. Criterion 1 (loads in TJT against a live client) is verified here.

Output: `IffSaveTargets.cs`, `ClientReloadDispatcher.cs`, `Save ▾` + `Reload in client` wiring in
`FormIffEditor`, the `FormTreBrowser` hand-off, `Plugin.cs` registration, and a live-SWG smoke
checkpoint. Cross-repo: this plan is entirely in `D:/Code/UtinniPlugins`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-RESEARCH.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-PATTERNS.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-UI-SPEC.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-04-SUMMARY.md

<interfaces>
<!-- 08-01 write path: -->
IffWriter.Write(MutableIffDocument) -> byte[]

<!-- FormTreBrowser.cs (D:/Code/UtinniPlugins): -->
private string ResolveClientTreDir()                       // lines 312-379: process-module-dir primary -> GetWorkingDirectory() -> [..]Dir ini fallback
private void tvTre_AfterSelect(object, TreeViewEventArgs)  // lines 452-503: selection plumbing
private void DispatchDetail(TreMetadata meta, bool resolved, byte[] payload)  // lines 505-536: resolved payload bytes
Game.IsRunning                                             // gate before any live-client touch (line 393)

<!-- Game-thread + reload bindings (UtinniCoreDotNet): -->
GameCallbacks.AddMainLoopCall(Action)                      // marshal onto the SWG game thread (GameCallbacks.cs 254-257)
Graphics.ReloadTextures()                                  // Generated/UtinniCore.cs 12060 -> textureListReloadTextures
GroundScene.ReloadTerrain()                                // Generated/UtinniCore.cs 14343
Graphics.flushResources(bool)                              // Generated/UtinniCore.cs 11769 (broader GPU-resource flush)
Game.AddSetSceneCallback(...)                              // scene-change-style reload fallback (Generated 11106)

<!-- Plugin.cs registration (try/catch isolation block, lines 62-69): forms.Add(new FormX(this)); GetSubPanels() stays null. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IffSaveTargets — loose-override + Save/Save-As (D-05.1/2)</name>
  <read_first>
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs (ResolveClientTreDir 312-379; off-UI-thread Task.Run + marshaling 225-265,486-503; CreateSettings clientDir analog 179-187)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-RESEARCH.md (Pitfall 7 loose-override dir derivation; Open Q2; Runtime State Inventory)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-04-SUMMARY.md (FormIffEditor [IffEditor] looseOverrideDir ini key, MutableIffDocument + IffEditController access)
  </read_first>
  <action>
    Create `Saving/IffSaveTargets.cs` in TheJawaToolboxDotNet (verbatim MIT banner) with two file save
    paths, both serializing via `IffWriter.Write(mutableDoc)` and writing with BCL `System.IO` on a
    background `Task` (UI mutation marshals back via the captured `SynchronizationContext` /
    `BeginInvoke`, the FormTreBrowser precedent): (1) `SaveLooseOverride` — derive the override base
    directory by reusing the `ResolveClientTreDir` approach (process-module-dir primary), then the
    loose-override sub-directory from the `[IffEditor] looseOverrideDir` ini fallback (Open Q2 — the exact
    sub-dir/priority is verified by the live smoke in Task 4); preserve the asset's relative path under
    that root; create directories as needed; use BCL path APIs only (no traversal — write only under the
    resolved root). (2) `SaveToPath` / `SaveInPlace` — write to a `SaveFileDialog` path, or overwrite the
    original loose file when the document was opened from one. Both report success/failure for the
    FormIffEditor status strip (Saved / Save-failed-edits-retained copy). Use the writer's 64 MB cap
    behavior (08-01) — surface a save-time validation rather than crash on an over-cap chunk.
  </action>
  <verify>
    <automated>grep -c "IffWriter.Write" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/IffSaveTargets.cs"</automated>
  </verify>
  <acceptance_criteria>
    - IffSaveTargets.cs exists with the MIT banner and methods for loose-override + Save/Save-As/Save-in-place
    - Both paths call IffWriter.Write and write via BCL System.IO on a background Task
    - The loose-override base dir derives from the ResolveClientTreDir approach + the [IffEditor] looseOverrideDir ini fallback
    - Writes occur only under the resolved client dir / user-chosen path (BCL path APIs; no traversal)
    - VS2026 MSBuild of TheJawaToolbox compiles clean
  </acceptance_criteria>
  <done>The editor can persist edits as a loose override and via Save/Save-As, byte-exact for untouched chunks, off the UI thread.</done>
</task>

<task type="auto">
  <name>Task 2: ClientReloadDispatcher — tiered D-06 forced reload (game thread)</name>
  <read_first>
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-RESEARCH.md (Pitfall 4 tiered reload + no general IFF reload hook; Pattern 3; Open Q3; naked-after-scene-change baseline note)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-PATTERNS.md (forced-reload helper analog + binding line numbers)
    - D:/Code/Utinni/UtinniCoreDotNet/Callbacks/GameCallbacks.cs (AddMainLoopCall 254-257)
  </read_first>
  <action>
    Create `Saving/ClientReloadDispatcher.cs` in TheJawaToolboxDotNet (verbatim MIT banner) implementing
    the D-06 tiered reload, ALL on the game thread via `GameCallbacks.AddMainLoopCall`: classify the saved
    asset and dispatch — texture/shader -> `Graphics.ReloadTextures()`; terrain -> `GroundScene.ReloadTerrain()`;
    otherwise attempt a scene-change-style re-setup (the user's established TJT repro path via the
    set-scene path) OR, when no live client (`!Game.IsRunning`) or no applicable hook, return a result
    that drives the candid UI-SPEC copy ("In-session reload isn't available for this asset — reload
    happens on the next scene change."). Gate the whole dispatch behind `Game.IsRunning`. NEVER call any
    reload binding off the game thread. Expose the outcome (reloaded-via-X / fallback / unavailable) so
    FormIffEditor can render the status-strip + button-tooltip states from UI-SPEC. Note in a code comment
    that post-scene-change "naked" is the documented baseline, not a reload failure.
  </action>
  <verify>
    <automated>grep -c "AddMainLoopCall" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/ClientReloadDispatcher.cs"</automated>
  </verify>
  <acceptance_criteria>
    - ClientReloadDispatcher.cs exists with the MIT banner
    - Every reload binding call (ReloadTextures/ReloadTerrain/scene re-setup) is inside a GameCallbacks.AddMainLoopCall lambda
    - The dispatcher gates on Game.IsRunning and returns a distinct outcome for texture / terrain / scene-change / unavailable
    - No reload binding is called directly from the UI thread
    - VS2026 MSBuild of TheJawaToolbox compiles clean
  </acceptance_criteria>
  <done>A tiered, game-thread-marshaled forced reload dispatches by asset class and degrades gracefully to the candid fallback copy when no hook applies.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Save▾ + Reload, the TRE Browser hand-off, and Plugin.cs registration</name>
  <read_first>
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormIffEditor.cs (Save▾/Reload placeholders + status strip + dirty state from 08-04)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs (tvTre_AfterSelect 452-503; DispatchDetail 505-536; metadata context-menu pattern)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs (forms.Add try/catch block 62-69; GetForms/GetSubPanels 99-109)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-UI-SPEC.md (Save-target menu labels + Copywriting; Reload states; single-document discard-confirm)
  </read_first>
  <action>
    In `FormIffEditor`: populate the `Save ▾` drop-down with the two file modes available now —
    `Save as loose override` (no confirm; calls `IffSaveTargets.SaveLooseOverride`) and `Save` /
    `Save As…` (no confirm; `IffSaveTargets.SaveToPath`/`SaveInPlace`). Leave `Patch live client (in
    memory)` and `Repack into source .tre…` as disabled placeholders (08-06 / 08-07 enable them). On a
    successful file-based save, clear dirty markers, set the `Saved <name> (<mode>)` status, then offer
    `Reload in client` -> `ClientReloadDispatcher` with the UI-SPEC reload states + tooltips; disable the
    button when `!Game.IsRunning`. Implement the single-document discard-confirm (open-replace / close
    while dirty) per UI-SPEC. Add an `Open…` toolbar action (OpenFileDialog -> read bytes -> IffReader ->
    MutableIffDocument.FromDocument -> bind). In `FormTreBrowser`, add an `Open in IFF Editor` entry to
    the selected-leaf context menu (extend the metadata-menu pattern) that passes the resolved payload
    bytes + the entry's logical path to a new `FormIffEditor` (find-or-create the existing instance from
    the plugin's forms list); keep the browser read-only. In `Plugin.cs`, register `FormIffEditor` inside
    the SAME try/catch isolation block the TRE Browser uses (`try { forms.Add(new FormIffEditor(this)); }
    catch (Exception ex) { Log.Info("Failed to create FormIffEditor; IFF Editor will be unavailable: " + ex); }`).
    `GetSubPanels()` stays null; do NOT widen the MEF SPI (CON-M-01/02, STAB-04).
  </action>
  <verify>
    <automated>grep -c "FormIffEditor" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs"</automated>
  </verify>
  <acceptance_criteria>
    - Save▾ offers the two file modes wired to IffSaveTargets; live-patch + repack are disabled placeholders
    - A successful save clears dirty state and shows the `Saved <name> (<mode>)` status; a failure retains dirty state with the UI-SPEC copy
    - Reload in client calls ClientReloadDispatcher and reflects the UI-SPEC reload/unavailable states; disabled when !Game.IsRunning
    - FormTreBrowser has an `Open in IFF Editor` context-menu entry passing resolved payload bytes; the browser stays read-only
    - Plugin.cs registers FormIffEditor in the try/catch block; GetSubPanels() stays null (SPI not widened)
    - VS2026 MSBuild of TheJawaToolbox compiles clean
  </acceptance_criteria>
  <done>Save▾ (file modes), Reload in client, the TRE Browser hand-off, and Plugin.cs registration are wired; the IFF Editor is reachable and can save+reload.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live-SWG smoke — open, edit, loose-override save, reload (Criteria 1, 2)</name>
  <files>(no files modified — Tier-4 manual verification checkpoint; see <how-to-verify>)</files>
  <action>
    Pause for the maintainer to run the live-SWG smoke in <how-to-verify>: build inline, inject into a
    live SWGEmu/Restoration client, open an IFF in the editor (via the TRE Browser hand-off or file
    picker), edit a leaf payload + one structural op, save as a loose override under the resolved client
    load path, and trigger the tiered in-session reload. The maintainer records the verified
    loose-override directory the client actually re-reads (resolves Open Q2) and the per-asset-class
    reload matrix (feeds TESTING.md / Open Q3) in the SUMMARY. Do NOT auto-advance — this is the
    documented Tier-4 residual the CLI round-trip harness (08-02) cannot cover.
  </action>
  <verify>
    <human-check>Criteria 1+2 confirmed against a live client: editor loads in TJT, opens an IFF, edits + saves a loose override, and the client reflects the edit on reload (or the candid fallback + a TJT scene change reflects it). The verified loose-override dir + reload matrix are recorded in the SUMMARY.</human-check>
  </verify>
  <what-built>
    The IFF Editor opens inside TJT against a live injected SWG client: open an IFF from the TRE Browser
    hand-off or file picker, edit a leaf payload + a structural op, save as a loose override under the
    resolved client load path, and trigger the tiered in-session reload. This is the Tier-4 manual
    residual the CLI round-trip harness (08-02) cannot cover.
  </what-built>
  <how-to-verify>
    1. Build TheJawaToolbox + UtinniCoreDotNet with VS2026 MSBuild (Release), inline on the main tree.
    2. Launch Utinni and inject into a live SWGEmu/Restoration client; open The Jawa Toolbox.
    3. Open the IFF Editor; verify it loads as a resizable window with the toolbar/tree/leaf-editor layout (Criterion 1).
    4. In the TRE Browser, right-click a resolvable IFF entry -> `Open in IFF Editor`; confirm the chunk tree + payloads load.
    5. Edit a leaf's bytes (hex) and perform one structural op (e.g. duplicate a chunk); confirm dirty markers + Undo/Redo work.
    6. Save ▾ -> `Save as loose override`; note the directory written (this confirms Open Q2 — which dir the client searches).
    7. Click `Reload in client`; observe the reload state copy. For a datatable/template, expect the candid "reload on next scene change" fallback; trigger a TJT scene change and confirm the edit is reflected (note: landing "naked" post-scene-change is the documented baseline, not a failure).
    8. Record in the SUMMARY: which loose-override dir the client actually re-reads, and the per-asset-class reload matrix observed (feeds TESTING.md, Open Q3).
  </how-to-verify>
  <resume-signal>Type "approved" with the verified loose-override dir + reload matrix, or describe issues.</resume-signal>
  <done>The maintainer has approved with the verified loose-override directory + per-asset reload matrix recorded in the SUMMARY; Criteria 1 + 2 confirmed against a live client.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor -> loose-override / Save-As path on disk | a user-driven local write into the client load path; must not traverse outside the resolved dir |
| editor -> game thread reload bindings | live-client touch must be marshaled to the game thread |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Tampering | path traversal via a crafted loose-override / Save-As path | mitigate | BCL path APIs; write only under the resolved client dir / user-chosen path; never auto-elevate (Task 1) |
| T-08-12 | Denial of Service | reload binding called off the game thread -> AV/crash | mitigate | All reload calls inside GameCallbacks.AddMainLoopCall; gate on Game.IsRunning (Task 2) |
| T-08-13 | Tampering | widening the MEF SPI when registering the editor | mitigate | Register via existing GetForms() try/catch; GetSubPanels() stays null (Task 3, STAB-04) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No external packages added this plan (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- VS2026 MSBuild of TheJawaToolbox (Release) compiles clean (inline; worktrees off).
- File saves serialize via IffWriter and are byte-exact for untouched chunks (covered automated by 08-02 roundtrip; spot-check fc /b on a saved file).
- Reload bindings only ever called inside AddMainLoopCall (grep gate).
- Live-SWG smoke (Task 4) confirms Criteria 1 + 2 and resolves Open Q2/Q3.
</verification>

<success_criteria>
- Loose-override + Save/Save-As save modes function and round-trip byte-exact for untouched chunks.
- Tiered forced reload dispatches by asset class on the game thread, degrading gracefully.
- The TRE Browser hand-off opens files in the editor; Plugin.cs registers it without widening the SPI.
- Live smoke confirms the editor loads and saves+reloads against a live client (Criteria 1, 2).
</success_criteria>

<output>
Create `.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-05-SUMMARY.md` when done (include the verified loose-override dir + per-asset reload matrix).
</output>
