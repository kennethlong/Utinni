---
phase: 08-tjt-subpanel-iff-editor-read-write
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/IffChunkTree.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/TreDetailPane.cs"
autonomous: true
requirements: [PROD-W1-IFF]
must_haves:
  truths:
    - "A shared IffChunkTree UserControl renders the IFF chunk tree (TAG [SubType] · N bytes · @offset) extracted from TreDetailPane"
    - "The shared control can render from an immutable IffDocument (read-only consumers) AND bind to a MutableIffDocument (editable consumers)"
    - "TreDetailPane consumes the shared control with its public read API (LoadIff, ShowEmpty, ShowReadable, ShowParseFailure, ShowEncrypted, ShowDecoding, ShowStringTable, ShowUnsupportedRaw) unchanged at the SIGNATURE LEVEL, so Phase 7's TRE Browser still works read-only — explicitly proven by a signature-pin regression test (08-REVIEWS LOW-2)"
    - "The chunk tree exposes a structural-op context menu surface and an AfterSelect event the editor can subscribe to"
  artifacts:
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/IffChunkTree.cs"
      provides: "Reusable themed chunk-tree UserControl (read + editable binding) for FormTreBrowser and FormIffEditor"
      contains: "class IffChunkTree"
  key_links:
    - from: "TreDetailPane.LoadIff"
      to: "IffChunkTree"
      via: "delegate to the shared control"
      pattern: "IffChunkTree"
    - from: "IffChunkTree"
      to: "MutableIffDocument"
      via: "editable binding for the IFF editor"
      pattern: "MutableIffDocument"
---

<objective>
Extract Phase 7's chunk-tree surface (`TreDetailPane.LoadIff` + `tvChunks` + `BuildChunkNode`) into a
shared themed `IffChunkTree` UserControl (D-09), then have `TreDetailPane` consume it without changing
its public read API. This is the reusable control both the read-only TRE Browser (`FormTreBrowser`)
and the new editable `FormIffEditor` (08-04) build on; Phases 9-11 reuse it too.

Purpose: D-09 — the TRE Browser stays read-only; the editor gets the same tree surface plus structural
editing. Phase 7 built `LoadIff`/`tvChunks` `public`/standalone for exactly this reuse (D-13).

Output: `IffChunkTree.cs` (new shared UserControl), `TreDetailPane.cs` (modified to delegate to it).
Cross-repo: this plan lives entirely in the sibling repo `D:/Code/UtinniPlugins`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-RESEARCH.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-PATTERNS.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-UI-SPEC.md
@.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-01-SUMMARY.md

<interfaces>
<!-- The surface to extract (D:/Code/UtinniPlugins .../UI/Controls/TreDetailPane.cs): -->
public class TreDetailPane : UserControl
  public void LoadIff(IffDocument doc)            // lines 188-198: BeginUpdate; Nodes.Clear; Nodes.Add(BuildChunkNode(doc.Root)); [0].Expand; EndUpdate
  private TreeNode BuildChunkNode(IffChunk chunk) // lines 491-507: node label "TAG [SubType]  ·  N bytes  ·  @offset"
  private readonly TreeView tvChunks              // lines 670-675 themed: BackColor=PrimaryHighlight, ForeColor=Font, BorderStyle=None, HideSelection=false, ShowLines=true
  private readonly TextBox txtHex                 // read-only hex dump (stays in TreDetailPane / moves with the editor in 08-04)
  private readonly UtinniContextMenuStrip metaMenu// lines 72,625-631 — the context-menu pattern to reuse
  ShowEmpty/ShowReadable/ShowParseFailure         // four-state degradation (one bad file never crashes)
  // Other public surface called by FormTreBrowser (full list, must remain signature-pinned):
  //   ShowDecoding(TreMetadata), ShowEmpty(), ShowReadable(TreMetadata, byte[]), ShowParseFailure(TreMetadata, string),
  //   ShowEncrypted(TreMetadata), ShowStringTable(TreMetadata, byte[]), ShowUnsupportedRaw(TreMetadata, byte[]),
  //   LoadIff(IffDocument)

<!-- 08-01 mutable model (UtinniCoreDotNet/Formats/Iff) the editable binding targets: -->
MutableIffDocument { MutableIffNode Root; FromDocument(IffDocument, byte[]); structural ops }
IffDocument (sealed) { IffChunk Root; IReadOnlyList<IffChunk> AllNodesInPreorder }   // existing read model
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the shared IffChunkTree UserControl</name>
  <read_first>
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/TreDetailPane.cs (LoadIff 188-198; BuildChunkNode 491-507; tvChunks theming 670-675; metaMenu 72,625-631; four-state ShowEmpty/ShowReadable/ShowParseFailure 113-254)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-01-SUMMARY.md (MutableIffDocument / MutableIffNode API)
    - D:/Code/Utinni/.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-UI-SPEC.md (Left region — editable chunk tree; structural-op context menu items)
  </read_first>
  <action>
    Create `UI/Controls/IffChunkTree.cs` in TheJawaToolboxDotNet (verbatim MIT banner) as a themed
    `UserControl` wrapping a `TreeView` (the extracted `tvChunks` theming: `BackColor =
    Colors.PrimaryHighlight()`, `ForeColor = Colors.Font()`, `BorderStyle = None`, `HideSelection = false`,
    `ShowLines = true`). Expose: (a) `public void LoadDocument(IffDocument doc)` building nodes via the
    extracted `BuildChunkNode` logic with the EXACT Phase 7 node label format `TAG [SubType]  ·  N bytes
    ·  @offset` (preserve for parity); (b) `public void LoadMutable(MutableIffDocument doc)` binding to
    the mutable model for editable consumers — each TreeNode's `Tag` references its `MutableIffNode` so
    the editor can map selection back to the model; (c) a public `AfterSelect`-style event (or expose the
    selected node) so a host can react to leaf/container selection; (d) a `UtinniContextMenuStrip` hook
    for the structural-op menu (the menu ITEMS + handlers are wired by the editor host in 08-04 — this
    control only provides the attachment point and the themed menu using the `metaMenu` pattern). Keep the
    four-state degradation philosophy (a malformed tree shows a state, never crashes). The control must NOT
    take a hard dependency on editor-only types; read-only consumers use `LoadDocument` and never touch
    the mutable surface.
  </action>
  <verify>
    <automated>grep -c "class IffChunkTree" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/IffChunkTree.cs"</automated>
  </verify>
  <acceptance_criteria>
    - IffChunkTree.cs exists under TheJawaToolboxDotNet/UI/Controls/ with the verbatim MIT banner
    - IffChunkTree.cs contains `class IffChunkTree` and `public void LoadDocument(IffDocument` and `public void LoadMutable(MutableIffDocument`
    - Node labels use the exact format `TAG [SubType]  ·  N bytes  ·  @offset` (parity with Phase 7)
    - The themed TreeView uses Colors.*() accessors only — no raw Color.FromArgb literals
    - The control exposes a selection event/property and a context-menu attachment point for structural ops
  </acceptance_criteria>
  <done>A reusable IffChunkTree UserControl renders the chunk tree from either an IffDocument (read-only) or a MutableIffDocument (editable), with the Phase 7 node label format and a structural-op menu attachment point.</done>
</task>

<task type="auto">
  <name>Task 2: TreDetailPane consumes IffChunkTree (read API unchanged + signature-pin regression — 08-REVIEWS LOW-2)</name>
  <read_first>
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/TreDetailPane.cs (full file — current tvChunks usage + LoadIff + ShowReadablePanel)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/IffChunkTree.cs (from Task 1)
    - D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs (every TreDetailPane public-API call site — the regression test's reference list)
  </read_first>
  <action>
    Modify `TreDetailPane.cs` to host an `IffChunkTree` instance in place of the inline `tvChunks`
    TreeView, delegating its existing `public void LoadIff(IffDocument doc)` to
    `iffChunkTree.LoadDocument(doc)`. The PUBLIC read API of TreDetailPane (`LoadIff`, `ShowEmpty`,
    `ShowReadable`, `ShowParseFailure`, `ShowEncrypted`, `ShowDecoding`, `ShowStringTable`,
    `ShowUnsupportedRaw`, and any members FormTreBrowser calls) MUST remain byte-compatible at the
    signature level — Phase 7's TRE Browser is built against it. Keep `txtHex` and the four-state panel
    logic in TreDetailPane (the read-only browser's detail pane is unchanged in behavior). Preserve the
    themed layout (Size-before-SplitterDistance, Dock.Fill added first — the WinForms gotchas at lines
    733-756). After this change, rebuild TJT in the same commit (the control's compile unit changed; TJT
    references UtinniCoreDotNet.dll).

    SIGNATURE-PIN REGRESSION (08-REVIEWS LOW-2): add an inline assertion at the end of TreDetailPane.cs
    (XML comment block listing the pinned public-method signature shapes that FormTreBrowser calls)
    AND ensure each of those public methods is still present AND callable from FormTreBrowser without
    edits — verify by adding a script-style grep gate against FormTreBrowser.cs that every pinned method
    name still resolves as a `_detail.<name>(` call site in the existing browser code, AND the TJT build
    of FormTreBrowser.cs compiles UNCHANGED. If any signature would have to change, STOP and surface in
    the SUMMARY rather than silently breaking Phase 7.
  </action>
  <verify>
    <automated>grep -c "IffChunkTree" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/TreDetailPane.cs"</automated>
  </verify>
  <acceptance_criteria>
    - TreDetailPane.cs references IffChunkTree and delegates LoadIff to it
    - TreDetailPane's public read API (LoadIff/ShowEmpty/ShowReadable/ShowParseFailure/ShowEncrypted/ShowDecoding/ShowStringTable/ShowUnsupportedRaw) signatures are unchanged — grep proves each public-method name is still present
    - FormTreBrowser.cs is NOT modified by this plan and STILL COMPILES (every call site like `_detail.ShowEmpty()`, `_detail.LoadIff(doc)`, etc. resolves without source edits) — read-only browser behavior preserved
    - VS2026 MSBuild of TheJawaToolbox compiles clean and the TRE Browser still loads an IFF chunk tree
    - No raw Color.FromArgb introduced; Colors.*() accessors only
  </acceptance_criteria>
  <done>TreDetailPane delegates its chunk tree to the shared IffChunkTree with no public-API change; the read-only TRE Browser is functionally unchanged and compiles against the pinned signature surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed IFF (possibly malformed) -> IffChunkTree rendering | a bad/oversized tree must render a state, never crash the editor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Denial of Service | IffChunkTree rendering a malformed/huge chunk tree | mitigate | Preserve the four-state degradation pattern (ShowEmpty/ShowReadable/ShowParseFailure); reader caps already bound node count/size upstream (08-01/07) |
| T-08-07 | Tampering | extraction silently changing TreDetailPane's read API and breaking Phase 7 | mitigate | Keep public read API byte-compatible; rebuild TJT in the same commit; verify TRE Browser still renders; signature-pin regression check on FormTreBrowser call sites (Task 2) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No external packages added this plan (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- VS2026 MSBuild of TheJawaToolbox (Release) compiles clean (build inline on the main tree; worktrees off).
- TRE Browser still renders an IFF chunk tree on a selected entry (manual quick check or existing Phase 7 path).
- IffChunkTree exposes both LoadDocument and LoadMutable.
- FormTreBrowser.cs is unedited by this plan and still compiles against TreDetailPane (signature pin).
</verification>

<success_criteria>
- Shared IffChunkTree UserControl exists, supporting read-only and editable binding.
- TreDetailPane delegates to it with no public read-API change.
- The TRE Browser is functionally unchanged (CON-T-05 / Phase 7 preservation).
</success_criteria>

<output>
Create `.planning/phases/08-tjt-subpanel-iff-editor-read-write/08-03-SUMMARY.md` when done.
</output>
</content>
</invoke>