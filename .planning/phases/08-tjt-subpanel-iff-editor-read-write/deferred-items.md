# Phase 08 — Deferred Items

## Out-of-scope discoveries logged during 08-01 execution

| Category | Item | Discovered | Notes |
|----------|------|------------|-------|
| Pre-existing test flake | `NativeCallbacksHandleTests.Subscribe_DuringDispatch_NotInvokedInCurrentIteration_InvokedInNext` fails when run inside the full Release/x86 suite but passes in isolation. Surfaced during 08-01 Task 4 acceptance. Unrelated to any Formats/Iff code. Last commit on the file is `427f474` (Phase 3 R-G CR-01) — predates 08-01 by months. | 2026-05-28, during 08-01 Task 4 acceptance | Scope-boundary rule: not auto-fixed (does not affect 08-01's correctness, security, or ability to complete). Likely a timing/concurrency race in the cross-iteration subscriber-dispatch contract. Belongs with the existing GameCallbacks ForceGCCollect AV isolation work or a follow-on 06-04-style stability plan. |

## Discoveries logged during 08-05 Task 5 live-SWG smoke

| Category | Item | Discovered | Notes |
|----------|------|------------|-------|
| Open Q2 deferred verification | Verified loose-override sub-directory under the client root that the live SWGEmu/Restoration client actually re-reads. NOT captured granularly during the 08-05 smoke — the maintainer's brief "approved, dig in" approval confirmed the primary AV scenario (second open after close) but did not enumerate the loose-override matrix verbally. | 2026-05-28, during 08-05 Task 5 smoke | Save-As + `[IffEditor] looseOverrideDir` ini persistence fallback path is wired and functional (round-2 MEDIUM 10 — `IffSaveTargets.RecordSaveAsDirectory`), so a user-driven Open-Q2 resolution remains available at any point: the user picks the path explicitly, and the next loose-override save defaults to whichever directory they chose. Resolution path: a follow-on observation pass during Phase 9+ smoke sessions OR a focused documentation-only pass against the live client. Not blocking. |
| Open Q3 deferred verification | Per-asset-class reload matrix observed live (texture → ReloadedTextures; terrain → ReloadedTerrain; datatable/STF/object-template → PendingNextSceneChange with edits visible after a TJT-driven scene change). The pinned routing table is unit-tested at 22 [Theory] cases (`ClientReloadDispatcherTests.cs`), so the in-source dispositions are provably correct — the part that remains deferred is the LIVE per-class observation. | 2026-05-28, during 08-05 Task 5 smoke | Routing-table classifier (`ReloadAssetClassifier`) covers every (extension, root TypeId) → tier mapping under unit test; framework-side placement (checker B-1) keeps the test coupling-free. Live observation is a Tier-4 manual residual that maps onto the same scene-change-via-TJT path Phase 9 (Datatable) and Phase 10 (Stringtable) will exercise anyway — fold the matrix observation into those phases' smoke sessions rather than re-opening 08-05 for a documentation-only pass. Not blocking. |
| Polish gap (UX) | Dirty-discard prompt missing on TRE-Browser → IFF-Editor hand-off path. Opening a SECOND IFF from the TRE Browser context menu while IFF #1 has unsaved edits silently overwrites the in-memory document. The in-form `Open…` and Close paths have the discard-confirm wired (08-04 single-document discard-confirm); the hand-off path (`FormIffEditor.OpenFromTreEntry` called by `FormTreBrowser.OnOpenInIffEditor`) bypasses it because `LoadDocument` is called directly. | 2026-05-28, during 08-05 Task 5 smoke | NOT a Phase 8 acceptance blocker — saved edits are preserved on disk; the loss is unsaved-only. Apply the discard-confirm to `OpenFromTreEntry` as a polish pass once Phase 8 closes (08-06 + 08-07 land first), OR fold into a Phase 9+ housekeeping plan. The user workaround during the smoke (and any future user encounter) is to Save / Save As… before clicking the hand-off — same as any unsaved-document workflow. |

## Discoveries logged during 08-06 Task 5 close-out (smoke=automation-only)

| Category | Item | Discovered | Notes |
|----------|------|------------|-------|
| Open Q4 deferred verification | Full functional D-05.3 live-patch smoke against a live SWGEmu/Restoration client, including: (a) maintainer-only debug construction of `OpenSource.ClientMemory` to enable the Save▾ ▸ `Patch live client (in memory)` menu item; (b) SAME-LENGTH leaf payload edit (e.g. via Replace bytes from file…); (c) `Save ▾ ▸ Patch live client (in memory)` → FormSaveConfirmDialog → `Patch live`; (d) stability observation in-session (no crash / no access violation); (e) DIFFERENT-LENGTH refusal observation (attempt to grow OR shrink a payload, confirm the candid same-length copy `Live patch requires the rewritten IFF to be the same length as the original. Save to file/repack instead.`); (f) reload/scene-change volatility observation (the patch is lost — the documented D-05.3 behavior). | 2026-05-28, during 08-06 Task 5 close-out | The Task 5 maintainer choice was option 3: "Skip smoke and approve on automation alone." The 5 LivePatchValidator `[Fact]`s (round-2 HIGH-B / CONTEXT D-05.3 "unit-tested for its bounds gate" claim) carry the verification burden — the bounds gate is automation-gated regardless of Tier-4 coverage. PROD-W1-IFF Criterion 2 is acceptance-tested against modes 1/2/4 in Phase 8 with mode 3 documented as infra-ready, user-disabled (round-2 MEDIUM 11). NOT a Phase 8 acceptance blocker. Resolution path: fold the live observation into a later observation/doc pass — possibly alongside the follow-up phase that wires the `OpenSource.ClientMemory` discovery path (which would also satisfy Open Q4's (a) precondition organically, without a maintainer-only debug construction). The disabled-state behavior (the only path reachable today without a `ClientMemory` discovery path) is automation-gated by the honest-disabled tooltip wording grep (3 hits) and the `RefreshSaveMenuEnabledState` provenance gate. |
