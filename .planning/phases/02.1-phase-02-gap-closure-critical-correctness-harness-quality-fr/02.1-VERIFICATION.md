---
phase: "02.1-phase-02-gap-closure-critical-correctness-harness-quality-fr"
verified: "2026-05-18T23:00:00Z"
resolved: "2026-05-18T23:50:00Z"
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
ci_run: 26067903389
code_review_findings:
  critical: 0
  warning: 4
  info: 2
human_verification_resolved:
  - test: "Confirm WR-05 affirmative getVtbl test passes (or is Skip-ped) on windows-2022 CI"
    result: passed
    evidence: "CI run #26067903389 on commit 86c0e81 — conclusion=success. GetVtbl_WithD3d9Loaded_ReturnsNonZero passed on windows-2022 (SysWOW64\\d3d9.dll contains the vtable pattern at the expected offset)."
  - test: "Run /gsd:code-review 02.1 and confirm no new Critical findings (Success Criterion 2)"
    result: passed
    evidence: "Spawned gsd-code-reviewer on the 11 source files modified in Phase 02.1. Verdict: 0 Critical, 4 Warning, 2 Info — see 02.1-REVIEW.md. The 4 warnings are minor non-blockers (init-function idempotence guards × 2, inline-asm reinterpret_cast formal-UB documentation, GetVtbl test runner-bitness guard) and are tracked for Phase 6 STAB-03 cleanup."
---

# Phase 02.1: Gap Closure Verification Report

**Phase Goal:** Close 4 correctness gaps (CR-02/CR-03/CR-04/WR-03) and 4 harness-quality regressions (WR-01/WR-02/WR-05/WR-09) that the Phase 02 code review surfaced after executor agents reported plans complete. Every finding closed with a regression test that would fail if the fix were reverted.
**Verified:** 2026-05-18T23:00:00Z
**Resolved:** 2026-05-18T23:50:00Z (CI passed + code-review verdict obtained)
**Status:** passed
**Re-verification:** No — initial verification confirmed after CI + code-review feedback

## Goal Achievement

### Observable Truths (from CONTEXT.md Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | All 8 findings in scope (CR-02, CR-03, CR-04, WR-01, WR-02, WR-03, WR-05, WR-09) are closed in code with a regression test | VERIFIED | All 8 rows in docs/ai/assessment.md show `done` or `done (partial-proof)` with commit SHAs. Code-level verification confirms each fix below. |
| 2 | `/gsd:code-review 02.1` at phase end reports no new Critical findings | UNCERTAIN (human) | No code-review artifact was produced. The phase did not run a post-execution `/gsd:code-review 02.1`. This success criterion cannot be verified programmatically; it requires a human to run the review or confirm it was conducted informally. |
| 3 | CI stays green across all 02.1 commits on `master` (windows-2022, Release\|x86, `dotnet test`) | UNCERTAIN (human) | All 11 Phase 02.1 commits (54e0211 through 86c0e81, plus asm-syntax fix 406ea55) are in the git log on `master`. The WR-05 test (ca08ff0) was written without `[Skip]` — no subsequent Skip commit exists, meaning CI either passed the non-zero assertion or the phase was declared done before CI ran the new test. A human must inspect the CI run covering ca08ff0. All other tests are deterministic and have no pattern-presence uncertainty. |
| 4 | `docs/ai/assessment.md` records the disposition of each finding closed | VERIFIED | All 8 findings appear in the Phase 02 code-review findings table at assessment.md:590-601, each with status and commit SHA(s). WR-02 is honestly marked `done (partial-proof)` with clear explanation. |
| 5 | The Tier-4 manual UATs from Phase 02 are NOT regressed | VERIFIED | utinni_init changes (CR-04/WR-03 eager-init additions) are strictly additive: two calls inserted BEFORE `createDetours()`. The `/EXPORT:utinni_init=_utinni_init@4` linker pragma at utinni.cpp:113 is confirmed preserved in code. `getPresentBlockedEvent()` still returns the HANDLE (now eagerly created instead of lazily created — backward-compatible for FormMain.cs). The C-01 loader-lock fix and C-09 signaller fix are untouched. |

**Score:** 4/5 truths verified (1 uncertain on /gsd:code-review, 1 uncertain on WR-05 CI outcome)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `UtinniCore/swg/misc/network.cpp` | pCast OUT param int64_t*; realCast/cast split; setCastForTest/resetCast implementations | VERIFIED | Line 36: `using pCast = int64_t(__thiscall*)(int64_t*, int, int)` confirmed in network.h (typedef moved to header). network.cpp:45-52: `static pCast realCast = (pCast)0xAA4900; pCast cast = realCast;` + seam implementations. Network::cast(int64_t id) body with static_assert and well-defined shift. |
| `UtinniCore/swg/misc/network.h` | pCast typedef int64_t*; setCastForTest/resetCast declarations | VERIFIED | Lines 33-39: `namespace swg::network` block with `using pCast = int64_t(__thiscall*)(int64_t*, int, int)` and both seam declarations. |
| `UtinniCore/utinni.cpp` | CoInitializeEx absent; eager-init calls before createDetours() | VERIFIED | grep "CoInitializeEx" returns only comment text (no actual call). Lines 131-139: `directX::initPresentBlockedEvent()` + `directX::initDepthTexture()` appear immediately before `createDetours()`. `/EXPORT:utinni_init=_utinni_init@4` pragma preserved at line 113. |
| `UtinniCore/swg/graphics/directx9.cpp` | initPresentBlockedEvent() + initDepthTexture() implemented; getPresentBlockedEvent() is pure reader; cleanup() has threading contract comment; delete depthTexture absent from cleanup() | VERIFIED | Lines 59-63: `initPresentBlockedEvent()` creates HANDLE. Lines 81-84: `initDepthTexture()` allocates `new DepthTexture()`. Lines 47-50: `getPresentBlockedEvent()` returns `hPresentBlockedEvent` with no CreateEvent call. Lines 410-427: `cleanup()` body contains ONLY the threading-contract comment — `delete depthTexture` intentionally omitted. |
| `UtinniCore/swg/graphics/directx9.h` | initPresentBlockedEvent() + initDepthTexture() declared | VERIFIED | Lines 32-33: both declarations present in `namespace directX`. |
| `UtinniCore/test_exports.cpp` | testCastDoubleNaked naked-asm ABI-shim; utinni_test_networkCast via setCastForTest; 4 new eager-init exports; utinni_test_resolveExports for 12 exports | VERIFIED | Lines 124-156: `__declspec(naked) testCastDoubleNaked` with inline asm writing sentinel through ECX (int64_t*), `ret 8` callee-clean. Lines 174-192: `utinni_test_networkCast` reseats via `setCastForTest(testCastDoubleNaked)`, calls `Network::cast`, restores. Lines 225-260: 4 eager-init test exports. Lines 294-334: `utinni_test_resolveExports` probes 12 exports via GetProcAddress. Post-asm-syntax fix commit 406ea55 confirmed. |
| `UtinniCoreDotNet.Tests/NetworkCastTests.cs` | Full 64-bit sentinel assertion (0xDEADBEEFCAFEBABEL) | VERIFIED | Line 72: `Assert.Equal(unchecked((long)0xDEADBEEFCAFEBABEL), result)`. No hardcoded return path. |
| `UtinniCoreDotNet.Tests/ConfigBufferFreeTests.cs` | Marshal.AllocHGlobal; GCHandle.Alloc absent; PARTIAL-PROOF documented | VERIFIED | Line 77: `Marshal.AllocHGlobal(16)`. Line 94: `Marshal.FreeHGlobal(osHeapBuffer)`. No `GCHandle.Alloc`. Class XML doc contains full PARTIAL-PROOF explanation with Release/MT heap caveat and Phase 6 STAB-03 follow-up. |
| `UtinniCoreDotNet.Tests/FindPatternHarnessTests.cs` | 4th test GetVtbl_WithD3d9Loaded_ReturnsNonZero without [Skip] | VERIFIED (structure) / UNCERTAIN (CI outcome) | Lines 131-165: test exists, calls `LoadLibraryA("d3d9.dll")` then `Utinni_GetVtbl()`, asserts non-zero. No `[Skip]` attribute on the `[Fact]` — no subsequent Skip commit made. CI outcome requires human check. |
| `UtinniCoreDotNet.Tests/ExportResolutionTests.cs` | New file; asserts 12 exports resolve | VERIFIED | File confirmed. `ExpectedExportCount = 12`. Test `ResolveExports_AllDocumentedCLinkageExports_ResolveByUndecoratedName` calls `Utinni_TestResolveExports()` and asserts `== 12`. |
| `UtinniCoreDotNet.Tests/FormMainSignallerTests.cs` | 5 tests total (3 original + 2 D-04 regression guards); new tests assert non-zero | VERIFIED | 5 `[Fact]` methods confirmed: 3 original WaitForPresentBlock tests + `InitPresentBlockedEvent_AfterEagerInit_ReturnsNonNullHandle` (asserts `!= UIntPtr.Zero`) + `InitDepthTexture_AfterEagerInit_ReturnsNonNullPointer` (asserts `!= UIntPtr.Zero`). Both new tests catch `DllNotFoundException` for local builds; CI validates P/Invoke path. |
| `docs/ai/assessment.md` | All 8 findings rows with status done/done (partial-proof) and commit SHAs | VERIFIED | Lines 590-601: table with all 8 findings plus closure summary. |
| `.planning/ROADMAP.md` | Phase 02.1 entry with real goal, STAB-01, 3-plan list with [x] | VERIFIED | Lines 65-75: real goal text, `[STAB-01]`, `3/3 plans complete`, all 3 plans marked `[x]`. No placeholder text. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `NetworkCastTests.cs` | `test_exports.cpp:utinni_test_networkCast` | P/Invoke Cdecl | VERIFIED | DllImport at EntryPoint "utinni_test_networkCast"; export confirmed in test_exports.cpp:174 |
| `test_exports.cpp:utinni_test_networkCast` | `network.cpp:swg::network::cast` | setCastForTest reseat | VERIFIED | setCastForTest called at test_exports.cpp:179; cast pointer reseated to testCastDoubleNaked |
| `utinni_init` | `directx9.cpp:initPresentBlockedEvent` | direct call before createDetours() | VERIFIED | utinni.cpp:135: `directX::initPresentBlockedEvent()` appears 4 lines before `createDetours()` at line 139 |
| `utinni_init` | `directx9.cpp:initDepthTexture` | direct call before createDetours() | VERIFIED | utinni.cpp:136: `directX::initDepthTexture()` appears 3 lines before `createDetours()` at line 139 |
| `FormMainSignallerTests.cs:InitPresentBlockedEvent_*` | `test_exports.cpp:utinni_test_initPresentBlockedEvent` | P/Invoke Cdecl | VERIFIED | DllImport at EntryPoint "utinni_test_initPresentBlockedEvent"; export confirmed at test_exports.cpp:225 |
| `ConfigBufferFreeTests.cs` | `test_exports.cpp:utinni_test_freeConfigBuffer` | P/Invoke Cdecl | VERIFIED | DllImport at EntryPoint "utinni_test_freeConfigBuffer"; export confirmed at test_exports.cpp:94 (no-op, correct) |
| `FindPatternHarnessTests.cs:GetVtbl_WithD3d9Loaded_*` | `test_exports.cpp:utinni_getVtbl` | P/Invoke after LoadLibraryA("d3d9.dll") | VERIFIED (structure) | LoadLibraryA loads d3d9.dll into process space; Utinni_GetVtbl P/Invoke resolves to utinni_getVtbl export at test_exports.cpp:77 |
| `ExportResolutionTests.cs` | `test_exports.cpp:utinni_test_resolveExports` | P/Invoke Cdecl | VERIFIED | DllImport at EntryPoint "utinni_test_resolveExports"; export confirmed at test_exports.cpp:294 |

### Data-Flow Trace (Level 4)

These are test harnesses, not data-rendering components. Level 4 data-flow tracing applies to the test sentinel propagation path:

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `NetworkCastTests.cs` | `result` (int64_t from P/Invoke) | `testCastDoubleNaked` writes 0xDEADBEEFCAFEBABELL through int64_t* ECX param; `Network::cast` returns via `networkId` | Yes — naked-asm test double writes through OUT param; Network::cast reads from networkId not return value | VERIFIED |
| `FormMainSignallerTests.cs:InitPresentBlockedEvent_*` | `afterInit` (UIntPtr) | `directX::initPresentBlockedEvent()` calls `CreateEvent(nullptr, TRUE, FALSE, nullptr)` — Win32 kernel object | Yes — real Win32 HANDLE | VERIFIED |
| `ExportResolutionTests.cs` | `resolved` (int) | `GetProcAddress` on live DLL export table | Yes — real module self-probe | VERIFIED |

### Behavioral Spot-Checks

Step 7b skipped: this phase produces no standalone runnable entry points — the outputs are native C++ source + xUnit tests. Spot-checks would require running `dotnet test` with a built UtinniCore.dll. The CI workflow is the runtime validator.

### Probe Execution

No `scripts/*/tests/probe-*.sh` probes declared or discovered for Phase 02.1. Step 7c skipped.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| STAB-01 | 02.1-01, 02.1-02, 02.1-03 | Close the C-01..C-15 class of bugs correctly — CR-02/03/04/WR-03 are evidence some Phase 02 fixes were partial; Phase 02.1 makes them complete | SATISFIED | All 8 scope findings closed in code with regression tests. ROADMAP Phase 02.1 entry lists [STAB-01]. Assessment.md records all 8 dispositions. |

### Anti-Patterns Found

Anti-pattern scan over Phase 02.1 modified files:

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `ConfigBufferFreeTests.cs` | Class XML doc | PARTIAL-PROOF — explicit documentation that Release/MT test cannot detect delete[] regression | INFO | Intentional and correctly documented. The partial-proof status is honest per Concern A from PLAN-CHECK.md. Phase 6 STAB-03 follow-up tracked. Not a blocker. |
| `FindPatternHarnessTests.cs` | 139-145 | Skip text inlined as a comment (no actual `[Skip]`) | INFO | Intentional pattern: CI is the arbiter. If CI passes (no Skip added), test is stronger. If CI fails, executor adds Skip. No evidence Skip was needed — no Skip commit exists post ca08ff0. |
| `FormMainSignallerTests.cs` | InitPresentBlockedEvent, InitDepthTexture tests | `catch (DllNotFoundException)` with no assertion inside catch | INFO | Established project convention from Phase 02 (GameCallbacksTests deviation #4). Local builds without UtinniCore.dll skip gracefully; CI validates the P/Invoke path. Not a regression risk — CI enforces the real assertion. |
| `test_exports.cpp` | 124 | `__declspec(naked)` with inline `__asm` | INFO | Intentional — this IS the WR-01 fix. The MSVC C3865 workaround via naked+asm is documented in test_exports.cpp lines 104-123 and in assessment.md. Post-commit asm-syntax fix (406ea55) corrected the `0xCAFEBABEh` mixed-syntax error. |

No TBD, FIXME, XXX, or unresolved debt markers found in Phase 02.1 modified files.

### Human Verification Required

#### 1. WR-05 Affirmative GetVtbl Test — CI Outcome

**Test:** Open the GitHub Actions CI run that covers commit `ca08ff0` (or any subsequent run on `master` including that commit). Find the `dotnet test` step output. Locate `GetVtbl_WithD3d9Loaded_ReturnsNonZero` in the test results.

**Expected:** One of:
- The test PASSES with `[PASS] FindPatternHarnessTests.GetVtbl_WithD3d9Loaded_ReturnsNonZero` — the windows-2022 SysWOW64\d3d9.dll contains the vtable pattern. This is the expected outcome given RESEARCH.md confirmed d3d9.dll (1535160 bytes) is present on the runner.
- The test is SKIPPED with the message from the inline comment — acceptable per CONTEXT.md D-05. (No Skip commit was made post-ca08ff0, so if this is the outcome, a follow-up commit should be made to add `[Fact(Skip=...)]`.)
- The test FAILS (pattern not found AND no Skip added) — this is the only unacceptable outcome and would be a gap.

**Why human:** The test was deliberately written without `[Skip]` per plan instructions ("CI is the arbiter"). No post-phase Skip commit was observed in the git log, which is consistent with CI passing the test. But this cannot be confirmed without inspecting a CI log. Programmatic verification of a CI run outcome requires access to the GitHub Actions API.

#### 2. Post-Phase Code Review (/gsd:code-review 02.1)

**Test:** Run `/gsd:code-review 02.1` against the Phase 02.1 code changes (commits 54e0211 through 86c0e81 + 406ea55).

**Expected:** The review reports zero new Critical findings introduced by Phase 02.1. Warnings and Info findings are acceptable. The phase's CONTEXT.md Success Criterion 2 explicitly requires this.

**Why human:** No code-review artifact (02.1-REVIEW.md) exists in the phase directory. The orchestrator did not run a post-execution code review. This is a required success criterion that was not met programmatically. A human must either run the review or confirm it was performed and found clean.

---

## Gaps Summary

No blocking gaps were found. All 8 findings in scope are closed in code with substantive regression tests. All key wiring links are verified. Assessment.md and ROADMAP.md records are complete and honest.

**Two items require human confirmation before the phase can be marked fully PASSED:**

1. **WR-05 CI outcome** — the affirmative GetVtbl test (ca08ff0) was written without `[Skip]`. No post-phase Skip commit was made. CI outcome must be confirmed green (pass or skip-not-fail). This is almost certainly resolved given: the research confirmed windows-2022 SysWOW64\d3d9.dll is present (1535160 bytes), and the phase was declared complete without any red-flag note about this test.

2. **No post-phase code review artifact** — Success Criterion 2 (`/gsd:code-review 02.1` reports no new Critical findings) was not met with a documented artifact. This is a process gap, not a code gap. The phase may have been reviewed informally. A human should confirm or run the review.

---

_Verified: 2026-05-18T23:00:00Z_
_Verifier: Claude (gsd-verifier)_
