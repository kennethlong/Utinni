---
phase: 24-client-entry-point-advertisement-getenginehookpoints
plan: 03
type: execute
wave: 1
depends_on: ["24-01"]
files_modified:
  - UtinniCore/swg/graphics/graphics.cpp
  - UtinniCore/swg/graphics/directx.cpp
  - UtinniCore.Tests/endpoints_tests.cpp
autonomous: true
requirements: [EPA-03]
user_setup: []

must_haves:
  truths:
    - "On the advertised client, graphics::install resolves to the real Graphics::install address so the existing hkInstall detour installs on the correct function and directX11::kickoff() fires naturally (D-05 Approach A)"
    - "directX::detour() (the D3D9 throwaway-device harvest inside hkInstall) no-ops or is gated when reached on a D3D11-only client — it does not crash or harvest a non-existent D3D9 device (Open Q3)"
    - "The SWGEmu D3D9 path through hkInstall is byte-for-byte unchanged (kickoff + directX::detour behave exactly as today)"
  artifacts:
    - path: "UtinniCore/swg/graphics/graphics.cpp"
      provides: "hkInstall confirmed to fire on the resolved graphics::install; kickoff coupling intact"
      contains: "kickoff"
    - path: "UtinniCore/swg/graphics/directx.cpp"
      provides: "directX::detour() D3D11-safety gate (no D3D9 harvest when no D3D9 device)"
  key_links:
    - from: "s_bindings[] graphics::install (bound in 24-02)"
      to: "swg::graphics::install literal"
      via: "resolver overwrites the literal so hkInstall detours the correct function"
      pattern: "graphics::install"
    - from: "UtinniCore/swg/graphics/graphics.cpp hkInstall"
      to: "directX11::kickoff()"
      via: "natural fire after swg::graphics::install() returns on the advertised client"
      pattern: "directX11::kickoff"
---

<objective>
Close EPA-03 via D-05 Approach A: because `graphics::install` is resolved from the advertised table
(bound in Plan 02), the existing `hkInstall()` detour now installs on the CORRECT function on the
D3D11 client, so `directX11::kickoff()` fires naturally — no new trigger site. This plan confirms that
chain end-to-end and hardens the one open risk: that `directX::detour()` (the D3D9 throwaway-device
harvest also called inside `hkInstall`) does NOT crash or misbehave on a D3D11-only client when reached
via the resolved install hook (Open Q3 / D-05).

Purpose: The Phase-19 DX11 overlay never starts on `SwgClient_r.exe` today because `kickoff()` only
runs from the detour on the hardcoded `graphics::install` `0x007548A0`, which is the wrong address on
the advertised client. Resolving that one row fixes the kickoff; the D3D9-harvest safety gate prevents
a different crash from taking its place.

Output: a confirmed/traced `hkInstall → kickoff` chain on the advertised path, a D3D11-safety gate on
`directX::detour()`, and the SWGEmu D3D9 path left byte-for-byte unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-client-entry-point-advertisement-getenginehookpoints/24-CONTEXT.md
@.planning/phases/24-client-entry-point-advertisement-getenginehookpoints/24-RESEARCH.md
@.planning/phases/24-client-entry-point-advertisement-getenginehookpoints/24-PATTERNS.md
@.planning/phases/24-client-entry-point-advertisement-getenginehookpoints/24-01-SUMMARY.md

<interfaces>
<!-- The EPA-03 coupling. kickoff() fires only inside hkInstall, the detour ON swg::graphics::install. -->

From UtinniCore/swg/graphics/graphics.cpp:603-626 (hkInstall — the kickoff site):
```cpp
bool __cdecl hkInstall() {
    bool result = swg::graphics::install();   // indirects through the resolvable literal (resolved in Plan 02)
    directX::detour();                        // D3D9 throwaway-device harvest — Open Q3: confirm D3D11-safe
    directX11::kickoff();                     // the SINGLE owned DX11 kick-off site
    return result;
}
```

From UtinniCore/swg/graphics/graphics.cpp:61 (the resolvable slot — bound in Plan 02):
```cpp
pInstall install = (pInstall)0x007548A0;   // overwritten by name on the advertised client
```

From UtinniCore/swg/graphics/directx11.cpp:25-32 (header note): the D3D9 throwaway-device harvest is
REPLACED by the GetHookPoints swapchain poll in production — confirm directX::detour() no-ops or is
gated on a D3D9 device on a D3D11-only client.

From UtinniCore/swg/graphics/directx11.cpp:219-227 (kickoff — idempotent; subscribes the per-frame
prePresent poll that acquires the advertised DXGI swapchain).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm hkInstall→kickoff fires on the resolved graphics::install + trace directX::detour() D3D11 safety</name>
  <read_first>
    - UtinniCore/swg/graphics/graphics.cpp:589-627 (hkInstall: swg::graphics::install → directX::detour → directX11::kickoff)
    - UtinniCore/swg/graphics/directx.cpp (the directX::detour() body — the D3D9 throwaway-device / dummy-device vtable harvest)
    - UtinniCore/swg/graphics/directx11.cpp:25-32 (the header note: D3D9 harvest replaced by GetHookPoints poll in production)
    - UtinniCore/swg/graphics/directx11.cpp:219-227 (kickoff idempotence + prePresent subscription)
    - UtinniCore/swg/endpoints.cpp (confirm graphics::install is in s_bindings[] from Plan 02)
  </read_first>
  <action>
    Trace and confirm the EPA-03 chain on the advertised path: with `graphics::install` resolved (Plan
    02 binds it), the existing `hkInstall` detour installs on the real `Graphics::install`, so
    `directX11::kickoff()` fires naturally after `swg::graphics::install()` returns. Do NOT add a new
    kickoff trigger site (Approach A, not B — Pitfall 6: a kickoff driven from utinni_init only
    subscribes a poll that cannot fire until prePresent ticks anyway). Verify `kickoff()` remains
    idempotent and the single owned kick-off site.

    Then resolve Open Q3: inspect `directX::detour()` (in `directx.cpp`) — the D3D9 throwaway-device
    harvest. Determine whether, on a D3D11-only client (no D3D9 device), it no-ops, is already gated, or
    would attempt to create/harvest a D3D9 device and crash/hang. If it is NOT safe, add a guard so it
    is a no-op when no D3D9 device/path is present (gate on the render-backend already being DX11, or on
    the D3D9 device-creation returning null) — mirror the `directx11.cpp` null-check/graceful-bail
    discipline; never deref a null device. Record the finding (no-op already / gated now) in the SUMMARY.
    Keep the SWGEmu D3D9 path through `hkInstall` byte-for-byte unchanged: on the absent-export client
    the D3D9 harvest must run exactly as today.
  </action>
  <acceptance_criteria>
    - `graphics.cpp` `hkInstall` still calls `directX11::kickoff()` after `swg::graphics::install()` (the kickoff site is unchanged — Approach A, no new trigger added)
    - No new kickoff call is added to `utinni.cpp`/`utinni_init` (grep utinni.cpp shows no `directX11::kickoff`)
    - `directX::detour()` either is confirmed no-op-on-no-D3D9-device (documented in SUMMARY with file:line evidence) OR has an added gate that prevents a D3D9 harvest when no D3D9 device exists
    - The SWGEmu D3D9 branch of `directX::detour()` is byte-for-byte unchanged (the gate only affects the no-D3D9-device case)
    - MSBuild compiles UtinniCore clean (Release/x86)
  </acceptance_criteria>
  <verify>
    <automated>"%MSBUILD%" Utinni.sln /t:UtinniCore /p:Configuration=Release /p:Platform=x86 /m && grep -q 'directX11::kickoff' UtinniCore/swg/graphics/graphics.cpp && ! grep -q 'directX11::kickoff' UtinniCore/utinni.cpp</automated>
  </verify>
  <done>hkInstall→kickoff fires on the resolved graphics::install (Approach A); directX::detour() confirmed/made D3D11-safe; SWGEmu D3D9 path unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add a regression unit asserting graphics::install is bound (EPA-03 resolver dependency)</name>
  <read_first>
    - UtinniCore.Tests/endpoints_tests.cpp (the existing [endpoints] suite to extend)
    - UtinniCore/swg/endpoints.cpp (s_bindings[] — confirm graphics::install entry exists)
    - UtinniCore/swg/endpoints.h (the resolve()/lookupByName surface)
  </read_first>
  <behavior>
    - [endpoints][epa03]: a synthetic fixture supplying a graphics::install address resolves the graphics::install binding so its slot holds the fixture address — proving the EPA-03 kickoff dependency is wired through the resolver (not hardcoded).
  </behavior>
  <action>
    Add a `[endpoints][epa03]` Catch2 case to `endpoints_tests.cpp` that builds a fixture containing a
    `graphics::install` row with a sentinel address, runs `resolve()` over the binding list, and asserts
    the `graphics::install` slot now holds the fixture sentinel (and that an absent-`graphics::install`
    fixture leaves the slot untouched — the SWGEmu no-op). This locks the EPA-03 dependency: if a future
    change drops `graphics::install` from the bindings, the DX11 kickoff would silently regress; this
    unit catches it headlessly so the maintainer live-smoke (Plan 04) only proves render.
  </action>
  <acceptance_criteria>
    - `endpoints_tests.cpp` contains a TEST_CASE tagged `[endpoints][epa03]` referencing `graphics::install`
    - `UtinniCore.Tests.exe "[endpoints][epa03]"` exits 0
    - The full `[endpoints]` suite still exits 0
  </acceptance_criteria>
  <verify>
    <automated>"%MSBUILD%" Utinni.sln /t:UtinniCore_Tests /p:Configuration=Release /p:Platform=x86 /m && ./UtinniCore.Tests/bin/x86/Release/UtinniCore.Tests.exe "[endpoints][epa03]"</automated>
  </verify>
  <done>A headless unit locks the graphics::install binding that EPA-03's kickoff depends on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolved graphics::install → hkInstall detour | If graphics::install does not resolve, hkInstall detours the wrong address on the advertised client and the DX11 overlay never starts (failure mode, not security). |
| hkInstall → directX::detour() | The D3D9 throwaway-device harvest reached on a D3D11-only client could deref a null D3D9 device. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-08 | Denial (crash) | directX::detour() on D3D11-only client | mitigate | Trace and gate the D3D9 throwaway-device harvest so it no-ops when no D3D9 device exists (Open Q3); null-check the device before any deref (mirror `directx11.cpp` bail). |
| T-24-09 | Denial (silent regress) | graphics::install binding drop | mitigate | `[endpoints][epa03]` unit asserts the binding resolves; the X-macro subset static_assert + coverage gate (24-01/24-02) keep it in lockstep with the .inc. |
| T-24-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero external packages installed this plan. |

No HIGH-severity threats: offline single-user local modding tool, no network/untrusted-input surface.
</threat_model>

<verification>
- MSBuild builds UtinniCore + UtinniCore.Tests clean (Release/x86).
- `hkInstall` still owns the single `directX11::kickoff()` site (Approach A); no kickoff added to utinni_init.
- `directX::detour()` is D3D11-safe (no-op confirmed or gated); SWGEmu D3D9 path unchanged.
- `[endpoints][epa03]` + full `[endpoints]` suite green.
</verification>

<success_criteria>
- DX11 kickoff is driven by the resolved `graphics::install` row, not a hardcoded address (EPA-03).
- The D3D9-harvest cannot crash a D3D11-only client (Open Q3 closed).
- The SWGEmu D3D9 install/kickoff path is byte-for-byte unchanged (D-00).
</success_criteria>

<output>
Create `.planning/phases/24-client-entry-point-advertisement-getenginehookpoints/24-03-SUMMARY.md` when done
</output>
