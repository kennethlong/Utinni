---
phase: 24-client-entry-point-advertisement-getenginehookpoints
plan: 04
type: execute
wave: 2
depends_on: ["24-02", "24-03"]
files_modified:
  - .planning/phases/24-client-entry-point-advertisement-getenginehookpoints/24-04-SUMMARY.md
autonomous: false
requirements: [EPA-01, EPA-02, EPA-03]
user_setup: []

must_haves:
  truths:
    - "dumpbin /exports on SwgClient_r.exe shows GetEngineHookPoints (the advertised contract is present in the build under test)"
    - "Injecting UtinniCore into SwgClient_r.exe no longer crashes in createDetours() — no 0xC0000005 READ target=0x00401000 (criterion 1 / EPA-01/EPA-02)"
    - "The DX11 overlay installs and renders on the advertised SwgClient_r.exe via the resolved graphics::install → kickoff path (criterion 4 / EPA-03 — closes D-08 + D-22)"
    - "The existing SWGEmu Pre-CU D3D9 live-smoke still passes unchanged — overlay renders, mouse + keyboard work as today (criterion 3 / no regression)"
  artifacts:
    - path: ".planning/phases/24-client-entry-point-advertisement-getenginehookpoints/24-04-SUMMARY.md"
      provides: "Recorded live-smoke acceptance results for the 3 maintainer checkpoints"
  key_links:
    - from: "the full headless suite (24-01..24-03)"
      to: "the 3 maintainer live-smokes"
      via: "full suite green is the precondition gate before live injection"
      pattern: "endpoints"
---

<objective>
The irreducible maintainer live-smoke acceptance for Phase 24. Plans 01–03 made everything else green
headlessly; this plan proves only inject + render via three maintainer-only checkpoints that have NO
headless path (AGENTS.md: live-SWG verification is maintainer-only). It closes the deferred Phase-18
(D-08) and Phase-19 (D-22) DX11 live-smokes on the advertised client.

Purpose: Phase-19's DX11 path is CI-green but its live acceptance was deferred because the RVA crash on
`SwgClient_r.exe` blocked injection. This plan verifies the resolver actually fixes that crash on the
real binary, the DX11 overlay renders, and the SWGEmu client did not regress.

Output: a SUMMARY recording all three checkpoint results + the `dumpbin` export confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-client-entry-point-advertisement-getenginehookpoints/24-CONTEXT.md
@.planning/phases/24-client-entry-point-advertisement-getenginehookpoints/24-RESEARCH.md
@.planning/phases/24-client-entry-point-advertisement-getenginehookpoints/24-VALIDATION.md
@.planning/phases/24-client-entry-point-advertisement-getenginehookpoints/24-01-SUMMARY.md
@.planning/phases/24-client-entry-point-advertisement-getenginehookpoints/24-02-SUMMARY.md
@.planning/phases/24-client-entry-point-advertisement-getenginehookpoints/24-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full headless suite + dumpbin export precheck (automation gate before live injection)</name>
  <files>.planning/phases/24-client-entry-point-advertisement-getenginehookpoints/24-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/24-client-entry-point-advertisement-getenginehookpoints/24-RESEARCH.md (Environment Availability — _o.exe must NOT be tested; bar is Debug+Release)
    - .planning/phases/24-client-entry-point-advertisement-getenginehookpoints/24-VALIDATION.md (Manual-Only Verifications table — the exact live-smoke instructions)
  </read_first>
  <action>
    Build the full solution (VS2026 MSBuild `Utinni.sln /p:Configuration=Release /p:Platform=x86`) and
    run the full suite: Catch2 `UtinniCore.Tests` (`[endpoints]` plus the existing native suite) +
    `dotnet test --no-build` (managed lanes). All must be green before any live injection. Then run
    `dumpbin /exports` on the build-under-test `swg-client-v2/stage/SwgClient_r.exe` and confirm
    `GetEngineHookPoints` is present (Runtime State Inventory: the export exists only if the
    `utinni_advertise.cpp` TU was compiled into that build). Do NOT test against `SwgClient_o.exe` (the
    Optimized flavor never exports — pre-existing LNK1281 SAFESEH). Record the dumpbin output line and
    the full-suite result in the SUMMARY. This is the automation gate; the live checkpoints follow only
    if this passes.
  </action>
  <acceptance_criteria>
    - MSBuild `Utinni.sln /p:Configuration=Release /p:Platform=x86` builds with zero errors
    - `UtinniCore.Tests.exe "[endpoints]"` exits 0 and the full native suite passes
    - `dotnet test --no-build` passes the managed lanes
    - `dumpbin /exports stage/SwgClient_r.exe` output contains `GetEngineHookPoints`
  </acceptance_criteria>
  <verify>
    <automated>"%MSBUILD%" Utinni.sln /p:Configuration=Release /p:Platform=x86 /m && ./UtinniCore.Tests/bin/x86/Release/UtinniCore.Tests.exe "[endpoints]" && dotnet test --no-build</automated>
  </verify>
  <done>Full headless suite green; GetEngineHookPoints export confirmed on SwgClient_r.exe; ready for live injection.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 1: No createDetours() crash on SwgClient_r.exe (criterion 1 / EPA-01/EPA-02)</name>
  <action>Maintainer-only live-smoke acceptance — no code changes. Inject UtinniCore into the advertised client and verify per how-to-verify; record the result in the SUMMARY.</action>
  <what-built>
    The `swg::endpoints` resolver runs first in `utinni_init` and overwrites the `swg::*` function-pointer
    literals by name on the advertised client before `createDetours()`. The first detour
    (`config::loadOverrideConfig`, hardcoded `0x00401000` on SWGEmu) now resolves through the advertised
    `GetEngineHookPoints` contract on `SwgClient_r.exe`, so DetourXS reads valid instruction bytes
    instead of crashing.
  </what-built>
  <how-to-verify>
    1. Confirm `dumpbin /exports stage/SwgClient_r.exe` shows `GetEngineHookPoints` (from Task 1).
    2. Launch `swg-client-v2/stage/SwgClient_r.exe` and inject UtinniCore (the usual Launcher path).
    3. Watch the Utinni log: confirm the resolver's resolved/missing/coverage summary line appears and
       that `config::loadOverrideConfig` resolved to a non-null table address.
    4. Confirm NO `VEH FATAL 0xC0000005 ... READ target=0x00401000` (the exact 2026-06-15 crash) and no
       crash in `createDetours()`. Reaching a stable post-detour state = pass (criterion 1 / EPA-01/EPA-02).
  </how-to-verify>
  <resume-signal>Type "approved" if no 0xC0000005 crash and the resolver summary shows config::loadOverrideConfig resolved; otherwise describe the crash target/log lines.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 2: DX11 overlay renders on SwgClient_r.exe (criterion 4 / EPA-03 — closes D-08/D-22)</name>
  <action>Maintainer-only live-smoke acceptance — no code changes. Inject into the D3D11 advertised client and verify the overlay renders + takes input per how-to-verify; record the result in the SUMMARY.</action>
  <what-built>
    EPA-03: `graphics::install` resolves from the advertised table (Plan 02), so the existing `hkInstall`
    detour installs on the correct function on the D3D11 client and `directX11::kickoff()` fires
    naturally, subscribing the per-frame `tryInstall()` poll that latches the advertised DXGI swapchain
    and renders the ImGui DX11 overlay. The D3D9 throwaway-device harvest was confirmed/gated D3D11-safe
    (Plan 03).
  </what-built>
  <how-to-verify>
    1. With UtinniCore injected into `SwgClient_r.exe` (rasterMajor=11 / D3D11 client), confirm the ImGui
       overlay is VISIBLE and renders.
    2. Confirm overlay input maps correctly (render-target space) — mouse/keyboard reach the overlay.
    3. Confirm the log shows `directX11::kickoff` + `directX11::tryInstall: D3D11 overlay installed`
       (the advertised swapchain latched). This closes the Phase-18 D-08 and Phase-19 D-22 live-smokes
       (criterion 4 / EPA-03).
  </how-to-verify>
  <resume-signal>Type "approved" if the DX11 overlay renders + takes input on SwgClient_r.exe; otherwise describe what failed (no overlay / no input / kickoff log absent).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 3: SWGEmu D3D9 live-smoke unchanged — no regression (criterion 3 / EPA-02 / D-00)</name>
  <action>Maintainer-only live-smoke acceptance — no code changes. Inject into the existing SWGEmu Pre-CU client and verify the absent-export no-op path leaves the overlay + input unchanged; record the result in the SUMMARY.</action>
  <what-built>
    The dual-path no-regression guarantee (D-00): on the SWGEmu Pre-CU client the `GetEngineHookPoints`
    export is absent, so the resolver is a strict no-op and every `swg::*` literal keeps its hardcoded
    RVA value. The existing SWGEmu D3D9 path must behave byte-for-byte as before this phase.
  </what-built>
  <how-to-verify>
    1. Inject UtinniCore into the existing SWGEmu Pre-CU (D3D9) client as you do today.
    2. Confirm the log shows the resolver's "no GetEngineHookPoints export — RVA path (SWGEmu)" info line
       (the strict no-op branch).
    3. Confirm the ImGui overlay renders and mouse + keyboard work exactly as before — no regression
       (criterion 3 / EPA-02). A TJT-driven scene change landing "naked but in world" is the documented
       baseline, NOT a regression.
  </how-to-verify>
  <resume-signal>Type "approved" if the SWGEmu D3D9 overlay + input are unchanged from today; otherwise describe the regression.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build artifact → live inject | The export exists only if swg-client-v2 was built with the `utinni_advertise.cpp` TU; an export-less rebuild silently falls to no-overlay. The `_o` flavor never exports. |
| advertised path vs SWGEmu path | The resolver must not regress the working SWGEmu D3D9 client — the absent-export no-op is the safety guarantee. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-10 | Denial (no overlay) | missing export on build-under-test | mitigate | `dumpbin /exports` precheck (Task 1) confirms `GetEngineHookPoints` before injection; do NOT test `_o.exe` (pre-existing SAFESEH, never exports). |
| T-24-11 | Denial (regression) | SWGEmu D3D9 path | mitigate | Dedicated no-regression checkpoint asserts the absent-export no-op leaves the SWGEmu overlay + input unchanged (criterion 3 / D-00). |
| T-24-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero external packages; this plan is live-smoke verification only. |

No HIGH-severity threats: offline single-user local modding tool, no network/untrusted-input surface.
</threat_model>

<verification>
- Full headless suite green + `GetEngineHookPoints` export confirmed (Task 1 automation gate).
- Criterion 1: no `0xC0000005 READ target=0x00401000` on `SwgClient_r.exe` (checkpoint 1).
- Criterion 4: DX11 overlay renders + takes input on the advertised client (checkpoint 2, closes D-08/D-22).
- Criterion 3: SWGEmu D3D9 live-smoke unchanged (checkpoint 3).
</verification>

<success_criteria>
- All three maintainer live-smokes pass; results recorded in the SUMMARY.
- The advertised client injects, detours, and renders the DX11 overlay (EPA-01/EPA-02/EPA-03 live-proven).
- The SWGEmu client is verified non-regressed (EPA-02 / D-00).
</success_criteria>

<output>
Create `.planning/phases/24-client-entry-point-advertisement-getenginehookpoints/24-04-SUMMARY.md` when done
</output>
