---
phase: 15-wave-2-editors-worldsnapshot-particle-presentation-residuals
plan: 21
type: execute
wave: 9
depends_on: ["15-19", "15-20"]
files_modified:
  - "D:/Code/Utinni/bin/Release/"
  - "D:/Code/Utinni/.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-SMOKE.md"
autonomous: false
gap_closure: true
requirements: [PROD-W2-PRT, RESID-03, RESID-04, PROD-W2-WS]

must_haves:
  truths:
    - "The deployable injection build at `D:/Code/Utinni/bin/Release/` is rebuilt with the 15-19 (B6) + 15-20 (D-ii) fixes and content-verified to carry them"
    - "B6 is re-verified: the Particle `Preview in client` honest no-hook/no-client candor is reachable (hover or click), not swallowed by a disabled control"
    - "D-ii is re-verified: a raw-`Open…` `.stf` loose-override save lands at `loose\string\en\ui_auc.stf` (subpath preserved), not flat `loose\ui_auc.stf`"
    - "The Maintainer Sign-Off block in `15-SMOKE.md` is updated to the final disposition that closes (or re-defers) Phase 15"
  artifacts:
    - path: "D:/Code/Utinni/.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-SMOKE.md"
      provides: "The B6 + D-ii re-verify results + the final signed Maintainer Sign-Off (the phase gate)"
      contains: "Maintainer Sign-Off"
  key_links:
    - from: "15-19 + 15-20 fixes"
      to: "the reassembled bin/Release build + the live/headless re-verify"
      via: "rebuild + content-verify, then re-verify B6 (Preview candor) + D-ii (loose subpath on disk)"
      pattern: "Release"
---

<objective>
Reassemble the deployable injection build with the round-3 fixes (15-19 B6, 15-20 D-ii), re-verify both, and
take the final Phase-15 sign-off. Unlike the prior round, BOTH defects are verifiable WITHOUT game-knowledge
or the in-game chat — so the re-verify can be Claude-driven via `windows-mcp` (the loop validated in the
15-18 smoke) plus a headless on-disk check, with a short final maintainer sign-off.

Purpose: B6 + D-ii are the only open defects from the 15-18 re-smoke; PROD-W2-PRT and RESID-03 cannot be
marked Validated until they are confirmed fixed against a deployed build. This plan closes the round-3 gate.

Output: a content-verified `bin/Release/` carrying both fixes, B6 + D-ii re-verify results recorded in
`15-SMOKE.md`, and a signed final Maintainer Sign-Off disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-SMOKE.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-17-SUMMARY.md

<interfaces>
<!-- Build env: VS2026 (Dev18) MSBuild on the D: drive:
  "D:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe"
  (the plan's %ProgramFiles% would be C:\ and is MISSING). dotnet build does NOT work for WinForms. -->
<!-- 15-17 is the reassembly precedent (full Release gate + bin/Release content-verify via reflection-only
  enumeration + byte-string grep). Mirror it: rebuild Utinni.sln + TheJawaToolbox.sln, redeploy bin/Release. -->
<!-- windows-mcp re-verify (per the updated reference memory): editor host window = "Utinni" (display 0,
  region 0,0,1920,1080, image coords 1:1); UIA labels do NOT surface so drive by coordinate off screenshots;
  Claude Code bypass-permissions ON avoids focus theft. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild + reassemble bin/Release with the 15-19/15-20 fixes, content-verify</name>
  <files>D:/Code/Utinni/bin/Release/</files>
  <read_first>
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-17-SUMMARY.md" (the reassembly + content-verify recipe to mirror)
  </read_first>
  <action>
    Rebuild `Utinni.sln` + `TheJawaToolbox.sln` Release|x86 (D: MSBuild path). Run the full automated gate
    (UtinniCoreDotNet.Tests incl. the new `LogicalAssetPath` facts; Utinni.Cli.Tests; native + [resid04]) —
    zero NEW regression beyond the one known pre-existing D3D9 harness failure. Reassemble `bin/Release/`
    exactly as 15-17 did (keep `netstandard.dll` + `utinni-cli.exe` closure). Content-verify (anti-stale): the
    deployed `TheJawaToolboxDotNet.dll` carries the 15-19 + 15-20 changes and the deployed
    `UtinniCoreDotNet.PathContainment.dll` (or the merged PathContainment surface) defines `LogicalAssetPath`.
    `git checkout -- UtinniCoreDotNet/Generated/UtinniCore.cs` after building.
  </action>
  <verify>
    <automated>cmd /c "\"D:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe\" \"D:/Code/Utinni/Utinni.sln\" /p:Configuration=Release /p:Platform=x86 /v:minimal && dotnet test \"D:/Code/Utinni/UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj\" --no-build -c Release"</automated>
  </verify>
  <acceptance_criteria>
    - Both solutions Release|x86 build (MSBuild exit 0); suites green except the one known pre-existing D3D9 harness failure.
    - `bin/Release/` reassembled; `netstandard.dll` + `utinni-cli.exe` closure still present.
    - Deployed binaries content-verified to carry `LogicalAssetPath` + the 15-19 Preview-candor change.
    - `Generated/UtinniCore.cs` reverted.
  </acceptance_criteria>
  <done>A content-verified bin/Release carries both round-3 fixes, ready for re-verify.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Re-verify B6 + D-ii (Claude-drivable via windows-mcp) + final Maintainer Sign-Off</name>
  <what-built>
    15-19 made the Particle Preview honest no-hook/no-client candor reachable; 15-20 made raw-dialog
    loose-override saves preserve the logical subpath. Both are in the reassembled bin/Release (Task 1).
  </what-built>
  <how-to-verify>
    Inject `D:/Code/Utinni/bin/Release/` via `Launcher.exe`. Both re-verifies can be Claude-driven via
    `windows-mcp` (bypass-permissions ON) — no in-game chat / game-knowledge needed:
    1. **B6:** open the Particle editor on a `.prt` with SWG running; hover AND click `Preview in client` →
       confirm the honest "Live preview isn't wired this build — edits show on the next scene change or relog."
       copy is now VISIBLE (no dead, explanation-less disabled button). No live retrigger is attempted.
    2. **D-ii:** open `string\en\ui_auc.stf` via the raw `Open…` dialog, edit a cell, Save as loose override →
       headlessly confirm the file lands at `D:\SWGEmu-Client\SWGEmu\loose\string\en\ui_auc.stf` (subpath
       preserved), NOT flat `loose\ui_auc.stf`. (Optionally also confirm the TRE-Browser hand-off still lands
       correctly.)
    Record both results in `15-SMOKE.md` (B6 + D-ii rows → PASS/defect), then update the **Maintainer Sign-Off**
    block to the final disposition. NOTE: RESID-03 live render-on-reload stays DEFERRED (loose searchPath
    disabled — unchanged by this round); the deferred residuals (D-render, fullscreen mouse-mapping, codec
    hard-abort) remain tracked todos.
  </how-to-verify>
  <resume-signal>Type "approved" (or "approved-with-deferred-residual") once B6 + D-ii re-verify PASS and 15-SMOKE.md is signed, or describe any remaining defect (routes to another `--gaps` round). Only on approval does the orchestrator run phase verification/completion.</resume-signal>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-15-21-01 | Repudiation (premature closure) | Marking PROD-W2-PRT/RESID-03 Validated without confirming the B6/D-ii fixes live | mitigate | autonomous:false; gated on the signed disposition; re-verify against the content-verified build |
| T-15-21-02 | Integrity | Injecting a stale build | mitigate | Task 1 content-verifies the deployed PEs carry both fixes before the re-verify |
| T-15-21-SC | Tampering | npm/pip/cargo installs | accept | Zero packages — rebuild + re-verify + doc updates |
</threat_model>

<verification>
- Both solutions Release|x86 MSBuild exit 0; suites green (except the one known pre-existing D3D9 harness failure)
- bin/Release reassembled + content-verified to carry `LogicalAssetPath` + the B6 change
- LIVE/headless (the gate): B6 candor reachable; D-ii loose override lands at `loose\string\en\ui_auc.stf`
- `15-SMOKE.md` B6 + D-ii rows recorded; Maintainer Sign-Off signed with a final disposition
- No phase.complete / requirement-Validated marking until the signed disposition
</verification>

<success_criteria>
The reassembled, content-verified build carries the 15-19 (B6) + 15-20 (D-ii) fixes; both are re-verified
(B6 candor reachable, D-ii loose-override subpath preserved); `15-SMOKE.md` is updated and the Maintainer
Sign-Off is signed. Phase 15 closes on an approved disposition (RESID-03 live render-on-reload remains a
tracked deferred residual gated on the disabled loose searchPath); this plan does not auto-advance.
</success_criteria>

<output>
Create `.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-21-SUMMARY.md` when done.
</output>
