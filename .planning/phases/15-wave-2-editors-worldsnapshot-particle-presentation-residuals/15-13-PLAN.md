---
phase: 15-wave-2-editors-worldsnapshot-particle-presentation-residuals
plan: 13
type: execute
wave: 5
depends_on: []
files_modified:
  - "D:/Code/Utinni/UtinniCoreDotNet/UI/Controls/PanelGame.cs"
autonomous: true
gap_closure: true
requirements: [RESID-04]

must_haves:
  truths:
    - "When SWG performs a WINDOW-LEVEL fullscreen (it restyles/repositions its own HWND without any new SetCooperativeLevel/EXCLUSIVE request), Utinni re-asserts the owned-popup embed: SWG's frame is re-stripped, the FormMain owner relationship is re-set, the window is repositioned over the PanelGame client rect, and focus/input are restored — the embed no longer detaches with a black right gutter and editor chrome falling behind"
    - "The re-assert is purely window-side (SetWindowLong + SetWindowPos); Utinni NEVER calls IDirect3DDevice9::Reset on SWG's device (D-13) — the existing [resid04] no-Reset Catch2 grep gate over directx9.cpp/direct_input.cpp/PanelGame.cs still passes (Reset-invocation count stays 0)"
    - "The owned-popup model is preserved across the re-assert: SWG stays WS_POPUP (never WS_CHILD, which would break DirectInput's top-level-HWND requirement); Z-order uses HWND_TOP + SWP_NOACTIVATE; RT-space imgui mouse/DisplaySize mapping continues to hold off the windowed COPY Present self-stretch"
  artifacts:
    - path: "D:/Code/Utinni/UtinniCoreDotNet/UI/Controls/PanelGame.cs"
      provides: "A window-style watchdog that detects SWG's window-level fullscreen restyle and re-asserts the reparent/pin + restores focus, window-side only"
      contains: "ReassertEmbed"
  key_links:
    - from: "PanelGame embed watchdog"
      to: "ReparentSwgWindow / RepositionSwgWindow"
      via: "on detecting SWG dropped WS_POPUP-owned-embed state, re-run the frame-strip + owner-set + reposition path"
      pattern: "ReassertEmbed"
    - from: "the re-assert path"
      to: "NO IDirect3DDevice9::Reset (D-13)"
      via: "pure SetWindowLong/SetWindowPos — the [resid04] grep gate counts Reset invocations == 0 across PanelGame.cs"
      pattern: "SetWindowPos"
---

<objective>
Close the BLOCKING RESID-04 live residual (15-SMOKE Checklist C3): SWG's windowed→fullscreen here is a WINDOW-LEVEL fullscreen, NOT a D3D9 exclusive switch (the 2026-06-13 smoke captured ZERO new `SetCooperativeLevel`/EXCLUSIVE request across the transition, so the D-12 DirectInput suppress correctly never engages, and D-13 held — no Utinni device Reset). SWG restyles/repositions its OWN HWND on fullscreen; nothing re-asserts Utinni's owned-popup reparent/pin, so the embed detaches (editor chrome falls behind, black right gutter), then focus drops to `0x0` (`WM_ACTIVATE INACTIVE` / `WM_KILLFOCUS gained-focus-to=0x00000000`), input dies, and the session is unrecoverable.

The current `PanelGame` reparent (verified `PanelGame.cs:172-280`) fires ONCE: the `reparentPollTimer` stops itself after the first successful `ReparentSwgWindow`, and thereafter only `Resize`/`LocationChanged` reposition the window — neither re-strips SWG's frame nor re-sets the owner nor restores focus after SWG re-styles its own HWND for fullscreen. So a window-level fullscreen restyle is never countered.

Purpose: this is THE live RESID-04 residual the phase was meant to close, and it gates phase closure (the C3 fullscreen re-verify is part of the maintainer's live re-smoke). HARD CONSTRAINT (LOCKED, from `feedback_d3d9_reset_third_party` + 15-05/D-13): do NOT call `pDevice->Reset` on SWG's device (it owns untracked default-pool resources → `D3DERR_INVALIDCALL` → DEVICELOST → fatal crash). The fix is window-side only.

Output: a window-style watchdog in `PanelGame.cs` that detects SWG dropping its owned-popup-embed state (style/owner/extent change from a window-level fullscreen) and re-asserts the embed — re-strip frame, re-set owner, reposition over the PanelGame rect (HWND_TOP + SWP_NOACTIVATE), and restore focus/input — purely via `SetWindowLong`/`SetWindowPos`, never a device Reset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-SMOKE.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-PATTERNS.md
@.planning/todos/pending/swg-window-resize-fullscreen-edge-cases.md

<interfaces>
<!-- The current ONE-SHOT reparent (verified PanelGame.cs):
  reparentPollTimer (100ms) -> ReparentPollTimer_Tick: on first GetSwgHwnd() != 0, calls
    ReparentSwgWindow(swgHwnd, ownerHwnd) THEN STOPS THE TIMER (swgReparented=true, line 224).
  ReparentSwgWindow (L206-232): strips frame styles -> u = (u & ~frameMask) | WS_POPUP;
    SetWindowLong(GWL_STYLE); SetWindowLong(GWLP_HWNDPARENT, ownerHwnd); RepositionSwgWindow().
  RepositionSwgWindow (L234-280): SetWindowPos(swgHwnd, HWND_TOP(IntPtr.Zero), screenOrigin, ClientSize,
    SWP_FRAMECHANGED | SWP_SHOWWINDOW | SWP_NOACTIVATE).  <-- window-side only, NO Reset (D-13).
  Resize += RepositionSwgWindow; OwnerForm_LocationChanged -> RepositionSwgWindow.
  ** Nothing re-runs ReparentSwgWindow after the first time, so a window-level fullscreen RESTYLE
     by SWG (which clears WS_POPUP / re-adds frame / changes owner) is never countered. ** -->

<!-- Available P/Invoke primitives (verified UtinniCoreDotNet/Utility/Native.cs):
  Native.GetSwgHwnd() -> IntPtr (0 until SWG's window exists)
  Native.GetWindowLong(hWnd, GWL_STYLE | GWLP_HWNDPARENT) -> int
  Native.SetWindowLong(hWnd, nIndex, dwNewLong) -> int
  Native.SetWindowPos(hWnd, hWndInsertAfter, x, y, cx, cy, uFlags) -> bool
  Constants: GWL_STYLE=-16, GWLP_HWNDPARENT=-8, WS_POPUP=0x80000000,
    WS_CAPTION/WS_THICKFRAME/WS_MINIMIZEBOX/WS_MAXIMIZEBOX/WS_SYSMENU/WS_BORDER/WS_DLGFRAME (frameMask),
    SWP_FRAMECHANGED=0x20, SWP_SHOWWINDOW=0x40, SWP_NOACTIVATE=0x10.
  ownerFormCached (FormMain) is cached at HandleCreated; ownerFormCached.Handle is the owner HWND.
  To restore focus window-side: bring SWG to HWND_TOP with SWP_NOACTIVATE (existing pattern), then
    re-activate the host group via the FormMain owner (SetForegroundWindow / Activate on ownerFormCached
    is the managed equivalent) — keep focus on the host, SWG visually on top, exactly as the original
    reparent comment (PanelGame.cs:250-259) describes. -->

<!-- The [resid04] no-Reset grep gate (verified 15-05): UtinniCore.Tests NoDeviceResetTests.cpp
  comment-strips directx9.cpp + direct_input.cpp + PanelGame.cs and asserts the count of `->Reset(` /
  `.Reset(` invocations == 0 (SWG-own hkReset free-function pass-through excluded). Any new Reset call
  in PanelGame.cs would fail it. The re-assert MUST be SetWindowLong/SetWindowPos only — and must NOT
  introduce the token `.Reset(` (e.g. avoid naming a method `ResetEmbed` or calling someTimer.Reset()). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Watchdog that re-asserts the owned-popup embed on SWG's window-level fullscreen restyle</name>
  <files>D:/Code/Utinni/UtinniCoreDotNet/UI/Controls/PanelGame.cs</files>
  <read_first>
    - "D:/Code/Utinni/UtinniCoreDotNet/UI/Controls/PanelGame.cs" (the entire reparent/reposition machinery — ReparentSwgWindow L206-232, RepositionSwgWindow L234-280, the one-shot reparentPollTimer, the Resize/LocationChanged reposition hooks; AND the L115-140 Phase-B-bis comment recording WHY Reset is fatal)
    - "D:/Code/Utinni/UtinniCoreDotNet/Utility/Native.cs" (lines 47-133 — the GWL_*/WS_*/SWP_* constants + GetWindowLong/SetWindowLong/SetWindowPos/GetSwgHwnd P/Invokes; the exact frameMask used in ReparentSwgWindow)
    - "D:/Code/Utinni/UtinniCore/swg/misc/direct_input.cpp" (lines 82-152, 275-288 — confirms the D-12 suppress only redirects EXCLUSIVE; a window-level fullscreen issues NO EXCLUSIVE request, so this native path does NOT fire for C3 — the fix must be window-side)
    - ".planning/todos/pending/swg-window-resize-fullscreen-edge-cases.md" (the RESID-04 matrix + the maintainer's recovery hint: dropping resolution off fullscreen re-establishes the embed)
  </read_first>
  <action>
    Edit `UtinniCoreDotNet/UI/Controls/PanelGame.cs` to add a window-style WATCHDOG that re-asserts the embed when SWG performs a window-level fullscreen restyle. Concretely:

    1. Add a low-frequency watchdog `Timer` (e.g. `embedWatchdogTimer`, Interval ~250ms) started right after the first successful reparent (in `ReparentSwgWindow`, after `swgReparented = true`) and stopped in `PanelGame_Disposed` alongside `reparentPollTimer`. Do NOT reuse `reparentPollTimer` (it self-stops by contract). Name nothing `*Reset*` and call no `.Reset(` on any object (the [resid04] grep gate forbids the token in this file).

    2. In the watchdog tick, when `swgReparented` and `GetSwgHwnd() != 0` and the host is not minimized, read SWG's current `GWL_STYLE` and `GWLP_HWNDPARENT`. Detect the DETACHED/RESTYLED condition: SWG has CLEARED `WS_POPUP` OR re-added any frame-mask bit (`WS_CAPTION | WS_THICKFRAME | WS_MINIMIZEBOX | WS_MAXIMIZEBOX | WS_SYSMENU | WS_BORDER | WS_DLGFRAME`) OR its owner (`GWLP_HWNDPARENT`) no longer equals `ownerFormCached.Handle`. Any of those means SWG restyled itself for a window-level fullscreen and the embed has popped out.

    3. On detecting the detached condition, call a new private `ReassertEmbed()` method that re-runs the embed assertion WITHOUT re-creating timers: re-apply the frame-strip (`u = (u & ~frameMask) | WS_POPUP; SetWindowLong(GWL_STYLE, …)`), re-set the owner (`SetWindowLong(GWLP_HWNDPARENT, ownerHwnd)`), then call `RepositionSwgWindow()` (which already does the HWND_TOP + SWP_FRAMECHANGED + SWP_NOACTIVATE window-side reposition). Restore focus/input window-side ONLY: after the reposition, re-activate the host group via `ownerFormCached.Activate()` (the managed equivalent of bringing the owned group forward — keeps focus on the host, SWG visually on top, exactly as the L250-259 reparent comment describes). Extract the shared frame-strip + owner-set logic so `ReparentSwgWindow` and `ReassertEmbed` do not duplicate it (one private helper that takes the swg+owner HWNDs), to keep a single source of the embed-assert behavior.

    4. Add a rate-limited diagnostic `Log.Info` line when a re-assert fires (cap like the existing `s_repositionLogCount` to avoid spam) recording the detected old style and the re-asserted style, so the live re-smoke can confirm the watchdog engaged across the fullscreen transition.

    HARD CONSTRAINTS (read the L115-140 comment): NEVER call `IDirect3DDevice9::Reset` — the re-assert is `SetWindowLong`/`SetWindowPos` only. Keep the owned-popup model — SWG stays `WS_POPUP`, NEVER `WS_CHILD`. Z-order via `HWND_TOP (IntPtr.Zero)` + `SWP_NOACTIVATE` (reuse `RepositionSwgWindow`). The imgui RT-space mouse/DisplaySize mapping rides on the windowed COPY Present self-stretch and needs no change here. After building, `git checkout -- UtinniCoreDotNet/Generated/UtinniCore.cs`.
  </action>
  <verify>
    <automated>cmd /c "\"%ProgramFiles%\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe\" \"D:/Code/Utinni/Utinni.sln\" /p:Configuration=Release /p:Platform=x86 /v:minimal && \"D:/Code/Utinni/UtinniCore.Tests/bin/Release/UtinniCore.Tests.exe\" [resid04]"</automated>
  </verify>
  <acceptance_criteria>
    - `PanelGame.cs` adds a watchdog `Timer` (distinct from `reparentPollTimer`) started after the first reparent and stopped in `PanelGame_Disposed`; its tick reads `GWL_STYLE` + `GWLP_HWNDPARENT` and detects WS_POPUP cleared / frame-mask re-added / owner changed (read the tick body).
    - A `ReassertEmbed()` method re-applies the frame-strip (`| WS_POPUP`), re-sets `GWLP_HWNDPARENT` to the FormMain owner, calls `RepositionSwgWindow()`, and re-activates the host group — using ONLY `SetWindowLong`/`SetWindowPos` (+ `ownerFormCached.Activate()`); no `WS_CHILD` anywhere in the file (grep `WS_CHILD` returns no match).
    - The frame-strip + owner-set logic is shared between `ReparentSwgWindow` and `ReassertEmbed` via a single private helper (no duplicated frameMask literal divergence).
    - The native `[resid04]` no-Reset grep gate passes: `UtinniCore.Tests.exe [resid04]` exits 0 (8 assertions / 1 case), i.e. the Reset-invocation count across directx9.cpp/direct_input.cpp/PanelGame.cs stays 0 — the new code introduces no `.Reset(`/`->Reset(` token.
    - `Utinni.sln` Release|x86 builds (MSBuild exit 0).
    - `Generated/UtinniCore.cs` reverted (git status clean for that file).
  </acceptance_criteria>
  <done>A window-side watchdog detects SWG's window-level fullscreen restyle and re-asserts the owned-popup embed (frame-strip + owner-set + reposition + host re-activate), purely via SetWindowLong/SetWindowPos with no device Reset (the [resid04] gate stays green) and no WS_CHILD; build green; Generated churn reverted. Live C3 re-verify lands in the 15-18 re-smoke against the 15-17 reassembled build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SWG-owned HWND ↔ Utinni embed/owner relationship | SWG restyles its own top-level window for fullscreen; Utinni must re-assert the owned-popup embed from outside without touching the D3D9 device |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-13-01 | Denial of Service (availability) | Window-level fullscreen detaches the embed → focus to 0x0 → input dead → unrecoverable session | mitigate | THIS is the gap: a watchdog detects the restyle and re-asserts the embed (frame-strip + owner-set + reposition + host re-activate), restoring input/focus (15-SMOKE C3) |
| T-15-13-02 | Denial of Service (crash) | Calling IDirect3DDevice9::Reset on SWG's device while reconciling the resize | accept-by-design / mitigate | HARD-CONSTRAINED out: the re-assert is window-side only (SetWindowLong/SetWindowPos); the [resid04] grep gate enforces zero Reset invocations in this file (D-13, feedback_d3d9_reset_third_party) |
| T-15-13-SC | Tampering | npm/pip/cargo installs | accept | This plan installs ZERO packages (C# edit + native Catch2 gate re-run) |
</threat_model>

<verification>
- `UtinniCore.Tests.exe [resid04]` exits 0 (no-Reset gate green; count == 0 across PanelGame.cs)
- `Utinni.sln` Release|x86 MSBuild exit 0
- Read confirms: watchdog detects WS_POPUP-cleared / frame-readded / owner-changed; ReassertEmbed re-strips+re-owns+repositions+re-activates window-side; no WS_CHILD; no Reset token
- `Generated/UtinniCore.cs` reverted
- LIVE (gated to 15-18): Checklist C3 windowed→fullscreen — embed survives (no detach/black-gutter), input/focus recover, NO crash, NO Utinni device Reset across the matrix (C15)
</verification>

<success_criteria>
SWG's window-level fullscreen restyle no longer detaches the embed: a window-style watchdog re-asserts the owned-popup reparent/pin and restores focus/input, purely window-side (SetWindowLong/SetWindowPos), never calling IDirect3DDevice9::Reset (the [resid04] gate stays green), keeping WS_POPUP and HWND_TOP + SWP_NOACTIVATE Z-order. RESID-04's C3 edge is closed pending the 15-18 live re-verify.
</success_criteria>

<output>
Create `.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-13-SUMMARY.md` when done.
</output>
