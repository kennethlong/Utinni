---
phase: 15-wave-2-editors-worldsnapshot-particle-presentation-residuals
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - "D:/Code/Utinni/UtinniCore/swg/scene/particle_preview.h"
  - "D:/Code/Utinni/UtinniCore/swg/scene/particle_preview.cpp"
  - "D:/Code/Utinni/.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-PARTICLE-PREVIEW-HOOK.md"
autonomous: true
requirements: [PROD-W2-PRT]

must_haves:
  truths:
    - "The native effect/particle manager re-trigger path is identified (ParticleManager vs ClientEffectManager) and documented"
    - "A CppSharp-exposable native export to hot-retrigger live scene effect instances exists, OR the absence is documented with a fallback"
    - "The retrigger marshals once per save/reload (not per frame) and is heap-free on any hot path"
  artifacts:
    - path: "D:/Code/Utinni/UtinniCore/swg/scene/particle_preview.cpp"
      provides: "Native hot-retrigger export (or a documented no-op stub if the manager hook is unreachable this phase)"
      min_lines: 20
    - path: "D:/Code/Utinni/.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-PARTICLE-PREVIEW-HOOK.md"
      provides: "Spike decision: which manager, the reachable export, heap-free marshalling plan, or documented absence + fallback"
      contains: "ParticleManager"
  key_links:
    - from: "particle_preview.cpp retrigger export"
      to: "native effect/particle manager"
      via: "re-trigger live scene instances"
      pattern: "[Rr]etrigger|[Rr]eTrigger"
---

<objective>
EARLY SPIKE for the D-09 live in-client preview = hot-retrigger of loaded effect instances. This is the ONE genuinely-greenfield, No-Analog item (PATTERNS.md §No Analog Found) and the maintainer-flagged highest-risk open question (RESEARCH Open Q2 / A3). It may require a NEW CppSharp-exposed `UtinniCore` export.

Purpose: De-risk the Particle editor (15-06) and the Wave-4 live smoke (15-08) by resolving EARLY whether a reachable native re-trigger path exists. If it does, ship the native export now (isolated file, no UI dependency). If it does NOT, document the absence + the degraded-badge fallback so 15-06 wires the honest tier-(b) candor and 15-08 doesn't block on a missing hook. Either outcome unblocks the editor.

Output: A native export (or documented stub) + a spike decision doc. The actual in-client retrigger is confirmed live in 15-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-CONTEXT.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-RESEARCH.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-PATTERNS.md

<interfaces>
<!-- Game-thread marshalling seam the retrigger must use (heap-free; marshal once per save/reload, NOT per frame) -->
GroundSceneCallbacks.AddUpdateLoopCall(...)   // managed seam
GameCallbacks.AddMainLoopCall(...)             // managed seam
// project_rh_snapshot_no_heap_alloc: per-frame vector::reserve() in dispatch fragmented SWG's
// allocator and crashed scene change at 0x0051fb0a. Use stack-allocated fixed-size snapshots; keep
// the hot path allocation-free.

<!-- swg-client-v2 references (READ-ONLY spec; copy NO code/identifiers) -->
// ParticleManager vs ClientEffectManager — identify which holds live scene instances + exposes a
// reachable re-trigger / re-create-from-template entry.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Identify the manager re-trigger path + write the spike decision doc</name>
  <read_first>
    - "D:/Code/swg-client-v2/.../clientParticle/" (READ-ONLY — ParticleManager / particle-effect lifecycle; identify the create/retrigger-from-template entry)
    - "D:/Code/swg-client-v2/.../clientGame/.../clientEffect/" (READ-ONLY — ClientEffectManager lifecycle; the other candidate manager)
    - "D:/Code/Utinni/UtinniCore/swg/scene/world_snapshot.h" (an existing CppSharp-exposed scene-side native header — the export-shape analog for particle_preview.h)
    - "D:/Code/Utinni/UtinniCore/swg/scene/" (existing scene hooks — how Utinni reaches scene-side managers under injection)
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-RESEARCH.md" (Open Q2; A3; Pitfall 7 heap-free hot path; Runtime State Inventory — live runtime instances)
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-PATTERNS.md" (§No Analog Found — the greenfield manager hook)
  </read_first>
  <action>
    Investigate which native manager (`ParticleManager` vs `ClientEffectManager`, per RESEARCH Open Q2) holds the live in-scene effect instances and exposes a reachable entry to re-trigger / re-create an effect from its (reloaded) template. Determine whether Utinni can reach that entry under injection (via an existing scene hook or a small new RVA/export).

    Write `15-PARTICLE-PREVIEW-HOOK.md` recording: (1) the chosen manager + rationale, (2) the exact reachable entry (signature / RVA / call path) OR a clear "no reachable hook this phase" finding, (3) the heap-free, marshal-once-per-save/reload plan (NOT per frame — `project_rh_snapshot_no_heap_alloc`), (4) the fallback if no hook exists: the Particle editor's `Preview in client` button stays state-encoded-disabled and the reload badge degrades to the locked tier-(b) candor `Reloads on next scene change or relog.` (so 15-06 + 15-07 wire honestly).
  </action>
  <verify>
    <automated>cmd /c "if exist \"D:/Code/Utinni/.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-PARTICLE-PREVIEW-HOOK.md\" (findstr /C:ParticleManager /C:ClientEffectManager \"D:/Code/Utinni/.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-PARTICLE-PREVIEW-HOOK.md\" >nul && echo DOC_OK) else (echo MISSING && exit 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `15-PARTICLE-PREVIEW-HOOK.md` exists and names the chosen manager (`ParticleManager` or `ClientEffectManager`) with rationale.
    - The doc states EITHER a reachable retrigger entry (signature/RVA/call path) OR an explicit "no reachable hook this phase" finding with the degraded-badge fallback.
    - The doc states the marshal-once-per-save/reload heap-free plan and cites `project_rh_snapshot_no_heap_alloc`.
  </acceptance_criteria>
  <done>The manager + reachability decision is documented with a heap-free marshalling plan and an honest fallback for the no-hook case.</done>
</task>

<task type="auto">
  <name>Task 2: Native particle_preview export (or documented no-op stub) — build green, no UtinniCore.cs commit</name>
  <read_first>
    - "D:/Code/Utinni/UtinniCore/swg/scene/world_snapshot.h" + a sibling scene .cpp (the UTINNI_API export-shape + CppSharp-exposure analog)
    - "D:/Code/Utinni/.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-PARTICLE-PREVIEW-HOOK.md" (the Task-1 decision this task implements)
    - "D:/Code/Utinni/UtinniCore/UtinniCore.vcxproj" (to register the new source files in the right config groups)
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-RESEARCH.md" (Anti-Pattern: committing Generated/UtinniCore.cs; Pitfall 7 heap-free)
  </read_first>
  <action>
    Create `UtinniCore/swg/scene/particle_preview.{h,cpp}` exporting a CppSharp-exposable `RetriggerLiveEffectInstances(...)` (name per the Task-1 decision) that, when the hook is reachable, re-triggers the live scene instances for a given effect/template on the game thread, heap-free. If Task 1 found NO reachable hook this phase, ship the export as a documented no-op stub (returns a "not-reachable" status) so the managed/editor side has a stable seam and the degraded badge path is exercised — DO NOT block the phase. Register the new files in `UtinniCore.vcxproj` (all relevant config groups; mind the `Debug/` gitignore trap — `project_ci_debug_gitignore_trap`).

    After building, if `Generated/UtinniCore.cs` regenerated/reordered from the new CppSharp-exposed type, `git checkout --` it — NEVER commit it (`project_utinnicore_cs_regen_churn`). Keep the export allocation-free on any path that could run per frame (`project_rh_snapshot_no_heap_alloc`).
  </action>
  <verify>
    <automated>cmd /c "\"%ProgramFiles%\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe\" \"D:/Code/Utinni/Utinni.sln\" /t:UtinniCore /p:Configuration=Release /p:Platform=x86 /v:minimal"</automated>
  </verify>
  <acceptance_criteria>
    - `particle_preview.h`/`.cpp` exist and export a retrigger entry (grep `[Rr]etrigger`); the export is named per the Task-1 doc.
    - The files are registered in `UtinniCore.vcxproj` (grep the vcxproj for `particle_preview`).
    - UtinniCore builds Release|x86 green via VS2026 MSBuild (MSB exit 0).
    - `Generated/UtinniCore.cs` is NOT modified in the commit (git status clean for that path).
    - No per-frame heap allocation introduced on the retrigger path (grep the .cpp for `reserve(`/`new ` on the hot path; none on a per-frame branch).
  </acceptance_criteria>
  <done>A native retrigger export (real or documented stub) builds green, is registered in the vcxproj, and does not churn UtinniCore.cs; live confirmation rolls into 15-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Managed editor → native effect manager (under injection) | The retrigger crosses into SWG's live runtime on the game thread |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Denial of Service | particle_preview retrigger on the game thread | mitigate | Marshal once per save/reload (not per frame); heap-free on any hot path (`project_rh_snapshot_no_heap_alloc` — per-frame `vector::reserve()` crashed scene change at 0x0051fb0a); stack-allocated fixed-size snapshots if a per-frame path is unavoidable |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | This plan installs ZERO packages; no install task present |
</threat_model>

<verification>
- UtinniCore builds Release|x86 green via VS2026 MSBuild
- `15-PARTICLE-PREVIEW-HOOK.md` documents the manager + reachability + heap-free plan + fallback
- `Generated/UtinniCore.cs` unchanged
</verification>

<success_criteria>
The D-09 live-preview hook is resolved EARLY: either a reachable native retrigger export ships (isolated, build-green) or its absence is documented with the honest degraded-badge fallback — so the Particle editor (15-06) and the live smoke (15-08) proceed without blocking on this open question.
</success_criteria>

<output>
Create `.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-03-SUMMARY.md` when done.
</output>
