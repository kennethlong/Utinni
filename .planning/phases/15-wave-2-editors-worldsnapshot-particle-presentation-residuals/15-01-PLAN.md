---
phase: 15-wave-2-editors-worldsnapshot-particle-presentation-residuals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/SWG/WorldSnapshotImpl.cs"
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/SubPanels/SnapshotPanel.cs"
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormSnapshotPlacements.cs"
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormSnapshotPlacements.Designer.cs"
  - "D:/Code/Utinni/UtinniCoreDotNet/Commands/WorldSnapshotBulkComposer.cs"
  - "D:/Code/Utinni/UtinniCoreDotNet.Tests/Commands/WorldSnapshotBulkComposerTests.cs"
autonomous: true
requirements: [PROD-W2-WS]

must_haves:
  truths:
    - "A modder can open a flat placements table (id / object-template / cell / position) over the loaded world snapshot, with multi-select bulk move/delete/retemplate (D-01)"
    - "Clicking a single row drives the existing gizmo + per-node controls"
    - "Multi-select + bulk move / delete / retemplate apply atomically on one game frame and are undoable, composing the existing per-node WorldSnapshotCommands (D-02)"
    - "The table is a new view over the native WorldSnapshotReaderWriter node list — zero new format code (D-02)"
    - "The panel conforms to the unchanged IEditorPlugin.GetSubPanels() seam with Phase-8 singleton hide-not-dispose and the CON-T-05 *Impl separation (D-03)"
  artifacts:
    - path: "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormSnapshotPlacements.cs"
      provides: "Companion resizable placements table window + bulk-op toolbar"
      min_lines: 120
    - path: "D:/Code/Utinni/UtinniCoreDotNet/Commands/WorldSnapshotBulkComposer.cs"
      provides: "Testable bulk-op command-composition helper (framework-leg)"
      contains: "WorldSnapshotBulkComposer"
  key_links:
    - from: "FormSnapshotPlacements"
      to: "WorldSnapshotImpl bulk ops"
      via: "Placements… button + bulk toolbar"
      pattern: "BulkMove|BulkDelete|BulkRetemplate"
    - from: "WorldSnapshotImpl bulk ops"
      to: "GroundSceneCallbacks.AddUpdateLoopCall"
      via: "one atomic update-loop call composing N WorldSnapshotCommands"
      pattern: "AddUpdateLoopCall"
---

<objective>
Grow the shipped TJT `SnapshotPanel` into a real WorldSnapshot editor (PROD-W2-WS): add a flat placements table + multi-select bulk operations (move / delete / retemplate) (D-01), hosted in a companion resizable `FormSnapshotPlacements` window launched from a new `Placements…` button on the existing 417px SubPanel — conforming to the unchanged `IEditorPlugin.GetSubPanels()` seam, Phase-8 singleton hide-not-dispose, and the CON-T-05 *Impl separation (D-03).

Purpose: This is the cheapest Wave-2 editor — zero new format code (D-02), reusing the shipped `WorldSnapshotReaderWriter` / `WorldSnapshotImpl` / `WorldSnapshotCommands`. It de-risks the SubPanel/companion-Form/bulk-op/undo UI machinery before the heavier Particle work. The only genuinely new code is UI + a testable bulk-op command-composition helper.

Output: A working placements-table editor that composes the existing per-node `IUndoCommand`s into atomic bulk operations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-CONTEXT.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-PATTERNS.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-UI-SPEC.md

<interfaces>
<!-- Native node list reader (CppSharp-exposed under UtinniCore.Utinni; verified world_snapshot.h:73-91).
     Read all of this on the game thread. -->
WorldSnapshotReaderWriter.Get() → instance
  int getNodeCount()
  Node getNodeAt(int i)
  string getObjectTemplateName(int idx)
  Node getNodeById(uint id[, parentObject])
  Node getLastNode()
Node: { uint Id; uint ParentId; float Radius; Transform Transform; /* ObjectTemplateName via getObjectTemplateName */ }

<!-- Shipped IUndoCommand set — UtinniCoreDotNet/Commands/WorldSnapshotCommands.cs.
     Each command ctor snapshots `new WorldSnapshotReaderWriter.Node(node)` and itself
     marshals via AddUpdateLoopCall (lines 46-52). -->
AddWorldSnapshotNodeCommand, RemoveWorldSnapshotNodeCommand,
WorldSnapshotNodePositionChangedCommand, WorldSnapshotNodeRotationChangedCommand

<!-- Shipped game-thread marshalling + undo push (WorldSnapshotImpl.cs) -->
GroundSceneCallbacks.AddUpdateLoopCall(Action)  // marshal to SWG game thread
editorPlugin.AddUndoCommand(this, new AddUndoCommandEventArgs(IUndoCommand))
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WorldSnapshotBulkComposer framework helper + WorldSnapshotImpl bulk ops</name>
  <read_first>
    - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/SWG/WorldSnapshotImpl.cs" (analog: SetSelectedNodePosition L279-310, RemoveNode L158-176 — the game-thread marshalling + per-node command composition idiom)
    - "D:/Code/Utinni/UtinniCoreDotNet/Commands/WorldSnapshotCommands.cs" (the four shipped IUndoCommand types + their L46-52 AddUpdateLoopCall self-marshalling)
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-PATTERNS.md" (§WorldSnapshotImpl bulk ops, L213-241 — composition pattern; §Game-thread marshalling Pattern 1)
    - "D:/Code/Utinni/UtinniCoreDotNet/UtinniCoreDotNet.Tests" (any existing test for project layout / framework-leg precedent)
  </read_first>
  <behavior>
    - Test 1: WorldSnapshotBulkComposer.ComposeMove(nodeIds, dx, dy, dz) returns one IReadOnlyList of N WorldSnapshotNodePositionChangedCommand-shaped descriptors (one per id), in input order.
    - Test 2: ComposeDelete(nodeIds) returns N remove descriptors in input order.
    - Test 3: ComposeRetemplate(nodeIds, newTemplate) returns the remove+add pairing per node (or a composite descriptor) per the chosen wiring.
    - Test 4: An empty / null id list yields zero descriptors (no throw).
    - Test 5: ComposeMove with a duplicate id list preserves count == input count (no silent dedupe that would desync undo).
  </behavior>
  <action>
    Create `UtinniCoreDotNet/Commands/WorldSnapshotBulkComposer.cs` — a pure, BCL-only helper (NO WinForms, NO native marshalling, NO GroundSceneCallbacks — checker B-1 framework-leg discipline, mirrors Phase-8 LooseOverridePath / Phase-9 CsvCellCoercion extraction) that, given a selection of node ids + bulk parameters, produces an ordered list of bulk-operation descriptors. Each descriptor names the shipped command to compose (Position-changed / Remove / Add) and its per-node args, so the TJT side enqueues exactly N existing commands inside ONE `AddUpdateLoopCall`. Decide undo wiring per CONTEXT discretion: a single composite descriptor wrapping N child commands OR N ordered descriptors — document the choice in the file header. Do NOT re-implement undo; this helper only computes the composition plan.

    Then modify `WorldSnapshotImpl.cs` to add `BulkMove(selectedIds, dx,dy,dz)`, `BulkDelete(selectedIds)`, `BulkRetemplate(selectedIds, newTemplate)`. Each method derives the plan via WorldSnapshotBulkComposer, then enqueues ONE `GroundSceneCallbacks.AddUpdateLoopCall` that iterates the plan: per node `WorldSnapshotReaderWriter.Get().GetNodeById(id)`, skip null, `editorPlugin.AddUndoCommand(...)` the shipped command, apply the mutation (e.g. `WorldSnapshot.RemoveNode(node)`), exactly as the shipped RemoveNode (L162-174) does. NEVER run snapshot edits on the WinForms thread (allocator corruption). Bulk retemplate = remove + add per node.

    Add `WorldSnapshotBulkComposerTests.cs` covering the 5 behaviors above in `UtinniCoreDotNet.Tests`.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests --no-build --filter SnapshotBulk</automated>
  </verify>
  <acceptance_criteria>
    - `WorldSnapshotBulkComposer.cs` exists in `UtinniCoreDotNet/Commands/`, references ONLY BCL types (no `System.Windows.Forms`, no `UtinniCore.Utinni`, no `GroundSceneCallbacks`) — assert by grep that none of those tokens appear.
    - `WorldSnapshotImpl.cs` gains `BulkMove`/`BulkDelete`/`BulkRetemplate`, each containing exactly one `GroundSceneCallbacks.AddUpdateLoopCall` call (grep `AddUpdateLoopCall` count rises by 3, one per method).
    - `dotnet test UtinniCoreDotNet.Tests --no-build --filter SnapshotBulk` passes with ≥5 facts.
    - Bulk delete composes `RemoveWorldSnapshotNodeCommand`; bulk move composes `WorldSnapshotNodePositionChangedCommand` (grep the impl).
  </acceptance_criteria>
  <done>WorldSnapshotBulkComposer + 5 passing facts; WorldSnapshotImpl exposes three bulk methods each composing N shipped commands in one atomic update-loop call.</done>
</task>

<task type="auto">
  <name>Task 2: FormSnapshotPlacements companion window + SnapshotPanel Placements… button</name>
  <read_first>
    - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/SubPanels/SnapshotPanel.cs" (the shipped panel to grow; analog: UpdateSelectedNodeControlsPosition L246-259 — the detach/reattach Pattern-2 idiom)
    - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormObjectTemplateEditor.cs" (shell analog: UtinniForm:IEditorForm, header + class decl L24-130, FormClosing adapter L70-73)
    - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs" (grid host analog — ThemedDataGridView Fill region, dock-fill add-first order)
    - "D:/Code/Utinni/UtinniCoreDotNet/UI/SingletonFormClosePolicy.cs" (ShouldHideInsteadOfDispose L58-61 — MANDATORY hide-not-dispose)
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-UI-SPEC.md" (§Host Placement WorldSnapshot; §Layout Contract FormSnapshotPlacements regions; §WS placements table columns; §Copywriting WS rows; §Reload Candor Contract WS badge)
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-PATTERNS.md" (§FormSnapshotPlacements L159-209)
  </read_first>
  <action>
    Create `FormSnapshotPlacements : UtinniForm` (+ Designer) — a resizable companion window (`DrawName = true`, TJT icon), default 900×600, min 720×420, title `Snapshot Placements — {snapshotName}`, persisting `[Snapshot] placementsWidth/placementsHeight`. Singleton per editor session: already-open → `Activate()` else `Show()`; FormClosing adapter delegates to `SingletonFormClosePolicy.ShouldHideInsteadOfDispose(reason)` → cancel close + `Hide()` on `CloseReason.UserClosing` (apply from commit 1).

    Layout per UI-SPEC (Controls.Add order with Fill grid FIRST per CF-09 / `feedback_winforms_dockfill_zorder`):
    1. Bulk toolbar (Dock=Top, 28px): `Move selected…` · `Delete selected` · `Retemplate selected…` · sep · `Refresh` · right cluster `lblReloadBadge` (240px, MiddleRight, LOCKED copy `Placements re-resolve on the next scene change.`) · `lblSelCount` (`{n} selected`).
    2. Filter row (Dock=Top, 28px): `UtinniLabel "Filter:"` · `UtinniTextbox txtFilter` (280px, substring across id/object-template/cell) · `lblRowCount` (`{shown} / {total} nodes`).
    3. Placements grid (Dock=Fill, added FIRST): `ThemedDataGridView` (Phase-9 token map verbatim), `MultiSelect=true`, `FullRowSelect`, `AllowUserToAddRows=false`, `AllowUserToDeleteRows=false`, `ReadOnly=true`. Columns: Id (right-aligned, stable key), Object template (Fill weight), Cell (resolved parent-id→cell name, empty for world-cell 0), Position (`({X:0.0}, {Y:0.0}, {Z:0.0})`). Populate from `WorldSnapshotReaderWriter.Get()` node list read ON THE GAME THREAD.
    4. Status strip (Dock=Bottom, 22px, inset 3px): `lblStatus` (Color.Red failure / Colors.Font() normal).

    Selection sync (Pattern 2, load-bearing): single-row click drives the shipped gizmo via the existing `UpdateSelectedNodeControls` path; detach/reattach the table `SelectionChanged` handler around programmatic re-selection. Multi-select updates `lblSelCount` and does NOT drive the single-node gizmo. Bulk buttons call `WorldSnapshotImpl.BulkMove/BulkDelete/BulkRetemplate`. Bulk-move/retemplate use small per-call `UtinniForm` modals (`Move {n} placements` Δ X/Y/Z UtinniNumericUpDown; `Retemplate {n} placements` New object template UtinniTextbox — both Apply/Cancel, non-destructive so NO red confirm). Bulk delete routes through the shipped `FormSaveConfirmDialog` (heading `Delete {n} placements?`, Color.Red body, verbs `Delete`/`Cancel`). Status lines: `Moved {n} placements.` / `Deleted {n} placements.` / `Retemplated {n} placements to {template}.`. Keyboard: Ctrl+A select all visible, Delete → Delete selected, Esc in filter clears filter.

    Surface the DEC-A3 preview-vs-author sentence (D-10) as a dimmed `Colors.FontDisabled()` footer/about line: `Utinni places, transforms, and retemplates existing object templates — authoring the templates or their meshes is the Blender lane.`

    Modify `SnapshotPanel.cs`: add ONE `Placements…` `UtinniButton` that opens the companion window (panel otherwise unchanged). The placements window is a VIEW over the same loaded snapshot — panel load/unload/save refresh/clear/re-baseline the table; closing the window does NOT unload the snapshot. Empty state (no snapshot): bulk/Refresh disabled, centered dimmed `No snapshot loaded` / `Load a snapshot from the Snapshot panel to see its placements.`, reload badge HIDDEN.

    Do NOT widen `GetSubPanels()` (stays as-is, CON-M-01/02). Register the form per planner discretion (UI-SPEC allows GetForms() or direct launch from the panel button).
  </action>
  <verify>
    <automated>cmd /c "\"%ProgramFiles%\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe\" \"D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolbox.sln\" /p:Configuration=Debug /p:Platform=x86 /v:minimal"</automated>
  </verify>
  <acceptance_criteria>
    - `FormSnapshotPlacements.cs` exists, extends `UtinniForm`, ≥120 lines; its FormClosing handler calls `SingletonFormClosePolicy.ShouldHideInsteadOfDispose` (grep).
    - The placements grid is a `ThemedDataGridView` with `MultiSelect=true`, `ReadOnly=true`, `AllowUserToAddRows=false` (grep the Designer/ctor).
    - The Fill grid is added to `Controls` BEFORE the Top/Bottom docked controls (CF-09 — assert add-order in Designer).
    - `SnapshotPanel.cs` gains exactly one `Placements…` `UtinniButton`; `GetSubPanels()` is NOT widened (grep returns same null shape).
    - The LOCKED WS reload badge copy `Placements re-resolve on the next scene change.` appears verbatim; the DEC-A3 D-10 sentence appears verbatim.
    - UtinniPlugins TJT solution builds Debug|x86 green via VS2026 MSBuild (MSB exit 0).
  </acceptance_criteria>
  <done>Companion placements window builds, hosts the multi-select read-only table over the native node list, wires single-select to the gizmo and bulk ops to WorldSnapshotImpl with honest reload candor; SubPanel gains the launch button; TJT Debug|x86 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WinForms UI thread → SWG game thread | All snapshot mutations must marshal; a UI-thread edit corrupts SWG's allocator |
| User multi-selection → bulk mutation of live scene node graph | Bulk delete/move/retemplate change live in-world state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Denial of Service | WorldSnapshotImpl bulk ops on the game thread | mitigate | All bulk ops marshal via a single `GroundSceneCallbacks.AddUpdateLoopCall`; the composition helper is pure/BCL-only with no per-frame allocation on a hot path (`project_rh_snapshot_no_heap_alloc`); never mutate the snapshot from the WinForms thread |
| T-15-05b | Tampering | Bulk delete removing N placements without confirmation | mitigate | Bulk delete routed through the shipped `FormSaveConfirmDialog` (Color.Red verb), undoable via composed `RemoveWorldSnapshotNodeCommand`s until save |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | This plan installs ZERO packages (RESEARCH Package Audit: zero external packages); no install task present |
</threat_model>

<verification>
- `dotnet test UtinniCoreDotNet.Tests --no-build --filter SnapshotBulk` ≥5 facts green
- UtinniPlugins TJT builds Debug|x86 green via VS2026 MSBuild
- Grep confirms framework-leg discipline (no WinForms/native tokens in WorldSnapshotBulkComposer) and CF-09 add-order
</verification>

<success_criteria>
A modder can open the placements table over a loaded snapshot, single-select to drive the gizmo, and multi-select to bulk move/delete/retemplate atomically + undoably — backed entirely by the shipped reader + commands (zero new format code). Live in-client demo is deferred to the Wave-4 Tier-4 smoke (15-08).
</success_criteria>

<output>
Create `.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-01-SUMMARY.md` when done.
</output>
</output>
