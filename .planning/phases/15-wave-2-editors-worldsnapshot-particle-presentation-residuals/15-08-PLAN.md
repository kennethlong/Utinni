---
phase: 15-wave-2-editors-worldsnapshot-particle-presentation-residuals
plan: 08
type: execute
wave: 4
depends_on: [15-01, 15-03, 15-05, 15-06, 15-07]
files_modified:
  - "D:/Code/Utinni/.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-SMOKE.md"
  - "D:/Code/Utinni/.planning/todos/pending/swg-window-resize-fullscreen-edge-cases.md"
  - "D:/Code/Utinni/.planning/todos/pending/phase10-stringtable-sc3-live-reload-residual.md"
autonomous: false
requirements: [PROD-W2-WS, PROD-W2-PRT, RESID-03, RESID-04]

must_haves:
  truths:
    - "Both new SubPanels/Forms load inside TJT against a live SWG client and follow the unchanged Wave-1 MEF seam"
    - "WorldSnapshot bulk ops visibly affect placements in-world; Particle Preview hot-retrigger visibly refreshes a live effect (or the degraded fallback is confirmed honest)"
    - "The RESID-04 edge-case matrix is filled live; the suppress fix keeps SWG windowed-embedded; no Utinni Reset"
    - "SC3 live-reload candor for .stf/.ot is observed and the badge copy is confirmed honest (RESID-03)"
  artifacts:
    - path: "D:/Code/Utinni/.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-SMOKE.md"
      provides: "Tier-4 maintainer smoke log: WS/Particle demo + RESID-03 SC3 observation + RESID-04 matrix"
      contains: "RESID-04"
  key_links:
    - from: "live injected SWG client"
      to: "WS bulk ops / Particle preview / RESID-04 suppress / RESID-03 reload"
      via: "maintainer-driven live session (Tier-4, automation cannot reach)"
      pattern: "SMOKE"
---

<objective>
Wave-4 Tier-4 maintainer-in-the-loop live-SWG smoke — the residual that automation cannot reach (CON-TT-03; Tier-3 mock-D3D9 deferred). This closes the four live-dependent deliverables of Phase 15 in ONE injected session: (1) PROD-W2-WS live demo (placements table + bulk move/delete/retemplate visibly affect in-world placements); (2) PROD-W2-PRT live demo (open a real `.prt`, edit, save, `Preview in client` hot-retrigger OR confirm the honest degraded fallback); (3) RESID-04 (fill the edge-case matrix live, read the `direct_input.cpp` DISCL log to confirm the A4 trigger + caller context, confirm the suppress fix keeps SWG windowed-embedded, verify no Utinni Reset); (4) RESID-03 (observe SC3 `.stf`/`.ot` render-on-reload, confirm the badge candor is honest).

Purpose: This is the maintainer-signed phase gate. The automatable codec/CLI/MCP/UI-build/classifier work (15-01..15-07) is green; this plan is the human verification that the live client behaves, with the smoke evidence recorded.

Output: A signed `15-SMOKE.md`; the two folded RESID todos updated/closed per the live findings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-CONTEXT.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-VALIDATION.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-03-SUMMARY.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-05-SUMMARY.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-06-SUMMARY.md
@.planning/todos/pending/swg-window-resize-fullscreen-edge-cases.md
@.planning/todos/pending/phase10-stringtable-sc3-live-reload-residual.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build both repos green + assemble the deployable injection build + draft the smoke log</name>
  <read_first>
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-VALIDATION.md" (the full-suite build/test commands + the Manual-Only Verifications table — the smoke checklist source)
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-03-SUMMARY.md" (whether the D-09 retrigger hook is live or the degraded fallback applies — sets the Particle preview expectation)
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-05-SUMMARY.md" (the RESID-04 suppress toggle + how to read the DISCL log)
    - ".planning/todos/pending/swg-window-resize-fullscreen-edge-cases.md" (the edge-case matrix to fill live)
  </read_first>
  <action>
    Build the full phase deliverable green in BOTH repos via VS2026 MSBuild (Debug+Release|x86): UtinniCore + UtinniCoreDotNet + Utinni.Cli + Utinni.Mcp (Utinni repo) and TheJawaToolbox (UtinniPlugins repo). Run the full managed suite (`dotnet test --no-build` across the three managed test projects) + the native `UtinniCore.Tests.exe` (including `[resid04]`). Assemble the deployable injection build (the Launcher + UtinniCore.dll + TJT plugin) the maintainer injects.

    Draft `15-SMOKE.md` with the four checklists ready for the maintainer to fill: (a) WS placements-table demo steps (load snapshot → `Placements…` → single-select drives gizmo → multi-select → bulk move/delete/retemplate → confirm in-world + undo), (b) Particle demo steps (open a real `.prt` extracted via the TRE Browser → edit emitter/color → save loose-override → `Preview in client` → confirm the live effect changes, OR record the honest degraded fallback per 15-03), (c) the RESID-04 edge-case matrix (windowed→fullscreen, login→world, chat-open Enter, maximize/restore, minimize/restore, free resize, multi-cycle, alt-tab, DPI change) + the DISCL-log read to confirm A4 + the suppress-toggle A/B + the no-Reset confirmation, (d) the RESID-03 SC3 observation (save `.stf`/`.ot` edit → trigger scene change → does it render? → confirm badge candor is honest).
  </action>
  <verify>
    <automated>cmd /c "\"%ProgramFiles%\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe\" \"D:/Code/Utinni/Utinni.sln\" /p:Configuration=Release /p:Platform=x86 /v:minimal && dotnet test \"D:/Code/Utinni/UtinniCoreDotNet.Tests\" --no-build && dotnet test \"D:/Code/Utinni/Utinni.Cli.Tests\" --no-build && dotnet test \"D:/Code/Utinni/Utinni.Mcp.Tests\" --no-build"</automated>
  </verify>
  <acceptance_criteria>
    - Both repos build Release|x86 green via VS2026 MSBuild (MSB exit 0 each).
    - Full managed suite green across `UtinniCoreDotNet.Tests` + `Utinni.Cli.Tests` + `Utinni.Mcp.Tests`; native `UtinniCore.Tests.exe [resid04]` green.
    - `15-SMOKE.md` exists with all four maintainer checklists (WS demo / Particle demo / RESID-04 matrix / RESID-03 SC3) ready to fill.
    - A deployable injection build is assembled.
  </acceptance_criteria>
  <done>Both repos green, full suite passes, the injection build is assembled, and the smoke log is drafted with the four checklists.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Maintainer live-SWG smoke — WS/Particle demo + RESID-04 matrix + RESID-03 SC3 (HUMAN-ACTION, Tier-4)</name>
  <what-built>
    Both Wave-2 editors (WorldSnapshot placements table + bulk ops; Particle editor with hot-retrigger preview) registered through the unchanged Wave-1 MEF seam, the RESID-04 exclusive-fullscreen suppress fix (toggle-guarded, no device Reset), and the RESID-03 honest reload-candor classification. All automatable work is green in both repos; the injection build is assembled.
  </what-built>
  <how-to-verify>
    Inject the assembled build into a live SWGEmu/Restoration client and complete `15-SMOKE.md`:
    1. **PROD-W2-WS:** TJT loads; open a snapshot in the Snapshot panel → `Placements…` opens the table; single-select drives the gizmo; multi-select + `Move selected…` / `Delete selected` (red confirm) / `Retemplate selected…` visibly change placements in-world; undo works. Confirm the SubPanel follows the unchanged MEF seam.
    2. **PROD-W2-PRT:** Open a real `.prt` (extract via the TRE Browser) in `FormParticleEditor`; the emitter tree + typed grid populate; raw-preserved unknowns render greyed-out hex; edit a typed emitter/color field → `Save (loose override)`; click `Preview in client` → confirm the live effect visibly changes (D-09 hot-retrigger) OR record the honest degraded fallback (button disabled / tier-(b) badge) per 15-03. Run `Explain effect` → confirm the read-assist summary fills the pane.
    3. **RESID-04:** Walk the edge-case matrix (windowed→fullscreen, login→world, chat-open Enter, maximize/restore, minimize/restore, free resize, multi-cycle, alt-tab, DPI). Read the `direct_input.cpp` DISCL log to confirm the exclusive-fullscreen trigger + caller (A4). A/B the suppress toggle: confirm with-suppress keeps SWG windowed-embedded (no detach) and input/chat still work. Confirm NO crash and NO Utinni-initiated device Reset.
    4. **RESID-03:** Save a `.stf` and an `.ot` edit (loose override) → trigger a scene change → observe whether the edit renders on reload vs relog-only; confirm the editor badge copy honestly matches what actually happened (no over-promise).
    Record outcomes + any defects in `15-SMOKE.md`; update/close the two folded RESID todos per the findings.
  </how-to-verify>
  <read_first>
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-SMOKE.md" (the drafted checklist from Task 1)
  </read_first>
  <action>
    HUMAN-ACTION checkpoint — this requires a maintainer-driven live injected SWG session that automation cannot reach (CON-TT-03; live render judgment; GPU/driver-specific D3D9 behavior). Present the assembled build + `15-SMOKE.md`. The maintainer injects, runs the four checklists, and records outcomes. Per Phase 8-11 precedent, automation-augmented smoke is acceptable where a live ACK is impractical, but the four live behaviors here (in-world placement change, live hot-retrigger, exclusive-fullscreen suppress, SC3 render-on-reload) are genuinely live-only. Update/close `swg-window-resize-fullscreen-edge-cases.md` (RESID-04) and `phase10-stringtable-sc3-live-reload-residual.md` (RESID-03) per the findings.
  </action>
  <verify>
    <human-check>Maintainer signs 15-SMOKE.md with the four checklists completed (or documents deferred-but-acceptable per the Phase 8-11 automation-augmented precedent); both folded RESID todos updated/closed.</human-check>
  </verify>
  <resume-signal>Maintainer types "approved" (or records outcomes + any follow-on defects) in 15-SMOKE.md.</resume-signal>
  <acceptance_criteria>
    - `15-SMOKE.md` is signed with the WS demo, Particle demo, RESID-04 matrix, and RESID-03 SC3 observations recorded.
    - The RESID-04 matrix confirms the suppress fix keeps SWG windowed-embedded with no Utinni Reset (or records the deferred fallback decision).
    - The RESID-03 badge candor is confirmed honest against the observed reload behavior.
    - `swg-window-resize-fullscreen-edge-cases.md` and `phase10-stringtable-sc3-live-reload-residual.md` are updated/closed per the findings.
  </acceptance_criteria>
  <done>The maintainer-signed Tier-4 smoke confirms both editors demo live through the unchanged MEF seam, RESID-04 stays windowed-embedded without a Reset, and RESID-03 candor is honest — the phase gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Utinni injection build → live third-party SWG process | The smoke exercises every live surface under real injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06 | Denial of Service | RESID-04 live fullscreen path / device Reset | mitigate | The smoke confirms the D-13 no-Reset constraint held live (no crash); the suppress toggle allows safe A/B; the `[resid04]` gate already enforces no Utinni-initiated Reset in code |
| T-15-05 | Denial of Service | live WS bulk ops + Particle hot-retrigger on the game thread | mitigate | All live edits marshalled + heap-free (15-01/15-03); the smoke confirms no scene-change/allocator crash |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | This plan installs ZERO packages; no install task present |
</threat_model>

<verification>
- Both repos build Release|x86 green; full managed + native suites green
- `15-SMOKE.md` signed with all four checklists; both folded RESID todos updated/closed
</verification>

<success_criteria>
Both Wave-2 editors demo end-to-end inside TJT against a live SWG client through the unchanged Wave-1 MEF seam; WorldSnapshot bulk ops + Particle hot-retrigger work live (or the honest fallback is confirmed); the RESID-04 suppress fix keeps SWG windowed-embedded without a device Reset; RESID-03 SC3 candor is confirmed honest — all recorded in a maintainer-signed smoke log.
</success_criteria>

<output>
Create `.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-08-SUMMARY.md` when done.
</output>
