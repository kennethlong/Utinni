---
phase: 15-wave-2-editors-worldsnapshot-particle-presentation-residuals
plan: 14
type: execute
wave: 5
depends_on: []
files_modified:
  - "D:/Code/Utinni/UtinniCoreDotNet/Commands/WorldSnapshotCommands.cs"
  - "D:/Code/Utinni/UtinniCoreDotNet/Commands/WorldSnapshotCommandGuard.cs"
  - "D:/Code/Utinni/UtinniCoreDotNet.Tests/Commands/WorldSnapshotCommandGuardTests.cs"
autonomous: true
gap_closure: true
requirements: [PROD-W2-WS]

must_haves:
  truths:
    - "The in-tree A9 revert fix in WorldSnapshotCommands.cs (SetPosition + SetRotation resolve the LIVE node by id from the live tree, make the in-world object OPTIONAL / node-required, pass the target position to PositionAndRotationChanged) is finalized: undo of a WS bulk Move/Rotate actually REVERTS (was a silent no-op even with the 15-09 crash guard holding) without crashing"
    - "All temporary [A9-diag] Log.Info lines are stripped from WorldSnapshotCommands.cs before the final commit — the deployed build carries no diagnostic logging on the undo hot path"
    - "WorldSnapshotCommandGuard exposes a node-only ShouldApply(node) overload (the one the revert fix calls) AND it has unit coverage; the two-arg ShouldApply(obj, node) facts remain green"
  artifacts:
    - path: "D:/Code/Utinni/UtinniCoreDotNet/Commands/WorldSnapshotCommands.cs"
      provides: "Finalized A9 revert fix: live-node-by-id resolution, optional obj / required node, target-position revert; ZERO [A9-diag] logging"
      contains: "WorldSnapshotCommandGuard.ShouldApply(node)"
    - path: "D:/Code/Utinni/UtinniCoreDotNet/Commands/WorldSnapshotCommandGuard.cs"
      provides: "node-only ShouldApply(object lookup) overload consumed by the revert fix"
      contains: "ShouldApply(object lookup)"
    - path: "D:/Code/Utinni/UtinniCoreDotNet.Tests/Commands/WorldSnapshotCommandGuardTests.cs"
      provides: "Unit coverage of the node-only ShouldApply overload"
      contains: "WorldSnapshotCommandGuard"
  key_links:
    - from: "WorldSnapshotNodePositionChangedCommand.SetPosition / SetRotation"
      to: "WorldSnapshotReaderWriter.Get().GetNodeById (live tree) + WorldSnapshotCommandGuard.ShouldApply(node)"
      via: "resolve live node by id; guard node-only; revert node data; move obj only when instantiated"
      pattern: "ShouldApply\\(node\\)"
    - from: "WorldSnapshotCommandGuardTests"
      to: "WorldSnapshotCommandGuard.ShouldApply(object)"
      via: "node-only facts (null -> false, non-null -> true)"
      pattern: "WorldSnapshotCommandGuard"
---

<objective>
Finalize the UNCOMMITTED working-tree A9 revert fix in `UtinniCoreDotNet/Commands/WorldSnapshotCommands.cs` (read its `git diff`). During the 2026-06-13 A9 re-verify, with the 15-09 crash guard holding, undo STILL silently did not revert: `Network.GetObjectById(id)` returns null for these snapshot placements (`objNull=True, nodeNull=False`), and the shipped 15-09 `ShouldApply(obj, node)` required BOTH non-null so it bailed BEFORE reverting; the undo also resolved the live node via the COPIED node's `ParentNode` (null on a copy). The in-tree fix resolves the LIVE node by id from the live tree (mirrors the working `BulkMove`), makes the in-world object OPTIONAL (node-required — revert the node data so the snapshot re-resolve repaints it), and passes the target position to `PositionAndRotationChanged`; the same fix is applied to the rotation undo. This work is LIVE-VERIFIED PASS (Undo now reverts + auto-refreshes), but it still carries temporary `[A9-diag]` `Log.Info` lines that MUST be stripped before the final commit, and the rotation/position bodies now call `WorldSnapshotCommandGuard.ShouldApply(node)` (the node-only overload).

Purpose: undo is a core advertised WS bulk-op affordance (Checklist A9); the 15-09 fix stopped the crash but a silent no-op undo is still a broken affordance. This plan finalizes the verified fix, removes the diagnostic noise, ensures the node-only guard overload is present + unit-covered, and lands a clean commit so the 15-17 reassembled build is diagnostic-free. (The `directx9.cpp` inject-crash fix is ALREADY committed `b26e4bd` — do NOT replan it.)

Output: a finalized, diagnostic-free `WorldSnapshotCommands.cs`; a node-only `ShouldApply` overload on `WorldSnapshotCommandGuard` with unit coverage; green build + managed suite; a clean commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-SMOKE.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-09-PLAN.md

<interfaces>
<!-- The UNCOMMITTED revert fix (read via `git diff UtinniCoreDotNet/Commands/WorldSnapshotCommands.cs`):
  SetPosition + SetRotation now do:
    var rw = WorldSnapshotReaderWriter.Get();
    var node = rw.GetNodeById(nodeCopy.Id);                       // LIVE node by id (not copy.ParentNode)
    if (node == null && nodeCopy.ParentId > 0) { var parent = rw.GetNodeById(nodeCopy.ParentId);
        node = parent != null ? parent.GetChildById(nodeCopy.Id) : null; }   // live-parent fallback
    var obj = Network.GetObjectById(nodeCopy.Id);                 // may be null (no live visual yet)
    Log.Info("[A9-diag] ...");                                    // <-- TEMPORARY, STRIP ALL OF THESE
    if (!WorldSnapshotCommandGuard.ShouldApply(node)) { Log.Info("[A9-diag] ... BAILED"); return; }  // node-only guard
    if (obj != null) { obj.Transform.Position = position; obj.PositionAndRotationChanged(false, position); }  // target pos
    node.Transform.Position = position;
    WorldSnapshot.DetailLevelChanged();
    Log.Info("[A9-diag] ... APPLIED");                            // <-- STRIP
  The `using UtinniCoreDotNet.Utility;` was added for Log — keep ONLY if Log is still referenced after
  stripping; if no Log call remains, remove the now-unused using. -->

<!-- WorldSnapshotCommandGuard ALREADY exposes both overloads (verified WorldSnapshotCommandGuard.cs:72-85):
    public static bool ShouldApply(object lookup)              => lookup != null;   // node-only (the revert fix calls this)
    public static bool ShouldApply(object objLookup, object nodeLookup) => both != null;
  The node-only overload EXISTS — confirm it stays and is unit-covered. The existing tests
  (WorldSnapshotCommandGuardTests.cs) already cover the single-arg overload (Tests 1+2) and the
  two-arg overload (Tests 3+4). Ensure the single-arg facts remain and are explicitly the node-only
  semantics (a null live node => bail). Do NOT remove the two-arg overload — it is still public API
  and the 15-09 tests assert it; the Add/Remove command bodies still use single-arg node guards. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip [A9-diag] logging + finalize the revert fix; reconcile the node-only guard + facts</name>
  <files>D:/Code/Utinni/UtinniCoreDotNet/Commands/WorldSnapshotCommands.cs, D:/Code/Utinni/UtinniCoreDotNet/Commands/WorldSnapshotCommandGuard.cs, D:/Code/Utinni/UtinniCoreDotNet.Tests/Commands/WorldSnapshotCommandGuardTests.cs</files>
  <read_first>
    - "D:/Code/Utinni/UtinniCoreDotNet/Commands/WorldSnapshotCommands.cs" (the FULL file PLUS its `git diff` — the SetPosition + SetRotation revert fix with the `[A9-diag]` lines to strip; AND the Add/Remove command bodies that still use single-arg node guards, to confirm none regress)
    - "D:/Code/Utinni/UtinniCoreDotNet/Commands/WorldSnapshotCommandGuard.cs" (both ShouldApply overloads already present — confirm the node-only single-arg overload is the one SetPosition/SetRotation call)
    - "D:/Code/Utinni/UtinniCoreDotNet.Tests/Commands/WorldSnapshotCommandGuardTests.cs" (existing facts — keep the single-arg node-only facts + two-arg facts; add an explicit node-only-semantics fact if missing)
    - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/SWG/WorldSnapshotImpl.cs" (lines 209-243 BulkMove — the working live-node-by-id + optional-obj pattern the revert fix mirrors; confirm the finalized SetPosition matches that resolution shape)
  </read_first>
  <action>
    Finalize the in-tree revert fix in `UtinniCoreDotNet/Commands/WorldSnapshotCommands.cs`:

    1. STRIP every temporary `[A9-diag]` line: remove all `Log.Info("[A9-diag] ...")` calls from `SetPosition` and `SetRotation` (the resolve-trace, the BAILED line, and the APPLIED line). Leave the actual fix logic intact: the live-node-by-id resolution (`WorldSnapshotReaderWriter.Get().GetNodeById(nodeCopy.Id)` with the live-parent `GetChildById` fallback for `ParentId > 0`), the node-only `WorldSnapshotCommandGuard.ShouldApply(node)` bail, the optional-object branch (`if (obj != null) { obj.Transform.Position = position; obj.PositionAndRotationChanged(false, position); }` for position; the rotation analog `obj.Transform.CopyRotation(transform); obj.PositionAndRotationChanged(false, node.Transform.Position)`), the node-data revert (`node.Transform.Position = position` / `node.Transform.CopyRotation(transform)`), and `WorldSnapshot.DetailLevelChanged()`.

    2. If stripping the `[A9-diag]` lines leaves `using UtinniCoreDotNet.Utility;` (the `Log` import added by the diff) unused, remove that using; if any other code in the file still references `Log`, keep it.

    3. Confirm the node-only guard call: `SetPosition`/`SetRotation` call `WorldSnapshotCommandGuard.ShouldApply(node)` (single-arg, node-only — node is the only true bail; obj is optional). The `WorldSnapshotCommandGuard.ShouldApply(object)` overload already exists — DO NOT remove the two-arg `ShouldApply(object, object)` overload (it is public API and the Add/Remove command bodies + 15-09 tests still use it). Keep the file header comment accurate (the single-arg overload's semantics are "node is the only true bail").

    4. Reconcile `WorldSnapshotCommandGuardTests.cs`: ensure there is at least one fact asserting the single-arg `ShouldApply(node)` node-only semantics — null node => false (the undo would bail / nothing to revert), non-null node => true. The existing Tests 1+2 cover the single-arg overload generically; rename/annotate or add a fact so the node-only intent is explicit (e.g. `ShouldApply_NullNode_ReturnsFalse_BailNothingToRevert`). Keep the two-arg facts. Class name stays containing `WorldSnapshotCommandGuard` for the verify filter.

    After building, `git checkout -- UtinniCoreDotNet/Generated/UtinniCore.cs`. Then commit `WorldSnapshotCommands.cs` + the guard + tests with a `fix(15-14): finalize A9 revert (live-node resolve, optional obj) + strip [A9-diag] logging` message (Co-Authored-By footer per repo policy).
  </action>
  <verify>
    <automated>cmd /c "\"%ProgramFiles%\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe\" \"D:/Code/Utinni/Utinni.sln\" /p:Configuration=Release /p:Platform=x86 /v:minimal && dotnet test \"D:/Code/Utinni/UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj\" --no-build -c Release --filter WorldSnapshotCommandGuard"</automated>
  </verify>
  <acceptance_criteria>
    - `WorldSnapshotCommands.cs` contains ZERO `[A9-diag]` occurrences (grep `A9-diag` returns no match) and ZERO `Log.Info` calls inside `SetPosition`/`SetRotation`.
    - `SetPosition` + `SetRotation` resolve the live node via `WorldSnapshotReaderWriter.Get().GetNodeById(nodeCopy.Id)` (NOT `nodeCopy.ParentNode`) with the `GetChildById` live-parent fallback, guard with `WorldSnapshotCommandGuard.ShouldApply(node)`, move the obj only inside an `if (obj != null)` branch, and revert the node data + call `WorldSnapshot.DetailLevelChanged()` (read both method bodies).
    - `WorldSnapshotCommandGuard.cs` still exposes BOTH `ShouldApply(object lookup)` and `ShouldApply(object objLookup, object nodeLookup)` (grep both signatures).
    - If `using UtinniCoreDotNet.Utility;` is now unused it is removed; the file builds with no unused-using/CS warnings introduced by this change.
    - `dotnet test … --filter WorldSnapshotCommandGuard` passes with the node-only single-arg fact(s) + the two-arg facts, 0 failed.
    - `Utinni.sln` Release|x86 builds (MSBuild exit 0); full `UtinniCoreDotNet.Tests` suite passes (no regression).
    - `Generated/UtinniCore.cs` reverted; the change is committed.
  </acceptance_criteria>
  <done>The A9 revert fix is finalized and diagnostic-free: live-node-by-id resolution + node-only guard + optional-obj revert, with all [A9-diag] logging stripped; the node-only ShouldApply overload is present + unit-covered; build + suite green; committed. Live A9 re-verify (already PASS on the pre-strip build) re-confirms against the 15-17 reassembled diagnostic-free build in 15-18.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editor undo invocation → live SWG node graph | Undo replays a stored command against the live scene; the live node must be resolved by id (the copied command node carries no live parent pointer) and the in-world object may be absent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-14-01 | Repudiation / Integrity (broken affordance) | Undo silently no-ops (does not revert) because the 15-09 two-arg guard bailed when the live object was absent and the live node was resolved via a null copied ParentNode | mitigate | THIS is the gap: resolve the LIVE node by id, guard node-only (obj optional), revert the node data so the snapshot re-resolve repaints it (15-SMOKE A9 RE-VERIFY follow-on #2) |
| T-15-14-02 | Information disclosure (log noise) | Temporary [A9-diag] Log.Info lines shipping in the deployed build, spamming the undo hot path | mitigate | Strip all [A9-diag] logging before the final commit so the 15-17 reassembled build is diagnostic-free |
| T-15-14-SC | Tampering | npm/pip/cargo installs | accept | This plan installs ZERO packages (C# edit + xUnit) |
</threat_model>

<verification>
- `dotnet test UtinniCoreDotNet.Tests --no-build -c Release --filter WorldSnapshotCommandGuard` green (node-only + two-arg facts)
- Full `UtinniCoreDotNet.Tests` Release suite green (no regression)
- `Utinni.sln` Release|x86 MSBuild exit 0
- grep `A9-diag` in WorldSnapshotCommands.cs returns nothing; no Log.Info in SetPosition/SetRotation
- Read confirms: live-node-by-id resolution, node-only guard, optional-obj branch, node-data revert
- `Generated/UtinniCore.cs` reverted; change committed
- LIVE (already PASS 2026-06-13; re-confirm in 15-18 against the diagnostic-free build): WS bulk Move/Rotate Undo reverts + auto-refreshes, no crash
</verification>

<success_criteria>
The A9 undo affordance both no-longer-crashes (15-09) AND actually reverts (this plan): SetPosition/SetRotation resolve the live node by id, guard node-only with the obj optional, and revert the node data + DetailLevelChanged; all [A9-diag] diagnostic logging is stripped; the node-only ShouldApply overload is present + unit-covered; the change builds, passes the suite, and is committed clean for the 15-17 reassembled build.
</success_criteria>

<output>
Create `.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-14-SUMMARY.md` when done.
</output>
