---
phase: 15-wave-2-editors-worldsnapshot-particle-presentation-residuals
plan: 06
type: execute
wave: 3
depends_on: [15-02, 15-03, 15-04]
files_modified:
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormParticleEditor.cs"
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormParticleEditor.Designer.cs"
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs"
  - "D:/Code/Utinni/UtinniCoreDotNet/UI/ParticleHandoffPolicy.cs"
  - "D:/Code/Utinni/UtinniCoreDotNet.Tests/UI/ParticleHandoffPolicyTests.cs"
autonomous: true
requirements: [PROD-W2-PRT]

must_haves:
  truths:
    - "A modder can open a .prt in a new FormParticleEditor (toolbar Open… + TRE Browser hand-off), see the emitter tree + typed param grid, conforming to the unchanged GetSubPanels() seam + Phase-8 singleton hide-not-dispose (D-03)"
    - "Raw-preserved unknown fields render greyed-out in Consolas hex with the locked 'preserved as original bytes' tooltip (D-05 made visible)"
    - "The in-app Explain effect button reuses the SAME .prt CLI/MCP read path (D-08) and is read-assist ONLY — it explains/summarizes, never writes .prt bytes, no prompt-to-mutate (D-07)"
    - "The Preview in client button is state-encoded (enabled only when Game.IsRunning + retrigger hook reachable); the reload badge shows honest live-capable vs degraded candor (D-09, D-14)"
    - "The DEC-A3 preview-vs-author sentence (D-11) is surfaced in the editor"
  artifacts:
    - path: "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormParticleEditor.cs"
      provides: "Particle editor Form (emitter tree + param grid + hex fallback + AI assist + preview)"
      min_lines: 200
    - path: "D:/Code/Utinni/UtinniCoreDotNet/UI/ParticleHandoffPolicy.cs"
      provides: "testable .prt hand-off visibility gate (mirrors OtHandoffPolicy)"
      contains: "ParticleHandoffPolicy"
  key_links:
    - from: "FormParticleEditor open/save"
      to: "Formats/Particle codec"
      via: "ParticleEffectDocument / MutableParticleEffect / ParticleEffectWriter"
      pattern: "ParticleEffect"
    - from: "FormParticleEditor Explain effect button"
      to: ".prt CLI/MCP read path"
      via: "reuse the decode/summarize verb (D-08)"
      pattern: "decode|summarize|roundtrip-particle"
    - from: "Plugin.cs GetForms()"
      to: "FormParticleEditor"
      via: "try/catch registration; GetSubPanels() stays null"
      pattern: "forms.Add"
---

<objective>
Build the new `FormParticleEditor : UtinniForm` (PROD-W2-PRT, UI half) — a Wave-1-shaped editor over a `.prt` (`FORM PEFT`) asset, registered via `GetForms()` in `Plugin.cs` (`GetSubPanels()` stays null, CON-M-01/02; Phase-8 singleton hide-not-dispose, D-03). It clones the Phase-11 Object Template editor shell: emitter tree (left) + typed param grid (right) with per-type widgets + Consolas-9pt hex fallback for greyed-out unknowns (the visible surface of D-05), the inherited Phase-8 `Save ▾` drop-down + provenance gating, editor-local undo/redo, an in-app `Explain effect` AI read-assist button reusing the SAME CLI/MCP read path (D-08) — read-assist ONLY, never writes `.prt` bytes, no prompt-to-mutate (D-07) — and a `Preview in client` hot-retrigger button (D-09) with honest live-capable-vs-degraded reload candor.

Purpose: This is the flashy AI-assist showcase editor. It composes the 15-02 codec, the 15-04 read path, and the 15-03 preview hook (or its documented fallback) into a single editor. WinForms-host UI is verified by MSBuild-green build (Phase 8-11 precedent); the testable logic (hand-off visibility gate) factors to `UtinniCoreDotNet`.

Output: A working Particle editor Form; live demo + hot-retrigger confirmed in 15-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-CONTEXT.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-UI-SPEC.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-PATTERNS.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-02-SUMMARY.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-03-SUMMARY.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-04-SUMMARY.md

<interfaces>
<!-- Shell + controls to clone (verified) -->
// FormObjectTemplateEditor.cs (header + class decl L24-130): UtinniForm:IEditorForm, FormClosing
//   adapter → SingletonFormClosePolicy (L70-73), editor-local ObjectTemplateEditController (L96-97),
//   inherited Save ▾ + OpenSource provenance gating (L111-130), background Task open/parse/save
//   (System.Threading.Tasks, L38; UI mutations via Control.Invoke).
// IffChunkTree (UtinniPlugins/.../UI/Controls/IffChunkTree.cs) — left emitter tree.
// ThemedDataGridView (.../UI/Controls/ThemedDataGridView.cs) — right param grid, Phase-9 token map.
// SingletonFormClosePolicy.ShouldHideInsteadOfDispose (UtinniCoreDotNet/UI) — hide-not-dispose.
// OtHandoffPolicy (UtinniCoreDotNet/UI) — the hand-off visibility-gate analog for ParticleHandoffPolicy.
// Plugin.cs:108-119 — one isolated try { forms.Add(new Form...(this)); } catch { Log.Info(...) } per editor.

<!-- 15-02 codec -->
// ParticleEffectDocument.FromIff / MutableParticleEffect.EditField / ParticleEffectWriter.Write
<!-- 15-04 read path (the SAME path the Explain effect button reuses, D-08) -->
// decode-iff (PEFT) / roundtrip-particle / the MCP particle read tool
<!-- 15-03 preview hook (or documented fallback) -->
// particle_preview retrigger export (real or stub) + the degraded-badge decision
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ParticleHandoffPolicy framework gate (testable .prt hand-off visibility)</name>
  <read_first>
    - "D:/Code/Utinni/UtinniCoreDotNet/UI/OtHandoffPolicy.cs" (the exact analog: ShouldOffer*Editor extension-gate + IsObjectTemplatePayload click-time content sniff that never throws)
    - "D:/Code/Utinni/UtinniCoreDotNet.Tests/UI" (existing OtHandoffPolicy tests for the framework-leg test layout)
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-UI-SPEC.md" (§Host Placement Particle entry points: TRE Browser hand-off HIDDEN when the sniff fails)
  </read_first>
  <behavior>
    - Test 1: ShouldOfferParticleEditor returns true for a `.prt` extension entry, false otherwise (extension gate).
    - Test 2: IsParticlePayload returns true when the bytes sniff as `FORM PEFT`, false for non-PEFT bytes (content sniff).
    - Test 3: IsParticlePayload never throws on malformed/truncated bytes (returns false).
  </behavior>
  <action>
    Create `UtinniCoreDotNet/UI/ParticleHandoffPolicy.cs` mirroring `OtHandoffPolicy.cs`: a `ShouldOfferParticleEditor(...)` extension-visibility gate (`.prt` only) + an `IsParticlePayload(byte[])` click-time content sniff that checks for the `FORM PEFT` root and NEVER throws (defensive, returns false on malformed). This is the framework-leg testable seam (the WinForms hand-off itself is build-verified). Add `ParticleHandoffPolicyTests.cs` (filter `ParticleHandoff`) covering the 3 behaviors.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests --no-build --filter ParticleHandoff</automated>
  </verify>
  <acceptance_criteria>
    - `ParticleHandoffPolicy.cs` exists with `ShouldOfferParticleEditor` (extension gate) + `IsParticlePayload` (content sniff that never throws).
    - `IsParticlePayload` returns false (no throw) on malformed/truncated input (Test 3).
    - `dotnet test ... --filter ParticleHandoff` passes with ≥3 facts.
  </acceptance_criteria>
  <done>The .prt hand-off visibility + payload sniff are a tested framework gate, ready for the TRE Browser hand-off to consume.</done>
</task>

<task type="auto">
  <name>Task 2: FormParticleEditor shell + emitter tree + param grid + hex fallback + AI assist + preview + registration</name>
  <read_first>
    - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormObjectTemplateEditor.cs" (READ header + class decl L24-130 — the direct clone target: shell, undo controller, Save ▾, provenance gating, hex fallback, background Task)
    - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/IffChunkTree.cs" (the left emitter tree control to reuse)
    - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/ThemedDataGridView.cs" (the right param grid — Phase-9 token map verbatim)
    - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs" (L108-119 registration pattern; GetSubPanels stays null)
    - "D:/Code/Utinni/UtinniCoreDotNet/UI/SingletonFormClosePolicy.cs" (hide-not-dispose adapter)
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-UI-SPEC.md" (§FormParticleEditor regions; §Particle param grid value-cell states; §States Particle rows; §Reload Candor Contract Particle; §Copywriting Particle rows incl. LOCKED tooltips/badges; §Host Placement Particle)
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-PATTERNS.md" (§FormParticleEditor L244-264; §Singleton hide-not-dispose; §MEF registration try/catch)
  </read_first>
  <action>
    Create `FormParticleEditor : UtinniForm, IEditorForm` (+ Designer) — a direct clone of the FormObjectTemplateEditor shell: resizable, custom titlebar (`DrawName = true`, TJT icon), title `Particle Editor` with leading `● ` dirty marker, default 1100×760, min 880×560, persist `[ParticleEditor] width/height/splitterDistance`. Singleton hide-not-dispose via `SingletonFormClosePolicy` (apply from commit 1; the Phase-8 *Impl-separation seam, D-03). Editor-local undo/redo (Ctrl+Z/Ctrl+Y caught at the form, INDEPENDENT of the scene `UndoRedoManager` — CON-M-05).

    Layout per UI-SPEC (Fill split added FIRST per CF-09; set `Size` before `SplitterDistance`):
    1. Editor toolbar (Dock=Top, 28px): `Open…` · `Save ▾` (inherited Phase-8 3 modes; live-patch DISABLED with the locked CF-03 tooltip) · sep · `Undo` · `Redo` · sep · `Explain effect` · sep · `Preview in client` · sep · `Reload in client`. Right cluster: `lblReloadBadge` (240px, MiddleRight, LOCKED Particle candor copy) · `lblDirty` (140px, `Unsaved changes` at `Colors.Secondary()`).
    2. Main split (Dock=Fill, added FIRST): LEFT ≈280px emitter tree (`IffChunkTree` or themed TreeView showing `PEFT → EMGP[i] → EMTR[j] → PTQD/PTMH`); RIGHT param `ThemedDataGridView` (Field / Value / Type) + a collapsed AI-assist `UtinniTextbox` results pane (multiline, Consolas-9pt acceptable) below it.
    3. Status strip (Dock=Bottom, 22px, inset 3px): `lblStatus` (Fill) + right `lblCounters` (`{emitterGroups} groups · {emitters} emitters · {raw} raw-preserved`).

    Param grid value-cell states (UI-SPEC): typed-clean (per-type widget on edit — `UtinniNumericUpDown` float/int via EditingControlShowing, checkbox bool, combobox enum), typed-dirty (`ForeColor = Colors.Secondary()` + `● ` row-header glyph + title `● ` + lblDirty), raw-preserved D-05 degrade (`Consolas 9pt`, `Colors.FontDisabled()`, read-only, hex of the raw bytes, LOCKED tooltip `This field isn't typed yet — its original bytes are preserved exactly and saved unchanged.`), and WaveForm/ColorRamp summary (read-only `{n} control points` / swatch strip + an `Edit…` per-call sub-editor floor — NO graphical curve editor this phase per UI-SPEC Assumption 4 / RESEARCH A6).

    Wire open/parse/save on a background `Task` (no modal spinners; UI mutations via `Control.Invoke`): open via toolbar `Open…` (`.prt`) AND the TRE Browser `Open in Particle Editor` hand-off (gated on `ParticleHandoffPolicy` from Task 1 — HIDDEN when the sniff fails). Open/parse uses the 15-02 codec (`ParticleEffectDocument.FromIff` → `MutableParticleEffect`); save uses the inherited Save ▾ (loose-override default; repack provenance-gated on `Source is OpenSource.TreArchive`; discard-while-dirty via the shipped `FormSaveConfirmDialog`).

    `Explain effect` button: call the SAME `.prt` decode/summarize CLI/MCP read path from 15-04 (D-08) — NO independent AI/format path. This is read-assist ONLY (D-07): the button explains/summarizes the effect and NEVER writes `.prt` bytes or accepts a prompt-to-mutate; results fill the read-only pane; on error `Couldn't read this effect — {reason}.` at `Color.Red`.

    `Preview in client` button (D-09): state-encoded — enabled ONLY when `Game.IsRunning` AND the 15-03 retrigger hook is reachable; calls the native retrigger export marshalled on the game thread, heap-free (per 15-03 decision doc); on success status `Re-triggered {n} live instance(s).` + a 1s `Colors.Secondary()` badge pulse + the live-capable reload badge (`Re-triggers live instances on Preview.`). When unavailable: disabled with tooltip `No live client — start SWG to preview in-scene.` and the reload badge degrades to the LOCKED tier-(b) copy `Reloads on next scene change or relog.` (RESID-03 / D-14 — never over-promise; this is the honest fallback the 15-03 doc specified).

    Surface the DEC-A3 preview-vs-author sentence (D-11) verbatim as a dimmed `Colors.FontDisabled()` footer/about line: `Utinni edits emitter, timing, and color parameters and swaps texture/mesh references — authoring the referenced meshes or textures stays in Blender.`

    Register in `Plugin.cs` `GetForms()` inside try/catch isolation (`try { forms.Add(new FormParticleEditor(this)); } catch (Exception ex) { Log.Info(...); }`); do NOT widen `GetSubPanels()` (stays null). Add the TRE Browser context-menu `Open in Particle Editor` hand-off (HIDDEN when `ParticleHandoffPolicy` sniff fails). Empty state per UI-SPEC: tree+grid empty, centered dimmed `No particle effect open` / `Open a .prt file from the toolbar, or use "Open in Particle Editor" from the TRE Browser.`, all edit/preview/reload/AI buttons disabled, reload badge HIDDEN.
  </action>
  <verify>
    <automated>cmd /c "\"%ProgramFiles%\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe\" \"D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolbox.sln\" /p:Configuration=Debug /p:Platform=x86 /v:minimal"</automated>
  </verify>
  <acceptance_criteria>
    - `FormParticleEditor.cs` exists, extends `UtinniForm`/`IEditorForm`, ≥200 lines; FormClosing calls `SingletonFormClosePolicy.ShouldHideInsteadOfDispose` (grep).
    - The Fill main split is added to `Controls` BEFORE the Top/Bottom docked controls (CF-09 add-order in Designer); the param grid is a `ThemedDataGridView`; the emitter tree reuses `IffChunkTree` or a themed TreeView.
    - The raw-preserved cell uses `Consolas` + `Colors.FontDisabled()` + read-only + the LOCKED tooltip string verbatim (grep the tooltip copy).
    - The `Explain effect` handler invokes the 15-04 read path (grep for the decode/summarize/roundtrip-particle verb or the MCP/CLI dispatch) — NO codec call inside the form's AI handler, and the handler is read-only (no save/write path, D-07).
    - `Preview in client` is disabled unless `Game.IsRunning` (grep the enable guard); the degraded reload badge copy `Reloads on next scene change or relog.` and live-capable copy `Re-triggers live instances on Preview.` both appear verbatim.
    - The DEC-A3 D-11 sentence appears verbatim; the live-patch CF-03 disabled tooltip appears verbatim.
    - `Plugin.cs` registers `FormParticleEditor` in `GetForms()` in try/catch; `GetSubPanels()` is NOT widened (grep).
    - UtinniPlugins TJT solution builds Debug|x86 green via VS2026 MSBuild (MSB exit 0).
  </acceptance_criteria>
  <done>The Particle editor Form builds, opens `.prt` via toolbar + TRE hand-off, shows the typed grid with honest D-05 hex fallback, reuses the 15-04 read path for AI assist, state-encodes the D-09 preview with honest candor, and surfaces the DEC-A3 boundary — live demo deferred to 15-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editor UI thread → SWG game thread (preview) | The D-09 hot-retrigger touches live scene instances |
| Untrusted `.prt` → editor open path | The editor opens arbitrary local `.prt` files |
| Save → loose-override / repack | Editor writes can target user assets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering / DoS | editor open path over untrusted `.prt` | mitigate | Open composes the 15-02 codec (truncation-safe cursor + count guards + degrade-don't-abort); a malformed file opens degraded rather than crashing the editor |
| T-15-05 | Denial of Service | Preview in client hot-retrigger | mitigate | Marshalled on the game thread, heap-free, marshal-once-per-preview (15-03 decision; `project_rh_snapshot_no_heap_alloc`); button state-encoded-disabled when unreachable |
| T-15-07 | Tampering | save escaping the resolved root / overwriting user assets | mitigate | Inherited Phase-8 `LooseOverridePath` containment + atomic write + `FormSaveConfirmDialog` for repack/discard; loose-override is the default |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | This plan installs ZERO packages (RESEARCH Package Audit; no charting package — WaveForm/ColorRamp use existing controls); no install task present |
</threat_model>

<verification>
- `dotnet test UtinniCoreDotNet.Tests --no-build --filter ParticleHandoff` ≥3 facts green
- UtinniPlugins TJT builds Debug|x86 green via VS2026 MSBuild
- Grep confirms: LOCKED copy strings verbatim, CF-09 add-order, AI button reuses 15-04 read path (no codec in the form), GetSubPanels() not widened
</verification>

<success_criteria>
A modder can open a `.prt` in `FormParticleEditor`, navigate the emitter tree, edit typed fields, see honest D-05 hex fallback for unknowns, run `Explain effect` (reusing the 15-04 read path, read-assist only per D-07), and (when injected) `Preview in client` — with honest live-capable-vs-degraded reload candor and the DEC-A3 boundary surfaced. Live demo + hot-retrigger confirmation is the 15-08 Tier-4 smoke.
</success_criteria>

<output>
Create `.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-06-SUMMARY.md` when done.
</output>
</output>
