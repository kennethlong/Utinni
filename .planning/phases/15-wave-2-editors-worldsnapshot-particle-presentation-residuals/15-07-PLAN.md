---
phase: 15-wave-2-editors-worldsnapshot-particle-presentation-residuals
plan: 07
type: execute
wave: 3
depends_on: []
files_modified:
  - "D:/Code/Utinni/UtinniCoreDotNet/Saving/ReloadAssetClassifier.cs"
  - "D:/Code/Utinni/UtinniCoreDotNet.Tests/SavingTests/ParticleSnapshotReloadRoutingTests.cs"
autonomous: true
requirements: [RESID-03]

must_haves:
  truths:
    - "The new .ws (WorldSnapshot) and .prt (Particle) reload paths classify with the existing tier-(b) PendingNextSceneChange routing"
    - "Reload-candor badge copy is HONEST and never over-promises (relog/scene-change-only says so; live-capable only when truly live)"
    - "The classifier's conservative unknown fallback is preserved"
  artifacts:
    - path: "D:/Code/Utinni/UtinniCoreDotNet.Tests/SavingTests/ParticleSnapshotReloadRoutingTests.cs"
      provides: "routing tests asserting honest tier-(b) classification for .ws and .prt"
      contains: "PendingNextSceneChange"
  key_links:
    - from: "ReloadAssetClassifier"
      to: ".ws / .prt tier-(b) routing"
      via: "extend the routing table for the new editor reload paths"
      pattern: "PendingNextSceneChange"
---

<objective>
RESID-03: confirm + honestly state SC3 live-reload candor for the new WorldSnapshot + Particle reload paths, reusing the shipped `ReloadAssetClassifier` tier-(b) routing (D-14). This is the automatable half: extend the classifier's routing table + test map so `.ws` and `.prt` classify with honest tier-(b) `PendingNextSceneChange` candor, preserving the conservative unknown fallback. The live SC3 observation for `.stf`/`.ot` (does the edit actually render on reload, or is it relog-only) is the Tier-4 maintainer smoke folded into 15-08.

Purpose: The LOCKED Reload Candor Contract (UI-SPEC) forbids loosening badge copy to over-promise. This plan makes the classification (the source of the badge copy) honest + tested for the two new editors, so the 15-01 WorldSnapshot badge (`Placements re-resolve on the next scene change.`) and the 15-06 Particle degraded badge (`Reloads on next scene change or relog.`) are backed by a tested tier-(b) classification rather than aspirational copy.

Output: Extended classifier + routing tests for `.ws`/`.prt`. Live SC3 confirmation in 15-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-CONTEXT.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-UI-SPEC.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-PATTERNS.md
@.planning/todos/pending/phase10-stringtable-sc3-live-reload-residual.md

<interfaces>
<!-- The shipped classifier to extend (verified) -->
// UtinniCoreDotNet/Saving/ReloadAssetClassifier.cs:107-139
//   4-tier routing: .iff(SHOT/STOT/SBOT) + .stf → ReloadTier.PendingNextSceneChange (tier-b),
//   with a CONSERVATIVE unknown fallback. Do NOT loosen the fallback.
// Existing routing tests (the test-map analog to extend):
//   UtinniCoreDotNet.Tests/SavingTests/StringTableReloadRoutingTests.cs
//   UtinniCoreDotNet.Tests/SavingTests/DatatableReloadRoutingTests.cs
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route .ws + .prt to honest tier-(b) PendingNextSceneChange + routing tests</name>
  <read_first>
    - "D:/Code/Utinni/UtinniCoreDotNet/Saving/ReloadAssetClassifier.cs" (READ IN FULL — the 4-tier routing table L107-139 + the conservative unknown fallback)
    - "D:/Code/Utinni/UtinniCoreDotNet.Tests/SavingTests/StringTableReloadRoutingTests.cs" (the routing-test analog to mirror)
    - "D:/Code/Utinni/UtinniCoreDotNet.Tests/Saving/ReloadAssetClassifierTests.cs" (the classifier-level test analog)
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-UI-SPEC.md" (§Reload Candor Contract — the LOCKED copy that this classification backs; planner may NOT loosen)
    - ".planning/todos/pending/phase10-stringtable-sc3-live-reload-residual.md" (the Step-7 scene-change + stale-sourceCrc checks; the candor disposition spec)
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-PATTERNS.md" (§RESID-03 reload candor — extend routing, never loosen)
  </read_first>
  <behavior>
    - Test 1: a `.ws` (WorldSnapshot) reload path classifies as `ReloadTier.PendingNextSceneChange` (tier-b).
    - Test 2: a `.prt` (Particle) reload path classifies as `ReloadTier.PendingNextSceneChange` (tier-b) in the degraded (no live retrigger) case.
    - Test 3: the conservative unknown-extension fallback is UNCHANGED (an unrecognized extension still routes to the conservative tier, not loosened).
    - Test 4 (honesty guard): no routing entry classifies `.ws`/`.prt` as an instant/live-refresh tier that would over-promise; the degraded copy maps to tier-(b).
  </behavior>
  <action>
    Extend `ReloadAssetClassifier.cs` so `.ws` and `.prt` route to `ReloadTier.PendingNextSceneChange` (tier-b), matching the existing `.stf`/`.iff` candor — the `.ws` path inherits `Placements re-resolve on the next scene change.` and the `.prt` DEGRADED path inherits `Reloads on next scene change or relog.` (the Particle LIVE-capable badge is NOT a classifier tier — it is a runtime affordance gated on the 15-03 hook + `Game.IsRunning`, set in the form, not here). Preserve the conservative unknown fallback exactly (do NOT loosen it). Add `ParticleSnapshotReloadRoutingTests.cs` (mirror `StringTableReloadRoutingTests.cs`; filter `ReloadRouting`) covering the 4 behaviors, including the honesty guard that neither new extension is classified as an instant/live tier.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests --no-build --filter ReloadRouting</automated>
  </verify>
  <acceptance_criteria>
    - `ReloadAssetClassifier.cs` routes `.ws` and `.prt` to `ReloadTier.PendingNextSceneChange` (grep both extensions near the tier-b routing).
    - The conservative unknown fallback is unchanged (grep the fallback branch; Test 3 asserts an unknown extension still routes conservatively).
    - No routing entry classifies `.ws`/`.prt` as an instant/live tier (Test 4 honesty guard).
    - `ParticleSnapshotReloadRoutingTests.cs` exists; `dotnet test ... --filter ReloadRouting` passes (existing StringTable/Datatable routing facts + ≥4 new).
  </acceptance_criteria>
  <done>The new `.ws`/`.prt` reload paths classify with honest tier-(b) candor backing the LOCKED badge copy, the conservative fallback is preserved, and the routing is tested. Live SC3 render-on-reload confirmation is the 15-08 smoke.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | RESID-03 is classification + UI copy; no new untrusted-input or write surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08 | Repudiation / honesty | reload-candor badge over-promising a reload that didn't happen | mitigate | Classifier routes `.ws`/`.prt` to tier-(b) with honest LOCKED copy; the honesty-guard test forbids an instant/live classification; the conservative unknown fallback is preserved (D-14, never loosen) |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | This plan installs ZERO packages; no install task present |
</threat_model>

<verification>
- `dotnet test UtinniCoreDotNet.Tests --no-build --filter ReloadRouting` green (existing + new facts)
- Grep confirms `.ws`/`.prt` route to tier-(b) and the conservative fallback is unchanged
</verification>

<success_criteria>
The new WorldSnapshot + Particle reload paths classify with honest tier-(b) candor, backing the LOCKED badge copy without over-promising, the conservative unknown fallback preserved, all tested. Live SC3 render-on-reload observation for `.stf`/`.ot` is the Tier-4 smoke in 15-08.
</success_criteria>

<output>
Create `.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-07-SUMMARY.md` when done.
</output>
