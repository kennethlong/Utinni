---
phase: 15-wave-2-editors-worldsnapshot-particle-presentation-residuals
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - "D:/Code/Utinni/UtinniCoreDotNet/Formats/Particle/ParticleFieldValue.cs"
  - "D:/Code/Utinni/UtinniCoreDotNet/Formats/Particle/WaveFormCodec.cs"
  - "D:/Code/Utinni/UtinniCoreDotNet/Formats/Particle/ColorRampCodec.cs"
  - "D:/Code/Utinni/UtinniCoreDotNet/Formats/Particle/ParticleEmitterDescription.cs"
  - "D:/Code/Utinni/UtinniCoreDotNet/Formats/Particle/ParticleEffectDocument.cs"
  - "D:/Code/Utinni/UtinniCoreDotNet/Formats/Particle/MutableParticleEffect.cs"
  - "D:/Code/Utinni/UtinniCoreDotNet/Formats/Particle/ParticleEffectWriter.cs"
  - "D:/Code/Utinni/UtinniCoreDotNet/Formats/Particle/ParticleParseException.cs"
  - "D:/Code/Utinni/UtinniCoreDotNet.Tests/Formats/Particle/ParticleFixtureBuilder.cs"
  - "D:/Code/Utinni/UtinniCoreDotNet.Tests/Formats/Particle/ParticleCodecTests.cs"
autonomous: true
requirements: [PROD-W2-PRT]

must_haves:
  truths:
    - "A .prt (FORM PEFT) file decodes into a typed MutableParticleEffect (emitter groups, emitters, timing, color)"
    - "A WaveForm field decodes to typed control points and re-encodes byte-identically"
    - "An unrecognized EMTR/PEFT FORM version is raw-preserved as bytes and round-trips byte-exact (never FATAL)"
    - "Editing one typed field re-encodes only the touched leaf; untouched bytes re-emit verbatim"
  artifacts:
    - path: "D:/Code/Utinni/UtinniCoreDotNet/Formats/Particle/ParticleEffectDocument.cs"
      provides: "PEFT root typed decode entry (FromIff)"
      contains: "PEFT"
    - path: "D:/Code/Utinni/UtinniCoreDotNet/Formats/Particle/WaveFormCodec.cs"
      provides: "WaveForm decode/encode reused across emitter fields"
      contains: "WaveForm"
    - path: "D:/Code/Utinni/UtinniCoreDotNet/Formats/Particle/ParticleEffectWriter.cs"
      provides: "byte-exact writer composing IffWriter"
      contains: "IffWriter"
  key_links:
    - from: "ParticleEffectDocument.FromIff"
      to: "IffReader.Read"
      via: "tree-walk parse of FORM PEFT"
      pattern: "IffReader\\.Read"
    - from: "ParticleEmitterDescription decode"
      to: "raw-preserve fallback"
      via: "catch DecoderException → FromRawBytes on the unrecognized FORM sub-tree"
      pattern: "FromRawBytes|RawBytes"
---

<objective>
Build the new `.prt` / `FORM PEFT` (Particle Effect Template) typed codec in `UtinniCoreDotNet/Formats/Particle/` (PROD-W2-PRT, format half). Full typed decode (D-04) of emitter / wave / timing / color fields modeled on the read-only `swg-client-v2 clientParticle` format spec, with mandatory degrade-don't-abort raw-byte preservation (D-05) at the unrecognized-FORM-version granularity.

Purpose: This is the phase's real format investment and highest format-risk item (15 EMTR versions, ~468 IFF calls in the reference, no Utinni fixtures). It is a structural clone of `Formats/ObjectTemplate/` composing the shipped tree-based `IffReader`/`IffWriter` + `IffPayloadCursor`. The codec lives in `UtinniCoreDotNet` (D-06) so format logic stays headless + CI-testable and out of TJT/MCP. WaveForm + ColorRamp recur everywhere, so they are ported FIRST as standalone reused units (Pitfall 3).

Output: A headless, CI-tested `.prt` codec with byte-exact round-trip and degrade-don't-abort.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-CONTEXT.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-RESEARCH.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-PATTERNS.md

<interfaces>
<!-- Shipped IFF primitives the codec composes — DO NOT hand-roll (verified) -->
IffReader.Read(Stream input) → IffDocument        // Formats/Iff/IffReader.cs:92
IffReader.Read(string path) → IffDocument          // :104
IffWriter.Write(MutableIffDocument doc, Stream output)  // Formats/Iff/IffWriter.cs:86
MutableIffDocument.FromDocument(IffDocument, byte[] originalBytes) → MutableIffDocument

<!-- Little-endian payload cursor — internal to UtinniCoreDotNet, truncation-safe (Formats/Decoders/IffPayloadCursor.cs) -->
IffPayloadCursor: ReadInt8, ReadInt32Le, ReadUInt32Le, ReadFloatLe,
                  ReadCString(Encoding), ReadBytes(int), .Remaining
// tags + chunk lengths are BIG-endian (IffReader handles); payload scalars are LITTLE-endian (Pitfall 6)

<!-- Degrade-don't-abort precedent to copy verbatim (the load-bearing pattern, D-05) -->
// ObjectTemplateParamCodec.cs:84-120  — consume-exactly-or-hex posture
// MutableObjectTemplate.FromMutableIff:194-216 — loop-level catch(DecoderException) → FromRawBytes
ObjectTemplateParamValue.FromRawBytes(payload, tag)  // raw-preserve a leaf

<!-- .prt nested taxonomy (the typed-decode tree to port; read swg-client-v2 layout only, copy NO code/identifiers) -->
FORM PEFT
└── FORM 0000|0001|0002        ParticleEffectDescription (0002 = current write version)
    ├── (PTIM)                 ParticleTiming
    ├── CHUNK 0000             int32 count [+ 4 floats in 0002]
    └── N × FORM EMGP          ParticleEmitterGroupDescription
        ├── (PTIM)             timing
        └── N × FORM EMTR      ParticleEmitterDescription (15 versions 0000-0014)
            ├── many WaveForm fields   (sharedMath WaveForm, 3 versions) ← port once
            ├── CHUNK 0000             emitter scalars/enums/bools
            └── FORM PTQD|PTMH         Quad|Mesh leaf (4 versions)
                ├── ColorRamp          ← port once
                └── (PTEX)             texture reference (D-11 swap target)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WaveFormCodec + ColorRampCodec + ParticleFieldValue + ParticleFixtureBuilder (port the recurring leaves first)</name>
  <read_first>
    - "D:/Code/Utinni/UtinniCoreDotNet/Formats/ObjectTemplate/ObjectTemplateParamValue.cs" (typed-union analog for ParticleFieldValue, incl. RawBytesHexFallback)
    - "D:/Code/Utinni/UtinniCoreDotNet/Formats/ObjectTemplate/ObjectTemplateParamCodec.cs" (L24-29 provenance header; L84-120 consume-exactly-or-hex)
    - "D:/Code/Utinni/UtinniCoreDotNet/Formats/Decoders/IffPayloadCursor.cs" (L24-28 provenance header; the little-endian methods + truncation-safe contract)
    - "D:/Code/Utinni/UtinniCoreDotNet/Formats/Iff/IffWriter.cs" (the IffWriter.Write signature the fixture builder uses)
    - "D:/Code/swg-client-v2/.../sharedMath/src/shared/WaveForm.cpp" (READ-ONLY spec — on-disk layout of WaveForm's 3 versions; copy NO code/identifiers)
    - "D:/Code/swg-client-v2/.../clientParticle/src/shared/ColorRamp.h" + "ColorRamp.cpp" (READ-ONLY spec — ColorRamp layout)
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-PATTERNS.md" (§Formats/Particle codec; §Formats/* provenance header — MANDATORY)
  </read_first>
  <behavior>
    - Test 1: WaveFormCodec.Read over a synth WaveForm leaf yields the expected control-point count + values; WaveFormCodec.Write re-emits byte-identical bytes.
    - Test 2: WaveFormCodec round-trips all 3 known versions byte-exact; an unknown version tag yields a raw-preserve ParticleFieldValue (no throw).
    - Test 3: ColorRampCodec.Read/Write round-trips a synth color ramp byte-exact.
    - Test 4: ParticleFieldValue typed union exposes Float/Int/Bool/Enum/WaveForm/ColorRamp/RawBytesHexFallback; FromRawBytes preserves the exact original bytes.
    - Test 5: ParticleFixtureBuilder produces a minimal valid FORM PEFT byte[] (via IffWriter) that IffReader.Read parses without error.
    - Test 6 (Pitfall 3 guard): the `m_particleWeight.scaleAll(0.28f)` load-time normalization is NOT applied on write-back — a weight field round-trips to original bytes.
  </behavior>
  <action>
    Create `Formats/Particle/ParticleParseException.cs` (mirror `ObjectTemplate` exception shape), `ParticleFieldValue.cs` (typed union cloned from `ObjectTemplateParamValue` — add WaveForm + ColorRamp cases + a `RawBytesHexFallback` case with a `FromRawBytes(bytes)` factory that captures the verbatim original bytes), `WaveFormCodec.cs` (decode/encode of the sharedMath WaveForm, 3 versions, modeled on the read-only spec layout — unknown version → raw-preserve), and `ColorRampCodec.cs` (decode/encode the color-over-life ramp). All payload reads use `IffPayloadCursor` (little-endian, Pitfall 6); NEVER hand-roll BitConverter index math. Each file MUST open with the MIT header + the LOCKED provenance comment ("Format understood by reading swg-client-v2 ... no code or identifiers copied ... original to Utinni under MIT"; clientParticle/sharedMath are SOE/Bootprint ARR — study on-disk layout only).

    Pitfall 3 / A5: the load-time `scaleAll(0.28f)` weight normalization in the reference EMTR load_0000 is a READ-only transform; the codec must round-trip the ORIGINAL bytes (favor raw-preserve where a typed transform would be lossy) so write-back is byte-exact.

    Create test infra in `UtinniCoreDotNet.Tests/Formats/Particle/`: `ParticleFixtureBuilder.cs` (synth a minimal `FORM PEFT` via `IffWriter`, mirroring the Datatable/STF synth-fixture precedent — no `.prt` fixtures exist today) and `ParticleCodecTests.cs` with the 6 facts above (filter prefix `ParticleWaveForm`).
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests --no-build --filter ParticleWaveForm</automated>
  </verify>
  <acceptance_criteria>
    - All four new `Formats/Particle/*.cs` files open with the LOCKED provenance header (grep "original to Utinni" + "swg-client-v2" in each).
    - `WaveFormCodec` + `ColorRampCodec` use `IffPayloadCursor` for payload reads (grep `IffPayloadCursor`); NO `BitConverter` index math (grep returns none in these files).
    - `ParticleFieldValue.FromRawBytes` preserves exact bytes (Test 4 asserts byte-equality).
    - WaveForm round-trips all 3 versions byte-exact AND an unknown version raw-preserves without throwing (Tests 1-2).
    - `dotnet test ... --filter ParticleWaveForm` passes with ≥6 facts.
  </acceptance_criteria>
  <done>WaveForm + ColorRamp + the typed-union value + the synth fixture builder are in place and byte-exact, with unknown-version raw-preserve and the no-scaleAll-on-write guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PEFT typed decode tree (ParticleEffectDocument + EMGP/EMTR) + MutableParticleEffect + ParticleEffectWriter</name>
  <read_first>
    - "D:/Code/Utinni/UtinniCoreDotNet/Formats/ObjectTemplate/MutableObjectTemplate.cs" (the exact structural model: FromMutableIff parse, EditOverride L226-241 in-place leaf edit, RewriteCount L291-302 machine-managed count)
    - "D:/Code/Utinni/UtinniCoreDotNet/Formats/ObjectTemplate/ObjectTemplateWriter.cs" (writer analog — composes IffWriter.Write)
    - "D:/Code/Utinni/Utinni.Cli/Commands/RoundtripOtCommand.cs" (ParseOt L190-199 — the bytes→IffReader.Read→MutableIffDocument.FromDocument→Mutable* entry idiom)
    - "D:/Code/swg-client-v2/.../clientParticle/src/shared/ParticleEffectDescription.cpp" (READ-ONLY — PEFT root load/write, versions 0000/0001/0002; copy NO code)
    - "D:/Code/swg-client-v2/.../clientParticle/src/shared/ParticleEmitterGroupDescription.cpp" (READ-ONLY — EMGP layer)
    - "D:/Code/swg-client-v2/.../clientParticle/src/shared/ParticleEmitterDescription.cpp" (READ-ONLY — EMTR, 15 versions, the WaveForm field sequence)
    - "D:/Code/swg-client-v2/.../clientParticle/src/shared/ParticleTiming.cpp" (PTIM), "ParticleDescriptionQuad.cpp" (PTQD), "ParticleDescriptionMesh.cpp" (PTMH), "ParticleTexture.cpp" (PTEX) — READ-ONLY leaf layouts
  </read_first>
  <behavior>
    - Test 1: ParticleEffectDocument.FromIff over a synth FORM PEFT 0002 yields the expected emitter-group count, per-group emitter count, and typed timing.
    - Test 2: A known-version EMTR decodes its WaveForm fields (via WaveFormCodec) + scalar/enum/bool chunk + PTQD/PTMH leaf with ColorRamp.
    - Test 3: MutableParticleEffect.EditField(emitter, fieldId, newValue) mutates only the touched leaf; ParticleEffectWriter re-emits a byte[] where untouched leaves are identical to the original.
    - Test 4: A structural change that alters an emitter count re-derives the machine-managed count leaf little-endian (mirror RewriteCount).
    - Test 5: Round-trip with NO edit is byte-identical to the input (ParticleEffectWriter(ParticleEffectDocument.FromIff(bytes)) == bytes).
    - Test 6 (DoS guard): an emitter/group count field exceeding a sane cap is rejected (division-form guard before the read loop) → ParticleParseException, never a huge allocation.
  </behavior>
  <action>
    Create `ParticleEffectDocument.cs` (PEFT root: `FromIff(IffDocument)` walks `FORM PEFT` → version sub-form → `(PTIM)` + count chunk + N `FORM EMGP`, each EMGP → `(PTIM)` + N `FORM EMTR`), `ParticleEmitterDescription.cs` (`EMTR` typed decode: the WaveForm field sequence via `WaveFormCodec`, the scalar/enum/bool chunk via `IffPayloadCursor`, and the `FORM PTQD|PTMH` leaf with `ColorRampCodec` + `(PTEX)` texture ref), `MutableParticleEffect.cs` (mutable model over a captured `MutableIffDocument` — `FromMutableIff` entry per the RoundtripOt ParseOt idiom; `EditField` mutates the captured leaf in place via `leaf.SetPayload(...)`; structural mutations re-derive counts little-endian like `RewriteCount`), and `ParticleEffectWriter.cs` (composes `IffWriter.Write` over the mutable doc — untouched leaves re-emit verbatim, the byte-exact round-trip mechanism).

    BEFORE every count-driven read loop, apply a division-form count guard (the established codec DoS pattern, e.g. cap the count against remaining payload / element-size, with a 16M-style absolute cap) so a malicious/oversized count cannot trigger a huge allocation (T-15-02 / V5 input validation).

    Add the 6 facts (filter prefix `ParticleDecode`) to `ParticleCodecTests.cs`, including the no-edit byte-identity round-trip (Test 5) and the count-guard rejection (Test 6).

    NOTE: if any of this work touches a CppSharp-exposed native type and regenerates `Generated/UtinniCore.cs`, `git checkout --` that file — never commit it (`project_utinnicore_cs_regen_churn`). (This codec is pure managed and should NOT touch it.)
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests --no-build --filter ParticleDecode</automated>
  </verify>
  <acceptance_criteria>
    - `ParticleEffectDocument.FromIff` composes `IffReader.Read` (grep `IffReader.Read` in the parse path) — no hand-rolled chunk/pad/endianness handling.
    - `ParticleEffectWriter` composes `IffWriter.Write` (grep `IffWriter`).
    - A no-edit round-trip is byte-identical (Test 5 asserts `writer(FromIff(bytes)) SequenceEqual bytes`).
    - Every count-driven loop is preceded by a division-form / cap guard (grep for the guard before each loop; Test 6 proves rejection).
    - `Generated/UtinniCore.cs` is unchanged (git status clean for that file).
    - `dotnet test ... --filter ParticleDecode` passes with ≥6 facts.
  </acceptance_criteria>
  <done>PEFT decodes into a typed mutable model that re-encodes byte-exact for untouched data and re-derives machine-managed counts; counts are DoS-guarded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Degrade-don't-abort raw-preserve at the FORM-version granularity (D-05)</name>
  <read_first>
    - "D:/Code/Utinni/UtinniCoreDotNet/Formats/ObjectTemplate/MutableObjectTemplate.cs" (FromMutableIff L194-216 — the loop-level catch(DecoderException) → FromRawBytes pattern, copy this EXACTLY)
    - "D:/Code/Utinni/UtinniCoreDotNet/Formats/Particle/ParticleEmitterDescription.cs" (the file from Task 2 this task hardens)
    - "D:/Code/Utinni/UtinniCoreDotNet/Formats/Particle/MutableParticleEffect.cs" (the parse entry to wrap with the degrade catch)
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-PATTERNS.md" (§Degrade-don't-abort raw-preserve Pattern 3, D-05, L70-89 — unit of raw-preservation = unrecognized FORM <version> sub-form)
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-RESEARCH.md" (Pitfall 2 — EMTR has 15 versions; SOE FATALs on unknown; Utinni must NEVER FATAL)
  </read_first>
  <behavior>
    - Test 1: An EMTR sub-form with an UNRECOGNIZED version tag (e.g. 0014 or a synth 00FF) is captured as a raw-preserved ParticleFieldValue/sub-node — decode does NOT throw, the effect still opens.
    - Test 2: Round-tripping that degraded file is byte-identical (the raw-preserved sub-tree re-emits verbatim through IffWriter).
    - Test 3: A PEFT root with an unrecognized version FORM degrades the same way (whole version sub-tree raw-preserved).
    - Test 4: A truncated / over-length leaf inside a typed emitter falls to consume-exactly-or-hex raw-preserve rather than throwing OOB (mirrors ObjectTemplateParamCodec consume-exactly-or-hex).
    - Test 5 (negative): the codec NEVER calls a fatal/throw on an unknown version — assert no `throw` reaches the caller for any synth unknown-version fixture; a typed edit on a raw-preserved emitter is refused gracefully (greyed-out, no corruption).
  </behavior>
  <action>
    Harden `ParticleEmitterDescription` decode and `MutableParticleEffect.FromMutableIff` with the loop-level `catch (DecoderException)` → raw-preserve, copied EXACTLY from `MutableObjectTemplate.FromMutableIff:194-216`. For `.prt` the unit of raw-preservation is the UNRECOGNIZED `FORM <version>` sub-form: when an EMTR/PEFT/leaf version tag is not in the typed set, capture the WHOLE sub-tree's bytes verbatim into a raw-preserved node so `IffWriter` re-emits them byte-identically. A `switch` on version MUST have a `default:` that raw-preserves — NEVER replicate the SOE `FATAL(true, "Unsupported data version")` (Pitfall 2). Over-length / truncated leaves inside a typed emitter fall to the consume-exactly-or-hex posture (ObjectTemplateParamCodec L84-120).

    Extend `ParticleFixtureBuilder` to synth an unknown-EMTR-version fixture and an unknown-PEFT-version fixture. Add the 5 facts (filter prefix `ParticleDegrade`) to `ParticleCodecTests.cs`, including the negative no-FATAL assertion and the byte-exact degraded round-trip.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests --no-build --filter ParticleDegrade</automated>
  </verify>
  <acceptance_criteria>
    - Every version `switch` in the codec has a `default:` that raw-preserves; grep the codec for `FATAL` returns NONE.
    - An unknown-version EMTR fixture decodes without throwing to the caller and round-trips byte-identical (Tests 1-2).
    - An unknown-version PEFT root degrades + round-trips byte-identical (Test 3).
    - A truncated/over-length typed leaf raw-preserves rather than OOB-throwing (Test 4).
    - `dotnet test ... --filter ParticleDegrade` passes with ≥5 facts.
  </acceptance_criteria>
  <done>The codec degrades any unrecognized FORM version (and over-length leaf) to byte-exact raw-preserve, never FATALs, and a degraded effect still opens + round-trips byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted `.prt` file → codec | `.prt` is arbitrary community/local input crossing into the parser |
| Attacker-controlled emitter/group count → allocation | A count field drives read loops + allocations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering / DoS | `.prt` payload reads (WaveForm/EMTR/leaf) | mitigate | All payload scalars read via truncation-safe `IffPayloadCursor` → `DecoderException`, never OOB (V5 input validation, ASVS L1) |
| T-15-02 | Denial of Service | emitter/group count fields | mitigate | Division-form count guard + absolute cap BEFORE each read loop (the established codec DoS pattern) — no huge allocation from an attacker count |
| T-15-03 | Availability | codec aborting on unrecognized version (corrupting/refusing a legit save) | mitigate | Degrade-don't-abort raw-preserve (D-05); NEVER `FATAL` like the SOE reference; a malformed/unknown file still opens + round-trips byte-exact |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | This plan installs ZERO packages (RESEARCH Package Audit: zero external packages); no install task present |
</threat_model>

<verification>
- `dotnet test UtinniCoreDotNet.Tests --no-build --filter Particle` (all three filters) green
- Grep confirms: provenance headers present, no `BitConverter` index math, no `FATAL`, division-form count guards, `IffReader.Read`/`IffWriter.Write` composition
- `Generated/UtinniCore.cs` unchanged (git status clean)
</verification>

<success_criteria>
The `.prt` / FORM PEFT codec decodes the typed common path, byte-exactly round-trips untouched data, re-derives machine-managed counts, DoS-guards counts, and degrades every unrecognized FORM version to byte-exact raw-preserve without ever FATAL-ing — all headless + CI-tested in `UtinniCoreDotNet`.
</success_criteria>

<output>
Create `.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-02-SUMMARY.md` when done.
</output>
