---
phase: 15-wave-2-editors-worldsnapshot-particle-presentation-residuals
plan: 15
type: execute
wave: 5
depends_on: []
files_modified:
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/ParticleReadAssist.cs"
autonomous: true
gap_closure: true
requirements: [PROD-W2-PRT]

must_haves:
  truths:
    - "ParticleReadAssist.LocateCli probes the Utinni inject root (the directory of the running UtinniCoreDotNet.dll / the Plugins/TheJawaToolbox parent) in addition to the executing-assembly dir and host-process dir, so utinni-cli.exe is found under injection (where the host process dir is the SWGEmu.exe dir, not the Utinni deploy)"
    - "When utinni-cli.exe IS present in the deploy, Explain effect (D-08 read-assist) shells utinni-cli decode-iff and fills the read-assist pane; when absent it still degrades honestly (existing 'utinni-cli.exe was not found next to the editor' message)"
  artifacts:
    - path: "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/ParticleReadAssist.cs"
      provides: "LocateCli with an additional inject-root probe path"
      contains: "LocateCli"
  key_links:
    - from: "ParticleReadAssist.LocateCli"
      to: "utinni-cli.exe in the Utinni inject root"
      via: "probe the directory of the executing assembly's PARENT (Plugins/TheJawaToolbox -> inject root) / GetSwgHwnd-independent path resolution"
      pattern: "LocateCli"
---

<objective>
Close the B7 packaging/probe gap (15-SMOKE Checklist B7): `Explain effect` shells `utinni-cli decode-iff`, but `LocateCli` fails under injection because (a) `utinni-cli.exe` is not shipped in the injection build and (b) `LocateCli` only probes the executing-assembly dir (`Plugins/TheJawaToolbox/`) and the host-process dir (the SWGEmu.exe dir under injection — wrong). This plan fixes the probe path so the CLI is found once it is deployed. (The physical deploy of `utinni-cli.exe` + its net472 dependency closure into `bin/Release/` is finalized by the rebuild/reassembly plan 15-17; this plan owns the `LocateCli` probe-path fix so the deployed exe is actually discovered.)

Purpose: `Explain effect` is the D-08 AI read-assist surface; B7 currently blocks it (compounded by B5 needing an on-disk save first — fixed in 15-12). With the probe fixed and the exe shipped, B7 passes the live re-smoke.

Output: `LocateCli` probes the Utinni inject root in addition to its current two candidates, so a deployed `utinni-cli.exe` resolves under injection; the honest CLI-absent degradation is preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-SMOKE.md

<interfaces>
<!-- The current LocateCli (verified ParticleReadAssist.cs:146-164):
  private const string CliExeName = "utinni-cli.exe";
  private static string LocateCli() {
    asmDir  = Path.GetDirectoryName(Assembly.GetExecutingAssembly().Location);  // Plugins/TheJawaToolbox/
    if (File.Exists(Path.Combine(asmDir, CliExeName))) return it;
    procDir = Path.GetDirectoryName(Process.GetCurrentProcess().MainModule.FileName);  // SWGEmu.exe dir (WRONG under injection)
    if (File.Exists(Path.Combine(procDir, CliExeName))) return it;
    return null; }   // -> "utinni-cli.exe was not found next to the editor"
  The deploy layout (15-SMOKE "Assembled injection build"):
    bin/Release/                       <-- the Utinni INJECT ROOT (UtinniCoreDotNet.dll, Launcher.exe)
      Plugins/TheJawaToolbox/          <-- the executing assembly dir (asmDir above)
        TheJawaToolboxDotNet.dll
  So the inject root = asmDir's parent's parent (Plugins/TheJawaToolbox -> Plugins -> bin/Release).
  utinni-cli.exe will be deployed into bin/Release (the inject root) by 15-17. -->

<!-- Available inject-root resolution from the plugin side: the Utinni inject root is the directory
  TWO levels up from the executing TheJawaToolboxDotNet.dll (Plugins/TheJawaToolbox/<dll> -> bin/Release),
  OR resolvable as the directory containing UtinniCoreDotNet.dll (the host CLR loaded it from the inject
  root). The simplest robust probe is: walk up from asmDir looking for a dir that contains
  Launcher.exe / UtinniCoreDotNet.dll, OR probe asmDir's parent's parent directly. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an inject-root probe to ParticleReadAssist.LocateCli</name>
  <files>D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/ParticleReadAssist.cs</files>
  <read_first>
    - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/ParticleReadAssist.cs" (LocateCli L146-164 + the click handler L87-91 that consumes it + the honest CLI-absent message)
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-SMOKE.md" (the "Assembled injection build" layout: utinni-cli.exe ships in bin/Release = the inject root; Plugins/TheJawaToolbox is two levels below it)
  </read_first>
  <action>
    Edit `ParticleReadAssist.LocateCli` to add an INJECT-ROOT probe between (or after) the two existing candidates:
      - Keep candidate 1 (executing-assembly dir) and candidate 2 (host-process dir).
      - Add candidate 3: the Utinni inject root, computed as the directory two levels up from the executing assembly dir (`Plugins/TheJawaToolbox/<dll>` → `Plugins` → `bin/Release` = inject root) via `Path.GetDirectoryName(Path.GetDirectoryName(asmDir))`. Guard each `GetDirectoryName` for null. Probe `Path.Combine(injectRoot, CliExeName)`.
      - Make the resolution robust: if the two-levels-up dir does not contain `utinni-cli.exe`, also try walking up from `asmDir` a bounded number of parents (e.g. up to 3) looking for a directory that contains `utinni-cli.exe` (or contains `Launcher.exe`/`UtinniCoreDotNet.dll` as the inject-root marker, then probe the CLI there). Keep the whole method inside its existing try/catch so locating the CLI never throws into the click handler (preserve that defensive posture).
    Return the first existing candidate; return null only when no candidate exists (preserving the honest "utinni-cli.exe was not found next to the editor" degradation in the caller). Do NOT change the click-handler error copy or the decode-iff dispatch. UtinniPlugins is the sibling repo (standing write authority); commit there.
  </action>
  <verify>
    <automated>cmd /c "\"%ProgramFiles%\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe\" \"D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolbox.sln\" /p:Configuration=Release /p:Platform=x86 /v:minimal"</automated>
  </verify>
  <acceptance_criteria>
    - `LocateCli` probes at least THREE candidates including the inject root computed two levels up from the executing-assembly dir (grep the method for a second `Path.GetDirectoryName(Path.GetDirectoryName(` nesting or an explicit inject-root walk-up loop).
    - The entire probe remains inside the existing try/catch (locating the CLI never throws); the method still returns null when no candidate exists (the honest CLI-absent path is preserved — read the caller at L87-91).
    - The click-handler error copy and the `decode-iff` dispatch are unchanged (grep for the unchanged "was not found next to the editor" string and the `decode-iff` verb).
    - `TheJawaToolbox.sln` Release|x86 builds (MSBuild exit 0).
  </acceptance_criteria>
  <done>LocateCli finds a deployed utinni-cli.exe under injection by probing the inject root (two levels up from the plugin dir / inject-root marker walk-up) while preserving the honest CLI-absent degradation; TJT Release|x86 green. Live B7 confirmation lands in 15-18 once 15-17 ships utinni-cli.exe into bin/Release.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin (TJT) ↔ on-disk utinni-cli.exe | The read-assist shells an external exe located by path probing; the probe must resolve the deployed exe, not an arbitrary one |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-15-01 | Denial of Service (feature unavailable) | Explain effect cannot find utinni-cli.exe under injection (host-process dir is SWGEmu.exe's, not the deploy) | mitigate | Probe the Utinni inject root (two levels up from the plugin dir / inject-root marker) so a deployed utinni-cli.exe resolves (15-SMOKE B7) |
| T-15-15-02 | Tampering / Elevation | Probing arbitrary parent dirs could shell an attacker-placed utinni-cli.exe | accept | Bounded walk-up (≤3 parents) scoped to the editor's own install tree under the inject root; the read-assist is read-only (decode-iff) and the inject root is the trusted Utinni deploy the maintainer launched |
| T-15-15-SC | Tampering | npm/pip/cargo installs | accept | This plan installs ZERO packages (C# edit) |
</threat_model>

<verification>
- `TheJawaToolbox.sln` Release|x86 MSBuild exit 0
- Read confirms: LocateCli probes the inject root (two-levels-up / marker walk-up); try/catch preserved; honest CLI-absent path preserved; click-handler copy + decode-iff dispatch unchanged
- LIVE (gated to 15-18, after 15-17 ships utinni-cli.exe): Explain effect on an on-disk .prt fills the read-assist pane via utinni-cli decode-iff
</verification>

<success_criteria>
ParticleReadAssist.LocateCli resolves a deployed utinni-cli.exe under injection by probing the Utinni inject root in addition to its existing candidates, while preserving the honest CLI-absent degradation; TJT Release|x86 builds clean. B7 closes once 15-17 deploys the exe + its dependency closure into bin/Release and 15-18 confirms it live.
</success_criteria>

<output>
Create `.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-15-SUMMARY.md` when done.
</output>
