---
phase: 15-wave-2-editors-worldsnapshot-particle-presentation-residuals
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - "D:/Code/Utinni/UtinniCore/swg/misc/direct_input.cpp"
  - "D:/Code/Utinni/UtinniCore/swg/graphics/directx9.cpp"
  - "D:/Code/Utinni/UtinniCoreDotNet/UI/Controls/PanelGame.cs"
  - "D:/Code/Utinni/UtinniCore.Tests/Graphics/NoDeviceResetTests.cpp"
autonomous: true
requirements: [RESID-04]

must_haves:
  truths:
    - "SWG's exclusive-fullscreen mode switch is intercepted/suppressed to keep the embed windowed (D-12)"
    - "Utinni NEVER calls IDirect3DDevice9::Reset on SWG's device (D-13) — enforced by a regression gate"
    - "Window resize is window-side SetWindowPos; RT-space mouse mapping stays correct across resize"
  artifacts:
    - path: "D:/Code/Utinni/UtinniCore/swg/misc/direct_input.cpp"
      provides: "SetCooperativeLevel shim extended to suppress/redirect the exclusive-fullscreen switch"
      contains: "SetCooperativeLevel"
    - path: "D:/Code/Utinni/UtinniCore.Tests/Graphics/NoDeviceResetTests.cpp"
      provides: "regression gate: Utinni never initiates pDevice->Reset"
      contains: "Reset"
  key_links:
    - from: "direct_input.cpp hkSetCooperativeLevel"
      to: "suppress/redirect DISCL_EXCLUSIVE"
      via: "force DISCL_NONEXCLUSIVE or intercept the mode switch (D-12)"
      pattern: "DISCL"
    - from: "PanelGame.cs resize"
      to: "SetWindowPos (no device Reset)"
      via: "window-side reposition, windowed COPY Present self-stretch"
      pattern: "SetWindowPos"
---

<objective>
RESID-04: enumerate + fix the SWG window-resize / windowed↔fullscreen edge cases. The prime suspect (RESEARCH A4) is SWG flipping to true D3D9 exclusive fullscreen, most likely via the DirectInput `SetCooperativeLevel(DISCL_EXCLUSIVE|DISCL_FOREGROUND)` path the existing `direct_input.cpp` shim already intercepts + logs. The fix (D-12) is to suppress/redirect that mode switch to keep SWG windowed-embedded. HARD CONSTRAINT (D-13): NEVER call `IDirect3DDevice9::Reset` on SWG's device — resize the window and let the windowed COPY `Present` self-stretch.

Purpose: This is the automatable + CI-gateable half of RESID-04 — the native intercept/suppress code, the PanelGame resize path (window-side `SetWindowPos`, no Reset), the RT-space mouse mapping across resize, and a regression gate asserting Utinni never initiates `Reset`. The live edge-case-matrix enumeration + suppress-confirmation is the Tier-4 maintainer smoke folded into 15-08 (the DISCL log read on the first live run confirms A4 + the trigger context).

Output: The native suppress fix + the no-Reset regression gate, build-green. Live confirmation in 15-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-CONTEXT.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-RESEARCH.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-PATTERNS.md
@.planning/todos/pending/swg-window-resize-fullscreen-edge-cases.md
@.planning/debug/chat-open-d3d9-fullscreen.md

<interfaces>
<!-- The three self-analogs to EXTEND (do NOT re-hook) — verified anchors -->
// direct_input.cpp:92-105  hkSetCooperativeLevel(IDirectInputDevice8A*, HWND, DWORD dwFlags)
//   already logs "DI::SetCooperativeLevel: device=... hwnd=... flags=%s caller=0x%p"
//   patched at vtbl[13] (L116-125). Suppress/redirect HERE (D-12).
//   return origSetCooperativeLevel(pThis, hwnd, dwFlags);

// directx9.cpp:265 hkPresent  — windowed COPY-stretch present path
// directx9.cpp:365 hkReset    — passes through SWG's OWN Reset (Utinni must NOT initiate one)
// directx9.cpp:273-275 in-source no-Reset rationale comment; pp.Windowed = TRUE at L488

// PanelGame.cs:192-210 owned-popup reparent (WS_POPUP + GWLP_HWNDPARENT); :252 SetWindowPos reposition
//   Anti-pattern: WS_CHILD reparent breaks DirectInput (needs top-level HWND) — keep owned-popup.
//   feedback_imgui_embedded_d3d9_rt_space: RT-space mouse + DisplaySize mapping must stay correct.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suppress/redirect the exclusive-fullscreen mode switch (direct_input.cpp + PanelGame resize) — toggle-guarded</name>
  <read_first>
    - "D:/Code/Utinni/UtinniCore/swg/misc/direct_input.cpp" (L92-125 — the existing SetCooperativeLevel shim + vtbl[13] patch; the DISCL logging to read on first live run)
    - "D:/Code/Utinni/UtinniCore/swg/graphics/directx9.cpp" (L265 hkPresent windowed COPY-stretch; L273-275 no-Reset rationale; L365 hkReset pass-through; L488 pp.Windowed=TRUE)
    - "D:/Code/Utinni/UtinniCoreDotNet/UI/Controls/PanelGame.cs" (L192-210 owned-popup reparent; L252 SetWindowPos reposition — the window-side resize path, NO device Reset)
    - ".planning/todos/pending/swg-window-resize-fullscreen-edge-cases.md" (the edge-case matrix, live observations, prime-suspect analysis — the implementation spec)
    - ".planning/debug/chat-open-d3d9-fullscreen.md" (the UNRESOLVED exclusive-fullscreen detach session — prime suspect, fold in)
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-RESEARCH.md" (Pitfall 5 — triggered from login→world, not just chat-open; A4 assumption; Open Q3 suppress specifics)
  </read_first>
  <action>
    Extend (do NOT re-hook) the existing `hkSetCooperativeLevel` shim in `direct_input.cpp`: when the incoming `dwFlags` request exclusive cooperative level (`DISCL_EXCLUSIVE`), suppress/redirect to keep SWG windowed-embedded per D-12 — force `DISCL_NONEXCLUSIVE` (preserving `DISCL_FOREGROUND`/`DISCL_BACKGROUND` as appropriate so chat/input keep working) before calling `origSetCooperativeLevel`. Keep the existing DISCL-flag + caller-PC logging (it is the confirmation instrument for A4 on the first live run). Gate the suppression behind a small runtime toggle (default ON) so the maintainer can A/B it live without a rebuild, and so the deferred detached-fullscreen-with-reattach fallback (CONTEXT deferred idea) remains reachable if suppress proves wrong (Open Q3).

    In `PanelGame.cs`, ensure any resize/reposition in response to the (now-suppressed) mode change is window-side `SetWindowPos` ONLY (the L252 path) — NEVER a device Reset. Keep the owned-popup model (`WS_POPUP` + `GWLP_HWNDPARENT`); do NOT switch to `WS_CHILD` (breaks DirectInput — RESEARCH Anti-Pattern). Keep RT-space mouse mapping (DisplaySize + mouse both scaled) correct across the resize per `feedback_imgui_embedded_d3d9_rt_space` (and mind the `project_swg_cursor_clip_deadzone` right-edge interaction). Do NOT touch `hkReset` to add a Utinni-initiated Reset; it stays a pass-through of SWG's own Reset (D-13).

    If this work regenerates `Generated/UtinniCore.cs`, `git checkout --` it — never commit (`project_utinnicore_cs_regen_churn`). Mind the `Debug/` gitignore trap for any new source (`project_ci_debug_gitignore_trap`).
  </action>
  <verify>
    <automated>cmd /c "\"%ProgramFiles%\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe\" \"D:/Code/Utinni/Utinni.sln\" /t:UtinniCore /p:Configuration=Release /p:Platform=x86 /v:minimal"</automated>
  </verify>
  <acceptance_criteria>
    - `hkSetCooperativeLevel` suppresses/redirects `DISCL_EXCLUSIVE` (grep `DISCL_NONEXCLUSIVE` near the shim) behind a default-ON runtime toggle.
    - The existing DISCL-flag + caller-PC logging is retained (grep the log string still present).
    - `PanelGame.cs` resize is `SetWindowPos`-only; NO Utinni-initiated `Reset` call introduced anywhere in the modified files (grep `Reset(` returns only the existing `hkReset` SWG-own pass-through).
    - No `WS_CHILD` reparent introduced (grep returns the owned-popup `WS_POPUP`/`GWLP_HWNDPARENT` shape unchanged).
    - UtinniCore builds Release|x86 green via VS2026 MSBuild; `Generated/UtinniCore.cs` unchanged.
  </acceptance_criteria>
  <done>The DirectInput exclusive-fullscreen switch is suppressed (toggle-guarded) to keep SWG windowed-embedded, resize is window-side only, and no Utinni-initiated device Reset is introduced.</done>
</task>

<task type="auto">
  <name>Task 2: No-device-Reset regression gate (D-13 enforcement)</name>
  <read_first>
    - "D:/Code/Utinni/UtinniCore/swg/graphics/directx9.cpp" (confirm the ONLY Reset reference is the SWG-own pass-through in hkReset, not a Utinni-initiated call)
    - "D:/Code/Utinni/UtinniCore.Tests" (an existing Catch2 test for project layout + the grep-gate precedent — e.g. the PreservationAudit-style grep facts)
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-RESEARCH.md" (Validation map: "hkPresent/reparent code asserts no Utinni-initiated Reset"; feedback_d3d9_reset_third_party)
  </read_first>
  <action>
    Add `UtinniCore.Tests/Graphics/NoDeviceResetTests.cpp` (Catch2, tag `[resid04]`) — a regression gate asserting Utinni never INITIATES `pDevice->Reset` on SWG's device (D-13, `feedback_d3d9_reset_third_party`). Implement as a source grep-gate over `directx9.cpp` + `PanelGame.cs` (and the RESID-04-touched files) that counts Utinni-initiated device `->Reset(` / `.Reset(` calls and asserts ZERO, EXCLUDING the legitimate `hkReset` pass-through of SWG's own Reset. Apply grep-gate hygiene: strip comment lines before counting (so the no-Reset RATIONALE comments do not self-invalidate the gate — `feedback_gsd_grep_gate_hygiene`); a bare unfiltered `== 0` gate is forbidden. If a comment carries the token `Reset` in a way that trips the gate, reword it while keeping the historical name in a non-gated doc.
  </action>
  <verify>
    <automated>cmd /c "\"%ProgramFiles%\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe\" \"D:/Code/Utinni/Utinni.sln\" /t:UtinniCore_Tests /p:Configuration=Release /p:Platform=x86 /v:minimal && \"D:/Code/Utinni/bin/Release/UtinniCore.Tests.exe\" [resid04]"</automated>
  </verify>
  <acceptance_criteria>
    - `NoDeviceResetTests.cpp` exists with a Catch2 TEST_CASE tagged `[resid04]`.
    - The gate strips comment lines before counting (grep the test for the comment-filter step); a bare unfiltered `== 0` is NOT used.
    - The gate asserts ZERO Utinni-initiated device Reset calls (excluding the `hkReset` SWG-own pass-through) and PASSES on the current tree.
    - `UtinniCore.Tests.exe [resid04]` runs green via VS2026 MSBuild + direct exe invocation.
  </acceptance_criteria>
  <done>A Catch2 regression gate enforces the D-13 no-Reset constraint and passes; reverting the suppress fix would not reintroduce a device Reset undetected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SWG D3D9/DirectInput runtime → Utinni hooks (third-party process) | Utinni intercepts SWG's own device/input calls under injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06 | Denial of Service | RESID-04 device Reset crashing the process | mitigate | Hard no-`IDirect3DDevice9::Reset` constraint (D-13); window-resize-only via `SetWindowPos`; enforced by the `NoDeviceResetTests` regression gate. Suppression is toggle-guarded so the deferred fallback stays reachable |
| T-15-06b | Tampering | DI cooperative-level suppression breaking chat/input | mitigate | Force `DISCL_NONEXCLUSIVE` while preserving FOREGROUND/BACKGROUND; retain DISCL logging to confirm input still routes; live A/B via the runtime toggle in 15-08 |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | This plan installs ZERO packages; no install task present |
</threat_model>

<verification>
- UtinniCore + UtinniCore.Tests build Release|x86 green via VS2026 MSBuild
- `UtinniCore.Tests.exe [resid04]` green (no-Reset gate)
- Grep: DISCL_NONEXCLUSIVE suppression present + toggle-guarded; no Utinni-initiated Reset; owned-popup model unchanged; Generated/UtinniCore.cs unchanged
</verification>

<success_criteria>
The exclusive-fullscreen mode switch is suppressed to keep SWG windowed-embedded (D-12), resize is window-side only with correct RT-space mouse mapping, and the D-13 no-Reset constraint is enforced by a green regression gate. The live edge-case-matrix confirmation is the Tier-4 smoke in 15-08.
</success_criteria>

<output>
Create `.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-05-SUMMARY.md` when done.
</output>
