---
phase: 15-wave-2-editors-worldsnapshot-particle-presentation-residuals
plan: 17
type: execute
wave: 6
depends_on: ["15-12", "15-13", "15-14", "15-15", "15-16"]
files_modified:
  - "D:/Code/Utinni/bin/Release/"
autonomous: true
gap_closure: true
requirements: [PROD-W2-WS, PROD-W2-PRT, RESID-03, RESID-04]

must_haves:
  truths:
    - "The full automated gate is green after the wave-5 gap fixes: Utinni Release|x86, TheJawaToolbox Release|x86, all managed + native suites pass with zero regression (incl. the [resid04] no-Reset gate and the new InjectedAssemblyResolver / WorldSnapshotCommandGuard facts)"
    - "The deployable injection build is reassembled at D:/Code/Utinni/bin/Release/ AND now ships the two previously-missing files: netstandard.dll (the B5 façade, next to UtinniCoreDotNet.dll) and utinni-cli.exe + its net472 dependency closure (the B7 read-assist exe)"
    - "The DEPLOYED binaries are content-verified to carry every wave-5 fix (the deployed UtinniCoreDotNet.dll defines InjectedAssemblyResolver; WorldSnapshotCommands.cs carries no [A9-diag]; netstandard.dll + utinni-cli.exe are present) — not just freshly-timestamped — so the maintainer cannot be handed a stale build"
  artifacts:
    - path: "D:/Code/Utinni/bin/Release/netstandard.dll"
      provides: "The netstandard2.0 façade the PathContainment loose-override save tier needs under injection (B5)"
    - path: "D:/Code/Utinni/bin/Release/utinni-cli.exe"
      provides: "The decode-iff read-assist exe the Particle Explain effect shells (B7)"
    - path: "D:/Code/Utinni/bin/Release/UtinniCoreDotNet.dll"
      provides: "Fixed core with the AssemblyResolve handler + finalized diagnostic-free A9 revert"
    - path: "D:/Code/Utinni/bin/Release/Plugins/TheJawaToolbox/TheJawaToolboxDotNet.dll"
      provides: "TJT with the inject-root LocateCli probe + the B4/B5/B6 polish"
  key_links:
    - from: "reassembled bin/Release layout (with netstandard.dll + utinni-cli.exe)"
      to: "15-18 live re-smoke (B5–B8, Checklist C full incl C3, Checklist D)"
      via: "maintainer injects the fixed+complete build and re-runs the still-open smoke"
      pattern: "Release"
---

<objective>
Reassemble the deployable injection build (Release|x86) after the wave-5 gap fixes (15-12 netstandard resolver, 15-13 C3 embed re-assert, 15-14 finalized A9 revert, 15-15 LocateCli probe, 15-16 managed polish) so plan 15-18's maintainer live-SWG re-smoke runs against the FIXED + COMPLETE binaries. Re-run the full automated gate to confirm zero regression, assemble the `D:/Code/Utinni/bin/Release/` layout, ADD the two previously-missing files (`netstandard.dll` façade for B5; `utinni-cli.exe` + its net472 dependency closure for B7), content-verify the deployed artifacts carry every fix, and hand the build to 15-18.

Purpose: the gap-closure scope ends with the injection build reassembled AND completed (the two missing files are the headline B5/B7 deploy gaps). This plan is the build/handoff seam, not new feature work. The hard requirement (mirrors 15-11): the maintainer is NOT handed a stale or incomplete build — so the deployed binaries are content-verified for the presence of the fix AND the presence of the two new files, not merely an mtime check.

Output: a green full gate + the reassembled, now-complete `bin/Release/` injection layout (content-verified) ready for the 15-18 live re-smoke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-SMOKE.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-HANDOFF-GAPS.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-11-PLAN.md

<interfaces>
<!-- The 15-08 Task 1 assembled-build layout (15-SMOKE "Assembled injection build") — EXTENDED here with
  the two new files:
  D:/Code/Utinni/bin/Release/
    Launcher.exe
    UtinniCore.dll
    UtinniCoreDotNet.dll
    netstandard.dll                 <-- NEW (B5 façade; 15-12 csproj deploy item should place it here)
    UtinniCoreDotNet.PathContainment.dll   (already shipped)
    utinni-cli.exe + its net472 dep DLLs   <-- NEW (B7; copy the whole Utinni.Cli/bin/Release/net472/ closure)
    Plugins/TheJawaToolbox/
      TheJawaToolbox.dll            (C++ plugin)
      TheJawaToolboxDotNet.dll      (managed TJT)
      Resources/  input.ini  settings.ini -->

<!-- The green-gate baseline to match/exceed (from 15-SMOKE Build/Test State, now + wave-5 facts):
  Utinni.sln Release|x86 (VS2026 MSBuild v145) -> exit 0
  TheJawaToolbox.sln Release|x86 -> exit 0
  UtinniCoreDotNet.Tests (Release) -> >= 697 + InjectedAssemblyResolver + node-only guard facts / 0 failed
  Utinni.Cli.Tests (Release) -> 249 pass / 2 skip
  Utinni.Mcp.Tests (net10, Release) -> 77 pass
  Native UtinniCore.Tests.exe (full) -> 84 assertions / 27 cases
  Native UtinniCore.Tests.exe [resid04] -> 8 assertions / 1 case  (the no-Reset gate — must stay green after 15-13) -->

<!-- Content-of-fix markers the deployed artifacts MUST carry:
  - UtinniCoreDotNet.dll defines a type named `InjectedAssemblyResolver` (15-12) AND still defines
    `WorldSnapshotCommandGuard` + exposes `UndoRedoManager.Clear` (15-09/15-10 carry-over)
  - bin/Release/netstandard.dll EXISTS (B5)
  - bin/Release/utinni-cli.exe EXISTS (B7)
  - the source WorldSnapshotCommands.cs carries NO `[A9-diag]` (15-14) — verified pre-build at source;
    the deployed UtinniCoreDotNet.dll should contain no `A9-diag` string either (strings/grep)
  - Plugins/TheJawaToolbox/TheJawaToolboxDotNet.dll exists (paired rebuild against 15-16 polish + 15-15 probe)
  PowerShell metadata/strings check over the deployed PEs (no UtinniCore native dep needed). -->

<!-- Build reality (15-HANDOFF-GAPS): VS2026 MSBuild at
  D:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe; dotnet build
  FAILS on .resx -> use MSBuild; run xUnit via `dotnet test --no-build -c Release`; revert
  Generated/UtinniCore.cs; worktrees OFF (inline on master). utinni-cli is built by Utinni.sln to
  Utinni.Cli/bin/Release/net472/. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full Release gate + reassemble bin/Release with netstandard.dll + utinni-cli.exe (content-verified)</name>
  <files>D:/Code/Utinni/bin/Release/ (assembled layout)</files>
  <read_first>
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-SMOKE.md" (the 'Assembled injection build' layout + the B5/B7 deploy gaps the two new files close + the Build/Test State baseline)
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-HANDOFF-GAPS.md" (build reality: VS2026 MSBuild path, dotnet build fails on .resx, Generated revert, worktrees OFF, init.execute-phase positional arg)
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-11-PLAN.md" (the prior reassembly + content-verify pattern to mirror — extend it with the two new file checks + the InjectedAssemblyResolver marker)
  </read_first>
  <action>
    1. Build Utinni Release|x86 with VS2026 MSBuild (`Utinni.sln` `/p:Configuration=Release /p:Platform=x86`) — this also produces `Utinni.Cli/bin/Release/net472/utinni-cli.exe` + its dependency closure and (via the 15-12 csproj item) drops `netstandard.dll` into the UtinniCoreDotNet output. Then build UtinniPlugins `TheJawaToolbox.sln` Release|x86 (paired rebuild — picks up 15-15 LocateCli + 15-16 polish). Use VS2026 MSBuild, NOT `dotnet build`.

    2. Run the full automated gate (match/exceed the baseline; the wave-5 facts are additive):
       - `dotnet test UtinniCoreDotNet.Tests --no-build -c Release` (>= 697 + InjectedAssemblyResolver + node-only guard facts, 0 failed)
       - `dotnet test Utinni.Cli.Tests --no-build -c Release` (249 pass / 2 skip)
       - `dotnet test Utinni.Mcp.Tests --no-build -c Release` (77 pass)
       - Native `UtinniCore.Tests.exe` full (84/27) + `[resid04]` (8/1 — the no-Reset gate MUST stay green after the 15-13 watchdog)
       Any failure is a regression — stop and surface it; do NOT reassemble a red build.

    3. `git checkout -- UtinniCoreDotNet/Generated/UtinniCore.cs`.

    4. Reassemble the `D:/Code/Utinni/bin/Release/` injection layout per the 15-SMOKE "Assembled injection build" block, EXTENDED with the two new files:
       - `Launcher.exe`, `UtinniCore.dll`, `UtinniCoreDotNet.dll`, `UtinniCoreDotNet.PathContainment.dll`, and `Plugins/TheJawaToolbox/{TheJawaToolbox.dll, TheJawaToolboxDotNet.dll, Resources/, input.ini, settings.ini}`.
       - NEW: `netstandard.dll` next to `UtinniCoreDotNet.dll` (from the 15-12 csproj deploy item / `$(SystemRoot)\Microsoft.NET\Framework\v4.0.30319\netstandard.dll` if the build did not already place it).
       - NEW: `utinni-cli.exe` AND its full net472 dependency closure (copy the entire `Utinni.Cli/bin/Release/net472/` payload — the exe alone will not run without its referenced DLLs/`.deps`) into `bin/Release/` (the inject root, where 15-15's LocateCli now probes).

    5. CONTENT-VERIFY the deployed artifacts (anti-stale gate — not mtime alone) via PowerShell:
       - the deployed `bin/Release/UtinniCoreDotNet.dll` defines a type named `InjectedAssemblyResolver` (15-12) AND still defines `WorldSnapshotCommandGuard` AND exposes `UndoRedoManager.Clear` (carry-over) — via `System.Reflection.Metadata.MetadataReader` or a strings/ildasm grep;
       - the deployed `UtinniCoreDotNet.dll` contains NO `A9-diag` string (proof 15-14 stripped the diagnostics);
       - `bin/Release/netstandard.dll` EXISTS (B5);
       - `bin/Release/utinni-cli.exe` EXISTS (B7);
       - `bin/Release/Plugins/TheJawaToolbox/TheJawaToolboxDotNet.dll` EXISTS.
       If ANY content/file marker is absent, the build is stale/incomplete — stop and rebuild/redeploy; do NOT hand off.
  </action>
  <verify>
    <automated>cmd /c "\"%ProgramFiles%\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe\" \"D:/Code/Utinni/Utinni.sln\" /p:Configuration=Release /p:Platform=x86 /v:minimal && \"%ProgramFiles%\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe\" \"D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolbox.sln\" /p:Configuration=Release /p:Platform=x86 /v:minimal && dotnet test \"D:/Code/Utinni/UtinniCoreDotNet.Tests/UtinniCoreDotNet.Tests.csproj\" --no-build -c Release && \"D:/Code/Utinni/UtinniCore.Tests/bin/Release/UtinniCore.Tests.exe\" [resid04] && powershell -NoProfile -Command \"$c=[IO.File]::ReadAllText('D:/Code/Utinni/bin/Release/UtinniCoreDotNet.dll',[Text.Encoding]::Latin1); $okCore=($c -match 'InjectedAssemblyResolver') -and ($c -match 'WorldSnapshotCommandGuard') -and ($c -match 'Clear') -and ($c -notmatch 'A9-diag'); $files=(Test-Path 'D:/Code/Utinni/bin/Release/netstandard.dll') -and (Test-Path 'D:/Code/Utinni/bin/Release/utinni-cli.exe') -and (Test-Path 'D:/Code/Utinni/bin/Release/Plugins/TheJawaToolbox/TheJawaToolboxDotNet.dll'); if($okCore -and $files){exit 0}else{Write-Error 'deployed build missing fix markers or the netstandard.dll/utinni-cli.exe files -- stale/incomplete, do not hand off'; exit 1}\""</automated>
  </verify>
  <acceptance_criteria>
    - `Utinni.sln` Release|x86 and `TheJawaToolbox.sln` Release|x86 both build (MSBuild exit 0).
    - `UtinniCoreDotNet.Tests` Release passes with >= 697 + the new InjectedAssemblyResolver + node-only guard facts, 0 failed; `Utinni.Cli.Tests` 249/2-skip; `Utinni.Mcp.Tests` 77; native full 84/27 + `[resid04]` 8/1 (no regression; the no-Reset gate stays green after 15-13).
    - `bin/Release/` contains the full base layout PLUS `netstandard.dll` (next to UtinniCoreDotNet.dll) AND `utinni-cli.exe` + its net472 dependency DLLs (Test-Path each).
    - CONTENT-OF-FIX (binding gate, not mtime): the deployed `UtinniCoreDotNet.dll` contains `InjectedAssemblyResolver`, `WorldSnapshotCommandGuard`, `Clear`, and does NOT contain `A9-diag`; `netstandard.dll`, `utinni-cli.exe`, and the deployed `TheJawaToolboxDotNet.dll` exist. Any missing marker/file fails the gate.
    - `Generated/UtinniCore.cs` reverted (git status clean for that file).
  </acceptance_criteria>
  <done>Full Release gate green with zero regression (incl. the [resid04] no-Reset gate); the fixed injection build is reassembled at bin/Release AND completed with netstandard.dll + utinni-cli.exe, content-verified (deployed DLL carries InjectedAssemblyResolver/WorldSnapshotCommandGuard/Clear, no A9-diag; the two new files present) ready for the 15-18 live re-smoke; Generated churn reverted.</done>
</task>

<task type="auto">
  <name>Task 2: Record the wave-5 gap-closure outcome + re-smoke pointer in 15-SMOKE.md</name>
  <files>D:/Code/Utinni/.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-SMOKE.md</files>
  <read_first>
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-SMOKE.md" (the still-open B5/B7/C3 defect records + the Maintainer Sign-Off block — to ANNOTATE, not overwrite)
  </read_first>
  <action>
    Append a dated gap-closure note to `15-SMOKE.md` (annotate, do NOT rewrite the existing defect records). State that:
    - B5 (netstandard façade / loose-override save break) fixed in 15-12 (inject-root AssemblyResolve handler + netstandard.dll shipped) — re-enables saves for every editor + unblocks Checklist D.
    - C3 (windowed→fullscreen embed detach + input lockup) fixed in 15-13 (window-side watchdog re-asserts the owned-popup embed; NO device Reset; [resid04] gate green).
    - A9 revert finalized in 15-14 ([A9-diag] logging stripped; live-node-by-id + optional-obj revert).
    - B7 (utinni-cli not shipped / LocateCli probe) fixed in 15-15 (inject-root probe) + 15-17 (utinni-cli.exe + closure shipped).
    - B4/B5 grid-rebind, B6 no-hook preview tooltip, A7 delete-confirm candor + BulkDelete DetailLevelChanged folded into 15-16.
    - The fixed + COMPLETE injection build is reassembled at `D:/Code/Utinni/bin/Release/` and content-verified (incl. netstandard.dll + utinni-cli.exe).
    - The remaining live re-smoke (B5–B8 now that save works, Checklist C full matrix INCLUDING the C3 fullscreen re-verify, Checklist D `.stf`/`.ot` saves) is the maintainer's continuation under plan 15-18 — record results back in THIS 15-SMOKE.md.
    Leave the Maintainer Sign-Off block untouched (the smoke is still the gate). This plan does NOT sign off the phase.
  </action>
  <verify>
    <automated>cmd /c "findstr /C:\"15-12\" /C:\"15-13\" /C:\"15-14\" /C:\"15-15\" /C:\"15-16\" /C:\"15-17\" \"D:/Code/Utinni/.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-SMOKE.md\""</automated>
  </verify>
  <acceptance_criteria>
    - `15-SMOKE.md` contains a dated note referencing 15-12 through 15-17 (findstr returns matches for all).
    - The note states the fixed+complete build is reassembled at `bin/Release/` (content-verified, incl. netstandard.dll + utinni-cli.exe) and that the live re-smoke (B5–B8, Checklist C incl C3, Checklist D) lands back in 15-SMOKE.md under 15-18.
    - The original B5/B7/C3 defect records + the A9 crash evidence are still present (grep `0xC0000005` + the B5 "Could not load file or assembly" string still return their original lines).
    - The Maintainer Sign-Off block is unchanged (still unsigned).
  </acceptance_criteria>
  <done>15-SMOKE.md records the wave-5 gap-closure fixes + the reassembled, content-verified, COMPLETE build (netstandard.dll + utinni-cli.exe present) + the 15-18 re-smoke pointer, without erasing the original defect evidence or pre-signing the maintainer gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Built binaries + deploy files → injection layout | A stale DLL or a missing netstandard.dll/utinni-cli.exe would re-ship the B5/B7 breaks; the layout must carry the freshly-built post-fix binaries AND the two new files, content-verified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-17-01 | Tampering / Integrity | bin/Release carrying a stale pre-fix DLL or missing netstandard.dll/utinni-cli.exe | mitigate | Content-verify the deployed DLL carries InjectedAssemblyResolver/WorldSnapshotCommandGuard/Clear + no A9-diag, AND Test-Path the two new files, before handoff — mtime alone is insufficient |
| T-15-17-02 | Denial of Service | utinni-cli.exe shipped without its net472 dependency closure (exe present but won't run) | mitigate | Copy the WHOLE Utinni.Cli/bin/Release/net472/ payload, not just the exe; the live B7 re-smoke is the end-to-end proof |
| T-15-17-SC | Tampering | npm/pip/cargo installs | accept | This plan installs ZERO packages (build + file assembly + doc note) |
</threat_model>

<verification>
- `Utinni.sln` + `TheJawaToolbox.sln` Release|x86 MSBuild exit 0
- All managed + native suites green incl. `[resid04]` (no regression vs baseline)
- `bin/Release/` complete: base layout + netstandard.dll + utinni-cli.exe (+ closure); deployed DLLs content-verified (InjectedAssemblyResolver/WorldSnapshotCommandGuard/Clear present, no A9-diag)
- `15-SMOKE.md` annotated with the wave-5 note + 15-18 re-smoke pointer; original defect evidence intact
- `Generated/UtinniCore.cs` reverted
</verification>

<success_criteria>
The full automated gate is green after the wave-5 fixes with zero regression; the deployable injection build is reassembled at `bin/Release/` AND completed with the two previously-missing files (netstandard.dll + utinni-cli.exe + closure), content-verified to carry every fix; 15-SMOKE.md records the fixes and points the live re-smoke to 15-18. The maintainer can resume the still-open smoke against the fixed, complete build.
</success_criteria>

<output>
Create `.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-17-SUMMARY.md` when done.
</output>
