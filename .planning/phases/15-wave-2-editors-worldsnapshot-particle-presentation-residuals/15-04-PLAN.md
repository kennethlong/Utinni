---
phase: 15-wave-2-editors-worldsnapshot-particle-presentation-residuals
plan: 04
type: execute
wave: 2
depends_on: [15-02]
files_modified:
  - "D:/Code/Utinni/Utinni.Cli/Commands/DecodeIffCommand.cs"
  - "D:/Code/Utinni/Utinni.Cli/Commands/RoundtripParticleCommand.cs"
  - "D:/Code/Utinni/Utinni.Cli/Program.cs"
  - "D:/Code/Utinni/Utinni.Cli.Tests/Commands/RoundtripParticleCommandTests.cs"
  - "D:/Code/Utinni/Utinni.Cli.Tests/Commands/DecodeParticleDispatchTests.cs"
  - "D:/Code/Utinni/Utinni.Mcp/Tools/ReadTools.cs"
  - "D:/Code/Utinni/Utinni.Mcp.Tests/ParticleReadToolTests.cs"
autonomous: true
requirements: [PROD-W2-PRT]

must_haves:
  truths:
    - "utinni-cli decodes a .prt to JSON (decode-iff auto-dispatches PEFT) with the standard envelope + exit-code taxonomy"
    - "utinni-cli roundtrip-particle is a byte-exact round-trip gate over a synth/extracted .prt fixture"
    - "an MCP read tool resolves the path under the pinned root and dispatches the verb by exit code with ZERO format logic (D-06), and the surface is read-assist ONLY — read/summarize, never a write/mutate verb (D-07)"
  artifacts:
    - path: "D:/Code/Utinni/Utinni.Cli/Commands/RoundtripParticleCommand.cs"
      provides: "byte-exact .prt round-trip CLI verb"
      contains: "JsonOutput"
    - path: "D:/Code/Utinni/Utinni.Mcp/Tools/ReadTools.cs"
      provides: "particle MCP read/summarize tool (thin dispatch)"
      contains: "ResolvedRoot"
  key_links:
    - from: "DecodeIffCommand / RoundtripParticleCommand"
      to: "Formats/Particle codec"
      via: "ParticleEffectDocument.FromIff + ParticleEffectWriter"
      pattern: "ParticleEffect"
    - from: "MCP particle read tool"
      to: "Utinni.Cli verb"
      via: "ResolvedRoot.Resolve → cli.RunAsync → CliResultMapper"
      pattern: "cli\\.RunAsync"
---

<objective>
Expose the new `.prt` codec as headless read tools (PROD-W2-PRT, CLI/MCP half) so an MCP client (Claude Desktop/Code) can read-assist against headless Utinni (D-08 surface a) and the in-TJT `Explain effect` button (15-06) reuses the SAME path. Adds: `.prt`/PEFT decode (extend `decode-iff` auto-dispatch), a `roundtrip-particle` byte-exact golden gate, and one thin MCP read tool. The surface is read-assist ONLY (D-07) — read/summarize verbs, never a prompt-to-mutate / write verb.

Purpose: Per Phase-14 rule (D-06), format logic lives in `UtinniCoreDotNet`, the CLI wraps it, and the MCP server dispatches by exit code with ZERO format logic. This plan keeps that strict separation and is the read-assist path both the MCP client AND the in-app button reuse — no independent AI/format path.

Output: `.prt` read verbs + golden round-trip + one MCP read tool, all CI-tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-CONTEXT.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-PATTERNS.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-02-SUMMARY.md

<interfaces>
<!-- CLI dispatch past the 16-verb cap is ALREADY solved (Program.cs:43-101). Add the option type to
     the Type[] (L48-69) and a case to Dispatch (L80-101) — do NOT re-solve the cap. -->
private static int Dispatch(object opts) {
    switch (opts) {
        case Commands.DecodeIffOptions o: return Commands.DecodeIffCommand.Run(o);
        // add: case Commands.RoundtripParticleOptions o: return Commands.RoundtripParticleCommand.Run(o);
    }
}

<!-- CLI envelope + exit-code taxonomy to copy from RoundtripOtCommand.cs -->
// JsonOutput.EmitSuccess / EmitError; exit codes:
//   0 success; 1 UsageError; 2 IffParseException/DecoderException/IOException; 3 FileNotFound.
//   Generic System.Exception is intentionally NOT caught.

<!-- decode-iff already auto-dispatches typed reads by root FORM; PEFT is a NEW branch (preferred over a
     standalone decode verb, per PATTERNS.md §MCP note + DecodeIffCommand) -->

<!-- MCP thin read tool — copy ReadTools.DecodeIff (L84-95) EXACTLY; ZERO format logic -->
[McpServerTool(Name = "...", ReadOnly = true, Idempotent = true)]
public static async Task<CallToolResult> Decode...(ResolvedRoot root, CliDispatcher cli,
    [Description(PathParamDescription)] string relativePath) {
    string abs = root.Resolve(relativePath);                 // throws on escape → SDK tool error
    CliInvocationResult r = await cli.RunAsync("<verb>", new[] { abs }).ConfigureAwait(false);
    return CliResultMapper.ToCallToolResult(r);              // verbatim envelope pass-through
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: decode-iff PEFT dispatch + roundtrip-particle byte-exact golden verb</name>
  <read_first>
    - "D:/Code/Utinni/Utinni.Cli/Commands/RoundtripOtCommand.cs" (READ IN FULL — the [Verb]/[Value]/[Option] options shape L38-55, JsonOutput envelope, exit-code taxonomy L70-72/174-186, ParseOt entry L190-199)
    - "D:/Code/Utinni/Utinni.Cli/Commands/DecodeIffCommand.cs" (the root-FORM auto-dispatch this task extends with a PEFT branch)
    - "D:/Code/Utinni/Utinni.Cli/Program.cs" (L43-101 — the Type[] + Dispatch switch; cap already solved)
    - "D:/Code/Utinni/UtinniCoreDotNet/Formats/Particle/ParticleEffectDocument.cs" + "ParticleEffectWriter.cs" (the codec from 15-02 these verbs wrap)
    - "D:/Code/Utinni/UtinniCoreDotNet.Tests/Formats/Particle/ParticleFixtureBuilder.cs" (reuse to synth the CLI golden fixture)
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-PATTERNS.md" (§RoundtripParticleCommand; §CLI JSON-envelope + exit-code taxonomy)
  </read_first>
  <behavior>
    - Test 1: `decode-iff <synth.prt>` returns exit 0 + a JSON envelope whose root FORM is PEFT and lists emitter-group/emitter counts (auto-dispatch routed to the codec).
    - Test 2: `roundtrip-particle <synth.prt>` with no edit returns exit 0 + reports byte-identical (the round-trip gate).
    - Test 3: `roundtrip-particle` over a degraded (unknown-EMTR-version) fixture returns exit 0 + byte-identical (raw-preserve held through the verb).
    - Test 4: a missing file returns exit 3 (FileNotFound); a malformed non-IFF input returns exit 2 (parse/decoder); a usage error returns exit 1.
    - Test 5: dispatch wiring — the new option type is reachable through `Program.Dispatch` (the case fires).
  </behavior>
  <action>
    Extend `DecodeIffCommand` so a `FORM PEFT` root auto-dispatches into the 15-02 codec (`ParticleEffectDocument.FromIff` → JSON summary: emitter-group count, per-group emitter count, timing, `{raw} raw-preserved` count), mirroring how it already routes other typed reads by root FORM (preferred over a standalone decode verb per PATTERNS.md §MCP note). Create `RoundtripParticleCommand.cs` cloned from `RoundtripOtCommand.cs`: same `[Verb("roundtrip-particle")]` + `[Value]/[Option]` shape, `JsonOutput.EmitSuccess/EmitError`, exit-code taxonomy (0/1/2/3; generic Exception NOT caught), parse entry copied from `ParseOt` but using `ParticleEffectDocument`/`ParticleEffectWriter` — the verb re-reads + re-writes and asserts byte-identity on untouched data (the byte-exact gate, the typed-`.prt` analog of `roundtrip-ot`/`roundtrip-tab`). Mind the 16-verb CommandLineParser cap is HISTORICAL (already solved) — just add the option type to the `Type[]` (Program.cs L48-69) and a `case` to `Dispatch` (L80-101).

    Add tests in `Utinni.Cli.Tests`: `RoundtripParticleCommandTests.cs` (byte-exact + degraded + exit-code taxonomy) and `DecodeParticleDispatchTests.cs` (PEFT auto-dispatch + Program.Dispatch wiring). Commit a synth golden fixture via `ParticleFixtureBuilder` (no real `.prt` fixtures exist today; extract-from-`.tre` is the documented alternate). Filter prefix `Particle`. If dispatch help/no-args goldens exist, refresh them for the new verb (the documented Rule-1 deviation precedent from 11-02/14).
  </action>
  <verify>
    <automated>dotnet test Utinni.Cli.Tests --no-build --filter Particle</automated>
  </verify>
  <acceptance_criteria>
    - `decode-iff` over a synth PEFT fixture returns exit 0 + a JSON envelope reporting PEFT + counts (Test 1).
    - `roundtrip-particle` returns exit 0 + byte-identical for both a clean and a degraded fixture (Tests 2-3).
    - Exit-code taxonomy holds: 3 FileNotFound / 2 parse-or-decoder / 1 usage (Test 4); generic Exception is NOT caught (grep the command — no broad `catch (Exception)`).
    - The new option type is in `Program.cs`'s `Type[]` AND its `Dispatch` switch (grep both); the verb fires through Dispatch (Test 5).
    - A synth golden `.prt` fixture is committed under `Utinni.Cli.Tests`.
    - `dotnet test Utinni.Cli.Tests --no-build --filter Particle` passes with ≥5 facts.
  </acceptance_criteria>
  <done>`.prt` reads + a byte-exact round-trip gate ship as CLI verbs with the standard envelope + exit-code taxonomy, golden-tested over a synth fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MCP particle read/summarize tool (thin dispatch-by-exit-code, zero format logic)</name>
  <read_first>
    - "D:/Code/Utinni/Utinni.Mcp/Tools/ReadTools.cs" (L84-95 — the DecodeIff tool to clone EXACTLY; the class doc-comment on why decode_iff is the single typed-read tool)
    - "D:/Code/Utinni/Utinni.Mcp/Tools/CliResultMapper.cs" (ToCallToolResult — verbatim envelope pass-through)
    - "D:/Code/Utinni/Utinni.Mcp.Tests" (an existing read-tool dispatch test for the test-stub seam / project layout)
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-PATTERNS.md" (§Utinni.Mcp/Tools/ReadTools.cs D-08; §MCP thin dispatch-by-exit-code)
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-CONTEXT.md" (D-06 codec out of MCP; D-07 AI read-assist only; D-08 same read path reused by the in-app button)
  </read_first>
  <behavior>
    - Test 1: the particle read tool resolves a relative path under the pinned `ResolvedRoot` and dispatches the verb (assert the dispatched verb name + that `ResolvedRoot.Resolve` was called).
    - Test 2: a path-escape attempt (`..\..\outside`) is rejected at the MCP boundary BEFORE any CLI spawn (no `cli.RunAsync` reached).
    - Test 3: the tool passes the CLI envelope through verbatim via `CliResultMapper` and decides nothing on the bytes (exit-code-only).
  </behavior>
  <action>
    Add ONE read tool to `ReadTools.cs` for `.prt` — either (preferred) rely on the existing `decode_iff` MCP tool now that the CLI auto-dispatches PEFT (then this task only adds a Particle-specific dispatch test + a doc note), OR add a dedicated `summarize_particle` (ReadOnly=true, Idempotent=true) tool if a distinct AI read-assist summary is wanted for D-08. The tool is read-assist ONLY (D-07) — `ReadOnly = true`, it read/summarizes and exposes NO write/mutate verb. Whichever: clone `DecodeIff` (L84-95) EXACTLY — `root.Resolve(relativePath)` (fail-closed path containment, throws on escape) → `cli.RunAsync("<verb>", new[]{abs})` → `CliResultMapper.ToCallToolResult(r)`. ZERO format logic in the server (D-06). Add `ParticleReadToolTests.cs` in `Utinni.Mcp.Tests` (filter `Particle`) covering the 3 behaviors via the existing test-stub `CliDispatcher` seam (path-escape blocked with ZERO CLI spawns).
  </action>
  <verify>
    <automated>dotnet test Utinni.Mcp.Tests --no-build --filter Particle</automated>
  </verify>
  <acceptance_criteria>
    - The particle read path resolves under `ResolvedRoot` (grep `root.Resolve`) and dispatches via `cli.RunAsync` → `CliResultMapper.ToCallToolResult` (grep both).
    - NO format/codec logic appears in `Utinni.Mcp` (grep `ParticleEffect`/`Formats.Particle` in `Utinni.Mcp/` returns NONE — D-06).
    - The tool is `ReadOnly = true` and exposes no write/mutate verb (D-07 read-assist only).
    - A path-escape attempt is rejected before any `cli.RunAsync` (Test 2 asserts zero spawns).
    - `dotnet test Utinni.Mcp.Tests --no-build --filter Particle` passes with ≥3 facts.
  </acceptance_criteria>
  <done>An MCP read tool surfaces `.prt` read/summarize through pure exit-code dispatch with fail-closed path containment and zero format logic; the same path the in-app button reuses (D-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP agent → server → CLI | Agent-supplied relative path crosses into the file system via the MCP tool |
| Untrusted `.prt` → CLI verb → codec | The CLI verb parses untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering (path escape) | MCP particle read tool | mitigate | `ResolvedRoot.Resolve` fail-closed path containment at the MCP boundary BEFORE any CLI spawn (Phase-14 V4 access control); path-escape test asserts zero spawns |
| T-15-01 | Tampering / DoS | CLI verb parsing untrusted `.prt` | mitigate | Verb wraps the 15-02 codec (truncation-safe `IffPayloadCursor` + count guards); exit 2 on parse/decoder failure, never OOB |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | This plan installs ZERO packages; no install task present |
</threat_model>

<verification>
- `dotnet test Utinni.Cli.Tests --no-build --filter Particle` + `dotnet test Utinni.Mcp.Tests --no-build --filter Particle` green
- Grep: no format logic in Utinni.Mcp; generic Exception not caught in the CLI verb; option type in both Type[] and Dispatch
</verification>

<success_criteria>
`.prt` read-assist is available headless (CLI decode + byte-exact round-trip golden) and over MCP (thin exit-code dispatch, fail-closed root, zero format logic, read-only per D-07) — the single read path that both the MCP client and the in-app `Explain effect` button reuse (D-08).
</success_criteria>

<output>
Create `.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-04-SUMMARY.md` when done.
</output>
</output>
