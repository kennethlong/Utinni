---
phase: 15-wave-2-editors-worldsnapshot-particle-presentation-residuals
plan: 16
type: execute
wave: 5
depends_on: []
files_modified:
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormParticleEditor.cs"
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormSnapshotPlacements.cs"
  - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/SWG/WorldSnapshotImpl.cs"
autonomous: true
gap_closure: true
requirements: [PROD-W2-PRT, PROD-W2-WS, RESID-03]

must_haves:
  truths:
    - "B4/B5: after a raw-hex leaf edit (and after Undo/Redo), the Particle param grid re-binds for the current selection so the edited cell shows the new hex immediately — no reselect needed"
    - "B6: when SWG is running but no hot-retrigger hook is reachable, the disabled Preview-in-client button shows an HONEST no-hook disabled-reason tooltip (distinct from the 'No live client — start SWG…' case), with badge/tooltip copy staying honest per UI-SPEC"
    - "A7: the bulk-delete confirm copy is amended to disclose that the in-world object persists until a scene change (no RESID-03-style over-promise), AND WorldSnapshotImpl.BulkDelete calls WorldSnapshot.DetailLevelChanged() for grid-refresh consistency with BulkMove/BulkRetemplate"
  artifacts:
    - path: "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormParticleEditor.cs"
      provides: "Param-grid re-bind after AfterModelMutated + honest no-hook preview tooltip"
      contains: "BindParamGrid"
    - path: "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormSnapshotPlacements.cs"
      provides: "Delete-confirm copy disclosing scene-change deferral"
      contains: "FormSaveConfirmDialog"
    - path: "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/SWG/WorldSnapshotImpl.cs"
      provides: "BulkDelete DetailLevelChanged() consistency"
      contains: "DetailLevelChanged"
  key_links:
    - from: "FormParticleEditor.AfterModelMutated"
      to: "BindParamGrid(currentSelection)"
      via: "re-bind the param grid to the selected emitter node after a model mutation (edit/undo/redo)"
      pattern: "BindParamGrid"
    - from: "FormParticleEditor.RefreshButtonsState"
      to: "honest disabled-reason tooltip for the no-hook case"
      via: "distinguish !Game.IsRunning from running-but-no-hook when setting the disabled Preview tooltip"
      pattern: "PreviewUnavailableTooltip"
    - from: "WorldSnapshotImpl.BulkDelete"
      to: "WorldSnapshot.DetailLevelChanged()"
      via: "call it inside the AddUpdateLoopCall like BulkMove/BulkRetemplate"
      pattern: "DetailLevelChanged"
---

<objective>
Fold the three non-blocking polish defects the 2026-06-13 smoke explicitly flagged "batched into the cleanup rebuild" into a single managed-only cleanup plan (all in UtinniPlugins):

- **B4/B5 (display-only):** `FormParticleEditor.ApplyLeafEdit` → `AfterModelMutated()` calls `emitterTree.RefreshMutable(...)` (tree only) and never re-runs `BindParamGrid(...)` for the current selection, so a raw-hex leaf edit leaves the grid cell showing stale hex until reselect (the edit is real — re-opening the hex dialog shows the new value; Save persists it). Also covers `DoUndo`/`DoRedo`, which share `AfterModelMutated()`.
- **B6 (candor):** the only disabled-Preview tooltip is the "No live client — start SWG…" case; there is no honest disabled-reason tooltip for the *no-hot-retrigger-hook* case (when `Game.IsRunning` but `IsRetriggerHookReachable()` is false — the actual state this phase). Add one; keep badge copy honest per UI-SPEC.
- **A7 (candor + consistency):** the delete-confirm says "This removes N object placements from the snapshot" with no mention the in-world object persists until a scene change (a RESID-03-style over-promise) — amend the copy to match the deferred reality. Also `BulkDelete` omits the `WorldSnapshot.DetailLevelChanged()` call that `BulkMove`/`BulkRetemplate` make (it relies on the 250ms `ScheduleRefresh` timer instead) — add it for immediate-grid-refresh consistency.

Purpose: these are honesty/UX polish items (none data-affecting) that the maintainer explicitly deferred into the gap-closure cleanup rebuild. Keeping the badge/confirm copy HONEST is the RESID-03 candor discipline (UI-SPEC Reload Candor Contract: never over-promise).

Output: the Particle grid re-binds after every model mutation; the disabled Preview button gives an honest no-hook reason; the delete confirm discloses the scene-change deferral; BulkDelete refreshes the grid immediately like its siblings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-SMOKE.md
@.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-UI-SPEC.md

<interfaces>
<!-- B4/B5 (verified FormParticleEditor.cs):
  ApplyLeafEdit (L578-595): effect.EditLeafPayload(...); undoStack.Push; AfterModelMutated();
  AfterModelMutated (L597-603): emitterTree.RefreshMutable(effect.SourceIff); UpdateDirtyVisuals();
      UpdateCounters(); RefreshButtonsState();   <-- never re-runs BindParamGrid for the selection
  DoUndo (L610-619) / DoRedo (L621-630): both call AfterModelMutated().
  BindParamGrid(MutableIffNode node) (L389) is the grid binder; the tree selection handler at L383
      calls BindParamGrid(node). Need: track the currently-selected emitter node and re-bind it after
      AfterModelMutated (the edited leaf belongs to that node). Use the emitterTree's current selection
      (the node whose params are shown) — re-call BindParamGrid for it if non-null. -->

<!-- B6 (verified FormParticleEditor.cs):
  const PreviewUnavailableTooltip = "No live client — start SWG to preview in-scene." (L81)
  IsRetriggerHookReachable() => false this phase (L688-693); PreviewAvailable() = Game.IsRunning && hook (L695-701)
  RefreshButtonsState (L1002-1025): btnPreview.Enabled = previewAvail;
      toolTip.SetToolTip(btnPreview, previewAvail ? "Re-trigger this effect's live instances in the client." : PreviewUnavailableTooltip);
  Need: when !previewAvail, choose the tooltip by REASON — if !Game.IsRunning -> the existing
      "No live client — start SWG…" copy; if Game.IsRunning but !hook -> a NEW honest no-hook copy.
  UI-SPEC honest copy basis (Reload Candor Contract): degraded badge = "Reloads on next scene change or relog.";
      the no-hook reason must not imply a hook exists. Suggested NEW tooltip (honest, UI-SPEC-aligned),
      e.g. "Live preview isn't wired this build — edits show on the next scene change or relog." Keep it
      consistent with the degraded reload-badge wording; do NOT invent a capability. -->

<!-- A7 (verified):
  FormSnapshotPlacements.OnDeleteSelectedClicked (L508-533): FormSaveConfirmDialog(
      "Delete " + ids.Count + " placements?",
      "This removes " + ids.Count + " object placements from the snapshot. This is undoable in the editor until you save.",
      "Delete", "Cancel"); ... worldSnapshot.BulkDelete(ids); ScheduleRefresh();
  WorldSnapshotImpl.BulkDelete (L246-270): AddUpdateLoopCall { foreach node: AddUndoCommand(Remove); WorldSnapshot.RemoveNode(node); }
      DisableGizmo(); UpdateSelectedNodeControls(null);   <-- NO WorldSnapshot.DetailLevelChanged()
  BulkMove (L242) and BulkRetemplate (L318) BOTH end with WorldSnapshot.DetailLevelChanged() inside the
      same AddUpdateLoopCall. The A7 history note (15-SMOKE): in-world object persists until a scene
      change (engine does not de-spawn an already-rendered object without one) — the confirm copy must
      say so. The LOCKED WS reload badge is "Placements re-resolve on the next scene change." -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: B4/B5 param-grid re-bind after model mutation + B6 honest no-hook preview tooltip</name>
  <files>D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormParticleEditor.cs</files>
  <read_first>
    - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormParticleEditor.cs" (AfterModelMutated L597-603, ApplyLeafEdit/DoUndo/DoRedo L578-630, BindParamGrid L389 + the tree-selection handler L383, RefreshButtonsState L1002-1025, the preview constants + IsRetriggerHookReachable/PreviewAvailable L79-101/688-701)
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-UI-SPEC.md" (Reload Candor Contract + the LOCKED degraded badge copy 'Reloads on next scene change or relog.' — the no-hook tooltip must stay honest and consistent with it)
  </read_first>
  <action>
    1. **B4/B5 — re-bind the param grid after a model mutation.** Track the currently-displayed emitter node (the node `BindParamGrid` was last bound to — e.g. capture it in the tree-selection handler at L383 into a `currentParamNode` field, or read the emitter tree's current selected node). In `AfterModelMutated()` (which `ApplyLeafEdit`, `DoUndo`, and `DoRedo` all call), after `emitterTree.RefreshMutable(...)`, re-call `BindParamGrid(currentParamNode)` when it is non-null so the edited leaf's cell re-renders with the new bytes. If `RefreshMutable` rebuilds tree nodes such that the prior node reference is stale, re-resolve the selection by id/path before re-binding (mirror how the selection handler resolves the node). The model is already correct — this is a display refresh only.

    2. **B6 — honest disabled-reason tooltip for the no-hook case.** In `RefreshButtonsState` (and anywhere the disabled-Preview tooltip is set), when `!previewAvail`, choose the tooltip by REASON: if `!Game.IsRunning` use the existing `PreviewUnavailableTooltip` ("No live client — start SWG to preview in-scene."); if `Game.IsRunning` but `!IsRetriggerHookReachable()` use a NEW honest no-hook constant that does NOT imply a hook exists and stays consistent with the LOCKED degraded reload-badge wording (e.g. `PreviewNoHookTooltip = "Live preview isn't wired this build — edits show on the next scene change or relog."`). Add the new constant alongside the existing preview/badge constants. Do NOT change the reload-badge copy itself (the degraded/live-capable badge strings are LOCKED). Keep the OnPreviewClicked defensive path unchanged.

    UtinniPlugins is the sibling repo (standing write authority); commit there.
  </action>
  <verify>
    <automated>cmd /c "\"%ProgramFiles%\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe\" \"D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolbox.sln\" /p:Configuration=Release /p:Platform=x86 /v:minimal"</automated>
  </verify>
  <acceptance_criteria>
    - `AfterModelMutated()` re-calls `BindParamGrid(...)` for the current selection (read the method: a `BindParamGrid` call follows `RefreshMutable`, guarded for a null/absent selection) — so `ApplyLeafEdit`, `DoUndo`, and `DoRedo` all refresh the grid.
    - A NEW disabled-Preview tooltip constant exists for the no-hook case, distinct from `PreviewUnavailableTooltip`, and `RefreshButtonsState` selects between them by `Game.IsRunning` (grep both constants + the branch).
    - The new no-hook tooltip copy does NOT imply a reachable hook and is consistent with the LOCKED degraded reload-badge copy ("…next scene change or relog."); the reload-badge constants (`ReloadBadgeLiveCapable`/`ReloadBadgeDegraded`) are unchanged (grep confirms they are untouched).
    - `TheJawaToolbox.sln` Release|x86 builds (MSBuild exit 0).
  </acceptance_criteria>
  <done>The Particle param grid re-binds after every model mutation (edit/undo/redo) so the edited cell shows the new hex without reselect; the disabled Preview button gives an honest reason for both the no-client and the no-hook states without over-promising; TJT Release|x86 green.</done>
</task>

<task type="auto">
  <name>Task 2: A7 delete-confirm candor + BulkDelete DetailLevelChanged consistency</name>
  <files>D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormSnapshotPlacements.cs, D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/SWG/WorldSnapshotImpl.cs</files>
  <read_first>
    - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormSnapshotPlacements.cs" (OnDeleteSelectedClicked L508-533 — the FormSaveConfirmDialog copy to amend)
    - "D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/SWG/WorldSnapshotImpl.cs" (BulkDelete L246-270 vs BulkMove L209-244 / BulkRetemplate L272-320 — the missing DetailLevelChanged() call)
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-SMOKE.md" (the A7 re-confirmed entry + the history note: in-world object persists until a scene change; LOCKED badge 'Placements re-resolve on the next scene change.')
    - ".planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-UI-SPEC.md" (the WS bulk-delete confirm copy row in the Copywriting Contract — amend honestly, do not over-promise)
  </read_first>
  <action>
    1. **A7 candor — amend the delete-confirm body.** In `FormSnapshotPlacements.OnDeleteSelectedClicked`, extend the `FormSaveConfirmDialog` body string so it discloses the deferred reality: keep "This removes N object placements from the snapshot. This is undoable in the editor until you save." and ADD a sentence that the already-rendered in-world object persists until the next scene change (matching the LOCKED WS badge "Placements re-resolve on the next scene change."), e.g. append " The in-world object stays visible until the next scene change." Do NOT imply an instant world de-spawn (RESID-03 over-promise discipline). Keep the heading and verbs unchanged.

    2. **A7 consistency — add DetailLevelChanged to BulkDelete.** In `WorldSnapshotImpl.BulkDelete`, inside the existing `GroundSceneCallbacks.AddUpdateLoopCall`, add `WorldSnapshot.DetailLevelChanged();` (matching BulkMove/BulkRetemplate) so the grid refreshes immediately rather than relying solely on the 250ms `ScheduleRefresh` timer. Place it consistently with the siblings (after the foreach, alongside the existing DisableGizmo/UpdateSelectedNodeControls(null) GAP-2 cleanup). Do NOT remove the GAP-2 gizmo/selection clear.

    Commit to UtinniPlugins (standing write authority). These are paired managed edits in the same repo.
  </action>
  <verify>
    <automated>cmd /c "\"%ProgramFiles%\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe\" \"D:/Code/UtinniPlugins/The Jawa Toolbox/TheJawaToolbox.sln\" /p:Configuration=Release /p:Platform=x86 /v:minimal"</automated>
  </verify>
  <acceptance_criteria>
    - The delete-confirm body in `OnDeleteSelectedClicked` discloses that the in-world object persists until the next scene change (grep the new sentence about the in-world object / next scene change) AND retains the "undoable in the editor until you save" clause; the heading "Delete {n} placements?" and the Delete/Cancel verbs are unchanged.
    - The new copy does NOT claim an instant world de-spawn (read the full body string).
    - `WorldSnapshotImpl.BulkDelete` calls `WorldSnapshot.DetailLevelChanged()` inside its `AddUpdateLoopCall` (grep the method) while retaining `DisableGizmo()` + `UpdateSelectedNodeControls(null)`.
    - `TheJawaToolbox.sln` Release|x86 builds (MSBuild exit 0).
  </acceptance_criteria>
  <done>The bulk-delete confirm honestly discloses the scene-change deferral (no over-promise), and BulkDelete refreshes the grid immediately via DetailLevelChanged() like BulkMove/BulkRetemplate while keeping the GAP-2 gizmo/selection clear; TJT Release|x86 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editor UI ↔ modder expectation | Confirm/tooltip copy must honestly describe deferred behavior; a stale grid or over-promising copy misleads the modder about what actually happened |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-16-01 | Repudiation / Integrity (misleading state) | Stale param-grid cell after a raw-hex edit implies the edit didn't take | mitigate | Re-bind the param grid after every model mutation so the displayed value matches the model (B4/B5) |
| T-15-16-02 | Repudiation (over-promise) | Delete confirm / preview tooltip implying behavior that didn't happen (RESID-03 candor) | mitigate | Amend delete-confirm copy to disclose scene-change deferral; add an honest no-hook preview tooltip; keep LOCKED badge copy unchanged (B6/A7) |
| T-15-16-SC | Tampering | npm/pip/cargo installs | accept | This plan installs ZERO packages (C# edits in UtinniPlugins) |
</threat_model>

<verification>
- `TheJawaToolbox.sln` Release|x86 MSBuild exit 0 (both tasks)
- Read confirms: AfterModelMutated re-binds the grid; no-hook tooltip constant + Game.IsRunning branch; LOCKED badge copy untouched; delete-confirm discloses scene-change deferral; BulkDelete calls DetailLevelChanged() + keeps GAP-2 clear
- LIVE (gated to 15-18): B4/B5 grid refreshes after hex edit; B6 disabled-Preview tooltip is honest; A7 delete confirm reads honestly + grid refreshes immediately
</verification>

<success_criteria>
The three deferred polish defects are closed managed-side: the Particle param grid re-binds after every model mutation; the disabled Preview button gives an honest reason for both the no-client and no-hook states (badge copy still honest per UI-SPEC); the bulk-delete confirm discloses the scene-change deferral without over-promising; and BulkDelete refreshes the grid immediately like its siblings. Confirmed live in 15-18.
</success_criteria>

<output>
Create `.planning/phases/15-wave-2-editors-worldsnapshot-particle-presentation-residuals/15-16-SUMMARY.md` when done.
</output>
