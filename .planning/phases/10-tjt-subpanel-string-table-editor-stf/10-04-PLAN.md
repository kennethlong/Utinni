---
phase: 10-tjt-subpanel-string-table-editor-stf
plan: 04
type: execute
wave: 3
depends_on: ["10-03"]
files_modified:
  - UtinniCoreDotNet/Formats/StringTable/StringTableCsvCoercion.cs
  - UtinniCoreDotNet/Formats/StringTable/StringTablePoExport.cs
  - UtinniCoreDotNet/Editing/StringTableEditCommands.cs
  - UtinniCoreDotNet/UtinniCoreDotNet.csproj
  - UtinniCoreDotNet.Tests/FormatsTests/StringTable/StringTableCsvCoercionTests.cs
  - UtinniCoreDotNet.Tests/FormatsTests/StringTable/StringTablePoExportTests.cs
  - UtinniCoreDotNet.Tests/FormatsTests/StringTable/StringTableEditControllerTests.cs
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/StringTableCsvSerializer.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStfCsvImportPreviewDialog.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStfCsvImportPreviewDialog.Designer.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStringTableEditor.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStringTableEditor.Designer.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"
autonomous: true
requirements: [PROD-W1-STF]
assumes:
  - "Plan 10-03 has shipped FormStringTableEditor with the Find/Replace + Filter + Import CSV… + Export CSV… + Export PO… toolbar buttons RENDERED DISABLED (stubs). Plan 10-04 replaces those stubs with the actual functionality. Plan 10-03 ships the StringTableEditController + the 4 T4 commands; Plan 10-04 ADDS an ApplyCsvImport transaction command (one undo entry for the whole import)."
  - "Cross-repo paired commits per `feedback_utinniplugins_authority` — Plan 10-04 modifies Utinni (2 new framework files + 1 controller-command extension + 3 test files) AND UtinniPlugins (1 serializer + 1 preview modal + Designer + FormStringTableEditor wiring + csproj). Commit atomically."
  - "DEVIATION (Rule 3): the unit-testable logic (CSV coercion + per-entry diff, PO export string generation) lives FRAMEWORK-SIDE (StringTableCsvCoercion + StringTablePoExport) so it is xUnit-coverable from CI; the WinForms serializer/modal/Form wiring is maintainer-smoke (10-06). Build green Debug+Release|x86 in BOTH repos."
  - "D-03c view-only sort + live filter MUST NOT mutate on-disk order — the writer always serializes canonically (10-01). The filter HIDES rows (Row.Visible=false); column-click sort is the built-in DataGridView view sort. NEITHER touches MutableStringTableDocument.Entries order. A framework xUnit fact (no UI) asserts a sort/filter does not change the serialized bytes (binds a plain DataGridView per the Phase 9 cross-repo UI-test-placement decision)."
  - "PO export (D-03d) is EXPORT-ONLY and the lowest-priority of the four bulk features; PO import is explicitly NOT V1. It is hand-rolled (~40 lines, msgid=key / msgstr=text). If genuine scope pressure hits during execution, PO export is the documented first cut-to-V2 candidate — but it has NO context/missing-info/dependency constraint, so it ships unless the developer explicitly approves cutting it."
must_haves:
  truths:
    - "CSV/TSV export (D-03b) writes (key, text) to a user-picked file via SaveFileDialog: header row `key,text`, UTF-8 WITH BOM (non-ASCII safety), RFC-4180-ish double-quote escape; multi-line text quoted. Round-trips back to UTF-16LE verbatim on import."
    - "CSV/TSV delta-import (D-03b) computes a per-entry diff via `StringTableCsvCoercion.PlanImport`: entries whose imported text EQUALS current text stay clean (preserve original bytes — D-02 / SC4); entries whose text differs queue an EditText patch; NEW keys in the CSV are ADDED (auto-id); missing keys are left untouched. A preview modal surfaces the LOCKED copy `{N} entries will change. {M} entries will stay as original bytes. New keys in the CSV will be added; missing keys are left untouched.` Apply runs a SINGLE `StringTableEditCommands.ApplyCsvImport` transaction (one undo entry)."
    - "CSV INVALID-KEY GUARD (F8): `PlanImport` ALSO produces an `Invalid` list — a CSV row whose key fails `ValidateName` (charset/leading-digit/uppercase/empty), an empty key, a duplicate CSV row (same key twice in the CSV), or a duplicate-after-fixup is flagged BEFORE anything is applied. The preview modal SURFACES the invalid-key list and BLOCKS the Import button when it is non-empty (the user fixes the CSV and re-imports). `ApplyCsvImport` is therefore only ever handed a plan with ZERO invalid rows — entries are NEVER created and then discovered invalid inside the transaction. An acceptance test asserts PlanImport flags each invalid case + that a plan with invalids is rejected before apply."
    - "Find/Replace pane (D-03a) toggles via Ctrl+F / Ctrl+H over BOTH the key + text columns; case toggle + regex opt-in; F3/Shift+F3 cycle matches; Esc collapses. Replacing into a KEY cell re-runs name-validation (invalid → blocked, status red, no commit); replacing into TEXT never fails."
    - "Live filter row (D-03c — NEW for Wave-1) toggles via Ctrl+L; a 250ms-debounced filter box over key+text HIDES non-matching rows (Row.Visible=false); `{shown} / {total}` count; view-only (NEVER mutates the document or on-disk order). The `Filter` toolbar button toggles the row; ✕ / Esc clears."
    - "Column-click view-only sort (D-03c): built-in DataGridView ▲/▼ sort; the LOCKED hover tooltip reads `View order only — save serializes strings by id and names alphabetically.`; a framework xUnit fact asserts sort/filter does NOT change the serialized bytes (D-03c on-disk-order guarantee)."
    - "PO/gettext export (D-03d) writes a .po file (msgid=key, msgstr=text, escaped per gettext) via `StringTablePoExport` + an Export PO… toolbar button; export-only (no PO import in V1). Non-ASCII text survives (UTF-8)."
  artifacts:
    - path: "UtinniCoreDotNet/Formats/StringTable/StringTableCsvCoercion.cs"
      provides: "Framework-side per-entry CSV diff planner (CsvImportPlan: Changes/Unchanged/Added/Invalid — F8) + SerializeRowToCsv helper; PlanImport validates every key via ValidateName + flags empty/duplicate-CSV-row/duplicate-after-fixup into Invalid so the caller can BLOCK import before apply; pure-managed BCL-only; xUnit-coverable"
      contains: "PlanImport"
    - path: "UtinniCoreDotNet/Formats/StringTable/StringTablePoExport.cs"
      provides: "Hand-rolled PO/gettext export (msgid=key / msgstr=text, gettext string-escape); ~40 lines; pure-managed"
      contains: "StringTablePoExport"
    - path: "UtinniCoreDotNet/Editing/StringTableEditCommands.cs"
      provides: "MODIFY — add ApplyCsvImport(MutableStringTableDocument, CsvImportPlan) transaction command (N EditText + M AddEntry wrapped as one undo entry; UndoOp reverses all)"
      contains: "ApplyCsvImport"
    - path: "UtinniCoreDotNet.Tests/FormatsTests/StringTable/StringTableCsvCoercionTests.cs"
      provides: "~10-14 [Fact]s: all-match→zero changes; one-differs→one change; new key→Added; missing key→untouched; SC4 round-trip (export→import→no change preserves original bytes); over-row-cap DoS guard; INVALID-KEY GUARD (F8) — a bad-charset key / empty key / duplicate CSV row / duplicate-after-fixup each lands in Invalid; a plan WITH invalids has HasBlockingErrors==true and ApplyCsvImport refuses it (entries never created-then-found-invalid)"
      contains: "StringTableCsvCoercionTests"
    - path: "UtinniCoreDotNet.Tests/FormatsTests/StringTable/StringTablePoExportTests.cs"
      provides: "~4-6 [Fact]s: msgid/msgstr framing; gettext escaping of quotes/newlines; non-ASCII (João) survives in UTF-8"
      contains: "StringTablePoExportTests"
    - path: "UtinniCoreDotNet.Tests/FormatsTests/StringTable/StringTableEditControllerTests.cs"
      provides: "MODIFY — add ~3-5 ApplyCsvImport facts (single-transaction undo identity; SC4 byte-exact when imported==current; mixed change+add transaction) + a Sort/Filter-does-not-mutate-serialized-bytes fact (plain DataGridView, framework writer — Phase 9 cross-repo UI-test placement)"
      contains: "ApplyCsvImport"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/StringTableCsvSerializer.cs"
      provides: "TJT-side CSV file I/O + hand-rolled RFC-4180-ish parser (< 120 lines, UTF-8 BOM, quote-escape); calls framework StringTableCsvCoercion.PlanImport + StringTablePoExport"
      contains: "StringTableCsvSerializer"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStfCsvImportPreviewDialog.cs"
      provides: "Per-call modal (using-block) — heading + LOCKED D-03b body + a ThemedDataGridView of changed/added entries + an INVALID-KEY list section (F8); the Import button is DISABLED when plan.HasBlockingErrors (invalid rows shown red with their reason); Cancel always enabled"
      contains: "FormStfCsvImportPreviewDialog"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStfCsvImportPreviewDialog.Designer.cs"
      provides: "Hand-written Designer; ThemedDataGridView Fill + footer Import/Cancel UtinniButtons"
      contains: "InitializeComponent"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStringTableEditor.cs"
      provides: "MODIFY — wire Find/Replace pane (key+text, regex opt-in, key-replace re-validates) + live filter row (Ctrl+L, 250ms debounce, Row.Visible) + column-click view-only sort (locked tooltip) + Import/Export CSV + Export PO buttons → StringTableCsvSerializer / StringTablePoExport"
      contains: "StringTableCsvSerializer"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStringTableEditor.Designer.cs"
      provides: "MODIFY — populate the Find/Replace pane controls + the filter row controls (txtFilter, lblFilterCount, btnClearFilter); enable the previously-disabled Find/Replace/Filter/Import/Export buttons"
      contains: "txtFilter"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"
      provides: "Old-style explicit-compile — adds <Compile Include> for StringTableCsvSerializer.cs + FormStfCsvImportPreviewDialog.cs + .Designer.cs"
      contains: "StringTableCsvSerializer.cs"
  key_links:
    - from: "FormStringTableEditor.OnImportCsvClick"
      to: "StringTableCsvSerializer.LoadAndPlan → FormStfCsvImportPreviewDialog → controller.Apply(StringTableEditCommands.ApplyCsvImport(plan))"
      via: "CSV import flow: load+plan → modal preview → single-transaction apply (D-03b)"
      pattern: "ApplyCsvImport"
    - from: "StringTableCsvSerializer.LoadAndPlan"
      to: "StringTableCsvCoercion.PlanImport (framework)"
      via: "per-entry diff: equal text → Unchanged (original bytes preserved per D-02); differ → Changes; new key → Added"
      pattern: "PlanImport"
    - from: "FormStringTableEditor live filter txtFilter.TextChanged (250ms debounce)"
      to: "Row.Visible = key||text matches; document UNCHANGED"
      via: "D-03c view-only filter; never mutates Entries order"
      pattern: "Row.Visible"
    - from: "Sort/Filter-does-not-mutate-bytes xUnit fact"
      to: "StringTableWriter.Serialize iterates MutableStringTableDocument.Entries (D-03c on-disk order)"
      via: "plain DataGridView sort/filter → serialize → bytes equal pre-sort serialize"
      pattern: "DoesNotMutate"
    - from: "FormStringTableEditor.OnExportPoClick"
      to: "StringTablePoExport (framework, msgid=key/msgstr=text)"
      via: "export-only PO/gettext; no import (D-03d)"
      pattern: "StringTablePoExport"
---

<objective>
Ship the Phase 10 **bulk / translation features**: Find/Replace (key+text, regex opt-in, D-03a),
CSV/TSV delta-import + export (D-03b — the byte-exact-on-untouched CSV gate), view-only sort + live filter
(D-03c — the live filter is the one thing Phase 9 deferred; it ships here), and PO/gettext export (D-03d).
All four are first-class V1 per CONTEXT D-03 (string tables ARE the localization surface — translation work
is the killer use case).

Purpose: completes SC2's productivity surface for real translation passes on hundreds-of-entry tables. The
SC4 byte-exact guarantee extends to CSV (matching entries keep original bytes via the per-entry diff). The
D-03c on-disk-order guarantee (sort/filter are view-only) is enforced by a framework xUnit fact.

Output: 2 framework files (CSV coercion + PO export) + 1 controller-command extension (ApplyCsvImport) +
3 framework test files; 1 TJT serializer + 1 preview modal + the FormStringTableEditor wiring.

**Why Wave 3 (parallel with 10-05):** depends only on 10-03's form + 10-01's controller. 10-04 and 10-05
modify the SAME FormStringTableEditor.cs but DIFFERENT method bodies (10-04: Find/Replace + filter + sort +
CSV/PO; 10-05: Save▾ + reload + hand-off). To keep them genuinely parallel WITHOUT a file conflict, they
are sequenced Wave 3 → but if executed concurrently the orchestrator MUST serialize the FormStringTableEditor.cs
edits. SAFEST: run 10-04 then 10-05 (10-05 rebases on 10-04's committed form). The depends_on graph keeps both
at Wave 3 depending on 10-03; the files_modified overlap on FormStringTableEditor.cs forces 10-05 to a later
slot if the wave engine detects the overlap. (See 10-05's objective for the same note.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-tjt-subpanel-string-table-editor-stf/10-CONTEXT.md
@.planning/phases/10-tjt-subpanel-string-table-editor-stf/10-UI-SPEC.md
@.planning/phases/10-tjt-subpanel-string-table-editor-stf/10-01-PLAN.md
@.planning/phases/10-tjt-subpanel-string-table-editor-stf/10-03-PLAN.md

# Phase 9 bulk-feature analogs — the shape Plan 10-04 mirrors (simpler: one text column, no type-invalid list)
@UtinniCoreDotNet/Formats/Datatable/CsvCellCoercion.cs
@UtinniCoreDotNet/Editing/DatatableEditCommands.cs
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/DatatableCsvSerializer.cs
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormCsvImportPreviewDialog.cs
@UtinniCoreDotNet.Tests/UITests/DatatableSortViewOnlyTests.cs

# 10-01 + 10-03 surfaces Plan 10-04 consumes
@UtinniCoreDotNet/Formats/StringTable/MutableStringTableDocument.cs
@UtinniCoreDotNet/Formats/StringTable/MutableStringTableEntry.cs
@UtinniCoreDotNet/Formats/StringTable/StringTableWriter.cs
@UtinniCoreDotNet/Editing/StringTableEditCommands.cs
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStringTableEditor.cs
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStringTableEditor.Designer.cs

<interfaces>
From 10-01: MutableStringTableDocument { Entries, AddEntry(), ValidateName() }; MutableStringTableEntry { Name, Text, SourceCrc }; StringTableWriter.Serialize(doc); StringTableEditCommands.{EditText, AddEntry}
From Phase 9 CsvCellCoercion.cs: the PlanImport diff shape (Changes/Unchanged/Invalid + DoS row cap) to specialize (Phase 10 has no type-invalid list — text never fails; instead an Added list for new keys)
From Phase 9 DatatableSortViewOnlyTests.cs: the STA plain-DataGridView sort-view-only fact pattern to mirror
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Framework StringTableCsvCoercion + StringTablePoExport + ApplyCsvImport command + xUnit</name>
  <files>UtinniCoreDotNet/Formats/StringTable/StringTableCsvCoercion.cs, UtinniCoreDotNet/Formats/StringTable/StringTablePoExport.cs, UtinniCoreDotNet/Editing/StringTableEditCommands.cs, UtinniCoreDotNet/UtinniCoreDotNet.csproj, UtinniCoreDotNet.Tests/FormatsTests/StringTable/StringTableCsvCoercionTests.cs, UtinniCoreDotNet.Tests/FormatsTests/StringTable/StringTablePoExportTests.cs, UtinniCoreDotNet.Tests/FormatsTests/StringTable/StringTableEditControllerTests.cs, UtinniCoreDotNet.Tests/UITests/StringTableSortViewOnlyTests.cs</files>
  <read_first>
    - UtinniCoreDotNet/Formats/Datatable/CsvCellCoercion.cs (the PlanImport diff + DoS cap pattern; Phase 10 simplifies to key/text with an Added list, no type-invalid list)
    - UtinniCoreDotNet/Editing/DatatableEditCommands.cs (the ApplyCsvImportCommand transaction-wrapper pattern: capture per-entry old state, Do applies all, UndoOp reverses in order)
    - UtinniCoreDotNet/Editing/StringTableEditCommands.cs (10-03 — the EditText/AddEntry commands the transaction composes; add ApplyCsvImport here)
    - UtinniCoreDotNet/Formats/StringTable/MutableStringTableDocument.cs + MutableStringTableEntry.cs (Entries/AddEntry/ValidateName; entry Name/Text)
    - UtinniCoreDotNet.Tests/UITests/DatatableSortViewOnlyTests.cs (the STA plain-DataGridView sort-view-only fact to mirror for StringTableSortViewOnlyTests)
    - UtinniCoreDotNet/UtinniCoreDotNet.csproj (old-style explicit-compile — 2 new Formats/StringTable files need <Compile Include>)
  </read_first>
  <behavior>
    - StringTableCsvCoercion: CsvImportPlan { List<EditPatch> Changes (entry, newText); List<UnchangedMatch> Unchanged; List<AddedEntry> Added (key, text); List<InvalidRow> Invalid (rowIndex, key, reason); bool HasBlockingErrors => Invalid.Count > 0; }. PlanImport(MutableStringTableDocument target, IReadOnlyList<(string key,string text)> rows): VALIDATION FIRST (F8) — for each CSV row, if the key is empty OR fails ValidateName (charset/leading-digit/uppercase) OR is a duplicate of an earlier CSV row OR collides with another row after any fixup → add to Invalid with the reason; rows in Invalid are NOT diffed/added. For each VALID row, find entry by key; if found and text == current → Unchanged (preserve original bytes per D-02); if found and text differs → Changes; if NOT found → Added (a new key the import will create — its key already passed ValidateName). When Invalid is non-empty the caller MUST block apply (the modal disables Import). DoS cap: reject > 100,000 rows or any cell > 64 KB with StringTableCsvParseException. SerializeRowToCsv(string key, string text) helper for export.
    - StringTablePoExport.ToPo(MutableStringTableDocument doc) → string: emit a minimal .po — for each entry `msgid "{escaped key}"` + `msgstr "{escaped text}"` with gettext escaping (\\ → \\\\, " → \\\", newline → \\n) and a blank line between entries; UTF-8 (the caller writes with UTF-8). ~40 lines. Export-only — no parse.
    - StringTableEditCommands ADD ApplyCsvImport(MutableStringTableDocument doc, CsvImportPlan plan): private nested transaction command. PRECONDITION (F8): throw/assert if plan.HasBlockingErrors — the command is NEVER handed a plan with invalid rows (the modal blocks Import upstream; this is a defensive guard, not the primary gate). Do: for each Changes patch capture old EntryState + set entry.Text; for each Added create doc.AddEntry() then set its Name (already ValidateName-passed in PlanImport) + Text + capture the created entry. UndoOp: reverse — RestoreState each changed entry, Remove each added entry (+ restore NextUniqueId). One undo entry for the whole import.
    - StringTableCsvCoercionTests (~10-14): all-match→0 changes; one-differs→1 change others unchanged; new key→Added; SC4 round-trip (SerializeRowToCsv then PlanImport → no change → original bytes preserved); over-row-cap throws; multi-line/quoted text round-trips; F8 invalid-key guard — bad-charset key flagged Invalid; empty key flagged; duplicate CSV row flagged; duplicate-after-fixup flagged; HasBlockingErrors true when Invalid non-empty; ApplyCsvImport throws/refuses a plan with HasBlockingErrors (entries not created-then-invalidated).
    - StringTablePoExportTests (~4-6): msgid/msgstr framing; quote+newline escaping; João survives in UTF-8.
    - StringTableEditControllerTests ADD (~3-5): ApplyCsvImport single-transaction undo identity; SC4 byte-exact when imported==current; mixed change+add transaction; redo re-applies.
    - StringTableSortViewOnlyTests (new STA file, plain System.Windows.Forms.DataGridView + framework StringTableWriter, NO TJT ref — Phase 9 cross-repo placement): bind entries to a plain grid, call grid.Sort + hide rows (simulate filter), serialize via StringTableWriter, assert bytes == pre-sort serialize (D-03c on-disk-order guarantee).
  </behavior>
  <action>
    Create the 2 Formats/StringTable files + modify StringTableEditCommands.cs (add ApplyCsvImport) +
    add the 2 csproj Compile entries. Create the 3 framework test files + extend
    StringTableEditControllerTests. 23-line MIT header, Allman, 4-space, `// ToDo` (no colon). XML cites
    the Phase 9 CsvCellCoercion analog. Tests FIRST (RED) then implement (GREEN). SDK auto-glob tests.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~StringTableCsvCoercionTests|FullyQualifiedName~StringTablePoExportTests|FullyQualifiedName~StringTableSortViewOnlyTests|FullyQualifiedName~ApplyCsvImport" 2>&1 | tail -20  # MUST exit 0 with >= 18 combined facts; grep -v '^#' UtinniCoreDotNet/UtinniCoreDotNet.csproj | grep -c "Formats\\\\StringTable\\\\StringTableCsvCoercion.cs"  # MUST return 1; grep -c "ApplyCsvImport" UtinniCoreDotNet/Editing/StringTableEditCommands.cs  # MUST return >= 1</automated>
  </verify>
  <done>StringTableCsvCoercion + StringTablePoExport ship with >= 12 framework facts; ApplyCsvImport transaction command added with >= 3 controller facts; the sort/filter-does-not-mutate-bytes STA fact green (D-03c); csproj has the 2 Compile entries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TJT StringTableCsvSerializer + import-preview modal + FormStringTableEditor Find/Replace + Filter + sort + CSV/PO wiring</name>
  <files>../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/StringTableCsvSerializer.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStfCsvImportPreviewDialog.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStfCsvImportPreviewDialog.Designer.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStringTableEditor.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStringTableEditor.Designer.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj</files>
  <read_first>
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/DatatableCsvSerializer.cs (the hand-rolled RFC-4180-ish parser + UTF-8-BOM export pattern; Phase 10 has key+text only)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormCsvImportPreviewDialog.cs + .Designer.cs (the per-call preview modal pattern; Phase 10 is simpler — one text column, no type-invalid red list)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs (the Phase 9 Find/Replace pane + column-sort + filter-debounce wiring to mirror; the 250ms debounce pattern is also in the Phase 7 TRE Browser filter)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStringTableEditor.cs + .Designer.cs (10-03 — the form to MODIFY; the disabled Find/Replace/Filter/Import/Export buttons + the reserved pnlFindReplace/pnlFilter panels)
    - UtinniCoreDotNet/Formats/StringTable/StringTableCsvCoercion.cs + StringTablePoExport.cs (Task 1 — PlanImport/SerializeRowToCsv/ToPo the serializer consumes)
    - UtinniCoreDotNet/Editing/StringTableEditCommands.cs (Task 1 — ApplyCsvImport factory)
    - .planning/phases/10-tjt-subpanel-string-table-editor-stf/10-UI-SPEC.md (§ Find/Replace pane, § Filter row, § States, § Copywriting incl. the LOCKED CSV-import-preview body + the LOCKED sort tooltip + the Ctrl+L filter chord)
  </read_first>
  <behavior>
    - StringTableCsvSerializer (TJT): Export(MutableStringTableDocument doc, string path) — UTF-8 BOM StreamWriter; header `key,text`; each entry `CsvEscape(key),CsvEscape(text)` (quote if comma/quote/newline; double internal quotes). LoadAndPlan(MutableStringTableDocument target, string csvPath) — read with detectEncodingFromByteOrderMarks; hand-rolled RFC-4180-ish parse (< 100 lines); first row header; map to (key,text) rows; call StringTableCsvCoercion.PlanImport. ExportPo(MutableStringTableDocument doc, string path) — write StringTablePoExport.ToPo(doc) as UTF-8.
    - FormStfCsvImportPreviewDialog (per-call modal, using-block): heading `Import {csvFileName} into {stfFileName}?`; body the LOCKED D-03b copy `{N} entries will change. {M} entries will stay as original bytes. New keys in the CSV will be added; missing keys are left untouched.`; a ThemedDataGridView listing changed + added entries; PLUS an invalid-key section (F8) listing each plan.Invalid row (rowIndex, key, reason) in red when non-empty; the Import button is DISABLED (with an explanatory note "Fix the {k} invalid key(s) in the CSV, then re-import.") whenever plan.HasBlockingErrors — only Cancel is available; Import/Cancel buttons (DialogResult OK/Cancel). 800×600.
    - FormStringTableEditor MODIFICATIONS:
      - Find/Replace pane (Ctrl+F / Ctrl+H): scope = key + text columns; case toggle (Aa) + regex toggle (.*); F3/Shift+F3 cycle; Esc collapse; match overlay; lblFindCount `{i} / {n}`; glyph buttons carry ToolTipText (Find previous (Shift+F3) / Find next (F3) / Close (Esc)). Replace into a KEY cell re-runs ValidateName (invalid → blocked, status red, no commit); replace into TEXT never fails.
      - Filter row (Ctrl+L): txtFilter (250ms-debounced) hides non-matching rows over key+text via Row.Visible=false; lblFilterCount `{shown} / {total}`; btnClearFilter ✕ (tooltip Clear filter (Esc)); view-only — never mutates the document. Persist [StringTableEditor] filterVisible.
      - Column-click view-only sort: enable SortMode on both columns; LOCKED hover tooltip `View order only — save serializes strings by id and names alphabetically.`.
      - Import CSV… → StringTableCsvSerializer.LoadAndPlan → FormStfCsvImportPreviewDialog → on OK controller.Apply(StringTableEditCommands.ApplyCsvImport(plan)). Export CSV… → SaveFileDialog → StringTableCsvSerializer.Export. Export PO… → SaveFileDialog → StringTableCsvSerializer.ExportPo. Status strip surfaces the import/export result copy from 10-UI-SPEC § States.
      - Enable the previously-disabled (10-03) Find/Replace/Filter/Import/Export buttons.
    - csproj: 3 new <Compile Include> (StringTableCsvSerializer.cs + FormStfCsvImportPreviewDialog.cs <SubType>Form</SubType> + .Designer.cs <DependentUpon>).
  </behavior>
  <action>
    Create StringTableCsvSerializer.cs + FormStfCsvImportPreviewDialog.cs + Designer; modify
    FormStringTableEditor.cs + .Designer.cs per the behavior block. 23-line MIT header, Allman, 4-space,
    `// ToDo` (no colon). LOCKED copy verbatim from 10-UI-SPEC. Add the 3 csproj Compile entries.
    Cross-repo paired commit. No new tests in this task — Task 1's framework facts cover the logic; the
    TJT wiring is exercised by 10-06 smoke. Build via VS2026 MSBuild Debug|x86.
  </action>
  <verify>
    <automated>powershell -Command "& 'D:/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe' '../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj' /p:Configuration=Debug /p:Platform=x86 /v:minimal" 2>&1 | tail -10  # build green; grep -c "StringTableCsvCoercion" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/StringTableCsvSerializer.cs"  # MUST return >= 1; grep -c "Saving\\\\StringTableCsvSerializer.cs" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"  # MUST return 1; grep -c "FormStfCsvImportPreviewDialog.cs" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"  # MUST return 2 (.cs + .Designer.cs); grep -c "View order only" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStringTableEditor.cs"  # MUST return >= 1 (locked sort tooltip)</automated>
  </verify>
  <done>StringTableCsvSerializer + FormStfCsvImportPreviewDialog ship + build clean Debug|x86; FormStringTableEditor wires Find/Replace (key+text, key-replace re-validates) + Ctrl+L filter (Row.Visible, 250ms debounce) + view-only sort (locked tooltip) + Import/Export CSV + Export PO; csproj entries added; cross-repo paired commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → process | Import CSV reads a user-supplied CSV (malformed / huge) |
| user input → model | CSV new keys + Replace-into-key cross the engine name rules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | DoS | StringTableCsvCoercion.PlanImport huge CSV | mitigate | Row cap 100,000 + cell cap 64 KB → StringTableCsvParseException surfaced in the preview modal before allocation explosion. |
| T-10-11 | Tampering (invalid key via CSV/Replace) | new-key add + key-cell replace | mitigate | New keys validated via ValidateName before AddEntry; Replace-into-key re-runs ValidateName (invalid blocked, no commit). |
| T-10-12 | Tampering (on-disk order changed by view) | sort/filter | mitigate | Sort/filter are view-only (Row.Visible / built-in sort); the writer always serializes canonically; the STA xUnit fact asserts bytes unchanged after sort/filter (D-03c). |
| T-10-SC | Tampering | npm/pip/cargo installs | N/A | Phase 10 adds ZERO external packages (hand-rolled CSV + PO; no gettext lib). |
</threat_model>

<verification>
1. `dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~StringTableCsvCoercionTests|FullyQualifiedName~StringTablePoExportTests|FullyQualifiedName~StringTableSortViewOnlyTests|FullyQualifiedName~ApplyCsvImport"` exits 0 with >= 18 facts.
2. TJT MSBuild Debug|x86 + Release|x86 succeed; Utinni Debug|x86 + Release|x86 succeed.
3. The LOCKED CSV-import body + the LOCKED sort tooltip appear verbatim.
4. The D-03c sort/filter-does-not-mutate-bytes fact is present and green.
5. Full UtinniCoreDotNet.Tests + PreservationAudit suites stay green.
</verification>

<success_criteria>
- D-03a Find/Replace (key+text, regex opt-in, key-replace re-validates) shipped.
- D-03b CSV/TSV export (UTF-8+BOM) + delta-import (per-entry diff, byte-exact-on-untouched, single-transaction undo, preview modal) shipped.
- F8 CSV invalid-key guard: PlanImport flags invalid/empty/duplicate keys into an Invalid list; the preview modal blocks Import before apply; entries are never created-then-found-invalid.
- D-03c view-only sort + live filter shipped; on-disk order never changes (framework fact proves it).
- D-03d PO/gettext export shipped (export-only; non-ASCII survives).
- SC4 extends to CSV: matching entries keep original bytes.
</success_criteria>

<output>
Create `.planning/phases/10-tjt-subpanel-string-table-editor-stf/10-04-SUMMARY.md` when done. Note the filter
chord chosen (Ctrl+L confirmed or alternative), whether PO export shipped or was cut-to-V2 (with developer
approval if cut), and the final CSV parser line count.
</output>
