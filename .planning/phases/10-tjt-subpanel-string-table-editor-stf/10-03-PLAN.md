---
phase: 10-tjt-subpanel-string-table-editor-stf
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStringTableEditor.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStringTableEditor.Designer.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"
  - UtinniCoreDotNet.Tests/FormatsTests/StringTable/StringTableNameValidationTests.cs
autonomous: true
requirements: [PROD-W1-STF]
assumes:
  - "Plan 10-01 has shipped `Formats/StringTable/*` (FromBytes reader, mutable model, writer) + `Editing/StringTableEditController` + the 4 T4 commands + the `MutableStringTableDocument.ValidateName` predicate. Plan 10-03 consumes these via the existing `UtinniCoreDotNet.dll` HintPath in `TheJawaToolboxDotNet.csproj` (CF-01)."
  - "Cross-repo paired commits per memory `feedback_utinniplugins_authority` — Plan 10-03 modifies UtinniPlugins (1 form + Designer + Plugin.cs + csproj) AND Utinni (1 framework test file confirming the name-validation predicate the WinForms cell-validator delegates to). Commit atomically; the live-SWG smoke (10-06) is the only required human-action checkpoint."
  - "`TheJawaToolboxDotNet.csproj` is OLD-STYLE explicit-compile — the new Form `.cs` (with `<SubType>Form</SubType>`) + `.Designer.cs` (`<DependentUpon>`) need explicit `<Compile Include>` entries. NO `.resx` (Phase 8/9 precedent: hand-written Designer). Build via VS2026 MSBuild; `dotnet test --no-build` for xUnit."
  - "The grid IS a Phase 9 `ThemedDataGridView` (already shipped TJT-side in Phase 9, inherited verbatim per 10-UI-SPEC § Inheritance) — Phase 10 does NOT re-create it. Phase 10 binds it with TWO editable columns (Key, Text) + one optional hidden read-only id column."
  - "DEVIATION (Rule 3): the WinForms form is NOT framework-test-coverable (Phase 8/9 precedent: no TJT-side test project). The ONLY framework-side test in this plan is StringTableNameValidationTests, which covers the pure `ValidateName` predicate the cell-validator delegates to (no WinForms instantiation, no TJT reference). The full form behavior is covered by the 10-06 maintainer smoke."
  - "The singleton-form hide-not-dispose pattern reuses Phase 9's `UtinniCoreDotNet.UI.SingletonFormClosePolicy` (already shipped + tested) — Phase 10 does NOT re-ship the policy or its test; FormStringTableEditor_FormClosing delegates to the existing helper."
must_haves:
  truths:
    - "A new `FormStringTableEditor` (resizable `UtinniForm`, default 1000×720, min 760×520, title `String-table Editor` with leading ● dirty marker) opens a `.stf` from disk via toolbar `Open…` and renders entries in a Phase-9 `ThemedDataGridView` with two editable columns (Key, Text) plus one optional hidden read-only id column (`Show id` toggle, default OFF) per 10-UI-SPEC § Columns."
    - "The Text column is `WrapMode=True` (multi-line localized strings) and applies NO validation, NO transformation — UTF-16LE verbatim (the SC4 Unicode-fidelity non-behavior: no smart-quote, no ellipsis/dash/CR normalization, no typo-fix; the editing TextBox has any OS/IME auto-substitution OFF)."
    - "The Key column is editable + validated on commit via `MutableStringTableDocument.ValidateName` (10-01): leading-digit / duplicate / empty are rejected — cell BackColor=Color.Red, status strip shows the locked validation copy at Color.Red, the edit REVERTS (CellValidating/CellEndEdit enforces before the controller sees it). The Text column NEVER enters this state. ASCII-only enforced for KEYS; full UTF-16 allowed for TEXT (Q3)."
    - "The four T4 verbs route through `StringTableEditController` so they are undoable: edit text (commit-on-CellEndEdit, not per-keystroke), `Add entry` (auto-id + `{NNN}_default` name, new Key cell enters edit mode for immediate rename), `Remove entry` (toolbar or Delete on a selected row), rename key. Ctrl+Z/Ctrl+Y in ProcessCmdKey dispatch to the controller (caught at the form BEFORE the grid; MUST NOT hit the scene UndoRedoManager — CF-04)."
    - "Dirty visuals per Phase 8/9 idiom: edited/renamed cell ForeColor=Colors.Secondary() + row-header ● glyph; added row ＋ glyph; window title leading ●; lblDirty `Unsaved changes` at Colors.Secondary(). The reload-status badge shows the LOCKED CF-05 tier-(b) wording `Reloads on next scene change.` from the moment a file loads."
    - "`FormStringTableEditor` is registered in `Plugin.cs` `GetForms()` inside its own try/catch isolation block (MEF SPI unchanged, CON-M-01/02; mirrors the Phase 7/8/9 entries). Singleton hide-not-dispose on CloseReason.UserClosing via the existing `SingletonFormClosePolicy`."
    - "Settings persistence via UtINI `[StringTableEditor]` section: width, height, findReplaceVisible, filterVisible, showIdColumn, looseOverrideDir (mirrors Phase 8/9). Save▾ + reload-button + Find/Replace/Filter toolbar controls are RENDERED DISABLED with explanatory tooltips (NOT NotImplementedException) — 10-04 wires Find/Replace + Filter, 10-05 wires Save▾ + reload + entry-point hand-off."
  artifacts:
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStringTableEditor.cs"
      provides: "Singleton UtinniForm host; LoadDocument(MutableStringTableDocument, OpenSource, string); Open… file picker; ThemedDataGridView two-column bind (Key editable+validated, Text editable+multiline+verbatim) + optional hidden read-only id column + Show id toggle; T4 verbs via StringTableEditController; CellValidating name-validation; ProcessCmdKey Ctrl+Z/Y/S; UpdateDirtyVisuals; reload badge CF-05 locked copy; BuildSaveMenu DISABLED stubs; hide-not-dispose via SingletonFormClosePolicy"
      contains: "FormStringTableEditor"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStringTableEditor.Designer.cs"
      provides: "Hand-written Designer; Controls.Add order per CF-09 (gridSurface Fill added FIRST, then pnlStatus Bottom, pnlFilter Top hidden, pnlFindReplace Top hidden, toolbar Top LAST); toolbar buttons incl. deferred-feature buttons rendered DISABLED with tooltips"
      contains: "this.Controls.Add(this.gridSurface)"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs"
      provides: "MEF entry — new try/catch block adds new FormStringTableEditor(this) to GetForms() (mirrors Phase 9 FormDatatableEditor registration)"
      contains: "FormStringTableEditor"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"
      provides: "Old-style explicit-compile — adds <Compile Include> for FormStringTableEditor.cs (<SubType>Form</SubType>) + .Designer.cs (<DependentUpon>)"
      contains: "FormStringTableEditor.cs"
    - path: "UtinniCoreDotNet.Tests/FormatsTests/StringTable/StringTableNameValidationTests.cs"
      provides: "~6-8 [Fact]s on the pure ValidateName predicate (leading-digit reject, duplicate reject, empty reject, ASCII-only reject for keys, valid name accept, exclude-self on rename) — the framework-side cover for the WinForms cell-validator"
      contains: "StringTableNameValidationTests"
  key_links:
    - from: "FormStringTableEditor.LoadDocument"
      to: "ThemedDataGridView two-column bind (Key + Text [+ hidden id])"
      via: "host wires the MutableStringTableDocument entries to the grid; Phase 9 ThemedDataGridView reused verbatim"
      pattern: "ThemedDataGridView"
    - from: "FormStringTableEditor CellValidating (Key column)"
      to: "MutableStringTableDocument.ValidateName (10-01)"
      via: "engine name rules enforced before the rename reaches the controller; invalid → Color.Red + revert"
      pattern: "ValidateName"
    - from: "FormStringTableEditor T4 toolbar/grid edits"
      to: "StringTableEditController.Apply(StringTableEditCommands.*)"
      via: "all four T4 verbs undoable; commit-on-CellEndEdit; Ctrl+Z/Y dispatch to controller not scene UndoRedoManager (CF-04)"
      pattern: "StringTableEditController"
    - from: "Plugin.cs GetForms()"
      to: "new FormStringTableEditor(this)"
      via: "MEF singleton registration in try/catch isolation block (Phase 9 precedent)"
      pattern: "FormStringTableEditor"
    - from: "FormStringTableEditor_FormClosing"
      to: "UtinniCoreDotNet.UI.SingletonFormClosePolicy.ShouldHideInsteadOfDispose (Phase 9, reused)"
      via: "singleton hide-not-dispose; do NOT re-ship the policy"
      pattern: "ShouldHideInsteadOfDispose"
---

<objective>
Ship the TJT-side **editor shell + two-column grid + T4 mutation** for Phase 10. This plan introduces
`FormStringTableEditor` (a resizable `UtinniForm` singleton, sibling of FormDatatableEditor, mirroring
its host shape with a STRICTLY SIMPLER surface per 10-UI-SPEC: a two-column Key/Text grid — no per-type
cell widgets, no type-change cascade, no column structural ops). It binds the Phase-9 `ThemedDataGridView`
(inherited verbatim), wires the four T4 verbs through 10-01's `StringTableEditController`, enforces
name validation on key edits via 10-01's `ValidateName`, and ships the SC4 Unicode-fidelity non-behavior
on the Text column (verbatim UTF-16LE; no smart-quote/typo-fix; OS auto-substitution off).

Purpose: SC1 (subpanel loads in TJT) and the editable surface of SC2 (open / view entries with keys /
edit text). The Save▾ / reload / Find/Replace / Filter / hand-off surfaces are RENDERED DISABLED here and
wired by 10-04 (bulk) + 10-05 (save + entry points). Entry points in THIS plan: file picker `Open…` only
(the TRE Browser hand-off is 10-05).

Output: 1 form + Designer + Plugin.cs registration + csproj; 1 framework-side name-validation test (the
form itself is maintainer-smoke per the Rule-3 deviation).

**Why Wave 2:** depends on 10-01's controller + model + ValidateName; it is the host that 10-04 and 10-05
(both Wave 3) extend.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-tjt-subpanel-string-table-editor-stf/10-CONTEXT.md
@.planning/phases/10-tjt-subpanel-string-table-editor-stf/10-UI-SPEC.md
@.planning/phases/10-tjt-subpanel-string-table-editor-stf/10-01-PLAN.md

# Phase 9 host pattern — port VERBATIM with the two-column grid replacing the typed multi-column grid
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.Designer.cs

# Phase 9 ThemedDataGridView (inherited verbatim) + Plugin.cs registration analog
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/ThemedDataGridView.cs
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj

# Phase 9 framework helper reused (do NOT re-ship)
@UtinniCoreDotNet/UI/SingletonFormClosePolicy.cs

# 10-01 framework primitives the form consumes
@UtinniCoreDotNet/Formats/StringTable/StringTableDocument.cs
@UtinniCoreDotNet/Formats/StringTable/MutableStringTableDocument.cs
@UtinniCoreDotNet/Formats/StringTable/MutableStringTableEntry.cs
@UtinniCoreDotNet/Editing/StringTableEditController.cs
@UtinniCoreDotNet/Editing/StringTableEditCommands.cs

<interfaces>
From FormDatatableEditor.cs (the verbatim host shape to mirror):
- public partial class FormDatatableEditor : UtinniForm, IEditorForm
- public FormDatatableEditor(IPlugin plugin)
- public void LoadDocument(...)   // Phase 10 → LoadDocument(MutableStringTableDocument, OpenSource, string)
- protected override bool ProcessCmdKey(ref Message msg, Keys keyData)   // Ctrl+Z/Y/S; Find/Replace/Filter deferred to 10-04
- private void FormDatatableEditor_FormClosing(...)   // hide-not-dispose via SingletonFormClosePolicy
- private void CreateSettings()   // [DatatableEditor] → [StringTableEditor]
- private void BuildSaveMenu() / RefreshSaveMenuEnabledState()   // DISABLED stubs in Phase 10-03; 10-05 wires
- private void UpdateDirtyVisuals()   // ● glyph + lblDirty
From UtinniCoreDotNet.UI.SingletonFormClosePolicy:
- public static bool ShouldHideInsteadOfDispose(CloseReason reason)
From 10-01:
- StringTableDocument.FromBytes(byte[]) → { Version, NextUniqueId, Mutable }
- MutableStringTableDocument { Entries, NextUniqueId, AddEntry(), RemoveEntryByName(), RenameKey(), ValidateName(name, exclude) → {Ok, Reason} }
- MutableStringTableEntry { Id, Name, Text, SourceCrc }
- StringTableEditController { Apply, Undo, Redo, IsDirty, MarkSaved, EditApplied }
- StringTableEditCommands.{EditText, AddEntry, RemoveEntry, RenameKey}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FormStringTableEditor + Designer + Plugin.cs registration + framework name-validation test</name>
  <files>../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStringTableEditor.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStringTableEditor.Designer.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj, UtinniCoreDotNet.Tests/FormatsTests/StringTable/StringTableNameValidationTests.cs</files>
  <read_first>
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs (FULL — the host shape; ctor, LoadDocument, CreateSettings, SetTitle, ProcessCmdKey, OnEditApplied+UpdateDirtyVisuals, BuildSaveMenu DISABLED stubs, FormClosing hide-not-dispose; Phase 10 drops the per-type cell widgets, cascade, and column ops)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.Designer.cs (the Controls.Add ORDER per CF-09; Phase 10 ports: gridSurface Fill FIRST → pnlStatus Bottom → pnlFilter Top hidden → pnlFindReplace Top hidden → toolbar Top LAST)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Controls/ThemedDataGridView.cs (the inherited grid; Phase 10 binds Key+Text columns; AllowUserToOrderColumns=false; Text column WrapMode=True)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs (the try/catch-isolated GetForms() entries; insert a fourth identical block for FormStringTableEditor)
    - .planning/phases/10-tjt-subpanel-string-table-editor-stf/10-UI-SPEC.md (§ Host Placement Decision, § Columns, § Name validation, § Layout Contract, § States, § Copywriting incl. the LOCKED CF-05 reload-badge text and the name-validation copy, § Unicode Fidelity non-behavior, § Add-order rules)
    - UtinniCoreDotNet/UI/SingletonFormClosePolicy.cs (reused — do NOT re-ship)
    - UtinniCoreDotNet/Editing/StringTableEditController.cs + StringTableEditCommands.cs + Formats/StringTable/MutableStringTableDocument.cs (10-01 public API the form drives)
  </read_first>
  <behavior>
    - FormStringTableEditor : UtinniForm, IEditorForm. ctor(IPlugin). Default 1000×720, min 760×520, title `String-table Editor` with leading ● when dirty. Persist [StringTableEditor] width/height/findReplaceVisible/filterVisible/showIdColumn/looseOverrideDir via CreateSettings.
    - Open… toolbar button → OpenFileDialog (filter `String tables (*.stf)|*.stf|All files|*.*`) → background read → StringTableDocument.FromBytes → LoadDocument(doc.Mutable, OpenSource.LooseFile, fileName). LoadDocument: instantiate StringTableEditController(mutable); subscribe EditApplied → UpdateDirtyVisuals; bind the grid.
    - Grid bind: two DataGridViewTextBoxColumns — Key (FillWeight ~35) + Text (FillWeight ~65, DefaultCellStyle.WrapMode=True, AutoSizeRowsMode planner discretion). One optional DataGridViewTextBoxColumn id (ReadOnly=true, ForeColor=Colors.FontDisabled(), BackColor=Colors.PrimaryShadow(), Visible=showIdColumn, default OFF, left of Key). AllowUserToAddRows=false / AllowUserToDeleteRows=false / AllowUserToOrderColumns=false. Populate one grid row per MutableStringTableEntry.
    - Show id toggle (UtinniToggleButton) flips the id column Visible + persists [StringTableEditor] showIdColumn.
    - Edit commit-back: CellEndEdit on the Text column → controller.Apply(StringTableEditCommands.EditText(entry, newText)). CellValidating on the Key column → run MutableStringTableDocument.ValidateName(newName, excludeEntry); if !Ok set e.Cancel=true, cell.BackColor=Color.Red, lblStatus.Text=result.Reason at Color.Red, the edit reverts; if Ok let CellEndEdit run controller.Apply(StringTableEditCommands.RenameKey(entry, newName)). The Text column has NO validation. Disable any OS/IME smart-substitution on the editing TextBox (the SC4 non-behavior: no smart-quote, no ellipsis/dash/CR normalization, no typo-fix — text bytes commit verbatim).
    - Add entry button → controller.Apply(StringTableEditCommands.AddEntry(mutable)); select the new row; put the Key cell into edit mode immediately (BeginEdit) so the user renames the {NNN}_default name.
    - Remove entry button (and Delete on a selected row header) → controller.Apply(StringTableEditCommands.RemoveEntry(entry)).
    - Undo/Redo toolbar buttons + Ctrl+Z/Ctrl+Y in ProcessCmdKey → controller.Undo()/Redo(); caught at the form BEFORE the grid; MUST NOT dispatch to the scene UndoRedoManager (CF-04).
    - UpdateDirtyVisuals: edited/renamed cell ForeColor=Colors.Secondary() + row-header ● glyph; added row ＋ glyph; title leading ●; lblDirty `Unsaved changes` at Colors.Secondary(); counters `{entries} entries · {dirty} dirty`.
    - Reload-status badge: lblReloadBadge shows the LOCKED CF-05 text `Reloads on next scene change.` from the moment a file loads (the actual reload dispatch is 10-05).
    - BuildSaveMenu / RefreshSaveMenuEnabledState: ship DISABLED stubs (items disabled with explanatory tooltips, NOT throwing) — 10-05 wires them. Find / Replace / Filter / Import CSV… / Export CSV… / Export PO… / Reload toolbar buttons rendered DISABLED with tooltips — 10-04 + 10-05 wire them.
    - FormClosing: delegate to SingletonFormClosePolicy.ShouldHideInsteadOfDispose(e.CloseReason) — on UserClosing cancel + Hide() (dirty-discard prompt wiring is fine to stub to a simple e.Cancel for now; the FormSaveConfirmDialog discard flow is 10-05's concern when save exists — for 10-03 a plain hide is acceptable since nothing persists yet).
    - Plugin.cs: add a try/catch block adding new FormStringTableEditor(this) to GetForms().
    - csproj: <Compile Include="UI\Forms\FormStringTableEditor.cs"><SubType>Form</SubType></Compile> + the .Designer.cs with <DependentUpon>FormStringTableEditor.cs</DependentUpon>.
    - StringTableNameValidationTests (framework-side, ~6-8 facts): ValidateName accepts a valid ASCII key; rejects empty (Reason matches the locked copy); rejects leading-digit; rejects duplicate (another entry with same name); rejects non-ASCII key; on rename, excludeEntry lets an entry keep its own name (not flagged duplicate against itself).
  </behavior>
  <action>
    Create FormStringTableEditor.cs + .Designer.cs (hand-written, no .resx) mirroring FormDatatableEditor's
    host shape with the simpler two-column surface; 23-line MIT header (copy from a TJT file), Allman,
    4-space, `// ToDo` (no colon). Apply the 10-UI-SPEC layout + add-order + copywriting verbatim (LOCKED
    CF-05 reload text; locked name-validation copy). Add the Plugin.cs try/catch block. Add the 2 csproj
    Compile entries. Create the framework-side StringTableNameValidationTests.cs (SDK auto-glob — no
    test-csproj edit). Cross-repo paired commit. Build via VS2026 MSBuild Debug|x86.
  </action>
  <verify>
    <automated>powershell -Command "& 'D:/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe' '../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj' /p:Configuration=Debug /p:Platform=x86 /v:minimal" 2>&1 | tail -10  # build green; grep -c "UI\\\\Forms\\\\FormStringTableEditor.cs" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"  # MUST return 1; grep -c "FormStringTableEditor" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Plugin.cs"  # MUST return >= 1; grep -c "ValidateName" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStringTableEditor.cs"  # MUST return >= 1 (key cell-validator delegates to the framework predicate); dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~StringTableNameValidationTests" 2>&1 | tail -10  # MUST exit 0 with >= 6 facts</automated>
  </verify>
  <done>FormStringTableEditor + Designer ship + build clean Debug|x86; Plugin.cs registers it in GetForms(); the Key cell-validator delegates to ValidateName; the Text column has no validation/transformation; the four T4 verbs route through StringTableEditController; reload badge shows the locked CF-05 copy; deferred-feature buttons are disabled-with-tooltip (not throwing); StringTableNameValidationTests green with >= 6 facts; cross-repo paired commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → model | Key renames + added-entry names are user text crossing into the engine name rules |
| disk → process | Open… reads a user-chosen .stf (inherits 10-01 FromBytes guards) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Tampering (invalid key corrupts lookup) | Key column rename | mitigate | CellValidating delegates to ValidateName (leading-digit/duplicate/empty/non-ASCII rejected) before the rename reaches the controller; invalid → revert. Covered by StringTableNameValidationTests. |
| T-10-08 | Tampering (text corruption) | Text column editor | mitigate | UTF-16LE verbatim; OS/IME smart-substitution off; no smart-quote/typo-fix (SC4 / STAB-03). The byte-exact proof is the 10-01/10-02 gates; this plan ensures the UI does not introduce a substitution. |
| T-10-09 | DoS (editor crash on re-open) | singleton form + MEF re-open | mitigate | Hide-not-dispose via the Phase 9 SingletonFormClosePolicy (already tested); MEF registration in a try/catch isolation block. |
| T-10-SC | Tampering | npm/pip/cargo installs | N/A | Phase 10 adds ZERO external packages. |
</threat_model>

<verification>
1. TJT MSBuild Debug|x86 + Release|x86 succeed.
2. `dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~StringTableNameValidationTests"` exits 0 with >= 6 facts.
3. `grep -c "FormStringTableEditor" Plugin.cs` >= 1; csproj has the Form + Designer Compile entries.
4. `grep -c "ValidateName" FormStringTableEditor.cs` >= 1; the Text column has no validation/transformation hook.
5. Full UtinniCoreDotNet.Tests + PreservationAudit suites stay green.
</verification>

<success_criteria>
- SC1: the subpanel loads in TJT (registered via GetForms()).
- SC2 (edit half): open a .stf, view entries with keys, edit text in a two-column grid; T4 add/remove/rename via the undoable controller.
- SC4 (UI half): the Text column commits UTF-16LE verbatim — no smart-quote/typo-fix substitution.
- Name validation (D-01/CF-04) + ASCII-only keys (Q3) enforced on commit; id machine-managed (never user-edited).
- Reload badge shows the locked CF-05 tier-(b) copy; Save▾/Find/Replace/Filter/hand-off rendered disabled for 10-04/10-05.
</success_criteria>

<output>
Create `.planning/phases/10-tjt-subpanel-string-table-editor-stf/10-03-SUMMARY.md` when done. Note the
AutoSizeRowsMode chosen for the multi-line Text column, the exact mechanism used to disable OS/IME
smart-substitution on the Text editing control, and confirm the cross-repo paired commit landed.
</output>
