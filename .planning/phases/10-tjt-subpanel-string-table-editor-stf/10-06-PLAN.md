---
phase: 10-tjt-subpanel-string-table-editor-stf
plan: 06
type: execute
wave: 5
depends_on: ["10-01", "10-02", "10-03", "10-04", "10-05"]
files_modified:
  - .planning/phases/10-tjt-subpanel-string-table-editor-stf/10-06-SMOKE-LOG.md
autonomous: false
requirements: [PROD-W1-STF]
assumes:
  - "Plans 10-01..05 are complete and CI-green: format core + controller (10-01); roundtrip-stf SC4 golden gate (10-02); FormStringTableEditor host + two-column grid + T4 (10-03); Find/Replace + filter + sort + CSV + PO (10-04); Save▾ modes 1/2/4 + reload + TRE Browser hand-off (10-05). Combined ~80+ framework + CLI [Fact]s across UtinniCoreDotNet.Tests + Utinni.Cli.Tests."
  - "Phase 8/9 smoke precedent (smoke=automation-augmented; live ACK deferred-but-acceptable for V1) applies verbatim: the byte-exact round-trip (incl. João SC4), the CSV/PO logic, the name validation, the save-target on-disk contract, and the reload routing are ALL automated. The Tier-4 maintainer-driven live-SWG observation residual is documented but NOT hard-blocking for V1 sign-off (Phase 8 P05/P06/P07 + Phase 9 09-07 precedent)."
  - "The maintainer's live-SWG observation is the only way to verify (a) the form opens against an injected SWG client, (b) the user's TJT chat-command scene change re-resolves an edited .stf (SC3 — OR confirms the LocalizationManager relog-only reality, in which case the CF-05 badge copy is amended to say so honestly), (c) the two entry points (file picker + TRE Browser hand-off) work under live MEF, (d) the singleton-form pattern survives multiple open/close cycles. None can be automated without a mock SWG client (V2 — REQ-V2-tier-3-mock-d3d9)."
  - "CF-05 RESEARCH confirmation is FOLDED INTO this smoke: whether an edited .stf re-resolves on scene change vs needs relog (LocalizationManager cache semantics). If the smoke reveals relog-only, the disposition is to AMEND the CF-05 badge copy to the honest relog wording (a small 10-07 gap-closure or inline fix) — NOT to claim scene-change reload works when it does not."
must_haves:
  truths:
    - "All Phase 10 automation gates (Plans 10-01..05) are green on CI before the smoke runs: full UtinniCoreDotNet.Tests + Utinni.Cli.Tests + PreservationAudit suites pass on Debug|x86 + Release|x86 across BOTH repos."
    - "The maintainer (or executor-driven automation-augmented disposition per Phase 8/9 precedent) observes: the String-table Editor opening against live SWG; opening a .stf via the file picker AND the TRE Browser `Open in String-table Editor` hand-off; editing text + adding an entry + renaming a key (with name-validation reject on a bad key) + removing an entry; saving via each enabled mode; triggering a TJT chat-command scene change and observing whether the edit propagates (SC3) OR documenting the relog-only reality."
    - "SC4 Unicode fidelity is confirmed: a `João`-bearing entry survives edit→save→reload with NO smart-quote substitution, NO STAB-03 typo-fix regression (the automated roundtrip-stf João golden is the primary proof; the smoke is the live confirmation)."
    - "Phase 10 SUMMARY documents the smoke outcome with the Phase 8/9 vocabulary: 'smoke=automation-augmented; live ACK deferred-but-acceptable for V1' OR 'smoke=full-live-OK'. The CF-05 reload-semantics finding (scene-change vs relog) is recorded with the badge-copy disposition. SC3 (F5b) is NOT signed off by automation alone — its status (confirmed via Step 7, or relog-amended, or named open residual) is recorded explicitly, along with the stale-crc check result."
    - "If the smoke reveals a defect affecting any of the 4 PROD-W1-STF success criteria, it is documented in 10-06-SMOKE-LOG.md AND a decision is recorded with maintainer sign-off: (a) inline fix / 10-07 gap-closure, (b) deferred-acceptable for V1 with rationale, (c) ship-blocker requiring re-plan. SC4 corruption is ALWAYS blocking."
  artifacts:
    - path: ".planning/phases/10-tjt-subpanel-string-table-editor-stf/10-06-SMOKE-LOG.md"
      provides: "Maintainer-signed smoke observation log: per-step pass/fail, defects (if any), the CF-05 scene-change-vs-relog finding + badge-copy disposition, V1 sign-off recommendation"
      contains: "smoke=automation-augmented"
  key_links:
    - from: "Plans 10-01..05 automated test surface (~80+ facts)"
      to: "Plan 10-06 smoke=automation-augmented outcome"
      via: "Phase 8/9 precedent (smoke approved on automation + augmented by live observation; live ACK deferred-but-acceptable)"
      pattern: "automation-augmented"
    - from: "10-06-SMOKE-LOG.md CF-05 finding"
      to: "the reload-status badge copy (kept or amended to relog-honest wording)"
      via: "the only remaining CF-05 RESEARCH confirmation; disposition recorded in the smoke log"
      pattern: "scene change|relog"
---

<objective>
Phase 10's **Tier-4 maintainer-driven live-SWG smoke** — the only plan requiring a human-action
checkpoint (Phase 8/9 precedent + memory `feedback_max_harness`: harness-first when achievable; for
Phase 10 the bulk of the testable surface IS automated, with only the live-client residual remaining).

Per Phase 8 Plan 08-07 maintainer direction, the on-disk save contract + byte-exact round-trip (incl.
the João SC4 golden) + CSV/PO logic + name validation + reload routing are ALL covered by 10-01..05's
automation (~80+ facts). What remains is the live-SWG observation: the form opening against an injected
client, the two entry points (file picker + TRE Browser hand-off) under live MEF, the singleton
hide-not-dispose surviving open/close cycles, and the CF-05 tier-(b) reload-on-next-scene-change
manifesting (or the LocalizationManager relog-only reality surfacing — in which case the badge copy is
amended honestly, NOT loosened to over-promise).

Outcome framing (Phase 8 P05/P06/P07 + Phase 9 09-07): smoke is approved on automation OR live
observation OR automation-only — Open Q residuals deferred-but-acceptable. The plan documents the smoke
surface, the maintainer steps, the disposition options, and feeds the result into the consolidated
Phase 10 SUMMARY for V1 sign-off.

**Why Wave 5 (single-task checkpoint):** the natural integration gate after all five implementation
plans land; depends on all of them; requires a human at a live SWG injection session.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-tjt-subpanel-string-table-editor-stf/10-CONTEXT.md
@.planning/phases/10-tjt-subpanel-string-table-editor-stf/10-UI-SPEC.md
@.planning/phases/10-tjt-subpanel-string-table-editor-stf/10-01-SUMMARY.md
@.planning/phases/10-tjt-subpanel-string-table-editor-stf/10-02-SUMMARY.md
@.planning/phases/10-tjt-subpanel-string-table-editor-stf/10-03-SUMMARY.md
@.planning/phases/10-tjt-subpanel-string-table-editor-stf/10-04-SUMMARY.md
@.planning/phases/10-tjt-subpanel-string-table-editor-stf/10-05-SUMMARY.md

# Phase 9 smoke precedent — the "automation-augmented" outcome shape Plan 10-06 mirrors
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-07-SMOKE-LOG.md
@.planning/phases/09-tjt-subpanel-datatable-editor-tab/09-07-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Maintainer Tier-4 live-SWG smoke for Phase 10 String-table Editor (smoke=automation-augmented; Phase 8/9 precedent)</name>
  <what-built>
    Plans 10-01..05 have shipped the complete Phase 10 String-table Editor feature surface:
    - Format core (10-01): UtinniCoreDotNet/Formats/StringTable/ with the flat-binary FromBytes reader
      (greenfield), mutable model, explicit byte-exact writer (canonical order + nextUniqueId/sourceCrc
      preserved; UTF-16LE verbatim, no smart-quote unfubar), StringTableEditController + 4 T4 commands.
    - CLI gate (10-02): utinni-cli roundtrip-stf with the SC4 byte-exact golden suite incl. the named
      João non-ASCII golden + the D-02b sourceCrc-preserve golden.
    - TJT host + grid (10-03): FormStringTableEditor with the two-column (Key, Text) ThemedDataGridView,
      name validation, T4 mutation via the controller, reload badge (CF-05 locked), Plugin.cs registration.
    - Bulk/translation (10-04): Find/Replace (key+text), CSV/TSV delta-import + export (UTF-8+BOM, per-entry
      diff, single-transaction undo, preview modal), view-only sort + live filter (Ctrl+L), PO/gettext export.
    - Save + entry points (10-05): StringTableSaveTargets (modes 1/2/4; mode 3 disabled; V6000 refusal),
      reload dispatch (tier-(b)), TRE Browser `Open in String-table Editor` hand-off (D-04).

    Combined automated coverage: ~80+ xUnit [Fact]s across UtinniCoreDotNet.Tests + Utinni.Cli.Tests +
    PreservationAudit; full suites green on Debug|x86 + Release|x86 across both repos.

    What remains is the maintainer-driven live-SWG observation that automation cannot reach without a mock
    SWG client (V2 — REQ-V2-tier-3-mock-d3d9), PLUS the one open CF-05 RESEARCH confirmation (scene-change
    reload vs LocalizationManager relog-only).
  </what-built>
  <how-to-verify>
    **Smoke disposition (Phase 8/9 precedent — choose ONE):**
    - Option A (full live observation): run all steps against an injected SWG session; record in 10-06-SMOKE-LOG.md.
    - Option B (automation-augmented): review the 10-01..05 passing facts + run the subset that does not need deep in-game observation; defer the live-ACK residual with the Phase 8/9 vocabulary.
    - Option C (automation-only): accept the automation surface alone for V1 sign-off of SC1/SC2/SC4 (Phase 8 P06 / Phase 9 09-07 precedent); defer the rest of the full smoke. NOTE (F5b): Option C is INSUFFICIENT for SC3 specifically — "live client renders edited strings on reload" cannot be signed off by automation alone; SC3 still requires the Step 7 live observation OR the honest relog-badge amendment (with the ROADMAP SC3=relog interpretation note). Choosing Option C therefore leaves SC3 as an explicit open residual until Step 7 (or the relog amendment) is done.

    **Smoke steps (Options A + B — perform what you can; document skips):**

    1. **Build + launch:** Build both repos Debug|x86 via VS2026 MSBuild; launch the Utinni editor host
       alongside an injected SWGEmu / Restoration client (memory `project_swg_context_routing` +
       `project_scene_change_via_tjt`). Confirm no JIT-debugger pop; confirm TJT loads + the String-table
       Editor is registered in GetForms (Plugin.cs try/catch block from 10-03).

    2. **Entry point 1 — File picker:** Open the String-table Editor; click `Open…`; pick a real `.stf`
       (extract one via the TRE Browser to a temp path if needed). Confirm: the two-column grid populates
       (Key + Text); the Text column shows multi-line localized text legibly; the reload badge reads the
       LOCKED CF-05 copy `Reloads on next scene change.`; `Show id` toggle reveals/hides the read-only id column.

    3. **Entry point 2 — TRE Browser hand-off (D-04):** Open the TRE Browser; right-click a `.stf` entry;
       confirm `Open in String-table Editor` appears (gated on StringTableHandoffPolicy); click it. Confirm
       the editor opens (or activates the singleton) with the entry loaded; provenance is TreArchive
       (visible indirectly: Save loose override + Save As + Repack enabled; Save in place disabled). Confirm
       there is NO IFF-Editor hand-off for `.stf` (D-04 — `.stf` is not IFF).

    4. **T4 edit + validation:** Edit a Text cell (commit-on-CellEndEdit); confirm ● row-header glyph +
       title ● + lblDirty. Ctrl+Z / Ctrl+Y → undo/redo. `Add entry` → new row with `{NNN}_default` name,
       Key cell in edit mode. Rename the key to a valid ASCII name. Try an INVALID key (leading digit /
       duplicate / empty) → confirm cell Color.Red + status red validation copy + the edit reverts. Try a
       non-ASCII KEY → rejected; confirm non-ASCII TEXT is allowed. `Remove entry` → row removed (undoable).

    5. **Save modes (10-05):** With a .stf loaded from a .tre (Step 3 provenance), exercise: `Save loose
       override` (green status; file appears under the override dir); `Save As…` (pick temp path; file
       exists); `Repack into source .tre…` (confirm modal; Replaced/backup OR V6000 refusal copy); `Save
       in place` disabled (TreArchive, not LooseFile) — confirm tooltip.

    6. **SC4 Unicode fidelity (live confirmation of the automated João golden):** Open a `.stf`, find or add
       an entry with a non-ASCII string (e.g. `João`, or paste a smart-quote `"curly"` and an ellipsis `…`).
       Save via any mode; re-open the saved file. Confirm the text survives EXACTLY — NO smart-quote
       substitution, NO `…`→`...` or `--` normalization, NO `Jo�o`→`João` typo-fix (STAB-03). (The automated
       roundtrip-stf João golden is the primary proof; this is the live confirmation.)

    7. **CF-05 reload semantics + stale-crc check (PROD-W1-STF SC3 — REQUIRED for SC3 sign-off, NOT automatable; F5b):**
       With an edit saved (Step 5), trigger a TJT chat-command scene change (`project_scene_change_via_tjt`).
       Observe whether the edited string renders in-game after the scene loads. **If it does → SC3 confirmed,
       badge copy correct. If it does NOT (LocalizationManager caches until relog) → SC3 needs relog; record
       this finding and the disposition: AMEND the CF-05 badge copy to the honest relog wording (a 10-07 inline
       fix or gap-closure) AND note that ROADMAP SC3 is interpreted as relog — NOT claim scene-change reload
       works.** (Per memory `project_tjt_scene_change_naked_baseline`: landing naked after the scene change is
       baseline, NOT a regression.)
       **EXPLICIT STALE-CRC CHECK (F5b):** the edited entry's `sourceCrc` is PRESERVED (left stale per D-02b).
       Confirm the edited text STILL RENDERS in-client with the stale crc. If it does NOT render BECAUSE of the
       stale crc, flip the edited entry's sourceCrc to `int(time(0))` (re-save) and record that the preserve-crc
       policy had to be amended for the render to work — this is the live counterpart to the 10-01 F5a
       `getLocalizedStringValue` finding (which predicts lookup does NOT consult sourceCrc, so the stale crc
       should be harmless). Record the observed result either way.
       **SC3 is NOT signed off by automation alone (F5b):** SC3 sign-off REQUIRES either this live Step 7
       observation OR, if reload is relog-only (RESEARCH A8), the honest relog-badge amendment plus the ROADMAP
       SC3=relog interpretation note. The other success criteria (SC1/SC2/SC4) MAY follow the Phase 8/9
       automation-only precedent, but SC3 specifically cannot.

    8. **CSV round-trip (D-03b):** `Export CSV…` → open in Excel/text editor; confirm UTF-8 BOM + `key,text`
       header + non-ASCII survives. Modify ONE text cell in the CSV; `Import CSV…`; confirm the preview modal
       shows the locked D-03b copy (`{N} entries will change. {M} entries will stay as original bytes. New
       keys in the CSV will be added; missing keys are left untouched.`); Import; confirm the single entry
       changes + others stay clean; save → confirm bytes match a direct single-cell edit (byte-exact-on-untouched).

    9. **Find/Replace + Filter + sort (D-03a/c):** Ctrl+F over key+text (case + regex toggles, F3/Shift+F3);
       Replace into a key re-validates (invalid blocked). Ctrl+L filter box hides non-matching rows
       (`{shown} / {total}`), view-only. Column-click sort shows ▲/▼ + the locked tooltip `View order only —
       save serializes strings by id and names alphabetically.`; save → confirm on-disk order unchanged.

    10. **PO export (D-03d):** `Export PO…` → confirm a .po with msgid=key / msgstr=text; non-ASCII survives.

    11. **Singleton hide-not-dispose:** Close the form (X); re-open from a file picker / TRE Browser. Confirm
        NO ObjectDisposedException; the form re-opens cleanly.

    **Pass conditions:** Option A — Steps 1-11 pass (or per-step defects documented). Option B — Steps 1-5 +
    6 + 11 pass (minimum V1 surface); the rest deferred with the deferred-acceptable vocabulary. Option C —
    maintainer accepts the 10-01..05 automation surface alone (CI green + ~80 facts) with the live-ACK
    deferred, citing the Phase 8/9 precedent — EXCEPT SC3 (F5b): Step 7 (the reload-semantics + stale-crc
    observation) is REQUIRED for SC3 sign-off under ANY option; SC3 cannot be closed by automation alone. If
    Step 7 is not yet run, SC3 stays an explicit open residual (sign-off = APPROVED-WITH-DEFERRED-RESIDUAL with
    SC3 named) until the live observation or the honest relog amendment is recorded.

    **Fail conditions:** form does not load (Steps 1-3) → BLOCKING (Plugin.cs/csproj regression → 10-07 fix).
    Saves write corrupted bytes (Step 5) → BLOCKING (10-01 writer or 10-05 dispatch). SC4 corruption (Step 6)
    → ALWAYS BLOCKING. Any other defect → deferred-acceptable UNLESS it affects an SC.

    **Record the outcome in `.planning/phases/10-tjt-subpanel-string-table-editor-stf/10-06-SMOKE-LOG.md`** with:
    smoke option (A/B/C) + rationale; per-step pass/fail; the CF-05 scene-change-vs-relog finding + badge
    disposition; defects + disposition; V1 sign-off recommendation (APPROVED / APPROVED-WITH-DEFERRED-RESIDUAL
    / REQUIRES-GAP-CLOSURE); maintainer signature `Smoke approved by: <name> on <date>; outcome: <vocab>`.
  </how-to-verify>
  <resume-signal>Type "approved" if the smoke passes per Options A/B/C; describe defects if any blocking issues surfaced. Include a one-line summary citing the disposition + the CF-05 finding + the F5b SC3/stale-crc result (e.g. "approved — Option B; SC4 João confirmed; SC3 = relog-only (badge copy amended via 10-07, ROADMAP SC3 read as relog); stale-crc harmless (edit rendered) confirming F5a; live ACK deferred for Steps 8-10 per Phase 9 precedent"). Remember SC3 cannot be signed off by automation alone (F5b) — name it as an open residual if Step 7 was not run.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated CI gate → live SWG observation | The ~80-fact automation covers format/CSV/PO/validation/save/reload; the live residual covers MEF + injection + scene-change + manual-edit-UX that needs a mock SWG client (V2). |
| maintainer judgment → V1 sign-off | Shipping with the CF-05 reload-semantics finding + live-ACK residual is the maintainer's call per Phase 8/9 precedent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-17 | Tampering (silent SC4 corruption found live) | StringTableWriter / save path | mitigate | Byte-exact enforced at TWO automated layers (10-01 writer facts + 10-02 CLI golden incl. João) PLUS the live smoke confirming the saved file re-loads cleanly. SC4 corruption is always blocking. |
| T-10-18 | Repudiation (smoke outcome not recorded) | 10-06-SMOKE-LOG.md | mitigate | The plan REQUIRES recording the outcome + signature in the named artifact (memory `feedback_max_harness` — record in the harness, not in conversation). |
| T-10-19 | Tampering (over-promised reload) | CF-05 badge copy / SC3 | mitigate | SC3 requires the live Step 7 observation (F5b — NOT automation-only); the smoke confirms scene-change-vs-relog + the explicit stale-crc check (edited entry renders with a preserved/stale sourceCrc, confirming the F5a getLocalizedStringValue finding); if relog-only, the badge copy is amended to the honest wording (10-07) and ROADMAP SC3 is interpreted as relog rather than claiming a reload that does not happen. |
| T-10-SC | Tampering | npm/pip/cargo installs | N/A | Plan 10-06 adds ZERO packages — observation + documentation only. |
</threat_model>

<verification>
1. Before the smoke: full suites green on BOTH repos, BOTH configs (UtinniCoreDotNet.Tests, Utinni.Cli.Tests, PreservationAudit; VS2026 MSBuild Debug|x86 + Release|x86 of Utinni.sln + the TJT solution).
2. `.planning/phases/10-tjt-subpanel-string-table-editor-stf/10-06-SMOKE-LOG.md` exists with: smoke option, the performed steps' pass/fail, the CF-05 reload-semantics finding + badge disposition, the V1 sign-off recommendation, the maintainer signature.
3. The consolidated Phase 10 SUMMARY cites the smoke log + records the sign-off disposition.
</verification>

<success_criteria>
- The 4 PROD-W1-STF ROADMAP success criteria are met (per the smoke + automation):
  1. Subpanel loads in TJT against live SWG (Step 1).
  2. Open a .stf (2 entry points — Steps 2-3), view entries with keys, edit text (Step 4), save back (Step 5).
  3. Live client renders edited strings on reload (Step 7 — REQUIRED live observation per F5b, NOT automation-only; OR the relog-only reality is documented + the badge amended + ROADMAP SC3 interpreted as relog; includes the explicit stale-crc check).
  4. Non-ASCII round-trips cleanly, João (Step 6 live + the automated roundtrip-stf João golden).
- The smoke produces a documented V1 sign-off recommendation in 10-06-SMOKE-LOG.md.
- The CF-05 scene-change-vs-relog finding is resolved (confirmed or badge amended).
- Phase 10 is ready for the consolidated sign-off; PROD-W1-STF contributes to the PROD-02 Wave-1 aggregate; the project advances to Phase 11 (Object Template Editor).
</success_criteria>

<output>
Create `.planning/phases/10-tjt-subpanel-string-table-editor-stf/10-06-SMOKE-LOG.md` (the smoke artifact) and
`.planning/phases/10-tjt-subpanel-string-table-editor-stf/10-06-SUMMARY.md` (the plan SUMMARY citing the smoke
log + the phase-end disposition). Phase 10 is COMPLETE when the smoke log + SUMMARY are committed and the
maintainer's V1 sign-off (incl. the CF-05 finding) is recorded.
</output>
