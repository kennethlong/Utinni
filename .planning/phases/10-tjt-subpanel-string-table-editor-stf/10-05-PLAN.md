---
phase: 10-tjt-subpanel-string-table-editor-stf
plan: 05
type: execute
wave: 4
depends_on: ["10-03", "10-04"]
files_modified:
  - UtinniCoreDotNet/UI/StringTableHandoffPolicy.cs
  - UtinniCoreDotNet/UtinniCoreDotNet.csproj
  - UtinniCoreDotNet.Tests/UITests/StringTableHandoffPolicyTests.cs
  - UtinniCoreDotNet.Tests/SavingTests/StringTableReloadRoutingTests.cs
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/StringTableSaveTargets.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStringTableEditor.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs"
  - "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"
autonomous: true
requirements: [PROD-W1-STF]
assumes:
  - "Plan 10-03 shipped FormStringTableEditor with the Save▾ menu + reload button rendered DISABLED (stubs) + the StringTableEditController + MarkSaved seam. Plan 10-05 replaces the stubs with actual save-target dispatch + reload dispatch + the TRE Browser hand-off. 10-05 is SERIAL after 10-04 (both modify FormStringTableEditor.cs; 10-04 lands the bulk-feature wiring first, then 10-05 rebases its Save▾ + reload + hand-off wiring on top — Phase 9 09-04→09-05 Wave-3→Wave-4 precedent)."
  - "Phase 8 framework save helpers (LooseOverridePath.Resolve, TreBackupPath, TreRepackLock) + the plugin-side IffSaveTargets / TreRepackSaveTarget / ClientReloadDispatcher are SHIPPED and READY for verbatim reuse. BUT `.stf` is FLAT, not IFF — the existing IffSaveTargets take a MutableIffDocument. StringTableSaveTargets builds RAW BYTES via StringTableWriter.Serialize and writes them directly (loose-override path-resolution + atomic write + Flush(true) barrier reused from the Phase 8 helpers; the repack path passes the .stf bytes to TreRepackSaveTarget.Apply which is byte-payload-based already). Do NOT force the .stf through IffSaveTargets' MutableIffDocument signature — compose at the bytes layer."
  - "`.stf` is already classified `ReloadTier.PendingNextSceneChange` in `UtinniCoreDotNet/Saving/ReloadAssetClassifier.cs` (`StringtableExtensions = { \".stf\" }` — confirmed present). CF-05 reload badge wiring consumes this directly; do NOT re-implement classification. RESEARCH note (CF-05): confirm whether an edited .stf re-resolves on scene change or needs relog (LocalizationManager cache semantics) — the badge wording is honest either way; if relog-only surfaces in the 10-06 smoke, the badge copy is amended there, not loosened here."
  - "TRE Browser hand-off (D-04) ports the Phase 9 `UtinniCoreDotNet/UI/DatatableHandoffPolicy.cs` framework helper VERBATIM into a `StringTableHandoffPolicy` (framework-side, pure, xUnit-coverable). For `.stf` the gate is SIMPLER: extension `.stf` OR the `StringTableDecoder.LooksLikeStf(payload)` magic sniff (the 0xABCD magic) AND not enumerate-only. There is NO 'datatables/ path segment' rule and NO IFF-Editor hand-off (`.stf` is not IFF — D-04). FormTreBrowser gets a new `Open in String-table Editor` context-menu item gated on this policy, mirroring the existing `_miOpenInDatatableEditor` wiring at FormTreBrowser.cs."
  - "Cross-repo paired commits per `feedback_utinniplugins_authority` — Plan 10-05 modifies Utinni (1 framework policy + its xUnit + 1 reload-routing xUnit) AND UtinniPlugins (1 save shim + FormStringTableEditor Save▾/reload wiring + FormTreBrowser hand-off + csproj). Commit atomically. Build green Debug+Release|x86 in BOTH repos."
  - "Mode 3 (in-memory live patch) stays DISABLED per CF-03 — no Phase 10 open path constructs OpenSource.ClientMemory; the Save▾ ▸ Patch live client item stays disabled with the inherited tooltip. Save modes 1/2/4 (loose override / Save·Save-As / .tre repack; repack refuses V6000 per Phase 8 WR-06) are the V1 set."
must_haves:
  truths:
    - "`StringTableHandoffPolicy.ShouldOfferStringTableEditor(logicalPath, payloadOrNull, enumerateOnly)` (framework, pure) returns true when (extension == `.stf` OR `StringTableDecoder.LooksLikeStf(payload)`) AND !enumerateOnly; false otherwise. xUnit-covered (no WinForms). Ports the Phase 9 DatatableHandoffPolicy shape, simplified for the flat .stf (no datatables/ path rule)."
    - "MAGIC-SNIFF LIVENESS (F11 — Wave-0 verification before wiring): confirm the FormTreBrowser `Open in String-table Editor` call site ACTUALLY HAS the file payload at context-menu-Opening time (the `Opening` event, before any resolve). If the payload is null/lazy there (TrePayloadResolver only resolves on click, mirroring the Datatable hand-off), the menu gate FALLS BACK to extension-based detection (extension == `.stf`) so the magic sniff is a SECONDARY affordance for extension-less `.stf` entries ONLY — never the sole gate. Do NOT ship a magic-sniff path the tests can't exercise: the policy's payload branch is unit-tested in isolation, but the live menu gate uses whatever inputs the call site genuinely has. Record which path (extension-only vs magic-sniff) the call site supports in the SUMMARY."
    - "TRE Browser D-04: a new `Open in String-table Editor` context-menu item appears on selected entries where `StringTableHandoffPolicy.ShouldOfferStringTableEditor(...)` is true (HIDDEN otherwise — Phase 8/9 hand-off shape); click handler mirrors the existing `OnOpenInDatatableEditor` (off-UI-thread TrePayloadResolver.TryResolve → BeginInvoke → FindOrCreateStringTableEditor → editor.OpenFromTreEntry(payload, archivePath, logicalPath, offset)). NO IFF-Editor hand-off (D-04)."
    - "Save▾ ▸ Save (in place) writes the .stf back to its OpenSource.LooseFile path (provenance-gated on Source is LooseFile); Save as loose override writes under the client loose-override dir via LooseOverridePath.Resolve (gated on LooseFile OR TreArchive); Save As… writes to a user-picked path (always enabled unless never-loaded). Each builds bytes via StringTableWriter.Serialize, writes atomically (Flush(true) barrier reused), and on success calls controller.MarkSaved() to reset dirty + clear the ● glyph."
    - "Save▾ ▸ Repack into source .tre… dispatches the StringTableWriter bytes to Phase 8's TreRepackSaveTarget.Apply (gated on Source is TreArchive); V6000 archive refusal inherited (Phase 8 WR-06); on a Replaced/BackedUpThenReplaced outcome calls controller.MarkSaved(). Save▾ ▸ Patch live client stays DISABLED (CF-03). OpenSource.Unknown → only Save As… enabled (Phase 8 escape-hatch)."
    - "After any successful save, the reload path routes via ReloadAssetClassifier.Classify(\".stf\", null) → PendingNextSceneChange (tier-(b)); the reload-status badge stays on the LOCKED CF-05 copy `Reloads on next scene change.` (10-03 already wires the badge text); no fresh binding call. A framework xUnit fact asserts Classify(\".stf\", null) == PendingNextSceneChange (defense-in-depth)."
    - "`FormStringTableEditor.OpenFromTreEntry(byte[] payload, string archivePath, string logicalPath, long offset)` is the public hand-off entry the TRE Browser calls: parses via StringTableDocument.FromBytes, resolves OpenSource (TreArchive via TreRecordIndexResolver.ResolveOrUnknown, mirroring Phase 9), LoadDocument."
  artifacts:
    - path: "UtinniCoreDotNet/UI/StringTableHandoffPolicy.cs"
      provides: "Pure framework hand-off gate — ShouldOfferStringTableEditor(logicalPath, payload, enumerateOnly) + LooksLikeStf delegation; ports DatatableHandoffPolicy shape, simplified for flat .stf; no WinForms/TJT ref"
      contains: "ShouldOfferStringTableEditor"
    - path: "UtinniCoreDotNet/UtinniCoreDotNet.csproj"
      provides: "Old-style explicit-compile — adds <Compile Include> for UI\\StringTableHandoffPolicy.cs"
      contains: "UI\\StringTableHandoffPolicy.cs"
    - path: "UtinniCoreDotNet.Tests/UITests/StringTableHandoffPolicyTests.cs"
      provides: "~8-10 [Fact]s mirroring DatatableHandoffPolicyTests: .stf offered; .stf offered WITH payload==null (F11 — extension-only gate works when the call site has no payload at menu-Opening); magic-sniff offered for an extension-less payload; enumerate-only never offered; non-.stf non-magic not offered; null/empty not offered"
      contains: "StringTableHandoffPolicyTests"
    - path: "UtinniCoreDotNet.Tests/SavingTests/StringTableReloadRoutingTests.cs"
      provides: "~2-3 [Fact]s: Classify(\".stf\", null) == PendingNextSceneChange; no-live-client → ReloadUnavailable for the .stf case (defense-in-depth)"
      contains: "StringTableReloadRoutingTests"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/StringTableSaveTargets.cs"
      provides: "Thin shim — build bytes via StringTableWriter.Serialize then write (loose-override path-resolution + atomic write reused from Phase 8 helpers) or pass bytes to TreRepackSaveTarget.Apply; < 120 lines; composes at the BYTES layer (not IffSaveTargets' MutableIffDocument signature)"
      contains: "StringTableWriter"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStringTableEditor.cs"
      provides: "MODIFY — replace Save▾ DISABLED stubs with click handlers → StringTableSaveTargets; provenance-gated RefreshSaveMenuEnabledState (LooseFile/TreArchive/Unknown); controller.MarkSaved() on save success; reload-button → ClientReloadDispatcher (badge stays CF-05 locked); public OpenFromTreEntry(...) for the D-04 hand-off; dirty-discard prompt on close/open-replace via FormSaveConfirmDialog (now that save exists)"
      contains: "StringTableSaveTargets"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs"
      provides: "MODIFY — add _miOpenInStringTableEditor context-menu item + Visible gate via StringTableHandoffPolicy + OnOpenInStringTableEditor handler + FindOrCreateStringTableEditor helper, mirroring the existing _miOpenInDatatableEditor wiring"
      contains: "FindOrCreateStringTableEditor"
    - path: "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj"
      provides: "Old-style explicit-compile — adds <Compile Include> for Saving\\StringTableSaveTargets.cs"
      contains: "Saving\\StringTableSaveTargets.cs"
  key_links:
    - from: "FormStringTableEditor.OnSaveInPlaceClick / OnSaveLooseOverrideClick / OnSaveAsClick / OnRepackTreClick"
      to: "StringTableSaveTargets.* (build via StringTableWriter.Serialize → write / TreRepackSaveTarget.Apply)"
      via: "bytes-layer composition; on success controller.MarkSaved()"
      pattern: "StringTableSaveTargets"
    - from: "FormStringTableEditor save-success handlers"
      to: "StringTableEditController.MarkSaved() (10-01 seam)"
      via: "reset dirty baseline + clear ● glyph after a successful write"
      pattern: "MarkSaved"
    - from: "FormTreBrowser._miOpenInStringTableEditor.Visible"
      to: "StringTableHandoffPolicy.ShouldOfferStringTableEditor (framework)"
      via: "mirrors the existing _miOpenInDatatableEditor.Visible = DatatableHandoffPolicy.ShouldOfferDatatableEditor(...) line"
      pattern: "ShouldOfferStringTableEditor"
    - from: "FormStringTableEditor.OnReloadClicked"
      to: "ClientReloadDispatcher.Dispatch(savedPath, null) → ReloadAssetClassifier.Classify(\".stf\", null) → PendingNextSceneChange"
      via: "tier-(b) routing; user-facing badge stays the locked CF-05 copy (no fresh binding)"
      pattern: "ClientReloadDispatcher"
    - from: "FormStringTableEditor.OpenFromTreEntry"
      to: "StringTableDocument.FromBytes + TreRecordIndexResolver.ResolveOrUnknown + LoadDocument"
      via: "D-04 TRE Browser hand-off entry; mirrors Phase 9 FormDatatableEditor.OpenFromTreEntry"
      pattern: "OpenFromTreEntry"
---

<objective>
Ship the **save targets + reload + TRE Browser hand-off** for Phase 10 — completing the editor's
connection to disk and live-client reload. This plan introduces a < 120-line `StringTableSaveTargets`
shim that builds raw `.stf` bytes via 10-01's `StringTableWriter.Serialize` and writes them (modes 1/2/4;
mode 3 disabled per CF-03; repack refuses V6000 per Phase 8 WR-06), a framework-side
`StringTableHandoffPolicy` (port of Phase 9's `DatatableHandoffPolicy`, simplified for the flat `.stf`:
extension `.stf` OR the 0xABCD magic sniff, no datatables/ path rule), the TRE Browser
`Open in String-table Editor` hand-off (D-04 — file picker + TRE Browser are the ONLY entry points;
NO IFF-Editor hand-off), and the reload dispatch (tier-(b) PendingNextSceneChange, badge stays the
locked CF-05 copy).

Purpose: SC2 (save back) + SC3 (live client renders edited strings on reload). The byte-exactness from
10-01 flows through unchanged (the shim composes at the bytes layer; it does NOT force `.stf` through
the IFF-shaped IffSaveTargets signature).

Output: 1 framework policy + its xUnit + 1 reload-routing xUnit; 1 TJT save shim + FormStringTableEditor
Save▾/reload wiring + FormTreBrowser hand-off + csproj.

**Why Wave 4 (SERIAL after 10-04):** Plan 10-05 has a hard `depends_on: [10-03, 10-04]` edge — it shares
FormStringTableEditor.cs with 10-04 (files_modified overlap forces the later wave). 10-04 lands the
bulk-feature wiring first; 10-05 rebases its Save▾ + reload + hand-off wiring on the committed form
(Phase 9 09-04→09-05 Wave-3→Wave-4 precedent — non-overlapping method bodies, sequenced, no merge risk).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-tjt-subpanel-string-table-editor-stf/10-CONTEXT.md
@.planning/phases/10-tjt-subpanel-string-table-editor-stf/10-UI-SPEC.md
@.planning/phases/10-tjt-subpanel-string-table-editor-stf/10-01-PLAN.md
@.planning/phases/10-tjt-subpanel-string-table-editor-stf/10-03-PLAN.md
@.planning/phases/10-tjt-subpanel-string-table-editor-stf/10-04-PLAN.md

# Phase 9 hand-off helper — PORT VERBATIM (simplified for flat .stf)
@UtinniCoreDotNet/UI/DatatableHandoffPolicy.cs
@UtinniCoreDotNet.Tests/UITests/DatatableHandoffPolicyTests.cs

# Phase 9 save shim + entry-point wiring — the shape Plan 10-05 mirrors (but composes at bytes layer)
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/DatatableSaveTargets.cs
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs

# Phase 8 save infrastructure — reuse at the bytes layer
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/IffSaveTargets.cs
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/TreRepackSaveTarget.cs
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/ClientReloadDispatcher.cs
@UtinniCoreDotNet/Saving/LooseOverridePath.cs
@UtinniCoreDotNet/Saving/ReloadAssetClassifier.cs
@UtinniCoreDotNet/Formats/Tre/TreRecordIndexResolver.cs
@UtinniCoreDotNet/Formats/Iff/OpenSource.cs

# 10-01 + 10-03 surfaces Plan 10-05 consumes
@UtinniCoreDotNet/Formats/StringTable/StringTableDocument.cs
@UtinniCoreDotNet/Formats/StringTable/StringTableWriter.cs
@UtinniCoreDotNet/Formats/StringTable/MutableStringTableDocument.cs
@UtinniCoreDotNet/Editing/StringTableEditController.cs
@UtinniCoreDotNet/Formats/Decoders/StringTableDecoder.cs
@../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStringTableEditor.cs

<interfaces>
From UtinniCoreDotNet/UI/DatatableHandoffPolicy.cs (the verbatim port shape):
- public static bool ShouldOfferDatatableEditor(string logicalPath, bool enumerateOnly)
- public static bool IsUnderDatatablesPath(string logicalPath)   // Phase 10 OMITS this rule
- public static bool IsDtiiPayload(byte[] payload)               // Phase 10 uses StringTableDecoder.LooksLikeStf instead
From FormTreBrowser.cs (the existing wiring to mirror):
- _miOpenInDatatableEditor.Visible = DatatableHandoffPolicy.ShouldOfferDatatableEditor(pn.FullPath, d.EnumerateOnly);
- OnOpenInDatatableEditor (off-UI-thread TrePayloadResolver.TryResolve → BeginInvoke → FindOrCreateDatatableEditor → editor.OpenFromTreEntry)
From UtinniCoreDotNet/Formats/Decoders/StringTableDecoder.cs:
- public static bool LooksLikeStf(byte[] data)   // 0xABCD magic sniff
From UtinniCoreDotNet/Saving/ReloadAssetClassifier.cs:
- public static ReloadTier Classify(string extension, string rootTypeIdOrNull)   // (".stf", *) → PendingNextSceneChange
- public static readonly string[] StringtableExtensions = { ".stf" }   // already present
From TheJawaToolboxDotNet/Saving/TreRepackSaveTarget.cs:
- public static Task<TreRepackResult> Apply(OpenSource.TreArchive ta, byte[] rewrittenBytes, bool createBackup)   // byte-payload based; refuses V6000
From UtinniCoreDotNet/Saving/LooseOverridePath.cs:
- public static string Resolve(string clientRoot, string subDir, string logicalAssetPath)
From UtinniCoreDotNet/Formats/Tre/TreRecordIndexResolver.cs:
- public static OpenSource ResolveOrUnknown(string treArchivePath, long archiveLocalOffset, string logicalPath)
From UtinniCoreDotNet/Formats/Iff/OpenSource.cs:
- OpenSource.LooseFile { Path } / TreArchive { TreArchivePath, RecordIndex, LogicalPath } / ClientMemory / Unknown.Instance
From 10-01:
- StringTableDocument.FromBytes(byte[]); StringTableWriter.Serialize(MutableStringTableDocument); StringTableEditController.MarkSaved()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StringTableHandoffPolicy (framework) + StringTableSaveTargets shim + reload-routing + handoff xUnit</name>
  <files>UtinniCoreDotNet/UI/StringTableHandoffPolicy.cs, UtinniCoreDotNet/UtinniCoreDotNet.csproj, UtinniCoreDotNet.Tests/UITests/StringTableHandoffPolicyTests.cs, UtinniCoreDotNet.Tests/SavingTests/StringTableReloadRoutingTests.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/StringTableSaveTargets.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj</files>
  <read_first>
    - UtinniCoreDotNet/UI/DatatableHandoffPolicy.cs (FULL — the verbatim port shape; Phase 10 drops the datatables/-path rule, uses StringTableDecoder.LooksLikeStf for the magic sniff)
    - UtinniCoreDotNet.Tests/UITests/DatatableHandoffPolicyTests.cs (the [Theory] table shape StringTableHandoffPolicyTests mirrors)
    - UtinniCoreDotNet/Formats/Decoders/StringTableDecoder.cs (LooksLikeStf — the 0xABCD magic sniff)
    - UtinniCoreDotNet/Saving/ReloadAssetClassifier.cs (Classify + StringtableExtensions — already routes .stf)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/DatatableSaveTargets.cs (the shim shape; Phase 10 composes at the BYTES layer because .stf is not IFF)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/IffSaveTargets.cs (the atomic-write + Flush(true) barrier + path-defense to reuse at the bytes layer)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/TreRepackSaveTarget.cs (byte-payload Apply + V6000 refusal)
    - UtinniCoreDotNet/Saving/LooseOverridePath.cs (Resolve — root-containment helper)
    - UtinniCoreDotNet/Formats/StringTable/StringTableWriter.cs + MutableStringTableDocument.cs (10-01 — bytes the shim writes)
    - UtinniCoreDotNet/UtinniCoreDotNet.csproj + the TJT csproj (old-style explicit-compile)
  </read_first>
  <behavior>
    - StringTableHandoffPolicy (framework, pure, namespace UtinniCoreDotNet.UI): ShouldOfferStringTableEditor(string logicalPath, byte[] payloadOrNull, bool enumerateOnly) → true iff !enumerateOnly AND (extension(logicalPath) == ".stf" (case-insensitive) OR (payloadOrNull != null AND StringTableDecoder.LooksLikeStf(payloadOrNull))). Null/empty path + null payload → false. No datatables/-path rule. (If the FormTreBrowser call site does not have the payload at menu-Opening time, the policy is fine to call with payload=null and gate on extension alone — the magic sniff is a secondary affordance for extension-less .stf entries; mirror however the existing DatatableHandoffPolicy call site supplies its inputs.)
    - StringTableSaveTargets (TJT, namespace TheJawaToolboxDotNet.Saving, static): four methods composing at the BYTES layer:
      - SaveInPlace(MutableStringTableDocument doc, OpenSource source) — gated on LooseFile; bytes = new StringTableWriter(doc).Serialize(); atomic write to source.Path with Flush(true) barrier (reuse the Phase 8 atomic-write helper if it exposes a bytes overload; else inline the same atomic-temp-then-replace + Flush pattern). Returns a SaveResult {SavedPath, Succeeded, ErrorMessage}.
      - SaveToPath(MutableStringTableDocument doc, string targetPath) — bytes → atomic write to targetPath.
      - SaveLooseOverride(MutableStringTableDocument doc, OpenSource source, string clientRoot, string subDir) — resolve dest via LooseOverridePath.Resolve(clientRoot, subDir, logicalPath-from-source); bytes → atomic write (root-containment defense inherited).
      - RepackIntoSourceTre(MutableStringTableDocument doc, OpenSource.TreArchive ta, bool createBackup) — bytes = Serialize(); return await TreRepackSaveTarget.Apply(ta, bytes, createBackup) (V6000 refusal inherited).
      - < 120 lines. NO dataGridView reference.
    - StringTableHandoffPolicyTests (~8-10 facts, framework, no WinForms): .stf extension offered; .STF case-insensitive; magic-sniff payload offered when extension absent; enumerate-only never offered; non-.stf non-magic not offered; null/empty path + null payload not offered.
    - StringTableReloadRoutingTests (~2-3 facts): ReloadAssetClassifier.Classify(".stf", null) == ReloadTier.PendingNextSceneChange; ".STF" case-insensitive routes the same; (optionally) the no-live-client → ReloadUnavailable path if ClientReloadDispatcher is callable headless.
    - csproj: Utinni adds UI\StringTableHandoffPolicy.cs; TJT adds Saving\StringTableSaveTargets.cs.
  </behavior>
  <action>
    Create StringTableHandoffPolicy.cs (port DatatableHandoffPolicy, drop the path rule, use
    LooksLikeStf) + StringTableSaveTargets.cs (bytes-layer shim) + the 2 framework test files. Add the
    csproj Compile entries in BOTH repos. 23-line MIT header, Allman, 4-space, `// ToDo` (no colon). XML
    cites the Phase 9 DatatableHandoffPolicy + DatatableSaveTargets analogs and notes the bytes-layer
    composition (`.stf` is flat, not IFF). Cross-repo paired commit. Tests FIRST (RED) then implement.
  </action>
  <verify>
    <automated>dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~StringTableHandoffPolicyTests|FullyQualifiedName~StringTableReloadRoutingTests" 2>&1 | tail -15  # MUST exit 0 with >= 10 facts; grep -v '^#' UtinniCoreDotNet/UtinniCoreDotNet.csproj | grep -c "UI\\\\StringTableHandoffPolicy.cs"  # MUST return 1; powershell -Command "& 'D:/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe' '../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj' /p:Configuration=Debug /p:Platform=x86 /v:minimal" 2>&1 | tail -8  # build green; grep -c "StringTableWriter" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/StringTableSaveTargets.cs"  # MUST return >= 1; grep -c "dataGridView" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/StringTableSaveTargets.cs"  # MUST return 0</automated>
  </verify>
  <done>StringTableHandoffPolicy + its xUnit (>= 8 facts) + StringTableReloadRoutingTests (>= 2 facts) green; StringTableSaveTargets ships < 120 lines composing at the bytes layer (no dataGridView ref); csproj entries in both repos; TJT builds Debug|x86; cross-repo paired commit landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire FormStringTableEditor Save▾ + reload-dispatch + OpenFromTreEntry + MarkSaved; FormTreBrowser D-04 hand-off</name>
  <files>../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStringTableEditor.cs, ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs</files>
  <read_first>
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormDatatableEditor.cs (the Phase 9 Save▾ click handlers + RefreshSaveMenuEnabledState provenance gate + OpenFromTreEntry + OnReloadClicked + saveInFlight state machine + MarkSaved-on-success — the verbatim shape to mirror, dropping the NeedsReview cascade gate)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStringTableEditor.cs (the file AS COMMITTED BY 10-04 — MODIFY target; the Save▾ DISABLED stubs + RefreshSaveMenuEnabledState stub + OnReloadClicked stub + the controller field)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs (lines around _miOpenInDatatableEditor + OnOpenInDatatableEditor + FindOrCreateDatatableEditor — mirror verbatim for the .stf hand-off, gated on StringTableHandoffPolicy)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/Saving/StringTableSaveTargets.cs (Task 1 — the four save methods)
    - UtinniCoreDotNet/UI/StringTableHandoffPolicy.cs (Task 1 — the gate the FormTreBrowser menu uses)
    - UtinniCoreDotNet/Editing/StringTableEditController.cs (10-01 — MarkSaved)
    - UtinniCoreDotNet/Formats/Iff/OpenSource.cs (LooseFile/TreArchive/Unknown pattern-match for provenance gating)
    - UtinniCoreDotNet/Formats/Tre/TreRecordIndexResolver.cs (ResolveOrUnknown for OpenFromTreEntry)
    - ../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormSaveConfirmDialog.cs (the inherited dirty-discard + repack-confirm modal)
    - .planning/phases/10-tjt-subpanel-string-table-editor-stf/10-UI-SPEC.md (§ States Save in progress/succeeded/failed; § Copywriting Save▾ menu labels + disabled tooltips + the reload-clicked status copy + the D-04 context-item caption `Open in String-table Editor` + the discard-while-dirty modal copy)
  </read_first>
  <behavior>
    - FormStringTableEditor MODIFICATIONS:
      - Save▾ click handlers (replace 10-03's DISABLED stubs):
        - OnSaveInPlaceClick: await StringTableSaveTargets.SaveInPlace(mutable, Source); on success status green `Saved {name} (in place)` + ClientReloadDispatcher.Dispatch(savedPath, null) (the user-facing badge stays the locked CF-05 copy) + controller.MarkSaved(); on failure status red `<reason>. Your edits are kept in the editor — try another save target.`.
        - OnSaveLooseOverrideClick: resolve clientRoot (Game.GetWorkingDirectory() Phase-7 helper OR ini fallback) + subDir from [StringTableEditor] looseOverrideDir; await SaveLooseOverride; status flow + MarkSaved; first-save hint if looseOverrideDir empty (Phase 8 posture).
        - OnSaveAsClick: SaveFileDialog (filter `String tables (*.stf)|*.stf|All files|*.*`); await SaveToPath; status flow + MarkSaved; always enabled unless never-loaded.
        - OnRepackTreClick: gated on Source is OpenSource.TreArchive ta; FormSaveConfirmDialog (inherited locked repack copy); await RepackIntoSourceTre; handle Replaced/BackedUpThenReplaced → green + MarkSaved; RefusedV6000Encrypted → red `This .tre archive is V6000-encrypted and cannot be repacked. Use Save as loose override.`; RefusedClientHoldsArchive → red loose-override-recommendation copy (no MarkSaved on a refusal).
        - OnPatchLiveClientClick: NOT WIRED (CF-03); item stays disabled with the inherited tooltip.
      - RefreshSaveMenuEnabledState: provenance gate ONLY (no cascade — .stf has no column types): isLooseFile/isTreArchive/isUnknown via Source pattern-match. SaveInPlace enabled iff LooseFile; SaveLooseOverride iff LooseFile||TreArchive; SaveAs always enabled (when a doc is loaded); RepackTre iff TreArchive; PatchLive always disabled (CF-03); Unknown → only SaveAs enabled. Each item carries the locked disabled tooltip from 10-UI-SPEC § Copywriting.
      - Public OpenFromTreEntry(byte[] payload, string archivePath, string logicalPath, long offset): doc = StringTableDocument.FromBytes(payload); src = TreRecordIndexResolver.ResolveOrUnknown(archivePath, offset, logicalPath); LoadDocument(doc.Mutable, src, Path.GetFileName(logicalPath)).
      - OnReloadClicked (extend 10-03's CF-05-locked stub): if lastSavedPath != null && Game.IsRunning (try/catch), ClientReloadDispatcher.Dispatch(lastSavedPath, null); user-facing status stays the locked CF-05 wording.
      - saveInFlight state machine (Phase 8 MEDIUM-9 stale-bytes barrier): during a Save▾ click set saveInFlight=true → disable Save▾ + Reload → after the await set false → RefreshSaveMenuEnabledState + reload-button state.
      - Dirty-discard prompt: now that save exists, close/open-replace while dirty routes through FormSaveConfirmDialog (heading `Discard unsaved changes?` per 10-UI-SPEC). (10-03 left this as a plain hide; 10-05 upgrades it.)
    - FormTreBrowser MODIFICATIONS (D-04, additive — mirror the existing _miOpenInDatatableEditor wiring):
      - F11 WAVE-0 CHECK (do FIRST): inspect the existing `_miOpenInDatatableEditor` Opening-event gate to determine whether the FormTreBrowser call site HAS the file payload at menu-Opening time. If it does NOT (payload resolves lazily on click via TrePayloadResolver — the likely Datatable-precedent reality), the `.stf` gate MUST be extension-only at the Opening event (pass payload=null); the LooksLikeStf magic sniff then only matters for extension-less entries and is exercised by the framework policy unit tests, not by a live path the menu can't feed. Document the finding in the SUMMARY.
      - Add `_miOpenInStringTableEditor` ToolStripMenuItem, caption `Open in String-table Editor` (LOCKED). Visible gate on the context-menu Opening event: `_miOpenInStringTableEditor.Visible = StringTableHandoffPolicy.ShouldOfferStringTableEditor(pn.FullPath, payloadOrNull, d.EnumerateOnly);` (mirror exactly how the existing _miOpenInDatatableEditor line supplies its inputs — same pn.FullPath / d.EnumerateOnly; per the F11 Wave-0 check pass the payload ONLY if the call site genuinely has it at Opening time, else null so the gate is extension-only and extension-less `.stf` entries still get the menu via the secondary sniff where a payload IS available).
      - OnOpenInStringTableEditor handler mirrors OnOpenInDatatableEditor VERBATIM: off-UI-thread TrePayloadResolver.TryResolve → BeginInvoke → FindOrCreateStringTableEditor() → editor.OpenFromTreEntry(payload, archivePath, logicalPath, offset) → Show + Activate.
      - FindOrCreateStringTableEditor() mirrors FindOrCreateDatatableEditor() (walk GetForms() for the FormStringTableEditor singleton; null if unregistered).
  </behavior>
  <action>
    Modify FormStringTableEditor.cs (Save▾ handlers + RefreshSaveMenuEnabledState provenance gate +
    OpenFromTreEntry + OnReloadClicked + saveInFlight + dirty-discard) and FormTreBrowser.cs (the
    _miOpenInStringTableEditor item + gate + handler + FindOrCreateStringTableEditor). Mirror the Phase 9
    FormDatatableEditor / FormTreBrowser wiring verbatim, dropping the NeedsReview cascade. 23-line MIT
    header preserved, Allman, 4-space, `// ToDo` (no colon). LOCKED copy verbatim from 10-UI-SPEC.
    Cross-repo paired commit. Build via VS2026 MSBuild Debug|x86. No new framework tests in this task —
    Task 1's policy + reload-routing facts + 10-01's MarkSaved fact cover the logic; the full Save▾ /
    hand-off flow is exercised by the 10-06 maintainer smoke.
  </action>
  <verify>
    <automated>powershell -Command "& 'D:/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe' '../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/TheJawaToolboxDotNet.csproj' /p:Configuration=Debug /p:Platform=x86 /v:minimal" 2>&1 | tail -10  # build green; grep -c "StringTableSaveTargets" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStringTableEditor.cs"  # MUST return >= 1; grep -c "MarkSaved" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormStringTableEditor.cs"  # MUST return >= 1; grep -c "FindOrCreateStringTableEditor" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs"  # MUST return >= 1; grep -c "ShouldOfferStringTableEditor" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs"  # MUST return >= 1; grep -c "Open in String-table Editor" "../UtinniPlugins/The Jawa Toolbox/TheJawaToolboxDotNet/UI/Forms/FormTreBrowser.cs"  # MUST return >= 1</automated>
  </verify>
  <done>FormStringTableEditor Save▾ (modes 1/2/4; mode 3 disabled) + reload-dispatch + OpenFromTreEntry + MarkSaved-on-success + provenance gate + dirty-discard wired; FormTreBrowser has the D-04 `Open in String-table Editor` item gated on StringTableHandoffPolicy + the hand-off handler + FindOrCreateStringTableEditor; both repos build Debug|x86; cross-repo paired commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process → disk | save modes write user edits to loose-override / chosen path / repacked .tre |
| disk → process | TRE Browser hand-off resolves + parses a .stf payload (inherits 10-01 FromBytes guards) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-13 | Tampering (loose-override path escape) | SaveLooseOverride | mitigate | LooseOverridePath.Resolve root-containment defense reused verbatim (Phase 8, 14-fact-tested). |
| T-10-14 | Tampering (V6000 archive corruption) | RepackIntoSourceTre | mitigate | TreRepackSaveTarget.Apply refuses V6000 (Phase 8 WR-06) + timestamped backup; .stf bytes flow through unchanged. |
| T-10-15 | Tampering (stale bytes on concurrent save) | saveInFlight | mitigate | saveInFlight disables Save▾ + Reload during a write (Phase 8 MEDIUM-9 barrier). |
| T-10-16 | Elevation (live-patch on .stf) | Save▾ Patch live client | mitigate | DISABLED per CF-03 — no open path constructs OpenSource.ClientMemory; item stays disabled with the inherited tooltip. |
| T-10-SC | Tampering | npm/pip/cargo installs | N/A | Phase 10 adds ZERO external packages. |
</threat_model>

<verification>
1. `dotnet test UtinniCoreDotNet.Tests --no-build --filter "FullyQualifiedName~StringTableHandoffPolicyTests|FullyQualifiedName~StringTableReloadRoutingTests"` exits 0 with >= 10 facts.
2. TJT MSBuild Debug|x86 + Release|x86 succeed; Utinni Debug|x86 + Release|x86 succeed.
3. `grep -c "ShouldOfferStringTableEditor" FormTreBrowser.cs` >= 1; `grep -c "Open in String-table Editor" FormTreBrowser.cs` >= 1.
4. `grep -c "MarkSaved" FormStringTableEditor.cs` >= 1; the four Save▾ handlers dispatch to StringTableSaveTargets.
5. Full UtinniCoreDotNet.Tests + PreservationAudit suites stay green.
</verification>

<success_criteria>
- SC2 (save back): Save▾ modes 1/2/4 write .stf bytes (mode 3 disabled per CF-03); byte-exactness from 10-01 flows through.
- SC3 (live reload): reload routes tier-(b) PendingNextSceneChange; badge stays the locked CF-05 copy; reload-routing fact green.
- D-04 entry points: file picker (10-03) + TRE Browser `Open in String-table Editor` (this plan); NO IFF-Editor hand-off.
- Provenance gating (LooseFile/TreArchive/Unknown) + V6000 refusal + Save-As escape hatch inherited from Phase 8.
</success_criteria>

<output>
Create `.planning/phases/10-tjt-subpanel-string-table-editor-stf/10-05-SUMMARY.md` when done. Note the
atomic-write mechanism used at the bytes layer (reused Phase 8 helper vs inlined), whether the FormTreBrowser
hand-off gate had the payload available at menu-Opening time (extension-only vs magic-sniff), and the
StringTableSaveTargets final line count.
</output>
